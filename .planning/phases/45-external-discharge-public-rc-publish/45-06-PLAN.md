---
phase: 45-external-discharge-public-rc-publish
plan: 06
title: "KAAN-ACTION-LEGAL.md §SHIP-01..13 runbooks (13-section discharge cookbook)"
type: execute
wave: 2
depends_on: [45-01, 45-02, 45-03, 45-04, 45-05]
files_modified:
  - KAAN-ACTION-LEGAL.md
  - tests/repo/test_kaan_action_ship_runbooks.py
autonomous: true
requirements: [SHIP-01, SHIP-02, SHIP-03, SHIP-04, SHIP-05, SHIP-06, SHIP-07, SHIP-08, SHIP-09, SHIP-10, SHIP-11, SHIP-12, SHIP-13]
req_ids: [SHIP-01, SHIP-02, SHIP-03, SHIP-04, SHIP-05, SHIP-06, SHIP-07, SHIP-08, SHIP-09, SHIP-10, SHIP-11, SHIP-12, SHIP-13]
tags: [kaan-action, discharge, runbooks, ship-01-thru-13, append-only, legal-capacity, autonomous-pre-stage]
must_haves:
  truths:
    - "KAAN-ACTION-LEGAL.md carries 13 new sections §SHIP-01..§SHIP-13 appended after the existing §LAUNCH-08 (line ~1834+)."
    - "Each section is in canonical §GATE-* / §LAUNCH-* 8-block format: header, who-discharges, prerequisites, exact commands, verification, post-discharge state update, what-unblocks, sign-off block."
    - "Every command cited in a runbook is one that landed in Plans 45-01..05 (or pre-existed via Phase 39/42/44). No invented commands; no placeholders."
    - "Wave-2 file-ownership rule honored: KAAN-ACTION-LEGAL.md is the ONLY file modified by this plan (plus the test). No script changes; no doc changes outside this file."
    - "CI test pins all 13 §SHIP-NN headers + minimum block-count per section so future edits can't silently drop discharge steps."
  artifacts:
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "13 §SHIP-NN sections — discharge cookbook for Kaan + Francesco + Bravoh team covering Apple Dev → SignPath OSS → DIST-19 verify → INSTALL-VM matrix → INSTALL-60S → OPS-14-SERVER → SHIP-CUT → SHIP-TWEET → SHIP-DISCORD → SHIP-TRANSFER → SHIP-ROTATE → INSTALL-DEFENDER → SHIP-V1-DECISION."
      min_lines_added: 600
    - path: "tests/repo/test_kaan_action_ship_runbooks.py"
      provides: "Structural pin: 13 §SHIP-NN H2s, each with required sub-blocks; cross-references valid (cited scripts exist on disk); preservation of pre-§SHIP sections verbatim."
      min_lines: 200
  key_links:
    - from: "KAAN-ACTION-LEGAL.md §SHIP-04"
      to: "scripts/dist/install_vm_matrix.sh (Plan 45-01)"
      via: "verification command"
      pattern: "install_vm_matrix"
    - from: "KAAN-ACTION-LEGAL.md §SHIP-08"
      to: "scripts/launch/launch_trigger.sh (Plan 45-02)"
      via: "discharge command (LAUNCH_REAL=1 ... --live)"
      pattern: "scripts/launch/launch_trigger"
    - from: "KAAN-ACTION-LEGAL.md §SHIP-06"
      to: "scripts/release/check_bravoh_server_ready.sh (Plan 45-03)"
      via: "verification command"
      pattern: "check_bravoh_server_ready"
    - from: "KAAN-ACTION-LEGAL.md §SHIP-13"
      to: "scripts/release/audit_ship_v1_decision.py (Plan 45-04)"
      via: "discharge command"
      pattern: "audit_ship_v1_decision"
    - from: "KAAN-ACTION-LEGAL.md §SHIP-11"
      to: "docs/launch-rotation.md §SHIP-11 (Plan 45-05)"
      via: "operational source-of-truth cross-link"
      pattern: "§SHIP-11"
    - from: "KAAN-ACTION-LEGAL.md §SHIP-07"
      to: "scripts/launch/cut_release.sh"
      via: "pre-flight + cut command"
      pattern: "cut_release"
    - from: "KAAN-ACTION-LEGAL.md §SHIP-10"
      to: "gh api repos/<owner>/vibemix/transfer -f new_owner=bravoh"
      via: "literal repo transfer command"
      pattern: "transfer.*new_owner"
---

<objective>
Ship the operator-facing discharge cookbook: 13 §SHIP-NN sections appended to `KAAN-ACTION-LEGAL.md` documenting every step of the public-RC publish cascade, from the Apple Dev Agreement (Francesco) through the T+30 SHIP-V1-DECISION (Kaan). Each section names who discharges, prerequisites, exact commands (referencing the scripts shipped in Plans 45-01..05), verification, post-discharge state update, what gets unblocked, and a sign-off block.

Purpose: The autonomous engineering scaffolding (Plans 45-01..05) is the LEFT side of the discharge chain. KAAN-ACTION-LEGAL §SHIP-01..13 is the RIGHT side — the "Kaan, on launch morning, run these commands in this order" cookbook. Without these runbooks, the engineering investment in 45-01..05 sits dormant.

Output: Append-only edit to KAAN-ACTION-LEGAL.md (the canonical Kaan-discharge document; current size 1968 lines, ending at §LAUNCH-08). New content: §SHIP-01..13 inserted chronologically after §LAUNCH-08. Plus a CI test pinning the 13-section structure. No other files touched (wave-2 file-ownership invariant).

Anti-scope: This plan does NOT execute any of the discharges — they require external clock (Apple, SignPath), live network, or Kaan's eyes-on. This plan only documents the protocol so the moment the external prerequisites land, the discharge is unambiguous + executable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-external-discharge-public-rc-publish/45-CONTEXT.md
@.planning/phases/45-external-discharge-public-rc-publish/45-01-PLAN.md
@.planning/phases/45-external-discharge-public-rc-publish/45-02-PLAN.md
@.planning/phases/45-external-discharge-public-rc-publish/45-03-PLAN.md
@.planning/phases/45-external-discharge-public-rc-publish/45-04-PLAN.md
@.planning/phases/45-external-discharge-public-rc-publish/45-05-PLAN.md

@KAAN-ACTION-LEGAL.md
@scripts/launch/cut_release.sh

<interfaces>
Existing KAAN-ACTION-LEGAL.md section topology (from `grep "^## §"`):
- §SHIP — Phase 39 (line 301)
- §POST-RC-CLEANUP (line 474)
- §GATE-01 / 02 / 03 / 05 (lines 832, 893, 960, 1065)
- §VIS-04, §VIS-09 (lines 1156, 1270)
- §LAUNCH-07, §LAUNCH-03, §LAUNCH-04, §LAUNCH-06, §LAUNCH-08 (lines 1371, 1479, 1592, 1712, 1834)

Append target: After line 1968 (current EOF), add 13 new H2 sections numbered §SHIP-01 through §SHIP-13 chronologically. The pre-existing `## §SHIP — Phase 39` (line 301) covers v2.1 carryover — it is preserved as-is and NOT touched by this plan; the new §SHIP-NN sections are v3.0-specific.

Canonical §GATE-* / §LAUNCH-* 8-block format (per Phase 44-06 §LAUNCH-06 / §LAUNCH-08):
1. Header (`## §SHIP-NN — <title>`)
2. Who-discharges line (Kaan / Francesco / Bravoh-team)
3. Prerequisites (`Pre-requisites:` bullet list — what must be GREEN before this fires)
4. Exact commands (`Discharge commands:` code block)
5. Verification (`Verification:` code block — commands that confirm completion)
6. Post-discharge state update (`Post-discharge:` — mark `[x]` in REQUIREMENTS.md + STATE.md updates)
7. What-unblocks (`Unblocks:` — downstream §SHIP-NN sections + ROADMAP success criteria)
8. Sign-off block (canonical `_____` placeholder per line + final `Sign-off by (Kaan):`)

This plan ships 13 sections × ~50 lines each ≈ 650 lines of append.
</interfaces>

<command_catalog>
The literal commands each §SHIP-NN section will cite — sourced from Plans 45-01..05 + existing scripts:

§SHIP-01 (Apple Dev Agreement — Francesco):
- Discharge: manual web flow at developer.apple.com (no CLI).
- Verification: GitHub Actions Apple-signing leg green via `gh run list --workflow=release.yml --status=success` after a tag push.

§SHIP-02 (SignPath OSS Foundation — Kaan):
- Discharge: SignPath OSS Foundation application web flow + ~1 week SLA wait.
- Verification: `gh secret list | grep SIGNPATH_API_TOKEN` returns the secret name.

§SHIP-03 (DIST-19 — signed binary smoke):
- Pre-req: §SHIP-01 + §SHIP-02 GREEN.
- Discharge: `gh workflow run release.yml --ref main`; wait for build; `gh run download <run-id>`.
- Verification: `for art in dist/*.{dmg,msi}; do uv run python scripts/dist/verify_signed.py --artifact "$art" --require-signed; done`.

§SHIP-04 (INSTALL-VM matrix — fresh-VM walks):
- Pre-req: §SHIP-03 GREEN + tart images present.
- Discharge: `bash scripts/dist/install_vm_matrix.sh --live --run-id $(date -u +%Y-%m-%dT%H-%M-%SZ)` (Plan 45-01).
- Verification: `bash scripts/dist/install_vm_matrix.sh --check-60s`.

§SHIP-05 (INSTALL-60S — onboarding-stopwatch confirms ≤60s):
- Pre-req: §SHIP-04 GREEN.
- Discharge: implicit (matrix runner already captures timing via `VIBEMIX_INSTALL_VM_RUN=1`).
- Verification: same `--check-60s` invocation as §SHIP-04; this section documents the contract.

§SHIP-06 (OPS-14-SERVER — Bravoh server endpoints):
- Pre-req: Bravoh-team deploy of 3 endpoints + `*/5 * * * *` healthz cron.
- Discharge: Bravoh team deploys (separate repo); Kaan-side verification only.
- Verification: `bash scripts/release/check_bravoh_server_ready.sh` (Plan 45-03) exits 0.

§SHIP-07 (SHIP-CUT — `gh release create v3.0.0-rc1 --draft`):
- Pre-req: §SHIP-03 + §SHIP-06 GREEN + `cut_release.sh v3.0.0-rc1` exits 0 (6 gates including new Gate 5b).
- Discharge: `bash scripts/launch/cut_release.sh v3.0.0-rc1` (prints the gh command); then `gh release create v3.0.0-rc1 --repo bravoh/vibemix --title "vibemix v3.0.0-rc1" --notes-file CHANGELOG-v3.0.0-rc1.md --draft --target main dist/*.dmg dist/*.msi dist/*.pkg dist/*.exe`.
- Note: tag regex in cut_release.sh is currently `^v2\.1\.0-rc[0-9]+$`; this section flags that the tag regex bump to v3.0.0-rc[0-9]+ is a prerequisite (one-line sed in cut_release.sh — Kaan-execute or a separate follow-up plan).

§SHIP-08 (SHIP-TWEET — 5-channel social publish):
- Pre-req: §SHIP-07 GREEN + sign-off footer locked (Plan 44-05 verified by launch_trigger.sh pre-publish gate).
- Discharge: 4 invocations at the cadence stages:
  - `LAUNCH_REAL=1 GITHUB_TOKEN=$(gh auth token) DISCORD_WEBHOOK_URL=... bash scripts/launch/launch_trigger.sh --live --phase T-30`
  - same for `--phase T+0`, `--phase T+5h`, `--phase T+24h` (Plan 45-02).
- Verification: `cat dist/launch-runs/*.jsonl | jq 'select(.mode=="live") | .channel + ":" + .stage' | sort -u` returns 12 unique channel:stage pairs per cadence_index.json.

§SHIP-09 (SHIP-DISCORD — #announcements launch post + provision live):
- Pre-req: §SHIP-08 T+0 fired + `BRAVOH_DISCORD_BOT_TOKEN` GH secret set.
- Discharge: `LAUNCH_REAL=1 uv run python scripts/dayzero/discord_provision.py --live --guild-id <id>` (Phase 44-06; one-time) + `LAUNCH_REAL=1 uv run python scripts/launch/post_discord_launch.py --real` (Phase 36; one-shot).
- Verification: visual confirmation in Discord #announcements + idempotency re-run of discord_provision shows 14 skips.

§SHIP-10 (SHIP-TRANSFER — repo to bravoh/vibemix):
- Pre-req: §SHIP-07 GREEN + `bash scripts/launch/check_bravoh_org_ready.sh` exits 0 (Phase 44-06).
- Discharge: `gh api -X POST repos/$CURRENT_OWNER/vibemix/transfer -f new_owner=bravoh`.
- Verification: `gh repo view bravoh/vibemix --json owner --jq .owner.login` returns `bravoh`.

§SHIP-11 (SHIP-ROTATE — 24h monitoring):
- Pre-req: §SHIP-07 + §SHIP-08 + §SHIP-09 GREEN.
- Discharge: Kaan executes 4 × 6h shifts per docs/launch-rotation.md §SHIP-11 (Plan 45-05).
- Verification: shift handoff notes posted; sign-off block in launch-rotation.md §SHIP-11 filled.

§SHIP-12 (INSTALL-DEFENDER — Windows SmartScreen propagation):
- Pre-req: §SHIP-07 GREEN.
- Discharge: passive observation 1-2 weeks; no commands. Kaan documents first-Defender-warning-gone date.
- Verification: fresh-VM Win 11 install no longer shows SmartScreen warning.

§SHIP-13 (SHIP-V1-DECISION — T+30 decision):
- Pre-req: §SHIP-07..§SHIP-12 dispositioned.
- Discharge: `GITHUB_TOKEN=$(gh auth token) uv run python scripts/release/audit_ship_v1_decision.py --live --release-tag v3.0.0-rc1 --bravoh-healthz-stats-url https://api.altidus.world/vibemix/healthz/stats --output .planning/decisions/v3.0-SHIP-V1-DECISION.md` (Plan 45-04). Then Kaan reads + signs off + checks one of 3 boxes.
- Verification: `.planning/decisions/v3.0-SHIP-V1-DECISION.md` exists with Kaan sign-off line filled.
</command_catalog>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin §SHIP-01..13 structural invariants via CI test (RED)</name>
  <files>tests/repo/test_kaan_action_ship_runbooks.py</files>
  <behavior>
    - Test 1: KAAN-ACTION-LEGAL.md exists.
    - Test 2: All 13 H2 headers present: `## §SHIP-01 ...` through `## §SHIP-13 ...` (exact count = 13; numeric uniqueness pinned).
    - Test 3: H2 order is chronological 01 → 13 (no re-ordering, no gaps).
    - Test 4: Each section contains the 8 canonical blocks (regex per block name): `Pre-requisites:`, `Discharge commands?:`, `Verification:`, `Post-discharge:`, `Unblocks:`, `Sign-off`.
    - Test 5: Cited scripts EXIST on disk (the test parses each Discharge block's bash invocation and asserts the script file is committed):
      - §SHIP-04 cites scripts/dist/install_vm_matrix.sh
      - §SHIP-06 cites scripts/release/check_bravoh_server_ready.sh
      - §SHIP-08 cites scripts/launch/launch_trigger.sh
      - §SHIP-11 cites docs/launch-rotation.md §SHIP-11 anchor
      - §SHIP-13 cites scripts/release/audit_ship_v1_decision.py
    - Test 6: Pre-§SHIP-01 content preserved verbatim (anchor: `## §LAUNCH-08` H2 at line ≥1834; new content appears AFTER that line).
    - Test 7: AI-slop blocklist clean for the new content (the §SHIP sections pass `python scripts/launch/check_no_ai_slop.py KAAN-ACTION-LEGAL.md`).
    - Test 8: Each §SHIP-NN section has a Sign-off block with at least one `_____________` placeholder line + a `Sign-off by` line.
    - Test 9: §SHIP-04 + §SHIP-08 + §SHIP-13 cite ENV variable requirements explicitly (e.g. `GITHUB_TOKEN`, `LAUNCH_REAL=1`, `DISCORD_WEBHOOK_URL`, `BRAVOH_DISCORD_BOT_TOKEN`) — pinned via regex.
    - Test 10: §SHIP-10 contains the literal `gh api -X POST` + `transfer -f new_owner=bravoh` command from CONTEXT §specifics.
    - Test 11: §SHIP-07 explicitly flags the tag regex bump (`^v2\.1\.0-rc[0-9]+$` → `^v3\.0\.0-rc[0-9]+$`) as a prerequisite, since the regex live in cut_release.sh hasn't been bumped yet.
  </behavior>
  <action>Author tests/repo/test_kaan_action_ship_runbooks.py. Each test reads KAAN-ACTION-LEGAL.md once + uses regex/anchor search for the 11 invariants. Tests 1, 6, 7 pass immediately (the existing file qualifies); Tests 2, 3, 4, 5, 8, 9, 10, 11 FAIL RED (no §SHIP-NN sections exist yet). Commit `test(45-06): pin KAAN-ACTION-LEGAL.md §SHIP-01..13 structure (RED — 8/11 fail expected)`.</action>
  <verify>
    <automated>uv run pytest tests/repo/test_kaan_action_ship_runbooks.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>Test file present with 11 tests; 8 fail RED (sections don't exist yet); 3 pass (existing file preserved + AI-slop clean).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author §SHIP-01..07 sections (prerequisites + cut chain) — GREEN part 1</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <behavior>
    - After Task 2, Tests 2-5, 7-11 partially advance: 7 of 13 H2s present (§SHIP-01 through §SHIP-07); ordering correct; all 7 sections have the 8 canonical blocks; AI-slop clean; ENV vars cited where applicable; tag regex flagged in §SHIP-07.
    - Test 6 (pre-§SHIP-01 preservation) still passes.
  </behavior>
  <action>Append §SHIP-01..§SHIP-07 to KAAN-ACTION-LEGAL.md after line 1968. Each section follows the canonical 8-block format from <interfaces>, using the literal commands from <command_catalog>. Total ~7 × ~50 lines = ~350 lines appended in this task. Use Kaan's voice per CONTEXT §specifics: "Direct, casual, type-these-commands-in-this-order. No corporate hedging. Read like git commit messages for steps." After each section, leave one blank line before the next §SHIP-NN H2. Commit `docs(45-06): append §SHIP-01..§SHIP-07 prerequisites + cut chain runbooks (GREEN part 1)`.</action>
  <verify>
    <automated>uv run pytest tests/repo/test_kaan_action_ship_runbooks.py -v 2>&1 | tail -25 && grep -c "^## §SHIP-0[1-7]" KAAN-ACTION-LEGAL.md</automated>
  </verify>
  <done>§SHIP-01 through §SHIP-07 appended; grep count = 7; ordering correct; pre-existing content preserved; partial test pass (Tests 2-5, 8-11 still red for §SHIP-08+ but pass for §SHIP-01..07 subset). Tests that include §SHIP-08..13 invariants still fail — expected, Task 3 closes the rest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author §SHIP-08..13 sections (publish + rotate + decide) — GREEN part 2</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <behavior>
    - All 11 tests in tests/repo/test_kaan_action_ship_runbooks.py pass.
    - Test 12 (added in this task): KAAN-ACTION-LEGAL.md word count grows by ≥4500 words (rough size proxy) over its pre-§SHIP-01 baseline.
    - Test 13 (added): Section cross-references resolve — every `§SHIP-NN` mention within the appended content corresponds to an actual H2 in the same file (no dangling references).
    - Test 14 (added): Plan 42-04's `tests/repo/test_cut_release_invokes_check_gate.py` STILL PASSES (regression baseline — we haven't touched cut_release.sh).
    - Test 15 (added): Plan 45-03's `tests/repo/test_cut_release_invokes_bravoh_server.py` STILL PASSES (same regression baseline).
  </behavior>
  <action>Append §SHIP-08..§SHIP-13 to KAAN-ACTION-LEGAL.md. Same canonical 8-block format. §SHIP-08 cites the 4 cadence-stage invocations from Plan 45-02 verbatim. §SHIP-09 cites Phase 44-06 discord_provision.py --live + Phase 36 post_discord_launch.py --real. §SHIP-10 cites the literal `gh api -X POST repos/.../transfer -f new_owner=bravoh` per CONTEXT §specifics + the check_bravoh_org_ready.sh pre-req. §SHIP-11 cross-links docs/launch-rotation.md §SHIP-11 (Plan 45-05) as the operational source-of-truth (don't duplicate the shift table — just reference it). §SHIP-12 documents the passive 1-2 week observation period. §SHIP-13 cites the audit script invocation + reads + sign-off protocol from Plan 45-04. Add 4 new tests (12-15). Commit `docs(45-06): append §SHIP-08..§SHIP-13 publish + rotate + decide runbooks (GREEN part 2 — completes §SHIP-01..13 cookbook)`.</action>
  <verify>
    <automated>uv run pytest tests/repo/test_kaan_action_ship_runbooks.py tests/repo/test_cut_release_invokes_check_gate.py tests/repo/test_cut_release_invokes_bravoh_server.py -v 2>&1 | tail -30 && grep -c "^## §SHIP-[01][0-9]" KAAN-ACTION-LEGAL.md</automated>
  </verify>
  <done>15 tests pass (11 in test_kaan_action_ship_runbooks.py + 4 regression baselines); §SHIP-01..§SHIP-13 all present (13 sections); cross-references resolve; no upstream regressions. KAAN-ACTION-LEGAL.md is now the complete v3.0 publish-cascade cookbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KAAN-ACTION-LEGAL.md → operator (Kaan / Francesco / Bravoh team) | Operational runbook — instructions hand-executed. Document accuracy IS the safety property. |
| §SHIP-NN cited commands → underlying scripts | Each script tested independently in its own plan (45-01..05). This plan's tests assert the citation is correct (script exists on disk + flag spelling matches). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-06-01 | Tampering | future edit silently changes a discharge command | mitigate | Test 5 + Test 9 pin the literal command shapes + ENV var requirements; CI catches drift. |
| T-45-06-02 | Info disclosure | runbook accidentally documents a secret value | mitigate | Test 7 AI-slop clean + manual review; secrets are NAMED (`GITHUB_TOKEN`, `BRAVOH_DISCORD_BOT_TOKEN`), never literal values. |
| T-45-06-03 | Repudiation | "did we actually discharge §SHIP-NN?" | mitigate | Each section has explicit `_____` sign-off placeholders + a `Sign-off by (Kaan):` line; Test 8 pins this. |
| T-45-06-04 | Elevation of privilege | runbook command escalates beyond stated scope | mitigate | §SHIP-10 transfer command is the highest-privilege action; the command is literal (not parameterized into a script) so Kaan eyeballs it before pressing return; PR review for any future edit to the command. |
| T-45-06-05 | DoS | a §SHIP-NN section is wrong and Kaan runs the wrong command | mitigate | Each command is sourced from a tested plan (45-01..05) or pre-existing tested script; cross-reference test (Test 5) asserts file existence + Test 9 asserts ENV-var spelling. |
| T-45-06-06 | Spoofing | someone edits KAAN-ACTION-LEGAL.md off-master to inject malicious commands | accept | PR review + commit history are the trust anchors; same threat envelope as all repo-tracked docs. |
</threat_model>

<verification>
- All 11 tests in tests/repo/test_kaan_action_ship_runbooks.py pass.
- 4 regression baseline tests pass (Plan 42-04 + 45-03 wire-in tests still GREEN).
- `grep -c "^## §SHIP-[01][0-9]" KAAN-ACTION-LEGAL.md` returns 13.
- `grep -n "^## §SHIP-" KAAN-ACTION-LEGAL.md` shows 14 matches (1 pre-existing Phase 39 §SHIP at line 301 + 13 new §SHIP-NN).
- Pre-§SHIP-01 content unchanged: `head -1968 KAAN-ACTION-LEGAL.md | md5` matches the pre-plan md5.
- AI-slop clean: `uv run python scripts/launch/check_no_ai_slop.py KAAN-ACTION-LEGAL.md` exits 0.
- File size: KAAN-ACTION-LEGAL.md grew from 1968 to ~2600 lines (+~600 for 13 sections × ~50 lines each).
</verification>

<success_criteria>
1. KAAN-ACTION-LEGAL.md carries 13 §SHIP-NN sections covering the entire v3.0 publish-cascade discharge chain.
2. Every section follows canonical 8-block format (header / who / pre-req / commands / verification / post-discharge / unblocks / sign-off).
3. Every cited command exists on disk (cross-reference test passes).
4. Pre-existing content (§GATE-*, §LAUNCH-*, §VIS-*, §SHIP Phase 39) preserved verbatim.
5. 15 tests GREEN (11 new + 4 regression baselines).
6. AI-slop blocklist clean.
7. Once Apple Dev + SignPath approvals land, Kaan can execute §SHIP-01..13 sequentially without further engineering — the discharge cookbook is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/45-external-discharge-public-rc-publish/45-06-SUMMARY.md` documenting:
- Files shipped: KAAN-ACTION-LEGAL.md (+~600 lines) + test file (~200 lines)
- 3 atomic commits (RED tests → GREEN §SHIP-01..07 → GREEN §SHIP-08..13)
- Decisions: 8-block canonical format reuse, tag regex bump flagged as §SHIP-07 prerequisite (not done in this plan), §SHIP-11 cross-link to docs/launch-rotation.md (no duplication), §SHIP-12 passive-observation framing
- Phase 45 complete: all 13 SHIP-* requirements now have engineering scaffolding (Plans 45-01..05) + Kaan-discharge runbooks (this plan)
- Ready for v3.0 milestone close once external clock (Apple + SignPath) discharges
</output>
