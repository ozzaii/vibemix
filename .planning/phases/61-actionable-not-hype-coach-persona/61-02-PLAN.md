---
phase: 61-actionable-not-hype-coach-persona
plan: 02
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - src/vibemix/prompts/matrix.py
  - tests/prompts/test_matrix.py
autonomous: true
requirements: [COACH-01, COACH-03, COACH-04]

must_haves:
  truths:
    - "COACH_BEGINNER carries the full observed→impact→prescribe shape (impact clause added), the canonical DJ-verb vocabulary, and a gentle grounded-only deck/harmonic path — its vague nudges ('more space', 'rushing the blend') are replaced with cited-observation + DJ-verb lines."
    - "COACH_INTERMEDIATE carries a positive-callout balance rule and a 'name it AND say the move in the same line' prescribe-clause, plus the DJ-verb vocabulary."
    - "COACH_PRO surfaces the explicit canonical DJ-verb list so harmonic turns read in the same register (otherwise unchanged — it is already strong)."
    - "All four `test_prompt_61_coach_*` contract asserts from 61-01 are now GREEN for all three coach skills."
    - "Every HYPE golden is byte-identical to baseline; the three coach `ANCHOR_PHRASES` entries are updated in lockstep with the cell edits (deliberate, Phase-61-noted); the 7 pre-existing full-suite failures stay at exactly 7."
  artifacts:
    - path: "src/vibemix/prompts/matrix.py"
      provides: "Sharpened COACH_BEGINNER/INTERMEDIATE/PRO cells (verbs + prescribe-clause + balance rule + beginner impact clause + beginner deck/harmonic path)"
      contains: "COACH_BEGINNER"
    - path: "tests/prompts/test_matrix.py"
      provides: "Three coach ANCHOR_PHRASES entries updated in lockstep with the new anchor wording, with a Phase-61 comment"
      contains: "Phase 61"
  key_links:
    - from: "src/vibemix/prompts/matrix.py COACH cells"
      to: "src/vibemix/state/coach.py KEY_CLASH arm verbs (kill/cut/filter/ride)"
      via: "shared DJ-verb vocabulary so a harmonic turn reads identically to a MIX_MOVE turn"
      pattern: "kill|cut|filter|ride|swap|tighten|wait"
    - from: "tests/prompts/test_matrix.py ANCHOR_PHRASES coach entries"
      to: "src/vibemix/prompts/matrix.py COACH cell anchor lines"
      via: "byte-pinned by test_prompt_01_each_cell_has_eight_anchor_phrases — must update in the same commit"
      pattern: "ANCHOR_PHRASES"
---

<objective>
Sharpen the three COACH cell constants in `prompts/matrix.py` to the
actionable-not-hype contract that 61-01's tests now fence, and update the three
coach `ANCHOR_PHRASES` entries in lockstep. This is the headline COACH-01 work.

Per the 61-RESEARCH per-cell audit:
- COACH_BEGINNER is the WEAKEST and gets the most work: add the impact clause
  (observed→impact→prescribe in full), surface the canonical DJ-verb vocabulary,
  replace the vague anchors ("more space", "rushing the blend") with
  cited-observation + DJ-verb lines kept gentle, and add a gentle grounded-only
  deck/harmonic path. Keep its existing ONE-THING-PER-TURN + ENCOURAGING TONE
  (the beginner balance rule).
- COACH_INTERMEDIATE: add a positive-callout balance rule (mirroring PRO's
  PROPS arm — closes the over-correction-to-cold Pitfall 3) and tighten the
  prescribe-clause to "name it AND say the move in the same line"; surface the
  DJ-verb vocabulary.
- COACH_PRO: already strong — only surface the explicit canonical DJ-verb LIST
  so harmonic turns read in the same register. One-line verb anchor; no
  restructure.

Hard constraints: NO new mode (preserve `_CELLS`/`_VALID_MODES`). Edit ONLY the
three COACH_* constants — do NOT touch shared blocks (`_ANTI_SLOP_FOOTER`,
`CITATION_GRAMMAR_BLOCK`, `COACH_TAG_DSL_BLOCK`, `COACH_CLOSING_BLOCK`) or any
shared `task_for_event` arm (MIX_MOVE/HEARTBEAT) — those shift hype too
(Pitfall 1). Do NOT touch `state/coach.py` (the Phase-60 harmonic arms already
own the grounded harmonic voice — verb-align the CELL prose TO them, per research
Assumption A2). Do NOT modify the CitationLinter.

Purpose: Make the coach voice a real prescriptive DJ mentor while the hype voice
stays frozen behind its goldens.
Output: Sharpened cells; 61-01's RED contract asserts now GREEN; hype byte-stable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-actionable-not-hype-coach-persona/61-RESEARCH.md
@.planning/phases/61-actionable-not-hype-coach-persona/61-PATTERNS.md
@.planning/phases/61-actionable-not-hype-coach-persona/61-VALIDATION.md
@.planning/research/FEATURES.md

<read_first>
- src/vibemix/prompts/matrix.py — COACH_BEGINNER (426-455, anchors 428-436), COACH_INTERMEDIATE (463-493, anchors 464-472), COACH_PRO (502-543, the target shape: DESERVED-CRITIQUE+FIX / NUDGE-FORWARD / PROPS arms at 505-510, READ-THE-MOMENT anti-quota at 520, DELIVERY calm block at 528). `_CELLS` (665), `_VALID_MODES == frozenset({"hype","coach"})` (675), `build_system_instruction` (678+), coach tag routing (789-793), `COACH_CLOSING_BLOCK` append (801-802). The shared `_ANTI_SLOP_FOOTER` is appended to every cell — do NOT edit it.
- src/vibemix/state/coach.py — KEY_CLASH arm verbs at 284 ("kill {b_side}'s mids, cut on the drop, filter one out, don't ride the pads") — the cell DJ-verb vocabulary must match these exactly (kill/cut/filter/ride) so a harmonic turn reads in the same voice. Do NOT edit this file.
- .planning/research/FEATURES.md — §126-133 GOOD actionable lines (observed→impact→prescribe, the BEGINNER/INTERMEDIATE anchor lift-targets); §135-137 BAD lines to retire (pure narration / adjective-soup); §147 the canonical DJ-verb list ("kill, swap, cut, filter, wait, ride, tighten, bring in"). For beginner, render present-tense GOOD lines as PAST-TENSE per Pitfall 4 (e.g. "you're in the breakdown — bring B's intro in now" → "that was a good window — you could've brought B's intro in there").
- tests/prompts/test_matrix.py — coach `ANCHOR_PHRASES` at 69-98 (UPDATABLE — update in lockstep this plan); FROZEN hype anchors 36-68; `test_prompt_01_each_cell_has_eight_anchor_phrases` (277-283) byte-asserts 8 anchors per cell — every anchor you change in a cell MUST be mirrored here in the SAME commit; `test_prompt_01_hype_intermediate_byte_identical_to_persona` (155-180) MUST stay green; `test_prompt_61_coach_*` (added in 61-01) are the contract you must satisfy.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sharpen COACH_PRO + COACH_INTERMEDIATE (verb list, balance rule, prescribe-clause)</name>
  <files>src/vibemix/prompts/matrix.py, tests/prompts/test_matrix.py</files>
  <action>
    Start with the two cells that need the least change so the fence is established before the headline beginner rewrite.

    COACH_PRO (502-543): add ONE line surfacing the canonical DJ-verb vocabulary anchor — e.g. a directive that prescriptions use the DJ-verb register "kill, swap, cut, filter, wait, tighten, ride, pull, push" (per FEATURES §147), so harmonic turns (which use kill/cut/filter/ride from coach.py:284) read in the same voice. Do NOT restructure the arms; PRO is already strong. PRO anchors (test_matrix.py:89-98) are already prescriptive — likely no anchor change; only add the verb-anchor prose. Satisfies COACH-01 (verb vocab) for PRO.

    COACH_INTERMEDIATE (463-493): three sharpenings per the audit —
    (1) add a positive-callout BALANCE rule mirroring PRO's PROPS arm ("when a move genuinely lands, call it and why — briefly, specific") so the cell isn't pure-critique (closes Pitfall 3 / COACH-04 over-correction guard);
    (2) tighten the prescribe-clause: change "say what wasn't and how to fix" toward PRO's "name it AND say the move in the SAME line" so observed→impact→prescribe is structurally required, not implied;
    (3) surface the canonical DJ-verb vocabulary line (same set as PRO).
    Keep the existing CONCRETE FEEDBACK + HONEST + LATENCY + LENGTH + LANGUAGE + EVIDENCE PACKET structure. The four existing anchors (464-468) are already prescriptive — keep them; if you reword any, mirror it in `ANCHOR_PHRASES[("intermediate","coach")]` (test_matrix.py:79-88) in this same commit with a Phase-61 comment.

    Apply the prose edits as directive English text in the cell constants (no fenced code). After editing, run the 61-01 contract asserts for pro+intermediate.

    Constraint check: edit ONLY the COACH_PRO and COACH_INTERMEDIATE constant bodies; do NOT touch `_ANTI_SLOP_FOOTER`, the citation grammar block, the tag DSL block, or any hype cell. Run the hype fence after editing to prove no shift.
  </action>
  <acceptance_criteria>
    - `test_prompt_61_coach_*` (verb/prescribe/balance) PASS for `pro` and `intermediate`.
    - The intermediate cell now carries a positive-callout balance rule + a "same line" prescribe-clause + the DJ-verb vocabulary.
    - The PRO cell carries the explicit DJ-verb vocabulary anchor.
    - HYPE byte-identity + all hype anchor tests still pass; any reworded intermediate anchor is mirrored in `ANCHOR_PHRASES` with a Phase-61 comment.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -m pytest -q tests/prompts/test_matrix.py -k "61_coach or hype_intermediate or anchor" 2>&1 | tail -20</automated>
  </verify>
  <done>PRO + INTERMEDIATE contract asserts green; INTERMEDIATE has the balance rule + prescribe-clause + verbs; hype goldens byte-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Sharpen COACH_BEGINNER (impact clause, verb vocab, deck/harmonic path) + lockstep anchors</name>
  <files>src/vibemix/prompts/matrix.py, tests/prompts/test_matrix.py</files>
  <action>
    The headline COACH-01 work. Rewrite the COACH_BEGINNER cell (426-455) to the full observed→impact→prescribe contract while staying gentle and keeping ONE-THING-PER-TURN + ENCOURAGING TONE (the beginner balance rule at 437-439 — keep it, it satisfies COACH-04 for this cell).

    Edits to the cell body:
    (1) Add the impact clause to the contract — instruct that a beginner note names the observed thing, says why it mattered (the impact, e.g. "muddied the breakdown"), then the gentle try-this move. The existing "low boost muddied the breakdown" (429) is the in-cell model; the directive should make impact a required middle step, not optional.
    (2) Surface the canonical DJ-verb vocabulary (same set as PRO/INTERMEDIATE), framed gently for a beginner.
    (3) Add a gentle grounded-only deck/harmonic path: a directive that when a KEY_CLASH/transition event fires, the beginner reads it as a soft cited note ("those two tracks were fighting a bit — try cutting one in cleaner", verb: cut, gentle) — tying to the observed/cited event only, never inventing a key (the linter enforces this structurally).

    Replace the vague anchors with cited-observation + DJ-verb lines kept gentle, lifting from FEATURES §126-133 rendered PAST-TENSE (Pitfall 4):
    - Keep "the cut felt early — try 8 bars later" and "low boost muddied the breakdown" (already observed→impact→prescribe shape).
    - Replace "give the build more space" / "more space" with a cited+verb line, e.g. "the build felt rushed — try holding it 8 bars longer".
    - Replace "you're rushing the blend" / "rushing the blend" with a cited+impact line, e.g. "the blend ran long and muddied the drop — tighten it next time".
    - Add a gentle harmonic anchor, e.g. "those two tracks were fighting a bit — try cutting one in cleaner".
    Keep exactly 8 anchors (the byte-asserted count) and keep them gentle/encouraging in tone.

    LOCKSTEP REQUIREMENT (load-bearing): every anchor you change in the cell MUST be updated in `ANCHOR_PHRASES[("beginner","coach")]` (test_matrix.py:69-78) in this SAME commit, with a comment citing Phase 61. The list and the cell anchor lines are byte-pinned against each other by `test_prompt_01_each_cell_has_eight_anchor_phrases`. This is the deliberate, noted coach-anchor update the research authorized — HYPE anchors stay frozen.

    Constraint check: edit ONLY the COACH_BEGINNER constant body + its anchor list. Do NOT touch shared blocks or hype cells. Keep the EVIDENCE PACKET, LATENCY (past-tense), LENGTH, LANGUAGE sections.
  </action>
  <acceptance_criteria>
    - `test_prompt_61_coach_*` (verb/prescribe/balance/calm-tag) PASS for `beginner` — all four contract asserts now green across all three coach skills.
    - The beginner cell carries an impact clause, the DJ-verb vocabulary, and a gentle grounded-only deck/harmonic path; vague anchors ("more space", "rushing the blend") are replaced with cited+verb lines.
    - Exactly 8 beginner-coach anchors; `ANCHOR_PHRASES[("beginner","coach")]` updated in lockstep with a Phase-61 comment; `test_prompt_01_each_cell_has_eight_anchor_phrases` passes.
    - All hype goldens byte-identical; KEY_CLASH/TRANSITION coach tests (61-01 Task 2) still pass.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -m pytest -q tests/prompts/test_matrix.py tests/state/test_coach.py tests/agent/test_persona.py tests/agent/test_hype_prompt_grounding.py 2>&1 | tail -15</automated>
  </verify>
  <done>All `test_prompt_61_coach_*` green for all three skills; beginner anchors sharpened + mirrored in lockstep; hype + persona + harmonic goldens all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cell text→both LLM paths | The edited COACH cell text becomes `self._prompt_body`, consumed unmodified by both genai and OpenRouter — no user input is interpolated (only the fixed `{mood_persona}` replace). |
| model output→user | Prescriptive harmonic/key claims cross to the user only after the existence-only CitationLinter strip (unchanged this phase). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Tampering | shared prompt blocks bleeding into hype | mitigate | Edit ONLY the 3 COACH_* constant bodies; run the full hype fence (`test_matrix.py` byte-identity + anchors, `test_persona.py`, `test_hype_prompt_grounding.py`) after every commit — a tripped hype golden is a real regression to investigate, never re-baseline (Pitfall 2). |
| T-61-04 | Information disclosure (false) | beginner deck/harmonic path inviting key invention | accept | The cell instructs grounded-only / cite-the-event; the existence-only CitationLinter (unmodified) strips any fabricated `[key:...]` structurally — prompt text is the soft layer, code is the hard guarantee. |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | None — prompt-text + test edits only, no package installs. |
</threat_model>

<verification>
- Hype fence green every commit: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/prompts/test_matrix.py tests/agent/test_persona.py tests/agent/test_hype_prompt_grounding.py 2>&1 | tail -5` — HYPE_INTERMEDIATE byte-identity + all hype anchors stable.
- All 61-01 coach-contract asserts now GREEN: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/prompts/test_matrix.py tests/state/test_coach.py tests/agent/test_dj_cohost_matrix_dispatch.py tests/agent/test_coach_prompt_grounding.py 2>&1 | tail -5`.
- Wave-merge full suite: `PYTHONPATH=src .venv/bin/python -m pytest -q 2>&1 | tail -3` — exactly 7 pre-existing failures, no new failures.
- Kaan-ear phase gate (manual, post-merge, before /gsd:verify-work): ~half coach turns are fresh positive callouts, no repeated praise lines, prescriptive-not-narration, warm-not-cold — the anti-slop quality bar.
- Update 61-VALIDATION.md Per-Task Verification Map with the two tasks (Plan 61-02) and mark `wave_0_complete: true` / `nyquist_compliant: true` once 61-01's fence + these edits are green.
</verification>

<success_criteria>
- COACH_BEGINNER carries impact clause + DJ-verb vocab + gentle grounded deck/harmonic path; vague anchors replaced; ONE-THING + ENCOURAGING TONE kept.
- COACH_INTERMEDIATE carries balance rule + same-line prescribe-clause + DJ-verb vocab.
- COACH_PRO surfaces the explicit DJ-verb list (otherwise unchanged).
- All `test_prompt_61_coach_*` green for all 3 coach skills.
- Coach `ANCHOR_PHRASES` updated in lockstep (Phase-61-noted); HYPE goldens byte-identical; `state/coach.py` and the CitationLinter untouched; no new mode; 7-failure baseline preserved.
</success_criteria>

<output>
Create `.planning/phases/61-actionable-not-hype-coach-persona/61-02-SUMMARY.md` when done
</output>
