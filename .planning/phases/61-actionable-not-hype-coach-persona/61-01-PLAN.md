---
phase: 61-actionable-not-hype-coach-persona
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/prompts/test_matrix.py
  - tests/state/test_coach.py
  - tests/agent/test_dj_cohost_matrix_dispatch.py
  - tests/agent/test_coach_prompt_grounding.py
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-04]

must_haves:
  truths:
    - "The coach-cell contract (DJ verbs, prescribe-clause, positive-callout balance, calm-tag routing) is asserted by tests for all 3 coach skill levels — failing now, so the cell edits in 61-02 have a target to satisfy."
    - "A KEY_CLASH coach prompt is fenced by a test asserting a DJ-verb move + both keys cited + the no-invent guard."
    - "The single coach `_prompt_body` is proven to reach BOTH the genai `_gen_cfg.system_instruction` AND the OpenRouter `stream_or` path (no new mode, dual-path)."
    - "A coach-context anti-slop assertion holds: a fabricated `[key:...]` is stripped (cited-only contract)."
    - "Every hype golden stays byte-stable; the 7 pre-existing full-suite failures stay at exactly 7 (no new failures introduced by this plan)."
  artifacts:
    - path: "tests/prompts/test_matrix.py"
      provides: "Per-coach-cell verb/prescribe/balance/calm-tag asserts (RED until 61-02)"
      contains: "def test_prompt_61_coach"
    - path: "tests/state/test_coach.py"
      provides: "KEY_CLASH coach-voice fence test"
      contains: "KEY_CLASH"
    - path: "tests/agent/test_dj_cohost_matrix_dispatch.py"
      provides: "dual-path coach _prompt_body assertion"
      contains: "_prompt_body"
    - path: "tests/agent/test_coach_prompt_grounding.py"
      provides: "coach-context anti-slop / cited assertion"
  key_links:
    - from: "tests/agent/test_dj_cohost_matrix_dispatch.py"
      to: "src/vibemix/agent/dj_cohost.py"
      via: "agent._gen_cfg.system_instruction and agent._prompt_body share one build_system_instruction body"
      pattern: "_prompt_body"
    - from: "tests/state/test_coach.py"
      to: "src/vibemix/state/coach.py"
      via: "AICoach.task_for_event(_ev('KEY_CLASH', ...))"
      pattern: "task_for_event"
---

<objective>
Lay the Wave-0 test fence for the coach-persona sharpening. This plan adds the
assertions that DEFINE the actionable-not-hype contract (DJ-verb vocabulary,
observed→impact→prescribe "same line" rule, positive-callout balance rule,
calm-only TTS tag routing) for all three COACH cells, plus the three Wave-0 gap
tests called out in 61-RESEARCH/61-VALIDATION: a KEY_CLASH/TRANSITION coach-voice
fence, the dual-path `_prompt_body` proof (COACH-02), and a coach-context
anti-slop assertion (COACH-04).

The new cell-contract asserts are expected to FAIL after this plan (the cells are
not sharpened yet — that is 61-02's job). This is deliberate: the tests are the
spec the cell edits must satisfy. The KEY_CLASH, dual-path, and anti-slop tests
should PASS immediately (they fence already-correct behavior). Hype goldens MUST
stay byte-stable — this plan touches ONLY test files, never prompt constants.

Purpose: A test cannot fence an edit that lands first. Defining the contract now
means 61-02's cell edits are graded against a fixed spec, and the hype fence is
re-run on every commit so a shared-prompt regression trips immediately.
Output: Four extended test files; the cell-contract asserts RED, the
fence/dual-path/anti-slop asserts GREEN, hype goldens untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-actionable-not-hype-coach-persona/61-RESEARCH.md
@.planning/phases/61-actionable-not-hype-coach-persona/61-PATTERNS.md
@.planning/phases/61-actionable-not-hype-coach-persona/61-VALIDATION.md

<read_first>
- tests/prompts/test_matrix.py — anchor table at lines 36-98 (hype anchors FROZEN, the three coach anchors at 69-98 are UPDATABLE but NOT touched in this plan); analogs: `test_prompt_01_coach_mode_includes_feedback_bias` (374-380) and `test_prompt_01_each_cell_has_eight_anchor_phrases` (277-283). Note `build_system_instruction(skill, mode)` is the entry point.
- tests/state/test_coach.py — `_ev(type_, extra)` helper at 221-222 (builds `Event(type=type_, state=MusicState(), extra=extra or {})`); analog `test_task_mix_move_LOAD_BEARING_anti_slop_clause` (279-298). FROZEN byte-`==` goldens: `test_task_layer_arrival_exact_string` (271-276), `test_task_heartbeat_LOAD_BEARING_anti_silence_clause` (301-309) — do not touch.
- src/vibemix/state/coach.py — `task_for_event` KEY_CLASH arm (267-290) reads `ev.extra` keys `a_side`, `a_camelot`, `b_side`, `b_camelot`, `semitones`; emits `[key:A:..]`, `[key:B:..]`, verbs kill/cut/filter/ride, and "Do NOT invent a key". TRANSITION_OPPORTUNITY arm (298-316) reads same keys + `clash`, emits past-tense + both keys cited.
- tests/agent/test_dj_cohost_matrix_dispatch.py — helpers `_coach_rendered` (28-39), `_build_state` (54-64), `_FakeRecorder` (43-52); analog `test_dispatch_06_gen_cfg_system_instruction_matches_dispatch` (180-205). The agent is built with `Agent.__init__` patched; `agent._gen_cfg.system_instruction` and `agent._prompt_body` are both set in the constructor.
- src/vibemix/agent/dj_cohost.py — `self._prompt_body = prompt_body` (380); genai path `system_instruction=prompt_body` (447, stored on `_gen_cfg`); OpenRouter path `stream_or(..., system_instruction=self._prompt_body)` (813-816). Both bodies are the SAME `build_system_instruction(...)` result.
- tests/agent/test_coach_prompt_grounding.py — existing scaffold-pin markers `CITATION_GRAMMAR_MARKER = "--- CITATION GRAMMAR"` and `ANTI_SLOP_FOOTER_MARKER = "DO NOT SAY"` (36-37); `EvidenceRegistry` snapshot helper (52-59). The CitationLinter contract (existence-only `[key:...]` strip) is the structural anti-slop guarantee — reference `src/vibemix/coach/citation_linter.py:41-54`, do NOT modify it.
- src/vibemix/prompts/matrix.py — `build_system_instruction` (678+) routes coach mode to `COACH_TAG_DSL_BLOCK` (calm set `[chill]/[whisper]/[slow]/[laugh]`, no `[excited]`/`[fast]`) at 789-793; `_VALID_MODES == frozenset({"hype","coach"})` (675). Read the three COACH cells (426-543) so the new asserts target phrasings the cells CAN realistically carry post-61-02.
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coach-cell contract asserts to test_matrix.py (RED spec for 61-02)</name>
  <files>tests/prompts/test_matrix.py</files>
  <action>
    Add new parametrized tests (parametrize skill over ["beginner","intermediate","pro"], mode fixed to "coach") that assert the actionable-not-hype contract each COACH cell MUST carry after 61-02 sharpens it. Name them with a `test_prompt_61_coach_*` prefix so they group cleanly under `-k "61_coach"` and never collide with the frozen `test_prompt_01_*` family. Build the body with `build_system_instruction(skill, "coach")`.

    Four assert groups, satisfying COACH-01 and COACH-04:
    (a) DJ-verb vocabulary present — assert the lowercased body contains the canonical verb set surfaced as a vocabulary anchor. Require at least the core set {kill, swap, cut, filter, wait, tighten, ride} appears (per FEATURES §147 and the KEY_CLASH arm verbs); assert each of these verbs is present as a word. Source the exact list from FEATURES.md §147 ("kill, swap, cut, filter, wait, ride, tighten, bring in").
    (b) Prescribe-clause / "same line" rule present — assert the body carries the observed→impact→prescribe "name it AND say the move/fix in the same line" directive (COACH_PRO already has "name it AND say the fix in the SAME line" at matrix.py:506; the assert should match a normalized substring like "same line" so all three cells must carry the rule after 61-02).
    (c) Positive-callout balance rule present — assert each coach cell carries an over-correction guard (a positive-callout / "say it when something works" rule). Match a normalized marker that PRO ("PROPS"), BEGINNER ("ENCOURAGING TONE"), and the post-edit INTERMEDIATE will all satisfy — prefer asserting `any(m in body.lower() for m in [...])` over a set of synonymous markers ("props", "encouraging", "balance", "when a move ... lands", "say it works") so the test is robust to wording but still fails if a cell has NO balance rule.
    (d) Calm-only TTS tag routing — build the body WITH the tag DSL included (the default `build_system_instruction` path) and assert the coach body contains the calm tags `[chill]`/`[whisper]` and does NOT contain `[excited]` or `[fast]`. This fences Pitfall 5 (calm-tag drift).

    Do NOT modify the FROZEN hype anchor lists (36-68), the `test_prompt_01_hype_intermediate_byte_identical_to_persona` test, or the three coach `ANCHOR_PHRASES` entries (69-98) — those are edited in 61-02 in lockstep with the cell text, not here. This task adds tests only.
  </action>
  <acceptance_criteria>
    - 4 new `test_prompt_61_coach_*` parametrized tests exist, each covering all 3 coach skills.
    - The verb-vocabulary, prescribe-clause, and balance-rule asserts currently FAIL for at least BEGINNER and INTERMEDIATE (they are the unsharpened cells) — this is the intended RED spec.
    - The calm-tag-routing assert PASSES now (routing already exists).
    - No hype test is modified; no `ANCHOR_PHRASES` entry is modified.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -m pytest -q tests/prompts/test_matrix.py -k "61_coach" 2>&1 | tail -20</automated>
  </verify>
  <done>The 4 new coach-contract tests collect; verb/prescribe/balance asserts are RED for the unsharpened cells; the hype byte-identity test and all `test_prompt_01_*` hype anchors still pass when the full file is run.</done>
</task>

<task type="auto">
  <name>Task 2: Fence the KEY_CLASH/TRANSITION coach voice in test_coach.py</name>
  <files>tests/state/test_coach.py</files>
  <action>
    Add a `test_task_key_clash_*` test (and a companion `test_task_transition_opportunity_*`) modeled on `test_task_mix_move_LOAD_BEARING_anti_slop_clause` (279-298), using the existing `_ev(type_, extra)` helper. This is the Wave-0 gap from 61-RESEARCH (no KEY_CLASH/harmonic test exists today).

    For KEY_CLASH: build `_ev("KEY_CLASH", {"a_side": "A", "a_camelot": "8A", "b_side": "B", "b_camelot": "2A", "semitones": 3})` and call `AICoach.task_for_event(...)`. Assert (satisfying COACH-01 prescriptive + COACH-04 cited):
    - a DJ-verb move is prescribed — assert at least one of "kill", "cut", "filter" appears;
    - BOTH keys are cited exactly: `[key:A:8A]` and `[key:B:2A]` are substrings;
    - the no-invent guard is present: "Do NOT invent a key" is a substring;
    - the system-owns-the-verdict framing is present ("you do NOT decide this" / "confirmed by the system").

    For TRANSITION_OPPORTUNITY: build `_ev("TRANSITION_OPPORTUNITY", {"a_side": "A", "a_camelot": "8A", "b_side": "B", "b_camelot": "9A", "clash": False})`. Assert past-tense framing (no present-tense imperative — assert "no present-tense advice" / "already gone" substring per coach.py:312-313), both keys cited, and "Do NOT invent a key".

    Use exact substrings from the live arm text (coach.py:280-316) so the test fences the real output. Do NOT touch the FROZEN byte-`==` goldens (`test_task_layer_arrival_exact_string` 271-276, `test_task_heartbeat_LOAD_BEARING_anti_silence_clause` 301-309) or the MIX_MOVE asserts.
  </action>
  <acceptance_criteria>
    - `test_task_key_clash_*` and `test_task_transition_opportunity_*` exist and PASS against the current (Phase-60) arms.
    - They assert DJ-verb move + both keys cited + no-invent guard (KEY_CLASH) and past-tense + both keys + no-invent (TRANSITION).
    - The frozen byte-`==` goldens in this file still pass.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -m pytest -q tests/state/test_coach.py -k "key_clash or transition or mix_move or heartbeat or layer_arrival" 2>&1 | tail -15</automated>
  </verify>
  <done>Both new harmonic-voice tests pass; the existing frozen coach goldens still pass; the harmonic voice is now fenced so a future cell edit cannot silently break it.</done>
</task>

<task type="auto">
  <name>Task 3: Dual-path COACH-02 proof + coach-context anti-slop assert</name>
  <files>tests/agent/test_dj_cohost_matrix_dispatch.py, tests/agent/test_coach_prompt_grounding.py</files>
  <action>
    Two additions, no production code touched.

    (1) In test_dj_cohost_matrix_dispatch.py — add `test_dispatch_61_coach_prompt_body_feeds_both_paths` modeled on `test_dispatch_06` (180-205). Set env `VIBEMIX_SKILL_LEVEL=pro`, `VIBEMIX_MODE=coach`; patch `Agent.__init__`; construct `DJCoHostAgent` with the existing `_build_state` / `_FakeRecorder` scaffold. Assert that `agent._gen_cfg.system_instruction == agent._prompt_body` (the genai path and the OpenRouter `stream_or` path consume the SAME body — proving the persona flows through both with no path-specific code; this is the COACH-02 proof point). Also assert `agent._prompt_body.startswith(_coach_rendered(COACH_PRO))` and `_VALID_MODES == frozenset({"hype","coach"})` is unchanged (import `_VALID_MODES` from `vibemix.prompts.matrix`) — the no-new-mode invariant. Reference dj_cohost.py:380 (`_prompt_body`), :447 (`_gen_cfg.system_instruction = prompt_body`), :813-816 (`stream_or(system_instruction=self._prompt_body)`).

    (2) In test_coach_prompt_grounding.py — add `test_coach_grounding_61_fabricated_key_strips` asserting the coach-context anti-slop contract (COACH-04). Use the existing `EvidenceRegistry` snapshot helper (52-59) to build a snapshot WITHOUT any observed key, then run a coach reply containing a fabricated `[key:A:12B]` through `CitationLinter` (import from `src/vibemix/coach/citation_linter.py`) and assert the WHOLE turn is stripped (existence-only branch). Reuse the linter's existing public API as exercised in `tests/coach/test_citation_linter.py` — do NOT duplicate that file's logic, just assert the contract holds in a coach-prompt context. Do NOT modify the linter.
  </action>
  <acceptance_criteria>
    - `test_dispatch_61_coach_prompt_body_feeds_both_paths` PASSES, proving `_gen_cfg.system_instruction == _prompt_body` and `_VALID_MODES` is exactly `{"hype","coach"}`.
    - `test_coach_grounding_61_fabricated_key_strips` PASSES, proving a fabricated `[key:...]` strips the turn.
    - No production module (`dj_cohost.py`, `matrix.py`, `coach.py`, `citation_linter.py`) is modified.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -m pytest -q tests/agent/test_dj_cohost_matrix_dispatch.py tests/agent/test_coach_prompt_grounding.py -k "61 or dispatch_06 or dispatch" 2>&1 | tail -15</automated>
  </verify>
  <done>Dual-path equality and no-new-mode invariant proven; fabricated-key strip proven in coach context; no production code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector→prompt builder | Harmonic facts (keys, semitone gap) enter `task_for_event` from the Phase-60 detector, NOT from user text. Must stay detector-supplied. |
| model output→user | Model may fabricate a `[key:...]`; the response crosses to the user only after the CitationLinter strip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | COACH cell prompt text | accept | Cells are fixed module constants; `.replace("{mood_persona}", <fixed dict value>)` only — no user input interpolated (matrix.py:46-49). This plan adds no interpolation. |
| T-61-02 | Information disclosure (false) | model-fabricated `[key:...]` | mitigate | Test 3 asserts the existence-only CitationLinter strips the whole turn — fences the structural guarantee the persona relies on. Linter NOT modified. |
| T-61-SC | Tampering | npm/pip/cargo installs | mitigate | None — this plan installs no packages (test-only edits in existing `.venv`). |
</threat_model>

<verification>
- Full fence subset green: `PYTHONPATH=src .venv/bin/python -m pytest -q tests/prompts/test_matrix.py tests/state/test_coach.py tests/agent/test_persona.py tests/agent/test_hype_prompt_grounding.py 2>&1 | tail -5` — every HYPE golden byte-stable (the new RED coach-contract asserts are the only expected failures, and they live under `-k 61_coach`).
- Baseline invariant: full suite still shows exactly 7 pre-existing failures (`tests/scripts/test_cut_release_preflight.py` x2, `tests/test_main_smoke.py` x1) — no NEW failures from this plan beyond the intended RED `61_coach` cell-contract asserts (which 61-02 turns green).
- Update 61-VALIDATION.md Per-Task Verification Map with the three tasks (Plan 61-01) and their commands.
</verification>

<success_criteria>
- The coach-cell contract (verbs/prescribe/balance/calm-tag) is asserted for all 3 coach skills; verb/prescribe/balance asserts are RED (spec for 61-02).
- KEY_CLASH + TRANSITION coach voice fenced (DJ-verb move, both keys cited, no-invent, past-tense) and GREEN.
- Dual-path `_prompt_body` equality + no-new-mode invariant proven GREEN.
- Coach-context fabricated-key strip proven GREEN.
- No hype golden modified; no production module modified; 7-failure baseline preserved.
</success_criteria>

<output>
Create `.planning/phases/61-actionable-not-hype-coach-persona/61-01-SUMMARY.md` when done
</output>
