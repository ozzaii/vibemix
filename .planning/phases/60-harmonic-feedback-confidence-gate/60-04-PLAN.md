---
phase: 60-harmonic-feedback-confidence-gate
plan: 04
type: execute
wave: 3
depends_on: ["60-02"]
files_modified:
  - src/vibemix/state/coach.py
  - src/vibemix/prompts/matrix.py
  - tests/state/test_coach_harmonic.py
autonomous: true
requirements: [HARMONIC-01, HARMONIC-04]
must_haves:
  truths:
    - "The KEY_CLASH coach fragment hands the LLM a pre-decided verdict + a CITE-both-keys instruction + forbids interval math — the LLM narrates, never computes"
    - "A fabricated [key:B:12B] the registry never observed strips the WHOLE turn (existence-only linter, inherited from Phase 59)"
    - "The TRANSITION_OPPORTUNITY fragment is past-tense / retrospective — no present-tense imperative verbs (bring/pull/drop)"
    - "No transition advice is emitted when the underlying signal is absent"
    - "Hype-mode + diet goldens are unchanged — replacing the stub arms touches only the two harmonic paths"
  artifacts:
    - path: "src/vibemix/state/coach.py"
      provides: "real cited KEY_CLASH + TRANSITION_OPPORTUNITY task_for_event arms (replacing the Phase-59 stubs)"
      contains: "a_camelot"
    - path: "tests/state/test_coach_harmonic.py"
      provides: "cited-fragment-shape + retrospective + uncited-strip tests"
      contains: "test_clash_fragment_is_cited_narration"
  key_links:
    - from: "src/vibemix/state/coach.py KEY_CLASH arm"
      to: "ev.extra a_side/a_camelot/b_side/b_camelot/semitones"
      via: "reads the extra keys the Plan-02 detector wrote"
      pattern: "ev.extra"
    - from: "src/vibemix/state/coach.py KEY_CLASH arm"
      to: "src/vibemix/coach/citation_linter.py (existence-only key source)"
      via: "instructs CITE both [key:...]; a fabricated key strips the turn"
      pattern: "key:"
---

<objective>
Replace the two Phase-59 stub `task_for_event` arms in `coach.py` with REAL cited
harmonic fragments (HARMONIC-01 narrate-only + HARMONIC-04 retrospective transition),
and reconcile the matrix grammar event-type list. The KEY_CLASH fragment hands the
LLM a system-decided verdict + both decks' keys to cite + a hard "do NOT compute
intervals" instruction. The TRANSITION_OPPORTUNITY fragment is strictly past-tense and
only speaks when the signal is grounded.

Purpose: This is where the deterministic verdict (Plan 01) + the gated detector
(Plan 02) become a grounded sentence the LLM merely narrates. The anti-slop guarantee
is structural — the existence-only CitationLinter strips any uncited harmonic claim.

Output: two real coach arms + a matrix grammar reconciliation + coach tests, with the
hype/diet goldens held byte-stable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-harmonic-feedback-confidence-gate/60-RESEARCH.md
@.planning/phases/60-harmonic-feedback-confidence-gate/60-PATTERNS.md

<interfaces>
<!-- Contracts the coach arm consumes. Executor uses these directly. -->

From the Plan-02 KEY_CLASH Event (the extra keys the detector writes):
- ev.extra["a_side"] = "A", ev.extra["a_camelot"] = "8A"
- ev.extra["b_side"] = "B", ev.extra["b_camelot"] = "3A"
- ev.extra["semitones"] = 1   (from harmonics.semitone_distance; may be None for cross-letter)
The TRANSITION_OPPORTUNITY Event extra carries both decks' resolved keys (Plan 02 §5 scope).

From src/vibemix/state/coach.py (shipped):
- `task_for_event(ev)` dispatch (lines 207-274) — the live MIX_MOVE arm (lines 238-251) is the
  fragment-construction model: read ev.extra, build an instruction string + a "single space to
  stay silent" escape. The STUB arms to REPLACE are at lines 264-273 (KEY_CLASH +
  TRANSITION_OPPORTUNITY, currently "...not live yet.").
- `ACK_ELIGIBLE_EVENTS` (line 44) — DO NOT add the two harmonic types to it (diet ValueError guard).
- `build_prompt(ev, *, registry_snapshot=None, diet=False)` (lines 276-310) — non-diet path tags `| event=TYPE`.

From src/vibemix/prompts/matrix.py (shipped):
- CITATION_GRAMMAR_BLOCK line 117: `[key:<deck>:<camelot>]  deck harmonic key, e.g. [key:A:8A]` (already present)
- Line 122-124: the "Event types currently tracked" list — currently the 7 v4 types, OMITS
  KEY_CLASH + TRANSITION_OPPORTUNITY. (Open Q1: _fire writes [ev:<TYPE>], so the LLM COULD
  validly cite [ev:KEY_CLASH@t]; add the two types here for grammar consistency — low risk,
  re-check the goldens after.)

From src/vibemix/coach/citation_linter.py (shipped Phase 59 — NO change):
- `key` is an existence-only source: a [key:A:8A] is valid iff the registry observed it; a
  fabricated [key:B:12B] strips the WHOLE turn. The anti-slop guarantee is structural.

The clash-fragment text model is in 60-RESEARCH §Pattern 4 (lines 357-370) — adapt, do not
copy a hype voice (Phase 61 owns the persona; keep this to the grounded FACTS).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the coach harmonic test (cited narration + retrospective + uncited-strip)</name>
  <read_first>
    - 60-RESEARCH.md §Validation lines 531-541 (the test names + behaviors) + §Pattern 4 (lines 352-373)
    - 60-PATTERNS.md "test_coach_harmonic.py" section (lines 169-171)
    - tests/state/test_coach_prompt_grounding.py + tests/state/test_coach_prompt_diet.py (the coach test idiom: build an Event, call AICoach.task_for_event / build_prompt, assert on the string)
    - src/vibemix/coach/citation_linter.py + an existing linter test (for the uncited-strip assertion)
  </read_first>
  <behavior>
    - test_clash_fragment_is_cited_narration: task_for_event(Event("KEY_CLASH", extra={a_side,a_camelot,b_side,b_camelot,semitones})) returns a string that (a) contains both [key:A:8A] and [key:B:3A], (b) states the verdict is system-decided (NOT the LLM's), (c) forbids interval/key math, (d) contains the "single space to stay silent" escape. Does NOT ask the LLM to compute compatibility.
    - test_clash_fragment_no_key_math: the fragment does not instruct the model to "calculate"/"figure out"/"determine if" the keys clash — it hands a decided verdict.
    - test_uncited_fabricated_key_strips_turn: a reply citing [key:B:12B] the registry never observed is stripped by CitationLinter (existence-only).
    - test_transition_is_retrospective: the TRANSITION_OPPORTUNITY fragment is past-tense; contains no present-tense imperative verbs ("bring", "pull", "drop it now", "cut it now").
    - test_both_types_not_ack_eligible: KEY_CLASH and TRANSITION_OPPORTUNITY are NOT in ACK_ELIGIBLE_EVENTS (build_prompt(diet=True) raises ValueError for them).
  </behavior>
  <action>
    Create tests/state/test_coach_harmonic.py mirroring the coach-golden test idiom. Build
    Events with the Plan-02 extra shape, call `AICoach.task_for_event(ev)` / `build_prompt(ev)`,
    and assert the behaviors above. For the uncited-strip test, reuse the existing CitationLinter
    test pattern (build a registry snapshot WITHOUT the fabricated key, run the linter, assert the
    turn is stripped). These are RED until Task 2 replaces the stub arms.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/state/test_coach_harmonic.py 2>&1 | grep -qE "(collected [1-9]|[0-9]+ (passed|failed))" && echo "TESTS-COLLECTED"</automated>
  </verify>
  <done>test_coach_harmonic.py exists with the five behaviors; pytest collects them (fail until Task 2 — expected).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the stub coach arms with cited fragments + reconcile matrix grammar</name>
  <read_first>
    - src/vibemix/state/coach.py (lines 207-310 — the dispatch, the MIX_MOVE/PHASE arms to model, the exact stub lines 264-273 to replace)
    - src/vibemix/prompts/matrix.py (lines 110-130 — the CITATION_GRAMMAR_BLOCK + the event-type list at 122-124)
    - 60-RESEARCH.md §Pattern 4 (lines 357-370) + Pitfall 5 (lines 437-441, the golden-regression guard)
  </read_first>
  <behavior>
    (same behaviors as Task 1 — implementation must turn every RED assertion GREEN, AND the
    hype/diet/grounding goldens must remain unchanged)
  </behavior>
  <action>
    In src/vibemix/state/coach.py, REPLACE the two stub arms (lines 264-273):
    - KEY_CLASH: read ev.extra a_side/a_camelot/b_side/b_camelot/semitones; return an instruction
      that (a) states "HARMONIC CLASH confirmed by the system (you do NOT decide this)", (b) names
      both decks + keys + the semitone count, (c) instructs CITE BOTH keys exactly as
      [key:<a_side>:<a_camelot>] and [key:<b_side>:<b_camelot>], (d) FORBIDS inventing a key or
      computing intervals — narrate the clash the code already proved, (e) tells Kaan the move in
      DJ verbs (kill mids, cut on the drop, filter one out — but keep it FACTUAL; Phase 61 sharpens
      the voice), (f) ends with the "output a single space to stay silent" escape. Guard the
      semitones interpolation against None (cross-letter → say "clashing" without a number).
    - TRANSITION_OPPORTUNITY: read the both-decks keys from ev.extra; return a RETROSPECTIVE,
      PAST-TENSE note ("you just blended A→B; harmonically the keys sat fine / clashed") scoped to
      what deck-state grounds. NO present-tense imperatives. NO phrase-alignment or bass-swap
      specifics (vibemix lacks per-deck phrase grid + dual-deck low-band — not groundable, stay
      silent on them). End with the "single space to stay silent" escape.
    Do NOT touch any other arm. Do NOT add either type to ACK_ELIGIBLE_EVENTS.
    In src/vibemix/prompts/matrix.py, add KEY_CLASH and TRANSITION_OPPORTUNITY to the
    "Event types currently tracked" list (line 124) for grammar consistency (Open Q1 resolution —
    _fire writes [ev:<TYPE>], so the model may validly cite them). Re-run the matrix/coach goldens
    after; if a golden shifts, that is a real regression to investigate — do NOT blindly re-baseline.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/state/test_coach_harmonic.py tests/state/test_coach_prompt_grounding.py tests/state/test_coach_prompt_diet.py</automated>
  </verify>
  <done>Both stub arms replaced with cited fragments; test_coach_harmonic.py green; the grounding + diet goldens unchanged (no regressions). KEY_CLASH/TRANSITION_OPPORTUNITY added to the matrix event-type list. Neither added to ACK_ELIGIBLE_EVENTS.</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/state/test_coach_harmonic.py` — green.
- `PYTHONPATH=src python3 -m pytest -q tests/state/test_coach_prompt_grounding.py tests/state/test_coach_prompt_diet.py` — unchanged, no golden churn.
- `PYTHONPATH=src python3 -m pytest -q` — full suite; the 7 pre-existing failures stay at 7, no new failures.
- `grep -n "a_camelot\|do NOT decide this\|single space" src/vibemix/state/coach.py` — confirms the cited narrate-only fragment.
- `grep -n "KEY_CLASH\|TRANSITION_OPPORTUNITY" src/vibemix/prompts/matrix.py` — both listed in the grammar block.
</verification>

<success_criteria>
- HARMONIC-01 narrate-only half: the clash fragment hands a system-decided verdict + cite-both-keys instruction + forbids key math; a fabricated key strips the turn (structural anti-slop).
- HARMONIC-04: the transition fragment is retrospective/past-tense and silent when the signal is absent; no phrase/bass-swap guesses.
- No scope creep: no next-track recommendation, no 1-10 scoring in either fragment.
- Hype/diet goldens held byte-stable (Phase 61 owns the persona voice).
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coach fragment → LLM | the fragment hands a verdict; the LLM must narrate, not decide |
| LLM reply → CitationLinter | untrusted generated text; uncited [key:...] strips the whole turn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04 | Spoofing (false expertise) | LLM fabricating a [key:...] the registry never observed | mitigate | CitationLinter existence-only strip of the `key` source (shipped Phase 59, no change) — the fragment only INSTRUCTS citation; the linter enforces it |
| T-60-05 | Tampering | LLM computing/inventing intervals despite the fragment | mitigate | semitones is pre-computed in the Event extra (Plan 01); the fragment forbids key math; verdict is is_clash() not the LLM |
| T-60-06 | Repudiation | golden regression masking a persona drift into this phase | mitigate | hold the hype/diet goldens byte-stable; investigate (don't re-baseline) any shift; Phase 61 owns the voice |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages; no install task exists |
</threat_model>

<output>
Create `.planning/phases/60-harmonic-feedback-confidence-gate/60-04-SUMMARY.md` when done.
</output>
