---
phase: 60-harmonic-feedback-confidence-gate
plan: 03
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - tests/fixtures/kaan_disagreed_pairs.json
  - tests/state/test_kaan_ear_veto.py
  - eval/harmonic/run_veto.py
autonomous: true
requirements: [HARMONIC-03]
must_haves:
  truths:
    - "A disagreed-pairs corpus exists — pairs Kaan would happily mix that MUST NOT flag"
    - "The corpus also holds a small set of TRUE clashes that SHOULD flag (proves the gate isn't vacuously suppressing everything)"
    - "A runnable check scores the corpus and surfaces a pass/fail KAAN-ACTION ship gate (non-zero exit on any false clash)"
    - "The detector ships GATED (harmonic_clash_enabled stays False) until Kaan signs off — conservative default = quiet"
  artifacts:
    - path: "tests/fixtures/kaan_disagreed_pairs.json"
      provides: "the disagreed-pairs corpus + true-clash control set"
      contains: "8A"
    - path: "tests/state/test_kaan_ear_veto.py"
      provides: "parametrized test asserting no disagreed pair flags + true clashes do flag"
      contains: "test_disagreed_pairs_never_flag"
    - path: "eval/harmonic/run_veto.py"
      provides: "runnable corpus scorer with documented floor + exit code (the KAAN-ACTION surface)"
      contains: "is_clash"
  key_links:
    - from: "tests/state/test_kaan_ear_veto.py"
      to: "src/vibemix/state/harmonics.py::is_clash"
      via: "asserts the predicate verdict over the corpus"
      pattern: "is_clash"
    - from: "eval/harmonic/run_veto.py"
      to: "tests/fixtures/kaan_disagreed_pairs.json"
      via: "loads the corpus and scores it"
      pattern: "kaan_disagreed_pairs"
---

<objective>
Build the Kaan-ear veto harness (HARMONIC-03 ship gate): a disagreed-pairs corpus
(pairs Kaan would happily mix that MUST NOT flag), a parametrized test enforcing it,
and a runnable scorer that surfaces the pass/fail as the KAAN-ACTION ship gate. The
detector stays GATED (`harmonic_clash_enabled=False`, set in Plan 02) until Kaan
signs off — conservative default means no false clash can reach the audience first.

Purpose: Over-flagging (a false clash on a pair Kaan would happily mix) is the failure
mode that trips the release gate. This harness is the proof that it can't happen,
modeled on the Phase-59 deck-vision eval ship-gate precedent.

Output: corpus fixture + veto test + runnable scorer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-harmonic-feedback-confidence-gate/60-RESEARCH.md
@.planning/phases/60-harmonic-feedback-confidence-gate/60-PATTERNS.md

<interfaces>
<!-- Contracts the harness uses. Executor uses these directly. -->

From src/vibemix/state/harmonics.py (Plan 60-01 — completed Wave 1):
- `def is_clash(a: str | None, b: str | None) -> bool`  — the predicate the corpus tests

The corpus shape (60-RESEARCH §Code Examples lines 460-481):
- Disagreed (MUST NOT flag): 8A/9A (adjacent fifth), 8A/8B (relative major), 8A/10A (+2 energy),
  5A/5A (same key), plus drift-band pairs Kaan rides (e.g. 8A/5A, 8A/12A).
- True clashes (SHOULD flag): 8A/3A, 8A/1A (1 semitone), 8A/2A (tritone).

Ship-gate precedent to mirror (60-PATTERNS lines 175-191):
- eval/deck_vision/run_eval.py — the ACCURACY_FLOOR block (lines 80-89) + WHY-THIS-GATE
  docstring + pass/fail exit-code reporting (lines 307-324). The conservative-default doc style:
  "below the floor degrades to the conservative default (detector stays OFF), NOT a crash".
- DeckPoller._vision_enabled (deck_poller.py:97-116) — the default-off flag the veto unlocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the disagreed-pairs corpus + the veto test</name>
  <read_first>
    - 60-RESEARCH.md §Code Examples (lines 460-481 — the corpus JSON shape + the two test functions, copy verbatim)
    - 60-PATTERNS.md "test_kaan_ear_veto.py" section (lines 175-191)
    - tests/state/test_harmonics.py (the parametrized-pairs + loader idiom to mirror)
  </read_first>
  <action>
    Create tests/fixtures/kaan_disagreed_pairs.json — a JSON array of objects, each
    {"a": "<camelot>", "b": "<camelot>", "verdict": "safe"|"clash", "note": "<why>"}.
    Populate the "safe" entries with pairs Kaan would happily mix (8A/9A adjacent fifth,
    8A/8B relative major, 8A/10A +2 energy, 5A/5A same key, 8A/5A + 8A/12A drift he rides) and
    the "clash" control entries (8A/3A, 8A/1A one-semitone, 8A/2A tritone). Each carries a human
    "note" explaining the relationship — this is Kaan's editable corpus, so keep it readable.
    Create tests/state/test_kaan_ear_veto.py: a `load_disagreed_pairs()` helper that reads the
    fixture, a `@pytest.mark.parametrize` `test_disagreed_pairs_never_flag` asserting
    `is_clash(p["a"], p["b"]) is False` for every "safe"-verdict pair, and
    `test_true_clashes_do_flag` asserting `is_clash` is True for every "clash"-verdict pair
    (proves the gate is NOT vacuous). Resolve the fixture path relative to the test file
    (do not hardcode an absolute path).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/state/test_kaan_ear_veto.py</automated>
  </verify>
  <done>The corpus fixture exists with safe + clash entries; both veto tests pass against the Plan-01 is_clash predicate.</done>
</task>

<task type="auto">
  <name>Task 2: Write the runnable veto scorer (the KAAN-ACTION ship-gate surface)</name>
  <read_first>
    - eval/deck_vision/run_eval.py (lines 1-30 module docstring, 80-126 the floor + scoring, 307-324 the exit-code reporting — mirror this harness shape)
    - the corpus fixture authored in Task 1
  </read_first>
  <action>
    Create eval/harmonic/run_veto.py — a standalone script (runnable as
    `PYTHONPATH=src python3 eval/harmonic/run_veto.py`) that loads
    tests/fixtures/kaan_disagreed_pairs.json, runs `is_clash` over every pair, and reports:
    per-pair PASS/FAIL (a "safe" pair flagging = FAIL = a false clash; a "clash" pair not
    flagging = FAIL = vacuous gate), a summary line, and the conservative-default doc note —
    mirror eval/deck_vision/run_eval.py's WHY-THIS-GATE docstring and exit-code discipline:
    exit 0 only when EVERY pair matches its declared verdict, non-zero otherwise. Add a short
    module docstring stating this is the HARMONIC-03 KAAN-ACTION ship gate: the runtime
    `harmonic_clash_enabled` flag (Plan 02) stays False until Kaan reviews this corpus + run
    and confirms zero false clash, then flips the flag — conservative default = detector quiet
    until sign-off. No new dependencies (stdlib json + the shipped harmonics import).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 eval/harmonic/run_veto.py; echo "exit=$?"</automated>
  </verify>
  <done>run_veto.py loads the corpus, scores it via is_clash, prints per-pair results + summary, and exits 0 (corpus passes against the conservative predicate). The KAAN-ACTION ship-gate is documented in its module docstring.</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/state/test_kaan_ear_veto.py` — green.
- `PYTHONPATH=src python3 eval/harmonic/run_veto.py` — exits 0, prints the corpus scorecard.
- `PYTHONPATH=src python3 -m pytest -q` — full suite; 7 pre-existing failures stay at 7.
- The runtime `harmonic_clash_enabled` flag (Plan 02) remains False in source — this harness is the gate that, once Kaan signs off, justifies flipping it (KAAN-ACTION; does not pause per gsd-autonomous fully — default-off is the safe state).
</verification>

<success_criteria>
- HARMONIC-03 ship gate: a runnable disagreed-pairs check exists; no pair Kaan would mix flags; true clashes still flag (non-vacuous).
- The corpus is human-editable JSON (Kaan can add his real disagreed pairs).
- The detector stays gated/quiet by default until the Kaan-ear veto passes and Kaan flips the flag.
</success_criteria>

<output>
Create `.planning/phases/60-harmonic-feedback-confidence-gate/60-03-SUMMARY.md` when done.
</output>
