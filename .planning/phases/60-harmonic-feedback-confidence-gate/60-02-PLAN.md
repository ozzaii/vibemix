---
phase: 60-harmonic-feedback-confidence-gate
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - src/vibemix/state/event_detector.py
  - tests/state/test_event_detector_harmonic.py
autonomous: true
requirements: [HARMONIC-02, HARMONIC-03]
must_haves:
  truths:
    - "The suppression gate runs BEFORE any clash note — no clash unless audible_deck=='mix', non-breakdown phase, rms ≥ LOW_RMS, not vocal_active, with tonal mid/high share"
    - "No clash on percussive/atonal or breakdown/acapella content"
    - "Cross-deck suppression: a clash requires BOTH decks independently resolved AND both confidence ≥ DECK_CITE_MIN_CONF (0.6)"
    - "The detector is OFF by default (harmonic_clash_enabled=False) — never fires until the Kaan-ear veto flips it"
    - "KEY_CLASH fires only on is_clash()==True; the 28s cooldown + the music-truly-playing gate are inherited"
    - "TRANSITION_OPPORTUNITY is groundable-only + retrospective; stays silent when the signal is absent"
  artifacts:
    - path: "src/vibemix/state/event_detector.py"
      provides: "_melodic_overlap_gate + KEY_CLASH branch + TRANSITION_OPPORTUNITY branch + harmonic_clash_enabled flag"
      contains: "_melodic_overlap_gate"
    - path: "tests/state/test_event_detector_harmonic.py"
      provides: "gate + firing + cross-deck-suppression + cooldown + default-off tests"
      contains: "test_detector_gated_by_default"
  key_links:
    - from: "src/vibemix/state/event_detector.py KEY_CLASH branch"
      to: "src/vibemix/state/harmonics.py::is_clash"
      via: "deterministic verdict call inside the branch"
      pattern: "is_clash\\("
    - from: "src/vibemix/state/event_detector.py _melodic_overlap_gate"
      to: "MusicState.audible_deck / phase / rms / vocal_active / bands"
      via: "read-only composition of shipped signals"
      pattern: "audible_deck"
---

<objective>
Wire the two harmonic detector branches into `EventDetector.detect()`: a
`_melodic_overlap_gate(state)` precondition (HARMONIC-02 suppression — built ENTIRELY
from shipped MusicState signals, no new detector stack), the `KEY_CLASH` branch
(fires only on the deterministic `is_clash()` verdict with cross-deck + cite-floor
suppression — HARMONIC-03 conservatism), the `TRANSITION_OPPORTUNITY` branch
(retrospective, groundable-only), and the default-off `harmonic_clash_enabled` flag
(mirrors `DeckPoller._vision_enabled` — the Kaan-ear ship gate).

Purpose: This is where conservatism is enforced at runtime. The clash CANNOT reach
the audience until the gate passes AND the flag flips. Under-flag over over-flag.

Output: Two new branches + a gate helper + a default-off flag in event_detector.py,
plus a detector test proving every suppression path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-harmonic-feedback-confidence-gate/60-RESEARCH.md
@.planning/phases/60-harmonic-feedback-confidence-gate/60-PATTERNS.md

<interfaces>
<!-- Contracts the detector reads/uses. Executor uses these directly — no exploration. -->

From src/vibemix/state/harmonics.py (Plan 60-01 — completed in Wave 1):
- `def is_clash(a: str | None, b: str | None) -> bool`  — the deterministic verdict
- `def semitone_distance(a: str | None, b: str | None) -> int | None`  — for the Event extra

From src/vibemix/state/event_detector.py (shipped):
- `EventDetector.__init__(self, audio_buf=None, *, evidence_registry=None)` (lines 82-122) — add the flag here
- `_cooldown_ok(self, ev_type, now) -> bool` (lines 124-127) — reuse as-is; KEY_CLASH=28s / TRANSITION_OPPORTUNITY=20s already in MIN_EVENT_GAP_PER_TYPE
- `_music_truly_playing(self, state, now) -> bool` (lines 129-143) — the gate-helper pattern to mirror; both new branches sit INSIDE it (auto-events)
- `detect()` MIX_MOVE branch (lines 269-275) — the diff→Event+_fire+return shape to copy
- `_fire(ev_type, now, state, *, cooldown_key=None)` (lines 296-333) — generic over ev_type; writes [ev:<TYPE>]; reuse as-is
- Branch placement: AFTER the MIX_MOVE block (line 275), BEFORE the genre-chain loop (line 284)
- Existing suppression idioms to copy from the LAYER_ARRIVAL branch: `state.rms > LOW_RMS` (line 232), `not state.vocal_active` (line 233)

From src/vibemix/state/music_state.py (shipped — READ-ONLY, single-writer rule):
- `audible_deck: str` ∈ {'A','B','mix','none'} (line 114)
- `phase: str`, `rms: float`, `onset_density: float` (line 33), `vocal_active: bool` (line 43)
- `bands: dict` = {'sub','low','mid','high'} float shares (line 32)
- `deck_state: DeckState` (line 69); `DeckState.decks: dict[str, DeckTrack]`
- `DeckTrack`: `camelot: str | None`, `confidence: float`, `source: str` (deck_state.py:39-46)

From src/vibemix/audio/constants.py (shipped — READ-ONLY):
- `SILENT_RMS = 0.012` · `LOW_RMS = 0.040` · `PEAK_RMS = 0.110` (lines 53-55)
  (NOTE: there is NO constant named `PEAK_FLOOR_RMS` — use LOW_RMS as the breakdown floor)

From src/vibemix/state/deck_poller.py: `DECK_CITE_MIN_CONF = 0.6` (line 73) — import it.
The `_vision_enabled` default-off pattern (lines 97-116) is the precedent for the new flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the harmonic detector test (gate + firing + suppression + default-off)</name>
  <read_first>
    - 60-VALIDATION.md (the per-task map + the anti-slop guarantees table)
    - 60-RESEARCH.md §Validation lines 533-539 (the exact test names + behaviors)
    - 60-PATTERNS.md "tests/state/test_event_detector_harmonic.py" section (lines 161-165)
    - existing tests/state/ event-detector tests (mirror the EventDetector construction + MusicState-snapshot feeding + assert-Event-or-None shape)
    - src/vibemix/state/event_detector.py detect() (lines 154-294 — to understand what state fields each branch reads, and the music-truly-playing preconditions a fired test must satisfy: state.audible True for MUSIC_PRESENCE_MIN_SECONDS + valid BPM)
  </read_first>
  <action>
    Create tests/state/test_event_detector_harmonic.py. Build an `EventDetector`, construct
    `MusicState` snapshots with deck_state.decks populated, and assert the returned `Event`
    (or None). To make a clash CAPABLE of firing, set the flag (construct the detector with the
    new harmonic flag True OR set the attribute), satisfy `_music_truly_playing` (audible True,
    advance past MUSIC_PRESENCE_MIN_SECONDS, valid BPM), set audible_deck="mix", a non-breakdown
    phase, rms ≥ LOW_RMS, vocal_active False, tonal band share present, and decks A/B both with
    camelot in a clash relationship + confidence ≥ DECK_CITE_MIN_CONF. Cover EVERY suppression
    path as separate tests:
      - test_detector_gated_by_default — flag False (default) → clash setup → returns None (never fires)
      - test_no_clash_single_deck — audible_deck != "mix" → None
      - test_no_clash_in_breakdown — phase ∈ {breakdown, silent, low} → None
      - test_no_clash_percussive — vocal_active True (and/or no tonal share) → None
      - test_no_clash_subfloor_deck — deck B confidence < DECK_CITE_MIN_CONF → None (cross-deck suppression)
      - test_no_clash_missing_second_deck — decks.get("B") absent → None
      - test_no_clash_safe_pair — both decks resolved + cited but is_clash False (8A/9A) → None
      - test_clash_fires_when_all_conditions_met — flag True + full setup + is_clash True (8A/3A) → Event("KEY_CLASH") with extra a_side/a_camelot/b_side/b_camelot/semitones
      - test_clash_respects_cooldown — second consecutive tick within 28s → None
    These are the RED scaffold for Task 2. Some will fail until the branch exists; that is fine —
    Task 2 makes them all pass. Reuse the `_cooldown_ok` semantics; do NOT re-implement cooldown
    logic in the test.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/state/test_event_detector_harmonic.py 2>&1 | grep -qE "(collected [1-9]|[0-9]+ (passed|failed))" && echo "TESTS-COLLECTED"</automated>
  </verify>
  <done>test_event_detector_harmonic.py exists with the nine suppression/firing/cooldown/default-off tests; pytest collects them (they fail until Task 2 — expected).</done>
</task>

<task type="auto">
  <name>Task 2: Add _melodic_overlap_gate + KEY_CLASH + TRANSITION_OPPORTUNITY branches + default-off flag</name>
  <read_first>
    - src/vibemix/state/event_detector.py (the full detect() + __init__ + _cooldown_ok + _fire — lines 80-334)
    - 60-RESEARCH.md §Pattern 2 (lines 271-310, the gate), §Pattern 3 (lines 312-350, branch placement + KEY_CLASH body)
    - 60-PATTERNS.md "event_detector.py" + "Shared Patterns / Default-off ship gate" (lines 56-90, 195-205)
    - src/vibemix/state/deck_poller.py lines 97-116 (the _vision_enabled default-False construction + rationale comment to mirror)
  </read_first>
  <action>
    Modify src/vibemix/state/event_detector.py:
    (1) Import `is_clash` and `semitone_distance` from `vibemix.state.harmonics`, and
        `DECK_CITE_MIN_CONF` from `vibemix.state.deck_poller` (or wherever it is defined —
        confirm via read; if importing from deck_poller creates a cycle, define a local
        module-level mirror constant with a comment pointing at the canonical 0.6).
    (2) In `__init__`, add `self._harmonic_clash_enabled = False` with a rationale comment
        copied in spirit from `DeckPoller._vision_enabled` (lines 97-116): the detector ships
        gated/quiet by default until the Kaan-ear veto corpus (Plan 60-03) passes — the
        KAAN-ACTION ship gate. Default False is conservative-by-design. Accept an optional
        keyword `harmonic_clash_enabled: bool = False` on `__init__` so a future caller / test
        can flip it without monkeypatching, mirroring the `vision_enabled` kwarg shape.
    (3) Add the private `_melodic_overlap_gate(self, state) -> bool` helper (mirror
        `_music_truly_playing` shape) per §Pattern 2: return False unless audible_deck=="mix"
        AND rms ≥ LOW_RMS AND phase not in {"breakdown","silent","low"} AND not vocal_active
        AND a tonal mid/high band share is present. For the tonal-share floor use a
        module-level constant (Claude's discretion — start conservative, e.g. require
        `state.bands["mid"] + state.bands["high"]` above a small floor; document the WHY).
        Build the gate ONLY from shipped fields; no new audio computation.
    (4) Insert the KEY_CLASH branch AFTER the MIX_MOVE block (line 275), BEFORE the genre-chain
        loop (line 284), copying the MIX_MOVE diff→Event+_fire+return shape (§Pattern 3): guard
        on `self._harmonic_clash_enabled and self._melodic_overlap_gate(state)` first, then read
        `decks = state.deck_state.decks; a, b = decks.get("A"), decks.get("B")`, require both
        present with camelot AND both `confidence >= DECK_CITE_MIN_CONF` (cross-deck +
        cite-floor suppression), then `is_clash(a.camelot, b.camelot)` AND
        `self._cooldown_ok("KEY_CLASH", now)`; on all-true build
        `Event("KEY_CLASH", state, extra={"a_side":"A","a_camelot":a.camelot,"b_side":"B",
        "b_camelot":b.camelot,"semitones":semitone_distance(a.camelot,b.camelot)})`, call
        `self._fire("KEY_CLASH", now, state)`, return ev.
    (5) Add the TRANSITION_OPPORTUNITY branch immediately after, scoped to ONLY what deck-state
        can ground retrospectively (per Open Q2 / §5: vibemix has NO per-deck phrase grid + NO
        dual-deck low-band, so phrase-alignment/bass-swap notes are NOT groundable → stay
        silent). Fire ONLY when both decks are resolved + cited AND there is a recent structural
        xfader/EQ move in state.recent_moves indicating a blend just happened (reuse the
        MIX_MOVE significance keys), gated by `self._harmonic_clash_enabled` and
        `self._cooldown_ok("TRANSITION_OPPORTUNITY", now)`. Emit a minimal Event whose extra
        carries both decks' resolved keys so the coach can narrate "you just blended A→B, the
        keys sit fine/clash" retrospectively. If the groundable conditions are not met, do NOT
        fire (silence over a guess — acceptable to be near-zero this phase).
    Keep BOTH types OUT of ACK_ELIGIBLE_EVENTS (no change to that set). Branches READ MusicState
    only — never write deck-state (single-writer rule). Exception-safe like the rest of detect().
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/state/test_event_detector_harmonic.py && grep -c "_melodic_overlap_gate" src/vibemix/state/event_detector.py</automated>
  </verify>
  <done>All test_event_detector_harmonic.py tests pass. `_melodic_overlap_gate` defined; KEY_CLASH + TRANSITION_OPPORTUNITY branches placed between MIX_MOVE and the genre chain; `harmonic_clash_enabled` defaults False; both types absent from ACK_ELIGIBLE_EVENTS.</done>
</task>

</tasks>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/state/test_event_detector_harmonic.py` — all green.
- `PYTHONPATH=src python3 -m pytest -q` — full suite; the 7 pre-existing failures stay at 7, no new failures, no golden regressions.
- `grep -nE "_harmonic_clash_enabled|_melodic_overlap_gate|is_clash\(" src/vibemix/state/event_detector.py` shows the flag (default False), the gate, and the verdict call.
- `grep -n "KEY_CLASH\|TRANSITION_OPPORTUNITY" src/vibemix/state/coach.py | grep ACK` — confirm neither was added to ACK_ELIGIBLE_EVENTS.
</verification>

<success_criteria>
- HARMONIC-02: clash CANNOT fire unless _melodic_overlap_gate returns True; percussive/breakdown/acapella/single-deck all suppressed.
- HARMONIC-03: cross-deck + cite-floor suppression (both decks ≥ 0.6); safe pairs never fire; detector OFF by default until the Kaan-ear veto flips the flag.
- TRANSITION_OPPORTUNITY fires only on groundable retrospective conditions; silent otherwise.
- No new detector stack — the gate composes shipped MusicState signals only.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| library/MIDI metadata → MusicState.deck_state | untrusted third-party key tags (~57-70% accurate) enter here; the detector must treat them conservatively |
| MusicState → EventDetector | read-only; detector never writes deck-state (single-writer rule in refresh.py) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Spoofing (false expertise) | KEY_CLASH branch firing on a wrong key tag | mitigate | Layered: is_clash NARROW (Plan 01) + both decks ≥ DECK_CITE_MIN_CONF + default-off flag until Kaan-ear veto (Plan 03) |
| T-60-02 | Tampering | track metadata reaching the detector | mitigate | camelot validated by harmonics.to_camelot/_parse before is_clash; verdict computed in Python, not from LLM-parsed metadata |
| T-60-03 | Denial of Service | odd key tag crashing detect() | mitigate | is_clash/semitone_distance never raise (honest-None contract, Plan 01); branch is exception-safe like the rest of detect() |
| T-60-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (pure-Python logic on shipped modules); no install task exists |
</threat_model>

<output>
Create `.planning/phases/60-harmonic-feedback-confidence-gate/60-02-SUMMARY.md` when done.
</output>
