---
phase: 59-full-deck-awareness-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/state/harmonics.py
  - src/vibemix/state/deck_state.py
  - src/vibemix/state/music_state.py
  - tests/state/test_harmonics.py
  - tests/state/test_deck_state.py
  - tests/state/test_coach_prompt_grounding.py
autonomous: true
requirements: [DECK-01, DECK-02]
tags: [deck-state, harmonics, camelot, music-state]

must_haves:
  truths:
    - "harmonics.to_camelot('Am') == '8A', to_camelot('F#m') == '11A' — Rekordbox musical notation normalizes to Camelot"
    - "to_camelot('') and to_camelot(None) and to_camelot('garbage') return None — NEVER raise (honest unknown)"
    - "An empty MusicState.deck_state (decks={}) leaves evidence_line byte-identical to the v4 baseline (golden-equivalence)"
    - "DeckTrack defaults: source='unknown', every harmonic field None/0.0 — no false-confident key by construction"
  artifacts:
    - path: "src/vibemix/state/harmonics.py"
      provides: "Pure to_camelot() over the 24-entry Camelot table; load-bearing dependency of every key: citation"
      contains: "def to_camelot"
      min_lines: 30
    - path: "src/vibemix/state/deck_state.py"
      provides: "DeckTrack + DeckState dataclasses"
      contains: "class DeckState"
    - path: "src/vibemix/state/music_state.py"
      provides: "Additive deck_state field on MusicState"
      contains: "deck_state: DeckState"
  key_links:
    - from: "src/vibemix/state/music_state.py"
      to: "src/vibemix/state/deck_state.py"
      via: "deck_state: DeckState = field(default_factory=DeckState)"
      pattern: "deck_state: DeckState"
    - from: "src/vibemix/state/deck_state.py"
      to: "src/vibemix/state/harmonics.py"
      via: "camelot field populated by to_camelot in _tick_once (Plan 59-04)"
      pattern: "camelot"
---

<objective>
Land the two zero-dependency foundations every other plan in Phase 59 builds on: the load-bearing pure function `harmonics.to_camelot()` (Rekordbox `Tonality` is musical notation `Am`/`F#m`, NOT Camelot — every `key:` citation depends on this conversion existing) and the additive `DeckState`/`DeckTrack` model embedded in `MusicState`.

Purpose: `to_camelot` MUST exist before any `key:` citation can carry the normalized Camelot the coach reasons on; the `DeckState` field must be additive so existing snapshot/prompt goldens stay byte-identical until populated (Pitfall 5).

Output: `state/harmonics.py` (NEW, pure), `state/deck_state.py` (NEW, dataclasses), `state/music_state.py` (MODIFIED, additive field), plus Wave-0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md
@.planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md

<interfaces>
<!-- Verified against branch live-tuning-or-brain. Use these directly. -->

state/music_state.py — additive-field discipline (every field uses a default so golden-equivalence holds):
- Module header: `from __future__ import annotations` (line 14), `from dataclasses import dataclass, field` (line 18)
- `class MusicState:` (line 22); Phase 52 precedent field `detected_genre: str = "unknown"` (line 53)
- Controller fields `deck_a: dict = field(default_factory=dict)` (line 93) — distinct from this plan's `deck_state`
- `_lock: threading.Lock = field(default_factory=threading.Lock)` (line 129)

RESEARCH §Code Examples ships the EXACT dataclass bodies (lines 200-219) and the full Camelot table (lines 417-445). Copy them verbatim — do not invent the table.

License header convention (line 1 of every src file): `# SPDX-License-Identifier: Apache-2.0`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: harmonics.to_camelot — the load-bearing pure function</name>
  <files>src/vibemix/state/harmonics.py, tests/state/test_harmonics.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Code Examples lines 417-445 — verbatim table + function; §Pitfall 1 lines 384-388)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§state/harmonics.py — analog audio/features.py, degradation discipline)
    - src/vibemix/state/track_resolver.py (lines 1-3 license header; honest-unknown degradation pattern)
  </read_first>
  <behavior>
    - to_camelot("Am") == "8A"; to_camelot("F#m") == "11A"; to_camelot("Cm") == "5A"; to_camelot("Gm") == "6A"; to_camelot("Dm") == "7A"
    - to_camelot("8A") == "8A" (Camelot input passes through, upper-cased); to_camelot("8a") == "8A"
    - to_camelot("C") == "8B"; to_camelot("A") == "11B" (majors → outer "B" wheel)
    - to_camelot("") is None; to_camelot(None) is None; to_camelot("not-a-key") is None; to_camelot("13A") is None — NEVER raises
    - All 24 musical-notation → Camelot pairs from the RESEARCH table map correctly (enharmonic aliases too: "G#m"=="Abm"=="1A")
  </behavior>
  <action>Create src/vibemix/state/harmonics.py with the SPDX header and a module docstring noting Phase 60 will consume is_clash/compatible on top of this; Phase 59 ships to_camelot ONLY. Copy the `_MUSICAL_TO_CAMELOT` table and the `_CAMELOT_RE` regex from RESEARCH §Code Examples lines 421-431 verbatim (do not re-derive the wheel). Implement `to_camelot(raw: str | None) -> str | None` exactly per RESEARCH lines 433-444: return None on falsy input; if `_CAMELOT_RE.match(s.upper())` return `s.upper()` (already-Camelot passthrough); if `s` in the musical table return the mapping; handle open-key form ("1m"/"1d") via an additional table; else return None. The function MUST NEVER raise (degrades to None → key=unknown), mirroring the track_resolver.py honest-unknown discipline. Do NOT add is_clash or compatible — those are Phase 60.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_harmonics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/state/test_harmonics.py exists with a table-driven test covering all 24 musical mappings + Camelot passthrough + open-key + empty/None/garbage → None
    - `PYTHONPATH=src python3 -c "from vibemix.state.harmonics import to_camelot; assert to_camelot('Am')=='8A' and to_camelot('F#m')=='11A' and to_camelot('')is None"` exits 0
    - `grep -c "def is_clash\|def compatible" src/vibemix/state/harmonics.py` returns 0 (Phase 60 owns those)
  </acceptance_criteria>
  <done>to_camelot returns correct Camelot for musical/Camelot/open-key input, None for empty/unrecognized, never raises; the 24-entry table matches RESEARCH verbatim; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeckState/DeckTrack model + additive MusicState.deck_state field</name>
  <files>src/vibemix/state/deck_state.py, src/vibemix/state/music_state.py, tests/state/test_deck_state.py, tests/state/test_coach_prompt_grounding.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Pattern 1 lines 200-225 — verbatim dataclasses; §Pitfall 5 lines 408-412 golden-equivalence)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§state/deck_state.py — TrackEntry shape analog; §state/music_state.py — additive-field comment density)
    - src/vibemix/state/music_state.py (lines 14-22 imports + class; line 53 detected_genre precedent comment; line 93 deck_a)
    - src/vibemix/library/rekordbox.py (lines 70-94 TrackEntry — typed-empties-on-absence shape: bpm=0.0 not None)
  </read_first>
  <behavior>
    - DeckTrack() defaults: title=None, track_id=None, bpm=0.0, key=None, camelot=None, open_key=None, energy=None, loaded_at=0.0, confidence=0.0, source="unknown"
    - DeckState() defaults: decks={} (empty dict), updated_at=0.0
    - MusicState().deck_state is a DeckState with decks=={} — an empty MusicState produces zero deck output anywhere
    - Golden-equivalence: evidence_line(MusicState()) is byte-identical to the pre-Phase-59 baseline (no deck text emitted when decks is empty)
  </behavior>
  <action>Create src/vibemix/state/deck_state.py with the SPDX header, `from __future__ import annotations`, `from dataclasses import dataclass, field`, and the `DeckTrack` + `DeckState` dataclasses copied verbatim from RESEARCH §Pattern 1 lines 202-219 (field set: title, track_id, bpm=0.0, key, camelot, open_key, energy, loaded_at, confidence=0.0, source="unknown"; DeckState.decks default_factory=dict, updated_at=0.0). Then modify src/vibemix/state/music_state.py: import DeckState, add the additive field `deck_state: DeckState = field(default_factory=DeckState)` (RESEARCH line 223) placed alongside other consumer-readable fields, with an inline comment matching the Phase-52 detected_genre comment density (RESEARCH/PATTERNS) documenting: single-writer ownership (_tick_once only, DECK-04), the additive/golden-equivalence rationale, and "ADDITIVE: default-empty DeckState preserves golden-equivalence". Do NOT touch evidence_line yet (that is Plan 59-04). Add the golden-equivalence regression assertion to tests/state/test_coach_prompt_grounding.py (empty deck_state keeps evidence_line byte-identical).</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_deck_state.py tests/state/test_coach_prompt_grounding.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/state/test_deck_state.py exists asserting DeckTrack/DeckState defaults (source=="unknown", decks=={}, every harmonic field None/0.0)
    - `PYTHONPATH=src python3 -c "from vibemix.state.music_state import MusicState; m=MusicState(); assert m.deck_state.decks=={} and m.deck_state.source if False else m.deck_state.updated_at==0.0"` exits 0
    - test_coach_prompt_grounding.py extended with an empty-deck_state byte-identity assertion and passes
    - Full suite stays green: `PYTHONPATH=src python3 -m pytest -q` (no golden flips)
  </acceptance_criteria>
  <done>DeckTrack/DeckState exist with honest-default fields; MusicState carries the additive deck_state field with single-writer documentation; empty deck_state preserves evidence_line golden-equivalence; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key tag → Camelot normalization | Untrusted/odd library key strings cross into to_camelot |
| DeckTrack defaults → consumers | Default state must read as "unknown", never a fabricated key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01-01 | Spoofing (of grounded fact) | to_camelot on malformed tag | mitigate | Returns None on unrecognized input, never raises and never guesses a Camelot code (table-tested over empty/garbage/out-of-range) |
| T-59-01-02 | Tampering | MusicState golden-equivalence | mitigate | deck_state is additive default-empty; regression test asserts byte-identical evidence_line until populated |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/test_harmonics.py tests/state/test_deck_state.py tests/state/test_coach_prompt_grounding.py -x -q` green
- Full suite `PYTHONPATH=src python3 -m pytest -q` green (no golden flips from the additive field)
- `grep -n "def to_camelot" src/vibemix/state/harmonics.py` present; `grep -n "deck_state: DeckState" src/vibemix/state/music_state.py` present
</verification>

<success_criteria>
- to_camelot correct over all 24 mappings + Camelot/open-key passthrough; None (never raise) on empty/unrecognized
- DeckTrack/DeckState shipped with honest defaults; MusicState.deck_state additive and golden-equivalent
- No is_clash/compatible (Phase 60 scope) leaked into harmonics.py
</success_criteria>

<output>
Create `.planning/phases/59-full-deck-awareness-grounding/59-01-SUMMARY.md` when done.
</output>
