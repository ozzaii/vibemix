---
phase: 59-full-deck-awareness-grounding
plan: 05
type: execute
wave: 3
depends_on: ["59-04"]
files_modified:
  - src/vibemix/state/deck_vision.py
  - src/vibemix/state/deck_poller.py
  - eval/deck_vision/README.md
  - eval/deck_vision/run_eval.py
  - tests/state/test_deck_vision.py
autonomous: false
requirements: [DECK-02]
tags: [gemini-vision, deck-read, eval-gated, anti-hallucination, structured-output]
user_setup:
  - service: gemini-vision-deck-read
    why: "The universal-fallback vision leg re-enables a path v4 deliberately killed for hallucination — it must pass a real-screenshot accuracy eval before it can feed deck-state. Kaan supplies the real-rig screenshot corpus."
    env_vars:
      - name: GEMINI_API_KEY
        source: ".env at repo root (already present — no new key)"

must_haves:
  truths:
    - "The vision deck-read is a SEPARATE structured-output Gemini call (its own prompt + JSON schema) — NOT the screenshot re-attached to the reaction turn; the reaction-path screen_jpeg = None killswitch stays untouched"
    - "Vision returns structured JSON {decks:[{side,title,key,bpm}]} with explicit null on any field not clearly visible; nulls map to confidence=0 / unknown (closes the free-text hallucination class)"
    - "The vision leg stays GATED behind the eval: deck_poller does NOT consume vision-sourced keys until the eval passes the defined accuracy floor on the real-screenshot corpus"
    - "Vision-sourced keys carry LOWER confidence than XML-matched keys (a misread badge is a silent error, same class as a wrong library tag)"
    - "Vision cadence is bounded: track-change / screen-change only, debounced (~1 read / 5-10s), skipped entirely in the audio-only tier — never per-tick"
  artifacts:
    - path: "src/vibemix/state/deck_vision.py"
      provides: "Separate structured-output Gemini deck-read (own prompt + JSON schema, null-defensive parse, cadence debounce)"
      contains: "response_schema"
      min_lines: 50
    - path: "eval/deck_vision/run_eval.py"
      provides: "Real-screenshot accuracy eval harness — measures Gemini deck-title + key/BPM extraction accuracy; sets the vision-confidence floor"
      contains: "accuracy"
    - path: "eval/deck_vision/README.md"
      provides: "KAAN-ACTION corpus instructions + the accuracy-floor gate definition"
      contains: "accuracy floor"
  key_links:
    - from: "src/vibemix/state/deck_poller.py"
      to: "src/vibemix/state/deck_vision.py"
      via: "vision ladder slot consumes deck_vision read ONLY when the eval gate is satisfied"
      pattern: "deck_vision\\|screen_vision"
    - from: "src/vibemix/state/deck_vision.py"
      to: "src/vibemix/platform/screen.py"
      via: "reuses CapturedFrame.jpeg + types.Part.from_bytes(image/jpeg) — existing capture infra"
      pattern: "CapturedFrame\\|from_bytes"
---

<objective>
Build the Gemini-vision deck-read as a SEPARATE, structured-output, eval-gated leg of the source ladder — the universal cross-app fallback (Serato/djay/Traktor/Engine, zero per-app parsers). This is the single biggest risk in the phase: `screen_jpeg = None` (dj_cohost.py:553) is a deliberate v4 anti-hallucination killswitch ("Screen + MIDI metadata caused hallucination"). Re-enabling vision is NOT a one-line flip — it is a constrained, structured, low-frequency call behind a real-screenshot accuracy eval that MUST pass before vision feeds deck-state.

Purpose: completes the DECK-02 source ladder (XML primary → vision fallback → numpy last-resort) for non-Rekordbox / no-tag cases, while keeping the v4 anti-hallucination invariant on the reaction path intact and refusing to let an unreliable badge read produce a false-confident key.

Output: `state/deck_vision.py` (NEW, structured call), the eval harness + KAAN-ACTION corpus instructions, and the gated wiring into the poller's vision slot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md
@.planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md
@.planning/phases/59-full-deck-awareness-grounding/59-04-SUMMARY.md

<interfaces>
<!-- Verified against branch live-tuning-or-brain. -->

CAPTURE INFRA TO REUSE (do NOT build a new capture/encode path):
platform/screen.py::CapturedFrame (line 30) — `.jpeg` bytes; ScreenBackend Protocol (line 39)
agent/dj_cohost.py — `self._screen_buf = screen_buf` (line 364); the inline-Part pattern `types.Part.from_bytes(data=screen_jpeg, mime_type="image/jpeg")` (line 681); `screen_jpeg = None` killswitch (line 553) — DO NOT FLIP; `SCREEN_SKIP_EVENTS` (line 89)

STRUCTURED-OUTPUT GEMINI:
agent/dj_cohost.py — `types.GenerateContentConfig(...)` (lines 428, 710); `generate_content_stream` (line 803). For a non-streaming structured deck-read use `generate_content` with `GenerateContentConfig(response_mime_type="application/json", response_schema=...)`. Gemini-only constraint holds (no new provider).

LADDER SLOT (from Plan 59-04):
state/deck_poller.py — the source="screen_vision" slot scaffolded in 59-04; this plan implements + GATES it.
state/harmonics.py::to_camelot — normalize the vision-read key (vision may return Camelot or musical form).

EVAL INFRA TO EXTEND:
eval/ already has corpus/, rubrics/, run_eval patterns + THRESHOLD-LOCK.md. Mirror that structure under eval/deck_vision/.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structured-output Gemini deck-vision read (separate call, null-defensive, cadence-bounded)</name>
  <files>src/vibemix/state/deck_vision.py, tests/state/test_deck_vision.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Spike 2 lines 292-310 — the load-bearing killswitch constraint + de-risked shape: separate structured call, JSON schema, null→unknown, cadence, lower confidence; §Pitfall 2 lines 390-394)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§state/deck_poller.py vision-leg paragraph lines 104-105; §agent/dj_cohost.py vision killswitch lines 278-284)
    - src/vibemix/platform/screen.py (lines 30-39 CapturedFrame + ScreenBackend)
    - src/vibemix/agent/dj_cohost.py (lines 428, 681, 710 — GenerateContentConfig + from_bytes inline-Part pattern; line 553 killswitch — DO NOT FLIP)
  </read_first>
  <behavior>
    - deck_vision.read(jpeg_bytes) issues a SEPARATE structured Gemini call (response_mime_type=application/json + response_schema), NOT the reaction prompt
    - The schema is {decks:[{side,title,key,bpm}]} with an explicit instruction: "if a field is not clearly visible, return null"
    - A null/absent field parses to None → confidence=0 / unknown (no free-text guess)
    - The returned key is normalized via harmonics.to_camelot (vision may emit Camelot or musical notation)
    - The call swallows its own exceptions (graceful degradation — mirrors TrackInfo.poll_once) and returns an empty/unknown result, never raising into the poller
    - Cadence: a debounce gate refuses reads more often than the configured interval (~5-10s) and skips entirely in the audio-only tier
    - The reaction-path screen_jpeg = None killswitch in dj_cohost.py is UNCHANGED (grep-asserted)
  </behavior>
  <action>Create src/vibemix/state/deck_vision.py with the SPDX header. Implement a `read(jpeg_bytes) -> dict` (or a small class) that issues a SEPARATE structured Gemini call per RESEARCH §Spike 2: build the deck-read prompt + a JSON `response_schema` for `{"decks":[{"side","title","key","bpm"}]}` via `types.GenerateContentConfig(response_mime_type="application/json", response_schema=...)`, attach the screenshot as `types.Part.from_bytes(data=jpeg_bytes, mime_type="image/jpeg")` (reuse the existing pattern — do NOT build a new capture/encode path), and call `generate_content` (non-streaming, Gemini-only). Parse defensively: any null/missing field → None → confidence=0/unknown (this constrained schema is what closes the free-text hallucination class that killed the original screen Part). Normalize the read key via harmonics.to_camelot. Swallow ALL exceptions returning an unknown result (TrackInfo.poll_once discipline). Add a cadence debounce (configurable interval ~5-10s, RESEARCH A2) and an audio-only-tier skip. Vision results MUST carry a LOWER confidence than XML matches (RESEARCH A3) — set a module constant VISION_CONF below the XML floor. DO NOT touch dj_cohost.py screen_jpeg = None (RESEARCH Pitfall 2 — the reaction path stays de-screened; this is a separate call). Create tests/state/test_deck_vision.py with a MOCKED Gemini client: structured-JSON happy path → DeckTrack fields; null fields → unknown; malformed/raising response → graceful unknown (no raise); cadence debounce blocks a too-soon second read; to_camelot normalization applied.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_deck_vision.py -x -q; grep -cE "^[[:space:]]+screen_jpeg = None$" src/vibemix/agent/dj_cohost.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/state/test_deck_vision.py passes with a mocked client (happy path, null→unknown, raising→graceful, debounce, to_camelot)
    - `grep -cE "^[[:space:]]+screen_jpeg = None$" src/vibemix/agent/dj_cohost.py` returns 1 (reaction-path killswitch untouched)
    - `grep -c "response_schema\|response_mime_type" src/vibemix/state/deck_vision.py` >= 1 (structured output, not free text)
    - `grep -c "generate_content_stream" src/vibemix/state/deck_vision.py` returns 0 (separate non-reaction call, not the streaming reaction path)
    - No new AI provider import (Gemini-only): `grep -ci "openai\|anthropic\|claude" src/vibemix/state/deck_vision.py` returns 0
  </acceptance_criteria>
  <done>A separate structured-output deck-vision read exists with null-defensive parsing, cadence debounce, to_camelot normalization, lower-than-XML confidence, and graceful degradation; the reaction-path killswitch is untouched; Gemini-only; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Real-screenshot accuracy eval harness + KAAN-ACTION corpus instructions + the gate definition</name>
  <files>eval/deck_vision/run_eval.py, eval/deck_vision/README.md</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Spike 2 lines 307-310 — the eval task MUST exist; §Validation Architecture line 584 vision-eval Wave-0 gap; §Open Q1 lines 512-515)
    - .planning/phases/59-full-deck-awareness-grounding/59-VALIDATION.md (the Gemini-vision deck-read accuracy row)
    - eval/README.md + eval/THRESHOLD-LOCK.md (the existing eval-harness + threshold-lock convention to mirror)
  </read_first>
  <action>Create eval/deck_vision/run_eval.py: a harness that loads a corpus of real screenshots (djay Pro, Serato, Traktor at minimum; light + dark themes) each with a hand-labeled ground-truth deck-state (side/title/key/bpm), runs deck_vision.read against each, and reports per-app + overall accuracy for deck-title and key/BPM extraction. Define the accuracy-floor gate as a module constant + assertion (per-app: if accuracy < floor, that app degrades to XML-or-unknown — never a guessed badge). Create eval/deck_vision/README.md documenting: (1) the KAAN-ACTION corpus collection instructions (Kaan supplies screenshots from his real rig — djay Pro primary; the second/silent deck badge is the key open question per RESEARCH Open Q1), (2) the ground-truth labeling format, (3) the accuracy-floor gate and what passing/failing means for each app, (4) that vision stays GATED out of deck_poller until the corpus eval passes the floor. Mirror the eval/THRESHOLD-LOCK.md convention. This harness runs against the corpus (network/integration-marked, opt-in) — no live Gemini call in the default fast suite.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -c "import ast; ast.parse(open('eval/deck_vision/run_eval.py').read())"; test -f eval/deck_vision/README.md && echo "README present"</automated>
  </verify>
  <acceptance_criteria>
    - eval/deck_vision/run_eval.py parses + defines an accuracy-floor gate constant + per-app + overall accuracy reporting
    - eval/deck_vision/README.md documents the KAAN-ACTION corpus instructions, ground-truth format, the accuracy-floor gate, and the "vision stays gated until eval passes" rule
    - The harness is opt-in (network/integration marker) — `PYTHONPATH=src python3 -m pytest -q` (default fast suite) does NOT make a live Gemini call
    - `grep -ci "accuracy floor" eval/deck_vision/README.md` >= 1
  </acceptance_criteria>
  <done>A real-screenshot accuracy eval harness + KAAN-ACTION corpus instructions + an explicit accuracy-floor gate exist; the default suite makes no live call; the gate definition is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Vision deck-read accuracy eval gate (KAAN-ACTION — real-screenshot corpus)</name>
  <what-built>The Gemini-vision deck-read (separate structured call, null-defensive, cadence-bounded, lower-than-XML confidence) and the eval harness + accuracy-floor gate. The vision leg is built but stays GATED OUT of deck_poller's live consumption until this checkpoint confirms the real-screenshot eval passes the floor — re-enabling vision is the single biggest hallucination risk in the phase (it un-does a deliberate v4 killswitch).</what-built>
  <how-to-verify>
    1. Kaan supplies a small corpus of REAL screenshots from his rig per eval/deck_vision/README.md — djay Pro (primary), and Serato/Traktor if available; light + dark themes; both decks visible where possible (the silent/second-deck badge readability is the key open question, RESEARCH Open Q1).
    2. Run the eval harness against the corpus: `PYTHONPATH=src python3 eval/deck_vision/run_eval.py <corpus_dir>` (or the documented invocation).
    3. Review the per-app + overall accuracy report against the documented accuracy floor.
    4. DECISION: if accuracy clears the floor for an app → approve enabling vision-sourced keys for that app at the documented (lower-than-XML) confidence. If an app is below the floor → that app degrades to XML-or-unknown (vision stays gated for it) — this is the conservative-by-design path, not a failure.
    5. Per `gsd-autonomous fully`: this is a Kaan-action live/ear gate. It surfaces as KAAN-ACTION; engineering does NOT pause downstream non-vision work, but vision MUST NOT feed deck-state until this passes (the killswitch-respecting gate).
  </how-to-verify>
  <resume-signal>Type "approved: enable vision for [apps] at conf X" with the eval result, OR "gated: vision stays XML-or-unknown" to keep vision dormant. Either way, record the outcome in 59-05-SUMMARY.md.</resume-signal>
  <files>eval/deck_vision/README.md, .planning/phases/59-full-deck-awareness-grounding/59-05-SUMMARY.md</files>
  <action>BLOCKING human-verify checkpoint (not auto-executable): Kaan supplies the real-screenshot corpus per eval/deck_vision/README.md, the executor runs eval/deck_vision/run_eval.py against it, and the per-app accuracy report is reviewed against the documented accuracy floor. Vision-sourced keys are enabled in deck_poller ONLY for apps that clear the floor (at the lower-than-XML confidence); below-floor apps stay XML-or-unknown (vision dormant). Per gsd-autonomous fully this surfaces as KAAN-ACTION and does not pause non-vision downstream work, but vision MUST NOT feed deck-state until the floor is cleared. Record the outcome (enabled apps + confidence, or gated) in 59-05-SUMMARY.md.</action>
  <verify>Eval report generated against the real-screenshot corpus; the enable/gate decision recorded in 59-05-SUMMARY.md; deck_poller consumes vision keys only for floor-clearing apps.</verify>
  <done>The vision accuracy eval has run on the real corpus; the per-app enable/gate decision is recorded; vision feeds deck-state only where it cleared the floor; the reaction-path killswitch remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot → Gemini → deck-state | The exact hallucination class v4 removed the screen Part for; must be constrained + eval-gated |
| vision-read key → key: citation | A misread badge is a silent error — must carry lower confidence + pass the eval floor |
| reaction path → screen Part | Must remain de-screened (killswitch untouched) — vision is a separate call only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05-01 | Spoofing (of grounded fact) | re-introduced screen Part → hallucination | mitigate | Separate structured-output call (not the reaction turn); JSON schema with explicit null; reaction-path screen_jpeg=None untouched (grep-asserted) |
| T-59-05-02 | Spoofing | misread key badge cited at high confidence | mitigate | Vision confidence below the XML floor; gated behind the real-screenshot accuracy eval; below-floor apps degrade to XML-or-unknown |
| T-59-05-03 | Denial of Service (budget) | per-tick vision-call cost runaway | mitigate | Cadence debounce (~5-10s, track/screen-change only); audio-only tier skips vision entirely |
| T-59-05-04 | Tampering | accidental killswitch flip | mitigate | dj_cohost.py screen_jpeg=None grep-asserted unchanged; vision is a separate module |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/test_deck_vision.py -x -q` green (mocked client)
- `grep -cE "^[[:space:]]+screen_jpeg = None$" src/vibemix/agent/dj_cohost.py` returns 1 (killswitch untouched)
- Default fast suite `PYTHONPATH=src python3 -m pytest -q` green and makes NO live Gemini call
- Vision stays gated out of deck_poller's live consumption until the checkpoint approves (recorded in SUMMARY)
</verification>

<success_criteria>
- Vision deck-read is a separate structured-output call, null-defensive, cadence-bounded, lower-than-XML confidence, graceful
- The reaction-path anti-hallucination killswitch is untouched
- A real-screenshot accuracy eval + floor gate exist; vision feeds deck-state ONLY after the eval passes (KAAN-ACTION checkpoint)
- Gemini-only; no per-tick vision cost
</success_criteria>

<output>
Create `.planning/phases/59-full-deck-awareness-grounding/59-05-SUMMARY.md` when done (record the vision-eval checkpoint outcome).
</output>
