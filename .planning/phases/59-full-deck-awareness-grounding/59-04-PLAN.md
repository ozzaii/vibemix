---
phase: 59-full-deck-awareness-grounding
plan: 04
type: execute
wave: 2
depends_on: ["59-01", "59-02", "59-03"]
files_modified:
  - src/vibemix/state/deck_poller.py
  - src/vibemix/state/refresh.py
  - src/vibemix/state/coach.py
  - src/vibemix/__main__.py
  - tests/state/test_deck_poller.py
  - tests/state/test_refresh_deck.py
  - tests/state/test_coach_prompt_grounding.py
autonomous: true
requirements: [DECK-01, DECK-02, DECK-04, DECK-05]
tags: [deck-poller, single-writer, refresh, evidence-registry, coach, cross-deck-suppression]

must_haves:
  truths:
    - "The deck poller is a read-only producer (own holder + threading.Lock + .snapshot() returning copies) — the THIRD external source after ControllerState/TrackInfo; it NEVER writes MusicState"
    - "_tick_once is the ONLY place state.deck_state.decks is assigned — single-writer rule holds (grep proves no assignment outside refresh.py)"
    - "Source ladder: XML library match (highest confidence) wins over vision over numpy; below all floors → confidence=0, source='unknown', harmonic fields None (honest unknown)"
    - "Cross-deck suppression: a deck the poller cannot independently resolve gets confidence=0 → no key: registry write → Phase 60's clash cannot cite it"
    - "key:/track: registry writes are change-only, gated on confidence >= DECK_CITE_MIN_CONF, inside the _tick_once with state._lock: block"
    - "evidence_line shows resolved decks the grounded way (decks[A='..' 8A 128bpm | ...]) and decks=unknown when none resolve — empty deck_state stays golden-equivalent"
    - "The deck poller imports/opens NO DJ-software DB in write mode — reuses the cache-warm RekordboxLibrary read-only (DECK-05 repo test from 59-03 stays green)"
  artifacts:
    - path: "src/vibemix/state/deck_poller.py"
      provides: "Read-only deck-state producer: source ladder (XML primary), audible-deck attribution, cross-deck suppression, own-holder snapshot()"
      contains: "def snapshot"
      min_lines: 60
    - path: "src/vibemix/state/refresh.py"
      provides: "_tick_once copies deck_snap under lock + change-only key:/track: registry writes"
      contains: "deck_state.decks"
    - path: "src/vibemix/state/coach.py"
      provides: "evidence_line deck block + KEY_CLASH/TRANSITION_OPPORTUNITY task_for_event stub arms"
      contains: "decks="
  key_links:
    - from: "src/vibemix/state/refresh.py"
      to: "src/vibemix/state/deck_poller.py"
      via: "deck_source.snapshot() copied into state.deck_state.decks inside with state._lock:"
      pattern: "deck_source.snapshot"
    - from: "src/vibemix/state/refresh.py"
      to: "src/vibemix/state/evidence_registry.py"
      via: "evidence_registry.write('key', f'{side}:{camelot}', t_session) change-only"
      pattern: "evidence_registry.write\\(\"key\""
    - from: "src/vibemix/state/deck_poller.py"
      to: "src/vibemix/state/track_resolver.py"
      via: "derive_audible_deck for which-deck attribution (reuse, do not hand-roll)"
      pattern: "derive_audible_deck"
    - from: "src/vibemix/__main__.py"
      to: "src/vibemix/state/deck_poller.py"
      via: "poller instantiated + passed into state_refresh_loop alongside controller_state/track_info"
      pattern: "DeckPoller\\|deck_source\\|deck_poller"
---

<objective>
Build the read-only deck poller (the third external snapshot producer after `ControllerState`/`TrackInfo`), wire it into `_tick_once` under the single-writer rule, write the change-only `key:`/`track:` registry observations, surface decks to the coach via `evidence_line`, and spawn the poller in `__main__`. This is the integration spine: it composes the already-shipped rails (`RekordboxLibrary` XML cache, `derive_audible_deck`, `harmonics.to_camelot`, the `key:` source) into a grounded, citable deck-state.

Purpose: DECK-01 (coach sees decks the grounded way), DECK-02 (source ladder + honest unknown), DECK-04 (single-writer integration), DECK-05 (read-only + cross-deck suppression). The vision and numpy legs are SCAFFOLDED as ladder slots here (source="screen_vision"/"numpy_key") but the vision implementation + eval is Plan 59-05 and the numpy estimator is deferred (Open Q2) — XML-primary covers the ladder this phase.

Output: `state/deck_poller.py` (NEW), `refresh.py`/`coach.py`/`__main__.py` (MODIFIED), Wave-0 poller + single-writer tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md
@.planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md
@.planning/phases/59-full-deck-awareness-grounding/59-01-SUMMARY.md
@.planning/phases/59-full-deck-awareness-grounding/59-02-SUMMARY.md
@.planning/phases/59-full-deck-awareness-grounding/59-03-SUMMARY.md

<interfaces>
<!-- Verified against branch live-tuning-or-brain. -->

PRODUCER ANALOGS (mirror these exactly):
midi/state.py::ControllerState — own `self._lock = threading.Lock()` (line 143); `deck_snapshot()` returns deep copies under lock (PATTERNS lines 84-91)
platform/_track_macos.py::TrackInfo — `poll_once()` swallows ALL subprocess errors silently returning last-known state (lines 48-66); `snapshot()` returns a fresh dict (line 76); `run_poll_loop(stop_event)` cadence wrapper (line 128)

REUSE (do NOT hand-roll):
library/rekordbox.py::RekordboxLibrary — `try_load_cache()` (cache-warm), `self.tracks: dict[str, TrackEntry]` (line 126), `lookup_by_id(track_id)` (line 233). NOTE: there is NO lookup_by_title — add a title→TrackEntry resolution helper that iterates self.tracks (read-only). TrackEntry has: track_id, title, artist, bpm (AverageBpm), key (raw Tonality "Am"/"F#m"), duration_s.
state/track_resolver.py::derive_audible_deck(deck_a, deck_b, xfader, connected) (line 31) — which-deck attribution; trusts fader+xfader over FLX4 play-state desync. Returns (deck, confidence).
state/harmonics.py::to_camelot (from Plan 59-01)

SINGLE-WRITER SITE — refresh.py::_tick_once (line 189) + state_refresh_loop (line 491):
- Both take `controller_state: ControllerState` (line 192/494) and `track_info: TrackInfo` (line 193/495) as positional args + `evidence_registry: EvidenceRegistry | None = None` (line 205). Mirror these for `deck_source`.
- The `with state._lock:` batch (line 303) contains: controller snapshot copy (lines 428-432 `cs = controller_state.deck_snapshot(); state.deck_a = cs["A"]; ...`), the `mix:audible_deck` change-only write (lines 437-446 — `prev_deck != aud_deck` guard, try/except, `t_session = max(0.0, now - state.set_start_at)`), and the track snapshot (line 449 `tsnap = track_info.snapshot()`).
- RESEARCH §Code Examples lines 447-456 ships the exact deck-copy block; Spike 3 lines 351-364 ships the change-only key:/track: write block.

coach/coach.py:
- `evidence_line` (line 54); the track=unknown gate (lines 76-81); `e.append(f"deck={state.audible_deck}")` (line 83)
- `_evidence_line_compact` (line 143) + `ACK_ELIGIBLE_EVENTS` (line 44) — deck-state is substantive: do NOT add it to the compact/ack path (RESEARCH line 243)
- `task_for_event` (line 184) — add KEY_CLASH/TRANSITION_OPPORTUNITY stub arms ONLY (no firing strings — Phase 60)
- RESEARCH §Code Examples lines 458-468 ships the exact evidence_line deck block.

__main__.py:
- RekordboxLibrary wiring at lines 883-888 (`lib = RekordboxLibrary(); if lib.try_load_cache(): registered = evidence_registry.register_library(lib)`)
- state_refresh_loop is invoked with controller_state/track_info/evidence_registry — add the poller alongside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-only deck poller — source ladder, audible-deck attribution, cross-deck suppression</name>
  <files>src/vibemix/state/deck_poller.py, tests/state/test_deck_poller.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Pattern 2 lines 227-231 source ladder; §Pattern 3 lines 233-236 cross-deck suppression; §Don't Hand-Roll lines 248-256; §Spike 1 lines 267-282 XML field set + cache-warm; §Spike 2 lines 292-310 vision GATED; Open Q2 lines 517-520 numpy deferred)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§state/deck_poller.py — ALL of it: ControllerState/TrackInfo analogs, snapshot-producer + graceful-degradation patterns, XML/audible-deck reuse, cross-deck suppression, GATED vision leg)
    - src/vibemix/midi/state.py (lines 117-203 ControllerState — own-holder + deck_snapshot)
    - src/vibemix/platform/_track_macos.py (lines 34-128 TrackInfo — poll_once swallow-all + snapshot + run_poll_loop)
    - src/vibemix/library/rekordbox.py (lines 70-94 TrackEntry; lines 106-235 RekordboxLibrary — tracks dict + lookup_by_id, NO title lookup)
    - src/vibemix/state/track_resolver.py (lines 31-89 derive_audible_deck)
  </read_first>
  <behavior>
    - DeckPoller exposes snapshot() -> dict[str, DeckTrack], built under its own threading.Lock, returning copies the caller cannot mutate
    - XML match path: given a resolved title that hits RekordboxLibrary, the DeckTrack gets title/track_id/bpm/key(raw Tonality) with source="rekordbox_xml" and high confidence
    - Source ladder ordering: a deck with both an XML match and a (future) vision read keeps the XML result (higher confidence)
    - Honest unknown: a deck with no resolvable source yields confidence=0.0, source="unknown", key=None/camelot=None
    - Cross-deck suppression: when only deck A is independently resolved, deck B's DeckTrack has confidence=0 (never resolved by "the other now-playing title")
    - Graceful degradation: every external read (XML re-stat, the future vision/numpy slots) swallows its own exception and returns last-known state — no exception escapes snapshot()
    - The poller opens NO DJ-software DB in write mode (reuses the cache-warm RekordboxLibrary read-only)
  </behavior>
  <action>Create src/vibemix/state/deck_poller.py with the SPDX header. Build `DeckPoller` mirroring ControllerState/TrackInfo: own `threading.Lock`, an internal `dict[str, DeckTrack]` holder, `snapshot() -> dict[str, DeckTrack]` returning copies under the lock (PATTERNS lines 84-93), and a `poll_once()` / `run_poll_loop(stop_event)` cadence wrapper that swallows all exceptions per the TrackInfo discipline (PATTERNS line 95). Implement the source ladder (PATTERNS §source-ladder, RESEARCH §Pattern 2): try XML library match (highest confidence) → leave a vision slot (source="screen_vision", populated by Plan 59-05, not implemented here) → leave a numpy slot (source="numpy_key", deferred per Open Q2); first source clearing its confidence floor wins; below all floors → confidence=0/source="unknown"/harmonic fields None. REUSE `RekordboxLibrary` cache-warm via try_load_cache (do NOT hand-roll an XML parser); add a read-only title→TrackEntry resolution helper that iterates self.tracks (RekordboxLibrary has only lookup_by_id). REUSE `derive_audible_deck(deck_a, deck_b, xfader, connected)` for which-deck attribution (do NOT hand-roll — it already trusts fader+xfader over the FLX4 desync). Implement cross-deck suppression (RESEARCH §Pattern 3 + Pitfall 4): set confidence=0 on any deck NOT independently confirmed (the silent deck needs its own source signal, never "the other now-playing title"). Do NOT call Gemini-vision here (the screen_jpeg killswitch stays; vision is Plan 59-05's separate structured eval-gated call). Set the camelot field as None in the poller — _tick_once normalizes via to_camelot (Task 2). Set DECK_CITE_MIN_CONF and the per-source confidence floors as module constants (~0.6 for XML, lower for vision per RESEARCH A3/A4 — document the rationale inline). Create tests/state/test_deck_poller.py covering: source-ladder ordering, confidence gating + honest unknown, cross-deck suppression (deck A resolved + deck B unresolved → B confidence=0), graceful degradation (a raising source does not escape snapshot), and read-only (no DJ-DB write).</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_deck_poller.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/state/test_deck_poller.py exists covering ladder ordering, confidence gating/unknown, cross-deck suppression (`-k suppress`), graceful degradation
    - `PYTHONPATH=src python3 -c "from vibemix.state.deck_poller import DeckPoller; p=DeckPoller(); s=p.snapshot(); assert isinstance(s, dict)"` exits 0 (adjust ctor args)
    - `grep -c "derive_audible_deck" src/vibemix/state/deck_poller.py` >= 1 (reused, not hand-rolled)
    - `grep -c "RekordboxXml\|load_xml\|Rekordbox6Database\|\.commit(" src/vibemix/state/deck_poller.py` returns 0 (no parser hand-roll, no live DB, no write)
    - tests/repo/test_repo_scrub.py::test_deck_readonly (from 59-03) stays green with the poller present
  </acceptance_criteria>
  <done>The deck poller is a read-only own-holder producer composing the XML cache + derive_audible_deck with cross-deck suppression and honest unknown; vision/numpy are scaffolded ladder slots; no DJ-DB write; tests + the 59-03 read-only repo test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the poller into _tick_once (single-writer) + change-only key:/track: registry writes</name>
  <files>src/vibemix/state/refresh.py, tests/state/test_refresh_deck.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Code Examples lines 447-456 deck-copy block; §Spike 3 lines 351-364 change-only registry writes; §Pitfall 3 lines 396-400 single-writer)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§state/refresh.py — ALL of it: snapshot-copy + change-only-write patterns, the INVARIANT note)
    - src/vibemix/state/refresh.py (lines 189-205 _tick_once signature; lines 303-460 the with state._lock: batch — controller copy 428-432, mix:audible_deck change-only write 437-446, track snapshot 449; lines 491-541 state_refresh_loop signature + _tick_once call)
  </read_first>
  <behavior>
    - state.deck_state.decks is assigned ONLY inside the _tick_once with state._lock: block (single-writer)
    - Inside the lock: deck_snap = deck_source.snapshot(); each dt.camelot = harmonics.to_camelot(dt.key); state.deck_state.decks = deck_snap; state.deck_state.updated_at = now
    - key: write fires only when a deck's (side, camelot) CHANGES and confidence >= DECK_CITE_MIN_CONF (change-only, bounded registry growth)
    - track: write fires alongside when dt.track_id present (mirrors the existing register_library track: resolution)
    - A deck below the confidence floor produces NO key: write → cross-deck claim uncitable
    - All registry writes wrapped in try/except (mirrors the mix: write); t_session = max(0.0, now - state.set_start_at)
    - deck_source is an optional positional/kwarg arg defaulting to None so existing direct _tick_once tests still pass (None → skip deck block)
  </behavior>
  <action>Edit src/vibemix/state/refresh.py: add `deck_source` to both the `_tick_once` (line 189) and `state_refresh_loop` (line 491) signatures, mirroring how controller_state/track_info are threaded (default None so existing tests pass when absent). Inside the `with state._lock:` batch (alongside the controller/track block ~line 428-460), add the deck-copy block from RESEARCH lines 447-456: guard on `deck_source is not None`, `deck_snap = deck_source.snapshot()`, loop `dt.camelot = harmonics.to_camelot(dt.key)` (pure µs-cost — safe inside the lock), `state.deck_state.decks = deck_snap`, `state.deck_state.updated_at = now`. Then add the change-only key:/track: registry writes from RESEARCH Spike 3 lines 351-364 / PATTERNS lines 166-177: gated on `dt.confidence >= DECK_CITE_MIN_CONF and dt.camelot`, only when the deck's (side, camelot) changed since last tick (mirror the mix:audible_deck `prev != new` change-only pattern at lines 437-446), wrapped in try/except, `evidence_registry.write("key", f"{side}:{dt.camelot}", t_session)` + the track: write when track_id present. Thread the call through state_refresh_loop's `_tick_once(...)` invocation (line 537). INVARIANT (Pitfall 3): `state.deck_state.decks = ...` must appear NOWHERE else in refresh.py or anywhere outside this lock block. Create tests/state/test_refresh_deck.py: single-writer copy (deck_source.snapshot copied into state.deck_state under the lock), change-only registry writes (no duplicate write when (side,camelot) unchanged), confidence-floor gating (low-confidence deck → no key: write), and a grep-style assertion that deck_state.decks is assigned only in refresh.py.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_refresh_deck.py tests/state/test_refresh.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/state/test_refresh_deck.py exists covering single-writer copy, change-only writes, confidence-floor gating
    - `grep -rn "deck_state.decks =" src/vibemix/ | grep -v "refresh.py"` returns nothing (single-writer invariant — assignment only in refresh.py)
    - `grep -c 'evidence_registry.write("key"' src/vibemix/state/refresh.py` >= 1
    - Existing tests/state/test_refresh.py stays green (deck_source defaults None — additive)
    - tests/repo/test_repo_scrub.py::test_deck_readonly stays green
  </acceptance_criteria>
  <done>_tick_once is the only writer of state.deck_state.decks; camelot normalized via to_camelot inside the lock; change-only confidence-gated key:/track: writes land; existing refresh tests unaffected; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Surface decks to the coach (evidence_line block + task_for_event stub arms) + spawn poller in __main__</name>
  <files>src/vibemix/state/coach.py, src/vibemix/__main__.py, tests/state/test_coach_prompt_grounding.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Code Examples lines 458-468 evidence_line deck block; §Pitfall 5 lines 408-412 golden-equivalence; line 243 NOT in compact/ack path)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§state/coach.py — track=unknown gate analog, deck block, task_for_event stub-arms-only)
    - src/vibemix/state/coach.py (lines 54-95 evidence_line incl. track=unknown gate + deck= line; line 143 _evidence_line_compact; line 184 task_for_event; line 44 ACK_ELIGIBLE_EVENTS)
    - src/vibemix/__main__.py (lines 883-888 RekordboxLibrary wiring; the state_refresh_loop invocation site)
  </read_first>
  <behavior>
    - evidence_line shows resolved decks: decks[A='<title>' 8A 128bpm | B='<title>' 5A 124bpm] when a deck has camelot and confidence >= 0.3
    - evidence_line shows decks=unknown when no deck resolves (honest unknown, mirrors track=unknown)
    - An empty deck_state (decks={}) emits decks=unknown (or nothing per the gate) and keeps evidence_line byte-identical to the 59-01 golden baseline
    - The deck block is NOT in _evidence_line_compact and KEY_CLASH/TRANSITION_OPPORTUNITY are NOT in ACK_ELIGIBLE_EVENTS (deck-state is substantive full-payload)
    - task_for_event has stub arms for KEY_CLASH + TRANSITION_OPPORTUNITY (no firing strings — Phase 60 fills them)
    - __main__ instantiates DeckPoller (reusing the cache-warm RekordboxLibrary) and passes it into state_refresh_loop alongside controller_state/track_info; the run_poll_loop task is created with the other producer tasks
  </behavior>
  <action>Edit src/vibemix/state/coach.py: add the deck block to evidence_line (RESEARCH lines 458-468 / PATTERNS lines 250-260) — build `resolved = {s: d for s, d in state.deck_state.decks.items() if d.camelot and d.confidence >= 0.3}`; if resolved, append `decks[<A=title camelot bpm> | ...]`, else append `decks=unknown` — placed additively near the existing track=unknown / deck= lines so empty deck_state stays golden-equivalent (Pitfall 5). Do NOT add the deck block to `_evidence_line_compact` and do NOT add the new event types to ACK_ELIGIBLE_EVENTS (RESEARCH line 243 — deck-state is substantive). In `task_for_event` add stub arms for "KEY_CLASH" and "TRANSITION_OPPORTUNITY" that return a minimal placeholder routed through the normal (non-diet) path — NO firing/clash narration (Phase 60 fills the task strings). Edit src/vibemix/__main__.py: instantiate a DeckPoller (reuse the cache-warm RekordboxLibrary already loaded at lines 883-888 — pass the lib in read-only), create its run_poll_loop task alongside the other producer tasks, and pass `deck_source=<poller>` into the state_refresh_loop invocation. Extend tests/state/test_coach_prompt_grounding.py: resolved-decks rendering, decks=unknown on no resolution, and the empty-deck_state golden-equivalence assertion (extends the 59-01 assertion).</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_coach_prompt_grounding.py tests/state/test_coach_prompt_diet.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - evidence_line emits `decks[...]` for resolved decks and `decks=unknown` otherwise; empty deck_state keeps byte-identical output (golden test green)
    - `grep -c "deck" src/vibemix/state/coach.py` shows the deck block in evidence_line but `grep -n "deck" src/vibemix/state/coach.py` confirms NO deck text in _evidence_line_compact
    - `PYTHONPATH=src python3 -c "from vibemix.state.coach import ACK_ELIGIBLE_EVENTS; assert 'KEY_CLASH' not in ACK_ELIGIBLE_EVENTS and 'TRANSITION_OPPORTUNITY' not in ACK_ELIGIBLE_EVENTS"` exits 0
    - `grep -c "DeckPoller\|deck_source" src/vibemix/__main__.py` >= 1 (poller wired into the live loop)
    - Full suite `PYTHONPATH=src python3 -m pytest -q` green
  </acceptance_criteria>
  <done>The coach sees decks the grounded way (decks[...]/decks=unknown), deck-state stays out of the diet/ack path, the two event types have stub arms (no firing), the poller is spawned in the live session and threaded into the single writer; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deck poller (executor cadence) → MusicState | A direct write would race the 10Hz single writer (torn snapshots) |
| unresolved second deck → harmonic claim | A guessed second-deck key would feed a false clash in Phase 60 |
| poller → RekordboxLibrary / DJ DB | Read-only; any write corrupts the user's collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04-01 | Tampering | poller writes MusicState directly | mitigate | Poller writes its own holder; _tick_once is the only copier under state._lock; grep asserts deck_state.decks assigned only in refresh.py |
| T-59-04-02 | Spoofing (false expertise) | cross-deck claim on unresolved 2nd deck | mitigate | Cross-deck suppression sets confidence=0 → no key: write → uncitable; tested with deck A resolved + B unresolved |
| T-59-04-03 | Tampering | write to user's DJ-software DB | mitigate | Poller reuses cache-warm RekordboxLibrary read-only; grep asserts no .commit()/Rekordbox6Database; 59-03 read-only repo test reasserted |
| T-59-04-04 | Denial of Service (budget) | unbounded registry growth | mitigate | key:/track: writes are change-only ((side,camelot) diff guard); confidence-gated |
| T-59-04-05 | Spoofing (of grounded fact) | citing a key the source did not provide | mitigate | key: write gated on confidence >= DECK_CITE_MIN_CONF; the 59-02 linter strips any unwritten key atom |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/ tests/coach/ tests/agent/ -x -q` green
- Single-writer invariant: `grep -rn "deck_state.decks =" src/vibemix/ | grep -v "refresh.py"` returns nothing
- Read-only: tests/repo/test_repo_scrub.py::test_deck_readonly green with the poller present
- Full suite `PYTHONPATH=src python3 -m pytest -q` green (no golden flips)
</verification>

<success_criteria>
- Deck poller is a read-only own-holder producer; XML-primary ladder + cross-deck suppression + honest unknown
- _tick_once is the only writer; change-only confidence-gated key:/track: registry writes
- Coach sees decks the grounded way; deck-state out of diet/ack; two event types have stub arms (no firing)
- Poller spawned in the live session; DECK-05 read-only guarantee holds
</success_criteria>

<output>
Create `.planning/phases/59-full-deck-awareness-grounding/59-04-SUMMARY.md` when done.
</output>
