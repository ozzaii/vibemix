---
phase: 59-full-deck-awareness-grounding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/state/evidence_registry.py
  - src/vibemix/coach/citation_linter.py
  - src/vibemix/prompts/matrix.py
  - src/vibemix/agent/dj_cohost.py
  - tests/state/test_evidence_registry.py
  - tests/coach/test_citation_linter.py
autonomous: true
requirements: [DECK-03]
tags: [citation, evidence-source, anti-slop, linter, harmonics]

must_haves:
  truths:
    - "'key' is in EVIDENCE_SOURCES; parse_citations('[key:A:8A]') == [('key', 'A:8A')]"
    - "CitationLinter STRIPS a response citing [key:A:12B] when the poller never wrote that key (fabricated harmonic atom uncitable-by-construction)"
    - "CitationLinter ACCEPTS a response citing [key:A:8A] when 'A:8A' was registered under the 'key' source"
    - "'key' is existence-only — NOT in _TIME_KEYED_SOURCES — no @t timestamp required (mirrors track:)"
    - "The 5 schema-mirror touchpoints (frozenset, regex, EBNF docstring, CITATION_GRAMMAR_BLOCK, citation-strip whitelist) all carry 'key' in lock-step"
  artifacts:
    - path: "src/vibemix/state/evidence_registry.py"
      provides: "'key' added to EVIDENCE_SOURCES + _SOURCE_ALT regex + EBNF docstring"
      contains: "key"
    - path: "src/vibemix/coach/citation_linter.py"
      provides: "'key' validated existence-only (NOT in _TIME_KEYED_SOURCES)"
      contains: "_TIME_KEYED_SOURCES"
    - path: "src/vibemix/prompts/matrix.py"
      provides: "CITATION_GRAMMAR_BLOCK teaches [key:<deck>:<camelot>]"
      contains: "key:"
  key_links:
    - from: "src/vibemix/coach/citation_linter.py"
      to: "src/vibemix/state/evidence_registry.py"
      via: "imports EVIDENCE_SOURCES, parse_citations; 'key' flows through existence-only branch"
      pattern: "from vibemix.state.evidence_registry import"
    - from: "src/vibemix/prompts/matrix.py"
      to: "src/vibemix/state/evidence_registry.py"
      via: "CITATION_GRAMMAR_BLOCK Forms kept in lock-step with EVIDENCE_SOURCES"
      pattern: "key:"
---

<objective>
Add the dedicated `key:` evidence source so harmonic/transition claims become citable per-deck per-moment — un-cited harmonic feedback is stripped by the existing `CitationLinter`. This MUST land before any harmonic prompt text (Phase 60 depends on it). Resolves the ARCHITECTURE↔PITFALLS divergence in favor of a dedicated source (CONTEXT-locked): `mix:` reuse would let a fabricated `12B` clash slip through; a dedicated `key:` with a `<deck>:<camelot>` body makes a hallucinated clash uncitable-by-construction.

Purpose: the anti-slop strip cannot catch a false clash today — there is no `key`/`harmonic` member in `EVIDENCE_SOURCES`. This is the single highest-cost retrofit if discovered at the release gate (Risk 2).

Output: `key:` in all 5 schema-mirror touchpoints, existence-only linter validation, lock-step grammar across registry/linter/prompt/citation-strip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md
@.planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md

<interfaces>
<!-- Verified against branch live-tuning-or-brain. -->

state/evidence_registry.py:
- `EVIDENCE_SOURCES: frozenset[str] = frozenset(...)` (line 95) — current: {ev, aud, midi, track, screen, mix, tend}
- `_SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend"` (line 104)
- `_INNER_ATOM = rf"(?:{_SOURCE_ALT}):[^\s,\]]+"` (line 105) — UNCHANGED (key:A:8A body has no whitespace/comma/bracket)
- `parse_citations(...)` splits on FIRST ':' via .partition(":") → parse_citations("[key:A:8A]") yields [("key", "A:8A")] (no parser change)
- `register_library(line 218)` — NO change (track:<id> already resolves)

coach/citation_linter.py:
- `from vibemix.state.evidence_registry import EVIDENCE_SOURCES, parse_citations` (line 40)
- `_TIME_KEYED_SOURCES: frozenset[str] = frozenset({"ev", "aud", "midi"})` (line 45) — key MUST stay OUT
- `_validate_atom`: `if source not in EVIDENCE_SOURCES` (line 186) → malformed; `if source in _TIME_KEYED_SOURCES` (line 191) → time-keyed branch; else existence-only `body in snapshot.get(source, {})`. NO code change to _validate_atom needed beyond the frozenset memberships.

prompts/matrix.py:
- `CITATION_GRAMMAR_BLOCK` (line 100); Forms list (lines 108-122); existing form `[mix:<derived>]  derived mix-state, e.g. [mix:audible_deck=A]` (line 114). Comment at line 96 states forms are kept in lock-step with EVIDENCE_SOURCES.

agent/dj_cohost.py:
- `_build_citation_strip` (line 146); the source allow-list `if source not in ("ev", "mix", "midi"): continue` (line 204). Chip timestamp comes from registry, NOT the citation body.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'key' source to the registry grammar (3 touchpoints) + existence-only linter rule</name>
  <files>src/vibemix/state/evidence_registry.py, src/vibemix/coach/citation_linter.py, tests/state/test_evidence_registry.py, tests/coach/test_citation_linter.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Spike 3 lines 312-348 — exact frozenset/regex/EBNF/linter additions + logic trace)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§evidence_registry.py three edit sites; §citation_linter.py the-only-change)
    - src/vibemix/state/evidence_registry.py (lines 89-118 EBNF docstring + EVIDENCE_SOURCES + _SOURCE_ALT)
    - src/vibemix/coach/citation_linter.py (lines 40-45 imports + _TIME_KEYED_SOURCES; lines 183-205 _validate_atom)
    - tests/coach/test_citation_linter.py (the track: existence-only test to clone, e.g. test_track_atom_existence_only ~lines 149-169)
  </read_first>
  <behavior>
    - "key" in EVIDENCE_SOURCES (frozenset now 8 members)
    - parse_citations("[key:A:8A]") == [("key", "A:8A")]; parse_citations("[track:t1,key:A:8A]") == [("track","t1"),("key","A:8A")]
    - _validate_atom("key", "A:8A", snapshot={"key":{"A:8A":(...)}}) is valid (existence-only)
    - _validate_atom("key", "A:12B", snapshot={"key":{"A:8A":(...)}}) is INVALID (body never observed)
    - "key" NOT in _TIME_KEYED_SOURCES → no @t parse attempted; a bare key:A:8A (no @timestamp) validates by presence
    - CitationLinter.check on a response with [key:A:12B] and no registered key → whole response stripped (binary response-level)
  </behavior>
  <action>Edit src/vibemix/state/evidence_registry.py at the three sites from RESEARCH §Spike 3: (1) add "key" to the EVIDENCE_SOURCES frozenset (line 95-97), (2) extend `_SOURCE_ALT` to "ev|aud|midi|track|screen|mix|tend|key" (line 104), (3) extend the EBNF docstring (lines 89-94) to document key-body := `<deck> ':' <camelot>` per RESEARCH lines 337-343. Leave `_INNER_ATOM` and `parse_citations` UNCHANGED (verified: key:A:8A matches the existing atom regex; partition(":") yields the correct split). Then edit src/vibemix/coach/citation_linter.py: confirm `_TIME_KEYED_SOURCES` (line 45) does NOT include "key" — "key" joins the existence-only set purely by being in EVIDENCE_SOURCES and absent here. Make NO code change to `_validate_atom` (RESEARCH line 348 — verified by logic trace; the existence-only final branch `body in snapshot.get(source, {})` already handles key:A:8A). Add tests to tests/state/test_evidence_registry.py (key in frozenset; parse_citations split) and tests/coach/test_citation_linter.py (clone the track: existence-only test for key:A:8A; add a strip test for fabricated key:A:12B; add an existence-only-no-@t test) per the PATTERNS analog.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_evidence_registry.py -k key tests/coach/test_citation_linter.py -k "key or existence" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=src python3 -c "from vibemix.state.evidence_registry import EVIDENCE_SOURCES, parse_citations; assert 'key' in EVIDENCE_SOURCES; assert parse_citations('[key:A:8A]')==[('key','A:8A')]"` exits 0
    - `PYTHONPATH=src python3 -c "from vibemix.coach.citation_linter import _TIME_KEYED_SOURCES; assert 'key' not in _TIME_KEYED_SOURCES"` exits 0 (adjust import if private)
    - A linter test proves a fabricated `[key:A:12B]` (never registered) is stripped; a registered `[key:A:8A]` is accepted
    - `git diff src/vibemix/coach/citation_linter.py` shows NO change to the `_validate_atom` body logic (only confirmation _TIME_KEYED_SOURCES excludes key)
  </acceptance_criteria>
  <done>'key' is a registered existence-only evidence source; parse_citations handles key:A:8A; the linter strips fabricated key atoms and accepts registered ones; no _validate_atom logic change; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the prompt + citation strip the key: form (touchpoints 4 + 5)</name>
  <files>src/vibemix/prompts/matrix.py, src/vibemix/agent/dj_cohost.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Spike 3 lines 366 — the 5 schema-mirror touchpoints)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§prompts/matrix.py — Forms analog line 114; §agent/dj_cohost.py — allow-list line 204, chip timestamp-from-registry contract)
    - src/vibemix/prompts/matrix.py (lines 96-122 CITATION_GRAMMAR_BLOCK + Forms list)
    - src/vibemix/agent/dj_cohost.py (lines 146-210 _build_citation_strip + the source allow-list at line 204)
  </read_first>
  <action>Edit src/vibemix/prompts/matrix.py: add one Form line to CITATION_GRAMMAR_BLOCK (after the existing `[mix:<derived>]` form at line 114), mirroring its shape: `[key:<deck>:<camelot>]  deck harmonic key, e.g. [key:A:8A]` per RESEARCH/PATTERNS. This is touchpoint #4 — honor the line-96 comment that the forms stay lock-step with EVIDENCE_SOURCES. Edit src/vibemix/agent/dj_cohost.py `_build_citation_strip`: add "key" to the source allow-list at line 204 (`if source not in ("ev", "mix", "midi", "key"): continue`) and derive a chip verb/label from the `A:8A` body (deck side + Camelot), taking the chip's `timestamp_s` from the registry observation — NOT from the citation body (anti-hallucination contract, RESEARCH lines 167-169). Do NOT touch the `screen_jpeg = None` killswitch (line 553) — that is the Plan 59-05 eval-gated vision leg, never a one-line flip. Keep all changes prompt/strip-only — no firing or detection logic. Extend or add a citation-strip test asserting a `[key:A:8A]` citation yields a chip and a registry-derived timestamp.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/agent/ tests/state/test_coach_prompt_diet.py -k "citation or strip or key" -x -q; grep -c "key:" src/vibemix/prompts/matrix.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/vibemix/prompts/matrix.py | grep -c 'key:<deck>:<camelot>'` returns 1 (the new Form line present)
    - `grep -n '"key"' src/vibemix/agent/dj_cohost.py` shows "key" added to the _build_citation_strip allow-list
    - `grep -c "screen_jpeg = None" src/vibemix/agent/dj_cohost.py` returns 1 (killswitch untouched)
    - Citation-strip test for [key:A:8A] passes; chip timestamp sourced from registry not body
    - Full suite `PYTHONPATH=src python3 -m pytest -q` green
  </acceptance_criteria>
  <done>The LLM is taught it can cite [key:A:8A]; the citation strip yields a key chip with a registry-derived timestamp; the vision killswitch is untouched; all 5 schema-mirror touchpoints carry 'key' in lock-step; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → user | Fabricated harmonic claims cross here; the linter is the gate |
| citation body → UI chip | Untrusted body must never set the chip timestamp (registry is the oracle) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02-01 | Spoofing (false expertise) | fabricated [key:A:12B] clash | mitigate | Dedicated key: source + existence-only linter strip; only poller-written A:camelot bodies validate — hallucinated bodies strip the whole turn |
| T-59-02-02 | Tampering | citation-body-driven chip timestamp | mitigate | Chip timestamp_s sourced from registry observation, never the citation body (existing anti-hallucination contract preserved) |
| T-59-02-03 | Spoofing | re-enabling screen Part as a side-effect | mitigate | screen_jpeg = None killswitch explicitly untouched; grep-asserted; vision is the separate Plan 59-05 eval-gated call |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/test_evidence_registry.py tests/coach/test_citation_linter.py tests/agent/ -x -q` green
- All 5 schema-mirror touchpoints carry 'key': `grep -l '"key"\|key:' src/vibemix/state/evidence_registry.py src/vibemix/coach/citation_linter.py src/vibemix/prompts/matrix.py src/vibemix/agent/dj_cohost.py`
- Full suite green (no golden flips)
</verification>

<success_criteria>
- 'key' is a citable existence-only evidence source; fabricated harmonic atoms stripped; registered ones accepted
- Lock-step across all 5 schema-mirror touchpoints
- screen_jpeg killswitch untouched; no firing/detection logic added (Phase 60 scope held back)
</success_criteria>

<output>
Create `.planning/phases/59-full-deck-awareness-grounding/59-02-SUMMARY.md` when done.
</output>
