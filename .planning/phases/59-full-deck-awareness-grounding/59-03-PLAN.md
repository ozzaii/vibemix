---
phase: 59-full-deck-awareness-grounding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/state/event.py
  - src/vibemix/audio/constants.py
  - tests/state/test_event_priority.py
  - tests/audio/test_constants.py
  - tests/repo/test_repo_scrub.py
autonomous: true
requirements: [DECK-04, DECK-05]
tags: [events, cooldowns, read-only, repo-scrub, deck-state]

must_haves:
  truths:
    - "EVENT_PRIORITY['KEY_CLASH'] == 7 and EVENT_PRIORITY['TRANSITION_OPPORTUNITY'] == 5 — types registered (plumbing only)"
    - "MIN_EVENT_GAP_PER_TYPE['KEY_CLASH'] == 28.0 and ['TRANSITION_OPPORTUNITY'] == 20.0 — cooldowns registered"
    - "Event(type='KEY_CLASH').priority == 7 via the EVENT_PRIORITY.get fallback (no longer silently 0)"
    - "A repo test asserts no DJ-software DB is ever opened in write mode and Rekordbox6Database/SQLCipher stays dormant in the deck path (DECK-05 read-only guarantee)"
  artifacts:
    - path: "src/vibemix/state/event.py"
      provides: "KEY_CLASH=7 + TRANSITION_OPPORTUNITY=5 in EVENT_PRIORITY"
      contains: "KEY_CLASH"
    - path: "src/vibemix/audio/constants.py"
      provides: "KEY_CLASH=28.0 + TRANSITION_OPPORTUNITY=20.0 in MIN_EVENT_GAP_PER_TYPE"
      contains: "KEY_CLASH"
    - path: "tests/repo/test_repo_scrub.py"
      provides: "DECK-05 read-only repo assertion (no DJ-DB write-mode open)"
      contains: "deck_readonly"
  key_links:
    - from: "tests/repo/test_repo_scrub.py"
      to: "src/vibemix/"
      via: "grep/subprocess assertion that no Rekordbox6Database / .commit() / write-mode open exists in the deck path"
      pattern: "Rekordbox6Database"
---

<objective>
Register the two new event TYPES (`KEY_CLASH`, `TRANSITION_OPPORTUNITY`) in the priority + cooldown maps — plumbing ONLY, the firing logic is Phase 60 — and land the DECK-05 strictly-read-only repo guarantee as a test (extending the proven SQLCipher-dormancy idiom).

Purpose: an unregistered event type silently gets priority 0 via the `EVENT_PRIORITY.get(type, 0)` fallback; registering the types + cooldowns now is the plumbing Phase 60's detector branches plug into. The read-only repo test is the hard guarantee that the deck path never corrupts the user's collection — the `master.db` write landmine (Risk 4).

Output: two event constants in `event.py` + `constants.py`, the DECK-05 read-only repo test, event-map regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md
@.planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md

<interfaces>
<!-- Verified against branch live-tuning-or-brain. -->

state/event.py:
- `EVENT_PRIORITY: dict[str, int] = {...}` (line 32) — current: MANUAL/DROP=10, KAAN_SPOKE=9, TRACK_CHANGE=7, PHASE=6, MIX_MOVE/DISTORTION_CLIMB/ACID_LINE_ENTRY=5, LAYER_ARRIVAL=4, HEARTBEAT=1
- `__post_init__` (line 60): `self.priority = EVENT_PRIORITY.get(self.type, 0)` — unregistered type → silent priority 0

audio/constants.py:
- `EVENT_GLOBAL_MIN_GAP = ...` (line 58) — value 22.0 floor inherited via _cooldown_ok
- `MIN_EVENT_GAP_PER_TYPE: dict[str, float] = {...}` (line 77) — current: TRACK_CHANGE=5, PHASE=10, MIX_MOVE=14.0, BREAKDOWN_KICK_KILL=20.0, HEARTBEAT=45, etc.; convention: every entry has a one-line `# why` comment

DECK-05 read-only analog:
- tests/library/test_rekordbox.py::test_no_sqlcipher_module_imported_after_load (lines 105-132) — subprocess dormancy idiom: `subprocess.run([sys.executable, "-c", script], ...)`, `assert result.stdout.strip() == "DORMANT"`
- tests/repo/test_repo_scrub.py::test_retired_poc_files_stay_gone (line 194) + _git_ls_files() (line 73) — grep/scrub idiom over tracked source
- src/vibemix/library/rekordbox.py — XML-only path; SQLCipher db6/Rekordbox6Database deliberately banned (grep-gate already exists); pyproject.toml line 135 overrides sqlcipher3-wheels out
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register KEY_CLASH + TRANSITION_OPPORTUNITY in priority + cooldown maps (plumbing only)</name>
  <files>src/vibemix/state/event.py, src/vibemix/audio/constants.py, tests/state/test_event_priority.py, tests/audio/test_constants.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Spike resolution — event types, lines 368-380 — exact priority/cooldown values + plumbing-only note)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§event.py EVENT_PRIORITY; §audio/constants.py MIN_EVENT_GAP_PER_TYPE — one-line-why convention)
    - src/vibemix/state/event.py (lines 32-62 EVENT_PRIORITY + __post_init__)
    - src/vibemix/audio/constants.py (lines 58, 77-114 EVENT_GLOBAL_MIN_GAP + MIN_EVENT_GAP_PER_TYPE)
    - tests/state/test_event_priority.py + tests/audio/test_constants.py (per-type value-assert shape to extend)
  </read_first>
  <action>Edit src/vibemix/state/event.py: add to EVENT_PRIORITY (RESEARCH lines 371-374) `"KEY_CLASH": 7` (tie with TRACK_CHANGE — a clashing overlay is react-now) and `"TRANSITION_OPPORTUNITY": 5` (tie with MIX_MOVE — structural, not urgent), each with the one-line rationale comment. Edit src/vibemix/audio/constants.py: add to MIN_EVENT_GAP_PER_TYPE (RESEARCH lines 376-378) `"KEY_CLASH": 28.0` (long gap — a clash persists; re-arm after it clears+recurs, ~25-30s band) and `"TRANSITION_OPPORTUNITY": 20.0` (medium — fires at the moment a blend COULD start), honoring the per-entry one-line-why comment convention. Make NO change to event_detector.py, no detector branch, no task_for_event firing — that is Phase 60 (RESEARCH line 380). Both inherit the EVENT_GLOBAL_MIN_GAP=22.0 floor via _cooldown_ok — no gate-logic change. Extend tests/state/test_event_priority.py with per-type asserts (Event(type="KEY_CLASH").priority==7, ...="TRANSITION_OPPORTUNITY").priority==5) and tests/audio/test_constants.py with the two new gap-value asserts.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_event_priority.py tests/audio/test_constants.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `PYTHONPATH=src python3 -c "from vibemix.state.event import EVENT_PRIORITY; assert EVENT_PRIORITY['KEY_CLASH']==7 and EVENT_PRIORITY['TRANSITION_OPPORTUNITY']==5"` exits 0
    - `PYTHONPATH=src python3 -c "from vibemix.audio.constants import MIN_EVENT_GAP_PER_TYPE as m; assert m['KEY_CLASH']==28.0 and m['TRANSITION_OPPORTUNITY']==20.0"` exits 0
    - `PYTHONPATH=src python3 -c "from vibemix.state.event import Event; assert Event(type='KEY_CLASH').priority==7"` exits 0 (adjust Event ctor args as needed)
    - `grep -c "def detect\|task_for_event" src/vibemix/state/event_detector.py` shows no NEW KEY_CLASH/TRANSITION_OPPORTUNITY detector branch (firing is Phase 60)
  </acceptance_criteria>
  <done>Both event types registered with documented priority + cooldown; the get-fallback no longer returns silent 0; no firing/detection logic added; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DECK-05 read-only repo guarantee — no DJ-DB write-mode open</name>
  <files>tests/repo/test_repo_scrub.py</files>
  <read_first>
    - .planning/phases/59-full-deck-awareness-grounding/59-RESEARCH.md (§Spike 1 lines 258-290 — XML-only confirmed, live-DB banned; §Validation Architecture lines 562, 568 — the read-only test contract)
    - .planning/phases/59-full-deck-awareness-grounding/59-PATTERNS.md (§Tests — read-only repo assertion analog, lines 288-298)
    - tests/library/test_rekordbox.py (lines 105-132 — test_no_sqlcipher_module_imported_after_load subprocess/DORMANT idiom)
    - tests/repo/test_repo_scrub.py (lines 73-82 _git_ls_files; lines 194-212 test_retired_poc_files_stay_gone grep idiom)
  </read_first>
  <behavior>
    - A grep over tracked src/vibemix/ finds NO `Rekordbox6Database`, no `pyrekordbox.db6`, no DJ-DB `.commit()`, no sqlite write-mode open against any DJ-software DB
    - The new test fails loudly if a future commit introduces a write-mode DJ-DB open (regression pin)
    - The existing test_no_sqlcipher_module_imported_after_load stays green (deck path does not import sqlcipher)
  </behavior>
  <action>Add a new test case to tests/repo/test_repo_scrub.py named `test_deck_readonly` mirroring the test_retired_poc_files_stay_gone + _git_ls_files grep idiom (PATTERNS lines 288-298): scan tracked source under src/vibemix/ and assert NO `Rekordbox6Database`, `pyrekordbox.db6`, or DJ-DB write-mode patterns (`sqlite3.connect(...` against a DJ DB without read-only URI, `.commit()` on a DJ-DB connection) exist in the deck path. Use the `grep -v '^#'` comment-stripping discipline so header prose cannot self-invalidate the gate. Where the assertion needs runtime confirmation, extend the proven subprocess-dormancy idiom from tests/library/test_rekordbox.py (spawn a fresh interpreter, exercise the deck-state import path, assert `DORMANT` — no *sqlcipher* module loaded). This is the DECK-05 hard guarantee. Do NOT modify any src/ file in this task — it is a test-first regression pin that the Plan 59-04 poller must satisfy.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/repo/test_repo_scrub.py -k deck_readonly tests/library/test_rekordbox.py -k dormant -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/repo/test_repo_scrub.py::test_deck_readonly exists and passes against the current (poller-less) tree
    - The test greps with `grep -v '^#'` comment-stripping (no bare `== 0` on unfiltered files)
    - `PYTHONPATH=src python3 -m pytest tests/repo/test_repo_scrub.py -k deck_readonly -x -q` green
    - The existing SQLCipher dormancy test still passes (deck path adds no sqlcipher import)
  </acceptance_criteria>
  <done>A repo test asserts the deck path is strictly read-only (no DJ-DB write-mode open, no Rekordbox6Database/SQLCipher); it will fail if Plan 59-04 or any future commit introduces a write path; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidecar → user's DJ-software DB (master.db) | Any write corrupts the user's collection — must never be crossed |
| unregistered event type → priority engine | Silent priority-0 fallback is a latent plumbing gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-01 | Tampering | write to user's master.db / collection | mitigate | DECK-05 repo test asserts no DJ-DB write-mode open; XML-only path; SQLCipher stays dormant (extends proven dormancy idiom) |
| T-59-03-02 | Repudiation/Tampering | bundling/extracting SQLCipher key | mitigate | Rekordbox6Database/db6 grep-gated dormant; repo test reasserts the ban in the deck path |
| T-59-03-03 | Denial of Service (latent) | unregistered event silently priority 0 | mitigate | Both new types registered in EVENT_PRIORITY; per-type asserts pin the values |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/test_event_priority.py tests/audio/test_constants.py tests/repo/test_repo_scrub.py tests/library/test_rekordbox.py -x -q` green
- `grep -rn "KEY_CLASH\|TRANSITION_OPPORTUNITY" src/vibemix/state/event.py src/vibemix/audio/constants.py` shows both in both maps
- No detector/firing logic added (Phase 60 scope held)
</verification>

<success_criteria>
- Both event types registered with documented priority + cooldown (plumbing only)
- DECK-05 read-only guarantee landed as a regression-pinned repo test
- No firing logic, no write path
</success_criteria>

<output>
Create `.planning/phases/59-full-deck-awareness-grounding/59-03-SUMMARY.md` when done.
</output>
