---
phase: 100-harden-clarify
plan: 07
type: execute
wave: 6
depends_on: ["100-04", "100-05", "100-06"]
files_modified:
  - tests/library/test_codex_curate_stop_reason.py
autonomous: true
requirements: [HARDEN-CLARIFY-07]
tags: [factor-7, clarification, integration-seal, uniform-propagation, end-to-end-pin, kaan-action-surface]

must_haves:
  truths:
    - "Integration seal proves the FULL chain: toolset.request_clarification (REAL handler) -> side-channel write (REAL helper) -> wrapper read (REAL curate_with_codex) -> CodexCurateResult -> _normalize_codex_curate_result -> format_reply renders correctly with question + numbered choices + leak-strip applied"
    - "Same seal for build-set path via build_set_with_codex (parallel wrapper) — uniform propagation across both wrappers"
    - "End-to-end CLI smoke (mocked Codex): exit code 11 + stderr rendered + re-run hint substring confirmed in ONE seal test, proving CLI/normalizer/Telegram all see the same shape"
    - "KAAN-ACTION HARDEN-PHASE-B-CLARIFICATION-TONE surfaced in SUMMARY with three sample question framings for ear-pass on funded Codex key"
    - "REQ coverage matrix in SUMMARY shows 7/7 HARDEN-CLARIFY REQ-IDs closed across Plans 100-01..07"
  artifacts:
    - path: "tests/library/test_codex_curate_stop_reason.py"
      provides: "Additional integration-seal tests proving uniform end-to-end propagation across toolset + wrapper + normalizer + CLI + Telegram surfaces"
      contains: "def test_clarification_full_chain_to_cli_curate"
  key_links:
    - from: "toolset.request_clarification + _build_clarification_payload + _write_side_channel"
      to: "telegram_bridge.format_reply (end-to-end)"
      via: "side-channel file -> wrapper short-circuit -> CodexCurateResult.question/choices -> normalizer -> format_reply"
      pattern: "integration seal test exercises every link"
---

<objective>
Wave-6 integration seal. Plans 100-01 through 100-05 each ship a NARROW slice (handler, exposure, wrapper, CLI, Telegram). Plan 100-06 ships AST gates. Plan 100-07 ships the END-TO-END SEAL that proves all those slices compose correctly — a small set of integration tests that take a `request_clarification` tool call from the toolset side, propagate through the side-channel, read through the wrapper, normalize it, and verify both the CLI exit-11 + stderr render AND the Telegram format_reply output. Same posture as Phase 99 Plan 99-08's seal.

This plan also formally surfaces the KAAN-ACTION §HARDEN-PHASE-B-CLARIFICATION-TONE checkpoint in the SUMMARY with three sample question framings.

Purpose: HARDEN-CLARIFY-07 final closure (uniform propagation seal). Plan-checker can verify REQ coverage 7/7 against this plan's SUMMARY + the prior plans' SUMMARYs.

Output:
- `tests/library/test_codex_curate_stop_reason.py` — extended with 2-3 end-to-end seal tests. Diff envelope: ~150 insertions. Zero src/ edits.

Plan 100-07 is GREEN-only (no RED → GREEN cycle needed). The seal tests verify EXISTING shipped behavior across Plans 100-01..05. If a seal test fails, that's evidence one of the prior plans drifted; fix the offending plan, do NOT relax the seal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-harden-clarify/100-CONTEXT.md
@.planning/phases/100-harden-clarify/100-01-SUMMARY.md
@.planning/phases/100-harden-clarify/100-02-SUMMARY.md
@.planning/phases/100-harden-clarify/100-03-SUMMARY.md
@.planning/phases/100-harden-clarify/100-04-SUMMARY.md
@.planning/phases/100-harden-clarify/100-05-SUMMARY.md
@.planning/phases/100-harden-clarify/100-06-SUMMARY.md
@.planning/phases/99-harden-retry/99-08-SUMMARY.md
@tests/library/test_codex_curate_stop_reason.py

<interfaces>
After Plans 100-01..05 land, the full chain is:

```
1. Codex calls request_clarification(question, choices) via MCP STDIO.
2. mcp_server.py @mcp.tool() delegates to toolset.request_clarification({"question": q, "choices": cs}).
3. toolset.request_clarification validates args; on accept:
   - writes self.stop_reason = _build_clarification_payload(q, cs)
   - calls self._write_side_channel(self.stop_reason)
   - returns {"clarification_needed": True, "question": q, "choices": cs}
4. Codex MCP child exits. Wrapper (curate_with_codex / build_set_with_codex) resumes.
5. Wrapper reads stop_reason.json INSIDE the with tempfile.TemporaryDirectory block.
6. Wrapper's elif branch on payload.get("reason") == "clarification_needed" fires.
7. Wrapper returns CodexCurateResult(stop_reason="clarification_needed", question=q, choices=cs).
8a. CLI: _cmd_library_curate_codex returns exit 11 with 2-block stderr render.
8b. Telegram: curate_fn -> _normalize_codex_curate_result -> format_reply renders multi-line message, leak-stripped.
```

The Phase 99 Plan 99-08 integration seal at tests/library/test_codex_curate_stop_reason.py lines 357-560 is the structural template Plan 100-07 mirrors.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end integration seal tests</name>
  <files>tests/library/test_codex_curate_stop_reason.py</files>
  <action>EXTEND tests/library/test_codex_curate_stop_reason.py with 2-3 NEW integration seal tests. Place them AFTER the Phase 100 propagation tests from Plan 100-03, with a section header comment "## Phase 100 HARDEN-CLARIFY-07 — integration seal (end-to-end uniform propagation)".

test_clarification_full_chain_to_cli_curate (END-TO-END SEAL — CLI surface):
- Setup: empty_lib + LibraryToolset constructed against it.
- Use monkeypatch.setenv to set VIBEMIX_STOP_REASON_FILE to a tmp_path / "stop_reason.json".
- Build the REAL payload via toolset._build_clarification_payload("What BPM range?", ["slow (90-110)", "fast (130-140)", "mixed"]).
- Use _runner_with_side_channel(side_channel_payload=payload, out_payload=None) — exact same helper Phase 99 uses for its seal tests. Posture: the fake runner inspects kw.get('env') to find the wrapper-allocated env-var path and writes the payload there, simulating what the real MCP child would do.
- Invoke curate_with_codex("ambiguous theme", empty_lib, codex_path=sys.executable, allow_shell=True, _runner=runner). Capture result.
- Pre-check: assert result.stop_reason == "clarification_needed", result.question == "What BPM range?", result.choices == ["slow (90-110)", "fast (130-140)", "mixed"].
- CLI dispatch leg: import _cmd_library_curate_codex from vibemix.__main__. Use unittest.mock.patch to make vibemix.library.codex_curate.curate_with_codex return the prepared result. Build argparse.Namespace fake args with theme="ambiguous theme", name=None. Capture stderr via capsys. Call _cmd_library_curate_codex(args, sentinel_lib). Assert return value == 11. Assert stderr contains "clarification_needed" AND "What BPM range?" AND "slow (90-110)" AND "fast (130-140)" AND "mixed" AND "Re-run with:" AND "library curate".

test_clarification_full_chain_to_telegram (END-TO-END SEAL — Telegram surface):
- Same setup (toolset -> side-channel -> wrapper -> CodexCurateResult).
- Normalizer leg: from vibemix.__main__ import _normalize_codex_curate_result. Call _normalize_codex_curate_result(result). Assert norm == {"ok": False, "stop_reason": "clarification_needed", "question": "What BPM range?", "choices": ["slow (90-110)", "fast (130-140)", "mixed"]}.
- Telegram render leg: from vibemix.library.telegram_bridge import format_reply. Call format_reply(norm). Assert returns a string containing "What BPM range?" + each choice + a leading discriminating glyph (anything but ⚠️ — accept whatever Plan 100-05 picked).
- Leak-strip defense: build a malicious-shaped norm with question = "Library at /Users/ozai/.cache/vibemix — pick context?" + choices = ["a", "b"]. Call format_reply. Assert "/Users/ozai" NOT in output, "[path]" IS in output, "pick context?" survives.

test_clarification_build_set_path_seal (END-TO-END SEAL — build-set wrapper parity):
- Use build_set_with_codex instead of curate_with_codex. brief="ambiguous brief". Otherwise mirror test_clarification_full_chain_to_cli_curate. Assert that:
  - result is CodexCurateResult with the expected fields populated.
  - CLI _cmd_library_build_set_codex returns 11.
  - stderr contains the same substrings PLUS "library build-set" (not "library curate") in the re-run hint.
- This proves the build_set wrapper propagates identically to the curate wrapper.

The seal tests DO NOT use mocks for the toolset, _build_clarification_payload, _write_side_channel, or the wrapper's side-channel-read logic. They use REAL implementations end-to-end, with only the codex subprocess (the EXTERNAL boundary) mocked via _runner_with_side_channel — same posture as Phase 99 Plan 99-08's seal.

Forbidden: do NOT modify ANY existing tests in this file (Phase 99 + Phase 100 prior tests stay byte-equivalent). Additive only.

If Plan 100-03 already added test_to_dict_serializes_clarification_shape, leave it alone — that test is the shape pin. If it was NOT added (executor of 100-03 deferred it), add it here as part of the seal.

Commit message: `test(100-07): GREEN — integration seal proving end-to-end uniform clarification propagation across toolset+wrapper+normalizer+CLI+Telegram`.</action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate_stop_reason.py tests/library/test_cli_exit_codes.py tests/library/test_telegram_bridge.py tests/library/test_toolset_clarification.py tests/library/test_mcp_server_clarification.py tests/library/test_request_clarification_no_track_surface.py tests/repo/test_no_seen_relaxation.py tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 2-3 new seal tests PASS — end-to-end chain works.
    All prior Phase 100 + Phase 99 tests STILL GREEN (no regressions).
    Manual smoke: `python3 -c "from vibemix.library.toolset import LibraryToolset, MIN_CHOICES, MAX_CHOICES; from vibemix.library.codex_curate import CodexCurateResult; from vibemix.__main__ import _normalize_codex_curate_result; from vibemix.library.telegram_bridge import format_reply; r = CodexCurateResult(theme='x', stop_reason='clarification_needed', question='Q?', choices=['a','b']); n = _normalize_codex_curate_result(r); print(format_reply(n))"` — prints the rendered clarification message.
    git diff --cached --name-only at commit shows ONLY tests/library/test_codex_curate_stop_reason.py.
    SUMMARY surfaces KAAN-ACTION HARDEN-PHASE-B-CLARIFICATION-TONE with three sample question framings (see Output section below).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| End-to-end integration test inputs -> the full Phase 100 surface | The seal test exercises every link. A drift in ANY one Plan (100-01..05) trips the seal. |
| KAAN-ACTION ear-pass surface -> milestone close | The clarification question copy comes from Codex (LLM-generated). vibemix's rendering is structurally locked; the TONE of the question text is the ear-pass surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-07-01 | Tampering | A future PR drifts ONE plan's surface (e.g. changes the stderr render format) without breaking that plan's narrow test | mitigate | Integration seal tests assert end-to-end substring presence. A render-format drift fails the seal even if the plan-narrow test still passes. |
| T-100-07-02 | Information disclosure | The integration test prints leaks during execution (tmp_path with FS info) | accept | tmp_path fixtures are pytest-managed, auto-cleaned. No leak to disk persists beyond test session. |
| T-100-07-03 | Anti-slop | Codex's actual clarification question copy is robotic / sloppy in production | accept (KAAN-ACTION ear-pass) | Plan 100-07 SUMMARY surfaces three sample question framings for KAAN-ACTION §HARDEN-PHASE-B-CLARIFICATION-TONE on funded key. Per CLAUDE.md tone is human-perceptual release gate; no automated check substitutes. Default per `gsd-autonomous fully` = defer to milestone close. |
| T-100-07-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. Stdlib only. |
</threat_model>

<verification>
1. **Plan 100-07 specific:** `PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate_stop_reason.py` — all 13+ tests GREEN (7 Phase 99 + 4 Phase 100-03 propagation + 2-3 Phase 100-07 seal).
2. **Full phase regression:** `PYTHONPATH=src python3 -m pytest -q tests/library/test_cli_exit_codes.py tests/library/test_telegram_bridge.py tests/library/test_toolset_clarification.py tests/library/test_mcp_server_clarification.py tests/library/test_request_clarification_no_track_surface.py tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py` — all GREEN.
3. **Repo gate:** `PYTHONPATH=src python3 -m pytest -q tests/repo/test_no_seen_relaxation.py` — GREEN.
4. **Broader regression:** `PYTHONPATH=src python3 -m pytest -q tests/library/` — Phase 99 + Phase 100 island broadly GREEN (deferred-items.md from Phase 99 listed pre-existing failures unrelated to this work; same disposition applies — document any drift in SUMMARY).
5. **REQ coverage matrix (in SUMMARY):**

   | REQ-ID | Closure plan | SUMMARY ref |
   |--------|--------------|-------------|
   | HARDEN-CLARIFY-01 | 100-01 | request_clarification handler + MIN/MAX choices + dispatch entry |
   | HARDEN-CLARIFY-02 | 100-02 | MCP FastMCP exposure + teaching docstring |
   | HARDEN-CLARIFY-03 | 100-03 | CodexCurateResult.question/choices + wrapper elif branches |
   | HARDEN-CLARIFY-04 | 100-04 | CLI exit code 11 + 2-block stderr render |
   | HARDEN-CLARIFY-05 | 100-05 | telegram_bridge format_reply clarification branch |
   | HARDEN-CLARIFY-06 | 100-04 + 100-06 | Single-turn re-run hint (100-04) + AST gate single-trip (100-06) |
   | HARDEN-CLARIFY-07 | 100-03 + 100-06 + 100-07 | Test coverage propagation (100-03) + AST gate (100-06) + integration seal (100-07) |
</verification>

<success_criteria>
- [ ] 2-3 new integration seal tests in test_codex_curate_stop_reason.py PASS end-to-end.
- [ ] All prior Phase 100 + Phase 99 tests STILL GREEN.
- [ ] Repo gate STILL GREEN.
- [ ] REQ coverage 7/7 verified in SUMMARY matrix.
- [ ] KAAN-ACTION §HARDEN-PHASE-B-CLARIFICATION-TONE surfaced with 3 sample question framings.
- [ ] Forward-compat verification: future stop_reasons (12, 13, etc.) drop in via the same pattern.
- [ ] Zero src/ edits.
- [ ] Single commit lands with named-path staging.
</success_criteria>

<output>
Create `.planning/phases/100-harden-clarify/100-07-SUMMARY.md`. This SUMMARY is the phase-level closure document. Required content:

1. **REQ coverage matrix (7/7 closed):**
   Table mapping each HARDEN-CLARIFY-NN REQ-ID to its closure plan + the test(s) that pin it.

2. **Forward-compat verification:**
   Demonstrate that adding a new stop_reason (e.g. `stop_reason="user_canceled"` in some future phase) requires only:
   (a) New handler in toolset.py (sibling of request_clarification)
   (b) New `_build_<reason>_payload` helper (sibling of `_build_clarification_payload`)
   (c) New `elif` in codex_curate.py wrappers (sibling of the clarification branch)
   (d) New `elif` in __main__.py CLI dispatch (sibling of exit-11)
   (e) New `elif` in telegram_bridge.format_reply (sibling of the clarification branch)
   (f) New `if` in __main__.py _normalize_codex_curate_result (sibling of clarification dict)
   No refactoring of existing code is needed. This is the discriminated-union pattern Plans 99 + 100 lock.

3. **KAAN-ACTION §HARDEN-PHASE-B-CLARIFICATION-TONE checkpoint:**
   Three sample question framings for ear-pass on funded Codex key:

   Sample A — context disambiguation:
   ```
   ❓ Where will you play this?
     1. bedroom-headphones
     2. peak-time-club
     3. festival-main-stage
     Re-run with: library curate "uplifting + <chosen option>"
   ```

   Sample B — BPM range disambiguation:
   ```
   ❓ What tempo range fits the vibe?
     1. slow (90-110)
     2. mid (118-128)
     3. fast (130-140)
     4. mixed
     Re-run with: library curate "energetic + <chosen option>"
   ```

   Sample C — mood register disambiguation:
   ```
   ❓ Which "dark" did you mean?
     1. dark-melodic
     2. dark-industrial
     3. dark-hypnotic
     Re-run with: library curate "dark techno + <chosen option>"
   ```

   Kaan's ear-pass question: "Does Codex's actual production framing sound like a real DJ friend asking for a quick check, or like a robot survey?"
   Verdict options: approved (ships) / tweak now / defer to milestone close.

4. **Plan-by-plan summary (commits + files):**
   Quick-glance table linking each Plan 100-NN to its commits + key files modified.

5. **Acid test verdict:**
   Pin the Phase 100 acid test (CONTEXT.md domain section): "When Codex sees an ambiguous theme, it calls request_clarification(question, choices); vibemix terminates with stop_reason='clarification_needed'; CLI exits with code 11 + 2-block render; Telegram renders numbered choices leak-stripped; vibemix retains NO state across the cycle." Status: PASSED (engineering surface), KAAN-ACTION ear-pass parked at milestone close.

6. **Cardinal invariants re-verified:**
   - #1: N/A direct (no MusicState writes). Single-writer analog of `stop_reason` extended additively (Phase 99 starvation + Phase 100 clarification write the same attribute; analog invariant holds because writes are confined to LibraryToolset instance + Plan 99-05's whitelist gate stays GREEN).
   - #2: Holds STRUCTURALLY — request_clarification has no track_id surface (Plan 100-06 AST gate proves it).
   - #3: N/A (live co-host untouched).
   - #4: N/A (CLI + MCP STDIO + Telegram long-poll only).

7. **Phase 100 deferred-items.md update (if any):**
   If broader test/library suite runs reveal pre-existing failures unrelated to Phase 100 (per the Phase 99 deferred-items.md precedent), document them in `.planning/phases/100-harden-clarify/deferred-items.md` and note the same scope-boundary disposition.

8. **Phase 100 VERIFICATION.md handoff:**
   Note that gsd-verifier should run the Plan 100-07 SUMMARY's REQ coverage matrix + acid test verdict + KAAN-ACTION surface as the Phase 100 verification report inputs.
</output>
