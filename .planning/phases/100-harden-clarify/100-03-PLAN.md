---
phase: 100-harden-clarify
plan: 03
type: execute
wave: 3
depends_on: ["100-01"]
files_modified:
  - src/vibemix/library/codex_curate.py
  - tests/library/test_codex_curate_stop_reason.py
autonomous: true
requirements: [HARDEN-CLARIFY-03, HARDEN-CLARIFY-07]
tags: [factor-7, clarification, hardening, sibling-extension, side-channel, propagation, wrapper, dataclass-extension]

must_haves:
  truths:
    - "CodexCurateResult dataclass has two NEW optional fields: question: str | None = None and choices: list[str] | None = None. Defaults preserve the cold (non-clarification) path — every existing call site sees the same shape."
    - "curate_with_codex side-channel short-circuit (codex_curate.py:524-548) adds an elif branch on payload.get('reason') == 'clarification_needed' alongside the existing tool_starvation branch. Branch populates CodexCurateResult.stop_reason='clarification_needed' + question + choices fields."
    - "build_set_with_codex side-channel short-circuit (codex_curate.py:847-863) gets the parallel elif branch. Symmetric to curate."
    - "_STOP_REASONS docstring/comment block at codex_curate.py:228-231 documents the new clarification_needed entry with a one-line note pointing at Plan 100-03."
    - "CodexCurateResult.to_dict() serializes the new question + choices fields uniformly — GUI/JSON consumers (CLI Plan 100-04 + Telegram Plan 100-05) read the same shape regardless of wrapper origin."
    - "Cold path (every non-clarification stop_reason: created / no_playlist / timeout / tool_starvation / codex_not_installed / codex_auth_required / codex_mcp_blocked / empty_output / error) — question and choices stay None. Zero regression on Phase 99 propagation tests."
    - "Tool-starvation branch is BYTE-EQUIVALENT to its pre-100-03 form. The new clarification branch sits BESIDE it as a sibling elif, NOT a refactor. Decision-tree shape: if reason==tool_starvation -> existing branch; elif reason==clarification_needed -> new branch; else fall-through to existing out.json parse."
  artifacts:
    - path: "src/vibemix/library/codex_curate.py"
      provides: "CodexCurateResult.question + .choices fields; clarification_needed elif branches in both wrappers; _STOP_REASONS comment block updated"
      contains: "question: str | None = None"
      contains_2: "choices: list[str] | None = None"
      contains_3: 'payload.get("reason") == "clarification_needed"'
    - path: "tests/library/test_codex_curate_stop_reason.py"
      provides: "Extended with clarification propagation tests (one per wrapper) + to_dict shape test + cold-path non-regression pin"
      contains: "def test_uniform_clarification_propagation_curate_path"
      contains_2: "def test_uniform_clarification_propagation_build_set_path"
      contains_3: "def test_to_dict_serializes_clarification_shape"
  key_links:
    - from: "src/vibemix/library/codex_curate.py side-channel short-circuit (curate_with_codex line 524-548)"
      to: "CodexCurateResult(stop_reason='clarification_needed', question=..., choices=...)"
      via: "elif payload.get('reason') == 'clarification_needed' branch"
      pattern: 'elif.*payload\.get\("reason"\) == "clarification_needed"'
    - from: "src/vibemix/library/codex_curate.py side-channel short-circuit (build_set_with_codex line 847-863)"
      to: "CodexCurateResult(stop_reason='clarification_needed', question=..., choices=...)"
      via: "same elif sibling branch in the parallel wrapper"
      pattern: 'elif.*payload\.get\("reason"\) == "clarification_needed"'
    - from: "CodexCurateResult dataclass"
      to: "GUI/JSON consumer (Plan 100-04 CLI, Plan 100-05 Telegram)"
      via: "to_dict() includes question + choices keys, default None"
      pattern: '"question":'
---

<objective>
Wave-3 wrapper propagation. The handler shipped in Plan 100-01 writes `self.stop_reason = {reason: 'clarification_needed', question, choices, tool: 'request_clarification'}` and the side-channel writer (Phase 99 helper, reused unchanged) persists it as JSON to the env-var path. Plan 100-03 reads that JSON inside both `curate_with_codex` and `build_set_with_codex` wrappers and translates it into a `CodexCurateResult` dataclass shape that downstream surfaces (CLI Plan 100-04, Telegram Plan 100-05) consume.

This is the SIBLING extension of Phase 99 Plan 99-04's side-channel propagation. Decision-tree shape: ` if payload.get("reason") == "tool_starvation": ... elif payload.get("reason") == "clarification_needed": ... else: <fall through>` — the existing tool_starvation branch is byte-equivalent; the new branch sits beside it without restructuring. Phase 99 Plan 99-04's SUMMARY explicitly authorized this drop-in: *"Phase 100 forward-compat: wrapper branch is `payload.get('reason') == 'tool_starvation'` (single-branch form). Phase 100 adds `elif payload.get('reason') == 'clarification_needed':` without refactoring."*

Purpose: HARDEN-CLARIFY-03 + HARDEN-CLARIFY-07 closure. Without Plan 100-03, the toolset writes a clarification payload to the side-channel file but the wrapper ignores it (the existing if-block only catches `tool_starvation`). With Plan 100-03, the wrapper surfaces a clarification result to CLI/Telegram/GUI uniformly.

Output:
- `src/vibemix/library/codex_curate.py` — two new optional dataclass fields + two parallel elif branches + one `_STOP_REASONS` comment block update.
- `tests/library/test_codex_curate_stop_reason.py` — extended with clarification-propagation tests (uniform with the existing starvation seal tests).

Diff envelope: ~60 insertions, 0 deletions in codex_curate.py; ~120 insertions in test file. Two atomic commits (RED test + GREEN impl). Disjointness preserved (no touches to __main__.py, telegram_bridge.py, or toolset.py — those land in Plans 100-04 / 100-05 separately).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/100-harden-clarify/100-CONTEXT.md
@.planning/phases/100-harden-clarify/100-01-PLAN.md
@.planning/phases/100-harden-clarify/100-01-SUMMARY.md
@.planning/phases/99-harden-retry/99-04-SUMMARY.md
@src/vibemix/library/codex_curate.py
@tests/library/test_codex_curate_stop_reason.py

<interfaces>
<!-- The CodexCurateResult dataclass after Plan 99 (current state) — Plan 100-03 extends it -->

From src/vibemix/library/codex_curate.py:201-218 (Phase 99 shipped state):

```python
@dataclass(slots=True)
class CodexCurateResult:
    """Outcome of one Codex curation run."""

    theme: str
    stop_reason: str  # see _STOP_REASONS below
    playlist_name: str | None = None
    track_ids: list[str] = field(default_factory=list)
    m3u_path: str | None = None
    json_path: str | None = None
    rationale: str = ""
    error: str | None = None
    export_path: str | None = None

    def to_dict(self) -> dict[str, Any]:
        return asdict(self)
```

There is ONLY ONE result dataclass (not separate CodexCurateResult + CodexBuildSetResult).
build_set_with_codex returns the same CodexCurateResult — verified at codex_curate.py:760+ (set-prep wrapper returns `CodexCurateResult(theme=brief, stop_reason="...", ...)`). The ROADMAP Phase 100 entry's prose ("surface question + choices payload in both CodexCurateResult + CodexBuildSetResult") was forward-looking — the actual codebase ships ONE dataclass for BOTH wrappers. Plan 100-03 adds question + choices fields to that one dataclass; both wrappers populate them on the new branch.

<!-- The side-channel short-circuit pattern (Phase 99 Plan 99-04 shipped) — Plan 100-03 extends it -->

From src/vibemix/library/codex_curate.py:524-548 (curate path) — Phase 99 shipped:

```python
if Path(stop_reason_path).exists():
    try:
        payload = json.loads(
            Path(stop_reason_path).read_text(encoding="utf-8")
        )
        if (
            isinstance(payload, dict)
            and payload.get("reason") == "tool_starvation"
        ):
            return CodexCurateResult(
                theme=theme,
                stop_reason="tool_starvation",
                error=str(
                    payload.get("hint")
                    or "no playlist — tool starvation, no hint available"
                ),
            )
    except (OSError, json.JSONDecodeError):
        pass  # fall through to existing parse logic
```

The parallel block at codex_curate.py:847-863 (build_set path) has the identical structure with `theme=brief` instead of `theme=theme`. Plan 100-03 extends BOTH blocks with an elif sibling.

<!-- _STOP_REASONS comment block (Phase 99 Plan 99-04 extended; Plan 100-03 extends again) -->

From src/vibemix/library/codex_curate.py:220-231:

```python
# created            — a grounded playlist came back
# codex_not_installed — no `codex` binary on PATH
# codex_auth_required — codex ran but is not logged in
# timeout            — outer wall-clock kill
# empty_output       — codex produced nothing parseable
# no_playlist        — output had no track_ids surviving library validation
# tool_starvation    — Plan 99-04 propagation: the MCP-side toolset hit the
#                      TOOL_STARVATION_THRESHOLD (3 consecutive empty/error
#                      tool returns) and wrote stop_reason.json to the env-var
#                      path; the wrapper short-circuits with the hint as error.
# error              — any other non-zero exit / failure
```

Plan 100-03 adds a `clarification_needed` entry between `tool_starvation` and `error`.

<!-- Phase 99 integration-seal test pattern (Plan 100-03 mirrors) -->

From tests/library/test_codex_curate_stop_reason.py:357-494 — the existing two integration-seal tests use `_runner_with_side_channel` (helper that writes the real generator-produced payload to the env-var path) + `_make_real_payload` (calls the toolset's real `_build_starvation_payload` to produce the payload). Plan 100-03 declares a `_make_real_clarification_payload` helper that calls toolset's real `_build_clarification_payload` (shipped by Plan 100-01) and reuses `_runner_with_side_channel` unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for clarification propagation through both wrappers</name>
  <files>tests/library/test_codex_curate_stop_reason.py</files>
  <behavior>
    Add 4 new tests, mirroring the Phase 99 seal posture at lines 357-494.

    test_uniform_clarification_propagation_curate_path (SEAL — curate wrapper):
    - Setup: empty_lib + a `LibraryToolset` instance constructed against it. Call `toolset._build_clarification_payload("What BPM range?", ["slow (90-110)", "fast (130-140)", "mixed"])` directly to get the REAL payload (the real generator, not a hand-rolled dict — mirrors Phase 99's _make_real_payload pre-check).
    - Assert generator pre-check: `payload["reason"] == "clarification_needed"`, `payload["question"] == "What BPM range?"`, `payload["choices"] == ["slow (90-110)", "fast (130-140)", "mixed"]`, `payload["tool"] == "request_clarification"`.
    - Use `_runner_with_side_channel(side_channel_payload=payload, out_payload=None)` to spawn a fake runner that writes the payload to the env-var path.
    - Invoke `curate_with_codex("ambiguous theme", lib, codex_path=sys.executable, allow_shell=True, _runner=runner)`.
    - Assert wrapper result is a `CodexCurateResult` with: stop_reason == "clarification_needed", question == "What BPM range?", choices == ["slow (90-110)", "fast (130-140)", "mixed"], track_ids == [], playlist_name is None, m3u_path is None. (error field is None OR carries a derived string; either is acceptable per Decision 3 — the canonical fields are question + choices.)
    - to_dict() round-trip: assert d["stop_reason"] == "clarification_needed", d["question"] == "What BPM range?", d["choices"] == ["slow (90-110)", "fast (130-140)", "mixed"], d["track_ids"] == [].

    test_uniform_clarification_propagation_build_set_path (SEAL — build-set wrapper):
    - Same posture, calling build_set_with_codex with brief="ambiguous brief" and a different question ("Which context?" / ["bedroom", "club", "festival"]).
    - Assert the parallel wrapper produces the same shape, proving uniform propagation across both wrappers.

    test_to_dict_serializes_clarification_shape (SEAL — dataclass shape pin):
    - Direct construction: `CodexCurateResult(theme="x", stop_reason="clarification_needed", question="Q?", choices=["A", "B", "C"])`. Call to_dict(); assert it contains the keys `stop_reason`, `question`, `choices`, AND ALSO the existing keys (theme, playlist_name, track_ids, m3u_path, json_path, rationale, error, export_path). The set of keys MUST equal the pre-100-03 set + {"question", "choices"} exactly — a future field rename/drop fails this assertion with a precise diff.
    - Cold-path serialization: `CodexCurateResult(theme="y", stop_reason="created", playlist_name="p", track_ids=["t1"])`. Assert d["question"] is None and d["choices"] is None — the cold path stays unchanged.

    test_clarification_side_channel_propagation_with_real_writer:
    - Wire the FULL chain: empty_lib, LibraryToolset, monkeypatch.setenv("VIBEMIX_STOP_REASON_FILE", tmp_path/"sr.json"), call toolset.request_clarification({"question": "Q?", "choices": ["a", "b"]}) — the REAL toolset handler writes the side-channel file.
    - Then call curate_with_codex with a fake runner that does NOT write the side-channel (it has already been written by the toolset). The wrapper's stop_reason_path is the same path. Assert the wrapper picks up the file and returns the clarification result.

    These tests are the equivalents of Phase 99's `test_uniform_propagation_curate_path` / `test_uniform_propagation_build_set_path` / `test_to_dict_serializes_starvation_shape`. They MUST FAIL on current source because:
    - CodexCurateResult does not yet have `question` + `choices` fields (TypeError on construction).
    - The wrapper does not yet have the elif clarification branch (it falls through to the existing out.json parse and returns no_playlist or empty_output).
  </behavior>
  <action>EXTEND tests/library/test_codex_curate_stop_reason.py with the 4 new tests. Place them AFTER the existing seal block (after test_to_dict_serializes_starvation_shape, currently around line 530+). Add a section header comment "## Phase 100 HARDEN-CLARIFY-03/07 seal tests" to separate the Phase 100 block visually.

Reuse existing fixtures (_empty_library, _RekordboxLibrary, _runner_with_side_channel) — they are file-scoped per the Phase 99 SUMMARY pattern. Declare a `_make_real_clarification_payload(lib, question, choices)` helper that constructs a LibraryToolset and calls its real `_build_clarification_payload` — mirrors Phase 99's `_make_real_payload`.

Forbidden: do NOT modify the existing 7 tests in this file. Their assertions are the Phase 99 regression pin. The 4 new tests are additive only.

Commit message: `test(100-03): RED — clarification propagation through curate + build_set wrappers + to_dict shape pin`.</action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate_stop_reason.py 2>&1 | tail -10</automated>
  </verify>
  <done>The 4 new tests FAIL (TypeError on construction with `question=...` kwarg / AssertionError on stop_reason being "no_playlist" or "empty_output" instead of "clarification_needed"). The 7 existing tests (Phase 99 starvation regression pin) STILL PASS. RED state for clarification, no regression for starvation. Named-path stage at commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend CodexCurateResult + add elif clarification branches in both wrappers + update _STOP_REASONS comment</name>
  <files>src/vibemix/library/codex_curate.py</files>
  <action>Edit src/vibemix/library/codex_curate.py with THREE additive edit sites:

(1) CodexCurateResult dataclass extension (lines 201-218):
Add TWO new fields at the end of the field list, AFTER `export_path: str | None = None` and BEFORE the `def to_dict` method:
```python
# Phase 100 HARDEN-CLARIFY-03: populated when the MCP-side toolset's
# request_clarification handler trips (stop_reason="clarification_needed").
# Defaults preserve the cold path — every non-clarification result keeps
# question + choices as None. CLI (Plan 100-04) + Telegram (Plan 100-05)
# read these fields to render the disambiguation prompt to the user.
question: str | None = None
choices: list[str] | None = None
```
The `to_dict()` method is unchanged — `asdict()` picks up the new fields automatically.

(2) _STOP_REASONS comment block update (lines 220-231):
Insert a new line between the `tool_starvation` entry and the `error` entry:
```
# clarification_needed — Plan 100-03 propagation: the MCP-side toolset's
#                        request_clarification handler validated args + wrote
#                        stop_reason.json with reason="clarification_needed"
#                        + question + choices. The wrapper short-circuits
#                        with those fields populated; CLI prints + exits 11,
#                        Telegram renders numbered choices.
```

(3) Two parallel elif branches — one per wrapper:

(3a) In `curate_with_codex` side-channel short-circuit (currently codex_curate.py:524-548 — the if block). REPLACE the single `if payload.get("reason") == "tool_starvation":` block with an `if / elif / except` structure:
```python
if Path(stop_reason_path).exists():
    try:
        payload = json.loads(
            Path(stop_reason_path).read_text(encoding="utf-8")
        )
        if (
            isinstance(payload, dict)
            and payload.get("reason") == "tool_starvation"
        ):
            return CodexCurateResult(
                theme=theme,
                stop_reason="tool_starvation",
                error=str(
                    payload.get("hint")
                    or "no playlist — tool starvation, no hint available"
                ),
            )
        # Phase 100-03: sibling extension of the tool_starvation branch.
        # Same side-channel file, same wrapper-side read, same short-circuit
        # posture. Question + choices propagate to CLI (exit 11) + Telegram.
        if (
            isinstance(payload, dict)
            and payload.get("reason") == "clarification_needed"
        ):
            q = payload.get("question")
            cs = payload.get("choices")
            return CodexCurateResult(
                theme=theme,
                stop_reason="clarification_needed",
                question=str(q) if isinstance(q, str) else None,
                choices=[str(c) for c in cs] if isinstance(cs, list) else None,
            )
    except (OSError, json.JSONDecodeError):
        pass  # fall through to existing parse logic
```

(Why `if / if` not `if / elif`: the existing tool_starvation block has a `return` on the success path, so a sibling `if` is functionally equivalent to `elif` — and the parallel-block split keeps the diff hunks small and the change atomic per branch. EITHER `if / if` OR `if / elif` is acceptable to the executor as long as semantic behavior is: matching either reason returns the appropriate result; missing reason / non-dict payload falls through to the existing out.json parse.)

(3b) Identical structure in `build_set_with_codex` (currently codex_curate.py:847-863) but with `theme=brief` instead of `theme=theme`. Mirror the curate-path edit verbatim except for the brief vs theme substitution.

(4) Defense: when question or choices fail their isinstance check, the wrapper still returns stop_reason="clarification_needed" but with the field set to None. This is structurally unreachable in production (Plan 100-01's `_build_clarification_payload` always populates both fields with the right shape — pinned by Plan 100-01's tests), but the defense keeps the wrapper safe against a future toolset drift. Same defense pattern as Phase 99's fallback hint string.

Forbidden:
- DO NOT modify the existing `if payload.get("reason") == "tool_starvation":` branch body (the fallback string, the return shape, the inline comments — all byte-equivalent).
- DO NOT change the `try / except (OSError, json.JSONDecodeError)` exception shape (Pitfall 4 from Phase 99 RESEARCH still applies — read INSIDE the with block, swallow file errors).
- DO NOT introduce a new dataclass `CodexBuildSetResult`. Both wrappers return CodexCurateResult per the shipped codebase shape.
- DO NOT touch the out.json parse at codex_curate.py:553-579 (curate) or :867-888 (build_set). Those are the post-side-channel-fallthrough paths.

Diff envelope: ~50-60 insertions, 0 deletions. All additive. Verify `git diff src/vibemix/library/codex_curate.py | grep -E "^-" | grep -v "^---"` returns empty (no deletions). Two parallel hunks (curate + build_set) + the dataclass field additions + the _STOP_REASONS comment.

Commit message: `feat(100-03): GREEN — clarification_needed propagation through both wrappers + CodexCurateResult.question/choices fields`.</action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate_stop_reason.py tests/library/test_codex_curate.py tests/library/test_cli_exit_codes.py tests/library/test_telegram_bridge.py tests/library/test_toolset_clarification.py tests/library/test_toolset_starvation.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 11+ tests in test_codex_curate_stop_reason.py PASS (4 new clarification + 7 existing starvation).
    No regressions in test_codex_curate.py / test_cli_exit_codes.py / test_telegram_bridge.py / test_toolset_clarification.py / test_toolset_starvation.py.
    grep -c "clarification_needed" src/vibemix/library/codex_curate.py returns at least 4 (2 branch sites + 1 _STOP_REASONS entry + 1 dataclass field comment / docstring mention). Comment-filtered count using `grep -v '^[[:space:]]*#'` returns at least 2 (the two branch sites' code).
    grep -c "question: str | None" src/vibemix/library/codex_curate.py returns exactly 1 (the new dataclass field).
    grep -c "choices: list\[str\] | None" src/vibemix/library/codex_curate.py returns exactly 1.
    grep -c 'payload.get("reason") == "clarification_needed"' src/vibemix/library/codex_curate.py returns exactly 2 (one per wrapper).
    grep -c 'payload.get("reason") == "tool_starvation"' src/vibemix/library/codex_curate.py returns at least 2 still (Phase 99 branches byte-equivalent).
    grep -c "tool_starvation" src/vibemix/library/codex_curate.py — same count as pre-100-03 baseline (Phase 99 starvation branch is byte-equivalent; new clarification branch does NOT mention tool_starvation).
    git diff --cached --name-only shows ONLY src/vibemix/library/codex_curate.py + tests/library/test_codex_curate_stop_reason.py.
    Cold-path regression smoke: `python3 -c "from vibemix.library.codex_curate import CodexCurateResult; r = CodexCurateResult(theme='x', stop_reason='created'); d = r.to_dict(); assert d['question'] is None and d['choices'] is None"` — succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stop_reason.json (file on disk) → wrapper json.loads | The wrapper reads an untrusted JSON file. `json.JSONDecodeError` is swallowed (Pitfall 4 from Phase 99 RESEARCH). The wrapper additionally type-checks the payload's `question` and `choices` fields. |
| CodexCurateResult → downstream consumers (CLI 100-04, Telegram 100-05, future GUI) | The dataclass is the cross-process propagation seam. New fields are additive — cold-path consumers see None and ignore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03-01 | Tampering | Side-channel file payload has clarification_needed reason but question/choices missing or wrong-type | mitigate | isinstance check + the defensive `None` fallback in the wrapper return. The downstream surfaces (CLI / Telegram) read the optional fields with `or` defaults. Structurally unreachable in production (Plan 100-01 always populates both fields correctly). |
| T-100-03-02 | Information disclosure | question/choices payload contains FS paths that leak through the wrapper | accept | The CONTEXT.md Decision 7 + Phase 99 precedent: CLI prints to stderr WITHOUT strip_leaks; Telegram surface (Plan 100-05) applies strip_leaks defensively. Plan 100-03's wrapper is a thin propagation layer — privacy scrub lives at the surface, not the wrapper. |
| T-100-03-03 | Spoofing | A different process writes a fake stop_reason.json to wedge the wrapper into clarification mode | mitigate | The side-channel file path is allocated inside `tempfile.TemporaryDirectory(prefix="viber-codex-...")` (mode 0o700 on macOS/Linux); env var is passed via subprocess env arg, NOT os.environ. Local-process injection attack surface is empty (same as Phase 99 T-99-06). |
| T-100-03-04 | DoS | A malformed side-channel file wedges the wrapper | mitigate | try/except (OSError, json.JSONDecodeError) + isinstance check on payload + isinstance check on question/choices. Three layers of defense-in-depth. Malformed file → fall through to existing out.json parse → existing empty_output / no_playlist branch handles. |
| T-100-03-DJ | Tampering | Disjointness violation with concurrent LiveKit / frontend sessions | mitigate | Plan 100-03 touches ONLY codex_curate.py + test_codex_curate_stop_reason.py. Both files are outside __main__.py:1353 (LiveKit zone), outside tauri/ui/, outside agent/, outside intel/. Named-path stage at every commit. |
| T-100-03-SHAPE | Tampering | A future field rename / drop in CodexCurateResult silently breaks consumers | mitigate | test_to_dict_serializes_clarification_shape pins the key set explicitly. Drift = precise field-name diff failure. |
| T-100-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. Stdlib only. |
</threat_model>

<verification>
1. **Plan 100-03 specific:** `PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate_stop_reason.py` — all 11+ tests GREEN (4 new + 7 existing Phase 99 regression).
2. **Codex-curate broader regression:** `PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate.py` — still GREEN (cold-path wrapper tests unaffected).
3. **CLI / Telegram pre-plan:** `PYTHONPATH=src python3 -m pytest -q tests/library/test_cli_exit_codes.py tests/library/test_telegram_bridge.py` — still GREEN (these consume CodexCurateResult but ignore the new optional fields when None; Plans 100-04 / 100-05 will add their own clarification branches).
4. **Phase 100 Wave 1 + 2 regression:** `PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_clarification.py tests/library/test_mcp_server_clarification.py` — still GREEN.
5. **Repo gate:** `PYTHONPATH=src python3 -m pytest -q tests/repo/test_no_seen_relaxation.py` — still GREEN. codex_curate.py is on the stop_reason whitelist; new clarification_needed references inside it are pre-authorized.
6. **Grep gates:**
   - `grep -c 'payload.get("reason") == "clarification_needed"' src/vibemix/library/codex_curate.py` = 2 (one per wrapper).
   - `grep -c 'payload.get("reason") == "tool_starvation"' src/vibemix/library/codex_curate.py` = 2 (unchanged — Phase 99 branches byte-equivalent).
   - `grep -c "question: str | None = None" src/vibemix/library/codex_curate.py` = 1.
   - `grep -c "choices: list\[str\] | None = None" src/vibemix/library/codex_curate.py` = 1.
7. **Disjointness:** `git diff --cached --name-only` shows ONLY codex_curate.py + test_codex_curate_stop_reason.py at each commit. Line 1353 of __main__.py and tauri/ui/ untouched.
</verification>

<success_criteria>
- [ ] CodexCurateResult dataclass has `question: str | None = None` and `choices: list[str] | None = None` fields (additive, default None preserves cold path).
- [ ] `curate_with_codex` side-channel short-circuit reads payload + checks for `clarification_needed` + returns `CodexCurateResult(stop_reason='clarification_needed', question=..., choices=...)`.
- [ ] `build_set_with_codex` side-channel short-circuit has the parallel branch (same structure, `theme=brief` substitution).
- [ ] `_STOP_REASONS` comment block at codex_curate.py:220-231 documents the new `clarification_needed` entry.
- [ ] `to_dict()` serializes `question` and `choices` keys (uniform GUI/JSON shape).
- [ ] 4 new tests in `test_codex_curate_stop_reason.py` PASS (RED → GREEN cycle).
- [ ] All Phase 99 starvation propagation tests STILL PASS (test_uniform_propagation_curate_path, test_uniform_propagation_build_set_path, test_to_dict_serializes_starvation_shape — byte-equivalent).
- [ ] Plan 100-01 / 100-02 regression suites STILL PASS.
- [ ] CLI + Telegram pre-100-04/05 tests STILL PASS (test_cli_exit_codes.py, test_telegram_bridge.py — the cold-path None propagation works).
- [ ] Repo gate (test_no_seen_relaxation.py) STILL PASSES.
- [ ] No git diff outside codex_curate.py + test_codex_curate_stop_reason.py.
- [ ] Both commits (RED + GREEN) land with named-path staging.
</success_criteria>

<output>
Create `.planning/phases/100-harden-clarify/100-03-SUMMARY.md` when done. Required fields: requires (list dependency chain from 100-01 + Phase 99 surfaces), provides (the two new fields + two new wrapper branches), affects (insertion counts), decisions (D-03 closure on payload shape; D-04 closure on terminal short-circuit reuse via the existing dispatch-top short-circuit; W3-fix-style minor-textual-fix documentation if grep counts inflate). Body sections per Phase 99 SUMMARY template. Critical follow-on note: Plan 100-04 will route exit 11 + stderr render on the new CodexCurateResult fields; Plan 100-05 will route Telegram format_reply branch.
</output>
