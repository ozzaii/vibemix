---
phase: 100-harden-clarify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/library/toolset.py
  - tests/library/test_toolset_clarification.py
autonomous: true
requirements: [HARDEN-CLARIFY-01]
tags: [factor-7, clarification, hardening, sibling-extension, request-clarification, single-turn, dispatch]

must_haves:
  truths:
    - "MIN_CHOICES = 2 and MAX_CHOICES = 5 module constants exist in src/vibemix/library/toolset.py (tunable for KAAN-ACTION §HARDEN-PHASE-B-CLARIFICATION-TONE)"
    - "_build_clarification_payload(question, choices) -> dict returns the discriminated-union shape {reason: 'clarification_needed', question, choices, tool: 'request_clarification'} (sibling-extension of Phase 99's _build_starvation_payload, NEVER LLM-generated)"
    - "request_clarification(args) handler validates: question is a non-empty str; choices is a list of 2-5 non-empty strs. Invalid args return {error: ..., rejected: True} WITHOUT writing self.stop_reason (the run continues)"
    - "On valid args, request_clarification writes self.stop_reason = _build_clarification_payload(question, choices) AND calls self._write_side_channel(self.stop_reason) (REUSED from Phase 99 — no helper change needed)"
    - "request_clarification handler returns the payload dict to the caller so Codex sees its own tool call succeeded (mirrors handler-result convention)"
    - "'request_clarification' entry added to dispatch table at toolset.py:1145-1163 alongside other handlers (drops in cleanly, single line addition)"
    - "Phase 99's terminal short-circuit at dispatch top (toolset.py:1142-1143) already handles 'after request_clarification, every subsequent dispatch returns stop_reason payload' — no change needed (the path is reused by construction)"
    - "request_clarification has NO track_id surface: AST-structural property — args dict is consumed only via args.get('question') + args.get('choices'); no read of args.get('track_id'); never touches self.seen / self.seen_sections / self.issued_*"
  artifacts:
    - path: "src/vibemix/library/toolset.py"
      provides: "MIN_CHOICES, MAX_CHOICES module constants + _build_clarification_payload helper + request_clarification handler + dispatch table entry"
      contains: "MIN_CHOICES: int = 2"
      contains_2: "MAX_CHOICES: int = 5"
      contains_3: "def _build_clarification_payload"
      contains_4: "def request_clarification"
      contains_5: '"request_clarification": self.request_clarification'
    - path: "tests/library/test_toolset_clarification.py"
      provides: "Failing-then-passing tests for handler validation matrix (0/1/6+ rejected, 2-5 accepted) + side-channel write on valid call + terminal short-circuit reuse + no-state-mutation on rejected args"
      contains: "def test_"
  key_links:
    - from: "src/vibemix/library/toolset.py request_clarification"
      to: "self.stop_reason"
      via: "writes _build_clarification_payload return value on valid args; never on rejected args"
      pattern: 'self\.stop_reason = self\._build_clarification_payload'
    - from: "src/vibemix/library/toolset.py request_clarification"
      to: "self._write_side_channel"
      via: "side-channel write reused from Phase 99 (helper-side discriminator-agnostic)"
      pattern: 'self\._write_side_channel\(self\.stop_reason\)'
    - from: "src/vibemix/library/toolset.py dispatch handlers"
      to: "request_clarification handler"
      via: "dispatch table entry"
      pattern: '"request_clarification": self\.request_clarification'
---

<objective>
Wave-1 foundation for Phase 100. Land the `request_clarification` handler + `MIN_CHOICES` / `MAX_CHOICES` module constants + `_build_clarification_payload` private helper + dispatch-table entry in `LibraryToolset`. This is the SIBLING extension of Phase 99's `_build_starvation_payload` + threshold-trip write — same `self.stop_reason` writer, same `self._write_side_channel` reader, same terminal short-circuit at dispatch top. Phase 99 paid for the infrastructure; Plan 100-01 drops in a second producer.

Purpose: HARDEN-CLARIFY-01 closure. Without this plan, Codex has no way to ask the user for disambiguation — it silently picks one heuristic when the theme is materially ambiguous ("uplifting" without context). With this handler, Codex calls `request_clarification(question, choices)` and the run terminates with `stop_reason="clarification_needed"`, propagated through the seam Phase 99 wired (Wave 2-4 of this phase consume that propagation).

Output:
- `src/vibemix/library/toolset.py` — `MIN_CHOICES`/`MAX_CHOICES` constants + `_build_clarification_payload` private method + `request_clarification` public handler + dispatch-table entry.
- `tests/library/test_toolset_clarification.py` — NEW file, validation-matrix + side-channel write + terminal short-circuit reuse tests (RED → GREEN cycle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-harden-clarify/100-CONTEXT.md
@.planning/phases/99-harden-retry/99-04-SUMMARY.md
@.planning/phases/99-harden-retry/99-VERIFICATION.md
@src/vibemix/library/toolset.py
@tests/library/test_toolset_starvation.py
@tests/repo/test_no_seen_relaxation.py

<interfaces>
<!-- Key shipped surface from Phase 99 the handler builds upon. Extracted from current source — executor uses these directly, no codebase exploration needed. -->

From src/vibemix/library/toolset.py (Phase 99 shipped state, line numbers verified at planning time):

```python
# Module-level (line 75) — Phase 99 precedent for the new MIN_CHOICES/MAX_CHOICES siblings:
TOOL_STARVATION_THRESHOLD: int = 3

# Instance attr (line 129) — the single write target for both starvation AND clarification:
self.stop_reason: dict[str, Any] | None = None

# Private helper (line 1022) — Phase 99 pattern Plan 100-01 mirrors:
def _build_starvation_payload(self, last_tool: str, args: dict[str, Any]) -> dict[str, Any]:
    """...deterministic three-case hint..."""
    return {
        "reason": "tool_starvation",
        "hint": hint,
        "tool": last_tool,
        "consecutive": self._consecutive_empties,
    }

# Private side-channel writer (line 1075) — Plan 100-01 REUSES unchanged. Phase 99's
# forward-compat comment at line 1096-1098 explicitly authorizes the reuse:
#   "Forward-compat (Phase 100): the payload's ``reason`` field is the
#    discriminator. Phase 100 will reuse the same file with
#    reason='clarification_needed'; this writer is unchanged."
def _write_side_channel(self, payload: dict[str, Any]) -> None:
    path = os.environ.get("VIBEMIX_STOP_REASON_FILE")
    if not path:
        return
    try:
        with open(path, "w", encoding="utf-8") as f:
            json.dump(payload, f)
    except (OSError, TypeError, ValueError):
        return

# Dispatch table (line 1145-1163) — the single insertion point. Drop in
# "request_clarification": self.request_clarification alongside other handlers:
handlers: dict[str, Callable[[dict[str, Any]], dict[str, Any]]] = {
    "search_vibe": self.search_vibe,
    "get_track_features": self.get_track_features,
    # ... 14 other entries ...
    "export_cues": self.export_cues,
}

# Terminal short-circuit (line 1142-1143) — REUSED unchanged. Once
# request_clarification writes self.stop_reason, every subsequent dispatch()
# returns the terminal echo. This is exactly what tool_starvation gets today.
if self.stop_reason is not None:
    return {"error": "tool_starvation", "stop_reason": dict(self.stop_reason)}
```

NOTE on terminal echo error string: Phase 99's short-circuit hardcodes `"error": "tool_starvation"` in its return payload. For Phase 100, after a `request_clarification` trip, the same short-circuit returns the literal string `"tool_starvation"` as the error key value, but the actual `self.stop_reason["reason"]` correctly carries `"clarification_needed"` in the `stop_reason` key of that dict. Plan 100-01 does NOT change the short-circuit (out of scope — Wave 3-4 wrappers read `payload.get("reason")`, never the literal "error" key). Document this as a known cosmetic in the handler docstring; if a future plan wants a discriminated error string, it's an independent decision.

From tests/repo/test_no_seen_relaxation.py:
- `BASELINE_SEEN_ADD_COUNT = 2` — Phase 100 MUST NOT increase this. `request_clarification` has zero `self.seen.add` sites.
- `STOP_REASON_WHITELIST` includes `src/vibemix/library/toolset.py` — Plan 100-01's edits stay within the whitelist.
- `_consecutive_empties` single-owner gate — Plan 100-01 does NOT touch `_consecutive_empties` (clarification is NOT counter-driven; it's a single-trip LLM-initiated path per CONTEXT.md Decision 4 rationale).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for clarification handler</name>
  <files>tests/library/test_toolset_clarification.py</files>
  <behavior>
    Validation matrix (rejected — handler returns dict with rejected=True, self.stop_reason stays None):
    - choices is empty list → rejected
    - choices is 1-element list → rejected
    - choices is 6-element list → rejected
    - choices is 10-element list → rejected
    - choices is not a list (string, dict, None) → rejected
    - choices contains a non-string element → rejected
    - choices contains an empty string → rejected
    - question is empty string → rejected
    - question is non-string (None, int, list) → rejected
    Validation matrix (accepted — handler writes self.stop_reason, returns payload):
    - choices is 2-element list of non-empty strs → accepted; self.stop_reason is set; returned dict has clarification_needed=True
    - choices is 3-element list → accepted
    - choices is 5-element list → accepted
    Stop-reason shape on accept (the discriminated-union per CONTEXT.md Decision 3):
    - self.stop_reason["reason"] == "clarification_needed"
    - self.stop_reason["question"] == question
    - self.stop_reason["choices"] == choices (list equality; preserves order)
    - self.stop_reason["tool"] == "request_clarification"
    Side-channel write reuse (Phase 99 _write_side_channel is REUSED):
    - With monkeypatch.setenv("VIBEMIX_STOP_REASON_FILE", tmp_path/"sr.json") set, valid call writes JSON to that path; parsed dict equals self.stop_reason.
    - Without VIBEMIX_STOP_REASON_FILE set, valid call still writes self.stop_reason in-process but no file is created (side-channel is a silent no-op — Phase 99 contract preserved).
    Terminal short-circuit reuse (Phase 99 short-circuit applies to clarification too):
    - After a valid request_clarification, dispatch("search_vibe", {"query": "x"}) returns the terminal echo dict ({"error": "tool_starvation", "stop_reason": {...with reason='clarification_needed'...}}) without invoking the search_vibe handler. This proves Phase 99's short-circuit handles BOTH terminal reasons; Plan 100-01 reuses it by construction.
    No-state-mutation on reject:
    - Rejected calls do NOT write self.stop_reason (stays None).
    - Rejected calls do NOT touch self.seen / self.seen_sections (per Cardinal Invariant #2).
    - Rejected calls do NOT increment self._consecutive_empties (clarification path is independent of counter — Phase 99 invariant).
    Module-constant pin:
    - from vibemix.library.toolset import MIN_CHOICES, MAX_CHOICES
    - assert MIN_CHOICES == 2 and MAX_CHOICES == 5 (CONTEXT.md Decision 2 lock).
  </behavior>
  <action>Create NEW test file tests/library/test_toolset_clarification.py. Mirror the posture of tests/library/test_toolset_starvation.py (the Phase 99 sibling): direct LibraryToolset construction with fake embedder/store/library fixtures, no Codex spawn, no MCP server. Reuse helper fixtures from test_toolset_starvation.py if they are import-clean; otherwise re-declare locally (the existing file uses fakes like _RekordboxLibrary / fake embedder — copy that shape). Write 12-15 atomic tests covering the validation matrix, accept-path shape, side-channel reuse with monkeypatch.setenv, terminal short-circuit reuse on next dispatch call, and the no-state-mutation rejected-args properties. Tests should FAIL on current source (handler does not exist yet). Commit message: `test(100-01): RED — request_clarification handler args validation + payload shape + short-circuit reuse`.</action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_clarification.py 2>&1 | tail -5</automated>
  </verify>
  <done>tests/library/test_toolset_clarification.py exists with 12+ tests; pytest run shows ALL tests FAILING (AttributeError on missing request_clarification / ImportError on missing MIN_CHOICES — the expected RED state). Commit lands cleanly with named-path stage (`git add tests/library/test_toolset_clarification.py`, NEVER `git add -A`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement MIN_CHOICES/MAX_CHOICES + _build_clarification_payload + request_clarification + dispatch entry</name>
  <files>src/vibemix/library/toolset.py</files>
  <action>Edit src/vibemix/library/toolset.py with FIVE additive insertions, in this exact order:

(1) Module constants — add immediately after TOOL_STARVATION_THRESHOLD (around line 76). Use the same comment-block convention Phase 99 used for TOOL_STARVATION_THRESHOLD (cite Phase 100 Decision 2 lock + KAAN-ACTION §HARDEN-PHASE-B-CLARIFICATION-TONE tunability):
```
MIN_CHOICES: int = 2
MAX_CHOICES: int = 5
```
With a 4-5 line comment block citing CONTEXT.md Decision 2 + the Hick's-law rationale + ear-pass tunability. Place AFTER the TOOL_STARVATION_THRESHOLD block (line 75) and BEFORE the `_EmbeddingProvider` Protocol (line 78).

(2) Private helper `_build_clarification_payload` — add immediately after `_build_starvation_payload` (around line 1022-1073). Mirror its docstring posture (3-paragraph: purpose, content, security/grounding note). Signature: `def _build_clarification_payload(self, question: str, choices: list[str]) -> dict[str, Any]`. Body returns the discriminated-union dict per CONTEXT.md Decision 3:
```
{
    "reason": "clarification_needed",
    "question": question,
    "choices": list(choices),  # defensive copy — prevents downstream mutation
    "tool": "request_clarification",
}
```
Docstring MUST cite: CONTEXT.md Decision 3 (payload shape); forward-compat note that this is the sibling of `_build_starvation_payload` and the writer (`_write_side_channel`) is reused unchanged; security/grounding: question + choices originate from Codex's tool-call args, not arbitrary user input, and the wrapper-side `strip_leaks` covers any FS-path leak defensively.

(3) Public handler `request_clarification` — add immediately after `_build_clarification_payload`. Signature: `def request_clarification(self, args: dict[str, Any]) -> dict[str, Any]` (Decision 1 lock). Logic:
- Read `question = args.get("question")` and `choices = args.get("choices")`.
- Validate question: must be a `str` AND `.strip()` non-empty. Else return `{"error": "request_clarification: 'question' must be a non-empty string", "rejected": True}` — DO NOT write self.stop_reason.
- Validate choices: must be a `list`; length in `[MIN_CHOICES, MAX_CHOICES]` inclusive; every element must be a `str` AND `.strip()` non-empty. Else return `{"error": "request_clarification: 'choices' must be a list of 2-5 non-empty strings", "rejected": True}` — DO NOT write self.stop_reason.
- On valid args: `self.stop_reason = self._build_clarification_payload(question, choices)` then `self._write_side_channel(self.stop_reason)` then `return {"clarification_needed": True, "question": question, "choices": list(choices)}` (so Codex sees its call succeeded).
- Handler must NOT touch self.seen / self.seen_sections / self.issued_* / self._consecutive_empties. Add an inline 2-line comment: "Cardinal Invariant #2: no track_id surface. Phase 100 AST gate at tests/library/test_request_clarification_no_track_surface.py pins this." NOTE: that AST gate ships in Plan 100-06; the comment forward-references it.
- Docstring MUST cite: CONTEXT.md Decisions 1 (signature), 2 (length bounds), 3 (payload shape), 4 (terminal short-circuit reuse), and 8 (test posture). Document the rejection-without-state-mutation contract explicitly.

(4) Dispatch table entry — in the `handlers` dict at line 1145-1163, add `"request_clarification": self.request_clarification,` alphabetically (between "quote_moment" and "retrieve_dj_knowledge" if alphabetical, or at the bottom — whichever matches the existing posture). The terminal short-circuit at line 1142-1143 already handles "after stop_reason set, every subsequent dispatch returns terminal echo" — verified by Phase 99's test_toolset_starvation.py and reused here per Decision 4.

(5) `__all__` extension (line 1341) — add `"MIN_CHOICES"` and `"MAX_CHOICES"` to the existing `__all__` tuple so they re-export cleanly. The tuple becomes `["TOOL_CALL_TIMEOUT_S", "TOOL_STARVATION_THRESHOLD", "MIN_CHOICES", "MAX_CHOICES", "LibraryToolset"]`.

Diff envelope: ~70 insertions, 0 deletions. All additive. Verify by `git diff src/vibemix/library/toolset.py | grep -E "^-" | grep -v "^---"` returns empty (no deletions).

Forbidden:
- DO NOT modify `_build_starvation_payload`, `_write_side_channel`, `_is_empty_or_error`, or the existing `dispatch()` body (NEVER touch the terminal short-circuit at line 1142-1143 — it works for BOTH terminal reasons by construction).
- DO NOT add an `elif name == "request_clarification"` branch anywhere in `dispatch()` — the dispatch table handles it.
- DO NOT add a counter increment / decrement / reset on the clarification path — it's independent of `_consecutive_empties` per CONTEXT.md Decision 4.
- DO NOT change the literal `"error": "tool_starvation"` in the short-circuit return at line 1143 (out of scope cosmetic — Wave 3-4 wrappers read `payload.get("reason")`).

Commit message: `feat(100-01): GREEN — request_clarification handler + MIN/MAX choices + dispatch entry`.</action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_clarification.py tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py tests/library/test_toolset.py tests/repo/test_no_seen_relaxation.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    All tests from Task 1's test_toolset_clarification.py PASS (RED → GREEN).
    Phase 99 regression suites (test_toolset_starvation.py, test_toolset_starvation_concurrency.py, test_toolset.py) STILL PASS (no regressions — the dispatch top-of-loop short-circuit is unchanged).
    tests/repo/test_no_seen_relaxation.py still PASSES — BASELINE_SEEN_ADD_COUNT stays at 2 (request_clarification has no self.seen.add); _consecutive_empties still confined to toolset.py only; stop_reason whitelist unchanged (toolset.py was already on the whitelist).
    grep -c "MIN_CHOICES" src/vibemix/library/toolset.py returns at least 3 (constant def + 1+ uses in handler + 1+ uses in helper docstring).
    grep -c "MAX_CHOICES" src/vibemix/library/toolset.py returns at least 3.
    grep -c "_build_clarification_payload" src/vibemix/library/toolset.py returns exactly 2 (def + 1 call from request_clarification).
    grep -c "def request_clarification" src/vibemix/library/toolset.py returns exactly 1.
    grep -c '"request_clarification": self.request_clarification' src/vibemix/library/toolset.py returns exactly 1 (the dispatch entry).
    grep -c "clarification_needed" src/vibemix/library/toolset.py returns at least 2 (the payload literal + at least 1 docstring/comment mention). The Plan 100-06 AST gate will pin this more strictly; loose count here.
    git diff --cached --name-only at commit time shows ONLY src/vibemix/library/toolset.py + tests/library/test_toolset_clarification.py (per concurrent-session named-path discipline — NEVER git add -A).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex MCP child → LibraryToolset.request_clarification | Codex's MCP harness calls into the handler with `args = {"question": ..., "choices": [...]}`. The handler is the validation boundary — invalid args return rejected=True without state mutation. |
| LibraryToolset.request_clarification → self.stop_reason | Single-writer analog of Cardinal Invariant #1. On valid args, the handler is the SECOND writer of `self.stop_reason` (Phase 99's threshold-trip block is the first). Both write sites are inside `LibraryToolset` (whitelist-confined per tests/repo/test_no_seen_relaxation.py). |
| request_clarification → self._write_side_channel | Reused Phase 99 helper. Side-channel file write is best-effort (try/except OSError swallow). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | request_clarification args dict (Codex-controlled) | mitigate | Strict type + length validation BEFORE any state mutation. Empty/non-list/wrong-length choices and empty/non-str question rejected with `rejected=True` and NO `self.stop_reason` write. Pinned by Task 1 validation matrix. |
| T-100-02 | Information disclosure | question/choices payload leaks into self.stop_reason (later side-channel + CLI/Telegram surfaces) | accept | Codex's tool-call args originate from the LLM's reasoning over the user's theme — not arbitrary user input. Downstream surfaces (Wave 4-5 — CLI + Telegram) defensively apply `strip_leaks` per Decision 7. Plan 100-01 does NOT surface the payload itself; it only writes the in-process attribute + side-channel file. |
| T-100-03 | Denial of service | LLM calls request_clarification repeatedly to wedge the run | mitigate | Phase 99's terminal short-circuit at dispatch line 1142-1143 fires on EVERY call after `self.stop_reason` is set. The first valid `request_clarification` writes the terminal payload; the second call returns the cached echo without invoking the handler. Run cannot "loop" on clarification. |
| T-100-04 | Spoofing | request_clarification handler writes `reason="tool_starvation"` accidentally (vs `clarification_needed`) | mitigate | Discriminator is hardcoded in `_build_clarification_payload`; tests in Task 1 pin `self.stop_reason["reason"] == "clarification_needed"`. Cosmetic note: Phase 99's short-circuit return still says `"error": "tool_starvation"` literally — but the wrapper-side reads `payload.get("reason")`, never the error key. Documented as known-cosmetic in handler docstring. |
| T-100-05 | Elevation of privilege | request_clarification accepts a `track_id` arg and elevates it into `self.seen` | mitigate (by construction) | Handler signature is `(self, args: dict)` but the body reads ONLY `args.get("question")` and `args.get("choices")`. NO read of `args.get("track_id")` / `args.get("trackId")` / etc. AST gate ships in Plan 100-06; Task 1 includes a behavioral check that `self.seen` size is unchanged after a valid call. |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. All stdlib (json, os already imported at top of toolset.py per Plan 99-04). No package legitimacy gate required. |
</threat_model>

<verification>
Verification gates for Plan 100-01:

1. **Phase 100 plan-specific (must PASS after Task 2):**
   `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_clarification.py` — all 12+ tests GREEN.

2. **Phase 99 regression (must remain GREEN — proves no breakage of the sibling-extended seam):**
   `PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py tests/library/test_codex_curate_stop_reason.py tests/library/test_cli_exit_codes.py tests/library/test_telegram_bridge.py` — all 53+ tests GREEN.

3. **Repo gate (must remain GREEN — Cardinal Invariants #1 + #2):**
   `PYTHONPATH=src python3 -m pytest -q tests/repo/test_no_seen_relaxation.py` — all 3 tests GREEN.
   - `test_seen_writes_unchanged_from_baseline` — confirms `self.seen.add` count stays at 2 (request_clarification has zero self.seen.add).
   - `test_consecutive_empties_single_writer` — confirms `_consecutive_empties` stays confined to toolset.py.
   - `test_stop_reason_writes_confined_to_toolset` — toolset.py is on the whitelist, so adding `clarification_needed` references inside it does not trip the gate.

4. **Grep-gate hygiene (informational; deviations documented in SUMMARY if any inflate):**
   - `grep -v '^[[:space:]]*#' src/vibemix/library/toolset.py | grep -c "request_clarification"` — expected: at least 3 (handler def + dispatch table entry + helper return value).
   - `grep -v '^[[:space:]]*#' src/vibemix/library/toolset.py | grep -c "clarification_needed"` — expected: 1 (the payload literal in `_build_clarification_payload`). Comment-only mentions are filtered out so the load-bearing code count is 1.

5. **Disjointness contract (CRITICAL — concurrent LiveKit + frontend sessions):**
   - `git diff --cached --name-only` at each commit shows ONLY `src/vibemix/library/toolset.py` + `tests/library/test_toolset_clarification.py`.
   - `git diff src/vibemix/library/toolset.py | grep -E "^@@"` shows hunks ONLY in the 70-1340 line range (well above __main__.py:1353 LiveKit zone, but irrelevant — this plan does not touch __main__.py at all).
   - NEVER `git add -A`; NEVER `git add .` — always named paths.

6. **Forward-compat verification (Phase 100 Waves 2-5 depend on the surface):**
   - `python3 -c "from vibemix.library.toolset import LibraryToolset, MIN_CHOICES, MAX_CHOICES; assert MIN_CHOICES == 2 and MAX_CHOICES == 5; assert hasattr(LibraryToolset, 'request_clarification'); assert hasattr(LibraryToolset, '_build_clarification_payload')"` — module imports cleanly + surface attrs present.
</verification>

<success_criteria>
Plan 100-01 SHIPPED when:

- [ ] `tests/library/test_toolset_clarification.py` exists with 12+ atomic tests covering validation matrix (rejected + accepted shapes), payload shape, side-channel write reuse, terminal short-circuit reuse on next dispatch, and no-state-mutation rejected-args properties.
- [ ] All tests in `test_toolset_clarification.py` PASS on current source (RED → GREEN cycle complete).
- [ ] `MIN_CHOICES = 2` and `MAX_CHOICES = 5` module constants exist in `src/vibemix/library/toolset.py` with 4-5 line citation comment block (CONTEXT.md Decision 2 + KAAN-ACTION tunability).
- [ ] `_build_clarification_payload(question, choices)` private method exists, returns the discriminated-union shape per CONTEXT.md Decision 3 (`{reason: 'clarification_needed', question, choices, tool: 'request_clarification'}`), uses `list(choices)` defensive copy.
- [ ] `request_clarification(args)` handler exists with strict validation (rejection without state mutation; accept writes `self.stop_reason` + calls `self._write_side_channel`), returns clarification_needed=True payload to Codex.
- [ ] Dispatch table at toolset.py:1145-1163 has the `"request_clarification": self.request_clarification` entry.
- [ ] All Phase 99 regression tests still PASS (test_toolset_starvation.py, test_codex_curate_stop_reason.py, test_cli_exit_codes.py, test_telegram_bridge.py, test_no_seen_relaxation.py).
- [ ] No `self.seen.add` count drift (still 2 per Plan 99-05's baseline).
- [ ] No `_consecutive_empties` leak outside toolset.py.
- [ ] No git diff outside `src/vibemix/library/toolset.py` + `tests/library/test_toolset_clarification.py` at any commit (disjointness contract — concurrent sessions on LiveKit + frontend honored).
- [ ] Both commits (RED + GREEN) lands on the branch with named-path staging (never `git add -A`).
- [ ] Forward-compat verification one-liner passes (module imports + surface attrs present).
</success_criteria>

<output>
Create `.planning/phases/100-harden-clarify/100-01-SUMMARY.md` when done. Required fields:
- frontmatter: `phase`, `plan`, `subsystem`, `tags`, `requires`, `provides`, `affects` (with insertion counts), `tech-stack`, `key-files`, `decisions`, `metrics` (duration, completed, tasks_completed, files_modified, tests_added, tests_passing, regressions=0).
- Body sections: What Shipped (A. constants, B. helper, C. handler, D. dispatch entry, E. tests), Exact Insertion-Site Line Numbers (a small table for Plan 100-03 / 100-04 / 100-05 / 100-06 / 100-07 to reference), Grep-Gate Verification (the verification block above as commands + actual outputs), Test Outcomes (before/after deltas), Cardinal Invariants Re-Verified, Threat Register disposition, Deviations from Plan (if any), Notes for Downstream Plans (Plan 100-02 will FastMCP-expose the handler; Plan 100-03 will wrapper-propagate; Plan 100-06 will pin the no-track-surface AST gate; the side-channel write is REUSED by Plan 100-03), Commits (RED hash + GREEN hash), Self-Check checklist.

Forbidden in SUMMARY: emojis, polite-AI hedging like "we ran into some issues but figured it out", scope-reduction phrases ("v1", "placeholder", "for now"). If a deviation happened (e.g. minor-textual-fix on a grep gate), document it explicitly per the Phase 99 SUMMARY precedent at .planning/phases/99-harden-retry/99-04-SUMMARY.md § Deviations from Plan.
</output>
