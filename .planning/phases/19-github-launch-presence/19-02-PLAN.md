---
phase: 19-github-launch-presence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - NOTICE
  - TRADEMARKS.md
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/ISSUE_TEMPLATE/new_controller.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/pull_request_template.md
  - scripts/dist/gen_notice.py
  - tests/repo/test_oss_hygiene.py
autonomous: true
requirements:
  - GH-13
  - GH-14
  - GH-15
user_setup: []

must_haves:
  truths:
    - "Every OSS-hygiene file required by GH-14 exists at the canonical path: LICENSE (already on disk), NOTICE, TRADEMARKS.md, SECURITY.md, CODE_OF_CONDUCT.md, CONTRIBUTING.md"
    - "CONTRIBUTING.md mandates DCO sign-off and documents 3 contribution paths (bug-fix, controller-mapping, prompt-template)"
    - "Three issue templates exist under .github/ISSUE_TEMPLATE/ (bug / feature / new-controller) and a config.yml caps blank-issue creation"
    - "NOTICE is generated from pyproject.toml + tauri/ui/package.json + tauri/src-tauri/Cargo.toml dep manifests (not hand-maintained)"
    - "LICENSE on disk is canonical Apache-2.0 boilerplate (checksum gate)"
  artifacts:
    - path: CONTRIBUTING.md
      provides: "DCO sign-off rule + 3-path contribution guide"
      min_lines: 80
      contains: "Signed-off-by"
    - path: CODE_OF_CONDUCT.md
      provides: "Contributor Covenant 2.1 verbatim with ozai@bravoh.com contact"
      contains: "Contributor Covenant"
    - path: SECURITY.md
      provides: "Vuln disclosure to security@bravoh.com + 90-day timeline"
      contains: "security@bravoh.com"
    - path: NOTICE
      provides: "Third-party dep + license aggregate"
      contains: "Apache License"
    - path: TRADEMARKS.md
      provides: "Nominative fair-use rules for Pioneer / Apple / Microsoft / Google + 'vibemix is a Bravoh trademark'"
      contains: "trademark"
    - path: .github/ISSUE_TEMPLATE/bug_report.yml
      provides: "Bug template with version + OS + controller + events.jsonl attachment"
    - path: .github/ISSUE_TEMPLATE/feature_request.yml
      provides: "Feature template with problem-statement / solution / scope assessment"
    - path: .github/ISSUE_TEMPLATE/new_controller.yml
      provides: "Controller-add template with USB VID/PID + MIDI map link + test-willingness"
    - path: scripts/dist/gen_notice.py
      provides: "NOTICE regenerator reading the three dep manifests"
      exports: ["main()"]
  key_links:
    - from: CONTRIBUTING.md
      to: src/vibemix/midi/profiles/
      via: "Controller-mapping path link"
      pattern: "midi/profiles"
    - from: CONTRIBUTING.md
      to: src/vibemix/prompts/
      via: "Prompt-template path link"
      pattern: "src/vibemix/prompts"
    - from: scripts/dist/gen_notice.py
      to: pyproject.toml
      via: "tomllib.load"
      pattern: "tomllib|toml"
---

<objective>
Ship every OSS-hygiene artifact GitHub treats as load-bearing for an open-source project: CONTRIBUTING.md (DCO + 3 paths), CODE_OF_CONDUCT.md (Contributor Covenant 2.1), SECURITY.md, NOTICE (generated from dep manifests, not hand-typed), TRADEMARKS.md, three issue templates + a PR template, and an issue-template `config.yml` that disables blank issues. Add a CI test that gates the presence + shape of every one of them.

Purpose: GH-13/14/15 are checkbox requirements but the failure mode is "first contributor opens a PR and there's no DCO requirement / no issue template / no code-of-conduct" — that's the AI-slop equivalent for an OSS launch. These files are what turn a code dump into a project people respect.

Output: 5 root-level OSS hygiene files + 4 GitHub template files + 1 NOTICE generator script + 1 CI test file. No new pip / npm deps (uses stdlib `tomllib` + existing `pyyaml` from Plan 19-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-github-launch-presence/19-CONTEXT.md

# CONTEXT Area 4 (OSS Hygiene Files) is load-bearing.
# Contributor Covenant 2.1 canonical source:
# https://www.contributor-covenant.org/version/2/1/code_of_conduct/
# Apache-2.0 NOTICE format spec: https://www.apache.org/licenses/LICENSE-2.0
# DCO text: https://developercertificate.org/

@LICENSE
@pyproject.toml
@tauri/ui/package.json
</context>

<interfaces>
<!-- pyproject.toml dep extraction (Python 3.11+ stdlib tomllib) -->

From pyproject.toml (extract pattern):
```
[project]
dependencies = [
  "livekit-agents>=1.5.8",
  "google-genai>=2.0.1",
  ...
]
```

From tauri/ui/package.json (extract pattern):
```json
{
  "dependencies": { "@tauri-apps/api": "^2.11" },
  "devDependencies": { "vite": "^6", ... }
}
```

NOTICE rendering format (Apache convention):
```
vibemix
Copyright 2026 Bravoh

This product includes software developed by:
- livekit-agents (Apache-2.0) — https://github.com/livekit/agents-py
- google-genai (Apache-2.0) — https://github.com/googleapis/python-genai
...
```

License-URL lookup is best-effort: if pyproject doesn't pin a license string,
fall back to "see upstream" placeholder. Manual review is expected post-generation
(NOTICE is "refreshed only on dep changes" per CONTEXT Area 4).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apache-2.0 ecosystem files — CODE_OF_CONDUCT.md + SECURITY.md + TRADEMARKS.md + NOTICE + gen_notice.py + LICENSE checksum gate</name>
  <files>CODE_OF_CONDUCT.md, SECURITY.md, TRADEMARKS.md, NOTICE, scripts/dist/gen_notice.py, tests/repo/test_oss_hygiene.py</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 4)
    - LICENSE (verify Apache-2.0 text — checksum gate)
    - pyproject.toml (dep list source)
    - tauri/ui/package.json (Node dep list)
    - tauri/src-tauri/Cargo.toml (Rust dep list; skip if absent — handle gracefully)
    - https://www.contributor-covenant.org/version/2/1/code_of_conduct/ (Covenant 2.1 text — copy verbatim)
  </read_first>
  <behavior>
    - Test 1: `CODE_OF_CONDUCT.md` exists, contains "Contributor Covenant", "2.1", and the contact `ozai@bravoh.com`.
    - Test 2: `SECURITY.md` exists, contains `security@bravoh.com`, the phrase "90 days", and a `## Supported Versions` heading.
    - Test 3: `TRADEMARKS.md` exists, contains "vibemix" + "Bravoh" + "Pioneer" + "Apple" + "Microsoft" + "Google" + "nominative fair use".
    - Test 4: `NOTICE` exists, starts with "vibemix" + copyright line, contains "Apache License", and references at least 5 dep names from pyproject.toml.
    - Test 5: `LICENSE` SHA-256 matches one of the canonical Apache-2.0 boilerplate digests (we accept multiple known checksums — the canonical Apache 2.0 text + the "appendix-stripped" variant — fail loudly with `expected one of [...]` if neither matches).
    - Test 6: `scripts/dist/gen_notice.py` exists, has SPDX header, executes `--help` without error.
    - Test 7: Running `python scripts/dist/gen_notice.py --check` (read-only diff mode) exits 0 against the committed NOTICE (proof NOTICE is up-to-date with current dep manifests).
  </behavior>
  <action>
    1. Create `CODE_OF_CONDUCT.md` with the Contributor Covenant 2.1 text verbatim from the canonical URL. Replace `[INSERT CONTACT METHOD]` with `ozai@bravoh.com`. Bottom attribution line stays canonical ("This Code of Conduct is adapted from the Contributor Covenant, version 2.1, available at https://www.contributor-covenant.org/version/2/1/code_of_conduct.html").

    2. Create `SECURITY.md` with the structure:
       - `## Reporting a Vulnerability` — instructions to email `security@bravoh.com`. PGP key fingerprint placeholder block: `<!-- TODO: paste PGP fingerprint when Bravoh ops generates one -->` (Kaan-action, surfaces in SUMMARY).
       - `## Disclosure Timeline` — 90-day coordinated disclosure window from initial confirmation.
       - `## Bug Bounty` — "No formal program for v1. Researchers credited in release notes."
       - `## Supported Versions` — table with `v0.1.x` row marked "supported" + a sentence "Older pre-release tags are not maintained."

    3. Create `TRADEMARKS.md` with the structure:
       - Opening line: "**vibemix** is a trademark of Bravoh."
       - `## Permitted Use` block listing nominative fair-use scenarios (review, comparison, integration mention).
       - `## Prohibited Use` block (no derivative product branding "vibemix Pro" / "vibemix for X", no logo modification, no implied endorsement).
       - `## Third-Party Marks` block listing Pioneer DJ + Numark + Hercules + Native Instruments + Apple + Microsoft + Google with the standard nominative fair-use disclaimer.
       - Final sentence: questions to `legal@bravoh.com`.

    4. Create `scripts/dist/gen_notice.py` (SPDX header, `from __future__ import annotations`). Uses stdlib `tomllib` (Python 3.11+) to parse `pyproject.toml`. Uses `json` for `tauri/ui/package.json`. Uses `tomllib` again for `tauri/src-tauri/Cargo.toml` if it exists; otherwise skip with a warning to stderr. For each parsed dep produces one bullet of the form `- <name> (<license-or-'see upstream'>) — <homepage-or-'see PyPI/npm/crates.io'>`. CLI has two modes: `python scripts/dist/gen_notice.py` writes `NOTICE`; `python scripts/dist/gen_notice.py --check` reads the on-disk NOTICE and diffs against the freshly-generated content — exit 0 if identical, exit 1 with diff if not. The committed NOTICE is the canonical artifact; the script is the regen path.

    5. Run the generator once to produce `NOTICE`. The first two lines are pinned:
       ```
       vibemix
       Copyright 2026 Bravoh

       This product includes software developed by various open-source contributors:
       ```
       Followed by the auto-generated dep bullet list.

    6. Verify `LICENSE` is the canonical Apache-2.0 boilerplate. Compute its SHA-256: `python -c "import hashlib, pathlib; print(hashlib.sha256(pathlib.Path('LICENSE').read_bytes()).hexdigest())"`. Record the value in `tests/repo/test_oss_hygiene.py` as an `ACCEPTED_LICENSE_SHA256S` frozenset (allow multiple values to accommodate trailing-newline variants). If the on-disk LICENSE has any non-canonical edits, regenerate from https://www.apache.org/licenses/LICENSE-2.0.txt and re-record the SHA.

    7. Create `tests/repo/test_oss_hygiene.py` with the 7 tests per `<behavior>` above. Use the `REPO_ROOT = Path(__file__).resolve().parent.parent.parent` pattern. The `--check` test for gen_notice runs via `subprocess.run([sys.executable, "scripts/dist/gen_notice.py", "--check"], cwd=REPO_ROOT, capture_output=True)`; assert returncode 0.

    8. Run `pytest tests/repo/test_oss_hygiene.py -x` — 7 tests pass.
  </action>
  <verify>
    <automated>pytest tests/repo/test_oss_hygiene.py -k "covenant or security or trademark or notice or license_sha or gen_notice"</automated>
  </verify>
  <acceptance_criteria>
    - `ls CODE_OF_CONDUCT.md SECURITY.md TRADEMARKS.md NOTICE LICENSE` exits 0.
    - `grep -c "Contributor Covenant" CODE_OF_CONDUCT.md` ≥ 1.
    - `grep -c "security@bravoh.com" SECURITY.md` ≥ 1.
    - `grep -c "nominative fair use" TRADEMARKS.md` ≥ 1.
    - `head -1 NOTICE` reads `vibemix`.
    - `python scripts/dist/gen_notice.py --check` exits 0.
    - `pytest tests/repo/test_oss_hygiene.py -k "covenant or security or trademark or notice or license_sha or gen_notice" -x` exits 0 with 7 tests passing.
  </acceptance_criteria>
  <done>
    Four OSS hygiene markdown files land at repo root with the locked text from CONTEXT Area 4; NOTICE is generated from dep manifests (not hand-typed); LICENSE on disk is verified canonical Apache-2.0 via SHA-256 gate; `gen_notice.py` is the documented regen path; `--check` mode lets CI verify NOTICE freshness without a human edit. Kaan-action surfaced in SUMMARY: paste PGP fingerprint into SECURITY.md when Bravoh ops generates one.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CONTRIBUTING.md (DCO + 3 paths) + 3 issue templates + PR template + config.yml + CI gate</name>
  <files>CONTRIBUTING.md, .github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/ISSUE_TEMPLATE/new_controller.yml, .github/ISSUE_TEMPLATE/config.yml, .github/pull_request_template.md, tests/repo/test_oss_hygiene.py (extended)</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 4 — CONTRIBUTING + issue-template specs)
    - src/vibemix/midi/profiles/ — list of 10 existing controller JSONs; link from CONTRIBUTING controller path
    - src/vibemix/prompts/ — directory referenced from CONTRIBUTING prompt-template path
    - https://github.com/github/docs/blob/main/.github/ISSUE_TEMPLATE/ (YAML form syntax reference — `type: textarea`, `type: dropdown`, `validations: required: true`)
    - https://developercertificate.org/ — DCO 1.1 text (copy verbatim into a code-fence block in CONTRIBUTING)
  </read_first>
  <behavior>
    - Test 1: `CONTRIBUTING.md` exists, contains the DCO 1.1 text inside a fenced code block, contains the literal `Signed-off-by:` example, and references `git commit -s`.
    - Test 2: `CONTRIBUTING.md` documents 3 contribution paths and links to: `src/vibemix/midi/profiles/` (controller path), `src/vibemix/prompts/` (prompt path), and a generic "Bug fix" section.
    - Test 3: `.github/ISSUE_TEMPLATE/bug_report.yml` parses as valid YAML, has `name`, `description`, `labels` (containing `bug`), `body` array with at least 4 fields including a `textarea` for repro steps and a `textarea` for `events.jsonl` attachment guidance.
    - Test 4: `.github/ISSUE_TEMPLATE/feature_request.yml` parses, has `labels` containing `enhancement`, `body` contains `problem-statement`, `proposed-solution`, `alternatives`, `scope-assessment` fields.
    - Test 5: `.github/ISSUE_TEMPLATE/new_controller.yml` parses, has `labels` containing `new-controller`, `body` contains fields for controller name, manufacturer, USB VID/PID, MIDI map JSON link, willingness to test.
    - Test 6: `.github/ISSUE_TEMPLATE/config.yml` parses, has `blank_issues_enabled: false`, has at least one `contact_links` entry (Discord stub link — per CONTEXT Deferred section the live invite is post-launch but a stub placeholder pointing to the roadmap doc is fine).
    - Test 7: `.github/pull_request_template.md` exists, references DCO sign-off, has a test checklist (`[ ] Tests added/updated`), has a breaking-change flag (`[ ] This is a breaking change`), has a linked-issue placeholder (`Closes #`).
    - Test 8: Every issue template YAML has both `name:` and `description:` top-level keys (GitHub requirement).
  </behavior>
  <action>
    1. Create `CONTRIBUTING.md`. Structure:
       - `# Contributing to vibemix`
       - Short intro paragraph (Bravoh's first OSS release, anti-slop mission).
       - `## Developer Certificate of Origin (DCO)` — explanation paragraph + the DCO 1.1 text inside a triple-fenced block + `git commit -s` example with the literal `Signed-off-by: Real Name <email@example.com>` trailer.
       - `## Contribution Paths` with 3 subsections:
         - `### Bug fix` — open issue first, branch from `main`, write tests, `git commit -s`, open PR. Mention the bug issue template.
         - `### Adding a controller mapping` — link to `src/vibemix/midi/profiles/` directory + link to `docs/midi-controllers.md` (exists from Phase 9). Walk through: copy `pioneer_ddj_flx4.json` as template, fill CC/note map, run `pytest tests/midi/ -k "controller_profiles"`, open PR using the `new_controller.yml` issue template as supporting context. Mention auto-merge for non-conflicting additions per CONTEXT Area 4.
         - `### Adding / improving a prompt template` — link to `src/vibemix/prompts/` + `docs/prompt-templates.md` (exists from Phase 10). Walk through: extend `matrix.py` or add an anti-slop entry in `negative_dict.py`, run `pytest tests/prompts/ -x`, open PR. Note: manual Kaan review for prompt changes (anti-slop dictionary applies).
       - `## Pull Request Checklist` — DCO sign-off, tests added, lint clean, linked issue, breaking-change flag.
       - `## Code of Conduct` — link to `CODE_OF_CONDUCT.md`.

    2. Create `.github/ISSUE_TEMPLATE/bug_report.yml`. GitHub-form YAML:
       - `name: Bug report`, `description: Something is broken or behaving unexpectedly.`, `title: "[bug] "`, `labels: ["bug"]`.
       - `body` array with:
         - markdown intro
         - `input` for vibemix version (`required: true`)
         - `dropdown` for OS (macOS / Windows; required)
         - `input` for controller (name + model — optional)
         - `textarea` for repro steps (required, 5 numbered)
         - `textarea` for expected vs actual (required)
         - `textarea` for `events.jsonl` paste / attachment guidance (optional, with "drag the file into this textarea or paste the last 50 lines")
         - `checkboxes` for "I've searched existing issues" (required: true)

    3. Create `.github/ISSUE_TEMPLATE/feature_request.yml`:
       - `name: Feature request`, `labels: ["enhancement"]`, `title: "[feat] "`.
       - body fields: problem-statement (required), proposed-solution (required), alternatives (optional), scope-assessment (dropdown: tiny / small / medium / large; required).

    4. Create `.github/ISSUE_TEMPLATE/new_controller.yml`:
       - `name: New controller mapping request`, `labels: ["new-controller"]`, `title: "[controller] "`.
       - body fields: controller name (required), manufacturer (required), USB VID/PID (required, with hint "lsusb on macOS, Device Manager on Windows"), MIDI map JSON link (Mixxx mapping repo, OEM PDF, etc.), willingness to test (dropdown: yes — own the controller / yes — can borrow / no — requesting on behalf).

    5. Create `.github/ISSUE_TEMPLATE/config.yml`:
       ```yaml
       blank_issues_enabled: false
       contact_links:
         - name: Discord (coming after v0.1.0 launch)
           url: https://altidus.world/vibemix#community
           about: Community chat will go live after the first public release. For now, please open an issue.
         - name: Security disclosure
           url: https://github.com/bravoh/vibemix/blob/main/SECURITY.md
           about: For security issues, please follow the responsible disclosure process in SECURITY.md, do NOT open a public issue.
       ```

    6. Create `.github/pull_request_template.md`:
       - `## Summary` placeholder.
       - `## Linked issue` (e.g. `Closes #`).
       - `## DCO` checklist item ("I have signed off on my commits per the DCO").
       - `## Test plan` checklist (tests added/updated, manual verification if applicable).
       - `## Breaking change` checkbox.
       - `## Screenshots` (for UI changes only).

    7. Extend `tests/repo/test_oss_hygiene.py` with the 8 tests per `<behavior>` above. YAML parsing via `yaml.safe_load`. The DCO check in test 1 uses `assert "Developer Certificate of Origin" in text and "Signed-off-by:" in text and "git commit -s" in text`. Test 8 iterates over all `.github/ISSUE_TEMPLATE/*.yml` (excluding `config.yml`) and asserts the top-level `name` + `description` keys are present.

    8. Run `pytest tests/repo/test_oss_hygiene.py -x`. All 15 tests (7 from Task 1 + 8 from Task 2) pass.
  </action>
  <verify>
    <automated>pytest tests/repo/test_oss_hygiene.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls CONTRIBUTING.md .github/ISSUE_TEMPLATE/bug_report.yml .github/ISSUE_TEMPLATE/feature_request.yml .github/ISSUE_TEMPLATE/new_controller.yml .github/ISSUE_TEMPLATE/config.yml .github/pull_request_template.md` exits 0.
    - `grep -c "Signed-off-by" CONTRIBUTING.md` ≥ 1.
    - `grep -c "src/vibemix/midi/profiles" CONTRIBUTING.md` ≥ 1.
    - `grep -c "src/vibemix/prompts" CONTRIBUTING.md` ≥ 1.
    - `python -c "import yaml; [yaml.safe_load(open(f'.github/ISSUE_TEMPLATE/{f}')) for f in ['bug_report.yml','feature_request.yml','new_controller.yml','config.yml']]"` exits 0.
    - `pytest tests/repo/test_oss_hygiene.py -x` exits 0 with 15 tests passing.
  </acceptance_criteria>
  <done>
    `CONTRIBUTING.md` ships with DCO + 3 contribution paths cross-linking to the existing Phase 9 + Phase 10 deliverables; 3 issue templates + 1 config.yml + 1 PR template land under `.github/`; full test suite gates the YAML shape + required fields + DCO sign-off contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external contributor → repo | First-time PRs may carry unsigned commits, no test coverage, security-sensitive changes. CONTRIBUTING.md + DCO enforcement + PR template are the policy surface. |
| security reporter → maintainer | Vulnerability disclosure must NOT go through public issues. SECURITY.md routes to `security@bravoh.com` + `config.yml` contact link prevents accidental public disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Repudiation | external PRs | mitigate | DCO sign-off requirement (CONTRIBUTING.md + PR template) records signed contributor consent. Without DCO, future relicensing or trademark dispute has no clean provenance. |
| T-19-06 | Information Disclosure | bug report attachments | mitigate | `bug_report.yml` `events.jsonl` field includes guidance "review for PII before pasting"; SECURITY.md issues route privately. |
| T-19-07 | Spoofing | issue templates | accept | GitHub form-style YAML is server-side validated; users can still file blank issues IF they bypass via API. Risk accepted — `blank_issues_enabled: false` in config.yml is the in-UI gate. |
| T-19-08 | Tampering | NOTICE freshness | mitigate | `gen_notice.py --check` mode + CI test gate fail when on-disk NOTICE drifts from current pyproject/package.json/Cargo.toml dep manifests. |
</threat_model>

<verification>
- `pytest tests/repo/test_oss_hygiene.py -x` exits 0 with 15 tests passing.
- 4 root markdown files (CONTRIBUTING / CODE_OF_CONDUCT / SECURITY / TRADEMARKS) + 1 NOTICE file + LICENSE present and verified.
- 5 GitHub template files (3 issue + 1 config + 1 PR) present and YAML-parseable.
- `python scripts/dist/gen_notice.py --check` exits 0 — NOTICE matches current deps.
- POC sentinel still present (cohost_v4.py, mocks/, fillers/).
</verification>

<success_criteria>
GH-13 (CONTRIBUTING.md + DCO + 3 paths), GH-14 (CODE_OF_CONDUCT/SECURITY/NOTICE/TRADEMARKS/Apache-2.0 LICENSE), and GH-15 (issue templates) are all on disk, gated by CI tests, and cross-link to the existing Phase 9 (`midi/profiles/`) + Phase 10 (`prompts/`) deliverables that contribution paths reference.
</success_criteria>

<output>
After completion, create `.planning/phases/19-github-launch-presence/19-02-SUMMARY.md`.
</output>
