---
phase: 19-github-launch-presence
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
  - 19-04
files_modified:
  - README.md
  - docs/midi-mapping.md
  - tests/repo/test_readme_shape.py
autonomous: true
requirements:
  - GH-02
  - GH-03
  - GH-04
  - GH-05
  - GH-06
  - GH-07
  - GH-08
  - GH-09
  - GH-10
  - GH-11
  - GH-12
  - GH-16
user_setup: []

must_haves:
  truths:
    - "README opens with the 12-section structure locked in CONTEXT Area 1: hero → tagline → demo block → badges → value prop → install buttons → feature matrix → controller grid → screenshots gallery → architecture SVG → FAQ → Bravoh footer"
    - "All 12 FAQ entries from CONTEXT Area 3 appear verbatim in the README"
    - "Feature matrix renders 6 cells (3 skill × 2 mode) with example reactions hand-written per Phase 10 anchor vocabulary"
    - "Controller grid renders all 10 controllers (FLX4, 400, FLX6, FLX10, 1000, SX3, XDJ-RX3, Numark Party Mix Live, Hercules Inpulse 300, 500) + `calibrate any other` callout"
    - "Bravoh footer link includes the utm tags `utm_source=github&utm_medium=oss&utm_campaign=vibemix_launch`"
    - "Install buttons link to GitHub Releases page (`/releases/latest`) so they auto-resolve to Phase 18's release.yml outputs"
    - "Badges row links to real shields.io URLs (build / release / license / platform / stars) — no fabricated badges"
    - "docs/midi-mapping.md (generic-fallback guide) exists so the 'calibrate any other' callout has a real link target"
  artifacts:
    - path: README.md
      provides: "Phase 19 launch README — 12 sections per CONTEXT Area 1"
      min_lines: 250
      contains: "vibemix"
    - path: docs/midi-mapping.md
      provides: "Generic MIDI fallback contributor guide (target of README 'calibrate any other' link)"
      min_lines: 30
      contains: "generic fallback"
    - path: tests/repo/test_readme_shape.py
      provides: "Structural CI gate for README — 12 sections + FAQ verbatim + controller list + utm + image references"
  key_links:
    - from: README.md
      to: docs/assets/architecture.svg
      via: "markdown image"
      pattern: "docs/assets/architecture\\.svg"
    - from: README.md
      to: docs/assets/hero.png
      via: "markdown image"
      pattern: "docs/assets/hero\\.png"
    - from: README.md
      to: CONTRIBUTING.md
      via: "FAQ Q12 link"
      pattern: "CONTRIBUTING\\.md"
    - from: README.md
      to: GitHub Releases page
      via: "install button anchor"
      pattern: "releases/latest"
    - from: README.md
      to: Bravoh waitlist
      via: "footer link with utm"
      pattern: "utm_source=github"
    - from: docs/midi-mapping.md
      to: README.md controller grid callout
      via: "anchor link"
      pattern: "midi-mapping\\.md"
---

<objective>
Rewrite `README.md` from its current 11-line placeholder into the full 12-section launch README locked in CONTEXT Area 1. The README is the product launch page — hero artwork, demo placeholder, badges, value prop, install buttons linked to Phase 18 release artifacts, feature matrix grounded in Phase 10's prompt anchors, controller grid pulling all 10 from Phase 9's profile JSONs, screenshot gallery placeholders pointing to `docs/assets/screenshots/`, branded architecture SVG (from Plan 19-04), the 12 FAQ entries verbatim from CONTEXT Area 3, and a `Built by Bravoh` footer with utm-tagged waitlist link. Plus `docs/midi-mapping.md` (the generic-fallback contributor guide that the controller-grid `calibrate any other` callout links to). Plus a CI test that gates the README's structural shape.

Purpose: This is the only thing 99% of GitHub visitors will read. Every other artifact this phase shipped (LICENSE, CONTRIBUTING, NOTICE, issue templates, architecture SVG) is plumbing that supports this one document. Closes 11 GH requirements in a single deliverable (GH-02 hero, GH-03 demo, GH-04 install, GH-05 matrix, GH-06 controllers, GH-07 screenshots, GH-08 architecture, GH-09 FAQ, GH-10 footer, GH-11 badges, GH-12 OG image reference, GH-16 release linkage).

Output: 1 full README.md rewrite (~280 lines), 1 new docs/midi-mapping.md guide (~40 lines), 1 CI gate test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-github-launch-presence/19-CONTEXT.md

# CONTEXT Area 1 (README section order) + Area 2 (feature matrix + controller grid)
# + Area 3 (FAQ 12 questions, locked verbatim) are load-bearing.

@src/vibemix/prompts/matrix.py
@src/vibemix/midi/profiles/__init__.py
@docs/midi-controllers.md
@docs/prompt-templates.md
@mocks/vibemix-cinematic-storyboard.html
@.github/workflows/release.yml
</context>

<interfaces>
<!-- Phase 9 controllers (canonical list — exact slugs match midi/profiles/*.json basenames) -->

The 10 controllers (5×2 grid, locked from Phase 9 SUMMARY):

| Row | Controller | Slug | Status |
|---|---|---|---|
| 1 | Pioneer DDJ-FLX4 | `pioneer_ddj_flx4` | Calibrated — live-verified |
| 1 | Pioneer DDJ-400 | `pioneer_ddj_400` | Calibrated — JSON only |
| 1 | Pioneer DDJ-FLX6 | `pioneer_ddj_flx6` | Calibrated — JSON only |
| 1 | Pioneer DDJ-FLX10 | `pioneer_ddj_flx10` | Calibrated — JSON only |
| 1 | Pioneer DDJ-1000 | `pioneer_ddj_1000` | Calibrated — JSON only |
| 2 | Pioneer DDJ-SX3 | `pioneer_ddj_sx3` | Calibrated — JSON only |
| 2 | Pioneer XDJ-RX3 | `pioneer_xdj_rx3` | Calibrated — JSON only |
| 2 | Numark Party Mix Live | `numark_party_mix_live` | Calibrated — JSON only |
| 2 | Hercules DJControl Inpulse 300 | `hercules_inpulse_300` | Calibrated — JSON only |
| 2 | Hercules DJControl Inpulse 500 | `hercules_inpulse_500` | Calibrated — JSON only |

<!-- Phase 10 prompt cells (canonical names, exact identifier shape) -->

From src/vibemix/prompts/matrix.py:
- HYPE_BEGINNER       — "yo / damn / sick" register
- HYPE_INTERMEDIATE   — byte-identical to v4 (the load-bearing tuned cell)
- HYPE_PRO            — insider DJ vocabulary, terse
- COACH_BEGINNER      — gentle, encouraging, one specific improvement nudge
- COACH_INTERMEDIATE  — concrete technical critique
- COACH_PRO           — peer-level critique, no hand-holding

<!-- Phase 17 RUBRIC anchors (FAQ Q7 reference; the rubric file is 17-RUBRIC.md once Phase 17 lands; until then reference Phase 16's gate target ≥95% grounded) -->

<!-- shields.io badge URL templates (the 5 from CONTEXT Area 1) -->

  Build:    https://img.shields.io/github/actions/workflow/status/bravoh/vibemix/release.yml?branch=main&style=for-the-badge&label=build
  Release:  https://img.shields.io/github/v/release/bravoh/vibemix?style=for-the-badge
  License:  https://img.shields.io/badge/license-Apache%202.0-blue?style=for-the-badge
  Platform: https://img.shields.io/badge/platform-macOS%20%7C%20Windows-lightgrey?style=for-the-badge
  Stars:    https://img.shields.io/github/stars/bravoh/vibemix?style=for-the-badge

<!-- Repo URL: github.com/bravoh/vibemix (per ROADMAP Phase 19 §). For all anchor hrefs use this base. -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full README.md rewrite — all 12 sections per CONTEXT Area 1 + 12 FAQ entries verbatim + feature matrix + controller grid</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Areas 1, 2, 3 — ALL three are locked content)
    - src/vibemix/prompts/matrix.py (Phase 10 anchor vocabulary for feature matrix example reactions)
    - src/vibemix/midi/profiles/ (the 10 JSON files — controller slug + manufacturer naming)
    - docs/midi-controllers.md (existing Phase 9 reference doc — link target from controller grid)
    - docs/prompt-templates.md (existing Phase 10 reference doc — link target from feature matrix)
    - mocks/vibemix-cinematic-storyboard.html (per CONTEXT — referenced from demo block)
    - .github/workflows/release.yml (badge URL anchors)
    - LICENSE, CONTRIBUTING.md (from Plan 19-02), CODE_OF_CONDUCT.md, SECURITY.md (linked from README)
    - docs/assets/architecture.svg, docs/assets/hero.png, docs/assets/demo-placeholder.gif (from Plan 19-04)
  </read_first>
  <behavior>
    - Test 1: README.md exists, length ≥ 250 lines.
    - Test 2: README contains all 12 section anchors in CONTEXT Area 1 order: (1) hero image, (2) tagline string, (3) demo block, (4) badges row, (5) value prop paragraph, (6) install buttons section, (7) feature matrix heading, (8) controller grid heading, (9) screenshots gallery heading, (10) architecture diagram, (11) FAQ heading, (12) Built by Bravoh footer. Test asserts each of 12 anchor strings is present, in order (cumulative line-index monotonic check).
    - Test 3: Hero image markdown references `docs/assets/hero.png` with an alt text.
    - Test 4: Tagline contains the exact phrase `AI co-host for your DJ set. Open source. Mac + Windows.`
    - Test 5: Demo block references `docs/assets/demo-placeholder.gif` and contains a TODO comment about the post-phase shoot.
    - Test 6: Badges row contains 5 distinct `https://img.shields.io/` URLs (build / release / license / platform / stars).
    - Test 7: Value prop paragraph contains the audio-privacy callout (FAQ Q2 echoes here per CONTEXT Specifics — "audio is streamed; no raw audio stored on Bravoh servers").
    - Test 8: Install section contains two prominent links to `https://github.com/bravoh/vibemix/releases/latest` (one for macOS, one for Windows) — uses `<a href=...>` or markdown `[]()`. Test does NOT require literal `.dmg` / `.msi` strings (Phase 18 hands off the binary filenames at tag-time).
    - Test 9: Feature matrix is a 3-row × 2-col markdown table. Each of the 6 cells contains at least one example reaction in `>` blockquote styling. The 6 cell labels match the Phase 10 matrix cell identifiers: `Beginner / Hype`, `Beginner / Coach`, `Intermediate / Hype`, `Intermediate / Coach`, `Pro / Hype`, `Pro / Coach`.
    - Test 10: Controller grid is a markdown table or grid with all 10 controller display names: `DDJ-FLX4`, `DDJ-400`, `DDJ-FLX6`, `DDJ-FLX10`, `DDJ-1000`, `DDJ-SX3`, `XDJ-RX3`, `Party Mix Live`, `Inpulse 300`, `Inpulse 500`. The "calibrate any other" callout links to `docs/midi-mapping.md`.
    - Test 11: Screenshots gallery references 5 image paths under `docs/assets/screenshots/` (calibration-wizard, mode-picker, voice-picker, in-session, recording-browser per CONTEXT Area 1 §9).
    - Test 12: Architecture section references `docs/assets/architecture.svg`.
    - Test 13: FAQ section contains all 12 question strings verbatim from CONTEXT Area 3 (Q1 "What is vibemix?" through Q12 "How do I contribute?"). Test asserts each of 12 question strings is present.
    - Test 14: Built by Bravoh footer contains the literal anchor with utm tags: `utm_source=github&utm_medium=oss&utm_campaign=vibemix_launch`.
    - Test 15: README references CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md, LICENSE in their canonical relative paths (each appears ≥ 1 time).
    - Test 16: README does NOT contain banned slop phrases per Phase 10 negative dictionary (sample: "amazing", "awesome", "let me know", "delve", "leverage", "as an AI"). Use `from vibemix.prompts.negative_dict import NEGATIVE_PHRASES` and assert no banned phrase appears as a whole-word match.
  </behavior>
  <action>
    1. Replace `README.md` entirely. Section-by-section per CONTEXT Area 1:

    Section 1 — Hero:
    ```markdown
    <p align="center">
      <img src="docs/assets/hero.png" alt="vibemix — AI co-host for your DJ set" width="100%" />
    </p>
    ```

    Section 2 — Tagline (immediately after hero):
    ```markdown
    <h3 align="center">vibemix — an AI co-host for your DJ set. Open source. Mac + Windows.</h3>
    ```

    Section 3 — Demo block:
    ```markdown
    <p align="center">
      <!-- TODO(GH-03): replace with the final 30-45s demo MP4/GIF after Kaan + Francesco shoot during a real set. Cinematic storyboard reference: mocks/vibemix-cinematic-storyboard.html. -->
      <img src="docs/assets/demo-placeholder.gif" alt="vibemix demo — coming soon" width="720" />
    </p>
    ```

    Section 4 — Badges row (centered, single line):
    ```markdown
    <p align="center">
      <a href="https://github.com/bravoh/vibemix/actions/workflows/release.yml"><img src="https://img.shields.io/github/actions/workflow/status/bravoh/vibemix/release.yml?branch=main&style=for-the-badge&label=build" alt="build status" /></a>
      <a href="https://github.com/bravoh/vibemix/releases/latest"><img src="https://img.shields.io/github/v/release/bravoh/vibemix?style=for-the-badge" alt="latest release" /></a>
      <a href="LICENSE"><img src="https://img.shields.io/badge/license-Apache%202.0-blue?style=for-the-badge" alt="license: Apache 2.0" /></a>
      <img src="https://img.shields.io/badge/platform-macOS%20%7C%20Windows-lightgrey?style=for-the-badge" alt="platform: macOS | Windows" />
      <a href="https://github.com/bravoh/vibemix/stargazers"><img src="https://img.shields.io/github/stars/bravoh/vibemix?style=for-the-badge" alt="github stars" /></a>
    </p>
    ```

    Section 5 — Value prop:
    Open with the locked tagline-paragraph from CONTEXT Area 1.5: `vibemix listens to your master output, watches your DJ software's screen, ingests your controller, and talks back into your headphones — like a real DJ friend in your ear, not generic AI commentary.` Follow with a one-paragraph standalone callout (per CONTEXT Specifics — privacy is #1 user concern): audio is streamed to Bravoh's proxy for Gemini routing; no raw audio is stored on Bravoh servers; recordings stay on the user's machine.

    Section 6 — Install:
    ```markdown
    ## Install

    > vibemix is pre-release. The first signed binaries land at `v0.1.0` (Phase 18). Until then, the buttons below point to the GitHub Releases page where the signed `.dmg` + `.msi` artifacts will appear.

    <p align="center">
      <a href="https://github.com/bravoh/vibemix/releases/latest"><img src="https://img.shields.io/badge/Download-macOS-000?style=for-the-badge&logo=apple" alt="Download for macOS" /></a>
      <a href="https://github.com/bravoh/vibemix/releases/latest"><img src="https://img.shields.io/badge/Download-Windows-0078d4?style=for-the-badge&logo=windows" alt="Download for Windows" /></a>
    </p>

    From clone to running in under 60 seconds:
    <!-- TODO(GH-04): install GIFs land here once Kaan records a one-shot screen capture of the post-binary-ship install flow. -->
    ```

    Section 7 — Feature matrix (markdown table, 3×2, each cell holds a hand-written example reaction per Phase 10 anchor vocabulary). Header note: "Each cell is a real example of what vibemix will say in that mode. No 'amazing', no 'awesome', no AI commentary glaze." Each cell example reaction is in `> blockquote` style. Examples (write your own per the Phase 10 anchor vocabulary in matrix.py):
    - Beginner / Hype: `> "Yo, that bassline switch was clean."`
    - Beginner / Coach: `> "The kick on the new track is louder than your outgoing one — pull channel B down a hair next time."`
    - Intermediate / Hype: `> "Filter sweep into the drop landed."`
    - Intermediate / Coach: `> "You let the EQ-killed lows ride for 8 bars longer than the build wanted. Drop them back in on bar 4 of the next phrase."`
    - Pro / Hype: `> "Clean 16-bar phrase match on that swap."`
    - Pro / Coach: `> "BPM drift across the last three transitions — you're trusting the sync more than the cue."`
    Link at bottom: `> See [docs/prompt-templates.md](docs/prompt-templates.md) for the full prompt template matrix and anti-slop rules.`

    Section 8 — Controller grid (5×2 markdown table). Each cell: `**<display-name>**<br/>` (small image tag for the logo at `docs/assets/controllers/<slug>.png` with `width="80"`) `<br/><sub>Calibrated</sub>` (or "Calibrated — live-verified" for FLX4). All 10 controllers per the table in the `<interfaces>` block above. Below the grid:
    ```markdown
    Don't see your controller? vibemix's generic-MIDI fallback lets you calibrate any other controller. See [docs/midi-mapping.md](docs/midi-mapping.md).
    ```

    Section 9 — Screenshots gallery (5 placeholder rows). Use HTML table for control over alt text:
    ```markdown
    <table>
      <tr>
        <td align="center"><img src="docs/assets/screenshots/calibration-wizard.png" alt="Calibration wizard" /><br/>Calibration wizard</td>
        <td align="center"><img src="docs/assets/screenshots/in-session.png" alt="Live session UI" /><br/>Live session</td>
      </tr>
      <tr>
        <td align="center"><img src="docs/assets/screenshots/mode-picker.png" alt="Mode picker" /><br/>Mode picker</td>
        <td align="center"><img src="docs/assets/screenshots/voice-picker.png" alt="Voice picker" /><br/>Voice picker</td>
      </tr>
      <tr>
        <td align="center" colspan="2"><img src="docs/assets/screenshots/recording-browser.png" alt="Recording browser" /><br/>Recording browser</td>
      </tr>
    </table>
    <!-- TODO(GH-07): Kaan ships final PNGs into docs/assets/screenshots/ post-phase. Until then these will 404 — keep the references so the swap-in is one drop. -->
    ```

    Section 10 — Architecture diagram:
    ```markdown
    ## How it works

    <p align="center">
      <img src="docs/assets/architecture.svg" alt="vibemix architecture — controller + master output flow into the Python sidecar, evidence-grounded prompts route through Bravoh's proxy to Gemini 3 Flash + Gemini TTS, audio chunks stream back to the user's headphones" width="100%" />
    </p>
    ```

    Section 11 — FAQ (the 12 questions verbatim from CONTEXT Area 3, each as a `<details>` block for collapsibility):
    ```markdown
    ## FAQ

    <details>
    <summary><strong>What is vibemix?</strong></summary>

    <answer text from CONTEXT Area 3 Q1>
    </details>

    <details>
    <summary><strong>Is my audio sent to the cloud?</strong></summary>

    <answer text from Q2>
    </details>

    ... (repeat for Q3 through Q12)
    ```
    All 12 question STRINGS appear EXACTLY as written in CONTEXT Area 3 (test 13 gates this). Answer prose is lifted from CONTEXT Area 3 with light polish (preserve the privacy / cost / Linux / Gemini / hallucination claims verbatim — they're load-bearing).

    Section 12 — Built by Bravoh footer:
    ```markdown
    ---

    <p align="center">
      <strong>Built by <a href="https://altidus.world?utm_source=github&utm_medium=oss&utm_campaign=vibemix_launch">Bravoh</a></strong> — the AI Artist Operating System.
      <br/>
      <sub>vibemix is Bravoh's first open-source release. <a href="https://altidus.world?utm_source=github&utm_medium=oss&utm_campaign=vibemix_launch">Join the Bravoh waitlist →</a></sub>
    </p>

    <p align="center">
      <sub>
        <a href="LICENSE">License</a> ·
        <a href="CONTRIBUTING.md">Contributing</a> ·
        <a href="CODE_OF_CONDUCT.md">Code of Conduct</a> ·
        <a href="SECURITY.md">Security</a> ·
        <a href="TRADEMARKS.md">Trademarks</a>
      </sub>
    </p>
    ```

    2. Self-check pass: open `src/vibemix/prompts/negative_dict.py` and verify none of the banned phrases ("amazing", "awesome", "great mix", "let me know", "delve", "leverage", "as an AI") appear in the README body. Replace any accidental hits with concrete language.

    3. Add a brief OG-image reference comment at the top of README (above the hero block) so visitors / X / Discord card-renderers pick up the right preview:
    ```markdown
    <!-- GH-12: OG / social-preview image. Set via GitHub repo settings (Social preview section) using docs/assets/hero.png until Kaan/Momo ship the final 1280×640 OG image as docs/assets/og-preview.png. -->
    ```
    Per CONTEXT Deferred, the OG image is set in GitHub repo settings UI (not by editing the repo) — the comment surfaces this as a Kaan post-phase action.

    4. Run `pytest tests/repo/test_readme_shape.py -x` (test file landed in Task 2). All 16 tests pass.
  </action>
  <verify>
    <automated>pytest tests/repo/test_readme_shape.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l README.md` reports ≥ 250 lines.
    - `grep -c "docs/assets/hero.png" README.md` ≥ 1.
    - `grep -c "docs/assets/architecture.svg" README.md` ≥ 1.
    - `grep -c "docs/assets/demo-placeholder.gif" README.md` ≥ 1.
    - `grep -c "shields.io" README.md` ≥ 5.
    - `grep -c "releases/latest" README.md` ≥ 2.
    - `grep -c "utm_source=github" README.md` ≥ 2.
    - `grep -c "FLX4\\|FLX6\\|FLX10\\|DDJ-1000\\|XDJ-RX3\\|SX3\\|Party Mix Live\\|Inpulse" README.md` ≥ 10.
    - Banned-phrase check: `grep -iwE "amazing|awesome|great mix|let me know|delve|leverage|as an AI" README.md | wc -l` returns 0 (case-insensitive whole-word).
    - `pytest tests/repo/test_readme_shape.py -x` exits 0 with 16 tests passing.
  </acceptance_criteria>
  <done>
    README.md is the full 12-section launch document with all locked content from CONTEXT Areas 1, 2, and 3. Every external dependency (hero.png, architecture.svg, demo-placeholder.gif from Plan 19-04; CONTRIBUTING.md, SECURITY.md from Plan 19-02; LICENSE on disk; controller logos in `docs/assets/controllers/`, screenshots in `docs/assets/screenshots/`) is correctly referenced. Banned-phrase gate clean. Kaan post-phase actions in SUMMARY: shoot demo MP4/GIF, ship final hero artwork, ship 10 controller logos, ship 5 screenshot PNGs, set GitHub repo Social Preview image.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: docs/midi-mapping.md generic-fallback guide + tests/repo/test_readme_shape.py CI gate</name>
  <files>docs/midi-mapping.md, tests/repo/test_readme_shape.py</files>
  <read_first>
    - docs/midi-controllers.md (existing Phase 9 reference — defines the 10 controllers + generic fallback semantics; new midi-mapping.md is the CONTRIBUTOR-side guide complementing this user-side doc)
    - src/vibemix/midi/profiles/pioneer_ddj_flx4.json (template structure — schema for new contributors copying)
    - src/vibemix/midi/generic.py (Phase 9 generic-fallback impl — semantics for the doc)
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 1 §8 reference)
  </read_first>
  <behavior>
    - Test 1: `docs/midi-mapping.md` exists, has ≥ 30 lines.
    - Test 2: References the contributing path (`src/vibemix/midi/profiles/`).
    - Test 3: References at least one existing profile as the template (`pioneer_ddj_flx4.json`).
    - Test 4: Contains a section explaining the generic-fallback semantics (knob_1 / knob_2 / fader_1 positional inference per Phase 9 SUMMARY).
    - Test 5: Links to CONTRIBUTING.md (`controller mapping path`).
    - Test 6: Links to the `new_controller.yml` issue template (`.github/ISSUE_TEMPLATE/new_controller.yml`).
    - Test 7: README test file (tests/repo/test_readme_shape.py) exists and contains all 16 tests from Task 1.
  </behavior>
  <action>
    1. Create `docs/midi-mapping.md`. Structure:
       - `# MIDI Mapping for vibemix`
       - Intro paragraph: vibemix ships 10 calibrated controllers + a generic fallback. This guide is for contributors adding a new mapping OR for users with an unmapped controller calibrating it on the fly.
       - `## Use the generic fallback (no code change)` — explanation of Phase 9's positional inference (knobs labeled knob_1..N, faders labeled fader_1..N, generic event shape). Note the caveat: less semantic context → Hype-man mode works fine, Coach mode is degraded.
       - `## Add a new calibrated mapping (PR-class contribution)` — walk through:
         1. Copy `src/vibemix/midi/profiles/pioneer_ddj_flx4.json` as a template.
         2. Fill `name`, `port_substrings`, `cc_map` (knobs / faders / crossfader), `note_map` (play / cue / sync / hotcue pads).
         3. Validate locally with `pytest tests/midi/ -k "controller_profiles"`.
         4. Open a PR using the `new_controller.yml` issue template as supporting context.
         5. CI runs the magnitude-aware event shape tests automatically. Auto-merge for non-conflicting additions per CONTRIBUTING.md.
       - `## Reference` — links to: CONTRIBUTING.md (`#adding-a-controller-mapping` section), docs/midi-controllers.md (user-side reference for the 10 shipped controllers), `.github/ISSUE_TEMPLATE/new_controller.yml`.
       - `## Why we ship JSON, not code` — one paragraph explaining the hand-validated JSON schema choice per Phase 6 Critical Constraint (no pydantic, no runtime code execution from contributor profiles — security gate).

    2. Create `tests/repo/test_readme_shape.py`. Structure: `from __future__ import annotations`, SPDX header, `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`. Imports: `re`, `pathlib.Path`. For the banned-phrase test, import from the production code: `from vibemix.prompts.negative_dict import NEGATIVE_PHRASES`. The 16 tests per the `<behavior>` block in Task 1. For test 2 (ordered section anchors), use a list of 12 anchor strings + a single sweep that asserts `text.find(anchor_i) < text.find(anchor_{i+1})` for each consecutive pair. For test 13 (FAQ verbatim), iterate over a Python list of the 12 exact question strings from CONTEXT Area 3 and assert each is `in` README.md text.

       Additionally, add the 7 tests for `docs/midi-mapping.md` (from `<behavior>` above) into the same file under a `class TestMidiMappingDoc` — keeps the Phase 19 doc-shape gates in one module.

    3. Run `pytest tests/repo/ -x` — all 19 (Plan 19-01) + 15 (Plan 19-02) + 18 (Plan 19-04 Task 2 written in 19-04 plan) + 23 (this plan: 16 README + 7 midi-mapping) tests pass cumulatively across the phase.
  </action>
  <verify>
    <automated>pytest tests/repo/test_readme_shape.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/midi-mapping.md tests/repo/test_readme_shape.py` exits 0.
    - `wc -l docs/midi-mapping.md` ≥ 30.
    - `grep -c "src/vibemix/midi/profiles" docs/midi-mapping.md` ≥ 1.
    - `grep -c "new_controller.yml" docs/midi-mapping.md` ≥ 1.
    - `grep -c "CONTRIBUTING.md" docs/midi-mapping.md` ≥ 1.
    - `pytest tests/repo/test_readme_shape.py -x` exits 0 with 23 tests passing.
  </acceptance_criteria>
  <done>
    `docs/midi-mapping.md` lands as the contributor-side companion to `docs/midi-controllers.md`. README's "calibrate any other" callout has a real link target. `tests/repo/test_readme_shape.py` gates the 12-section README structure + 12 FAQ verbatim + controller grid completeness + banned-phrase compliance. Phase 19 closes with all 18 GH requirements covered across the 4 plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → unauthenticated visitor | Public landing page; first signal of project quality. AI-slop language or 404'd images = visitor leaves. |
| README install buttons → GitHub Releases | One-click downloads route to Phase 18 release.yml artifacts. Wrong URL = users download nothing or a third-party impostor. |
| README utm-tagged Bravoh link → analytics | Marketing attribution depends on utm tags persisting through markdown render + GitHub anchor handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Spoofing | Install buttons | mitigate | Buttons link to canonical `github.com/bravoh/vibemix/releases/latest` (resolved at click-time by GitHub). No third-party download mirror is referenced. |
| T-19-13 | Tampering | FAQ content | mitigate | All 12 FAQ entries verbatim from CONTEXT Area 3 (locked by user); test 13 in `test_readme_shape.py` asserts each question string is present. A contributor cannot quietly soften the privacy claim (Q2) without failing CI. |
| T-19-14 | Information Disclosure | FAQ Q2 (privacy claim) | mitigate | Claim is bounded: "audio chunks streamed to Bravoh proxy" (truthful per Phase 5 architecture), "no raw audio stored on Bravoh servers" (truthful per proxy design), "recordings stay on your machine" (truthful per Phase 15). FAQ test gates the wording survives future README edits. |
| T-19-15 | Repudiation (slop creep) | future README PRs | mitigate | Banned-phrase test (test 16) imports `NEGATIVE_PHRASES` directly from the production prompts module. Any PR adding "amazing" or "awesome" to README fails CI on the same gate that catches AI slop in reactions. Single source of truth across product copy + marketing copy. |
</threat_model>

<verification>
- `pytest tests/repo/test_readme_shape.py -x` exits 0 with 23 tests passing.
- `pytest tests/repo/ -x` exits 0 cumulatively (~75 tests across all four plans).
- README.md renders without 404s when previewed (every `<img src=...>` path either exists or is a known post-phase Kaan-action — controllers and screenshots are .gitkeep'd dirs reserved in Plan 19-04).
- POC sentinel unchanged: `cohost_v4.py`, `mascot.html`, `mocks/` files diff-untouched.
- `grep -iwE "amazing|awesome|great mix|let me know|delve|leverage|as an AI" README.md | wc -l` returns 0.
</verification>

<success_criteria>
GH-02 through GH-12 + GH-16 are closed by a single README.md rewrite + companion doc. All 12 sections from CONTEXT Area 1 ship in order, all 12 FAQ entries land verbatim from CONTEXT Area 3, all 10 controllers from Phase 9 appear in the grid, all 6 feature matrix cells reference Phase 10 anchor vocabulary, all 5 image references resolve to committed placeholders or .gitkeep'd reserved dirs, the Built by Bravoh footer carries the utm tags. The README is the product page Phase 19 was scoped to deliver.
</success_criteria>

<output>
After completion, create `.planning/phases/19-github-launch-presence/19-03-SUMMARY.md`.
</output>
