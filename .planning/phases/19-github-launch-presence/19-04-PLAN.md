---
phase: 19-github-launch-presence
plan: 04
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - scripts/dist/render_architecture.py
  - scripts/dist/render_hero_placeholder.py
  - docs/assets/architecture.svg
  - docs/assets/hero.png
  - docs/assets/demo-placeholder.gif
  - docs/assets/controllers/.gitkeep
  - docs/assets/screenshots/.gitkeep
  - tests/repo/test_docs_assets.py
autonomous: true
requirements:
  - GH-02
  - GH-08
user_setup: []

must_haves:
  truths:
    - "A README pasted into GitHub renders a hero image + architecture diagram even before Kaan ships final artwork (placeholders are committed)"
    - "Architecture SVG uses CDJ Whisper v5 palette (amber accent + void blacks + silk muted) and renders 4 horizontal swim-lanes (User Hardware / vibemix Client / Network / Gemini)"
    - "Architecture SVG is regenerable: `python scripts/dist/render_architecture.py` writes byte-identical SVG (deterministic — same input dict → same output)"
    - "Hero placeholder is a 1280×640 amber gradient PNG, NOT a copyrighted stock image"
    - "Demo GIF placeholder exists so the README `<img>` doesn't 404 until Kaan + Francesco shoot the real demo"
  artifacts:
    - path: scripts/dist/render_architecture.py
      provides: "Deterministic SVG generator emitting docs/assets/architecture.svg"
      exports: ["main()", "render()"]
      min_lines: 120
    - path: scripts/dist/render_hero_placeholder.py
      provides: "Pillow-based 1280×640 amber-gradient PNG generator"
      exports: ["main()"]
    - path: docs/assets/architecture.svg
      provides: "Branded SVG diagram referenced from README §10"
      contains: "vibemix Client"
    - path: docs/assets/hero.png
      provides: "Placeholder hero banner — replaced by Kaan/Momo final artwork post-phase"
    - path: docs/assets/demo-placeholder.gif
      provides: "5s loop placeholder until real demo shot"
    - path: docs/assets/controllers/.gitkeep
      provides: "Reserved directory for 10 controller logo PNGs (post-phase Kaan-provided)"
    - path: docs/assets/screenshots/.gitkeep
      provides: "Reserved directory for 5 screenshot PNGs (post-phase Kaan-provided)"
  key_links:
    - from: scripts/dist/render_architecture.py
      to: docs/assets/architecture.svg
      via: "Path.write_text"
      pattern: "architecture\\.svg"
    - from: docs/assets/architecture.svg
      to: README.md (in Plan 19-03)
      via: "README §10 reference"
      pattern: "architecture\\.svg"
---

<objective>
Generate the two branded image assets the README depends on before Plan 19-03 writes the README itself: a CDJ Whisper v5-styled architecture SVG (4 horizontal swim-lanes: User Hardware / vibemix Client / Network / Gemini) and a 1280×640 amber-gradient hero PNG placeholder that ships until Kaan/Momo deliver final artwork. Plus a tiny placeholder demo GIF and reserved subdirectories for controller logos + screenshot gallery (Kaan-provided post-phase).

Purpose: GH-02 (hero banner) and GH-08 (branded architecture diagram, NOT default Mermaid) are gating requirements. We don't have final artwork from the design lead yet, but a placeholder amber gradient + a generator script gives us a README that renders sanely on day 1 and a clean swap-in surface for final art later. The SVG is the load-bearing piece — it's the only "designed asset" we author this phase; it must look professional in the v5 palette, not auto-generated.

Output: 2 generator scripts + 3 committed image artifacts (hero.png, architecture.svg, demo-placeholder.gif) + 2 .gitkeep'd subdirectories + 1 CI test gating their presence + shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-github-launch-presence/19-CONTEXT.md

# CONTEXT Area 7 (Architecture Diagram) is load-bearing.
# CDJ Whisper v5 palette source: mocks/vibemix-direction-final.html.
# Phase 14 token names: tauri/ui/src/styles/tokens.css.

@mocks/vibemix-direction-final.html
@tauri/ui/src/styles/tokens.css
</context>

<interfaces>
<!-- CDJ Whisper v5 palette extract (canonical source: tauri/ui/src/styles/tokens.css) -->
<!-- The generator script will hard-code these literals — do NOT introduce a token loader. -->

```
--void-0: #0a0b0d            (deepest black background)
--void-1: #14171c            (panel base)
--void-2: #1f242c            (panel elevated)
--amber-22: #ff8a3d          (the accent)
--amber-18: rgba(255,138,61,0.72)
--silk-1: #e6e3dc            (text base)
--silk-3: rgba(230,227,220,0.48)  (muted text)
--rave-1: rgba(255,138,61,0.04)   (ambient haze)
--glow-amber: rgba(255,138,61,0.55) (border glow)
```

Layout: 4 horizontal swim-lanes, top-to-bottom:
  1. User Hardware  — boxes: DJ Controller, Headphones, Master output (BlackHole/WASAPI)
  2. vibemix Client — boxes: Python sidecar (sensing + reactions + recording), Tauri UI (live session + settings + wizard), Mascot overlay
  3. Network        — single box: Bravoh proxy (api.altidus.world, JWT + rate-limit)
  4. Gemini         — boxes: Gemini 3 Flash multimodal, Gemini TTS streaming

Arrows: audio flow upward (master → sidecar → proxy → Gemini → TTS chunks back down to headphones).
SVG canvas: 1200×720, swim-lane height 160, gap 20.
Stroke: --silk-3 for separators, --amber-22 for active arrows, drop-shadow filter on Gemini node only.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: render_architecture.py + deterministic CDJ Whisper v5 SVG output</name>
  <files>scripts/dist/render_architecture.py, docs/assets/architecture.svg, tests/repo/test_docs_assets.py</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 7)
    - mocks/vibemix-direction-final.html (visual reference — extract palette colors used)
    - tauri/ui/src/styles/tokens.css (canonical hex values for the v5 palette)
    - Phase 14 SUMMARY for context on which tokens are load-bearing
  </read_first>
  <behavior>
    - Test 1: `scripts/dist/render_architecture.py` exists, has SPDX header, `python scripts/dist/render_architecture.py --help` exits 0.
    - Test 2: Running `python scripts/dist/render_architecture.py` writes `docs/assets/architecture.svg`. Re-running it produces a byte-identical file (deterministic).
    - Test 3: `docs/assets/architecture.svg` is well-formed XML (parses with `xml.etree.ElementTree`).
    - Test 4: SVG contains all four swim-lane labels: `User Hardware`, `vibemix Client`, `Network`, `Gemini`.
    - Test 5: SVG references each of the v5 amber accent colors at least once (literal `#ff8a3d` or its rgba equivalent).
    - Test 6: SVG references each of the v5 void backgrounds at least once (literal `#0a0b0d` or `#14171c`).
    - Test 7: SVG contains at least 8 boxes (DJ controller, Headphones, BlackHole/WASAPI, Sidecar, Tauri UI, Mascot, Proxy, Gemini Flash, Gemini TTS).
    - Test 8: SVG references the literal strings `Bravoh proxy`, `Python sidecar`, `Tauri UI`, `Gemini 3 Flash`, `Gemini TTS` for textual anchoring.
    - Test 9: SVG `<svg>` root has `viewBox="0 0 1200 720"` and `xmlns="http://www.w3.org/2000/svg"`.
  </behavior>
  <action>
    1. Create `scripts/dist/render_architecture.py`. SPDX header + `from __future__ import annotations`. Imports: stdlib only — `xml.etree.ElementTree as ET`, `argparse`, `sys`, `pathlib.Path`. NO new deps.

    2. At module top, define palette constants (literals, NOT loaded from tokens.css):
       ```
       VOID_0 = "#0a0b0d"
       VOID_1 = "#14171c"
       VOID_2 = "#1f242c"
       AMBER_22 = "#ff8a3d"
       AMBER_18 = "rgba(255,138,61,0.72)"
       SILK_1 = "#e6e3dc"
       SILK_3 = "rgba(230,227,220,0.48)"
       RAVE_1 = "rgba(255,138,61,0.04)"
       GLOW_AMBER = "rgba(255,138,61,0.55)"
       ```

    3. Define the diagram as a pure data structure:
       ```
       SWIM_LANES = [
         {"label": "User Hardware", "y": 40,  "boxes": [
            {"label": "DJ Controller",       "x": 80,  "w": 220, "subtitle": "DDJ-FLX4 / 10 mapped + generic"},
            {"label": "Master output",       "x": 340, "w": 220, "subtitle": "BlackHole 2ch / WASAPI loopback"},
            {"label": "Headphones",          "x": 600, "w": 220, "subtitle": "sounddevice OutputStream @ 24 kHz"},
         ]},
         {"label": "vibemix Client", "y": 220, "boxes": [
            {"label": "Python sidecar",      "x": 80,  "w": 260, "subtitle": "MusicState · EventDetector · AICoach"},
            {"label": "Tauri UI",            "x": 380, "w": 260, "subtitle": "wizard · session · settings · mascot"},
            {"label": "Local recording",     "x": 680, "w": 260, "subtitle": "input.wav · voice.wav · events.jsonl"},
         ]},
         {"label": "Network", "y": 400, "boxes": [
            {"label": "Bravoh proxy",        "x": 340, "w": 520, "subtitle": "api.altidus.world · JWT · 60 rpm / 2000 rpd"},
         ]},
         {"label": "Gemini", "y": 560, "boxes": [
            {"label": "Gemini 3 Flash",      "x": 200, "w": 360, "subtitle": "multimodal: audio + evidence"},
            {"label": "Gemini TTS",          "x": 620, "w": 360, "subtitle": "streaming PCM chunks"},
         ]},
       ]
       ```

    4. Define `render(swim_lanes) -> str` that returns the SVG as a string:
       - Root `<svg viewBox="0 0 1200 720" xmlns="http://www.w3.org/2000/svg">`.
       - `<defs>` block with: a linear-gradient `lane-bg` from `VOID_0` to `VOID_1`, a drop-shadow filter named `glow` with `GLOW_AMBER` stdDeviation 6, a marker `arrow-end` for arrow heads.
       - Background `<rect>` filling the entire canvas with `VOID_0`.
       - For each swim lane: a `<rect>` with the gradient fill, `SILK_3` 1px stroke, the lane label as a `<text>` at left edge (Saira font, fill=SILK_1, font-size 18, letter-spacing 2px, text-transform uppercase).
       - For each box: a rounded `<rect>` (rx=8) with fill=`VOID_2`, stroke=`SILK_3` 1px, plus a 2-line label (title 16px SILK_1, subtitle 11px SILK_3).
       - Connecting arrows: 4 amber arrows joining successive swim lanes (controller→sidecar, master→sidecar, sidecar→proxy, proxy→Gemini, Gemini→headphones return-path). Use `<path>` with stroke=AMBER_22, stroke-width=2, `marker-end="url(#arrow-end)"`, opacity=0.85.
       - The Gemini lane gets `filter="url(#glow)"` for an amber halo.
       - Deterministic output: sort all `<defs>` children, no timestamps, no random IDs. Use `ET.tostring(root, encoding="unicode")` and prepend the XML declaration manually so the byte-identical re-run test holds.

    5. Define `main(argv=None)` with argparse: `--output PATH` (default `docs/assets/architecture.svg`), `--check` (read existing file and diff, exit 1 if mismatch; mirrors `gen_notice.py --check` pattern). The default invocation writes the file. `if __name__ == "__main__": sys.exit(main())`.

    6. Run the generator once: `python scripts/dist/render_architecture.py`. Commit the output at `docs/assets/architecture.svg`. Verify by opening the SVG in a browser (sanity-check only — the test is the gate).

    7. Add a test stanza in `tests/repo/test_docs_assets.py` (created in Task 2) for the 9 architecture tests above. The determinism test (Test 2) runs the script twice into a tmp file and computes SHA-256 of both outputs.
  </action>
  <verify>
    <automated>python scripts/dist/render_architecture.py --check && pytest tests/repo/test_docs_assets.py -k "architecture" -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/assets/architecture.svg scripts/dist/render_architecture.py` exits 0.
    - `python scripts/dist/render_architecture.py --check` exits 0 (committed SVG matches generator output).
    - `python -c "import xml.etree.ElementTree as ET; ET.parse('docs/assets/architecture.svg')"` exits 0.
    - `grep -c "ff8a3d" docs/assets/architecture.svg` ≥ 1.
    - `grep -c "vibemix Client\\|User Hardware\\|Network\\|Gemini" docs/assets/architecture.svg` ≥ 4.
    - `pytest tests/repo/test_docs_assets.py -k "architecture" -x` exits 0 with 9 tests passing.
  </acceptance_criteria>
  <done>
    `scripts/dist/render_architecture.py` deterministic generator ships as the regen path; `docs/assets/architecture.svg` is the committed artifact rendered in CDJ Whisper v5 palette with 4 horizontal swim-lanes, 8 boxes, amber arrows + Gemini lane glow. `--check` mode in CI catches drift between the committed SVG and the script. Plan 19-03's README §10 references this file directly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hero PNG placeholder + demo GIF placeholder + reserved dirs + CI gate</name>
  <files>scripts/dist/render_hero_placeholder.py, docs/assets/hero.png, docs/assets/demo-placeholder.gif, docs/assets/controllers/.gitkeep, docs/assets/screenshots/.gitkeep, tests/repo/test_docs_assets.py (extended)</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 1 — hero artwork spec) and Specifics block (hero gradient from --void-1 → --amber-22 → --void-1)
    - tauri/ui/src/styles/tokens.css (palette literal hex)
    - Pillow API: PIL.Image.new + PIL.ImageDraw.Draw + linear gradient via row-wise interpolate
  </read_first>
  <behavior>
    - Test 1: `scripts/dist/render_hero_placeholder.py` exists, has SPDX header, `--help` works.
    - Test 2: Running `python scripts/dist/render_hero_placeholder.py` writes `docs/assets/hero.png`.
    - Test 3: `docs/assets/hero.png` is a valid PNG file (PIL can open it).
    - Test 4: PNG dimensions are exactly 1280×640.
    - Test 5: A 1-pixel column at x=640 (image center) contains an amber pixel (R > 200, G in [100, 180], B < 100) — proof the gradient hits the amber midpoint.
    - Test 6: A 1-pixel column at x=0 (left edge) is dark (sum(RGB) < 90) — proof the gradient starts from void-1.
    - Test 7: `docs/assets/demo-placeholder.gif` exists and is a valid GIF (PIL can open it, `image.format == "GIF"`).
    - Test 8: `docs/assets/controllers/.gitkeep` and `docs/assets/screenshots/.gitkeep` exist (reserved dirs for Kaan-provided assets).
    - Test 9: `docs/assets/architecture.svg` exists (from Task 1; this test is a sentinel for Plan 19-03 dependency).
  </behavior>
  <action>
    1. Create `scripts/dist/render_hero_placeholder.py` (SPDX header). Imports: `argparse`, `sys`, `pathlib.Path`, `PIL.Image`, `PIL.ImageDraw`, `PIL.ImageFont`. Note: Pillow is already a transitive dep (from Phase 8 `pyobjc-framework-ScreenCaptureKit` chain — verify by `python -c "import PIL; print(PIL.__version__)"`; if not present, add `pillow>=10` to `[dependency-groups] dev`).

    2. The PNG generator:
       - Canvas 1280×640.
       - Horizontal 3-stop gradient: `#14171c` at x=0 → `#ff8a3d` at x=640 → `#14171c` at x=1280. Implement by row-wise lerp: for each x in [0, 1280), compute the interpolated RGB and `draw.line([(x, 0), (x, 640)], fill=(r, g, b))`.
       - Overlay: large centered text `vibemix` in white (`#e6e3dc`) at ~96px font; underline subtitle `AI co-host for your DJ set` at 32px in `#e6e3dc99`. Use `ImageFont.load_default()` — no custom font file (avoids new dep / new vendored asset). The placeholder doesn't need brand-final typography; Kaan/Momo replace this whole PNG post-phase.
       - Bottom-right corner: small `placeholder — final artwork by Bravoh design lead post-launch` tag in `#e6e3dc55` so anyone viewing the README knows this is temporary.

    3. `main(argv=None)` with argparse: `--output PATH` (default `docs/assets/hero.png`). Idempotent — re-running produces the same bytes (PIL's PNG encoder is deterministic for the same input).

    4. Run once → commit `docs/assets/hero.png`.

    5. Generate `docs/assets/demo-placeholder.gif` once via a tiny Python one-liner in a docstring example: a 320×180 GIF, 3 frames, each filled with `#14171c` and centered text `demo · coming soon` in `#ff8a3d`, frame duration 800ms, looping. This GIF is committed as-is; the script does not need to be a permanent artifact (it's a one-time scaffold, generated by hand in the action step). However, to be safe, embed the same logic as a `--demo-gif` subcommand of `render_hero_placeholder.py` so the regen path is preserved. The committed GIF must be < 200 KB.

    6. Create `docs/assets/controllers/.gitkeep` and `docs/assets/screenshots/.gitkeep` as empty files. These are the drop zones for Kaan-provided controller logos (10 PNGs, post-phase) and screenshot gallery (5 PNGs, post-phase). The README in Plan 19-03 references these paths; CI test 8 gates the directories exist so the README links don't 404 even before Kaan ships the real images.

    7. Create `tests/repo/test_docs_assets.py` with the 9 tests for Task 1 + 9 tests for Task 2 (18 total). Test 5 + 6 use PIL `Image.open + image.getpixel` to read center + edge columns and assert color ranges. Determinism test for the hero PNG: write to two tmp paths and compare SHA-256.

    8. Run `pytest tests/repo/test_docs_assets.py -x` → 18 tests pass.
  </action>
  <verify>
    <automated>pytest tests/repo/test_docs_assets.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/assets/hero.png docs/assets/demo-placeholder.gif docs/assets/architecture.svg docs/assets/controllers/.gitkeep docs/assets/screenshots/.gitkeep` exits 0.
    - `python -c "from PIL import Image; im=Image.open('docs/assets/hero.png'); assert im.size==(1280,640); print('ok')"` prints `ok`.
    - `python -c "from PIL import Image; im=Image.open('docs/assets/demo-placeholder.gif'); assert im.format=='GIF'; print('ok')"` prints `ok`.
    - File size sanity: `ls -l docs/assets/hero.png` ≤ 500 KB; `ls -l docs/assets/demo-placeholder.gif` ≤ 200 KB.
    - `pytest tests/repo/test_docs_assets.py -x` exits 0 with 18 tests passing.
  </acceptance_criteria>
  <done>
    All four image assets land at the README-expected paths: `hero.png` (1280×640 amber gradient with placeholder typography), `demo-placeholder.gif` (5s loop, < 200 KB), `architecture.svg` (from Task 1), and two reserved `.gitkeep'd` subdirs for the Kaan-provided controller logos + screenshots. Plan 19-03 can write the README without worrying that any `<img src="docs/assets/...">` tag will 404. Kaan-action surfaced in SUMMARY: deliver final hero.png, demo MP4/GIF, 10 controller logos (`docs/assets/controllers/<slug>.png`), 5 screenshots (`docs/assets/screenshots/<surface>.png`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → unauthenticated visitor | Hero image + architecture diagram are the first impression on a public repo. Default Mermaid auto-render or 404'd image = signal of low-effort project. |
| generator script → file system | render_architecture.py + render_hero_placeholder.py write deterministic outputs; `--check` modes catch drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering | docs/assets/architecture.svg | mitigate | `--check` mode + CI test pin the committed SVG to the generator output. Drift means either the script changed (intentional, recommit) or someone edited the SVG by hand (rejected). |
| T-19-10 | Information Disclosure | docs/assets/hero.png | accept | Placeholder PNG contains no sensitive content; will be replaced by Kaan/Momo final artwork. |
| T-19-11 | Spoofing (visual) | architecture diagram | mitigate | Diagram text is authoritative (file paths + module names match the actual codebase); a contributor cannot ship a PR with a misleading diagram because the test gates the box labels. |
</threat_model>

<verification>
- `pytest tests/repo/test_docs_assets.py -x` exits 0 with 18 tests passing.
- `python scripts/dist/render_architecture.py --check` exits 0.
- All 5 assets / dirs present at canonical paths.
- POC sentinel unchanged.
</verification>

<success_criteria>
GH-02 (README hero banner) and GH-08 (branded architecture diagram, NOT default Mermaid) have committed placeholder artwork + deterministic regenerators. Plan 19-03's README can reference these paths without 404s. Kaan/Momo's post-phase work is a clean swap-in (replace hero.png + drop 10 controller PNGs + 5 screenshot PNGs into reserved dirs).
</success_criteria>

<output>
After completion, create `.planning/phases/19-github-launch-presence/19-04-SUMMARY.md`.
</output>
