---
phase: 44-launch-positioning-pre-stage
plan: 04
title: "Bravoh waitlist toggle in debrief settings (LAUNCH-05)"
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts
  - tauri/ui/src/debrief/components/bravoh-waitlist-toggle.test.ts
  - tauri/ui/src/debrief/debrief-window.ts
  - tauri/ui/src/debrief/styles/debrief.css
  - src/vibemix/runtime/config_store.py
  - tests/runtime/test_config_store_bravoh_waitlist.py
autonomous: true
requirements: [LAUNCH-05]
req_ids: [LAUNCH-05]
tags: [debrief, ui, bravoh-funnel, opt-in, telemetry]
must_haves:
  truths:
    - "Debrief window settings drawer has a new 'Join Bravoh waitlist (optional)' toggle, default OFF (CONTEXT §LAUNCH-05)"
    - "Toggle is opt-in only — does NOT gate any functionality; when OFF, debrief save flow is unchanged"
    - "When ON, debrief save-success view surfaces a subtle link: 'Join the Bravoh waitlist →' pointing at the canonical UTM URL: `https://bravoh.com/waitlist?utm_source=vibemix&utm_medium=app&utm_campaign=oss-launch` (CONTEXT §LAUNCH-05 verbatim URL)"
    - "Link is a plain `<a target=\"_blank\" rel=\"noopener noreferrer\">` — no form intercept, no modal, no analytics tracking fire from app side beyond the UTM params on the destination URL itself"
    - "Telemetry default-off: no signed-out event fires when toggle is OFF (CONTEXT 'signed-out telemetry default-off'); when toggle is ON, a single `events.jsonl` row `bravoh_waitlist_opt_in: true` is written ONCE per session at the moment of toggle activation (NOT on every save) — for diagnostic purposes only, never network-transmitted"
    - "Toggle state persists across sessions via the existing `config_store.py` JSON-backed settings store (key: `bravoh_waitlist_opt_in: bool`, default `false`, Phase 12 D-Area-4.4 dataclass convention preserved)"
    - "`config_store.py` round-trips the new key — preserves unknown top-level keys on save (per Phase 12 superset contract); test pins this"
    - "Component test pins: default OFF; toggle on → link visible; toggle off → link hidden; click link opens external URL (mock); no event fires unless explicitly toggled on"
  artifacts:
    - path: "tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts"
      provides: "Settings-drawer toggle + conditional waitlist link"
      min_lines: 60
    - path: "tauri/ui/src/debrief/components/bravoh-waitlist-toggle.test.ts"
      provides: "Pins default-OFF + link visibility + no-telemetry contract"
    - path: "src/vibemix/runtime/config_store.py"
      provides: "Adds `bravoh_waitlist_opt_in: bool = False` field to ConfigStore dataclass"
    - path: "tests/runtime/test_config_store_bravoh_waitlist.py"
      provides: "Pins config round-trip + default + unknown-key preservation"
  key_links:
    - from: "tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts"
      to: "src/vibemix/runtime/config_store.py"
      via: "Tauri IPC `get_config` / `set_config` (existing settings persistence)"
      pattern: "bravoh_waitlist_opt_in"
    - from: "tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts"
      to: "https://bravoh.com/waitlist?utm_source=vibemix&utm_medium=app&utm_campaign=oss-launch"
      via: "anchor href with UTM params (canonical URL from CONTEXT §LAUNCH-05)"
      pattern: "bravoh\\.com/waitlist\\?utm_source=vibemix"
---

<objective>
Add an opt-in "Join Bravoh waitlist" toggle to the debrief window settings drawer (LAUNCH-05). When toggled ON, a subtle link to `https://bravoh.com/waitlist?utm_source=vibemix&utm_medium=app&utm_campaign=oss-launch` appears in the debrief save-success view. Default OFF. Not a gating flow. Not a form intercept. No signed-out telemetry. Reuses the existing `config_store.py` JSON-backed persistence layer.

Purpose: LAUNCH-05 is the Bravoh funnel CTA — vibemix warms the Bravoh waitlist. The constraint (CONTEXT §domain + memory `feedback_no_scope_creep_clean_utility`): subtle, opt-in, not gating. The bar: a user who never toggles on never sees a Bravoh link and never has a signed-out event fire from the funnel surface.

Output: New `bravoh-waitlist-toggle.ts` debrief component + test; `config_store.py` schema extension + test; CSS lives in `debrief.css` (token-driven, frontend-enforcement skill). Wave 1 — no file overlap with 44-01/02/03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-launch-positioning-pre-stage/44-CONTEXT.md
@src/vibemix/runtime/config_store.py
@tauri/ui/src/debrief/debrief-window.ts
@tauri/ui/src/debrief/components/ear-test-toggle.ts
@tauri/ui/src/tokens.css

<interfaces>
<!-- config_store.py Phase 12 dataclass convention (per its module docstring): -->
<!-- - hand-written @dataclass (no pydantic) -->
<!-- - atomic write: json.dumps → tmp file → os.replace -->
<!-- - PRESERVES UNKNOWN TOP-LEVEL KEYS on save (round-trip contract with Rust shell) -->
<!-- - `muted` is explicitly NOT persisted -->
<!-- Add `bravoh_waitlist_opt_in: bool = field(default=False)` to the dataclass + the load/save dict cycle. -->

<!-- ear-test-toggle.ts (the existing Plan 42-03 toggle, same neighborhood) is the -->
<!-- canonical pattern for "opt-in toggle in debrief": -->
<!--   - export interface props with mount payload + session metadata -->
<!--   - imperative DOM API (no React) — element returned with internal state -->
<!--   - showErrorBanner imported from "./error-banner.js" for failure surfaces -->
<!--   - test in tauri/ui/src/debrief/__tests__/ (vitest) -->

<!-- The canonical UTM URL is VERBATIM from CONTEXT §LAUNCH-05: -->
<!--   https://bravoh.com/waitlist?utm_source=vibemix&utm_medium=app&utm_campaign=oss-launch -->
<!-- Do NOT modify. Pin as a module constant `BRAVOH_WAITLIST_URL`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `config_store.py` schema + test (TDD)</name>
  <files>src/vibemix/runtime/config_store.py, tests/runtime/test_config_store_bravoh_waitlist.py</files>
  <behavior>
    - `ConfigStore` dataclass gets a new field `bravoh_waitlist_opt_in: bool` with default `False`
    - `load()` reads the key from JSON; absent key → default `False`
    - `save()` writes the key; round-trip preserves unknown top-level keys (existing Phase 12 contract)
    - Test fixtures (in `tmp_path`):
      a. Default load (no config file) → `bravoh_waitlist_opt_in == False`
      b. Load existing file with key set to `true` → `bravoh_waitlist_opt_in == True`
      c. Save mutated config + reload → key round-trips
      d. Load file with unknown future key + save → unknown key preserved (existing Phase 12 invariant, regression-tested)
  </behavior>
  <action>Add the `bravoh_waitlist_opt_in: bool = field(default=False)` field to the `ConfigStore` dataclass in `src/vibemix/runtime/config_store.py`; wire it into the JSON load/save cycle (mirror existing fields like `voice`, `mode`, `retention_days`). Write `tests/runtime/test_config_store_bravoh_waitlist.py` with the 4 fixtures.</action>
  <verify>
    <automated>uv run pytest tests/runtime/test_config_store_bravoh_waitlist.py -v</automated>
  </verify>
  <done>4 tests green; existing config_store regression tests still pass (`uv run pytest tests/runtime/test_config_store*.py` green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship `bravoh-waitlist-toggle.ts` + test + debrief mount</name>
  <files>tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts, tauri/ui/src/debrief/components/bravoh-waitlist-toggle.test.ts, tauri/ui/src/debrief/debrief-window.ts, tauri/ui/src/debrief/styles/debrief.css</files>
  <behavior>
    - Module-level constant `BRAVOH_WAITLIST_URL = "https://bravoh.com/waitlist?utm_source=vibemix&utm_medium=app&utm_campaign=oss-launch"` (frozen)
    - Export `mountBravohWaitlistToggle(container: HTMLElement, props: { initialOptIn: boolean; onToggle: (next: boolean) => void; }): { setOptIn(next: boolean): void; destroy(): void; }`
    - Renders:
      a. A toggle in the settings drawer labeled "Join Bravoh waitlist (optional)"; subtitle "vibemix is built by Bravoh — get notified when the main product opens"
      b. When toggle is ON, a small inline `<a class="vmx-bravoh-waitlist-link" href="${BRAVOH_WAITLIST_URL}" target="_blank" rel="noopener noreferrer">Join the Bravoh waitlist →</a>` appears in the save-success view (a sibling DOM hook the debrief-window.ts can mount conditionally)
      c. Toggle off → link hidden (display:none or remove from DOM — Claude's discretion); no telemetry fires
    - Persistence: on toggle change, invoke Tauri IPC `set_config({ bravoh_waitlist_opt_in: bool })`; on mount, read initial state from `get_config()`
    - Event row: when toggle transitions OFF → ON, fire a single local diagnostic `events.jsonl` row via existing recorder helper (search the codebase for the "events.jsonl" writer used by `ear-test-toggle.ts`; reuse the same path) with payload `{ kind: "bravoh_waitlist_opt_in", value: true, ts: <iso8601> }`. NO network call. NO event when toggle goes ON → OFF (LAUNCH-05 contract: signed-out telemetry default-off; the ON transition is a user-explicit opt-in moment worth diagnosing).
    - Test cases (vitest, mirror `ear-test-toggle` test pattern in `tauri/ui/src/debrief/__tests__/`):
      i. Default state OFF when `initialOptIn=false` — no link in DOM
      ii. Toggle ON → link visible + href matches `BRAVOH_WAITLIST_URL` exactly
      iii. Toggle OFF after ON → link removed
      iv. Click toggle invokes `onToggle` callback with new state
      v. No setConfig invocation on mount (only on user toggle)
    - CSS: lives in `tauri/ui/src/debrief/styles/debrief.css` (existing debrief stylesheet); new classes `.vmx-bravoh-waitlist-row`, `.vmx-bravoh-waitlist-link` — token-driven (use existing `--c-link-fg`, `--c-text-secondary`, `--glow-faint` tokens; if missing, add semantic aliases in tokens.css per frontend-enforcement skill)
  </behavior>
  <action>Create the component per the behavior block. Mirror `ear-test-toggle.ts` import structure + DOM API. Read the canonical UTM URL from CONTEXT §LAUNCH-05 verbatim and pin as a frozen constant. Wire `debrief-window.ts` to mount the toggle in the settings drawer + conditionally render the link in the save-success view based on the toggle state. Add token-driven CSS to `debrief.css`. Write the vitest test file in `tauri/ui/src/debrief/__tests__/bravoh-waitlist-toggle.spec.ts` covering the 5 cases.</action>
  <verify>
    <automated>cd tauri/ui && npm test -- bravoh-waitlist-toggle 2>&1 | tee /tmp/44-04-task2.log; grep -q "PASS" /tmp/44-04-task2.log</automated>
  </verify>
  <done>Component + test + mount + CSS shipped; 5 vitest cases green; `BRAVOH_WAITLIST_URL` constant matches CONTEXT verbatim; debrief regression tests still green (`cd tauri/ui && npm test -- debrief` clean).</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/runtime/test_config_store_bravoh_waitlist.py -v` — green.
- `uv run pytest tests/runtime/test_config_store_*.py` — full config_store regression clean.
- `cd tauri/ui && npm test -- bravoh-waitlist-toggle debrief` — green.
- `grep -c "https://bravoh.com/waitlist?utm_source=vibemix&utm_medium=app&utm_campaign=oss-launch" tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts` — exactly 1 match (verbatim URL).
- Manual smoke: open debrief window with toggle OFF — no Bravoh link, no events.jsonl row. Toggle ON — link appears in save-success view, one row in events.jsonl. Toggle OFF — link hidden, no second row.
</verification>

<success_criteria>
- LAUNCH-05 closed engineering-green.
- Subtle, opt-in, not gating — anti-scope-creep memory `feedback_no_scope_creep_clean_utility` honored.
- Signed-out telemetry default-off — privacy contract preserved.
- UTM URL locked verbatim from CONTEXT.
</success_criteria>

<output>
After completion, create `.planning/phases/44-launch-positioning-pre-stage/44-04-SUMMARY.md`.
</output>
