---
phase: 44-launch-positioning-pre-stage
plan: 05
title: "SHIP-TWEET 5-channel copy lock + AI-slop grep gate + §LAUNCH-07 runbook (LAUNCH-07)"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dayzero/launch_copy/discord.txt
  - scripts/dayzero/launch_copy/twitter.txt
  - scripts/dayzero/launch_copy/instagram.txt
  - scripts/dayzero/launch_copy/linkedin.txt
  - scripts/dayzero/launch_copy/reddit.txt
  - scripts/launch/check_no_ai_slop.py
  - tests/launch/test_no_ai_slop.py
  - KAAN-ACTION-LEGAL.md
autonomous: true
requirements: [LAUNCH-07]
req_ids: [LAUNCH-07]
tags: [launch, social-copy, anti-slop, kaan-discharge, francesco]
must_haves:
  truths:
    - "All 5 launch-copy files exist under `scripts/dayzero/launch_copy/`: twitter.txt, instagram.txt, linkedin.txt, reddit.txt (already drafted Phase 36) + discord.txt (NEW this plan — the 5th channel)"
    - "Each of the 5 files carries a sign-off footer block with two signature lines: `Kaan signature: ____` and `Francesco signature: ____` + dated marker (CONTEXT §LAUNCH-07: 'Kaan-sign + Francesco-sign')"
    - "`scripts/launch/check_no_ai_slop.py` (new) reads all 5 launch-copy files and asserts: NONE of the 16 AI-slop blocklist tokens (CONTEXT §specifics verbatim list) appears in ANY file (case-insensitive); also asserts `\\bdeeply\\s+\\w+` regex matches zero times"
    - "Same script asserts the POSITIVE anchor phrases (CONTEXT §specifics): across the 5 files COMBINED (not per-file), each of {'real DJ friend in your ear', 'built by DJs', 'your audio doesn't leave', 'open-source' OR 'open source', 'Mac + Windows'} appears AT LEAST ONCE"
    - "`tests/launch/test_no_ai_slop.py` (new) runs the script against the real 5 files + 3 negative synthetic-copy cases (slop token, missing anchor, missing channel file)"
    - "`KAAN-ACTION-LEGAL.md` carries `## §LAUNCH-07 — SHIP-TWEET 5-channel copy sign-off` runbook (Kaan + Francesco mutual sign-off discharge in canonical §GATE-* format)"
    - "AI-slop blocklist is a single source of truth — exposed as a module-level tuple `AI_SLOP_BLOCKLIST` in `scripts/launch/check_no_ai_slop.py`; the 44-01 and 44-02 check scripts can optionally re-import this constant (planner-discretion DRY)"
    - "discord.txt (5th channel) drafted with #announcements-style framing — 5-10 lines, anchor phrases at least once, blocklist clean, ends with the sign-off footer block"
  artifacts:
    - path: "scripts/dayzero/launch_copy/discord.txt"
      provides: "5th channel launch copy (#announcements post for Bravoh Discord on T-0)"
      min_lines: 5
    - path: "scripts/launch/check_no_ai_slop.py"
      provides: "CI gate: AI-slop blocklist + anchor-phrase presence + signature-footer presence across 5 launch-copy files"
    - path: "tests/launch/test_no_ai_slop.py"
      provides: "Pins the no-slop check + 3 negative cases"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "§LAUNCH-07 sign-off discharge runbook"
      contains: "## §LAUNCH-07"
  key_links:
    - from: "scripts/launch/check_no_ai_slop.py"
      to: "scripts/dayzero/launch_copy/*.txt"
      via: "grep gate across all 5 files"
      pattern: "AI_SLOP_BLOCKLIST"
    - from: "KAAN-ACTION-LEGAL.md §LAUNCH-07"
      to: "scripts/dayzero/launch_copy/"
      via: "Francesco sign-off discharge runbook"
      pattern: "## §LAUNCH-07"
---

<objective>
Lock the 5-channel SHIP-TWEET launch copy: ship the missing 5th channel file (`discord.txt`), add Kaan + Francesco sign-off footers to all 5 files, ship `scripts/launch/check_no_ai_slop.py` as the CI gate enforcing the AI-slop blocklist + positive anchor phrases (CONTEXT §specifics verbatim list), and document Francesco's sign-off as the §LAUNCH-07 Kaan-discharge in `KAAN-ACTION-LEGAL.md`.

Purpose: LAUNCH-07 is the SHIP-TWEET copy-lock. The grep gate prevents AI-slop language regressions during the lock pass; the positive anchor phrases enforce the "real DJ friend in your ear" + "built by DJs" + "your audio doesn't leave" positioning across the launch surface. Francesco sign-off is the human credibility lock — engineering ships the runbook, Kaan + Francesco execute the signatures on the actual file headers.

Output: discord.txt drafted; 5 sign-off footers; check_no_ai_slop.py + test; §LAUNCH-07 runbook. Wave 1 — no file overlap with other wave-1 plans (44-03 / 44-04). Reuses CONTEXT §specifics AI-slop blocklist verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-launch-positioning-pre-stage/44-CONTEXT.md
@scripts/dayzero/launch_copy/twitter.txt
@scripts/dayzero/launch_copy/instagram.txt
@scripts/dayzero/launch_copy/linkedin.txt
@scripts/dayzero/launch_copy/reddit.txt
@scripts/launch/check_storyboard_palette.py
@KAAN-ACTION-LEGAL.md

<interfaces>
<!-- AI-slop blocklist (CONTEXT §specifics — VERBATIM; do not edit, do not extend): -->
<!--   leverage, synergize, revolutionize, game-changer, next-generation, -->
<!--   cutting-edge, seamless, robust, powerful, intuitive, delightful experience, -->
<!--   AI-powered, harness the power, unlock, transformative, paradigm -->
<!--   plus the regex: \bdeeply\s+\w+ -->

<!-- Positive anchor phrases (CONTEXT §specifics — VERBATIM): -->
<!--   "real DJ friend in your ear" -->
<!--   "built by DJs" -->
<!--   "your audio doesn't leave" -->
<!--   "open-source" OR "open source" -->
<!--   "Mac + Windows" -->

<!-- Existing twitter.txt drafts (Phase 36-era) — use as template for tone -->
<!-- when drafting discord.txt: short, punchy, anchor phrases woven in naturally. -->

<!-- KAAN-ACTION-LEGAL.md canonical §discharge format (§GATE-01 at line 832): -->
<!-- 8-block structure: REQ-ID / Owner / Status / Effort / Blocking-for / -->
<!-- Why-this-is-Kaan-action / Files-involved / Kaan-oneliner / Verification / -->
<!-- What-unblocks / Sign-off-block -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship `check_no_ai_slop.py` + test (TDD red — discord.txt doesn't exist yet)</name>
  <files>scripts/launch/check_no_ai_slop.py, tests/launch/test_no_ai_slop.py</files>
  <behavior>
    - Module-level `AI_SLOP_BLOCKLIST: tuple[str, ...]` — exact 16 tokens from CONTEXT §specifics, no additions
    - Module-level `ANCHOR_PHRASES: tuple[str, ...]` — the 5 positive phrases from CONTEXT §specifics
    - `LAUNCH_COPY_DIR = Path("scripts/dayzero/launch_copy")`
    - `LAUNCH_COPY_FILES = ("twitter.txt", "instagram.txt", "linkedin.txt", "reddit.txt", "discord.txt")`
    - Script reads all 5 files, asserts:
      a. All 5 files exist
      b. Each file contains BOTH `Kaan signature:` AND `Francesco signature:` markers (sign-off footer present, signatures may be blank — that's a Kaan-discharge concern, not engineering's gate)
      c. NONE of `AI_SLOP_BLOCKLIST` tokens appears in ANY file (case-insensitive substring)
      d. `\bdeeply\s+\w+` regex matches zero times across all files
      e. Across the 5 files combined, each anchor phrase appears at least once (case-insensitive)
    - Exit 0 on full pass; exit 1 + named failed gate otherwise
    - CLI: `--dir PATH` (default `scripts/dayzero/launch_copy`), `--quiet`
    - Test cases (mirror `test_readme_hero_lock.py`):
      i. Happy path against live 5 files — currently RED (discord.txt missing + signature footers absent)
      ii. Negative: tmp_path with a synthetic file carrying a slop token → exit 1
      iii. Negative: tmp_path with all 5 files but missing one anchor across the set → exit 1
      iv. Negative: tmp_path with one file missing the signature-footer markers → exit 1
  </behavior>
  <action>Create `scripts/launch/check_no_ai_slop.py` per the contract. Pin AI_SLOP_BLOCKLIST and ANCHOR_PHRASES VERBATIM from CONTEXT §specifics. Pair with `tests/launch/test_no_ai_slop.py` covering the happy path (currently RED) + 3 negative synthetic cases. Initial test run MUST be red on happy path.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_no_ai_slop.py::test_negative_slop_token tests/launch/test_no_ai_slop.py::test_negative_missing_anchor tests/launch/test_no_ai_slop.py::test_negative_missing_signature_footer -v</automated>
  </verify>
  <done>Check script + test files exist; 3 negative-case tests GREEN; happy path against real launch_copy/ RED (discord.txt absent + signatures absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draft `discord.txt` + add sign-off footers to all 5 channels (TDD green)</name>
  <files>scripts/dayzero/launch_copy/discord.txt, scripts/dayzero/launch_copy/twitter.txt, scripts/dayzero/launch_copy/instagram.txt, scripts/dayzero/launch_copy/linkedin.txt, scripts/dayzero/launch_copy/reddit.txt</files>
  <behavior>
    - `discord.txt` drafted as a Bravoh-#announcements T-0 post: 5-12 lines, opens with "vibemix is live —" header, hits at least 2 anchor phrases naturally, ends with a "github.com/bravoh/vibemix" link line, voice matches the existing twitter.txt punch + tone
    - All 5 files get a 4-line sign-off footer appended (between a blank line + the existing content):
      ```
      ---
      Kaan signature:     ____  (date: ____)
      Francesco signature: ____  (date: ____)
      Locked for: v3.0.0-rc1 launch
      ```
    - After both edits, the combined 5-file corpus contains EVERY anchor phrase at least once (sweep existing 4 files; add anchors to discord.txt or to whichever file is missing one — Claude's discretion which file to update if an anchor is short by one)
    - Re-run Task 1 happy-path test — MUST go GREEN
  </behavior>
  <action>Draft discord.txt per the brief — punchy, anchor-phrase-bearing, link-out to the repo. Add the 4-line sign-off footer to all 5 files. Audit the combined corpus against ANCHOR_PHRASES; if any anchor is missing, weave it into whichever file fits best (e.g. add "built by DJs" to instagram.txt if missing). Audit against AI_SLOP_BLOCKLIST; rewrite any offender. Confirm no `deeply <word>` constructions slip in.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_no_ai_slop.py -v</automated>
  </verify>
  <done>discord.txt drafted; all 5 files carry sign-off footer; all 5 anchor phrases covered across the corpus; zero blocklist hits; happy-path test GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: §LAUNCH-07 Kaan-discharge runbook (Francesco sign-off)</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <action>Append `## §LAUNCH-07 — SHIP-TWEET 5-channel copy sign-off` to KAAN-ACTION-LEGAL.md in canonical §GATE-01 format. Owner: Kaan + Francesco (mutual). Status checklist: ☐ all 5 files engineering-green ☐ Kaan signature ☐ Francesco signature ☐ locked-for tag confirms v3.0.0-rc1. Why-this-is-Kaan-action: Francesco sign-off requires Francesco's actual response; engineering can't fake it. Files involved: list all 5 launch_copy paths + check_no_ai_slop.py. Kaan oneliner: ascii diff lines showing where to sign + commit signature with `git commit -m "lock(launch): SHIP-TWEET 5-channel copy locked v3.0.0-rc1"`. Verification: `uv run pytest tests/launch/test_no_ai_slop.py` green + signatures non-blank. What unblocks: SHIP-TWEET (P45 SHIP-08) live publish via `launch_trigger.sh --publish`. Sign-off block: dates + Kaan-signature + Francesco-signature blanks.</action>
  <verify>
    <automated>grep -c "^## §LAUNCH-07" KAAN-ACTION-LEGAL.md | grep -q "^1$"</automated>
  </verify>
  <done>§LAUNCH-07 runbook in place; canonical 8-block format; references all 5 launch_copy files + check_no_ai_slop.py; sign-off block ready for Kaan + Francesco to date + signature on actual lock.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/launch/test_no_ai_slop.py -v` — all assertions green.
- `uv run python scripts/launch/check_no_ai_slop.py` — exit 0 (assuming current launch_copy/ has no slop after Task 2).
- `ls scripts/dayzero/launch_copy/*.txt | wc -l` — exactly 5.
- `grep -l "Kaan signature:" scripts/dayzero/launch_copy/*.txt | wc -l` — exactly 5.
- `grep -c "^## §LAUNCH-07" KAAN-ACTION-LEGAL.md` — exactly 1.
</verification>

<success_criteria>
- LAUNCH-07 closed engineering-green; Francesco-sign-off + Kaan-sign-off are the only remaining KAAN-ACTION discharge items.
- ROADMAP Phase 44 success criterion 5 satisfied (engineering side): "SHIP-TWEET copy files signed off (Kaan + Francesco mutual approval) for all 5 channels (twitter/instagram/linkedin/reddit/discord)." Engineering has shipped all 5 + the gate + the runbook; sign-off is a 60-second Kaan+Francesco discharge.
- AI-slop blocklist is now a CI gate across launch surfaces, not an aspiration.
</success_criteria>

<output>
After completion, create `.planning/phases/44-launch-positioning-pre-stage/44-05-SUMMARY.md`.
</output>
