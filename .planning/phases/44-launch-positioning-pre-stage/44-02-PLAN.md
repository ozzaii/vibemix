---
phase: 44-launch-positioning-pre-stage
plan: 02
title: "DJ-software grid + controller grid + README a11y check (LAUNCH-03 + LAUNCH-04)"
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - README.md
  - docs/assets/dj-software/.gitkeep
  - docs/assets/dj-software/rekordbox.svg
  - docs/assets/dj-software/serato.svg
  - docs/assets/dj-software/traktor.svg
  - docs/assets/dj-software/djay-pro.svg
  - docs/assets/dj-software/virtualdj.svg
  - docs/assets/dj-software/mixxx.svg
  - scripts/launch/check_readme_grids_a11y.py
  - tests/launch/test_readme_grids_a11y.py
  - KAAN-ACTION-LEGAL.md
autonomous: true
requirements: [LAUNCH-03, LAUNCH-04]
req_ids: [LAUNCH-03, LAUNCH-04]
tags: [readme, launch, controllers, dj-software, a11y]
must_haves:
  truths:
    - "README.md carries a new '## Works alongside whatever DJ app you already use' section with a 6-cell logo grid: rekordbox, Serato, Traktor, djay Pro, VirtualDJ, Mixxx (CONTEXT §LAUNCH-03)"
    - "Each DJ-software cell has a real `<img src=docs/assets/dj-software/<slug>.svg alt='<app name> logo' width=...>` — SVG placeholders ship in this plan (text-based wordmark per CONTEXT discretion); real-logo upload is §LAUNCH-03 KAAN-discharge"
    - "README.md '## Supported controllers' section is updated to the canonical 10 mapped controllers, sourced from `src/vibemix/midi/controllers/*.json` (10 files: ddj-200, ddj-400, ddj-flx4, ddj-rev1, kontrol-s2, kontrol-s4, mc-6000, mc-7000, mixtrack-platinum-fx, mixtrack-pro-fx)"
    - "Each controller cell has alt-text per LAUNCH-04 ('<vendor> <model>') and a 'Calibrate any other controller' callout linking to `docs/midi-mapping.md` (CONTEXT §LAUNCH-04)"
    - "`scripts/launch/check_readme_grids_a11y.py` (new) asserts: every `<img>` in README.md has a non-empty `alt=` attribute; DJ-software grid has exactly 6 cells; controller grid has exactly 10 cells; alt-text doesn't contain blocklist tokens; grid is balanced (3×2 or 2×3 for DJ-software, 5×2 or 2×5 for controllers)"
    - "`tests/launch/test_readme_grids_a11y.py` (new) exercises the script with the real README + 3 negative cases (missing alt, wrong cell count, unbalanced grid)"
    - "`KAAN-ACTION-LEGAL.md` carries `## §LAUNCH-03 — DJ-software real logo upload` runbook + `## §LAUNCH-04 — Controller logo discharge` runbook in canonical §GATE-* format"
    - "Existing README.md 'Supported controllers' table (currently lists 10 Pioneer/Numark/Hercules controllers but doesn't match the actual JSON profile set under `src/vibemix/midi/controllers/`) is reconciled to match the actual canonical 10 from `src/vibemix/midi/controllers/*.json` — drift between README and source-of-truth closed"
  artifacts:
    - path: "docs/assets/dj-software/*.svg"
      provides: "6 SVG placeholder wordmark logos for rekordbox, Serato, Traktor, djay Pro, VirtualDJ, Mixxx"
      min_lines: 1
    - path: "scripts/launch/check_readme_grids_a11y.py"
      provides: "CI-gate enforcing alt-text + cell count + grid balance for both README grids"
    - path: "tests/launch/test_readme_grids_a11y.py"
      provides: "Pins the a11y check; runs in `pytest tests/launch`"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "§LAUNCH-03 + §LAUNCH-04 discharge runbooks (real-logo upload)"
      contains: "## §LAUNCH-03"
  key_links:
    - from: "README.md"
      to: "src/vibemix/midi/controllers/*.json"
      via: "controller grid sourced from canonical JSON profile set"
      pattern: "ddj-flx4\\.json|kontrol-s2\\.json"
    - from: "scripts/launch/check_readme_grids_a11y.py"
      to: "README.md"
      via: "HTML parse + alt-text + cell count assertions"
      pattern: "Supported controllers|Works alongside"
    - from: "KAAN-ACTION-LEGAL.md §LAUNCH-03"
      to: "docs/assets/dj-software/"
      via: "real-logo upload discharge runbook"
      pattern: "## §LAUNCH-03"
---

<objective>
Add a 6-cell DJ-software-logo grid (rekordbox / Serato / Traktor / djay Pro / VirtualDJ / Mixxx) to README.md, refresh the existing supported-controllers grid to match the canonical 10 controllers in `src/vibemix/midi/controllers/*.json`, ship a `check_readme_grids_a11y.py` CI gate, and document the real-logo upload as a Kaan-discharge in `KAAN-ACTION-LEGAL.md §LAUNCH-03` + `§LAUNCH-04`.

Purpose: LAUNCH-03 + LAUNCH-04 close the "this works with my software" + "this works with my controller" objections at first glance. The README grids are the primary surface where a visiting DJ decides whether vibemix is for them. Alt-text + cell-count + balance gates prevent broken-render regressions during launch. Real-logo upload requires Kaan's hand (trademark-aware sourcing) so it's pre-staged via runbook, not blocked.

Output: Two new README sections (DJ-software grid + refreshed controller grid), six SVG placeholder logos under `docs/assets/dj-software/`, a11y check script + pinning test, and §LAUNCH-03/§LAUNCH-04 runbooks in KAAN-ACTION-LEGAL.md. Serializes after 44-01 because both plans edit README.md (file ownership rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-launch-positioning-pre-stage/44-CONTEXT.md
@.planning/phases/44-launch-positioning-pre-stage/44-01-PLAN.md
@README.md
@KAAN-ACTION-LEGAL.md
@scripts/launch/check_storyboard_palette.py

<interfaces>
<!-- Canonical 10 controller profiles — README MUST mirror this exact set: -->
<!--   src/vibemix/midi/controllers/ddj-200.json -->
<!--   src/vibemix/midi/controllers/ddj-400.json -->
<!--   src/vibemix/midi/controllers/ddj-flx4.json -->
<!--   src/vibemix/midi/controllers/ddj-rev1.json -->
<!--   src/vibemix/midi/controllers/kontrol-s2.json (Native Instruments Traktor Kontrol S2) -->
<!--   src/vibemix/midi/controllers/kontrol-s4.json (Native Instruments Traktor Kontrol S4) -->
<!--   src/vibemix/midi/controllers/mc-6000.json (Denon DJ MC6000) -->
<!--   src/vibemix/midi/controllers/mc-7000.json (Denon DJ MC7000) -->
<!--   src/vibemix/midi/controllers/mixtrack-platinum-fx.json (Numark Mixtrack Platinum FX) -->
<!--   src/vibemix/midi/controllers/mixtrack-pro-fx.json (Numark Mixtrack Pro FX) -->

<!-- Existing README.md "Supported controllers" table lists wrong models (FLX6/FLX10/1000/SX3/XDJ-RX3/Numark Party Mix Live/Hercules Inpulse 300/500) — drift between README and source-of-truth. Plan 44-02 reconciles. -->

<!-- KAAN-ACTION-LEGAL.md canonical §discharge format (per §GATE-01 at line 832): -->
<!-- ## §LAUNCH-XX — <title> -->
<!-- **REQ-ID:** LAUNCH-XX (Phase 44-02) -->
<!-- **Owner:** Kaan -->
<!-- **Status:** ☐ pre-discharge ☐ ... -->
<!-- **Effort:** ~N minutes -->
<!-- **Blocking for:** <downstream> -->
<!-- ### Why this is KAAN-action -->
<!-- ### Files involved -->
<!-- ### Kaan oneliner -->
<!-- ### Verification -->
<!-- ### What unblocks -->
<!-- ### Sign-off block -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A11y check script + test (TDD red)</name>
  <files>scripts/launch/check_readme_grids_a11y.py, tests/launch/test_readme_grids_a11y.py</files>
  <behavior>
    - Script parses README.md, locates the two grid sections by H2/H3 heading match: "Works alongside whatever DJ app" (LAUNCH-03 section) + "Supported controllers" (LAUNCH-04 section)
    - For each grid, extracts all `<img ...>` tags within the section bounds and asserts:
      a. Every `<img>` has `alt="..."` with a non-empty value (no `alt=""`, no missing `alt`)
      b. DJ-software grid: exactly 6 `<img>` cells
      c. Controller grid: exactly 10 `<img>` cells
      d. Alt-text contains none of the AI-slop blocklist tokens from 44-01 (re-use the same constant)
      e. Grid balance: DJ-software cell count is divisible by 2 or 3 (so it's 3×2 or 2×3); controller cell count is divisible by 2 or 5 (so it's 5×2 or 2×5)
    - Exits 0 if all pass; exits 1 + names which gate failed
    - Pytest mirrors `test_readme_hero_lock.py` shape from 44-01: subprocess invocation against live README (currently RED — DJ-software grid doesn't exist yet) + 3 negative synthetic-README cases
  </behavior>
  <action>Create `scripts/launch/check_readme_grids_a11y.py` per behavior. Use Python `re` + simple section-bounds parsing (find the H2 line, read until next H2). Import the AI-slop blocklist from `scripts.launch.check_readme_hero_lock` (rename module to allow import) OR copy the tuple verbatim (Claude's discretion — single-source-of-truth is cleaner; if you import, expose `_AI_SLOP_BLOCKLIST` as a public-by-convention module attr). Pair with `tests/launch/test_readme_grids_a11y.py`. Initial test run MUST be RED because the LAUNCH-03 section doesn't exist yet.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_readme_grids_a11y.py::test_negative_missing_alt tests/launch/test_readme_grids_a11y.py::test_negative_wrong_cell_count -v</automated>
  </verify>
  <done>Check script + test files exist; negative-case tests GREEN (script correctly rejects synthetic violations); happy-path test against real README RED (DJ-software grid not yet present).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship 6 DJ-software SVG placeholder wordmarks + add "Works alongside" grid section to README + reconcile controller grid to canonical 10 (TDD green)</name>
  <files>docs/assets/dj-software/rekordbox.svg, docs/assets/dj-software/serato.svg, docs/assets/dj-software/traktor.svg, docs/assets/dj-software/djay-pro.svg, docs/assets/dj-software/virtualdj.svg, docs/assets/dj-software/mixxx.svg, README.md</files>
  <behavior>
    - Six SVG files written under `docs/assets/dj-software/` — each is a minimal text-wordmark SVG (CONTEXT §Claude's discretion: text-based SVG with brand name preferred over boxed initials for clarity). Each SVG: 200×80px viewBox, single `<text>` element rendering the app name in a generic sans-serif (system font, not bundled), grey-on-transparent (`fill="#888"`); accompanied by a comment `<!-- placeholder per KAAN-ACTION-LEGAL §LAUNCH-03 — real logo via Kaan-discharge -->`
    - README.md gets a new H2 section inserted between the "## No AI slop" hook (added in 44-01) and "## Install":
      `## Works alongside whatever DJ app you already use`
      Followed by a brief one-paragraph framing (`vibemix doesn't care which DJ app you run — it listens to the master output. Confirmed working with:`) and a 2-row × 3-col `<table>` of 6 `<img>` cells (each cell labels the app underneath the logo)
    - Existing README.md `## Supported controllers` table is REPLACED with the canonical 10 from `src/vibemix/midi/controllers/*.json`, mapped to vendor + model display names (e.g. `ddj-flx4.json` → "Pioneer DDJ-FLX4"; `kontrol-s2.json` → "Native Instruments Traktor Kontrol S2"; `mc-6000.json` → "Denon DJ MC6000"; `mixtrack-platinum-fx.json` → "Numark Mixtrack Platinum FX"). Existing logo references under `docs/assets/controllers/` are kept for the controllers that already have them (DDJ-FLX4 and DDJ-400 — verify via `ls docs/assets/controllers/`); the rest get text-wordmark SVG placeholders following the same pattern as DJ-software (write them only if the PNG/SVG doesn't already exist)
    - "Calibrate any other controller — see [docs/midi-mapping.md](docs/midi-mapping.md)." callout preserved
    - The a11y check from Task 1 now passes against the live README
  </behavior>
  <action>Ship the 6 DJ-software SVG placeholders per CONTEXT §Claude's discretion text-based wordmark style. Insert the "Works alongside whatever DJ app you already use" section into README.md immediately after the "## No AI slop" hook (from 44-01). Replace the existing "## Supported controllers" table with the canonical 10 sourced from `src/vibemix/midi/controllers/*.json` — read the JSON files to discover the canonical `vendor` + `model` fields if present, otherwise derive from filename. For controllers without an existing logo asset under `docs/assets/controllers/`, ship a text-wordmark SVG placeholder following the DJ-software pattern. Re-run the Task 1 test suite; happy path MUST go GREEN.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_readme_grids_a11y.py -v</automated>
  </verify>
  <done>6 DJ-software SVG placeholders shipped; "Works alongside" section in README; controller grid reconciled to canonical 10 (matches `src/vibemix/midi/controllers/*.json` exactly); calibrate callout preserved; a11y check + pytest GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: §LAUNCH-03 + §LAUNCH-04 KAAN-discharge runbooks</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <action>Append two new sections to `KAAN-ACTION-LEGAL.md`: `## §LAUNCH-03 — DJ-software real logo upload` and `## §LAUNCH-04 — Controller logo discharge`. Follow the canonical §GATE-01 format (REQ-ID / Owner / Status / Effort / Blocking-for / Why-this-is-Kaan-action / Files-involved / Kaan-oneliner / Verification / What-unblocks / Sign-off-block). §LAUNCH-03 documents: 6 placeholder SVGs shipped engineering-side; Kaan to source trademark-compliant logos (rekordbox dotted-square mark, Serato wordmark, Traktor "+T", djay Pro icon, VirtualDJ "DJ" mark, Mixxx wave) — optimized PNG or SVG at 200×80px, dropped into `docs/assets/dj-software/<slug>.svg|png`, README `<img src>` updated. §LAUNCH-04 documents: same pattern for the 10 controllers, but only the rows where placeholder still in use (DDJ-FLX4 + DDJ-400 may already have real logos — confirm via existing `docs/assets/controllers/` ls). Verification line: `uv run pytest tests/launch/test_readme_grids_a11y.py` stays green after real-logo swap. Sign-off block per template.</action>
  <verify>
    <automated>grep -c "^## §LAUNCH-0[34]" KAAN-ACTION-LEGAL.md | grep -q "^2$"</automated>
  </verify>
  <done>Both §LAUNCH-03 and §LAUNCH-04 runbook sections present in KAAN-ACTION-LEGAL.md, each with the canonical 8-block §GATE-* structure.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/launch/test_readme_grids_a11y.py -v` — all assertions green.
- `uv run pytest tests/launch/test_readme_hero_lock.py -v` — still green (44-01 regression check).
- `ls docs/assets/dj-software/` — exactly 6 SVG files: rekordbox, serato, traktor, djay-pro, virtualdj, mixxx.
- `grep -c "src=\"docs/assets/dj-software/" README.md` — exactly 6 matches.
- README controller grid matches `ls src/vibemix/midi/controllers/*.json | wc -l` (= 10).
- `grep -c "^## §LAUNCH-0[34]" KAAN-ACTION-LEGAL.md` — exactly 2.
</verification>

<success_criteria>
- LAUNCH-03 + LAUNCH-04 closed engineering-green.
- ROADMAP Phase 44 success criterion 3 satisfied: "DJ-software grid + controllers grid render in README; alt-text + accessibility checks pass."
- README controller-grid drift closed: README is now sourced-of-truth-by-grep from `src/vibemix/midi/controllers/*.json`.
- Real-logo upload is pre-staged as Kaan-discharge — no blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/44-launch-positioning-pre-stage/44-02-SUMMARY.md`.
</output>
