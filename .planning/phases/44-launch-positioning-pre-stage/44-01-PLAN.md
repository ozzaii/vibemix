---
phase: 44-launch-positioning-pre-stage
plan: 01
title: "README hero rewrite + 'no AI slop' hook section (LAUNCH-01)"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - tests/launch/test_readme_hero_lock.py
  - scripts/launch/check_readme_hero_lock.py
autonomous: true
requirements: [LAUNCH-01]
req_ids: [LAUNCH-01]
tags: [readme, launch, hero, anti-slop]
must_haves:
  truths:
    - "README.md hero `<em>` tagline reads VERBATIM: 'the only AI co-host that actually listens to your set' (CONTEXT §specifics locked one-liner — no variations, no tweaks)"
    - "A new '## No AI slop' section appears immediately below the hero block (after the closing `<!-- vibemix:hero-end -->` sentinel, before the existing `## Install` heading)"
    - "The 'No AI slop' section contains 2-3 paragraphs hitting these anchor phrases verbatim (CONTEXT §specifics positive list): 'real DJ friend in your ear', 'built by DJs', 'your audio doesn't leave', 'open source' (or 'open-source'), 'Mac + Windows'"
    - "Existing hero artefacts preserved: `<picture>`/`<video>` block, `docs/assets/hero.png` reference, `docs/assets/demo.mp4` reference + `docs/assets/demo-placeholder.gif` fallback, vibemix:hero-start/end sentinels, sha256=PLACEHOLDER sentinel for `scripts/check_readme_hero_hash.py`"
    - "AI-slop blocklist (CONTEXT §specifics): NONE of these tokens appear in README.md: 'leverage', 'synergize', 'revolutionize', 'game-changer', 'next-generation', 'cutting-edge', 'seamless', 'robust', 'powerful', 'intuitive', 'delightful experience', 'AI-powered', 'harness the power', 'unlock', 'transformative', 'paradigm', plus any 'deeply <adverb>' construction"
    - "`scripts/launch/check_readme_hero_lock.py` (new) asserts the locked one-liner appears verbatim AND the anchor phrases all appear AND the blocklist is empty"
    - "`tests/launch/test_readme_hero_lock.py` (new) exercises the check script + pins the contract"
  artifacts:
    - path: "README.md"
      provides: "Hero one-liner + 'No AI slop' anti-slop hook section"
      contains: "the only AI co-host that actually listens to your set"
    - path: "scripts/launch/check_readme_hero_lock.py"
      provides: "CI-gate script enforcing locked hero one-liner + anchor phrases + AI-slop blocklist"
    - path: "tests/launch/test_readme_hero_lock.py"
      provides: "Pins the check script contract; runs as part of `pytest tests/launch`"
  key_links:
    - from: "README.md"
      to: "scripts/launch/check_readme_hero_lock.py"
      via: "grep gate enforcement"
      pattern: "the only AI co-host that actually listens to your set"
    - from: "tests/launch/test_readme_hero_lock.py"
      to: "scripts/launch/check_readme_hero_lock.py"
      via: "subprocess invocation + exit code assertion"
      pattern: "check_readme_hero_lock\\.py"
---

<objective>
Rewrite the README.md hero to frontload the locked one-liner "the only AI co-host that actually listens to your set" (CONTEXT §specifics, ROADMAP Phase 44 success criterion 1) and ship a new "No AI slop" hook section immediately below the hero. Add a CI-gate check script + test pinning both the positive contract (locked one-liner + anchor phrases) and the negative contract (AI-slop blocklist).

Purpose: README is the public face of the OSS launch (LAUNCH-01). The hero one-liner is the lock; the hook section is the anti-slop credibility move that pre-empts the "another AI assistant" framing. Anchor phrases (`real DJ friend in your ear`, `built by DJs`, `your audio doesn't leave`, `open source`, `Mac + Windows`) are the positive surface; the blocklist (`leverage`, `synergize`, `seamless`, etc. — CONTEXT §specifics) is the negative gate.

Output: Updated README.md hero + hook section; new check script + test; both wired into the standard `tests/launch/` suite that already runs in CI alongside `test_storyboard_palette.py` and `test_ui_audit_driver.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-launch-positioning-pre-stage/44-CONTEXT.md
@README.md
@scripts/launch/check_storyboard_palette.py
@tests/launch/test_storyboard_palette.py

<interfaces>
<!-- README.md hero block as it currently stands (lines 1-37 of README.md). -->
<!-- Locked artefacts that MUST be preserved verbatim through this edit: -->

```
<p align="center">
  <img src="docs/assets/hero.png" alt="vibemix — AI co-host for your DJ set" width="100%" />
</p>

<h1 align="center">vibemix</h1>

<p align="center"><em>An AI co-host for your DJ set. Open source. Mac + Windows.</em></p>

<!-- vibemix:hero-start sha256=PLACEHOLDER path=docs/assets/demo.mp4 -->
...
<!-- vibemix:hero-end -->
```

The hero `<em>` tag is what's being rewritten — replace
"An AI co-host for your DJ set. Open source. Mac + Windows."
with the locked one-liner
"the only AI co-host that actually listens to your set".

The "Open source. Mac + Windows." framing migrates into the new
"## No AI slop" section anchor phrases (preserving the existing
`scripts/check_readme_hero_hash.py` happy path since that hash gate
checks the `vibemix:hero-start sha256=...` sentinel, not the `<em>` text).

<!-- check_storyboard_palette.py pattern: subprocess script + exit code, paired with a pytest in tests/launch/ that runs the script and asserts the exit code. Use the same pattern here. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write hero-lock check script + failing test (TDD red)</name>
  <files>scripts/launch/check_readme_hero_lock.py, tests/launch/test_readme_hero_lock.py</files>
  <behavior>
    - `check_readme_hero_lock.py` reads README.md and asserts:
      a. Locked one-liner appears verbatim exactly once: "the only AI co-host that actually listens to your set"
      b. All five anchor phrases appear at least once each (case-insensitive): "real DJ friend in your ear", "built by DJs", "your audio doesn't leave", "open source" OR "open-source", "Mac + Windows"
      c. AI-slop blocklist (case-insensitive substring): NONE of {"leverage", "synergize", "revolutionize", "game-changer", "next-generation", "cutting-edge", "seamless", "robust", "powerful", "intuitive", "delightful experience", "AI-powered", "harness the power", "unlock", "transformative", "paradigm"} appears in README.md
      d. The "deeply <word>" regex `\bdeeply\s+\w+` matches zero times
      e. Exits 0 if all pass; exits 1 + prints which gate failed otherwise
    - `test_readme_hero_lock.py` runs the script via subprocess against the live README.md and asserts exit 0. Also includes 2 negative-case tests: synthetic README copies (in tmp_path) that violate one rule each, asserting exit 1.
    - Pattern reuse: mirror `scripts/launch/check_storyboard_palette.py` + `tests/launch/test_storyboard_palette.py` shape (CLI script with `argparse` + `sys.exit`, paired pytest with `subprocess.run` and `capfd`).
    - Initial run MUST be RED — README.md hero still contains the old `<em>` tagline, so the locked one-liner gate fails.
  </behavior>
  <action>Create `scripts/launch/check_readme_hero_lock.py` per LAUNCH-01 spec: locked one-liner + 5 anchor phrases + 15-token AI-slop blocklist + `\bdeeply\s+\w+` regex gate. CLI accepts `--readme PATH` (default `README.md`) and `--quiet`. Pair with `tests/launch/test_readme_hero_lock.py` covering happy path (real README, currently RED) + 2 negative synthetic-README cases (locked one-liner missing, slop token present). Pin AI-slop blocklist as a `_AI_SLOP_BLOCKLIST` module-level tuple (case-insensitive). Use the exact CONTEXT §specifics list — no additions, no removals.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_readme_hero_lock.py -v 2>&1 | tee /tmp/44-01-task1.log; ! grep -q "test_real_readme_passes_lock PASSED" /tmp/44-01-task1.log</automated>
  </verify>
  <done>Check script + test files exist; happy-path test FAILS (locked one-liner not yet in README — TDD red); negative-case tests PASS (script correctly rejects synthetic slop).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite README.md hero `<em>` tagline + insert "No AI slop" hook section (TDD green)</name>
  <files>README.md</files>
  <behavior>
    - Hero `<em>` line replaced: old "An AI co-host for your DJ set. Open source. Mac + Windows." → new "the only AI co-host that actually listens to your set"
    - New `## No AI slop` heading inserted after `<!-- vibemix:hero-end -->` and BEFORE the existing badge row (or between hero-end and the existing `**A real DJ friend in your ear...**` paragraph — Claude's discretion which fits cleanest given that paragraph already hits the "real DJ friend" anchor; the existing paragraph stays as-is, the new H2 sits above or below it)
    - Section body: 2-3 paragraphs that hit ALL five anchor phrases: "real DJ friend in your ear", "built by DJs", "your audio doesn't leave" (followed naturally by "your machine" or "the machine"), "open source" or "open-source", "Mac + Windows"
    - No AI-slop blocklist token appears anywhere in the new section
    - Existing artefacts UNCHANGED: `docs/assets/hero.png`, `docs/assets/demo.mp4`, `docs/assets/demo-placeholder.gif`, `vibemix:hero-start/end` sentinels with `sha256=PLACEHOLDER`, all 5 shield badges, `## Install`, all downstream sections
    - The check script from Task 1 now passes against the live README.
  </behavior>
  <action>Edit README.md per the behavior block. Replace the hero `<em>` text verbatim. Insert the new `## No AI slop` H2 section with 2-3 paragraphs that organically hit all 5 anchor phrases. Keep the section concise — 200-400 words. Voice: direct, plainspoken, no marketing flourish. Mirror the existing README tone (the "A real DJ friend in your ear — no AI slop." sentence below the badges already sets the register). Re-run the Task 1 test suite; it MUST go GREEN.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_readme_hero_lock.py -v</automated>
  </verify>
  <done>README.md hero one-liner replaced verbatim; new "## No AI slop" section present; all 5 anchor phrases hit; zero blocklist hits; check script + pytest both GREEN; existing hero artefacts (img/video/sentinels/sha256 placeholder) preserved.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/launch/test_readme_hero_lock.py -v` — all assertions green.
- `uv run python scripts/launch/check_readme_hero_lock.py` — exit 0.
- `uv run python scripts/launch/check_readme_hero_lock.py --readme /dev/null` — exit 1 (negative sanity).
- `grep -F "the only AI co-host that actually listens to your set" README.md` — exactly one match.
- `grep -iEc "leverage|synergize|revolutionize|game-changer|next-generation|cutting-edge|seamless|robust|powerful|intuitive|delightful experience|AI-powered|harness the power|unlock|transformative|paradigm" README.md` (with comment-line filter via `grep -v '^<!--'`) — zero matches.
- `uv run python scripts/check_readme_hero_hash.py` (existing Phase 39 gate) — still green; sha256=PLACEHOLDER sentinel preserved.
</verification>

<success_criteria>
- LAUNCH-01 closed engineering-green.
- ROADMAP Phase 44 success criterion 1 satisfied: "README hero section frontloads 'the only AI co-host that actually listens to your set'; one-line opinionated pitch above fold; static screenshot or GIF in place; demo.mp4 references resolvable post-shoot."
- Hero one-liner locked in via verbatim grep gate (no future drift).
- AI-slop blocklist becomes a CI gate, not an aspiration.
</success_criteria>

<output>
After completion, create `.planning/phases/44-launch-positioning-pre-stage/44-01-SUMMARY.md`.
</output>
