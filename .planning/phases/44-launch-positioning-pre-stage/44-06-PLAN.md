---
phase: 44-launch-positioning-pre-stage
plan: 06
title: "Bravoh GH org standup + Discord auto-provision dry-run lock + check scripts (LAUNCH-06 + LAUNCH-08)"
type: execute
wave: 2
depends_on: [44-05]
files_modified:
  - scripts/launch/check_bravoh_org_ready.sh
  - scripts/dayzero/discord_provision.py
  - scripts/dayzero/discord_taxonomy.json
  - tests/dayzero/test_discord_provision_dryrun.py
  - tests/launch/test_check_bravoh_org_ready.sh
  - KAAN-ACTION-LEGAL.md
autonomous: true
requirements: [LAUNCH-06, LAUNCH-08]
req_ids: [LAUNCH-06, LAUNCH-08]
tags: [github-org, discord, kaan-discharge, dayzero, pre-stage]
must_haves:
  truths:
    - "`scripts/launch/check_bravoh_org_ready.sh` (new) polls `https://api.github.com/orgs/bravoh` via `gh api` (or `curl` fallback) and exits 0 if the org exists, 1 otherwise; pre-stage scaffolding for Plan 45 SHIP-TRANSFER (CONTEXT §LAUNCH-06)"
    - "`scripts/dayzero/discord_provision.py` confirmed to have `--dry-run` mode (default per its current docstring); if missing, added; dry-run prints channel + role plan with zero API calls (CONTEXT §LAUNCH-08)"
    - "Channel + role taxonomy locked via `scripts/dayzero/discord_taxonomy.json` — single source of truth that `discord_provision.py` reads at runtime; existing in-code TARGET_ROLES + TARGET_CHANNELS tuples are migrated to consume from the JSON"
    - "Taxonomy contents (CONTEXT §LAUNCH-08 locked set, MERGED with existing in-script taxonomy): roles = ['founder', 'contributor', 'DJ', 'lurker', 'moderator']; channels = ['announcements', 'general', 'help', 'show-and-tell', 'controllers', 'ai-misbehavior', 'dev', 'bugs', 'showcase']"
    - "NOTE on taxonomy merge: CONTEXT §LAUNCH-08 lists `#general / #help / #showcase / #announcements / #bugs` + roles `@member / @moderator / @kaan / @francesco`. Existing script ships `announcements / help / show-and-tell / controllers / ai-misbehavior / dev` + `founder / contributor / DJ / lurker`. CONTEXT is the lock target; merged set preserves existing script channels (no removals — would regress prior Phase 36 work) and adds CONTEXT additions. Personal-role `@kaan / @francesco` collapse to `founder` (existing). `@member` collapses to `lurker` (existing). `@moderator` is NEW (added). This merge resolution is the canonical taxonomy."
    - "`BRAVOH_DISCORD_BOT_TOKEN` env var slot prepared (CONTEXT §LAUNCH-08); discord_provision.py reads it (existing) + GH secret stub documented in §LAUNCH-08 runbook (live execution = SHIP-09 in P45)"
    - "`tests/dayzero/test_discord_provision_dryrun.py` (new or extend existing) pins: dry-run mode default; dry-run prints all 9 channels + 5 roles per taxonomy.json; dry-run makes ZERO network calls (assert via httpx/requests transport mock); taxonomy JSON is parseable + matches expected set"
    - "`tests/launch/test_check_bravoh_org_ready.sh` (new bash test or pytest wrapping subprocess) covers: org-exists path (mock 200) → exit 0; org-missing path (mock 404) → exit 1"
    - "`KAAN-ACTION-LEGAL.md` carries `## §LAUNCH-06 — Bravoh GH org standup` runbook (billing-flag resolution + org creation steps) + `## §LAUNCH-08 — Discord live-execution discharge` runbook (bot-token sourcing + `--live` execution) — both in canonical §GATE-* format"
  artifacts:
    - path: "scripts/launch/check_bravoh_org_ready.sh"
      provides: "Polling readiness check for Plan 45 SHIP-TRANSFER gate"
      min_lines: 20
    - path: "scripts/dayzero/discord_taxonomy.json"
      provides: "Locked channel + role taxonomy (single source of truth)"
    - path: "scripts/dayzero/discord_provision.py"
      provides: "Confirmed --dry-run + taxonomy.json consumption + bot-token env var slot"
    - path: "tests/dayzero/test_discord_provision_dryrun.py"
      provides: "Pins dry-run = zero network + taxonomy parsing"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "§LAUNCH-06 + §LAUNCH-08 discharge runbooks"
      contains: "## §LAUNCH-06"
  key_links:
    - from: "scripts/dayzero/discord_provision.py"
      to: "scripts/dayzero/discord_taxonomy.json"
      via: "json.load at startup → TARGET_ROLES + TARGET_CHANNELS derived"
      pattern: "discord_taxonomy\\.json"
    - from: "KAAN-ACTION-LEGAL.md §LAUNCH-06"
      to: "scripts/launch/check_bravoh_org_ready.sh"
      via: "verification one-liner"
      pattern: "check_bravoh_org_ready\\.sh"
    - from: "scripts/launch/check_bravoh_org_ready.sh"
      to: "Plan 45 SHIP-TRANSFER"
      via: "exits 0 unblocks SHIP-TRANSFER gate"
      pattern: "api\\.github\\.com/orgs/bravoh"
---

<objective>
Pre-stage the two external-execution items that block public launch: Bravoh GitHub org standup (LAUNCH-06) + Discord auto-provision (LAUNCH-08). Engineering ships the readiness-check script, the taxonomy lock, the dry-run verification, and the discharge runbooks. Live execution = Kaan-discharge (billing-flag resolution + bot-token sourcing both need Kaan's Bravoh-billing login + Bravoh Discord admin seat).

Purpose: LAUNCH-06 produces the `bravoh` org that Plan 45 SHIP-TRANSFER consumes. LAUNCH-08 produces the provisioned Discord server that Plan 45 SHIP-DISCORD consumes. Both need a real external login at execution time, but the engineering surface (taxonomy lock, dry-run, polling script, runbook) ships now so the discharge is a 5-minute Kaan task at P45 launch time.

Output: `check_bravoh_org_ready.sh` polling script + test; `discord_taxonomy.json` + provision script refactor + dry-run pinning; §LAUNCH-06 + §LAUNCH-08 runbooks. Wave 2 — serializes after 44-05 because both plans append to KAAN-ACTION-LEGAL.md (file ownership rule: same-file modifications must serialize).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-launch-positioning-pre-stage/44-CONTEXT.md
@scripts/dayzero/discord_provision.py
@scripts/dayzero/healthz_check.sh
@scripts/launch/sync_github_meta.sh
@KAAN-ACTION-LEGAL.md

<interfaces>
<!-- Existing scripts/dayzero/discord_provision.py shape: -->
<!-- - TARGET_ROLES: tuple = ("founder", "contributor", "DJ", "lurker") -->
<!-- - TARGET_CHANNELS: tuple = ("announcements", "help", "show-and-tell", "controllers", "ai-misbehavior", "dev") -->
<!-- - GUILD_NAME: str = "vibemix" -->
<!-- - argparse: --live (default off), --guild-id, --dry-run is the implicit default per docstring -->
<!-- - reads DISCORD_BOT_TOKEN env var (note: CONTEXT specifies BRAVOH_DISCORD_BOT_TOKEN — accept both for back-compat, prefer BRAVOH_DISCORD_BOT_TOKEN per Bravoh naming convention) -->
<!-- - `discord.py` dep is imported LAZILY; dry-run requires zero deps (current Phase 36 contract — preserve) -->

<!-- KAAN-ACTION-LEGAL.md canonical format: §GATE-01 at line 832 (8-block structure). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discord taxonomy.json + provision script refactor + dry-run test (TDD)</name>
  <files>scripts/dayzero/discord_taxonomy.json, scripts/dayzero/discord_provision.py, tests/dayzero/test_discord_provision_dryrun.py</files>
  <behavior>
    - `discord_taxonomy.json` ships with the canonical merged taxonomy (5 roles + 9 channels per the must-have truth list)
    - `discord_provision.py` refactored to read `TARGET_ROLES` + `TARGET_CHANNELS` from `discord_taxonomy.json` at module-load (via `json.load` of a path relative to the script — `Path(__file__).parent / "discord_taxonomy.json"`)
    - Provision script continues to support `--dry-run` (default) + `--live` modes; lazy `discord.py` import preserved
    - Bot-token env var: script reads `BRAVOH_DISCORD_BOT_TOKEN` FIRST, falls back to `DISCORD_BOT_TOKEN` for back-compat
    - `tests/dayzero/test_discord_provision_dryrun.py` covers:
      a. `discord_taxonomy.json` is parseable JSON with the expected keys + counts (5 roles, 9 channels)
      b. Provision script in dry-run mode prints all 5 roles + 9 channels (capture stdout)
      c. Provision script in dry-run mode makes ZERO network calls (mock `httpx.AsyncClient` / `aiohttp` / `discord.py` — assert no instantiation)
      d. Provision script reads `BRAVOH_DISCORD_BOT_TOKEN` env preferentially when both env vars set
  </behavior>
  <action>Create `discord_taxonomy.json` with shape: `{ "roles": ["founder", "contributor", "DJ", "lurker", "moderator"], "channels": ["announcements", "general", "help", "show-and-tell", "controllers", "ai-misbehavior", "dev", "bugs", "showcase"], "guild_name": "vibemix" }`. Refactor `discord_provision.py` to load it; preserve dry-run default + lazy discord.py import + idempotent behavior. Add `BRAVOH_DISCORD_BOT_TOKEN` env var preferred-read (fallback to `DISCORD_BOT_TOKEN`). Pair with `tests/dayzero/test_discord_provision_dryrun.py` (vitest-style fixtures using subprocess + capfd + monkeypatch for env vars).</action>
  <verify>
    <automated>uv run pytest tests/dayzero/test_discord_provision_dryrun.py -v</automated>
  </verify>
  <done>taxonomy.json shipped; provision script consumes it; dry-run pinning test green; back-compat with DISCORD_BOT_TOKEN preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `check_bravoh_org_ready.sh` polling script + test</name>
  <files>scripts/launch/check_bravoh_org_ready.sh, tests/launch/test_check_bravoh_org_ready.sh</files>
  <behavior>
    - Bash script polls `https://api.github.com/orgs/bravoh`; uses `gh api orgs/bravoh` if `gh` is on PATH, otherwise falls back to `curl -fsS -o /dev/null -w "%{http_code}"`
    - Exit 0 if org exists (HTTP 200 / gh exit 0); exit 1 otherwise (404 / gh failure / network error)
    - CLI: `--org NAME` flag (default `bravoh`) for test injection; `--quiet` suppresses output
    - Test (`tests/launch/test_check_bravoh_org_ready.sh` — either bash-with-bats or a pytest wrapper using subprocess; pytest wrapper is preferred for project consistency with other `tests/launch/*.py` files): mock-org case: invoke against a known-existing org like `bravoh` (live) → expected to fail with exit 1 (since the org doesn't exist yet — that's the entire point of the §LAUNCH-06 KAAN-discharge); inverse: invoke against `--org github` (well-known existing org) → expected exit 0. This is a live network-touch test; mark it with `@pytest.mark.network` so CI can skip if offline.
  </behavior>
  <action>Write the bash script with `set -euo pipefail` + `gh`-or-`curl` detection. Make it executable (`chmod +x` — preserve via git). Write `tests/launch/test_check_bravoh_org_ready.py` (rename `.sh` → `.py` per project convention — the .sh in the must-haves was shorthand; .py wrapper matches `test_storyboard_palette.py` style). Use `@pytest.mark.network` for live-network tests.</action>
  <verify>
    <automated>chmod +x scripts/launch/check_bravoh_org_ready.sh; bash -n scripts/launch/check_bravoh_org_ready.sh; uv run pytest tests/launch/test_check_bravoh_org_ready.py -v -m "not network" 2>&1 | tail -10</automated>
  </verify>
  <done>Script syntax-clean (bash -n passes); pytest wrapper handles network-skip gracefully; live-network test executable on demand.</done>
</task>

<task type="auto">
  <name>Task 3: §LAUNCH-06 + §LAUNCH-08 Kaan-discharge runbooks</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <action>Append two new sections to KAAN-ACTION-LEGAL.md, both in canonical §GATE-01 8-block format.

§LAUNCH-06 — Bravoh GH org standup: Owner Kaan. Why-this-is-Kaan-action: requires Kaan's Bravoh Enterprise billing-flag resolution + `gh org create bravoh` requires Kaan's GH account auth context (engineering agent never holds GH tokens with org-create scope). Files involved: `scripts/launch/check_bravoh_org_ready.sh`, `KAAN-ACTION-LEGAL.md §LAUNCH-06`. Kaan oneliner: ascii block with `# 1. Resolve Bravoh Enterprise billing flag` (link to existing `signpath-application.md` reference in CONTEXT §LAUNCH-06) + `gh api -X POST /admin/organizations -f login=bravoh ...` (or correct API path — leave as KAAN-VERIFY since the exact GH org-create endpoint depends on Bravoh's GH plan tier) + `gh org member add bravoh kaanozk` + `gh org member add bravoh francescotural`. Verification: `bash scripts/launch/check_bravoh_org_ready.sh` exit 0. What unblocks: Plan 45 SHIP-TRANSFER + the `bravoh/vibemix` README install URLs become live. Sign-off block: dated Kaan-signature.

§LAUNCH-08 — Discord live-execution discharge: Owner Kaan. Why-this-is-Kaan-action: bot token sourcing requires Bravoh Discord admin seat (Kaan-only); `--live` execution against a real Discord guild requires the token. Files involved: `scripts/dayzero/discord_provision.py`, `scripts/dayzero/discord_taxonomy.json`, `KAAN-ACTION-LEGAL.md §LAUNCH-08`. Kaan oneliner: `# 1. Create Bravoh-vibemix Discord guild (Bravoh Discord admin panel)` + `# 2. Source bot token from Bravoh Discord Developer Portal` + `export BRAVOH_DISCORD_BOT_TOKEN=...` + `# 3. Persist into GH secret BRAVOH_DISCORD_BOT_TOKEN` (`gh secret set BRAVOH_DISCORD_BOT_TOKEN`) + `# 4. Dry-run verify` (`uv run python scripts/dayzero/discord_provision.py --guild-id <ID>`) + `# 5. Live execute` (`uv run python scripts/dayzero/discord_provision.py --live --guild-id <ID>`). Verification: `uv run pytest tests/dayzero/test_discord_provision_dryrun.py` green + manual guild inspection confirms 5 roles + 9 channels created. What unblocks: Plan 45 SHIP-DISCORD (#announcements launch post + live execution). Sign-off block: dated Kaan-signature.</action>
  <verify>
    <automated>grep -c "^## §LAUNCH-0[68]" KAAN-ACTION-LEGAL.md | grep -q "^2$"</automated>
  </verify>
  <done>§LAUNCH-06 + §LAUNCH-08 runbooks present in canonical §GATE-* 8-block format; verification one-liners reference the new check + dry-run scripts; sign-off blocks ready.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/dayzero/test_discord_provision_dryrun.py -v` — green.
- `uv run python scripts/dayzero/discord_provision.py` (no args, default dry-run) — prints 9 channels + 5 roles + plan summary, exit 0, ZERO network calls.
- `bash -n scripts/launch/check_bravoh_org_ready.sh` — syntax clean.
- `bash scripts/launch/check_bravoh_org_ready.sh --org github --quiet && echo OK` — prints OK (live network smoke; well-known org exists).
- `grep -c "^## §LAUNCH-0[68]" KAAN-ACTION-LEGAL.md` — exactly 2.
- `python -c 'import json; d=json.load(open("scripts/dayzero/discord_taxonomy.json")); assert len(d["roles"])==5 and len(d["channels"])==9'` — exit 0.
</verification>

<success_criteria>
- LAUNCH-06 + LAUNCH-08 engineering pre-stage GREEN; only Kaan-discharge items remain (billing-flag resolution + bot-token sourcing + live execution).
- ROADMAP Phase 44 success criteria 4 + 6 (engineering side) satisfied: "`bravoh` GitHub org exists" — pending Kaan-discharge per §LAUNCH-06 runbook; "Discord provision dry-run completes without errors" — green via dry-run test.
- Taxonomy locked as a single source of truth (drift between docs + code closed).
- Plan 45 SHIP-TRANSFER + SHIP-DISCORD have clean entry points.
</success_criteria>

<output>
After completion, create `.planning/phases/44-launch-positioning-pre-stage/44-06-SUMMARY.md`.
</output>
