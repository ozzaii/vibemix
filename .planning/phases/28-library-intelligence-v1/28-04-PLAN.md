---
phase: 28-library-intelligence-v1
plan: 04
type: execute
wave: 3
depends_on: [28-01, 28-02]
files_modified:
  - src/vibemix/library/grounding.py
  - src/vibemix/library/embed.py
  - src/vibemix/library/__init__.py
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/__main__.py
  - tests/library/test_grounding.py
  - tests/integration/test_library_wired_into_main.py
  - tests/integration/test_track_citation_validates_end_to_end.py
  - scripts/probe_proxy_embed.py
autonomous: true
requirements: [LIBRARY-13]

must_haves:
  truths:
    - "When EvidenceRegistry emits a track-aware event, grounding.identify_playing embeds the recent audio and runs cosine top-K against the library."
    - "If top match cosine >= 0.7, a [track:<id>] citation is added to the next Gemini prompt as text reference + audio anchor."
    - "Cosine in [0.6, 0.7) emits decision=uncertain telemetry but no citation."
    - "Cosine < 0.6 emits decision=below_threshold telemetry, no citation."
    - "register_library remains invoked at __main__.py:672-682 (P48 invocation test passes)."
    - "End-to-end: drag-drop XML import → live track playing → [track:<id>] citation passes linter (P48 E2E test passes)."
    - "Grounding is EVENT-GATED (Option B): embed calls fire only on EvidenceRegistry event emission — keeping monthly cost ~ €27/month at 1000 DAU."
    - "Wave 0 Bravoh proxy probe (scripts/probe_proxy_embed.py) verifies models:embedContent endpoint."
  artifacts:
    - path: "src/vibemix/library/grounding.py"
      provides: "identify_playing + Grounding subscriber + 0.7 threshold gate"
      exports: ["identify_playing", "Citation", "Grounding", "CITATION_THRESHOLD", "UNCERTAIN_THRESHOLD", "TRACK_AWARE_EVENTS"]
      contains: "def identify_playing"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "Citation flows into Gemini prompt as text reference + audio anchor (memory feedback_mic_audio_as_multimodal_part)"
      contains: "track:"
    - path: "tests/integration/test_library_wired_into_main.py"
      provides: "P48 invocation test"
      contains: "test_main_calls_register_library_when_library_loaded"
    - path: "tests/integration/test_track_citation_validates_end_to_end.py"
      provides: "P48 E2E test"
      contains: "test_drag_drop_xml_then_live_track_citation_validates"
    - path: "scripts/probe_proxy_embed.py"
      provides: "Wave 0 Bravoh proxy embedContent probe"
  key_links:
    - from: "src/vibemix/library/grounding.py"
      to: "src/vibemix/state/evidence_registry.py"
      via: "evidence_registry.on_event + event-gated subscription"
      pattern: "on_event"
    - from: "src/vibemix/library/grounding.py"
      to: "src/vibemix/library/store.py"
      via: "store.search"
      pattern: "store\\.search"
    - from: "src/vibemix/agent/dj_cohost.py"
      to: "src/vibemix/library/grounding.py"
      via: "get_latest_citation + Part injection"
      pattern: "track:"
---

<objective>
Build the "what's playing" grounding pipeline — vibemix's core anti-hallucination feature. When a track-aware event fires, snapshot recent audio, embed via 3-excerpt strategy, cosine-search against the library, and if cosine >= 0.7 inject `[track:<id>]` into the next Gemini prompt as text reference + audio anchor (per memory `feedback_mic_audio_as_multimodal_part`).

**Critical correction over CONTEXT (RESEARCH Open Q1 + Budget Telemetry):** lock **Option B event-gated grounding** — embed only on EvidenceRegistry event emission, NOT continuous 3-excerpts/min. The continuous path blows the €50 budget by ~60× at 1000 DAU; event-gated lands at ~€27/month.

**This plan also closes Pitfall P48** — adds the invocation test (proves `register_library` actually fires from `__main__.py`) and the E2E test (drag-drop → live citation → linter passes).

**Wave 0 Bravoh proxy probe (Assumption A10):** `scripts/probe_proxy_embed.py` verifies the proxy passes `models:embedContent`. Failure surfaces a structured blocker; under `gsd-autonomous fully` the plan either auto-patches the Bravoh proxy (if Kaan has push access) or writes KAAN-ACTION-PROXY.md and continues with a `MOCK_PROXY_FOR_DEV=1` test bypass.

Output:
- `src/vibemix/library/grounding.py` + extended `embed.py` (`embed_audio_buffer`)
- `src/vibemix/agent/dj_cohost.py` — kwargs-only `grounding=` (Pitfall P53)
- `src/vibemix/__main__.py` — Grounding construction after register_library (P48 preserved)
- 2 integration tests for P48 closure
- Wave 0 proxy probe script
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-library-intelligence-v1/28-CONTEXT.md
@.planning/phases/28-library-intelligence-v1/28-RESEARCH.md
@.planning/phases/28-library-intelligence-v1/28-01-SUMMARY.md
@.planning/phases/28-library-intelligence-v1/28-02-SUMMARY.md
@src/vibemix/state/evidence_registry.py
@src/vibemix/library/embed.py
@src/vibemix/library/store.py
@src/vibemix/__main__.py
@src/vibemix/agent/dj_cohost.py
@src/vibemix/agent/proxy_client.py

<interfaces>
From `src/vibemix/state/evidence_registry.py` (Phase 18 shipped):
- `EvidenceRegistry.register_library(lib: RekordboxLibrary) -> int`
- Event emission API — verify exact method name in code; likely `on_event(callback)` or `subscribe(callback)` pattern. The Grounding subscriber pattern is ADDITIVE — never replaces existing subscribers.

From `__main__.py:672-682` (Plan 27-05, frozen for P48 — DO NOT MODIFY):
```python
library_cache = Path.home() / ".cache" / "vibemix" / "library.pkl"
if library_cache.exists():
    lib = RekordboxLibrary()
    if lib.try_load_cache():
        registered = evidence_registry.register_library(lib)
        print(f"-> library: {registered} tracks registered for [track:<id>] citations")
```

From Plan 01 (`embed.py`): reuse `LibraryEmbedder._extract_excerpts` + `_call_gemini_audio`. Plan 04 adds a new method `embed_audio_buffer(pcm_int16, sample_rate, duration_s)` for live-buffer grounding.

From Plan 02 (`store.py`): `LibraryStore.search(qvec, k)` is the single search chokepoint.

From `src/vibemix/agent/dj_cohost.py`: existing 4-kwarg `__init__` (Pitfall P53). The new `grounding=` MUST be kwargs-only at the END so existing tests stay byte-identical when `grounding=None`.

From AudioBuffer (POC variants, shipped equivalent in `src/vibemix/audio/`): `snapshot_features(seconds=60)` returns recent PCM int16 — used at event time to grab the audio window for embedding.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 Bravoh proxy embedContent probe</name>
  <files>scripts/probe_proxy_embed.py</files>
  <action>
Create `scripts/probe_proxy_embed.py` — a standalone Wave 0 probe for Assumption A10 (RESEARCH `## Assumptions Log`): the Bravoh proxy at `api.altidus.world` passes through `gemini-embedding-2:embedContent`.

The script:
1. Reads `VIBEMIX_PROXY_BASE_URL` (default `https://api.altidus.world`) and `VIBEMIX_PROXY_JWT` env vars. If JWT absent, print `ERROR: VIBEMIX_PROXY_JWT not set — get a sidecar JWT from local-dev login flow.` and exit 1.
2. Builds `client = build_proxy_genai_client(jwt, base_url)` via `vibemix.agent.proxy_client`.
3. Sends `client.models.embed_content(model='gemini-embedding-2', contents='ping', config=types.EmbedContentConfig(output_dimensionality=768))`.
4. On success (200): print `PROXY EMBED OK: dim=768 took=Xms`. Assert `len(result.embeddings[0].values) == 768`. Exit 0.
5. On 404 / unsupported endpoint: print `PROXY EMBED FAIL: endpoint not routed. Bravoh proxy needs models:embedContent passthrough. Blocked.` Exit 1.
6. On 401/403: print `PROXY EMBED AUTH: <reason>. JWT may be invalid or expired.` Exit 2.
7. On network / 5xx: print `PROXY EMBED ERROR: <reason>` + exit 3.

**Behavior under `gsd-autonomous fully`:**
- If probe exits 1 (endpoint not routed), the executor opens the Bravoh proxy repo (`/var/www/bravoh-backend/` per CLAUDE.md) and checks the proxy router. If the developer has push access, auto-patch and deploy. If not, write `.planning/phases/28-library-intelligence-v1/KAAN-ACTION-PROXY.md` with full reproduction curl + Bravoh repo path, and continue Plan 04 with `MOCK_PROXY_FOR_DEV=1` test bypass (tests run against stubbed `LibraryEmbedder`).
- Document the probe verdict in `28-04-SUMMARY.md`.

This is the single high-asymmetry risk in Phase 28 (Assumption A10 marked HIGH if wrong). The probe surfaces it at hour 0.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && test -f scripts/probe_proxy_embed.py && python -c "import ast; ast.parse(open('scripts/probe_proxy_embed.py').read()); print('OK')"</automated>
  </verify>
  <done>
- `scripts/probe_proxy_embed.py` exists and parses
- Reads env vars, exits with documented codes
- Probe outcome recorded in 28-04-SUMMARY.md (proxy passes embedContent OR KAAN-ACTION-PROXY.md exists)
  </done>
</task>

<task type="auto">
  <name>Task 2: grounding.py + embed_audio_buffer extension</name>
  <files>src/vibemix/library/grounding.py, src/vibemix/library/embed.py, src/vibemix/library/__init__.py</files>
  <action>
**Extend `src/vibemix/library/embed.py`** (Plan 01) with `LibraryEmbedder.embed_audio_buffer(pcm_int16, sample_rate, duration_s) -> np.ndarray`:
- Convert int16 PCM to WAV bytes via stdlib `wave` module (write to BytesIO, get bytes).
- If `duration_s > 180`: write to tempfile, slice 3 ffmpeg WAV excerpts (same intro/mid/outro shape as `_extract_excerpts`), embed each as `types.Part.from_bytes(data=wav_bytes, mime_type='audio/wav')`, mean + L2-normalize. Use WAV not MP3 (Open Q6 — no transcode needed for live buffer).
- If `duration_s <= 180`: single call with full buffer as WAV.
- Do NOT add to content-hash cache — live grounding doesn't benefit from caching (different audio each event).

**Create `src/vibemix/library/grounding.py`:**

Module-level constants:
- `CITATION_THRESHOLD = 0.7` (CONTEXT)
- `UNCERTAIN_THRESHOLD = 0.6` (CONTEXT)
- `GROUNDING_BUFFER_SECONDS = 60` (window of recent audio to embed)
- `TRACK_AWARE_EVENTS = frozenset({"TRACK_CHANGE", "LAYER_ARRIVAL", "MIX_MOVE", "MANUAL"})`

Dataclass `Citation` (frozen, slots):
- `track_id: str | None` — None when decision != "cited"
- `cosine: float`
- `decision: Literal["cited", "uncertain", "below_threshold"]`
- `event_id: str`
- `ts: float`

`identify_playing(embedder, store, pcm_int16, sample_rate, duration_s, event_id="manual") -> Citation`: pure stateless function. Embeds the buffer, runs `store.search(qvec, k=1)`. Maps similarity to decision per the threshold table. Returns Citation. Used directly by tests (no async/subscription concerns).

Class `Grounding`:
- `__init__(self, embedder, store, evidence_registry, audio_buffer)` — store refs. Subscribe to events via `evidence_registry.on_event(self._on_event)` (use whatever subscription API exists; if EvidenceRegistry doesn't expose subscriptions, add a minimal `on_event(callback)` method as an ADDITIVE extension — verify in code).
- `_on_event(self, ev_type, ev_id, payload)`: if `ev_type not in TRACK_AWARE_EVENTS`: return. Else schedule async work: `asyncio.create_task(self._identify(ev_id, ev_type, payload))`. Wrap in try/except — a grounding failure must NOT crash the live event loop.
- `async def _identify(self, ev_id, ev_type, payload)`:
  - Offload audio snapshot to executor: `pcm, sr, dur = await loop.run_in_executor(None, self.audio_buffer.snapshot_features, GROUNDING_BUFFER_SECONDS)` (adapt signature to whatever `snapshot_features` returns; if it returns a dict, extract pcm/sr from it).
  - `cite = await loop.run_in_executor(None, identify_playing, self.embedder, self.store, pcm, sr, dur, ev_id)`.
  - Acquire `self._lock`, set `self._latest_citation = cite`.
  - Emit telemetry: `self.evidence_registry.emit_telemetry("library.confidence", asdict(cite))` (Plan 09 wires the IPC schema; this plan emits the structured event so the IPC layer can subscribe).
- `def get_latest_citation(self) -> Citation | None` — thread-safe accessor used by DJCoHostAgent.

Update `src/vibemix/library/__init__.py`: re-export `Grounding`, `Citation`, `identify_playing`, `CITATION_THRESHOLD`, `UNCERTAIN_THRESHOLD`, `TRACK_AWARE_EVENTS`.

Critical: NO timer-based sampling. Only event-gated. Grep gate enforced: no `asyncio.sleep` in a loop with periodic embed calls.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && python -c "
from vibemix.library.grounding import identify_playing, Citation, Grounding, CITATION_THRESHOLD, UNCERTAIN_THRESHOLD, TRACK_AWARE_EVENTS
assert CITATION_THRESHOLD == 0.7
assert UNCERTAIN_THRESHOLD == 0.6
assert 'TRACK_CHANGE' in TRACK_AWARE_EVENTS
print('OK')
"</automated>
  </verify>
  <done>
- `Grounding` class subscribes to EvidenceRegistry events (additive)
- `identify_playing()` is a pure stateless function for testability
- Thresholds locked at 0.7 / 0.6
- `LibraryEmbedder.embed_audio_buffer()` extension exists
- No timer-based grounding (event-gated only) — grep returns 0 for periodic sample patterns
  </done>
</task>

<task type="auto">
  <name>Task 3: DJCoHostAgent citation injection (kwargs-only, P53)</name>
  <files>src/vibemix/agent/dj_cohost.py</files>
  <action>
Add `grounding: "Grounding | None" = None` as the LAST kwargs-only parameter to `DJCoHostAgent.__init__`. Store as `self.grounding`. Pitfall P53: default `None` means existing 4-kwarg construction is byte-identical.

In the existing prompt-building method (the one that assembles `contents` for `generate_reply` / multimodal LLM call — verify exact method name; likely `_build_turn_contents`, `run_one_turn`, or similar), add a pre-emit step:

```python
if self.grounding is not None:
    cite = self.grounding.get_latest_citation()
    if cite is not None and cite.decision == "cited":
        track_label = f"Currently playing: [track:{cite.track_id}] (cosine {cite.cosine:.2f}). Cite this if you mention the track."
        contents.insert(0, types.Part.from_text(text=track_label))
```

Per memory `feedback_mic_audio_as_multimodal_part`: the citation TEXT references the audio Part that's already being attached by the existing path — Plan 04 does NOT add a second audio Part. The text label anchors Gemini's attention to the existing audio Part.

Add a docstring comment near the new kwarg citing Pitfall P53 (kwargs-only) and memory `feedback_mic_audio_as_multimodal_part`.

If the existing dj_cohost.py constructor signature is unclear after reading it, search for `class DJCoHostAgent` and identify the `__init__` signature exactly. The 5th kwarg slot is critical — must NOT alter existing positional/keyword order.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && python -c "
import inspect
from vibemix.agent.dj_cohost import DJCoHostAgent
sig = inspect.signature(DJCoHostAgent.__init__)
params = list(sig.parameters.values())
assert params[-1].name == 'grounding', f'last param is {params[-1].name}'
assert params[-1].default is None
print('OK')
"</automated>
  </verify>
  <done>
- `DJCoHostAgent.__init__` last kwarg = `grounding=None` (P53 preservation)
- Citation text label injected only when `decision == "cited"`
- Existing 4-kwarg construction unchanged
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire Grounding into __main__.py (after register_library)</name>
  <files>src/vibemix/__main__.py</files>
  <action>
Extend `src/vibemix/__main__.py` AFTER the register_library block (line 682). The 672-682 block MUST remain untouched (P48).

Read the file region 660-720 first to see exact variable names (`evidence_registry`, `audio_buf` or `audio_buffer`, `genai_client`, the AgentSession construction). Then insert AFTER line 682:

```python
# ── Plan 28-04 grounding wiring (closes LIBRARY-13; event-gated per RESEARCH Open Q1) ──
from vibemix.library import open_store, LibraryEmbedder, Grounding
grounding = None
if library_cache.exists() and lib is not None and lib.tracks:
    try:
        _store = open_store()
        _embedder = LibraryEmbedder(client=genai_client)
        grounding = Grounding(
            embedder=_embedder,
            store=_store,
            evidence_registry=evidence_registry,
            audio_buffer=audio_buf,
        )
        print(f"-> grounding: enabled (event-gated, threshold >= 0.7)")
    except Exception as e:
        print(f"-> grounding: disabled — init failed: {e}", file=sys.stderr)
```

If `audio_buf` is constructed LATER in main() than the register_library block, move this wire-up to immediately after `audio_buf` is built. The constraint: Grounding subscriber must exist before `await session.start(agent)`.

Then find the `DJCoHostAgent(...)` construction (currently 4 kwargs) and add `grounding=grounding` as the LAST kwarg. Verify the existing 4 kwargs are unchanged.

If the existing `genai_client` construction (line 539: `genai_client = genai.Client(api_key=api_key)`) is NOT proxy-routed, replace it with `genai_client = build_proxy_genai_client(jwt, proxy_base_url)`. Per CLAUDE.md security constraint: API key embedded in distributed binary is forbidden — must go through Bravoh proxy. If line 539 is direct-key, this is a P48-adjacent security gap that Plan 04 MUST also close.

Final sanity:
- `grep -c "register_library" src/vibemix/__main__.py` ≥ 1
- `grep -c "Grounding(" src/vibemix/__main__.py` == 1
- `grep -c "grounding=grounding" src/vibemix/__main__.py` ≥ 1
- The file parses with `python -c "import ast; ast.parse(open('src/vibemix/__main__.py').read())"`
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && python -c "import ast; ast.parse(open('src/vibemix/__main__.py').read()); print('AST OK')" && bash -c "test $(grep -c 'register_library' src/vibemix/__main__.py) -ge 1 && test $(grep -c 'Grounding(' src/vibemix/__main__.py) -eq 1 && test $(grep -c 'grounding=grounding' src/vibemix/__main__.py) -ge 1 && echo 'GREP-GATES OK'"</automated>
  </verify>
  <done>
- All 3 grep gates green: register_library preserved, Grounding constructed once, grounding= passed to agent
- File parses
- The `genai_client` build path is proxy-routed (no direct AIza key in __main__.py)
- AgentSession + DJCoHostAgent construction order preserves event subscription before session.start
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: P48 invocation test + E2E test + grounding unit tests</name>
  <files>tests/library/test_grounding.py, tests/integration/test_library_wired_into_main.py, tests/integration/test_track_citation_validates_end_to_end.py</files>
  <behavior>
**tests/library/test_grounding.py:**
- `test_identify_playing_above_threshold_cites`: mocked store returns `[("t-100", 0.85)]` → Citation(track_id="t-100", decision="cited").
- `test_identify_playing_uncertain_zone`: store returns `[("t-100", 0.65)]` → Citation(track_id=None, decision="uncertain", cosine=0.65).
- `test_identify_playing_below_threshold`: store returns `[("t-100", 0.4)]` → Citation(track_id=None, decision="below_threshold").
- `test_identify_playing_empty_store`: store returns [] → Citation(track_id=None, cosine=0.0, decision="below_threshold").
- `test_grounding_event_gated_only`: simulate non-track-aware event (e.g. ev_type="PHASE") → no embed call. Then track-aware event → embed call fires.
- `test_grounding_no_timer_loop`: instantiate Grounding; verify NO `asyncio.create_task` is scheduled at construction time (only subscriber registered, no polling loop) — assert via `len(asyncio.all_tasks())` delta == 0.
- `test_citation_to_prompt_part`: construct DJCoHostAgent with mocked Grounding returning a cited Citation; call the prompt-building method; assert `contents[0].text` contains `[track:<id>]`.
- `test_no_citation_when_uncertain`: same with Citation(decision="uncertain") → no `[track:` in contents.

**tests/integration/test_library_wired_into_main.py (P48 invocation test):**
- `test_main_calls_register_library_when_library_loaded`: monkey-patch `RekordboxLibrary.try_load_cache` to return True and `register_library` to a `Mock`. Boot `__main__.main()` up to the wiring point (use a short-circuit env var like `VIBEMIX_TEST_BOOT_ONLY=1` to bail before audio device init — add this guard to `__main__.py` Task 4 if not present). Assert `evidence_registry.register_library.call_count == 1`.
- `test_grounding_disabled_when_no_library_cache`: ensure `library.pkl` absent (point HOME to tmp_path); boot; assert prints "-> library: no cache" and `grounding is None`.

**tests/integration/test_track_citation_validates_end_to_end.py (P48 E2E test):**
- `test_drag_drop_xml_then_live_track_citation_validates`: end-to-end smoke.
  1. Pre-populate `tmp_home/.cache/vibemix/library.pkl` with a synthetic RekordboxLibrary (3 tracks).
  2. Pre-populate `tmp_home/.cache/vibemix/library.db` (sqlite-vec) with 3 known L2-normalized vectors.
  3. Instantiate Grounding directly (skip full main()).
  4. Trigger a synthetic TRACK_CHANGE event via evidence_registry.
  5. Inject a mocked `embed_audio_buffer` returning a vector that matches track 1 at cosine 0.85.
  6. Wait for async _identify to complete (via `asyncio.wait_for(task, timeout=2)`).
  7. Assert `grounding.get_latest_citation().track_id == "track-1"`.
  8. Construct DJCoHostAgent with this Grounding, invoke prompt-build, assert `[track:track-1]` appears in contents.
  9. Run the existing Phase 20 citation linter against a sample response "Nice acid line in [track:track-1]" — assert linter passes (track-1 is registered in evidence_registry → citation resolves).

Mock all Gemini API calls. No network.
  </behavior>
  <action>
Create the three test files per the behaviors. Key implementation notes:

- For the `VIBEMIX_TEST_BOOT_ONLY` guard: add to `__main__.py` Task 4 (a single early-return after the grounding wiring block, gated on env var) so the invocation test can exercise the wiring path without launching the full live session.
- For E2E test, build a `SyntheticRekordboxLibrary` helper that constructs a real RekordboxLibrary via its public API (try_load_cache + tracks list) and saves to pickle. If RekordboxLibrary doesn't have a public "set tracks" API, monkey-patch `try_load_cache` to return True and `.tracks` to return the synthetic list.
- The citation linter is from Phase 20 (`src/vibemix/agent/citation_linter.py` per shipped code; verify path). Reuse its `validate(response_text, evidence_registry)` entry point.
- All tests use `tmp_path` and `monkeypatch.setenv("HOME", str(tmp_path))` to isolate from user's real cache.

Run via `pytest tests/library/test_grounding.py tests/integration/test_library_wired_into_main.py tests/integration/test_track_citation_validates_end_to_end.py -x`.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && pytest tests/library/test_grounding.py tests/integration/test_library_wired_into_main.py tests/integration/test_track_citation_validates_end_to_end.py -x -q</automated>
  </verify>
  <done>
- All grounding + integration tests pass
- P48 invocation test asserts register_library is actually called from __main__.py
- P48 E2E test asserts drag-drop XML → live citation → linter pass
- No real network in any test
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live AudioBuffer → Gemini Embedding 2 via Bravoh proxy | Recent audio (60s) leaves user's machine on each track-aware event. |
| EvidenceRegistry → Grounding subscriber | Internal; same-process. |
| Grounding citation → DJCoHostAgent → Gemini prompt | Internal; same-process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Information Disclosure | Live audio uploaded to Gemini | accept | User opted in by running vibemix; Privacy disclosure surface in Phase 34 SEC-08. All traffic via Bravoh proxy (no direct AIza key path). |
| T-28-04-02 | DoS (cost amplification) | Event-driven embedding | mitigate | Event-gated (Option B): ~10 events/session × 5 sessions/month × 1000 DAU = €27/month. Plan 08 asserts budget projection. |
| T-28-04-03 | Spoofing | Track citation `[track:<id>]` | mitigate | EvidenceRegistry resolves track_id at linter time (Phase 20). Unknown IDs stripped. Cosine ≥ 0.7 threshold prevents low-confidence citations. |
| T-28-04-04 | Tampering | Citation injected into prompt | mitigate | Citation text is template-built (f-string), no user input concatenation. Track ID format is `\\w+`-constrained at EvidenceRegistry level. |
| T-28-04-05 | DoS | Grounding crashes mid-event | mitigate | Try/except around `_on_event` callback; failures logged, do not propagate to live event loop. |
| T-28-04-06 | Information Disclosure | Telemetry payload `library.confidence` | accept | Local IPC only (Plan 09 schema, port 8765 localhost). Includes cosine + decision + track_id (track_id is metadata, not file content). |
| T-28-04-07 | Information Disclosure | API key extraction from binary | mitigate | Plan 04 enforces proxy-only path: if `__main__.py:539` was direct-key, Plan 04 Task 4 replaces with `build_proxy_genai_client`. CLAUDE.md security constraint. |
</threat_model>

<verification>
- `pytest tests/library/test_grounding.py tests/integration/test_library_wired_into_main.py tests/integration/test_track_citation_validates_end_to_end.py -x` → all pass
- `grep -c "register_library" src/vibemix/__main__.py` ≥ 1 (P48)
- `grep -c "Grounding(" src/vibemix/__main__.py` == 1
- `grep -c "grounding=grounding" src/vibemix/__main__.py` ≥ 1
- `grep -c "asyncio.sleep" src/vibemix/library/grounding.py` == 0 (no polling loop)
- `grep -c "api_key=" src/vibemix/library/grounding.py` == 0 (proxy-only)
- `grep -c "api_key=os.environ" src/vibemix/__main__.py` returns the count from BEFORE Plan 04 OR 0 if Plan 04 closed the direct-key gap (record outcome in SUMMARY).
- Wave 0 proxy probe outcome recorded in 28-04-SUMMARY.md.
- AIza-leak scan: 0 matches in new/modified files.
</verification>

<success_criteria>
- Live track-aware event → recent audio embedded → cosine top-K → citation decision per threshold table.
- `[track:<id>]` flows into Gemini prompt as text + audio anchor for cited tracks only.
- register_library invocation test passes (P48 closed at invocation level).
- End-to-end drag-drop → citation → linter test passes (P48 closed at runtime level).
- Event-gated grounding only (no timer / sampling loop).
- Wave 0 proxy probe verified or KAAN-ACTION-PROXY.md written.
- DJCoHostAgent kwargs-only contract preserved (P53).
</success_criteria>

<output>
After completion, create `.planning/phases/28-library-intelligence-v1/28-04-SUMMARY.md` documenting:
- Wave 0 proxy probe verdict (passed / KAAN-ACTION-PROXY filed)
- The exact EvidenceRegistry subscription API used (or added if missing)
- Whether `__main__.py:539` direct-key path was closed (and how)
- Citation decision distribution observed in tests (cited / uncertain / below_threshold)
- Final DJCoHostAgent __init__ signature
</output>
