---
phase: 78-perceive-deeper-generalized-ear
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/state/test_coach_perceive.py
  - tests/state/test_refresh_perceive.py
  - tests/library/test_genre_prototypes.py
  - tests/state/conftest.py
autonomous: true
requirements: [PERCEIVE-01, PERCEIVE-02, PERCEIVE-03]

must_haves:
  truths:
    - "A cold default MusicState renders a prompt byte-identical to the v8.0 baseline (pinned, not xfail)."
    - "Every PERCEIVE-0x behavior has a failing (xfail-strict) test that flips to a real pass when its implementation plan lands."
  artifacts:
    - path: "tests/state/test_coach_perceive.py"
      provides: "PERCEIVE-01/02 render + cold-path byte-identity scaffolds"
      contains: "xfail"
    - path: "tests/state/test_refresh_perceive.py"
      provides: "single-writer prev_snapshot + trajectory + genre-feed + reconciliation scaffolds"
      contains: "xfail"
    - path: "tests/library/test_genre_prototypes.py"
      provides: "prototype build + classify floor/margin scaffolds (synthetic vectors, tmp cache)"
      contains: "xfail"
  key_links:
    - from: "tests/state/test_coach_perceive.py"
      to: "src/vibemix/state/coach.py::evidence_line"
      via: "AICoach.evidence_line(state) assertions"
      pattern: "evidence_line"
---

<objective>
Wave-0 RED scaffolds for all three PERCEIVE requirements + a hard cold-path byte-identity pin.

Purpose: Establish the Nyquist safety net BEFORE any implementation. Every PERCEIVE-0x acceptance criterion gets a failing `xfail(strict=True)` test that flips to a real pass when its implementation plan lands (02/04). The cold-path byte-identity pin is a REAL green now (it asserts the v8.0 baseline that implementation must not break).

Output: 3 new test files + shared synthetic fixtures. No `src/` change. Full suite stays green (new xfails count as expected-fail, not failures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-RESEARCH.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-VALIDATION.md

<interfaces>
<!-- Contracts the scaffolds assert against. Extracted from live source this session. -->

From src/vibemix/state/coach.py (AICoach is a static-method class):
```
@staticmethod
def evidence_line(state: MusicState, *, registry_snapshot=None, recall_moments=None) -> str
```
Existing render gates (cold-path precedent — every block is `if <field>:`-gated):
- audible block: `hearing[rms=… sub=… … bpm=…]` (coach.py:284-289)
- genre gate (UNCHANGED by this phase): `if state.detected_genre != "unknown" and state.genre_confidence >= 0.5: e.append(f"genre={state.detected_genre}")` (coach.py:334-335)
- recent_moves[8s] gated branch (coach.py:346-353) — the exact `if <falsy>: ... else: NONE` idiom.

From src/vibemix/state/music_state.py:
```
@dataclass
class MusicState:
    detected_genre: str = "unknown"   # line 55
    genre_confidence: float = 0.0     # line 56
    phase_history: list  # [(t, from, to)] last 6 (line 127)
    recent_moves: list   # last 12s, deck-attributed (line 123)
    long_arc: list       # ~120s RMS, 10s hop (line 126)
    buildup_score: float # trailing 8s monotonic climb (line 102)
    _lock: threading.Lock
```
NEW fields this phase will add (assert their existence is what flips the xfails):
- `prev_perceive: dict = field(default_factory=dict)` (PERCEIVE-01)
- `trajectory_narrative: str = ""` (PERCEIVE-02)

From src/vibemix/state/refresh.py:
```
def _tick_once(state, audio_buf, controller_state, track_info, *, now, last_audible_high,
    last_audible_low, bpm_cache, last_bpm_at, bpm_ring=None, crest_smoother=None,
    vocal_detector=None, hysteresis_state=None, feature_history=None,
    evidence_registry=None, genre_hysteresis=None, deck_source=None) -> tuple[float,float,float,float]
```
Single-writer batch boundary: `with state._lock:` at refresh.py:306. Genre DSP write at refresh.py:339-352.

From src/vibemix/library/genre_prototypes.py (NEW — does not exist yet; the import itself is the xfail trigger):
```
def build_prototypes(...) -> tuple[np.ndarray, list[str]]
def classify(track_embedding: np.ndarray, protos, labels, centroid, *, floor=..., margin=...) -> tuple[str, float]
```

From tests/library/test_centering.py — copy `_anisotropic_corpus(n, seed)` synthetic-corpus builder (N L2-normalized vectors sharing a common direction).
Cache-path monkeypatch gotcha (CLAUDE.md + RESEARCH Pitfall 5): MUST monkeypatch `RekordboxLibrary.CACHE_PATH`, `centering.CENTROID_PATH`, `centering.CENTROID_META_PATH`, and the new prototype path to `tmp_path` in every PERCEIVE-03 test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PERCEIVE-01/02 render scaffolds + cold-path byte-identity pin (test_coach_perceive.py)</name>
  <files>tests/state/test_coach_perceive.py</files>
  <read_first>
    - tests/state/test_coach.py:44-296 (the `MusicState(...)` construct + `AICoach.evidence_line(state)` + `assert "x" in/not in out` idiom; the genre golden at :260-296; the byte-identity baseline at :163-201)
    - src/vibemix/state/coach.py:256-432 (evidence_line, render gates, recent_moves[8s] gated branch)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (test analog section, lines 158-167)
  </read_first>
  <action>
    Create tests/state/test_coach_perceive.py. Mirror tests/state/test_coach.py construction idiom (build a `MusicState(audible=True, ...)`, call `AICoach.evidence_line(state)`, assert substrings).

    REAL GREEN pin (no xfail — this asserts the v8.0 baseline that implementation must preserve): `test_cold_path_byte_identical` — a default `MusicState()` and a `MusicState(audible=True, rms=..., bands=..., bpm=...)` with NO prior snapshot (`prev_perceive == {}`) and empty `trajectory_narrative` produce evidence_line output containing NO `trajectory[` token and NO `Δ`/`rose`/`fell` delta phrasing. Assert the cold-state evidence_line equals the current v8.0 output for that same state (capture the current output as the golden). This test passes TODAY and must KEEP passing after 02 lands.

    XFAIL-STRICT scaffolds (mark each with `@pytest.mark.xfail(strict=True, reason="PERCEIVE-01/02 — flips when Plan 02 lands")`):
    - `test_delta_rendered_when_change_significant` — construct a state whose `prev_perceive` holds a prior scalar set and whose current scalars differ above the floor; assert evidence_line contains delta phrasing (e.g. a substring like `rose` or `fell` or a `Δ`/`%` token per the render helper contract). This will currently fail because `MusicState` has no `prev_perceive` field and coach.py has no delta branch.
    - `test_delta_abstains_below_floor` — prior present but current change below floor → assert the bare-scalar fact is NOT replaced by a delta line AND no `0%` delta line is asserted (abstain = omit). Anti-slop contract.
    - `test_empty_trajectory_omitted` — `trajectory_narrative == ""` → assert `"trajectory["` not in output (cold byte-identity). Phrase this as xfail only if it references a not-yet-existing field; otherwise make it a real green pin alongside the byte-identity test.

    Use `from __future__ import annotations`. NO live API, NO `genai.Client`, NO `GEMINI_API_KEY`. Pure dataclass construction + render assertions.
  </action>
  <acceptance_criteria>
    - `tests/state/test_coach_perceive.py` exists and imports cleanly.
    - `pytest tests/state/test_coach_perceive.py -q` reports the byte-identity test PASSED and the PERCEIVE-01/02 render tests XFAILED (strict) — zero unexpected failures, zero xpass.
    - Existing `tests/state/test_coach.py` stays fully green (run it to confirm no collision).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_coach_perceive.py tests/state/test_coach.py -q</automated>
  </verify>
  <done>New coach scaffolds collected; cold-path byte-identity is a real green pin; PERCEIVE-01/02 render assertions are xfail-strict.</done>
</task>

<task type="auto">
  <name>Task 2: PERCEIVE single-writer + prototype scaffolds + shared fixtures (test_refresh_perceive.py, test_genre_prototypes.py, conftest.py)</name>
  <files>tests/state/test_refresh_perceive.py, tests/library/test_genre_prototypes.py, tests/state/conftest.py</files>
  <read_first>
    - src/vibemix/state/refresh.py:191-210 (the `_tick_once` signature) and :306-352 (the `with state._lock:` batch + genre DSP write)
    - tests/library/test_centering.py (the `_anisotropic_corpus` synthetic builder + tmp_path cache monkeypatch fixtures)
    - src/vibemix/library/centering.py:48-143 (CENTROID_PATH, CENTROID_META_PATH, load_or_compute_centroid signature)
    - src/vibemix/library/_cosine.py:56,79 (EMBEDDING_DIM=1536, cosine_topk signature)
    - src/vibemix/library/rekordbox.py:93,123,233 (TrackEntry.filepath, RekordboxLibrary.CACHE_PATH, lookup_by_id)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (test analog section, lines 158-171)
  </read_first>
  <action>
    Create tests/state/conftest.py (or extend if present) with TWO shared fixtures:
    - a synthetic `MusicState` sequence builder helper that yields a (prior, current) pair with controllable scalar deltas (for PERCEIVE-01/02 single-writer tests).
    - a synthetic `(vectors, ids, label_of)` fixture: reuse the `_anisotropic_corpus(n, seed)` shape from test_centering.py to build N L2-normalized 1536-dim vectors, assign folder-style labels (`label_of[id] = "hardtechno"` etc.), for PERCEIVE-03 prototype tests.

    Create tests/state/test_refresh_perceive.py (ALL xfail-strict, reason="PERCEIVE-01/02/03 — flips when Plan 02/04 lands"). Drive single ticks via `_tick_once` with fake snapshots (mirror tests/state/test_refresh.py idiom):
    - `test_prev_snapshot_written_in_lock` — after a `_tick_once`, `state.prev_perceive` holds the just-written scalar set (rms/bands/onset_density/bpm/crest); asserts it is captured (single-writer, last inside the lock).
    - `test_trajectory_composed_bounded` — after `_tick_once` on a state with non-empty phase_history/recent_moves/buildup_score, `state.trajectory_narrative` is a non-empty bounded string; assert length is bounded (no unbounded growth across repeated ticks).
    - `test_genre_fed_single_writer` — `state.detected_genre`/`genre_confidence` are written ONLY by `_tick_once` reading an off-loop holder; assert no second writer (a smoke assertion that the genre value reflects the holder result after a tick).
    - `test_genre_reconciliation` — embedding-genre vs DSP-genre agree case AND disagree case → one coherent `detected_genre` per tick (the flagged-risk pin). For the disagree case, assert embedding-wins-when-confident; for sub-floor embedding, assert DSP fallback.

    Create tests/library/test_genre_prototypes.py (ALL xfail-strict, reason="PERCEIVE-03 — flips when Plan 03 lands"). MANDATORY: monkeypatch `RekordboxLibrary.CACHE_PATH`, `centering.CENTROID_PATH`, `centering.CENTROID_META_PATH`, and the new prototype cache path to `tmp_path` in every test (CLAUDE.md gotcha + RESEARCH Pitfall 5):
    - `test_build_centered_means` — from the synthetic `(vectors, ids, label_of)` fixture, `build_prototypes` produces one centered-mean prototype per label, each L2-normalized, shape (n_labels, 1536).
    - `test_classify_floor_and_margin` — `classify` returns the nearest-prototype label above floor; below floor OR within tie-margin → `("unknown", conf)`. These currently fail at import because `vibemix.library.genre_prototypes` does not exist — that import error IS the xfail trigger.

    `from __future__ import annotations`. NO live API, NO `genai.Client`, NO `GEMINI_API_KEY`.
  </action>
  <acceptance_criteria>
    - All three files exist and collect under pytest.
    - `pytest tests/state/test_refresh_perceive.py tests/library/test_genre_prototypes.py -q` reports every new test XFAILED (strict) — zero unexpected pass, zero failure.
    - No test mutates the real `~/.cache/vibemix/` (verified by the tmp_path monkeypatches; spot-check that `library.pkl` mtime is unchanged after the run).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_refresh_perceive.py tests/library/test_genre_prototypes.py -q</automated>
  </verify>
  <done>Single-writer, trajectory, genre-feed, reconciliation, prototype-build, classify scaffolds all xfail-strict; shared synthetic fixtures in conftest; real cache untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → real cache (`~/.cache/vibemix/`) | A prototype/library test that forgets the path monkeypatch can clobber the real `library.pkl` / `genre_prototypes.npy` (dev-data tampering). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01-01 | Tampering (dev data) | tests/library/test_genre_prototypes.py | mitigate | Every PERCEIVE-03 test monkeypatches `RekordboxLibrary.CACHE_PATH` + `centering.CENTROID_PATH`/`CENTROID_META_PATH` + the new prototype path to `tmp_path`; acceptance criterion spot-checks real `library.pkl` mtime unchanged. |
| T-78-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit is vacuously satisfied — pytest already configured). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/test_coach_perceive.py tests/state/test_refresh_perceive.py tests/library/test_genre_prototypes.py tests/state/test_coach.py -q` — new xfail-strict scaffolds + the real byte-identity pin + the existing coach goldens all in expected states.
- Full suite (`source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q`) stays green: the new xfails register as xfailed, not failed; baseline ~4451/0 holds.
</verification>

<success_criteria>
- 3 new test files + shared fixtures exist, collect cleanly.
- Cold-path byte-identity is a REAL GREEN pin (asserts v8.0 baseline).
- All PERCEIVE-01/02/03 behavior tests are xfail-strict (RED), ready to flip in Plans 02/04 (and 03's module unblocks the prototype xfails at classify time).
- Real `~/.cache/vibemix/` untouched.
- Honest green: no `genai.Client`, no `GEMINI_API_KEY`.
</success_criteria>

<output>
Create `.planning/phases/78-perceive-deeper-generalized-ear/78-01-SUMMARY.md` when done
</output>
