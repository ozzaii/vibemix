---
phase: 80-ground-gemini-as-secondary-ear
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - src/vibemix/prompts/matrix.py
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/__main__.py
  - src/vibemix/llm/_router_config.py
autonomous: true
requirements: [GROUND-01, GROUND-02]

must_haves:
  truths:
    - "With VIBEMIX_GROUND_SECONDARY_EAR unset/OFF the reaction request + prompt are byte-identical to the v8.0 baseline (the audio Part-1 still feeds, but no new framing)."
    - "With the flag ON the prompt carries a secondary-ear framing clause naming the audio a secondary grounding signal and the structured evidence as authoritative."
    - "Even with the audio Part attached, a Gemini claim about an event absent from the EvidenceRegistry strips the whole turn to <silence/> — the audio cannot fabricate."
    - "The reaction model resolves via model_router.resolve('live_coach') with zero hardcoded literals; live_coach is documented as the Phase-81 bench-swap alias."
  artifacts:
    - path: "src/vibemix/prompts/matrix.py"
      provides: "Gated secondary_ear framing extension to build_parts_description"
      contains: "secondary_ear"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "Threads the secondary_ear flag from kwarg through the build_parts_description call (Part-1 attach untouched)"
      contains: "secondary_ear"
    - path: "src/vibemix/__main__.py"
      provides: "VIBEMIX_GROUND_SECONDARY_EAR env read passed as a default-False DJCoHostAgent kwarg"
      contains: "VIBEMIX_GROUND_SECONDARY_EAR"
    - path: "src/vibemix/llm/_router_config.py"
      provides: "live_coach documented as the bench-swap alias (comment only, no literal change)"
      contains: "live_coach"
  key_links:
    - from: "src/vibemix/__main__.py"
      to: "src/vibemix/agent/dj_cohost.py DJCoHostAgent"
      via: "secondary_ear kwarg (default False) from VIBEMIX_GROUND_SECONDARY_EAR env read"
      pattern: "secondary_ear="
    - from: "src/vibemix/agent/dj_cohost.py"
      to: "src/vibemix/prompts/matrix.py build_parts_description"
      via: "secondary_ear kwarg threaded into the parts_clause call (contents Part attach untouched)"
      pattern: "build_parts_description"
    - from: "src/vibemix/agent/dj_cohost.py llm_node"
      to: "src/vibemix/coach/citation_linter.py CitationLinter.check"
      via: "registry-backed, prompt/audio-blind gate strips un-backed audio claims"
      pattern: "_linter\\.check"
---

<objective>
Implement Phase 80 over the existing machinery (research-verified: the master audio is ALREADY Part 1 of every reaction turn, the model ALREADY resolves via `model_router`, the linter is ALREADY prompt/audio-blind). This is framing + a gated flag + flipping the Wave-0 guard tests — NOT new audio capture.

GROUND-01: add a default-False `secondary_ear` kwarg to `build_parts_description` that, when True, appends a short clause naming the Part-1 audio a *secondary grounding signal* (structured evidence is authoritative; never claim an event it doesn't list). Thread the flag from a default-OFF `VIBEMIX_GROUND_SECONDARY_EAR` env read in `__main__.py` → a default-False `DJCoHostAgent` kwarg → the `build_parts_description` call. The Part-1 audio attach stays UNCONDITIONAL (gating it breaks byte-identity — Pitfall 2). Flip the two Wave-0 xfail scaffolds to real-green; the byte-identity + router pins stay green.

GROUND-02: confirm the reaction model resolves via `model_router.resolve("live_coach")` (already true) and document `live_coach` in `_router_config.py` as the one-line Phase-81 bench-swap alias (comment only — no literal change, grep-gate stays green).

Purpose: make the secondary-ear contract deliberate, gated, and bench-measurable while proving (via the flipped guard test) the audio Part can never widen the citable set.
Output: gated framing in `matrix.py` + threaded flag in `dj_cohost.py`/`__main__.py` + bench-alias doc comment in `_router_config.py`; the Wave-0 module goes 4 passed / 0 xfailed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-ground-gemini-as-secondary-ear/80-RESEARCH.md
@.planning/phases/80-ground-gemini-as-secondary-ear/80-PATTERNS.md
@.planning/phases/80-ground-gemini-as-secondary-ear/80-01-PLAN.md

<interfaces>
<!-- Exact anchors verified against live source 2026-05-26 — no exploration needed. -->

build_parts_description (src/vibemix/prompts/matrix.py:599-678) — pure function, 4-way deterministic dispatch keyed on (has_mic_part, has_lookahead_part). Current signature:
    def build_parts_description(audio_seconds: float, has_mic_part: bool, has_lookahead_part: bool) -> str
Each branch returns a string ending in the shared `refrain` ("Your ears are the referee — the evidence above is grounded context.") The 1-Part baseline (matrix.py:644-649) is the byte-identity target. Pinned by tests/prompts/test_matrix_3part_labeling.py.

The call site (src/vibemix/agent/dj_cohost.py:1505-1509) — pass the new flag through; DO NOT touch the contents Part attach at :1511-1514 (Part 1 is unconditional in v8.0).

DJCoHostAgent.__init__ (src/vibemix/agent/dj_cohost.py:388 onward) — add the new kwarg alongside the existing default-False/None flags (e.g. `recall_enabled: bool = False` at the signature, `self._recall_enabled = ...` in the body). Mirror that pattern: `secondary_ear: bool = False` → `self._secondary_ear: bool = secondary_ear`.

Flag-read + agent-construction site (src/vibemix/__main__.py): the `VIBEMIX_RECALL_ENABLED` read is at :1035; the `DJCoHostAgent(...)` construction is at :1058 with `recall_enabled=recall_enabled` at :1100. Add the GROUND read next to :1035 and the kwarg next to :1100.

GROUND-02 (src/vibemix/agent/config.py:25): `LLM_MODEL: str = resolve("live_coach")[0]` — already config-resolved; the reaction call passes `model=LLM_MODEL`. The alias table entry is `_router_config.py:27`: `"live_coach": ("gemini-3.5-flash", ServiceTier.STANDARD)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gated secondary-ear framing in build_parts_description + flip the framing xfail</name>
  <files>src/vibemix/prompts/matrix.py, tests/agent/test_dj_cohost_ground_secondary.py</files>
  <behavior>
    - Flag OFF (default): all 4 returned strings byte-identical to today (test_matrix_3part_labeling.py + test_flag_off_byte_identical stay green).
    - Flag ON: every branch's return contains the substring `"secondary grounding signal"` (the framing clause), appended consistently (to the common tail before/around the refrain so all 4 branches carry it uniformly).
    - The framing clause states the audio is a secondary grounding signal, the structured evidence above is authoritative, and to never claim an event it does not list — mirroring the existing anti-prediction guard phrasing style (matrix.py:665-667).
  </behavior>
  <read_first>
    - src/vibemix/prompts/matrix.py:599-678 (the function being modified — the 4 branches + the shared `refrain` + the anti-prediction guard phrasing to mirror).
    - tests/prompts/test_matrix_3part_labeling.py (the locked strings pinned — flag-OFF must keep these byte-identical).
    - tests/agent/test_dj_cohost_ground_secondary.py (the Wave-0 framing scaffold `test_flag_on_audio_framed` that asserts `"secondary grounding signal"` in `contents[0]`).
  </read_first>
  <action>
    Add a default-False `secondary_ear: bool = False` keyword-only-acceptable parameter to `build_parts_description` (append it to the signature so existing positional/keyword call sites stay valid). Update the docstring to note the gated extension and the flag-OFF byte-identity guarantee.

    Define the framing clause as a module-local string built only when `secondary_ear` is True — e.g. a `secondary_clause` that reads (wording is planner's discretion; it MUST contain the literal token `"secondary grounding signal"` the Wave-0 test asserts): the live audio is a *secondary grounding signal*, the structured evidence above is authoritative, and to never claim an event the evidence does not list. Append `secondary_clause` to EVERY branch's return uniformly — cleanest is to compute the per-branch base string as today, then return `base + (secondary_clause if secondary_ear else "")`. When `secondary_ear` is False the function returns exactly the current strings (no trailing change, not even whitespace) → byte-identity.

    Do NOT route a model literal anywhere; do NOT add a new import beyond stdlib. The function stays pure.

    Then flip `test_flag_on_audio_framed` in `tests/agent/test_dj_cohost_ground_secondary.py` from `xfail(strict=True)` to a real test — but note the agent-side kwarg thread it depends on lands in Task 2; if the flag-ON construction path requires the `DJCoHostAgent` kwarg, leave this scaffold xfail and flip it in Task 2 after the thread exists. (Decision: flip whichever of Test 3 only needs `build_parts_description(secondary_ear=True)` directly — if Test 3 calls the builder directly it flips here; if it drives `llm_node` it flips in Task 2. Match the test's actual construction from Plan 01.)
  </action>
  <acceptance_criteria>
    - `PYTHONPATH=src python3 -m pytest tests/prompts/test_matrix_3part_labeling.py -q` → all pass (flag-OFF byte-identity holds; the 4 locked strings unchanged).
    - `build_parts_description(60.0, False, False, secondary_ear=False) == build_parts_description(60.0, False, False)` (byte-identical default).
    - `"secondary grounding signal" in build_parts_description(60.0, False, False, secondary_ear=True)` is True for all four `(has_mic_part, has_lookahead_part)` combinations.
    - No model literal introduced (`PYTHONPATH=src python3 -m pytest tests/repo/test_model_literal_gate.py -q` green).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/prompts/test_matrix_3part_labeling.py tests/repo/test_model_literal_gate.py -q</automated>
  </verify>
  <done>`build_parts_description` carries a gated `secondary_ear` kwarg; flag-OFF byte-identical to v8.0 across all 4 branches; flag-ON appends the `"secondary grounding signal"` clause uniformly; matrix labeling + model-literal gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread the flag (dj_cohost + __main__) + flip the guard xfails + document the bench alias</name>
  <files>src/vibemix/agent/dj_cohost.py, src/vibemix/__main__.py, src/vibemix/llm/_router_config.py, tests/agent/test_dj_cohost_ground_secondary.py</files>
  <behavior>
    - DJCoHostAgent accepts `secondary_ear: bool = False`; with it False the `parts_clause` (and thus the reaction request) is byte-identical to v8.0 (test_flag_off_byte_identical + test_dj_cohost*.py goldens stay green).
    - With `secondary_ear=True` the framing substring `"secondary grounding signal"` appears in `contents[0]` (test_flag_on_audio_framed flips to real-green).
    - Even with the audio Part attached + flag ON, a reply citing `[ev:PHANTOM_DROP@45.2]` (absent from the registry) strips the whole turn — `llm_node` yields nothing (test_unbacked_audio_claim_strips flips to real-green).
    - `LLM_MODEL == resolve("live_coach")[0]` (test_model_via_router stays green) and `live_coach` is documented as the bench-swap alias.
  </behavior>
  <read_first>
    - src/vibemix/agent/dj_cohost.py:388 onward (the `__init__` signature + the `recall_enabled`/`self._recall_enabled` pattern to mirror for `secondary_ear`).
    - src/vibemix/agent/dj_cohost.py:1505-1514 (thread the flag into the `build_parts_description` call; DO NOT touch the contents Part-1 attach).
    - src/vibemix/__main__.py:1035 (the `VIBEMIX_RECALL_ENABLED` env-read pattern) + :1058-1100 (the `DJCoHostAgent(...)` construction with `recall_enabled=recall_enabled`).
    - src/vibemix/llm/_router_config.py:27 (the `live_coach` alias entry — add a doc comment only).
    - tests/agent/test_dj_cohost_ground_secondary.py (the two scaffolds to flip).
  </read_first>
  <action>
    In `src/vibemix/agent/dj_cohost.py`: add `secondary_ear: bool = False` to `DJCoHostAgent.__init__` (next to the other default-False flags), store `self._secondary_ear: bool = secondary_ear` in the body (mirror the `self._recall_enabled` line). At the `build_parts_description` call (:1505-1509), pass `secondary_ear=self._secondary_ear`. Leave the `contents` Part-1 attach at :1511-1514 EXACTLY as-is — Part 1 is unconditional (Pitfall 2). The framing flows through `parts_clause` → `contents[0]`, which is shared by BOTH the genai and OpenRouter brain paths (Assumption A3) — no path-specific branch.

    In `src/vibemix/__main__.py`: read the flag next to the recall read (:1035) — `ground_secondary_ear = os.environ.get("VIBEMIX_GROUND_SECONDARY_EAR", "0").strip().lower() not in ("0", "false", "no", "off", "")` (default OFF). Pass `secondary_ear=ground_secondary_ear` as a kwarg to the `DJCoHostAgent(...)` construction (next to `recall_enabled=recall_enabled`). Add a one-line `-> ...` startup print mirroring the recall print (e.g. `print("-> secondary-ear: ON" if ground_secondary_ear else "-> secondary-ear: OFF (set VIBEMIX_GROUND_SECONDARY_EAR=1 to flip)")`).

    In `src/vibemix/llm/_router_config.py`: add a comment on/above the `"live_coach"` entry (:27) documenting it as the Phase-81 BENCH bench-swap alias — "swap the winning reaction model here (one-line edit, no code change); resolved at agent/config.py:25 via resolve('live_coach')". NO change to the literal tuple itself (grep-gate stays green — `gemini-3.5-flash` is not banned and lives in the single allowlisted file).

    Flip the two `xfail(strict=True)` scaffolds in `tests/agent/test_dj_cohost_ground_secondary.py` to real tests: `test_flag_on_audio_framed` (now the `secondary_ear=True` kwarg exists → framing substring present, Part 1 survives) and `test_unbacked_audio_claim_strips` (construct with real linter/registry/tracker + `secondary_ear=True`, fake the `[ev:PHANTOM_DROP@45.2]` reply, assert no yield). If the agent construction in those tests needs extra kwargs to wire the linter, add them inline using the `_build_agent` template.
  </action>
  <acceptance_criteria>
    - `PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -q` → 4 passed, 0 xfailed (the two scaffolds flipped; xpassed would be a HARD failure had they been left xfail).
    - Flag-OFF byte-identity: `test_flag_off_byte_identical` still passes (the v8.0 string present, framing token absent) + existing `tests/agent/test_dj_cohost*.py` goldens unchanged.
    - The un-backed-claim guard: with the audio Part attached + flag ON, the `[ev:PHANTOM_DROP@45.2]` reply yields nothing (turn strips).
    - `PYTHONPATH=src python3 -m pytest tests/repo/test_model_literal_gate.py -q` green (no literal introduced; `_router_config.py` change is comment-only).
    - `grep -rn "VIBEMIX_GROUND_SECONDARY_EAR" src/vibemix/__main__.py` → exactly the one read; the kwarg defaults False so omitting the env var = v8.0 behavior.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py tests/agent/test_dj_cohost.py tests/repo/test_model_literal_gate.py -q</automated>
  </verify>
  <done>The flag threads OFF-by-default from env → kwarg → `build_parts_description`; flag-OFF byte-identical; flag-ON frames the audio + the guard test proves an un-backed audio claim strips; `live_coach` documented as the bench alias; the Wave-0 module is 4 passed / 0 xfailed; full suite green additively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio → Gemini | Part-1 master-output audio is local BlackHole/WASAPI bytes — audience-perspective master mix, NOT mic/PII, NOT user-supplied network input. Already sent in v8.0; this phase adds prompt framing only, not a new audio channel. |
| env flag → agent | `VIBEMIX_GROUND_SECONDARY_EAR` is a local process env var read once at boot, validated to a bool (default OFF). No untrusted network input. |
| Gemini reply → audience | Every emitted reaction crosses the registry-backed `CitationLinter` gate before audio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | audio Part lets Gemini fabricate an un-grounded claim | mitigate | The framing change is prompt-only; the `CitationLinter` (inputs `(full_text, registry_snapshot)`, prompt/audio-blind) still strips any atom missing from the registry. Pinned by `test_unbacked_audio_claim_strips` (flipped real-green here). Trust-the-audio invariant #3 holds by construction. |
| T-80-02 | Tampering / config drift | model-literal regression while documenting the bench alias | mitigate | `_router_config.py` change is comment-only; no literal added. `tests/repo/test_model_literal_gate.py` runs in both task verifies. The model resolves via `resolve("live_coach")` only. |
| T-80-03 | Spoofing/Elevation | flag flips cold-path behavior unexpectedly | mitigate | Flag defaults OFF; the Part-1 attach is unconditional (never gated); `test_flag_off_byte_identical` + the matrix labeling goldens pin the cold-path request byte-for-byte. |
| T-80-04 | Information disclosure | secret leak via error logs on the audio/LLM path | accept | No new log surface added; existing `_emit_connection_error` uses `str(err)[:300]` key-free classification. This phase adds one key-free startup print. |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (additive intra-package wiring + stdlib only). No package-legitimacy gate needed. |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -q` → 4 passed, 0 xfailed.
- `PYTHONPATH=src python3 -m pytest tests/prompts/test_matrix_3part_labeling.py tests/agent/test_dj_cohost.py tests/repo/test_model_literal_gate.py -q` → all green (cold-path byte-identity + no literal).
- Full suite green additively: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q` → no NEW failures; the 2 Plan-01 xfails are now real passes (net +2 passes, -2 xfails vs Plan-01 baseline).
- `grep -rn "VIBEMIX_GROUND_SECONDARY_EAR" src/` → only the one `__main__.py` read.
</verification>

<success_criteria>
- GROUND-01: a default-OFF `VIBEMIX_GROUND_SECONDARY_EAR` flag threads through to `build_parts_description`; flag OFF → byte-identical to v8.0 (audio Part still feeds, no new framing); flag ON → the prompt names the audio a secondary grounding signal with the structured evidence authoritative; an un-backed audio-derived claim strips to `<silence/>` (the flipped guard test proves it).
- GROUND-02: the reaction model resolves via `model_router.resolve("live_coach")` with zero literals (gate green); `live_coach` documented as the Phase-81 bench-swap alias.
- Honest green: no API key, no `genai.Client`, no model literal, no new package. The "is the second ear worth it / which model wins" judgment is Phase-81 BENCH + KAAN-ACTION (parked, never faked, never blocks).
</success_criteria>

<output>
Create `.planning/phases/80-ground-gemini-as-secondary-ear/80-02-SUMMARY.md` when done.
</output>
