---
phase: 80-ground-gemini-as-secondary-ear
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agent/test_dj_cohost_ground_secondary.py
autonomous: true
requirements: [GROUND-01, GROUND-02]

must_haves:
  truths:
    - "The hallucination-guard test exists and FAILS today (xfail-strict) because the guard test asserts the gate strips an un-backed audio-derived claim — it flips to a real pass when Plan 02 lands."
    - "The flag-OFF byte-identity test pins today's v8.0 reaction request shape so Plan 02 cannot regress the cold path."
    - "The GROUND-02 confirming test passes today (the model already resolves via model_router.resolve('live_coach') with no literal)."
  artifacts:
    - path: "tests/agent/test_dj_cohost_ground_secondary.py"
      provides: "Wave-0 Nyquist safety net for GROUND-01 (guard + framing) and GROUND-02 (router resolve)"
      contains: "test_dj_cohost_ground_secondary"
      min_lines: 120
  key_links:
    - from: "tests/agent/test_dj_cohost_ground_secondary.py"
      to: "vibemix.agent.dj_cohost.DJCoHostAgent.llm_node"
      via: "fake genai stream + _drive_llm_node helper copied from test_dj_cohost_mic_part.py"
      pattern: "generate_content_stream"
    - from: "tests/agent/test_dj_cohost_ground_secondary.py"
      to: "vibemix.llm.model_router.resolve"
      via: "assert LLM_MODEL == resolve('live_coach')[0]"
      pattern: "resolve\\(.live_coach.\\)"
---

<objective>
Install the Wave-0 Nyquist safety net for Phase 80 BEFORE any `src/` change: a new test file that pins every GROUND-01 / GROUND-02 acceptance criterion. The hallucination-guard and gated-framing behaviors that Plan 02 implements are written as `xfail(strict=True)` scaffolds (they flip to real passes when Plan 02 lands; a silent early-pass becomes an `xpassed` → HARD failure). The two behaviors already true today — the flag-OFF cold-path request shape and the model-resolves-via-router contract — are pinned as REAL-GREEN guards that must KEEP holding through Plan 02.

Purpose: the guard test IS the phase's core deliverable (prove an audio Part cannot widen the citable set); writing it RED first guarantees Plan 02's framing change cannot silently weaken the citation-grounding gate (invariant #2) or break cold-path byte-identity.
Output: `tests/agent/test_dj_cohost_ground_secondary.py` — 2 real-green pins (byte-identity + router-resolve) + 2 xfail-strict scaffolds (flag-ON framing + un-backed-claim-strips guard). No `src/` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-ground-gemini-as-secondary-ear/80-RESEARCH.md
@.planning/phases/80-ground-gemini-as-secondary-ear/80-PATTERNS.md

<interfaces>
<!-- Helpers to copy verbatim from the primary analog — no codebase exploration needed. -->

From tests/agent/test_dj_cohost_mic_part.py (the fake-genai-stream + Agent.__init__-patch template):
- `_async_iter(chunks)` — wraps a list of strings into an async chunk generator (each chunk = `type("Chunk", (), {"text": c})()`).
- `_FakeRecorder(session_dir)` — `.log_event(kind, **fields)` appends to `.events`; `.push_voice(pcm)` no-op.
- `_build_state()` — audible MusicState (deck A, "Daft Punk - Around the World", phase peak, rms 0.05, bpm 128).
- `_build_agent(mocker, tmp_path, mic_audio_buf)` — patches `Agent.__init__` to no-op, constructs `DJCoHostAgent(genai_client=…, clean_audio_buf=MagicMock, screen_buf=MagicMock, state=…, recorder=…, llm_inst=MagicMock, tts_inst=MagicMock, mic_audio_buf=…)`, returns `(agent, genai_client, recorder, state)`.
- `_drive_llm_node(agent)` — `asyncio.run` over `agent.llm_node(chat_ctx=None, tools=[], model_settings=None)`, collects yielded chunks into a list.
- Imports: `from google.genai import types`, `from vibemix.agent import DJCoHostAgent`, `from vibemix.state import AICoach, Event, MusicState`, `from tests.audio.conftest import int16_sine`, `from vibemix.audio import AudioBuffer, INPUT_SR_TARGET`.

From tests/agent/test_dj_cohost_grounding.py (registry / citation-gate construction):
- `EvidenceRegistry()` + `reg.register_library(fake_lib, t_session=0.0)` seeds citable track ids.
- `reg.has("ev", "PHANTOM_DROP", 45.2) is False` — an un-seeded atom does NOT resolve → the linter strips that turn.

DJCoHostAgent linter-wiring contract (src/vibemix/agent/dj_cohost.py:173-176):
- `self._linter_wired = all((citation_linter, evidence_registry, stripped_rate_tracker))` — to exercise the strip path in Test 3 the agent MUST be constructed with all three non-None (real `CitationLinter`, real `EvidenceRegistry`, real `StrippedRateTracker`).

GROUND-02 resolution point (src/vibemix/agent/config.py:25):
- `LLM_MODEL: str = resolve("live_coach")[0]` — `from vibemix.llm.model_router import resolve`; `from vibemix.agent.config import LLM_MODEL`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-green pins — flag-OFF cold-path byte-identity + GROUND-02 router-resolve</name>
  <files>tests/agent/test_dj_cohost_ground_secondary.py</files>
  <read_first>
    - tests/agent/test_dj_cohost_mic_part.py:38-131 (the `_async_iter` / `_FakeRecorder` / `_build_state` / `_build_agent` / `_drive_llm_node` helpers + the 2-Part baseline assert at lines 111-131 — copy these verbatim).
    - tests/repo/test_model_literal_gate.py (the resolve-via-router assert intent — mirror it as a direct `LLM_MODEL == resolve("live_coach")[0]` assertion).
    - src/vibemix/agent/config.py:25 (`LLM_MODEL = resolve("live_coach")[0]`).
  </read_first>
  <action>
    Create `tests/agent/test_dj_cohost_ground_secondary.py` with the SPDX header (`# SPDX-License-Identifier: Apache-2.0`), `from __future__ import annotations`, and the helper block copied verbatim from `test_dj_cohost_mic_part.py` (`_async_iter`, `_FakeRecorder`, `_build_state`, `_build_agent`, `_drive_llm_node`).

    Write TWO real-green tests (these describe behavior true TODAY and MUST keep passing after Plan 02):
    (1) `test_flag_off_byte_identical` — build the agent (mic_audio_buf=None), patch `vibemix.agent.dj_cohost.snapshot_wav` → `b"RIFFFAKEWAVMIX"`, patch `AICoach.build_prompt` → `"EVIDENCE: x"`, set the genai stream to `_async_iter(["ok"])`, fire a `HEARTBEAT` event, drive `llm_node`. Pull `contents = ...generate_content_stream.call_args.kwargs["contents"]`. Assert `len(contents) == 2`, `isinstance(contents[1], types.Part)`, AND that `contents[0]` (the text packet) STILL contains the v8.0 substring `"(audience perspective)"` and the refrain `"Your ears are the referee"` — but does NOT yet contain any secondary-ear framing substring (use the exact framing token Plan 02 will add — coordinate the literal: assert `"secondary grounding signal"` is NOT in `contents[0]`). This is the cold-path byte-identity pin: with no flag set, the request is the v8.0 1-Part baseline string.
    (2) `test_model_via_router` — `from vibemix.llm.model_router import resolve` + `from vibemix.agent.config import LLM_MODEL`; assert `LLM_MODEL == resolve("live_coach")[0]`. This is the GROUND-02 confirming pin (no hardcoded literal on the reaction path; the `live_coach` alias is the bench-swap point).

    Do NOT touch `src/`. Do NOT add a conftest fixture (reuse `tests/audio/conftest.py::int16_sine` + the mic-part helpers).
  </action>
  <acceptance_criteria>
    - `PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -k "flag_off_byte_identical or model_via_router" -x` → 2 passed.
    - `contents[0]` assertion proves the v8.0 baseline string is present AND the secondary-ear framing token is absent (cold-path byte-identity).
    - No `genai.Client` constructed, no `GEMINI_API_KEY` read, no `src/` file modified.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -k "flag_off_byte_identical or model_via_router" -x</automated>
  </verify>
  <done>Both real-green pins pass; the byte-identity test asserts the v8.0 string present + framing token absent; the router test asserts `LLM_MODEL == resolve("live_coach")[0]`.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds — flag-ON framing + un-backed-audio-claim strips (the core guard)</name>
  <files>tests/agent/test_dj_cohost_ground_secondary.py</files>
  <read_first>
    - tests/agent/test_dj_cohost_mic_part.py:269-275 (the Part 1 mime_type/`b"RIFF"` assertion shape to mirror for the flag-ON framing test).
    - tests/agent/test_dj_cohost_grounding.py:230-248 (`EvidenceRegistry` construction + `reg.has(...)` miss assertion — the registry-miss → strip property).
    - src/vibemix/agent/dj_cohost.py:173-176 (the `_linter_wired = all((citation_linter, evidence_registry, stripped_rate_tracker))` gate — construct the agent with all three real for Test 3) and :1973-1976 + :2050-2060 (linter chokepoint → strip-to-`<silence/>`, no yield).
  </read_first>
  <action>
    Append TWO `@pytest.mark.xfail(strict=True, reason="...")` scaffolds to the same file. Each describes Plan-02 behavior and MUST fail today (the framing flag does not exist; the guard test asserts the gate behavior under the audio Part):

    (3) `test_flag_on_audio_framed` — construct the agent with the (not-yet-existing) secondary-ear flag turned ON. Because Plan 02 will thread the flag as a default-False `DJCoHostAgent` kwarg (planner's chosen name `secondary_ear`), this test passes the kwarg `secondary_ear=True` to `_build_agent`-style construction (extend the helper inline, or build directly). Drive `llm_node` as in Test 1 and assert: Part 1 is STILL present (`contents[1]` is a `types.Part`, `mime_type == "audio/wav"`, data starts `b"RIFF"`) AND `contents[0]` NOW contains the secondary-ear framing substring `"secondary grounding signal"`. Today this fails because the kwarg + framing do not exist (TypeError on the kwarg or the substring missing) → xfail-strict.

    (4) `test_unbacked_audio_claim_strips` — THE core guard. Construct the agent with a REAL `CitationLinter`, REAL `EvidenceRegistry`, REAL `StrippedRateTracker` (so `_linter_wired` is True) and the secondary-ear flag ON. Seed the registry with only real observations (NOT `PHANTOM_DROP`); assert `reg.has("ev", "PHANTOM_DROP", 45.2) is False` as a precondition. Fake the genai stream to return a reply containing `[ev:PHANTOM_DROP@45.2]` (an audio-derived claim for an event the DSP never detected). Drive `llm_node` and assert it yields NOTHING (the whole turn strips to `<silence/>` — `chunks == []` or no audible text). This proves the audio Part cannot widen the citable set: trust-the-audio (invariant #3) wins by construction. Mark `xfail(strict=True)` because the flag-ON construction path does not exist yet (the test cannot be constructed today without the kwarg) — Plan 02 flips it to real-green.

    Run the full module to confirm the new xfails register correctly (2 passed from Task 1 + 2 xfailed).
  </action>
  <acceptance_criteria>
    - `PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -q` → 2 passed, 2 xfailed (xpassed = HARD failure under strict).
    - Test 4 seeds the registry WITHOUT `PHANTOM_DROP` and asserts `reg.has("ev","PHANTOM_DROP",45.2) is False` as an explicit precondition.
    - Test 3 asserts Part 1 survives (mime_type audio/wav, `b"RIFF"`) AND the framing substring `"secondary grounding signal"` appears in `contents[0]`.
    - No `genai.Client`, no `GEMINI_API_KEY`, no `src/` change.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -q</automated>
  </verify>
  <done>The module reports 2 passed + 2 xfailed; the two scaffolds encode the flag-ON framing presence and the un-backed-audio-claim strip, both keyed to the `secondary_ear` flag Plan 02 adds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio → Gemini | The master-output audio (Part 1) is local BlackHole/WASAPI bytes (NOT user-supplied network input, NOT mic/PII — it is the audience-perspective master mix). Already sent in v8.0. |
| Gemini reply → audience | Gemini's emitted text crosses the `CitationLinter` gate before any audio output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering | the audio Part lets Gemini fabricate an un-grounded claim | mitigate | This plan's Test 4 (`test_unbacked_audio_claim_strips`) pins the property that an audio-derived claim citing an event absent from `EvidenceRegistry` strips the whole turn. The linter is structurally prompt/audio-blind (inputs are `(full_text, registry_snapshot)` only). |
| T-80-02 | Repudiation | a silent early-pass hides that the guard never fired | mitigate | `xfail(strict=True)` → an `xpassed` is a HARD failure, so the guard scaffold cannot pass before Plan 02 wires the flag-ON path. |
| T-80-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages (test-only; reuses existing pytest infra). No package-legitimacy gate needed. |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_ground_secondary.py -q` → 2 passed, 2 xfailed.
- Full suite green additively: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q` → no NEW failures (baseline ~4501 passed + the 2 new passes + 2 new xfails).
- No `src/` change in this plan (the implementation is Plan 02).
</verification>

<success_criteria>
- The hallucination-guard scaffold (Test 4) and the flag-ON framing scaffold (Test 3) exist as `xfail(strict=True)` — they FAIL today and will flip to real passes in Plan 02.
- The flag-OFF byte-identity pin (Test 1) and the GROUND-02 router-resolve pin (Test 2) are real-green and must keep passing through Plan 02.
- Honest green: no API key, no `genai.Client`, no model literal, no new package.
</success_criteria>

<output>
Create `.planning/phases/80-ground-gemini-as-secondary-ear/80-01-SUMMARY.md` when done.
</output>
