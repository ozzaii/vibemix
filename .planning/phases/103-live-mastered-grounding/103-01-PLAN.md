---
phase: 103-live-mastered-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/learn/skill_tree.py
  - tests/learn/test_skill_tree.py
autonomous: true
requirements: [MAST-01, MAST-04]
tdd: true

must_haves:
  truths:
    - "record_live_demo is a NO-OP for a skill that is not yet Competent (no buffered backfill)"
    - "N grounded demos flip a Competent skill to mastered=True"
    - "first_mastered_at is stamped exactly once and never overwritten by later demos"
    - "live_proof_count + mastered + first_mastered_at survive a save->load round-trip and SkillTree.compute reads stage='mastered'"
  artifacts:
    - path: "src/vibemix/learn/skill_tree.py"
      provides: "record_live_demo(progress, skill_id, *, now) mutator + mastered_threshold on SkillSpec"
      contains: "def record_live_demo"
    - path: "tests/learn/test_skill_tree.py"
      provides: "record_live_demo flip/idempotent/reload/locked-no-op tests"
      contains: "test_first_mastered_at_idempotent"
  key_links:
    - from: "src/vibemix/learn/skill_tree.py::record_live_demo"
      to: "LearnProgress.skills live-portion block"
      via: "in-place setdefault + increment of progress.skills[skill_id]"
      pattern: "progress\\.skills"
    - from: "src/vibemix/learn/skill_tree.py::record_live_demo"
      to: "SkillTree.compute MAST-01 Competent gate"
      via: "competent check before any mutation"
      pattern: "competent"
---

<objective>
Add the deferred-from-102 live-portion mutator `record_live_demo` to `skill_tree.py`, plus the per-skill `mastered_threshold` it reads, so a grounded live demonstration can advance a Competent skill toward Mastered and persist the result. This is the persistence/state floor the Wave-2 recognizer (Plan 02) calls into.

Purpose: MAST-04 (N grounded demos flip Mastered with persisted count + `first_mastered_at`) and the MAST-01 locked-until-Competent gate are implemented here as a pure transform over the `skills` block that Phase 102 left as the forward seam. Phase 102 already SHIPPED the storage fields + the `stage="mastered"` derivation; this plan only WRITES the slot.
Output: `record_live_demo(progress, skill_id, *, now) -> LearnProgress` + `mastered_threshold: int = 3` on `SkillSpec`, with the 6 manifest entries left uniform at the default; extended `tests/learn/test_skill_tree.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-live-mastered-grounding/103-RESEARCH.md
@.planning/phases/103-live-mastered-grounding/103-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from source this session — no codebase exploration needed. -->

From src/vibemix/learn/skill_tree.py (Phase 102, SHIPPED — extend additively):
- `@dataclass(frozen=True) class SkillSpec` has fields `lesson_ids: tuple[str, ...]` and `gate: str`. ADD a new field `mastered_threshold: int = 3` (default keeps the 6 existing SKILL_MANIFEST entries valid without edits — frozen dataclass field with default).
- `SKILL_MANIFEST: dict[str, SkillSpec]` keys are EXACTLY the 6 skill ids: `deck_control`, `beatmatching`, `eq_mixing`, `harmonic_mixing`, `transitions`, `phrasing_performance`.
- `@dataclass class SkillProgress` exposes `competent: bool` and `stage: str` ("locked" | "competent" | "mastered").
- `class SkillTree` has `compute(self, progress) -> dict[str, SkillProgress]` — PURE, single-arg. `SkillTree().compute(progress)[skill_id].competent` is the canonical Competent read (NOT a stored field — `competent` is DERIVED, never persisted).
- `_assert_manifest_matches_curriculum()` runs at import; adding a defaulted field to SkillSpec does not affect it.

From src/vibemix/learn/progress.py (Phase 102, SHIPPED — do NOT modify):
- `LearnProgress.skills: dict[str, dict[str, Any]]` — per skill id `{"live_proof_count": int, "mastered": bool, "first_mastered_at": str | None}`. Seeded by `_fresh_skills_block()` (factory). `from_dict` only isinstance-checks the TOP-LEVEL `skills` dict, never inner value types — so a hand-edited / garbage `live_proof_count` reaches the mutator untouched (degrade with the same `int(... or 0)` + try/except guard that `skill_tree.py:294-297` uses; never raise).
- `save_progress(progress: LearnProgress) -> Path` (progress.py:413) — atomic tmp + os.replace. `load_progress() -> tuple[LearnProgress, bool]` (progress.py:374). `progress_path()` is a monkeypatchable function (set `VIBEMIX_LEARN_PROGRESS_PATH` env or monkeypatch `vibemix.learn.progress.progress_path`).
- Timestamp shape used project-wide (mark_completed precedent, progress.py:204): ISO-8601 UTC `"%Y-%m-%dT%H:%M:%SZ"`. record_live_demo takes `now` INJECTED (a string) for determinism — it does NOT call a clock itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mastered_threshold to SkillSpec + write the failing record_live_demo behavior tests</name>
  <read_first>
    - src/vibemix/learn/skill_tree.py (the file being modified — SkillSpec, SKILL_MANIFEST, SkillProgress, SkillTree.compute)
    - src/vibemix/learn/progress.py:90-215 (LearnProgress.skills shape, _fresh_skills_block, save_progress/load_progress signatures)
    - tests/learn/test_skill_tree.py (the test idioms to extend — _complete_all helper, _EXPECTED_SKILLS, fixture style)
    - src/vibemix/state/event_detector.py:509 (confirms the live-portion is the only thing P103 writes — read-only context, do NOT edit)
  </read_first>
  <behavior>
    - test_record_live_demo_flips_mastered_at_threshold: a Competent skill, after `mastered_threshold` (3) calls to record_live_demo, has mastered=True in progress.skills[skill_id]; before the Nth call it is still False.
    - test_first_mastered_at_idempotent: after the flip, two more record_live_demo calls leave first_mastered_at byte-equal to the value stamped at the flip (set ONLY on the not-mastered→mastered transition); live_proof_count keeps incrementing past N.
    - test_live_demo_noop_when_not_competent: a sub-Competent skill (no recital gate passed) — record_live_demo leaves live_proof_count at 0 and mastered=False no matter how many times it is called (MAST-01; no buffered backfill).
    - test_mastered_persists_across_reload: save_progress after the flip then load_progress → SkillTree().compute(loaded)[skill_id].stage == "mastered" and live_proof_count preserved (complements existing test_mastered_live_portion_promotes_stage).
    - test_record_live_demo_degrades_garbage_count: a skills block with a non-numeric live_proof_count (e.g. "x") does not raise — record_live_demo treats it as 0 and increments to 1.
  </behavior>
  <action>
    Write these five tests in tests/learn/test_skill_tree.py FIRST (RED) against the not-yet-written record_live_demo. Build a Competent skill the SAME way the existing engine tests do: complete the skill's lessons first-try via the _complete_all helper AND set the gating recital flag (e.g. progress.course_3_unlocked = True for eq_mixing / transitions / phrasing_performance; course_2_unlocked for deck_control) so SkillTree().compute(progress)[skill_id].competent is True. Use eq_mixing as the primary Competent fixture (its gate is course_3_unlocked, lessons L1.14/L2.04/L2.05). For the locked-no-op test, leave the recital gate False so competent stays False. Inject now as a fixed ISO string (e.g. "2026-05-29T12:00:00Z") — never call datetime in the test or the mutator. For the reload test, monkeypatch vibemix.learn.progress.progress_path to a tmp_path file (mirror the existing persistence-test idiom; the skills/library gotcha about CACHE_PATH does not apply here, but DO route through progress_path so the real ~/.cache file is never touched). Run the suite and confirm these five fail (record_live_demo undefined). Do NOT implement the function in this task.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree.py -k "record_live_demo or first_mastered_at or noop_when_not_competent or mastered_persists or degrades_garbage" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/learn/test_skill_tree.py contains the five named test functions (test_record_live_demo_flips_mastered_at_threshold, test_first_mastered_at_idempotent, test_live_demo_noop_when_not_competent, test_mastered_persists_across_reload, test_record_live_demo_degrades_garbage_count).
    - The five tests FAIL at this task (RED) with an AttributeError/ImportError on record_live_demo, NOT with a fixture error.
    - No production code (record_live_demo body) is written yet.
  </acceptance_criteria>
  <done>The five behavior tests exist and fail red against the unwritten mutator; Competent fixtures correctly set both the fill (lessons) AND the recital gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement record_live_demo + the per-skill threshold to turn the suite green</name>
  <read_first>
    - src/vibemix/learn/skill_tree.py (the file being modified — SkillSpec to extend, SkillTree.compute for the Competent read, the int(... or 0) guard at :294-297 to mirror)
    - tests/learn/test_skill_tree.py (the five RED tests from Task 1 that must go green)
    - src/vibemix/learn/progress.py:90-133 (_fresh_skills_block live-portion shape the mutator setdefaults into)
  </read_first>
  <action>
    Add `mastered_threshold: int = 3` as a defaulted field on the frozen SkillSpec (the 6 SKILL_MANIFEST entries stay valid unchanged — uniform N=3 per RESEARCH Deliverable 4; per-skill override is now cheap for the §EARNED-MASTERY-THRESHOLD-TUNE KAAN-ACTION). Add a module-level `_threshold_for(skill_id, manifest=SKILL_MANIFEST) -> int` helper returning the spec's mastered_threshold (default 3 for an unknown id). Implement `record_live_demo(progress, skill_id, *, now: str) -> LearnProgress` as a pure transform per MAST-01/MAST-04: (1) MAST-01 GATE — compute Competent via SkillTree().compute(progress)[skill_id].competent; if NOT Competent (or skill_id unknown), return progress unchanged (NO-OP, no setdefault write — locked skills ignore live events, no buffered backfill). (2) setdefault the skills[skill_id] block to the live-portion defaults (live_proof_count=0, mastered=False, first_mastered_at=None) — mirror _fresh_skills_block's shape. (3) Read the current count with the int(block.get("live_proof_count", 0) or 0) try/except guard copied from compute (never raise on garbage). (4) If already mastered: increment count only, NEVER re-stamp first_mastered_at, return (idempotency — Pitfall 2). (5) Else increment count; if count >= _threshold_for(skill_id) AND not mastered, set mastered=True and first_mastered_at=now ONCE. Return progress. Mutate ONLY progress.skills[skill_id]; touch no other field; call no clock and no I/O (now is injected; persistence is the caller's job via save_progress). Add a docstring explaining WHY first_mastered_at is guarded (true-first-mastery moment) and WHY the count guard mirrors compute (never-raises contract). Run the Task-1 tests to green, then the full skill_tree suite.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree.py 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - skill_tree.py contains `def record_live_demo(progress, skill_id, *, now` and `mastered_threshold` on SkillSpec and a `_threshold_for` helper.
    - All five Task-1 tests pass; the existing 102 skill_tree tests (incl. test_mastered_live_portion_promotes_stage, test_quality_weighted_fill_ordering) still pass — `pytest -q tests/learn/test_skill_tree.py` is fully green.
    - `grep -n "first_mastered_at" src/vibemix/learn/skill_tree.py` shows it set only inside the `not mastered ... >= threshold` branch (idempotency by construction).
    - record_live_demo performs ZERO write when the skill is sub-Competent (verified by test_live_demo_noop_when_not_competent staying green).
  </acceptance_criteria>
  <done>record_live_demo + mastered_threshold implemented; all skill_tree tests green; first_mastered_at idempotent and the Competent gate enforced by construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live event → skill credit | A detected (or fabricated) event proposes mastery credit. The Plan-02 recognizer gates it on a citation; THIS plan's mutator is reached only after that gate, but must itself not over-credit. |
| on-disk learn-progress.json → engine | A hand-edited / corrupt `skills` block (user owns the file) must never crash the mutator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Spoofing | record_live_demo crediting a non-Competent skill (skipping the earned floor) | mitigate | MAST-01 gate: compute Competent via SkillTree.compute before any write; sub-Competent → NO-OP (pinned by test_live_demo_noop_when_not_competent). |
| T-103-02 | Tampering | first_mastered_at overwritten on later demos, losing the true first-mastery moment | mitigate | Stamp ONLY on the not-mastered→mastered transition; once mastered, only increment count (pinned by test_first_mastered_at_idempotent). |
| T-103-03 | DoS | Corrupt/hand-edited `live_proof_count` (non-numeric) wedges the engine via uncaught ValueError | mitigate | Mirror compute's `int(... or 0)` + try/except guard; degrade garbage to 0 (pinned by test_record_live_demo_degrades_garbage_count). Local-single-user file tamper is out of v11.0 scope; never-raises is the control. |
| T-103-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (zero new deps per RESEARCH § Package Legitimacy Audit — pure-Python first-party). No [ASSUMED]/[SUS] packages exist; legitimacy checkpoint not applicable. |
</threat_model>

<verification>
Full learn island green (baseline 479 passed / 1 skipped before this phase; this plan adds tests, removes none):
`source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/learn`
Confirm Invariant #1 / #4 / privacy pins in test_skill_tree_invariants.py still pass (adding a defaulted field + a pure mutator imports no MusicState, opens no port, writes no profile.json).
</verification>

<success_criteria>
- MAST-04: `record_live_demo` increments `live_proof_count`; at `mastered_threshold` (3) it flips `mastered=True` and stamps `first_mastered_at`; count + flag + timestamp persist across save→load and `compute` reads `stage="mastered"`.
- MAST-01: a sub-Competent skill is a NO-OP — `record_live_demo` writes nothing and never advances Mastered.
- `first_mastered_at` is stamped exactly once (idempotent on later demos).
- The mutator never raises on a garbage `skills` block.
- `pytest -q tests/learn/test_skill_tree.py` fully green; full `tests/learn` green.
</success_criteria>

<output>
Create `.planning/phases/103-live-mastered-grounding/103-01-SUMMARY.md` when done.
</output>
