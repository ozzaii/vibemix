---
phase: 07-windows-port-audio-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/platform/__init__.py
  - src/vibemix/platform/_midi_common.py
  - src/vibemix/platform/_midi_macos.py
  - pyproject.toml
  - tests/test_platform.py
  - tests/test_platform_selector.py
  - tests/test_midi_common.py
  - tests/test_midi_macos.py
autonomous: true
requirements:
  - ARCH-02   # platform/ Protocol firewall — selector dispatch
tags:
  - platform-selector
  - midi-common
  - windows-deps
must_haves:
  truths:
    - "from vibemix.platform import AudioImpl, ScreenImpl, MidiImpl, TrackImpl resolves on macOS to the macOS impls"
    - "from vibemix.platform import AudioImpl on macOS does NOT pull pyaudiowpatch / win32 / winsdk into sys.modules"
    - "_midi_common.py exposes a reusable midi_listener_thread that both _midi_macos.py and a future _midi_windows.py can call"
    - "Phase 3 MidiMacOS golden behavior is byte-identical before and after the _midi_common refactor (same ControllerState mutations for same MIDI message sequence)"
    - "uv sync on macOS succeeds and does NOT attempt to install pyaudiowpatch / pywin32 / winsdk"
    - "tests/test_platform.py::test_no_os_leaks still passes (selector imports stay underscore-only)"
  artifacts:
    - path: "src/vibemix/platform/_midi_common.py"
      provides: "Cross-platform midi_listener_thread(controller_state, stop_event, port_hint, mido_module) factory + connect/retry loop"
      min_lines: 60
    - path: "src/vibemix/platform/__init__.py"
      provides: "sys.platform-dispatched AudioImpl / ScreenImpl / MidiImpl / TrackImpl re-exports"
      contains: "if sys.platform == \"darwin\""
    - path: "pyproject.toml"
      provides: "Windows-only deps with sys_platform == 'win32' markers (pyaudiowpatch / pywin32 / winsdk)"
      contains: "sys_platform == 'win32'"
    - path: "tests/test_platform_selector.py"
      provides: "Asserts AudioImpl is AudioMacOS on darwin + no Windows deps in sys.modules"
      min_lines: 40
    - path: "tests/test_midi_common.py"
      provides: "Mocked mido module; feeds a scripted sequence into midi_listener_thread; asserts ControllerState mutations match expected"
      min_lines: 50
  key_links:
    - from: "src/vibemix/platform/__init__.py"
      to: "src/vibemix/platform/_audio_macos.py"
      via: "if sys.platform == 'darwin' import AudioMacOS as AudioImpl"
      pattern: "AudioImpl"
    - from: "src/vibemix/platform/_midi_macos.py"
      to: "src/vibemix/platform/_midi_common.py"
      via: "MidiMacOS.start_listener_thread delegates to _midi_common.midi_listener_thread"
      pattern: "midi_listener_thread"
---

<objective>
Wave 1 lays the cross-platform groundwork before any Windows-specific code lands:

1. Extend `src/vibemix/platform/__init__.py` with `sys.platform`-dispatched concrete-impl re-exports (`AudioImpl` / `ScreenImpl` / `MidiImpl` / `TrackImpl`) per CONTEXT.md "Platform Selector (locked)" section. On macOS today this resolves to the Phase 2/3 impls; once Waves 2-4 land their files, `win32` will resolve to the Windows impls.
2. Extract the shared MIDI listener thread from `_midi_macos.py` into `_midi_common.py` so Wave 4's `_midi_windows.py` reuses it verbatim. Preserve Phase 3 MidiMacOS byte-equivalent behavior — pin via a golden mocked-mido test.
3. Add Windows-only dependency markers to `pyproject.toml` (`pyaudiowpatch` / `pywin32` / `winsdk` gated on `sys_platform == 'win32'`) so `uv sync` skips them on macOS but bundles them on Windows.

Purpose: unblock Waves 2-4 — they need the selector to wire into and `_midi_common.py` to import. Refusing the refactor leaves duplicated `midi_listener_thread` logic across mac and Windows files (CONTEXT Decision: Claude's Discretion → "Recommend YES in Phase 7").

Output: extended `__init__.py` (selector), new `_midi_common.py` (shared listener), refactored `_midi_macos.py` (thin subclass / delegator), `pyproject.toml` with Windows markers, three test files (`test_platform_selector.py` / `test_midi_common.py` / regression-updated `test_midi_macos.py`). Phase 3 macOS test suite stays 100% green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-windows-port-audio-screen/07-CONTEXT.md
@src/vibemix/platform/__init__.py
@src/vibemix/platform/_midi_macos.py
@src/vibemix/platform/midi.py
@src/vibemix/platform/audio.py
@src/vibemix/platform/screen.py
@src/vibemix/platform/track.py
@tests/test_platform.py
@tests/test_midi_macos.py
@pyproject.toml

<interfaces>
<!-- Phase 1 Protocols + Phase 3 MidiMacOS extraction target. Lifted from current code. -->

From src/vibemix/platform/midi.py (Phase 1 Protocol — DO NOT MUTATE):
```python
class MidiPort(Protocol):
    name: str
    def poll(self) -> MidiMessage | None: ...
    def close(self) -> None: ...

class MidiBackend(Protocol):
    def list_input_ports(self) -> list[str]: ...
    def open_input(self, port_name: str) -> MidiPort: ...
```

From src/vibemix/platform/_midi_macos.py (Phase 3 — extraction target):
- `_CC_MAP` (dict, 13 entries) — DDJ-FLX4 control_change map.
- `_NOTE_MAP` (dict, 12 entries) — DDJ-FLX4 note_on map.
- `_knob_label(v: int) -> str` — 6-tier EQ/filter helper.
- `_xfader_label(v: int) -> str` — 5-tier xfader helper.
- `ControllerState` class — lock-protected; `mark_connected(name)`, `handle_msg(msg)`, `deck_snapshot()`, `moves_since(t)`.
- `_MidoPortAdapter` — wraps mido.IOPort to satisfy `MidiPort`.
- `MidiMacOS` class — `controller_state`, `list_input_ports()`, `open_input(name)`, `start_listener_thread(stop_event) -> Thread`.
- `start_listener_thread` inner `_run()` body (lines ~302-327): port_hint = "DDJ-FLX4"; while-not-stop loop; mido.get_input_names() match; mido.open_input(match); poll-and-handle inner loop.

From src/vibemix/platform/_audio_macos.py (Phase 2 — selector consumes this):
- `AudioMacOS(registry, recorder)` — constructor signature stays unchanged.
- `assert_device_sample_rate(device_index, expected)` — re-exported via __init__.

Selector target shape (per CONTEXT Decisions §Platform Selector):
```python
# src/vibemix/platform/__init__.py — additions
import sys
if sys.platform == "darwin":
    from vibemix.platform._audio_macos import AudioMacOS as AudioImpl
    from vibemix.platform._screen_macos import ScreenMacOS as ScreenImpl
    from vibemix.platform._midi_macos import MidiMacOS as MidiImpl
    from vibemix.platform._track_macos import TrackMacOS as TrackImpl
elif sys.platform == "win32":
    from vibemix.platform._audio_windows import AudioWindows as AudioImpl
    from vibemix.platform._screen_windows import ScreenWindows as ScreenImpl
    from vibemix.platform._midi_windows import MidiWindows as MidiImpl
    from vibemix.platform._track_windows import TrackWindows as TrackImpl
else:
    raise RuntimeError(f"Unsupported platform: {sys.platform}. vibemix supports macOS and Windows only.")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared MIDI listener into _midi_common.py + refactor _midi_macos.py</name>
  <files>src/vibemix/platform/_midi_common.py, src/vibemix/platform/_midi_macos.py, tests/test_midi_common.py, tests/test_midi_macos.py</files>
  <behavior>
    - test_midi_common_listener_finds_port_and_dispatches: with a fake `mido` module exposing `get_input_names() -> ["Foo", "DDJ-FLX4 Bus 1"]` and `open_input(name)` returning a fake port whose `poll()` yields a scripted [control_change(ch=0, cc=0x13, value=120)] then None, the listener thread (a) calls `controller_state.mark_connected("DDJ-FLX4 Bus 1")` and (b) calls `controller_state.handle_msg(msg)` exactly once with the scripted message; thread exits cleanly when stop_event is set.
    - test_midi_common_listener_retries_when_no_port_match: with a fake mido returning `[]` from get_input_names(), the listener loops + sleeps 2s + retries; assert no mark_connected, no handle_msg; thread exits when stop_event set during the sleep.
    - test_midi_common_listener_swallows_open_input_exception: fake mido whose open_input raises RuntimeError → listener prints "[midi listener err]" to stderr (capsys), sleeps 2s, retries; second iteration succeeds; thread exits cleanly.
    - test_midi_common_listener_substring_match_case_insensitive: get_input_names() returns `["ddj-flx4 USB MIDI"]`, port_hint="DDJ-FLX4" → matches.
    - test_midi_macos_golden_unchanged_behavior_after_refactor (regression): feed the existing v4 CC + Note message sequence (re-use the message fixtures from `tests/test_midi_macos.py::test_handle_msg_*`) through `MidiMacOS.controller_state.handle_msg(...)` directly AND through `_midi_common.midi_listener_thread` (with a fake mido); assert `deck_snapshot()` and `moves_since(0)` are EQUAL — proves the refactor preserves byte-identical state mutations.
  </behavior>
  <action>
    Create `src/vibemix/platform/_midi_common.py` exposing a top-level function:
    `def midi_listener_thread(controller_state, stop_event, port_hint, mido_module) -> None`
    that implements the v4:730-756 / _midi_macos.py:302-327 body verbatim — port enumeration, substring-match (case-insensitive), open_input + mark_connected, poll loop with 5ms sleep when poll() returns None, exception swallow with stderr log + 2s retry. The function is DESIGNED to be called as a Thread target. Accepts `mido_module` as a parameter (not a top-level import) so unit tests inject a fake; production callers pass `mido` directly.

    Add a thin helper `def spawn_listener(controller_state, stop_event, port_hint, mido_module) -> threading.Thread` that wraps the above in a daemon thread, calls `t.start()`, and returns the Thread for join.

    Module docstring: explain this is the cross-platform extraction; both `_midi_macos.py` and Wave 4's `_midi_windows.py` call it. Note the test-injection seam.

    Refactor `src/vibemix/platform/_midi_macos.py` `MidiMacOS.start_listener_thread`: delete the inline `_run` body; replace with `return spawn_listener(self.controller_state, stop_event, "DDJ-FLX4", mido)`. The `try: import mido` block at top of file stays (mido is the macOS mido module). Keep `_HAS_MIDO` guard — if false, return a dummy thread (early-exit `_run`) per existing pattern. Module docstring: append "Phase 7 Wave 1 refactor: listener thread body moved to `_midi_common.py` — this file is now a thin wrapper around mido + the v4 maps."

    `_CC_MAP`, `_NOTE_MAP`, `_knob_label`, `_xfader_label`, `ControllerState`, `_MidoPortAdapter` STAY in `_midi_macos.py` — those are the v4 maps + decoder, not the listener thread. (Future Phase 9 may extract them into `_midi_common.py` too; Wave 1 ships the minimum extraction.)

    Update `tests/test_midi_macos.py`: the existing tests for `_CC_MAP` / `_NOTE_MAP` / `ControllerState.handle_msg` / `_knob_label` / `_xfader_label` stay UNTOUCHED (they test the maps which still live in `_midi_macos.py`). Append the `test_midi_macos_golden_unchanged_behavior_after_refactor` regression test.

    Create `tests/test_midi_common.py` with the 4 listener tests (port match, no-match retry, exception swallow, substring case-insensitivity) using a pytest-mock-style fake mido module (mock with `types.SimpleNamespace` or a hand-rolled class with `get_input_names()` and `open_input(name)`). Use `threading.Event` + a 0.1s wait pattern; do NOT actually sleep 2s in tests (monkeypatch `time.sleep` to short-circuit, or set stop_event immediately so the retry never executes).

    Per D-Selector + D-MIDI-refactor in CONTEXT.md.

    Avoid: importing mido at top of `_midi_common.py` (test injection pattern requires it as a parameter). Avoid touching the v4 maps. Avoid adding any windows-specific logic — listener stays platform-agnostic.
  </action>
  <verify>
    <automated>uv run pytest tests/test_midi_common.py tests/test_midi_macos.py -x -q 2>&amp;1 | tail -25</automated>
  </verify>
  <done>
    `_midi_common.py` exports `midi_listener_thread` + `spawn_listener`; `_midi_macos.py::MidiMacOS.start_listener_thread` delegates to it; 4 new common tests pass; existing 50+ midi_macos tests still pass; golden regression test confirms byte-identical state mutations.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Platform selector dispatch in __init__.py + Windows-only deps in pyproject.toml</name>
  <files>src/vibemix/platform/__init__.py, pyproject.toml, tests/test_platform_selector.py, tests/test_platform.py</files>
  <behavior>
    - test_selector_resolves_macos_impls_on_darwin: on `sys.platform == "darwin"` (skip otherwise), `from vibemix.platform import AudioImpl, ScreenImpl, MidiImpl, TrackImpl` succeeds and `AudioImpl is AudioMacOS`, `ScreenImpl is ScreenMacOS`, `MidiImpl is MidiMacOS`, `TrackImpl is TrackMacOS`.
    - test_selector_does_not_import_windows_only_modules_on_macos: after `import vibemix.platform` on darwin, `sys.modules` contains NEITHER `pyaudiowpatch` NOR any module starting with `win32` NOR `winsdk` NOR `vibemix.platform._audio_windows` / `_screen_windows` / `_midi_windows` / `_track_windows`.
    - test_selector_raises_on_unsupported_platform: monkeypatch `sys.platform = "linux"`, then reload `vibemix.platform` (importlib.reload after sys.modules pop), expect `RuntimeError` with substring "Unsupported platform" + "macOS and Windows only".
    - test_pyproject_contains_windows_only_markers: parse `pyproject.toml` via `tomllib`, walk `project.dependencies`, assert every spec in {`pyaudiowpatch`, `pywin32`, `winsdk`} is present AND has marker substring `sys_platform == 'win32'`.
    - test_uv_lock_or_sync_dry_run_excludes_windows_deps_on_macos: `uv pip compile --no-emit-package pyaudiowpatch ...` is brittle; instead run `uv pip install --dry-run -e . 2>&1` and grep that pyaudiowpatch / pywin32 / winsdk do NOT appear in the resolve plan on darwin. (Mark `pytest.mark.skipif(sys.platform != "darwin")`.)
    - test_no_os_leaks still passes (test_platform.py): the selector additions use only `sys` + underscore-prefixed concrete-impl modules; no forbidden top-level imports leak into `__init__.py`.
  </behavior>
  <action>
    Extend `src/vibemix/platform/__init__.py` per CONTEXT Decisions §Platform Selector (locked):

    Below the existing Protocol + Phase 2/3 macOS re-exports, append:
    ```python
    import sys as _sys  # local alias to avoid polluting __all__

    if _sys.platform == "darwin":
        from vibemix.platform._audio_macos import AudioMacOS as AudioImpl
        from vibemix.platform._screen_macos import ScreenMacOS as ScreenImpl
        from vibemix.platform._midi_macos import MidiMacOS as MidiImpl
        from vibemix.platform._track_macos import TrackMacOS as TrackImpl
    elif _sys.platform == "win32":
        from vibemix.platform._audio_windows import AudioWindows as AudioImpl
        from vibemix.platform._screen_windows import ScreenWindows as ScreenImpl
        from vibemix.platform._midi_windows import MidiWindows as MidiImpl
        from vibemix.platform._track_windows import TrackWindows as TrackImpl
    else:
        raise RuntimeError(
            f"Unsupported platform: {_sys.platform}. vibemix supports macOS and Windows only."
        )
    ```

    Append `"AudioImpl", "ScreenImpl", "MidiImpl", "TrackImpl"` to `__all__`. The existing Phase 2/3 `AudioMacOS` / `ScreenMacOS` / `MidiMacOS` / `TrackMacOS` re-exports STAY (Phase 3 code imports them by name).

    Add Windows-only deps to `pyproject.toml` `[project] dependencies` list, with `sys_platform == 'win32'` markers — same style as existing pyobjc lines. Per CONTEXT Decision §Windows-Only Dependencies:
    ```toml
    "pyaudiowpatch>=0.2.12 ; sys_platform == 'win32'",
    "pywin32>=308 ; sys_platform == 'win32'",
    "winsdk>=1.0 ; sys_platform == 'win32'",
    ```
    (Base-deps with markers, NOT a `[project.optional-dependencies] windows` group — CONTEXT's recommended option since uv sync skips them on macOS automatically AND PyInstaller picks them up on Windows without an extra flag.)

    Create `tests/test_platform_selector.py` with the 5 selector tests + 1 pyproject parse test listed in <behavior>. Use `importlib.reload` + `sys.modules` patching for the unsupported-platform test. Use `tomllib` (stdlib in 3.11+) to parse pyproject. The dry-run test is best-effort; if `uv pip install --dry-run` is too slow / brittle, drop it and rely on the marker-parse test (the marker test is sufficient evidence — uv honors sys_platform markers).

    Do NOT modify `tests/test_platform.py::test_no_os_leaks` — its `FORBIDDEN_TOP_LEVEL` set already excludes `sys`. The `if sys.platform == "win32"` branch in `__init__.py` imports underscore-prefixed module paths (`vibemix.platform._audio_windows`, etc.) which on darwin never execute — so the AST scan sees them as conditional-import nodes but the test's existing `_is_forbidden` check only flags by top-level module name (`vibemix` is not in FORBIDDEN_TOP_LEVEL). Verify by running the existing test.

    Per D-Selector + D-WindowsDeps in CONTEXT.md.

    Avoid: `try/except ImportError` around the win32 branch — that masks Wave 2/3/4 import bugs. Avoid making the selector lazy — eager import at module load is fine because the underscore-prefixed Windows files use lazy-imports for their actual OS deps (Wave 2-4 contracts). Avoid touching the existing `[dependency-groups] dev` block.
  </action>
  <verify>
    <automated>uv run pytest tests/test_platform_selector.py tests/test_platform.py -x -q 2>&amp;1 | tail -25 &amp;&amp; uv sync --dry-run 2>&amp;1 | grep -v -E "(pyaudiowpatch|pywin32|winsdk)" | tail -10 || true</automated>
  </verify>
  <done>
    Selector dispatches to macOS impls on darwin; `pyaudiowpatch` / `pywin32` / `winsdk` markers present in pyproject; 6+ selector tests green; `test_no_os_leaks` still green; full pytest suite remains green (≥531 baseline).
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_platform.py tests/test_platform_selector.py tests/test_midi_common.py tests/test_midi_macos.py -x -q` → all pass.
- `uv run pytest -x -q 2>&1 | tail -5` → full suite green (≥531 + 10 new = ~541 tests).
- `uv sync` on darwin succeeds; `cat uv.lock | grep -E "(pyaudiowpatch|pywin32|winsdk)" || echo "absent — correct"`.
- `python -c "import vibemix.platform; import sys; assert 'pyaudiowpatch' not in sys.modules; assert 'winsdk' not in sys.modules; print('no leak')"`.
- `uv run ruff check src/vibemix/platform/__init__.py src/vibemix/platform/_midi_common.py src/vibemix/platform/_midi_macos.py` clean.
- `uv run ruff format --check src/vibemix/platform/ tests/` clean.
- `git diff --name-only main -- cohost*.py run*.sh fillers/ mascot.html` → empty (POC files untouched).
</verification>

<success_criteria>
1. `from vibemix.platform import AudioImpl, ScreenImpl, MidiImpl, TrackImpl` works on darwin and dispatches to the four macOS impls.
2. `_midi_common.py` exists, exposes `midi_listener_thread` + `spawn_listener`, accepts an injectable `mido_module` parameter for testing.
3. `_midi_macos.py::MidiMacOS.start_listener_thread` delegates to `_midi_common.spawn_listener` — body shrinks; behavior pinned identical via golden test.
4. `pyproject.toml` has `pyaudiowpatch>=0.2.12 ; sys_platform == 'win32'` + `pywin32>=308 ; sys_platform == 'win32'` + `winsdk>=1.0 ; sys_platform == 'win32'` in `[project] dependencies`.
5. Full pytest suite green (≥541 tests).
6. `test_no_os_leaks` still passes — selector did not leak forbidden top-level imports into the Protocol-only modules.
7. POC files diff-untouched.
8. Atomic commit: `feat(07): wave 1 — platform selector + _midi_common refactor + windows-only deps`.
</success_criteria>

<output>
After completion, the next plan (07-02) implements `AudioWindows`. Wave 1 ships the foundation it depends on: the selector that will route to it on win32, the `_midi_common.py` it doesn't use directly (that's Wave 4) but that proves the refactor pattern works.

No SUMMARY file for this wave — the rolled-up `07-SUMMARY.md` lands in Wave 5.
</output>
