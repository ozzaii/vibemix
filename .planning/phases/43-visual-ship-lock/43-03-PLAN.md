---
phase: 43-visual-ship-lock
plan: 03
title: "UI audit closure — wizard + calibration + settings drawer + hover-glow sweep"
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - tauri/ui/src/wizard/onboarding-flow.ts
  - tauri/ui/src/wizard/step0-intro.ts
  - tauri/ui/src/wizard/step1-permissions.ts
  - tauri/ui/src/wizard/step2-output-device.ts
  - tauri/ui/src/wizard/step3-controller.ts
  - tauri/ui/src/wizard/step-profile-consent.ts
  - tauri/ui/src/wizard/step-telemetry-consent.ts
  - tauri/ui/src/settings/SettingsDrawer.ts
  - .planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md
  - .planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md
autonomous: true
requirements: [VIS-01, VIS-02]
req_ids: [VIS-01, VIS-02]
must_haves:
  truths:
    - "Every interactive element in `tauri/ui/src/wizard/` (across all 6 onboarding steps + onboarding-flow + router) carries `--glow-faint` on `:hover` and `:focus-visible`"
    - "Settings drawer (`tauri/ui/src/settings/SettingsDrawer.ts`) shares the same hover-glow contract (in-scope as a sibling of wizard per CONTEXT integration-point note — hosts Phase 42 ear-test toggle)"
    - "`UI-REVIEW-wizard.md` exists with seeded findings covering all 6 step files + onboarding-flow + router; status flipped to `HIGH-findings-closed` after critique→execute loop"
    - "`UI-REVIEW-calibration.md` exists with seeded findings covering step1-permissions + step2-output-device (the wizard tail that handles first-run calibration per CONTEXT); status flipped to `HIGH-findings-closed`"
    - "Each `UI-REVIEW-<surface>.md` audit-loop log carries ≥2 iteration rows beyond seed"
    - "Zero hex literals in component .ts CSS strings under `tauri/ui/src/wizard/` and `tauri/ui/src/settings/`"
  artifacts:
    - path: ".planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md"
      provides: "Closed audit for the wizard surface (6 onboarding steps + flow controller)"
      contains: "## Surface: wizard"
    - path: ".planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md"
      provides: "Closed audit for the calibration sub-surface (permissions + output device steps)"
      contains: "## Surface: calibration"
  key_links:
    - from: ".planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md"
      to: "tauri/ui/src/wizard/step*.ts"
      via: "Per-step findings + remediation references"
      pattern: "step[0-9-]+"
    - from: "tauri/ui/src/settings/SettingsDrawer.ts"
      to: "tokens.css (--glow-faint)"
      via: "Hover-glow application per VIS-02 sweep"
      pattern: "--glow-faint"
---

<objective>
Run the critique→execute audit loop on the wizard surface (6 onboarding steps + flow + router) and the calibration sub-surface (permissions + output device), and complete the hover-glow coverage sweep (VIS-02) for both surfaces and the settings drawer. Per CONTEXT §VIS-01 + the integration point note ("Phase 42 ear-test toggle in debrief is a UI surface that should be in scope of VIS-01 audit"), the settings drawer is included as a sibling because that's where the ear-test toggle lives.

Purpose: complete VIS-01 closure for the remaining two Tier-1 surfaces (wizard + calibration) and finish the VIS-02 hover-glow sweep for everything outside the live session bucket. Depends only on 43-01 (audit driver + INDEX); does NOT depend on 43-04 (meter.ts) because the wizard + calibration paths don't render the meter.

Output:
- Source edits across all 6 wizard step files + onboarding-flow + router.ts + settings drawer to close every HIGH finding + apply hover-glow.
- `UI-REVIEW-wizard.md` seeded + closed.
- `UI-REVIEW-calibration.md` seeded + closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-visual-ship-lock/43-CONTEXT.md
@.planning/phases/43-visual-ship-lock/43-01-SUMMARY.md
@.planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md

@tauri/ui/src/wizard/onboarding-flow.ts
@tauri/ui/src/wizard/router.ts
@tauri/ui/src/wizard/step0-intro.ts
@tauri/ui/src/wizard/step1-permissions.ts
@tauri/ui/src/wizard/step2-output-device.ts
@tauri/ui/src/wizard/step3-controller.ts
@tauri/ui/src/wizard/step-profile-consent.ts
@tauri/ui/src/wizard/step-telemetry-consent.ts
@tauri/ui/src/settings/SettingsDrawer.ts
@tauri/ui/src/tokens.css
@mocks/vibemix-settings-drawer.html

<interfaces>
<!-- Repo facts the executor needs. -->

6 wizard onboarding steps (per CONTEXT §VIS-01 — wizard = 6 steps):
1. step0-intro.ts
2. step1-permissions.ts          (CALIBRATION sub-surface — input/output device + perms)
3. step2-output-device.ts         (CALIBRATION sub-surface — output device test tone)
4. step3-controller.ts
5. step-profile-consent.ts
6. step-telemetry-consent.ts

Flow + routing:
- onboarding-flow.ts — top-level flow orchestrator
- router.ts — step-to-step router

Calibration sub-surface (CONTEXT explicit):
- "calibration / first-run" sourced from the wizard tail; step1-permissions + step2-output-device own the perm grants + output device test tone (the calibration UX).

Settings drawer:
- tauri/ui/src/settings/SettingsDrawer.ts — drawer entry
- Hosts Phase 42 ear-test toggle in debrief per integration-point note
- Visual reference: mocks/vibemix-settings-drawer.html

Hover-glow contract (same as Plan 43-02):
- Selector union: button:not([disabled]), a, [role="button"]:not([aria-disabled="true"]), [data-interactive]
- Apply `--glow-faint` on :hover + :focus-visible
- `transition: box-shadow var(--motion-snap) ease-out`

Audit-loop methodology:
- Driver: `uv run python scripts/launch/run_ui_audit.py --surface wizard --dry-run` writes skeleton if missing; same for `--surface calibration`
- Seed real findings by reading the step files
- Iterate with gsd-ui-checker + gsd-ui-auditor; loop until zero HIGH
- Append iteration rows to `### Audit Loop Log`
- Flip `status: HIGH-findings-closed` when done

Existing Vitest specs that must stay green:
- tauri/ui/tests/wizard/* (wizard.tokens.test.ts)
- tauri/ui/tests/settings/* (settings.tokens.test.ts)
- Touching the wizard step files MUST NOT introduce token violations (the legacy-detect test will fail)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hover-glow sweep across all 6 wizard steps + flow + router + settings drawer</name>
  <files>tauri/ui/src/wizard/onboarding-flow.ts, tauri/ui/src/wizard/step0-intro.ts, tauri/ui/src/wizard/step1-permissions.ts, tauri/ui/src/wizard/step2-output-device.ts, tauri/ui/src/wizard/step3-controller.ts, tauri/ui/src/wizard/step-profile-consent.ts, tauri/ui/src/wizard/step-telemetry-consent.ts, tauri/ui/src/settings/SettingsDrawer.ts</files>
  <action>
Per D-VIS-02 — apply `--glow-faint` hover sweep across every interactive element in the wizard + settings drawer surfaces.

For each file: read it fully; locate the CSS string registered via the `_style-registry.ts` `registerStyle` pattern. For every selector matching the interactive union (`button:not([disabled])`, `a`, `[role="button"]:not([aria-disabled="true"])`, `[data-interactive]`), add or extend a `:hover, :focus-visible` block that sets `box-shadow: var(--glow-faint)` and `transition: box-shadow var(--motion-snap) ease-out`. When a hover block already exists with a different shadow, append the glow comma-separated rather than overwrite.

Step controls (Continue, Back, Skip) MUST carry the glow. Permission-grant chips in step1-permissions.ts MUST carry the glow (they're `[data-interactive]` per the existing pattern). Settings drawer toggle rows (including the Phase 42 ear-test toggle) MUST carry the glow.

Token-only scrub: `grep -nE '#[0-9a-fA-F]{6}' tauri/ui/src/wizard/ tauri/ui/src/settings/` — replace any hex literal in string templates with the corresponding token from `tokens.css`. Skip comment-only references. `grep -nE 'rgba?\\(' tauri/ui/src/wizard/ tauri/ui/src/settings/` — replace literal rgba with token form.

Each touched file: leading comment in the CSS string MUST cite VIS-02 hover-glow sweep + Plan 43-03.

Do NOT add new behavior; this is a tactile-feel polish pass. The functional flow (perm-grant request, device probe, controller select, consent toggles) stays identical.
  </action>
  <verify>
    <automated>cd tauri/ui && grep -RcE '\-\-glow-faint' src/wizard/ src/settings/ | awk -F: '{s+=$2} END{ if (s < 6) { print "FAIL: glow-faint applied to fewer than 6 sites: "s; exit 1 } else { print "OK: "s } }' && (grep -RnE '#[0-9a-fA-F]{6}' src/wizard/ src/settings/ 2>/dev/null | grep -vE '^[^:]+:\s*//|^[^:]+:\s*/\*' | head -1 >/dev/null && echo "WARN: hex literal in code strings" || echo "OK: no hex literals in wizard/settings code strings")</automated>
  </verify>
  <done>Each of the 8 files in the file set carries at least one `--glow-faint` site; zero hex literals in code strings (comment-only OK); transitions use `var(--motion-snap)`; leading comments cite VIS-02 + 43-03; existing wizard/settings token Vitest specs stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Seed UI-REVIEW-wizard.md + UI-REVIEW-calibration.md; run critique→execute loop to closure</name>
  <files>.planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md, .planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md</files>
  <action>
Per D-VIS-01 — seed the two audits with real findings, then run the critique→execute loop.

(a) Seed UI-REVIEW-wizard.md: run `uv run python scripts/launch/run_ui_audit.py --surface wizard --dry-run --phase-dir .planning/phases/43-visual-ship-lock` to write the skeleton. Read all 6 step files + onboarding-flow + router. Surface real HIGH/MEDIUM/LOW findings:
- HIGH candidates: any interactive element without `--glow-faint` (after Task 1 — should be zero, but verify); any hex literal in code strings; any step lacking keyboard `:focus-visible` mirror; any consent button with ambiguous opt-in/opt-out copy (anti-dark-pattern); any contrast ratio below 4.5:1 (WCAG AA).
- MEDIUM candidates: density mismatches between step1-permissions and step2-output-device; typography weight inconsistency in step headers; transition timings not using `var(--motion-snap)`.
- LOW candidates: cosmetic polish (label alignment, icon padding).
- Each finding row: `**[FINDING-ID]** <step-file>:<element> — <one-line violation>; **remediation:** <one-line fix>; **owner:** 43-03`.

(b) Seed UI-REVIEW-calibration.md: run `uv run python scripts/launch/run_ui_audit.py --surface calibration --dry-run --phase-dir .planning/phases/43-visual-ship-lock`. Read step1-permissions + step2-output-device. Surface findings specific to first-run UX:
- HIGH candidates: any permission denial path that lacks a "fix this in System Settings" affordance; test tone playback button without amber-feedback state; input meter (if present in calibration) — cross-link to VIS-03 / Plan 43-04; any modal that traps keyboard focus poorly.
- MEDIUM candidates: copy clarity (Mac vs Windows permission language); device probe progress indicator (spinner vs progress bar inconsistency).
- LOW candidates: icon weight, label spacing.
- Same row format as wizard.

(c) Critique → execute loop for both surfaces: invoke `gsd-ui-checker` agent on each surface (Task tool launch; entry files listed; `mocks/vibemix-direction-final.html` as baseline; `mocks/vibemix-settings-drawer.html` as paired reference for the consent toggles). Invoke `gsd-ui-auditor` on the same surfaces. Apply remediation directly in source files (Task 1 already covers hover-glow; this task covers non-hover findings — density, contrast, copy, focus management). Append iteration rows: `iteration=N | agent=gsd-ui-checker / gsd-ui-auditor | verdict=<...> | files_changed=<...> | notes=<...>`. Loop until zero HIGH per surface.

(d) When zero HIGH remain: flip front-matter `status: HIGH-findings-closed`; update `### HIGH findings` to `### HIGH findings — CLOSED (zero open)`; move closed findings to `### Closed findings — history` with closure iteration.

(e) MEDIUMs that need broader scope are deferred to v3.1 with explicit row `**deferred-to:** v3.1` (per CONTEXT no-scope-creep carveout). LOWs may be left open and noted as nice-to-haves.

If the agents are unavailable, fall back to heuristic audit using mocks + tokens.css as baseline; document fallback in iteration row (`agent=manual (agent unavailable)`).
  </action>
  <verify>
    <automated>grep -qE "^status: HIGH-findings-closed" .planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md && grep -qE "^status: HIGH-findings-closed" .planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md && grep -cE "iteration=" .planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md | awk '{ if ($1 < 3) exit 1 }' && grep -cE "iteration=" .planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md | awk '{ if ($1 < 3) exit 1 }' && grep -qE "step1-permissions|step2-output-device" .planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md && echo OK</automated>
  </verify>
  <done>Both UI-REVIEW files carry front-matter `status: HIGH-findings-closed`; wizard log has ≥3 iteration markers (seed + ≥2 closure passes); calibration log has ≥3 iteration markers; closed findings moved to history; remediation source edits live in `tauri/ui/src/wizard/` + `tauri/ui/src/settings/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit agents → wizard source | gsd-ui-checker + gsd-ui-auditor drive source edits in the onboarding path; mis-applied closures could regress consent UX or perm-grant affordances |
| consent toggle copy | Dark-pattern-adjacent surfaces (telemetry, profile consent) — audit must flag ambiguous defaults explicitly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03-01 | Tampering | Consent button copy | mitigate | Audit-loop HIGH findings explicitly include opt-in/opt-out clarity; closure requires unambiguous default reading in step-telemetry-consent.ts + step-profile-consent.ts |
| T-43-03-02 | Information Disclosure | Permission denial affordance | mitigate | HIGH finding pinned in calibration audit; closure includes the System-Settings deep-link affordance so user is never stranded |
| T-43-03-03 | Repudiation | Audit log iteration trail | mitigate | Each iteration row names files_changed; closures verifiable via `git log -p tauri/ui/src/wizard/` |
</threat_model>

<verification>
- Hover-glow applied: `grep -RcE '\-\-glow-faint' tauri/ui/src/wizard/ tauri/ui/src/settings/ | awk -F: '{s+=$2} END{ if (s < 6) exit 1 }'`
- UI-REVIEW-wizard.md + UI-REVIEW-calibration.md both at status `HIGH-findings-closed`
- ≥3 iteration markers per audit (seed + closure passes)
- Token-only contract holds (no hex literals in code strings under wizard/ + settings/)
- Existing Vitest token specs stay green: `cd tauri/ui && npx vitest run tests/wizard tests/settings --reporter=min`
</verification>

<success_criteria>
- VIS-01 closure complete for wizard + calibration surfaces (combined with Plan 43-02 = all 4 Tier-1 surfaces closed)
- VIS-02 hover-glow sweep applied across wizard (6 steps + flow + router) + settings drawer
- Both UI-REVIEW files closed with ≥3 iteration markers
- Token-only contract preserved (wizard.tokens.test.ts + settings.tokens.test.ts still green)
- No regression in existing wizard onboarding flow tests
</success_criteria>

<output>
After completion, create `.planning/phases/43-visual-ship-lock/43-03-SUMMARY.md` using the standard summary template.
</output>
