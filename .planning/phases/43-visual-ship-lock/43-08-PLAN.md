---
phase: 43-visual-ship-lock
plan: 08
title: "Hero demo storyboard v5 re-mock — 8-cut shot list + chip overlay frames"
type: execute
wave: 2
depends_on: [43-07]
files_modified:
  - mocks/vibemix-cinematic-storyboard.html
  - scripts/launch/check_cut_count.py
  - tests/launch/test_cut_count.py
autonomous: true
requirements: [VIS-08]
req_ids: [VIS-08]
must_haves:
  truths:
    - "`mocks/vibemix-cinematic-storyboard.html` carries exactly 8 cut frames marked via `<section data-cut=\"N\">` or `<article class=\"frame\" data-cut=\"N\">` (N = 1..8); existing frame sections retrofitted with the `data-cut` attribute"
    - "The 8 cuts match CONTEXT §VIS-08 sequence exactly: (1) cold open — DJ hands on FLX4 + dim room; (2) vibemix wizard Welcome frame 1s hold; (3) Calibration screen with live audio meter rising; (4) Live session — mascot overlay subtle reaction; (5) AI line caption pop 'nice kick swap @ 2:33'; (6) EvidenceRegistry chip strip render (anti-slop receipts); (7) Mascot Hype-man celebrate animation; (8) End card — vibemix logo + altidus.world/vibemix + 'open-source'"
    - "Each cut frame has the CDJ Whisper v5 UI chip overlay inline-rendered: vibemix wizard, calibration meter, session UI, AI caption pop, EvidenceRegistry chip strip — re-mocked from prior Workbench/DSEG7-styled drafts to Saira + amber-on-warm-black"
    - "`scripts/launch/check_cut_count.py` parses the storyboard and asserts exactly 8 `data-cut` elements (the ≤8 cuts hard gate per CONTEXT §VIS-08)"
    - "`tests/launch/test_cut_count.py` invokes the checker and asserts pass"
    - "Storyboard palette gate from Plan 43-07 still passes (no new palette drift introduced by the re-mock)"
    - "End card carries the open-source CTA copy: 'open-source · MIT · github.com/bravoh/vibemix' per CONTEXT specifics (star-goal funnel anchor)"
  artifacts:
    - path: "scripts/launch/check_cut_count.py"
      provides: "Parses the storyboard HTML and asserts exactly 8 `data-cut` elements (≤8 hard gate)"
      exports: ["main", "count_cuts", "MAX_CUTS"]
    - path: "tests/launch/test_cut_count.py"
      provides: "Pytest sanity invoking check_cut_count against the storyboard"
  key_links:
    - from: "mocks/vibemix-cinematic-storyboard.html (cut N=2 wizard, N=3 calibration, N=4 session, N=5 AI caption, N=6 EvidenceRegistry)"
      to: "mocks/vibemix-app-ui.html + mocks/vibemix-direction-final.html"
      via: "UI chip overlays inline-render the v5 surfaces per the locked baselines"
      pattern: "data-cut"
    - from: "scripts/launch/check_cut_count.py"
      to: "mocks/vibemix-cinematic-storyboard.html"
      via: "HTML parse → count of `data-cut=` attributes"
      pattern: "data-cut="
---

<objective>
Re-mock the hero demo storyboard to CDJ Whisper v5 (UI chip overlay frames inline-rendered from the locked baselines) and finalize the 8-cut shot list per CONTEXT §VIS-08. Add the ≤8 cuts hard gate via `scripts/launch/check_cut_count.py`. Depends on 43-07 (which migrated fonts + vocab + palette) — this plan does the frame-content + structure work on top of the cleaned baseline.

Purpose: per CONTEXT §VIS-08 — the storyboard is the cut script Francesco hands to capture day post-Phase 43. Frames must reflect the final shipped UI; chip overlays must be CDJ Whisper v5 (not the legacy Workbench/DSEG7 aesthetic — Plan 43-07 cleaned that). The 8-cut count is a hard ship gate enforced via the new `check_cut_count.py`.

Output:
- Storyboard with exactly 8 `data-cut` frames in the CONTEXT-specified sequence
- UI chip overlays in cuts 2-6 re-mocked to CDJ Whisper v5
- End-card carries the open-source CTA copy
- `check_cut_count.py` script + pytest sanity
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-visual-ship-lock/43-CONTEXT.md
@.planning/phases/43-visual-ship-lock/43-07-SUMMARY.md

@mocks/vibemix-cinematic-storyboard.html
@mocks/vibemix-direction-final.html
@mocks/vibemix-app-ui.html
@mocks/vibemix-settings-drawer.html
@scripts/launch/check_storyboard_palette.py

<interfaces>
<!-- Repo facts the executor needs. -->

CONTEXT §VIS-08 8-cut shot list (verbatim):
1. Cold open — DJ hands on FLX4 + dim room
2. vibemix wizard "Welcome" frame (1 sec hold)
3. Calibration screen with live audio meter rising
4. Live session — mascot overlay subtle reaction
5. AI line caption pop ("nice kick swap @ 2:33")
6. EvidenceRegistry chip strip render (anti-slop receipts)
7. Mascot Hype-man celebrate animation (mid-track moment)
8. End card — vibemix logo + altidus.world/vibemix + "open-source"

Current storyboard structure (verified):
- 9 `<article class="frame">` blocks exist (lines ~676, ~747, ~828, ~900, ~971, ~1067, ~1146, ~1228, ~1293, ~1361). Per CONTEXT this is 1 over the gate — must consolidate to exactly 8 OR mark exactly 8 as cuts via `data-cut` while leaving any extra frames as non-cut B-roll references.
- The simplest path: add `data-cut="N"` to the 8 frames that match the CONTEXT sequence; for any extra `frame` block, either:
  (a) Remove the extra frame entirely (preferred — CONTEXT says ≤8 hard gate), OR
  (b) Strip its `class="frame"` and re-mark it as `class="bsv-extra"` so the cut counter ignores it (escape hatch only).
- The `<section class="cutsheet">` starts at line ~1425; review its content too if it duplicates the cut list — keep it consistent with the new 8-cut sequence.

UI chip overlays (cuts 2-6):
- Cut 2 (wizard Welcome): inline-render the CDJ Whisper wizard step0-intro UI as a chip overlay. Reference `tauri/ui/src/wizard/step0-intro.ts` for the actual content; build a minimal SVG/HTML mockup inline in the storyboard. Saira typography, amber accent, warm-black panel.
- Cut 3 (calibration meter): inline-render the LED meter (16-segment, amber zone bands, peak hold) from Plan 43-04 visual. SVG-only is fine — capture the LED-strip aesthetic.
- Cut 4 (session UI + mascot subtle reaction): inline-render the session UI shape from `mocks/vibemix-app-ui.html`. Show the mascot canvas + meter + status bar.
- Cut 5 (AI caption pop): inline-render the caption bubble. CONTEXT specifies copy: "nice kick swap @ 2:33". Saira italic 500. Amber tint.
- Cut 6 (EvidenceRegistry chip strip): inline-render 2-3 evidence chips (e.g. `[kick swap @ 2:33]`, `[layer drop @ 4:50]`). Anti-slop receipt vocabulary. Reference `tauri/ui/src/debrief/` for the evidence chip pattern if it exists.

Cut 8 (end card):
- vibemix wordmark in Saira (per Plan 43-07 cleanup)
- altidus.world/vibemix URL beneath
- "open-source · MIT · github.com/bravoh/vibemix" copy (per CONTEXT specifics — star-goal funnel anchor)
- Caveat handwriting "made by bravoh · with love" optional (already exists in current storyboard)

Cuts 1, 7 are "live capture" frames — no UI chip overlay needed; just the directorial description + reference photo SVG (already in storyboard from earlier drafts).

`data-cut` convention (CONTEXT §VIS-08 hard gate uses):
- Mark the 8 cut frames via `<article class="frame" data-cut="1">` through `data-cut="8"` (extend the existing class with the attribute).
- The check script counts the elements matching `[data-cut]` attribute presence; any element with `data-cut` counts as a cut. If there are 9 elements with `data-cut`, fail; if 7, fail. Exactly 8 = pass.

Palette gate from Plan 43-07:
- `scripts/launch/check_storyboard_palette.py` must still exit 0 after re-mock. New SVG / chip overlays in cuts 2-6 must use the CDJ Whisper palette (no new hex literals introduced).

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut counter script + pytest sanity</name>
  <files>scripts/launch/check_cut_count.py, tests/launch/test_cut_count.py</files>
  <behavior>
    - Test 1: `from scripts.launch.check_cut_count import MAX_CUTS, count_cuts, main` imports without error.
    - Test 2: `MAX_CUTS == 8`.
    - Test 3: `count_cuts(html_text)` returns 0 for HTML with no `data-cut` attributes; returns N for HTML with N `data-cut="..."` attributes.
    - Test 4: `main([])` against the (cleaned) storyboard returns exit 0 after Plan 43-08 Task 2 lands (the 8 cuts are marked); pre-Task-2 it may return non-zero — that's expected.
    - Test 5: A tmp HTML with 9 `data-cut` attributes fails with non-zero exit + stderr "FAIL: 9 cuts (max 8)".
    - Test 6: A tmp HTML with 7 `data-cut` attributes fails with non-zero exit + stderr "FAIL: 7 cuts (need 8)".
  </behavior>
  <action>
Per D-VIS-08 — ship the cut count gate first, then re-mock the storyboard to satisfy it.

Create `scripts/launch/check_cut_count.py`:
```python
"""Cut counter for hero demo storyboard.

VIS-08 (Phase 43, Plan 43-08): hard gate enforcing exactly 8 cuts in
mocks/vibemix-cinematic-storyboard.html (per CONTEXT §VIS-08 ≤8 cuts
gate). Counts `data-cut="N"` attribute occurrences.

Exit 0 = exactly MAX_CUTS cuts; non-zero = drift.
"""
from __future__ import annotations
import argparse, re, sys
from pathlib import Path

MAX_CUTS: int = 8
_CUT_RE = re.compile(r'\bdata-cut\s*=\s*["\']?([^"\'\s>]+)', re.IGNORECASE)


def count_cuts(html_text: str) -> int:
    return len(_CUT_RE.findall(html_text))


def main(argv: list[str] | None = None) -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--file", type=Path,
                        default=Path("mocks/vibemix-cinematic-storyboard.html"))
    args = parser.parse_args(argv)
    if not args.file.exists():
        print(f"ERROR: file not found: {args.file}", file=sys.stderr)
        return 1
    n = count_cuts(args.file.read_text(encoding="utf-8"))
    if n > MAX_CUTS:
        print(f"FAIL: {n} cuts (max {MAX_CUTS})", file=sys.stderr)
        return 2
    if n < MAX_CUTS:
        print(f"FAIL: {n} cuts (need {MAX_CUTS})", file=sys.stderr)
        return 3
    print(f"PASS: {n} cuts in {args.file}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

Create `tests/launch/test_cut_count.py` covering the 6 behaviors above. Test 4 (real storyboard pass) — use `pytest.mark.xfail` or skip if the storyboard hasn't been re-mocked yet; flip to assert-pass after Task 2 lands. Simpler: just write the test to assert exit 0 — it will fail until Task 2 closes, then go green. Order-dependent within this plan, but the failure points to the obvious next step.
  </action>
  <verify>
    <automated>uv run pytest tests/launch/test_cut_count.py -q --no-header 2>&1 | tail -10</automated>
  </verify>
  <done>Cut counter script ships + pytest tests 1-3, 5-6 pass; test 4 may fail pre-Task-2 (acceptable; Task 2 closure makes it green); the script enforces exactly 8.</done>
</task>

<task type="auto">
  <name>Task 2: Mark 8 cuts via data-cut attribute; consolidate to exactly 8 frames; re-mock UI chip overlays in cuts 2-6</name>
  <files>mocks/vibemix-cinematic-storyboard.html</files>
  <action>
Per D-VIS-08 — restructure the storyboard to carry exactly 8 cuts in the CONTEXT-specified sequence, with UI chip overlays in cuts 2-6 re-mocked to CDJ Whisper v5.

(a) Read the full file to inventory existing `<article class="frame">` blocks (verified earlier: 9 frames at lines 676, 747, 828, 900, 971, 1067, 1146, 1228, 1293, 1361 — let me re-count and confirm during execution).

(b) Map each existing frame to a CONTEXT cut number. The 8 CONTEXT cuts are:
1. Cold open — DJ hands on FLX4 + dim room
2. vibemix wizard "Welcome" frame (1 sec hold)
3. Calibration screen with live audio meter rising
4. Live session — mascot overlay subtle reaction
5. AI line caption pop ("nice kick swap @ 2:33")
6. EvidenceRegistry chip strip render (anti-slop receipts)
7. Mascot Hype-man celebrate animation (mid-track moment)
8. End card — vibemix logo + altidus.world/vibemix + "open-source"

(c) For each frame:
- If it maps to a CONTEXT cut, add `data-cut="N"` attribute (N = 1..8).
- If it's an extra/B-roll frame that doesn't map: remove it OR strip `class="frame"` and re-tag as `class="frame frame-bsv"` so it's visually present but doesn't count.

(d) Re-mock UI chip overlays in cuts 2-6. The existing storyboard has stage-direction text + sometimes SVG illustrations; replace any old UI chip rendering with new inline mockups:

- **Cut 2 (wizard Welcome):** add an inline HTML/SVG chip showing the wizard welcome step. Saira typography. Amber accent. Warm-black panel. ~200×120 box. Copy: "Welcome to vibemix" + "Set up in 60 seconds" + Continue button. Reference: `tauri/ui/src/wizard/step0-intro.ts` for actual content shape.

- **Cut 3 (calibration meter):** add an inline SVG of the 16-segment LED meter from Plan 43-04 — bottom 5 amber-pale (safe), middle 8 amber (warm), top 3 amber-deep (clip). Peak-hold lozenge at ~70%. Caption "calibrating · listening to your master out".

- **Cut 4 (live session):** add an inline HTML mockup of the session UI shape (titlebar + 3 meters + mascot canvas placeholder + status bar). Match `mocks/vibemix-app-ui.html` layout. Mascot canvas position bottom-right ~100×100.

- **Cut 5 (AI caption pop):** add a chip showing the caption bubble. Copy verbatim: "nice kick swap @ 2:33" (per CONTEXT). Italic Saira 500. Amber tint. Floating above the session UI mock at top-center.

- **Cut 6 (EvidenceRegistry chip strip):** add an inline strip of 3 evidence chips:
  - `[kick swap @ 2:33]`
  - `[layer drop @ 4:50]`
  - `[bpm shift @ 6:00]`
  - Saira Mono / Geist Mono 12px. Amber border. Warm-black background. Caption above: "anti-slop receipts — every AI line tied to a real event".

(e) Cut 8 (end card) — verify it has:
- vibemix wordmark in Saira amber (already done in Plan 43-07's SVG update)
- "altidus.world/vibemix" URL beneath in Geist Mono
- "open-source · MIT · github.com/bravoh/vibemix" copy beneath the URL in Geist Mono 11px
- Existing "made by bravoh · with love" Caveat handwriting (preserve)

(f) Verify the `<section class="cutsheet">` at the bottom still summarizes the 8 cuts correctly. Update the cutsheet text rows to match the new 8-cut sequence if drift exists.

(g) Run the cut counter: `uv run python scripts/launch/check_cut_count.py` — must exit 0.

(h) Run the palette checker from Plan 43-07: `uv run python scripts/launch/check_storyboard_palette.py` — must still exit 0. If new chip overlays introduce a new hex color, either replace it with an existing token-equivalent OR add it to `ALLOWED_PALETTE` in `check_storyboard_palette.py` with a comment justifying the addition.

Constraints:
- DO NOT add additional cinematic frames (cuts) beyond 8.
- DO NOT change cut sequence — CONTEXT specifies the order.
- DO NOT re-introduce Workbench / DSEG7 / cyan-teal anywhere.
- Preserve the existing brief / intent narrative at the top of the storyboard.
  </action>
  <verify>
    <automated>uv run python scripts/launch/check_cut_count.py && uv run python scripts/launch/check_storyboard_palette.py && grep -cE 'data-cut="[1-8]"' mocks/vibemix-cinematic-storyboard.html | awk '{ if ($1 != 8) exit 1 }' && grep -qE "nice kick swap @ 2:33" mocks/vibemix-cinematic-storyboard.html && grep -qE "open-source.*MIT.*github\\.com/bravoh/vibemix" mocks/vibemix-cinematic-storyboard.html && echo OK</automated>
  </verify>
  <done>Storyboard has exactly 8 `data-cut="N"` attributes (N=1..8); CONTEXT cut sequence matches; UI chip overlays in cuts 2-6 rendered with CDJ Whisper v5 vocab; end-card carries open-source CTA copy; both check_cut_count.py and check_storyboard_palette.py exit 0; pytest test 4 (storyboard pass) now green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storyboard → Francesco capture day | The cut sequence + UI chip overlays drive the actual shoot; drift here means wrong-UI footage |
| 8-cut gate → ship-cut | The gate is mandatory in CI; a 9th cut would silently bloat the demo length |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08-01 | Tampering | Cut count drift (>8 or <8) | mitigate | `check_cut_count.py` hard gate (exact match); pytest test 5+6 catch over/under |
| T-43-08-02 | Tampering | UI chip overlay palette regression | mitigate | `check_storyboard_palette.py` from Plan 43-07 re-invoked as part of Task 2 verify |
| T-43-08-03 | Repudiation | Cut sequence mismatch with CONTEXT | mitigate | `data-cut="N"` attribute encoded in DOM; manual cross-check at audit time straightforward |
| T-43-08-04 | Information Disclosure | github.com/bravoh/vibemix URL on end-card | accept | Bravoh GH org is the planned destination per Phase 44/45 roadmap; URL is repo-public |
</threat_model>

<verification>
- Cut count gate: `uv run python scripts/launch/check_cut_count.py` → exit 0
- Palette gate still passing: `uv run python scripts/launch/check_storyboard_palette.py` → exit 0
- Pytest cut count tests pass: `uv run pytest tests/launch/test_cut_count.py -q`
- Pytest storyboard palette tests still pass: `uv run pytest tests/launch/test_storyboard_palette.py -q`
- CONTEXT cut sequence honored: `grep -E 'data-cut="[1-8]"' mocks/vibemix-cinematic-storyboard.html | wc -l` → 8
- End-card CTA present: `grep -qE "open-source.*MIT.*github\\.com/bravoh/vibemix" mocks/vibemix-cinematic-storyboard.html`
- AI caption verbatim: `grep -qE "nice kick swap @ 2:33" mocks/vibemix-cinematic-storyboard.html`
- No Workbench/DSEG7 regression: `! grep -qE "Workbench|DSEG7" mocks/vibemix-cinematic-storyboard.html`
</verification>

<success_criteria>
- Storyboard carries exactly 8 cuts in CONTEXT-specified sequence
- UI chip overlays in cuts 2-6 inline-rendered with CDJ Whisper v5 vocab + palette
- End-card carries open-source CTA copy per CONTEXT specifics
- Cut count gate + palette gate both exit 0
- Pytest sanity green
- Plan 43-09 can hand off the finalized storyboard to Francesco
</success_criteria>

<output>
After completion, create `.planning/phases/43-visual-ship-lock/43-08-SUMMARY.md` using the standard summary template.
</output>
