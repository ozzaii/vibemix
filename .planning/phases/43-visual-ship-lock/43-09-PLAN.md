---
phase: 43-visual-ship-lock
plan: 09
title: "Francesco pre-production handoff package + §VIS-09 discharge runbook"
type: execute
wave: 3
depends_on: [43-05, 43-08]
files_modified:
  - docs/launch-prep/SHOT-LIST.md
  - docs/launch-prep/AUDIO-CAPTURE.md
  - docs/launch-prep/DEMO-MODE-CONFIG.md
  - docs/launch-prep/README.md
  - src/vibemix/runtime/demo_mode.py
  - tests/runtime/test_demo_mode_sequence.py
  - KAAN-ACTION-LEGAL.md
autonomous: true
requirements: [VIS-09]
req_ids: [VIS-09]
must_haves:
  truths:
    - "`docs/launch-prep/SHOT-LIST.md` exists and documents the 8-cut sequenced shot list with per-cut timing budget + B-roll suggestions; cut sequence matches Plan 43-08's storyboard `data-cut` order exactly"
    - "`docs/launch-prep/AUDIO-CAPTURE.md` documents 3 separate recording tracks: Gemini-voice, ambient (room mic), headphone-return; clapboard sync; 48kHz 24-bit WAV; vibemix session.wav recorded alongside as canonical reference mix"
    - "`docs/launch-prep/DEMO-MODE-CONFIG.md` documents the `vibemix.demo_mode` config flag + deterministic 30-event sequence (track A → 2:33 kick swap → mascot celebrate → 4:50 layer drop → mascot teacher line → 6:00 end) + `vibemix --demo-mode reset` between takes + 1080p / 60fps / 48kHz spec"
    - "`src/vibemix/runtime/demo_mode.py` (new) implements the deterministic 30-event sequencer; exposes `load_sequence()`, `step()`, `reset()`, `DEMO_SEQUENCE` constants"
    - "`tests/runtime/test_demo_mode_sequence.py` pins the deterministic sequence: 30 events, exact timestamps, kick swap at 2:33, layer drop at 4:50, end at 6:00; `reset()` returns sequence to step 0"
    - "`KAAN-ACTION-LEGAL.md` carries `## §VIS-09 — Francesco capture day discharge` runbook in canonical §GATE-* format (why-defer / Francesco steps / verification / unblocks / sign-off)"
    - "`docs/launch-prep/README.md` is the index for the 3 docs above + cross-links the storyboard at `mocks/vibemix-cinematic-storyboard.html`"
  artifacts:
    - path: "docs/launch-prep/SHOT-LIST.md"
      provides: "8-cut sequenced shot list with timing budget + B-roll for Francesco"
      contains: "Cut 1: cold open"
    - path: "docs/launch-prep/AUDIO-CAPTURE.md"
      provides: "Audio capture plan — 3-track separation + clapboard sync + 48kHz 24-bit WAV spec"
      contains: "Gemini voice"
    - path: "docs/launch-prep/DEMO-MODE-CONFIG.md"
      provides: "Demo-mode deterministic 30-event sequence + reset workflow + AV spec"
      contains: "30-event sequence"
    - path: "src/vibemix/runtime/demo_mode.py"
      provides: "Demo-mode sequencer for repeatable take-to-take runs"
      exports: ["load_sequence", "step", "reset", "DEMO_SEQUENCE"]
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "New `## §VIS-09` section appended; Francesco capture day discharge runbook"
      contains: "## §VIS-09"
  key_links:
    - from: "docs/launch-prep/SHOT-LIST.md"
      to: "mocks/vibemix-cinematic-storyboard.html (the 8 data-cut frames)"
      via: "1-to-1 cut correspondence (cut 1 = cold open; etc.)"
      pattern: "Cut [1-8]:"
    - from: "docs/launch-prep/DEMO-MODE-CONFIG.md"
      to: "src/vibemix/runtime/demo_mode.py"
      via: "Documentation references the sequencer API"
      pattern: "DEMO_SEQUENCE|--demo-mode"
    - from: "KAAN-ACTION-LEGAL.md §VIS-09"
      to: "docs/launch-prep/*.md"
      via: "Francesco runbook references the 3 handoff docs"
      pattern: "docs/launch-prep"
---

<objective>
Ship the pre-production handoff package Francesco consumes on capture day, plus the §VIS-09 KAAN-discharge runbook in `KAAN-ACTION-LEGAL.md`. Per CONTEXT §VIS-09 — Phase 43 ships engineering (the 3 docs + demo-mode sequencer); Francesco handles capture day post-Phase 43.

Depends on 43-08 (storyboard finalized with 8 cuts) — the SHOT-LIST.md is a 1-to-1 derivation from the storyboard cut sequence; can't write it until 43-08 lands.

Output:
- `docs/launch-prep/SHOT-LIST.md` — 8-cut shot list with timing + B-roll
- `docs/launch-prep/AUDIO-CAPTURE.md` — 3-track capture plan
- `docs/launch-prep/DEMO-MODE-CONFIG.md` — deterministic sequence + reset + spec
- `docs/launch-prep/README.md` — index
- `src/vibemix/runtime/demo_mode.py` — sequencer implementation
- `tests/runtime/test_demo_mode_sequence.py` — sequencer pinning
- `KAAN-ACTION-LEGAL.md §VIS-09` — Francesco discharge runbook
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-visual-ship-lock/43-CONTEXT.md
@.planning/phases/43-visual-ship-lock/43-08-SUMMARY.md

@mocks/vibemix-cinematic-storyboard.html
@scripts/launch/check_cut_count.py
@src/vibemix/runtime/__init__.py
@KAAN-ACTION-LEGAL.md

<interfaces>
<!-- Repo facts the executor needs. -->

CONTEXT §VIS-09 deliverables (verbatim):
1. `SHOT-LIST.md` — 8-cut sequenced shot list with timing budget per cut + B-roll suggestions
2. `AUDIO-CAPTURE.md` — Gemini-voice + ambient + headphone-return as 3 separate tracks; sync via clapboard; sample rate 48kHz 24-bit WAV; record vibemix's session.wav alongside as canonical mix
3. `DEMO-MODE-CONFIG.md` — `vibemix.demo_mode` config flag seeded with deterministic 30-event sequence (track A → 2:33 kick swap → mascot celebrate → 4:50 layer drop → mascot teacher line → 6:00 end); reset between takes via `vibemix --demo-mode reset`; spec 1080p min, 60fps min, 48kHz audio

CONTEXT §VIS-09 deterministic event sequence:
- Track A starts at 0:00 (sequence step 1)
- 2:33: kick swap event (mascot celebrate trigger)
- 4:50: layer drop event (mascot teacher line trigger)
- 6:00: track ends
- Between these anchors: 25-27 filler events (controller moves, BPM shifts, micro-mood transitions) to give the demo natural texture. Total 30 events.

Existing `src/vibemix/runtime/`:
- The directory exists; current contents to be read at execution time. CONTEXT says "config flag (already partially in `src/vibemix/runtime/`)" — verify the partial implementation; this plan completes it.
- `demo_mode.py` is the new file. May extend an existing config module or run alongside.

Existing KAAN-ACTION-LEGAL.md sections (for format precedent):
- §AUDIO-05/06/07, §GATE-01/02/03/05, §VIS-04 (added by Plan 43-05)
- §VIS-09 will be appended at the tail in the same canonical format

Test environment:
- pytest under `tests/runtime/`
- existing runtime closeouts at `tests/runtime_closeouts/` (precedent)
- Demo-mode sequencer test is deterministic (no audio I/O); pure pytest

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Demo-mode sequencer implementation + pytest pinning</name>
  <files>src/vibemix/runtime/demo_mode.py, tests/runtime/test_demo_mode_sequence.py</files>
  <behavior>
    - Test 1: `from src.vibemix.runtime.demo_mode import DEMO_SEQUENCE, load_sequence, step, reset` imports without error.
    - Test 2: `len(DEMO_SEQUENCE) == 30` (30-event deterministic sequence per CONTEXT).
    - Test 3: Sequence contains an event of kind `"kick_swap"` at timestamp `153.0` seconds (2:33).
    - Test 4: Sequence contains an event of kind `"layer_drop"` at timestamp `290.0` seconds (4:50).
    - Test 5: Sequence ends at timestamp `360.0` seconds (6:00) with an event of kind `"track_end"`.
    - Test 6: Events sorted by timestamp ascending (monotonic invariant).
    - Test 7: `load_sequence()` returns a fresh iterator/state with `step_index == 0`.
    - Test 8: `step()` advances the cursor; calling `step()` 30 times exhausts the sequence; the 31st `step()` returns a sentinel (None or raises StopIteration — define + test).
    - Test 9: `reset()` returns cursor to step 0.
    - Test 10: After Track-A start (step 1), `step()` returns the kick_swap event when the cursor crosses 153s — sequencer is timestamp-driven OR step-index driven; both are acceptable; the test pins whichever the implementation chooses.
  </behavior>
  <action>
Per D-VIS-09 — implement the deterministic 30-event sequencer.

Create `src/vibemix/runtime/demo_mode.py`:
```python
"""Demo-mode deterministic event sequencer.

VIS-09 (Phase 43, Plan 43-09): seeds a fixed 30-event sequence that
plays back identically across capture-day takes. Hooks into the
existing event dispatcher when `vibemix --demo-mode` is set.

Sequence anchors (per CONTEXT §VIS-09):
  0:00   track_start (Track A)
  2:33   kick_swap (mascot celebrate trigger)
  4:50   layer_drop (mascot teacher line trigger)
  6:00   track_end

Between anchors: filler events (controller_move, bpm_shift, mood_tick)
distributed to feel natural. Total = 30 events.

Reset workflow: `vibemix --demo-mode reset` calls reset() to return
the cursor to step 0 for the next take.
"""
from __future__ import annotations
from dataclasses import dataclass, field
from typing import Optional


@dataclass(frozen=True)
class DemoEvent:
    timestamp_s: float
    kind: str  # "track_start" | "kick_swap" | "layer_drop" | "track_end"
                # | "controller_move" | "bpm_shift" | "mood_tick"
    payload: dict = field(default_factory=dict)


# 30-event deterministic sequence (per CONTEXT §VIS-09).
# Times in seconds; track plays 0:00 → 6:00.
DEMO_SEQUENCE: tuple[DemoEvent, ...] = (
    DemoEvent(0.0,   "track_start",     {"track": "A"}),
    # Pre-kick-swap fillers (0:15 → 2:30) — 8 events of natural texture
    DemoEvent(15.0,  "controller_move", {"control": "filter_hi", "delta": 0.1}),
    DemoEvent(30.0,  "bpm_shift",       {"from": 128.0, "to": 128.0}),  # nudge
    DemoEvent(45.0,  "mood_tick",       {"mood": "coach"}),
    DemoEvent(60.0,  "controller_move", {"control": "eq_mid_a", "delta": -0.05}),
    DemoEvent(75.0,  "controller_move", {"control": "fader_a",  "delta": 0.0}),
    DemoEvent(90.0,  "bpm_shift",       {"from": 128.0, "to": 128.5}),
    DemoEvent(105.0, "mood_tick",       {"mood": "coach"}),
    DemoEvent(125.0, "controller_move", {"control": "filter_lo", "delta": 0.05}),
    # Anchor: 2:33 = 153.0s
    DemoEvent(153.0, "kick_swap",       {"from": "kick_a", "to": "kick_b"}),
    # Post-kick-swap fillers (2:45 → 4:45) — 9 events
    DemoEvent(170.0, "mood_tick",       {"mood": "hype-man"}),  # ramp up
    DemoEvent(185.0, "controller_move", {"control": "fx_echo", "delta": 0.15}),
    DemoEvent(200.0, "controller_move", {"control": "eq_hi_b", "delta": 0.08}),
    DemoEvent(215.0, "bpm_shift",       {"from": 128.5, "to": 129.0}),
    DemoEvent(230.0, "controller_move", {"control": "filter_hi", "delta": -0.1}),
    DemoEvent(245.0, "mood_tick",       {"mood": "hype-man"}),
    DemoEvent(260.0, "controller_move", {"control": "cue_b", "delta": 1.0}),  # cue press
    DemoEvent(275.0, "controller_move", {"control": "fader_a", "delta": -0.2}),  # transition
    DemoEvent(285.0, "controller_move", {"control": "fader_b", "delta": 0.4}),
    # Anchor: 4:50 = 290.0s
    DemoEvent(290.0, "layer_drop",      {"layer": "synth_pad", "energy": 0.7}),
    # Post-layer-drop fillers (4:55 → 5:55) — 9 events
    DemoEvent(295.0, "mood_tick",       {"mood": "teacher"}),  # teach moment
    DemoEvent(305.0, "controller_move", {"control": "eq_lo_b", "delta": 0.1}),
    DemoEvent(315.0, "controller_move", {"control": "fx_filter", "delta": 0.2}),
    DemoEvent(325.0, "bpm_shift",       {"from": 129.0, "to": 129.0}),
    DemoEvent(335.0, "controller_move", {"control": "fader_b", "delta": 0.0}),
    DemoEvent(345.0, "mood_tick",       {"mood": "coach"}),
    DemoEvent(350.0, "controller_move", {"control": "filter_lo", "delta": -0.05}),
    DemoEvent(355.0, "controller_move", {"control": "cue_a", "delta": 1.0}),
    DemoEvent(358.0, "controller_move", {"control": "fader_b", "delta": -0.5}),
    # Anchor: 6:00 = 360.0s
    DemoEvent(360.0, "track_end",       {"track": "A"}),
)

assert len(DEMO_SEQUENCE) == 30, f"DEMO_SEQUENCE must have 30 events, got {len(DEMO_SEQUENCE)}"

# Monotonic invariant
_timestamps = [e.timestamp_s for e in DEMO_SEQUENCE]
assert _timestamps == sorted(_timestamps), "DEMO_SEQUENCE must be sorted by timestamp"


@dataclass
class DemoState:
    step_index: int = 0


_state = DemoState()


def load_sequence() -> DemoState:
    """Reset and return a fresh state object."""
    reset()
    return _state


def step() -> Optional[DemoEvent]:
    """Return the current event and advance. Returns None when exhausted."""
    if _state.step_index >= len(DEMO_SEQUENCE):
        return None
    event = DEMO_SEQUENCE[_state.step_index]
    _state.step_index += 1
    return event


def reset() -> None:
    """Return cursor to step 0 (for next take)."""
    _state.step_index = 0
```

Create `tests/runtime/test_demo_mode_sequence.py` covering the 10 behaviors. The kick_swap, layer_drop, and track_end timestamps + counts are the load-bearing pins; the filler timings are flexible (CONTEXT calls them "filler events").
  </action>
  <verify>
    <automated>uv run pytest tests/runtime/test_demo_mode_sequence.py -q --no-header 2>&1 | tail -10 | grep -E "10 passed|✓.*10" && echo OK</automated>
  </verify>
  <done>demo_mode.py exists with DEMO_SEQUENCE (30 events), load_sequence/step/reset API; 10 pytest pins green; kick_swap @ 153s, layer_drop @ 290s, track_end @ 360s anchors locked.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 3 launch-prep docs + index README</name>
  <files>docs/launch-prep/SHOT-LIST.md, docs/launch-prep/AUDIO-CAPTURE.md, docs/launch-prep/DEMO-MODE-CONFIG.md, docs/launch-prep/README.md</files>
  <action>
Per D-VIS-09 — write the 3 handoff docs Francesco consumes on capture day.

(a) `docs/launch-prep/SHOT-LIST.md`:
```markdown
# vibemix hero demo — Shot list (Phase 43 / VIS-09)

8 cuts. Total target runtime: ~30 seconds (CONTEXT — "30s hero demo").

Sourced from `mocks/vibemix-cinematic-storyboard.html` (8 `data-cut` frames).

| Cut | Description | Timing | B-roll suggestions |
|-----|-------------|--------|---------------------|
| 1 | Cold open — DJ hands on FLX4 + dim room | 0:00 – 0:03 (3s) | close-up of jog wheel; faders at neutral; dim purple-amber room light |
| 2 | vibemix wizard "Welcome" frame (hold 1s) | 0:03 – 0:05 (2s) | screen capture of wizard step 0; subtle amber accent glow on Continue button |
| 3 | Calibration screen with live audio meter rising | 0:05 – 0:09 (4s) | screen capture of LED meter climbing from safe (1-5) to warm (6-13); peak-hold lozenge floats up |
| 4 | Live session — mascot overlay subtle reaction | 0:09 – 0:14 (5s) | screen-in-screen: mascot in corner doing micro-head-turn while session UI fills frame |
| 5 | AI line caption pop — "nice kick swap @ 2:33" | 0:14 – 0:17 (3s) | caption bubble fade-in over session UI; audio overlay of Gemini voice line |
| 6 | EvidenceRegistry chip strip — anti-slop receipts | 0:17 – 0:20 (3s) | chip strip render: `[kick swap @ 2:33]` `[layer drop @ 4:50]` `[bpm shift @ 6:00]` |
| 7 | Mascot Hype-man celebrate animation (mid-track) | 0:20 – 0:25 (5s) | mascot full-frame; Pioneer-CDJ headbob (NOT generic VTuber dance per CDJ Whisper baseline) |
| 8 | End card — vibemix logo + altidus.world/vibemix + "open-source" | 0:25 – 0:30 (5s) | wordmark in Saira amber on warm-black; URL beneath in Geist Mono; "open-source · MIT · github.com/bravoh/vibemix" CTA |

**Notes for Francesco:**
- Cuts 1, 7 are real-world capture (DJ hands + booth). Cuts 2-6, 8 are screen capture composited or hybrid.
- Mascot in cut 7 must feel like a Pioneer CDJ headbob, not a VTuber dance — per CONTEXT specifics. If the take feels "slop", re-take with a more reserved Mixamo source.
- Mascot overlay in cut 4 should be SUBTLE — micro-motion, not a celebrate.

**Cross-references:**
- Storyboard mock: `mocks/vibemix-cinematic-storyboard.html`
- Visual baseline: `mocks/vibemix-direction-final.html` (CDJ Whisper locked)
- Audio capture plan: `AUDIO-CAPTURE.md`
- Demo-mode config (deterministic playback for repeatable takes): `DEMO-MODE-CONFIG.md`
```

(b) `docs/launch-prep/AUDIO-CAPTURE.md`:
```markdown
# vibemix hero demo — Audio capture plan (Phase 43 / VIS-09)

## 3 separate tracks (recorded simultaneously)

### Track 1 — Gemini voice
- Source: vibemix's `playback_queue` → speaker bus
- Capture: line-out from headphone amp, or USB recording from vibemix audio output
- Format: 48kHz 24-bit WAV mono
- Filename: `take_NN_gemini.wav`

### Track 2 — Ambient (room)
- Source: room mic (boom or shotgun, off-axis from speakers)
- Purpose: room tone + crowd-feel atmosphere
- Format: 48kHz 24-bit WAV stereo
- Filename: `take_NN_ambient.wav`

### Track 3 — Headphone return (DJ's cue)
- Source: DJ headphone monitor split via Y-cable or audio interface
- Purpose: shows what the DJ is hearing in cue (often the next track)
- Format: 48kHz 24-bit WAV stereo
- Filename: `take_NN_headphones.wav`

## Sync

- **Clapboard** at the head of every take (visual + audio clap, single slate).
- All 4 sources (3 mics + vibemix session.wav) align via clapboard transient.
- vibemix `session.wav` is recorded by vibemix itself (Phase 28 recording loop) as the canonical reference mix — always saved to `~/Library/Application Support/vibemix/recordings/<session-id>/session.wav` on macOS.

## AV spec (CONTEXT §VIS-09 minimums)

| Spec | Minimum | Recommended |
|------|---------|-------------|
| Video resolution | 1080p (1920×1080) | 4K (3840×2160) downsampled in post |
| Frame rate | 60fps | 60fps (don't go higher; matches mascot animation rate) |
| Audio sample rate | 48kHz | 48kHz (matches vibemix's session.wav) |
| Audio bit depth | 24-bit | 24-bit |
| Codec | ProRes 422 or H.264 high | ProRes 422 |

## Take workflow

1. Reset vibemix demo-mode: `vibemix --demo-mode reset` (resets the deterministic sequence to step 0).
2. Slate the take (clapboard).
3. Start all 3 audio tracks + video roll.
4. Trigger demo-mode start in vibemix (the deterministic 30-event sequence plays out over 6:00 — see `DEMO-MODE-CONFIG.md`).
5. Capture cuts 1, 4, 7 (real-world) during the 6-minute demo-mode playback.
6. End take. Stop all recorders.

## Cross-references
- Shot list: `SHOT-LIST.md`
- Demo-mode config: `DEMO-MODE-CONFIG.md`
```

(c) `docs/launch-prep/DEMO-MODE-CONFIG.md`:
```markdown
# vibemix hero demo — Demo-mode config (Phase 43 / VIS-09)

## Purpose

`vibemix.demo_mode` replaces the live audio + MIDI input pipeline with a deterministic 30-event sequence so capture-day takes are repeatable across attempts. The mascot reacts, the AI lines fire, the EvidenceRegistry chips render — all at the same timestamps every take.

## CLI

```bash
# Reset sequence to step 0 (call between takes)
vibemix --demo-mode reset

# Start vibemix with demo-mode active (replaces live input)
vibemix --demo-mode start

# Stop demo-mode
vibemix --demo-mode stop
```

## Deterministic 30-event sequence

Defined in `src/vibemix/runtime/demo_mode.py` as `DEMO_SEQUENCE`. Total runtime 0:00 → 6:00 (360s).

### Anchor events (load-bearing for the demo film)

| Time | Event | Mascot reaction |
|------|-------|-----------------|
| 0:00 | track_start (Track A) | mascot enters idle pose |
| 2:33 | **kick_swap** | mascot celebrate trigger (Hype-man) |
| 4:50 | **layer_drop** | mascot teacher line trigger (Teacher mood) |
| 6:00 | track_end | mascot fade to idle |

Between anchors: 25-27 filler events (controller_move, bpm_shift, mood_tick) for natural texture.

## AV spec

- **Video:** 1080p+ resolution, 60fps+ frame rate
- **Audio:** 48kHz sample rate (matches vibemix session.wav)

## Take repeatability

Because the sequence is deterministic + the demo-mode resets cleanly, Francesco can:
1. Run take 1; review playback
2. `vibemix --demo-mode reset`
3. Run take 2 with identical timing; pick the better visual

## Cross-references
- Sequencer source: `src/vibemix/runtime/demo_mode.py`
- Shot list: `SHOT-LIST.md`
- Audio capture plan: `AUDIO-CAPTURE.md`
```

(d) `docs/launch-prep/README.md` — index:
```markdown
# vibemix launch-prep — Hero demo handoff package

Phase 43 / VIS-09 — engineering-side deliverables for Francesco's capture day.

## Documents

| Doc | Purpose |
|-----|---------|
| [`SHOT-LIST.md`](./SHOT-LIST.md) | 8-cut sequenced shot list with timing + B-roll suggestions |
| [`AUDIO-CAPTURE.md`](./AUDIO-CAPTURE.md) | 3-track capture plan (Gemini-voice + ambient + headphone-return) + clapboard sync + AV spec |
| [`DEMO-MODE-CONFIG.md`](./DEMO-MODE-CONFIG.md) | Demo-mode deterministic 30-event sequence + reset workflow |

## Cross-references

- Storyboard mock: `mocks/vibemix-cinematic-storyboard.html` (8 `data-cut` frames matching the shot list)
- Visual direction baseline: `mocks/vibemix-direction-final.html` (CDJ Whisper locked)
- Demo-mode sequencer: `src/vibemix/runtime/demo_mode.py`
- Francesco discharge runbook: `KAAN-ACTION-LEGAL.md §VIS-09`

## Handoff status

- [x] Storyboard locked (Plan 43-08)
- [x] Shot list written (this plan, Task 2)
- [x] Audio capture plan written (this plan, Task 2)
- [x] Demo-mode config + sequencer written (this plan, Tasks 1 + 2)
- [ ] Francesco capture day (post-Phase 43; tracked in `KAAN-ACTION-LEGAL.md §VIS-09`)
```
  </action>
  <verify>
    <automated>test -f docs/launch-prep/SHOT-LIST.md && test -f docs/launch-prep/AUDIO-CAPTURE.md && test -f docs/launch-prep/DEMO-MODE-CONFIG.md && test -f docs/launch-prep/README.md && grep -qE "Cut 1:|^\\| 1 \\|" docs/launch-prep/SHOT-LIST.md && grep -qE "48kHz" docs/launch-prep/AUDIO-CAPTURE.md && grep -qE "kick_swap|2:33" docs/launch-prep/DEMO-MODE-CONFIG.md && grep -qE "Pioneer.CDJ headbob" docs/launch-prep/SHOT-LIST.md && echo OK</automated>
  </verify>
  <done>4 launch-prep docs exist; SHOT-LIST has 8 cuts with timing + Pioneer-CDJ-headbob aesthetic constraint surfaced; AUDIO-CAPTURE has 3-track + 48kHz spec; DEMO-MODE-CONFIG references the kick_swap @ 2:33 anchor; README indexes all three.</done>
</task>

<task type="auto">
  <name>Task 3: Append §VIS-09 Francesco discharge runbook to KAAN-ACTION-LEGAL.md</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <action>
Per D-VIS-09 — append the §VIS-09 runbook in canonical format (matches §GATE-05 / §VIS-04 precedent).

Append at the file tail:

```markdown
---

## §VIS-09 — Francesco capture day discharge

**REQ-ID:** VIS-09 (Phase 43-09)
**Owner:** Francesco (capture day) + Kaan (verification of footage)
**Status:** ☐ pre-discharge (Plan 43-09 ships handoff package + demo-mode sequencer)  ☐ pre-production review with Francesco  ☐ capture day complete  ☐ footage reviewed by Kaan  ☐ final cut signed off
**Effort:** ~1 day pre-production review + ~1 day shoot + ~2 days edit
**Blocking for:** Phase 44 (Launch Pre-stage) README hero artefact (demo.mp4 references) + Phase 45 (External Discharge) social-publish demo video.

### Why this is Francesco-discharge

vibemix is a real-world product — the hero demo requires real DJ hands on a real Pioneer DDJ-FLX4 in a real room with real lighting. None of this can be automated. Francesco owns capture day as cofounder per CONTEXT — engineering ships the deterministic playback environment (demo-mode + storyboard + shot list + audio capture plan); Francesco brings the booth, the DJ ear, and the cinema craft.

Per CONTEXT specifics: "Mascot in cut 7 should feel like a Pioneer CDJ headbob, NOT a generic VTuber dance" — Francesco's qualitative judgment closes that aesthetic gate.

### Files involved

- **Shot list:** `docs/launch-prep/SHOT-LIST.md` (Plan 43-09 Task 2)
- **Audio capture plan:** `docs/launch-prep/AUDIO-CAPTURE.md` (Plan 43-09 Task 2)
- **Demo-mode config:** `docs/launch-prep/DEMO-MODE-CONFIG.md` (Plan 43-09 Task 2)
- **Demo-mode sequencer:** `src/vibemix/runtime/demo_mode.py` (Plan 43-09 Task 1)
- **Storyboard:** `mocks/vibemix-cinematic-storyboard.html` (Plan 43-08)
- **Output location:** `docs/launch-prep/takes/take_NN/` (gitignored; final cut goes to Phase 44's `demo.mp4` reference)

### Francesco steps

1. **Pre-production review (1 day before shoot):**
   - Walk the shot list (`SHOT-LIST.md`) with Kaan; confirm cut sequence + B-roll feasibility.
   - Confirm audio capture chain (3-track plan in `AUDIO-CAPTURE.md`).
   - Test-run vibemix demo-mode locally: `vibemix --demo-mode start` plays the 30-event sequence end-to-end.
2. **Capture day prep:**
   - Set up booth with Pioneer DDJ-FLX4 + headphones + ambient mic + cameras (1080p+ 60fps+).
   - Mount vibemix demo-mode display visible to camera 1 (screen-cap or external monitor).
3. **Per take:**
   - `vibemix --demo-mode reset` before each take.
   - Slate (clapboard) for sync.
   - Roll all recorders.
   - Run demo-mode start; capture the 30 events across 6 minutes.
   - End take; review playback.
4. **Pickup B-roll:**
   - Cut 1 (DJ hands on FLX4 in dim room) — separate capture; ≥ 5 angles
   - Cut 7 (mascot celebrate full-frame) — screen capture during demo-mode (mascot animation at 2:33 anchor when kick_swap fires)
5. **Edit:**
   - Sync 4 audio sources via clapboard (3 mics + vibemix session.wav).
   - Cut to 8 cuts per `SHOT-LIST.md` timing budget.
   - Final master at 1080p+ 60fps+ 48kHz; export `demo.mp4` for Phase 44 README hero.

### Verification

```bash
# Pre-shoot: demo-mode sequencer integrity
uv run pytest tests/runtime/test_demo_mode_sequence.py -q

# Pre-shoot: shot list ↔ storyboard cut count parity
uv run python scripts/launch/check_cut_count.py    # exit 0
grep -cE "^\\| [1-8] \\|" docs/launch-prep/SHOT-LIST.md    # 8

# Post-shoot: footage spec check (Kaan reviews)
ffprobe take_01_master.mp4 | grep -E "1920x1080|3840x2160|fps=60|48000 Hz"
```

### What unblocks

- Phase 44 README hero artefact — `demo.mp4` referenced in the README is the output of this capture day.
- Phase 45 social-publish — Instagram/Twitter/TikTok cuts derive from the master.

### Sign-off block

```
VIS-09 PRE-PROD REVIEW on:     _____________________   (date — with Kaan + Francesco)
VIS-09 CAPTURE DAY on:          _____________________   (date)
VIS-09 TAKES SHOT:              _____________________   (count)
VIS-09 AV SPEC HELD:            _____________________   (1080p+ / 60fps+ / 48kHz — yes / no)
VIS-09 PIONEER-HEADBOB FEEL OK: _____________________   (yes / no — Kaan judgment on mascot cut 7)
VIS-09 FINAL CUT SIGNED:        _____________________   (date)
Sign-off by (Francesco):        _____________________
Sign-off by (Kaan):             _____________________
```
```

Read the file tail to find the insertion point (after §VIS-04 added by Plan 43-05). Append after the last existing `## §` section.
  </action>
  <verify>
    <automated>grep -qE "^## §VIS-09 " KAAN-ACTION-LEGAL.md && grep -qE "Francesco" KAAN-ACTION-LEGAL.md && grep -qE "Pioneer.CDJ headbob" KAAN-ACTION-LEGAL.md && grep -qE "docs/launch-prep" KAAN-ACTION-LEGAL.md && grep -qE "src/vibemix/runtime/demo_mode.py" KAAN-ACTION-LEGAL.md && echo OK</automated>
  </verify>
  <done>§VIS-09 section appended with canonical block (why-defer / Francesco steps / verification / unblocks / sign-off); cites docs/launch-prep/*.md + demo_mode.py + Pioneer-CDJ-headbob aesthetic gate; sign-off block has dual signatures (Francesco + Kaan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo-mode sequence → live UI | Demo-mode replaces real audio input; if accidentally left on in production, user gets fake events instead of real reactions |
| capture-day audio chain → final cut | 3-track separation depends on Francesco's audio interface routing; misroute = unrecoverable in post |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-09-01 | Tampering | DEMO_SEQUENCE timestamps | mitigate | 10 pytest pins lock the anchor events (kick_swap @ 153s, layer_drop @ 290s, track_end @ 360s); any reorder fails CI |
| T-43-09-02 | Denial of Service | Demo-mode accidentally enabled in shipped binary | mitigate | CLI requires explicit `--demo-mode` flag; default behavior is live input; document the flag clearly in DEMO-MODE-CONFIG.md |
| T-43-09-03 | Information Disclosure | Capture day footage | accept | Kaan + Francesco review; no PII or sensitive content in demo-mode (Track A is synthetic; no third-party copyright) |
| T-43-09-04 | Repudiation | Take provenance + spec compliance | mitigate | §VIS-09 sign-off block requires Francesco + Kaan dual signature; AV-spec check via ffprobe in verification |
</threat_model>

<verification>
- Demo-mode sequencer tests green: `uv run pytest tests/runtime/test_demo_mode_sequence.py -q`
- Shot list ↔ storyboard parity: `grep -cE "^\\| [1-8] \\|" docs/launch-prep/SHOT-LIST.md` returns ≥8
- Audio capture plan documents 3 tracks + 48kHz: `grep -qE "Gemini voice|ambient|headphone" docs/launch-prep/AUDIO-CAPTURE.md`
- Demo-mode config references the kick_swap @ 2:33 anchor + AV spec
- README indexes all 3 docs + cross-refs the storyboard + demo_mode.py + KAAN-ACTION-LEGAL.md §VIS-09
- §VIS-09 section in KAAN-ACTION-LEGAL.md
- No regression: `uv run pytest tests/runtime/ -q` stays green
</verification>

<success_criteria>
- VIS-09 deliverables shipped: SHOT-LIST.md + AUDIO-CAPTURE.md + DEMO-MODE-CONFIG.md + README.md
- Demo-mode sequencer with 30-event deterministic sequence (kick_swap @ 2:33, layer_drop @ 4:50, track_end @ 6:00)
- 10 pytest pins green
- §VIS-09 Francesco discharge runbook in KAAN-ACTION-LEGAL.md (canonical format)
- Cross-references intact: docs ↔ storyboard ↔ sequencer ↔ runbook
- No regression in runtime tests
</success_criteria>

<output>
After completion, create `.planning/phases/43-visual-ship-lock/43-09-SUMMARY.md` using the standard summary template.
</output>
