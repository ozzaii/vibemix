---
phase: 77-wire-connect-the-islands
plan: 03
type: execute
wave: 1
depends_on: [77-01]
files_modified:
  - src/vibemix/__main__.py
autonomous: true
requirements: [WIRE-06]

must_haves:
  truths:
    - "After _load_env_robust(), a value from .env wins over a stale shell GEMINI_API_KEY"
    - "The env-loading diagnostic prints WHICH file loaded, never the key value (no secret leak)"
    - "The docstring records the deliberate reversal of the prior override=False (Tauri-injection) decision"
  artifacts:
    - path: "src/vibemix/__main__.py"
      provides: "_load_env_robust() with override=True (both load_dotenv call sites) + updated docstring"
      contains: "override=True"
  key_links:
    - from: "src/vibemix/__main__.py::_load_env_robust"
      to: "os.environ['GEMINI_API_KEY']"
      via: "load_dotenv(override=True) — .env wins"
      pattern: "override=True"
---

<objective>
WIRE-06: fix the diagnosed live bug where a stale/ghost shell `GEMINI_API_KEY` (`...QSFyBQ`) shadows the funded `.env` key (`...32u744`). Flip the two `load_dotenv(..., override=False)` calls in `_load_env_robust()` to `override=True` so `.env` wins, and update the docstring to record this deliberate reversal of the prior Tauri-injection precedence.

Purpose: a real shipped-bug fix — without it the live runtime can use the dead ghost key even with credits in `.env`.
Output: two-line behavior flip + docstring update + a passing override test (Wave-0 stub un-xfailed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-wire-connect-the-islands/77-RESEARCH.md
@.planning/phases/77-wire-connect-the-islands/77-PATTERNS.md

<interfaces>
<!-- Extracted from live source 2026-05-26. -->
From src/vibemix/__main__.py:
- `_load_env_robust()` (:117-182). Docstring (:128-133) deliberately chose override=False so a Tauri-shell-injected process key is NOT clobbered by a stale on-disk .env.
- `load_dotenv(dotenv_path=str(cand), override=False)` at `:173` (per-candidate load).
- `load_dotenv(override=False)` at `:182` (no-.env-found fallback).
- Diagnostic at `:184+` prints WHICH file loaded, never the key value (V7 secrets — keep it that way).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip override=True + update the docstring + un-xfail the WIRE-06 test</name>
  <files>src/vibemix/__main__.py, tests/runtime/test_load_env_override.py</files>
  <read_first>
    - src/vibemix/__main__.py:117-187 (_load_env_robust full body + docstring + diagnostic print)
    - tests/runtime/test_load_env_override.py (the Wave-0 stub — un-xfail the override tier)
  </read_first>
  <behavior>
    - With os.environ["GEMINI_API_KEY"]="DECOY" set AND a .env containing GEMINI_API_KEY=REAL_FUNDED, after _load_env_robust() os.environ["GEMINI_API_KEY"] == "REAL_FUNDED".
    - The diagnostic output never contains the key VALUE substring (security pin stays green).
  </behavior>
  <action>
    In `__main__.py::_load_env_robust()`: change `load_dotenv(dotenv_path=str(cand), override=False)` (:173) → `override=True`, and `load_dotenv(override=False)` (:182) → `override=True`. Update the docstring (:128-133): replace the "earlier-loaded values win (override=False)" rationale with the WIRE-06 decision — `.env` is the source of truth for the dev/local path; flipping to `override=True` fixes the diagnosed ghost-key shadowing bug (stale shell `GEMINI_API_KEY` no longer wins). Add a KAAN-ACTION note IN THE DOCSTRING: "Reverses the prior Tauri-injection precedence (A1). If a Tauri-bundled install ever injects the key via process env instead of .env, THIS is the knob to revisit — see WIRE-06 / 77-RESEARCH Pitfall 3." Do NOT touch the diagnostic print at `:184+` — it must keep printing the FILE, never the key value. No other behavior change. Then remove the `xfail(strict=True)` marker on the override test in `tests/runtime/test_load_env_override.py` so it asserts a real pass.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/runtime/test_load_env_override.py</automated>
  </verify>
  <acceptance_criteria>
    - Both `load_dotenv` calls use `override=True`; source grep `grep -n "override=" src/vibemix/__main__.py` shows no `override=False` remaining in `_load_env_robust`.
    - Docstring updated with the reversal rationale + KAAN-ACTION knob note.
    - Override test passes (decoy → .env value wins); key-not-logged security pin still green.
    - Diagnostic print unchanged (still prints file path, never the key value).
  </acceptance_criteria>
  <done>override=True flipped, docstring records the reversal + KAAN-ACTION knob, override test green, no key leaked to logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell env / .env file → process secret | API key enters the process from two sources with a now-reversed precedence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-03-01 | Information Disclosure | API key leaked to stdout/stderr by the env diagnostic | mitigate | Diagnostic at __main__.py:184+ prints WHICH file loaded, NEVER the key value — left untouched; the Wave-0 security pin asserts the key string is absent from captured output. |
| T-77-03-02 | Tampering/Spoofing | Wrong (stale/ghost) key loaded → live runtime uses a dead key | mitigate | override=True makes the funded .env win over a ghost shell var — this is the fix. A1: if a Tauri-injected process key is ever needed, the docstring records the knob to revisit (KAAN-ACTION). |
| T-77-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/runtime/test_load_env_override.py` green.
- `grep -v '^#' src/vibemix/__main__.py | grep -c 'override=False'` returns 0 within `_load_env_robust` (manual scope check acceptable).
- Full suite stays green; no API key needed (decoy values only).
</verification>

<success_criteria>
- `.env` wins over a stale shell `GEMINI_API_KEY`.
- Docstring records the deliberate reversal + the KAAN-ACTION knob for the Tauri-injection path.
- No secret value reaches logs.
</success_criteria>

<output>
Create `.planning/phases/77-wire-connect-the-islands/77-03-SUMMARY.md` when done. Note the KAAN-ACTION flag (A1 Tauri-injection precedence reversal) for the milestone's KAAN-ACTION surface.
</output>
