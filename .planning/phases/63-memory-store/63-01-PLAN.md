---
phase: 63-memory-store
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/memory/__init__.py
  - tests/memory/test_store.py
  - tests/memory/test_store_parity.py
  - tests/memory/test_no_live_path_import.py
  - tests/memory/test_no_extraction.py
  - tests/memory/test_retention.py
autonomous: true
requirements: [STORE-01, STORE-02, STORE-03, STORE-04]

must_haves:
  truths:
    - "tests/memory/ exists as a package with the full contract suite present"
    - "All STORE-01..04 behaviors have a named test (round-trip, parity, fallback, cascade, retention, path-traversal, no-live-import, no-extraction)"
    - "Tests fail with collection/import errors (vibemix.memory does not exist yet) — pinning the contract, not green-on-empty"
  artifacts:
    - path: "tests/memory/__init__.py"
      provides: "package marker"
    - path: "tests/memory/test_store.py"
      provides: "round-trip, numpy-fallback, delete-cascade, path-traversal units"
      contains: "def test_add_query_roundtrip"
    - path: "tests/memory/test_store_parity.py"
      provides: "Mac/Win bit-identity parity gate"
      contains: "pytest.mark.parity"
    - path: "tests/memory/test_no_live_path_import.py"
      provides: "import-boundary gate (static AST + subprocess dormancy)"
      contains: "test_memory_does_not_import_live_path_statically"
    - path: "tests/memory/test_no_extraction.py"
      provides: "raw-in/raw-out gate (tokenize-stripped generation-surface scan)"
      contains: "test_memory_calls_only_embed_content"
    - path: "tests/memory/test_retention.py"
      provides: "oldest-session-first whole-session eviction"
  key_links:
    - from: "tests/memory/test_store_parity.py"
      to: "vibemix.library._cosine"
      via: "import EMBEDDING_DIM, l2_normalize, cosine_topk"
      pattern: "from vibemix.library._cosine import"
    - from: "tests/memory/test_no_live_path_import.py"
      to: "src/vibemix/memory"
      via: "AST scan of memory pkg for forbidden imports"
      pattern: "src.*vibemix.*memory"
---

<objective>
Scaffold the full Phase 63 test contract — six files under `tests/memory/` — BEFORE the `vibemix.memory` package exists. These tests pin the `MemoryStore` interface, the Mac/Win parity gate, the import-boundary invariant, and the raw-in/raw-out invariant. They are expected to FAIL on collection/import (the package they target does not exist yet); that failure is the contract being asserted, and Plans 02/03 turn them green.

Purpose: TDD-pin the storage contract so executors of Plans 02/03 build against fixed expectations (no scavenger hunt), and so the anti-slop / no-live-path invariants are enforced by tests from the first commit.
Output: `tests/memory/` package with 6 contract test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-memory-store/63-RESEARCH.md
@.planning/phases/63-memory-store/63-PATTERNS.md
@.planning/phases/63-memory-store/63-VALIDATION.md

<interfaces>
<!-- Contracts the test files pin. The MemoryStore these tests target is built in Plans 02/03. -->
<!-- Imported VERBATIM (never forked): -->
From src/vibemix/library/_cosine.py:
  EMBEDDING_DIM = 768
  def l2_normalize(vec: np.ndarray) -> np.ndarray
  def cosine_topk(query, vectors, ids, k) -> list[tuple[str, float]]   # tie-break: sim DESC, id ASC

From src/vibemix/library/index_numpy.py:
  class NumpyStore(vectors_path=..., ids_path=...)   # add_batch / load_all -> (list[str], np.ndarray) / delete / snapshot_hash / close

<!-- TARGET interface these tests pin (built in Plans 02/03): -->
  class MemoryStore(db_path: Path, prefer_sqlite_vec: bool = True)
    add_record(record_id: str, session_id: str, ts: float, kind: str, signature: str, embedding: np.ndarray) -> None
    query_topk(query_embedding: np.ndarray, k: int = 8, *, exclude_session: str | None = None) -> list[Record]
    delete_session(session_id: str) -> int
  # query_topk returns Record objects exposing .record_id, .session_id, .ts, .kind, .signature, .score
</interfaces>

<!-- Ready-to-clone test bodies live in RESEARCH §Code Examples (63-RESEARCH.md:342-443) and PATTERNS §test sections. Use them. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/memory package + the parity, round-trip, fallback, cascade, path-traversal suite</name>
  <read_first>
    - tests/library/test_store_parity.py (the parity test template + `_sqlite_vec_available()` probe + `@pytest.mark.parity` + in-test rng fixture + tie-break + float32 round-trip cases)
    - tests/library/__init__.py (package-marker analog)
    - src/vibemix/library/_cosine.py (EMBEDDING_DIM, l2_normalize, cosine_topk — the imports the tests use)
    - .planning/phases/63-memory-store/63-RESEARCH.md §Code Examples (ready clone at lines 342-380)
    - .planning/phases/63-memory-store/63-PATTERNS.md (test_store_parity / test_store sections)
  </read_first>
  <files>tests/memory/__init__.py, tests/memory/test_store_parity.py, tests/memory/test_store.py</files>
  <action>
    Create `tests/memory/__init__.py` (empty package marker, clone `tests/library/__init__.py`).

    Create `tests/memory/test_store_parity.py` as a clone of `tests/library/test_store_parity.py`: copy the `_sqlite_vec_available()` probe + `SQLITE_VEC_AVAILABLE` module flag verbatim; generate synthetic 768-dim L2-normalized float32 vectors with `np.random.default_rng(seed)` IN-TEST (do NOT add a fixture file). Write `test_memory_sqlite_vec_topk_matches_numpy` decorated `@pytest.mark.parity` + `@pytest.mark.skipif(not SQLITE_VEC_AVAILABLE, ...)`: build two `MemoryStore` instances over `tmp_path` (one `prefer_sqlite_vec=True`, one `False`), `add_record` the same 100 records to both, assert identical `record_id` rank order from `query_topk(q, k=10)`. Also clone `test_tie_break_track_id_asc` and `test_float32_round_trip_bit_identical` shapes against `MemoryStore`. Import `EMBEDDING_DIM`, `l2_normalize`, `cosine_topk` from `vibemix.library._cosine` and `MemoryStore` from `vibemix.memory.store`.

    Create `tests/memory/test_store.py` with the STORE-01/02/03 units: `test_add_query_roundtrip` (add a record, query it back, assert the raw `signature` is returned verbatim and unmodified — raw-in/raw-out, STORE-02); `test_numpy_fallback` (force `prefer_sqlite_vec=False`, assert store is fully functional, STORE-01); `test_delete_cascade` (add records across 2 sessions, `delete_session(s1)`, assert s1 records gone from both vector backend AND metadata, s2 untouched, no orphan retrievable, STORE-03); `test_session_id_path_traversal` (a crafted `session_id` containing `../` or absolute path is rejected before reaching the filesystem, STORE-03 security — mirror `recordings_index` gate behavior, expect a raise or rejection).

    Do NOT inline implementation code in the action — clone the bodies from the RESEARCH §Code Examples and PATTERNS test sections.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/memory/test_store_parity.py tests/memory/test_store.py --co 2>&1 | grep -qiE "error|ModuleNotFoundError|vibemix.memory" && echo "EXPECTED-RED (vibemix.memory missing)" || echo "CHECK"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/memory/__init__.py` exists.
    - `tests/memory/test_store_parity.py` contains `pytest.mark.parity` and imports `from vibemix.library._cosine import` (chokepoint reused, never forked).
    - `tests/memory/test_store.py` defines `test_add_query_roundtrip`, `test_numpy_fallback`, `test_delete_cascade`, `test_session_id_path_traversal`.
    - Collection fails on `vibemix.memory` not existing (EXPECTED-RED) — confirms the contract targets the unbuilt package, not a stub.
    - No test file imports or defines a bespoke KNN / `ORDER BY distance`; ranking references only `cosine_topk`.
  </acceptance_criteria>
  <done>Three files created; parity + round-trip + fallback + cascade + path-traversal contracts pinned; collection RED on missing `vibemix.memory`.</done>
</task>

<task type="auto">
  <name>Task 2: Create the import-boundary gate, the no-extraction gate, and the retention contract</name>
  <read_first>
    - tests/repo/test_repo_scrub.py (the subprocess `sys.modules` dormancy idiom ~line 406, and `_strip_comments_and_docstrings` tokenize stripper ~line 272-310)
    - tests/repo/test_model_literal_gate.py (the existing model-literal gate that already auto-covers new memory/ files — reference, do NOT duplicate)
    - src/vibemix/runtime/recordings_index.py (`run_retention_sweep`, `RetentionSweepResult` ~line 490-528 — retention semantics to mirror)
    - .planning/phases/63-memory-store/63-RESEARCH.md §Code Examples (ready clones at lines 382-443)
    - .planning/phases/63-memory-store/63-PATTERNS.md (test_no_live_path_import / test_no_extraction / test_retention sections)
  </read_first>
  <files>tests/memory/test_no_live_path_import.py, tests/memory/test_no_extraction.py, tests/memory/test_retention.py</files>
  <action>
    Create `tests/memory/test_no_live_path_import.py` (no-live-path invariant): a static AST scan `test_memory_does_not_import_live_path_statically` walks `src/vibemix/memory/*.py` and asserts no `import`/`from` targets `vibemix.state.coach`, `vibemix.state.refresh`, `vibemix.agent`, `vibemix.prompts`, `vibemix.runtime.ws_bus`, and no imported name is `MusicState` or `EventDetector`; plus `test_importing_memory_loads_no_coach_loop` cloning the subprocess `sys.modules` dormancy idiom from `test_repo_scrub.py` — import `vibemix.memory.store` in a subprocess, assert no `vibemix.state.coach` / `vibemix.agent` / `ws_bus` module leaked (prints `CLEAN`).

    Create `tests/memory/test_no_extraction.py` (raw-in/raw-out, STORE-02): clone the `_strip_comments_and_docstrings` tokenize stripper from `test_repo_scrub.py` (so docstrings documenting the ban don't self-trip), then `test_memory_calls_only_embed_content` scans stripped `src/vibemix/memory/*.py` for forbidden generation surfaces `generate_content`, `generate_reply`, `.chats.`, `GenerateContentConfig` and asserts none present — only `embed_content` may reach Gemini from `memory/`.

    Create `tests/memory/test_retention.py` (STORE-03): assert the memory retention sweep evicts OLDEST-SESSION-FIRST and WHOLE-SESSION (never partial). Build a `MemoryStore` with several sessions at increasing `ts`, set a small cap, run the sweep, assert the oldest whole session(s) were removed via `delete_session` semantics and newest sessions survive intact; assert no partial-session state remains. Mirror the `RetentionSweepResult(deleted, bytes_pruned)`-style return + `∞`-sentinel short-circuit semantics from `recordings_index.run_retention_sweep`.

    Note in a module comment that STORE-04's no-model-literal requirement is already covered by the shipped `tests/repo/test_model_literal_gate.py` (which scans all of `src/vibemix/` including the new `memory/`) — do NOT duplicate that gate here.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/memory/test_no_live_path_import.py tests/memory/test_no_extraction.py tests/memory/test_retention.py --co 2>&1 | grep -qiE "error|collected" && echo "FILES-PARSE-OR-RED" || echo "CHECK"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/memory/test_no_live_path_import.py` defines `test_memory_does_not_import_live_path_statically` and `test_importing_memory_loads_no_coach_loop`; FORBIDDEN import list includes `vibemix.state.coach`, `vibemix.agent`, `vibemix.runtime.ws_bus`.
    - `tests/memory/test_no_extraction.py` defines `test_memory_calls_only_embed_content`; FORBIDDEN tuple includes `generate_content`, `generate_reply`, `GenerateContentConfig`; uses a tokenize-based comment/docstring stripper.
    - `tests/memory/test_retention.py` asserts oldest-session-first AND whole-session eviction.
    - No file duplicates the model-literal gate logic (it references `tests/repo/test_model_literal_gate.py` in a comment instead).
    - All three files parse without `SyntaxError` (collection-time errors against the not-yet-built `memory/` package are expected and acceptable).
  </acceptance_criteria>
  <done>Three gate/contract files created; import-boundary, no-extraction, and retention contracts pinned; STORE-04 explicitly delegated to the existing literal gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test source → repo invariants | These test files ARE the enforcement mechanism for the no-live-path and no-extraction invariants — they are the boundary, not crossing one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering | future `memory/` imports the live reaction path | mitigate | `test_no_live_path_import.py` static AST + subprocess dormancy gate created in this plan blocks the build if violated |
| T-63-02 | Spoofing | future `memory/` calls a generation model (confabulation) | mitigate | `test_no_extraction.py` tokenize-stripped scan created in this plan fails the build if any generation surface appears |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: zero net-new deps; pytest + parity marker already present) |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/memory --co` collects (or RED on missing `vibemix.memory` — expected) — all 6 files present, no SyntaxError.
- `grep -rn "ORDER BY distance\|vec_distance_cosine\|MATCH" tests/memory/` returns nothing (no native-KNN ranking in tests).
- `grep -rn "cosine_topk\|_cosine" tests/memory/test_store_parity.py` confirms the chokepoint is reused.
</verification>

<success_criteria>
- `tests/memory/` is a package with all 6 contract files from VALIDATION.md Wave-0 list.
- Every STORE-01..04 behavior has a named, RED (or collection-RED) test pinning it.
- No test forks `cosine_topk`, adds a dependency, hardcodes a model literal, or asserts a native-KNN ranking path.
</success_criteria>

<output>
Create `.planning/phases/63-memory-store/63-01-SUMMARY.md` when done.
</output>
