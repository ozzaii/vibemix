---
phase: 63-memory-store
plan: 03
type: execute
wave: 2
depends_on: ["63-02"]
files_modified:
  - src/vibemix/memory/store.py
  - src/vibemix/memory/retention.py
autonomous: true
requirements: [STORE-03]

must_haves:
  truths:
    - "delete_session removes a session's vectors AND its moments rows in one transaction — no orphaned vectors"
    - "A per-install retention budget evicts oldest-session-first, whole-session (never partial), capped on moment count and/or age"
    - "A crafted session_id (path-traversal) is rejected before any filesystem/delete operation"
    - "A boot-time orphan-reconciliation sweep drops vec records with no moments row (and danglers vs the recordings index where cheap)"
  artifacts:
    - path: "src/vibemix/memory/retention.py"
      provides: "run_memory_retention_sweep (oldest-session-first whole-session eviction) + RetentionSweepResult + reconcile_orphans"
      contains: "def run_memory_retention_sweep"
    - path: "src/vibemix/memory/store.py"
      provides: "hardened delete_session (path-traversal gate + atomic cascade) + reconcile_orphans hook"
      contains: "is_relative_to"
  key_links:
    - from: "src/vibemix/memory/store.py"
      to: "vibemix.runtime.recordings_index (SESSION_DIR_RE / is_relative_to gate pattern)"
      via: "path-traversal defense on session_id"
      pattern: "is_relative_to"
    - from: "src/vibemix/memory/retention.py"
      to: "src/vibemix/memory/store.py delete_session"
      via: "whole-session eviction calls delete_session"
      pattern: "delete_session"
---

<objective>
Harden the memory store for STORE-03: make `delete_session` a path-traversal-defended, atomic cascade (vectors + `moments` rows in one transaction, no orphans); add `run_memory_retention_sweep` (oldest-session-first, whole-session eviction under a per-install count/age cap, mirroring `recordings_index.run_retention_sweep`); and add a boot-time `reconcile_orphans` sweep that drops vec records with no `moments` row (the defensive backstop for the numpy-path vector-first write ordering and the delete-cascade hook). This is the cascade hook the recordings-delete path will call (call-site wiring deferred — see scope note).

Purpose: turn the RED retention + cascade + path-traversal contract from Plan 01 GREEN; subject embeddings to the same delete/retention/privacy contract as raw artifacts.
Output: `src/vibemix/memory/retention.py` (new) + hardened `MemoryStore.delete_session`/orphan hook in `store.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-memory-store/63-RESEARCH.md
@.planning/phases/63-memory-store/63-PATTERNS.md

<interfaces>
From src/vibemix/runtime/recordings_index.py (the patterns to mirror — copy, don't import the live path):
  SESSION_DIR_RE = re.compile(r"^\d{8}-\d{6}$")          # the recordings-session-dir shape
  class RetentionSweepResult(NamedTuple)                  # (deleted, bytes_pruned) return shape
  def run_retention_sweep(...)                            # ∞-sentinel short-circuit, best-effort per-entry, oldest-first
  # delete gate (lines 388-401): SESSION_DIR_RE.match + (root/name).resolve().is_relative_to(root.resolve()) + refuse root itself

From src/vibemix/memory/store.py (built in Plan 02 — extend it here):
  class MemoryStore: add_record(...) / query_topk(...) / delete_session(session_id) -> int
  # backend.delete(record_ids) + moments connection already available
</interfaces>

<!-- Shapes to clone: RESEARCH Pattern 4 (delete-cascade, 63-RESEARCH.md:265-280); §Common Pitfalls 2 & 3 (orphans / numpy split-brain); PATTERNS Shared Patterns (path-traversal lines 198-211, retention lines 223-225). -->
<!-- NOTE: memory record_id is f"{session_id}:{seq}". The path-traversal gate applies to any session_id that reaches the filesystem. Validate session_id shape (reject ../, absolute, separators, null) before it indexes a path or a sibling DB filename. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden delete_session (path-traversal gate + atomic cascade) and add reconcile_orphans</name>
  <read_first>
    - src/vibemix/runtime/recordings_index.py (the SESSION_DIR_RE + is_relative_to two-layer gate at lines 388-401; the delete return shape)
    - src/vibemix/memory/store.py (the delete_session built in Plan 02, to harden in place)
    - tests/memory/test_store.py (::test_delete_cascade and ::test_session_id_path_traversal — the RED contract)
    - .planning/phases/63-memory-store/63-RESEARCH.md §Pattern 4 + §Common Pitfalls 2-3 (lines 265-330)
    - .planning/phases/63-memory-store/63-PATTERNS.md §Shared Patterns (path-traversal, lines 198-211)
  </read_first>
  <files>src/vibemix/memory/store.py</files>
  <behavior>
    - test_delete_cascade: after delete_session(s1), s1 records are gone from BOTH the vector backend and moments; s2 untouched; query_topk never returns an s1 record_id with an empty moments join
    - test_session_id_path_traversal: a session_id containing "../", an absolute path, a path separator, or a null byte is rejected (raises or returns 0 without touching the filesystem)
    - reconcile_orphans: a vec record with no matching moments row is dropped; a moments row with a live vec is kept
  </behavior>
  <action>
    In `src/vibemix/memory/store.py`, harden `delete_session`:
    - Add a path-traversal guard validating `session_id` shape BEFORE any delete or filename derivation: reject any session_id containing a path separator (`/`, `\`), `..`, a leading absolute marker, or a null byte. Where a session_id derives a sibling-DB filename or path, additionally apply the `resolve().is_relative_to(app_data_dir().resolve())` check (mirror `recordings_index.py:388-401`) and refuse the root itself. (The recordings `SESSION_DIR_RE = ^\d{8}-\d{6}$` is the canonical session-dir shape; reuse it where the caller passes a recordings-session id, but keep the generic separator/`..`/null rejection as the floor so non-recordings ids are still safe.)
    - Make the cascade atomic: look up the session's record_ids in `moments`, `backend.delete(record_ids)`, `DELETE FROM moments WHERE session_id = ?`, then a SINGLE commit (Pattern 4). Return the count deleted.

    Add `reconcile_orphans(self) -> int` to `MemoryStore`: load all vec record_ids via `backend.load_all()`, find those with no `moments` row, `backend.delete(...)` them in one transaction, return the count. Document that this is the defensive backstop for the numpy-path vector-first write ordering (Pitfall 3) and is intended to run at boot. Keep it transactional and best-effort (never raise on a single bad entry). Do NOT import the recordings index live path or the coach loop — if a recordings-index cross-check is added, import only the read-only `RecordingsIndex`/`SESSION_DIR_RE` utility (not the reaction path), and only if it does not trip the no-live-path gate.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/memory/test_store.py::test_delete_cascade tests/memory/test_store.py::test_session_id_path_traversal tests/memory/test_no_live_path_import.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/memory/test_store.py::test_delete_cascade` passes (no orphaned vectors after delete).
    - `tests/memory/test_store.py::test_session_id_path_traversal` passes (crafted session_id rejected before fs op).
    - `src/vibemix/memory/store.py` contains `is_relative_to` and a separator/`..`/null rejection in `delete_session`.
    - `MemoryStore.reconcile_orphans` exists, is transactional, and drops only vec records lacking a moments row.
    - `tests/memory/test_no_live_path_import.py` still passes (no live-path import introduced by the recordings cross-check).
    - delete is atomic: a single commit covers both the vector delete and the moments delete.
  </acceptance_criteria>
  <done>delete_session is path-traversal-defended + atomic; reconcile_orphans drops danglers; no-live-path invariant intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add run_memory_retention_sweep (oldest-session-first, whole-session eviction)</name>
  <read_first>
    - src/vibemix/runtime/recordings_index.py (run_retention_sweep + RetentionSweepResult + ∞-sentinel short-circuit, lines 490-528)
    - src/vibemix/memory/store.py (delete_session + the moments table — the eviction primitive)
    - tests/memory/test_retention.py (the RED contract — oldest-session-first, whole-session)
    - .planning/phases/63-memory-store/63-PATTERNS.md §Shared Patterns (retention, lines 223-225)
    - src/vibemix/runtime/config_store.py (config store — for the per-install configurable cap)
  </read_first>
  <files>src/vibemix/memory/retention.py</files>
  <behavior>
    - run_memory_retention_sweep evicts whole sessions oldest-first until under cap (moment count and/or age)
    - never partial-session: a session is either fully retained or fully evicted (via delete_session)
    - ∞/None cap → no-op short-circuit (RetentionSweepResult with nothing deleted)
    - returns a RetentionSweepResult-style (deleted_sessions, moments_pruned) summary; best-effort per session
  </behavior>
  <action>
    Create `src/vibemix/memory/retention.py`. Define a `RetentionSweepResult` NamedTuple mirroring `recordings_index.RetentionSweepResult` (e.g. `deleted_sessions: list[str]`, `moments_pruned: int`). Write `run_memory_retention_sweep(store: MemoryStore, *, max_moments: int | None = 10000, max_age_days: int | None = 180) -> RetentionSweepResult`: read default caps from the existing config store where configured (within the recommended ranges — ~10,000 moments / ~180 days are Claude's-discretion defaults per CONTEXT); short-circuit to a no-op when both caps are `None`/∞ (mirror the `∞`-sentinel short-circuit). Compute per-session min/max ts + counts by grouping the `moments` table by `session_id`; evict WHOLE sessions OLDEST-FIRST (by oldest ts) — first any session older than `max_age_days`, then, while total moments exceed `max_moments`, the oldest remaining session — each eviction via `store.delete_session(session_id)` (never partial). Best-effort per session (a single delete failure does not abort the sweep). Return the result. This function is intended to be called at boot + session-close by the runtime (call-site wiring is the runtime's concern; ship the sweep + its test here).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/memory/test_retention.py -x; PYTHONPATH=src python3 -m pytest -q tests/memory tests/repo/test_model_literal_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/vibemix/memory/retention.py` defines `run_memory_retention_sweep` and a `RetentionSweepResult` NamedTuple.
    - `tests/memory/test_retention.py` passes: oldest-session-first AND whole-session eviction proven.
    - Both-caps-None (∞) input is a no-op short-circuit.
    - Eviction routes exclusively through `store.delete_session` (no partial-session deletes; no second ranking path).
    - Full `tests/memory` suite + `tests/repo/test_model_literal_gate.py` green.
    - `retention.py` imports nothing from the live reaction path (no-live-path gate still green).
  </acceptance_criteria>
  <done>run_memory_retention_sweep evicts oldest-session-first, whole-session, under configurable count/age caps; full memory suite + literal gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → delete_session(session_id) | caller-supplied session_id (untrusted) may index a path or sibling-DB filename |
| retention sweep → filesystem | whole-session eviction deletes durable user data — must be whole-session, never partial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-07 | Tampering / EoP | path traversal via crafted session_id in delete_session | mitigate | separator/`..`/null rejection + `resolve().is_relative_to(app_data_dir())` (clone recordings_index gate); `test_session_id_path_traversal` enforces |
| T-63-08 | Tampering | orphaned vectors after delete (data integrity) | mitigate | atomic `delete_session` (single commit) + boot `reconcile_orphans` backstop; `test_delete_cascade` enforces |
| T-63-09 | Information disclosure | embeddings exempt from retention/delete (data lingers) | mitigate | embeddings subject to the same delete cascade + oldest-first retention sweep as raw artifacts |
| T-63-10 | Denial of service | unbounded memory.db growth | mitigate | per-install count/age retention budget evicts oldest-session-first |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | zero net-new deps (RESEARCH §Package Legitimacy Audit); no install step |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/memory` — full memory suite green (round-trip, parity, fallback, cascade, path-traversal, retention, no-live-import, no-extraction).
- `pytest -m parity` green.
- `tests/repo/test_model_literal_gate.py` green.
- `grep -rn "import" src/vibemix/memory/ | grep -E "agent|prompts|state.coach|ws_bus|MusicState"` returns nothing.
</verification>

<success_criteria>
- STORE-03: delete-cascade (no orphans), per-install oldest-session-first retention budget, path-traversal-defended, local-only + user-deletable.
- The cascade hook (`delete_session`) is shippable for the recordings-delete call-site; retention sweep ready for boot/session-close wiring.
- No live-path import, no native KNN, no new dependency.
</success_criteria>

<output>
Create `.planning/phases/63-memory-store/63-03-SUMMARY.md` when done.

## KAAN-ACTION (surface, do not block)
- **sqlite-vec clean-VM round-trip (external clock):** prove a `memory.db` write→read round-trip on a clean Mac + Windows (incl. ARM64) VM in the e2e install matrix. The `vec0.*` binary is already signed in shipping builds; `memory.db` is a data file with zero new signing surface. Rides the in-flight Apple-notarization + SignPath approvals — NOT a Phase 63 engineering blocker. (Per STATE.md / RESEARCH §sqlite-vec Install/Signing Landmine.)
- **Call-site wiring (deferred):** wiring `recordings_index.delete → MemoryStore.delete_session` and `boot/session-close → run_memory_retention_sweep` is deferred to the phase that owns the recordings-UI delete flow (avoids importing scope into the storage spine). Phase 63 ships the store contract + tests; the call-sites land downstream.
</output>
