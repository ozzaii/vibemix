---
phase: 63-memory-store
plan: 02
type: execute
wave: 1
depends_on: ["63-01"]
files_modified:
  - src/vibemix/memory/__init__.py
  - src/vibemix/memory/index_sqlite_vec_memory.py
  - src/vibemix/memory/store.py
autonomous: true
requirements: [STORE-01, STORE-02, STORE-04]

must_haves:
  truths:
    - "A per-install memory.db (sqlite-vec) stores embedded moments + a moments metadata table; falls back to NumpyStore when the extension can't load"
    - "MemoryStore.add_record + query_topk round-trip a record; the raw signature is returned verbatim (never an LLM-extracted insight)"
    - "Ranking goes through the imported cosine_topk verbatim — Mac/Win bit-identical, no native vec0 KNN"
    - "Embedding model is resolved via model_router.resolve('embedding') on FLEX (no hardcoded literal anywhere in memory/)"
    - "memory/ imports nothing from the live reaction path (coach loop, MusicState, ws_bus) and calls no generation model"
  artifacts:
    - path: "src/vibemix/memory/index_sqlite_vec_memory.py"
      provides: "SqliteVecMemoryStore — vec0 backend over memory.db with vec_memory table + moments sibling table"
      contains: "vec_memory"
    - path: "src/vibemix/memory/store.py"
      provides: "MemoryStore (add_record/query_topk/delete_session) + open_memory_store() probe + moments schema"
      contains: "from vibemix.library._cosine import"
    - path: "src/vibemix/memory/__init__.py"
      provides: "re-export surface (MemoryStore, open_memory_store, Record)"
  key_links:
    - from: "src/vibemix/memory/store.py"
      to: "vibemix.library._cosine.cosine_topk"
      via: "import + sole ranking call in query_topk"
      pattern: "cosine_topk\\("
    - from: "src/vibemix/memory/store.py"
      to: "vibemix.runtime.config_store.app_data_dir"
      via: "MEMORY_DB_PATH = app_data_dir() / 'memory.db'"
      pattern: "app_data_dir\\(\\)"
    - from: "src/vibemix/memory/store.py"
      to: "vibemix.library.embed.LibraryEmbedder"
      via: "embedding seam (resolve('embedding'), FLEX)"
      pattern: "LibraryEmbedder|embed_query"
---

<objective>
Build the storage core of the memory layer: `SqliteVecMemoryStore` (vec0 backend cloned from `library/index_sqlite_vec.py` with the `vec_library`→`vec_memory` rename + a `moments` sibling table), the `MemoryStore` facade (composing the vec0/numpy backend + owning the `moments` connection), and `open_memory_store()` (the probe-and-fall-through cloned from `library/store.py::open_store`, pointed at `app_data_dir()/"memory.db"`). Expose `add_record`, `query_topk` (with the locked `exclude_session=None` seam), and a stub `delete_session` (full cascade lands in Plan 03). Embedding routes through the reused `LibraryEmbedder` — `resolve("embedding")` on FLEX, never a literal.

Purpose: turn the RED contract from Plan 01 GREEN for STORE-01/02/04 — a parity-correct, fallback-safe, raw-in/raw-out store with the single `cosine_topk` ranking chokepoint and zero net-new dependency.
Output: the `src/vibemix/memory/` package (3 files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-memory-store/63-RESEARCH.md
@.planning/phases/63-memory-store/63-PATTERNS.md

<interfaces>
<!-- REUSED VERBATIM (import, never fork): -->
From src/vibemix/library/_cosine.py:
  EMBEDDING_DIM = 768
  def l2_normalize(vec: np.ndarray) -> np.ndarray
  def cosine_topk(query, vectors, ids, k) -> list[tuple[str, float]]   # float32 + (768,) asserts; tie-break sim DESC / id ASC

From src/vibemix/library/store.py (the open_store probe shape + _Backend protocol to clone):
  class _Backend(Protocol):
    add_batch(items: list[tuple[str, np.ndarray]]) -> None
    load_all() -> tuple[list[str], np.ndarray]
    delete(track_ids: list[str]) -> None
    snapshot_hash() -> str
    close() -> None
  def open_store(prefer_sqlite_vec=True) -> LibraryStore   # probe SqliteVecStore, except → NumpyStore, structured log

From src/vibemix/library/index_sqlite_vec.py (clone this ~120-line class; rename vec_library→vec_memory; add moments table):
  class SqliteVecStore(db_path: Path = DB_PATH)   # self.db = sqlite3 conn; vec0 table; add_batch/load_all/delete/snapshot_hash/close
  # vec_library is hardcoded in SQL at lines 51,77,81,89,104,111 — rename ALL to vec_memory

From src/vibemix/library/index_numpy.py (REUSE AS-IS via import — do NOT clone):
  class NumpyStore(vectors_path=..., ids_path=...)   # same _Backend interface

From src/vibemix/library/embed.py:
  GEMINI_EMBEDDING_MODEL = resolve("embedding")[0]   # never hardcode; ("gemini-embedding-2", ServiceTier.FLEX)
  class LibraryEmbedder: embed_query(text) -> np.ndarray (768-dim L2-normalized float32)

From src/vibemix/runtime/config_store.py:
  def app_data_dir() -> Path   # OS-aware; darwin ~/Library/Application Support/vibemix ; monkeypatch-friendly
</interfaces>

<!-- Implementation shapes (clone, do not invent): RESEARCH Patterns 1-3 (63-RESEARCH.md:193-263) + PATTERNS store.py / index_sqlite_vec_memory.py sections. -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clone the vec0 backend as SqliteVecMemoryStore (vec_memory + moments table)</name>
  <read_first>
    - src/vibemix/library/index_sqlite_vec.py (the ~120-line class to copy — init/add_batch/load_all/delete/snapshot_hash/close; the float32 + (768,) asserts at lines 63-70; the delete-then-insert replace idiom; `ORDER BY track_id ASC`)
    - src/vibemix/library/_cosine.py (EMBEDDING_DIM)
    - .planning/phases/63-memory-store/63-PATTERNS.md (index_sqlite_vec_memory.py section, lines 79-101)
    - .planning/phases/63-memory-store/63-RESEARCH.md §Pattern 2 (moments schema, lines 228-245)
  </read_first>
  <files>src/vibemix/memory/index_sqlite_vec_memory.py</files>
  <behavior>
    - load_all() returns the same (ids, vectors) shape and dtype (float32, (N,768)) as NumpyStore for an identical add_batch sequence (parity precondition)
    - add_batch enforces float32 + (768,) per vector (dimension-drift guard); a 3072-dim vector raises
    - the moments sibling table is created on the SAME connection so add_record can commit vec0 + metadata atomically downstream
    - delete([ids]) removes exactly those vec rows; close() closes the connection
  </behavior>
  <action>
    Copy the `SqliteVecStore` class from `library/index_sqlite_vec.py` into `src/vibemix/memory/index_sqlite_vec_memory.py` as `SqliteVecMemoryStore`. Two changes only: (1) rename the hardcoded `vec_library` table to `vec_memory` in ALL SQL strings; (2) in `__init__`, after creating the `vec_memory` vec0 virtual table on `self.db`, also `CREATE TABLE IF NOT EXISTS moments (record_id TEXT PRIMARY KEY, session_id TEXT NOT NULL, ts REAL NOT NULL, kind TEXT NOT NULL, signature TEXT NOT NULL)` + `CREATE INDEX IF NOT EXISTS idx_moments_session ON moments(session_id)` on the same connection, then commit. Keep `db_path` as a constructor arg (no `~/.cache` default — MemoryStore supplies `app_data_dir()/"memory.db"`). Keep the `sqlite_vec.load(self.db)` line so it re-raises on a host with no extension (the fallback trigger). Keep the float32 + `(EMBEDDING_DIM,)` asserts and the `ORDER BY track_id ASC` verbatim — they are the dimension-drift guard and the parity contract. Expose `self.db` (the single connection) so `MemoryStore` can own the `moments` reads/writes on it. Do NOT add native KNN, `MATCH`, or `ORDER BY distance`. Document the single-writer invariant in the module docstring.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -c "from vibemix.memory.index_sqlite_vec_memory import SqliteVecMemoryStore; print('OK')" 2>&1; grep -c "vec_library" src/vibemix/memory/index_sqlite_vec_memory.py | grep -qx 0 && echo "NO-vec_library-LEAK"; grep -q "vec_memory" src/vibemix/memory/index_sqlite_vec_memory.py && echo "HAS-vec_memory"</automated>
  </verify>
  <acceptance_criteria>
    - `src/vibemix/memory/index_sqlite_vec_memory.py` defines `class SqliteVecMemoryStore`.
    - `grep -c vec_library` on the file returns 0 (no stale table name); `vec_memory` present.
    - File contains `CREATE TABLE IF NOT EXISTS moments` and `idx_moments_session`.
    - Float32 + `(EMBEDDING_DIM,)` assert preserved (dimension-drift guard intact).
    - No `ORDER BY distance`, `vec_distance_cosine`, or `MATCH` in the file (storage-only, ranking stays in cosine_topk).
    - Module importable without sqlite-vec installed failing the import at module level (the load happens in `__init__`, not import).
  </acceptance_criteria>
  <done>SqliteVecMemoryStore exists; vec_memory table + moments sibling table on one connection; dimension guard + parity ordering preserved; no native KNN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build MemoryStore facade + open_memory_store() + barrel; turn STORE-01/02/04 contract GREEN</name>
  <read_first>
    - src/vibemix/library/store.py (open_store probe-and-fall-through at lines 78-114; LibraryStore.search chokepoint at 56-61; __all__ style at 117)
    - src/vibemix/library/index_numpy.py (NumpyStore constructor args vectors_path/ids_path — reused as-is)
    - src/vibemix/library/embed.py (LibraryEmbedder.embed_query — the only model call; resolve('embedding') seam)
    - src/vibemix/library/__init__.py (re-export surface analog)
    - src/vibemix/runtime/config_store.py (app_data_dir)
    - src/vibemix/memory/index_sqlite_vec_memory.py (the backend built in Task 1 — its self.db connection + moments table)
    - tests/memory/test_store.py + tests/memory/test_store_parity.py (the RED contract this task turns green)
    - .planning/phases/63-memory-store/63-RESEARCH.md §Patterns 1 & 3 (open_memory_store + query_topk, lines 193-263)
    - .planning/phases/63-memory-store/63-PATTERNS.md (store.py section, lines 28-75)
  </read_first>
  <files>src/vibemix/memory/store.py, src/vibemix/memory/__init__.py</files>
  <behavior>
    - test_add_query_roundtrip: add_record then query_topk returns the record with .signature byte-identical to what was stored (raw-in/raw-out)
    - test_numpy_fallback: MemoryStore(prefer_sqlite_vec=False) is fully functional (add + query work) using NumpyStore + a sibling moments sqlite file
    - test_memory_sqlite_vec_topk_matches_numpy (parity): sqlite-vec and numpy backends produce identical record_id rank order via cosine_topk
    - query_topk accepts exclude_session=None and, when set, omits that session's records before ranking (seam shipped; Phase 65 uses it)
  </behavior>
  <action>
    Create `src/vibemix/memory/store.py`. Define `MEMORY_DB_PATH = app_data_dir() / "memory.db"` (durable tier — NOT `~/.cache`, per the PATTERNS drift note). Write `open_memory_store(db_path, prefer_sqlite_vec=True)` cloning `library/store.py::open_store`: try `SqliteVecMemoryStore(db_path=...)`, on ANY exception log the structured `-> memory store: backend=NumpyStore reason=sqlite_vec_unavailable (...)` line and fall through to `NumpyStore(vectors_path=app_data_dir()/"memory_vectors.npy", ids_path=app_data_dir()/"memory_ids.json")` (paths derived from db_path's parent for tmp_path test isolation). Write a minimal typed `Record` dataclass (record_id, session_id, ts, kind, signature, score) — raw fields only, NO signature builders (those are Phase 64).

    Define `class MemoryStore(db_path: Path = MEMORY_DB_PATH, prefer_sqlite_vec: bool = True)`: hold the backend from `open_memory_store`, and own the `moments` sqlite connection — reuse the backend's `self.db` on the sqlite-vec path (one connection, single-writer, atomic), or open a sibling `memory_moments.db` on the numpy path. Implement:
    - `add_record(record_id, session_id, ts, kind, signature, embedding)`: assert embedding is float32 (768,) (or rely on the backend assert); write the vector via `backend.add_batch([(record_id, embedding)])` FIRST, then INSERT/REPLACE the `moments` row, then commit (vector-first ordering per RESEARCH Pitfall 3 so a crash leaves at worst a reconcilable orphan).
    - `query_topk(query_embedding, k=8, *, exclude_session=None)`: `ids, vectors = backend.load_all()`; if `exclude_session` set, filter ids/vectors by joining the moments session column; call `cosine_topk(query_embedding, vectors, ids, k)` (the ONLY ranking path); JOIN `moments` on the returned record_ids; return `list[Record]` with `.score` = the cosine value. NEVER `ORDER BY distance`.
    - `delete_session(session_id) -> int`: ship the minimal correct form (lookup record_ids in moments, `backend.delete(...)`, `DELETE FROM moments`, single commit) — Plan 03 extends it with the path-traversal guard, retention sweep, and orphan reconciliation.

    Embedding seam: import the reused `LibraryEmbedder` / `embed_query` path so the store's ONLY model call is the embed call — do NOT import or call any generation model, `vibemix.agent`, `vibemix.prompts`, `vibemix.state.coach`, or `MusicState`. Do NOT hardcode an embedding-model literal — rely on `resolve("embedding")` via the library embedder.

    Create `src/vibemix/memory/__init__.py` re-exporting `MemoryStore`, `open_memory_store`, `Record` (mirror `library/__init__.py` `__all__` style).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/memory/test_store.py::test_add_query_roundtrip tests/memory/test_store.py::test_numpy_fallback tests/memory/test_store_parity.py -x; PYTHONPATH=src python3 -m pytest -q tests/memory/test_no_live_path_import.py tests/memory/test_no_extraction.py tests/repo/test_model_literal_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/memory/test_store.py::test_add_query_roundtrip` and `::test_numpy_fallback` pass.
    - `tests/memory/test_store_parity.py` parity test passes (sqlite-vec ↔ numpy bit-identical) where sqlite-vec available.
    - `tests/memory/test_no_live_path_import.py` and `test_no_extraction.py` pass (no live-path import, only embed_content).
    - `tests/repo/test_model_literal_gate.py` passes (no embedding-model literal introduced in memory/).
    - `grep -n "ORDER BY distance\|vec_distance_cosine\|MATCH" src/vibemix/memory/store.py` returns nothing.
    - `grep -n "cosine_topk" src/vibemix/memory/store.py` shows it imported from `vibemix.library._cosine` and called in `query_topk`.
    - `query_topk` signature includes the `exclude_session=None` keyword-only param.
    - `MEMORY_DB_PATH` derives from `app_data_dir()`, not `~/.cache`.
  </acceptance_criteria>
  <done>MemoryStore + open_memory_store + barrel shipped; round-trip, fallback, parity, no-live-import, no-extraction, model-literal gates all GREEN; cosine_topk is the sole ranking path; embedding via resolve('embedding') on FLEX.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → MemoryStore.add_record | caller-supplied embedding (untrusted shape/dtype) crosses into vec0 storage |
| MemoryStore → Gemini embed call | the only external network crossing (proxied, FLEX); structurally NOT a generation call |
| memory/ → live reaction path | a boundary that must NOT be crossed (gated by Plan 01 import test) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03 | Tampering | dimension drift — wrong-shape vector reaches vec0 | mitigate | float32 + (768,) assert preserved verbatim from `index_sqlite_vec.py` + `cosine_topk` shape assert |
| T-63-04 | Spoofing | confabulation via a generation-model call in the store path | mitigate | store imports only `LibraryEmbedder`/embed path; enforced by `test_no_extraction.py` (Plan 01) |
| T-63-05 | Tampering | parity break via native vec0 KNN ranking | mitigate | ranking routes through imported `cosine_topk` only; `test_store_parity.py` (Plan 01) + grep gate enforce |
| T-63-06 | Information disclosure | memory.db written outside the sanctioned dir | mitigate | all paths derive from `app_data_dir()`; never `~/.cache`, never Hermes/LM-Studio privacy paths |
| T-63-SC | Tampering | npm/pip/cargo installs | accept | zero net-new deps (RESEARCH §Package Legitimacy Audit); no install step |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/memory/test_store.py tests/memory/test_store_parity.py tests/memory/test_no_live_path_import.py tests/memory/test_no_extraction.py` — round-trip + fallback + parity + invariants green.
- `pytest -m parity tests/memory/test_store_parity.py` green where sqlite-vec available.
- `tests/repo/test_model_literal_gate.py` green (no literal regression).
- `grep -rn "import" src/vibemix/memory/ | grep -E "agent|prompts|state.coach|ws_bus|MusicState"` returns nothing.
</verification>

<success_criteria>
- STORE-01: per-install `memory.db` with vec0 + numpy fallback, parity-identical ranking via `cosine_topk`.
- STORE-02: `add_record`/`query_topk` round-trip, raw signature returned verbatim, no generation surface.
- STORE-04: embedding via `resolve("embedding")` on FLEX, no literal anywhere in `memory/`.
- The `cosine_topk` chokepoint is imported (never forked); zero net-new dependency; no live-path import.
</success_criteria>

<output>
Create `.planning/phases/63-memory-store/63-02-SUMMARY.md` when done.
</output>
