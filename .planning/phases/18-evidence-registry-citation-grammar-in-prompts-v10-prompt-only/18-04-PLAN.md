---
phase: 18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only
plan: 04
type: execute
wave: 4
depends_on:
  - "18-01"
  - "18-03"
files_modified:
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/state/evidence_registry.py
  - tests/agent/test_dj_cohost.py
  - tests/state/test_evidence_registry.py
autonomous: true
requirements:
  - GROUND-02
user_setup: []
must_haves:
  truths:
    - "Every Gemini AI turn writes a `citation_count` line to events.jsonl with the integer count of citations parsed from the response text"
    - "Rolling 50-response average citation_count is exposed via EvidenceRegistry.citation_telemetry() snapshot — Phase 16 ear-test consumes this directly"
    - "Telemetry is best-effort — a parser failure or missing recorder MUST NOT break the LLM response path (cohost_v4 anti-pattern parity)"
    - "Telemetry events.jsonl line shape matches CONTEXT.md §Telemetry: {kind: 'citation_count', count: <int>, response_id: <id>, t: <float>}"
    - "Telemetry fires for ALL turns including suppressed ones (silence_short_circuit / slop_suppressed) so the rolling average reflects true Gemini-emission rate (not just spoken responses)"
  artifacts:
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "llm_node post-stream block parses citations from full_text via parse_citations + writes citation_count event + updates EvidenceRegistry rolling buffer"
      contains: "citation_count"
    - path: "src/vibemix/state/evidence_registry.py"
      provides: "EvidenceRegistry.record_citation_count(n: int) + citation_telemetry() snapshot returning rolling 50-response avg"
      exports: ["record_citation_count", "citation_telemetry"]
    - path: "tests/agent/test_dj_cohost.py"
      provides: "Asserts citation_count events.jsonl line shape + rolling average update per turn"
    - path: "tests/state/test_evidence_registry.py"
      provides: "Asserts rolling 50-response buffer + average computation"
  key_links:
    - from: "src/vibemix/agent/dj_cohost.py"
      to: "src/vibemix/state/evidence_registry.py"
      via: "self._registry.record_citation_count(n) after each llm_node response"
      pattern: "record_citation_count"
    - from: "src/vibemix/agent/dj_cohost.py"
      to: "src/vibemix/audio/recorder.py"
      via: "self._recorder.log_event('citation_count', count=n, response_id=..., t=...)"
      pattern: "log_event\\(.citation_count."
---

<objective>
Add citation-count telemetry to every Gemini turn — closes ROADMAP success criterion #4 (events.jsonl records citation_count_per_response per AI turn) and feeds Phase 16 ear-test the rolling-average signal that gates Phase 20 enforcement readiness. v1.0 pure observability, zero behavior change to the LLM response path.

Purpose: Without telemetry, Plan 18-03's prompt-only seeding is invisible — we can't tell if Gemini is learning to cite. With telemetry, Phase 16 sees a per-session "Gemini cited N times across M responses" signal that informs P20's stripped_rate threshold tuning.

Output: Per-turn JSONL line + in-process rolling buffer queried by Phase 16's audit surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-CONTEXT.md
@.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-01-PLAN.md
@.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-03-PLAN.md

# Existing source — must read before editing.
@src/vibemix/agent/dj_cohost.py
@src/vibemix/state/evidence_registry.py
@src/vibemix/audio/recorder.py

<interfaces>
# From Plan 18-01:
#   parse_citations(text: str) -> list[tuple[str, str]]
#     # returns per-source pairs from a citation-bearing text; empty list
#     # if no citations found. NEVER raises.
#   class EvidenceRegistry:
#     # existing methods from Plan 18-01 + 18-02
#
# From Plan 18-03:
#   DJCoHostAgent.__init__(..., evidence_registry: EvidenceRegistry | None = None)
#   DJCoHostAgent.llm_node — currently logs events at the post-stream
#     suppression branch (silence_short_circuit / slop_suppressed / ai_text)
#     via self._recorder.log_event(...). Plan 18-04 adds ONE citation_count
#     event per turn, BEFORE the suppression branch (so suppressed turns
#     also get counted — the corpus signal for P20 readiness needs the
#     true Gemini emission rate, not the post-suppression rate).
#
# events.jsonl line shape (CONTEXT.md §Telemetry):
#   {"kind": "citation_count", "count": <int>, "response_id": <str>, "t": <float>}
#   - response_id: existing invoke_n + invoke_ts pattern from llm_node
#                  (e.g. "0042_153012") so the line cross-references the
#                  per-invocation dump folder.
#   - t: seconds since session start (matches recorder.log_event convention —
#        VoiceRecorder.log_event already injects `t` automatically as
#        `time.time() - self.start_time`; the plan code passes
#        kind="citation_count" + kwargs only).
#
# Rolling buffer:
#   collections.deque with maxlen=50 — last 50 turn citation counts.
#   citation_telemetry() returns:
#     {"window_size": <int 0..50>, "mean": <float>, "total_turns_observed": <int>}
#   Phase 16 ear-test reads `mean` to track P20 readiness; CONTEXT.md
#   §Telemetry says "rolling 50-response average computed in
#   AICoach.on_response_complete" but the actual call site is
#   DJCoHostAgent.llm_node post-stream (AICoach is a static-method
#   namespace, no instance state). We add the rolling buffer to
#   EvidenceRegistry instead — keeps state centralized and lets Phase 20's
#   linter read the same buffer for the stripped_rate guard pre-wire.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 [BLOCKING]: Extend EvidenceRegistry with rolling citation-count buffer + snapshot API</name>
  <files>src/vibemix/state/evidence_registry.py, tests/state/test_evidence_registry.py</files>
  <behavior>
    - Test Z (record_citation_count basic — GROUND-02 telemetry): registry.record_citation_count(3) then registry.citation_telemetry() returns {"window_size": 1, "mean": 3.0, "total_turns_observed": 1}.
    - Test AA (rolling window 50 — D-LOCKED): record 75 counts (varied integers); citation_telemetry() returns window_size=50, total_turns_observed=75, mean = average of LAST 50 counts.
    - Test AB (zero counts handled): registry.record_citation_count(0); telemetry mean is 0.0 (not NaN, not None). Empty buffer (no calls yet): citation_telemetry() returns {"window_size": 0, "mean": 0.0, "total_turns_observed": 0}.
    - Test AC (thread-safe across record + snapshot): 8 threads each calling record_citation_count(1) 100 times concurrently; total_turns_observed == 800, mean == 1.0 (no torn writes, no lost updates). Same lock contract as Plan 18-01 Test 3.
    - Test AD (clear() resets the buffer): registry.record_citation_count(5); registry.clear(); citation_telemetry() returns the empty-buffer shape. Locks per-session reset semantic.
  </behavior>
  <action>
    Edit `src/vibemix/state/evidence_registry.py`:

    1. In `EvidenceRegistry.__init__`:
       - Add `self._citation_buffer: collections.deque[int] = collections.deque(maxlen=50)`
       - Add `self._total_turns: int = 0`
       Import collections at module top.

    2. Add `record_citation_count(self, n: int) -> None`:
       - Acquire self._lock (the same lock that guards the source dict —
         single-lock contract preserved per Plan 18-01).
       - Append n to self._citation_buffer (deque maxlen=50 auto-evicts
         oldest).
       - Increment self._total_turns by 1.

    3. Add `citation_telemetry(self) -> dict[str, int | float]`:
       - Acquire self._lock.
       - Return {"window_size": len(self._citation_buffer),
                 "mean": (sum(self._citation_buffer) / len(self._citation_buffer))
                          if self._citation_buffer else 0.0,
                 "total_turns_observed": self._total_turns}
       - Mean is a Python float; no rounding here (Phase 16 / P20 own display
         rounding).

    4. Update existing `clear(self)` (added in Plan 18-01) to ALSO reset
       _citation_buffer and _total_turns. Existing Plan 18-01 Test 3
       (snapshot frozen) and any clear() test stay green; add Test AD as
       a new explicit assertion.

    5. Re-export `record_citation_count` and `citation_telemetry` are
       methods of EvidenceRegistry (already exported via the class). No
       __all__ change needed.

    6. Add Tests Z–AD to tests/state/test_evidence_registry.py. Use
       collections.deque maxlen=50 implicitly via the assertions (Test AA
       drives 75 records).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/pytest tests/state/test_evidence_registry.py -x 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All existing test_evidence_registry.py tests still green + 5 new tests Z-AD green. Rolling buffer maxlen=50 + thread-safe + clear() resets it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [BLOCKING]: Wire DJCoHostAgent.llm_node to count citations + emit events.jsonl line + update rolling buffer</name>
  <files>src/vibemix/agent/dj_cohost.py, tests/agent/test_dj_cohost.py</files>
  <behavior>
    - Test AE (citation_count event written for every turn — ROADMAP success #4): drive llm_node with a mocked Gemini stream that returns "great drop [ev:KICK_SWAP@45.2] [aud:bpm@45.2]"; assert recorder.log_event was called with kind="citation_count", count=2, response_id matching the invoke_n+invoke_ts pattern. The recorder.log_event auto-injects `t`.
    - Test AF (citation_count fires even for suppressed turns — corpus signal integrity): drive llm_node with a Gemini stream returning "<silence/> [ev:HEARTBEAT@30.0]"; even though the turn is suppressed (silence_short_circuit), the citation_count event is still written with count=1. Rationale: Phase 16 needs to see Gemini's emission rate, not the post-suppression rate.
    - Test AG (zero citations handled): mocked stream returns "great drop"; citation_count event written with count=0.
    - Test AH (registry.record_citation_count called per turn): same scenario as AE; assert registry.citation_telemetry()["total_turns_observed"] increments by 1 per llm_node call. Pre-call: 0; after one call: 1; after two calls: 2.
    - Test AI (telemetry path is best-effort — never raises into LLM path): mock parse_citations to raise (simulate corrupt regex state); llm_node MUST still complete, yield chunks, and write the standard ai_text event. The citation_count event may be missing or count=0 — but no exception escapes.
    - Test AJ (None registry path skips record_citation_count silently — backward-compat): DJCoHostAgent constructed without evidence_registry; llm_node still writes citation_count event to recorder (events.jsonl line is recorder-side, not registry-side); registry-side rolling buffer is simply not updated.
  </behavior>
  <action>
    Edit `src/vibemix/agent/dj_cohost.py`:

    1. Import parse_citations at module top:
       `from vibemix.state import parse_citations` (already exported via
       Plan 18-01 Task 3). Also import EvidenceRegistry if not already
       (Plan 18-03 Task 2 added it).

    2. In llm_node, AFTER the streaming loop completes (after line 256
       `print()` and the `stripped = full_text.strip()` line) and BEFORE
       the silence + slop gate (around line 260), add the telemetry
       block:

       # Plan 18-04: count citations in the FULL response text BEFORE
       # the suppression gate. Even silence/slop suppressed turns get
       # their emissions counted — Phase 16 needs Gemini's true emission
       # rate, not the post-suppression rate.
       try:
           citation_pairs = parse_citations(full_text)
           citation_count = len(citation_pairs)
       except Exception:
           citation_count = 0

       response_id = f"{invoke_n:04d}_{invoke_ts}"
       try:
           self._recorder.log_event(
               "citation_count",
               count=citation_count,
               response_id=response_id,
           )
       except Exception:
           pass  # best-effort; recorder write failure must not block LLM path

       if self._registry is not None:
           try:
               self._registry.record_citation_count(citation_count)
           except Exception:
               pass

    3. Position the block BEFORE the suppression branches (silence /
       slop / clean) so all three paths emit telemetry. Specifically:
       insert after line 258 (`stripped = full_text.strip()`) and BEFORE
       line 260 (`# ---- Silence + slop gate (Phase 10) ----`).

    4. The response_id format `f"{invoke_n:04d}_{invoke_ts}"` matches
       the per-invocation dump folder name pattern at line 175
       (`f"{invoke_n:04d}_{invoke_ts}_{ev.type if ev else 'MANUAL'}"`),
       making cross-reference between events.jsonl + the per-invocation
       dump folder trivial. Document this in a code comment.

    5. The events.jsonl line `t` field is auto-injected by
       VoiceRecorder.log_event (recorder.py:303) — DO NOT pass `t`
       manually; the recorder's own `t = time.time() - self.start_time`
       is authoritative and matches CONTEXT.md §Telemetry's t-since-
       session-start convention.

    6. Add Tests AE–AJ to tests/agent/test_dj_cohost.py. Use existing
       fixtures + monkeypatch + spy. Tests AE/AG mock the Gemini stream
       via the genai.Client fixture; AI/AJ test the failure-mode and
       None-registry paths via real EvidenceRegistry instance + parse
       failure injection.

    7. Sanity check the SUMMARY artifact in 18-03-SUMMARY.md (created
       last Wave) for the documented "where to count Gemini's emitted
       citations" hand-off — confirm this plan implements that hand-off
       exactly. If 18-03-SUMMARY.md isn't yet present at execute time
       (Wave 4 should run after Wave 3 SUMMARY exists), do not block —
       the contract is the same: post-stream, pre-suppression.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/pytest tests/agent/test_dj_cohost.py -x 2>&amp;1 | tail -8</automated>
  </verify>
  <done>All existing test_dj_cohost.py tests green + 6 new tests AE-AJ green. citation_count events.jsonl line written per turn (including suppressed turns). EvidenceRegistry rolling buffer updated per turn. Best-effort guarantee verified — telemetry exceptions never escape into the LLM response path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 [NORMAL]: End-of-phase integration smoke test — Phase 16 readiness signal pull</name>
  <files>tests/agent/test_dj_cohost.py</files>
  <behavior>
    - Test AK (P16 readiness signal end-to-end — ROADMAP success #4): construct full stack (registry + EventDetector(registry) + DJCoHostAgent(registry)). Drive 10 mock LLM turns with varying citation counts ([3, 0, 2, 1, 4, 0, 2, 5, 1, 3] = 21 total / 10 turns = 2.1 mean). After all turns, registry.citation_telemetry() returns {window_size: 10, mean: 2.1, total_turns_observed: 10}. This is the EXACT signal Phase 16 ear-test will consume to gate P20 readiness.
  </behavior>
  <action>
    Append Test AK to tests/agent/test_dj_cohost.py. Use a parametrized
    mock-Gemini-stream fixture that yields 10 different citation-bearing
    response strings. Assert the rolling-average signal end-to-end.

    No new source-file edits — pure integration smoke locking the Phase 16
    consumption contract.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/pytest tests/agent/test_dj_cohost.py -k "readiness or citation_telemetry or AK" -x 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Test AK passes. End-to-end Phase 16 readiness signal contract locked: registry.citation_telemetry() returns the rolling-50-turn citation-count average that Phase 16 ear-test queries to gate Phase 20 enforcement readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini response text → parse_citations | Untrusted LLM output; parser is regex-anchored and never raises (Plan 18-01 contract). |
| llm_node telemetry path → events.jsonl | Outbound disk write; best-effort wrapped in try/except per VoiceRecorder pattern. |
| EvidenceRegistry record_citation_count → in-process state | Lock-protected; same threading.Lock contract as source-dict writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | DoS | parse_citations on adversarial Gemini output (e.g., 10MB of nested brackets) | mitigate | Plan 18-01 regex uses `[^\s\]]+` (bounded by close-bracket); worst-case linear in input length. Gemini max_output_tokens=220 (dj_cohost.py:145) caps input size. Test AI gates the never-raise contract. |
| T-18-04-02 | DoS | Rolling buffer growth | mitigate | deque(maxlen=50) — bounded memory. |
| T-18-04-03 | Tampering | Telemetry exception breaking LLM response stream | mitigate | All telemetry calls wrapped in try/except: pass. Test AI is the gate. |
| T-18-04-04 | Repudiation | citation_count line missing for some turns due to recorder write failure | accept | Best-effort matches the v4 recorder pattern (cohost_v4.py:823-836). The dump folder still contains response.txt with the raw response — Phase 16 can recompute counts offline if needed. |
| T-18-04-05 | Information Disclosure | response_id format leaks invoke counter externally | accept | response_id is local to events.jsonl + per-invocation dump folder; both are session-private (recorder dirs are 0o700). No cross-session correlation risk. |
</threat_model>

<verification>
## Plan-Level Checks

- `cd /Users/ozai/projects/dj-set-ai && .venv/bin/pytest tests/state/test_evidence_registry.py tests/agent/test_dj_cohost.py -x` → all green
- `cd /Users/ozai/projects/dj-set-ai && .venv/bin/pytest tests/ -x` → no regression elsewhere (full suite)
- POC files untouched: `git status --short cohost.py cohost_v2.py cohost_lk.py cohost_v4.py` → empty
- Hand-off contract intact: `python -c "from vibemix.state import EvidenceRegistry; r = EvidenceRegistry(); r.record_citation_count(3); r.record_citation_count(5); print(r.citation_telemetry())"` → `{'window_size': 2, 'mean': 4.0, 'total_turns_observed': 2}`
- ROADMAP success criterion #4 closed: events.jsonl receives `citation_count` lines per AI turn (verified via Test AE).
</verification>

<success_criteria>
- 5 new test_evidence_registry.py tests + 6 new test_dj_cohost.py tests + 1 integration smoke = 12 new green tests
- All existing tests green
- citation_count events.jsonl line shape matches CONTEXT.md §Telemetry exactly: `{"kind": "citation_count", "count": <int>, "response_id": <str>, "t": <float>}`
- Rolling 50-turn average exposed via EvidenceRegistry.citation_telemetry()
- Telemetry fires for ALL turns including silence/slop suppressed (corpus signal integrity for Phase 16)
- Best-effort guarantee verified: telemetry exceptions never escape into LLM response path
- POC files untouched
</success_criteria>

<output>
After completion, create `.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-04-SUMMARY.md` capturing:
- Final events.jsonl line shape sample (real example from a synthetic test run)
- Final EvidenceRegistry.citation_telemetry() return shape + sample value
- Phase 16 ear-test consumption pattern: how to read the rolling avg during a live session
- Phase 20 hand-off: the SAME registry now holds (a) the source-dict for has() lookups AND (b) the rolling buffer for stripped_rate guard pre-wire — single state object, single lock, single import path
- Confirmation that ROADMAP success criterion #4 is fully closed
</output>
