---
phase: 18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only
plan: 03
type: execute
wave: 3
depends_on:
  - "18-01"
  - "18-02"
files_modified:
  - src/vibemix/prompts/matrix.py
  - src/vibemix/agent/dj_cohost.py
  - tests/prompts/test_matrix.py
  - tests/agent/test_dj_cohost.py
autonomous: true
requirements:
  - GROUND-02
  - GROUND-03
user_setup: []
must_haves:
  truths:
    - "Citation grammar EBNF block is present in EVERY built system instruction (all 6 prompt cells: HYPE_BEGINNER/INTERMEDIATE/PRO + COACH_BEGINNER/INTERMEDIATE/PRO)"
    - "EBNF block lists all 7 source forms from CONTEXT.md + multi-citation form + concrete examples Gemini can pattern-match against"
    - "EBNF block is appended via build_system_instruction (single source-of-truth) — NOT pasted into each cell verbatim"
    - "v1.0 prompt-only seeding language is explicit: 'cites are encouraged, not required' — Gemini fails open in v1.0 (no enforcement = no penalty for missing cites)"
    - "DJCoHostAgent.llm_node calls AICoach.build_prompt with the live registry_snapshot so the evidence_corpus footer primes Gemini per turn"
    - "HYPE_INTERMEDIATE byte-identical-to-v4 invariant: the v4-port BODY is unchanged; the EBNF block is appended AFTER the v4 body via build_system_instruction (preserves v4 golden test) — gated via a feature flag if the existing v4 golden test asserts byte-equality on the full output"
  artifacts:
    - path: "src/vibemix/prompts/matrix.py"
      provides: "build_system_instruction returns body + appended EBNF block; new constant CITATION_GRAMMAR_BLOCK"
      contains: "CITATION_GRAMMAR_BLOCK"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "DJCoHostAgent.__init__ accepts evidence_registry; llm_node passes registry_snapshot through to build_prompt"
      contains: "evidence_registry"
    - path: "tests/prompts/test_matrix.py"
      provides: "Asserts EBNF block present in all 6 cells + 7 source forms + multi-citation form + 'encouraged not required' phrasing"
    - path: "tests/agent/test_dj_cohost.py"
      provides: "Asserts llm_node calls build_prompt with registry_snapshot when registry is wired"
  key_links:
    - from: "src/vibemix/prompts/matrix.py"
      to: "src/vibemix/state/evidence_registry.py"
      via: "CITATION_GRAMMAR_BLOCK references the same 7 EBNF source forms as EVIDENCE_SOURCES (cross-validated in tests)"
      pattern: "CITATION_GRAMMAR_BLOCK"
    - from: "src/vibemix/agent/dj_cohost.py"
      to: "src/vibemix/state/coach.py"
      via: "AICoach.build_prompt(ev, registry_snapshot=registry.snapshot())"
      pattern: "registry_snapshot"
---

<objective>
Bake the citation grammar EBNF into Gemini's system instruction (GROUND-03 — v1.0 prompt-only seeding) and wire `DJCoHostAgent.llm_node` to pass the live evidence-corpus snapshot into `AICoach.build_prompt`. Closes the production-corpus seeding loop: Gemini SEES the grammar in the prompt, SEES the per-turn evidence_corpus footer (Plan 18-02), and learns to emit citations in shape — Phase 16 ear-test then consumes the citation_count rolling average (Plan 18-04) as the P20 enforcement readiness signal.

Purpose: Teach Gemini the citation grammar without enforcing it. Gemini's emissions become the corpus that P20 will validate against EvidenceRegistry. v1.0 fails open — un-cited responses are NOT penalized; Plan 20 (next milestone phase) lands the linter + ack-bank fallback.

Output: Single shared CITATION_GRAMMAR_BLOCK constant appended to every cell via the build_system_instruction dispatcher; DJCoHostAgent threads the live registry through every llm_node invocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-CONTEXT.md
@.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-01-PLAN.md
@.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-02-PLAN.md

# Existing source — must read before editing.
@src/vibemix/prompts/matrix.py
@src/vibemix/agent/dj_cohost.py
@src/vibemix/state/coach.py
@src/vibemix/state/evidence_registry.py

<interfaces>
# From Plan 18-01:
#   EVIDENCE_SOURCES: frozenset = {"ev", "aud", "midi", "track", "screen", "mix", "tend"}
#
# From Plan 18-02:
#   AICoach.build_prompt(ev, registry_snapshot=None) -> str
#   AICoach.evidence_line(state, registry_snapshot=None) -> str
#   EventDetector(evidence_registry=None)
#   state_refresh_loop(..., evidence_registry=None)
#
# DJCoHostAgent.llm_node currently calls (line 162):
#   text_prompt = AICoach.build_prompt(ev)
# Plan 18-03 changes to:
#   text_prompt = AICoach.build_prompt(
#       ev,
#       registry_snapshot=self._registry.snapshot() if self._registry else None,
#   )
#
# build_system_instruction current signature (matrix.py:377):
#   def build_system_instruction(skill, mode, mood) -> str
# Returns the cell BODY only (no grammar block today).
# Plan 18-03 changes to: return body + "\n\n" + CITATION_GRAMMAR_BLOCK
# (when a new opt-in flag is set; see Task 1 action for the v4-byte-identity
# preservation strategy.)
#
# v4-byte-identity gate: the test in tests/prompts/test_matrix.py (or
# similar) asserts HYPE_INTERMEDIATE === v4 SYSTEM_INSTRUCTION verbatim.
# That test exists per Phase 4 invariant. Confirm + handle: the EBNF
# block appended at the END of the system instruction is OUTSIDE the
# v4-port boundary, so the existing test should compare against the
# CELL BODY (HYPE_INTERMEDIATE constant), not against the dispatcher
# output build_system_instruction(). Verify the test signature; if it
# asserts on dispatcher output, gate the EBNF block append behind a
# kwarg `include_citation_grammar: bool = True` (default True for new
# code paths; the v4 golden test passes False).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 [BLOCKING]: Add CITATION_GRAMMAR_BLOCK constant + opt-in append in build_system_instruction</name>
  <files>src/vibemix/prompts/matrix.py, tests/prompts/test_matrix.py</files>
  <behavior>
    - Test O (CITATION_GRAMMAR_BLOCK content — GROUND-02): the constant string contains all 7 EBNF source forms (literal substrings: `[ev:`, `[aud:`, `[midi:`, `[track:`, `[screen:`, `[mix:`, `[tend:`) AND the multi-citation form `[ev:KICK_SWAP@45.2,aud:bpm@45.0]` (literal example) AND the phrase "encouraged, not required" (locks the v1.0 fail-open semantic).
    - Test P (block appended for all 6 cells — GROUND-03): build_system_instruction(skill, mode, mood) for each of the 6 (skill, mode) cells × default mood returns a string CONTAINING CITATION_GRAMMAR_BLOCK. Default kwarg behavior is "include EBNF block".
    - Test Q (v4 byte-identity preserved): the HYPE_INTERMEDIATE constant string (NOT build_system_instruction output) is byte-identical to the existing v4-port — search for any current test asserting on the constant directly; assert it stays unchanged. If existing test asserts on build_system_instruction("intermediate", "hype", "hype-man") output, add an opt-out kwarg `include_citation_grammar: bool = True` and have the v4 golden test pass `include_citation_grammar=False`.
    - Test R (cross-validation against EVIDENCE_SOURCES — D-LOCKED): for each source in EVIDENCE_SOURCES from Plan 18-01, the literal `[<source>:` substring appears in CITATION_GRAMMAR_BLOCK. Locks the contract that the prompt grammar matches the registry's accepted source vocabulary.
    - Test S (block has explicit "v1.0 = no enforcement" wording — D-LOCKED v1.0 scope): the substring "encouraged" or "no penalty" appears so Gemini does not over-cite (which would be its own slop class).
    - Test T (block contains explicit example for each event TYPE the v1.0 EventDetector fires): substrings include `KAAN_SPOKE`, `MANUAL`, `TRACK_CHANGE`, `PHASE`, `LAYER_ARRIVAL`, `MIX_MOVE`, `HEARTBEAT`. (P17 will add KICK_SWAP / SUB_LAYER_ARRIVAL / etc. to the registry — the prompt block does NOT need to enumerate those in v1.0 because the regex is permissive on event TYPE; once P17 ships, a future plan can extend examples.)
  </behavior>
  <action>
    Edit `src/vibemix/prompts/matrix.py`:

    1. Add a new module-level constant CITATION_GRAMMAR_BLOCK as a triple-
       quoted string. Content (the EBNF block — write directly, do not use
       f-strings unless interpolating EVIDENCE_SOURCES):

       --- CITATION GRAMMAR (v1.0 — encouraged, not required) ---

       When you reference a specific event, audio feature, controller move,
       track, screen element, mix-state, or user-profile fact, attach a
       grounded citation in this exact bracket form. Cites help the human
       team verify what you heard; they're encouraged, not required, and
       there is NO penalty for omitting them in v1.0.

       Forms (each is a single citation; the linter accepts any of these):
         [ev:<TYPE>@<t>]     event citation, e.g. [ev:KICK_SWAP@45.2]
         [aud:<key>@<t>]     audio feature, e.g. [aud:bpm@45.2] or [aud:rms@45.2]
         [midi:<event>@<t>]  controller event, e.g. [midi:cue_a@12.7]
         [track:<id>]        track reference, e.g. [track:Marlon Hoffstadt - Atlas]
         [screen:<key>]      screen element, e.g. [screen:waveform_deck_a]
         [mix:<derived>]     derived mix-state, e.g. [mix:audible_deck=A]
         [tend:<fact>]       user-profile fact, e.g. [tend:user_likes_acid]

       Multi-citation (comma-separated, no whitespace inside brackets):
         [ev:KICK_SWAP@45.2,aud:bpm@45.0,midi:fader_a_up@45.1]

       Event types currently tracked (use exactly these in [ev:<TYPE>] —
       UPPER_SNAKE_CASE):
         KAAN_SPOKE, MANUAL, TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, HEARTBEAT

       Timestamps (`<t>`) are SECONDS SINCE SESSION START, 1-decimal precision
       (e.g. 45.2 means 45.2 seconds into the session — the same clock as
       the evidence_corpus[…] footer). If you don't know the exact timestamp,
       OMIT the cite — never invent one.

       Why this matters: in a future version the cascade will validate every
       cite against the runtime evidence corpus. Cites you emit now are
       seeding that contract — but in v1.0 there is no penalty for missing
       cites, so prefer SILENCE over an invented citation. "Trust the audio,
       cite when you can, stay silent when you can't."

    2. Modify `build_system_instruction` to accept `include_citation_grammar:
       bool = True` kwarg. Inside, after the existing body resolution
       (the `body = body.replace("{mood_persona}", persona)` block for
       coach cells, OR the unchanged HYPE body), do:
         if include_citation_grammar:
             body = body + "\n\n" + CITATION_GRAMMAR_BLOCK
       Return body.

    3. Add CITATION_GRAMMAR_BLOCK to __all__ (or to the module's export
       conventions if there's an explicit list).

    4. Search tests for any existing v4-byte-identity assertion: run
       `grep -rn "HYPE_INTERMEDIATE\|byte" tests/prompts/`. If a test
       calls build_system_instruction and compares to a v4 golden file,
       update that test to pass `include_citation_grammar=False`. If
       the test compares to the HYPE_INTERMEDIATE constant directly,
       no change needed (the constant itself is unchanged — only the
       dispatcher append behavior changed).

    5. Add tests/prompts/test_matrix.py tests O–T per the behavior list.
       Use parametrize for Test P (6 cells) and Test R (7 sources).
       Cross-validate Test R via:
         from vibemix.state import EVIDENCE_SOURCES
         from vibemix.prompts.matrix import CITATION_GRAMMAR_BLOCK
         for source in EVIDENCE_SOURCES:
             assert f"[{source}:" in CITATION_GRAMMAR_BLOCK
       This locks the registry-vocabulary ↔ prompt-vocabulary contract.

    6. v4 byte-identity preservation: HYPE_INTERMEDIATE constant body
       (matrix.py:109-172) MUST be untouched. Confirm via diff: only
       additions are the new CITATION_GRAMMAR_BLOCK constant + the new
       kwarg + the conditional append. The existing v4-port body stays
       byte-for-byte identical.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/pytest tests/prompts/test_matrix.py -x 2>&amp;1 | tail -8</automated>
  </verify>
  <done>All existing test_matrix.py tests green (with include_citation_grammar=False where v4-byte-identity is asserted) + 6 new tests O-T green. CITATION_GRAMMAR_BLOCK exports cleanly. Cross-validation against EVIDENCE_SOURCES confirms registry-prompt contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [BLOCKING]: Wire DJCoHostAgent to thread registry_snapshot through llm_node</name>
  <files>src/vibemix/agent/dj_cohost.py, tests/agent/test_dj_cohost.py</files>
  <behavior>
    - Test U (registry kwarg accepted, default None — backward-compat): DJCoHostAgent(genai_client=mock, ..., evidence_registry=None) constructs cleanly. Existing tests that don't pass evidence_registry continue to pass.
    - Test V (llm_node calls build_prompt with snapshot when registry wired): construct DJCoHostAgent(..., evidence_registry=registry); pre-load registry with `registry.write("ev", "TRACK_CHANGE", 30.0)`; spy on AICoach.build_prompt; trigger llm_node with set_next_event(Event("TRACK_CHANGE", state)); assert build_prompt was called with kwarg registry_snapshot containing the loaded observation.
    - Test W (llm_node calls build_prompt WITHOUT snapshot when registry None — preserves Phase 4 behavior): DJCoHostAgent(..., evidence_registry=None); spy on build_prompt; assert called with registry_snapshot=None (or kwarg absent — implementation choice).
    - Test X (snapshot is taken FRESH per llm_node call — no stale corpus): two consecutive llm_node invocations; between them, write a new observation; the second build_prompt call MUST receive a snapshot containing that new observation. Locks "snapshot per turn" semantic.
  </behavior>
  <action>
    Edit `src/vibemix/agent/dj_cohost.py`:

    1. Add `evidence_registry: EvidenceRegistry | None = None` to
       DJCoHostAgent.__init__ kwargs (alongside the existing kwargs at
       line 107-117). Store on `self._registry`. Import EvidenceRegistry
       at module top: `from vibemix.state import EvidenceRegistry`.

    2. In `llm_node`, change line 162:
         text_prompt = AICoach.build_prompt(ev)
       to:
         snapshot = self._registry.snapshot() if self._registry is not None else None
         text_prompt = AICoach.build_prompt(ev, registry_snapshot=snapshot)
       Mirror the same change at line 165 (the MANUAL fallback path):
         text_prompt = AICoach.build_prompt(
             Event(type="MANUAL", state=self._state, extra={}),
             registry_snapshot=snapshot,
         )

    3. The `_gen_cfg.system_instruction` (line 142) currently uses
       `prompt_body` resolved in __init__ via `_resolve_prompt_cell`.
       That call site uses `build_system_instruction` (transitively).
       After Plan 18-03 Task 1 ships, `_resolve_prompt_cell` returns
       a body that ALREADY contains CITATION_GRAMMAR_BLOCK (default
       include_citation_grammar=True). Verify by adding Test U
       extension: assert `agent._gen_cfg.system_instruction.find("[ev:") != -1`
       — proves the grammar block is in the system instruction Gemini sees.

    4. Update tests/agent/test_dj_cohost.py: append Tests U-X. Use
       unittest.mock.patch to spy on AICoach.build_prompt; the existing
       test file likely already has DJCoHostAgent fixtures — reuse
       them. Construct a real EvidenceRegistry for Test V/X (no need
       to mock it — Plan 18-01's threading.Lock implementation is
       safe in tests).

    5. Honor "trust the audio" / no-screen rule: do NOT add screen_jpeg
       or any other multimodal Part in this plan. The single-modality
       audio Part contract from cohost_v4 is preserved. The grammar
       block is TEXT only — appended to the system instruction.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/pytest tests/agent/test_dj_cohost.py -x 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All existing test_dj_cohost.py tests green + 4 new tests U-X green. DJCoHostAgent accepts evidence_registry kwarg; llm_node calls build_prompt with fresh snapshot per turn; system_instruction Gemini sees contains [ev: literal substring (proves grammar block reached the LLM).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 [NORMAL]: Cross-package smoke test — full prompt path with grammar block + corpus footer</name>
  <files>tests/agent/test_dj_cohost.py</files>
  <behavior>
    - Test Y (end-to-end smoke — GROUND-02 + GROUND-03): construct EvidenceRegistry, EventDetector(evidence_registry=registry), DJCoHostAgent(..., evidence_registry=registry). Drive a synthetic state through detect() to fire a TRACK_CHANGE event (writes [ev:TRACK_CHANGE@t] to registry). Call llm_node with the fired event; intercept the `contents[0]` string passed to genai.aio.models.generate_content_stream (mock the genai client). Assert the prompt text contains:
        a) "evidence_corpus[ev=" (Plan 18-02 footer present — proves snapshot threaded through)
        b) "[ev:TRACK_CHANGE" or similar (registry observation visible in the corpus footer)
      AND the system_instruction passed via _gen_cfg contains:
        c) The CITATION_GRAMMAR_BLOCK content (via "encouraged, not required" substring check)

    This locks the entire Plan 18 prompt-side wiring in one assertion.
  </behavior>
  <action>
    Append Test Y to tests/agent/test_dj_cohost.py. Mock genai.Client (or
    use the existing fixture) so the actual API call is bypassed but the
    `contents` argument is captured. The test exercises:
    - Plan 18-01 (registry instantiation)
    - Plan 18-02 (EventDetector._fire writes ev observation; AICoach
      threads snapshot into evidence_line corpus footer)
    - Plan 18-03 (build_system_instruction appends CITATION_GRAMMAR_BLOCK;
      DJCoHostAgent.llm_node passes snapshot through)

    No new source-file edits in this task — pure cross-package smoke.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/pytest tests/agent/test_dj_cohost.py -k "test_full_prompt_path or smoke or evidence_corpus" -x 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Test Y passes. Locks the full v1.0 prompt-only seeding contract in a single executable assertion: registry → snapshot → corpus footer → prompt text + grammar block → system_instruction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini system_instruction text → Gemini API | Outbound; no untrusted input. Grammar block is fixed text, no user input interpolated. |
| Gemini response text → Plan 18-04 telemetry | Untrusted LLM output; parse_citations (from Plan 18-01) is the safe parser boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Tampering | CITATION_GRAMMAR_BLOCK accepts user input via {placeholder} interpolation | mitigate | Block is a constant string with NO interpolation. The MOOD_PERSONAS pattern (matrix.py:50-63) is the model — fixed dict, never user-derived. Test O literal-substring checks lock the content. |
| T-18-03-02 | Spoofing | Grammar drift between cells (one cell omits a source) | mitigate | build_system_instruction appends the SAME constant to every cell — single source of truth. Test P parametrized over 6 cells is the gate. |
| T-18-03-03 | Information Disclosure | registry.snapshot() exposed in prompt could leak Kaan's session arc | accept | The snapshot footer contains COUNTS only (`[ev=N,aud=M,mix=K]`), not the actual timestamps or values. Plan 18-02 already enforces this. The full prompt body ALREADY contains rich session evidence (cohost_v4 evidence_line); the corpus footer adds only counts on top. |
| T-18-03-04 | Repudiation | Gemini emits an [ev:fake@99] cite that doesn't map to a real observation | accept | v1.0 prompt-only seeding — no enforcement; this is exactly the Pitfall P12 scenario CONTEXT.md §"Pitfall prevention" says ships v1.0 without enforcement. Plan 20 (next phase) closes via CitationLinter.has() lookup. |
| T-18-03-05 | Tampering | EBNF grammar block changes prompt token budget (cache disruption — Pitfall P11) | mitigate | Block is ~800 tokens (rough estimate). Phase 19 cached_content (LATENCY-02) MUST account for the v1.0 grammar block in cache-content sizing — flagged as a hand-off note in 18-03-SUMMARY.md to alert Plan 19 owner. v1.0 itself doesn't ship caching. |
</threat_model>

<verification>
## Plan-Level Checks

- `cd /Users/ozai/projects/dj-set-ai && .venv/bin/pytest tests/prompts/test_matrix.py tests/agent/test_dj_cohost.py -x` → all green
- `cd /Users/ozai/projects/dj-set-ai && .venv/bin/pytest tests/ -x` → no regression elsewhere
- HYPE_INTERMEDIATE constant unchanged: `git diff src/vibemix/prompts/matrix.py | grep -A 5 "HYPE_INTERMEDIATE: str ="` shows no edits inside the v4-port body lines (109-172)
- POC files untouched
- Cross-package contract: `python -c "from vibemix.state import EVIDENCE_SOURCES; from vibemix.prompts.matrix import CITATION_GRAMMAR_BLOCK; print(all(f'[{s}:' in CITATION_GRAMMAR_BLOCK for s in EVIDENCE_SOURCES))"` → True
</verification>

<success_criteria>
- 6 new test_matrix.py tests + 4 new test_dj_cohost.py tests + 1 cross-package smoke test = 11 new green tests
- All existing tests green; HYPE_INTERMEDIATE byte-identical-to-v4 invariant preserved
- CITATION_GRAMMAR_BLOCK contains all 7 EBNF source forms + multi-citation form + "encouraged, not required" v1.0 fail-open language + 7 v1.0 event TYPE enumeration
- build_system_instruction(include_citation_grammar=True) (default) appends block; v4 golden test path uses include_citation_grammar=False
- DJCoHostAgent.llm_node threads fresh registry.snapshot() into AICoach.build_prompt per turn
- POC files untouched
- Hand-off note in SUMMARY for Plan 19 (cached_content sizing must include grammar block tokens)
</success_criteria>

<output>
After completion, create `.planning/phases/18-evidence-registry-citation-grammar-in-prompts-v10-prompt-only/18-03-SUMMARY.md` capturing:
- Final CITATION_GRAMMAR_BLOCK content (or excerpt) + token-count estimate (for Plan 19 cache sizing)
- Confirmation HYPE_INTERMEDIATE v4-byte-identity preserved
- DJCoHostAgent constructor signature delta (evidence_registry kwarg added)
- Cross-package contract test passing (registry sources ↔ prompt grammar)
- Hand-off to Plan 18-04: where to count Gemini's emitted citations (in llm_node, after streaming completes — use parse_citations from Plan 18-01)
</output>
