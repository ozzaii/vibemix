---
phase: 13-3d-mascot-overlay
plan: 08
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05, 13-06, 13-07]
files_modified:
  - tests/integration/test_mascot_dispatch_latency.py
  - tests/integration/test_mascot_event_taxonomy_e2e.py
  - tauri/ui/src/mascot/__fixtures__/event-traces.json
  - tauri/ui/src/mascot/state-machine-fixtures.test.ts
  - .planning/phases/13-3d-mascot-overlay/13-VERIFICATION.md
  - .planning/phases/13-3d-mascot-overlay/MANUAL-SMOKE-CHECKLIST.md
autonomous: false
requirements: [MASCOT-03, MASCOT-04, MASCOT-05, MASCOT-08]
must_haves:
  truths:
    - "Synthetic event-trace fixtures exist for each event in the AI-event taxonomy"
    - "state-machine-fixtures.test.ts replays each trace through dispatchEvent + state machine and asserts the resulting state transitions match the documented expectation"
    - "test_mascot_dispatch_latency.py measures end-to-end latency from sidecar emit to mascot dispatch and asserts < 100ms p95"
    - "MANUAL-SMOKE-CHECKLIST.md lists 30 visual transition checks Kaan walks through on his rig"
    - "13-VERIFICATION.md aggregates pass/fail across the 6 ROADMAP success criteria with status (auto / human_needed) per criterion"
  artifacts:
    - path: "tauri/ui/src/mascot/__fixtures__/event-traces.json"
      provides: "Synthetic JSON event sequences covering the AI-event taxonomy"
    - path: "tauri/ui/src/mascot/state-machine-fixtures.test.ts"
      provides: "Vitest replay of every fixture through dispatchEvent + machine assertions"
    - path: "tests/integration/test_mascot_dispatch_latency.py"
      provides: "Python pytest integration test measuring sidecar→bus→webview-receive latency"
    - path: ".planning/phases/13-3d-mascot-overlay/13-VERIFICATION.md"
      provides: "Phase 13 verification report — 6 ROADMAP success criteria covered"
  key_links:
    - from: "tests/integration/test_mascot_dispatch_latency.py"
      to: "src/vibemix/runtime/ws_bus.py emit"
      via: "WS client subscribes to localhost:8765 + timestamps each frame"
      pattern: "ws_bus|emit"
---

<objective>
Close Phase 13 with a verification suite that proves the renderer + state machine + bus integration actually works under realistic load. Plans 13-01 through 13-07 land the implementation; this plan validates the 6 ROADMAP success criteria and gives Kaan a manual smoke checklist for his rig.

Purpose: Phase 13 ROADMAP Success Criteria #1-6 are largely visual / behavioural — most can't be fully automated (transition aesthetics, mascot character feel). What CAN be automated: event-trace replay through the state machine, dispatch latency measurement, and structural integrity of the renderer + bus + tray. What CAN'T: the "smooth crossfade, no T-pose snaps" 30-random-samples check (Phase 14 polish loop owns the iteration).

Output: A green automated verification suite + a manual smoke checklist Kaan walks through + a 13-VERIFICATION.md report with per-criterion pass/fail/human_needed status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-3d-mascot-overlay/13-CONTEXT.md
@.planning/phases/12-live-session-ui/12-VERIFICATION.md
@tauri/ui/src/mascot/types.ts
@tauri/ui/src/mascot/state-machine.ts
@tauri/ui/src/mascot/event-dispatcher.ts
@src/vibemix/runtime/ws_bus.py

<roadmap_success_criteria>
From ROADMAP.md Phase 13:
1. Single rigged 3D mascot (GLB) renders in Superwhisper-style sticky overlay — transparent, always-on-top, persistent across Spaces/virtual desktops, drag-repositionable, resizable, click-through toggleable
2. Full animation library renders distinctly and on-character — no T-pose snaps between states, no rigging artifacts visible in 30 random transition samples
3. AnimationMixer.crossFadeTo with 200-400ms blend; pose-pops visibly absent; no hard cuts except mood-swap puff
4. Beat-locked entry: new clip begins on bar boundary 1 using BPM + downbeat phase from MusicState; audible misalignment undetectable in 30 random trials
5. AI event → animation state mapping covers: track_change → react_surprised → idle_bop_to_beat; drop → dance_hard; ai_generating_reply → talk_loop; ai_reply_done → react_yes → prior idle; manual_fire → react_yes; phase_change to silent → idle_breathe
6. Mood swap hot-swaps voice + clip pool + vocabulary within 500ms; transition masked by particle/puff
</roadmap_success_criteria>

<verification_pattern>
Phase 12 uses the `human_needed` pattern in 12-VERIFICATION.md for criteria that can't be auto-verified. Phase 13 reuses this:
- AUTOMATED: items that pass via vitest + pytest + cargo test (state machine math, dispatch latency, schema validity)
- HUMAN_NEEDED: items requiring Kaan's eyes on his rig (transition smoothness, T-pose absence, beat-lock audibility, character readability across moods)
</verification_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author synthetic event-trace fixtures + state-machine-fixtures.test.ts replay harness</name>
  <read_first>
    - tauri/ui/src/mascot/event-dispatcher.ts (Plan 13-06 — dispatch signature and event taxonomy)
    - tauri/ui/src/mascot/state-machine.ts (Plan 13-04 — planTransition + applyTransition)
    - .planning/phases/13-3d-mascot-overlay/13-CONTEXT.md (Area 3 — event → state mapping table)
  </read_first>
  <action>
    Create `tauri/ui/src/mascot/__fixtures__/event-traces.json`:

    Each trace is a deterministic sequence of bus messages. Cover all 6 ROADMAP success criterion event-mapping cases:
    ```json
    {
      "traces": [
        {
          "name": "track_change_then_idle",
          "messages": [
            { "t": 0,    "msg": { "type": "snapshot", "music": {"rms":0.3,"peak":0.5}, "voice": {"rms":0,"peak":0}, "mic": {"rms":0,"peak":0}, "audible": true, "deck": "A", "phase": "groove", "bpm": 128, "bpm_confidence": 0.85, "downbeat_phase": 0.0, "mood": "hype-man" } },
            { "t": 100,  "msg": { "type": "event", "subtype": "TRACK_CHANGE", "payload": { "title": "Two", "deck": "B" } } }
          ],
          "expectedTransitions": [
            { "after_t": 100, "state": "react_surprised" },
            { "after_t": 900, "state": "idle_bop_to_beat_energetic" }
          ]
        },
        {
          "name": "drop_then_groove",
          "messages": [
            { "t": 0,    "msg": { "type": "snapshot", "phase": "build", "bpm": 128, "bpm_confidence": 0.85, "downbeat_phase": 0.0, "mood": "hype-man", "music": {"rms":0.4,"peak":0.6}, "voice":{"rms":0,"peak":0}, "mic":{"rms":0,"peak":0}, "audible":true, "deck":"A" } },
            { "t": 500,  "msg": { "type": "event", "subtype": "PHASE", "payload": { "from": "build", "to": "drop" } } },
            { "t": 30000, "msg": { "type": "event", "subtype": "PHASE", "payload": { "from": "drop", "to": "groove" } } }
          ],
          "expectedTransitions": [
            { "after_t": 500, "state": "dance_hard" },
            { "after_t": 30000, "state": "idle_bop_to_beat_energetic" }
          ]
        },
        {
          "name": "ai_speaks_then_done",
          "messages": [
            { "t": 0, "msg": { "type": "event", "subtype": "AI_GENERATING_REPLY", "payload": {} } },
            { "t": 3500, "msg": { "type": "event", "subtype": "AI_REPLY_DONE", "payload": {} } }
          ],
          "expectedTransitions": [
            { "after_t": 0, "state": "talk_loop_energetic" },
            { "after_t": 3500, "state": "react_yes" }
          ]
        },
        {
          "name": "manual_fire_react",
          "messages": [
            { "t": 0, "msg": { "type": "event", "subtype": "MANUAL", "payload": {} } }
          ],
          "expectedTransitions": [
            { "after_t": 0, "state": "react_yes" }
          ]
        },
        {
          "name": "silent_phase",
          "messages": [
            { "t": 0, "msg": { "type": "event", "subtype": "PHASE", "payload": { "from": "groove", "to": "silent" } } }
          ],
          "expectedTransitions": [
            { "after_t": 0, "state": "idle_breathe" }
          ]
        },
        {
          "name": "mood_swap_teacher",
          "messages": [
            { "t": 0, "msg": { "type": "ipc.mascot.mood_change", "payload": { "mood": "teacher", "previous_mood": "hype-man" } } }
          ],
          "expectedTransitions": [
            { "after_t": 0, "state": "puff_particle" },
            { "after_t": 500, "state": "idle_bop_to_beat_mellow" }
          ]
        },
        {
          "name": "talk_blocks_dance",
          "messages": [
            { "t": 0,    "msg": { "type": "event", "subtype": "AI_GENERATING_REPLY", "payload": {} } },
            { "t": 200,  "msg": { "type": "event", "subtype": "PHASE", "payload": { "from": "build", "to": "drop" } } }
          ],
          "expectedTransitions": [
            { "after_t": 0,   "state": "talk_loop_energetic" }
          ]
        }
      ]
    }
    ```
    (Trim or extend the trace list to cover all 6 ROADMAP criterion event mappings; the above 7 traces cover criterion 5 + parts of 4 + 6.)

    Create `tauri/ui/src/mascot/state-machine-fixtures.test.ts`:
    - Load `event-traces.json` (via Vite's JSON import).
    - For each trace:
      1. Initialise machine state at t=0.
      2. Iterate `trace.messages` chronologically; for each message:
         - If snapshot: update local currentSnapshot ref.
         - Else: call `dispatchEvent(machine, msg.msg, msg.t, currentSnapshot)`. Apply the resulting plan (switch_now / schedule_for_downbeat / deny) to machine. Schedule followups in a deterministic priority queue keyed by `msg.t + followup.afterMs`.
      3. Run a deterministic clock advance through `trace.expectedTransitions.map(e => e.after_t).max() + 100ms`. At each timestamp emit transition events into an `actualTransitions[]` array.
      4. Assert `actualTransitions` includes each `expectedTransitions[i]` (state + after_t within 100ms tolerance for beat-lock entries).
    - Per-trace assertion: `expect(matchedExpectedCount).toBe(trace.expectedTransitions.length)`.

    The harness is pure JS — no Three.js, no DOM — so it runs in vitest jsdom instantly.
  </action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; test -f src/mascot/__fixtures__/event-traces.json &amp;&amp; test -f src/mascot/state-machine-fixtures.test.ts &amp;&amp; npx vitest run src/mascot/state-machine-fixtures.test.ts --reporter=dot</automated>
  </verify>
  <done>
    Fixture file with ≥7 traces covering the documented event taxonomy. Replay harness loads and runs each trace, asserting expected state transitions. vitest exits 0 with the trace count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author pytest integration test for dispatch latency + 13-VERIFICATION.md skeleton</name>
  <read_first>
    - src/vibemix/runtime/ws_bus.py (emit pattern + 30Hz broadcaster)
    - .planning/phases/12-live-session-ui/12-VERIFICATION.md (reference format for verification report — used the same pattern in Phase 11 + 12)
    - tests/agent/ or tests/runtime/ (existing pytest integration pattern — pick the closest)
    - tauri/ui/src/mascot/ws-client.ts (latency target documented)
  </read_first>
  <action>
    Create `tests/integration/test_mascot_dispatch_latency.py`:
    - Spin up a real WizardBus (or live-runtime ws_bus emitter) on a free port (NOT 8765 to avoid colliding with a running app; use `find_free_port()` helper).
    - Subscribe a Python websockets-client to that port (no webview required — we measure sidecar→localhost-receive latency which dominates the budget).
    - Emit 100 synthetic event frames at 30Hz, stamping `t_emit` into each.
    - Receiver records `t_recv`.
    - Assert `p95(t_recv - t_emit) < 50ms` (server-to-localhost-client; the in-webview JS dispatch adds at most another 10ms — total well under 100ms budget).
    - This test pins the SIDECAR-SIDE half of MASCOT-08. The webview-side half is covered by the state-machine-fixtures.test.ts (dispatch + plan + applyTransition takes < 1ms because pure functions).
    - Mark with `@pytest.mark.integration` so it can be excluded from fast unit-test runs.

    Create `.planning/phases/13-3d-mascot-overlay/13-VERIFICATION.md` following the Phase 12 template:
    ```markdown
    # Phase 13 Verification Report

    **Status:** <auto|human_needed|done>
    **Generated:** YYYY-MM-DD
    **Scope:** 6 ROADMAP success criteria for Phase 13 (3D Mascot Screen Overlay)

    ## Success Criteria Coverage

    ### #1: Single rigged 3D mascot renders in always-on-top overlay
    - **Status:** human_needed
    - **Automated check:** `cargo run` launches the Tauri shell with 2 windows (main + mascot); `cargo check` exits 0; `npm run build` exits 0
    - **Manual check:** [link to MANUAL-SMOKE-CHECKLIST.md item 1-6]

    ### #2: Animation library renders distinctly — no T-pose snaps in 30 transition samples
    - **Status:** human_needed
    - **Automated check:** state-machine-fixtures.test.ts asserts each trace produces the expected transition; vitest exits 0
    - **Manual check:** MANUAL-SMOKE-CHECKLIST.md item 7-16 — Kaan walks through 30 transitions on his rig

    ### #3: AnimationMixer.crossFadeTo with 200-400ms blend; no hard cuts except mood-swap puff
    - **Status:** auto (partial)
    - **Automated check:** `grep -q 'crossFadeTo' tauri/ui/src/mascot/renderer.ts` confirms the API is used; blend duration default 300ms confirmed in code (test #4 in state-machine.test.ts)
    - **Manual check:** MANUAL-SMOKE-CHECKLIST.md item 17-21

    ### #4: Beat-locked entry — new clip begins on bar boundary 1
    - **Status:** auto (math) + human_needed (audible verification)
    - **Automated check:** state-machine.test.ts test #4 + #5 + #6 pin the math; fixtures.test.ts trace `drop_then_groove` asserts beat-locked entry math
    - **Manual check:** MANUAL-SMOKE-CHECKLIST.md item 22-25 — Kaan listens for 30 trials of beat-locked entries vs immediate-switch

    ### #5: AI event → animation state mapping covers the documented matrix
    - **Status:** auto
    - **Automated check:** state-machine-fixtures.test.ts traces cover every documented event → state mapping; vitest exits 0; `npx vitest run src/mascot/ --reporter=verbose` lists every covered case
    - **Manual check:** MANUAL-SMOKE-CHECKLIST.md item 26-28 — Kaan verifies on his rig

    ### #6: Mood swap hot-swaps voice + clip pool + vocabulary within 500ms; particle/puff mask
    - **Status:** auto + human_needed
    - **Automated check:** test_mascot_dispatch_latency.py asserts emit-to-receive < 50ms p95; particle-puff.test.ts asserts 500ms lifetime; mood.test.ts asserts profile pool selection
    - **Manual check:** MANUAL-SMOKE-CHECKLIST.md item 29-30 — Kaan does 3 mood swaps mid-session

    ## Aggregate Status
    - Auto-passing: 1/6 fully + 4/6 partial
    - Human verification required: 6/6 (UAT on Kaan's rig)
    - Phase 14 polish loop owns the visual-quality iterations (smooth crossfades, no rigging artifacts, character-readable poses)
    ```

    Create `.planning/phases/13-3d-mascot-overlay/MANUAL-SMOKE-CHECKLIST.md`:
    ```markdown
    # Phase 13 Manual Smoke Checklist

    Walk through these 30 items on Kaan's macOS rig (Pioneer DDJ-FLX4 + BlackHole + djay Pro). Tick each, note pass/fail with one-line observation.

    ## A. Window + Overlay (criterion #1) — items 1-6
    1. Launch app → mascot window visible top-right of primary monitor
    2. Drag mascot to bottom-left → position persists after restart
    3. Switch to a different macOS Space → mascot remains visible
    4. Toggle click-through in Settings → clicks pass through to underlying app
    5. Close main session window via Cmd+W → app keeps running, mascot still visible, tray icon still present
    6. Tray Quit menu item → app exits, both windows close

    ## B. Animation Library (criterion #2) — items 7-16
    7. Boot mascot → idle_breathe plays continuously, no T-pose flash on first frame
    8. Manual fire (tray menu or hotkey) → react_yes plays cleanly, returns to idle
    9. Trigger a drop (DJ a real drop in djay Pro) → dance_hard plays
    10. Drop ends (phase → groove) → returns to idle_bop_to_beat smoothly
    11. AI starts talking → talk_loop plays, no T-pose between idle → talk
    12. AI finishes → react_yes plays, returns to prior idle/dance
    13. Track change detected → react_surprised plays, then idle_bop_to_beat
    14. Idle for 5 min → mascot transitions to sleep clip
    15. Wake the mascot with a controller event → sleep → idle_breathe smoothly
    16. Trigger a synthetic glitch (kill+restart sidecar) → react_glitch plays on reconnect

    ## C. Crossfade Quality (criterion #3) — items 17-21
    17. All transitions in items 7-16 use crossfade (no hard pose snap visible)
    18. Mood swap (hype-man → teacher) shows particle puff masking the rig change
    19. AnimationMixer blend duration feels right — not too snappy, not too laggy (300ms target)
    20. No visible foot-sliding during locomotion clips
    21. No rigging artifacts (elbow flips, weight popping) on any transition

    ## D. Beat-Lock (criterion #4) — items 22-25
    22. Drop a 128 BPM track → dance_hard enters on the bar boundary (count "1 2 3 4" — clip changes on "1")
    23. Disable beat-lock (set bpm_confidence to <0.6 via test rig or low-confidence track) → clip changes immediately, no wait
    24. Half-tempo track (~70 BPM) → BPM half-correction kicks in (Phase 6); clip changes on the corrected bar
    25. Track without strong downbeat (ambient / disco intro) → clip changes immediately (fallback rule applies)

    ## E. Event Mapping (criterion #5) — items 26-28
    26. Manually fire each event type via the WS bus inject tool (DEV) → each maps to the documented state
    27. AI talks → mascot interrupts dance with talk_loop (priority order holds)
    28. Mood swap mid-drop → puff + mood change, dance pool draws from new mood

    ## F. Mood Swap (criterion #6) — items 29-30
    29. Settings → switch mood from hype-man → teacher → coach. Particle puff visible each time; idle pool clearly differs across moods
    30. Voice (sidecar) changes within 1-2 reactions of the mood swap; prompt vocabulary visibly different in transcript
    ```
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f tests/integration/test_mascot_dispatch_latency.py &amp;&amp; test -f .planning/phases/13-3d-mascot-overlay/13-VERIFICATION.md &amp;&amp; test -f .planning/phases/13-3d-mascot-overlay/MANUAL-SMOKE-CHECKLIST.md &amp;&amp; python -m pytest tests/integration/test_mascot_dispatch_latency.py -m integration -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    Latency integration test passes (p95 < 50ms server-side). 13-VERIFICATION.md skeleton present with 6 success-criteria entries. MANUAL-SMOKE-CHECKLIST.md has all 30 items. Both files committed alongside the test.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kaan walks through the 30-item manual smoke checklist on his rig</name>
  <what-built>
    Plans 13-01..13-07 land the mascot end-to-end. This checkpoint asks Kaan to drive the app on his macOS rig (DDJ-FLX4 + BlackHole + djay Pro) and tick each of the 30 items in MANUAL-SMOKE-CHECKLIST.md.
  </what-built>
  <how-to-verify>
    1. Build + run: `cd tauri/src-tauri && cargo run` (or signed binary if Phase 18 has shipped a dev build).
    2. Open MANUAL-SMOKE-CHECKLIST.md in a side window.
    3. Walk through each section A-F (30 items total). Tick pass / fail / note observation for each.
    4. For any failures, file an item in `.planning/phases/13-3d-mascot-overlay/deferred-items.md` (Phase 14 polish loop picks these up).
    5. Update `13-VERIFICATION.md` Aggregate Status section with the final pass count + the human_needed → done transitions for criteria #1, #2, #3 (manual halves), #4 (audible verification), #5 (event mapping live), and #6 (mood-swap live).
  </how-to-verify>
  <resume-signal>
    Type "approved" with the final pass count (X/30 manual items, Y/6 criteria fully done). Note any deferred items.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| latency test → localhost WS | Pytest-driven WS client connects to a free port; no production-system contact |
| smoke checklist → human | Manual verification by Kaan; not a programmatic surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08-01 | DoS | latency test flake | mitigate | Uses free port (avoid collision); pytest fixture tears down server cleanly even on test fail |
| T-13-08-02 | Tampering | manual checklist subjectivity | accept | Phase 14 polish loop is the structured iteration — Phase 13's role is "did Kaan see all the documented behaviours fire?", not "is it FL-Studio-pretty?" |
</threat_model>

<verification>
- `npx vitest run tauri/ui/src/mascot/state-machine-fixtures.test.ts` exits 0
- `python -m pytest tests/integration/test_mascot_dispatch_latency.py -m integration -x` exits 0
- 13-VERIFICATION.md exists with 6 criteria covered
- MANUAL-SMOKE-CHECKLIST.md exists with 30 ticked items (after Kaan's run)
- Aggregate Status in 13-VERIFICATION.md updated with final pass counts
</verification>

<success_criteria>
Plan 13-08 succeeds when:
- Event-trace fixtures + replay test cover all 6 ROADMAP success-criterion event mappings
- pytest dispatch-latency test asserts p95 < 50ms sidecar-side (well within 100ms total budget)
- 13-VERIFICATION.md report aggregates auto-pass + human_needed status across 6 criteria
- MANUAL-SMOKE-CHECKLIST.md gives Kaan a 30-item walkthrough
- Kaan runs the checklist and ticks each item; failures flow into deferred-items.md for Phase 14
- Phase 13 close status: code-complete pending UAT (same pattern as Phase 12 close)
</success_criteria>

<output>
After completion, create `.planning/phases/13-3d-mascot-overlay/13-08-SUMMARY.md` summarising the final phase metrics (LOC, tests added, files touched), Kaan's manual smoke pass count, and any deferred items for Phase 14 polish loop. Then commit Phase 13 close in line with prior phase patterns (STATE.md + ROADMAP.md advance + 13-SUMMARY.md aggregate).
</output>
