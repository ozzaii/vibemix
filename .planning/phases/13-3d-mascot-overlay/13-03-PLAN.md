---
phase: 13-3d-mascot-overlay
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/ui/src/session/components/cohost.ts
  - tauri/ui/src/session/SessionLayout.ts
  - tauri/ui/src/session/state.ts
  - tauri/ui/src/settings/SettingsDrawer.ts
  - tauri/ui/src/settings/components/mascot-group.ts
  - tauri/ui/src/settings/state.ts
  - tauri/ui/src/session/components/cohost.test.ts
  - tauri/ui/src/settings/components/mascot-group.test.ts
autonomous: true
requirements: [MASCOT-02, MASCOT-07]
must_haves:
  truths:
    - "Phase 12 session UI right column has NO 42×42 mascot placeholder bubble; the AVERY chip header is restructured to show only status text (LISTENING/TALKING/IDLE)"
    - "Settings drawer has a new MASCOT group with two controls: click-through toggle (default OFF) and mood selector (hype-man / teacher / coach)"
    - "Selecting a mood in settings calls emitIpc('ipc.settings.set', { field: 'mood', value: <mood> }) AND invokes set_mascot_click_through Tauri command on toggle change"
    - "SessionState.settings includes mood ('hype-man' | 'teacher' | 'coach') and click_through (bool)"
    - "vitest passes for the modified cohost.test.ts and new mascot-group.test.ts"
  artifacts:
    - path: "tauri/ui/src/settings/components/mascot-group.ts"
      provides: "Settings MASCOT group component (click-through toggle + mood radio)"
    - path: "tauri/ui/src/session/components/cohost.ts"
      provides: "Updated cohost header — no 42×42 mascot bubble, restructured layout"
  key_links:
    - from: "tauri/ui/src/settings/SettingsDrawer.ts"
      to: "tauri/ui/src/settings/components/mascot-group.ts"
      via: "mountSettingsDrawer appends MascotGroup() after the existing groups"
      pattern: "MascotGroup"
    - from: "tauri/ui/src/settings/components/mascot-group.ts"
      to: "tauri Tauri shell"
      via: "invoke('set_mascot_click_through', { enabled }) + emitIpc('ipc.settings.set', { field: 'mood', ... })"
      pattern: "set_mascot_click_through|ipc\\.settings\\.set"
---

<objective>
Drop the 42×42 mascot placeholder from the Phase 12 session UI right-column cohost header (per CONTEXT.md Open Q 2 — "corner dropped entirely") AND add a new MASCOT settings group to the existing Settings drawer for click-through + mood control. Wire mood + click_through into `SessionState.settings` so the renderer (Plan 13-04) and event dispatcher (Plan 13-06) can read them.

Purpose: With the mascot living as an overlay window (Plan 13-02), the embedded placeholder is dead weight that fights the design — Kaan: "*it IS the AI, dancing on your screen*". The Settings drawer is the only place to control mascot behaviour without dropping focus on the live session.

Output: Phase 12 session UI loses the placeholder bubble; Settings drawer gains a MASCOT group; SessionState carries the two new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-3d-mascot-overlay/13-CONTEXT.md
@.claude/skills/frontend-enforcement/SKILL.md
@tauri/ui/src/session/components/cohost.ts
@tauri/ui/src/session/state.ts
@tauri/ui/src/settings/SettingsDrawer.ts
@tauri/ui/src/settings/components/group.ts
@tauri/ui/src/settings/components/hotkey-capture.ts

<interfaces>
<!-- Existing patterns the executor MUST follow rather than reinvent. -->

From `tauri/ui/src/session/state.ts`:
```typescript
export interface SettingsView {
  voice: string;
  mode: "hype" | "coach";
  genre: string;
  output_device_id: string | null;
  output_profile: "hp" | "spk";
  retention_days: number;
  push_to_mute_hotkey: string;
}
```
Extend with `mood: "hype-man" | "teacher" | "coach"` and `click_through: boolean`. Default mood = `"hype-man"` per CONTEXT.md Area 4.

From `tauri/ui/src/session/components/cohost.ts` (lines 281-285):
```typescript
const mascot = document.createElement("div");
mascot.className = "vmx-cohost__mascot";
mascot.setAttribute("aria-hidden", "true");
mascot.innerHTML = MASCOT_PLACEHOLDER_SVG;
header.append(mascot);
```
This block + the `.vmx-cohost__mascot` CSS rules + the `MASCOT_PLACEHOLDER_SVG` import all go. The cohost header keeps only the AVERY name + status row (LISTENING/TALKING/IDLE).

From `tauri/ui/src/settings/SettingsDrawer.ts` group composition pattern (Phase 12 Wave 4):
- The drawer body contains a vertical stack of group components: PERSONA / OUTPUT / HOTKEY / RECORDING / CALIBRATION.
- Each group uses `Group()` from `./components/group.ts` for its container with title + slot.
- MascotGroup follows the same shape: a `<section data-group="mascot">` with title "MASCOT" + two rows (toggle + radio).

From `tauri/ui/src/settings/state.ts`:
The settings drawer-local UX state singleton already exists. No changes needed here — the persistent settings are in SessionState.settings.

IPC contract for `ipc.settings.set`:
```typescript
emitIpc('ipc.settings.set', { field: 'mood', value: 'teacher' })
// Sidecar acks via ipc.settings.state broadcast — SessionState picks up via existing ws-bridge.
```

Tauri command for click-through (lands in Plan 13-02):
```typescript
import { invoke } from "@tauri-apps/api/core";
await invoke("set_mascot_click_through", { enabled: true });
```
</interfaces>

<frontend_enforcement_constraints>
<!-- From .claude/skills/frontend-enforcement/SKILL.md — MUST be honoured. -->
- 20/80 rule: dominant tone is `--ink-engraved` panel; accent (phosphor amber) only on selected mood radio active-state.
- Material feel: mood radio buttons use the existing `panel.ts` brushed-aluminum gradient + inset shadow on selected state (NOT flat `#1a1a1a`).
- No Inter / no Tailwind defaults. Use Workbench display font (already loaded) for "MASCOT" heading; DM Mono for radio labels.
- Toggle uses the existing `rocker.ts` component from session/components (don't author a new toggle shape).
</frontend_enforcement_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop mascot placeholder from cohost.ts + extend SessionState.settings + update typegate</name>
  <read_first>
    - tauri/ui/src/session/components/cohost.ts (full file — lines 1-end; identify .vmx-cohost__mascot CSS + the SVG import + the placeholder div in render())
    - tauri/ui/src/session/state.ts (SettingsView interface — extend)
    - tauri/ui/src/session/SessionLayout.ts (verify the AVERY chip layout aligns with the new no-bubble header — check the cohost mount point grid math doesn't rely on the 42×42 footprint)
    - tauri/ui/src/session/components/cohost.test.ts (existing vitest spec — update assertions that check for the placeholder bubble)
    - tauri/ui/src/session/icons/mascot-placeholder.svg.ts (the imported SVG — once unused, remove the import statement; the file itself can stay for now since the wizard still references mascot-corner separately)
  </read_first>
  <action>
    In `tauri/ui/src/session/components/cohost.ts`:
    - DELETE the `import { MASCOT_PLACEHOLDER_SVG } from "../icons/mascot-placeholder.svg.js";` line.
    - DELETE the `.vmx-cohost__mascot` CSS rules block (around lines 66-83 — width / height / border-radius / svg width treatments).
    - DELETE the `mascot` `div` creation block (around lines 281-285) and the `header.append(mascot)` call.
    - Restructure the cohost header: it now contains ONLY the "AVERY" name (Workbench 13px) + status row (LISTENING/TALKING/IDLE in DM Mono). Single horizontal flex row, left-aligned. Update the docstring at top of file (lines 1-30) to remove "mascot header" references and replace with "transcript header" wording.
    - Update existing `cohost.test.ts` vitest assertions: any test referencing `.vmx-cohost__mascot` selector or `MASCOT_PLACEHOLDER_SVG` content must be removed; ADD a new assertion `expect(container.querySelector('.vmx-cohost__mascot')).toBeNull()` to pin the deletion.

    In `tauri/ui/src/session/state.ts`:
    - Extend `SettingsView` interface:
      ```typescript
      export interface SettingsView {
        voice: string;
        mode: "hype" | "coach";
        genre: string;
        output_device_id: string | null;
        output_profile: "hp" | "spk";
        retention_days: number;
        push_to_mute_hotkey: string;
        // Phase 13 additions
        mood: "hype-man" | "teacher" | "coach";
        click_through: boolean;
      }
      ```
    - Update `makeDefault().settings` to include `mood: "hype-man"` and `click_through: false`.

    In `tauri/ui/src/session/SessionLayout.ts`:
    - Search the grid CSS for any column-width assumption that depended on the 42×42 placeholder. The grid uses `var(--col-right)` so the column width is fine. Just verify the cohost mount visually re-flows correctly when one element drops — no SessionLayout edits are expected unless the grid relies on the placeholder footprint (it shouldn't).

    Per CONTEXT.md Open Q 2: the freed space is used for "breathing room around the meters + transcript" — no new component to fill the gap. The transcript ring (already 200-line capacity) just renders with more vertical room.

    Do NOT delete `tauri/ui/src/session/icons/mascot-placeholder.svg.ts` — the wizard mascot-corner (Phase 11) still imports it. Only delete the import from `cohost.ts`.
  </action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; npm run check:ipc &amp;&amp; ! grep -q 'MASCOT_PLACEHOLDER_SVG' src/session/components/cohost.ts &amp;&amp; ! grep -q 'vmx-cohost__mascot' src/session/components/cohost.ts &amp;&amp; grep -q 'mood:' src/session/state.ts &amp;&amp; grep -q 'click_through' src/session/state.ts &amp;&amp; npx vitest run src/session/components/cohost.test.ts --reporter=dot</automated>
  </verify>
  <done>
    `npm run check:ipc` exits 0 (TypeScript compile passes). cohost.ts has no `MASCOT_PLACEHOLDER_SVG` import, no `.vmx-cohost__mascot` CSS, no placeholder div. SessionState.settings includes `mood` and `click_through` fields with the documented defaults. vitest for `cohost.test.ts` passes including the new `.toBeNull()` assertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MascotGroup settings component + wire it into SettingsDrawer + vitest spec</name>
  <read_first>
    - tauri/ui/src/settings/SettingsDrawer.ts (full file — identify where group components are appended in mountSettingsDrawer; see lines 230-310 around drawer body composition)
    - tauri/ui/src/settings/components/group.ts (existing Group() function — title + slot wrapper)
    - tauri/ui/src/session/components/rocker.ts (existing toggle component — reuse for click-through)
    - tauri/ui/src/settings/components/hotkey-capture.ts (radio-button-like layout reference for mood selector — radios styled per UI-SPEC)
    - tauri/ui/src/ipc/client.ts (emitIpc signature)
    - .claude/skills/frontend-enforcement/SKILL.md (UI rules)
  </read_first>
  <action>
    Create `tauri/ui/src/settings/components/mascot-group.ts`:

    Export a `MascotGroup()` function returning `HTMLElement`. Internal layout:
    ```
    Group({ title: "MASCOT", children: [
      Row 1: "Click-through" label + Rocker toggle (DM Mono 11px label, rocker on the right)
      Row 2: "Mood" label + 3 segmented buttons (hype-man / teacher / coach), pill-shaped, selected = filled --accent
    ]})
    ```

    CSS injection follows the registerStyle() singleton pattern from `_style-registry.ts`. Scope: `[data-component="mascot-group"]`. Use existing CSS variables ONLY (no hardcoded hex):
    - Background: `var(--ink-engraved)` panel
    - Selected mood pill: `background: var(--accent-faint); color: var(--accent); border: 1px solid var(--accent-dim)` — phosphor-amber active-state
    - Unselected mood pill: `background: transparent; color: var(--ink-muted); border: 1px solid var(--rule-dim)`
    - Inset shadow on selected: `box-shadow: inset 0 1px 3px rgba(0,0,0,0.4)` (material recess; permitted because the value is RGB-on-zero, not a brand color)

    Subscribe to `SessionState` via the existing `subscribeSessionState` (or equivalent helper used by other groups — check SettingsDrawer.ts imports). On state change, re-derive `selectedMood = state.settings.mood` and `clickThroughOn = state.settings.click_through`. Update DOM (toggle data-on attr + selected pill data-active attr).

    Click handlers:
    - Click-through Rocker `onChange(enabled)`: `await invoke("set_mascot_click_through", { enabled }); emitIpc('ipc.settings.set', { field: 'click_through', value: enabled });` — both calls fire so the Rust side AND the sidecar's settings store update.
    - Mood pill click: `emitIpc('ipc.settings.set', { field: 'mood', value: 'teacher' });` — sidecar acks via `ipc.settings.state` which the existing ws-bridge applies; UI updates on next tick.

    In `tauri/ui/src/settings/SettingsDrawer.ts`:
    - Add `import { MascotGroup } from "./components/mascot-group.js";`
    - In `mountSettingsDrawer` body composition, append `MascotGroup()` to the drawer body AFTER the existing CALIBRATION group (last group). Order: PERSONA / OUTPUT / HOTKEY / RECORDING / CALIBRATION / **MASCOT**.

    Create `tauri/ui/src/settings/components/mascot-group.test.ts` (jsdom vitest pattern from Phase 12):
    - Test 1: Component renders with title "MASCOT" + 1 toggle + 3 mood pills
    - Test 2: When SessionState.settings.mood === "teacher", the teacher pill has data-active="true" and the other two have data-active="false"
    - Test 3: When SessionState.settings.click_through === true, the rocker has data-on="true"
    - Test 4: Clicking the "coach" pill calls emitIpc with `{ field: 'mood', value: 'coach' }` (use vi.fn() mock from existing test patterns)
    - Test 5: Toggling click-through rocker calls invoke('set_mascot_click_through', { enabled: true }) AND emitIpc('ipc.settings.set', { field: 'click_through', value: true })

    Per `frontend-enforcement` SKILL.md: NO Inter, NO Tailwind defaults, NO purple gradients. All colors via existing CSS vars in tokens.css.
  </action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; npm run check:ipc &amp;&amp; test -f src/settings/components/mascot-group.ts &amp;&amp; test -f src/settings/components/mascot-group.test.ts &amp;&amp; grep -q 'MascotGroup' src/settings/SettingsDrawer.ts &amp;&amp; ! grep -E '#[0-9a-fA-F]{6}' src/settings/components/mascot-group.ts &amp;&amp; npx vitest run src/settings/components/mascot-group.test.ts --reporter=dot</automated>
  </verify>
  <done>
    `npm run check:ipc` passes. `mascot-group.ts` exists with `MascotGroup()` export. SettingsDrawer.ts imports and appends it. No hex literals anywhere in mascot-group.ts (only `var(--*)` references). vitest spec passes 5 tests. Manual: opening the settings drawer in Vite dev mode shows the MASCOT group at the bottom with the click-through toggle and 3 mood pills.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → tauri command | `set_mascot_click_through` invocation crosses the IPC boundary; capability allowlist gates |
| webview → sidecar via WS | `ipc.settings.set` emits string field/value; sidecar schema-validates on receive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Tampering | mood value | mitigate | TypeScript narrows mood to `"hype-man" \| "teacher" \| "coach"`; sidecar IPC schema validates against the same enum (Plan 13-05 extends ipc.settings.set schema) |
| T-13-03-02 | Info disclosure | mood as PII | accept | Mood is a UX preference; no PII tagging concerns |
| T-13-03-03 | DoS | rapid mood-toggle spam | mitigate | Existing ws-bridge throttles ipc.settings.set; mood changes capped at the same rate as other settings updates |
</threat_model>

<verification>
- `cd tauri/ui && npm run check:ipc` exits 0
- `grep -c 'vmx-cohost__mascot' tauri/ui/src/session/components/cohost.ts` == 0
- `grep -c 'mood' tauri/ui/src/session/state.ts` ≥ 1
- `grep -c 'click_through' tauri/ui/src/session/state.ts` ≥ 1
- `npx vitest run tauri/ui/src/session/components/cohost.test.ts tauri/ui/src/settings/components/mascot-group.test.ts` exits 0
- `grep -E '#[0-9a-fA-F]{6}' tauri/ui/src/settings/components/mascot-group.ts` returns zero matches
- Manual (Vite dev `npm run dev` with `?dev=session-mock`): settings drawer shows MASCOT group + 5 group entries above it; click-through toggle + 3 mood pills render with brushed-aluminum/phosphor-amber styling
</verification>

<success_criteria>
Plan 13-03 succeeds when:
- Session UI right column has no 42×42 mascot placeholder
- SessionState.settings includes `mood` and `click_through` fields
- Settings drawer renders MASCOT group at the bottom with click-through toggle + 3 mood pills
- Mood pill click emits the right IPC payload; click-through toggle invokes the Tauri command AND emits IPC
- vitest specs pass; TypeScript compile passes; no hex literals in new component
</success_criteria>

<output>
After completion, create `.planning/phases/13-3d-mascot-overlay/13-03-SUMMARY.md` documenting the cohost header restructure (before/after layout sketch) and the MASCOT group component shape.
</output>
