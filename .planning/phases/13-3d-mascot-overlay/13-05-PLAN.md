---
phase: 13-3d-mascot-overlay
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/vibemix/state/music_state.py
  - src/vibemix/audio/features.py
  - src/vibemix/runtime/ws_bus.py
  - src/vibemix/runtime/settings.py
  - src/vibemix/agent/coach.py
  - src/vibemix/prompts/matrix.py
  - tauri/ui/src/ipc/messages.schema.json
  - tauri/ui/src/ipc/messages.ts
  - src/vibemix/ui_bus/messages.py
  - tests/state/test_music_state_mood.py
  - tests/state/test_downbeat_phase.py
  - tests/agent/test_coach_mood_template.py
  - tests/ui_bus/test_mood_change_envelope.py
autonomous: true
requirements: [MASCOT-04, MASCOT-07]
must_haves:
  truths:
    - "MusicState gains 3 new fields: mood (Literal['hype-man','teacher','coach']), bpm_confidence (float 0..1), downbeat_phase (float 0..1)"
    - "state_refresh_loop is the only writer of mood / bpm_confidence / downbeat_phase — invariant preserved per Phase 3 Critical Constraint 7"
    - "ws_bus broadcast payload includes mood, bpm_confidence, downbeat_phase in the 30Hz state snapshot"
    - "ipc.settings.set with field='mood' is a valid IPC envelope; sidecar applies it to ConfigStore + MusicState; ipc.settings.state ack carries the new mood"
    - "Mood change emits a dedicated ipc.mascot.mood_change envelope on the WS bus (NEW message type) the moment ConfigStore applies it"
    - "Coach prompt template (build_system_instruction) references {mood} placeholder and rendered with current mood"
    - "downbeat_phase is computed from BPM + onset timing in features.py (best-effort; falls back to 0.0 with bpm_confidence=0 when BPM is invalid)"
  artifacts:
    - path: "src/vibemix/state/music_state.py"
      provides: "MusicState dataclass with 3 new fields"
    - path: "src/vibemix/audio/features.py"
      provides: "compute_downbeat_phase() helper + bpm_confidence reporter"
    - path: "src/vibemix/runtime/ws_bus.py"
      provides: "Updated broadcast payload + ipc.mascot.mood_change schema-validated emit"
    - path: "src/vibemix/runtime/settings.py"
      provides: "SettingsApplier handles 'mood' field — writes to MusicState + ConfigStore + emits mood_change"
    - path: "tauri/ui/src/ipc/messages.schema.json"
      provides: "ipc.mascot.mood_change schema added to oneOf; ipc.settings.set field allowlist extended to include 'mood' and 'click_through'"
  key_links:
    - from: "src/vibemix/runtime/settings.py SettingsApplier.apply"
      to: "src/vibemix/state/music_state.py MusicState.mood field"
      via: "MusicState write under lock"
      pattern: "state.mood = "
    - from: "src/vibemix/runtime/settings.py"
      to: "src/vibemix/runtime/ws_bus.py emit"
      via: "emit(ipc.mascot.mood_change envelope)"
      pattern: "ipc\\.mascot\\.mood_change"
    - from: "src/vibemix/agent/coach.py build_prompt"
      to: "src/vibemix/prompts/matrix.py {mood} placeholder"
      via: ".format(mood=...)"
      pattern: "\\{mood\\}"
---

<objective>
Wire the Python sidecar to be the source of truth for mascot mood + beat-lock signals. Specifically: add `mood` / `bpm_confidence` / `downbeat_phase` fields to `MusicState`, extend `SettingsApplier` to handle mood changes, broadcast the new state fields over the existing 30Hz WS snapshot, emit a dedicated `ipc.mascot.mood_change` envelope on mood swap, extend the IPC schema + codegen to cover the new envelope + the `mood` settings field, and surface `{mood}` in the Coach prompt template.

Purpose: The mascot renderer (Plan 13-04) and event dispatcher (Plan 13-06) need to read mood + bpm_confidence + downbeat_phase from the bus — without sidecar-side wiring, they read garbage defaults. The Coach prompt template needs `{mood}` so the AI's persona shifts when Kaan flips moods.

Output: Sidecar end-to-end writes mood + beat-lock fields to MusicState, broadcasts them at 30Hz, fires `ipc.mascot.mood_change` on swaps, and the Coach prompt template renders with the active mood persona.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-3d-mascot-overlay/13-CONTEXT.md
@src/vibemix/state/music_state.py
@src/vibemix/audio/features.py
@src/vibemix/runtime/ws_bus.py
@src/vibemix/runtime/settings.py
@src/vibemix/agent/coach.py
@src/vibemix/prompts/matrix.py
@tauri/ui/src/ipc/messages.schema.json
@tauri/ui/scripts/codegen-ipc.mjs
@scripts/check_ipc_schema.py
@src/vibemix/ui_bus/messages.py

<interfaces>
Existing MusicState shape (Phase 3 + 6 baseline) — DO NOT change defaults of existing fields, only ADD new ones. Per Phase 6 retro, MusicState writers are STATE_REFRESH_LOOP-only.

```python
# Add to MusicState:
mood: str = "hype-man"           # Literal["hype-man", "teacher", "coach"]; backward-compat default
bpm_confidence: float = 0.0       # 0..1 — 0 means "no BPM lock yet"; >= 0.6 means renderer can beat-lock
downbeat_phase: float = 0.0       # 0..1 — fraction through current bar; 0 = on downbeat, 0.5 = mid-bar
```

Existing ws_bus broadcast payload (Phase 4):
```python
payload = {
    "type": "snapshot",
    **levels.snapshot(),  # music/voice/mic rms
    "audible": state.audible,
    "deck": state.audible_deck,
    "phase": state.phase,
    "bpm": state.bpm,
    # Phase 13 adds:
    "mood": state.mood,
    "bpm_confidence": state.bpm_confidence,
    "downbeat_phase": state.downbeat_phase,
}
```

IPC schema extension (`messages.schema.json` — Phase 11 W0 + 12 W1 baseline, Draft-07):

Add a NEW envelope:
```json
{
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "payload"],
  "properties": {
    "type": { "const": "ipc.mascot.mood_change" },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mood"],
      "properties": {
        "mood": { "type": "string", "enum": ["hype-man", "teacher", "coach"] },
        "previous_mood": { "type": "string", "enum": ["hype-man", "teacher", "coach"] },
        "at": { "type": "number" }
      }
    }
  }
}
```

Extend `ipc.settings.set` payload schema's `field` enum (current set is `voice / mode / genre / output_device_id / output_profile / retention_days / push_to_mute_hotkey`) to add `mood` and `click_through`. The value oneOf must accept `string` (for mood) and `boolean` (for click_through).

Python wrapper to add in `src/vibemix/ui_bus/messages.py` (Phase 11 W0 convention — `@dataclass(frozen=True, slots=True)`):
```python
@dataclass(frozen=True, slots=True)
class MascotMoodChangePayload:
    mood: str                       # Literal but kept as str at this layer per Phase 6 no-pydantic rule
    previous_mood: str | None = None
    at: float | None = None

@dataclass(frozen=True, slots=True)
class MascotMoodChangeMessage:
    type: str = "ipc.mascot.mood_change"
    payload: MascotMoodChangePayload = ...
```

Count-parity invariant: `messages.schema.json` `oneOf` count MUST equal Python wrapper-dataclass count. Currently 26 (Phase 12 W1) — becomes 27 after this plan.

`check_ipc_schema.py` runs the count assertion. Update the literal 26 (if present) to 27 OR keep the introspection-based count which auto-tracks.

Coach prompt template (`src/vibemix/prompts/matrix.py`):
The 6-cell matrix already exists (HYPE_BEGINNER/INTERMEDIATE/PRO + COACH_BEGINNER/INTERMEDIATE/PRO). Plan 13-05 adds a single `{mood}` placeholder inside each COACH_* cell that renders the active mood persona description. `build_system_instruction(mode, skill, mood)` gains a `mood` kwarg with default `"hype-man"` for backward compat.

Mood persona fragments (~120 chars each):
- hype-man: "You're high-energy, all-caps emotional, party-anchored — celebrate every clean move, react to the floor"
- teacher: "You're patient, vocabulary-rich, framework-anchored — name techniques, reference structure, slow your pace"
- coach: "You're frank, post-mortem-anchored, debrief-style — call out what went wrong and what to repeat next time"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MusicState + state_refresh_loop + features.py compute_downbeat_phase()</name>
  <read_first>
    - src/vibemix/state/music_state.py (full file — extend dataclass)
    - src/vibemix/audio/features.py (BPM + onset functions — where downbeat_phase computation lands)
    - src/vibemix/runtime/state_refresh.py (or wherever state_refresh_loop lives — the ONLY MusicState writer)
    - src/vibemix/state/event_detector.py (consumer side — must not regress)
    - .planning/phases/13-3d-mascot-overlay/13-CONTEXT.md (Open Q 4 — best-effort with 0.6 confidence threshold)
    - .planning/STATE.md (Phase 6 Critical Constraint 7 — hysteresis state in state_refresh_loop local scope, NOT MusicState; mood is a CONSUMER-readable evidence field, IS appropriate to live in MusicState)
  </read_first>
  <behavior>
    - MusicState({}) with default args has mood == "hype-man", bpm_confidence == 0.0, downbeat_phase == 0.0
    - compute_downbeat_phase(samples, bpm, sample_rate) returns a float in [0.0, 1.0)
    - compute_downbeat_phase with invalid bpm (0, negative, NaN) returns (0.0, 0.0) confidence
    - compute_downbeat_phase with valid BPM + onset envelope produces a phase that increments monotonically (mod 1) over a synthetic looped onset train
    - state_refresh_loop calls compute_downbeat_phase and writes both fields to MusicState under the lock
  </behavior>
  <action>
    In `src/vibemix/state/music_state.py`:
    - Add three fields to the MusicState dataclass:
      ```
      mood: str = "hype-man"
      bpm_confidence: float = 0.0
      downbeat_phase: float = 0.0
      ```
    - Place them in the "evidence" cluster (near phase / bpm) for organisational consistency. Update the module docstring if it enumerates fields.

    In `src/vibemix/audio/features.py`:
    - Add `def compute_downbeat_phase(samples: np.ndarray, bpm: float, sample_rate: int, *, prior_phase: float = 0.0) -> tuple[float, float]:` that returns `(downbeat_phase, bpm_confidence)`.
    - Implementation (best-effort, anti-hallucination — never invent confidence the audio doesn't support):
      1. If `bpm <= 0 or bpm > 220 or math.isnan(bpm)` → return `(0.0, 0.0)` immediately (no fake confidence).
      2. Compute `msPerBar = (60 / bpm) * 4 * 1000`. Compute the expected onset interval `samples_per_beat = (60 / bpm) * sample_rate`.
      3. Compute an onset envelope (existing `onset_envelope()` helper if present, else use spectral flux). Find peaks within the last `4 * samples_per_beat` window.
      4. If fewer than 4 peaks detected → `(prior_phase, 0.0)` — not enough evidence; preserve last known phase but flag zero confidence.
      5. Cross-correlate the peak comb against a synthetic comb at `bpm`; the lag of the best match modulo `samples_per_beat * 4` (one bar) gives the phase. Normalize to `[0.0, 1.0)`.
      6. Confidence: `(peak_correlation - mean_correlation) / std_correlation` clamped to `[0, 1]`. Cap at `min(1.0, n_peaks / 16.0)` so few-peak signals don't fake high confidence.
    - The function is pure (no global state) and synchronous. Tests stub np.ndarray inputs.

    In `src/vibemix/runtime/state_refresh.py` (or the file hosting `state_refresh_loop`):
    - Within the 10Hz loop body (the SINGLE WRITER — Phase 6 invariant), AFTER `bpm` is computed and written to MusicState, also call `compute_downbeat_phase(audio_window, state.bpm, sample_rate, prior_phase=state.downbeat_phase)` and write both returned values under the same lock. Same site as the existing crest_factor / bpm_corrected writes.
    - Mood is a settings-driven field, NOT computed from audio — its writer is `SettingsApplier` (Task 2). state_refresh_loop NEVER writes `state.mood`. Add a one-line comment at the state_refresh_loop write site reading `# mood is owned by SettingsApplier, never touched here`.

    Create `tests/state/test_music_state_mood.py`:
    - Test 1: `MusicState()` default has mood == "hype-man", bpm_confidence == 0.0, downbeat_phase == 0.0
    - Test 2: writing mood to a MusicState instance under the lock holds (no race) — call from two threads, assert final value
    - Test 3: existing MusicState fields unchanged (regression — assert `MusicState().phase == "silent"` etc.)

    Create `tests/state/test_downbeat_phase.py`:
    - Test 1: compute_downbeat_phase(silent_audio, bpm=120, sample_rate=16000) → (0.0, 0.0)
    - Test 2: compute_downbeat_phase(audio, bpm=0, ...) → (0.0, 0.0)
    - Test 3: compute_downbeat_phase(audio, bpm=NaN, ...) → (0.0, 0.0)
    - Test 4: compute_downbeat_phase(synthetic_kick_train@120bpm, bpm=120, sample_rate=16000) → phase ∈ [0, 1), confidence > 0.5 (synthetic ideal kicks)
    - Test 5: prior_phase fallback when fewer than 4 peaks detected — assert returned phase equals prior_phase, confidence == 0.0
    - Test 6: same audio called twice with the same prior_phase returns the same (phase, confidence) — pure function determinism
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; python -m pytest tests/state/test_music_state_mood.py tests/state/test_downbeat_phase.py -x -q</automated>
  </verify>
  <done>
    MusicState carries the 3 new fields with documented defaults. compute_downbeat_phase pure function in features.py with anti-hallucination guards (returns 0,0 on invalid input). state_refresh_loop is the only writer of bpm_confidence + downbeat_phase. pytest passes the 9 new tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettingsApplier handles mood + click_through; ws_bus broadcasts new fields + emits ipc.mascot.mood_change</name>
  <read_first>
    - src/vibemix/runtime/settings.py (SettingsApplier — current dispatch matrix for the 7 settings fields)
    - src/vibemix/runtime/ws_bus.py (existing 30Hz broadcast + WizardBus class with `emit` pattern)
    - src/vibemix/ui_bus/messages.py (wrapper-dataclass pattern from Phase 11 W0)
    - tauri/ui/src/ipc/messages.schema.json (current 26-oneOf schema; locate ipc.settings.set entry to extend the field enum)
    - tauri/ui/scripts/codegen-ipc.mjs (programmatic json2ts wrapper — TS messages regenerate on schema change)
    - scripts/check_ipc_schema.py (count-parity invariant — wrappers vs schema oneOf)
    - .planning/phases/13-3d-mascot-overlay/13-CONTEXT.md (Open Q 3: mood swap fires mood_change event on WS bus)
  </read_first>
  <behavior>
    - SettingsApplier.apply("mood", "teacher") writes MusicState.mood AND ConfigStore mood AND emits ipc.mascot.mood_change with payload {mood: "teacher", previous_mood: "hype-man"}
    - SettingsApplier.apply("click_through", true) writes ConfigStore click_through; does NOT touch MusicState (click_through is webview/Rust concern)
    - SettingsApplier.apply("mood", "invalid") raises ValueError (no silent fallback)
    - ws_bus 30Hz broadcast payload includes mood, bpm_confidence, downbeat_phase fields
    - ipc.mascot.mood_change envelope passes the new schema validation
    - check_ipc_schema.py exits 0 (count parity holds at 27)
  </behavior>
  <action>
    Update `tauri/ui/src/ipc/messages.schema.json`:
    - Add a new oneOf entry for `ipc.mascot.mood_change` per the schema in `<interfaces>` (verbatim).
    - Locate the `ipc.settings.set` entry. Find its `payload.properties.field.enum` array. Extend with `"mood"` and `"click_through"`. Locate its `payload.properties.value` — make it `{ "oneOf": [{ "type": "string" }, { "type": "number" }, { "type": "boolean" }] }` if it isn't already wide enough (Phase 12 may have made it string-only; widen to accept boolean for click_through and number for retention_days).
    - Run `npm run codegen:ipc` to regenerate `messages.ts`. Commit the regenerated file alongside the schema (Phase 11 W0 invariant — codegen output committed alongside).

    Add to `src/vibemix/ui_bus/messages.py`:
    - `MascotMoodChangePayload` frozen-slots dataclass per `<interfaces>`.
    - `MascotMoodChangeMessage` frozen-slots dataclass with `type = "ipc.mascot.mood_change"` and the payload field.
    - Register the new wrapper in whatever lookup table the existing module uses (the count-parity test introspects `__dataclass_fields__["type"]` so any wrapper dataclass with a `type` field auto-counts).

    Update `src/vibemix/runtime/settings.py`:
    - Locate the `SettingsApplier.apply(field, value)` dispatch matrix.
    - Add case `field == "mood"`:
      ```
      if value not in {"hype-man", "teacher", "coach"}:
          raise ValueError(f"invalid mood: {value!r}")
      previous = self._music_state.mood
      with self._music_state._lock:
          self._music_state.mood = value
      self._config_store.set("mood", value)
      now_ts = time.monotonic()
      emit_message = MascotMoodChangeMessage(
          payload=MascotMoodChangePayload(mood=value, previous_mood=previous, at=now_ts)
      )
      self._ws_bus.emit(_dataclass_to_dict(emit_message))
      ```
    - Add case `field == "click_through"`:
      ```
      if not isinstance(value, bool):
          raise ValueError(f"click_through expects bool, got {type(value).__name__}")
      self._config_store.set("click_through", value)
      # No MusicState write (click_through is a Rust-side window concern).
      ```
    - The webview also invokes the Tauri `set_mascot_click_through` command (Plan 13-02) so the OS-side window state stays in sync. The sidecar ConfigStore holds the canonical bit.

    Update `src/vibemix/runtime/ws_bus.py`:
    - Extend the 30Hz broadcast payload (the `snapshot` message) to include `mood`, `bpm_confidence`, `downbeat_phase` from MusicState. Be defensive — read with the existing `state._lock` if the broadcaster already locks. If broadcasting is lock-free (snapshot copy pattern), add the new fields to the snapshot copy.

    Update `scripts/check_ipc_schema.py`:
    - The script auto-counts via `__dataclass_fields__["type"]` introspection (Phase 11 W0 invariant). If a hardcoded count constant exists, bump from 26 → 27. If only introspection, no edit needed beyond verifying the new wrapper is picked up.

    Create `tests/agent/test_coach_mood_template.py` (stub for Task 3 — kept here so Task 2 alone is a complete commit).

    Create `tests/ui_bus/test_mood_change_envelope.py`:
    - Test 1: MascotMoodChangeMessage with valid payload roundtrips through `parse_message` / `validate_message` (Phase 11 W0 pattern)
    - Test 2: schema validation rejects mood="party-rocker" (not in enum)
    - Test 3: schema validation rejects payload without mood field
    - Test 4: `check_ipc_schema.py` count-parity invariant holds (27 wrappers == 27 schema oneOf entries)
    - Test 5: Settings apply("mood", "teacher") emits an ipc.mascot.mood_change frame matching the schema (use a fake ws_bus emit recorder)
    - Test 6: Settings apply("mood", "invalid") raises ValueError
    - Test 7: Settings apply("click_through", "not-a-bool") raises ValueError
    - Test 8: 30Hz snapshot payload includes mood/bpm_confidence/downbeat_phase keys (use a fake broadcaster spy)
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; python scripts/check_ipc_schema.py &amp;&amp; cd tauri/ui &amp;&amp; npm run check:ipc &amp;&amp; cd /Users/ozai/projects/dj-set-ai &amp;&amp; python -m pytest tests/ui_bus/test_mood_change_envelope.py -x -q</automated>
  </verify>
  <done>
    `check_ipc_schema.py` exits 0 with count parity 27 == 27. `npm run check:ipc` exits 0. Schema includes the new `ipc.mascot.mood_change` envelope and the extended `field` enum on `ipc.settings.set`. Python wrappers register. SettingsApplier handles `mood` and `click_through`. ws_bus broadcasts the 3 new fields. pytest passes the 8 new tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Coach prompt template gains {mood} placeholder + persona fragments</name>
  <read_first>
    - src/vibemix/prompts/matrix.py (6-cell template matrix — Phase 10 baseline)
    - src/vibemix/agent/coach.py (build_prompt — where {mood} renders)
    - src/vibemix/agent/dj_cohost.py (DJCoHostAgent — current build_system_instruction call site)
    - .planning/STATE.md Phase 10 entry (negative_dict + filter context — `{mood}` cannot collide with negative-dict entries)
    - .planning/phases/13-3d-mascot-overlay/13-CONTEXT.md (Area 4 — mood persona descriptions, Open Q 3 — mood is visible to AI)
  </read_first>
  <behavior>
    - build_system_instruction(mode="coach", skill="intermediate", mood="hype-man") returns a string containing "high-energy"
    - build_system_instruction(mode="coach", skill="intermediate", mood="teacher") returns a string containing "patient"
    - build_system_instruction(mode="coach", skill="intermediate", mood="coach") returns a string containing "post-mortem"
    - Existing calls build_system_instruction(mode, skill) WITHOUT mood kwarg still work (default mood="hype-man") — Phase 10 backward compat preserved
    - The rendered prompt does NOT contain literal "{mood}" — placeholder substitution happens
  </behavior>
  <action>
    In `src/vibemix/prompts/matrix.py`:
    - Add a `MOOD_PERSONAS` dict at module top:
      ```
      MOOD_PERSONAS: dict[str, str] = {
          "hype-man": "You're high-energy, all-caps emotional, party-anchored — celebrate every clean move, react to the floor",
          "teacher":  "You're patient, vocabulary-rich, framework-anchored — name techniques, reference structure, slow your pace",
          "coach":    "You're frank, post-mortem-anchored, debrief-style — call out what went wrong and what to repeat next time",
      }
      ```
    - Each COACH_BEGINNER / COACH_INTERMEDIATE / COACH_PRO template string gets a `{mood_persona}` placeholder inserted near the persona definition (top of each cell). Hype templates ALSO accept the placeholder but default to the hype-man persona — Phase 10's HYPE_* templates already encode hype-man tone, so the placeholder is optional there. For minimal blast radius: add `{mood_persona}` placeholder to all 6 templates but only render it when `mood != "hype-man"` (so existing HYPE_INTERMEDIATE byte-identical-to-v4 test pins continue holding when mood is the default).

    Update `build_system_instruction(mode: str, skill: str, mood: str = "hype-man") -> str`:
    - Look up the cell template per (mode, skill) as today.
    - Compute `mood_persona = MOOD_PERSONAS[mood]` (raise on unknown mood).
    - Run `.format(mood_persona=mood_persona)` (and any other existing placeholders).
    - Return the formatted string.

    Update `src/vibemix/agent/coach.py` and `src/vibemix/agent/dj_cohost.py` (or wherever `build_system_instruction` is called) to pass `mood=current_state.mood`. The MusicState snapshot read inside the agent's prompt-build path picks up the mood live — mood changes mid-session naturally render the next prompt with the new persona.

    Create `tests/agent/test_coach_mood_template.py`:
    - Test 1: build_system_instruction(mode="coach", skill="intermediate", mood="hype-man") contains "high-energy"
    - Test 2: build_system_instruction(mode="coach", skill="intermediate", mood="teacher") contains "patient"
    - Test 3: build_system_instruction(mode="coach", skill="intermediate", mood="coach") contains "post-mortem"
    - Test 4: build_system_instruction(mode="coach", skill="intermediate") (no mood kwarg) defaults to "hype-man" persona — contains "high-energy"
    - Test 5: build_system_instruction(mode="coach", skill="intermediate", mood="invalid") raises KeyError or ValueError
    - Test 6: rendered prompt does NOT contain literal substring "{mood_persona}" (placeholder must be substituted)
    - Test 7: HYPE_INTERMEDIATE byte-identical-to-v4 invariant held WHEN mood="hype-man" (Phase 10 golden test must still pass — assert build_system_instruction("hype", "intermediate", "hype-man") matches the pinned golden)
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; python -m pytest tests/agent/test_coach_mood_template.py -x -q &amp;&amp; python -m pytest tests/prompts/ -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    matrix.py exports MOOD_PERSONAS with 3 entries. build_system_instruction signature gains `mood` kwarg with `"hype-man"` default. Each prompt template renders the active mood persona. pytest passes the 7 new tests AND the existing tests/prompts/ suite stays green (no regression on Phase 10 golden invariants).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview ipc.settings.set → sidecar | Mood string passes WS — sidecar validates against {"hype-man","teacher","coach"} enum |
| state_refresh_loop → MusicState | Single-writer invariant per Phase 6 Constraint 7; mood is owned by SettingsApplier, never written from state_refresh_loop |
| MusicState.mood → Coach prompt template | Mood value substituted into AI prompt — must not allow prompt injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | mood value | mitigate | SettingsApplier rejects any value not in the literal 3-element set with ValueError; no silent default fallback |
| T-13-05-02 | Tampering | downbeat_phase | mitigate | compute_downbeat_phase returns 0.0 confidence on invalid BPM input; renderer below 0.6 confidence skips beat-lock (Plan 13-04) — bad audio cannot mis-time animations |
| T-13-05-03 | Spoofing | ipc.mascot.mood_change emit | mitigate | Schema-validated on emit (Phase 11 W0 envelope contract); count-parity check_ipc_schema.py exits non-zero if wrapper missing |
| T-13-05-04 | Info disclosure | mood in WS bus | accept | Mood is a UX preference, not PII; bus is localhost-only (127.0.0.1:8765 — Phase 5 invariant) |
| T-13-05-05 | Repudiation | who changed mood | mitigate | SettingsApplier emits at(=time.monotonic()) timestamp + previous_mood in the mood_change envelope; events.jsonl recorder (Phase 2) logs the trace |
| T-13-05-06 | Elevation | prompt-injection via mood | mitigate | mood is a LiteralEnum-constrained string; the persona fragment is a fixed constant in MOOD_PERSONAS dict; no user-controlled string enters the prompt |
</threat_model>

<verification>
- `python scripts/check_ipc_schema.py` exits 0 with count-parity at 27 == 27
- `cd tauri/ui && npm run check:ipc` exits 0 (codegen + TS compile)
- `python -m pytest tests/state/test_music_state_mood.py tests/state/test_downbeat_phase.py tests/ui_bus/test_mood_change_envelope.py tests/agent/test_coach_mood_template.py -x -q` passes all new tests
- `python -m pytest tests/prompts/ tests/state/ tests/ui_bus/ -x -q` no regressions on prior Phase 6/10/11/12 tests
- `grep -c "mood:" src/vibemix/state/music_state.py` ≥ 1
- `grep -c "ipc.mascot.mood_change" tauri/ui/src/ipc/messages.schema.json` ≥ 1
- `grep -c "MOOD_PERSONAS" src/vibemix/prompts/matrix.py` ≥ 1
</verification>

<success_criteria>
Plan 13-05 succeeds when:
- MusicState has the 3 new fields with documented defaults
- state_refresh_loop is the sole writer of bpm_confidence / downbeat_phase; SettingsApplier is the sole writer of mood
- ipc.mascot.mood_change is a valid IPC envelope with schema + Python wrapper + TS codegen
- ipc.settings.set accepts mood + click_through fields
- ws_bus 30Hz snapshot includes the 3 new fields
- Coach prompt template renders the active mood persona
- All test suites green; check_ipc_schema.py exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-3d-mascot-overlay/13-05-SUMMARY.md` documenting the count-parity transition (26 → 27), the chosen downbeat-detection algorithm (cross-correlation vs other approaches considered), and any prompt-template regression notes.
</output>
