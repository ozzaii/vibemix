---
phase: 65-memory-retrieval-seam
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/memory/test_retrieval.py
  - tests/state/test_coach.py
  - tests/state/test_evidence_registry.py
  - tests/prompts/test_matrix.py
  - tests/agent/test_dj_cohost_linter.py
  - tests/coach/test_citation_linter.py
autonomous: true
requirements: [RECALL-01, RECALL-02, RECALL-03, RECALL-04]
user_setup: []

must_haves:
  truths:
    - "A fabricated [recall:<unregistered>] strips the whole turn (RED until Plan 04)"
    - "evidence_line(state) with no recall is byte-identical to the v5.0 silent golden"
    - "A below-floor / current-session / HEARTBEAT retrieval injects nothing (RED until Plan 03)"
    - "The lockstep source/grammar count tests expect 9 sources incl. recall (RED until Plan 02)"
  artifacts:
    - path: "tests/memory/test_retrieval.py"
      provides: "RED unit tests for MemoryRecall: heartbeat-never-retrieves, below-floor, current-session-excluded, deadline-miss, embed-called-once"
      contains: "def test_heartbeat_never_retrieves"
    - path: "tests/agent/test_dj_cohost_linter.py"
      provides: "RED headline poisoning test: fabricated recall strips turn"
      contains: "def test_fabricated_recall_strips_turn"
    - path: "tests/state/test_coach.py"
      provides: "RED populated/empty recall block tests; existing silent golden preserved"
      contains: "def test_evidence_line_recall_block_present"
  key_links:
    - from: "tests/state/test_evidence_registry.py"
      to: "EVIDENCE_SOURCES (9 incl recall)"
      via: "sources-count assertion updated 8→9"
      pattern: "recall"
    - from: "tests/prompts/test_matrix.py"
      to: "CITATION_GRAMMAR_BLOCK ([recall: form)"
      via: "grammar-forms assertion updated 8→9"
      pattern: "\\[recall:"
---

<objective>
RED-first Wave-0 test scaffold for the entire phase. Author every NEW test and UPDATE every lockstep count-test BEFORE any implementation lands, so each downstream plan turns a precise set of named RED tests GREEN. This is the milestone's anti-slop release gate — the fabricated-`[recall:]`-strips-turn poisoning test is the headline RED.

Purpose: Pin the exact behavioral contract (per 65-VALIDATION.md) as failing tests first. The byte-identical-cold golden must already pass at baseline; the new tests must fail with clear "not implemented" signals, not collection errors.
Output: 1 new test file, 2 new RED tests appended to existing files, 2 updated lockstep count-tests, 1 extended linter test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-memory-retrieval-seam/65-VALIDATION.md
@.planning/phases/65-memory-retrieval-seam/65-RESEARCH.md
@.planning/phases/65-memory-retrieval-seam/65-PATTERNS.md

<interfaces>
<!-- Contracts the downstream implementation will satisfy; tests assert against these. -->

The MemoryRecall service (Plan 03 will create src/vibemix/memory/retrieval.py):
  RECALL_SIMILARITY_FLOOR = 0.7
  RECALL_TOP_K = 3
  RECALL_DEADLINE_S = 0.5
  RECALL_EVENT_GATE = frozenset({"TRACK_CHANGE", "PHASE", "LAYER_ARRIVAL"})  # NEVER HEARTBEAT
  class MemoryRecall(embedder, store):
      on_event(event_type: str, query_text: str, current_session_id: str) -> list[Record]
      get_latest() -> list[Record]
      clear() -> None

The Record carrier (memory/store.py:147 — VERIFIED):
  @dataclass Record: record_id: str  # f"{session_id}:{seq}"
                     session_id: str; ts: float; kind: str
                     signature: str   # raw stored text
                     score: float = 0.0   # cosine, set on query results

The read seam (memory/store.py:312 — VERIFIED):
  MemoryStore.query_topk(query_embedding, k=8, *, exclude_session=None) -> list[Record]

evidence_line gains a kwarg (Plan 04 edits coach.py:54):
  AICoach.evidence_line(state, *, registry_snapshot=None, recall_moments: list[Record] | None = None) -> str
  # recall_moments None or [] → emit NOTHING (falsy gate, like decks[…] / registry_snapshot)
  # populated → appends: "FROM A PAST SESSION (not happening now): [recall:<id>] <sig> || ..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New MemoryRecall RED unit tests + extended linter test</name>
  <read_first>
    - tests/memory/test_ingest.py (sibling fixture conventions: MemoryStore(db_path=tmp/"memory.db", prefer_sqlite_vec=False), _FakeEmbedder, library._cosine.l2_normalize for synthetic 768-dim vecs, add_record seeding)
    - tests/memory/test_no_live_path_import.py and tests/memory/test_no_extraction.py (confirm both rglob memory/*.py so they auto-cover the future retrieval.py — assert in a comment, no edit)
    - src/vibemix/memory/store.py:147 (Record), :312-340 (query_topk + exclude_session)
    - src/vibemix/coach/citation_linter.py:175-213 (existence-only branch — for the registered-recall-passes test)
    - tests/coach/test_citation_linter.py (existing existence-only test shape for track/key to mirror)
  </read_first>
  <action>
    Create tests/memory/test_retrieval.py with RED tests for the not-yet-existing MemoryRecall (import from vibemix.memory.retrieval): test_heartbeat_never_retrieves (on_event("HEARTBEAT", ...) returns [] AND the embedder's embed_query is never called — use a spy/counter _FakeEmbedder); test_below_floor_injects_nothing (seed records whose cosine to the query is < 0.7 → survivors []); test_current_session_excluded (seed a record under the current session_id → query_topk(exclude_session=current) drops it → []); test_deadline_miss_injects_nothing (the contract: a slow embed past RECALL_DEADLINE_S → get_latest() returns [] — exercise the service-level behavior the agent relies on; if deadline is enforced agent-side, assert get_latest() is [] after clear() and document that the agent-side wait_for is covered by Plan 04's static assert); test_embed_called_once (a single track-aware on_event triggers exactly one embed_query). Use synthetic L2-normalized 768-dim vectors via library._cosine.l2_normalize; never call a live API. Then extend tests/coach/test_citation_linter.py with test_recall_existence_only_valid: a registered [recall:<id>] (snapshot["recall"] contains the body) passes the existence-only branch (valid). These reference recall before it exists in EVIDENCE_SOURCES, so the linter test will fail until Plan 02 — that is the intended RED.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_retrieval.py --collect-only 2>&1 | grep -q 'test_heartbeat_never_retrieves\|test_below_floor_injects_nothing\|test_current_session_excluded\|test_deadline_miss_injects_nothing'</automated>
  </verify>
  <done>tests/memory/test_retrieval.py collects with all 5 named tests; tests/coach/test_citation_linter.py has test_recall_existence_only_valid. Both are RED (ImportError on vibemix.memory.retrieval / recall not in sources) — expected until Plans 02/03.</done>
</task>

<task type="auto">
  <name>Task 2: RED coach golden tests + headline poisoning RED + lockstep count updates</name>
  <read_first>
    - tests/state/test_coach.py:47-53 (test_evidence_line_silent_state_full_format — the byte-identical golden that MUST stay green)
    - src/vibemix/state/coach.py:54-58 (evidence_line signature), :96-109 (decks[…] gate template), :156 (registry_snapshot falsy gate)
    - tests/state/test_evidence_registry.py:210 (test_evidence_11_sources_constant_locked_GROUND02 — asserts exact 8-source set), :227 (test_evidence_12_registry_grammar_coherence — auto-iterates EVIDENCE_SOURCES)
    - tests/prompts/test_matrix.py:396 (test_o_citation_grammar_block_contains_eight_source_forms_and_multi_cite)
    - src/vibemix/prompts/matrix.py:101-117 (CITATION_GRAMMAR_BLOCK forms, [key: at :117)
    - tests/agent/test_dj_cohost_linter.py (existing linter-strip test shape in the same file)
    - src/vibemix/agent/dj_cohost.py:205-213 (_build_citation_strip, parse + chip allow-list), :1112 (whole-turn strip path)
  </read_first>
  <action>
    In tests/state/test_coach.py: add test_evidence_line_recall_block_present — construct a populated recall_moments list of 2 Record objects (record_id "20260520-2200:7", signature text) and assert evidence_line(state, recall_moments=[...]) contains the literal "FROM A PAST SESSION (not happening now): " fence AND both "[recall:20260520-2200:7]" tokens AND that the recall segment appears AFTER the live evidence (subordinate ordering). Add test_evidence_line_recall_empty_no_block — assert evidence_line(state, recall_moments=[]) == evidence_line(state) (the [] falsy gate emits nothing). Do NOT modify test_evidence_line_silent_state_full_format (:47) — it stays byte-identical and must pass at baseline AND after Plan 04.
    In tests/agent/test_dj_cohost_linter.py: add test_fabricated_recall_strips_turn — the headline poisoning RED: a reaction text containing a [recall:<unregistered-id>] whose record_id the registry never wrote → CitationLinter.check(..., mode="live") returns invalid → the whole turn strips. Mirror the existing fabricated-ev/fabricated-key strip test shape in the file.
    UPDATE the two lockstep count-tests to expect 9: tests/state/test_evidence_registry.py sources-constant test (add "recall" to the asserted set, 8→9) and tests/prompts/test_matrix.py grammar-forms test (8→9 forms + assert "[recall:" substring present). These updated assertions go RED now (recall not added until Plan 02) — intended.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_coach.py::test_evidence_line_silent_state_full_format -x</automated>
  </verify>
  <done>The silent-state golden still PASSES (untouched). The 2 new coach tests, the fabricated-recall poisoning test, and the 2 updated lockstep count-tests collect and are RED (recall vocabulary/block not implemented yet). RED set is the precise contract for Plans 02 + 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| past-session signature → live prompt | Raw stored text from memory.db crosses into the live reaction prompt — untrusted (could contain a [recall:…] lookalike). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Spoofing | fabricated [recall:<id>] in reaction text | mitigate | This plan AUTHORS the headline guard test (test_fabricated_recall_strips_turn) RED-first; the structural strip lands in Plans 02+04. A test gate ensures the guard cannot regress silently. |
| T-65-02 | Tampering | silent regex-whitelist gap (recall in EVIDENCE_SOURCES but not _SOURCE_ALT) | mitigate | The updated lockstep count-tests (8→9) + the fabricated-recall RED force sites 1 and 2 to land together in Plan 02; a half-edit leaves this plan's RED un-greened. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (research §Package Legitimacy Audit: zero net-new deps; pure in-repo + stdlib). N/A. |
</threat_model>

<verification>
- `tests/memory/test_retrieval.py` collects with the 5 named tests (RED on ImportError — expected).
- `tests/state/test_coach.py::test_evidence_line_silent_state_full_format` PASSES (untouched baseline).
- The 2 new coach tests + the fabricated-recall test + the 2 updated lockstep tests collect and are RED.
- No production source file is modified in this plan (test-only).
</verification>

<success_criteria>
All Wave-0 test files exist/updated per 65-VALIDATION.md §Wave 0 Requirements. The byte-identical-cold golden is green; every new/updated test is RED with a clear not-implemented signal (no collection errors). Downstream plans have a precise named-RED contract to turn green.
</success_criteria>

<output>
Create `.planning/phases/65-memory-retrieval-seam/65-01-SUMMARY.md` when done
</output>
