---
phase: 65-memory-retrieval-seam
plan: 02
type: execute
wave: 1
depends_on: ["65-01"]
files_modified:
  - src/vibemix/state/evidence_registry.py
  - src/vibemix/prompts/matrix.py
autonomous: true
requirements: [RECALL-01]
user_setup: []

must_haves:
  truths:
    - "recall is a registered existence-only evidence source (in EVIDENCE_SOURCES, out of _TIME_KEYED_SOURCES)"
    - "parse_citations matches [recall:<record_id>] — the regex alternation whitelists recall"
    - "The citation grammar block teaches the [recall:<record_id>] form"
    - "A fabricated [recall:…] CAN now reach the linter (no silent poisoning hole)"
  artifacts:
    - path: "src/vibemix/state/evidence_registry.py"
      provides: "recall added to EVIDENCE_SOURCES (site 1) + _SOURCE_ALT regex + EBNF docstring (site 2)"
      contains: "recall"
    - path: "src/vibemix/prompts/matrix.py"
      provides: "[recall:<record_id>] form in CITATION_GRAMMAR_BLOCK (site 3)"
      contains: "[recall:"
  key_links:
    - from: "EVIDENCE_SOURCES (site 1)"
      to: "_SOURCE_ALT regex (site 2)"
      via: "MUST land in the same commit — the silent-poisoning-hole guard"
      pattern: "recall"
    - from: "_SOURCE_ALT"
      to: "parse_citations"
      via: "regex alternation enables [recall:…] matching → linter can validate"
      pattern: "recall"
---

<objective>
Add the existence-only `recall` evidence source across the schema-mirror vocabulary sites that the registry/regex/grammar read — with ZERO new linter code (RECALL-01). The linter LOGIC is byte-unchanged: `recall` joins the existence-only set purely by being IN `EVIDENCE_SOURCES` and ABSENT from `_TIME_KEYED_SOURCES`. The dangerous omission this plan exists to prevent is the silent poisoning hole: site 1 without site 2.

Purpose: Make `[recall:<record_id>]` a citable-by-construction source. A `record_id` the registry never wrote → the existing `CitationLinter` existence-only branch strips the whole turn. The `key` source (Phase 59) is the exact precedent.
Output: 3 schema-mirror sites (1, 2, 3) updated; the 2 RED lockstep count-tests from Plan 01 turn GREEN.

CRITICAL: Sites 1 (EVIDENCE_SOURCES) and 2 (_SOURCE_ALT regex) MUST land in the SAME commit. Adding `recall` to EVIDENCE_SOURCES without adding it to the regex alternation = a fabricated `[recall:…]` is never parsed → never stripped → rides through (the exact poisoning the phase prevents).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-memory-retrieval-seam/65-RESEARCH.md
@.planning/phases/65-memory-retrieval-seam/65-PATTERNS.md

<interfaces>
<!-- VERIFIED against live source 2026-05-22. -->

src/vibemix/state/evidence_registry.py:
  :100-102  SCHEMA-MIRROR contract docstring (EVIDENCE_SOURCES is source-of-truth; keep _SOURCE_ALT etc. in lockstep)
  :103-105  EVIDENCE_SOURCES = frozenset({"ev","aud","midi","track","screen","mix","tend","key"})  # add "recall" → 9
  :117      _SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key"  # append "|recall"
  :118      _INNER_ATOM = rf"(?:{_SOURCE_ALT}):[^\s,\]]+"  # UNCHANGED — recall:<id> body has no ws/comma/bracket; parse_citations splits on FIRST ':', so record_id "20260520-2200:7" survives like "key:A:8A"
  _TIME_KEYED_SOURCES (citation_linter.py:52 = {"ev","aud","midi"})  # recall MUST stay OUT — existence-only

src/vibemix/prompts/matrix.py:
  :101-117  CITATION_GRAMMAR_BLOCK forms list; [key:<deck>:<camelot>] at :117  # add [recall:<record_id>] form

Registration precedent (NOT edited here — Plan 04 uses it): register_library bulk-writes track ids via write("track", id, 0.0) (evidence_registry.py:234); recall mirrors this in the agent.

DO-NOT (off-pattern):
  - Do NOT add recall to _TIME_KEYED_SOURCES (would force @t parse, break existence-only).
  - Do NOT add recall to memory/ingest.py:79 _SOURCE_ALT (ingest-time extraction; a stored past reaction never cited recall; leave ingest at 8 sources — flag this asymmetry in a comment).
  - Do NOT touch citation_linter.py logic (the existence-only branch at :212 already handles any new source).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add recall to EVIDENCE_SOURCES + _SOURCE_ALT regex + EBNF doc (sites 1+2, one commit)</name>
  <read_first>
    - src/vibemix/state/evidence_registry.py:100-135 (SCHEMA-MIRROR contract, EVIDENCE_SOURCES, _SOURCE_ALT, _INNER_ATOM, EBNF docstring)
    - src/vibemix/coach/citation_linter.py:52 (_TIME_KEYED_SOURCES — confirm recall stays OUT), :175-213 (existence-only dispatch — confirm no logic edit needed)
    - tests/state/test_evidence_registry.py:210 (sources-constant lockstep, updated to 9 in Plan 01), :227 (grammar-coherence auto-iterates EVIDENCE_SOURCES)
    - The Phase 59 key-source diff if findable in git log (the precedent edit shape)
  </read_first>
  <behavior>
    - EVIDENCE_SOURCES contains "recall" (9 sources); recall is NOT in _TIME_KEYED_SOURCES.
    - _SOURCE_ALT alternation matches "recall" so parse_citations emits [recall:<id>] atoms.
    - test_evidence_11_sources_constant_locked (updated 8→9) passes; test_evidence_12_registry_grammar_coherence (recall matchable + writable) passes.
  </behavior>
  <action>
    Add "recall" to the EVIDENCE_SOURCES frozenset (site 1) AND append "|recall" to the _SOURCE_ALT regex string (site 2) in the SAME edit/commit — these are the silent-poisoning-hole pair. Update the EBNF source-grammar docstring line to include `| 'recall'` and document the `recall:<record_id>` body. Leave _INNER_ATOM untouched (the existing `[^\s,\]]+` body already accepts `record_id` with its inner colon). Confirm (no edit) that recall is absent from _TIME_KEYED_SOURCES so it routes to the existence-only validation branch like key/track. Add a short comment near _SOURCE_ALT noting that memory/ingest.py:79's copy intentionally stays at 8 sources (recall is retrieval-time, never ingest-time) so a future maintainer does not "fix" the asymmetry. Per RECALL-01.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_evidence_registry.py -k 'sources_constant or grammar_coherence' -x</automated>
  </verify>
  <done>EVIDENCE_SOURCES has 9 members incl. recall; _SOURCE_ALT matches recall; recall absent from _TIME_KEYED_SOURCES; the two registry lockstep tests pass. ingest.py:79 untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add [recall:<record_id>] form to the citation grammar block (site 3)</name>
  <read_first>
    - src/vibemix/prompts/matrix.py:96-138 (the "8 source forms kept in lock-step with EVIDENCE_SOURCES" comment + the forms list, [key: at :117)
    - tests/prompts/test_matrix.py:396 (grammar-forms lockstep, updated to 9 + "[recall:" in Plan 01)
  </read_first>
  <behavior>
    - CITATION_GRAMMAR_BLOCK lists a 9th form: [recall:<record_id>] with a past-moment description + example.
    - The lock-step comment count updates 8→9.
    - test_o_citation_grammar_block_... (updated) passes.
  </behavior>
  <action>
    Append a 9th source form to CITATION_GRAMMAR_BLOCK: `[recall:<record_id>]  past-moment reference, e.g. [recall:20260520-2200:7]`. Update the "8 source forms" lock-step comment to "9 source forms". Keep the description verb-less/existence-only in tone (a reference to a past moment, not a live action) — recall parallels track/tend, not ev/key. Per RECALL-01.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/prompts/test_matrix.py -k citation_grammar_block -x</automated>
  </verify>
  <done>CITATION_GRAMMAR_BLOCK contains the [recall:<record_id>] form; the grammar-forms lockstep test passes (9 forms, [recall: present).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini-emitted citation → CitationLinter | An emitted [recall:<id>] must resolve to a registered record_id or the turn strips. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Spoofing | fabricated [recall:<id>] | mitigate | recall joins EVIDENCE_SOURCES (existence-only) → the unchanged linter existence-only branch (citation_linter.py:212) strips the whole turn on an unregistered record_id. Zero new linter code. |
| T-65-02 | Tampering | silent regex-whitelist gap | mitigate | Sites 1 (EVIDENCE_SOURCES) + 2 (_SOURCE_ALT) land in the SAME commit (Task 1). Without site 2, parse_citations never matches [recall:…] → fabricated cite rides un-stripped. Pinned by the updated lockstep tests + Plan 04's fabricated-recall RED. |
| T-65-03 | Tampering | recall mis-added to _TIME_KEYED_SOURCES or ingest.py:79 | mitigate | Task 1 explicitly keeps recall OUT of _TIME_KEYED_SOURCES (else @t parse breaks existence-only) and OUT of ingest.py:79 (retrieval-time, not ingest-time) with a comment guarding the asymmetry. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No installs (zero net-new deps per research). N/A. |
</threat_model>

<verification>
- `pytest tests/state/test_evidence_registry.py -k 'sources_constant or grammar_coherence'` passes (9 sources, recall matchable + writable).
- `pytest tests/prompts/test_matrix.py -k citation_grammar_block` passes (9 forms, [recall: present).
- `grep -n recall src/vibemix/coach/citation_linter.py` returns NOTHING (linter logic byte-unchanged).
- `grep -c recall src/vibemix/memory/ingest.py` (excluding the guard comment) confirms recall NOT added to ingest's _SOURCE_ALT.
- No regression: `PYTHONPATH=src python3 -m pytest -q tests/state tests/prompts` shows no NEW failures beyond the still-RED Plan 04 targets.
</verification>

<success_criteria>
RECALL-01 vocabulary lands citably-by-construction across sites 1–3 with zero new linter code. The two RED lockstep count-tests (Plan 01) are GREEN. A fabricated `[recall:…]` is now parseable and therefore strippable — the silent poisoning hole is closed. The fabricated-recall whole-turn-strip test (Plan 01) remains RED only because the agent wiring/registration (Plan 04) is not yet in place.
</success_criteria>

<output>
Create `.planning/phases/65-memory-retrieval-seam/65-02-SUMMARY.md` when done
</output>
