---
phase: 65-memory-retrieval-seam
plan: 03
type: execute
wave: 1
depends_on: ["65-01"]
files_modified:
  - src/vibemix/memory/retrieval.py
autonomous: true
requirements: [RECALL-03, RECALL-04]
user_setup: []

must_haves:
  truths:
    - "A non-track-aware event (HEARTBEAT) never embeds and returns []"
    - "Below-floor (cosine < 0.7) hits are dropped — survivors []"
    - "The current session is excluded from its own retrieval"
    - "retrieval.py imports no live-reaction-path module and makes only the embed call (auto-gates pass)"
  artifacts:
    - path: "src/vibemix/memory/retrieval.py"
      provides: "MemoryRecall enrichment service + build_recall_query + RECALL_* constants — a Grounding clone"
      contains: "class MemoryRecall"
      min_lines: 40
  key_links:
    - from: "MemoryRecall.on_event"
      to: "MemoryStore.query_topk(exclude_session=...)"
      via: "current-session-excluded retrieval over the pre-embedded corpus"
      pattern: "query_topk\\(.*exclude_session"
    - from: "MemoryRecall.on_event"
      to: "LibraryEmbedder.embed_query"
      via: "single FLEX query embed, gated to track-aware events"
      pattern: "embed_query"
---

<objective>
Ship the `MemoryRecall` enrichment service in `src/vibemix/memory/retrieval.py` — a ~40-line clone of `library/grounding.py::Grounding`: lock-guarded `_latest`, `on_event`/`get_latest`/`clear`, event-gated to track-aware events, similarity-floored, current-session-excluded. It reads `ev.state` (via the agent passing a query string) + `MemoryStore`; it writes only its own `_latest`. NEVER writes `MusicState`, never opens a socket, never imports the live reaction path. Lives in `memory/` so the shipped no-live-path + no-extraction gates auto-cover it.

Purpose: The structural anti-poisoning core (RECALL-03: below-floor → nothing, current-session excluded) + the off-path/budget controls (RECALL-04: event-gate, single FLEX embed, cosine-only conservative default). The cosine-vs-time blend / half-life / exact floor are DEFERRED to the Kaan-ear pass (KAAN-ACTION) — ship cosine-only + 0.7 floor.
Output: `src/vibemix/memory/retrieval.py`; the MemoryRecall RED tests (Plan 01) turn GREEN; the shipped memory/ auto-gates cover the new file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-memory-retrieval-seam/65-RESEARCH.md
@.planning/phases/65-memory-retrieval-seam/65-PATTERNS.md
@.planning/phases/63-memory-store/63-03-SUMMARY.md
@.planning/phases/64-session-ingest/64-RESEARCH.md

<interfaces>
<!-- VERIFIED against live source 2026-05-22. -->

Architectural twin to clone — src/vibemix/library/grounding.py::Grounding (:169-220):
  __init__(embedder, store): self._lock = threading.Lock(); self._latest = None
  on_event(event_type, ...) -> result   # gated, stores _latest under lock, returns result
  get_latest_*() -> result | None        # lock-guarded read
  clear() -> None                        # lock-guarded reset, called end-of-turn
  CITATION_THRESHOLD = 0.7 (:39)         # the cosine floor to MIRROR (copy value, do NOT import)

Read seam — src/vibemix/memory/store.py:
  :147  @dataclass Record(record_id, session_id, ts, kind, signature, score=0.0)
  :312  query_topk(query_embedding, k=8, *, exclude_session=None) -> list[Record]
        # exclude_session drops one session's records BEFORE cosine_topk ranking (:329-337)
        # .score = cosine on query results

Live query embed — src/vibemix/library/embed.py:
  :362  LibraryEmbedder.embed_query(query: str) -> np.ndarray   # FLEX, proxy-only, 768-dim L2-normalized, NO content-hash cache

Event taxonomy — src/vibemix/state/event.py (string literals, VERIFIED): TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, HEARTBEAT, KAAN_SPOKE, MANUAL, KEY_CLASH, TRANSITION_OPPORTUNITY.

Query template head (Phase 64 coach_line signature, 64-RESEARCH.md): "coach_line | track=… | phase=… | deck=… | event=…" — at retrieval there is no said: yet, so build_recall_query produces the context PREFIX only (omit cite=/said:) for query↔stored embedding symmetry.

Constants to define in this module (research §Retrieval Policy Defaults):
  RECALL_SIMILARITY_FLOOR = 0.7   # mirrors library/grounding.py:39 CITATION_THRESHOLD (COPY with comment, do NOT import — avoids memory→library coupling)
  RECALL_TOP_K = 3
  RECALL_DEADLINE_S = 0.5         # the agent (Plan 04) enforces via asyncio.wait_for; defined here as the shared default
  RECALL_EVENT_GATE = frozenset({"TRACK_CHANGE", "PHASE", "LAYER_ARRIVAL"})   # NEVER HEARTBEAT/MIX_MOVE/KAAN_SPOKE/MANUAL

DO-NOT (off-pattern):
  - Do NOT import MusicState / the coach loop / ws_bus / the agent at module level (no-live-path gate).
  - Do NOT call any generation model — only embed_query (no-extraction gate).
  - Do NOT hardcode a model literal (model_literal_gate — embed_query resolves the model internally).
  - Do NOT add a time-decay blend (cosine-only v1; decay = KAAN-ACTION).
  - Do NOT open a socket / write MusicState.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryRecall service + constants + build_recall_query</name>
  <read_first>
    - src/vibemix/library/grounding.py:39,45-47,169-220 (Grounding shape, CITATION_THRESHOLD, TRACK_AWARE_EVENTS — note recall's gate deliberately differs: TRACK_CHANGE/PHASE/LAYER_ARRIVAL, NOT MIX_MOVE)
    - src/vibemix/memory/store.py:147 (Record), :312-340 (query_topk + exclude_session)
    - src/vibemix/library/embed.py:362-369 (embed_query — no cache)
    - src/vibemix/memory/ingest.py (Phase 64 coach_line template constants — reuse for build_recall_query head if exported; else replicate the prefix shape)
    - tests/memory/test_retrieval.py (Plan 01 — the RED contract this task greens)
    - tests/memory/test_no_live_path_import.py and tests/memory/test_no_extraction.py (the auto-gates this file must satisfy)
  </read_first>
  <behavior>
    - on_event("HEARTBEAT", q, sess) returns [] and does NOT call embed_query (test_heartbeat_never_retrieves).
    - on_event("TRACK_CHANGE", q, sess) calls embed_query exactly once, then query_topk(qvec, RECALL_TOP_K, exclude_session=sess), then filters score >= RECALL_SIMILARITY_FLOOR (test_embed_called_once).
    - below-floor hits dropped → survivors [] (test_below_floor_injects_nothing).
    - current session excluded via exclude_session (test_current_session_excluded).
    - get_latest() returns a copy of survivors under the lock; clear() resets to [] (so a deadline-miss / next HEARTBEAT replays nothing).
  </behavior>
  <action>
    Create src/vibemix/memory/retrieval.py mirroring library/grounding.py::Grounding. Define the module constants RECALL_SIMILARITY_FLOOR=0.7 (with a "# mirrors library/grounding.py:39 CITATION_THRESHOLD" comment — COPY the value, do not import), RECALL_TOP_K=3, RECALL_DEADLINE_S=0.5, RECALL_EVENT_GATE=frozenset({"TRACK_CHANGE","PHASE","LAYER_ARRIVAL"}). Implement class MemoryRecall(embedder: LibraryEmbedder, store: MemoryStore) with a threading.Lock + _latest: list[Record]; on_event(event_type, query_text, current_session_id) returns [] early if event_type not in RECALL_EVENT_GATE (no embed), else embeds the query once, queries query_topk(qvec, RECALL_TOP_K, exclude_session=current_session_id), filters survivors by score >= RECALL_SIMILARITY_FLOOR, stores survivors under the lock, returns them; get_latest() returns a lock-guarded copy; clear() resets _latest under the lock. Add build_recall_query(ev) -> str that builds the Phase-64 coach_line context prefix (track/phase/deck/event) from ev.state + ev.type, omitting cite=/said:, for query↔stored embedding symmetry — reuse Phase 64's template constants if importable from memory/ingest.py (same package, no live-path violation), else replicate the prefix string shape with a comment pointing at the ingest template. Cosine-only — NO time-decay term (defer to KAAN-ACTION). Import nothing from the live reaction path, coach loop, ws_bus, or agent. Per RECALL-03 + RECALL-04.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_retrieval.py tests/memory/test_no_live_path_import.py tests/memory/test_no_extraction.py tests/repo/test_model_literal_gate.py -x</automated>
  </verify>
  <done>MemoryRecall ships; the 5 MemoryRecall RED tests pass; the no-live-path / no-extraction / model-literal gates pass with retrieval.py covered. No MusicState write, no socket, no generation call, no time-decay blend, no hardcoded model literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| memory.db corpus → live prompt enrichment | MemoryRecall reads pre-embedded past records and surfaces survivors for the live prompt. |
| MemoryRecall → live reaction path | One-way: the agent (Plan 04) reads MemoryRecall; MemoryRecall never reaches back into the live path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-04 | Spoofing | below-floor (irrelevant) moment injected | mitigate | Survivors filtered by score >= RECALL_SIMILARITY_FLOOR (0.7); below-floor → []. The floor is the dominant structural poisoning defense. |
| T-65-05 | Spoofing | current session "remembers itself" | mitigate | query_topk(..., exclude_session=current_session_id) drops the live session's own records before ranking (store.py:329-337). |
| T-65-06 | DoS (cost/UX) | budget blowout / TTFT regression | mitigate | Event-gate (no HEARTBEAT — the highest-frequency class) → one FLEX embed per track-aware event only; corpus pre-embedded (no re-embed). The agent (Plan 04) bounds the embed off-loop with RECALL_DEADLINE_S. |
| T-65-07 | Tampering (invariant break) | live-path coupling via the retrieval import | mitigate | retrieval.py imports no live-reaction-path module; reads ev.state (read-only via passed query string) + MemoryStore; writes only _latest; no socket; no MusicState write. The shipped no-live-path/no-extraction gates auto-cover memory/*.py. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No installs (zero net-new deps per research). N/A. |
</threat_model>

<verification>
- `pytest tests/memory/test_retrieval.py` — all 5 MemoryRecall tests green.
- `pytest tests/memory/test_no_live_path_import.py tests/memory/test_no_extraction.py` — green WITH retrieval.py in scope (the globs see it).
- `pytest tests/repo/test_model_literal_gate.py` — green (no model literal in retrieval.py).
- `grep -nE 'MusicState|ws_bus|coach_loop|websockets|from vibemix.agent|generate' src/vibemix/memory/retrieval.py` returns NOTHING.
- `grep -n 'exp(\|decay\|half_life\|half-life' src/vibemix/memory/retrieval.py` returns NOTHING (cosine-only v1).
</verification>

<success_criteria>
The off-path, event-gated, floored, current-session-excluded retrieval service ships as a Grounding clone with conservative cosine-only defaults. RECALL-03 (below-floor → nothing; current-session excluded) and RECALL-04 (event-gate, single FLEX embed, no live-path coupling, no extraction, no model literal) hold structurally. The cosine-vs-time blend / half-life / exact floor remain DEFERRED to the Kaan-ear pass (KAAN-ACTION) — not an engineering blocker.
</success_criteria>

<output>
Create `.planning/phases/65-memory-retrieval-seam/65-03-SUMMARY.md` when done
</output>
