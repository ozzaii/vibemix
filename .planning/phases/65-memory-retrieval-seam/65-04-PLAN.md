---
phase: 65-memory-retrieval-seam
plan: 04
type: execute
wave: 2
depends_on: ["65-02", "65-03"]
files_modified:
  - src/vibemix/state/coach.py
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/__main__.py
autonomous: true
requirements: [RECALL-01, RECALL-02, RECALL-03, RECALL-04]
user_setup: []

must_haves:
  truths:
    - "evidence_line(state) with recall_moments=None is byte-identical to the v5.0 silent golden"
    - "A populated recall_moments appends a PAST-tense fenced, subordinate recall[…] block with [recall:<id>] tokens"
    - "Survivor record_ids are registered before the LLM call; a fabricated [recall:<id>] strips the whole turn"
    - "Retrieval runs off-loop pre-dispatched with a hard deadline; a miss → no block, no TTFT regression"
  artifacts:
    - path: "src/vibemix/state/coach.py"
      provides: "recall_moments kwarg + gated recall[…] block (verbatim decks[…] shape) threaded through build_prompt → evidence_line"
      contains: "FROM A PAST SESSION"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "MemoryRecall wiring: off-loop pre-dispatch + deadline, register survivors, thread recall_moments, clear() after turn — behind a recall_enabled flag"
      contains: "recall"
  key_links:
    - from: "dj_cohost.py set_next_event seam"
      to: "MemoryRecall.on_event (off-loop via run_in_executor + wait_for)"
      via: "pre-dispatch on a track-aware event with RECALL_DEADLINE_S — never inline await in llm_node"
      pattern: "run_in_executor|wait_for"
    - from: "dj_cohost.py llm_node"
      to: "registry.write('recall', record_id, t) + build_prompt(recall_moments=...)"
      via: "register survivors before the snapshot, thread into the prompt"
      pattern: "recall"
    - from: "evidence_line recall block"
      to: "the byte-identical cold golden"
      via: "if recall_moments: falsy gate (None AND [] → nothing)"
      pattern: "if recall_moments"
---

<objective>
Wire the retrieval seam into the live reaction path: the gated `recall[…]` block in `coach.py::evidence_line` (a verbatim copy of the Phase 59 `decks[…]` gate → cold golden byte-identical) and the agent orchestration in `dj_cohost.py` (off-loop pre-dispatch + hard deadline, register survivors before the LLM call, thread `recall_moments` into `build_prompt`, `clear()` after each turn) — behind a `recall_enabled` flag (default OFF in wiring; the live-relevance veto flip is KAAN-ACTION). This is the ONLY plan that touches the live reaction path.

Purpose: Close RECALL-02 (gated block, byte-identical cold), the past-tense fence + registration half of RECALL-03/01, and the TTFT-unchanged half of RECALL-04. The headline poisoning gate (fabricated `[recall:]` strips the turn) goes fully green here because survivors are registered before the once-per-turn snapshot.
Output: `coach.py` + `dj_cohost.py` + `__main__.py` edited; the cold golden stays green; the populated/empty recall tests, the fabricated-recall poisoning test, the registered-recall-passes test, and the deadline-miss test all turn GREEN. Full suite at the live-tuning-or-brain baseline (zero NEW failures).

CRITICAL: NEVER inline-`await embed_query` in `llm_node` (TTFT regression). Pre-dispatch `MemoryRecall.on_event` off-loop at the `set_next_event` seam with `asyncio.wait_for(timeout=RECALL_DEADLINE_S)`; `llm_node` pulls `get_latest()` — `[]` if not ready → no block. Do NOT add `recall` to the `_build_citation_strip:213` chip allow-list (the recall chip is Phase 66; recall is verb-less existence-only, parallels track/tend).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-memory-retrieval-seam/65-RESEARCH.md
@.planning/phases/65-memory-retrieval-seam/65-PATTERNS.md

<interfaces>
<!-- VERIFIED against live source 2026-05-22. -->

src/vibemix/state/coach.py:
  :54-58  def evidence_line(state, *, registry_snapshot=None) -> str   # add recall_moments: list[Record] | None = None
  :96-109 the decks[…] gate (VERBATIM template for the recall[…] block — falsy gate "if state.deck_state.decks:")
  :156    "if registry_snapshot:" — the default-None falsy-gate precedent
  :320-351 def build_prompt(ev, *, registry_snapshot=None, diet=False); calls evidence_line(ev.state, registry_snapshot=...) at :351
  :166    _evidence_line_compact (the diet path — NO recall block; diet = ACK_ELIGIBLE_EVENTS incl HEARTBEAT, never a retrieval event — CORRECT to skip)
  Phase 60 past-tense precedent: TRANSITION_OPPORTUNITY "PAST-TENSE only … the moment's already gone" (:291-316)

The recall[…] block to append (additive, gated identically, PAST-tense fenced — RECALL-02/03):
  if recall_moments:                       # None or [] → emit NOTHING (cold golden byte-identical)
      parts = ["[recall:" + m.record_id + "] " + m.signature for m in recall_moments]
      e.append("FROM A PAST SESSION (not happening now): " + " || ".join(parts))
  # Appended AFTER the live evidence (subordinate; trust-the-audio). The diet/compact path gets NO recall block.

src/vibemix/agent/dj_cohost.py (VERIFIED):
  :489  def set_next_event(ev): self._pending_event = ev   # THE pre-dispatch seam — kick MemoryRecall.on_event off-loop here for track-aware events
  :508  ev = self._pending_event; self._pending_event = None  (in llm_node)
  :523  snapshot = self._registry.snapshot() if self._registry is not None else None  # once-per-turn — register survivors BEFORE this
  :532  diet = ev_type in ACK_ELIGIBLE_EVENTS
  :537-545 AICoach.build_prompt(ev, registry_snapshot=snapshot, diet=diet)  # add recall_moments=
  :205-213 _build_citation_strip: parse_citations (regex-driven, already matches recall via Plan 02 site 2) + chip allow-list ("ev","mix","midi","key") at :213 — DO NOT add recall here (Phase 66 chip)
  :1112 the whole-turn strip path when the linter returns invalid
  self._recorder.session_dir (:553) — basename = current_session_id
  Grounding wiring precedent: __main__.py wires grounding via an agent kwarg ("agent reads grounding via kwargs"); mirror with a recall (MemoryRecall) kwarg + recall_enabled flag, default the agent to None/disabled when no recall service is passed (backward-compat with all existing dj_cohost tests).

From src/vibemix/memory/retrieval.py (Plan 03): MemoryRecall.on_event/get_latest/clear, build_recall_query(ev), RECALL_DEADLINE_S, RECALL_EVENT_GATE.

DO-NOT:
  - No inline await embed_query in llm_node (TTFT regression).
  - No recall in _build_citation_strip:213 chip allow-list (Phase 66).
  - No new socket / no MusicState write from the agent's recall wiring.
  - Default recall service None so existing dj_cohost tests stay byte-identical (the cold/feature-off path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gated recall[…] block in coach.py (verbatim decks[…] shape, byte-identical cold)</name>
  <read_first>
    - src/vibemix/state/coach.py:54-58 (evidence_line signature), :96-109 (decks[…] gate — copy this falsy-gate shape), :156 (registry_snapshot None-gate), :166 (_evidence_line_compact — no recall block), :320-351 (build_prompt threading)
    - src/vibemix/memory/store.py:147 (Record — recall_moments element type)
    - tests/state/test_coach.py:47 (the silent golden — MUST stay byte-identical), plus the Plan-01 RED tests test_evidence_line_recall_block_present / test_evidence_line_recall_empty_no_block
  </read_first>
  <behavior>
    - evidence_line(state) and evidence_line(state, recall_moments=None) and evidence_line(state, recall_moments=[]) all == the v5.0 silent golden string (falsy gate emits nothing).
    - evidence_line(state, recall_moments=[Record, Record]) appends "FROM A PAST SESSION (not happening now): " + the [recall:<id>] tokens, AFTER the live evidence.
    - build_prompt threads recall_moments through to evidence_line; the diet/compact path appends NO recall block.
  </behavior>
  <action>
    Add a keyword-only `recall_moments: list[Record] | None = None` param to evidence_line (mirror the registry_snapshot=None default at :57). After the live evidence is assembled (append AFTER hearing/track/decks so recall is subordinate — trust-the-audio), add the gated block copying the decks[…] falsy-gate shape verbatim: `if recall_moments:` build the parts list `f"[recall:{m.record_id}] {m.signature}"` and append `"FROM A PAST SESSION (not happening now): " + " || ".join(parts)`. None AND [] are both falsy → zero bytes appended → cold golden byte-identical. Thread `recall_moments=` through build_prompt (add the same keyword-only default-None param) into the evidence_line call at :351. Do NOT add the block to _evidence_line_compact (diet path never retrieves). Per RECALL-02 + RECALL-03 (past-tense fence).
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_coach.py -x</automated>
  </verify>
  <done>All of tests/state/test_coach.py green: the silent golden byte-identical with the default kwarg; the populated path shows the past-tense fence + tokens after the live evidence; the empty/[] path emits no block. The diet/compact path is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent wiring — off-loop pre-dispatch + deadline, register survivors, thread + clear, behind recall_enabled</name>
  <read_first>
    - src/vibemix/agent/dj_cohost.py:489-500 (set_next_event seam), :502-545 (llm_node: ev pull, snapshot at :523, build_prompt at :537-545), :205-213 (_build_citation_strip chip allow-list — do NOT touch), :1112 (strip path), :553 (recorder.session_dir)
    - src/vibemix/library/grounding.py:188-220 (the on_event/get_latest/clear dispatch shape) + how __main__.py wires grounding via kwarg (around :921-942)
    - src/vibemix/memory/retrieval.py (Plan 03 — MemoryRecall API + RECALL_DEADLINE_S + RECALL_EVENT_GATE + build_recall_query)
    - src/vibemix/state/evidence_registry.py:200,234 (write / register_library — the registration mirror)
    - tests/agent/test_dj_cohost_linter.py (test_fabricated_recall_strips_turn — Plan 01 RED), tests/memory/test_retrieval.py::test_deadline_miss_injects_nothing
    - existing tests/agent/ dj_cohost construction tests (confirm default-None recall keeps them byte-identical)
  </read_first>
  <behavior>
    - With no recall service (default None / recall_enabled False): the agent behaves byte-identically to today (all existing dj_cohost tests pass; cold golden path).
    - On a track-aware event (TRACK_CHANGE/PHASE/LAYER_ARRIVAL): MemoryRecall.on_event is dispatched off-loop (run_in_executor) bounded by asyncio.wait_for(RECALL_DEADLINE_S); a miss/timeout → get_latest() [] → no block.
    - In llm_node: survivors are registered via registry.write("recall", record_id, t) BEFORE the once-per-turn snapshot (:523); recall_moments threaded into build_prompt; recall.clear() after the turn.
    - A fabricated [recall:<unregistered>] in the reaction strips the whole turn (test_fabricated_recall_strips_turn green).
    - No inline await embed_query in llm_node; recall NOT added to the _build_citation_strip:213 chip allow-list.
  </behavior>
  <action>
    Add an optional MemoryRecall to the agent (constructor kwarg + recall_enabled flag, mirroring the grounding-via-kwargs pattern in __main__.py); default None/disabled so existing dj_cohost tests stay byte-identical. At the set_next_event seam (:489), when recall is enabled AND ev.type is in RECALL_EVENT_GATE, pre-dispatch self._recall.on_event(ev.type, build_recall_query(ev), current_session_id) off the loop via loop.run_in_executor(None, ...) wrapped in asyncio.wait_for(..., timeout=RECALL_DEADLINE_S); on TimeoutError/exception, swallow and leave _latest empty (late memory is worse than no memory). Derive current_session_id from self._recorder.session_dir basename. In llm_node, BEFORE the snapshot at :523: recall_moments = self._recall.get_latest() if recall enabled else []; for each m, self._registry.write("recall", m.record_id, ev.state.set_seconds) to register it; then take the snapshot (so survivors are in it); thread recall_moments= into both build_prompt call sites (:537 and :541); after the turn completes, call self._recall.clear() so the next HEARTBEAT turn replays nothing. Do NOT inline-await the embed. Do NOT add recall to the _build_citation_strip:213 chip allow-list (Phase 66 chip; recall parses via Plan 02 site 2 and reaches the linter automatically). Wire __main__.py to construct + pass the MemoryRecall only behind the recall_enabled flag defaulting OFF so the cold/feature-off default stays byte-identical; note the live-enable flip as a KAAN-ACTION (the seam ships wired + tested regardless). Per RECALL-01/02/03/04.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/agent/test_dj_cohost_linter.py::test_fabricated_recall_strips_turn tests/coach/test_citation_linter.py::test_recall_existence_only_valid tests/memory/test_retrieval.py::test_deadline_miss_injects_nothing tests/agent -x</automated>
  </verify>
  <done>The fabricated-recall poisoning test strips the whole turn; the registered-recall-passes test is green; the deadline-miss test confirms no block; all existing tests/agent tests pass (default-None byte-identical). No inline await embed_query in llm_node; recall absent from the :213 chip allow-list.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate — cold golden + baseline + static no-inline-await assert</name>
  <read_first>
    - .planning/phases/65-memory-retrieval-seam/65-VALIDATION.md (the live-tuning-or-brain 8-failure baseline; zero NEW failures rule)
    - src/vibemix/agent/dj_cohost.py:502-545 (llm_node — confirm no `await ...embed_query` inline)
  </read_first>
  <action>
    Run the full suite and confirm zero NEW failures beyond the documented live-tuning-or-brain baseline (per 65-VALIDATION.md §Sampling Rate). Confirm the byte-identical-cold golden (tests/state/test_coach.py::test_evidence_line_silent_state_full_format) is green. Add a static assertion (extend tests/memory/test_retrieval.py or a small repo-level check) that llm_node does NOT inline-await embed_query — grep llm_node's source range for `embed_query` and assert it is reached only via the off-loop pre-dispatch path, not an inline await in the reaction body (the RECALL-04 TTFT-unchanged static gate). This task adds no production behavior — it pins the phase gates green.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_coach.py::test_evidence_line_silent_state_full_format tests/memory tests/state tests/prompts tests/agent tests/coach -x</automated>
  </verify>
  <done>The cold golden is byte-identical green; all phase target tests pass; the full suite shows zero NEW failures vs the live-tuning-or-brain baseline; the static no-inline-await-embed_query assertion passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| past-session signature → live prompt | Raw stored text crosses into the live reaction prompt — past-tense fenced + subordinate. |
| Gemini-emitted [recall:<id>] → CitationLinter | Must resolve to a registered record_id or the whole turn strips. |
| MemoryRecall (off-loop) → llm_node | One-way pull via get_latest(); a missed deadline yields [] — never blocks the turn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Spoofing | fabricated [recall:<id>] (THE headline threat) | mitigate | Survivors registered via registry.write("recall", record_id, t) BEFORE the once-per-turn snapshot; an unregistered record_id → existence-only branch → invalid → whole-turn strip (dj_cohost.py:1112). Pinned by test_fabricated_recall_strips_turn. |
| T-65-03 | Tampering | injected text in a retrieved signature (prompt-injection via stored past reaction) | mitigate | The "FROM A PAST SESSION (not happening now)" fence subordinates recall to live audio; any citation must resolve to a registered record_id; the signature is raw text (no extraction), carrying no instruction the linter trusts. |
| T-65-06 | DoS (UX) | TTFT regression / live-path stall | mitigate | Off-loop run_in_executor + asyncio.wait_for(RECALL_DEADLINE_S); miss → get_latest() [] → no block. No inline await embed_query in llm_node (Task 3 static gate). Single-in-flight reaction gate untouched. |
| T-65-08 | Tampering (scope creep) | premature Phase-66 recall chip | mitigate | recall NOT added to _build_citation_strip:213 chip allow-list — parses via site 2 + reaches the linter, but yields no chip (parallels track/tend). The chip is Phase 66. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No installs (zero net-new deps per research). N/A. |
</threat_model>

<verification>
- `pytest tests/state/test_coach.py` — cold golden byte-identical + populated/empty recall tests green.
- `pytest tests/agent/test_dj_cohost_linter.py::test_fabricated_recall_strips_turn` — exits 0 (the headline poisoning gate).
- `pytest tests/coach/test_citation_linter.py::test_recall_existence_only_valid` — exits 0.
- `pytest tests/memory/test_retrieval.py::test_deadline_miss_injects_nothing` — exits 0.
- `grep -n 'await .*embed_query' src/vibemix/agent/dj_cohost.py` returns NOTHING (no inline embed await on the reaction path).
- `grep -n 'recall' src/vibemix/agent/dj_cohost.py` shows wiring/registration only — recall NOT in the `:213` chip allow-list tuple.
- Full suite: zero NEW failures vs the live-tuning-or-brain baseline (65-VALIDATION.md).
</verification>

<success_criteria>
The retrieval seam is live behind a default-OFF `recall_enabled` flag: the gated recall[…] block is byte-identical when cold and PAST-tense fenced + subordinate when populated; survivors are registered before the snapshot so a fabricated `[recall:]` strips the whole turn (RECALL-01); retrieval runs off-loop with a hard deadline so TTFT is unchanged (RECALL-04). The live-relevance veto flip + threshold/blend tuning are KAAN-ACTION (autonomous `fully` — not an engineering blocker). All four cardinal invariants hold by construction.
</success_criteria>

<output>
Create `.planning/phases/65-memory-retrieval-seam/65-04-SUMMARY.md` when done
</output>
