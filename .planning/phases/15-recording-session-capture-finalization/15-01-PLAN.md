---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/ui/src/ipc/messages.schema.json
  - src/vibemix/ui_bus/messages.py
  - tauri/ui/src/ipc/messages.ts
  - scripts/check_ipc_schema.py
  - tests/ui_bus/test_recordings_messages.py
autonomous: true
requirements:
  - REC-05
  - REC-06
tags: [ipc, schema, drift-gate, recordings]

must_haves:
  truths:
    - "Schema source-of-truth declares 34 ipc.* families (27 baseline + recordings.list / list_result / delete / delete_ack / usage / events / events_result = 7 new oneOf entries — note: list+list_result, delete+delete_ack, and events+events_result are SEPARATE families per Phase 12 settings.set/state convention; usage is push-only). Count delta: 27 → 34 (+7)."
    - "Python @dataclass wrapper count equals schema oneOf count; `python scripts/check_ipc_schema.py` exits 0."
    - "TS codegen produces a discriminated union containing all 7 new RecordingsXxx members; `npm run check:ipc` exits 0."
    - "session_dir payload field accepts ONLY `^[0-9]{8}-[0-9]{6}$` (V12 path-traversal gate enforced at the schema) on `recordings.delete`, `recordings.delete_ack`, `recordings.events`, and on every `RecordingSummary` element."
  artifacts:
    - path: "tauri/ui/src/ipc/messages.schema.json"
      provides: "7 new oneOf entries (recordings.list, list_result, delete, delete_ack, usage, events, events_result) + RecordingSummary payload definition shared between list_result and usage + RecordingEvent payload element used by events_result"
      contains: "ipc.recordings.list"
    - path: "src/vibemix/ui_bus/messages.py"
      provides: "7 new @dataclass(frozen=True, slots=True) wrappers + 8 payload structs (incl. RecordingSummary element struct)"
      contains: "class RecordingsListResult"
    - path: "tauri/ui/src/ipc/messages.ts"
      provides: "Codegen output (committed alongside schema per Phase 11 W0 convention)"
      contains: "RecordingsListResult"
    - path: "scripts/check_ipc_schema.py"
      provides: "Drift-gate baseline count bumped 27 → 34"
      contains: "34"
    - path: "tests/ui_bus/test_recordings_messages.py"
      provides: "Roundtrip tests for 7 new families + session_dir regex rejection test + events_result event-list shape test"
  key_links:
    - from: "tauri/ui/src/ipc/messages.schema.json"
      to: "src/vibemix/ui_bus/messages.py"
      via: "scripts/check_ipc_schema.py wrapper-count parity (34 == 34)"
      pattern: "ipc\\.recordings\\."
    - from: "tauri/ui/src/ipc/messages.schema.json"
      to: "tauri/ui/src/ipc/messages.ts"
      via: "tauri/ui/scripts/codegen-ipc.mjs (json-schema-to-typescript)"
      pattern: "RecordingsListResult"
---

<objective>
Add the 4 new IPC FAMILIES (7 distinct schema entries — `recordings.list` / `recordings.list_result` / `recordings.delete` / `recordings.delete_ack` / `recordings.usage` / `recordings.events` / `recordings.events_result`) that Plan 15-03 (RecordingsIndex + RetentionSweep + events sidecar handler) emits and Plan 15-04 + 15-05 (UI + drawer wiring) consume. Follow the Phase 11 W0 schema-as-source-of-truth pattern: hand-written `@dataclass(frozen=True, slots=True)` wrappers in `src/vibemix/ui_bus/messages.py` + JSON Schema oneOf entries in `tauri/ui/src/ipc/messages.schema.json` + codegen → `messages.ts` + drift gate `scripts/check_ipc_schema.py` count parity. **Count correction:** current IPC count is 27 (Phase 13 added `mascot.mood_change`); Phase 15 adds 4 families surfaced as 7 schema entries (request/response pairs are separate `oneOf` entries by Phase 12 precedent — see `settings.set`/`settings.state` + `calibration.probe_audio`/`audio_result` precedents). Final count: 34.

Purpose: Phase 15's downstream plans depend on these wire shapes. Without them, RecordingsIndex has nowhere to emit to, SettingsDrawer has no client surface to consume, and the recording-row transcript overlay has no way to fetch events.jsonl. This plan ships only the contract — no sidecar handlers, no UI consumers. Per the Wave-0 interface-first pattern, this is the dependency root.

Output: 7 oneOf entries in the schema + 8 new payload structs in messages.py (5 result/request payloads + RecordingSummary + RecordingEvent + empty-request struct shared where applicable) + 7 new wrapper classes + drift-gate count bumped to 34 + codegen output committed + 1 new test file (`test_recordings_messages.py`) with roundtrip + path-traversal regression coverage + events_result list shape coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-recording-session-capture-finalization/15-CONTEXT.md
@.planning/phases/15-recording-session-capture-finalization/15-RESEARCH.md
@.planning/phases/15-recording-session-capture-finalization/15-UI-SPEC.md
@src/vibemix/ui_bus/messages.py
@tauri/ui/src/ipc/messages.schema.json
@scripts/check_ipc_schema.py
@tauri/ui/scripts/codegen-ipc.mjs

<interfaces>
<!-- Extracted Phase 11 W0 / Phase 12 patterns the executor will reproduce exactly.
     No codebase exploration needed — these are the contracts. -->

From src/vibemix/ui_bus/messages.py — wrapper-class pattern (precedent: SettingsSet / SettingsState / MascotMoodChange):
- `@dataclass(frozen=True, slots=True)` on every payload struct AND every top-level wrapper
- Top-level wrappers have exactly three fields: `type: Literal["ipc.<family>"]`, `ts: str`, `payload: <Payload struct>`
- `@classmethod make(...) -> "<Wrapper>"` constructs the wrapper using `_now_iso()` and a fresh payload
- `to_json(self) -> str` calls module-level `_serialize(self)` which recursively converts tuples to lists before `json.dumps`
- Frozen dataclasses with sequences MUST use `tuple[...]` (not list) for hashability — the existing `_tuples_to_lists` helper handles serialization
- Array-element structs (precedent: `LevelPair`, `MetersTriple`, `MidiEventEntry`) are top-level @dataclass too, used only inside payloads

From tauri/ui/src/ipc/messages.schema.json — Draft-07 envelope shape (precedent: every existing family):
- Each family is one entry inside the top-level `oneOf` array
- Each entry has `"$id"`, `"type": "object"`, `"additionalProperties": false`, `"required": ["type", "ts", "payload"]`, `"properties": { "type": {"const": "ipc.<family>"}, "ts": {"type": "string", "format": "date-time"}, "payload": { "type": "object", "additionalProperties": false, "required": [...], "properties": {...} } }`
- Pattern + enum guards used at field level (precedent: `settings.set` `field` enum, `permission.check` `kind` enum)
- Tuples-vs-arrays gotcha: jsonschema rejects tuples for `type: array`; `_tuples_to_lists` in `messages.py:46-61` is the existing fix — do NOT introduce a parallel serializer

From scripts/check_ipc_schema.py — drift-gate contract:
- Parses oneOf entries, counts them, counts wrapper dataclasses (any dataclass with a `type` field), asserts equality
- `EXPECTED_FAMILIES_COUNT = 27` is the load-bearing baseline to bump (search the file; the variable name may differ but the literal `27` is what changes — replace with `34`)
- Per-wrapper roundtrip: instantiate via `.make(...)`, parse via `validate_message`, assert lossless

From confirm-dialog.ts:33 — existing `variant?: "default" | "danger"` matches CONTEXT's "destructive variant" requirement; Plan 15-04 will pass `variant: "danger"`, no Phase 12 component extension needed.

events.jsonl record shape (verbatim POC, recorder.py log_event call sites): every record is a JSON object with at minimum `t: float` (seconds since session start) + `kind: str` (one of `session_start`, `trigger`, `trigger_fired`, `ai_text`, `controller_move`, `midi_event`, `turn_error`, `session_error`, ...). Additional fields vary per kind. The `recordings.events_result` schema MUST allow arbitrary additional fields per record (`additionalProperties: true` on the per-event object) while requiring `t` + `kind` at minimum.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema additions + Python wrappers + count-parity bump</name>
  <files>tauri/ui/src/ipc/messages.schema.json, src/vibemix/ui_bus/messages.py, scripts/check_ipc_schema.py, tests/ui_bus/test_recordings_messages.py</files>
  <read_first>
    - src/vibemix/ui_bus/messages.py (entire file — wrapper + payload patterns; especially the MascotMoodChange family added in Phase 13 + the SettingsSet/SettingsState pair from Phase 12)
    - tauri/ui/src/ipc/messages.schema.json (entire file — copy the SettingsSet + SettingsState oneOf entries as the request/response-pair pattern template; copy MascotMoodChange as the push pattern template)
    - scripts/check_ipc_schema.py (locate the count baseline literal; the integer `27` will be replaced with `34`)
    - .planning/phases/15-recording-session-capture-finalization/15-RESEARCH.md Pattern 4 (lines ~318-370 — full Python wrapper example for the 5 originally-planned families; extend the same pattern to events + events_result) + Pitfall 7 (lines ~449-453 — tuple-vs-array serialization gotcha)
    - .planning/phases/15-recording-session-capture-finalization/15-UI-SPEC.md §IPC Contract Surface (full table of family shapes) + §Row expanded state (lines ~234-241 — events.jsonl bold/dim line contract that consumes the events_result payload)
    - .planning/phases/15-recording-session-capture-finalization/15-CONTEXT.md §Area 1 (events.jsonl record shape — load-bearing for the events_result element schema) + §Area 3 (locked field names: `session_dir`, `sessions`, `bytes_total`, `crashed`)
  </read_first>
  <behavior>
    - Test 1: `RecordingsList.make()` round-trips through `validate_message(json.loads(wrapper.to_json()))` — wrapper survives untouched (payload empty dict, type literal `"ipc.recordings.list"`)
    - Test 2: `RecordingsListResult.make(sessions=(RecordingSummary(session_dir="20260513-210410", started_at_iso="2026-05-13T21:04:10+02:00", duration_s=5040.0, event_count=38, bytes_total=12345678, crashed=False),), bytes_total=12345678)` round-trips through schema validation; the `sessions` tuple serializes as a JSON array
    - Test 3: `RecordingsDelete.make(session_dir="20260513-210410")` validates; `RecordingsDelete.make(session_dir="../../etc/passwd")` validation raises `jsonschema.ValidationError` (regex pattern `^[0-9]{8}-[0-9]{6}$` rejects)
    - Test 4: `RecordingsDeleteAck.make(session_dir="20260513-210410", ok=True, error=None)` round-trips; `RecordingsDeleteAck.make(session_dir="20260513-210410", ok=False, error="locked")` also round-trips (error field is `str | None`)
    - Test 5: `RecordingsUsage.make(sessions=12, bytes_total=3656838349)` round-trips
    - Test 6: `RecordingsEvents.make(session_dir="20260513-210410")` round-trips; `RecordingsEvents.make(session_dir="../../etc/passwd")` raises `jsonschema.ValidationError` (same `^[0-9]{8}-[0-9]{6}$` regex)
    - Test 7: `RecordingsEventsResult.make(session_dir="20260513-210410", events=({"t": 0.0, "kind": "session_start", "wall_clock_iso": "2026-05-13T21:04:10+02:00", "session_dir": "20260513-210410"}, {"t": 3.21, "kind": "trigger", "reason": "phase_change"}, {"t": 5.04, "kind": "ai_text", "text": "Nice transition."}, {"t": 7.11, "kind": "controller_move", "control": "filter_a"}))` round-trips; the per-event dict allows arbitrary additional fields while requiring `t` (float ≥ 0) + `kind` (string).
    - Test 8: drift-gate count parity — `scripts/check_ipc_schema.py` runs to success with the new families present; baseline integer is 34
  </behavior>
  <action>
    Add the 7 new oneOf entries to `tauri/ui/src/ipc/messages.schema.json` (placed lexically after `ipc.mascot.mood_change` to keep diff-locality with Phase 13). Each entry follows the existing envelope: `$id`, `type: "object"`, `additionalProperties: false`, `required: ["type","ts","payload"]`, `properties: { type: {const: "ipc.recordings.<family>"}, ts: {type:"string", format:"date-time"}, payload: {...} }`. Payload schemas exactly as documented in 15-UI-SPEC §IPC Contract Surface and 15-RESEARCH Pattern 4. The `session_dir` field on `recordings.delete`, `recordings.delete_ack`, `recordings.events`, `recordings.events_result`, and on each `RecordingSummary` element MUST have `"type":"string", "pattern":"^[0-9]{8}-[0-9]{6}$"` per the V12 path-traversal gate (per 15-RESEARCH Security Domain). Define `RecordingSummary` inline inside the relevant payload entries (no $defs — match Phase 11/12 convention).

    Per-family payload schemas:
    - `recordings.list.payload`: empty object `{}` with `required: []` and `additionalProperties: false` (matches `settings.get` precedent).
    - `recordings.list_result.payload`: `sessions` (array of RecordingSummary), `bytes_total` (integer minimum 0).
    - `recordings.delete.payload`: `session_dir` (string, pattern as above).
    - `recordings.delete_ack.payload`: `session_dir` (with pattern), `ok` (boolean), `error` (`["string","null"]` — precedent: `settings.set` ack).
    - `recordings.usage.payload`: `sessions` (integer minimum 0), `bytes_total` (integer minimum 0).
    - `recordings.events.payload`: `session_dir` (string, pattern as above) — request shape mirrors `recordings.delete`.
    - `recordings.events_result.payload`: `session_dir` (string, pattern as above), `events` (array of per-event objects). Each event object: `type: "object"`, `required: ["t","kind"]`, `additionalProperties: true`, with `t: {type:"number", minimum: 0}` and `kind: {type:"string"}`. The `additionalProperties: true` is the load-bearing part — each event kind has different additional fields (e.g., `ai_text` has `text`, `trigger` has `reason`, `controller_move` has `control`/`value`/`channel`), and the schema must not enumerate them all (open extensibility per Phase 13 mascot.mood_change precedent for free-form payload fields).

    RecordingSummary inline shape (used in recordings.list_result.payload.sessions.items): `session_dir` (string, pattern as above), `started_at_iso` (string, format date-time), `duration_s` (number, minimum 0), `event_count` (integer, minimum 0), `bytes_total` (integer, minimum 0), `crashed` (boolean). All required, `additionalProperties: false`.

    Add the 7 wrapper @dataclass(frozen=True, slots=True) classes and 8 payload structs (5 result/request payloads + events request + events_result + 1 `RecordingSummary` array-element) to `src/vibemix/ui_bus/messages.py` (placed after the MascotMoodChange wrapper). Field types:
    - `RecordingSummary.session_dir: str`, `started_at_iso: str`, `duration_s: float`, `event_count: int`, `bytes_total: int`, `crashed: bool`.
    - `RecordingsListResultPayload.sessions: tuple[RecordingSummary, ...]`, `bytes_total: int`. Default `sessions=()`.
    - `RecordingsDeletePayload.session_dir: str`.
    - `RecordingsDeleteAckPayload.session_dir: str`, `ok: bool`, `error: str | None = None`.
    - `RecordingsUsagePayload.sessions: int`, `bytes_total: int`.
    - `RecordingsEventsPayload.session_dir: str`.
    - `RecordingsEventsResultPayload.session_dir: str`, `events: tuple[dict, ...]` (heterogeneous dict elements — each event dict is opaque to the wrapper, validated at the schema level). Default `events=()`.

    For each wrapper add `@classmethod make(cls, ...) -> "Wrapper"` taking the payload fields as kwargs and constructing the payload internally. Implement `to_json(self) -> str` by calling the module-level `_serialize(self)` helper (already in messages.py at lines ~46-61 — DO NOT add a parallel serializer per RESEARCH Pitfall 7). For `RecordingsEventsResult.to_json`, the dict elements pass through `_serialize` unchanged (dicts are JSON-native; only tuples need converting).

    Bump the drift-gate baseline literal in `scripts/check_ipc_schema.py` from 27 to 34 (search the file for the assertion of expected count parity — the literal is in an `assert` line or a module-level constant; preserve the surrounding load-bearing comment style).

    Create `tests/ui_bus/test_recordings_messages.py` with 8 tests covering Tests 1-8 above. Use the existing test pattern from `tests/ui_bus/test_messages.py` (parametrized roundtrip + status_tick edge cases) — same imports, same `validate_message` call, same `json.loads(wrapper.to_json())` assertion idiom. For Test 3 + Test 6 path-traversal rejection, use `pytest.raises(jsonschema.ValidationError)` around `validate_message(...)` on the bad payload (the validate-after-serialize path, not the wrapper constructor — `make()` doesn't validate).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && python scripts/check_ipc_schema.py 2>&1 && python -m pytest tests/ui_bus/test_recordings_messages.py -x -v</automated>
  </verify>
  <done>scripts/check_ipc_schema.py exits 0 with baseline 34 == 34; pytest collects + passes 8 tests in test_recordings_messages.py; messages.schema.json has 34 oneOf entries (verify: `python -c "import json; print(len(json.load(open('tauri/ui/src/ipc/messages.schema.json'))['oneOf']))"` prints `34`).</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate TS codegen + vitest coverage for new families</name>
  <files>tauri/ui/src/ipc/messages.ts, tauri/ui/src/ipc/validator.spec.ts</files>
  <read_first>
    - tauri/ui/src/ipc/messages.ts (current file — confirm AUTO-GENERATED banner on line 1 and discriminated-union shape; do NOT hand-edit — regenerate)
    - tauri/ui/scripts/codegen-ipc.mjs (the programmatic json2ts wrapper; understand the entry path so the executor knows `npm run check:ipc` will rewrite messages.ts)
    - tauri/ui/src/ipc/validator.spec.ts (existing pattern — copy a status_tick or settings_state spec for each new family; same `parseIpcMessage` import + same `expect(parsed.type).toBe("ipc.<x>")` assertion shape)
    - tauri/ui/package.json scripts section (confirm `check:ipc` script chain — codegen → tsc --noEmit)
  </read_first>
  <action>
    Run `npm run check:ipc` from the `tauri/ui/` directory once to regenerate `tauri/ui/src/ipc/messages.ts` against the updated `messages.schema.json` from Task 1. The codegen script overwrites the file in place; commit the regenerated output alongside the schema (Phase 11 W0 invariant: codegen output is committed, not gitignored). The `VibemixIPCMessages` discriminated union should now include `RecordingsList | RecordingsListResult | RecordingsDelete | RecordingsDeleteAck | RecordingsUsage | RecordingsEvents | RecordingsEventsResult` (json2ts will PascalCase the `$id` slugs; verify the exact generated names by reading the regenerated file post-codegen and using those names in the vitest spec — Phase 11 W0 had a similar "json2ts PascalCases differently than expected" auto-fix; replicate that fix forward if names diverge).

    Extend `tauri/ui/src/ipc/validator.spec.ts` with 7 new vitest cases (one per new family). Each case constructs a sample message inline (matching the Python `.make()` output shape verbatim from Task 1), passes through `parseIpcMessage`, asserts the discriminator narrows correctly. Pattern from existing spec: `it("accepts recordings.list", () => { const msg = parseIpcMessage({ type: "ipc.recordings.list", ts: "2026-05-13T21:04:10.000Z", payload: {} }); expect(msg.type).toBe("ipc.recordings.list"); });`. For `recordings.delete_ack` include both the ok-true (error null) and ok-false (error string) branches. For `recordings.events_result` include a 3-event sample exercising `session_start`, `ai_text`, and `controller_move` kinds (additionalProperties: true coverage).

    For the path-traversal rejection cases (mirror Python Test 3 + Test 6): add vitest cases that assert `parseIpcMessage({ type: "ipc.recordings.delete", ts: ..., payload: { session_dir: "../../etc/passwd" } })` AND `parseIpcMessage({ type: "ipc.recordings.events", ts: ..., payload: { session_dir: "../../etc/passwd" } })` throw (or return the validator's rejection shape — match the pattern the file already uses for schema-invalid frames; the existing validator either throws or returns null/false depending on the contract, so MIRROR what the spec file currently does for `accepts X / rejects Y` pairs).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai/tauri/ui && npm run check:ipc 2>&1 && npx vitest run src/ipc/validator.spec.ts</automated>
  </verify>
  <done>npm run check:ipc exits 0 (regenerates messages.ts cleanly + tsc --noEmit passes); vitest validator.spec.ts collects and passes the new family cases including both path-traversal rejection cases; messages.ts contains an exported type alias whose discriminated union mentions `"ipc.recordings.list"`, `"ipc.recordings.list_result"`, `"ipc.recordings.delete"`, `"ipc.recordings.delete_ack"`, `"ipc.recordings.usage"`, `"ipc.recordings.events"`, `"ipc.recordings.events_result"` (verify by grep — all 7 literals appear).</done>
</task>

</tasks>

<verification>
- `python scripts/check_ipc_schema.py` exits 0 with count parity 34 == 34
- `python -c "import json; assert len(json.load(open('tauri/ui/src/ipc/messages.schema.json'))['oneOf']) == 34"` exits 0
- `python -m pytest tests/ui_bus/test_recordings_messages.py -x` passes 8 tests
- `npm run check:ipc` exits 0 (regenerates + tsc --noEmit clean)
- `npx vitest run src/ipc/validator.spec.ts` passes new family cases
- Grep gate: `grep -c '"ipc.recordings\.' tauri/ui/src/ipc/messages.schema.json` returns 7
- Path-traversal gate: schema rejects `session_dir: "../../etc/passwd"` on both recordings.delete and recordings.events (covered by Test 3 + Test 6 + vitest negative cases)
</verification>

<success_criteria>
- 34 ipc.* families declared in the schema (was 27)
- All 7 new families have @dataclass mirror + codegen-produced TS member + vitest coverage + pytest coverage
- Drift gate baseline updated to 34 in scripts/check_ipc_schema.py
- Path-traversal regex enforced at the schema layer (session_dir matches `^[0-9]{8}-[0-9]{6}$`) for all family entries that accept a session_dir field
- Events payload allows arbitrary per-event additional fields (open extensibility for the heterogeneous events.jsonl record shape)
- Zero new pip / npm deps (one-click-install rule preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/15-recording-session-capture-finalization/15-01-SUMMARY.md` following the standard summary template with sections: created files, modified files, IPC count delta (27 → 34), new wrapper classes enumerated, drift-gate state, events_result open-extensibility design note.
</output>
