---
phase: 15
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - src/vibemix/runtime/recordings_index.py
  - src/vibemix/runtime/session_loop.py
  - src/vibemix/runtime/settings.py
  - src/vibemix/__main__.py
  - tests/recording/test_recordings_index.py
  - tests/recording/test_retention_sweep.py
autonomous: false
requirements:
  - REC-05
  - REC-06
tags: [recordings-index, retention-sweep, ipc-handlers, scandir]

must_haves:
  truths:
    - "`ipc.recordings.list` returns a deterministic RecordingsListResult with sessions sorted newest-first by started_at_unix; sessions tuple includes legacy dirs (no session.json) with synthesized summaries."
    - "`ipc.recordings.delete` validates session_dir against `^[0-9]{8}-[0-9]{6}$` regex AND verifies the resolved path is_relative_to(recordings_root) before shutil.rmtree; out-of-range payload returns delete_ack with ok=false + error=`path_traversal_rejected` (V12 ASVS gate)."
    - "Retention sweep runs from three call sites: SessionLoop.boot(), SettingsApplier._apply_retention(), and a new SessionLoop.on_session_close() hook. All three call run_retention_sweep(root, retention_days) with the live ConfigStore.retention_days value."
    - "Sentinel `retention_days >= 36500` (Phase 12 ∞ stop) returns [] without scanning — explicit short-circuit."
    - "`ipc.recordings.usage` is emitted on the bus after EVERY sweep trigger (including no-op sweeps where deleted == []) with the current sessions count + bytes_total — drawer's disk-usage line stays live without polling."
    - "RecordingsIndex.compute_usage() uses os.scandir + entry.stat().st_size — no os.walk, no per-file extra stat call (RESEARCH state-of-the-art)."
  artifacts:
    - path: "src/vibemix/runtime/recordings_index.py"
      provides: "RecordingsIndex (scandir-based list + size sum + legacy-dir synth) + run_retention_sweep + emit_usage helper"
      min_lines: 150
      contains: "class RecordingsIndex"
    - path: "src/vibemix/runtime/session_loop.py"
      provides: "3 new IPC handlers (recordings.list, recordings.delete) + on_session_close() hook + boot-time sweep call"
      contains: "_on_recordings_list"
    - path: "src/vibemix/runtime/settings.py"
      provides: "_apply_retention extended to fire sweep + emit usage after persisting retention_days"
      contains: "run_retention_sweep"
    - path: "tests/recording/test_recordings_index.py"
      provides: "list + delete + usage coverage including legacy-dir + path-traversal + scandir-perf assertion"
    - path: "tests/recording/test_retention_sweep.py"
      provides: "3-trigger coverage + ∞-sentinel skip + OSError best-effort + Windows-locked-file behaviour"
  key_links:
    - from: "src/vibemix/runtime/session_loop.py"
      to: "src/vibemix/runtime/recordings_index.py"
      via: "SessionLoop constructs RecordingsIndex(recordings_root) once; reused by all 3 handlers + close hook"
      pattern: "RecordingsIndex\\("
    - from: "src/vibemix/runtime/settings.py"
      to: "src/vibemix/runtime/recordings_index.py"
      via: "SettingsApplier._apply_retention calls run_retention_sweep + emit_usage after save_config"
      pattern: "run_retention_sweep"
    - from: "src/vibemix/__main__.py"
      to: "src/vibemix/runtime/session_loop.py"
      via: "passes recordings_root to SessionLoop constructor so the handlers know where to scan/delete"
      pattern: "recordings_root="
---

<objective>
Wire the Python sidecar half of the recording browser. Three deliverables:

1. **`RecordingsIndex`** in `src/vibemix/runtime/recordings_index.py`: scandir-based directory walker that builds `RecordingSummary` lists (precedent: 15-01's payload struct), computes total disk usage in a single scandir pass per session, synthesizes summaries for legacy pre-Phase-15 dirs (no session.json — derive from WAV header + JSONL line count per RESEARCH Pitfall 9), and exposes `delete_session(name)` that path-traversal-validates + `shutil.rmtree`.

2. **`run_retention_sweep`** colocated in `recordings_index.py`: best-effort sweep with `ignore_errors=True` + post-delete existence verification per RESEARCH Pattern 3. Sentinel 36500 short-circuits. Returns deleted-name list.

3. **SessionLoop + SettingsApplier wiring:** 3 IPC handlers on the WizardBus (`recordings.list`, `recordings.delete`) + a `recordings.usage` push emitter. SettingsApplier._apply_retention calls `run_retention_sweep` immediately after `save_config`. SessionLoop.boot() calls `sweep_crashed_sessions` (from Plan 15-02) BEFORE constructing VoiceRecorder, then calls `run_retention_sweep` BEFORE accepting new connections. A new `SessionLoop.on_session_close()` hook called from `__main__` close path fires the third sweep trigger.

Purpose: this is the IPC server side. Plan 15-04 (UI components) and Plan 15-05 (drawer wiring) consume the wire shapes from 15-01; this plan implements the producers + the retention enforcement that makes the disk usage line meaningful.

Output: 1 new module (~180 LOC) + handler registration in SessionLoop (+~80 LOC) + 1 new line in SettingsApplier._apply_retention + boot-sweep call in __main__ + 2 new test files (~250 LOC combined).

**Why autonomous: false** — Task 3 wires a brand-new sweep call into the boot path. After the wire is in place, the executor should pause and let Kaan acknowledge the boot-time delete-behavior change (default 7-day retention will start pruning old `recordings/` dirs on his rig the first time he relaunches). This is a one-time UAT pause, not a checkpoint:decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-recording-session-capture-finalization/15-CONTEXT.md
@.planning/phases/15-recording-session-capture-finalization/15-RESEARCH.md
@src/vibemix/runtime/session_loop.py
@src/vibemix/runtime/settings.py
@src/vibemix/runtime/ws_bus.py
@src/vibemix/audio/recorder.py
@src/vibemix/ui_bus/messages.py

<interfaces>
<!-- Handlers registered on WizardBus follow the Phase 11 W4 / Phase 12 W2 pattern.
     SessionLoop currently has 4 handlers: session.mute / settings.set / settings.get / status.recheck.
     New handlers fit the same shape: validate inbound, do work in executor (sync I/O), emit response on the bus. -->

From src/vibemix/runtime/session_loop.py — handler signature precedent:
- `async def _on_session_mute(self, msg: dict) -> None`: takes parsed dict, awaits result, emits ack via self.bus.emit(...)
- `_wrap_with_validation(handler, original_type: str)`: existing wrapper that re-validates and emits ipc.error on bad inbound (lines ~495-540)
- Synchronous I/O (file ops) is offloaded via `loop.run_in_executor(None, fn)` — sweep + scandir are sync, must run in executor to keep the 30Hz snapshot loop responsive

From src/vibemix/runtime/settings.py — _apply_retention (lines 251-262):
- Current body: type-check int, range-check >= 0, persist to config_store.retention_days, save_config — return (True, None) on success
- Plan extends: after save_config + before return, call run_retention_sweep(self.recordings_root, iv) wrapped in try/except + emit usage via bus

<!-- RESEARCH Pattern 3 (lines ~265-310) is the verbatim sweep body. Lift it without modification. -->

<!-- RESEARCH Pitfall 9 (legacy dir): for any session dir without session.json:
     - started_at_iso from datetime.strptime(dir.name, "%Y%m%d-%H%M%S").isoformat()
     - duration_s from wave.open(voice.wav).getnframes() / .getframerate() — wrap in try/except (Pitfall 5: crashed WAVs have wrong header; in that case duration_s=0.0)
     - event_count from counting non-empty lines in events.jsonl (or 0 if file missing)
     - bytes_total from scandir entry.stat().st_size sum
     - crashed=False (legacy dirs predate the crashed concept) -->

<!-- session_dir regex: re.compile(r"^\d{8}-\d{6}$") — used for both schema (15-01) and runtime validation (this plan). Validate at sidecar entry to defense-in-depth even though schema already filters at the boundary. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RecordingsIndex module + tests</name>
  <files>src/vibemix/runtime/recordings_index.py, tests/recording/test_recordings_index.py</files>
  <read_first>
    - src/vibemix/audio/recorder.py (post-Plan-15-02 — see session.json schema for what to read)
    - src/vibemix/ui_bus/messages.py (post-Plan-15-01 — RecordingSummary @dataclass + RecordingsUsage payload field names)
    - .planning/phases/15-recording-session-capture-finalization/15-RESEARCH.md Pattern 3 (verbatim sweep body) + Pitfall 9 (legacy-dir summary synthesis) + scandir-based size summation example (lines ~520-533)
    - .planning/phases/15-recording-session-capture-finalization/15-CONTEXT.md §Area 3 (retention triggers + ∞ sentinel)
    - tests/recording/conftest.py (from Plan 15-02 — make_fake_session fixture is reused)
  </read_first>
  <behavior>
    - Test 1: `RecordingsIndex(tmp_recordings_dir).list()` on 3 fake sessions (2 with session.json, 1 legacy without) returns a tuple of 3 RecordingSummary structs sorted newest-first by started_at_unix.
    - Test 2: `RecordingsIndex(tmp_recordings_dir).list()` on the legacy dir synthesizes `duration_s` from voice.wav header (use a 1-second silent fake WAV via Python `wave` module), `event_count` from events.jsonl line count, `started_at_iso` from the dir name parse, `crashed=False`.
    - Test 3: `RecordingsIndex(tmp_recordings_dir).list()` on a crashed-marked session (ended_at_iso=None, crashed=True from sweep_crashed_sessions) returns a summary with `crashed=True`.
    - Test 4: `RecordingsIndex.delete("20260513-210410")` on a valid session removes the dir; returns (True, None). On `"../../etc/passwd"` returns (False, "path_traversal_rejected") and does NOT touch any file. On a name that doesn't exist returns (False, "not_found"). On a name that matches the regex but is_relative_to fails (e.g., symlink redirect) returns (False, "path_traversal_rejected").
    - Test 5: `RecordingsIndex.compute_usage()` returns (sessions_count, bytes_total) where bytes_total sums every file across every session via scandir. Asserted on a fixture with 3 sessions of known file sizes — sum matches exactly.
    - Test 6: `RecordingsIndex.list()` on a non-existent recordings_root returns empty tuple (no raise) — fresh-install case.
    - Test 7: `RecordingsIndex.list()` on a dir with one entry whose session.json is malformed JSON falls back to legacy-dir synthesis (Pitfall 9 defensive — bad JSON is treated as "no session.json").
  </behavior>
  <action>
    Create `src/vibemix/runtime/recordings_index.py` with:
    - `SESSION_DIR_RE = re.compile(r"^\d{8}-\d{6}$")` — used by both list filtering AND delete validation.
    - `_synthesize_legacy_summary(session_dir: Path) -> RecordingSummary | None`: handles Pitfall 9. Parses dir name, opens voice.wav with `wave.open` in try/except (broken header → duration_s=0.0), counts events.jsonl lines, sums file sizes via os.scandir. Returns None only if dir name doesn't match SESSION_DIR_RE.
    - `_read_session_summary(session_dir: Path) -> RecordingSummary | None`: opens session.json, builds RecordingSummary from fields (started_at_iso, duration_s from `ended_at_unix - started_at_unix` OR session.json's `duration_s` if set, event_count, bytes_total via fresh scandir of the dir to handle ongoing sessions where session.json byte counts are stale, crashed). Falls back to `_synthesize_legacy_summary` on FileNotFoundError or JSONDecodeError.
    - `class RecordingsIndex` with `__init__(self, recordings_root: Path)`:
        - `def list(self) -> tuple[RecordingSummary, ...]`: scandir recordings_root, filter by `is_dir() and SESSION_DIR_RE.match(name)`, call `_read_session_summary` for each, drop Nones, sort by started_at_unix descending. Wrap in try/except OSError → return empty tuple (fresh-install).
        - `def compute_usage(self) -> tuple[int, int]`: returns (sessions_count, bytes_total). Single scandir pass per session; uses `entry.stat().st_size` (cached on Windows per PEP 471). Wrap entry-level stat failures in try/except (continue past unreadable files).
        - `def delete(self, session_dir_name: str) -> tuple[bool, str | None]`: regex-validate `session_dir_name`, resolve `target = (self.recordings_root / session_dir_name).resolve()`, validate `target.is_relative_to(self.recordings_root.resolve())`, then `shutil.rmtree(target, ignore_errors=True)`. Verify target no longer exists (Windows-locked-file diagnostic per RESEARCH Pattern 3); return (False, "locked_or_in_use") if still present. NOT_FOUND case: return (False, "not_found") before rmtree if `not target.exists()`.
    - `def run_retention_sweep(recordings_root: Path, retention_days: int, *, now: datetime | None = None) -> list[str]`: verbatim from RESEARCH Pattern 3. Sentinel `retention_days >= 36500` returns []. `now` default `datetime.now()` (no tz; matches the dir name format which is local-time per recorder.py:52). Wrap each entry rmtree in try/except OSError + log.warning per RESEARCH Pitfall 4.

    Create `tests/recording/test_recordings_index.py` covering Tests 1-7. Use the `make_fake_session` fixture from conftest.py (Plan 15-02). For voice.wav header generation in Test 2 use Python's `wave` module to write a real 0.5-second silent 24kHz mono int16 WAV (small fixture; <50KB). For Test 4 path-traversal-rejected, attempt `delete("../../etc/passwd")` AND `delete("../../../tmp/whatever")` AND `delete("20260513-210410/../../escape")` — all three return (False, "path_traversal_rejected") with no filesystem mutation observable via `tmp_recordings_dir` snapshot.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && python -m pytest tests/recording/test_recordings_index.py -x -v</automated>
  </verify>
  <done>7 tests pass; `grep -c "is_relative_to" src/vibemix/runtime/recordings_index.py` >= 1 (path-traversal gate active); `grep -c "ignore_errors=True" src/vibemix/runtime/recordings_index.py` >= 1 (best-effort rmtree); module imports cleanly: `python -c "from vibemix.runtime.recordings_index import RecordingsIndex, run_retention_sweep, SESSION_DIR_RE; print('ok')"` prints `ok`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retention sweep 3-trigger integration + SettingsApplier wiring + tests</name>
  <files>src/vibemix/runtime/settings.py, src/vibemix/runtime/session_loop.py, tests/recording/test_retention_sweep.py</files>
  <read_first>
    - src/vibemix/runtime/settings.py (full file — understand `_apply_retention` at lines 251-262 + how `self.bus` ref is acquired; SettingsApplier currently has access to the bus for ipc.settings.state emits)
    - src/vibemix/runtime/session_loop.py (full file — understand handler registration in __init__ + the _on_session_mute precedent + the `_emit_settings_state` pattern for "after-save emit")
    - src/vibemix/runtime/recordings_index.py (just-written from Task 1)
    - src/vibemix/audio/recorder.py (post-Plan-15-02 — `sweep_crashed_sessions` import target)
    - .planning/phases/15-recording-session-capture-finalization/15-CONTEXT.md §Area 3 (3 triggers in priority order)
  </read_first>
  <behavior>
    - Test 1 (boot trigger): `SessionLoop(recordings_root=tmp).boot()` — on init OR a first-tick — calls `run_retention_sweep(tmp, config_store.retention_days)` exactly once with current config. Mock sweep + assert single call with correct args.
    - Test 2 (settings-change trigger): `await applier._apply_retention(3)` (3 days) — after save_config, `run_retention_sweep(applier.recordings_root, 3)` is called. Mock sweep + assert called with `3` (not the previous value).
    - Test 3 (session-close trigger): `SessionLoop.on_session_close()` invokes `run_retention_sweep` with current `config_store.retention_days`. Test calls the method directly + asserts.
    - Test 4 (∞ sentinel): `_apply_retention(36500)` calls `run_retention_sweep`, which returns [] without doing any filesystem work. Mock os.scandir + assert NOT called when retention_days=36500 (use the public sweep function, not the index).
    - Test 5 (usage emit after sweep): after every sweep call (boot / settings / close), `bus.emit(...)` is called with an `ipc.recordings.usage` message whose payload `sessions` + `bytes_total` matches `RecordingsIndex.compute_usage()`. Verify call shape, not just call count.
    - Test 6 (best-effort): sweep that raises OSError on one entry continues past it and still emits usage; the SessionLoop's snapshot tick keeps running (no exception propagation into the main loop).
  </behavior>
  <action>
    Edit `src/vibemix/runtime/settings.py`:
    - Extend `SettingsApplier.__init__` to accept `recordings_root: Path | None = None` and `bus: WizardBus | None = None` (if not already present). Match existing optional-kwarg pattern (audio_core, music_state are already Optional).
    - In `_apply_retention`, after `save_config(self.config_store)` and before `return (True, None)`:
        ```
        # Phase 15 Plan 15-03 — fire sweep + emit usage (best-effort)
        if self.recordings_root is not None:
            try:
                from vibemix.runtime.recordings_index import RecordingsIndex, run_retention_sweep  # noqa: PLC0415
                deleted = run_retention_sweep(self.recordings_root, iv)
                log.info("retention sweep (settings-change): deleted %d session(s)", len(deleted))
                if self.bus is not None:
                    idx = RecordingsIndex(self.recordings_root)
                    sessions, bytes_total = idx.compute_usage()
                    from vibemix.ui_bus.messages import RecordingsUsage  # noqa: PLC0415
                    msg = RecordingsUsage.make(sessions=sessions, bytes_total=bytes_total)
                    await self.bus.emit(json.loads(msg.to_json()))
            except Exception:
                log.exception("retention sweep on settings change failed")
        ```
        Inline imports + `log.exception` for best-effort + never raise into SettingsApplier (the settings-set ack must always come back to the UI).

    Edit `src/vibemix/runtime/session_loop.py`:
    - Add `recordings_root: Path | None = None` kwarg to SessionLoop.__init__. Store on self.
    - Register 2 new IPC handlers via `self.bus.register_handler("ipc.recordings.list", self._wrap_with_validation(self._on_recordings_list, "ipc.recordings.list"))` and similarly for `"ipc.recordings.delete"`. (No handler for `recordings.usage` — it's push-only from sidecar; no inbound shape.)
    - Implement `async def _on_recordings_list(self, msg: dict) -> None`: if `self.recordings_root is None`, emit `ipc.error` with field=recordings_root + reason=not_wired. Otherwise: `loop = asyncio.get_running_loop()`; `summaries = await loop.run_in_executor(None, lambda: RecordingsIndex(self.recordings_root).list())`; compute usage via the same index; build `RecordingsListResult.make(sessions=summaries, bytes_total=usage_bytes)` and emit. Wrap in try/except — emit ipc.error on failure.
    - Implement `async def _on_recordings_delete(self, msg: dict) -> None`: extract `session_dir = msg["payload"]["session_dir"]`; `loop.run_in_executor(None, lambda: RecordingsIndex(self.recordings_root).delete(session_dir))` returns (ok, err); build `RecordingsDeleteAck.make(session_dir=session_dir, ok=ok, error=err)` and emit. Then ALSO emit a fresh `RecordingsUsage.make(...)` (delete changes total). Wrap in try/except.
    - Implement `def _emit_usage_sync(self) -> None`: helper that gets the running loop, schedules `compute_usage` in executor, then emits via `asyncio.create_task(self.bus.emit(...))`. Used by the boot path.
    - Add `async def on_session_close(self) -> None`: best-effort sweep + usage emit. Called from `__main__` close path AFTER the recorder.close() completes. Pattern: same as Settings's wrapper above — try import + run_retention_sweep + emit usage + except.
    - Boot extension: in `SessionLoop.run()` (or wherever boot occurs — search for the lifecycle init), before the 30Hz snapshot loop starts, call `await loop.run_in_executor(None, vibemix.audio.recorder.sweep_crashed_sessions, self.recordings_root)` (NOTE: Plan 15-02 added sweep_crashed_sessions; this is the boot-time call), then `await loop.run_in_executor(None, run_retention_sweep, self.recordings_root, self.config_store.retention_days)`, then emit one `recordings.usage`. Order is: crash-mark → retention-prune → usage-emit.

    Edit `src/vibemix/__main__.py`:
    - Pass `recordings_root=recordings_root` to BOTH `SessionLoop(...)` and `SettingsApplier(...)` constructors at their current call sites.
    - On shutdown (search for the close path — SIGTERM handler or `finally`), call `await session_loop.on_session_close()` BEFORE process exit so the third trigger fires.

    Create `tests/recording/test_retention_sweep.py` covering Tests 1-6 above. Use `unittest.mock.patch` to mock `run_retention_sweep` for trigger-counting tests; use real sweep on a `tmp_recordings_dir` for ∞-sentinel + OSError-best-effort. For Test 6 OSError best-effort, monkeypatch `shutil.rmtree` to raise on ONE specific dir name; assert the other (older) dir is still pruned + usage still emitted. Use the existing `pytest-asyncio` if SessionLoop/SettingsApplier tests already use it (check tests/runtime/ for the pattern).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && python -m pytest tests/recording/test_retention_sweep.py -x -v</automated>
  </verify>
  <done>6 tests pass; SessionLoop ipc handler registration includes recordings.list + recordings.delete (verify: `grep -c "ipc.recordings\." src/vibemix/runtime/session_loop.py` >= 2); SettingsApplier._apply_retention call site references run_retention_sweep (verify: `grep -c "run_retention_sweep" src/vibemix/runtime/settings.py` >= 1); on_session_close exists (verify: `grep -c "on_session_close" src/vibemix/runtime/session_loop.py` >= 1); existing SessionLoop + SettingsApplier tests in tests/runtime/ still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Kaan-rig boot acknowledgement — first run with retention prune</name>
  <what-built>
    SessionLoop.boot now calls run_retention_sweep with default retention_days=7 BEFORE accepting any IPC traffic. On Kaan's rig, the FIRST relaunch after Plan 15-03 lands will prune any `recordings/` dirs older than 7 days (legacy dirs from Phase 2-14 development). Each pruned dir is logged at INFO level + a final `recordings.usage` push lands on the bus showing the post-prune state.

    Plan 15-02 also added a boot-time `sweep_crashed_sessions` call that rewrites any `session.json` with `ended_at_iso=null AND mtime > 30s` to set `crashed=true`. Legacy dirs (no session.json) are LEFT ALONE by this sweep — only retention prunes them.

    The expected end state on Kaan's rig:
    - All `recordings/<YYYYMMDD-HHMMSS>/` dirs from > 7 days ago are deleted.
    - Any pre-existing dir that has a session.json with ended_at_iso=null gets crashed=true marked.
    - `~/Library/Application Support/vibemix/recordings/` is created if it didn't exist (production path); `Path.cwd()/recordings` (POC-default path used by cohost_v4.py) is untouched.
  </what-built>
  <how-to-verify>
    1. Pull the branch on Kaan's rig.
    2. `ls -la ~/Library/Application\ Support/vibemix/recordings/ 2>/dev/null` — note how many session dirs exist + their ages.
    3. `ls -la recordings/` from the repo root — note how many POC-format dirs exist (these are POC, NOT touched by the new sweep — should match counts post-run).
    4. `./run_v4.sh` to confirm POC still writes to `recordings/` in CWD (writes one new session dir, no error).
    5. `python -m vibemix` (production path) — observe stderr logs for `retention sweep (boot)` line with count of deleted dirs.
    6. After production run completes (CTRL+C or session end), re-check `~/Library/Application Support/vibemix/recordings/` — confirm only sessions ≤7 days old remain.
    7. Confirm `~/Library/Application Support/vibemix/recordings/<new-session-dir>/session.json` exists + has the 16 fields populated.
    8. Confirm `recordings/` (CWD) still has its v4 dirs intact (POC path didn't get swept).
    9. If unexpected behavior observed: describe what was deleted + what should not have been. The `on_session_close` and SettingsApplier paths fire the sweep too — confirm they're idempotent (running them again deletes nothing new).
  </how-to-verify>
  <resume-signal>Type "approved" if boot prune behaves correctly + POC path untouched + production path populated. Type "blocked: <reason>" if any deletion looks wrong or POC was disturbed.</resume-signal>
  <files>(none — human-verify checkpoint; pauses for live-rig observation only)</files>
  <action>Pause execution and surface the what-built + how-to-verify content above to Kaan. Do NOT proceed to Plan 15-04 until Kaan replies with "approved" or "blocked: <reason>" or "defer: <reason>" via the resume-signal handler. If "blocked", capture the reason in `.planning/phases/15-recording-session-capture-finalization/15-03-checkpoint-blockers.md` and re-loop into Task 2 with the regression fix; if "approved" or "defer", record the result in the plan summary and proceed.</action>
  <verify>
    <automated>echo "Human checkpoint — gsd-execute-phase awaits Kaan's resume signal"</automated>
  </verify>
  <done>Kaan has typed "approved", "blocked: ...", or "defer: ...". The result is captured in 15-03-SUMMARY.md under a "Kaan-rig boot-prune UAT" section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webview → sidecar (ipc.recordings.delete) | Untrusted session_dir crosses here |
| sidecar → disk (rmtree) | Best-effort delete with path-traversal gate |
| sidecar → bus (recordings.usage push) | Trusted producer; bus subscribers are local-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | `ipc.recordings.delete {session_dir: "../../etc/passwd"}` deletes outside recordings_root | mitigate | Regex `^\d{8}-\d{6}$` at handler entry + `Path.is_relative_to(recordings_root.resolve())` after path resolution — defense in depth (schema layer already filters per 15-01, this is the second gate). |
| T-15-03-02 | DoS | Delete-spam on the same session_dir causes shutil.rmtree thrashing | accept | Tauri 10s IPC timeout caps blast radius; rmtree is idempotent after the dir is gone (returns "not_found" — cheap). |
| T-15-03-03 | Information Disclosure | RecordingsIndex.list() exposes session timestamps + counts to webview without auth | accept | Webview is local-only (Tauri WebView2/WKWebView, 127.0.0.1 bus); same trust boundary as the rest of Phase 11+12 IPC. |
| T-15-03-04 | DoS | scandir on a recordings_root with 10000 entries blocks boot for seconds | accept | scandir is O(n) + 1 syscall per file on Windows; 200 sessions = <100ms (RESEARCH); 10000 = >1 year of daily 60-min sessions, not a v1 concern. |
| T-15-03-05 | Tampering | Symlink in recordings_root redirects rmtree to /tmp or beyond | mitigate | `is_relative_to(recordings_root.resolve())` resolves symlinks before comparison — rejected as "path_traversal_rejected". |
| T-15-03-06 | Data integrity | Sweep mid-runs against the live session's dir | mitigate | Live session's dir is < retention_days old by definition (just-created); sweep cutoff filters it out. Crashed-detection sweep skips dirs with mtime < 30s (active write window). |
| T-15-03-07 | DoS | OSError in rmtree on Windows file-in-use blocks subsequent sweep iterations | mitigate | `ignore_errors=True` + outer try/except per RESEARCH Pitfall 4; locked dir retries on next sweep trigger. |
</threat_model>

<verification>
- pytest tests/recording/test_recordings_index.py exits 0 (7 tests)
- pytest tests/recording/test_retention_sweep.py exits 0 (6 tests)
- pytest tests/runtime/ -x (all existing tests still pass with new SessionLoop + SettingsApplier kwargs)
- pytest tests/recording/ -x -m "not slow" — full Phase 15 (so far) test suite green
- Kaan-rig manual verification (Task 3) — boot prune behavior approved
- `grep -c "ipc.recordings\." src/vibemix/runtime/session_loop.py` returns ≥3 (2 handlers + push emitter)
- `grep -c "is_relative_to" src/vibemix/runtime/recordings_index.py` returns ≥1
- Zero new pip / npm deps
</verification>

<success_criteria>
- RecordingsIndex.list returns deterministic newest-first ordered tuple including legacy + crashed sessions
- Path-traversal blocked at 2 layers (schema + runtime)
- Retention sweep fires from all 3 trigger sites (boot, settings change, session close)
- ∞ sentinel (36500) explicitly short-circuits without scanning
- recordings.usage push lands on the bus after every sweep
- Kaan acknowledges the boot-prune behavior change on his rig
</success_criteria>

<output>
After completion, create `.planning/phases/15-recording-session-capture-finalization/15-03-SUMMARY.md` capturing: RecordingsIndex API surface, retention sweep trigger map, IPC handler registration sites, Kaan's verification result, count of legacy dirs pruned on Kaan's rig (anonymized count is fine — no per-dir details).
</output>
