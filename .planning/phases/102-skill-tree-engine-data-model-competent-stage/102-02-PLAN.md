---
phase: 102-skill-tree-engine-data-model-competent-stage
plan: 02
type: tdd
wave: 2
depends_on: ["102-01"]
files_modified:
  - src/vibemix/learn/skill_tree.py
  - tests/learn/test_skill_tree.py
  - tests/learn/test_skill_tree_invariants.py
autonomous: true
requirements: [SKILL-01, SKILL-02, SKILL-03, COMP-01, COMP-02]
must_haves:
  truths:
    - "All 6 named DJ skills resolve a stage (locked/competent/mastered) from a LearnProgress"
    - "A first-try (0-strikes) completion fills more than a with-strikes completion, which fills more than a click-through (quality weighting)"
    - "100% lesson fill with NO recital pass is NOT Competent — the stage stays locked (headline anti-slop gate)"
    - "A skill reaches Competent only when fill >= COMPETENT_THRESHOLD AND its gating recital flag is True"
    - "Fill never decreases across a reload (monotonic display)"
    - "skill_tree.py never imports or writes MusicState, opens no ws port, and never touches profile.json"
  artifacts:
    - path: "src/vibemix/learn/skill_tree.py"
      provides: "SKILL_MANIFEST, SkillProgress dataclass, SkillTree.compute pure function, fill-math constants"
      contains: "def compute("
      min_lines: 60
    - path: "tests/learn/test_skill_tree.py"
      provides: "fill-math, manifest-drift, Competent recital-gate, headline click-through tests"
      contains: "def test_full_clickthrough_without_recital_not_competent"
    - path: "tests/learn/test_skill_tree_invariants.py"
      provides: "3 static-grep invariant pins (#1 no MusicState, #4 no new port, privacy no profile.json)"
      contains: "def test_skill_tree_never_mutates_musicstate"
  key_links:
    - from: "src/vibemix/learn/skill_tree.py::SKILL_MANIFEST"
      to: "vibemix.learn.curriculum.CURRICULUM"
      via: "import-time assertion every referenced lesson id exists (anti-drift)"
      pattern: "assert lesson_id in CURRICULUM"
    - from: "src/vibemix/learn/skill_tree.py::compute"
      to: "LearnProgress.course_2_unlocked / course_3_unlocked"
      via: "Competent gate reads the recital honest-score unlock flags"
      pattern: "course_(2|3)_unlocked"
---

<objective>
Add the pure-logic skill-tree engine `src/vibemix/learn/skill_tree.py`: a readable `SKILL_MANIFEST`
mapping the 6 locked DJ skills onto real curriculum lesson IDs + their gating recitals, a
`SkillProgress` dataclass, and a `SkillTree.compute(progress) -> dict[str, SkillProgress]` pure
function that DERIVES quality-weighted Competent fill and gates Competent behind the recital
honest-score. Lands the 3 invariant pins.

Purpose: This is the engine — the sole writer of skill state and the "nothing is given; every notch
is earned" gate in executable form. The decisive lock is COMP-02: a recital pass is AND-ed into
Competent, so 100% click-through can NEVER reach Competent regardless of threshold. Phase 103 will
add `record_live_demo`; Phase 104 reads the computed dict for display.

Output: `skill_tree.py` (pure, I/O-free, MusicState-free), its unit suite (fill math + Competent
gate + the headline anti-slop assertion + manifest↔curriculum drift), and the 3 invariant pins.
Implements SKILL-01/02/03, COMP-01/02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-skill-tree-engine-data-model-competent-stage/102-CONTEXT.md
@.planning/phases/102-skill-tree-engine-data-model-competent-stage/102-RESEARCH.md

<interfaces>
<!-- Contracts the engine reads. Extracted from the codebase — no exploration needed. -->

From src/vibemix/learn/progress.py (post-Plan-01, schema v2):
- `LearnProgress.lessons: dict[str, dict]` — each row `{"completed": bool, "completed_at": str|None,
  "strikes_used": int}`. A lesson ABSENT from this dict = never reached = floor (0.0). `completed=True`
  with `strikes_used=0` = first-try; `completed=True` with `strikes_used>=1` = with-strikes.
- `LearnProgress.course_2_unlocked: bool` — True iff the Course-1 recital (L1.16) was passed 5/5.
- `LearnProgress.course_3_unlocked: bool` — True iff the Course-2 recital (L2.14) was passed 5/5 + ≥3 distinct types.
- `LearnProgress.skills: dict[str, dict]` — live-portion `{"live_proof_count": int, "mastered": bool,
  "first_mastered_at": str|None}` per skill id (read-only here; Phase 103 writes).

From src/vibemix/learn/curriculum.py:
- `CURRICULUM: dict[str, LessonMeta]` — keys are the canonical lesson IDs. Verified set:
  course_0: `L0.00-press-play` (feeds NO skill).
  C1 (course_1_anatomy): `L1.01`..`L1.16` (L1.16 = recital).
  C2 (course_2_transitions): `L2.01`..`L2.14` (L2.14 = recital).
  C3 (course_3_play_mode): `L3.01`..`L3.06` (NO recital, NO completion flag).

From tests/learn/test_runtime_invariants.py (the static-grep gate idiom to MIRROR):
- `LEARN_DIR = repo_root / "src" / "vibemix" / "learn"`; `_walk_python_files` rglob `*.py`.
- `FORBIDDEN_WRITES` regexes for `MusicState`/`ControllerState` field assignments; skip `#`-comment lines.

From src/vibemix/profile/schema.py (the privacy contract the pin protects):
- `PROFILE_SCHEMA["required"]` = exactly 5 fields: `preferred_genre`, `avg_session_duration`,
  `mix_style_tags`, `tempo_preference_bin`, `event_type_response_preferences`. `additionalProperties: False`.

LOCKED SKILL_MANIFEST mapping (gating recitals are CONTEXT-locked; lesson assignment per RESEARCH § Code Examples):
- `deck_control`        → lessons L1.02–L1.09 ; gate `course_2_unlocked`
- `beatmatching`        → lessons L2.01, L2.02 ; gate `course_3_unlocked`
- `eq_mixing`           → lessons L1.14, L2.04, L2.05 ; gate `course_3_unlocked`
- `harmonic_mixing`     → lessons L2.11 ; gate `course_3_unlocked`
- `transitions`         → lessons L2.03, L2.06, L2.07, L2.08, L2.09 ; gate `course_3_unlocked`
- `phrasing_performance`→ lessons L1.10–L1.13, L2.10, L2.12, L2.13, L3.01–L3.06 ; gate `course_3_unlocked` (RESEARCH Open-Q#1: the honest C2-recital pass that earns play-mode entry — NO C3 completion flag exists)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: skill_tree.py engine — manifest + SkillProgress + SkillTree.compute + fill math + Competent gate</name>
  <files>src/vibemix/learn/skill_tree.py, tests/learn/test_skill_tree.py</files>
  <read_first>
    - src/vibemix/learn/progress.py (post-Plan-01: the v2 `skills` shape + `lessons`/`course_2_unlocked`/`course_3_unlocked` fields the engine reads)
    - src/vibemix/learn/curriculum.py (CURRICULUM keys — the manifest source of truth)
    - tests/learn/test_progress_persistence.py (synthetic LearnProgress / lesson-row construction idiom to mirror in the new tests)
    - .planning/phases/102-skill-tree-engine-data-model-competent-stage/102-RESEARCH.md (§ Code Examples fill math, § Pitfall 4 monotonicity, § Open-Q#1 phrasing_performance gate)
  </read_first>
  <behavior>
    - SKILL-01: `compute(progress)` returns `dict[str, SkillProgress]` with exactly the 6 skill ids; each `stage` in {"locked","competent","mastered"}. `test_six_skills_present_with_stage`.
    - SKILL-02: `compute` is pure — twice on the same `LearnProgress` returns equal results and does not mutate the input (no I/O, no clock). `test_compute_is_pure_no_side_effects`.
    - SKILL-03: every lesson id in `SKILL_MANIFEST` exists in `CURRICULUM`. `test_manifest_lesson_ids_exist_in_curriculum`.
    - COMP-01: quality-weighted fill ∈ [0,1]; all-first-try fills strictly MORE than all-with-strikes, which fills strictly MORE than all-click-through (absent). `test_quality_weighted_fill_ordering`.
    - COMP-01: fill never decreases across reload — once `completed=True`, contribution is fixed by stored `strikes_used`; a fewer-strikes replay only raises fill. `test_fill_monotonic_on_reload`.
    - COMP-02 (HEADLINE): a skill with ALL lessons first-try (fill=1.0) but the gating recital flag False is NOT competent and `.stage == "locked"`. `test_full_clickthrough_without_recital_not_competent`.
    - COMP-02: Competent requires `fill >= COMPETENT_THRESHOLD` AND the gating `course_N_unlocked` True. `test_competent_requires_threshold_and_recital`.
  </behavior>
  <action>
    RED→GREEN→REFACTOR. Write `tests/learn/test_skill_tree.py` FIRST (fails on the absent module), then
    implement `src/vibemix/learn/skill_tree.py` to pass. Module: `from __future__ import annotations` +
    SPDX header. Constants (UPPER_SNAKE_CASE): `WEIGHT_FIRST_TRY = 1.0`, `WEIGHT_WITH_STRIKES = 0.6`,
    `WEIGHT_FLOOR = 0.0`, `COMPETENT_THRESHOLD = 0.6` — document the rationale in a docstring (first-try
    > with-strikes > click-through; the decisive gate is the recital, threshold is the secondary "you did
    the lessons" floor; Claude's discretion per CONTEXT GA2, defended by the fill-math tests). Define
    `@dataclass(frozen=True) SkillSpec` with `lesson_ids: tuple[str, ...]` and `gate: str` (the
    `course_N_unlocked` attribute name), and `SKILL_MANIFEST: dict[str, SkillSpec]` declaring the 6 skills
    EXACTLY per the LOCKED mapping in the &lt;interfaces&gt; block above. Lazy-import `CURRICULUM` and
    `assert lesson_id in CURRICULUM` for every referenced id at import time (anti-drift; `L0.00-press-play`
    and `L1.01` feed no skill — valid no-ops). Define `@dataclass SkillProgress` with `skill_id`, `stage`,
    `learn_fill` (DERIVED), `competent` (DERIVED), `live_proof_count`, `mastered`, `first_mastered_at: str | None`.
    Add `_weight_for(row) -> float`: absent/None → WEIGHT_FLOOR; completed + 0 strikes → WEIGHT_FIRST_TRY;
    completed + ≥1 strikes → WEIGHT_WITH_STRIKES; not completed → WEIGHT_FLOOR. `class SkillTree` holds the
    manifest; `compute(self, progress) -> dict[str, SkillProgress]`: per skill, `learn_fill =
    sum(_weight_for(progress.lessons.get(lid)) for lid in spec.lesson_ids) / len(spec.lesson_ids)`; read the
    gate via `getattr(progress, spec.gate)`; `competent = learn_fill >= COMPETENT_THRESHOLD and gate_passed`;
    read live-portion from `progress.skills.get(skill_id, {})` with safe defaults; `stage = "mastered" if
    mastered else "competent" if competent else "locked"`. PURE — no I/O, no clock, no MusicState import. Do
    NOT add `record_live_demo` (Phase 103). NO fenced code in implementation — directive prose only.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def compute(" src/vibemix/learn/skill_tree.py` returns a match.
    - `grep -n "SKILL_MANIFEST\|class SkillProgress\|COMPETENT_THRESHOLD" src/vibemix/learn/skill_tree.py` returns 3 matches.
    - `grep -n "assert lesson_id in CURRICULUM" src/vibemix/learn/skill_tree.py` returns a match.
    - `grep -n "def test_full_clickthrough_without_recital_not_competent" tests/learn/test_skill_tree.py` returns a match.
    - `PYTHONPATH=src python3 -c "from vibemix.learn.skill_tree import SKILL_MANIFEST; assert set(SKILL_MANIFEST) == {'deck_control','beatmatching','eq_mixing','harmonic_mixing','transitions','phrasing_performance'}"` exits 0.
    - `PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree.py` exits 0.
  </acceptance_criteria>
  <done>`skill_tree.py` exposes the 6-skill `SKILL_MANIFEST`, `SkillProgress`, and a pure `SkillTree.compute`; quality-weighted fill orders first-try > with-strikes > click-through; Competent AND-gates the recital flag so 100% click-through stays locked (the headline assertion is green); fill is monotonic on reload.</done>
</task>

<task type="auto">
  <name>Task 2: 3 invariant pins — no MusicState write (#1), no new ws port (#4), no profile.json leak (privacy)</name>
  <files>tests/learn/test_skill_tree_invariants.py</files>
  <read_first>
    - tests/learn/test_runtime_invariants.py (the line-oriented `re` static-grep gate idiom to MIRROR — `LEARN_DIR`, `_walk_python_files`, `#`-comment skip)
    - src/vibemix/learn/skill_tree.py (the module under test — from Task 1)
    - src/vibemix/profile/schema.py (PROFILE_SCHEMA — the 5-field `additionalProperties:false` contract the privacy pin asserts on)
  </read_first>
  <action>
    Create `tests/learn/test_skill_tree_invariants.py` mirroring `tests/learn/test_runtime_invariants.py`'s
    line-oriented `re` idiom (resolve `skill_tree.py` via `repo_root`; skip `#`-comment lines).
    `test_skill_tree_never_mutates_musicstate` (Invariant #1): grep `skill_tree.py` source for any
    `import`/`from` of `state.music_state` / `vibemix.state.music_state` AND any `MusicState.<field> = `
    write → assert zero offenders. `test_no_new_ws_port` (Invariant #4): grep for `websockets.serve`,
    `WS_PORT`, and the literals `8765` / `8766` → assert zero. `test_skills_never_in_profile_json`
    (privacy): (a) grep `skill_tree.py` for `profile.schema` / `profile.json` / `serialize_profile` →
    assert zero; (b) import `PROFILE_SCHEMA` from `vibemix.profile.schema` and assert
    `set(PROFILE_SCHEMA["required"])` equals exactly `{"preferred_genre","avg_session_duration",
    "mix_style_tags","tempo_preference_bin","event_type_response_preferences"}`, `PROFILE_SCHEMA["additionalProperties"]
    is False`, and none of `skills`/`live_proof_count`/`mastered` appears in `PROFILE_SCHEMA["properties"]`.
    For any shell-style count gate, filter comment lines first (`grep -v '^#' | grep -c token`; bare
    `== 0` gates on unfiltered files are forbidden per CLAUDE grep-gate hygiene).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree_invariants.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_skill_tree_never_mutates_musicstate" tests/learn/test_skill_tree_invariants.py` returns a match.
    - `grep -n "def test_no_new_ws_port" tests/learn/test_skill_tree_invariants.py` returns a match.
    - `grep -n "def test_skills_never_in_profile_json" tests/learn/test_skill_tree_invariants.py` returns a match.
    - `grep -c "music_state\|MusicState" src/vibemix/learn/skill_tree.py` returns 0.
    - `PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree_invariants.py` exits 0.
  </acceptance_criteria>
  <done>All 3 invariant pins green: `skill_tree.py` never imports/writes MusicState (#1), opens no ws port (#4), and the `PROFILE_SCHEMA` privacy contract is still exactly 5 fields with `additionalProperties:false` and no skill keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LearnProgress → skill state | the engine reads lesson/recital outcomes; a click-through must never cross into Competent |
| skill_tree.py → MusicState / ws / profile.json | the engine must touch NONE of these (invariants #1, #4, privacy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-06 | Elevation of Privilege | click-through reaching Competent (anti-slop) | mitigate | Competent AND-gates the recital flag (`course_N_unlocked`); pinned by the HEADLINE `test_full_clickthrough_without_recital_not_competent` (fill=1.0, gate False → locked) |
| T-102-07 | Tampering | `skill_tree.py` mutating MusicState (Invariant #1) | mitigate | Pure function, no `state.music_state` import; `test_skill_tree_never_mutates_musicstate` static grep |
| T-102-08 | Spoofing | a second ws listener (Invariant #4) | mitigate | Engine opens no socket; `test_no_new_ws_port` greps for `websockets.serve`/`8765`/`8766` (zero) |
| T-102-09 | Information Disclosure | skill data into `profile.json` privacy contract | mitigate | `test_skills_never_in_profile_json`: no profile import + `PROFILE_SCHEMA` still exactly 5 fields, `additionalProperties:false` |
| T-102-10 | Tampering | manifest↔curriculum lesson-ID drift | mitigate | Import-time `assert lesson_id in CURRICULUM` + `test_manifest_lesson_ids_exist_in_curriculum` |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/learn/test_skill_tree.py tests/learn/test_skill_tree_invariants.py` green.
- Per wave merge: `PYTHONPATH=src python3 -m pytest -q tests/learn` green (the whole learn suite, including the existing `test_runtime_invariants.py`).
- Phase gate: `PYTHONPATH=src python3 -m pytest -q` (full suite) green — confirms the new module + SCHEMA_VERSION bump red nothing cross-tree.
</verification>

<success_criteria>
- SKILL-01: 6 skills, each resolving a stage from a LearnProgress.
- SKILL-02: `compute` is pure (no side effects / I/O); Invariant #1 AST/grep gate green.
- SKILL-03: readable `SKILL_MANIFEST`; every referenced lesson id exists in CURRICULUM.
- COMP-01: first-try > with-strikes > click-through fill; fill ∈ [0,1]; monotonic on reload.
- COMP-02 (HEADLINE): 100% fill without the recital pass is NOT Competent (stage locked).
- Invariant #4 no-new-port + privacy no-profile.json pins green.
</success_criteria>

<output>
Create `.planning/phases/102-skill-tree-engine-data-model-competent-stage/102-02-SUMMARY.md` when done.
</output>
