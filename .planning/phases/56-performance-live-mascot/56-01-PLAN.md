---
phase: 56-performance-live-mascot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/ui/src/mascot/event-dispatcher.ts
  - tauri/ui/src/mascot/types.ts
  - tauri/ui/src/mascot/index.ts
  - tauri/ui/src/mascot/state-machine-fixtures.test.ts
  - tauri/ui/src/mascot/__fixtures__/event-traces.json
autonomous: true
requirements: [LIVE-05, LIVE-05a]
must_haves:
  truths:
    - "A phase=drop frame with music below PEAK_RMS does NOT trigger the peak/dance mode (held — anti-slop)"
    - "A phase=breakdown frame with music at-or-above LOW_RMS does NOT trigger the breakdown mode (held — anti-slop)"
    - "The mascot reads music + voice levels off the bus snapshot, not just bpm/mood/downbeat"
    - "The synthetic multi-mode trace silent→build→drop→breakdown→voice>0 enters the correct mode at each step"
    - "Hysteresis prevents a single contradictory frame from flipping the mode"
  artifacts:
    - path: "tauri/ui/src/mascot/event-dispatcher.ts"
      provides: "music/voice in SnapshotSlice + music-confirmation guard in stateForPhase/PHASE case"
      contains: "PEAK_RMS"
    - path: "tauri/ui/src/mascot/__fixtures__/event-traces.json"
      provides: "multi-mode trace + anti-slop contradiction trace"
      contains: "music"
    - path: "tauri/ui/src/mascot/state-machine-fixtures.test.ts"
      provides: "music/voice threaded into the harness SnapshotSlice reader + new trace assertions"
      contains: "music"
  key_links:
    - from: "tauri/ui/src/mascot/index.ts"
      to: "tauri/ui/src/mascot/event-dispatcher.ts (SnapshotSlice)"
      via: "currentSnapshot.music / .voice threaded from the live ws frame"
      pattern: "currentSnapshot\\.music"
    - from: "tauri/ui/src/mascot/event-dispatcher.ts (PHASE case)"
      to: "stateForPhase music guard"
      via: "snapshot.music passed into the guarded mode selector"
      pattern: "stateForPhase\\("
---

<objective>
Close the LIVE-05a anti-slop gap on the shipped Three.js rig: extend the rig's `SnapshotSlice` to carry `music`/`voice` levels, add the defence-in-depth music-confirmation guard to `drop`/`breakdown` mode entry (a `phase` that disagrees with the level is treated as "no signal" → current mode persists), and add the new anti-slop synthetic-frame fixtures the contract requires.

Purpose: A spurious `phase=="drop"` during a quiet section currently fires the peak animation — pure AI-slop (RESEARCH Pitfall 6). The guard is the single highest-value anti-slop addition in the phase. This plan makes the mascot's mode boundaries agree with `state.phase` by mirroring the Python source-of-truth thresholds.

Output: A hardened `event-dispatcher.ts` + extended `SnapshotSlice` + threaded `index.ts` snapshot reader + new vitest fixtures asserting (a) the full multi-mode sequence and (b) the contradictory-frame held-mode acceptance.

Scope guardrails honored:
- This is EXTEND-AND-PIN. Do NOT rebuild a sprite state machine, do NOT touch root `mascot.html` (dev/CI overlay only — keep `mascot-audit` green).
- `low`/`peak` are ALREADY mapped in `stateForPhase` (PATTERNS refines RESEARCH) — the gap is the music-confirmation guard, NOT a missing case.
- Keep the FSM PURE: no `Date.now`, no `setTimeout`, no `three` imports in `event-dispatcher.ts`/`state-machine.ts` (the verifier greps for these literal tokens).
- Mirror Python thresholds verbatim — never invent new ones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-performance-live-mascot/56-PATTERNS.md
@.planning/phases/56-performance-live-mascot/56-UI-SPEC.md
@.claude/skills/frontend-enforcement/SKILL.md

<interfaces>
<!-- Extracted from codebase this session — executor uses these directly, no exploration needed. -->

Source-of-truth thresholds (src/vibemix/audio/constants.py):
  SILENT_RMS = 0.012
  LOW_RMS    = 0.040
  PEAK_RMS   = 0.110

Current SnapshotSlice (tauri/ui/src/mascot/event-dispatcher.ts:58-63) — carries bpm/bpm_confidence/downbeat_phase/mood, MISSING music/voice:
  interface SnapshotSlice { bpm: number; bpm_confidence: number; downbeat_phase: number; mood: string; }

dispatchEvent signature (event-dispatcher.ts:157-162):
  dispatchEvent(machine, message, now: number, snapshot: SnapshotSlice): DispatchResult | null

stateForPhase (event-dispatcher.ts:110-129) — ALREADY maps all 7 phases incl. low/peak; gap = no music confirmation:
  function stateForPhase(phase: string): MascotState | null  // drop/peak→dance_hard, groove/build→idle_bop_to_beat_energetic, low→idle_bop_to_beat_mellow, silent/breakdown→idle_breathe, default→null

PHASE case (event-dispatcher.ts:200-223) — calls stateForPhase(to); null target falls through (anti-slop). This is the guard plug-in site.

Live ws frame (src/vibemix/runtime/ws_bus.py:266-288): flat floats from levels.snapshot() → music/voice/mic are NUMBERS at top level. Frame also carries phase/bpm/mood/bpm_confidence/downbeat_phase/beat_phase/emotion/reaction_intent/detected_genre/genre_confidence.

index.ts snapshot reader (tauri/ui/src/mascot/index.ts:186-208): reads m.bpm/m.bpm_confidence/m.downbeat_phase/m.mood off the flat frame; music/voice NOT yet threaded. currentSnapshot default at index.ts:168-173.

Fixture trace schema (VERIFIED on disk, __fixtures__/event-traces.json + the TS `Trace` interface at state-machine-fixtures.test.ts:56-62):
  Each trace = { "name": string, "criterion": number, "description": string,
                 "messages": [ { "t": number, "msg": { ... } } ],
                 "expectedTransitions": [ { "after_t": number, "state": MascotState } ] }
  - `criterion` is REQUIRED (TS interface + the aggregate test at :291-301 reads `t.criterion`; criteria 4/5/6 must each be covered by ≥1 trace).
  - The well-formed check (:262-267) requires `expectedTransitions.length >= 1` for EVERY trace — there is NO `expected[]` field; a "no transition" trace must still carry ≥1 entry (assert the PRIOR state persists).
  - Snapshot frames inside `msg`: `"type": "snapshot"` with NESTED `"music": {"rms": <n>, "peak": <n>}` / `"voice": {...}` (NOT flat floats). The harness reader (state-machine-fixtures.test.ts:182-197) currently reads bpm/bpm_confidence/downbeat_phase/mood and ignores music/voice.
  - BOOT seed: `initialMachineState(0)` boots to `idle_breathe` (the `actual[]` seed at :94-95 is `{ at: 0, state: machine.current, source: "boot" }`).

Anti-slop discipline to mirror (event-dispatcher.ts:259-261): `default: return null` on unknown subtype — copy this exact "contradiction → return null, never throw, never default to a decorative mode" shape for the level guard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SnapshotSlice with music/voice + thread it through the live frame and the fixture harness</name>
  <read_first>
    - tauri/ui/src/mascot/event-dispatcher.ts (SnapshotSlice at :58-63, dispatchEvent at :157-162) — the file being modified
    - tauri/ui/src/mascot/index.ts (:168-173 currentSnapshot default, :186-208 snapshot reader) — analog: how bpm/mood are threaded off the flat ws frame
    - tauri/ui/src/mascot/state-machine-fixtures.test.ts (:39-44 DEFAULT_SNAPSHOT, :182-197 snapshot reader) — analog: how the harness updates the SnapshotSlice ref
    - tauri/ui/src/mascot/__fixtures__/event-traces.json (snapshot frames carry music/voice as {rms,peak}) — the nested level shape the harness must read
  </read_first>
  <behavior>
    - SnapshotSlice gains `music: number` and `voice: number` (0..1, default 0).
    - index.ts reads `m.music`/`m.voice` as flat numbers off the live ws frame (matching levels.snapshot() which broadcasts top-level floats), defaulting to the prior value when absent.
    - The fixture harness reads `music.rms`/`voice.rms` from the nested snapshot frame shape into the SnapshotSlice ref (the live frame is flat; the fixture frame is nested — both feed the same SnapshotSlice numeric fields).
    - currentSnapshot default and DEFAULT_SNAPSHOT both gain `music: 0, voice: 0`.
    - No behavior change to any existing transition (music/voice are read-only additions at this step).
  </behavior>
  <action>
    Add `music: number` and `voice: number` to the `SnapshotSlice` interface in event-dispatcher.ts (keep the const-asserted, zero-runtime-cost discipline). In index.ts: add `music: 0, voice: 0` to the `currentSnapshot` default object and, in the snapshot-reader branch, read `m.music`/`m.voice` as flat numbers (`typeof m.music === "number" ? m.music : currentSnapshot.music`) and assign to `currentSnapshot.music`/`.voice` — mirroring the existing bpm/mood read pattern. In state-machine-fixtures.test.ts: add `music: 0, voice: 0` to `DEFAULT_SNAPSHOT` and, in the snapshot-reader block, read the NESTED fixture shape — pull `m.music`/`m.voice` as objects and take their `.rms` field (`typeof m.music === "object" && m.music ? (m.music as Record<string,number>).rms : snapshot.music`), assigning into the `snapshot.music`/`.voice` numeric fields. Do NOT change `dispatchEvent`'s logic yet — this task only widens the data carried.
  </action>
  <verify>
    <automated>cd tauri/ui && npm test -- state-machine-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `cd tauri/ui && npm test -- state-machine-fixtures` passes (all existing traces still green — no regression from widening SnapshotSlice).
    - `grep -n "music: number" tauri/ui/src/mascot/event-dispatcher.ts` returns the new SnapshotSlice field.
    - `grep -c "currentSnapshot.music" tauri/ui/src/mascot/index.ts` is ≥ 1 (live-frame thread present).
    - `cd tauri/ui && npm run typecheck` (or `tsc --noEmit`) passes — no type error from the widened interface.
  </acceptance_criteria>
  <done>SnapshotSlice carries music/voice; both the live frame reader (index.ts, flat floats) and the fixture harness reader (nested {rms}) populate them; all existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the music-confirmation defence-in-depth guard to drop/breakdown mode entry</name>
  <read_first>
    - tauri/ui/src/mascot/event-dispatcher.ts (stateForPhase at :110-129, PHASE case at :200-223, anti-slop `default: return null` at :259-261) — the file being modified
    - tauri/ui/src/mascot/state-machine.ts (:179-206 beat-lock typed-guard conjunction) — the sibling pattern: "all conditions must hold or fall through to no-op"
    - .planning/phases/56-performance-live-mascot/56-PATTERNS.md (Pattern Assignments → event-dispatcher.ts section, lines 39-83) — the exact guard shape + threshold constants
    - .planning/phases/56-performance-live-mascot/56-UI-SPEC.md (§The 6 modes table, drop + breakdown rows) — the contract: drop requires music≥PEAK_RMS, breakdown requires music<LOW_RMS
  </read_first>
  <behavior>
    - `phase=="drop"` AND `music >= 0.110` → drop mode (dance_hard). `phase=="drop"` with `music < 0.110` → guard returns null → current mode held (anti-slop).
    - `phase=="breakdown"` AND `music < 0.040` → breakdown mode (idle_breathe). `phase=="breakdown"` with `music >= 0.040` → guard returns null → current mode held.
    - `peak` keeps requiring `music >= 0.110` (same family as drop — apply the same PEAK_RMS confirmation).
    - All other phases (groove/build/low/silent) unchanged — they were not gated by the contract.
    - Unknown phase still → null (existing anti-slop default preserved verbatim).
  </behavior>
  <action>
    Change `stateForPhase` to accept the confirming level: thread `snapshot.music` into the call at the PHASE case (line 203) — pass `stateForPhase(to, snapshot.music)`. Inside `stateForPhase`, add named constants mirroring `src/vibemix/audio/constants.py` (a comment citing that file: SILENT_RMS=0.012, LOW_RMS=0.040, PEAK_RMS=0.110 — never magic numbers). For the `"drop"` and `"peak"` cases, return `dance_hard` only when `music >= PEAK_RMS`, else `return null`. For the `"breakdown"` case, return `idle_breathe` only when `music < LOW_RMS`, else `return null`. Use the SAME conjunction shape as the state-machine.ts beat-lock guard ("all conditions hold or fall through") and the SAME "contradiction → return null" discipline as the `default` arm — never throw, never default to a decorative mode. Leave `groove`/`build`/`low`/`silent` returns unchanged. The null target already falls through cleanly at PHASE-case line 204 (`if (target === null) return null`) — the contradictory frame exits there = current mode persists.
  </action>
  <verify>
    <automated>cd tauri/ui && npm test -- state-machine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PEAK_RMS\|LOW_RMS" tauri/ui/src/mascot/event-dispatcher.ts` shows the named constants (no inline magic numbers).
    - PURITY GATE (must stay clean): `grep -nE "Date\.now|setTimeout|from \"three\"|from 'three'" tauri/ui/src/mascot/event-dispatcher.ts | grep -v '^#'` returns zero matches (the doc-comment intentionally splits these tokens; a real occurrence breaks the build).
    - `cd tauri/ui && npm test -- state-machine` passes (existing dispatcher/state-machine tests green with the new guard signature).
    - `cd tauri/ui && npm run typecheck` passes.
  </acceptance_criteria>
  <done>drop/peak require music≥PEAK_RMS, breakdown requires music<LOW_RMS; a contradictory frame returns null (mode held); FSM purity preserved; existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the multi-mode + anti-slop contradiction fixtures (the LIVE-05a acceptance traces)</name>
  <read_first>
    - tauri/ui/src/mascot/__fixtures__/event-traces.json (existing 9 traces; VERIFIED schema: name/criterion/description/messages[{t,msg}]/expectedTransitions[{after_t,state}]; snapshot frames carry nested music/voice {rms,peak}) — the file being extended
    - tauri/ui/src/mascot/state-machine-fixtures.test.ts (:56-62 `Trace` interface incl. required `criterion`; :90-229 replayTrace; :237-254 matchExpected ±100ms; :262-267 well-formed check `expectedTransitions.length > 0`; :291-301 aggregate `t.criterion` read) — the replay harness that consumes the new traces
    - tests/integration/test_mascot_event_taxonomy_e2e.py (:110 _ENVELOPE_TYPES, :131-226 well-formed/canonical-subtype/expected-state assertions) — the Python cross-language pin that ALSO reads this fixture; new traces must keep it green
    - .planning/phases/56-performance-live-mascot/56-RESEARCH.md (Code Examples → Mascot synthetic-frame mode test, lines 312-321) — the exact two traces required
  </read_first>
  <behavior>
    - Trace "multi_mode_sequence" (criterion: 5): snapshot+PHASE frames silent → build → drop(music.rms≥0.110) → breakdown(music.rms<0.040) → AI_GENERATING_REPLY; each step's `expectedTransitions` entry matches the contract (silent→idle_breathe, build→idle_bop_to_beat_energetic, drop→dance_hard, breakdown→idle_breathe, speaking→talk_loop) within the harness's ±100ms tolerance.
    - Trace "anti_slop_drop_quiet" (criterion: 5): drive the machine into a known music mode (dance_hard via a real PEAK_RMS drop), then fire a snapshot setting music.rms=0.05 followed by a PHASE→drop event → mode does NOT change (held). This is the highest-value new assertion. The HELD assertion is expressed POSITIVELY (the harness requires `expectedTransitions.length >= 1` — an empty list is illegal and fails the well-formed check at :262-267): assert that the PRIOR seeded state (dance_hard) is STILL the active transition at a timestamp AFTER the contradictory low-music drop frame.
    - Each new trace carries the mandatory `criterion: number` field (the `Trace` TS interface at :56-62 requires it; the aggregate test at :291-301 reads `t.criterion`). Both new traces use `criterion: 5` — they extend the criterion-5 event→state mapping family.
    - The Python taxonomy test (well-formed structure, canonical subtypes, canonical expected states) stays green with the additions.
  </behavior>
  <action>
    Add two new traces to `__fixtures__/event-traces.json` following the EXACT on-disk schema (verified this session): each trace object is `{ "name": <string>, "criterion": <number>, "description": <string>, "messages": [ { "t": <number>, "msg": { ... } } ], "expectedTransitions": [ { "after_t": <number>, "state": <MascotState> } ] }`. There is NO `expected[]` field — assertions live in `expectedTransitions[]` of `{ after_t, state }`. Each new trace MUST set `"criterion": 5` (the TS `Trace` interface requires the field and the aggregate test reads `t.criterion`). Inside each `messages[i].msg`: snapshot frames use `"type": "snapshot"` with the NESTED level shape `"music": { "rms": <n>, "peak": <n> }` and `"voice": { ... }`; event frames use `"type": "event"`, `"subtype"`, `"payload"`.

    For "multi_mode_sequence": interleave snapshot frames carrying the nested `music: {rms,...}` level matching each phase (silent at music.rms<0.012, build at mid music, drop at music.rms≥0.110, breakdown at music.rms<0.040) with `event/PHASE` frames whose `payload.to` is the phase, plus a closing `event/AI_GENERATING_REPLY` for the speaking step. Populate `expectedTransitions[]` with `{ after_t, state }` entries — the canonical target state at each step's timestamp, matched within the harness's ±100ms tolerance.

    For "anti_slop_drop_quiet": (1) seed the machine into dance_hard with a real drop — snapshot `music: {rms: 0.30}` + `event/PHASE` `to:"drop"` (with low bpm_confidence so it switches now, mirroring low_confidence_immediate_switch) producing `{ after_t: <t1>, state: "dance_hard" }`; (2) a snapshot setting `music: {rms: 0.05}`; (3) `event/PHASE` `to:"drop"` at a LATER `t2`. The contradictory low-music drop is guard-rejected (stateForPhase returns null → dispatchEvent returns null → applyTransition is never called), so dance_hard persists across the window. Express the HELD assertion POSITIVELY: add an `expectedTransitions` entry `{ after_t: <covers the post-t2 window>, state: "dance_hard" }` — the harness matches `state` present AND `|actual.at - after_t| <= 100ms`; since no new transition fired after t1, the dance_hard transition at t1 remains the latest actual, and it matches an `after_t` set within ±100ms of t1. Pick `after_t` ≈ t1 so the prior-state match is valid AND it semantically asserts "dance_hard is still active after the contradictory drop". Do NOT emit an empty `expectedTransitions` (the well-formed check at :262-267 requires length ≥ 1).

    Use ONLY canonical subtypes (`PHASE`, `AI_GENERATING_REPLY`) and canonical MascotState targets so `test_mascot_event_taxonomy_e2e.py` stays green. The trace-iterating runner (state-machine-fixtures.test.ts:270-289) auto-covers any well-formed trace; add an explicit `it(...)` only if a finer per-step assertion is needed.
  </action>
  <verify>
    <automated>cd tauri/ui && npm test -- state-machine-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `cd tauri/ui && npm test -- state-machine-fixtures` passes including the two new traces (auto-covered by the trace-iterating runner).
    - `grep -c "multi_mode_sequence\|anti_slop_drop_quiet" tauri/ui/src/mascot/__fixtures__/event-traces.json` returns 2.
    - Both new trace objects carry `"criterion": 5` — `grep -A2 '"name": "multi_mode_sequence"' tauri/ui/src/mascot/__fixtures__/event-traces.json | grep -c '"criterion"'` ≥ 1 and the same for `anti_slop_drop_quiet` (TS `Trace` interface + the aggregate `t.criterion` read both require it).
    - Each new trace asserts via `expectedTransitions` (the on-disk schema) with length ≥ 1 — there is NO `expected[]` field; `anti_slop_drop_quiet`'s entry is a positive `{ after_t, state: "dance_hard" }` proving the prior state persists past the contradictory low-music drop (no empty expectedTransitions; well-formed check at :262-267 holds).
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_event_taxonomy_e2e.py -m integration -x -q` passes (cross-language fixture contract held).
    - The anti-slop trace proves a `phase=drop`+`music.rms=0.05` frame produces NO new mode transition (the held dance_hard `expectedTransition` matches and is green).
  </acceptance_criteria>
  <done>Two new fixtures green in vitest, each carrying `criterion: 5` and asserting via `expectedTransitions`; the held-mode anti-slop case is proven by a positive prior-state (dance_hard) assertion with ≥1 expectedTransition (no empty list); Python taxonomy test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ws bus (127.0.0.1:8765) → webview | Untrusted-shaped JSON frames cross into the mascot rig; a malformed/contradictory frame must never crash or fabricate a mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | event-dispatcher.ts mode selection | mitigate | Music-confirmation guard: `phase`/`music` disagreement → `return null` (mode held). A spoofed/misclassified `phase=drop` cannot pump the mascot without a real PEAK_RMS level. Doubles as the LIVE-05a anti-slop control. |
| T-56-02 | Denial of Service | webview JSON.parse of ws frame | accept | Existing try/catch + drop-on-unknown (ws-client.ts:117-127, dispatchEvent default:return null) already handles malformed frames; this plan preserves that discipline (no throw on a bad field). |
| T-56-03 | Information Disclosure | ws bus binding | accept | Bus is `127.0.0.1` loopback only (verified ws://127.0.0.1:8765); no secrets on the wire. Out of this plan's surface; unchanged. |
</threat_model>

<verification>
- `cd tauri/ui && npm test` (full vitest) green — no mascot rig regression.
- `cd tauri/ui && npm run typecheck` green.
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_event_taxonomy_e2e.py -m integration -x -q` green.
- Purity grep gate clean on event-dispatcher.ts (no Date.now/setTimeout/three import).
- `mascot-audit` CI gates (`mascot-tauri-only-grep`, `mascot-anti-slop`, `mascot-event-coverage`) stay green — new tests target TS logic/fixtures, never grep root `mascot.html`.
</verification>

<success_criteria>
- A `phase=drop` frame with `music < PEAK_RMS` does NOT flip to drop mode (proven by the anti_slop_drop_quiet fixture).
- A `phase=breakdown` frame with `music >= LOW_RMS` does NOT flip to breakdown mode (held).
- The mascot consumes music+voice levels off the bus snapshot (SnapshotSlice extended + threaded both sides).
- The multi-mode sequence enters the right mode at each step within ±100ms.
- FSM purity preserved; all existing + new tests green; CI audit green.
</success_criteria>

<output>
Create `.planning/phases/56-performance-live-mascot/56-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
