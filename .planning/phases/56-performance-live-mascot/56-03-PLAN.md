---
phase: 56-performance-live-mascot
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - tauri/ui/src/mascot/event-dispatcher.ts
  - tauri/ui/src/mascot/state-machine-fixtures.test.ts
  - tauri/ui/src/mascot/__fixtures__/event-traces.json
  - tests/integration/test_mascot_dispatch_latency.py
autonomous: true
requirements: [LIVE-05, LIVE-05a, PERF-03]
must_haves:
  truths:
    - "All six contract modes are reachable from real bus events: idle (silent/sub-SILENT_RMS), vibing (groove), building (build), drop (drop+music>=PEAK_RMS), breakdown (breakdown+music<LOW_RMS), speaking (AI_GENERATING_REPLY)"
    - "The speaking path (talk_loop, priority 80) overrides every music mode and is proven to block an incoming dance/idle request mid-talk"
    - "mood tints the same mode (variant) rather than adding a 7th mode; emotion is a finer nudge and emotion==null is a no-op"
    - "A synthetic mode-transition frame in the dispatch-latency test keeps p95 sidecar->client under the 50ms budget (mode-select adds <1ms)"
  artifacts:
    - path: "tauri/ui/src/mascot/__fixtures__/event-traces.json"
      provides: "a six-mode reachability trace + a speaking-overrides-music trace exercising all contract modes"
      contains: "talk_loop"
    - path: "tauri/ui/src/mascot/state-machine-fixtures.test.ts"
      provides: "assertions that each of the six modes is entered from its real bus event and speaking blocks music modes"
      contains: "talk_loop"
    - path: "tests/integration/test_mascot_dispatch_latency.py"
      provides: "a synthetic mode-transition frame in the emitted set + p95 < 50ms assertion holds"
      contains: "P95_BUDGET_MS"
  key_links:
    - from: "tauri/ui/src/mascot/__fixtures__/event-traces.json"
      to: "state-machine-fixtures.test.ts replay harness"
      via: "six-mode + speaking-block traces replayed through dispatchEvent + planTransition"
      pattern: "talk_loop"
    - from: "tests/integration/test_mascot_dispatch_latency.py"
      to: "the 100ms MASCOT-08 budget (sidecar-side half = 50ms)"
      via: "emit a mode-transition frame and assert p95(t_recv - t_emit) < P95_BUDGET_MS"
      pattern: "P95_BUDGET_MS"
---

<objective>
Prove the LIVE-05a contract is fully satisfied on the shipped Three.js rig: all six distinct modes are reachable from real bus events, the speaking/emoting path overrides music reactivity (and is proven to block lower-priority music transitions mid-talk), and mood/emotion tint the same mode rather than spawning new ones. Then pin PERF-03's webview-side budget by adding a synthetic mode-transition frame to the dispatch-latency test and confirming the p95 sidecar->client budget still holds.

Purpose: Plan 56-01 closed the highest-value anti-slop gap (the music-confirmation guard). This plan closes the remaining LIVE-05a contract items — the "≥6 distinct modes, each gated to a real event" reachability proof and the speaking-overrides-music rule — and the PERF-03 60fps-floor assertion (mode-select is a handful of float compares, <1ms, so the existing 50ms p95 budget holds with a mode-transition frame in the stream). RESEARCH/PATTERNS confirm the rig ALREADY has the states, the priority ladder (talk=80 > dance=40), and the downbeat-scheduled hysteresis — so the work is reachability traces + the speaking-block proof + the latency-frame extension, NOT new modes or a new priority system.

Output: a six-mode reachability trace + a speaking-overrides-music trace in `event-traces.json` with matching assertions in the vitest harness, and an extended `test_mascot_dispatch_latency.py` whose emitted set includes a mode-transition frame while p95 stays under 50ms.

Scope guardrails honored:
- EXTEND-AND-PIN. Use the EXISTING states (`idle_breathe`, `idle_bop_to_beat_mellow`, `idle_bop_to_beat_energetic`, `dance_hard`, `talk_loop`) and the EXISTING priority ladder — do NOT add union members or a parallel priority system.
- Speaking stays the EVENT-DRIVEN path (`AI_GENERATING_REPLY` -> `talk_loop`) per PATTERNS — do NOT add a parallel level-driven (`voice>0.05`) speaking path unless a reachability gap forces it; the talk-blocks-dance rule (state-machine.ts:165-174, talk priority 80 > dance 40) already enforces "speaking overrides music".
- mood tints the same mode (mood.ts MOOD_PROFILES hype-man/teacher/coach); emotion (neutral/focused/hyped/concerned/null) is a finer nudge; emotion==null is a no-op. Do NOT confuse mood with emotion (Pitfall 5).
- Keep the FSM PURE (no Date.now/setTimeout/three imports in event-dispatcher.ts — the verifier greps for these literal tokens). Hysteresis stays the existing planTransition/schedule_for_downbeat approach.
- mascot-audit CI gates stay green: do NOT grep/import root mascot.html from tests; keep the event-coverage-matrix covered; new captions/clip names off the anti-slop blocklist.
- Do NOT mark the felt "mascot feels alive across modes under a real set" success criterion auto-complete — it is the Kaan-action live-drive carveout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-performance-live-mascot/56-PATTERNS.md
@.planning/phases/56-performance-live-mascot/56-UI-SPEC.md
@.claude/skills/frontend-enforcement/SKILL.md
@.planning/phases/56-performance-live-mascot/56-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase this session — executor uses these directly, no exploration needed. -->
<!-- NOTE: SnapshotSlice already carries music/voice after Plan 56-01 (read 56-01-SUMMARY.md for the final field set). -->

The six contract modes -> EXISTING MascotState (no new union members):
  idle (dead-air)   -> idle_breathe              (phase "silent"; or music < SILENT_RMS 0.012)
  vibing (groove)   -> idle_bop_to_beat_energetic (phase "groove")  | low -> idle_bop_to_beat_mellow
  building          -> idle_bop_to_beat_energetic (phase "build")
  drop / peak       -> dance_hard                 (phase "drop"/"peak" AND music >= PEAK_RMS 0.110 — guard added in 56-01)
  breakdown / chill -> idle_breathe               (phase "breakdown" AND music < LOW_RMS 0.040 — guard added in 56-01)
  speaking / emoting-> talk_loop                  (event subtype AI_GENERATING_REPLY)

stateForPhase (event-dispatcher.ts) — maps all 7 phases; drop/peak/breakdown now level-confirmed (56-01).
PHASE case (event-dispatcher.ts:200-223) — calls stateForPhase; null target falls through (anti-slop, mode held).
AI_GENERATING_REPLY case (event-dispatcher.ts:225-230) -> runRequest(state:"talk_loop", trigger:"ai_generating_reply").

Priority ladder (types.ts STATE_PRIORITY): effect 100 > talk 80 > anticipation 70 > react 60 > dance 40 > explanation 30 > idle 20 > misc 10.
Block rule (state-machine.ts:165-174): when currentClass is "talk" or "effect" AND requestedPriority < currentPriority -> { action: "deny", reason: "blocked_by_talk" }. THIS is why talk_loop blocks incoming dance/idle. Do not rebuild it.

mood (mood.ts MOOD_PROFILES): "hype-man" | "teacher" | "coach" — a variant TINT on the same mode, NOT a 7th mode. carried on SnapshotSlice.mood.
emotion (emotion_router.py -> bus field): "neutral" | "focused" | "hyped" | "concerned" | null — finer eye/brow nudge; null = no-op.

Fixture trace schema (VERIFIED on disk, __fixtures__/event-traces.json + the TS `Trace` interface at state-machine-fixtures.test.ts:56-62):
  Each trace = { "name": string, "criterion": number, "description": string,
                 "messages": [ { "t": number, "msg": { ... } } ],
                 "expectedTransitions": [ { "after_t": number, "state": MascotState } ] }
  - `criterion` is REQUIRED (TS interface + aggregate test at :291-301 reads `t.criterion`; criteria 4/5/6 must each stay covered).
  - The well-formed check (:262-267) requires `expectedTransitions.length >= 1` for EVERY trace — there is NO `expected[]` field. A "no transition / mode held" trace MUST still carry ≥1 entry (assert the PRIOR state persists), exactly like the existing `talk_blocks_dance` trace (which asserts only `{ after_t: 50, state: "talk_loop" }` — the later contradictory PHASE→drop produces no new transition, so talk_loop persists).
  - Snapshot frames inside `msg`: `"type": "snapshot"` with NESTED `"music": {"rms": <n>, "peak": <n>}` / `"voice": {...}`.

Fixture harness (state-machine-fixtures.test.ts): replayTrace (:90-229) + matchExpected (:237-254), ±100ms tolerance, pure (no Three.js/DOM). DEFAULT_SNAPSHOT (:39-44, now includes music/voice after 56-01). Trace-iterating runner at :270-289 auto-covers any well-formed trace. Existing traces include drop_then_groove, ai_speaks_then_done, talk_blocks_dance, silent_phase.

Cross-language pin: tests/integration/test_mascot_event_taxonomy_e2e.py replays the same fixture Python-side — use ONLY canonical subtypes (PHASE, AI_GENERATING_REPLY, AI_REPLY_DONE) + canonical MascotState targets so it stays green.

Dispatch-latency test (tests/integration/test_mascot_dispatch_latency.py): real websockets.serve/connect. Server emits a FIXED FRAME_COUNT=100 (:54) frames @EMIT_HZ=30 (:57) in `for i in range(FRAME_COUNT)` (:126-133); each frame is `json.dumps({"seq": i, "t_emit_ns": time.perf_counter_ns()})` (:127-128). Client drains EXACTLY FRAME_COUNT frames in `for _ in range(FRAME_COUNT)` (:141-151), recording `(t_recv_ns - t_emit_ns)/1e6` per frame. The sanity gate (:184) requires `len(latencies_ms) >= int(FRAME_COUNT * 0.95)` — i.e. ≥95 valid samples, each carrying a usable `t_emit_ns` int (:147-149 skips frames where `t_emit_ns` is not an int). Asserts `p95 < P95_BUDGET_MS = 50.0` (:62, :202). @pytest.mark.integration (:162). The server-emit count and client-drain count MUST stay equal (both bound by FRAME_COUNT) or the drain loop hangs / under-samples. Do NOT grep/import mascot.html (CI gate forbids it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the six-mode reachability trace — each contract mode entered from its real bus event</name>
  <read_first>
    - tauri/ui/src/mascot/__fixtures__/event-traces.json (existing traces; VERIFIED schema name/criterion/description/messages[{t,msg}]/expectedTransitions[{after_t,state}]; nested music/voice level shape) — the file being extended
    - tauri/ui/src/mascot/state-machine-fixtures.test.ts (:56-62 `Trace` interface incl. required `criterion`; replayTrace :90-229; matchExpected :237-254; DEFAULT_SNAPSHOT :39-44 — now with music/voice from 56-01; aggregate `t.criterion` read :291-301) — the harness consuming the trace
    - tauri/ui/src/mascot/event-dispatcher.ts (stateForPhase + PHASE case + AI_GENERATING_REPLY case :225-230 — level-confirmed after 56-01) — the dispatcher under test
    - .planning/phases/56-performance-live-mascot/56-UI-SPEC.md (§The 6 modes table :127-140; per-mode visual contract :171-186) — the contract each mode must satisfy
    - tests/integration/test_mascot_event_taxonomy_e2e.py (canonical subtypes/states it validates) — the Python cross-language pin to keep green
  </read_first>
  <behavior>
    - Trace "six_mode_reachability" (criterion: 5): a single sequence that reaches all six contract modes from real bus events, each within ±100ms:
        silent (snapshot music.rms<SILENT_RMS + PHASE->silent)     -> idle_breathe
        groove (snapshot mid music + PHASE->groove)                -> idle_bop_to_beat_energetic
        build  (snapshot rising music + PHASE->build)              -> idle_bop_to_beat_energetic
        drop   (snapshot music.rms>=0.110 + PHASE->drop)           -> dance_hard
        breakdown (snapshot music.rms<0.040 + PHASE->breakdown)    -> idle_breathe
        speaking (event AI_GENERATING_REPLY)                        -> talk_loop
    - The trace carries the mandatory `criterion: 5` field (it extends the criterion-5 event→state mapping family; the TS `Trace` interface requires it).
    - The trace uses ONLY canonical subtypes (PHASE, AI_GENERATING_REPLY) and canonical MascotState targets so the Python taxonomy test stays green.
    - The harness asserts each expected state is reached at its step (proves all six modes are reachable from a real signal — the LIVE-05a "≥6 distinct modes" acceptance).
  </behavior>
  <action>
    Add a "six_mode_reachability" trace to `__fixtures__/event-traces.json` in the EXACT on-disk schema: `{ "name": "six_mode_reachability", "criterion": 5, "description": "...", "messages": [ { "t": <n>, "msg": { ... } } ], "expectedTransitions": [ { "after_t": <n>, "state": <MascotState> } ] }`. The `"criterion": 5` field is MANDATORY (TS interface + aggregate `t.criterion` read). Interleave snapshot frames (`"type": "snapshot"` with nested `"music": {"rms": <n>, ...}`) matching each phase (silent at music.rms<0.012, groove at mid, build rising, drop at music.rms>=0.110, breakdown at music.rms<0.040) with `event/PHASE` frames (payload.to = the phase) and a closing `event/AI_GENERATING_REPLY` for the speaking step. Populate `expectedTransitions` with the six canonical target states at their step timestamps using the harness's ±100ms tolerance. The trace-iterating runner (:270-289) auto-covers the new trace; add an explicit `it(...)` only if a finer per-step assertion is needed. Reference LIVE-05/LIVE-05a in the trace description. Keep all subtypes/states canonical.
  </action>
  <verify>
    <automated>cd tauri/ui && npm test -- state-machine-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `cd tauri/ui && npm test -- state-machine-fixtures` passes including the six_mode_reachability trace.
    - `grep -c "six_mode_reachability" tauri/ui/src/mascot/__fixtures__/event-traces.json` returns 1.
    - The trace carries `"criterion": 5` — `grep -A2 '"name": "six_mode_reachability"' tauri/ui/src/mascot/__fixtures__/event-traces.json | grep -c '"criterion"'` ≥ 1 (TS `Trace` interface + aggregate read require it).
    - The trace's expectedTransitions reach all six canonical states: idle_breathe, idle_bop_to_beat_energetic, dance_hard, talk_loop (idle_breathe + idle_bop_to_beat_energetic each appear for >1 phase, so >=4 distinct states across >=6 phase/event steps).
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_event_taxonomy_e2e.py -m integration -x -q` passes (cross-language contract held).
  </acceptance_criteria>
  <done>All six contract modes are proven reachable from real bus events via one replayed trace carrying `criterion: 5`; canonical subtypes/states keep the Python taxonomy pin green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the speaking path overrides music modes (talk-blocks-music) + mood/emotion tint discipline</name>
  <read_first>
    - tauri/ui/src/mascot/state-machine.ts (block rule :165-174 — talk/effect blocks lower priority; planTransition :137-215) — the override mechanism under test
    - tauri/ui/src/mascot/event-dispatcher.ts (AI_GENERATING_REPLY case :225-230; AI_REPLY_DONE case :232-250; afterReactYesState :145-153) — the speaking enter/exit path
    - tauri/ui/src/mascot/__fixtures__/event-traces.json (existing `talk_blocks_dance` trace at the verified schema — the analog to mirror; note it asserts ONLY `{ after_t: 50, state: "talk_loop" }` and the later contradictory PHASE→drop yields no new transition) — the file being extended
    - tauri/ui/src/mascot/state-machine-fixtures.test.ts (:56-62 `Trace` interface; well-formed check :262-267 requires expectedTransitions.length >= 1; aggregate `t.criterion` :291-301) — the harness
    - tauri/ui/src/mascot/mood.ts (MOOD_PROFILES hype-man/teacher/coach) + types.ts (MascotEmotion neutral/focused/hyped/concerned) — the tint vocabulary
    - .planning/phases/56-performance-live-mascot/56-UI-SPEC.md (§Mood as variant tint :142-152; speaking mode row :133, hysteresis speaking enter/exit :160-164) — the contract
    - .planning/phases/56-performance-live-mascot/56-PATTERNS.md (state-machine.ts hysteresis section :101-120; "Mood-as-variant-tint" shared pattern :272-274; Pitfall 5 mood vs emotion) — the discipline
  </read_first>
  <behavior>
    - Trace "speaking_overrides_music" (criterion: 5): while in a music mode (dance_hard after a real drop), an AI_GENERATING_REPLY enters talk_loop; an incoming music PHASE event (PHASE->groove) DURING talk is DENIED (blocked_by_talk) — the mascot stays talk_loop. After AI_REPLY_DONE, it returns to a music-context state (react_yes -> afterReactYesState). This proves "speaking overrides every music mode" (UI-SPEC speaking row).
    - The HELD assertion is expressed POSITIVELY, exactly like the existing `talk_blocks_dance` trace: the contradictory PHASE->groove during talk produces NO new transition (the block rule denies it), so talk_loop persists. The well-formed check (:262-267) forbids an empty `expectedTransitions`, so assert the PRIOR talk_loop state is STILL the active transition at a timestamp AFTER the denied groove frame — `{ after_t: <covers the post-deny window>, state: "talk_loop" }`. Do NOT include a groove/idle state in `expectedTransitions` during the talk window.
    - The trace carries the mandatory `criterion: 5` field (TS `Trace` interface requires it; it extends the criterion-5 event→state mapping family).
    - mood is asserted as a variant tint on the SnapshotSlice (mood drives WHICH variant, NOT a distinct MascotState/mode) — a mood change does not by itself add or change the mode in the dispatcher; assert the dispatcher does not branch the mode on mood (mood lives on the snapshot ref, consumed as a tint).
    - emotion==null is a no-op (backward-compatible) — assert no transition is produced by an emotion-only/null-emotion frame.
    - No new states, no parallel level-driven speaking path (event-driven path is sufficient — confirmed by the reachability trace in Task 1 + this block proof).
  </behavior>
  <action>
    Add a "speaking_overrides_music" trace to `__fixtures__/event-traces.json` in the EXACT on-disk schema (`name`, `criterion: 5`, `description`, `messages[{t,msg}]`, `expectedTransitions[{after_t,state}]`), mirroring the existing `talk_blocks_dance` trace. Sequence: (1) snapshot `music: {rms: 0.30}` + `event/PHASE` `to:"drop"` (low bpm_confidence so it switches now) -> dance_hard; (2) `event/AI_GENERATING_REPLY` -> talk_loop at `t_talk`; (3) `event/PHASE` `to:"groove"` at a later `t_deny` that MUST be denied (machine stays talk_loop); (4) `event/AI_REPLY_DONE` returning to a music-context state. Express the HELD assertion POSITIVELY in `expectedTransitions`: include `{ after_t: ~t_talk, state: "talk_loop" }` (the latest actual transition before AND after the denied groove is talk_loop, so it matches the post-deny window within ±100ms) — do NOT add a groove/idle entry during the talk window, and do NOT emit an empty `expectedTransitions` (the well-formed check at :262-267 requires length ≥ 1). Mirror exactly how `talk_blocks_dance` proves the hold with a single prior-state entry.

    Add the matching assertion in state-machine-fixtures.test.ts proving the music PHASE during talk did not change the active state (talk_loop held) — reuse the `talk_blocks_dance` assertion shape. Add (or extend an existing) assertion that mood is carried on the SnapshotSlice and does NOT branch the dispatcher's mode selection (a mood-only snapshot update produces no transition), and that an emotion==null frame is a no-op. Use ONLY canonical subtypes (AI_GENERATING_REPLY, PHASE, AI_REPLY_DONE) + canonical states so the Python taxonomy test stays green. Do NOT modify state-machine.ts's block rule (it already enforces the override) and do NOT add a level-driven speaking path. Keep event-dispatcher.ts pure if any edit is needed there (no Date.now/setTimeout/three).
  </action>
  <verify>
    <automated>cd tauri/ui && npm test -- state-machine-fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `cd tauri/ui && npm test -- state-machine-fixtures` passes including the speaking_overrides_music trace.
    - `grep -c "speaking_overrides_music" tauri/ui/src/mascot/__fixtures__/event-traces.json` returns 1.
    - The trace carries `"criterion": 5` — `grep -A2 '"name": "speaking_overrides_music"' tauri/ui/src/mascot/__fixtures__/event-traces.json | grep -c '"criterion"'` ≥ 1.
    - The held assertion is POSITIVE and well-formed: `expectedTransitions` has length ≥ 1 and asserts `state: "talk_loop"` persists past the denied music PHASE (no empty list; no groove/idle entry during the talk window) — the speaking-overrides-music acceptance, mirroring `talk_blocks_dance`.
    - PURITY GATE: `grep -nE "Date\.now|setTimeout|from \"three\"|from 'three'" tauri/ui/src/mascot/event-dispatcher.ts | grep -v '^ \*' | grep -v '^//'` returns zero real-code matches (doc-comment token-splitting preserved).
    - `cd tauri/ui && npm test` (full vitest) passes — event-coverage-matrix + all mascot tests green (mascot-audit discipline held).
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_event_taxonomy_e2e.py -m integration -x -q` passes.
  </acceptance_criteria>
  <done>Speaking provably overrides music modes via the existing talk-block rule (proven by a positive talk_loop-persists `expectedTransition`, criterion 5, no empty list); mood is a tint not a mode and emotion==null is a no-op; FSM purity + CI matrix preserved; no parallel speaking path added.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the dispatch-latency test with a synthetic mode-transition frame (PERF-03 webview floor)</name>
  <read_first>
    - tests/integration/test_mascot_dispatch_latency.py (FRAME_COUNT=100/EMIT_HZ=30 :54-57; P95_BUDGET_MS=50.0 :62; server_handler emit loop `for i in range(FRAME_COUNT)` :126-133, frame payload `{"seq","t_emit_ns"}` :127-128; client drain `for _ in range(FRAME_COUNT)` + latency calc :141-151; sanity gate `len >= FRAME_COUNT*0.95` :184; @pytest.mark.integration :162) — the file being modified
    - .planning/phases/56-performance-live-mascot/56-UI-SPEC.md (§Motion / 60fps budget :190-208 — mode-select < 0.5ms/frame, transform/opacity/filter only, single rAF loop) — the budget contract
    - .planning/phases/56-performance-live-mascot/56-PATTERNS.md (test_mascot_dispatch_latency.py section :220-222; "Observe-don't-instrument hot paths" :268-270) — the extend-don't-rebuild discipline
    - .planning/phases/56-performance-live-mascot/56-RESEARCH.md (PERF-03 row :53; Test Map PERF-03 :410) — the p95 < 50ms sidecar-side budget rationale
  </read_first>
  <behavior>
    - The emitted frame set includes at least one synthetic mode-transition frame (a frame shaped like a real PHASE/snapshot mode change carrying its emit timestamp `t_emit_ns`) interleaved with the existing synthetic frames.
    - The server-emit count and the client-drain count STAY EQUAL (the harness binds both to FRAME_COUNT and the sanity gate at :184 requires ≥95 valid samples each carrying a usable `t_emit_ns` int). The mode-transition frame REPLACES one of the FRAME_COUNT seq frames (it still carries `seq` + `t_emit_ns`) — it does NOT add an extra emit beyond FRAME_COUNT. So the loop bound stays `range(FRAME_COUNT)` on BOTH sides and the drain count matches the emit count; total valid samples stay = FRAME_COUNT ≥ 95.
    - Every emitted frame (including the mode-transition one) carries `t_emit_ns` as an int so it is NOT skipped at :147-149 — the ≥95-sample sanity gate holds.
    - The p95(t_recv - t_emit) assertion still holds under P95_BUDGET_MS = 50.0 (mode-select is <1ms) — proving the mode machine adds no measurable sidecar->client latency.
    - The test stays @pytest.mark.integration (deselected from the fast run); no new harness, no second loop, no grep/import of mascot.html.
  </behavior>
  <action>
    In tests/integration/test_mascot_dispatch_latency.py extend the `server_handler` emit loop so that ONE of the FRAME_COUNT emitted frames is a synthetic mode-transition frame instead of a plain seq frame (e.g. on a chosen iteration `i`, emit a PHASE-shaped payload `{"seq": i, "t_emit_ns": <perf_counter_ns>, "type": "event", "subtype": "PHASE", "payload": {"to": "drop"}}` — note it STILL carries `seq` and `t_emit_ns`). Do NOT add an extra frame beyond FRAME_COUNT and do NOT change the `range(FRAME_COUNT)` bound on either the server emit loop OR the client drain loop — the mode-transition frame REPLACES a seq frame so server-emit count == client-drain count == FRAME_COUNT, keeping the :184 sanity gate (≥95 valid samples) satisfied. Keep `t_emit_ns` mandatory on every frame (the client skips frames lacking an int `t_emit_ns` at :147-149; the mode-transition frame must carry it). Keep the client-side latency calculation and the existing `assert p95 < P95_BUDGET_MS` (50.0) unchanged — confirm it still holds with the mode-transition frame present. Reuse FRAME_COUNT/EMIT_HZ and the existing websockets.serve/connect plumbing; do NOT add a second server, a new budget constant, or import/grep mascot.html. Reference PERF-03 + the MASCOT-08 100ms (sidecar-side 50ms half) budget in the docstring. Keep @pytest.mark.integration.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_dispatch_latency.py -m integration -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_dispatch_latency.py -m integration -x -q` passes with the mode-transition frame in the emitted set.
    - The mode-transition frame REPLACES a seq frame (not an extra emit): server emit loop and client drain loop both stay bound to `FRAME_COUNT` and the `len(latencies_ms) >= int(FRAME_COUNT * 0.95)` sanity gate (:184) still passes (≥95 valid samples, each carrying `t_emit_ns`).
    - `grep -c "t_emit_ns" tests/integration/test_mascot_dispatch_latency.py` is ≥ 2 (mode-transition frame still carries the emit timestamp — `t_emit_ns` remains mandatory on every frame).
    - `grep -c "P95_BUDGET_MS" tests/integration/test_mascot_dispatch_latency.py` is >= 1 and the value 50.0 is unchanged (no budget loosening).
    - `grep -c "mascot.html" tests/integration/test_mascot_dispatch_latency.py` returns 0 (mascot-tauri-only-grep gate respected).
    - The default fast run (`pytest -q`) still deselects this integration test.
  </acceptance_criteria>
  <done>The dispatch-latency test emits a mode-transition frame (replacing one seq frame so emit==drain==FRAME_COUNT, each carrying t_emit_ns) and the p95 sidecar->client budget (50ms) still holds — PERF-03's webview-side floor is pinned without loosening the budget, breaking the FRAME_COUNT contract, or touching mascot.html.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ws bus (127.0.0.1:8765) -> webview | Untrusted-shaped JSON frames cross into the mascot rig; a malformed/contradictory frame must never crash or fabricate a mode or a gesture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-07 | Tampering | mascot mode selection (six-mode reachability) | mitigate | Every mode entry is gated to a real canonical bus event; the reachability + speaking-block traces prove no decorative/random flips. The 56-01 music-confirmation guard prevents a spoofed phase from forcing a mode. |
| T-56-08 | Spoofing | speaking-overrides-music | mitigate | talk_loop (priority 80) blocks lower-priority music transitions via the existing block rule; a spurious music PHASE cannot interrupt the AI talking. Proven by the speaking_overrides_music trace. |
| T-56-09 | Denial of Service | dispatch-latency under mode-transition load | accept | Mode-select is <1ms; the p95 < 50ms budget holds with a mode-transition frame present. Observed from a real client, never instrumenting the hot path. |
</threat_model>

<verification>
- `cd tauri/ui && npm test` (full vitest) green — all mascot rig tests incl. the two new traces + event-coverage-matrix.
- `cd tauri/ui && npm run typecheck` green.
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/integration/test_mascot_event_taxonomy_e2e.py tests/integration/test_mascot_dispatch_latency.py -m integration -x -q` green.
- Purity grep gate clean on event-dispatcher.ts (no Date.now/setTimeout/three import).
- mascot-audit CI gates (mascot-tauri-only-grep, mascot-anti-slop, mascot-event-coverage) stay green — new tests target TS logic/fixtures, never grep root mascot.html.
</verification>

<success_criteria>
- All six contract modes (idle, vibing, building, drop, breakdown, speaking) are proven reachable from real canonical bus events (LIVE-05a "≥6 distinct modes").
- Speaking provably overrides every music mode via the existing talk-block rule (LIVE-05a speaking row).
- mood tints the same mode (not a 7th mode); emotion==null is a no-op (LIVE-05).
- A synthetic mode-transition frame keeps p95 sidecar->client under 50ms — the mode machine adds no measurable latency (PERF-03 floor).
- The felt "mascot feels alive across its modes under a real set on Kaan's Mac" confirmation is explicitly NOT marked complete — it rides the Kaan-action live-drive surface.
</success_criteria>

<output>
Create `.planning/phases/56-performance-live-mascot/56-03-SUMMARY.md` when done.
</output>
</content>
