---
phase: 56-performance-live-mascot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/runtime/test_ttft.py
  - tests/llm/test_thinking_gate.py
  - tests/e2e/test_phase_41_latency_stack_integration.py
  - tests/runtime/test_soak_stability.py
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "Replayed real-shaped reaction traffic keeps TTFTMeter rolling-avg at or below a named budget constant"
    - "The production live GenerateContentConfig (_gen_cfg) passes validate_live_config (positive pin)"
    - "A MEDIUM/HIGH thinking or FLEX-tier live config is rejected by validate_live_config (negative pin — proves the 7s+ regression protection is live)"
    - "A real PlaybackQueue driven under simulated both-mode reaction traffic records zero playback underruns"
  artifacts:
    - path: "tests/runtime/test_ttft.py"
      provides: "TTFT budget-assertion test: replay samples through TTFTMeter, assert rolling_avg_ms() <= LIVE_TTFT_BUDGET_MS"
      contains: "rolling_avg_ms"
    - path: "tests/llm/test_thinking_gate.py"
      provides: "positive+negative pin that the production _gen_cfg passes the gate and a non-MINIMAL/FLEX config is rejected"
      contains: "validate_live_config"
    - path: "tests/runtime/test_soak_stability.py"
      provides: "both-mode reaction-traffic soak scenario asserting result.underruns == 0 via assert_healthy(max_underruns=0)"
      contains: "max_underruns=0"
  key_links:
    - from: "tests/runtime/test_ttft.py"
      to: "runtime/ttft.py::TTFTMeter.rolling_avg_ms"
      via: "replay event_fired->first_chunk samples then assert <= budget"
      pattern: "rolling_avg_ms\\(\\)"
    - from: "tests/llm/test_thinking_gate.py"
      to: "agent/dj_cohost.py::_gen_cfg (the production live config) via thinking_gate.validate_live_config"
      via: "positive pin on the real minimal config + negative pin on a crafted MEDIUM/FLEX config"
      pattern: "validate_live_config\\("
    - from: "tests/runtime/test_soak_stability.py"
      to: "runtime/soak.py::run_soak / SoakCounters.observe_pull against a real PlaybackQueue"
      via: "drive the queue under simulated both-mode traffic, assert zero underruns"
      pattern: "underruns"
---

<objective>
Pin the two server-side performance budgets — TTFT (PERF-01) and playback dropouts (PERF-02) — by EXTENDING the existing Python perf tests. No production code changes: the substrates (`TTFTMeter`, `thinking_gate.validate_live_config`, `soak.SoakCounters`/`run_soak`) already exist and are green. This plan asserts they hold under simulated real-session load.

Purpose: PERF-01's real lever is the boot-time MINIMAL-thinking gate (anything higher adds 7s+ TTFT regression); TTFTMeter itself is telemetry-only now (ack-bank retired — there is NO `should_fire` method, do not re-introduce a runtime gate). PERF-02 reuses the Phase-51 soak underrun counter — the CONTEXT explicitly forbids inventing a new counter. This plan turns both into automated regression floors that run in CI without real hardware; the felt "TTFT instant / zero dropouts under a real set on Kaan's Mac" confirmation is the Kaan-action live-drive carveout and is NOT marked auto-complete here.

Output: an extended `test_ttft.py` (budget assertion over replayed traffic), an extended `test_thinking_gate.py` + `test_phase_41_latency_stack_integration.py` (positive+negative pin on the production live config), and an extended `test_soak_stability.py` (both-mode reaction-traffic soak → zero underruns).

Scope guardrails honored:
- TTFTMeter is telemetry-only — assert `rolling_avg_ms() <= budget`, never call a `should_fire` method (none exists). Pin the real lever (`validate_live_config`) separately.
- No new underrun/RSS counter — reuse `soak.is_underrun` / `SoakCounters.observe_pull` / `assert_healthy` / `run_soak`.
- The budget number is undefined in code (Open Q2 / Assumption A1): introduce a single named `LIVE_TTFT_BUDGET_MS` constant set to the 1500.0 ms sentinel floor (the conservative regression floor RESEARCH specifies), with a comment flagging Kaan to tighten on real hardware. Do NOT hardcode a magic number inline.
- This is EXTEND-AND-PIN — modify only the four test files; do NOT edit `ttft.py`, `soak.py`, `thinking_gate.py`, or `dj_cohost.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-performance-live-mascot/56-PATTERNS.md
@.planning/phases/56-performance-live-mascot/56-RESEARCH.md

<interfaces>
<!-- Extracted from codebase this session — executor uses these directly, no exploration needed. -->

TTFTMeter (src/vibemix/runtime/ttft.py):
  class TTFTMeter(window=8, default_ms=1500.0, time_fn=...)
  .record_event_fired(now: float | None = None) -> None
  .record_first_chunk(now: float | None = None) -> None
  .rolling_avg_ms() -> float        # returns default_ms (1500.0) sentinel when empty
  .samples_count() -> int
  NO should_fire method — telemetry only (ack-bank retired). Window=8, single-threaded asyncio.

thinking_gate (src/vibemix/llm/thinking_gate.py):
  validate_live_config(cfg: GenerateContentConfig) -> None   # raises LiveCoachConfigError
  _ALLOWED_THINKING = frozenset({"MINIMAL"})   # non-MINIMAL thinking_level -> raise
  _DISALLOWED_TIER  = frozenset({"FLEX"})       # service_tier == FLEX -> raise
  Aggregates all violations into one " | "-joined message.
  __all__ = ["LiveCoachConfigError", "validate_live_config"]

Production live config seam:
  src/vibemix/agent/dj_cohost.py:413  self._gen_cfg = types.GenerateContentConfig(thinking_config=ThinkingConfig(thinking_level="minimal"), ...)
  src/vibemix/agent/dj_cohost.py:427  validate_live_config(self._gen_cfg)   # the agent re-runs the gate against the actual per-turn config
  src/vibemix/agent/llm_factory.py:27 gen_cfg = types.GenerateContentConfig(thinking_config=ThinkingConfig(thinking_level="minimal"), ...)

Existing thinking-gate tests (tests/llm/test_thinking_gate.py): pass/fail pairs at :34-147; production pins
  test_llm_factory_passes_with_default_config (:163, mocker.spy on validate_live_config), test_llm_factory_raises_on_bad_thinking_override (:181).

Existing e2e latency scenario (tests/e2e/test_phase_41_latency_stack_integration.py):
  test_agent_validates_live_config (:145) — production-shape _gen_cfg passes; test_thinking_gate_rejects_flex_on_live (:162); test_thinking_gate_rejects_higher_than_minimal_thinking (:181). @pytest.mark.e2e. SDK-boundary mock posture (no cassettes; mock the genai client directly).

soak (src/vibemix/runtime/soak.py):
  is_underrun(chunk, requested, *, had_pending, held=None) -> bool   # all-zero full-length pull == underrun (PlaybackQueue zero-pads, so length is useless)
  class SoakCounters: underruns: int; .observe_pull(...) increments underruns
  class SoakResult: underruns: int; .growth_bytes (delta, not absolute) property
  assert_healthy(result, *, max_growth_bytes: int, max_underruns: int = 0) -> None   # raises on RSS growth OR underruns over limit
  run_soak(duration_s, tick_hz, queue=<real PlaybackQueue>) -> SoakResult

Existing soak tests (tests/runtime/test_soak_stability.py): real PlaybackQueue drive (PlaybackQueue from vibemix.audio.buffers, :29);
  test_short_synthetic_soak_bounded_rss_zero_underruns (:169, @pytest.mark.slow, run_soak(duration_s=1.0, tick_hz=200, queue=pq) then assert_healthy(..., max_underruns=0)).
  3-layer structure: unit classifier / counter / slow soak. The real >=30-min soak stays Kaan-action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the TTFT budget as a telemetry assertion over replayed reaction traffic (PERF-01)</name>
  <read_first>
    - tests/runtime/test_ttft.py (deterministic time_fn injection at :28-37, :98-109 — the clock-driving pattern) — the file being modified
    - src/vibemix/runtime/ttft.py (TTFTMeter public surface :44-95; default_ms=1500.0 sentinel at :54; NO should_fire) — the meter under test
    - .planning/phases/56-performance-live-mascot/56-PATTERNS.md (test_ttft.py section :202-204 + "Telemetry-budget assertion, NOT a runtime gate" shared pattern :264-266) — the exact assertion shape
    - .planning/phases/56-performance-live-mascot/56-RESEARCH.md (Pitfall 1 :249-253; Open Q2 :359-362; Assumption A1 :343-348) — the telemetry-not-gate correction + budget-number guidance
  </read_first>
  <behavior>
    - Define a single named module-level constant `LIVE_TTFT_BUDGET_MS = 1500.0` in the test (with a comment citing it as the conservative regression floor = TTFTMeter default sentinel, flagged for Kaan to tighten on real hardware per Open Q2).
    - Replay a sequence of realistic event_fired->first_chunk pairs (e.g. a mix of ~400-1200 ms TTFT samples representing real reaction traffic, more than one window's worth to exercise the rolling deque) through a `TTFTMeter` using the existing deterministic `time_fn`/clock-driving pattern, then assert `meter.rolling_avg_ms() <= LIVE_TTFT_BUDGET_MS`.
    - Assert `meter.samples_count()` reflects the replayed samples (proves the meter actually recorded them, not just returned the empty sentinel).
    - A negative-control assertion: a meter that has only seen samples whose avg exceeds the budget fails the `<= budget` check (prove the assertion has teeth — express as a separate small case asserting `rolling_avg_ms() > LIVE_TTFT_BUDGET_MS` for an over-budget replay).
    - Do NOT call any `should_fire` method (it does not exist).
  </behavior>
  <action>
    In tests/runtime/test_ttft.py add `LIVE_TTFT_BUDGET_MS = 1500.0` as a named module constant with a comment: conservative regression floor = TTFTMeter default sentinel; flagged Open Q2 for Kaan to tighten on real hardware. Add a test that constructs a `TTFTMeter` with the existing injected-clock pattern (mirror the `state[0]`/`time_fn` driving used at :28-37,:98-109), replays a realistic sequence of `record_event_fired`/`record_first_chunk` pairs covering >8 samples (so the rolling window of 8 is meaningful) with per-sample TTFT in a realistic in-budget range, then asserts `meter.rolling_avg_ms() <= LIVE_TTFT_BUDGET_MS` and `meter.samples_count() >= 8`. Add a second small test that replays an over-budget sequence and asserts `meter.rolling_avg_ms() > LIVE_TTFT_BUDGET_MS` (negative control proving the floor has teeth). Reuse the file's existing clock helper rather than reading wall-clock. Reference PERF-01 in a test docstring. Do not modify ttft.py.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_ttft.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_ttft.py -x -q` passes including the new budget + negative-control tests.
    - `grep -n "LIVE_TTFT_BUDGET_MS" tests/runtime/test_ttft.py` shows the named constant (no inline magic budget number).
    - `grep -c "should_fire" tests/runtime/test_ttft.py` returns 0 (no resurrected runtime gate).
    - The in-budget replay asserts `rolling_avg_ms() <= LIVE_TTFT_BUDGET_MS` AND `samples_count() >= 8`; the over-budget control asserts `> LIVE_TTFT_BUDGET_MS`.
  </acceptance_criteria>
  <done>TTFT is pinned as a telemetry budget over replayed traffic with a named floor constant and a negative control; no should_fire gate introduced; ttft.py unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Positive + negative pin the production live config against the thinking-budget gate (PERF-01 lever)</name>
  <read_first>
    - tests/llm/test_thinking_gate.py (pass/fail case pairs :34-147; production pins test_llm_factory_passes_with_default_config :163, test_llm_factory_raises_on_bad_thinking_override :181) — the file being modified
    - tests/e2e/test_phase_41_latency_stack_integration.py (test_agent_validates_live_config :145; test_thinking_gate_rejects_flex_on_live :162; test_thinking_gate_rejects_higher_than_minimal_thinking :181; @pytest.mark.e2e) — the e2e leg to extend
    - src/vibemix/llm/thinking_gate.py (validate_live_config :73-107; _ALLOWED_THINKING :40; _DISALLOWED_TIER :43) — the gate under test
    - src/vibemix/agent/dj_cohost.py (:413 _gen_cfg construction with thinking_level="minimal"; :427 validate_live_config(self._gen_cfg)) — the production live config seam being pinned
    - .planning/phases/56-performance-live-mascot/56-PATTERNS.md (test_thinking_gate.py section :208-210; thinking_gate.py section :184-198) — the positive+negative pin shape
  </read_first>
  <behavior>
    - Positive pin: a GenerateContentConfig shaped exactly like the production `_gen_cfg` (thinking_level="minimal", no FLEX tier) passes `validate_live_config` without raising.
    - Negative pin (thinking): a config with thinking_level MEDIUM (and one with HIGH) raises `LiveCoachConfigError` with `thinking_level` in the message — proving the 7s+ regression protection is live.
    - Negative pin (tier): a config with service_tier FLEX raises `LiveCoachConfigError`.
    - The e2e latency leg asserts the same production-shape config passes end-to-end and that thinking=MINIMAL holds across the latency stack (extend the existing test_agent_validates_live_config scenario, not a parallel one).
    - Existing thinking-gate tests stay green (no regression to the pass/fail pairs).
  </behavior>
  <action>
    In tests/llm/test_thinking_gate.py add (if not already covered by the factory pins) an explicit assertion that a production-shape `GenerateContentConfig(thinking_config=ThinkingConfig(thinking_level="minimal"))` passes `validate_live_config` (positive pin) AND that MEDIUM/HIGH thinking + FLEX tier each raise `LiveCoachConfigError` — reuse the existing pass/fail case-pair style at :34-147 and the `ThinkingConfig`/`ThinkingLevel` imports already used at :181+. Reference PERF-01 in the docstring. In tests/e2e/test_phase_41_latency_stack_integration.py, ensure the PERF-01 leg under `test_agent_validates_live_config` (or a sibling tagged @pytest.mark.e2e) asserts the production `_gen_cfg`-shape config passes the gate (positive) and a non-MINIMAL config is rejected (negative), keeping the SDK-boundary mock posture (no cassettes; mock the genai client directly). Do not modify thinking_gate.py or dj_cohost.py.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/llm/test_thinking_gate.py -x -q && PYTHONPATH=src python3 -m pytest tests/e2e/test_phase_41_latency_stack_integration.py -m e2e -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/llm/test_thinking_gate.py -x -q` passes (positive + negative pins green).
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/e2e/test_phase_41_latency_stack_integration.py -m e2e -x -q` passes.
    - At least one test asserts the production-shape minimal config passes `validate_live_config` (positive) and at least one asserts a MEDIUM/HIGH/FLEX config raises `LiveCoachConfigError` (negative).
    - `grep -c "validate_live_config" tests/llm/test_thinking_gate.py` is >= 1.
  </acceptance_criteria>
  <done>The production live config is positively pinned against the gate and a non-MINIMAL/FLEX config is proven rejected, end-to-end; production code unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Assert zero playback underruns under simulated both-mode reaction traffic (PERF-02)</name>
  <read_first>
    - tests/runtime/test_soak_stability.py (real PlaybackQueue drive :29-37; test_short_synthetic_soak_bounded_rss_zero_underruns :169 with run_soak(duration_s=1.0, tick_hz=200, queue=pq) + assert_healthy(..., max_underruns=0); 3-layer unit/counter/slow structure) — the file being modified
    - src/vibemix/runtime/soak.py (is_underrun :41; SoakCounters.observe_pull :78; assert_healthy :125 with max_underruns=0; run_soak :177; SoakResult.growth_bytes :121) — the reused substrate
    - src/vibemix/audio/buffers.py (PlaybackQueue — the real queue, zero-pads empty pulls) — the component under load
    - .planning/phases/56-performance-live-mascot/56-PATTERNS.md (soak.py section :168-180; test_soak_stability.py section :214-216; "Observe-don't-instrument hot paths" shared pattern :268-270) — the reuse-don't-invent discipline
  </read_first>
  <behavior>
    - A new soak scenario drives a REAL `PlaybackQueue` under simulated both-mode reaction traffic (party + feedback both pushing playback chunks into the queue at a steady tick) and asserts `result.underruns == 0` via `assert_healthy(result, max_growth_bytes=..., max_underruns=0)`.
    - The scenario reuses `run_soak` / `SoakCounters` / `is_underrun` — no new counter is introduced.
    - Keep the existing unit/slow/deselect-guard 3-layer structure intact; the new both-mode scenario carries the appropriate marker (slow/integration) so the default fast run still skips it.
    - The real >=30-min soak remains Kaan-action (documented, not run in CI).
  </behavior>
  <action>
    In tests/runtime/test_soak_stability.py add a scenario that constructs a real `PlaybackQueue` (mirror the :169 setup), drives it under simulated both-mode reaction traffic — model both modes generating playback by feeding the queue a steady stream of full chunks at the production cadence so the pull side never starves under nominal load (use `run_soak` with an appropriately scaled `duration_s`/`tick_hz`, or push both-mode chunks then drain) — and asserts `result.underruns == 0` through `assert_healthy(result, max_growth_bytes=<existing-style limit>, max_underruns=0)`. Reuse `SoakCounters.observe_pull` / `is_underrun` exactly as the existing tests do; do NOT add a new counter. Tag the scenario with the existing slow/deselect marker so the default `-q` run skips it. Reference PERF-02 + the "both modes generating traffic" CONTEXT requirement in the docstring. Do not modify soak.py or buffers.py.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_soak_stability.py -m slow -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_soak_stability.py -m slow -x -q` passes including the new both-mode scenario.
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_soak_stability.py -q` (default fast run) still passes and the new slow scenario is deselected (not run by default).
    - `grep -c "max_underruns=0" tests/runtime/test_soak_stability.py` is >= 1 (the new scenario asserts zero underruns via assert_healthy).
    - The new scenario imports nothing beyond `soak` + `PlaybackQueue` (no new counter — `grep` confirms reuse of `run_soak`/`SoakCounters`/`assert_healthy`).
  </acceptance_criteria>
  <done>A real PlaybackQueue under simulated both-mode traffic records zero underruns via the reused soak counter; the slow marker keeps the fast run clean; soak.py unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini stream -> TTFTMeter / live config | The live coach's GenerateContentConfig governs latency; a mis-set thinking budget silently degrades live UX (7s+ TTFT). |
| PlaybackQueue pull boundary | The audio hot path; an empty/partial pull under load is a dropout the user hears. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04 | Tampering | live GenerateContentConfig thinking budget | mitigate | `validate_live_config` boot-time gate rejects non-MINIMAL thinking / FLEX tier; this plan adds a positive+negative regression pin so a future config change that re-introduces the 7s+ TTFT regression fails CI. |
| T-56-05 | Denial of Service | PlaybackQueue underrun under load | mitigate | Reuse `soak.is_underrun` + zero-underrun assertion under simulated both-mode traffic; a regression that starves the queue fails the soak test. Hot path is observed, never instrumented. |
| T-56-06 | Information Disclosure | TTFT / soak telemetry | accept | Telemetry carries timing/RSS numbers only, no secrets; assertions run locally in CI. No exposure surface. |
</threat_model>

<verification>
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_ttft.py tests/llm/test_thinking_gate.py -x -q` green.
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/e2e/test_phase_41_latency_stack_integration.py -m e2e -x -q` green.
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/runtime/test_soak_stability.py -m slow -x -q` green.
- Default fast suite (`pytest -q`) unaffected — new slow/e2e scenarios deselected.
- No production code modified (ttft.py / soak.py / thinking_gate.py / dj_cohost.py / buffers.py untouched — verify with git diff that only the four test files changed).
</verification>

<success_criteria>
- Replayed reaction traffic keeps TTFTMeter rolling-avg at or below the named LIVE_TTFT_BUDGET_MS floor; a negative control proves the floor has teeth (PERF-01 telemetry budget).
- The production live config passes the thinking-budget gate (positive) and a MEDIUM/HIGH/FLEX config is rejected (negative) — the 7s+ TTFT regression protection is provably live (PERF-01 lever).
- A real PlaybackQueue under simulated both-mode reaction traffic records zero underruns via the reused soak counter (PERF-02).
- The felt "TTFT instant / zero dropouts under a real set" confirmation is explicitly NOT marked complete here — it rides the Kaan-action live-drive surface.
</success_criteria>

<output>
Create `.planning/phases/56-performance-live-mascot/56-02-SUMMARY.md` when done.
</output>
