---
phase: 14-cdj-whisper-v5-migration-polish
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - tauri/ui/src/wizard/components/window-picker.ts
  - tauri/ui/src/wizard/components/dropdown-device.ts
  - tauri/ui/src/wizard/components/controller-probe.ts
  - tauri/ui/src/wizard/components/audio-test-button.ts
  - tauri/ui/src/wizard/components/blackhole-banner.ts
  - tauri/ui/src/wizard/components/status-bar.ts
  - tauri/ui/src/wizard/components/permissions-card.ts
  - tauri/ui/src/wizard/components/step-indicator.ts
  - tauri/ui/src/wizard/components/primary-panel.ts
  - tauri/ui/src/wizard/components/button.ts
  - tauri/ui/src/wizard/components/mascot-corner.ts
  - tauri/ui/src/wizard/smoke-test.ts
  - tauri/ui/src/wizard/step1-permissions.ts
  - tauri/ui/src/wizard/controllers/ddj-flx4.svg.ts
  - tauri/ui/src/wizard/icons/speaker.svg.ts
  - tauri/ui/src/wizard/icons/shield.svg.ts
  - tauri/ui/src/wizard/router.ts
  - tauri/ui/index.html
  - tauri/ui/tests/wizard.tokens.test.ts
  - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
autonomous: false
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
tags:
  - ui
  - migration
  - wizard

must_haves:
  truths:
    - "Every wizard consumer reads v5 primitives directly (--glass-*, --silk-*, --amber*, --glow-*, --type-*) — zero legacy shim refs (--phosphor*, --brushed-*, --bezel-*, --panel*, --groove, --ink*, --col-mascot) in tauri/ui/src/wizard/"
    - "PrimaryPanel() has <div class='border-anim'> as its first child (before .vmx-glass-streak)"
    - "wizard-grid collapses to single-column — mascot-corner.ts deleted, index.html removes <aside id='mascot-corner'>, router.ts removes mascotMount refs"
    - "DSEG7 48px LCD becomes JetBrains Mono 48px tabular-nums on controller-probe; Workbench labels become Saira var(--type-display) wdth 85 wght 600; DM Mono body becomes Saira var(--type-body) wdth 100 wght 400-500"
    - "vitest wizard.tokens.test.ts no longer needs describe.skip — passes green"
    - "gsd-ui-checker zero findings on wizard surface AND gsd-ui-auditor 3 audits green (20/80, no-faux-3D-bevel, typography pairing) — recorded in 14-POLISH-LOG.md with cycle count"
  artifacts:
    - path: "tauri/ui/src/wizard/components/primary-panel.ts"
      provides: "PrimaryPanel() factory with border-anim first child + glass-streak utility — sole entry point for wizard glass panel"
      contains: "createElement('div').className = 'border-anim'"
    - path: "tauri/ui/src/wizard/components/window-picker.ts"
      provides: "Window list with v5 glass-2/glass-edge/silk/amber tokens — the 37-ref migration recipe"
      contains: "var(--glass-2), var(--glass-edge), var(--rad-md), var(--type-body), var(--type-mono), var(--silk-65)"
    - path: "tauri/ui/src/wizard/components/controller-probe.ts"
      provides: "DSEG7 → JetBrains Mono tabular-nums + Workbench → Saira var(--type-display) wdth 85"
      contains: "var(--type-mono), font-variant-numeric: tabular-nums, var(--amber), var(--glow-soft)"
    - path: "tauri/ui/index.html"
      provides: "Single-column wizard-grid — <aside id='mascot-corner'> removed"
      contains: "<section id=\"wizard-primary\" class=\"primary-panel-mount\"></section>"
    - path: "tauri/ui/src/wizard/components/mascot-corner.ts"
      provides: "DELETED file — 256×256 reserved corner gone in v5"
  key_links:
    - from: "tauri/ui/src/wizard/components/primary-panel.ts"
      to: ".border-anim utility in tokens.css:302"
      via: "createElement('div').className='border-anim'"
      pattern: "border-anim.*first.*child|createElement\\(.div.\\).className.*border-anim"
    - from: "tauri/ui/src/wizard/components/controller-probe.ts"
      to: "var(--type-mono) JetBrains Mono"
      via: "font-family: var(--type-mono); font-variant-numeric: tabular-nums"
      pattern: "font-family:\\s*var\\(--type-mono\\)"
    - from: "tauri/ui/src/wizard/router.ts"
      to: "deleted mascot-corner mount"
      via: "removed mascotMount references at router.ts:202 and :344"
      pattern: "mascotMount"
---

<objective>
Migrate every wizard surface consumer (15 files, ~225 legacy-token refs per RESEARCH inventory) from shim aliases to v5 primitives, insert the animated border on PrimaryPanel(), collapse the wizard grid to single-column by deleting mascot-corner, and run the ui-checker → fix → ui-auditor critique loop (max 3 cycles per CONTEXT Area 3) until the surface passes.

Purpose: Wave 1 is the largest token surface and validates the migration recipe before tackling per-frame meters. Wizard first per CONTEXT Area 1 (structural HTML only, no streaming render loop, easier baseline capture). The mascot-corner deletion frees layout space and aligns with Phase 13 (mascot = overlay window, not embedded — see HANDOFF watchout).

Output: 15 wizard files migrated to v5, 1 file deleted (mascot-corner), index.html + router.ts updated for single-column, vitest wizard.tokens.test.ts unskipped + green, polish log rows populated for wizard cycles 1-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Commit policy (plan-checker W-3 resolution — 20 file mutations across this wave):**
Commit per-component or per logical-group, NOT as a single atomic mega-commit. If context pressure hits mid-task, commit the partial diff and continue — the migration is mechanical token substitution, so per-component commits are safe and don't block the next task. Suggested grouping for Task 14-02-01: (a) primary-panel + button + step-indicator + index.html structural, (b) status-bar + permissions-card + blackhole-banner, (c) mascot-corner deletion + router.ts/smoke-test/step1-permissions cleanups + icon SVGs. Task 14-02-02 can commit the 5 heavy-ref files as one or split into two.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md
@mocks/vibemix-direction-final.html
@tauri/ui/src/tokens.css
@tauri/ui/src/session/components/panel.ts
@tauri/ui/src/settings/SettingsDrawer.ts

<interfaces>
<!-- GOLD-STANDARD ANALOGS (already-correct v5 — copy patterns from these) -->

tauri/ui/src/session/components/panel.ts (156 lines — canonical glass-panel anatomy):
- background: var(--glass-2)
- backdrop-filter: var(--blur-glass-light) + -webkit- pair
- border: 1px solid var(--glass-edge)
- border-radius: var(--rad-md)
- box-shadow stack: inset 0 1px 0 var(--glass-top), inset 0 -1px 0 rgba(0,0,0,0.45), 0 16px 36px rgba(0,0,0,0.55)
- Header uses font-variation-settings: "wdth" 85, "wght" 600
- Engraved labels add text-shadow: 0 1px 0 rgba(0, 0, 0, 0.7)

tauri/ui/src/settings/SettingsDrawer.ts:62-237 (the CSS block — canonical drawer + button vocabulary):
- Uses Saira variable axes: font-variation-settings: "wdth" 85, "wght" 700
- Uses every silk/amber alpha token correctly
- :focus-visible + hover transition vocabulary modelled

tauri/ui/src/wizard/components/primary-panel.ts (already mostly v5-compliant — line numbers per PATTERNS.md):
- Lines 18–69 read --glass-2 / --blur-glass-light / --glass-edge / --glass-top / --rad-md / --type-display / --silk / --amber* / --sp-3 / --sp-5
- Legacy residue is only in jsdoc lines 4–8 + needs border-anim insertion at lines 73–82

<!-- PER-TOKEN MIGRATION MAP (PATTERNS.md table — source of truth) -->

| Legacy | v5 replacement |
|--------|----------------|
| var(--panel-lift) | var(--glass-2) |
| var(--panel) (gradient stop) | var(--glass-1) |
| var(--panel-deep) | var(--glass-3) recessed OR var(--void-2) button-pressed |
| var(--bezel-1) | var(--glass-edge) |
| var(--bezel-2) / var(--bezel-3) | DELETE (no 3D bevels in v5) |
| var(--brushed-hi) | var(--glass-top) inside inset 0 1px 0 |
| var(--brushed-lo) | inline rgba(0,0,0,0.45) |
| var(--ink) | var(--silk) |
| var(--ink-dim) | var(--silk-65) |
| var(--ink-deep) | var(--silk-40) |
| var(--ink-engraved) | var(--silk-22) |
| var(--phosphor) | var(--amber) |
| var(--phosphor-warm) | var(--amber-deep) |
| var(--phosphor-dim) | var(--amber-22) |
| var(--phosphor-soft) | var(--amber-22) for border; linear-gradient(180deg, rgba(255,138,61,0.09), rgba(255,138,61,0.025)) for fills |
| var(--phosphor-glow) | var(--glow-soft) |
| var(--phosphor-halo) | var(--glow-soft) outer + var(--amber-22) 1px border |
| --sp-xs | --sp-1 |
| --sp-sm | --sp-2 |
| --sp-md | --sp-4 |
| --sp-lg | --sp-5 |
| --sp-xl (32) | inline 32px /* mock-verbatim */ OR --sp-5 / --sp-6 at author's call |

<!-- FORBIDDEN FONT-FAMILY SWAPS -->

| Legacy | v5 swap |
|--------|---------|
| "Workbench", "Courier New", monospace | var(--type-display); font-variation-settings: "wdth" 85, "wght" 600-700; UPPERCASE labels add text-shadow: 0 1px 0 rgba(0,0,0,0.7) |
| "DM Mono", monospace (body) | var(--type-body); font-variation-settings: "wdth" 100, "wght" 400-500 |
| "DM Mono", monospace (numeric small) | var(--type-mono); font-variant-numeric: tabular-nums; font-feature-settings: "tnum" |
| "DSEG7", "DM Mono", monospace | var(--type-mono); font-variant-numeric: tabular-nums; font-feature-settings: "tnum" |

<!-- BORDER-ANIM INSERTION CONTRACT (PATTERNS.md Shared Pattern 1) -->

Source: tokens.css:302–331 utility (already in file)
Parent invariants: position: relative + overflow: hidden
Border-anim itself: z-index 4
Component content: z-index 1+
Insert as FIRST child via:
  const borderAnim = document.createElement("div");
  borderAnim.className = "border-anim";  // wizard uses default 22s forward
  borderAnim.setAttribute("aria-hidden", "true");
  root.append(borderAnim);

<!-- ACCENT RESERVATION LIST (UI-SPEC Color §20% — gate amber usage) -->

Amber may appear ONLY in:
1. Live-dot pulse · 2. AI-talking indicator · 3. Brand bullet · 4. Active mood pill · 5. Active button state (internal backlight, no external halo) · 6. Meter fills · 7. Drop countdown digits · 8. AI-line left border · 9. AI-line .who label · 10. Animated border sweep · 11. Section index numerals · 12. Crash banner restart · 13. Mascot eyes · 14. Tray live-dot · 15. Focus ring · 16. Hero word accent

Wizard maps to: focus ring (15), active button state (5 — button.ts on state), animated border sweep (10 — added by this plan), audio-test-button rings (similar to mood-pill active state — use 5), controller-probe LCD digits (6 — same as countdown semantically), step-indicator active dot (similar to live-dot — use 1's vocabulary). Anywhere else amber appears in wizard chrome, refactor to silk-* alphas.

<!-- COPY PURGE (UI-SPEC Copywriting Contract) -->

Forbidden in chrome strings (NOT comments) in tauri/ui/src/wizard/:
brushed, anodised, phosphor (lowercase or uppercase), retro-futurist, knob/fader physics, knurled, tactile (manual review only)

Known purge sites from RESEARCH:
- wizard/components/mascot-corner.ts:47 → "AVERY · arriving phase 13" (file deleted entirely in this plan)
- wizard/components/primary-panel.ts:5 jsdoc → "brushed-metal vertical streak via ::before" — rewrite to "shared glass-fingerprint streak"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-02-01: Cycle 1 — Migrate wizard panel + structural files (primary-panel, button, step-indicator, mascot-corner deletion, index.html, router.ts, smoke-test, step1-permissions, ddj-flx4.svg, speaker/shield icons)</name>
  <files>tauri/ui/src/wizard/components/primary-panel.ts, tauri/ui/src/wizard/components/button.ts, tauri/ui/src/wizard/components/step-indicator.ts, tauri/ui/src/wizard/components/permissions-card.ts, tauri/ui/src/wizard/components/blackhole-banner.ts, tauri/ui/src/wizard/components/mascot-corner.ts, tauri/ui/src/wizard/smoke-test.ts, tauri/ui/src/wizard/step1-permissions.ts, tauri/ui/src/wizard/controllers/ddj-flx4.svg.ts, tauri/ui/src/wizard/icons/speaker.svg.ts, tauri/ui/src/wizard/icons/shield.svg.ts, tauri/ui/src/wizard/router.ts, tauri/ui/index.html</files>
  <read_first>
    - tauri/ui/src/wizard/components/primary-panel.ts (gold-standard for this surface — mostly v5 already)
    - tauri/ui/src/session/components/panel.ts (canonical glass-panel anatomy across the whole project)
    - tauri/ui/src/settings/SettingsDrawer.ts lines 198–222 (.vmx-settings-drawer__btn — button anatomy analog for button.ts)
    - tauri/ui/src/crash-banner.ts block in tokens.css:466–530 (amber-glow-against-glass — analog for blackhole-banner pulse + step-indicator active state)
    - tauri/ui/src/wizard/router.ts (current — lines 202 and 344 contain mascotMount references to delete; line numbers per PATTERNS.md)
    - tauri/ui/index.html (current — lines 29-32 contain the two-column wizard-grid markup)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Surface 1 — Wizard (Wave 1)" (the migration recipe + before/after pairs)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Surface 1 — Wizard" (component-by-component contract)
  </read_first>
  <action>
For each file in <files>:

**primary-panel.ts** — Insert .border-anim as FIRST child of root inside PrimaryPanel() factory (before the .vmx-glass-streak append), per PATTERNS.md Shared Pattern 1 + the before/after at PATTERNS.md:102–132. Rewrite the jsdoc lines 4–8 to remove "brushed-metal" wording — replace with "shared glass-fingerprint streak — unifies wizard surfaces with the session deck panels".

**button.ts** — Adopt mock §02 .btn anatomy (idle/hover/on/pressed). Idle: var(--glass-2) + var(--glass-edge) 1px border + inset 0 1px 0 var(--glass-top) + inset 0 -1px 0 rgba(0,0,0,0.5). Hover: linear-gradient(180deg, rgba(255,138,61,0.09), rgba(255,138,61,0.025)) /* mock-verbatim */. On (.btn.on): inset 0 0 14px var(--amber-22) + 1px var(--amber-40) border + var(--amber) text + text-shadow: 0 0 4px var(--amber-65). Pressed: var(--void-2) background. DROP all --bezel-* and --brushed-* references.

**step-indicator.ts** — Step dots use var(--silk-22) (pending) / var(--amber) (active, with var(--glow-soft) text-shadow) / var(--silk-65) (passed). Connecting line is 1px var(--glass-edge). Active dot pulse uses liveBlink keyframe analog from crash-banner block (1.4s ease-in-out infinite).

**permissions-card.ts** — Migrate `--ink*` → `--silk-*`, `--panel*` → `--glass-1/2`, `--bezel-*` deleted. Keep OS-aware content; only retone CSS.

**blackhole-banner.ts** — Drop --phosphor-halo entirely. Adopt --glow-soft outer + 1px var(--amber-40) border + inset 0 0 14px var(--amber-22). Phosphor → amber.

**mascot-corner.ts** — DELETE the file entirely via `rm tauri/ui/src/wizard/components/mascot-corner.ts`. Then grep `tauri/ui/src/wizard/` for any `import.*mascot-corner` references and remove the import lines + any mounting/render calls.

**smoke-test.ts** — Heading "WIZARD COMPLETE" stays uppercase but font-family becomes var(--type-display) with font-variation-settings: "wdth" 85, "wght" 700 + text-shadow: 0 1px 0 rgba(0,0,0,0.7). --phosphor → --amber; success badge pulse uses --glow-soft to --glow-strong.

**step1-permissions.ts** — Same swap table; 2 --phosphor* refs → --amber + --glow-soft.

**ddj-flx4.svg.ts** — 2 --phosphor* refs in SVG fill → --amber.

**speaker.svg.ts** + **shield.svg.ts** — 1 --ink* each → --silk.

**router.ts** — Remove mascotMount declaration + the two references at lines 202 and 344 (per PATTERNS.md) — the import for mascot-corner is also removed by mascot-corner.ts deletion.

**index.html** — Change lines 29-32 per PATTERNS.md:296–311:
  BEFORE: `<div class="wizard-grid"><section id="wizard-primary" ...></section><aside id="mascot-corner"></aside></div>`
  AFTER:  `<div class="wizard-grid"><section id="wizard-primary" ...></section></div>`

After all edits, run `bash scripts/check_v5_migration.sh --surface=wizard` and verify ref count dropped substantially (start ~225 → target after task: <30, the remaining heavy-ref files are W2 of this plan).
  </action>
  <verify>
    <automated>cd tauri/ui && npm run check:ipc 2>/dev/null && cd ../.. && bash scripts/check_v5_migration.sh --surface=wizard --warn-only | tee /tmp/wizard-cycle1.log && test ! -e tauri/ui/src/wizard/components/mascot-corner.ts && ! grep -q "mascot-corner" tauri/ui/index.html && ! grep -q "mascotMount" tauri/ui/src/wizard/router.ts && grep -F "border-anim" tauri/ui/src/wizard/components/primary-panel.ts</automated>
  </verify>
  <done>Wizard structural files migrated. mascot-corner.ts deleted. index.html single-column. PrimaryPanel has border-anim first child. button.ts adopts v5 .btn anatomy. permissions/blackhole-banner/step-indicator/smoke-test/step1-permissions/icons all read v5 tokens. router.ts has zero mascotMount refs. `check_v5_migration.sh --surface=wizard` shows substantial drop from baseline 225.</done>
</task>

<task type="auto">
  <name>Task 14-02-02: Cycle 1 — Migrate the four heavy-ref wizard components (window-picker 37, dropdown-device 31, controller-probe 31, audio-test-button 30, status-bar 17) + unskip wizard.tokens.test.ts</name>
  <files>tauri/ui/src/wizard/components/window-picker.ts, tauri/ui/src/wizard/components/dropdown-device.ts, tauri/ui/src/wizard/components/controller-probe.ts, tauri/ui/src/wizard/components/audio-test-button.ts, tauri/ui/src/wizard/components/status-bar.ts, tauri/ui/tests/wizard.tokens.test.ts</files>
  <read_first>
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Before/after: wizard/components/window-picker.ts:30–110" + "Before/after: forbidden-font 'Workbench' swap (controller-probe.ts:79)" + "Before/after: DSEG7 → JetBrains Mono tabular-nums (controller-probe.ts:112–133)"
    - tauri/ui/src/session/components/picker.ts (analog for dropdown-device pattern)
    - tauri/ui/src/session/components/meter.ts (analog for audio-test-button amber pulse rings)
    - tauri/ui/src/session/components/timecode.ts (analog for controller-probe JetBrains Mono tabular-nums LCD)
    - tauri/ui/src/session/components/status-bar.ts (analog for wizard/components/status-bar.ts)
    - tauri/ui/src/wizard/components/window-picker.ts (current — read for the existing list-row/hover/selected patterns)
    - tauri/ui/src/wizard/components/dropdown-device.ts (current)
    - tauri/ui/src/wizard/components/controller-probe.ts (current — verify DSEG7 48px LCD lives at the line range PATTERNS.md cites)
    - tauri/ui/src/wizard/components/audio-test-button.ts (current — 4-ring concentric pulse animation)
    - tauri/ui/src/wizard/components/status-bar.ts (current)
  </read_first>
  <action>
**window-picker.ts** — Apply the before/after recipe at PATTERNS.md:161–231 verbatim. All 37 legacy refs go: `--panel-lift` / `--panel` linear-gradient → `var(--glass-2)` + `backdrop-filter: var(--blur-glass-light)` + `-webkit-` pair; `--bezel-1` → `var(--glass-edge)`; `--panel-deep` (thumb) → `var(--glass-3)`; `--ink` → `var(--silk)`; `--ink-dim` → `var(--silk-65)`; font-family "DM Mono" (app title 14px) → `var(--type-body)` + `font-variation-settings: "wdth" 100, "wght" 500`; font-family "DM Mono" (title 11px) → `var(--type-mono)`. Padding `--sp-md` → `--sp-4`; gap `--sp-md` → `--sp-4`. List-row hover background → `var(--silk-12)`; selected row → `var(--glass-2)` + `var(--amber)` text + `var(--glow-faint)`.

**dropdown-device.ts** — Same swap table applied to all 31 refs. AUTO pill keeps amber styling per UI-SPEC §Color accent-reservation 5 (active button state). Hover background `--phosphor-soft` → `linear-gradient(180deg, rgba(255,138,61,0.09) 0%, rgba(255,138,61,0.025) 100%) /* mock-verbatim */`. max-height: 240px scroll preserved.

**controller-probe.ts** — Apply before/after at PATTERNS.md:235–292:
- `.cmp-ctrl-probe__connected` Workbench label → `var(--type-display)` + `"wdth" 85, "wght" 600` + `letter-spacing: 0.22em` (was 0.32em — mock-verbatim) + `color: var(--led-ok)` + engraved text-shadow.
- `.cmp-ctrl-probe__lcd` DSEG7 48px → `var(--type-mono)` + `font-variant-numeric: tabular-nums` + `font-feature-settings: "tnum"` + `color: var(--amber)` + `text-shadow: var(--glow-soft)`.
- 4 expanding rings recolored amber: outer → inner = `var(--amber-22)` → `var(--amber-40)` → `var(--amber-65)`.
- All 7 `--ink*` refs migrated per token table.

**audio-test-button.ts** — 4-ring concentric pulse animation stays (motion-rings-audio 1500ms). Ring colors `--amber-22 → --amber-40 → --amber-65` outward. DSEG7 readout → JetBrains Mono tabular-nums with the same swap pattern as controller-probe.

**status-bar.ts** — Migrate 4 `--phosphor*` LED pulses → `--amber` with `var(--glow-soft)` for connecting state. The "connecting" pulse uses `liveBlink 1.4s ease-in-out infinite` keyframe analog from crash-banner block. 6 `--ink*` refs → `--silk-*` per table.

**wizard.tokens.test.ts** — Remove the `describe.skip(...)` wrapper. The spec now imports PrimaryPanel + WindowPicker + ControllerProbe + DropdownDevice and asserts containsLegacyToken(rendered) is false. Run the spec — it MUST pass green. If it fails, identify the remaining legacy refs and fix in this same task before committing.

After all edits, run `bash scripts/check_v5_migration.sh --surface=wizard --strict`. Strict mode must pass (zero legacy refs in wizard surface) — that's the cycle 1 acceptance bar.
  </action>
  <verify>
    <automated>cd tauri/ui && npm run test -- wizard.tokens.test.ts --run --reporter=dot && cd ../.. && bash scripts/check_v5_migration.sh --surface=wizard --strict && bash scripts/check_v5_fonts.sh --surface=wizard --strict && cd tauri/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Five heavy-ref wizard components migrated. wizard.tokens.test.ts passes green (no skip). `check_v5_migration.sh --surface=wizard --strict` exits 0. `check_v5_fonts.sh --surface=wizard --strict` exits 0 (zero Workbench / DM Mono / DSEG7 / Caveat refs in wizard). `npm run build` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 14-02-03: Critique loop cycle 1 — ui-checker + ui-auditor + Kaan side-by-side screenshot</name>
  <what-built>Wizard surface fully migrated to v5 primitives (15 files migrated, 1 deleted). Animated border on PrimaryPanel. Single-column wizard-grid. All legacy tokens absent. All forbidden fonts absent. vitest green.</what-built>
  <how-to-verify>
    1. Kaan-side: `cd tauri/ui && npm run tauri dev` — open the wizard at boot
    2. Step through the four wizard steps in dev mode (use `__vibemixDev.advanceTo(step)` if needed)
    3. Run `Skill(skill="gsd-ui-checker", args="14 --surface=wizard")` — attach output reference to `14-POLISH-LOG.md` row "wizard | 1 | <ref> | — | — | 🟡"
    4. Run `Skill(skill="gsd-ui-auditor", args="14 --surface=wizard")` — three audits (20/80 dominance, no-faux-3D-bevel, typography pairing). Attach output reference.
    5. Screenshot wizard step 1 (permissions card) AND wizard step 4 (smoke-test) at native resolution; place side-by-side with `mocks/vibemix-direction-final.html` (use mock §02 fragment as the closest comparison — the mock is a session/spec mock, not a wizard mock; the comparison is for glass-anatomy + amber accent + typography parity, NOT layout parity).
    6. Attach screenshots to `14-POLISH-LOG.md` "Side-by-Side Screenshots" table row "wizard"
    7. If ui-checker has findings: state which ones; orchestrator may dispatch a cycle 2 follow-up task (auto-generated). If both audits are green AND zero ui-checker findings: cycle 1 closes wizard surface. If cycle 3 still has unresolved findings: log as polish debt and escalate.
  </how-to-verify>
  <resume-signal>Type "wizard cycle 1 green" to mark the surface complete, OR "wizard cycle 2 needed: <findings>" to spawn a cycle-2 follow-up task</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview render → desktop composition | Wizard renders inside Tauri webview; no new capability surface added |

## STRIDE Threat Register (security_enforcement = ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Capability allowlist drift (Elevation) | Tauri capability config | accept | Wizard migration is presentation-only; no new Tauri invoke commands; allowlist regression check is Phase 11/12 carryover, not re-run here |
| T-14-02-02 | Information disclosure | Wizard `WindowInfo.title` rendered in window-picker | accept | Carryover from Phase 11 W4 T-11-W4-06 — title crosses WS bus only, never logged; this plan retones the visual but does NOT alter the privacy contract |
</threat_model>

<verification>
- `bash scripts/check_v5_migration.sh --surface=wizard --strict` exits 0
- `bash scripts/check_v5_fonts.sh --surface=wizard --strict` exits 0
- `cd tauri/ui && npm run test -- wizard.tokens.test.ts --run` exits 0 (no skip)
- `cd tauri/ui && npm run build` exits 0
- `tauri/ui/src/wizard/components/mascot-corner.ts` does NOT exist
- `tauri/ui/index.html` does NOT contain `<aside id="mascot-corner">`
- `tauri/ui/src/wizard/router.ts` does NOT contain `mascotMount`
- `tauri/ui/src/wizard/components/primary-panel.ts` contains `borderAnim.className = "border-anim"`
- gsd-ui-checker + gsd-ui-auditor recorded in 14-POLISH-LOG.md with status ✅ (or polish-debt note if escalated)
</verification>

<success_criteria>
- 15 wizard files migrated to v5 primitives + 1 file deleted (mascot-corner.ts)
- Zero legacy shim refs in `tauri/ui/src/wizard/` (per --strict gate)
- Zero forbidden font-family declarations in `tauri/ui/src/wizard/` (Workbench, DM Mono, DSEG7, Caveat all gone)
- vitest wizard.tokens.test.ts green (no skip)
- PrimaryPanel renders with .border-anim as first child (verified by jsdom in vitest)
- Wizard grid collapses to single-column visibly (index.html + tokens.css cascade)
- gsd-ui-checker zero findings on wizard surface
- gsd-ui-auditor 3 audits green on wizard surface (20/80, no-faux-3D-bevel, typography pairing)
- Side-by-side screenshot pair attached to 14-POLISH-LOG.md
- Cycle count ≤3 (CONTEXT Area 3 iteration cap)
</success_criteria>

<output>
After completion, create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-02-SUMMARY.md` documenting:
- Per-file before/after legacy-ref count (15 files + 1 deletion)
- Cycle count for wizard surface (1, 2, or 3)
- ui-checker output ref + ui-auditor output ref + fix commit SHAs
- Screenshot pair (live wizard step 1 + step 4 vs mock §02 fragment)
- Any polish debt escalated to Kaan
</output>
