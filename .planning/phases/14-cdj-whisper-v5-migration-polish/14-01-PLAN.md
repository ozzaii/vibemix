---
phase: 14-cdj-whisper-v5-migration-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check_v5_migration.sh
  - scripts/check_v5_fonts.sh
  - scripts/check_v5_copy.sh
  - tauri/ui/tests/tokens.legacy-detect.test.ts
  - tauri/ui/tests/wizard.tokens.test.ts
  - tauri/ui/tests/session.tokens.test.ts
  - tauri/ui/tests/settings.tokens.test.ts
  - tauri/ui/tests/mascot.chrome.test.ts
  - tauri/ui/public/fonts/Saira-VariableFont_wdth,wght.woff2
  - tauri/ui/public/fonts/JetBrainsMono-Regular.woff2
  - tauri/ui/public/fonts/JetBrainsMono-Medium.woff2
  - tauri/ui/public/fonts/JetBrainsMono-SemiBold.woff2
  - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - POLISH-01
  - POLISH-04
  - POLISH-06
tags:
  - ui
  - migration
  - tooling

must_haves:
  truths:
    - "Three executable grep gates exist at scripts/check_v5_{migration,fonts,copy}.sh and exit 0 in warn-only mode + non-zero in --strict mode when fixtures regress"
    - "Vitest harness can fail RED on a legacy-token fixture before any surface migration runs (proves the gate detects regressions)"
    - "Saira variable WOFF2 + JetBrains Mono Regular/Medium/SemiBold WOFF2 land in tauri/ui/public/fonts/ before Wave 1 starts, so each surface plan can adopt --type-* without remote @import"
    - "14-POLISH-LOG.md skeleton exists with the surface · cycle · ui-checker · ui-auditor · fix-SHA · status table per CONTEXT Area 3"
    - "ROADMAP success criterion #4 typeface text reconciled to Saira + JetBrains Mono (mock wins over stale Geist + Fraunces)"
  artifacts:
    - path: "scripts/check_v5_migration.sh"
      provides: "Repo-wide legacy CSS-token + forbidden-font grep gate. --strict blocks shim-delete commit; --baseline / --warn-only print informational counts; --surface=<name> scopes to one of {wizard,session,settings,mascot}."
      contains: "LEGACY_TOKEN_PATTERN, FORBIDDEN_FONT_PATTERN, --strict / --baseline / --surface= flags"
    - path: "scripts/check_v5_fonts.sh"
      provides: "Forbidden font-family declaration gate (Workbench/DM Mono/DSEG7/Caveat/Geist/Fraunces/Inter/system-ui-as-primary)"
      contains: "font-family pattern grep with allowlist via var(--type-*)"
    - path: "scripts/check_v5_copy.sh"
      provides: "Purge-dictionary grep gate (brushed/anodised/phosphor/retro-futurist/knob-fader physics/tactile + slop bans amazing/awesome/great mix/let me know/delve/leverage/as an AI/unleash/seamless/journey/craft/elevate)"
      contains: "case-insensitive purge_dict pattern against tauri/ui/src/**/*.{ts,tsx} string literals"
    - path: "tauri/ui/tests/tokens.legacy-detect.test.ts"
      provides: "Vitest RED proof — feeding a fixture string containing --phosphor must fail the gate-helper module"
      contains: "describe('legacy token detector') with two cases: clean fixture passes, legacy fixture fails"
    - path: "tauri/ui/tests/wizard.tokens.test.ts"
      provides: "Per-surface vitest fixture — imports and renders representative wizard components and asserts zero legacy token names in computed inline-style / className strings"
      contains: "import { PrimaryPanel } / { WindowPicker } and assert no /--phosphor|--brushed-|--bezel-|--panel-|--groove|--ink-/.test(html)"
    - path: "tauri/ui/tests/session.tokens.test.ts"
      provides: "Same shape as wizard.tokens.test.ts for session surface"
    - path: "tauri/ui/tests/settings.tokens.test.ts"
      provides: "Same shape for settings surface"
    - path: "tauri/ui/tests/mascot.chrome.test.ts"
      provides: "Snapshot/structural test — mascot.html parsed DOM must contain .mascot-window > .border-anim.slow.rev as first child and reference var(--glass-3)"
    - path: "tauri/ui/public/fonts/Saira-VariableFont_wdth,wght.woff2"
      provides: "Vendored Saira variable font (wdth 75-125, wght 300-800) — replaces fonts.googleapis.com @import in Wave 5"
    - path: "tauri/ui/public/fonts/JetBrainsMono-Regular.woff2"
      provides: "Vendored JetBrains Mono 400"
    - path: "tauri/ui/public/fonts/JetBrainsMono-Medium.woff2"
      provides: "Vendored JetBrains Mono 500"
    - path: "tauri/ui/public/fonts/JetBrainsMono-SemiBold.woff2"
      provides: "Vendored JetBrains Mono 600"
    - path: ".planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md"
      provides: "Per-cycle critique-loop log with surface | cycle | ui-checker output ref | ui-auditor output ref | fix commit SHA | status table"
  key_links:
    - from: "scripts/check_v5_migration.sh"
      to: "tauri/ui/src/**"
      via: "grep -rnE LEGACY_TOKEN_PATTERN ... --include='*.ts' --include='*.css' --include='*.html' | grep -v tokens.css"
      pattern: "LEGACY_TOKEN_PATTERN.*phosphor.*brushed.*bezel.*panel.*groove.*ink.*charcoal.*col-mascot"
    - from: "tauri/ui/tests/{wizard,session,settings,mascot}.tokens.test.ts"
      to: "components under audit"
      via: "vitest imports + jsdom render + regex assertion"
      pattern: "import.*\\{.+\\}.+from.+wizard/components|session/components|settings|mascot"
    - from: "tauri/ui/public/fonts/*.woff2"
      to: "tauri/ui/src/tokens.css (added in Wave 5)"
      via: "Vite asset serving at /fonts/* path"
      pattern: "url\\('/fonts/(Saira-VariableFont|JetBrainsMono)"
---

<objective>
Stand up the Wave 0 reconciliation surface that every later wave depends on: three scripted grep gates, four vitest harnesses (one RED proof + three per-surface fixtures + one mascot snapshot), four vendored WOFF2 files, the 14-POLISH-LOG.md skeleton, and the ROADMAP typeface text reconciliation.

Purpose: Lock the migration's validation contract before any consumer code moves so Waves 1-4 each have a deterministic per-surface gate, and Wave 5's shim-delete commit has a one-shot pre-commit hook ready to wire. The vendored WOFF2 files ship now so Wave 5's @font-face replacement is purely a tokens.css surgery — not a "did fonts download?" dependency.

Output: Three executable shell scripts, five vitest specs, four .woff2 binaries, one polish-log skeleton, one ROADMAP text reconciliation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-VALIDATION.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md
@.planning/HANDOFF-cdj-whisper-v5-ui-migration.md
@scripts/check_ipc_schema.py
@tauri/ui/src/tokens.css

<interfaces>
<!-- Closest analog for the scripted-gate pattern is the existing standalone Python check at
     scripts/check_ipc_schema.py (Phase 11 W0 — non-framework single-file gate, no test runner). -->

scripts/check_ipc_schema.py (canonical standalone-script analog):
- Single-file standalone script
- No framework dependencies
- Exits non-zero on regression detection
- Designed for both CI and local pre-commit invocation

Existing vitest harness (Phase 12 W2):
- tauri/ui/vitest.config.ts — jsdom env, ts-loader, no watch by default
- Run via: `cd tauri/ui && npm run test`
- Imports work for any tauri/ui/src/ module via tsconfig paths

Existing token contract (from tauri/ui/src/tokens.css lines 175–231 — the shim block):
- Legacy token names live in shim block: `--phosphor`, `--phosphor-warm`, `--phosphor-dim`, `--phosphor-soft`, `--phosphor-glow`, `--phosphor-halo`, `--brushed-hi`, `--brushed-lo`, `--bezel-1`, `--bezel-2`, `--bezel-3`, `--panel`, `--panel-lift`, `--panel-deep`, `--panel-hover-top`, `--panel-pressed-bottom`, `--groove`, `--ink`, `--ink-dim`, `--ink-deep`, `--ink-engraved`, `--charcoal` (note: --charcoal isn't currently in shim but legacy comment retains it; gate includes for defence in depth), `--col-mascot`
- v5 primitives live above shim: `--void/--void-1..4`, `--glass-1/2/3`, `--glass-edge`, `--glass-edge-up`, `--glass-top`, `--silk/--silk-65/--silk-40/--silk-22/--silk-12`, `--amber/--amber-deep/--amber-pale/--amber-22/--amber-40/--amber-65`, `--rave-magenta/--rave-pink/--rave-cyan/--rave-purple/--rave-teal`, `--glow-faint/--glow-soft/--glow-strong`, `--type-display/--type-body/--type-mono`, `--rad-sm/-md/-lg`, `--sp-1..8`, `--motion-border-sweep`

Forbidden font families (consumer side — outside `tokens.css` @font-face declarations):
- "Workbench" — 21 refs across wizard/* — remove
- "DM Mono" — 33 refs across wizard/session/settings — remove
- "DSEG7" — 10 refs — remove
- "Caveat" — 3 refs — remove
- "Geist" — must be 0 (ROADMAP success-criterion was stale)
- "Fraunces" — must be 0 (ROADMAP success-criterion was stale)
- "Inter" — must be 0 (frontend-enforcement skill also bans)
- system-ui as primary (only OK as fallback inside `--type-*` chain in tokens.css)

Purge dictionary (UI-rendered chrome strings only, NOT code comments):
- Hardware-vocab residue: `brushed`, `anodised`, `phosphor`, `retro-futurist`, `knob/fader physics`, `knurled` — case-insensitive grep gate
- Generic slop: `amazing`, `awesome`, `great mix`, `let me know`, `delve`, `leverage`, `as an AI`, `unleash`, `seamless`, `journey`, `craft`, `elevate` — case-insensitive
- Manual-review-only: `tactile` (may be legit UI-behavior word — gate prints as warning, not block)

Existing vitest example for componentless DOM assertions:
- tauri/ui/tests/* directory (Phase 12 W2 established it via `tauri/ui/vitest.config.ts`)
- ts-loader works for `.ts` files; jsdom for document/window
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-01-01: Vendor Saira + JetBrains Mono WOFF2 + write three scripted grep gates</name>
  <files>tauri/ui/public/fonts/Saira-VariableFont_wdth,wght.woff2, tauri/ui/public/fonts/JetBrainsMono-Regular.woff2, tauri/ui/public/fonts/JetBrainsMono-Medium.woff2, tauri/ui/public/fonts/JetBrainsMono-SemiBold.woff2, scripts/check_v5_migration.sh, scripts/check_v5_fonts.sh, scripts/check_v5_copy.sh</files>
  <read_first>
    - scripts/check_ipc_schema.py (existing single-file standalone-script pattern; mirror its shape, NOT its content)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md lines 575-674 (canonical script + vendored @font-face snippets — copy verbatim)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Shim Removal Surgery" + "Copywriting Contract" (purge dictionary source-of-truth)
    - tauri/ui/public/fonts/ (existing layout — verify the 5 legacy WOFF2 files: Workbench-Regular, DMMono-Regular, DMMono-Medium, DSEG7Classic-Bold, Caveat-Bold — none deleted in this task; deletion happens in Wave 5)
  </read_first>
  <action>
Download Saira variable-font WOFF2 (wdth + wght axes, weights 300-800, widths 75-125) from the official Google Fonts mirror or fonts.google.com release; download JetBrainsMono Regular/Medium/SemiBold WOFF2 from the JetBrains official release (https://github.com/JetBrains/JetBrainsMono/releases). Place at exact paths in <files>. Use `shasum -a 256 <file>` to compute SHA-256 attestations and capture them in the commit message (Wave 5 will reflect them into LICENSE-3RD-PARTY.md; this task only ships the binaries).

Create scripts/check_v5_migration.sh from RESEARCH.md lines 621-674 verbatim, plus add `--surface=<wizard|session|settings|mascot>` flag (subset the grep path to `tauri/ui/src/<surface>/`) and `--baseline` flag (alias for warn-only with explicit "baseline" header line so the post-Wave-0 baseline capture is grep-able from the polish log). The LEGACY_TOKEN_PATTERN must catch every alias listed in tokens.css:182–230 plus `--charcoal` (defence in depth) and `--cue` is excluded (forbidden-but-declared, Phase 11 invariant — not migrated, just never consumed).

Create scripts/check_v5_fonts.sh — same shape, but the pattern is `font-family:\s*["'](Workbench|DM Mono|DSEG7|Caveat|Geist|Fraunces|Inter)["']` AND `font-family:\s*system-ui` when NOT preceded by `--type-` (i.e., consumer-side system-ui-as-primary, not the fallback chain in tokens.css). `--strict` exits 1 on any hit; `--surface=` scopes; `--warn-only` (default) prints counts.

Create scripts/check_v5_copy.sh — case-insensitive grep on `tauri/ui/src/**/*.{ts,tsx}` extracting *string literals only* (use awk or python wrapper to ignore // and /* */ comments). Match the purge dictionary (UI-SPEC Copywriting Contract): hardware-vocab residue (`brushed`, `anodised`, `phosphor`, `retro-futurist`, `knob/fader physics`, `knurled`) blocks in --strict; slop bans (`amazing|awesome|great mix|let me know|delve|leverage|as an AI|unleash|seamless|journey|craft|elevate`) block in --strict; `tactile` is warn-only even in --strict (manual review). Per CONTEXT Area 4: scrub scope = `tauri/ui/src/{wizard,session,settings,mascot}/**`; exclude comments, console logs, error messages.

chmod +x on all three scripts. Each script exits 0 / non-zero per RESEARCH spec.
  </action>
  <verify>
    <automated>bash scripts/check_v5_migration.sh --baseline && bash scripts/check_v5_fonts.sh --warn-only && bash scripts/check_v5_copy.sh --warn-only && ls -la tauri/ui/public/fonts/Saira-VariableFont_wdth,wght.woff2 tauri/ui/public/fonts/JetBrainsMono-Regular.woff2 tauri/ui/public/fonts/JetBrainsMono-Medium.woff2 tauri/ui/public/fonts/JetBrainsMono-SemiBold.woff2 && bash scripts/check_v5_migration.sh --surface=wizard 2>&1 | grep -E "legacy CSS-token refs|warn-only" && bash -c 'set -e; tmp=$(mktemp -d); printf "color: var(--phosphor);\n" > "$tmp/fake.css"; ! grep -rnE "LEGACY_TOKEN_PATTERN" "$tmp" 2>/dev/null; rm -rf "$tmp"'</automated>
  </verify>
  <done>Three executable scripts exist and exit 0 in default warn-only mode (baseline state). Saira + 3 JetBrains Mono WOFF2 files exist at exact paths. `check_v5_migration.sh --surface=wizard` prints a non-zero baseline ref count (current state is 225 wizard legacy refs — that's the starting point). `check_v5_migration.sh --strict` would currently exit 1 (because shim consumers exist) — that's also expected; do NOT verify --strict-pass here. SHA-256 attestations captured in commit message body.</done>
</task>

<task type="auto">
  <name>Task 14-01-02: Write 5 vitest specs (1 RED proof + 4 per-surface fixtures) and 14-POLISH-LOG.md skeleton</name>
  <files>tauri/ui/tests/tokens.legacy-detect.test.ts, tauri/ui/tests/wizard.tokens.test.ts, tauri/ui/tests/session.tokens.test.ts, tauri/ui/tests/settings.tokens.test.ts, tauri/ui/tests/mascot.chrome.test.ts, .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md</files>
  <read_first>
    - tauri/ui/vitest.config.ts (existing — confirm jsdom env, no watch in CI)
    - tauri/ui/tests/ (existing dir; mirror the established pattern from Phase 12 W2 specs — check existing test files for jsdom import, render pattern, registerStyle invocation)
    - tauri/ui/src/wizard/components/primary-panel.ts (Wave 1 will modify; this test asserts current state has legacy refs)
    - tauri/ui/src/session/SessionLayout.ts (Wave 2 surface)
    - tauri/ui/src/settings/SettingsDrawer.ts (Wave 3 surface)
    - tauri/ui/src/mascot/index.ts + tauri/ui/mascot.html (Wave 4 surface)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-VALIDATION.md "Per-Task Verification Map" + "Wave 0 Requirements"
  </read_first>
  <action>
Create tauri/ui/tests/tokens.legacy-detect.test.ts: a vitest spec that defines a tiny helper `containsLegacyToken(s: string): boolean` matching the same LEGACY_TOKEN_PATTERN as scripts/check_v5_migration.sh, then two cases — (a) `expect(containsLegacyToken("color: var(--silk);")).toBe(false)` and (b) `expect(containsLegacyToken("color: var(--phosphor);")).toBe(true)`. This proves the detection logic is sound (RED for the legacy fixture, GREEN for the clean fixture). Export `containsLegacyToken` so per-surface specs can import + reuse.

Create tauri/ui/tests/wizard.tokens.test.ts: imports `PrimaryPanel` from `../src/wizard/components/primary-panel` AND `WindowPicker` from `../src/wizard/components/window-picker` (the largest-ref file at 37 hits) AND renders each with minimal props. Concatenates `outerHTML + getAllStyleSheetRules()` into a string and asserts `containsLegacyToken(rendered)` is `false`. This test is **expected to FAIL in Wave 0** (current state has legacy refs) — that's the contract. Wave 1 fixes wizard surface; once it passes, it stays green. Add a `describe.skip('wizard tokens — wave 1 will green this')` wrapper so CI doesn't break in Wave 0; Wave 1's first task unskips it.

Create tauri/ui/tests/session.tokens.test.ts: same shape but imports `SessionLayout` from `../src/session/SessionLayout` and `Meter` from `../src/session/components/meter` and `Titlebar` from `../src/session/components/titlebar`. Wrap in `describe.skip(...wave 2 will green...)`.

Create tauri/ui/tests/settings.tokens.test.ts: imports `SettingsDrawer` from `../src/settings/SettingsDrawer` and `RetentionSlider` / `HotkeyCapture` / `MascotGroup` from their respective modules. Wrap in `describe.skip(...wave 3 will green...)`.

Create tauri/ui/tests/mascot.chrome.test.ts: reads `tauri/ui/mascot.html` via `fs.readFileSync` (jsdom mounts the parsed document); asserts (a) `document.querySelector('.mascot-window > .border-anim.slow.rev')` is the FIRST child of `.mascot-window`, (b) the file imports `tokens.css` via `<link>` OR has the v5 vars inlined in `<style>`, (c) `containsLegacyToken(document.documentElement.outerHTML)` is false. Wrap in `describe.skip(...wave 4 will green...)`.

Create .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md with this exact skeleton (one row per surface × cycle, populated as critique loops run — leave empty for Wave 0):

```markdown
# Phase 14 — Polish Log

Per-surface critique-loop record. Wave-based: each surface gets up to 3 cycles of ui-checker → fix → ui-auditor. After 3 cycles with unresolved findings, the row is escalated to Kaan as polish debt.

## Critique Cycles

| Surface | Cycle | ui-checker output ref | ui-auditor output ref | Fix commit SHA | Status |
|---------|-------|------------------------|------------------------|----------------|--------|
| wizard | 1 | — | — | — | ⬜ pending |
| wizard | 2 | — | — | — | ⬜ not started |
| wizard | 3 | — | — | — | ⬜ not started |
| session | 1 | — | — | — | ⬜ pending |
| session | 2 | — | — | — | ⬜ not started |
| session | 3 | — | — | — | ⬜ not started |
| settings | 1 | — | — | — | ⬜ pending |
| settings | 2 | — | — | — | ⬜ not started |
| settings | 3 | — | — | — | ⬜ not started |
| mascot | 1 | — | — | — | ⬜ not started |
| mascot | 2 | — | — | — | ⬜ not started |
| mascot | 3 | — | — | — | ⬜ not started |

*Status: ⬜ not started · 🟡 in progress · ✅ green · ❌ red · ⚠️ polish debt (escalated)*

## Side-by-Side Screenshots

| Surface | Live screenshot | Mock reference | Attached commit |
|---------|------------------|----------------|------------------|
| wizard | — | mocks/vibemix-direction-final.html §02 | — |
| session | — | mocks/vibemix-direction-final.html §01 left | — |
| settings | — | mocks/vibemix-direction-final.html §02 spec-panel | — |
| mascot | — | mocks/vibemix-direction-final.html §01 right | — |

## Perf Verification (POLISH-05 + CONTEXT Area 3 — must close before phase end)

| Platform | Default blur | data-blur-perf="on" | prefers-reduced-motion | Verifier note |
|----------|--------------|----------------------|--------------------------|----------------|
| macOS (Kaan M-series) | ⬜ | ⬜ | ⬜ | — |
| Windows (non-dev) | ⬜ | ⬜ | ⬜ | — |
```
  </action>
  <verify>
    <automated>cd tauri/ui && npm run test -- tokens.legacy-detect.test.ts --run --reporter=dot && cd ../.. && test -f .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md && grep -q "Critique Cycles" .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md && grep -q "Perf Verification" .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md && ls tauri/ui/tests/wizard.tokens.test.ts tauri/ui/tests/session.tokens.test.ts tauri/ui/tests/settings.tokens.test.ts tauri/ui/tests/mascot.chrome.test.ts</automated>
  </verify>
  <done>tokens.legacy-detect.test.ts passes both cases (RED for legacy fixture, GREEN for clean fixture). Four per-surface specs exist, all currently `describe.skip(...)` so Wave 0 CI is green. Polish log skeleton exists with three sections (Critique Cycles, Side-by-Side Screenshots, Perf Verification) and 12 + 4 + 2 rows.</done>
</task>

<task type="auto">
  <name>Task 14-01-03: Reconcile ROADMAP success-criterion #4 typeface text (Saira + JetBrains Mono, not Geist + Fraunces)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 236-249 (Phase 14 entry — current text has "Geist for chrome + Fraunces for headlines")
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md "Area 4 — Copy Purge + Typeface Reconciliation" (locked decision — mock wins over stale ROADMAP)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Typography" (Saira + JetBrains Mono is the contract)
  </read_first>
  <action>
Edit .planning/ROADMAP.md Phase 14 entry. Find Success Criterion #4 — currently reads "gsd-ui-auditor passes ... the typography pairing audit (Geist for chrome + Fraunces for headlines, no Inter / no system-ui)". Replace with "the typography pairing audit (Saira variable wdth + wght axes for chrome + JetBrains Mono for numerics, no Inter / no system-ui as primary / no Geist / no Fraunces / no Workbench / no DM Mono / no DSEG7 / no Caveat)". Add a single-line trailing note under the phase entry: "_Typeface reconciled per CONTEXT.md Area 4 (mock wins over stale Geist/Fraunces text)._"

Do NOT alter any other ROADMAP entry (Phase 14 only). Do NOT alter REQUIREMENTS.md POLISH-04 (its text already says "Saira + JetBrains Mono only" indirectly via the broader "no Inter / no system-ui" framing — verify by grep, no edit needed).
  </action>
  <verify>
    <automated>grep -F "Saira variable wdth + wght axes for chrome + JetBrains Mono" .planning/ROADMAP.md && ! grep -F "Geist for chrome + Fraunces for headlines" .planning/ROADMAP.md && grep -F "Typeface reconciled per CONTEXT.md Area 4" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP Phase 14 success criterion #4 now references Saira + JetBrains Mono. Stale "Geist for chrome + Fraunces for headlines" string is absent. Reconciliation note is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → repo | Pre-commit hook from Wave 5 runs scripts/check_v5_migration.sh --strict; this Wave 0 task creates the script but does NOT wire the hook globally |
| Vite asset pipeline → WebView2/WKWebView | Vendored WOFF2 files served as static assets from /fonts/* path; no remote fetch |

## STRIDE Threat Register (security_enforcement = ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Supply-chain (Tampering) | Vendored Saira + JetBrainsMono WOFF2 binaries | mitigate | SHA-256 attestations captured in commit body; Wave 5 reflects into LICENSE-3RD-PARTY.md so any future regen can re-attest |
| T-14-02 | Capability allowlist drift (Elevation) | Tauri capability config | accept | This phase is presentation-only; no Tauri capabilities added/removed; allowlist regression check covered by Phase 11/12 carryover (npm run check:ipc + cargo check) — not re-run here |
| T-14-03 | Info disclosure | scripts/check_v5_copy.sh accidentally scanning Gemini transcript log fixtures | mitigate | Scrub scope per CONTEXT Area 4 is explicit: `tauri/ui/src/{wizard,session,settings,mascot}/` only — exclude planning docs, code comments, console logs, error messages, prompt templates, Gemini transcript runtime content |
</threat_model>

<verification>
- Three scripted gates exist, executable, and pass in --warn-only / --baseline mode (the strict pass is Wave 5's gate, not Wave 0's)
- Vitest harness has one always-green spec (tokens.legacy-detect.test.ts) + four `describe.skip` per-surface specs that Wave 1-4 will unskip as each surface lands
- Four vendored WOFF2 binaries exist at the exact paths Wave 5 will reference in tokens.css @font-face declarations
- 14-POLISH-LOG.md exists with the canonical 3-section skeleton
- ROADMAP success criterion #4 reads Saira + JetBrains Mono, not Geist + Fraunces
</verification>

<success_criteria>
- `bash scripts/check_v5_migration.sh --baseline` exits 0 and prints baseline ref counts
- `bash scripts/check_v5_fonts.sh --warn-only` exits 0 and prints baseline forbidden-font counts (matches RESEARCH numbers: ~67 hits across 4 families)
- `bash scripts/check_v5_copy.sh --warn-only` exits 0 and prints baseline purge-dictionary counts
- `cd tauri/ui && npm run test -- tokens.legacy-detect.test.ts --run` exits 0
- `ls tauri/ui/public/fonts/{Saira-VariableFont_wdth\,wght,JetBrainsMono-Regular,JetBrainsMono-Medium,JetBrainsMono-SemiBold}.woff2` succeeds
- ROADMAP grep for "Geist for chrome + Fraunces" returns zero hits; grep for "Saira variable wdth + wght axes for chrome + JetBrains Mono" returns one hit
- 14-POLISH-LOG.md exists and contains the three section headers (Critique Cycles, Side-by-Side Screenshots, Perf Verification)
</success_criteria>

<output>
After completion, create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-01-SUMMARY.md` documenting:
- The three script paths + flags they accept
- The five vitest spec paths + which are currently `describe.skip`
- SHA-256 hashes of the four vendored WOFF2 files (will reflect into LICENSE-3RD-PARTY.md in Wave 5)
- Baseline grep counts (the starting state Wave 1-4 will drive to zero)
</output>
