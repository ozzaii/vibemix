---
phase: 14-cdj-whisper-v5-migration-polish
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - tauri/ui/src/session/SessionLayout.ts
  - tauri/ui/src/session/components/titlebar.ts
  - tauri/ui/src/session/components/meter.ts
  - tauri/ui/src/session/components/drop-chip.ts
  - tauri/ui/src/session/components/rocker.ts
  - tauri/ui/src/session/components/panel.ts
  - tauri/ui/src/session/components/cohost.ts
  - tauri/ui/src/session/components/phase-tape.ts
  - tauri/ui/src/session/components/status-bar.ts
  - tauri/ui/src/session/components/timecode.ts
  - tauri/ui/src/session/components/picker.ts
  - tauri/ui/src/session/components/event-ribbon.ts
  - tauri/ui/src/session/components/muted-banner.ts
  - tauri/ui/src/tokens.css
  - tauri/ui/src/main.ts
  - tauri/ui/tests/session.tokens.test.ts
  - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
autonomous: false
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-05
tags:
  - ui
  - migration
  - session
  - perf

must_haves:
  truths:
    - "Every session consumer reads v5 primitives directly — zero legacy shim refs in tauri/ui/src/session/"
    - "SessionLayout root has <div class='border-anim'> as its first child (before screws loop)"
    - "tokens.css gains @media (prefers-reduced-motion: reduce) + html[data-blur-perf='on'] blocks that swap --blur-glass-* to lighter variants and freeze .border-anim on reduced-motion"
    - "main.ts boot() reads settings.performance.lighter_blur via the existing settings IPC and applies data-blur-perf attribute to <html> before first paint (default-off if field is absent — graceful)"
    - "vitest session.tokens.test.ts passes green (no skip)"
    - "gsd-ui-checker zero findings on session surface AND gsd-ui-auditor 3 audits green — recorded in 14-POLISH-LOG.md"
  artifacts:
    - path: "tauri/ui/src/session/SessionLayout.ts"
      provides: "Session root composer with border-anim first child + v5 glass-panel anatomy"
      contains: "border-anim, var(--glass-1), var(--blur-glass), var(--sp-5), var(--silk-22)"
    - path: "tauri/ui/src/session/components/cohost.ts"
      provides: "AI-transcript ring with amber-22 left border + amber .who label + tabular-nums timestamps"
      contains: "border-left: 1px solid var(--amber-22), var(--amber), var(--glow-soft)"
    - path: "tauri/ui/src/session/components/phase-tape.ts"
      provides: "Phase tape with --silk-22 (passed) / --amber-40 (current) / --glass-3 (upcoming) segments"
      contains: "var(--silk-22), var(--amber-40), var(--glass-3)"
    - path: "tauri/ui/src/tokens.css"
      provides: "Perf-fallback block — prefers-reduced-motion override + data-blur-perf runtime toggle"
      contains: "@media (prefers-reduced-motion: reduce), html[data-blur-perf=\"on\"]"
    - path: "tauri/ui/src/main.ts"
      provides: "Boot-time perf preference read + data-blur-perf attribute application"
      contains: "applyBlurPerfPreference, data-blur-perf"
  key_links:
    - from: "tauri/ui/src/session/SessionLayout.ts"
      to: ".border-anim utility in tokens.css:302"
      via: "borderAnim.className = 'border-anim' as first append"
      pattern: "border-anim"
    - from: "tauri/ui/src/tokens.css perf block"
      to: "session/settings glass panels via CSS cascade"
      via: "html[data-blur-perf='on'] selector swaps --blur-glass-* to lighter variants"
      pattern: "html\\[data-blur-perf"
    - from: "tauri/ui/src/main.ts boot()"
      to: "ipc settings.get for performance.lighter_blur"
      via: "sendIpcRequest or invoke read_settings"
      pattern: "data-blur-perf|applyBlurPerfPreference"
---

<objective>
Migrate every live session UI consumer (~13 files, ~25 legacy refs + ~58 hardcoded amber rgba + ~6 hardcoded silk rgba per RESEARCH inventory) from shim aliases to v5 primitives, insert the animated border on SessionLayout root, and ship the backdrop-filter perf fallback (CSS-only: prefers-reduced-motion override + html[data-blur-perf="on"] block + main.ts boot-time read).

Purpose: Wave 2 is the most expensive surface visually (30Hz rAF render loop). The perf fallback ships now (CONTEXT Area 3) so Settings drawer in Wave 3 has a UI toggle to wire. session/components/panel.ts is the gold-standard for the whole project — it's already v5 and serves as the canonical analog (do NOT modify).

Output: 13 session files migrated to v5, tokens.css gains the perf-fallback block, main.ts wires boot-time data-blur-perf, vitest session.tokens.test.ts unskipped + green, polish log rows populated for session cycles 1-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md
@mocks/vibemix-direction-final.html
@tauri/ui/src/tokens.css
@tauri/ui/src/session/components/panel.ts

<interfaces>
GOLD-STANDARD ANALOG — `tauri/ui/src/session/components/panel.ts` is canonical v5 (DO NOT MODIFY; lift patterns FROM it):
- background: var(--glass-2) (line 29)
- backdrop-filter: var(--blur-glass-light) + -webkit- pair (lines 30–31)
- border: 1px solid var(--glass-edge) (line 32)
- border-radius: var(--rad-md) (line 33)
- box-shadow stack (lines 34–38): inset 0 1px 0 var(--glass-top), inset 0 -1px 0 rgba(0,0,0,0.45), 0 16px 36px rgba(0,0,0,0.55)
- .vmx-glass-streak injection (lines 113–117)
- Saira variable-axis header (lines 49–50): font-variation-settings: "wdth" 85, "wght" 600
- Engraved text-shadow (line 55): 0 1px 0 rgba(0, 0, 0, 0.7)

GOLD-STANDARD ANALOG — `tauri/ui/src/settings/SettingsDrawer.ts:62-237`:
- Already on v5; lift for cohost.ts ai-line styling, picker.ts dropdown, event-ribbon glass-tile.

BORDER-ANIM INSERTION SITE (PATTERNS.md Surface 2 — site at SessionLayout.ts around lines 183–194; SessionLayout.ts is 636 lines so verify by reading):

  // CURRENT (before screws loop):
  const root = document.createElement("div");
  root.className = "vmx-session";
  for (const corner of ["tl", "tr", "bl", "br"] as const) { ... }

  // AFTER (insert border-anim FIRST child, BEFORE screws):
  const root = document.createElement("div");
  root.className = "vmx-session";
  const borderAnim = document.createElement("div");
  borderAnim.className = "border-anim";
  borderAnim.setAttribute("aria-hidden", "true");
  root.append(borderAnim);
  for (const corner of ["tl", "tr", "bl", "br"] as const) { ... }

SESSIONLAYOUT CSS ADJUSTMENTS (per PATTERNS.md — locate by reading the file; line numbers are approximate):
- `--gap-col: var(--sp-lg);` → `--gap-col: var(--sp-5);`
- corner screws color `var(--bezel-3);` → `var(--silk-22);`
- panel padding `var(--sp-xl);` → `32px; /* mock-verbatim — no v5 equivalent */`
- gaps `var(--sp-md);` → `var(--sp-4);` (three occurrences in PATTERNS)
- top padding `var(--sp-md) 0 0;` → `var(--sp-4) 0 0;`

PERF FALLBACK CSS BLOCK (verbatim from RESEARCH.md:696–721 — insert after :root closes at tokens.css:231, before /* === Reset === at tokens.css:233):

  /* === Perf fallback — accessibility + user-toggled lighter blur ======== */
  @media (prefers-reduced-motion: reduce) {
    :root {
      --blur-glass:         blur(16px);
      --blur-glass-light:   blur(8px);
      --blur-glass-display: blur(4px);
      --motion-border-sweep: 0s;
    }
    .border-anim { animation: none; opacity: 0.6; }
  }
  html[data-blur-perf="on"] {
    --blur-glass:         blur(16px);
    --blur-glass-light:   blur(8px);
    --blur-glass-display: blur(4px);
  }

BOOT-TIME PERF WIRING (PATTERNS.md Shared Pattern 5 — insert in main.ts boot() after initCrashBanner; the existing main.ts uses sendIpcRequest from tauri/ui/src/ipc/client.ts, not @tauri-apps/api invoke; verify by reading main.ts):

  // Apply boot-time perf-blur preference per CONTEXT Area 3.
  try {
    const result = await sendIpcRequest({ type: "settings.get", field: "performance" }, { timeoutMs: 2000 });
    const lighter = (result?.value as { lighter_blur?: boolean } | undefined)?.lighter_blur === true;
    if (lighter) document.documentElement.setAttribute("data-blur-perf", "on");
  } catch (err) {
    // Default-off blur is the safe path; do not block boot on a missing field.
    console.warn("[boot] perf preference read failed:", err);
  }

NOTE: If `settings.get` doesn't accept `field: "performance"` today, read the full settings blob via `{ type: "settings.get" }` and index into `value.performance.lighter_blur`. Verify the schema via tauri/ui/src/ipc/messages.ts before writing. The `performance.lighter_blur` SCHEMA addition + actual SettingsApplier write path lands in Plan 14-04 — for THIS plan the boot read defaults off gracefully if the field is missing.

COMPONENT MIGRATION CONTRACT (UI-SPEC Surface 2):

| Component | Migration target |
|-----------|------------------|
| SessionLayout.ts | Border-anim first child + 6 CSS line adjustments above + recolor screws |
| titlebar.ts | .top-strip layout; live-tag liveBlink keyframe + var(--glow-strong); brand bullet var(--amber) 4px filled circle; DSEG7 → var(--type-mono) tabular-nums clock |
| timecode.ts | --glass-3 recessed; var(--type-mono) 64px tabular-nums; ms span 36px var(--silk-40); inset 0 0 18px rgba(255,138,61,0.035) |
| meter.ts | Three .m-row; .m-fill linear-gradient(90deg, var(--amber-deep), var(--amber) 65%, var(--amber-pale)); scanline ::after; mBreathe 2s ease-in-out infinite alternate keyframe |
| cohost.ts | Ring of .ai-line entries; border-left: 1px solid var(--amber-22); .who 9px var(--type-display) wdth 85 wght 500 var(--amber) + text-shadow: 0 0 4px var(--amber-65); animated dot |
| drop-chip.ts | .countdown 40px var(--type-mono) tabular-nums var(--amber) + var(--glow-soft) text-shadow; sub-label 9px var(--type-display) wdth 85 wght 500 var(--silk-65) UPPERCASE 0.22em |
| rocker.ts | Three .mood-btn; active state: linear-gradient(180deg, rgba(255,138,61,0.09), rgba(255,138,61,0.025)) /* mock-verbatim */ + 1px var(--amber-40) border + inset 0 0 14px var(--amber-22) + var(--amber) text |
| status-bar.ts | Four .led instances (livekit / gemini / midi / screen); --led-ok / --led-warn / --led-fault / off=rgba(214,207,199,0.18); 26 hardcoded rgba — migrate silk to tokens, shadows stay inline |
| phase-tape.ts | Segments: --silk-22 (passed) / --amber-40 (current) / --glass-3 (upcoming); --sp-3 margin; NOT a hero |
| event-ribbon.ts | MIDI ribbon — .ai-line glass-tile pattern but .who label "midi · {channel} · {event}" 9px var(--silk-40); NEVER amber (informational, per accent-reservation list) |
| muted-banner.ts | Retone --phosphor*/--bezel-*/--panel-* refs to v5; banner pulse uses --glow-soft on amber |
| picker.ts | List-row hover var(--silk-12); selected var(--glass-2) + var(--amber) text + var(--glow-faint); 16 hardcoded rgba migrated to tokens where possible |
| panel.ts | DO NOT MODIFY — already canonical v5 |

ACCENT RESERVATION SANITY (UI-SPEC §Color §20%):
Session maps to: 1 (live-dot), 2 (AI-talking), 6 (meter fills), 7 (countdown), 8 (AI-line left border), 9 (AI-line .who), 10 (animated border), 11 (section index — if used), 14 (tray live-dot — owned by Phase 13 not this plan).
MIDI event-ribbon explicitly does NOT use amber — use --silk-40 for the .who label.
Status-bar dots are LED-semantic (--led-ok/warn/fault), NOT amber.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-03-01: Migrate SessionLayout + 9 session components + ship perf-fallback CSS</name>
  <files>tauri/ui/src/session/SessionLayout.ts, tauri/ui/src/session/components/titlebar.ts, tauri/ui/src/session/components/meter.ts, tauri/ui/src/session/components/drop-chip.ts, tauri/ui/src/session/components/rocker.ts, tauri/ui/src/session/components/timecode.ts, tauri/ui/src/session/components/picker.ts, tauri/ui/src/session/components/status-bar.ts, tauri/ui/src/session/components/muted-banner.ts, tauri/ui/src/tokens.css</files>
  <read_first>
    - tauri/ui/src/session/components/panel.ts (CANONICAL — read end-to-end; do NOT modify; lift patterns FROM it)
    - tauri/ui/src/session/SessionLayout.ts (current — 636 lines; locate the actual screws-loop, gap-col, and --sp-* declarations by reading; PATTERNS.md line numbers are approximate)
    - tauri/ui/src/tokens.css lines 122–125 + lines 231–233 (insertion site for perf block)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Surface 2 — Live Session UI (Wave 2)" + "Shared Pattern 1, 2, 3, 4"
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Surface 2 — Live Session UI"
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md lines 690–721 (perf fallback CSS — verbatim)
    - mocks/vibemix-direction-final.html (live session mock §01 — the contract)
  </read_first>
  <action>
For each component listed in <files>, apply the per-component migration from the table in <interfaces>. Specifically:

**SessionLayout.ts** — Insert `.border-anim` as FIRST child of `.vmx-session` root BEFORE the corner-screws loop. Apply the 6 CSS line adjustments (gap-col → --sp-5, screws color → --silk-22, panel padding → 32px mock-verbatim, gaps → --sp-4, top-padding → --sp-4).

**titlebar.ts** — Migrate 7 legacy refs. DSEG7 22px clock → `var(--type-mono)` + `font-variant-numeric: tabular-nums`. Brand bullet → `var(--amber)` 4px filled circle. live-tag uses `liveBlink 1.4s ease-in-out infinite` keyframe + `var(--glow-strong)`.

**meter.ts** — Migrate 4 legacy refs. Three `.m-row`; `.m-fill` is `linear-gradient(90deg, var(--amber-deep), var(--amber) 65%, var(--amber-pale))`. Scanline `::after` via `repeating-linear-gradient(0deg, transparent 0, transparent 2px, rgba(0,0,0,0.08) 2px, rgba(0,0,0,0.08) 3px)`. Add `@keyframes mBreathe { from { opacity: 0.92 } to { opacity: 1 } }` and apply `animation: mBreathe 2s ease-in-out infinite alternate`. Trough background `var(--glass-3)`.

**drop-chip.ts (countdown)** — 3 legacy refs. `.countdown` 40px `var(--type-mono)` + `font-variant-numeric: tabular-nums` + `color: var(--amber)` + `text-shadow: var(--glow-soft)`. Sub-label per <interfaces> table.

**rocker.ts** — 2 legacy refs. Three `.mood-btn` toggles per <interfaces> table.

**timecode.ts** — Display-window pattern: `var(--glass-3)` background + `var(--blur-glass-display)` + `inset 0 2px 6px rgba(0,0,0,0.9), inset 0 0 18px rgba(255,138,61,0.035)`. Time-hero 64px `var(--type-mono)` tabular-nums. ms span 36px `var(--silk-40)`.

**picker.ts** — List-row hover `var(--silk-12)`; selected `var(--glass-2)` + `var(--amber)` text + `var(--glow-faint)`. Migrate 16 hardcoded rgba — amber to `--amber-*`, silk to `--silk-*`, keep `rgba(0,0,0,*)` shadows inline if mock-verbatim.

**status-bar.ts** — Four `.led` instances. Idle/off `rgba(214,207,199,0.18)` (semantic, not amber). ok/warn/fault use the three --led-* tokens. Migrate 26 hardcoded rgba — silk ones to --silk-*, shadows stay inline.

**muted-banner.ts** — Retone --phosphor*/--bezel-*/--panel-* refs.

**tokens.css** — Insert the perf-fallback block VERBATIM from RESEARCH.md:696–721 between the closing `}` of `:root` (line 231) and the `/* === Reset ===` comment (line 233).
  </action>
  <verify>
    <automated>cd tauri/ui && npm run check:ipc 2>/dev/null; cd "$OLDPWD" 2>/dev/null || cd /Users/ozai/projects/dj-set-ai; bash scripts/check_v5_migration.sh --surface=session --warn-only; grep -F "border-anim" tauri/ui/src/session/SessionLayout.ts; grep -F "prefers-reduced-motion" tauri/ui/src/tokens.css; grep -F "data-blur-perf" tauri/ui/src/tokens.css; cd tauri/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>10 session files migrated to v5 primitives. SessionLayout has .border-anim first child + 6 CSS line adjustments. tokens.css has both perf-fallback blocks (prefers-reduced-motion + data-blur-perf). `check_v5_migration.sh --surface=session --warn-only` shows substantial drop from baseline 25. `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 14-03-02: Migrate cohost + phase-tape + event-ribbon + panel-passthrough + wire main.ts boot read + unskip session.tokens.test.ts</name>
  <files>tauri/ui/src/session/components/cohost.ts, tauri/ui/src/session/components/phase-tape.ts, tauri/ui/src/session/components/event-ribbon.ts, tauri/ui/src/session/components/panel.ts, tauri/ui/src/main.ts, tauri/ui/tests/session.tokens.test.ts</files>
  <read_first>
    - tauri/ui/src/session/components/panel.ts (DO NOT MODIFY — read for the canonical pattern; verify no legacy refs survive)
    - tauri/ui/src/main.ts (current — locate boot() function; verify whether existing surface uses sendIpcRequest or @tauri-apps invoke; locate insertion point after initCrashBanner / first paint)
    - tauri/ui/src/ipc/client.ts (current — verify the exact sendIpcRequest signature + timeout option)
    - tauri/ui/src/ipc/messages.ts (current — verify whether `settings.get` accepts a `field` parameter or only returns full blob)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Shared Pattern 5 — Perf fallback runtime wiring"
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md lines 723–747 (TypeScript wiring pseudocode)
    - tauri/ui/tests/session.tokens.test.ts (Wave 0 created; currently describe.skip)
  </read_first>
  <action>
**cohost.ts** — Migrate 23 hardcoded rgba (mostly `rgba(255,138,61,*)` amber → `--amber-*` tokens). Adopt mock §AI-line pattern: each `.ai-line` is `border-left: 1px solid var(--amber-22)` + `padding-left: var(--sp-4)`. `.who` label 9px `var(--type-display)` "wdth" 85 "wght" 500 `var(--amber)` UPPERCASE 0.22em with `text-shadow: 0 0 4px var(--amber-65)` and a small `::before` animated dot (4px circle, `var(--amber)`, `box-shadow: var(--glow-faint)`, animation: `liveBlink 1.4s ease-in-out infinite`). Body text: `var(--silk)` 14px `var(--type-body)` "wdth" 100 "wght" 400. Timestamp (if present): `var(--type-mono)` tabular-nums 11px `var(--silk-40)`.

**phase-tape.ts** — Migrate 18 hardcoded rgba. Segment grid: small horizontal strip with N segments. Segment fill: `var(--silk-22)` (passed) / `var(--amber-40)` (current) / `var(--glass-3)` (upcoming). NOT a hero — fits between time-hero and meters at `--sp-3` margin. Border-radius `var(--rad-sm)` per segment.

**event-ribbon.ts** — MIDI ribbon. Same `.ai-line` glass-tile pattern as cohost.ts BUT `.who` label is "midi · {channel} · {event}" in 9px `var(--silk-40)` (NOT amber per accent-reservation). Body text describes the event in `var(--silk-65)`.

**panel.ts** — Read-only audit. If grep finds ANY legacy ref in this file, fix it; if it's already on v5 (expected — it's the canonical analog), no change. Either way, log the file in <files> to ensure CI re-check coverage.

**main.ts** — Insert boot-time perf-blur preference read per PATTERNS.md Shared Pattern 5. Use `sendIpcRequest({ type: "settings.get" })` (verify the existing signature) + 2000ms timeout. If `value.performance?.lighter_blur === true`, set `document.documentElement.setAttribute("data-blur-perf", "on")`. Catch errors and log a warning — default-off blur is the safe path. Insert AFTER the existing `initCrashBanner()` call (or wherever first-paint setup happens — verify by reading the file).

**session.tokens.test.ts** — Remove the `describe.skip(...)` wrapper. The spec imports `SessionLayout` + `Meter` (or whatever the meter export name is) + `Titlebar` + `Cohost` + `DropChip` and asserts `containsLegacyToken(rendered)` is false for each. Run the spec — MUST pass green.

After all edits, `bash scripts/check_v5_migration.sh --surface=session --strict` MUST exit 0.
  </action>
  <verify>
    <automated>cd tauri/ui && npm run test -- session.tokens.test.ts --run --reporter=dot; cd /Users/ozai/projects/dj-set-ai && bash scripts/check_v5_migration.sh --surface=session --strict && bash scripts/check_v5_fonts.sh --surface=session --strict && grep -F "data-blur-perf" tauri/ui/src/main.ts && cd tauri/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>cohost / phase-tape / event-ribbon / panel-passthrough migrated. main.ts boots with data-blur-perf attribute set when settings.performance.lighter_blur is true. session.tokens.test.ts green (no skip). --strict gate exits 0 for session surface. --strict fonts gate exits 0 for session surface (zero Workbench/DM Mono/DSEG7/Caveat refs).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 14-03-03: Critique loop cycle 1 — ui-checker + ui-auditor + Kaan side-by-side screenshot + perf-toggle verification (CSS-only path)</name>
  <what-built>Session surface fully migrated to v5 primitives (13 files). Border-anim on SessionLayout. tokens.css ships the perf-fallback block. main.ts boots with data-blur-perf preference. vitest green.</what-built>
  <how-to-verify>
    1. Kaan-side: `cd tauri/ui && npm run tauri dev` — open the live session (boot must reach session — use `__vibemixDev.advanceTo('session')` if wizard hasn't been bypassed in dev)
    2. Verify visually: meters animate at 30Hz with the breathing amber gradient, AI transcript renders with amber-22 left border + amber .who label + animated dot, phase tape is faint segments NOT a hero element, MIDI ribbon's .who label is silk-40 NOT amber, status bar has 4 LED dots in --led-ok/warn/fault, timecode tabular-nums 64px is recessed in --glass-3
    3. Open DevTools console: run `document.documentElement.setAttribute('data-blur-perf', 'on')` — visually confirm panels' blur drops to 16px (less ambient color bleed), then `document.documentElement.removeAttribute('data-blur-perf')` — full blur restores
    4. macOS System Settings → Accessibility → "Reduce motion" ON → reload — visually confirm the border-anim freezes (opacity 0.6, no rotation) and blurs drop. Toggle OFF — animation resumes.
    5. Run `Skill(skill="gsd-ui-checker", args="14 --surface=session")` — attach output ref to 14-POLISH-LOG.md row "session | 1 | <ref> | — | — | 🟡"
    6. Run `Skill(skill="gsd-ui-auditor", args="14 --surface=session")` — three audits. Attach output ref.
    7. Screenshot live session at 1440×900; place side-by-side with `mocks/vibemix-direction-final.html` §01 left column. Attach to 14-POLISH-LOG.md.
    8. If ui-checker has findings: orchestrator may dispatch cycle 2 follow-up. If green: cycle 1 closes session surface. Cycle 3 unresolved = polish-debt + escalate to Kaan.
    9. Mark macOS row in "Perf Verification" table: defaultBlur ✅, data-blur-perf ✅, prefers-reduced-motion ✅. Windows row stays ⬜ — Wave 5 / Phase 20 own that machine.
  </how-to-verify>
  <resume-signal>Type "session cycle 1 green" to mark surface complete, OR "session cycle 2 needed: <findings>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Webview render → GPU compositor | backdrop-filter blur runs on compositor; perf fallback drops cost from blur(32px) saturate(140%) to blur(16px) |
| Settings IPC → boot read | main.ts reads via sendIpcRequest; failure mode = default off |

## STRIDE Threat Register (security_enforcement = ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Availability (DoS via blur cost) | backdrop-filter on multiple glass panels | mitigate | --blur-glass-perf token + prefers-reduced-motion override + html[data-blur-perf="on"] runtime escape hatch all ship in this plan (CONTEXT Area 3) |
| T-14-03-02 | Capability allowlist drift (Elevation) | Tauri capability config | accept | Presentation-only; existing settings IPC reuse, no new capability surface |
| T-14-03-03 | DoS via failing settings read at boot | main.ts perf preference fetch | mitigate | Wrap in try/catch; default-off is safe path; never block boot on missing field |
</threat_model>

<verification>
- `bash scripts/check_v5_migration.sh --surface=session --strict` exits 0
- `bash scripts/check_v5_fonts.sh --surface=session --strict` exits 0
- `cd tauri/ui && npm run test -- session.tokens.test.ts --run` exits 0 (no skip)
- `cd tauri/ui && npm run build` exits 0
- `tauri/ui/src/session/SessionLayout.ts` contains `borderAnim.className = "border-anim"` as first child append
- `tauri/ui/src/tokens.css` contains `@media (prefers-reduced-motion: reduce)` block AND `html[data-blur-perf="on"]` block
- `tauri/ui/src/main.ts` boot() sets `data-blur-perf` attribute when setting is on
- 14-POLISH-LOG.md row "session | 1 | <ref> | <ref> | <SHA> | ✅" recorded (or escalation note if cycle 3 still red)
- 14-POLISH-LOG.md Perf Verification macOS row updated with three ✅
</verification>

<success_criteria>
- 13 session files migrated to v5 primitives (12 modified + panel.ts re-audit)
- Zero legacy shim refs in `tauri/ui/src/session/` (per --strict gate)
- Zero forbidden font-family declarations in `tauri/ui/src/session/`
- tokens.css contains the perf-fallback block (both @media and html[data-blur-perf="on"])
- main.ts boots with data-blur-perf attribute correctly applied
- vitest session.tokens.test.ts green (no skip)
- SessionLayout renders with .border-anim as first child (verified by jsdom)
- gsd-ui-checker zero findings on session surface
- gsd-ui-auditor 3 audits green
- Perf toggle verified on Kaan's M-series rig (default + data-blur-perf="on" + prefers-reduced-motion)
- Side-by-side screenshot pair attached to 14-POLISH-LOG.md
- Cycle count ≤3
</success_criteria>

<output>
After completion, create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-03-SUMMARY.md` documenting:
- Per-file before/after legacy-ref + hardcoded-rgba counts (13 files)
- Cycle count for session surface (1, 2, or 3)
- ui-checker + ui-auditor output refs + fix commit SHAs
- Perf toggle test results on Kaan's M-series Mac (all three states)
- Screenshot pair (live session 1440×900 vs mock §01 left column)
- Any polish debt escalated
