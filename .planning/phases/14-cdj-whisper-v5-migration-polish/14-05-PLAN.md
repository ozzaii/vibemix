---
phase: 14-cdj-whisper-v5-migration-polish
plan: 05
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03", "14-04"]
files_modified:
  - tauri/ui/mascot.html
  - tauri/ui/src/mascot/index.ts
  - tauri/ui/src/mascot/chrome.css
  - tauri/ui/tests/mascot.chrome.test.ts
  - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
autonomous: false
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-05
tags:
  - ui
  - migration
  - mascot
  - phase-13-interop

must_haves:
  truths:
    - "tauri/ui/mascot.html has a .mascot-window wrapper with <div class='border-anim slow rev' aria-hidden='true'> as the FIRST child of the wrapper (32s reverse sweep — de-sync from session's 22s forward sweep per CONTEXT Area 3)"
    - "The mascot overlay window's chrome reads v5 primitives only (--glass-3, --blur-glass-display, --glass-edge, --glass-top, --silk-*, --amber, --rad-md 8px) — zero legacy shim refs in tauri/ui/src/mascot/"
    - "mascot.html links tokens.css via <link> (so the .border-anim utility + v5 vars cascade in) AND overrides html/body background to transparent !important so Phase 13's Three.js overlay still composites over the desktop"
    - "mascot/index.ts resolveCssColor() calls migrate from --phosphor / --phosphor-soft / --ink-deep to --amber / --silk / --silk-40 with v5 hex fallbacks (#ff8a3d / #d6cfc7 / #3d424c — only 3 hex literals outside tokens.css are permitted, lines 403/407/410)"
    - "Three.js scene itself is UNTOUCHED — only the overlay window's HTML chrome wrapper is added; renderer.ts not modified"
    - "Top label 'OVERLAY · STICKY · ALL SPACES · {W}×{H}' + bottom state caption 'idle · bop-to-beat' render in 9px var(--type-mono) var(--silk-40)"
    - "Mascot eyes amber (--glow-strong) stay the brightest pixels in the overlay rect regardless of mood"
    - "vitest mascot.chrome.test.ts passes green (no skip)"
    - "gsd-ui-checker zero findings on mascot surface AND gsd-ui-auditor 3 audits green — recorded in 14-POLISH-LOG.md"
  artifacts:
    - path: "tauri/ui/mascot.html"
      provides: "Mascot overlay HTML with v5 chrome wrapper + border-anim.slow.rev + tokens.css link + transparent body override"
      contains: "<div class=\"mascot-window\">, <div class=\"border-anim slow rev\""
    - path: "tauri/ui/src/mascot/chrome.css"
      provides: "NEW file — mascot-window CSS lifted from session/components/panel.ts adapted for transparent canvas wrapper"
      contains: ".mascot-window, --glass-3, --blur-glass-display, --glass-edge, border-radius: 8px"
    - path: "tauri/ui/src/mascot/index.ts"
      provides: "Bootstrap with v5 token migration in resolveCssColor + top/bottom caption mounts"
      contains: "resolveCssColor(\"--amber\", \"#ff8a3d\"), resolveCssColor(\"--silk\", \"#d6cfc7\"), resolveCssColor(\"--silk-40\", \"#3d424c\")"
  key_links:
    - from: "tauri/ui/mascot.html"
      to: ".border-anim.slow.rev utility in tokens.css:330–331"
      via: "div.border-anim.slow.rev as first child of .mascot-window"
      pattern: "border-anim slow rev"
    - from: "tauri/ui/mascot.html"
      to: "tokens.css var stack"
      via: "<link rel=\"stylesheet\" href=\"/src/tokens.css\"> + transparent body override"
      pattern: "tokens.css|transparent !important"
    - from: "tauri/ui/src/mascot/index.ts handleMoodChange"
      to: "v5 amber/silk/silk-40 tokens"
      via: "resolveCssColor with v5 token names + v5 hex fallbacks"
      pattern: "resolveCssColor\\([\"']--(amber|silk|silk-40)"
---

<objective>
Wrap the Phase 13 mascot overlay window in v5 glass chrome with the animated-border surround (32s reverse sweep), migrate the 6 legacy token refs + 3 hardcoded hex literals in `mascot/index.ts` resolveCssColor() to v5 tokens (per PATTERNS.md Surface 4 + RESEARCH.md mascot audit lines 174–183), link tokens.css into mascot.html with a transparent-body override, and run the critique loop until the mascot surface passes.

Purpose: Mascot last per CONTEXT Area 1 (Phase 13 just shipped the renderer + Meshy material fix at commit 2b608b6 — refactoring now is lowest-risk). The 3D scene itself is untouched — only the overlay window's HTML chrome wrapper is added for the first time (PATTERNS.md "there is no already-v5 mascot frame in code"; analog is `session/components/panel.ts` adapted for a transparent canvas wrapper).

Output: mascot.html gains a .mascot-window wrapper with border-anim.slow.rev + tokens.css link + transparent body override; mascot/index.ts resolveCssColor() reads v5 token names; new src/mascot/chrome.css owns the wrapper styling; vitest mascot.chrome.test.ts unskipped + green; polish log row populated for mascot cycles 1-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md
@mocks/vibemix-direction-final.html
@tauri/ui/src/tokens.css
@tauri/ui/src/session/components/panel.ts
@tauri/ui/mascot.html
@tauri/ui/src/mascot/index.ts

<interfaces>
GOLD-STANDARD ANALOG — `tauri/ui/src/session/components/panel.ts:27–40` (canonical glass-panel anatomy adapted here for a transparent canvas wrapper)

The mascot.html structural change (PATTERNS.md Surface 4 — line 466 onwards):

CURRENT (`tauri/ui/mascot.html:51–54`):
  <body style="background: transparent; margin: 0; padding: 0; overflow: hidden;">
    <canvas id="mascot-canvas" aria-hidden="true"></canvas>
    <script type="module" src="/src/mascot/index.ts"></script>
  </body>

AFTER (wrap canvas in v5 chrome with .border-anim.slow.rev):
  <body style="background: transparent; margin: 0; padding: 0; overflow: hidden;">
    <div class="mascot-window">
      <div class="border-anim slow rev" aria-hidden="true"></div>
      <div class="mascot-window__top-label">
        <span class="mascot-window__caption">OVERLAY · STICKY · ALL SPACES · 320×420</span>
      </div>
      <canvas id="mascot-canvas" aria-hidden="true"></canvas>
      <div class="mascot-window__state-caption" aria-hidden="true">idle · bop-to-beat</div>
    </div>
    <script type="module" src="/src/mascot/index.ts"></script>
  </body>

CRITICAL — tokens.css is NOT loaded by mascot.html currently. PATTERNS.md verified this. Options:
(a) Add `<link rel="stylesheet" href="/src/tokens.css">` in <head>
(b) Inline the necessary v5 CSS vars into mascot.html's <style>

RECOMMENDED: (a) — keeps mascot chrome composing through the same cascade. BUT — adding tokens.css will set `body { background-color: var(--void); }` (tokens.css:260) which **breaks the transparent overlay** required for Phase 13 desktop compositing. MANDATORY workaround: after linking tokens.css, override in mascot.html's inline <style>:

  html, body { background: transparent !important; background-image: none !important; }

The `!important` is justified because the overlay window must composite over the desktop (Phase 13 always-on-top, transparent-bg invariant). Also disable the body radial-gradients (PATTERNS.md note — they would render over the desktop, breaking transparency).

ALSO CRITICAL — body::before film-grain SVG at tokens.css:274–285 sets `position: fixed; inset: 0; z-index: 9999` which would also block transparency. Disable in mascot.html with: `body::before { display: none !important; }`.

NEW FILE `tauri/ui/src/mascot/chrome.css` (lifted from PATTERNS.md Surface 4 lines 493–535; imported by mascot/index.ts):

  .mascot-window {
    position: relative;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    border-radius: 8px;                                /* 8px per UI-SPEC §Mascot — vs 6px elsewhere */
    background: var(--glass-3);                        /* recessed; minimal blur per Pitfall 1 */
    backdrop-filter: var(--blur-glass-display);
    -webkit-backdrop-filter: var(--blur-glass-display);
    border: 1px solid var(--glass-edge);
    box-shadow:
      inset 0 1px 0 var(--glass-top),
      inset 0 -1px 0 rgba(255, 138, 61, 0.06),         /* faint amber undertone on bottom edge */
      0 32px 64px rgba(0, 0, 0, 0.85),
      0 0 0 1px rgba(255, 255, 255, 0.018);
  }
  .mascot-window > * { position: relative; z-index: 1; }
  .mascot-window > .border-anim { z-index: 4; }       /* keep utility's z-index */
  .mascot-window__top-label {
    position: absolute;
    top: var(--sp-2);
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
  }
  .mascot-window__caption {
    font-family: var(--type-mono);
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--silk-40);
  }
  .mascot-window__state-caption {
    position: absolute;
    bottom: var(--sp-2);
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    font-family: var(--type-mono);
    font-size: 9px;
    letter-spacing: 0.18em;
    color: var(--silk-40);
  }
  #mascot-canvas {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 2;
  }

MASCOT INDEX.TS resolveCssColor MIGRATION (PATTERNS.md Surface 4 lines 542–612; RESEARCH.md line 177–183):

The 3 hardcoded hex literals at lines 376/403/407/410 are the ONLY 3 hex literals outside tokens.css. Rewrite as:

  CURRENT (tauri/ui/src/mascot/index.ts):
    // Line 376 comment:
    "The fallback amber hex is the canonical --phosphor token VALUE (#ffa12e) from tokens.css — duplicated here only as a literal-string default for THREE.Color when CSS resolution fails."
    // Line 403:
    color = resolveCssColor("--phosphor", "#ffa12e");
    // Line 407:
    color = resolveCssColor("--phosphor-soft", "#efe6d6");
    // Line 410:
    color = resolveCssColor("--ink-deep", "#3d424c");

  AFTER:
    // Line 376 comment rewrite: drop the stale --phosphor reference. New text:
    "Resolve a CSS variable to a THREE.Color. Falls back to v5 amber if the var is missing/empty. tokens.css remains the single source-of-truth at the CSS layer; this duplicates the hex only as a literal-string default for THREE.Color when CSS resolution fails."
    // Line 403:
    color = resolveCssColor("--amber", "#ff8a3d");
    // Line 407 (teacher mood — cream tint, plan-authorised inline fallback):
    color = resolveCssColor("--silk", "#d6cfc7");
    // Line 410 (coach mood — slate; --silk-40 reads as desaturated coach tint):
    color = resolveCssColor("--silk-40", "#3d424c");

The 3 hex fallbacks remain — they're load-bearing for Three.js Color() construction when CSS resolution fails. They're the only hex literals permitted outside tokens.css; the audit must verify NO new hex literals appear.

ALSO in mascot/index.ts:
- Add `import "./chrome.css";` at the top so Vite bundles the wrapper styling
- Wire the top label dimensions: read `window.innerWidth × window.innerHeight` at mount + on resize; write into `.mascot-window__caption` text content
- Wire the bottom state caption: subscribe to the existing mascot state-machine (Phase 13 — verify the state machine emits state names like "idle.bop-to-beat") and write into `.mascot-window__state-caption` text content (lowercase, "·" separator per UI-SPEC Copywriting Contract row "Mascot state label")

ACCENT RESERVATION SANITY (UI-SPEC §Color §20%):
Mascot surface maps to: 10 (animated border sweep — added by this plan), 13 (Mascot eyes — already in Phase 13 renderer; verify --glow-strong is the brightest pixel composition). NO amber elsewhere in chrome — the top/bottom captions are --silk-40 (NOT amber per the silkscreen-label convention).

MOOD-SWAP CHROME INVARIANT (UI-SPEC Surface 4):
Chrome color stays IDENTICAL across moods (hype-man / teacher / coach). Mood-swap changes are: (a) Gemini TTS voice (no visual), (b) animation clip pool (motion-visual on Three.js scene), (c) prompt vocabulary (no visual), (d) brief particle puff effect (mascot-internal). Chrome (border-anim + amber undertone + caption color) does NOT tint with mood. Audit gate.

TAURI WEBVIEW2 TRANSPARENCY NOTE (RESEARCH context):
Tauri WebView2 transparent-window backdrop bug (tauri/tauri#10064) requires real Windows hardware to repro. macOS doesn't show the bug. CONTEXT Area 3 perf rehearsal on a Windows non-dev machine is a checkpoint task — Wave 5 owns the actual repro check OR borrows from Phase 20 fresh-machine rehearsal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-05-01: Add .mascot-window chrome to mascot.html + create src/mascot/chrome.css + migrate mascot/index.ts resolveCssColor</name>
  <files>tauri/ui/mascot.html, tauri/ui/src/mascot/chrome.css, tauri/ui/src/mascot/index.ts</files>
  <read_first>
    - tauri/ui/mascot.html (current — 55 lines; verify the existing inline <style> block + body element)
    - tauri/ui/src/mascot/index.ts (current — 490 lines; locate resolveCssColor function + handleMoodChange function + state-machine subscription + the mount/init function for the canvas)
    - tauri/ui/src/session/components/panel.ts (canonical glass-panel anatomy — DO NOT MODIFY, lift CSS from)
    - tauri/ui/src/tokens.css lines 259–270 (body radial-gradient stack — confirms what mascot.html must override) and lines 274–285 (body::before film-grain — confirms what mascot.html must disable)
    - tauri/ui/src/mascot/renderer.ts (Phase 13 — DO NOT MODIFY; verify by skim that the Three.js scene boots from canvas element with id "mascot-canvas" and that mount logic doesn't assume any particular parent)
    - tauri/ui/src/mascot/state-machine.ts (verify state-emitting interface — find the existing API for subscribing to current animation state name)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Surface 4 — Mascot Overlay Window Chrome (Wave 4)"
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Surface 4 — Mascot Overlay Window Chrome"
  </read_first>
  <action>
**mascot.html** — Edit the existing file (55 lines). Structural changes:
1. In `<head>`, add `<link rel="stylesheet" href="/src/tokens.css">` (verify the existing src/ path style; the wizard's index.html may use a different path style — match it)
2. In the existing inline `<style>` block, add the transparent-overlay overrides:
   - `html, body { background: transparent !important; background-image: none !important; }`
   - `body::before { display: none !important; }`
3. Replace `<body>` content (currently `<canvas id="mascot-canvas">` + script) with the new wrapper markup:
   - `<div class="mascot-window">`
   - First child: `<div class="border-anim slow rev" aria-hidden="true"></div>`
   - Second child: `<div class="mascot-window__top-label"><span class="mascot-window__caption">OVERLAY · STICKY · ALL SPACES · 320×420</span></div>` (the 320×420 is initial — runtime updates it from index.ts)
   - Third child: `<canvas id="mascot-canvas" aria-hidden="true"></canvas>` (MOVED from direct body child into wrapper)
   - Fourth child: `<div class="mascot-window__state-caption" aria-hidden="true">idle · bop-to-beat</div>` (initial — runtime updates)
   - `</div>`
4. Keep the `<script type="module" src="/src/mascot/index.ts"></script>` at the body bottom — it still mounts the renderer onto `#mascot-canvas`

**chrome.css** — CREATE NEW FILE per <interfaces> CSS block verbatim. Located at `tauri/ui/src/mascot/chrome.css`. Use `@import "../tokens.css";` at the top is NOT required (mascot.html links tokens.css directly; if you prefer a Vite-bundled import path you can also `@import` here — it's idempotent). The file is pure CSS, no TS.

**mascot/index.ts** — Three changes:
1. Add `import "./chrome.css";` at the top imports
2. Migrate resolveCssColor() calls per <interfaces> table:
   - Line 376 comment: rewrite to remove stale `--phosphor`/`#ffa12e` text
   - Line 403: `resolveCssColor("--phosphor", "#ffa12e")` → `resolveCssColor("--amber", "#ff8a3d")`
   - Line 407: `resolveCssColor("--phosphor-soft", "#efe6d6")` → `resolveCssColor("--silk", "#d6cfc7")`
   - Line 410: `resolveCssColor("--ink-deep", "#3d424c")` → `resolveCssColor("--silk-40", "#3d424c")`
3. Add caption wiring (NEW code in the existing init/mount function):
   - On mount + window.addEventListener("resize"): write `${innerWidth}×${innerHeight}` into `.mascot-window__caption` text content (template: `OVERLAY · STICKY · ALL SPACES · ${W}×${H}`)
   - Subscribe to the state-machine's "state changed" event (verify the API in state-machine.ts) and write `${categoryLowercase} · ${animationNameLowercase}` into `.mascot-window__state-caption` text content (e.g. "idle · bop-to-beat", "talk · greeting", "dance · drop-hard")
   - Use `querySelector` defensively — if the element doesn't exist (mascot.html doesn't have the wrapper yet on some dev path), silently bail with a console.debug

After edits:
- `bash scripts/check_v5_migration.sh --surface=mascot --warn-only` shows substantial drop from baseline 6 refs
- `cd tauri/ui && npm run build` exits 0 (Vite + tsc green)
- `cd tauri/ui && npm run dev` launches without console errors on first paint
  </action>
  <verify>
    <automated>bash scripts/check_v5_migration.sh --surface=mascot --warn-only; grep -F "border-anim slow rev" tauri/ui/mascot.html; grep -F "mascot-window" tauri/ui/mascot.html; grep -F "background: transparent !important" tauri/ui/mascot.html; test -f tauri/ui/src/mascot/chrome.css; grep -F "var(--glass-3)" tauri/ui/src/mascot/chrome.css; grep -F "border-radius: 8px" tauri/ui/src/mascot/chrome.css; grep -F 'resolveCssColor("--amber"' tauri/ui/src/mascot/index.ts; grep -F 'resolveCssColor("--silk"' tauri/ui/src/mascot/index.ts; grep -F 'resolveCssColor("--silk-40"' tauri/ui/src/mascot/index.ts; ! grep -F "phosphor" tauri/ui/src/mascot/index.ts; ! grep -F '#ffa12e' tauri/ui/src/mascot/index.ts; cd tauri/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>mascot.html has .mascot-window wrapper with border-anim.slow.rev first child + tokens.css link + transparent body override + body::before disabled. chrome.css exists with the .mascot-window + caption styling. mascot/index.ts: 3 resolveCssColor calls migrated, "phosphor" string absent, `#ffa12e` hex absent (3 v5 hex literals at lines 403/407/410 are the ONLY hex literals outside tokens.css). `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 14-05-02: Unskip mascot.chrome.test.ts + verify chrome composes correctly without leaking the Three.js scene</name>
  <files>tauri/ui/tests/mascot.chrome.test.ts</files>
  <read_first>
    - tauri/ui/tests/mascot.chrome.test.ts (Wave 0 created; currently describe.skip — fixture should parse mascot.html via fs.readFileSync + jsdom)
    - tauri/ui/mascot.html (just edited — verify the structural test assumptions are accurate)
    - tauri/ui/src/mascot/chrome.css (just created — verify the test imports + asserts class names match)
    - tauri/ui/src/mascot/index.ts (just edited — verify the test can import without throwing)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Surface 4 — Mascot Overlay Window Chrome"
  </read_first>
  <action>
**mascot.chrome.test.ts** — Remove the `describe.skip(...)` wrapper. The spec should:
1. `fs.readFileSync("tauri/ui/mascot.html", "utf8")` and parse via jsdom's DOMParser or JSDOM constructor
2. Assert `document.querySelector(".mascot-window")` exists
3. Assert `document.querySelector(".mascot-window > .border-anim.slow.rev")` exists AND is the first child of `.mascot-window` (use `firstElementChild.matches(".border-anim.slow.rev")`)
4. Assert the mascot.html `<head>` contains a `<link rel="stylesheet"` reference to `tokens.css`
5. Assert the inline `<style>` block contains `background: transparent !important`
6. Assert `containsLegacyToken(document.documentElement.outerHTML)` is false (no --phosphor/--brushed-/--bezel-/etc. in inline styles)
7. Import the chrome.css file as a string (fs.readFileSync) and assert it contains `var(--glass-3)`, `var(--blur-glass-display)`, `var(--glass-edge)`, and `border-radius: 8px` — confirming the wrapper anatomy matches PATTERNS.md
8. Optionally: dynamic `import("../src/mascot/index.ts")` — must NOT throw (verifies the chrome.css import resolves and resolveCssColor migration didn't break anything; mock window for jsdom)

Run the spec — MUST pass green.

After edit, the entire vitest suite must stay green:
- `cd tauri/ui && npm run test --reporter=dot` exits 0
- Specifically `mascot.chrome.test.ts` no longer has `describe.skip`

Also run `bash scripts/check_v5_migration.sh --surface=mascot --strict` — MUST exit 0 (zero legacy refs in mascot surface).
  </action>
  <verify>
    <automated>cd tauri/ui && npm run test -- mascot.chrome.test.ts --run --reporter=dot; cd /Users/ozai/projects/dj-set-ai && bash scripts/check_v5_migration.sh --surface=mascot --strict && bash scripts/check_v5_fonts.sh --surface=mascot --strict; cd tauri/ui && npm run test --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <done>mascot.chrome.test.ts passes green (no skip). --strict gates exit 0 for mascot surface. Full vitest suite stays green (no other tests broken by the chrome.css import or mascot.html structural change).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 14-05-03: Critique loop cycle 1 — ui-checker + ui-auditor + Kaan side-by-side + mood-swap chrome invariant verification</name>
  <what-built>Mascot overlay window has v5 chrome wrapper with border-anim.slow.rev (32s reverse — de-sync from session sweep). Phase 13 Three.js scene unchanged. resolveCssColor reads v5 tokens. Top/bottom captions render. Transparent overlay still composites over desktop. vitest green.</what-built>
  <how-to-verify>
    1. Kaan-side: `cd tauri/ui && npm run tauri dev` — launch the app; the mascot overlay window should appear (Phase 13 behavior — separate Tauri window)
    2. Visual check: the overlay window has a faint glass-3 rectangle with rounded corners (8px), a subtle amber undertone on the bottom edge, and the amber border-anim sweeping the perimeter in REVERSE direction at 32s period (slower than session's 22s forward)
    3. Top label reads "OVERLAY · STICKY · ALL SPACES · {W}×{H}" in 9px JetBrains Mono var(--silk-40); bottom caption reads the current mascot state in same style
    4. Drag the overlay window — chrome + border-anim move with it; Three.js scene renders smoothly
    5. Resize the overlay (if Tauri allows runtime resize) — top label W×H updates dynamically
    6. Mood swap chrome invariance test: open settings drawer → mascot group → click "hype-man" / "teacher" / "coach" pills in sequence. Verify the chrome border-anim color + glass-3 background + bottom-edge amber undertone remain IDENTICAL across all three moods. Only the Three.js scene (animation + lighting) changes. Mascot eyes (--amber + --glow-strong) stay the brightest pixels in the rect.
    7. Verify the overlay is STILL transparent: drag a window behind it; you should see the desktop / other windows through the corners (8px radius mask) and through the body radial wash should NOT appear (we disabled body radial-gradients + body::before film-grain in mascot.html)
    8. Run `Skill(skill="gsd-ui-checker", args="14 --surface=mascot")` — attach to 14-POLISH-LOG.md row "mascot | 1 | <ref> | — | — | 🟡"
    9. Run `Skill(skill="gsd-ui-auditor", args="14 --surface=mascot")` — three audits. Attach.
    10. Screenshot the overlay at native resolution; place side-by-side with mocks/vibemix-direction-final.html §01 right column (.mascot-window). Attach to 14-POLISH-LOG.md.
    11. If ui-checker has findings: cycle 2. Cycle 3 unresolved = polish debt + escalate.
    12. CONTEXT Area 3 — perf rehearsal: prefers-reduced-motion ON should freeze the mascot border-anim too; verify.
  </how-to-verify>
  <resume-signal>Type "mascot cycle 1 green" to mark surface complete, OR "mascot cycle 2 needed: <findings>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri overlay webview → desktop composition | Transparent overlay window; mascot.html must NOT introduce opaque backdrops; phase-13 invariant |
| Three.js canvas → resolveCssColor | CSS-variable lookup with hex fallback; 3 hex literals are the ONLY hex outside tokens.css |

## STRIDE Threat Register (security_enforcement = ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Availability (overlay transparency broken on Windows) | Tauri WebView2 transparent-window backdrop bug (#10064) | mitigate | Wave 5 includes a Windows non-dev rehearsal (borrowed from Phase 20 if needed); CONTEXT Area 3 explicit perf rehearsal scope |
| T-14-05-02 | Capability allowlist drift (Elevation) | Tauri overlay window config | accept | Phase 13 owns the overlay-window capability; this plan does NOT modify Tauri capability config |
| T-14-05-03 | Information disclosure | tokens.css link from mascot.html | accept | tokens.css contains no secrets, only design tokens; same file already linked by all other UI surfaces |
</threat_model>

<verification>
- `bash scripts/check_v5_migration.sh --surface=mascot --strict` exits 0
- `bash scripts/check_v5_fonts.sh --surface=mascot --strict` exits 0
- `cd tauri/ui && npm run test -- mascot.chrome.test.ts --run` exits 0 (no skip)
- `cd tauri/ui && npm run test` exits 0 (full suite stays green)
- `cd tauri/ui && npm run build` exits 0
- `tauri/ui/mascot.html` contains `<div class="mascot-window">` + `<div class="border-anim slow rev"` + `<link rel="stylesheet" href="/src/tokens.css">` + `background: transparent !important`
- `tauri/ui/src/mascot/chrome.css` exists with `.mascot-window`, `var(--glass-3)`, `border-radius: 8px`
- `tauri/ui/src/mascot/index.ts` has zero `phosphor` strings, zero `#ffa12e` hex, and three resolveCssColor calls with v5 token names
- Mascot overlay renders chrome correctly on Kaan's M-series Mac with transparent body
- Mood swap (hype-man ↔ teacher ↔ coach) leaves chrome color identical
- gsd-ui-checker + gsd-ui-auditor recorded in 14-POLISH-LOG.md
</verification>

<success_criteria>
- mascot.html wrapped in .mascot-window with border-anim.slow.rev first child + tokens.css link + transparent body override
- New chrome.css owns the wrapper styling; lifted from session/components/panel.ts adapted for transparent canvas wrapper
- 3 resolveCssColor calls migrated to v5 tokens with v5 hex fallbacks
- Zero legacy shim refs in `tauri/ui/src/mascot/` (per --strict gate)
- Zero forbidden font-family declarations in `tauri/ui/src/mascot/`
- Zero "phosphor" strings + zero stale `#ffa12e` hex in mascot/index.ts
- The 3 v5 hex literals (#ff8a3d, #d6cfc7, #3d424c at lines 403/407/410) are the ONLY hex literals outside tokens.css repo-wide
- Three.js scene + renderer.ts UNTOUCHED
- vitest mascot.chrome.test.ts green (no skip)
- Mood-swap chrome invariance verified (Kaan-side)
- gsd-ui-checker zero findings + gsd-ui-auditor 3 audits green
- Side-by-side screenshot pair attached
- Cycle count ≤3
</success_criteria>

<output>
After completion, create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-05-SUMMARY.md` documenting:
- mascot.html structural diff (5 changes: link, transparent override, body::before disable, wrapper markup, captions)
- chrome.css full contents
- mascot/index.ts resolveCssColor migration (3 lines)
- Cycle count for mascot surface
- ui-checker + ui-auditor output refs + fix commit SHAs
- Mood-swap chrome invariance test results (3 modes × 1 invariant check)
- Screenshot pair (live overlay vs mock §01 right column)
- Tauri WebView2 transparency status on Kaan's Mac (Windows rehearsal noted as deferred to Wave 5)
- Any polish debt escalated
</output>
