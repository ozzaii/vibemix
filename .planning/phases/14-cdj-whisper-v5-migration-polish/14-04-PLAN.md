---
phase: 14-cdj-whisper-v5-migration-polish
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - tauri/ui/src/settings/SettingsDrawer.ts
  - tauri/ui/src/settings/state.ts
  - tauri/ui/src/settings/components/retention-slider.ts
  - tauri/ui/src/settings/components/mascot-group.ts
  - tauri/ui/src/settings/components/hotkey-capture.ts
  - tauri/ui/src/settings/components/confirm-dialog.ts
  - tauri/ui/src/settings/components/performance-group.ts
  - tauri/ui/src/ipc/messages.schema.json
  - tauri/ui/src/ipc/messages.ts
  - src/vibemix/ui_bus/messages.py
  - src/vibemix/runtime/settings.py
  - tauri/ui/tests/settings.tokens.test.ts
  - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
autonomous: false
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
tags:
  - ui
  - migration
  - settings
  - perf

must_haves:
  truths:
    - "Every settings consumer reads v5 primitives directly — zero legacy shim refs in tauri/ui/src/settings/"
    - "SettingsDrawer has <div class='border-anim'> as its first child + overflow: hidden + child z-index 5"
    - "New settings/components/performance-group.ts component renders one row ('lighter blur' toggle) using mascot-group.ts as the analog; off-state = --glass-3 background, on-state = mood-pill amber backlight"
    - "Performance toggle writes performance.lighter_blur via existing settings.set IPC; SettingsApplier persists; toggle applies data-blur-perf attribute immediately"
    - "Settings IPC schema knows about performance.lighter_blur boolean field (Python dataclass + JSON schema + TS codegen agree — count parity check stays at 26 messages, no new message family)"
    - "vitest settings.tokens.test.ts passes green (no skip)"
    - "gsd-ui-checker zero findings on settings surface AND gsd-ui-auditor 3 audits green — recorded in 14-POLISH-LOG.md"
  artifacts:
    - path: "tauri/ui/src/settings/SettingsDrawer.ts"
      provides: "Drawer composer with border-anim first child + overflow: hidden + z-index discipline + Performance group reference"
      contains: "border-anim, overflow: hidden, performance"
    - path: "tauri/ui/src/settings/components/performance-group.ts"
      provides: "NEW component — Performance settings group with 'lighter blur' toggle"
      contains: "renderSettingsGroup, header: 'PERFORMANCE', applyBlurPerfPreference, toggleBlurPerf"
    - path: "tauri/ui/src/settings/state.ts"
      provides: "Extended SettingsState with performance.lighter_blur field; SessionState read-path"
      contains: "performance: { lighter_blur: boolean }"
    - path: "src/vibemix/runtime/settings.py"
      provides: "SettingsApplier dispatch for performance.lighter_blur field write path"
      contains: "performance.lighter_blur"
    - path: "src/vibemix/ui_bus/messages.py"
      provides: "Settings message dataclasses updated with performance struct (JSON-schema-validated)"
      contains: "performance, lighter_blur"
  key_links:
    - from: "tauri/ui/src/settings/components/performance-group.ts toggle"
      to: "<html data-blur-perf attribute"
      via: "applyBlurPerfPreference (immediate) + toggleBlurPerf (await sendIpcRequest settings.set)"
      pattern: "data-blur-perf"
    - from: "tauri/ui/src/ipc/messages.schema.json"
      to: "Python ui_bus mirror + TS codegen"
      via: "performance struct in settings.set payload + npm run check:ipc parity"
      pattern: "performance.*lighter_blur"
    - from: "tauri/ui/src/settings/SettingsDrawer.ts"
      to: ".border-anim utility in tokens.css:302"
      via: "borderAnim.className = 'border-anim' first append"
      pattern: "border-anim"
---

<objective>
Migrate every settings drawer consumer (~5 files, ~15 legacy refs per RESEARCH inventory) from shim aliases to v5 primitives, insert the animated border on SettingsDrawer root, add a NEW Performance settings group with the "lighter blur" toggle that writes through the existing settings IPC, and run the critique loop until the surface passes.

Purpose: Settings drawer is the third surface (CONTEXT Area 1 order) and the only one that adds a new component (Performance group). The Performance toggle wires the user-facing path for the perf-fallback CSS that Wave 2 already shipped — Settings → Performance → "Lighter blur" hot-swaps backdrop-filter blur strength without restart.

Output: 5 settings files migrated to v5 + 1 new component (performance-group.ts) + IPC schema delta for performance.lighter_blur + SettingsApplier write path + vitest settings.tokens.test.ts unskipped + green, polish log rows populated for settings cycles 1-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md
@mocks/vibemix-direction-final.html
@tauri/ui/src/tokens.css
@tauri/ui/src/settings/SettingsDrawer.ts
@tauri/ui/src/settings/components/mascot-group.ts

<interfaces>
GOLD-STANDARD ANALOG — `tauri/ui/src/settings/SettingsDrawer.ts:62-237` is canonical v5 (already on v5 — only audit needed here is adding border-anim, 5 --sp-* legacy refs, overflow: hidden, and z-index discipline):
- background: var(--glass-1) (line 87)
- backdrop-filter: var(--blur-glass) + -webkit- pair (lines 88–89)
- border-left: 1px solid var(--glass-edge) (line 90)
- box-shadow stack with inset 1px 0 0 var(--glass-top) (lines 91–94)
- Header title: Saira variable axes + engraved text-shadow (lines 117–124)
- Amber ::before pre-bullet on header (lines 126–133): uses var(--amber) + composite glow
- Button anatomy (lines 198–222): .vmx-settings-drawer__btn — idle/hover/on/pressed states with linear-gradient(180deg, rgba(255,138,61,0.09), rgba(255,138,61,0.025)) /* mock-verbatim */

GOLD-STANDARD ANALOG — `tauri/ui/src/settings/components/mascot-group.ts`:
- Same group-factory shape as Performance group will use
- Lifts toggle CSS from SettingsDrawer.ts button anatomy
- Default off-state: var(--glass-3) background
- On-state: amber-backlit per .mood-btn.on anatomy

BORDER-ANIM INSERTION SITE (PATTERNS.md Surface 3 — site at SettingsDrawer.ts:284–289):

  // CURRENT (lines 284–289):
  const drawer = document.createElement("aside");
  drawer.className = "vmx-settings-drawer";
  drawer.dataset.open = "false";
  drawer.setAttribute("aria-label", "settings");
  drawer.setAttribute("role", "complementary");

  // AFTER (insert border-anim FIRST child before header):
  const drawer = document.createElement("aside");
  drawer.className = "vmx-settings-drawer";
  drawer.dataset.open = "false";
  drawer.setAttribute("aria-label", "settings");
  drawer.setAttribute("role", "complementary");
  const borderAnim = document.createElement("div");
  borderAnim.className = "border-anim";
  borderAnim.setAttribute("aria-hidden", "true");
  drawer.append(borderAnim);

CSS adjustment needed at SettingsDrawer.ts:78–98 (per PATTERNS.md):
- Confirm `position: fixed` exists (line 79) — .border-anim absolute child is fine
- Add `overflow: hidden` to `.vmx-settings-drawer` block
- Add `.vmx-settings-drawer > :not(.border-anim) { position: relative; z-index: 5; }` so header + body sit above the sweep peak (z-index 4)

NEW COMPONENT `tauri/ui/src/settings/components/performance-group.ts` (lift function-component shape from mascot-group.ts; lift toggle CSS anatomy from SettingsDrawer.ts:198–222):

  import { sendIpcRequest } from "../../ipc/client";
  import { renderSettingsGroup } from "./group";  // (or wherever the group factory lives — verify by reading mascot-group.ts)

  /** Apply current state on boot — read from SettingsApplier, default false. */
  export function applyBlurPerfPreference(enabled: boolean): void {
    if (enabled) document.documentElement.setAttribute("data-blur-perf", "on");
    else document.documentElement.removeAttribute("data-blur-perf");
  }

  /** Toggle handler — apply locally for instant feedback, then persist. */
  export async function toggleBlurPerf(enabled: boolean): Promise<void> {
    applyBlurPerfPreference(enabled);
    await sendIpcRequest({
      type: "settings.set",
      field: "performance.lighter_blur",
      value: enabled,
    });
  }

  export function PerformanceGroup(currentValue: boolean): HTMLElement {
    return renderSettingsGroup({
      header: "PERFORMANCE",
      children: buildToggleRow("LIGHTER BLUR", currentValue, (on) => {
        void toggleBlurPerf(on);
      }),
    });
  }

CSS for the toggle (lift from SettingsDrawer.ts:198–222):
- Off state: background `var(--glass-3)`, border `1px var(--glass-edge)`, label `var(--silk-65)`
- On state: background `linear-gradient(180deg, rgba(255,138,61,0.09) 0%, rgba(255,138,61,0.025) 100%) /* mock-verbatim */`, border `1px var(--amber-40)`, inset `0 0 14px var(--amber-22)`, label `var(--amber)` + text-shadow `0 0 4px var(--amber-65)`, focus ring via `*:focus-visible` rule from tokens.css:367–369

IPC SCHEMA DELTA — `tauri/ui/src/ipc/messages.schema.json` (DECISION PRE-COMMITTED — plan-checker W-2 resolution):

- `settings.set.field` is enum-restricted (verified: `messages.schema.json:529` enumerates 9 flat values — `voice, mode, genre, output_device_id, output_profile, retention_days, push_to_mute_hotkey, mood, click_through`). No dot-paths — every existing entry is a single flat identifier.
- **Strategy locked: flat enum entry `"lighter_blur"`.** Add `"lighter_blur"` as the 10th value in the `settings.set.field` enum. Value type is `boolean`. Do NOT use dot-path `"performance.lighter_blur"` — it would be the only dot-path entry, inconsistent with all 9 existing flat names + dispatcher matches on flat strings at `src/vibemix/runtime/settings.py:155-172`.
- **`SettingsState` payload schema update (REQUIRED — W-2):** `messages.schema.json` `SettingsState.required` array currently lists `[voice, mode, genre, output_device_id, output_profile, retention_days, push_to_mute_hotkey, muted]`. Append `lighter_blur` so the sidecar boot-snapshot includes the new field; without this, Plan 14-03's `main.ts` `data-blur-perf` boot read times out and defaults off.
- Mirror in Python `src/vibemix/ui_bus/messages.py` — add `lighter_blur: bool` to the `SettingsState` dataclass (same level as `muted`).
- Regenerate TS via `cd tauri/ui && npm run check:ipc` (runs codegen + tsc).
- Verify count parity: `python scripts/check_ipc_schema.py` must exit 0 (wrapper count == oneOf count == 26 — this is a new field on an existing message, not a new message family).

PYTHON SETTINGS APPLIER WRITE PATH — `src/vibemix/runtime/settings.py:143` (`async def apply` dispatcher, verified line numbers):
- `class SettingsApplier` lives at `src/vibemix/runtime/settings.py:105`. The dispatcher is `async def apply(self, field: str, value: Any)` at line 143, which is a flat `if field == "<name>"` chain at lines 155–172.
- Add a new branch after line 172 (after `click_through`): `if field == "lighter_blur": return await self._apply_lighter_blur(value)`. Flat string match per the schema decision above. Then add private handler `async def _apply_lighter_blur(self, value: Any) -> tuple[bool, str | None]` modeled on `_apply_click_through` (presentation-only, no session restart).
- ConfigStore: extend the JSON shape with a top-level `lighter_blur: bool` field (default `False`). Read `src/vibemix/runtime/config_store.py` (or wherever ConfigStore lives — discover via grep `class ConfigStore`); add `lighter_blur` to the dataclass + defaults dict + the atomic write path.
- `lighter_blur` is presentation-only — NO audio/IPC/MIDI side effects, NO session restart, NO cascade_agent touch. Just validates bool + persists to disk + returns `(True, None)`.

ACCENT RESERVATION SANITY:
Settings drawer maps to UI-SPEC accent-reservation items 4 (active mood pill — mascot-group), 5 (active button state — Performance toggle ON), 8/9 (AI-line — not in settings), 10 (animated border — drawer sweep), 15 (focus ring). DO NOT add amber to header titles or group dividers.

COPY PURGE — UI-SPEC Copywriting Contract:
- mascot-group jsdoc currently says "phosphor amber" — purge to remove "phosphor" word entirely (use "amber" if you must name it)
- retention-slider jsdoc may say "knurled-knob" — purge to "retention discs" (just describe the function)
- New copy: "LIGHTER BLUR" toggle label (UPPERCASE per UI-SPEC label convention; per Copywriting Contract row "Performance toggle: 'lighter blur' (off/on switch, no exclamation, no marketing copy)" — but UI-SPEC label convention is UPPERCASE silkscreen, so "LIGHTER BLUR" in display CSS matches the v5 mock convention)
- Settings group headers update: existing 6 groups (PERSONA / OUTPUT / HOTKEY / RECORDING / CALIBRATION / MASCOT) become 7: add PERFORMANCE between MASCOT and the end (or wherever the existing group order suggests — read mascot-group.ts placement)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-04-01: Migrate SettingsDrawer + 5 existing settings components + create performance-group.ts</name>
  <files>tauri/ui/src/settings/SettingsDrawer.ts, tauri/ui/src/settings/components/retention-slider.ts, tauri/ui/src/settings/components/mascot-group.ts, tauri/ui/src/settings/components/hotkey-capture.ts, tauri/ui/src/settings/components/confirm-dialog.ts, tauri/ui/src/settings/components/performance-group.ts, tauri/ui/src/settings/state.ts</files>
  <read_first>
    - tauri/ui/src/settings/SettingsDrawer.ts (current — 660 lines; locate the lines 78–98 + 284–289 + 198–222 referenced in PATTERNS.md; the file is already mostly v5 — minimal CSS touches needed beyond border-anim + overflow + z-index)
    - tauri/ui/src/settings/components/mascot-group.ts (analog for performance-group.ts shape; also has phosphor-residue jsdoc to purge)
    - tauri/ui/src/settings/components/retention-slider.ts (3 --phosphor* + 1 --panel*; knurled-knob jsdoc residue to purge)
    - tauri/ui/src/settings/components/hotkey-capture.ts (2 --phosphor* pulse state)
    - tauri/ui/src/settings/components/confirm-dialog.ts (mostly rgba(0,0,0,*) modal backdrop — verify against UI-SPEC §confirm modal)
    - tauri/ui/src/settings/state.ts (current SettingsState shape — extend with performance.lighter_blur)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-PATTERNS.md "Surface 3 — Settings Drawer"
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Surface 3 — Settings Drawer"
    - tauri/ui/src/crash-banner.ts block in tokens.css:466–530 (amber-glow-against-glass — analog for hotkey-capture pulse state)
  </read_first>
  <action>
**SettingsDrawer.ts** — Insert `.border-anim` as FIRST child of `.vmx-settings-drawer` aside, before the header. Add `overflow: hidden` to the `.vmx-settings-drawer` CSS block. Add new rule: `.vmx-settings-drawer > :not(.border-anim) { position: relative; z-index: 5; }` so header + body sit above the sweep. Migrate the 5 remaining legacy --sp-* aliases to v5 (`--sp-md` → `--sp-4`, `--sp-lg` → `--sp-5`, etc. — read the file and locate the actual occurrences). Wire the PerformanceGroup() render call into the drawer body (place it AFTER mascot-group per CONTEXT Copywriting Contract group order: PERSONA · OUTPUT · HOTKEY · RECORDING · CALIBRATION · MASCOT · PERFORMANCE).

**retention-slider.ts** — Migrate 4 legacy refs (3 --phosphor* + 1 --panel*). Horizontal track: `1px var(--glass-edge)` recessed; lit portion `linear-gradient(90deg, var(--amber-22), var(--amber))`; 6-stop retention discs become small recessed `var(--glass-3)` circles with `var(--amber)` fill when active. **Delete the "knurled-knob" jsdoc copy** — rewrite to "retention discs (one per retention tier)".

**mascot-group.ts** — Migrate 4 legacy refs. **Delete the "phosphor amber" jsdoc** — rewrite to "amber" only. Chrome stays amber per UI-SPEC accent-reservation item 4 (active mood pill).

**hotkey-capture.ts** — Migrate 3 legacy refs (mostly --phosphor* pulse state). Idle chip: `var(--silk)`. Capturing: `var(--amber)` with `var(--glow-soft)` pulse animation (use crash-banner block analog from tokens.css:466–530). Conflict state: `var(--rec)` red 1px border + inline copy "reserved combo — choose another".

**confirm-dialog.ts** — Audit for any --bezel-* / --panel-* / --ink-* refs. Modal background `var(--glass-1)` + `var(--blur-glass)` strong. Primary CTA destructive variant: `var(--rec)` for "delete recordings" etc. Default variant stays amber (`var(--amber-40)` border + amber fill via mock-verbatim gradient). Most 23 rgba literals are `rgba(0,0,0,*)` backdrop — verify against UI-SPEC §confirm modal and migrate silk ones to --silk-* tokens.

**performance-group.ts** — CREATE NEW FILE per <interfaces> pseudocode. Export `applyBlurPerfPreference(enabled: boolean)`, `toggleBlurPerf(enabled: boolean): Promise<void>`, and `PerformanceGroup(currentValue: boolean): HTMLElement`. Group header `"PERFORMANCE"` (UPPERCASE 9–10px silkscreen var(--silk-40) + engraved text-shadow). Single row: label "LIGHTER BLUR" + toggle pill (off: --glass-3 bg, on: amber backlight per mock-verbatim gradient). Toggle calls `toggleBlurPerf(newValue)` on click. registerStyle() per Phase 11 W3 pattern.

**state.ts** — Extend SettingsState type with `performance: { lighter_blur: boolean }` field. Default value `false`. Add a `selectLighterBlur(state)` selector or equivalent for the drawer composer.
  </action>
  <verify>
    <automated>cd tauri/ui && npm run check:ipc 2>/dev/null; cd /Users/ozai/projects/dj-set-ai && bash scripts/check_v5_migration.sh --surface=settings --warn-only; grep -F "border-anim" tauri/ui/src/settings/SettingsDrawer.ts; grep -F "overflow: hidden" tauri/ui/src/settings/SettingsDrawer.ts; test -f tauri/ui/src/settings/components/performance-group.ts; grep -F "applyBlurPerfPreference" tauri/ui/src/settings/components/performance-group.ts; grep -F "toggleBlurPerf" tauri/ui/src/settings/components/performance-group.ts; cd tauri/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>SettingsDrawer has border-anim + overflow: hidden + z-index discipline. 5 existing settings components migrated. performance-group.ts NEW file exports the three required functions. state.ts extended with performance.lighter_blur. `check_v5_migration.sh --surface=settings --warn-only` shows substantial drop. `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 14-04-02: Wire performance.lighter_blur IPC end-to-end + unskip settings.tokens.test.ts</name>
  <files>tauri/ui/src/ipc/messages.schema.json, tauri/ui/src/ipc/messages.ts, src/vibemix/ui_bus/messages.py, src/vibemix/runtime/settings.py, tauri/ui/tests/settings.tokens.test.ts</files>
  <read_first>
    - tauri/ui/src/ipc/messages.schema.json (current — identify the `settings.set` payload shape; verify whether `field` is enum-restricted or free-string)
    - tauri/ui/src/ipc/messages.ts (current codegen output — must regenerate after schema edits)
    - src/vibemix/ui_bus/messages.py (current — identify the SettingsSet wrapper dataclass + dispatch struct)
    - src/vibemix/runtime/settings.py (current — identify the per-field dispatcher; locate where to add the performance.lighter_blur case)
    - src/vibemix/runtime/config_store.py (if it exists — verify the JSON storage shape; performance group needs a default)
    - scripts/check_ipc_schema.py (the count-parity gate — must stay green after edits)
    - tauri/ui/tests/settings.tokens.test.ts (Wave 0 created; currently describe.skip)
  </read_first>
  <action>
**messages.schema.json** — Add `"lighter_blur"` to the `settings.set.field` enum at line 529 (10th flat value, after `click_through`). Value type stays generic per existing envelope. ALSO append `"lighter_blur"` to the `SettingsState.required` array (line 558+) so boot snapshots include it. Message family count stays 26 (this is a new field on existing messages, not a new family).

**messages.ts** — Regenerate via `cd tauri/ui && npm run check:ipc` (which runs codegen-ipc.mjs + tsc). Verify the regenerated file still has the AUTO-GENERATED banner on line 1 and that `VibemixIPCMessages` union still has 26 members.

**ui_bus/messages.py** — Mirror the schema delta: add `lighter_blur` to whatever enum/typing structure the existing 9 `settings.set.field` values use (read the file first to identify the pattern — likely `Literal[...]` or an enum class). Add `lighter_blur: bool` field to the `SettingsState` dataclass with default `False`. Run `python scripts/check_ipc_schema.py` after — wrapper count == oneOf count == 26 invariant holds.

**`src/vibemix/runtime/settings.py:143`** — Add a new branch in the `apply` dispatcher (after `click_through` at line 172): `if field == "lighter_blur": return await self._apply_lighter_blur(value)`. Implement private handler `async def _apply_lighter_blur(self, value: Any) -> tuple[bool, str | None]` modeled on `_apply_click_through`:
1. `if not isinstance(value, bool): return (False, "lighter_blur must be a boolean")`
2. `self.config_store.lighter_blur = value` (flat field on ConfigStore)
3. ConfigStore.write() handles the atomic persist
4. Return `(True, None)`
NO restart, NO session teardown — this is presentation-only. ConfigStore default for new installs is `False`. Verify the ConfigStore class has `lighter_blur: bool = False` declared (extend the dataclass + the to_dict/from_dict roundtrip if those methods exist).

**settings.tokens.test.ts** — Remove the `describe.skip(...)` wrapper. Spec imports `SettingsDrawer`, `RetentionSlider`, `HotkeyCapture`, `MascotGroup`, `PerformanceGroup` (the new component) and asserts `containsLegacyToken(rendered)` is false for each. Additionally, assert `PerformanceGroup(false)` renders the toggle with off-state styling (glass-3) and `PerformanceGroup(true)` renders the on-state (amber backlight markers in computed inline style). Run — MUST pass green.

After all edits:
- `bash scripts/check_v5_migration.sh --surface=settings --strict` exits 0
- `cd tauri/ui && npm run check:ipc` exits 0
- `python scripts/check_ipc_schema.py` exits 0
- `cd tauri/ui && npm run test -- settings.tokens.test.ts --run` exits 0
  </action>
  <verify>
    <automated>cd tauri/ui && npm run check:ipc; cd /Users/ozai/projects/dj-set-ai && python scripts/check_ipc_schema.py && bash scripts/check_v5_migration.sh --surface=settings --strict && bash scripts/check_v5_fonts.sh --surface=settings --strict && cd tauri/ui && npm run test -- settings.tokens.test.ts --run --reporter=dot && cd /Users/ozai/projects/dj-set-ai && python -m pytest -q tests/ui_bus/ 2>&1 | tail -3 && cd tauri/ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>IPC schema + Python mirror + TS codegen agree on performance.lighter_blur path. ipc count parity stays 26. SettingsApplier writes the field through ConfigStore. settings.tokens.test.ts green (no skip). --strict gates exit 0 for settings surface. pytest ui_bus suite stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 14-04-03: Critique loop cycle 1 — ui-checker + ui-auditor + Kaan side-by-side + Performance toggle end-to-end test</name>
  <what-built>Settings drawer fully migrated to v5 primitives (5 files + 1 new performance-group.ts). border-anim on drawer aside. Performance group renders the "LIGHTER BLUR" toggle. IPC end-to-end wired. vitest green.</what-built>
  <how-to-verify>
    1. Kaan-side: `cd tauri/ui && npm run tauri dev` — open the session UI, then click the settings gear button
    2. Drawer slides in from right with `border-anim` visible sweeping the perimeter; backdrop blur is present
    3. Scroll the drawer body to find the new PERFORMANCE group (after MASCOT). Verify the "LIGHTER BLUR" toggle is in off-state (--glass-3 bg, --silk-65 label)
    4. Click the toggle — toggle animates to amber backlight + label becomes --amber. Immediately verify session glass panels visible behind drawer have lighter blur (verify by inspecting computed `--blur-glass` in DevTools, OR by quitting + relaunching the app and confirming the toggle state persists + blur is still lighter)
    5. Click toggle back off — blur restores to default
    6. Open mascot-group in same drawer; verify mood pills still work (regression check — Phase 13 didn't break)
    7. Run `Skill(skill="gsd-ui-checker", args="14 --surface=settings")` — attach to 14-POLISH-LOG.md
    8. Run `Skill(skill="gsd-ui-auditor", args="14 --surface=settings")` — three audits. Attach.
    9. Screenshot settings drawer (open) at 1440×900 with PERFORMANCE group visible; place side-by-side with `mocks/vibemix-direction-final.html` §02 spec-panel (closest analog — the mock doesn't have a drawer mockup but spec-panel glass-anatomy + amber accent + group anatomy parity is the comparison). Attach to 14-POLISH-LOG.md.
    10. Per CONTEXT Area 3 perf rehearsal: also test prefers-reduced-motion (System Settings → Accessibility) — drawer's border-anim should freeze at opacity 0.6.
    11. If ui-checker has findings: orchestrator spawns cycle 2 follow-up. Cycle 3 unresolved = polish debt + escalate.
  </how-to-verify>
  <resume-signal>Type "settings cycle 1 green" to mark surface complete, OR "settings cycle 2 needed: <findings>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS performance-group.ts → Python SettingsApplier | New `performance.lighter_blur` field path traverses existing ipc.settings.set envelope; presentation-only — no audio/MIDI side effects |
| ConfigStore atomic write | Persisted boolean preference; default false on first launch |

## STRIDE Threat Register (security_enforcement = ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering (IPC schema drift) | settings.set field enum or free-form payload | mitigate | scripts/check_ipc_schema.py count-parity gate stays green; npm run check:ipc + python scripts/check_ipc_schema.py both run in CI |
| T-14-04-02 | Capability allowlist drift (Elevation) | Tauri capability config | accept | No new Tauri invoke command added — reuses existing ipc.settings.set; allowlist unchanged |
| T-14-04-03 | Information disclosure | performance.lighter_blur preference | accept | Boolean preference; no PII or sensitive data; persisted to OS-aware config dir per Phase 12 W2 existing security posture |
</threat_model>

<verification>
- `bash scripts/check_v5_migration.sh --surface=settings --strict` exits 0
- `bash scripts/check_v5_fonts.sh --surface=settings --strict` exits 0
- `cd tauri/ui && npm run check:ipc` exits 0 (codegen + tsc green)
- `python scripts/check_ipc_schema.py` exits 0 (wrapper count == oneOf count == 26)
- `cd tauri/ui && npm run test -- settings.tokens.test.ts --run` exits 0 (no skip)
- `python -m pytest -q tests/ui_bus/` exits 0
- `tauri/ui/src/settings/components/performance-group.ts` exists and exports `applyBlurPerfPreference`, `toggleBlurPerf`, `PerformanceGroup`
- `tauri/ui/src/settings/SettingsDrawer.ts` contains `border-anim` + `overflow: hidden`
- Settings drawer renders the PERFORMANCE group at runtime (verified by Kaan-side dev build)
- Performance toggle round-trips through SettingsApplier (verified by quit/relaunch persistence)
- gsd-ui-checker + gsd-ui-auditor recorded in 14-POLISH-LOG.md
</verification>

<success_criteria>
- 5 settings files migrated to v5 primitives + 1 new component (performance-group.ts)
- Zero legacy shim refs in `tauri/ui/src/settings/` (per --strict gate)
- Zero forbidden font-family declarations in `tauri/ui/src/settings/`
- Performance toggle wires the data-blur-perf attribute via SettingsApplier with persistence
- IPC schema count parity stays at 26
- vitest settings.tokens.test.ts green (no skip)
- SettingsDrawer renders with .border-anim as first child + overflow: hidden + z-index 5 for body
- gsd-ui-checker zero findings on settings surface
- gsd-ui-auditor 3 audits green
- Performance toggle end-to-end test passes (toggle ON → blur lightens immediately + persists across restart → toggle OFF → blur restores)
- Side-by-side screenshot pair attached to 14-POLISH-LOG.md
- Cycle count ≤3
</success_criteria>

<output>
After completion, create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-04-SUMMARY.md` documenting:
- Per-file before/after legacy-ref + rgba counts (5 modified + 1 new)
- IPC schema delta exact shape (enum vs. free-form decision + count parity confirmed)
- SettingsApplier write path code excerpt
- Cycle count for settings surface
- ui-checker + ui-auditor output refs + fix commit SHAs
- Performance toggle end-to-end test results + persistence verification
- Screenshot pair (live drawer 1440×900 + PERFORMANCE group expanded vs mock §02 spec-panel)
- Any polish debt escalated
</output>
