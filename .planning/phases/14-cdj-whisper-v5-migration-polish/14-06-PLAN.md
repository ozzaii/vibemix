---
phase: 14-cdj-whisper-v5-migration-polish
plan: 06
type: execute
wave: 5
depends_on: ["14-01", "14-02", "14-03", "14-04", "14-05"]
files_modified:
  - tauri/ui/src/tokens.css
  - tauri/ui/public/fonts/Workbench-Regular.woff2
  - tauri/ui/public/fonts/DMMono-Regular.woff2
  - tauri/ui/public/fonts/DMMono-Medium.woff2
  - tauri/ui/public/fonts/DSEG7Classic-Bold.woff2
  - tauri/ui/public/fonts/Caveat-Bold.woff2
  - tauri/ui/LICENSE-3RD-PARTY.md
  - .git/hooks/pre-commit
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
autonomous: false
requirements:
  - POLISH-01
  - POLISH-04
  - POLISH-06
tags:
  - ui
  - migration
  - subtractive
  - close

must_haves:
  truths:
    - "tokens.css lines 175–231 (the entire backward-compat shim block) deleted in a single commit"
    - "tokens.css lines 38–76 (legacy @font-face for Workbench/DM Mono/DSEG7/Caveat) deleted"
    - "tokens.css line 35 @import from fonts.googleapis.com REPLACED with vendored @font-face for Saira (variable wdth + wght) + JetBrains Mono Regular/Medium/SemiBold WOFF2 (file format='woff2-variations' fallback to 'woff2')"
    - "4 legacy WOFF2 files at tauri/ui/public/fonts/ deleted (Workbench-Regular, DMMono-Regular, DMMono-Medium, DSEG7Classic-Bold, Caveat-Bold)"
    - "tauri/ui/LICENSE-3RD-PARTY.md updated: 4 family entries dropped (Workbench, DM Mono, DSEG7, Caveat) + 2 entries added with SHA-256 (Saira-VariableFont_wdth,wght + JetBrains Mono Regular/Medium/SemiBold)"
    - ".git/hooks/pre-commit wired to run scripts/check_v5_migration.sh --strict ONLY for this commit; deleted IMMEDIATELY AFTER the shim-delete commit lands"
    - "Repo-wide --strict gates all green: check_v5_migration.sh + check_v5_fonts.sh + check_v5_copy.sh"
    - "Repo-wide ui-checker + ui-auditor final sweep run across all four surfaces; results recorded in 14-POLISH-LOG.md"
    - "STATE.md + ROADMAP.md updated: Phase 14 ✅ complete; progress percent ticks up"
  artifacts:
    - path: "tauri/ui/src/tokens.css"
      provides: "Final tokens.css — :root contains ONLY v5 primitives + spacing + radius + motion budget; shim deleted; legacy @font-face deleted; Google Fonts @import replaced by vendored @font-face; perf fallback block (added Wave 2) intact"
      contains: "var(--glass-1), var(--silk), var(--amber)"
    - path: "tauri/ui/LICENSE-3RD-PARTY.md"
      provides: "Updated attestation table — Saira + JetBrains Mono entries with SHA-256 hashes; four legacy font entries removed"
      contains: "Saira-VariableFont_wdth,wght.woff2, JetBrainsMono-Regular.woff2, JetBrainsMono-Medium.woff2, JetBrainsMono-SemiBold.woff2"
    - path: ".planning/STATE.md"
      provides: "Phase 14 marked complete; progress bumped to 14/20 phases"
      contains: "Phase 14"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 progress row Updated to 6/6 plans complete"
      contains: "Phase 14"
  key_links:
    - from: "tokens.css"
      to: "Wave 1-4 migrated consumers"
      via: "deleted shim block — all consumers now reference v5 primitives only"
      pattern: "(--phosphor|--brushed-|--bezel-|--panel-(lift|deep|hover|pressed)|--groove|--ink-|--col-mascot)"
    - from: ".git/hooks/pre-commit"
      to: "scripts/check_v5_migration.sh --strict"
      via: "exec gate on shim-delete commit; pre-commit hook removed after commit lands"
      pattern: "scripts/check_v5_migration.sh --strict"
    - from: "tauri/ui/LICENSE-3RD-PARTY.md"
      to: "vendored WOFF2 binaries"
      via: "SHA-256 attestation per file"
      pattern: "Saira-VariableFont|JetBrainsMono-"
---

<objective>
Single subtractive commit that closes Phase 14: delete the backward-compat shim from tokens.css, delete the legacy @font-face block, replace the Google Fonts remote @import with vendored @font-face for Saira + JetBrains Mono (using the WOFF2 files vendored in Wave 0), delete the 4 legacy WOFF2 files, update LICENSE-3RD-PARTY.md, wire and unwire the one-shot pre-commit grep gate, run the repo-wide ui-checker + ui-auditor final sweep, and update STATE.md + ROADMAP.md.

Purpose: Phase 14 closes with one clean diff. If a regression slips through, a single revert restores the shim (and Wave 1-4 consumers keep working via cascade). Pre-commit hook is wired ONLY for this commit per CONTEXT Area 2 — every other commit in the phase runs the script in --warn-only mode. The hook is REMOVED immediately after the commit lands so subsequent unrelated commits aren't blocked.

Output: tokens.css final form (v5 primitives + perf-fallback only, no shim, no legacy fonts), 4 deleted WOFF2 files, updated LICENSE-3RD-PARTY.md, pre-commit hook lifecycle (create → run → delete), 14-POLISH-LOG.md final sweep results, STATE.md + ROADMAP.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-CONTEXT.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md
@.planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md
@tauri/ui/src/tokens.css
@tauri/ui/LICENSE-3RD-PARTY.md
@scripts/check_v5_migration.sh

<interfaces>
TOKENS.CSS SURGERY (precise per RESEARCH.md + UI-SPEC §Shim Removal Surgery):

DELETE these blocks from tauri/ui/src/tokens.css:

1. Line 35 — Google Fonts @import:
   `@import url('https://fonts.googleapis.com/css2?family=Saira:wdth,wght@75..125,300..800&family=JetBrains+Mono:wght@400;500;600&display=swap');`
   REPLACE with vendored @font-face declarations (full block from RESEARCH.md:582–614 verbatim). Read SHA-256 hashes from `.planning/phases/14-cdj-whisper-v5-migration-polish/14-01-FONT-ATTESTATION.md` (created by Wave 0 Task 14-01-01 — plan-checker I-1 resolution) and copy them into LICENSE-3RD-PARTY.md.

2. Lines 38–76 — Legacy @font-face block (5 declarations: Workbench, DM Mono 400, DM Mono 500, DSEG7, Caveat).
   DELETE entirely.

3. Lines 175–231 — Backward-compat shim block (the entire shim region from `/* === Backward-compat shim ============= */` opening comment through the spacing aliases closing `--sp-3xl: var(--sp-7);   /* 64 */`).
   DELETE entirely. The aliases deleted: --bg, --panel, --panel-lift, --panel-deep, --groove, --bezel-1/2/3, --brushed-hi, --brushed-lo, --ink, --ink-dim, --ink-deep, --ink-engraved, --phosphor, --phosphor-warm, --phosphor-dim, --phosphor-soft, --phosphor-glow, --phosphor-halo, --rec, --cue, --ok, --crash-grad-top, --crash-grad-bottom, --panel-hover-top, --panel-pressed-bottom, --sp-xs, --sp-sm, --sp-md, --sp-lg, --sp-xl, --sp-2xl, --sp-3xl, --col-mascot.

4. The `.wizard-grid` rule at tokens.css:439–446 — its `grid-template-columns: var(--col-primary) var(--col-mascot)` becomes `grid-template-columns: var(--col-primary)` (since --col-mascot is deleted; or use 1fr if the wizard wants the column to flex — verify against current Wave 1 layout).

5. Crash banner block at tokens.css:466–530 still uses `var(--rec)` and `var(--crash-grad-top)` / `var(--crash-grad-bottom)`. Since these tokens are deleted in step 3, replace with direct values per UI-SPEC §Shim Removal table:
   - `var(--rec)` → `var(--led-fault)` (semantic alias collapses to the canonical token)
   - `var(--crash-grad-top)` → inline `rgba(37, 24, 28, 0.7)` /* mock-verbatim — destructive banner amber/red tint */
   - `var(--crash-grad-bottom)` → inline `rgba(26, 16, 20, 0.7)` /* mock-verbatim */

6. Wizard frame layout block at tokens.css:383–463 still uses `var(--sp-lg)` and `var(--sp-xl)` — replace with direct values per UI-SPEC §Spacing Scale "Exceptions":
   - `var(--sp-lg)` (24px) → `var(--sp-5)` (24px — same value, v5 name)
   - `var(--sp-xl)` (32px) → inline `32px` /* mock-verbatim — no v5 equivalent */
   - `var(--sp-md)` (16px) → `var(--sp-4)`

PRESERVE these blocks (ALL added in earlier waves of THIS phase — keep them intact):
- tokens.css :root v5 primitives (lines 81–173 — void / glass / silk / amber / rave / led / blur-glass / glow / type / radius / spacing / layout / motion budget)
- The perf-fallback block (added Wave 2 — @media prefers-reduced-motion + html[data-blur-perf="on"])
- Reset block + body radial-gradient stack + body::before film-grain + .border-anim utility + .vmx-glass-streak utility + focus-visible rule + button reset + wizard-grid (modified per step 4 above) + titlebar + status-bar + crash-banner (modified per step 5 above)

LEGACY WOFF2 DELETION (4 files):
- tauri/ui/public/fonts/Workbench-Regular.woff2
- tauri/ui/public/fonts/DMMono-Regular.woff2
- tauri/ui/public/fonts/DMMono-Medium.woff2
- tauri/ui/public/fonts/DSEG7Classic-Bold.woff2
- tauri/ui/public/fonts/Caveat-Bold.woff2

(5 deletions total — UI-SPEC says 4 families × 5 files because DM Mono has Regular + Medium. Verify by `ls tauri/ui/public/fonts/` post-deletion; only Saira variable + JetBrains Mono Regular/Medium/SemiBold remain.)

LICENSE-3RD-PARTY.md UPDATE (UI-SPEC §Typography "Saira + JetBrains Mono attestation"):
- DROP 4 entries: Workbench, DM Mono, DSEG7 Classic, Caveat (each with their existing SHA-256 + OFL-1.1 attribution)
- ADD 2 entries with SHA-256 hashes captured in Wave 0 Task 14-01-01 commit body:
  * Saira-VariableFont_wdth,wght.woff2 — OFL-1.1 — Inma Anaya design — sha256: {captured-from-wave-0-commit}
  * JetBrainsMono-{Regular,Medium,SemiBold}.woff2 — OFL-1.1 — JetBrains — sha256: {three hashes captured in wave 0}

PRE-COMMIT HOOK LIFECYCLE (RESEARCH.md:676–688 + CONTEXT Area 2):

Step 1 — At START of this task (before any deletion):
  cat > .git/hooks/pre-commit <<'EOF'
  #!/usr/bin/env bash
  exec "$(git rev-parse --show-toplevel)/scripts/check_v5_migration.sh" --strict
  EOF
  chmod +x .git/hooks/pre-commit

Step 2 — Make all the deletions/edits.

Step 3 — `git add` the modified files (tokens.css, LICENSE-3RD-PARTY.md) + `git rm` the 5 deleted WOFF2 files.

Step 4 — `git commit -m "..."`. The pre-commit hook fires; check_v5_migration.sh --strict runs; expects zero hits in tauri/ui/src/ (outside tokens.css). Because the shim itself is being deleted in this commit, the strict gate now passes since consumers were already migrated in Waves 1-4. If it FAILS — DO NOT bypass; investigate which file in tauri/ui/src/ still references a legacy token (likely a missed surface item from Wave 1-4) and fix BEFORE running --no-verify (which is FORBIDDEN per GSD rules).

Step 5 — IMMEDIATELY AFTER the commit lands: `rm .git/hooks/pre-commit`. The hook is one-shot per CONTEXT Area 2 + Pitfall 8 — leaving it in place would block every subsequent unrelated commit (e.g. Wave 5 finishing touches, STATE.md update, etc.).

REPO-WIDE FINAL SWEEP (CONTEXT Area 3 — runs AFTER the shim-delete commit lands):
- All four surfaces (wizard, session, settings, mascot) get one final ui-checker pass
- Cross-surface audit: are there any cross-surface visual conflicts (e.g. session vs settings drawer border-anim animation cadence syncing accidentally — they're both 22s forward by default per Wave 2/3 design)?
- gsd-ui-auditor 20/80 dominance audit at the page level (does the dominant warm-black hold across the live session UI + open settings drawer + visible mascot overlay simultaneously?)
- Performance toggle round-trip (set, restart, verify, unset, restart, verify)

STATE.md + ROADMAP.md update (UI-SPEC checker sign-off → Phase 14 close):
- STATE.md: bump Phase 14 to ✅ complete; add a session-summary line; ProgressMetric phase count 13/20 → 14/20
- ROADMAP.md: tick the Phase 14 plan checkboxes (6/6 plans complete) + check the Phase 14 entry box (currently `[ ]`); update the Progress table row for Phase 14
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 14-06-01: Wire pre-commit hook + delete shim block + delete legacy @font-face + replace @import + delete 5 WOFF2 + update LICENSE-3RD-PARTY.md + commit + unwire pre-commit hook</name>
  <files>tauri/ui/src/tokens.css, tauri/ui/LICENSE-3RD-PARTY.md, .git/hooks/pre-commit, tauri/ui/public/fonts/Workbench-Regular.woff2, tauri/ui/public/fonts/DMMono-Regular.woff2, tauri/ui/public/fonts/DMMono-Medium.woff2, tauri/ui/public/fonts/DSEG7Classic-Bold.woff2, tauri/ui/public/fonts/Caveat-Bold.woff2</files>
  <read_first>
    - tauri/ui/src/tokens.css (current — verify all line numbers match the surgery plan; tokens.css may have shifted lines if Wave 2 added the perf-fallback block above or below the shim)
    - tauri/ui/LICENSE-3RD-PARTY.md (current — identify the existing structure; preserve formatting style)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-01-SUMMARY.md (if produced — for SHA-256 hashes of Saira + JetBrains Mono WOFF2)
    - scripts/check_v5_migration.sh (must be executable + --strict mode tested)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-RESEARCH.md lines 575-688 (vendored @font-face block + pre-commit hook lifecycle)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-UI-SPEC.md "Shim Removal Surgery" section
  </read_first>
  <action>
**STEP 1 — Wire the one-shot pre-commit hook** (before any edits, so the hook fires when the commit lands later):

```
cat > .git/hooks/pre-commit <<'EOF'
#!/usr/bin/env bash
exec "$(git rev-parse --show-toplevel)/scripts/check_v5_migration.sh" --strict
EOF
chmod +x .git/hooks/pre-commit
```

**STEP 2 — tokens.css surgery** (the precise edit list from <interfaces>):

a) Replace line 35 (`@import url('https://fonts.googleapis.com/...')`) with the vendored @font-face block VERBATIM from RESEARCH.md:582–614 (Saira variable + JetBrains Mono Regular/Medium/SemiBold).

b) DELETE the entire legacy @font-face block (lines 38–76 of the original — but verify current line numbers — 5 declarations: Workbench, DM Mono Regular, DM Mono Medium, DSEG7, Caveat).

c) DELETE the entire backward-compat shim block (lines 175–231 of the original — verify current line numbers; the block runs from the opening comment `/* === Backward-compat shim ===` through the closing `--sp-3xl: var(--sp-7);   /* 64 */`).

d) Update `.wizard-grid` rule (around tokens.css:439–446): change `grid-template-columns: var(--col-primary) var(--col-mascot);` to `grid-template-columns: var(--col-primary);` (single-column — `--col-mascot` is gone; alternative: `var(--col-primary) 1fr` if the wizard flex/grow is desired — match what Wave 1's index.html structure expects).

e) Update wizard frame layout block (around tokens.css:383–463) — replace all remaining `var(--sp-lg)` with `var(--sp-5)`, all `var(--sp-md)` with `var(--sp-4)`, all `var(--sp-xl)` with literal `32px /* mock-verbatim — no v5 equivalent */`.

f) Update crash-banner block (lines 466–530) — replace `var(--rec)` with `var(--led-fault)`, `var(--crash-grad-top)` with `rgba(37, 24, 28, 0.7) /* mock-verbatim */`, `var(--crash-grad-bottom)` with `rgba(26, 16, 20, 0.7) /* mock-verbatim */`. Verify all other tokens in that block are v5 primitives (--amber, --amber-40, --amber-65, --amber-22, --silk, --silk-65, --type-display, --type-mono, --rad-sm, --blur-glass-light, --motion-snap).

g) Preserve the v5 primitives block (current lines 78–173), the perf-fallback block added in Wave 2 (Reset block + radial gradients + film grain + .border-anim utility + .vmx-glass-streak utility + focus-visible + button reset + wizard-grid + titlebar + status-bar + crash-banner — all retained, just retoned per above).

**STEP 3 — Delete 5 legacy WOFF2 files**:
```
git rm tauri/ui/public/fonts/Workbench-Regular.woff2
git rm tauri/ui/public/fonts/DMMono-Regular.woff2
git rm tauri/ui/public/fonts/DMMono-Medium.woff2
git rm tauri/ui/public/fonts/DSEG7Classic-Bold.woff2
git rm tauri/ui/public/fonts/Caveat-Bold.woff2
```

**STEP 4 — Update LICENSE-3RD-PARTY.md**: drop the 4 family entries (Workbench, DM Mono, DSEG7, Caveat); add 2 new entries with SHA-256 hashes captured in Wave 0 Task 14-01-01 commit body (reread that commit's body or the 14-01-SUMMARY.md). New entries:
- `Saira-VariableFont_wdth,wght.woff2` — author: Inma Anaya — license: OFL-1.1 — sha256: {hash}
- `JetBrainsMono-Regular.woff2`, `JetBrainsMono-Medium.woff2`, `JetBrainsMono-SemiBold.woff2` — author: JetBrains — license: OFL-1.1 — sha256: {three hashes}

**STEP 5 — Pre-commit verification (dry run BEFORE committing)**:
- Run `bash scripts/check_v5_migration.sh --strict` manually — MUST exit 0 (zero hits in tauri/ui/src/ outside tokens.css; tokens.css is now also clean post-shim-deletion)
- Run `bash scripts/check_v5_fonts.sh --strict` — MUST exit 0
- Run `bash scripts/check_v5_copy.sh --strict` — MUST exit 0
- Run `cd tauri/ui && npm run check:ipc` — MUST exit 0
- Run `cd tauri/ui && npm run build` — MUST exit 0 (Vite + tsc)
- Run `cd tauri/ui && npm run test` — MUST exit 0 (all vitest specs, including the four per-surface tokens specs + mascot.chrome)
- Run `python scripts/check_ipc_schema.py` — MUST exit 0 (parity check)
- Run `python -m pytest -q` — MUST exit 0 (full Python suite; ensure Wave 4 settings.set field addition didn't break ui_bus tests)

If ANY check fails, identify the culprit, fix in place, re-run. Do NOT bypass via --no-verify.

**STEP 6 — Commit**:
```
git add tauri/ui/src/tokens.css tauri/ui/LICENSE-3RD-PARTY.md
git commit -m "$(cat <<'EOF'
feat(14): delete v5 backward-compat shim + vendor Saira + JetBrains Mono

Subtractive commit closing Phase 14. tokens.css shim block deleted; consumers
on Waves 1-4 already migrated to v5 primitives. Legacy @font-face block (Workbench,
DM Mono, DSEG7, Caveat) deleted along with 5 WOFF2 files. Google Fonts remote @import
replaced with vendored Saira (variable wdth+wght) + JetBrains Mono Regular/Medium/SemiBold
WOFF2 (sha256 attestations in LICENSE-3RD-PARTY.md).

Per CONTEXT.md Area 2 — single subtractive commit + one-shot pre-commit hook that
runs check_v5_migration.sh --strict; hook is removed immediately after this commit.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

The pre-commit hook fires automatically. It runs `check_v5_migration.sh --strict` and expects zero hits. If hits remain → fix in place; DO NOT use --no-verify.

**STEP 7 — Remove the one-shot pre-commit hook IMMEDIATELY**:
```
rm .git/hooks/pre-commit
```

Verify the hook is gone (`ls -la .git/hooks/pre-commit` returns "No such file"). This frees subsequent commits (Wave 5 finishing touches + STATE.md + ROADMAP.md updates) from running the strict gate on every commit.
  </action>
  <verify>
    <automated>bash scripts/check_v5_migration.sh --strict && bash scripts/check_v5_fonts.sh --strict && bash scripts/check_v5_copy.sh --strict && cd tauri/ui && npm run check:ipc && npm run build 2>&1 | tail -3 && npm run test 2>&1 | tail -3 && cd /Users/ozai/projects/dj-set-ai && python scripts/check_ipc_schema.py && ! grep -E "(--phosphor|--brushed-|--bezel-|--panel-lift|--panel-deep|--groove|--ink|--col-mascot)" tauri/ui/src/tokens.css && ! grep -q "fonts.googleapis.com" tauri/ui/src/tokens.css && ! test -f tauri/ui/public/fonts/Workbench-Regular.woff2 && ! test -f tauri/ui/public/fonts/DMMono-Regular.woff2 && ! test -f tauri/ui/public/fonts/DMMono-Medium.woff2 && ! test -f tauri/ui/public/fonts/DSEG7Classic-Bold.woff2 && ! test -f tauri/ui/public/fonts/Caveat-Bold.woff2 && test -f tauri/ui/public/fonts/Saira-VariableFont_wdth,wght.woff2 && ! test -e .git/hooks/pre-commit && grep -F "Saira-VariableFont" tauri/ui/LICENSE-3RD-PARTY.md && grep -F "JetBrainsMono" tauri/ui/LICENSE-3RD-PARTY.md && git log -1 --format=%s | grep -F "delete v5 backward-compat shim"</automated>
  </verify>
  <done>Shim block deleted from tokens.css; legacy @font-face block deleted; Google Fonts @import replaced with vendored @font-face; 5 legacy WOFF2 files deleted; LICENSE-3RD-PARTY.md updated; pre-commit hook wired + ran + removed; shim-delete commit lives in git log. All --strict gates green repo-wide. npm run build + npm run test green. pytest green. check_ipc_schema.py green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 14-06-02: Repo-wide final critique sweep — ui-checker + ui-auditor across all four surfaces + Performance toggle persistence rehearsal + Windows transparency check (deferred or done now)</name>
  <what-built>Shim deleted; all four surfaces (wizard, session, settings, mascot) on v5 primitives directly. tokens.css final form: 60% smaller, only v5 primitives + perf-fallback + body bg + film grain + .border-anim utility + .vmx-glass-streak + reset + wizard frame layout retoned + crash-banner retoned. 5 legacy fonts gone.</what-built>
  <how-to-verify>
    1. Kaan-side: `cd tauri/ui && npm run tauri dev` — launch the app fresh
    2. Cycle through all four surfaces: wizard step 1 → wizard step 4 (smoke test) → live session (use `__vibemixDev.advanceTo('session')` if needed) → open settings drawer → scroll to mascot group → click each mood pill → mascot overlay window visible across all moods. Verify visually nothing regressed from Wave 1-5 per-surface acceptance.
    3. Run `Skill(skill="gsd-ui-checker", args="14 --repo-wide")` — final sweep. Attach output to 14-POLISH-LOG.md "Final Sweep" row.
    4. Run `Skill(skill="gsd-ui-auditor", args="14 --repo-wide")` — three audits at the page level (20/80 dominance held; no faux 3D bevels anywhere; typography pairing is Saira + JetBrains Mono only). Attach.
    5. Performance toggle persistence rehearsal: set "Lighter blur" ON in Settings → close + reopen app → verify the toggle is still ON and blur is still lighter. Set OFF → close + reopen → verify OFF + full blur.
    6. macOS prefers-reduced-motion rehearsal: System Settings → Accessibility → Reduce motion ON → reload app → all four surfaces' border-anim freeze at opacity 0.6, blurs drop to 16/8/4. Restore → animations resume.
    7. Windows non-dev rehearsal (CONTEXT Area 3 requirement): IF Kaan has a Windows machine available, install the dev build there and verify the mascot overlay window transparency still works (tauri WebView2 backdrop bug #10064 mitigation). If no Windows machine right now: log "deferred to Phase 20 fresh-machine rehearsal" in 14-POLISH-LOG.md Perf Verification Windows row with a clear note.
    8. Update STATE.md + ROADMAP.md — see Task 14-06-03.
  </how-to-verify>
  <resume-signal>Type "phase 14 final sweep green" to mark phase complete, OR "phase 14 cycle: <findings>" if outstanding work surfaces</resume-signal>
</task>

<task type="auto">
  <name>Task 14-06-03: Update STATE.md + ROADMAP.md + finalize 14-POLISH-LOG.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md</files>
  <read_first>
    - .planning/STATE.md (current — locate the Current Position section, Last Session log, Progress Metrics)
    - .planning/ROADMAP.md (current — locate Phase 14 entry + Progress table row 14)
    - .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md (current — populate the remaining Critique Cycles status + final sweep + perf verification rows)
  </read_first>
  <action>
**STATE.md** (drift-safe — read current values before increment, plan-checker I-2 resolution):
1. Read current frontmatter: `CURR_PHASES=$(grep 'completed_phases:' .planning/STATE.md | head -1 | awk '{print $2}')`, `CURR_PLANS=$(grep 'completed_plans:' .planning/STATE.md | head -1 | awk '{print $2}')`. The hard-coded numbers in this plan body (13 → 14 / 53 → 59) are STALE — current STATE.md may show different values. Trust the file, not the plan body.
2. Increment: `completed_phases = CURR_PHASES + 1` (Phase 14 now closed)
3. Increment: `completed_plans = CURR_PLANS + 6` (this phase's 6 plans)
4. Recompute: `percent = round((NEW_PLANS / total_plans) * 100)` where `total_plans` is the existing frontmatter value
5. Update `last_updated` to today's date
6. Append to "Last Session" section: a one-paragraph summary of Phase 14 close — 6 plans across 6 waves, ~272 legacy-token refs eliminated, 67 forbidden-font refs eliminated, shim block deleted, Saira + JetBrains Mono vendored, perf-fallback block (prefers-reduced-motion + data-blur-perf="on") shipped, mascot overlay window now wears v5 chrome with .border-anim.slow.rev, ui-checker + ui-auditor green across all four surfaces, polish-debt items (if any) noted with Kaan-escalation. Reference the shim-delete commit SHA.
7. "Next Session" line: "Continue with Phase 15 — Recording & Session Capture Finalization" (per ROADMAP next-in-line)

**ROADMAP.md**:
1. Change `- [ ] **Phase 14: CDJ Whisper v5 Migration + Polish**` line to `- [x] **Phase 14: ...** _Completed YYYY-MM-DD._`
2. Under "Plans: TBD" replace with the actual 6-plan inventory:
   ```
   Plans: 6 plans
   Plans:
   - [x] 14-01-PLAN.md — Wave 0 reconciliation: scripted gates + vitest harness + vendored WOFF2 + polish log
   - [x] 14-02-PLAN.md — Wave 1 wizard surface migration (225 legacy refs eliminated)
   - [x] 14-03-PLAN.md — Wave 2 live session UI migration + perf-fallback CSS shipped (25 legacy refs + perf toggle wiring)
   - [x] 14-04-PLAN.md — Wave 3 settings drawer migration + Performance group UI (15 legacy refs + new component)
   - [x] 14-05-PLAN.md — Wave 4 mascot overlay window chrome migration (greenfield wrapper + resolveCssColor v5)
   - [x] 14-06-PLAN.md — Wave 5 subtractive shim-delete + repo-wide final sweep
   ```
3. Progress table row 14: `| 14. CDJ Whisper v5 Migration + Polish | 6/6 | Complete | YYYY-MM-DD |`

**14-POLISH-LOG.md**:
1. Populate the remaining Critique Cycles rows with the final SHA + status from each surface's plan SUMMARY (14-02, 14-03, 14-04, 14-05)
2. Add a "Final Sweep" sub-section at the bottom with the Wave 5 repo-wide ui-checker + ui-auditor output refs
3. Update Perf Verification table: macOS row marked ✅ ✅ ✅; Windows row marked either ✅ ✅ ✅ (if Kaan had Windows access) OR ⬜ ⬜ ⬜ with a "deferred to Phase 20 fresh-machine rehearsal" note
4. Add a "Polish Debt" section if any cycle 3 escalation happened (otherwise mark "none")
  </action>
  <verify>
    <automated>grep -F "completed_phases: 14" .planning/STATE.md && grep -F "Phase 14: CDJ Whisper v5 Migration + Polish" .planning/ROADMAP.md | grep -F "[x]" && grep -F "14-06-PLAN.md" .planning/ROADMAP.md && grep -F "Final Sweep" .planning/phases/14-cdj-whisper-v5-migration-polish/14-POLISH-LOG.md && grep -F "6/6" .planning/ROADMAP.md</automated>
  </verify>
  <done>STATE.md progress bumped to 14/20 phases complete with the phase summary line added. ROADMAP.md Phase 14 marked [x] with completion date; 6 plans listed all [x]; Progress table updated. 14-POLISH-LOG.md final sweep recorded; perf verification matrix complete (macOS done, Windows deferred to Phase 20 if needed); polish-debt section populated (or "none" if all cycles closed clean).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-commit hook (one-shot) | Wraps the shim-delete commit with strict-gate enforcement; removed immediately after to avoid blocking unrelated commits |
| Supply chain (vendored WOFF2 → distributed binary) | Saira + JetBrains Mono WOFF2 hashed in LICENSE-3RD-PARTY.md; future regen requires re-attestation |

## STRIDE Threat Register (security_enforcement = ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Tampering (font swap drift) | Vendored WOFF2 binaries | mitigate | SHA-256 attestation in LICENSE-3RD-PARTY.md; both fonts under OFL-1.1; Wave 5 final-sweep verifies hashes match commit body |
| T-14-06-02 | DoS (pre-commit hook persists) | .git/hooks/pre-commit | mitigate | Hook is one-shot — wired at start of task, removed at end of task; cleanup is part of the verify gate (test ! -e .git/hooks/pre-commit) |
| T-14-06-03 | Information disclosure | Phase 14 polish log + shim-delete commit | accept | tokens.css contains no secrets; legacy-token names are public design-system internals; no risk |
| T-14-06-04 | Capability allowlist drift (Elevation) | Tauri capability config | accept | This phase ended visual-only; no Tauri capability surface changed; Phase 11/12/13 invariants intact |
</threat_model>

<verification>
- `bash scripts/check_v5_migration.sh --strict` exits 0 (repo-wide)
- `bash scripts/check_v5_fonts.sh --strict` exits 0 (repo-wide)
- `bash scripts/check_v5_copy.sh --strict` exits 0 (repo-wide)
- `cd tauri/ui && npm run check:ipc` exits 0
- `cd tauri/ui && npm run build` exits 0
- `cd tauri/ui && npm run test` exits 0 (full suite green; all 4 per-surface specs unskipped + mascot.chrome.test.ts green)
- `python scripts/check_ipc_schema.py` exits 0
- `python -m pytest -q` exits 0
- `! grep -E "(--phosphor|--brushed-|--bezel-|--panel-lift|--panel-deep|--groove|--ink|--col-mascot)" tauri/ui/src/tokens.css` (zero hits — shim deleted)
- `! grep -q "fonts.googleapis.com" tauri/ui/src/tokens.css` (Google Fonts @import deleted)
- All 5 legacy WOFF2 files absent under `tauri/ui/public/fonts/`
- Saira-VariableFont_wdth,wght.woff2 + JetBrains Mono Regular/Medium/SemiBold .woff2 present
- `! test -e .git/hooks/pre-commit` (one-shot hook removed)
- `git log -1 --format=%s` contains "delete v5 backward-compat shim"
- LICENSE-3RD-PARTY.md contains Saira-VariableFont + JetBrainsMono entries with SHA-256
- STATE.md `completed_phases: 14` and ROADMAP.md Phase 14 marked [x] with 6/6 plans
- 14-POLISH-LOG.md has all surfaces' final cycle rows + final sweep section + perf-verification matrix
</verification>

<success_criteria>
- Single subtractive commit closes Phase 14 cleanly (one revert restores the shim if needed)
- Repo-wide grep gates green in --strict mode
- ui-checker + ui-auditor green at the repo-wide level (POLISH-06 satisfied)
- POLISH-01 satisfied (dedicated polish phase with the critique → execute loop)
- POLISH-02 satisfied (shim removed; v5 primitives consumed directly)
- POLISH-03 satisfied (mascot overlay composed with v5 chrome — animated-border sweep present, mood swap doesn't tint chrome)
- POLISH-04 satisfied (no FL-Studio tactile residue; Saira + JetBrains Mono only; zero Inter/system-ui/Geist/Fraunces/Workbench/DM Mono/DSEG7/Caveat)
- POLISH-05 satisfied (copy purge green; backdrop-filter perf verified on Kaan's M-series; Windows verification done now OR explicitly deferred to Phase 20)
- POLISH-06 satisfied (ui-checker + ui-auditor green per surface AND repo-wide; backdrop-filter perf toggle verified end-to-end)
- STATE.md + ROADMAP.md reflect Phase 14 complete
- 14-POLISH-LOG.md is the durable artifact of the critique-loop discipline
</success_criteria>

<output>
After completion, create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-06-SUMMARY.md` documenting:
- The shim-delete commit SHA + diff summary (lines deleted from tokens.css, files deleted, files modified)
- LICENSE-3RD-PARTY.md before/after diff
- Pre-commit hook lifecycle (wired @ task start, ran on commit, removed @ task end)
- Repo-wide --strict gate output (zero hits per gate)
- gsd-ui-checker + gsd-ui-auditor repo-wide output refs (POLISH-06)
- Performance toggle persistence rehearsal results (macOS confirmed; Windows confirmed OR deferred to Phase 20)
- Final 14-POLISH-LOG.md state (all surfaces + final sweep)
- Phase 14 close metrics (commits, lines deleted, lines added, refs eliminated)
And create `.planning/phases/14-cdj-whisper-v5-migration-polish/14-SUMMARY.md` (the phase-level summary aggregating all 6 plans' SUMMARYs)
</output>
