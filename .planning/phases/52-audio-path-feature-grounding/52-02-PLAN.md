---
phase: 52-audio-path-feature-grounding
plan: 02
title: "Add psytrance genre profile (GENRE-01 data layer)"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/state/genre/profiles/psytrance.json
  - src/vibemix/state/genre/profiles/__init__.py
  - tests/state/test_psytrance_profile.py
  - tests/state/test_genre_profile.py
autonomous: true
requirements: [GENRE-01]
req_ids: [GENRE-01]
tags: [genre-profile, psytrance, schema, genre-01]
must_haves:
  truths:
    - "src/vibemix/state/genre/profiles/psytrance.json exists and validates through the hand-written _parse_profile (NO pydantic) into a GenreProfile with bpm_range ~[138,150], heavy sub/low band signature, high crest, vocal_likelihood=rare."
    - "psytrance.json mirrors the locked schema 1:1 — all required keys (name, label, bpm_range, absolute_thresholds.{silent_rms,low_rms,peak_rms}, expected_crest_factor, band_signature.{sub,low,mid,high}, vocal_likelihood, build_climb_threshold, breakdown_ratio, drop_jump_threshold) present and well-formed."
    - "list_profiles() returns 6 names including psytrance (sorted: disco, drum_and_bass, house, pop, psytrance, techno); the existing 5-element assertion in test_genre_profile.py is UPDATED so the default suite stays green."
    - "psytrance bpm_range is within [BPM_VALID_MIN=100, BPM_VALID_MAX=180] so the BPM gate + validate_bpm chain never re-introduces an out-of-range value with this profile active."
  artifacts:
    - path: "src/vibemix/state/genre/profiles/psytrance.json"
      provides: "Psytrance GenreProfile JSON mirroring the locked schema — bpm_range [138,150], heavy sub/low offbeat-bass signature, crest [4.0,7.0], vocal_likelihood rare, steeper build/drop thresholds than techno."
    - path: "tests/state/test_psytrance_profile.py"
      provides: "Unit test: load_profile('psytrance') returns a valid GenreProfile with the locked values; bpm_range within [100,180]; band shares sum near 1.0; the hand-written validator accepts it."
      min_lines: 50
    - path: "tests/state/test_genre_profile.py"
      provides: "Updated 5-list assertion to a 6-list including psytrance; any PROFILE_NAMES iteration covers psytrance."
  key_links:
    - from: "tests/state/test_psytrance_profile.py"
      to: "src/vibemix/state/genre/profiles/psytrance.json"
      via: "load_profile('psytrance') then _parse_profile"
      pattern: "psytrance"
---

<objective>
Add the missing `psytrance` genre profile — the direct cause of Kaan's psytrance misclassification (today the library has only disco/drum_and_bass/house/pop/techno, so psytrance ran under no profile or `techno`'s wrong band-signature + phase thresholds). Ship `src/vibemix/state/genre/profiles/psytrance.json` mirroring the locked schema, validated through the existing hand-written `_parse_profile` (no pydantic — Critical Constraint 6), and update the one default-suite test that hard-asserts exactly 5 profiles so the suite stays green.

Purpose: this is the data prerequisite for the GENRE-01 auto-detector (Plan 52-03) — the detector scores nearest-match across `list_profiles()`, so psytrance must be in the library before the detector can ever pick it. Anti-slop: the profile must be grounded (real psy band signature + tempo), not a guess.

Output: one JSON profile + its load-validation test + the count-assertion fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-audio-path-feature-grounding/52-CONTEXT.md
@.planning/phases/52-audio-path-feature-grounding/52-RESEARCH.md

@src/vibemix/state/genre/profile.py
@src/vibemix/state/genre/profiles/techno.json
@src/vibemix/state/genre/profiles/drum_and_bass.json
@tests/state/test_genre_profile.py

<interfaces>
<!-- Verified against HEAD 177ff38. -->

Locked schema (profile.py:9-27 docstring + _parse_profile at :87). Required JSON keys:
  name (non-empty str), label (non-empty str), bpm_range ([lo,hi] numbers),
  absolute_thresholds {silent_rms, low_rms, peak_rms} (non-negative numbers),
  expected_crest_factor ([lo,hi] numbers),
  band_signature {sub, low, mid, high} (each [lo,hi] numbers),
  vocal_likelihood (one of: rare, occasional, frequent, very_frequent),
  build_climb_threshold (number), breakdown_ratio (number), drop_jump_threshold (number).
_parse_profile raises ValueError on any missing/malformed field — no silent defaults.

list_profiles() (profile.py:179) returns sorted .json stems. load_profile(name) (profile.py:197) returns GenreProfile or None. GenreProfile is a frozen dataclass (profile.py:55).

BPM_VALID_MIN=100, BPM_VALID_MAX=180 (audio/constants.py:112-113).

THE LANDMINE: tests/state/test_genre_profile.py:28 has PROFILE_NAMES = ["disco","drum_and_bass","house","pop","techno"] and test_list_profiles_returns_all_five (around :47) asserts list_profiles() == PROFILE_NAMES. Adding psytrance.json makes list_profiles() return 6 entries (psytrance sorts between pop and techno). This test WILL go red unless updated in THIS plan. Also check for any loop over PROFILE_NAMES that asserts per-profile sanity — psytrance must satisfy the same sanity checks (band shares sum near 1.0, bpm range increasing within 80-200, crest range positive, RMS thresholds strictly increasing).
</interfaces>

<recommended_values>
<!-- Grounded recommendation from 52-RESEARCH Q3. Mirror techno's absolute_thresholds; tune the rest for psy. -->
name psytrance; label "Psytrance / Goa / Full-On"; bpm_range [138, 150];
absolute_thresholds silent_rms 0.012, low_rms 0.040, peak_rms 0.110;
expected_crest_factor [4.0, 7.0];
band_signature sub [0.30,0.50], low [0.20,0.35], mid [0.08,0.20], high [0.05,0.15];
vocal_likelihood rare; build_climb_threshold 0.028; breakdown_ratio 0.40; drop_jump_threshold 0.065.
(bpm_range fully inside [100,180]. Band lows/highs differ enough from techno's sub [0.25,0.45] / mid [0.10,0.25] that the detector can discriminate on the heavier-sub/lighter-mid psy signature.)
</recommended_values>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author psytrance.json mirroring the locked schema</name>
  <files>src/vibemix/state/genre/profiles/psytrance.json, src/vibemix/state/genre/profiles/__init__.py</files>
  <read_first>
    - src/vibemix/state/genre/profiles/techno.json (exact JSON shape + key order to mirror)
    - src/vibemix/state/genre/profiles/drum_and_bass.json (high-sub heavy-bass reference)
    - src/vibemix/state/genre/profile.py (_parse_profile required keys + valid vocal_likelihood set)
  </read_first>
  <action>
    Create src/vibemix/state/genre/profiles/psytrance.json with the recommended grounded values from the recommended_values block: name psytrance, label "Psytrance / Goa / Full-On", bpm_range [138, 150], absolute_thresholds silent_rms 0.012 / low_rms 0.040 / peak_rms 0.110, expected_crest_factor [4.0, 7.0], band_signature sub [0.30,0.50] / low [0.20,0.35] / mid [0.08,0.20] / high [0.05,0.15], vocal_likelihood rare, build_climb_threshold 0.028, breakdown_ratio 0.40, drop_jump_threshold 0.065. Match techno.json key ordering + 2-space indent.
    Update src/vibemix/state/genre/profiles/__init__.py docstring: the "5 shipped profiles" line becomes "6 shipped profiles (techno / house / drum_and_bass / disco / pop / psytrance)".
    Commit with feat(52-02): add psytrance genre profile (GENRE-01).
  </action>
  <acceptance_criteria>
    - src/vibemix/state/genre/profiles/psytrance.json exists and is valid JSON.
    - bpm_range equals [138, 150], vocal_likelihood equals rare, band_signature has sub/low/mid/high pairs, sub upper bound greater than mid upper bound (heavier sub signature).
    - profiles/__init__.py docstring names 6 profiles including psytrance.
    - load_profile('psytrance') returns a GenreProfile with name psytrance and bpm_range (138.0, 150.0).
  </acceptance_criteria>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -c "from vibemix.state.genre import load_profile, list_profiles; p=load_profile('psytrance'); assert p is not None and p.name=='psytrance' and p.bpm_range==(138.0,150.0) and p.vocal_likelihood=='rare'; print('OK', list_profiles())"</automated>
  </verify>
  <done>psytrance.json exists, validates via _parse_profile, and is in list_profiles().</done>
</task>

<task type="auto">
  <name>Task 2: Profile load-validation test (GENRE-01)</name>
  <files>tests/state/test_psytrance_profile.py</files>
  <read_first>
    - tests/state/test_genre_profile.py (test_load_profile_techno_returns_genreprofile shape + the sensible-values pattern + the _reset_active_profile autouse fixture)
    - src/vibemix/state/genre/profile.py (GenreProfile fields + _parse_profile)
    - src/vibemix/state/genre/profiles/psytrance.json (the values to assert)
  </read_first>
  <action>
    Create tests/state/test_psytrance_profile.py. Mirror test_load_profile_techno_returns_genreprofile: assert load_profile('psytrance') is a GenreProfile with name psytrance, label "Psytrance / Goa / Full-On", bpm_range (138.0, 150.0), expected_crest_factor (4.0, 7.0), vocal_likelihood rare. Assert the band_signature dict has sub/low/mid/high tuple pairs and that the sub band signature is heavier than mid (sub[1] > mid[1]) — the offbeat-rolling-bass property. Assert bpm_range is fully within [BPM_VALID_MIN, BPM_VALID_MAX] (import from vibemix.audio.constants). Assert the absolute RMS thresholds are strictly increasing (silent < low < peak). Add a test that the profile passes the hand-written validator without raising (load_profile already calls _parse_profile, so a successful non-None load proves it; optionally re-validate the raw JSON via _parse_profile directly to be explicit).
    Commit with test(52-02): pin psytrance profile loads + validates with locked values (GENRE-01).
  </action>
  <acceptance_criteria>
    - tests/state/test_psytrance_profile.py asserts the locked psytrance values (bpm_range, crest, vocal_likelihood, band signature shape).
    - A test asserts bpm_range within [BPM_VALID_MIN, BPM_VALID_MAX].
    - A test asserts RMS thresholds strictly increasing and sub band heavier than mid.
    - pytest -q tests/state/test_psytrance_profile.py exits 0.
  </acceptance_criteria>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_psytrance_profile.py 2>&1 | tail -20</automated>
  </verify>
  <done>psytrance profile values pinned; the hand-written validator accepts it.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the 5-profile count assertion (keep the default suite green)</name>
  <files>tests/state/test_genre_profile.py</files>
  <read_first>
    - tests/state/test_genre_profile.py (PROFILE_NAMES at :28; test_list_profiles_returns_all_five; any loop over PROFILE_NAMES for sensible-values checks)
    - src/vibemix/state/genre/profiles/psytrance.json (so the per-profile sanity loop, if any, will pass for psytrance)
  </read_first>
  <action>
    Update tests/state/test_genre_profile.py: change PROFILE_NAMES to the 6-element sorted list ["disco","drum_and_bass","house","pop","psytrance","techno"]. Update test_list_profiles_returns_all_five (rename to ..._returns_all_six if a name is asserted in messages, optional) so it asserts list_profiles() equals the 6-element list. Confirm any sensible-values loop that iterates PROFILE_NAMES now also covers psytrance and that psytrance satisfies the same invariants (band shares sum near 1.0, bpm range increasing within 80-200, crest range positive, RMS thresholds strictly increasing) — psytrance's values were chosen to satisfy these, but verify by running the test.
    Commit with test(52-02): expand profile-count assertion to include psytrance (GENRE-01).
  </action>
  <acceptance_criteria>
    - PROFILE_NAMES in test_genre_profile.py is the 6-element sorted list including psytrance.
    - The list-equality test asserts the 6-element list (no longer 5).
    - pytest -q tests/state/test_genre_profile.py exits 0 (incl. any per-profile sanity loop over psytrance).
  </acceptance_criteria>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_genre_profile.py 2>&1 | tail -20</automated>
  </verify>
  <done>Adding psytrance does not break the default suite; the count assertion reflects 6 profiles.</done>
</task>

</tasks>

<verification>
- source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/state/test_psytrance_profile.py tests/state/test_genre_profile.py exits 0.
- source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q exits 0 (full default suite green — the new profile did not break the count assertion or any PROFILE_NAMES loop).
- python3 -c "import json; json.load(open('src/vibemix/state/genre/profiles/psytrance.json'))" exits 0 (valid JSON).
</verification>

<success_criteria>
1. psytrance.json exists, validates through the hand-written _parse_profile (no pydantic), and carries grounded psy values (bpm [138,150], heavy sub, crest [4,7], rare vocals) — GENRE-01 data layer.
2. list_profiles() includes psytrance; the detector (Plan 03) can now score it.
3. The default suite stays green — the 5-profile count assertion is updated to 6.
</success_criteria>

<output>
After completion, create .planning/phases/52-audio-path-feature-grounding/52-02-SUMMARY.md documenting:
- The final psytrance values shipped + the grounding rationale (vs techno).
- Confirmation it validates via the hand-written _parse_profile (no pydantic).
- The count-assertion fix that kept the suite green.
- Commits (3 atomic).
</output>
