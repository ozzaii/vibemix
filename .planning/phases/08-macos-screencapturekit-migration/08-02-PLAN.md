---
phase: 08-macos-screencapturekit-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - docs/macos-screencapturekit.md
  - .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md
autonomous: true
requirements:
  - SCREEN-01   # macOS screen capture — Phase 8 documentation + phase close-out
tags:
  - docs
  - phase-summary
  - macos-12.3
  - permissions
must_haves:
  truths:
    - "docs/macos-screencapturekit.md exists and is a single-page reference for the macOS 12.3+ minimum + Screen & System Audio Recording permission flow + Phase 11 calibration-wizard handoff."
    - "08-SUMMARY.md exists with the Phase 7 SUMMARY shape (wave_commits frontmatter + acceptance gates table + What Phase 8 Delivered + Architecture Decisions Pinned + What's Next)."
    - "Both files are committed; orchestrator (not this plan) advances STATE.md and ROADMAP.md."
    - "Acceptance gates table verifies all 4 ROADMAP success criteria for Phase 8 are PASS."
  artifacts:
    - path: "docs/macos-screencapturekit.md"
      provides: "User-facing doc — macOS minimum, permission prompt flow, troubleshooting for first-run prompt + System Settings → Privacy & Security → Screen Recording, what changed vs the old mss-based capture, what's deferred to Phase 11 (calibration wizard) and Phase 16 (live verification)."
      min_lines: 60
    - path: ".planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md"
      provides: "Phase 8 rollup — wave commits, test count delta, 4 ROADMAP success criteria mapped to acceptance gates, decisions pinned, deferred items, what's next."
      min_lines: 70
  key_links:
    - from: "docs/macos-screencapturekit.md"
      to: "Phase 11 calibration wizard"
      via: "explicit handoff section — wizard owns the first-run permission-prompt UX"
      pattern: "Phase 11"
    - from: ".planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md"
      to: "08-01-PLAN.md wave commits"
      via: "wave_commits frontmatter listing the 3 commit SHAs"
      pattern: "wave_commits:"
---

<objective>
Wave 2 closes Phase 8:

1. Ship `docs/macos-screencapturekit.md` — a one-page user-facing doc covering the macOS 12.3+ minimum, the Screen & System Audio Recording permission prompt, what changed vs. the old `mss`-based capture, and the explicit Phase 11 (calibration wizard) + Phase 16 (live verification) handoffs. Mirrors `docs/windows-setup.md` in style + length (~60-100 lines, sectioned).
2. Write `08-SUMMARY.md` rolling up Wave 1 — 3 wave commits, test count delta, acceptance gates table mapping the 4 ROADMAP Success Criteria to verified results, decisions pinned, deferred items, what's next (Phase 9).

Purpose: gives Kaan a single doc to point Bravoh-side users at when they hit "ScreenCaptureKit needs permission" + gives the next phase a clean handoff record (same shape as Phase 7 SUMMARY which Phase 8 explicitly references).

This plan does NOT advance STATE.md or ROADMAP.md — the orchestrator (`/gsd-transition` or `/gsd-autonomous`) owns that step per CONTEXT instruction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-macos-screencapturekit-migration/08-CONTEXT.md
@.planning/phases/08-macos-screencapturekit-migration/08-01-PLAN.md
@.planning/phases/07-windows-port-audio-screen/07-SUMMARY.md
@docs/windows-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/macos-screencapturekit.md</name>
  <files>docs/macos-screencapturekit.md</files>
  <action>
    Create `docs/macos-screencapturekit.md` with the following section structure (mirror `docs/windows-setup.md` tone — direct, no fluff, code blocks where they help):

    1. **Title + one-paragraph summary** — "Starting Phase 8, vibemix's macOS screen capture uses Apple's ScreenCaptureKit (`pyobjc-framework-ScreenCaptureKit`) instead of the older `mss` library. This page covers what you need (macOS 12.3+), what to expect on first run (a system permission prompt), and where to look if capture stops working."
    2. **macOS minimum** — 12.3+ is required (ScreenCaptureKit's minimum since 2022 WWDC). macOS 11 / 10.x explicitly unsupported (matches PROJECT.md). On macOS 14 and earlier, the old `Quartz.CGWindowListCreateImageFromArray` path still works but is deprecated; macOS 15.0 obsoletes it entirely — vibemix would silently fail on macOS 15+ without this migration. Hence Phase 8.
    3. **What ScreenCaptureKit needs (one-time setup)** — On the first launch where vibemix tries to capture screen frames, macOS pops the **Screen & System Audio Recording** permission prompt. Approving it adds vibemix to **System Settings → Privacy & Security → Screen & System Audio Recording**. The Phase 11 calibration wizard surfaces this proactively (so the prompt fires during the friendly first-run flow, not mid-set).
    4. **What stayed the same** — `Quartz.CGWindowListCopyWindowInfo` still enumerates windows for the picker. That Quartz API is NOT deprecated; ScreenCaptureKit's `SCShareableContent` is heavier and async for what's essentially a synchronous list-windows query, so we kept the lightweight Quartz path. (Decision D-Enumeration API in Phase 8 CONTEXT.)
    5. **What changed under the hood (one paragraph for the curious)** — Window-targeted capture uses `SCContentFilter.initWithDesktopIndependentWindow_` against the SCWindow whose ID matches the Quartz-enumerated `kCGWindowNumber`. `SCStream` delivers frames to an Objective-C delegate on a private dispatch queue; the delegate converts CMSampleBuffer → CVPixelBuffer → PIL → JPEG synchronously and pushes the bytes into the same thread-safe `_ScreenBuffer` the old `mss` path used. The asyncio runtime reads via `latest()` at 1Hz. Privacy gate: vibemix never captures full-screen — a specific window must be picked in the calibration wizard, no exceptions, no fallback.
    6. **Troubleshooting**
       - "Screen capture stopped working after a macOS update" → System Settings → Privacy & Security → Screen & System Audio Recording → make sure vibemix is checked. macOS sometimes resets these on major version bumps.
       - "No window appears in the picker" → confirm the DJ app is open and on the active Space; vibemix uses `kCGWindowListOptionOnScreenOnly` so off-screen windows don't appear.
       - "Permission prompt didn't appear" → quit vibemix, delete the app from the privacy list (if present), re-launch. The prompt only fires on the FIRST `SCShareableContent.getShareableContentWithCompletionHandler_` call after a permission reset.
       - "macOS 11 user reports broken capture" → not supported. Direct them to macOS 12.3 or later. (Same as PROJECT.md.)
    7. **What's deferred to later phases**
       - **Phase 11 (Tauri Shell + Calibration Wizard)** owns the first-run UX — surfacing the permission prompt friendly-style, showing the picker, persisting the chosen window/app handle, and handling re-pick on app launch.
       - **Phase 16 (Hallucination Verification Gate)** runs the first authoritative live screen-capture tests on real macOS 15 with a real Rekordbox / djay / Serato window.
       - **Phase 20 (Day-Zero Operations)** repeats the live capture verification on a fresh macOS install (the "I just bought a new Mac" scenario).
       - **System-audio capture via ScreenCaptureKit** is out of scope — audio is owned by Phase 2's BlackHole/sounddevice path; ScreenCaptureKit's audio API isn't used.

    Length target: 60-100 lines. Keep it scannable. No fluff.
  </action>
  <verify>
    <automated>test -f docs/macos-screencapturekit.md && wc -l docs/macos-screencapturekit.md | awk '{exit ($1>=60 && $1<=140)?0:1}' && grep -q "12.3" docs/macos-screencapturekit.md && grep -q "Screen & System Audio Recording" docs/macos-screencapturekit.md && grep -q "Phase 11" docs/macos-screencapturekit.md && grep -q "Phase 16" docs/macos-screencapturekit.md && grep -qi "no full-screen" docs/macos-screencapturekit.md</automated>
  </verify>
  <done>
    `docs/macos-screencapturekit.md` exists, 60-140 lines, covers all 7 sections, references macOS 12.3 + Screen Recording permission + Phase 11 + Phase 16 + the no-full-screen privacy invariant. Commit message: `docs(08-02): macOS ScreenCaptureKit user reference + Phase 11 handoff`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 08-SUMMARY.md + acceptance gates table</name>
  <files>.planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md</files>
  <action>
    Create `.planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md` mirroring `.planning/phases/07-windows-port-audio-screen/07-SUMMARY.md` shape. Required sections:

    1. **YAML frontmatter** with `phase: 08-macos-screencapturekit-migration`, `plan: rollup`, `type: summary`, `status: complete`, `completed_at: 2026-05-11` (or actual date at execute time), `requirements_covered: [SCREEN-01]`, `wave_commits` (3 SHAs from 08-01 RED/GREEN/regression + 1 from 08-02 docs commit — fill in at execute time via `git log --oneline -10 | head`), `test_count`, `test_delta` (vs Phase 7's 614 baseline).

    2. **Verdict line** — one sentence: "All N acceptance gates PASS — Phase 8 is shipped — ScreenCaptureKit is now the macOS capture path; `mss` removed from the macOS dep set."

    3. **What Phase 8 Delivered** — bullets for production code (`_screen_macos.py` rewrite), tests (new SCKit-shape tests + lazy-import contract pin), deps (`pyobjc-framework-ScreenCaptureKit` added; `mss` scoped to win32), docs (`macos-screencapturekit.md`).

    4. **Architecture Decisions Pinned** — at minimum:
       - D-Capture API: ScreenCaptureKit SCStream + delegate-on-dispatch-queue + thread-safe ring (single-slot) for frame delivery. Single-shot `capture()` waits one frame via `threading.Event`; `run_capture_loop` runs a long-lived stream and reads via `latest()` at 1Hz.
       - D-Enumeration API: Quartz.CGWindowListCopyWindowInfo retained — NOT deprecated; lighter than SCShareableContent.
       - D-Privacy gate: `capture(bounds=None)` raises; no full-screen fallback; grep-asserted in tests.
       - D-Async bridge: `loop.run_in_executor` for SCKit synchronous start/stop helpers; matches Phase 7's winsdk pattern explicitly anticipated in 07-SUMMARY §Architecture Decisions Pinned #2.
       - `mss` removed from macOS path; scoped to `sys_platform == 'win32'` for ScreenWindows.
       - macOS minimum bumped to 12.3 (matches PROJECT.md and ScreenCaptureKit's framework minimum).
       - `pyobjc-framework-ScreenCaptureKit>=12.1` added (or `pyobjc` umbrella per the fallback path documented in 08-01 if the per-framework wheel name was wrong on PyPI).

    5. **Acceptance Gates Table** — 4 rows mapping the ROADMAP Success Criteria to verified results:
       | # | Description | Status | Evidence |
       |---|-------------|--------|----------|
       | 1 | macOS 15.0+ screen capture works via SCStream callback | ✅ Structural | Mocked SCStream delegate tests pass; live verification deferred to Phase 16 / Phase 20 per CONTEXT |
       | 2 | macOS 12.3-14.x compatibility preserved | ✅ Structural | `requires-python` + dep markers, mocked tests run on 12.3+ CI |
       | 3 | Window picker enumeration via CGWindowListCopyWindowInfo | ✅ PASS | Phase 3 tests retained byte-identical; verified by `grep -c CGWindowListCopyWindowInfo` |
       | 4 | Privacy gate — no full-screen fallback | ✅ PASS | `test_capture_raises_when_bounds_is_none` + grep gate `test_no_full_screen_fallback_in_source` |

    Plus operational gates:
       | # | Description | Status |
       |---|-------------|--------|
       | 5 | Full test suite green (Phase 7 baseline + Phase 8 deltas) | ✅ PASS |
       | 6 | `ruff check src/ tests/` clean on touched files | ✅ PASS |
       | 7 | `ruff format --check` clean on touched files | ✅ PASS |
       | 8 | POC files (`cohost*.py`, `run*.sh`, `fillers/`, `mascot.html`) untouched | ✅ PASS |
       | 9 | `CGWindowListCreateImageFromArray` grep returns 0 in `src/vibemix/` | ✅ PASS |
       | 10 | `mss` import grep returns 0 in `_screen_macos.py` | ✅ PASS |

    6. **Test Count Delta** — Phase 7 baseline 614; Phase 8 final = 614 + (net new from 08-01 Task 1 + Task 3 ≈ +10-12 tests).

    7. **Deferred to Future Phases** — bullets:
       - Live ScreenCaptureKit verification on macOS 15 with Rekordbox/djay/Serato → Phase 16 + Phase 20.
       - Permission-prompt UX (friendly first-run flow) → Phase 11 calibration wizard.
       - ScreenCaptureKit system-audio capture → out of v1 scope (audio path is Phase 2's BlackHole/sounddevice).
       - macOS 11 / 10.x → permanently dropped per PROJECT.md.

    8. **What's Next** — "Phase 9 (MIDI Controller Library — 10 + Generic Fallback) is unblocked and explicitly parallel to Phase 8 per ROADMAP. ControllerState extraction from `_midi_macos` into `_midi_common.py` (carry-over from Phase 7 SUMMARY §What's Next) lands in Phase 9."

    Commit message: `docs(08-02): phase SUMMARY + acceptance gates — Phase 8 closed`.

    DO NOT touch `.planning/STATE.md` or `.planning/ROADMAP.md` — orchestrator owns those (per CONTEXT.md).
  </action>
  <verify>
    <automated>test -f .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md && grep -q "^phase: 08-macos-screencapturekit-migration" .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md && grep -q "wave_commits:" .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md && grep -q "ScreenCaptureKit" .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md && grep -q "CGWindowListCopyWindowInfo" .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md && grep -q "Phase 9" .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md && wc -l .planning/phases/08-macos-screencapturekit-migration/08-SUMMARY.md | awk '{exit ($1>=70)?0:1}' && git status .planning/STATE.md .planning/ROADMAP.md 2>&1 | grep -qE 'nothing to commit|not modified|^$' || git diff --quiet .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <done>
    `08-SUMMARY.md` exists, has the Phase 7 shape (frontmatter + verdict + sections), maps all 4 ROADMAP success criteria + 6 operational gates to verified results, references Phase 9 as next. STATE.md and ROADMAP.md untouched by this plan's commits. Commit landed.
  </done>
</task>

</tasks>

<verification>
- `docs/macos-screencapturekit.md` exists, sectioned, references macOS 12.3, Screen Recording permission, Phase 11 + Phase 16 handoffs, and the no-full-screen invariant.
- `08-SUMMARY.md` exists with Phase 7 SUMMARY shape — frontmatter + acceptance gates table + decisions pinned + what's next.
- Both commits land with `docs(08-02):` prefix.
- STATE.md and ROADMAP.md are NOT modified by this plan's commits (orchestrator owns those).
- POC files unchanged.
</verification>

<success_criteria>
- Documentation deliverable: one page, complete, scannable.
- Phase SUMMARY: shape matches Phase 7's; gates table proves the 4 ROADMAP success criteria.
- Orchestrator can transition Phase 8 → Phase 9 after this plan ships because all close-out artifacts are in place.
</success_criteria>

<output>
After completion, the orchestrator (`/gsd-transition` or `/gsd-autonomous`) runs to advance `.planning/STATE.md` (`progress.completed_phases: 8`, position → Phase 9) and to check Phase 8 in `.planning/ROADMAP.md`.
</output>
