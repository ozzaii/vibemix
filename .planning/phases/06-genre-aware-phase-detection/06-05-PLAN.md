---
phase: 06-genre-aware-phase-detection
plan: 05
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - .planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - SENSE-03
  - SENSE-04
  - SENSE-06
  - SENSE-07
  - SENSE-08
  - SENSE-10
  - UX-03
must_haves:
  truths:
    - "All 10 verification gates below PASS (see <verification>) — import probe, all 5 profiles loadable, Phase 3 golden equivalence, percentile detector on synthetic curves, BPM half/double matrix, crest factor math, vocal detector hysteresis, EventDetector LAYER_ARRIVAL gate, MusicState 4 new fields, full pytest suite + ruff."
    - "`.planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md` exists and follows the same template as `.planning/phases/03-sensing-state-port/03-SUMMARY.md` (frontmatter + sections: What Phase 6 Delivered / Requirements Coverage / Files / Architectural Decisions Locked / Deviations from Plan / Dependent Phases Unlocked / Open Items Carried Forward / v4-vs-Phase-6 Phase-Classification Diff / Verification Snapshot / Commit History / Self-Check)."
    - "`.planning/STATE.md` updated: Current Position phase advanced to `07 — Windows Port (Audio + Screen) (next)`. Progress bar reflects 6/20 phases complete (`[██████              ] 30%`). Performance Metrics `Phases complete` shows 6/20. Phase 6 marked ✅ in Last Session block with the 4 wave commit hashes."
    - "`.planning/ROADMAP.md` Phase 6 entry: `- [ ]` → `- [x]` with completion date. Plans list at the Phase 6 detail block: all 5 plan checkboxes flip to `- [x]`."
    - "v4-vs-Phase-6 diff section in the SUMMARY shows the practical classification differences Kaan should expect (Critical Constraint 10)."
    - "POC files diff-untouched throughout the entire phase (cohost_v4.py + run_v4.sh continue to function unchanged)."
  artifacts:
    - path: ".planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md"
      provides: "Phase 6 rollup — what shipped, requirements covered, files, decisions, deviations, downstream-unlocked phases, verification table, commit history, v4-vs-P6 phase-diff section, self-check."
      contains: "Phase 6 — Genre-Aware Phase Detection — Summary"
    - path: ".planning/STATE.md"
      provides: "Updated Current Position + Last Session + Next Session + Performance Metrics reflecting Phase 6 completion."
      contains: "Phase 6 ✅"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 entry marked complete; Plans list 5/5 checked."
      contains: "Phase 6: Genre-Aware Phase Detection"
  key_links:
    - from: ".planning/STATE.md"
      to: "wave commits"
      via: "git log"
      pattern: "feat\\(06\\):"
---

<objective>
Verification gate + phase SUMMARY for Phase 6. Runs after waves 1-4 have shipped (4 atomic `feat(06)` commits). Verifies the phase delivered against ROADMAP.md success criteria + the 10 acceptance gates below. On PASS, writes `06-SUMMARY.md`, advances STATE.md to Phase 7, marks ROADMAP entry complete.

Purpose: Lock the close, capture decisions, surface the v4-vs-Phase-6 classification diff so Kaan understands what changed when he runs the binary post-Phase-6.
Output: 06-SUMMARY.md + STATE.md + ROADMAP.md updated; single docs commit; phase closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-genre-aware-phase-detection/06-CONTEXT.md
@.planning/phases/06-genre-aware-phase-detection/06-01-PLAN.md
@.planning/phases/06-genre-aware-phase-detection/06-02-PLAN.md
@.planning/phases/06-genre-aware-phase-detection/06-03-PLAN.md
@.planning/phases/06-genre-aware-phase-detection/06-04-PLAN.md

<!-- Reference template — match its structure for the new SUMMARY: -->
@.planning/phases/03-sensing-state-port/03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 10-gate verification matrix</name>
  <files></files>
  <action>
Execute each verification command below. Capture exit codes and key output snippets. If ANY gate fails, stop and surface the failure for triage BEFORE writing the SUMMARY — do not paper over.

**Gate 1 — Import probe (all symbols accessible):**
```
uv run python -c "from vibemix.state.genre import GenreProfile, load_profile, list_profiles, set_active_profile, get_active_profile, classify_phase_percentile, HysteresisState, crest_factor, EmaSmoother, validate_bpm, VocalDetector; print('ok')"
```
Expected: exit 0, prints `ok`.

**Gate 2 — All 5 profiles loadable:**
```
uv run python -c "from vibemix.state.genre import list_profiles, load_profile; names=list_profiles(); assert names==['disco','drum_and_bass','house','pop','techno']; profs=[load_profile(n) for n in names]; assert all(p is not None for p in profs); print('ok')"
```

**Gate 3 — Phase 3 golden equivalence preserved (classify_phase with profile=None matches v4-equivalent path):**
```
uv run pytest tests/state/test_phase.py -v -k "profile_none_equals_phase_3 or positional_call_still_works"
```
Expected: tests pass.

**Gate 4 — Percentile detector produces sensible outputs on synthetic curves:**
```
uv run pytest tests/state/test_genre_detector.py -v
```
Expected: ~14 tests green.

**Gate 5 — BPM half/double matrix:**
```
uv run pytest tests/state/test_bpm_validator.py -v
```
Expected: ~12 tests green (62→124 boundary case + 250→125 + 200→100 pop + zero/negative short-circuit).

**Gate 6 — Crest factor math:**
```
uv run pytest tests/state/test_crest_factor.py -v
```
Expected: ~9 tests green (sine ≈ √2, square ≈ 1, impulse > 20).

**Gate 7 — Vocal detector hysteresis:**
```
uv run pytest tests/state/test_vocal_detector.py -v
```
Expected: ~12 tests green covering 1.5s in / 2.5s out + brief-dip-resilience + rule combinations.

**Gate 8 — EventDetector LAYER_ARRIVAL gate verified suppressed AND verified fires-when-not-vocal:**
```
uv run pytest tests/state/test_event_detector.py -v -k "vocal"
```
Expected: 4 new tests green.

**Gate 9 — MusicState has 4 new fields with backward-compat defaults:**
```
uv run python -c "from vibemix.state.music_state import MusicState; s=MusicState(); assert s.crest_factor==0.0 and s.vocal_active is False and s.bpm_corrected is False and s.genre_profile_name=='unknown'; print('ok')"
```

**Gate 10 — Full pytest suite + ruff hygiene:**
```
uv run pytest tests/ -q --ignore=tests/test_audio_macos_live.py --ignore=tests/test_main_live.py
uv run ruff check src/vibemix/ tests/
uv run ruff format --check src/vibemix/ tests/
```
Expected: all green. Target test count: ~450+ (Phase 5 baseline 385 + ~65-85 new across Phase 6 waves).

**POC-untouched check** (additional safety check, not numbered as a gate but mandatory before SUMMARY write):
```
git diff --name-only $(git log --format=%H --all -- cohost_v4.py | tail -1)..HEAD -- 'cohost*.py' 'run*.sh' mascot.html fillers/ cohost.streaming.py.bak
```
Expected: empty output. If anything appears here, halt — POC files MUST be diff-untouched (Critical Constraint 1).

Record each gate's pass/fail + key numbers (test counts, version strings) for the SUMMARY's Verification Snapshot table.

If any gate fails:
- Investigate root cause (read the failing test output, the file that changed it).
- If trivial (typo, missing field): fix and re-run the gate. Do NOT write the SUMMARY yet.
- If non-trivial: surface the failure to Kaan with a clear summary; do not auto-fix architectural issues. Halt the close.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q --ignore=tests/test_audio_macos_live.py --ignore=tests/test_main_live.py</automated>
  </verify>
  <done>10 gates green + POC-untouched verified. Verification snapshot data captured for SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Surface verification report and confirm Phase 6 close</name>
  <files></files>
  <action>
This is a blocking human-verify checkpoint. Before writing the SUMMARY, surface a brief verification report to Kaan and wait for explicit approval.

**Report to print:**
- Wave commits (4x `feat(06)` SHAs from `git log --oneline | grep "feat(06):"`).
- Verification gate table (10 PASS lines from Task 1).
- Total test count delta (Phase 5: 385 → Phase 6 actual count).
- v4-vs-Phase-6 classification diff preview (one-paragraph version of the SUMMARY section).
- POC-untouched confirmation (`git diff --name-only ...` empty output line).

**Why this checkpoint exists:** Phase 6 changes the runtime behavior of phase detection (the heart of the AI's reactivity). Kaan should confirm he wants to advance STATE.md + ROADMAP.md before the docs commit lands.
  </action>
  <verify>
    <human-checks>
1. Read the verification report printed above.
2. Optional: `uv run python -c "from vibemix.state import set_active_profile, get_active_profile; set_active_profile('techno'); print(get_active_profile())"` to spot-check the active-profile singleton.
3. Optional: spot-check one profile JSON, e.g. `cat src/vibemix/state/genre/profiles/techno.json`.
4. Optional: confirm `cohost_v4.py` still runs (Critical Constraint 1) — optional live check, `./run_v4.sh` on Kaan's rig.
    </human-checks>
  </verify>
  <done>Kaan typed "approved" or equivalent affirmative. Resume to write SUMMARY + advance STATE.md + ROADMAP.md.</done>
  <resume-signal>Type "approved" to write the SUMMARY + advance STATE.md + ROADMAP.md, or describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 06-SUMMARY.md following the 03-SUMMARY template + v4-vs-P6 diff section</name>
  <files>.planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md</files>
  <action>
Mirror the structure of `.planning/phases/03-sensing-state-port/03-SUMMARY.md` (front-to-back). Sections in order:

1. **Frontmatter**: phase id, completion date, plan count (5/5), wave count (4 implementation + 1 close), test count delta, requirement IDs covered.

2. **What Phase 6 Delivered** — bullet rundown by wave:
   - Wave 1: genre subpackage + GenreProfile dataclass + 5 JSON profiles + active-profile singleton + ~18 tests.
   - Wave 2: crest_factor + EmaSmoother + validate_bpm + VocalDetector (1.5s/2.5s hysteresis) + ~33 tests.
   - Wave 3: 4 new MusicState fields + classify_phase_percentile + HysteresisState + classify_phase dispatch + state_refresh_loop wiring + ~30 tests; Phase 3 golden-equivalence pinned.
   - Wave 4: EventDetector LAYER_ARRIVAL gate on vocal_active + VIBEMIX_GENRE_PROFILE env (default 'techno') + vibemix.state re-exports + ~9 tests.

3. **Requirements Coverage** — table mapping the 7 requirement IDs to plans:
   | Req | Phase | Where shipped |
   |-----|-------|----------------|
   | SENSE-03 | Phase 6 | Percentile detector (06-03 detector.py + phase.py dispatch) |
   | SENSE-04 | Phase 6 | 5 genre profile JSONs (06-01 profiles/*.json) |
   | SENSE-06 | Phase 6 | Crest factor + EMA (06-02 crest_factor.py) + state_refresh_loop write (06-03) |
   | SENSE-07 | Phase 6 | BPM half/double validator (06-02 bpm_validator.py) + state_refresh_loop apply (06-03) |
   | SENSE-08 | Phase 6 | Vocal detector + hysteresis (06-02) + LAYER_ARRIVAL gate (06-04) + state.vocal_active write (06-03) |
   | SENSE-10 | Phase 6 (PARTIAL) | Detector implemented; 30-min recorded-set validation harness deferred to Phase 16 per CONTEXT out-of-scope clause. |
   | UX-03 | Phase 6 | VIBEMIX_GENRE_PROFILE env var dispatch (06-04 __main__.py + _main_helpers.py). Phase 12 will surface as Settings UI dropdown reading list_profiles(). |

4. **Files Created / Modified** — table with path + provides + line count.

5. **Architectural Decisions Locked** — capture from CONTEXT:
   - Percentile thresholds: p30 / p70 / p95.
   - 3-tick hysteresis at 10Hz = 300ms minimum dwell (silent commits immediately).
   - Cold start (< 30 samples ≈ first 3s) falls back to profile's absolute thresholds, not v4 constants.
   - JSON profile schema frozen; hand-validated, no pydantic dependency.
   - Active profile singleton (module-level) — flipping via Phase 12 UI re-loads on next tick.
   - `VIBEMIX_GENRE_PROFILE=none` (or 'unknown' / '') is a first-class fallback path (Phase 3 absolute-threshold behavior preserved).
   - `classify_phase` return-type contract is conditional on whether `profile` is passed (str when None, tuple otherwise) — backward-compat trick that keeps existing call sites working.
   - Hysteresis state lives in `state_refresh_loop` local scope, NOT in MusicState — it's internal detector state, not consumer-readable evidence.
   - BPM validator: half→double order; zero/negative short-circuit; out-of-range pass-through (downstream BPM_VALID_MIN/MAX gate filters).
   - Vocal detector heuristics: 2-of-3 rules; profile parameter accepted but not used in v1 (reserved).
   - EventDetector: LAYER_ARRIVAL gate is the ONLY event-type change; other 5 unchanged.
   - No new heavy DSP deps (no librosa, no madmom — Critical Constraint 6).

6. **Deviations from Plan**:
   - CONTEXT mentioned pydantic-validated load; planner chose hand-written validator for zero new deps (CLAUDE's discretion clause).
   - CONTEXT example "62→124 (techno)" is a boundary case — 124 falls below techno's 125. Validator returns (62.0, False) at that exact boundary; pinned via test. Use 63→126 as the in-range half-corrected case.
   - Build detection threshold: percentile path uses `>= 4` monotonic climbs (strict, all 4 deltas positive); v4 fallback path uses `>= 3` (looser). Two different thresholds for two different paths — documented in detector.py.
   - Added `_main_helpers.apply_genre_env()` extraction (not in CONTEXT) for testability of `__main__.py` env handling.
   - Added defensive zero/negative short-circuit in `validate_bpm` (not in CONTEXT text, but pinned via test).

7. **Dependent Phases Unlocked**:
   - Phase 10 (Prompt Template Matrix): AICoach.evidence_line can now surface `vocal=on/off`, `crest=N.N`, `bpm=corrected/raw`, `genre=<name>`.
   - Phase 12 (Settings UI): Genre picker reads `list_profiles()`, writes via `set_active_profile`. Mid-session profile flips work via the per-tick `get_active_profile()` re-read.
   - Phase 16 (Hallucination Verification): per-genre F1 ≥85% gate has its detector to measure. SENSE-10's 30-min recorded-set validation harness lives here.

8. **Open Items Carried Forward**:
   - 30-min recorded sets per genre (Kaan + Francesco's DJ network). Phase 16 work; can begin collection now.
   - Per-deck profile inference (deck A = techno, deck B = house). Post-v1 per CONTEXT.
   - Auto-detect genre from audio fingerprint. Post-v1.
   - Genre profile training UI. Post-v1.
   - Vocal-section confidence (0..1 score). Conditional on Phase 10 prompt-work needing it.
   - DDJ-FLX4 tempo CC overriding autocorr. Phase 9 fix territory.

9. **v4-vs-Phase-6 Phase Classification Diff** (Critical Constraint 10 — required section):
   - **What stays the same:** When `VIBEMIX_GENRE_PROFILE=none` (or env unset and user opts out via Settings), behavior is byte-equivalent to v4. Pinned via golden test in `tests/state/test_phase.py`.
   - **What changes when a profile is active (default 'techno'):**
     - Phase labels become more sensitive to the track's OWN loudness distribution rather than global thresholds. A quiet techno track that previously sat in 'low' for its entire run can now hit 'groove' / 'peak' as its dynamics expand against its own p30/p70/p95.
     - 'drop' is more conservative — requires a jump > `profile.drop_jump_threshold` (0.060 for techno) on top of being ≥ p95. v4 fired 'drop' on a single PEAK_RMS crossing if any of the previous 3 samples sat below LOW_RMS — looser.
     - 3-tick hysteresis (300ms at 10Hz) prevents flicker between adjacent labels. v4 had no hysteresis — single-tick transitions allowed.
     - 'silent' commits immediately (no hysteresis) when `audible=False` or `last < silent_rms` — anti-hallucination, preserves v4's instinct.
     - Build detection stricter (4 monotonic climbs required vs 3 in v4) in the percentile path. The cold-start path under 30 samples still uses 3.
   - **Side effects on EventDetector:**
     - PHASE events fire on the committed (post-hysteresis) phase, NOT the raw classifier output. Brief transient labels (1-2 ticks of 'peak' inside a 'groove' run) no longer fire a PHASE event. This is anti-slop tightening.
     - LAYER_ARRIVAL is suppressed during vocal sections (state.vocal_active=True). The AI no longer fires a "new layer arrived" reaction every time a singer enters. v4 had no such gate.
   - **Side effects on AICoach (Phase 10 surface):**
     - `state.vocal_active` exposed for prompts.
     - `state.crest_factor` exposed for prompts ("track is heavily compressed" / "dynamic master").
     - `state.bpm_corrected` exposed ("BPM=128 [corrected from autocorr half-detection of 64]").
     - `state.genre_profile_name` exposed ("operating in techno mode").
   - **What to expect in a live session:**
     - Fewer noisy PHASE flips per minute (hysteresis kills the 10Hz flutter).
     - LAYER_ARRIVAL silenced during vocals (D&B + pop + disco see the biggest behavioral shift).
     - Drop detection more deliberate; might miss a few sustained-peak drops that v4 would have flagged.
     - BPM displayed in evidence_line is more often correct (half-detection auto-snaps).
     - The percentile detector's "tracks adapts to its own dynamics" is the biggest qualitative win — Kaan should hear AI reactions that follow HIS curve, not a fixed loudness scale.

10. **Verification Snapshot** — table of the 10 gates with pass/fail + numbers.

11. **Commit History** — table of the 4 wave commit SHAs + this docs commit (will be the 5th).

12. **Self-Check** — every CONTEXT D-LOCKED decision honored; no Deferred Idea implemented; POC files diff-untouched; ROADMAP/STATE updated.

Write `.planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md` with the structure above. Aim for ~300-450 lines (consistent with 03-SUMMARY.md).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md &amp;&amp; grep -q "v4-vs-Phase-6" .planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md &amp;&amp; grep -q "Verification Snapshot" .planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md</automated>
  </verify>
  <done>06-SUMMARY.md exists with all required sections including the v4-vs-Phase-6 phase classification diff.</done>
</task>

<task type="auto">
  <name>Task 3: Advance STATE.md to Phase 7 + mark ROADMAP entry complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**Update `.planning/STATE.md`:**

1. **Last updated** header → today's date + `(Phase 6 complete)`.
2. **Current Position** → flip to `07 — Windows Port (Audio + Screen) (next)`. Update Status block to summarize Phase 6 close: 4 wave commits + verification gate + 06-SUMMARY produced. List the wave commit SHAs (run `git log --oneline | grep "feat(06):"` to retrieve). Note key facts: 5 profiles ship, percentile detector + 3-tick hysteresis active, MusicState +4 fields, EventDetector LAYER_ARRIVAL vocal-gated, VIBEMIX_GENRE_PROFILE env honored. Capture the test count delta (385 → expected ~450+).
3. **Progress bar** → `[██████              ] 30% (6/20 phases)`.
4. **Performance Metrics** → `Phases complete | 6 / 20` and bump `v1 requirements complete` by 6 (SENSE-03, SENSE-04, SENSE-06, SENSE-07, SENSE-08, UX-03 — SENSE-10 only partially complete since the 30-min validation harness lives in Phase 16; count it as 0.5 if your audit needs to show a half, or document as "in flight" in a footnote).
5. **Decisions Locked** section → add the Phase 6 decisions block:
   - Percentile thresholds (p30/p70/p95), 3-tick hysteresis (silent commits immediate).
   - Cold start uses profile's absolute thresholds (not global v4 constants).
   - JSON profile schema frozen; hand-written validator (no pydantic dep).
   - VIBEMIX_GENRE_PROFILE env: default 'techno', 'none'/'unknown'/'' = Phase 3 fallback, invalid = sys.exit.
   - classify_phase dispatch: positional/profile=None → v4 plain-str path; profile=<GenreProfile> → tuple percentile path.
   - Hysteresis state lives in state_refresh_loop local scope, NOT MusicState.
   - BPM validator: half→double order; zero/negative short-circuit.
   - VocalDetector: 2-of-3 heuristic rules + 1.5s in / 2.5s out hysteresis; profile parameter reserved (genre-agnostic in v1).
   - EventDetector LAYER_ARRIVAL gated on `not state.vocal_active`; other 5 event types byte-identical to v4.
6. **Open Todos** → add carry-overs:
   - 30-min recorded sets per genre for Phase 16 (Kaan + Francesco's DJ network — collection can begin in parallel with Phase 7 development).
   - Per-genre F1 ≥85% measurement on those recordings — Phase 16 work.
7. **Last Session** → add a Phase 6 entry mirroring the Phase 4 / 5 style. Wave commits + key invariants + POC-untouched confirmation.
8. **Next Session** → flip to: "Continue from Phase 7 (Windows Port — Audio + Screen). PyAudioWPatch WASAPI loopback + mss + pywin32 EnumWindows + sample-rate sanity test. Parallelizes with Phases 8-9. Kaan-side outstanding: SignPath OSS application (~1 week SLA), proxy deployment to api.altidus.world (Phase 5 carry), 30-min recorded sets per genre (Phase 16 inputs)."

**Update `.planning/ROADMAP.md`:**

1. Phase 6 entry in the top-level Phases list: `- [ ] **Phase 6:** ...` → `- [x] **Phase 6:** ... _**Complete YYYY-MM-DD.**_`. Today's date.
2. Phase 6 detail block (the `### Phase 6: ...` section): change `**Plans**: TBD` → `**Plans:** 5 plans`.
3. Plans list under the Phase 6 detail block (currently absent — the section only has `**Plans**: TBD`). Add the standard plan-list format mirroring the Phase 5 detail block style:

```
Plans:
- [x] 06-01-PLAN.md — Genre profile system + 5 JSON profiles (techno / house / D&B / disco / pop) + active-profile singleton
- [x] 06-02-PLAN.md — Crest factor (peak/RMS + EMA smoother) + BPM half/double validator + VocalDetector hysteresis (1.5s in / 2.5s out)
- [x] 06-03-PLAN.md — Percentile phase detector + MusicState +4 fields + state_refresh_loop wiring + Phase 3 golden equivalence pinned
- [x] 06-04-PLAN.md — EventDetector LAYER_ARRIVAL vocal gate + VIBEMIX_GENRE_PROFILE env + vibemix.state re-exports
- [x] 06-05-PLAN.md — 10-gate verification + 06-SUMMARY (with v4-vs-Phase-6 phase-diff) + STATE/ROADMAP advance
```

4. Progress table at the bottom of ROADMAP.md: `| 6. Genre-Aware Phase Detection | 0/? | Not started | - |` → `| 6. Genre-Aware Phase Detection | 5/5 | Complete | YYYY-MM-DD |`.

Run a grep sanity check: `grep -c "Phase 6" .planning/ROADMAP.md` and `grep -c "Phase 6" .planning/STATE.md` both > 0; `grep "06-01-PLAN.md" .planning/ROADMAP.md` returns the plan-list line.
  </action>
  <verify>
    <automated>grep -q "Phase 6 ✅\|Phase 6 complete\|Phase 6.*Complete" .planning/STATE.md &amp;&amp; grep -q "06-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "5/5 | Complete" .planning/ROADMAP.md</automated>
  </verify>
  <done>STATE.md advanced to Phase 7. ROADMAP.md Phase 6 marked complete with 5/5 plans checked.</done>
</task>

<task type="auto">
  <name>Task 4: Final docs commit closing Phase 6</name>
  <files>.planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Commit the SUMMARY + STATE + ROADMAP changes as a single docs commit. Use `gsd-sdk query commit`:

```
gsd-sdk query commit "docs(06): Phase 6 complete — genre-aware phase detection shipped" --files .planning/phases/06-genre-aware-phase-detection/06-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md
```

(Or via git if the SDK helper isn't producing what you need — use the standard HEREDOC pattern with Co-Authored-By trailer.)

Commit body:
```
- 06-SUMMARY.md — what shipped across 4 waves, requirements coverage, decisions locked, deviations, v4-vs-Phase-6 phase-classification diff section, verification snapshot, commit history
- STATE.md — Current Position advanced to Phase 7 (Windows Port); Phases complete 6/20 (30%); Phase 6 decisions appended; open todos updated with 30-min recorded-set collection carry-forward
- ROADMAP.md — Phase 6 entry marked [x] with completion date; Plans 5/5 listed and checked; progress table updated

Wave commits: feat(06) wave 1 / wave 2 / wave 3 / wave 4. All 10 verification gates PASS. ~450+ tests green (385 Phase 5 baseline + ~65-85 Phase 6 new). POC files (cohost_v4.py + run_v4.sh + cohost*.py + mascot.html + fillers/) diff-untouched throughout the phase.
```

After commit, run `git log --oneline -6` and confirm the 5 Phase 6 commits sit on top: 4 `feat(06)` waves + 1 `docs(06)` close.

Report the docs commit SHA to Kaan + the path of 06-SUMMARY.md.
  </action>
  <verify>
    <automated>git log --oneline -6 | grep -c "(06)"</automated>
  </verify>
  <done>5 Phase 6 commits on git log (4 feat + 1 docs). Phase 6 closed.</done>
</task>

</tasks>

<verification>
- 10 gates from Task 1 all PASS.
- 06-SUMMARY.md exists with all required sections including v4-vs-Phase-6 diff.
- STATE.md Current Position == 'Phase 07'; Progress 6/20 (30%).
- ROADMAP.md Phase 6 entry marked [x] with date; 5/5 plans listed and checked.
- Final docs commit lands; `git log --oneline -6 | grep -c '(06)'` == 5.
- POC files diff-untouched across the entire phase: `git diff --name-only $(git log --format=%H -- cohost_v4.py | tail -1)..HEAD -- 'cohost*.py' 'run*.sh' mascot.html fillers/` empty.
- `uv run pytest tests/ -q --ignore=tests/test_audio_macos_live.py --ignore=tests/test_main_live.py` — full suite green at close.
- `uv run ruff check src/ tests/` AND `uv run ruff format --check src/ tests/` — zero findings.
</verification>

<success_criteria>
- All 10 verification gates PASS.
- 06-SUMMARY.md captures wave-by-wave delivery, requirements coverage, decisions, deviations, dependent-phase unlocks, v4-vs-Phase-6 classification diff, verification snapshot.
- STATE.md and ROADMAP.md correctly reflect Phase 6 completion + Phase 7 as next.
- Final docs commit lands cleanly.
- POC files diff-untouched throughout the phase.
- Phase 6 closed; Phase 7 (Windows Port) is the explicit next step.
</success_criteria>

<output>
After Task 4 commits, Phase 6 is closed. Surface to Kaan:

- 4 wave commits + 1 docs commit (5 commits total under `feat(06)` / `docs(06)`).
- Path to 06-SUMMARY.md.
- Test count delta (e.g. 385 → 450+ green).
- The headline v4-vs-Phase-6 behavioral change (one line — "Hysteresis kills 10Hz phase flicker; LAYER_ARRIVAL no longer fires on vocal entry; percentile detector adapts to each track's own dynamics; v4 fallback preserved via VIBEMIX_GENRE_PROFILE=none").
- Suggest next: `/gsd-execute-phase 07` (Windows Port — parallelizable with Phases 8-9).
</output>
