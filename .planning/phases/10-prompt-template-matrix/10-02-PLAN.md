---
phase: 10-prompt-template-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - docs/prompt-templates.md
  - .planning/phases/10-prompt-template-matrix/10-SUMMARY.md
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05
  - PROMPT-06
  - PROMPT-07
tags:
  - docs
  - phase-rollup
must_haves:
  truths:
    - "docs/prompt-templates.md exists; documents the 6-cell matrix + env var picker + negative-dict bans + <silence/> + scorecard."
    - "10-SUMMARY.md exists with Phase 9 SUMMARY shape — frontmatter wave_commits + acceptance gates table + what shipped + decisions pinned + what's next."
  artifacts:
    - path: "docs/prompt-templates.md"
      provides: "User-facing reference for prompt configuration: env vars, the 6 cells, what each emphasizes, the anti-slop filter behavior, scorecard bands."
      min_lines: 80
    - path: ".planning/phases/10-prompt-template-matrix/10-SUMMARY.md"
      provides: "Phase 10 rollup — wave commits, requirements coverage, ROADMAP success criteria mapping, decisions pinned, deferred items, what's next."
      min_lines: 70
---

<objective>
Wave 2 closes Phase 10: docs + SUMMARY. No new code lands — Wave 1 ships everything.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/prompt-templates.md</name>
  <files>docs/prompt-templates.md</files>
  <action>
    Create `docs/prompt-templates.md` with sections:
    1. **What this is** — vibemix's reactions are driven by a 6-cell prompt matrix (skill × mode). Pick the right cell via `VIBEMIX_SKILL_LEVEL` + `VIBEMIX_MODE` env vars (defaults: `intermediate` / `hype`).
    2. **The 6 cells** — table with cell name, who it's for, anchor-phrase samples (3-5 each).
    3. **Anti-slop stack** — explain the 3-layer enforcement: prompt-level bans, post-hoc filter, tests. List the ~40 banned phrases.
    4. **TurnHistory** — last-12-utterances ring injected into every prompt as `<recent_turns>` block.
    5. **`<silence/>` short-circuit** — when the model decides nothing's worth saying.
    6. **Coach scorecard** — at session end, Coach mode summarizes the set with one of 4 qualitative bands (clean / decent / abrupt / train-wreck) — never numeric.
    7. **Reaction throttle** — per-event-type cooldowns (Phase 3) + global 8-second min-gap cap (Phase 10).
    8. **What's deferred** — Phase 11/12 settings UI for in-app picking; Phase 16/17 A/B testing; multi-language out of v1.
  </action>
  <verify>
    <automated>test -f docs/prompt-templates.md && wc -l docs/prompt-templates.md | awk '{exit ($1>=80)?0:1}' && grep -q "VIBEMIX_SKILL_LEVEL" docs/prompt-templates.md && grep -q "negative dictionary\|negative-dict\|banned" docs/prompt-templates.md && grep -q "&lt;silence/&gt;\|<silence/>" docs/prompt-templates.md</automated>
  </verify>
  <done>Doc exists, ≥80 lines, covers all 8 sections.</done>
</task>

<task type="auto">
  <name>Task 2: Write 10-SUMMARY.md + close phase</name>
  <files>.planning/phases/10-prompt-template-matrix/10-SUMMARY.md</files>
  <action>
    Mirror Phase 9 09-SUMMARY shape. Frontmatter with requirements_covered (PROMPT-01..07), wave_commits (1 wave + this docs commit), test_count + test_delta. Body with: What Phase 10 Delivered (modules, tests, docs) + Architecture Decisions Pinned (matrix shape, 3-layer filter, silence/slop suppression, TurnHistory format, scorecard bands) + ROADMAP Success Criteria mapping table + Deferred + What's Next (Phase 11 — Tauri Shell + Calibration Wizard).
  </action>
  <verify>
    <automated>test -f .planning/phases/10-prompt-template-matrix/10-SUMMARY.md && head -20 .planning/phases/10-prompt-template-matrix/10-SUMMARY.md | grep -q "status: complete"</automated>
  </verify>
  <done>SUMMARY committed.</done>
</task>

</tasks>

<verification>
- docs/prompt-templates.md exists, ≥80 lines.
- 10-SUMMARY.md exists with `status: complete`.
</verification>
