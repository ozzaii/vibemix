---
phase: 42-hallucination-gate-v3-hybrid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/generate_ack_audio_resume.py
  - scripts/eval/record_cassettes.py
  - tests/eval/cassettes/.gitkeep
  - eval/corpus/MANIFEST.md
  - eval/corpus/LICENSES.md
  - eval/corpus/sessions/.gitkeep
  - .gitattributes
  - KAAN-ACTION-LEGAL.md
  - tests/eval/test_ack_resume_idempotent.py
  - tests/eval/test_record_cassettes_invokable.py
  - tests/eval/test_corpus_lfs_layout.py
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]
must_haves:
  truths:
    - "`scripts/eval/generate_ack_audio_resume.py` is invokable as `--dry-run` and reports the 20 missing OPUS entries without calling Gemini"
    - "`scripts/eval/record_cassettes.py` is invokable and writes-no-bytes when `VCR_RECORD_MODE=none`; under `VCR_RECORD_MODE=new_episodes` it scans `tests/eval/` and would invoke the existing Phase 27 VCR-decorated tests (dry-run path verified without real API calls)"
    - "`eval/corpus/sessions/` directory exists with a `.gitkeep` and is LFS-tracked via a `.gitattributes` rule pointing `eval/corpus/sessions/**/*.wav filter=lfs diff=lfs merge=lfs -text`"
    - "`eval/corpus/MANIFEST.md` template documents the schema for the 6 sessions (id, genre, duration_s, source URL, license, attribution)"
    - "`eval/corpus/LICENSES.md` template aggregates per-session license slots for the 6 real-corpus WAVs"
    - "`KAAN-ACTION-LEGAL.md` carries three new sections — `§GATE-01` (ack-bank quota refresh runbook), `§GATE-02` (one-time VCR cassette recording runbook), `§GATE-03` (real-corpus WAV acquisition runbook)"
    - "No real WAV bytes, no real ack OPUS bytes, no real cassette bytes are committed in this plan — engineering scaffolding only; real artifact discharge tracked in `KAAN-ACTION-LEGAL.md`"
  artifacts:
    - path: "scripts/eval/generate_ack_audio_resume.py"
      provides: "Idempotent resume wrapper that invokes the existing `scripts/generate_ack_audio.py` with skip-existing semantics; emits a dry-run inventory of present/missing OPUS files"
      exports: ["main", "list_missing_entries", "DEFAULT_MANIFEST"]
    - path: "scripts/eval/record_cassettes.py"
      provides: "One-time VCR cassette population helper that respects `VCR_RECORD_MODE` env var and routes through the existing Phase 27 tests/eval suite"
      exports: ["main", "discover_vcr_tests"]
    - path: "eval/corpus/MANIFEST.md"
      provides: "Schema-first markdown template for the 6 real-corpus sessions (replaces or supplements existing manifest.json)"
      contains: "## Sessions\n### hard_tek_01\n### techno_01"
    - path: "eval/corpus/LICENSES.md"
      provides: "Per-session license attribution slots, ≥2 genres mandated by check_ear_test contract"
    - path: ".gitattributes"
      provides: "LFS filter rule for `eval/corpus/sessions/**/*.wav`"
      contains: "eval/corpus/sessions/**/*.wav filter=lfs"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "Three new sections appended: §GATE-01, §GATE-02, §GATE-03 — runbooks for Kaan-discharge of the real artifacts"
      contains: "## §GATE-01"
  key_links:
    - from: "scripts/eval/generate_ack_audio_resume.py"
      to: "scripts/generate_ack_audio.py"
      via: "subprocess invocation in resume mode (or direct import of `_load_manifest` + skip-existing loop)"
      pattern: "from scripts.generate_ack_audio|generate_ack_audio.py"
    - from: "scripts/eval/record_cassettes.py"
      to: "tests/eval/cassettes/"
      via: "VCR cassettes directory output of the existing Phase 27 VCR-decorated tests"
      pattern: "tests/eval/cassettes"
    - from: ".gitattributes"
      to: "eval/corpus/sessions/"
      via: "git-LFS filter rule"
      pattern: "eval/corpus/sessions/\\*\\*/\\*\\.wav filter=lfs"
    - from: "KAAN-ACTION-LEGAL.md"
      to: "scripts/eval/generate_ack_audio_resume.py"
      via: "§GATE-01 runbook references the script as the one-liner Kaan runs"
      pattern: "scripts/eval/generate_ack_audio_resume.py"
---

<objective>
Ship the engineering scaffolding for the three corpus-side Kaan-discharge items (GATE-01 ack-bank top-up, GATE-02 VCR cassettes, GATE-03 real-corpus WAVs in git-LFS). No real artifacts (audio bytes, cassette JSONs, WAV files) are committed in this plan — the actual bytes discharge through `KAAN-ACTION-LEGAL.md` runbooks added here.

Purpose: Plans 42-02 (threshold recalibration) and 42-04 (`check_gate.sh`) consume the corpus directory structure and the recalibration runbook. Without this scaffold those plans cannot wire against a stable surface. Per CONTEXT — "Engineering scaffolding ships engineering-green; real-world artifacts discharge in KAAN-ACTION-LEGAL.md §GATE-*".

Output:
- `scripts/eval/generate_ack_audio_resume.py` — idempotent resume wrapper over the existing Phase 27 `scripts/generate_ack_audio.py` (already has skip-existing in `_DEFAULT_OUTPUT`).
- `scripts/eval/record_cassettes.py` — one-time VCR cassette population helper.
- `eval/corpus/sessions/` LFS-tracked directory + `.gitattributes` rule.
- `eval/corpus/MANIFEST.md` + `eval/corpus/LICENSES.md` schema templates.
- Three new sections in `KAAN-ACTION-LEGAL.md`: §GATE-01, §GATE-02, §GATE-03.
- 3 sanity tests confirming the scripts are invokable + the LFS layout is correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-CONTEXT.md

@scripts/generate_ack_audio.py
@eval/corpus/manifest.json
@eval/corpus/LICENSES.md
@.github/workflows/eval.yml
@KAAN-ACTION-LEGAL.md

<interfaces>
<!-- Repo facts the executor needs. Do not explore — use these. -->

From `scripts/generate_ack_audio.py` (Phase 27 / LATENCY-15 — already shipped):
- `_DEFAULT_MANIFEST = _PROJECT_ROOT / "assets" / "ack_bank" / "manifest.json"`
- `_DEFAULT_OUTPUT  = _PROJECT_ROOT / "src" / "vibemix" / "audio" / "ack_bank"`
- Already idempotent: "by default skips entries whose .opus file already exists"
- Bucket layout (per existing `assets/ack_bank/manifest.json`): 5 buckets × 8 ids = 40 entries
- Buckets present today (verified): drop_hit, generic_filler, mix_move, silence_break, track_change
- Existing CLI flags: `--force`, `--dry-run`, `--bucket <name>`
- The "resume" wrapper in this plan is a thin idempotency-audit script — it lists which of the 40 entries are missing OPUS files (the residual 20 from `bug_acceptance` ACK-BANK-REMAINING-20) without calling Gemini

From `eval/corpus/manifest.json` (Phase 27-03 — already shipped):
- 6 placeholder session entries with `"source": "TBD-archive-org"` / `"TBD-ccmixter"` / `"TBD-fma-electronic"`
- Diversity gate already enforces hard_tek=33% across 3 genres (hard_tek/techno/house)
- LICENSES.md template already exists (placeholders); this plan EXPANDS it for the 6 real WAVs by tightening license-row schema

From `tests/eval/cassettes/` (Phase 27 — directory exists, currently empty):
- VCR.py cassette directory consumed by tests decorated with `@pytest.fixture(autouse=True)` cassettes
- One-time population: `VCR_RECORD_MODE=new_episodes pytest tests/eval/` — Kaan runs once after API key + quota are available
- This plan ships a wrapper `scripts/eval/record_cassettes.py` that documents + invokes that subprocess (and dry-runs without `GEMINI_API_KEY`)

From `KAAN-ACTION-LEGAL.md` (existing structure):
- Section convention: `## §SECTION-ID` headers; appended below the AUDIO-05/06/07 sections
- New sections: `§GATE-01`, `§GATE-02`, `§GATE-03` — each one a runbook with: (a) why this defers, (b) exact one-liner Kaan runs, (c) verification (`pytest …` or `ls …`), (d) what unblocks downstream

From `.gitattributes`:
- Read current contents first; if `eval/corpus/sessions/**/*.wav filter=lfs` rule already present, leave it; if absent, append it under an `# eval corpus — Phase 42` comment header

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ack-bank resume helper + VCR cassette recorder helper + sanity tests</name>
  <files>scripts/eval/generate_ack_audio_resume.py, scripts/eval/record_cassettes.py, tests/eval/test_ack_resume_idempotent.py, tests/eval/test_record_cassettes_invokable.py</files>
  <action>
Per D-GATE-01 + D-GATE-02 — create two thin Python helper scripts that wrap the existing Phase 27 surfaces:

(a) `scripts/eval/generate_ack_audio_resume.py`:
- Module docstring: cite Phase 27-08, GATE-01, ACK-BANK-REMAINING-20.
- `list_missing_entries(manifest_path: Path, output_dir: Path) -> list[dict]` — loads manifest.json (5 buckets × 8 ids = 40), walks `src/vibemix/audio/ack_bank/<bucket>/<id>.opus`, returns the entries whose .opus file is absent. No Gemini calls.
- `main()` argparse CLI: `--manifest`, `--output`, `--dry-run` (default true), `--really` (gates subprocess invocation of `scripts/generate_ack_audio.py`). When `--really` is set and `GEMINI_API_KEY` is present, exec `python scripts/generate_ack_audio.py` via subprocess; otherwise print the missing-entries inventory + the Kaan-discharge one-liner.
- Exit codes: 0 = clean (or dry-run successful), 1 = manifest missing / corrupt.
- Use stdlib only (argparse, pathlib, json, subprocess, sys, os). Do NOT import `google.genai` — this script is a quota-aware wrapper that itself spends $0.

(b) `scripts/eval/record_cassettes.py`:
- Module docstring: cite Phase 27-04 (eval.yml's nightly canary uses `VCR_RECORD_MODE=new_episodes`), GATE-02, EVAL-VCR-CASSETTES.
- `discover_vcr_tests(eval_test_dir: Path) -> list[str]` — globs `tests/eval/test_*.py` and returns the set decorated with VCR (heuristic: grep file body for `@pytest.fixture` + `vcr` or `@vcr.use_cassette` or `cassette_library_dir`).
- `main()` argparse CLI: `--test-dir tests/eval`, `--cassette-dir tests/eval/cassettes`, `--record-mode {none,new_episodes,all}` (default none), `--really` (gates the subprocess pytest invocation).
- When `--really` is set + `record-mode=new_episodes` + `GEMINI_API_KEY` present: subprocess `pytest tests/eval/ -p vcr --vcr-record=new_episodes` (or env `VCR_RECORD_MODE=new_episodes pytest tests/eval/`).
- Default path (no `--really`): emit the inventory of discovered VCR-decorated test files + the Kaan-discharge one-liner. Exit 0.
- This script never produces cassette bytes by itself; it is a typed wrapper documenting the one-time Kaan-discharge.

(c) `tests/eval/test_ack_resume_idempotent.py`:
- `test_list_missing_entries_reads_manifest()` — point `list_missing_entries` at the real manifest + a tmp empty output dir; expect 40 missing entries (every bucket × id pair).
- `test_list_missing_entries_skips_present()` — write a single fake `<bucket>/<id>.opus` byte file under tmp output dir; expect 39 missing.
- `test_resume_dry_run_does_not_call_gemini()` — invoke `main(["--dry-run"])`, assert no `google.genai` import is reachable via `sys.modules` after call. Use `monkeypatch.delenv("GEMINI_API_KEY", raising=False)` to be safe.

(d) `tests/eval/test_record_cassettes_invokable.py`:
- `test_discover_vcr_tests_returns_list()` — confirm `discover_vcr_tests(Path("tests/eval"))` returns a (possibly empty) list without errors.
- `test_main_default_does_not_subprocess_pytest(monkeypatch)` — invoke `main([])`, assert subprocess.run is not called (use `monkeypatch.setattr` to record + `assert calls == []`).
- `test_main_really_requires_api_key(monkeypatch)` — invoke `main(["--really", "--record-mode", "new_episodes"])` with `GEMINI_API_KEY` unset; expect exit 1 + stderr message naming the env var.

Verify imports work in the project's `python3` (Phase 27 patterns: `uv run python -c "from scripts.eval.generate_ack_audio_resume import list_missing_entries; ..."`).
  </action>
  <verify>
    <automated>uv run pytest tests/eval/test_ack_resume_idempotent.py tests/eval/test_record_cassettes_invokable.py -q --no-header</automated>
  </verify>
  <done>Both new tests files pass (≥6 tests total); both scripts importable; `uv run python scripts/eval/generate_ack_audio_resume.py --dry-run` exits 0 + prints the 20-or-40 missing inventory; `uv run python scripts/eval/record_cassettes.py` (no flags) exits 0 + prints the Kaan-discharge one-liner.</done>
</task>

<task type="auto">
  <name>Task 2: LFS corpus layout + manifest/license templates + .gitattributes rule + corpus layout test</name>
  <files>eval/corpus/MANIFEST.md, eval/corpus/LICENSES.md, eval/corpus/sessions/.gitkeep, tests/eval/cassettes/.gitkeep, .gitattributes, tests/eval/test_corpus_lfs_layout.py</files>
  <action>
Per D-GATE-03 — set up the git-LFS directory structure for the 6 × 30-min real-corpus WAVs without committing any actual WAV bytes.

(a) `eval/corpus/MANIFEST.md`:
- Schema-first markdown template documenting the 6 sessions. Each session block has: `**Session ID** | **Genre** | **Duration (s)** | **Source URL** | **License** | **Attribution** | **SHA256** (placeholder until Kaan ffmpeg-normalizes and commits)`.
- Order: hard_tek_01, hard_tek_02, techno_01, techno_02, house_01, house_02 (matches existing `eval/corpus/manifest.json` order).
- Header note: "Public-domain or CC0 sources only — per CONTEXT EVAL-03 + Pitfall P43. Kaan-discharge runbook in KAAN-ACTION-LEGAL.md §GATE-03."
- ≥2 genres invariant call-out so `check_ear_test.sh` (Plan 42-03) contract is visible at the manifest level.

(b) `eval/corpus/LICENSES.md`:
- Update existing file (already has placeholder shape from Phase 27-03). Expand each session block with: source URL slot, license slot (one of: CC0 / CC-BY / Public Domain), attribution slot, retrieval date slot, ffmpeg normalization invocation (`ffmpeg -i input -ac 1 -ar 16000 output.wav` template).

(c) `eval/corpus/sessions/.gitkeep`:
- Empty file. Tracks the directory.

(d) `tests/eval/cassettes/.gitkeep`:
- Empty file IF the directory is currently empty. If `tests/eval/cassettes/` already has files (Phase 27 cassettes), do not add. Read first.

(e) `.gitattributes`:
- Read current contents. If `eval/corpus/sessions/**/*.wav filter=lfs` already present, leave file alone.
- If absent, append a block:
```
# eval corpus — Phase 42 GATE-03 (real DJ session WAVs, 200MB git-LFS)
eval/corpus/sessions/**/*.wav filter=lfs diff=lfs merge=lfs -text
```

(f) `tests/eval/test_corpus_lfs_layout.py`:
- `test_corpus_sessions_dir_exists()` — assert `eval/corpus/sessions/.gitkeep` is a file.
- `test_manifest_md_lists_six_sessions()` — read `eval/corpus/MANIFEST.md`, assert all 6 session IDs appear (hard_tek_01, hard_tek_02, techno_01, techno_02, house_01, house_02).
- `test_manifest_md_documents_two_genre_minimum()` — assert the phrase `≥2 genres` (or `>=2 genres` or `2 genres minimum`) appears (the ear-test contract surface).
- `test_gitattributes_has_lfs_rule_for_corpus_wavs()` — read `.gitattributes` at repo root; assert a line matches the regex `eval/corpus/sessions/\*\*/\*\.wav\s+filter=lfs`.
- `test_licenses_md_has_six_session_slots()` — assert all 6 IDs appear in `eval/corpus/LICENSES.md`.

NO real WAV bytes commit. NO `git lfs track` invocation from the script. The runbook in KAAN-ACTION-LEGAL.md §GATE-03 (Task 3) directs Kaan to do the actual `git lfs track + add + commit`.
  </action>
  <verify>
    <automated>uv run pytest tests/eval/test_corpus_lfs_layout.py -q --no-header</automated>
  </verify>
  <done>All 5 layout tests pass; `.gitattributes` carries the LFS rule (idempotent if rule already present); `eval/corpus/MANIFEST.md` + `eval/corpus/LICENSES.md` both document the 6-session schema with placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Append §GATE-01, §GATE-02, §GATE-03 runbooks to KAAN-ACTION-LEGAL.md</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <action>
Per D-GATE-01 + D-GATE-02 + D-GATE-03 — append three new sections to the bottom of `KAAN-ACTION-LEGAL.md`. Match the existing `## §AUDIO-05` / `## §AUDIO-06` / `## §AUDIO-07` section format (already in file).

(a) `## §GATE-01 — Ack-bank quota refresh (20 → 40 OPUS files)`
- WHY DEFER: Gemini TTS free-tier quota; ~$0.10 spend when reset window opens. Engineering scaffolding is in place (Phase 27-08 + Plan 42-01 resume helper).
- KAAN ONE-LINER:
  ```
  GEMINI_API_KEY=... uv run python scripts/eval/generate_ack_audio_resume.py --really
  ```
- VERIFICATION:
  ```
  uv run python scripts/eval/generate_ack_audio_resume.py --dry-run | grep -E "missing: 0"
  uv run pytest tests/runtime_closeouts/test_ack_bank_real_audio.py
  ```
- UNBLOCKS: GATE-04 threshold recalibration (Plan 42-02) can run against a 40/40 ack-bank-complete eval.

(b) `## §GATE-02 — VCR cassette population (one-time)`
- WHY DEFER: Real Gemini API spend; one-time cost ~$1-2 to record cassettes for the Phase 27 test suite. CI then runs against cassettes ($0).
- KAAN ONE-LINER:
  ```
  GEMINI_API_KEY=... uv run python scripts/eval/record_cassettes.py --really --record-mode new_episodes
  ```
- VERIFICATION:
  ```
  ls tests/eval/cassettes/   # expect non-empty after run
  CI: gh workflow run "Eval Gate"  # should pass with VCR_RECORD_MODE=none on PR mode
  ```
- UNBLOCKS: PR-mode eval gate no longer requires GEMINI_API_KEY (currently CI references the secret; cassettes make this $0).

(c) `## §GATE-03 — Real-corpus DJ session WAVs (6 × 30-min, 200 MB git-LFS)`
- WHY DEFER: 200 MB of public-domain DJ session audio needs human curation (license check, genre balance, ffmpeg normalization). Plan 42-01 ships the LFS scaffold + manifest schema + runbook.
- KAAN STEPS:
  1. Source 6 sessions across ≥2 genres (hard_tek/techno/house) from archive.org / CCMixter / FMA Electronic. Public-domain or CC0 only.
  2. Normalize each: `ffmpeg -i raw.wav -ac 1 -ar 16000 normalized.wav`
  3. Fill in `eval/corpus/MANIFEST.md` + `eval/corpus/LICENSES.md` rows (source URL, license, attribution, SHA256, duration).
  4. `git lfs track "eval/corpus/sessions/**/*.wav"` (already in `.gitattributes` if Plan 42-01 ran cleanly)
  5. `git add eval/corpus/sessions/*.wav eval/corpus/MANIFEST.md eval/corpus/LICENSES.md`
  6. `git commit -m "corpus: GATE-03 real DJ session WAVs (200MB LFS)"`
- VERIFICATION:
  ```
  uv run python -c "from scripts.eval.corpus_manifest import validate_manifest; from pathlib import Path; r=validate_manifest(Path('eval/corpus/manifest.json')); assert r['valid'], r"
  uv run pytest tests/eval/test_corpus_diversity_gate.py
  du -sh eval/corpus/sessions/  # expect ~200 MB
  git lfs ls-files | grep "eval/corpus/sessions" | wc -l  # expect 6
  ```
- UNBLOCKS: GATE-04 real-corpus threshold recalibration (Plan 42-02 `--check-real-corpus` mode no longer 0-sessions-warns).

Each section uses the same block structure as `§AUDIO-05` (read that as the canonical template). Sections are appended to the bottom of the file, in this order: §GATE-01, §GATE-02, §GATE-03.
  </action>
  <verify>
    <automated>grep -qE "^## §GATE-01 " KAAN-ACTION-LEGAL.md && grep -qE "^## §GATE-02 " KAAN-ACTION-LEGAL.md && grep -qE "^## §GATE-03 " KAAN-ACTION-LEGAL.md && echo OK</automated>
  </verify>
  <done>Three sections appended in correct order; each contains the four canonical blocks (why-defer / one-liner / verification / unblocks); the one-liner commands match the script names + flags introduced in Tasks 1+2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaan-disk → repo | Real-corpus WAVs Kaan curates from third-party CC0/public-domain sources cross into the repo via git-LFS |
| repo → CI | `.gitattributes` LFS rule defines what CI fetches via `lfs: true` checkout step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | `KAAN-ACTION-LEGAL.md §GATE-*` runbooks | mitigate | Runbook commands use explicit env-var-then-uv-run form; no piped curl-to-bash; each one-liner is reviewable as a single shell command |
| T-42-01-02 | Information Disclosure | `eval/corpus/LICENSES.md` | accept | License records are public-attribution data by design; no Kaan-recordings or proprietary content per CONTEXT EVAL-03 |
| T-42-01-03 | Repudiation | Real-corpus WAV provenance | mitigate | MANIFEST.md schema requires source URL + license + SHA256 per session; LICENSES.md aggregates the audit chain |
| T-42-01-04 | Denial of Service | Ack-bank quota refresh | accept | Gemini free-tier rate-limit triggers known 429 backoff path (already in generate_ack_audio.py); $0.10 worst-case spend |
</threat_model>

<verification>
- Full Phase 42-01 test suite: `uv run pytest tests/eval/test_ack_resume_idempotent.py tests/eval/test_record_cassettes_invokable.py tests/eval/test_corpus_lfs_layout.py -q`
- No regression: `uv run pytest tests/eval/ -q --no-header` (Phase 27 baseline must stay green)
- KAAN-ACTION audit: `grep -c "^## §GATE-0" KAAN-ACTION-LEGAL.md` → expect 3
- LFS rule present: `grep -E "eval/corpus/sessions.*filter=lfs" .gitattributes`
- No real artifact bytes leaked: `ls eval/corpus/sessions/*.wav 2>/dev/null | wc -l` → expect 0 (only `.gitkeep`)
</verification>

<success_criteria>
- 3 new test files (≥10 tests total) pass
- 3 KAAN-ACTION-LEGAL.md sections committed with the canonical runbook structure
- `.gitattributes` LFS rule present + idempotent
- `eval/corpus/sessions/`, `eval/corpus/MANIFEST.md`, `eval/corpus/LICENSES.md` all in place; zero `.wav` bytes committed
- `scripts/eval/generate_ack_audio_resume.py --dry-run` runs without `GEMINI_API_KEY` and reports the missing-OPUS inventory
- `scripts/eval/record_cassettes.py` (default) runs without API calls
- Existing Phase 27 eval tests still pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/42-hallucination-gate-v3-hybrid/42-01-SUMMARY.md` using the standard summary template.
</output>
