---
phase: 42-hallucination-gate-v3-hybrid
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - .planning/decisions/P85-OVERRIDE-RETIRED.md
  - tests/repo/test_phase_16_override_expiry.py
  - tests/repo/test_gate_42_hybrid_in_force.py
  - .planning/STATE.md
autonomous: true
requirements: [GATE-08]
must_haves:
  truths:
    - "`.planning/decisions/P85-OVERRIDE-RETIRED.md` is the Decision Log entry that formally retires the v2.1 Phase 16 ear-test override — cites the v2.1 audit trail commits + Phase 27-04 lock + v3.0 hybrid replacement"
    - "`tests/repo/test_phase_16_override_expiry.py` is DELETED (the override no longer exists to expire)"
    - "`tests/repo/test_gate_42_hybrid_in_force.py` is the positive-assertion replacement, pinning: (a) cut_release.sh invokes check_gate.sh as Gate-2b; (b) check_gate.sh reads both nightly + ear-test inputs; (c) no `OVERRIDE_*` constants remain in release scripts; (d) no P85 reminder text in cut_release.sh"
    - "`.planning/STATE.md` removed the 'Phase 16 ear-test memory override' decision-locked line (or annotated it as RETIRED with cross-reference to P85-OVERRIDE-RETIRED.md) so Plan 42-04 wire-in is self-consistent"
  artifacts:
    - path: ".planning/decisions/P85-OVERRIDE-RETIRED.md"
      provides: "Decision Log entry — formal retirement of the v2.1 P85 autonomous-only override"
      contains: "P85"
    - path: "tests/repo/test_gate_42_hybrid_in_force.py"
      provides: "Positive-assertion test replacing the deleted expiry test; pins the v3.0 hybrid gate is wired"
  key_links:
    - from: "tests/repo/test_gate_42_hybrid_in_force.py"
      to: "scripts/launch/cut_release.sh"
      via: "grep assertion that cut_release invokes check_gate.sh AND no P85 reminder text remains"
      pattern: "check_gate.sh"
    - from: "tests/repo/test_gate_42_hybrid_in_force.py"
      to: "scripts/release/check_gate.sh"
      via: "grep assertion that check_gate.sh reads both .planning/eval-runs/ AND invokes check_ear_test.sh"
      pattern: "check_ear_test.sh"
    - from: ".planning/decisions/P85-OVERRIDE-RETIRED.md"
      to: ".planning/STATE.md"
      via: "Decision log cross-references the STATE.md decision-list line"
      pattern: "STATE.md"
---

<objective>
Retire the v2.1 P85 override formally and replace its expiry-clock test with a positive-assertion test that pins the v3.0 hybrid regime is in force.

Purpose: Per CONTEXT D-GATE-08 — the v2.1 Phase 16 ear-test override was time-limited to one milestone. v3.0 hybrid gate (Plans 42-03 + 42-04) replaces it. The current `test_phase_16_override_expiry.py` asserts STATE.md / cut_release.sh contain reminder lines reminding Kaan to clean up. Plan 42-04 already removes those reminder lines — which would BREAK the expiry test. Plan 42-05 deletes the expiry test in the same commit that adds the positive-assertion test.

Wave / dependency: this plan's positive-assertion test reads `scripts/launch/cut_release.sh` + `scripts/release/check_gate.sh`, both of which Plan 42-04 creates / modifies. Therefore `depends_on: [04]` and `wave: 4`. This wave choice keeps the dependency graph honest even though the deletion (Task 1) could conceptually land earlier.

Output:
- `.planning/decisions/P85-OVERRIDE-RETIRED.md` — Decision Log entry.
- `tests/repo/test_phase_16_override_expiry.py` — DELETED.
- `tests/repo/test_gate_42_hybrid_in_force.py` — NEW positive-assertion test.
- `.planning/STATE.md` — annotate the "Phase 16 ear-test memory override" line as RETIRED with cross-reference (preserve traceability per existing STATE.md conventions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-CONTEXT.md

@tests/repo/test_phase_16_override_expiry.py
@scripts/launch/cut_release.sh
@scripts/release/check_gate.sh

<interfaces>
<!-- The exact surfaces this plan touches. -->

From `tests/repo/test_phase_16_override_expiry.py` (Phase 39-08, will be DELETED):
- 4 tests: test_state_md_exists, test_state_md_has_phase_16_override_line, test_state_md_has_expires_post_v2_1_marker, test_cut_release_sh_reminds_to_remove_override
- The last 2 of these would BREAK once Plan 42-04 removes the cut_release.sh reminder lines + Plan 42-05 retires the STATE.md decision line
- The whole test_file's existence-purpose is the expiry clock; it has no remaining utility in v3.0

From `.planning/STATE.md` (current):
- Decision section line: `**Phase 16 ear-test memory override accepted for v2.1 only** — Phase 27 autonomous proxy gate substituted. Override EXPIRES post-v2.1 (P85 enforced in Phase 39-08). v2.2 must re-route hallucination-gate strategy.`
- This plan annotates that line as `[RETIRED — see .planning/decisions/P85-OVERRIDE-RETIRED.md (Plan 42-05)]` rather than deleting it — keeps the audit trail visible

From `scripts/launch/cut_release.sh` (after Plan 42-04 lands):
- Gate 2b block invoking `check_gate.sh` exists
- P85 reminder lines REMOVED
- `[GATE-06]` success-path reminder ADDED

From `scripts/release/check_gate.sh` (created by Plan 42-04):
- Reads `.planning/eval-runs/` glob
- Invokes `scripts/release/check_ear_test.sh`

From `.planning/decisions/` — directory may not yet exist:
- Run `ls .planning/decisions/` first; if absent, create it (no `.gitkeep` needed — the P85 file populates it)

</interfaces>

@.planning/phases/42-hallucination-gate-v3-hybrid/42-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decision Log entry + delete expiry test + STATE.md annotation</name>
  <files>.planning/decisions/P85-OVERRIDE-RETIRED.md, .planning/STATE.md, tests/repo/test_phase_16_override_expiry.py</files>
  <action>
Per D-GATE-08 — formally retire the override.

(a) `.planning/decisions/P85-OVERRIDE-RETIRED.md`:
- Title: `# P85 Override Retired — v2.1 → v3.0 Transition`.
- Sections:
  - **Status:** RETIRED (replaced by v3.0 hybrid gate).
  - **History:** v2.1 milestone shipped under autonomous-only proxy gate (Phase 27); Phase 16 ear-test memory override accepted as one-milestone carveout per `gsd-autonomous fully`. Override expiry tracked by Pitfall P85 + `tests/repo/test_phase_16_override_expiry.py` + cut_release.sh reminder lines.
  - **Replacement:** v3.0 Phase 42 ships the hybrid gate — Phase 27 autonomous proxy stays as fast lane (PR + nightly canary); Kaan ear-test added back as slow lane (release-cut Gate-2b veto). See:
    - `scripts/release/check_gate.sh` (Plan 42-04)
    - `scripts/release/check_ear_test.sh` (Plan 42-03)
    - `eval/EAR-TEST-PROTOCOL.md` (Plan 42-03)
  - **Audit Trail:**
    - v2.1 lock: `eval/THRESHOLD-LOCK.md` autonomous-signed `kaan_signed: autonomous_phase27` (2026-05-15)
    - v2.1 reminder enforcement: Phase 39-08 `tests/repo/test_phase_16_override_expiry.py`
    - v3.0 retirement: Phase 42-05 (this entry) + `tests/repo/test_gate_42_hybrid_in_force.py` replacement
  - **What changes:**
    - DELETED: `tests/repo/test_phase_16_override_expiry.py`
    - DELETED: cut_release.sh P85 reminder lines (Plan 42-04)
    - ADDED: hybrid gate enforcement at Gate 2b (Plan 42-04)
    - ADDED: positive-assertion test (Plan 42-05 Task 2)
    - ANNOTATED: STATE.md "Phase 16 ear-test memory override" line marked RETIRED with cross-reference here.
  - **Anti-feature carveout:** Do NOT build a more aggressive autonomous judge to replace the ear-test. CONTEXT D-GATE-08 + memory `project_phase_16_kaan_dj_testing` lock the hybrid design as final for v3.0.

(b) `.planning/STATE.md` modification:
- Find the line: `**Phase 16 ear-test memory override accepted for v2.1 only** — Phase 27 autonomous proxy gate substituted. Override EXPIRES post-v2.1 (P85 enforced in Phase 39-08). v2.2 must re-route hallucination-gate strategy.`
- Replace with: `**Phase 16 ear-test memory override [RETIRED post-v2.1]** — replaced by v3.0 hybrid gate (Phase 42). See .planning/decisions/P85-OVERRIDE-RETIRED.md. Phase 27 autonomous proxy = fast lane; Kaan ear-test = slow lane (release-cut Gate 2b veto).`
- Preserves the audit trail (line still mentions the override) while making the retirement explicit.

(c) `tests/repo/test_phase_16_override_expiry.py`:
- DELETE the file via `git rm tests/repo/test_phase_16_override_expiry.py` (or `rm` + the next git add will stage the deletion).
- Also delete any compiled shadow under `tests/repo/__pycache__/test_phase_16_override_expiry*.pyc` — `find tests/repo -name '*phase_16_override*' -delete` is the safe one-liner (matches both the `.py` and the `.pyc`).
- The replacement test goes in Task 2.
  </action>
  <verify>
    <automated>test -f .planning/decisions/P85-OVERRIDE-RETIRED.md && test ! -f tests/repo/test_phase_16_override_expiry.py && grep -q "RETIRED" .planning/STATE.md && grep -q "P85-OVERRIDE-RETIRED.md" .planning/STATE.md && echo OK</automated>
  </verify>
  <done>Decision Log entry exists with all four canonical sections; expiry test file fully deleted (no `.py` AND no `.pyc` shadow remains); STATE.md line annotated RETIRED with cross-reference.</done>
</task>

<task type="auto">
  <name>Task 2: Positive-assertion test pinning v3.0 hybrid gate is in force</name>
  <files>tests/repo/test_gate_42_hybrid_in_force.py</files>
  <action>
Per D-GATE-08 — write the positive-assertion replacement test.

`tests/repo/test_gate_42_hybrid_in_force.py`:
- SPDX header. Module docstring cites GATE-08, Plan 42-05, and "this is the v3.0 replacement for the deleted test_phase_16_override_expiry.py".
- Constants at module top:
  ```
  REPO_ROOT = Path(__file__).resolve().parents[2]
  CUT_RELEASE = REPO_ROOT / "scripts" / "launch" / "cut_release.sh"
  CHECK_GATE  = REPO_ROOT / "scripts" / "release" / "check_gate.sh"
  CHECK_EAR_TEST = REPO_ROOT / "scripts" / "release" / "check_ear_test.sh"
  DECISION_FILE = REPO_ROOT / ".planning" / "decisions" / "P85-OVERRIDE-RETIRED.md"
  STATE_MD = REPO_ROOT / ".planning" / "STATE.md"
  ```
- Tests (positive-assertion replaces every test in the deleted file with the inverse assertion + new contract pins):
  - `test_cut_release_invokes_check_gate_at_gate_2b()` — read CUT_RELEASE.read_text(); assert it contains both `[Gate 2b]` AND `check_gate.sh`.
  - `test_check_gate_reads_nightly_eval_runs()` — read CHECK_GATE.read_text(); assert it contains `.planning/eval-runs` (the nightly canary output dir).
  - `test_check_gate_invokes_ear_test_gate()` — read CHECK_GATE.read_text(); assert it contains `check_ear_test.sh`.
  - `test_no_override_constants_remain_in_release_scripts()` — iterate every `*.sh` under `scripts/launch/` and `scripts/release/`; for each file, filter out comment-only lines (`re.match(r'^\s*#', line)`); assert no remaining line contains `OVERRIDE_` or `OVERRIDE =` or `OVERRIDE:`.
  - `test_no_p85_reminder_in_cut_release_echo_lines()` — read CUT_RELEASE; for each line that starts (after leading whitespace) with `echo "`, assert it does NOT contain `[P85]` OR `Phase 16 override cleanup reminder` OR `Phase 16 ear-test memory override`. Comment lines (starting with `#`) are exempt.
  - `test_cut_release_success_path_references_gate_06()` — read CUT_RELEASE; locate the success block (find `ALL GATES PASS` marker; pass_idx = body.find("ALL GATES PASS")); assert `[GATE-06]` appears at a body index ≥ pass_idx.
  - `test_decision_log_entry_exists_and_cites_p85()` — DECISION_FILE.exists() AND contents include all of: `P85`, `RETIRED`, `check_gate.sh`, `check_ear_test.sh`.
  - `test_state_md_phase_16_line_is_annotated_retired()` — STATE_MD.read_text() — find a line that contains `Phase 16 ear-test memory override`; assert that line contains `RETIRED` and that the file body contains `P85-OVERRIDE-RETIRED.md` (cross-reference path).
  - `test_expiry_test_file_actually_deleted()` — `(REPO_ROOT / "tests/repo/test_phase_16_override_expiry.py").exists() is False`. Load-bearing: catches future revert.
  - `test_check_gate_sh_is_executable()` — `os.access(CHECK_GATE, os.X_OK) is True`.

Grep hygiene (planner core rule — `grep -c` counts comments, so header prose can self-invalidate):
- Use file-content reads + line-by-line filtering with `re.match(r'^\s*#', line)` to exclude comment lines.
- Scope every assertion to specific file PATH constants defined at module top — never glob `**/*.sh` recursively into the test file itself.
- This test file body WILL contain strings like `[P85]` and `Phase 16 override` for docstring + variable-name reasons. The scoped reads against `CUT_RELEASE` / `CHECK_GATE` / `CHECK_EAR_TEST` paths prevent self-collision.
  </action>
  <verify>
    <automated>uv run pytest tests/repo/test_gate_42_hybrid_in_force.py -q --no-header</automated>
  </verify>
  <done>≥10 positive-assertion tests pass; the test file does NOT self-invalidate (scopes greps to specific file constants); replaces the expiry-clock test with hybrid-in-force pins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision log entry → STATE.md cross-reference | Two repo-internal files reference each other; tampering one without the other breaks the audit chain |
| test_gate_42_hybrid_in_force.py → release scripts | Grep-based assertions over file contents — risk of self-invalidation if tokens leak from test body into scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Tampering | Future revert resurrects override | mitigate | `test_expiry_test_file_actually_deleted` pins absence; future PR adding the file back is caught |
| T-42-05-02 | Tampering | Override constants sneak back via copy-paste | mitigate | `test_no_override_constants_remain_in_release_scripts` scoped to `*.sh` files; grep filters comments |
| T-42-05-03 | Repudiation | STATE.md decision line silently edited | accept | Git history is the audit chain; test pins the RETIRED annotation pattern only |
| T-42-05-04 | Information Disclosure | Decision log contents | accept | Decision log is internal-facing planning doc; no secrets, no Kaan-private content |
</threat_model>

<verification>
- Decision log exists: `test -f .planning/decisions/P85-OVERRIDE-RETIRED.md`
- Expiry test fully removed: `! test -f tests/repo/test_phase_16_override_expiry.py && ! ls tests/repo/__pycache__/*phase_16_override* 2>/dev/null`
- Positive-assertion test passes: `uv run pytest tests/repo/test_gate_42_hybrid_in_force.py -q`
- Phase 27 / 39 baselines unchanged: `uv run pytest tests/repo/test_g5_poc_files_untouched.py tests/security/test_bundle_id_locked.py -q`
- STATE.md cross-reference present: `grep -q "P85-OVERRIDE-RETIRED.md" .planning/STATE.md`
- Full tests/repo/ green (now that expiry test is gone + hybrid test is in): `uv run pytest tests/repo/ -q --no-header`
</verification>

<success_criteria>
- `.planning/decisions/P85-OVERRIDE-RETIRED.md` shipped with all four canonical sections (Status / History / Replacement / Audit Trail)
- `tests/repo/test_phase_16_override_expiry.py` DELETED (no `.py` AND no compiled `.pyc` shadow)
- `tests/repo/test_gate_42_hybrid_in_force.py` shipped with ≥10 positive-assertion tests, all passing
- `.planning/STATE.md` Phase 16 override decision line annotated RETIRED with cross-reference to the Decision Log entry
- Grep hygiene observed in the new test (no self-invalidation from token leakage)
</success_criteria>

<output>
After completion, create `.planning/phases/42-hallucination-gate-v3-hybrid/42-05-SUMMARY.md`.
</output>
