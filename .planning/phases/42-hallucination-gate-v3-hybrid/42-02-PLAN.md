---
phase: 42-hallucination-gate-v3-hybrid
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/eval/recalibrate_thresholds.py
  - eval/THRESHOLD-RECALIBRATION-LOG.md
  - .github/workflows/eval.yml
  - tests/eval/test_recalibrate_thresholds.py
  - tests/eval/test_check_real_corpus_mode.py
autonomous: true
requirements: [GATE-04]
must_haves:
  truths:
    - "`scripts/eval/recalibrate_thresholds.py` runs the existing 2-judge eval against `eval/corpus/sessions/` and produces a per-session + per-genre F1 / substance / cited-cosine / bypass table"
    - "When measured F1 is within ±0.10 of the value in `eval/THRESHOLD-LOCK.md`, the script appends an in-tolerance audit entry to `eval/THRESHOLD-RECALIBRATION-LOG.md` and exits 0 without modifying THRESHOLD-LOCK.md"
    - "When measured F1 is outside ±0.10, the script exits 1 with a structured `RECALIBRATION_REQUIRED` reason — re-signing THRESHOLD-LOCK.md is a deliberate human action, NOT an autonomous edit (per Phase 27 re-tuning protocol)"
    - "`.github/workflows/eval.yml` gains a `--check-real-corpus` mode that fails the workflow if `eval/corpus/sessions/` has fewer than 6 sessions OR the recalibration log lookback shows no entry in the last 30 days"
    - "`eval/THRESHOLD-RECALIBRATION-LOG.md` is the audit trail; entries are timestamped (ISO8601 UTC) and structured (genre, session_id, measured F1 / substance / cited_cosine / bypass, delta vs lock, verdict)"
  artifacts:
    - path: "scripts/eval/recalibrate_thresholds.py"
      provides: "Recalibration driver that wraps replay_harness + parses scorecard JSON + computes ±0.10 tolerance band + appends audit log"
      exports: ["main", "measure_against_corpus", "compute_delta", "RECALIBRATION_TOLERANCE"]
    - path: "eval/THRESHOLD-RECALIBRATION-LOG.md"
      provides: "Append-only audit trail of every recalibration run; new entries written by the script"
      contains: "## Audit Trail"
    - path: ".github/workflows/eval.yml"
      provides: "Extended with `--check-real-corpus` mode that gates on corpus size + recalibration log freshness"
      contains: "check-real-corpus"
    - path: "tests/eval/test_recalibrate_thresholds.py"
      provides: "Unit tests for tolerance-band math, audit-log append, in-tolerance-no-mod invariant, out-of-tolerance-exit-1"
    - path: "tests/eval/test_check_real_corpus_mode.py"
      provides: "YAML-parses + step-grep tests pinning the workflow contract (corpus-size + log-freshness gates)"
  key_links:
    - from: "scripts/eval/recalibrate_thresholds.py"
      to: "scripts/eval/replay_harness.py"
      via: "subprocess invocation OR direct import of `replay_one_session` + iteration loop"
      pattern: "from scripts.eval.replay_harness|replay_harness.py"
    - from: "scripts/eval/recalibrate_thresholds.py"
      to: "eval/THRESHOLD-LOCK.md"
      via: "`scripts.eval.threshold_lock.parse_threshold_lock_frontmatter` for the reference values"
      pattern: "parse_threshold_lock_frontmatter"
    - from: "scripts/eval/recalibrate_thresholds.py"
      to: "eval/THRESHOLD-RECALIBRATION-LOG.md"
      via: "append-only write of structured audit entry"
      pattern: "THRESHOLD-RECALIBRATION-LOG"
    - from: ".github/workflows/eval.yml"
      to: "scripts/eval/recalibrate_thresholds.py"
      via: "GitHub Actions step invocation under `--check-real-corpus`"
      pattern: "recalibrate_thresholds.py"
---

<objective>
Ship the threshold-recalibration tooling so the locked Phase 27 thresholds (f1≥0.80, substance≥0.65, cited-cosine≥0.40, bypass≤0.15, per-genre F1≥0.70) can be tested against the real-corpus WAVs once Kaan discharges GATE-03. The ±0.10 tolerance band logic is the load-bearing invariant — within band = audit entry only; outside band = exit 1 + RECALIBRATION_REQUIRED.

Purpose: Per CONTEXT D-GATE-04 — "if measured F1 is within ±0.10 of the locked values, no re-lock needed; just commit the audit-trail entry. If outside, recalibrate + re-sign `THRESHOLD-LOCK.md`." Autonomous re-signing of THRESHOLD-LOCK.md is FORBIDDEN (Phase 27 re-tuning protocol); the script flags the human action explicitly.

CI extension: `.github/workflows/eval.yml` gains a `--check-real-corpus` step that fails when `eval/corpus/sessions/` has <6 sessions or recalibration log is stale (>30d). This converts "corpus-empty + thresholds-untested" from silent-pass to red.

Output:
- `scripts/eval/recalibrate_thresholds.py` — recalibration driver + ±0.10 tolerance math.
- `eval/THRESHOLD-RECALIBRATION-LOG.md` — audit trail seed file.
- Extended `.github/workflows/eval.yml` with `--check-real-corpus` mode.
- 2 test files covering tolerance math + workflow YAML contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-CONTEXT.md

@scripts/eval/replay_harness.py
@scripts/eval/threshold_lock.py
@scripts/eval/scorecard.py
@scripts/eval/f1.py
@eval/THRESHOLD-LOCK.md
@.github/workflows/eval.yml

<interfaces>
<!-- Phase 27 surfaces this plan extends. -->

From `scripts/eval/replay_harness.py` (Phase 27-01):
- CLI: `--corpus`, `--judges`, `--threshold-lock`, `--output`, `--vcr-mode`
- Writes `<output>/eval_report.json` + `<output>/scorecard.md`
- Per-session results carry: `session`, `genre`, `f1`, `substance`, `cited_cosine`, `bypass`, `verdict`
- Exit 0 = pass-thresholds; exit 1 = any session below threshold (CONTEXT EVAL-06)
- Direct import path: `from scripts.eval.replay_harness import replay_one_session` (verified — Plan 42-02 imports this for hermetic per-session iteration when no subprocess is desired)

From `scripts/eval/threshold_lock.py` (Phase 27-04):
- `parse_threshold_lock_frontmatter(path: Path) -> dict` returns `{thresholds: {f1_min, substance_min, cited_cosine_min, bypass_max, per_genre_f1_min}, ...}`
- `is_signed(parsed: dict) -> bool` — checks `kaan_signed` field truthiness
- `DEFAULT_THRESHOLDS` constant (mirror of CONTEXT EVAL-06 values)
- Uses `yaml.safe_load` (V5 ASVS); never `yaml.load`

From `eval/THRESHOLD-LOCK.md` (Phase 27-04, autonomous-signed):
- Frontmatter values: f1_min=0.80, substance_min=0.65, cited_cosine_min=0.4, bypass_max=0.15, per_genre_f1_min=0.70
- Re-tuning protocol explicitly forbids autonomous edits after first sign

From `.github/workflows/eval.yml` (Phase 27-04, existing structure):
- Job: `eval`, triggers: `pull_request` + `schedule '0 5 * * *'` + `workflow_dispatch`
- Current step names: "Verify corpus integrity", "Run replay harness (PR mode — Flash only)", "Run replay harness (nightly canary — Pro + Flash)", "Post scorecard to PR", "Commit eval-run artifact (nightly only)"
- Adds a NEW step: "Check real-corpus + recalibration freshness (--check-real-corpus)" — runs on nightly schedule AND workflow_dispatch (skip on PR mode where cassettes are sufficient)

</interfaces>

@.planning/phases/42-hallucination-gate-v3-hybrid/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: recalibrate_thresholds.py + audit log seed + tolerance-band tests</name>
  <files>scripts/eval/recalibrate_thresholds.py, eval/THRESHOLD-RECALIBRATION-LOG.md, tests/eval/test_recalibrate_thresholds.py</files>
  <action>
Per D-GATE-04 — build the recalibration driver + ±0.10 tolerance band + audit-log writer.

(a) `scripts/eval/recalibrate_thresholds.py`:
- Module docstring: cite Phase 27 re-tuning protocol, GATE-04, RECALIBRATION_TOLERANCE=0.10. Explicit warning that the script NEVER auto-edits THRESHOLD-LOCK.md.
- Constants: `RECALIBRATION_TOLERANCE = 0.10`; `LOG_PATH = Path("eval/THRESHOLD-RECALIBRATION-LOG.md")`; `LOCK_PATH = Path("eval/THRESHOLD-LOCK.md")`; `DEFAULT_CORPUS = Path("eval/corpus/sessions")`.
- `compute_delta(measured: float, locked: float) -> tuple[float, str]` — returns `(delta, verdict)` where verdict is one of `"in_tolerance"` (|delta| ≤ 0.10) or `"out_of_tolerance"`.
- `measure_against_corpus(corpus_dir: Path, judges: list[str]) -> dict` — invokes the existing replay_harness (subprocess preferred for isolation; direct import acceptable). Returns aggregate `{f1, substance, cited_cosine, bypass}` plus per-session + per-genre breakdowns parsed from `<output>/eval_report.json`.
- `format_audit_entry(measured: dict, locked: dict, timestamp: str) -> str` — produces a markdown block matching the audit log schema (see (b) below).
- `append_audit_entry(log_path: Path, entry: str) -> None` — append-only; never truncates.
- `main()` argparse: `--corpus`, `--judges` (default `"gemini-3-flash,gemini-3-pro"`), `--lock-path`, `--log-path`, `--dry-run`.
- Exit codes: `0` = in_tolerance (audit entry appended); `1` = out_of_tolerance (audit entry appended + stderr "RECALIBRATION_REQUIRED: re-sign eval/THRESHOLD-LOCK.md manually after re-running both judges"); `2` = corpus too small (<6 sessions) or judges unavailable.
- Uses `scripts.eval.threshold_lock.parse_threshold_lock_frontmatter` for the locked values. yaml.safe_load only (V5 ASVS).

(b) `eval/THRESHOLD-RECALIBRATION-LOG.md`:
- Seed file with header explaining the audit trail, then a `## Audit Trail` section + `## Entry Schema` block. Entry schema:
  ```
  ### YYYY-MM-DDTHH:MM:SSZ — verdict={in_tolerance|out_of_tolerance}
  - corpus: eval/corpus/sessions/ (N sessions, M genres)
  - judges: gemini-3-flash, gemini-3-pro
  - measured: f1=X.XX  substance=X.XX  cited_cosine=X.XX  bypass=X.XX
  - locked:   f1=0.80  substance=0.65  cited_cosine=0.40  bypass=0.15
  - delta:    f1=±X.XX substance=±X.XX cited_cosine=±X.XX bypass=±X.XX
  - per-genre: hard_tek f1=X.XX  techno f1=X.XX  house f1=X.XX
  - verdict: <in_tolerance | out_of_tolerance>
  - action:  <none | RECALIBRATION_REQUIRED — re-sign THRESHOLD-LOCK.md after re-run>
  ```
- One seed entry block showing the schema with placeholder values (verdict=`schema_example`).
- Cross-references: `scripts/eval/recalibrate_thresholds.py`, `eval/THRESHOLD-LOCK.md`, Phase 27 re-tuning protocol.

(c) `tests/eval/test_recalibrate_thresholds.py`:
- `test_compute_delta_in_tolerance()` — measured=0.83, locked=0.80 → delta=+0.03, verdict="in_tolerance".
- `test_compute_delta_on_boundary()` — measured=0.70, locked=0.80 → delta=-0.10, verdict="in_tolerance" (|±0.10| is INCLUSIVE).
- `test_compute_delta_outside_band()` — measured=0.65, locked=0.80 → delta=-0.15, verdict="out_of_tolerance".
- `test_format_audit_entry_iso8601_timestamp()` — produced markdown contains a regex-matching ISO8601 timestamp.
- `test_append_audit_entry_is_append_only(tmp_path)` — write 3 entries sequentially, assert all 3 present + original header intact.
- `test_script_never_writes_to_lock_file(tmp_path, monkeypatch)` — mock a corpus of 6 sessions returning out-of-tolerance F1, run `main(["--corpus", str(tmp_path)])`, assert original `eval/THRESHOLD-LOCK.md` md5 unchanged after exit 1.
- `test_main_exits_2_when_corpus_too_small(tmp_path)` — empty corpus dir → exit 2.
- `test_main_exits_2_when_no_recent_log_entry()` — N/A here (workflow concern, covered in Task 2 test).

Use `monkeypatch` + tmp_path liberally — no real Gemini calls in this test file.
  </action>
  <verify>
    <automated>uv run pytest tests/eval/test_recalibrate_thresholds.py -q --no-header</automated>
  </verify>
  <done>≥7 tests pass; `scripts/eval/recalibrate_thresholds.py --dry-run` runs cleanly with corpus=existing-placeholder-dir + exits 2 (corpus too small, expected); `eval/THRESHOLD-RECALIBRATION-LOG.md` carries the schema block.</done>
</task>

<task type="auto">
  <name>Task 2: Extend .github/workflows/eval.yml with --check-real-corpus mode + workflow test</name>
  <files>.github/workflows/eval.yml, tests/eval/test_check_real_corpus_mode.py</files>
  <action>
Per D-GATE-04 CI gate enforcement — extend the existing Phase 27 workflow.

(a) `.github/workflows/eval.yml`:
- Insert a NEW step after "Run replay harness (nightly canary — Pro + Flash)" and before "Post scorecard to PR":
  ```yaml
  - name: "Real-corpus calibration freshness gate (Phase 42 GATE-04)"
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
    run: |
      # Plan 42-02 — fail if corpus has <6 sessions OR recalibration log
      # has no entry in last 30 days. Does NOT auto-recalibrate.
      uv run python scripts/eval/recalibrate_thresholds.py \
        --corpus eval/corpus/sessions \
        --check-only \
        --lock-path eval/THRESHOLD-LOCK.md \
        --log-path eval/THRESHOLD-RECALIBRATION-LOG.md
  ```
- Add `--check-only` flag handling to `recalibrate_thresholds.py` (Task 1 already exposes the CLI surface; extend in this task only if missing): when `--check-only`, do NOT invoke replay_harness; just check (i) `len(list(corpus.glob("*/input.wav"))) >= 6` and (ii) most recent audit entry timestamp is within 30 days. Exit 0 if both, exit 1 if either fails with structured stderr.
- Keep YAML parse-safe — quote any step names containing colons (Phase 27-04 Rule-1 deviation precedent).
- Do NOT modify existing PR-mode / nightly-canary steps. This step ONLY runs on schedule + workflow_dispatch.

(b) `tests/eval/test_check_real_corpus_mode.py`:
- `test_eval_yml_parses_as_valid_yaml()` — `yaml.safe_load(open(".github/workflows/eval.yml"))` succeeds without exception.
- `test_eval_yml_has_check_real_corpus_step()` — parsed YAML structure contains a step whose `name` matches the regex `Real-corpus calibration freshness gate`.
- `test_check_real_corpus_step_gated_on_schedule_only()` — that step's `if` expression contains both `schedule` and `workflow_dispatch` and NOT `pull_request`.
- `test_check_real_corpus_step_invokes_recalibrate_script()` — the step's `run` body contains `recalibrate_thresholds.py` AND `--check-only`.
- `test_check_only_mode_exits_1_on_small_corpus(tmp_path, monkeypatch)` — empty corpus dir + monkeypatched recent log → `subprocess.run([sys.executable, "scripts/eval/recalibrate_thresholds.py", "--check-only", ...])` returns nonzero with stderr matching `fewer than 6 sessions`.
- `test_check_only_mode_exits_1_on_stale_log(tmp_path)` — corpus with 6 placeholder session dirs but recalibration log empty → exit 1, stderr matching `no recalibration log entry in last 30 days` (or `stale recalibration log`).
- `test_check_only_mode_exits_0_when_both_fresh(tmp_path)` — both invariants met → exit 0.

These tests use `subprocess.run` against the script in tmp_path fixtures; no real Gemini calls.
  </action>
  <verify>
    <automated>uv run pytest tests/eval/test_check_real_corpus_mode.py -q --no-header && uv run python -c "import yaml; yaml.safe_load(open('.github/workflows/eval.yml'))" && echo OK</automated>
  </verify>
  <done>≥6 workflow tests pass; eval.yml still YAML-parses; new step lives between nightly-canary and post-scorecard; `--check-only` invocation exits as designed for all three coverage paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI nightly → THRESHOLD-LOCK.md | The recalibration script READS the lock but NEVER WRITES (autonomous re-sign forbidden) |
| recalibrate_thresholds.py → replay_harness.py | Subprocess boundary; eval_report.json crosses as untrusted JSON |
| External judge results → audit log | Real Gemini API returns become structured markdown rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | THRESHOLD-LOCK.md autonomous edit | mitigate | Task 1 test `test_script_never_writes_to_lock_file` pins md5 invariance after run; script's only LOCK_PATH operation is `parse_threshold_lock_frontmatter` (read-only via yaml.safe_load) |
| T-42-02-02 | Tampering | THRESHOLD-RECALIBRATION-LOG.md mutation | mitigate | append-only write helper; test pins 3-entry sequential append preserves original |
| T-42-02-03 | Information Disclosure | eval_report.json parsing | mitigate | T-27-01-01 scorecard renderer already strips response text; recalibrate_thresholds.py only consumes numeric metric fields, never `responses/<event>.txt` |
| T-42-02-04 | Spoofing | Audit log timestamp authenticity | accept | Single-machine local timestamp via `datetime.now(timezone.utc)`; no signing required for an internal audit trail |
| T-42-02-05 | Denial of Service | Real-corpus replay cost | mitigate | `--check-only` mode skips replay_harness invocation entirely (no Gemini calls); full recalibration is a Kaan-discharge (nightly canary only, not PR) |
</threat_model>

<verification>
- New tests pass: `uv run pytest tests/eval/test_recalibrate_thresholds.py tests/eval/test_check_real_corpus_mode.py -q`
- Phase 27 regression clean: `uv run pytest tests/eval/ -q --no-header`
- THRESHOLD-LOCK.md unchanged: `md5 eval/THRESHOLD-LOCK.md` before/after script run with synthetic out-of-tolerance corpus
- eval.yml structural: `yq '.jobs.eval.steps[].name' .github/workflows/eval.yml | grep "Real-corpus"`
- Audit log syntax: `grep -E "verdict=(in_tolerance|out_of_tolerance|schema_example)" eval/THRESHOLD-RECALIBRATION-LOG.md`
</verification>

<success_criteria>
- `scripts/eval/recalibrate_thresholds.py` shipped with ±0.10 tolerance band + audit log writer + `--check-only` mode
- `eval/THRESHOLD-RECALIBRATION-LOG.md` seeded with schema block
- `.github/workflows/eval.yml` extended with the real-corpus freshness gate (schedule + dispatch only, not PR)
- ≥13 new tests pass across 2 test files
- THRESHOLD-LOCK.md never auto-mutated — verified by md5 + by explicit test
- Phase 27 eval baseline untouched
</success_criteria>

<output>
After completion, create `.planning/phases/42-hallucination-gate-v3-hybrid/42-02-SUMMARY.md`.
</output>
