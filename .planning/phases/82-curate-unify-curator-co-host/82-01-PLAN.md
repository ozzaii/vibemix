---
phase: 82-curate-unify-curator-co-host
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/library/test_toolset.py
  - tests/library/test_curate_unify.py
autonomous: true
requirements: [CURATE-01, CURATE-02]
must_haves:
  truths:
    - "Each CURATE-01/02 acceptance criterion has a failing xfail(strict=True) scaffold that flips green only when Plan 02 lands the seam"
    - "The Phase-79 lens-shared regression is pinned real-green (persona half already DONE — never re-implemented)"
    - "The existing curate/Telegram/pill/MCP surfaces have a real-green behavioral pin that Plan 02 must not break"
    - "A real-green import-boundary pin proves the curator does NOT import the live MusicState path"
  artifacts:
    - path: "tests/library/test_curate_unify.py"
      provides: "CURATE-01 genre-via-prototypes + CURATE-02 taste-read xfail scaffolds + one-mechanism/no-MusicState/regression real-green pins"
      contains: "xfail(strict=True)"
    - path: "tests/library/test_toolset.py"
      provides: "extended get_track_features genre-via-prototypes scaffold reusing the existing in-memory library fixture"
      contains: "genre"
  key_links:
    - from: "tests/library/test_curate_unify.py"
      to: "vibemix.library.genre_prototypes"
      via: "asserts the curator genre path routes through the ONE prototype mechanism"
      pattern: "genre_prototypes"
    - from: "tests/library/test_curate_unify.py"
      to: "vibemix.profile.load_profile"
      via: "asserts the curator taste hint reads the shared profile, guarded+lazy"
      pattern: "load_profile|render_profile_for_cache"
---

<objective>
Install the Nyquist safety net for Phase 82 BEFORE any `src/` change: every CURATE-01/02 acceptance criterion gets a failing `xfail(strict=True)` scaffold that flips to a real pass only when Plan 02 lands the seam (a silent early-pass becomes an `xpassed` → HARD failure). Real-green pins lock the things that must STAY true: the Phase-79 lens sharing (persona half already DONE), the existing curate/Telegram/pill/MCP surfaces, and the import-boundary gate (curator must not touch the live `MusicState` path).

Purpose: This is the documented Phase 77-81 convention — Wave-0 RED scaffolds spec the implementation by their tests so Plan 02 can never fake green. No `src/vibemix/` file is touched here.
Output: One new test file (`tests/library/test_curate_unify.py`) + an extension to `tests/library/test_toolset.py`. No package installs, no API key, no Gemini client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-curate-unify-curator-co-host/82-CONTEXT.md
@.planning/phases/82-curate-unify-curator-co-host/82-RESEARCH.md
@.planning/phases/82-curate-unify-curator-co-host/82-PATTERNS.md
@.planning/phases/82-curate-unify-curator-co-host/82-VALIDATION.md

<interfaces>
<!-- Contracts the scaffolds assert against. Extracted from live source 2026-05-26. Do NOT explore the codebase — use these. -->

From src/vibemix/library/toolset.py (SEAM #1 target — `genre: None` today at :124):
```
class LibraryToolset:
    def __init__(self, embedder, store: LibraryStore, library: RekordboxLibrary) -> None: ...
    def get_track_features(self, args: dict) -> dict   # returns {... "genre": None}
    # self._store is the LibraryStore; self._library is the RekordboxLibrary
```

From src/vibemix/library/genre_prototypes.py (the ONE shared perception mechanism — Phase 78, already read by the co-host at state/refresh.py:254):
```
def classify(track_embedding, protos, labels, centroid, *, floor=0.25, margin=0.05) -> tuple[str, float]
def load_or_build_prototypes(store) -> tuple[np.ndarray, list[str], np.ndarray | None]
class GenrePrototypeLookup:
    def __init__(self, store=None) -> None: ...
    def _cached_embedding(self, track_id: str) -> np.ndarray | None  # €0, reads cached vector
    def classify_playing(self, track_id: str) -> tuple[str, float]   # ("unknown", 0.0) abstain when no embedding
```

From src/vibemix/profile/cache_render.py + profile/__init__.py (the ONE taste layer — co-host-only today, the CURATE-02 gap):
```
def load_profile() -> dict | None              # None when absent/invalid/consent-OFF (default OFF)
def render_profile_for_cache(profile: dict | None) -> str   # "" when None; only 5 allowlisted fields, NO track titles
```

From src/vibemix/library/agent.py (SEAM #2 attach sites — gemini backend):
```
def _system_instruction() -> str       # build site :147 — build_curator_instruction(lens) + "\n" + _RULES_BLOCK
def _interactive_system_instruction() -> str   # build site :166 — build_curator_instruction(lens) + "\n" + _INTERACTIVE_FLOW_BLOCK
```

From src/vibemix/library/codex_curate.py (SEAM #2 attach site — codex backend):
```
def _system_prompt() -> str            # build site :125 — build_curator_instruction(lens) + " " + _RULES_BLOCK
```

ALREADY SHARED (Phase 79 — regression-pin only, do NOT re-implement):
- `read_shared_lens(store, default)` (runtime/settings.py:68) read by BOTH backends' `_shared_lens()`.
- `build_curator_instruction(lens)` (prompts/matrix.py) called at agent.py:145/164 + codex_curate.py:123.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test_toolset.py with the CURATE-01 genre-via-prototypes scaffold</name>
  <read_first>
    - tests/library/test_toolset.py (the file being extended — reuse its existing `_make_track` :22, `library` :36, `toolset` :43 fixtures; the fixture sets `lib.tracks` in-memory and avoids the cache — KEEP that discipline, no `RekordboxLibrary.CACHE_PATH` write)
    - src/vibemix/library/toolset.py:108-125 (the `get_track_features` SEAM — `genre: None` today)
    - src/vibemix/library/genre_prototypes.py:299-335 (`_cached_embedding` + `classify_playing` — the mechanism the curator must route through; abstain is `("unknown", 0.0)`)
    - 82-PATTERNS.md "tests/library/test_toolset.py (extend)" section (the analog + the library.pkl gotcha note)
  </read_first>
  <action>
    Append xfail-strict scaffolds to `tests/library/test_toolset.py` (do NOT modify the existing passing tests). Add:
    (a) `test_genre_via_prototypes_when_classified` (xfail strict, reason="CURATE-01 SEAM #1 — Plan 02"): build a toolset over the in-memory `library` fixture, monkeypatch the curator's genre resolution so a known track classifies to a real label (monkeypatch `GenrePrototypeLookup.classify_playing` or the module-level `classify`/`_cached_embedding` the seam will call — assert `get_track_features({"track_id": "t000"})["genre"]` equals that real label, NOT `None`). Resolve the exact monkeypatch target against whatever name the seam binds — the assertion is "genre is the prototype-resolved label, not None".
    (b) `test_genre_honest_none_on_abstain` (xfail strict): when the prototype mechanism abstains (`("unknown", 0.0)` — e.g. no cached embedding for the track), `get_track_features(...)["genre"]` is `None` or `"unknown"` (honest-null, byte-identical class to today's `genre: None` and the existing Camelot honest-null at :116) — NEVER a fabricated genre.
    Reuse the existing `MagicMock()` embedder/store from the `toolset` fixture; keep the in-memory `lib.tracks` discipline (no real cache write). Per Pitfall 2: the scaffolds assert the curator routes through `genre_prototypes`, never a fresh `np.mean`/cosine. Per CONTEXT D (CURATE-01): one shared mechanism, both surfaces. NO fenced code in the implementation prose — these are test scaffolds you write directly.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset.py --runxfail -x 2>&1 | tail -5 ; PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The two new scaffolds are collected as `xfailed` (strict) on a plain run; with `--runxfail` they FAIL today with a clean error (genre is `None`, not the prototype label) — proving they are RED and will flip when Plan 02 lands.
    - The 6 existing `test_toolset.py` tests stay green (the `test_features_camelot_is_deterministic` + grounding-gate tests untouched).
    - No `RekordboxLibrary.CACHE_PATH` write (the real `~/.cache/vibemix/library.pkl` mtime unchanged — the in-memory fixture discipline holds).
    - No `genai.Client`, no `GEMINI_API_KEY` read in the new tests.
  </acceptance_criteria>
  <done>`tests/library/test_toolset.py` carries 2 strict-xfail CURATE-01 scaffolds (genre-classified + honest-none-abstain), the existing tests stay green, no cache write.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_curate_unify.py — CURATE-02 taste scaffold + one-mechanism/no-MusicState/regression real-green pins</name>
  <read_first>
    - src/vibemix/library/agent.py:114-170 (`_shared_lens` lazy+guarded analog at :128-134; `_system_instruction` build :147; `_interactive_system_instruction` build :166 — the SEAM #2 attach sites)
    - src/vibemix/library/codex_curate.py:95-127 (twin `_shared_lens` + `_system_prompt` build :125 — SEAM #2 twin site)
    - src/vibemix/profile/cache_render.py:31-57 (`render_profile_for_cache` — "" on None, only 5 allowlisted fields, NO titles) + src/vibemix/profile/__init__.py (`load_profile` → None when consent-OFF)
    - tests/memory/test_no_live_path_import.py:48-56 (FORBIDDEN_IMPORTS = vibemix.agent/vibemix.prompts/live path; FORBIDDEN_NAMES = MusicState/EventDetector — the import-boundary gate the curator reads must NOT trip)
    - tests/library/test_curator_persona_seam.py (the Phase-79 lens regression — already green; reference for how the lens-shared pin is asserted)
    - 82-PATTERNS.md "Construction-Site Map" (why SEAM #1 reaches both backends via shared toolset; SEAM #2 is two edits)
  </read_first>
  <action>
    Create `tests/library/test_curate_unify.py`. Add the CURATE-02 taste scaffolds (xfail strict, reason="CURATE-02 taste half — Plan 02") + the real-green pins:
    (a) `test_curator_taste_hint_present_when_profile_set` (xfail strict): monkeypatch `vibemix.profile.load_profile` to return a populated allowlisted profile dict (preferred_genre/mix_style_tags/tempo_preference_bin — NO titles); reset the gemini backend's `_SYSTEM_INSTRUCTION_CACHE`/`_SYSTEM_INSTRUCTION_LENS` module globals; assert the built `_system_instruction()` (agent.py) CONTAINS a profile-derived token (e.g. the `preferred_genre` value or the `render_profile_for_cache` header). Today it does NOT → RED.
    (b) `test_curator_taste_cold_path_identical_when_no_profile` (xfail strict): with `load_profile` → `None` (consent-OFF default), the built `_system_instruction()` is byte-identical to `build_curator_instruction(lens) + "\n" + _RULES_BLOCK` (today's cold path) — proving the taste seam is additive and cold-path-identical. Today this is already true, so mark xfail strict ONLY IF the seam changes the cold-path string; if the seam is correctly additive the cold path stays identical → make this a REAL-GREEN pin instead (the cold path must STAY identical through Plan 02). Choose real-green: this is a "must-stay-true" guard.
    (c) `test_taste_hint_reaches_codex_backend` (xfail strict): same as (a) but against `codex_curate._system_prompt()` (reset its `_SYSTEM_PROMPT_CACHE`/`_SYSTEM_PROMPT_LENS` globals first) — proving SEAM #2 reaches BOTH backends (the CURATE acid test). Today RED.
    (d) `test_taste_hint_no_track_titles_leak` (REAL-GREEN, ASVS V5 / Pitfall 4): construct a profile dict and assert `render_profile_for_cache(profile)` emits NONE of the profile's would-be title strings — only the 5 allowlisted fields. This pins the privacy threat (T-82-01) and stays green through Plan 02.
    (e) `test_lens_shared_across_both_backends` (REAL-GREEN regression pin, Pitfall 1 — persona DONE): assert BOTH `agent._shared_lens` and `codex_curate._shared_lens` resolve the SAME `read_shared_lens`-driven value (monkeypatch a config store extra["lens"]="critique" → both return "critique"). Persona half is ALREADY shared — this pin proves it and forbids re-implementation.
    (f) `test_curator_does_not_import_musicstate` (REAL-GREEN, invariant #1): import `vibemix.library.toolset`, `vibemix.library.agent`, `vibemix.library.codex_curate` fresh in a subprocess (clone the `test_no_live_path_import.py` subprocess sys.modules idiom) and assert NO `vibemix.state.music_state` / `MusicState` leaked into `sys.modules` from the curator path. The shared perception is the LIBRARY representation, never the live state object.
    (g) `test_existing_surfaces_still_import` (REAL-GREEN regression): import `vibemix.library.agent.ViberAgent`, `vibemix.library.telegram_bridge`, `vibemix.library.next_suggestion`, `vibemix.library.mcp_server.build_toolset` — all still importable (cheap smoke pin that Plan 02 doesn't break the surfaces; the deeper behavioral pins live in their own test files which Plan 02 must keep green).
    Mirror the `_shared_lens` lazy+guarded shape (PATTERNS Pattern 1) in any helper. Where a built instruction is asserted, reset the relevant module-cache globals first (the backends cache the instruction keyed by lens). NO fenced code in this prose — write the scaffolds directly in the test file.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_curate_unify.py 2>&1 | tail -5 ; PYTHONPATH=src python3 -m pytest -q tests/memory/test_no_live_path_import.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The CURATE-02 taste scaffolds (a, c) collect as `xfailed` strict; with `--runxfail` they FAIL today (no taste token in the built instruction) — RED, flip in Plan 02.
    - The real-green pins (b cold-path, d no-leak, e lens-shared, f no-MusicState, g surfaces-import) PASS today and must STAY green through Plan 02.
    - `tests/memory/test_no_live_path_import.py` stays green (the curator's lazy profile/genre reads do not leak the live path).
    - No `genai.Client`, no `GEMINI_API_KEY`, no package install.
  </acceptance_criteria>
  <done>`tests/library/test_curate_unify.py` exists with the CURATE-02 taste xfail scaffolds + the one-mechanism/no-MusicState/lens-shared/no-leak/surfaces-import real-green pins; the import-boundary gate stays CLEAN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.json → curator prompt | The DJ profile (local 2KB allowlisted JSON) crosses into the curator's system instruction. Only allowlisted fields may cross; NO track titles / free-form. |
| library cached embedding → curator genre | Deterministic library-side fact crosses into the tool surface; the model never supplies genre. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Information Disclosure | profile read into curator instruction | mitigate | Task 2(d) REAL-GREEN pin: `render_profile_for_cache` emits only the 5 allowlisted fields, NO titles/free-form; profile schema (`additionalProperties:false`) + consent-default-OFF enforce this upstream. |
| T-82-02 | Tampering | curator-supplied genre (hallucinated fact) | mitigate | Tasks 1(a/b) scaffolds pin genre as a deterministic `genre_prototypes` library fact (not LLM-computed); honest-`None` on abstain. The seen-set grounding gate (`toolset.py:137-147`) is untouched. |
| T-82-03 | Tampering | import-boundary breach (live path leaks via curator) | mitigate | Task 2(f) REAL-GREEN no-MusicState pin + `tests/memory/test_no_live_path_import.py` stays CLEAN; lazy-import discipline (Plan 02). |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO packages (RESEARCH Package Legitimacy Audit — pure internal wiring); no slopcheck needed. No `[ASSUMED]`/`[SUS]` packages exist. |
</threat_model>

<verification>
- `pytest -q tests/library/test_toolset.py tests/library/test_curate_unify.py` — new scaffolds collect correctly (CURATE xfails + real-green pins).
- `pytest -q tests/memory/test_no_live_path_import.py` — import-boundary gate CLEAN.
- Full suite green at the phase-start baseline (4523 passed) + the new strict-xfails (no NEW failures, no xpass).
- No `genai.Client`, no `GEMINI_API_KEY`, no package install (grep the new test files).
</verification>

<success_criteria>
- Every CURATE-01/02 acceptance criterion has a failing `xfail(strict=True)` scaffold (flips in Plan 02).
- The lens-shared regression + no-MusicState + no-title-leak + existing-surfaces pins are REAL-GREEN and stay green.
- No `src/vibemix/` file touched; no package installed; honest green (no API key).
</success_criteria>

<output>
Create `.planning/phases/82-curate-unify-curator-co-host/82-01-SUMMARY.md` when done.
</output>
