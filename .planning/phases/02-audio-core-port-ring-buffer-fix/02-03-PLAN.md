---
phase: 02-audio-core-port-ring-buffer-fix
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/vibemix/audio/features.py
  - src/vibemix/audio/recorder.py
  - src/vibemix/audio/__init__.py
  - tests/audio/test_features.py
  - tests/audio/test_recorder.py
autonomous: true
requirements:
  - ARCH-07
  - AUDIO-07
  - AUDIO-09
  - RECORD-01
  - RECORD-02
must_haves:
  truths:
    - "`snapshot_features(audio_buffer, seconds=5.0)` returns a dict with the exact v4 key set `{silent, rms, onsets_per_sec, sub_share, low_share, mid_share, high_share}` (or `{silent, rms}` on the size<sr/4 early-out path)."
    - "`estimate_bpm(audio_buffer, seconds=6.0)` returns a float in `[0.0, 200.0]` for the BPM autocorr (verbatim port of v4:410-436)."
    - "`snapshot_wav(audio_buffer, seconds, normalize_peak_dbfs=-3.0)` returns RIFF-WAV bytes with the correct header (16kHz mono int16) and never produces int16 values at -32768 from peak-normalization (RESEARCH.md Pitfall 4)."
    - "`energy_curve(audio_buffer, seconds=12.0, hop=1.0)` and `long_arc_curve(audio_buffer, seconds=120.0, hop=10.0)` return list[float] matching v4:381-408 shapes."
    - "`VoiceRecorder` creates `recordings/<YYYYMMDD-HHMMSS>/` with mode 0o700 (per RESEARCH.md Security V8), writes `input.wav` (16kHz mono int16) + `voice.wav` (24kHz mono int16) + `events.jsonl` with a `session_start` event as line 1."
    - "`VoiceRecorder.log_event(kind, **fields)` writes a JSONL record `{t, kind, **fields}` with `t` = seconds-since-start rounded to 3 decimals."
    - "Recorder root is configurable — `VoiceRecorder(root=Path('/some/dir'))` overrides the `cwd()/recordings` default (fixes v4:771 anti-pattern flagged in PATTERNS.md §AntiPatterns-4)."
    - "Snapshot helpers accept an optional pre-allocated `out: np.ndarray` parameter so Plan 03's downstream consumers (Phase 3 state_refresh_loop) can avoid per-call allocation on the 10Hz read path."
  artifacts:
    - path: "src/vibemix/audio/features.py"
      provides: "FFT / RMS / band-share / onsets / BPM autocorr / snapshot_wav / energy_curve / long_arc_curve — verbatim math port of cohost_v4.py:331-436 as free functions taking an AudioBuffer (RESEARCH.md A1 — pure functions are unit-testable without standing up a buffer)"
      exports: ["snapshot_features", "snapshot_wav", "energy_curve", "long_arc_curve", "estimate_bpm"]
    - path: "src/vibemix/audio/recorder.py"
      provides: "VoiceRecorder — per-session dir + input.wav + voice.wav + events.jsonl. Lifted from cohost_v4.py:769-848 with recording root made configurable and dir created with mode=0o700 (privacy guard for Kaan's voice in recordings)"
      contains: "class VoiceRecorder"
    - path: "tests/audio/test_features.py"
      provides: "Dict-shape parity test (v4 contract), peak-normalize correctness test (RESEARCH.md Pitfall 4), BPM range sanity test, energy_curve / long_arc_curve length tests"
    - path: "tests/audio/test_recorder.py"
      provides: "Session dir creation + 0o700 perms + WAV RIFF header validity + events.jsonl session_start line + configurable root tests"
  key_links:
    - from: "src/vibemix/audio/features.py"
      to: "src/vibemix/audio/buffers.py"
      via: "AudioBuffer.snapshot(n_samples) read for the input PCM"
      pattern: "buf.snapshot("
    - from: "src/vibemix/audio/features.py"
      to: "src/vibemix/audio/constants.py"
      via: "SILENT_RMS imported for the `silent` flag in snapshot_features"
      pattern: "from vibemix.audio.constants import SILENT_RMS"
    - from: "src/vibemix/audio/recorder.py"
      to: "src/vibemix/audio/constants.py"
      via: "INPUT_SR_TARGET + OUTPUT_SR imported for WAV header sample rates"
      pattern: "from vibemix.audio.constants import"
---

<objective>
Port the v4 DSP math (FFT/RMS/onsets/band-shares/BPM/peak-normalize/WAV-wrap) as free functions in `features.py`, and the per-session recording machinery (`VoiceRecorder`) into `recorder.py`. The features split from buffers (PATTERNS.md §File-Map-Recommendation note 1) makes the DSP unit-testable in isolation; the recorder lift makes the per-session WAV/JSONL writer importable by Plan 04's macOS callbacks without dragging the buffers module along.

Purpose:
- `features.snapshot_features(buf, seconds=5.0) -> dict` — the LOAD-BEARING contract Phase 3 EventDetector reads to fire PHASE / LAYER_ARRIVAL events. The 7-key return shape from v4:371-379 is the boundary; any drift breaks Phase 3.
- `features.estimate_bpm(buf, seconds=6.0) -> float` — autocorr math from v4:410-436. Phase 3 BPM_VALID_MIN/MAX (constants from Plan 01) filter the result.
- `features.snapshot_wav(buf, seconds, normalize_peak_dbfs=-3.0) -> bytes` — Phase 4 DJCoHostAgent.llm_node consumes this as the inline audio Part to the LLM. Peak-normalize math (v4:313-321) MUST clip before int16 cast (RESEARCH.md Pitfall 4).
- `features.energy_curve` + `long_arc_curve` — for Phase 3 evidence packet (`set_arc=...` field).
- `recorder.VoiceRecorder` — verbatim port of v4:769-848 with two improvements: (1) configurable `root: Path | None = None` defaulting to `Path.cwd() / "recordings"` (fixes the v4:771 `Path(__file__).parent` anti-pattern that would write WAVs into site-packages on a packaged install); (2) the session dir is created with `mode=0o700` (RESEARCH.md Security V8 — recordings contain Kaan's voice, matches the HARD privacy rule in `~/CLAUDE.md`).

Output:
- `src/vibemix/audio/features.py` — five free functions
- `src/vibemix/audio/recorder.py` — `VoiceRecorder` class
- `tests/audio/test_features.py` + `tests/audio/test_recorder.py` — ≥10 cases total
- `__init__.py` re-exports `snapshot_features`, `snapshot_wav`, `energy_curve`, `long_arc_curve`, `estimate_bpm`, `VoiceRecorder`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-CONTEXT.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-RESEARCH.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-PATTERNS.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-01-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-02-PLAN.md
@cohost_v4.py
@src/vibemix/audio/buffers.py
@src/vibemix/audio/constants.py

<interfaces>
<!-- cohost_v4.py source line anchors for verbatim math port: -->

- snapshot_features → cohost_v4.py:331-379
  - Early-out: `if arr.size < self._sr // 4: return {"silent": True, "rms": 0.0}`  (v4:335-336)
  - RMS: `float(np.sqrt(np.mean(arr * arr)))`  (v4:338)
  - Onset detection: 1/50-second windows, energy deltas, threshold = `max(0.005, deltas.mean() + deltas.std())`  (v4:340-350)
  - FFT: `spec_win = 1 << 14` (16384 samples), Hanning window, np.fft.rfft, np.abs  (v4:352-358)
  - Five band slices via `band_energy(lo, hi)` closure: sub 20-100, low 100-300, mid_low 300-1000, mid_hi 1000-4000, high 4000-8000  (v4:360-369)
  - Total + 1e-9 denom; round shares to 2 decimals; round rms to 4; round onsets to 1  (v4:369-379)
  - Return dict shape: {"silent": bool, "rms": float, "onsets_per_sec": float, "sub_share", "low_share", "mid_share" (= mid_low+mid_hi/total), "high_share"}

- snapshot_wav → cohost_v4.py:304-329
  - Lock-guarded copy of last n samples
  - Peak-normalize: if peak > 0, scale = target / peak, target = int(32767 * 10**(dbfs/20))
  - CLIP BEFORE CAST: `np.clip(pcm.astype(np.float32) * scale, -32768, 32767).astype(np.int16)` — order matters per RESEARCH.md Pitfall 4
  - Wave-wrap: BytesIO, wave.open, setnchannels(1), setsampwidth(2), setframerate(sr), writeframes(pcm.tobytes())

- energy_curve → cohost_v4.py:381-392
- long_arc_curve → cohost_v4.py:394-408
- estimate_bpm → cohost_v4.py:410-436
  - 6s window, 1/100-sec frames, mean-subtracted envelope, full np.correlate, take positive lags only
  - lo_lag=30, hi_lag=60 (corresponds to ~100-200 BPM)
  - Return `round(60.0 * 100.0 / best_lag, 1)` or 0.0 on insufficient data

- VoiceRecorder → cohost_v4.py:769-848
  - Session dir: `recordings/<YYYYMMDD-HHMMSS>/` (datetime.now().strftime — keep verbatim)
  - voice.wav: 1 channel, 2-byte samples, OUTPUT_SR=24000
  - input.wav: 1 channel, 2-byte samples, INPUT_SR_TARGET=16000
  - events.jsonl: line 1 is `session_start` with `t=0.0, kind="session_start", wall_clock_iso, wall_clock_unix, session_dir`
  - log_event(kind, **fields) → JSONL record `{t: round(time.time() - start_time, 3), kind, **fields}` + flush
  - All writers wrap in `try/except Exception: pass` — recording is best-effort (v4:807-810 etc.)
  - close() closes all three handles

Free-function shape in features.py (RESEARCH.md A1):
```python
def snapshot_features(buf: AudioBuffer, seconds: float = 5.0) -> dict:
    n = int(buf._sr * seconds)
    arr_int16 = buf.snapshot(n)
    arr = arr_int16.astype(np.float32) / 32768.0
    sr = buf._sr
    # ... port v4:335-379 verbatim from here
```
Free functions take `(buf: AudioBuffer, ...)` so tests can build a tiny AudioBuffer with synthetic input and assert the dict shape without standing up the whole audio package.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Port DSP math to features.py — verbatim from cohost_v4.py:304-436</name>
  <files>src/vibemix/audio/features.py</files>
  <action>Create `src/vibemix/audio/features.py` with SPDX-License-Identifier (Apache-2.0) header, future annotations, `import io`, `import wave`, `import numpy as np`, imports from `vibemix.audio.buffers` (`AudioBuffer`) and `vibemix.audio.constants` (`SILENT_RMS`). Module docstring: "DSP free functions over AudioBuffer. Verbatim math port of cohost_v4.py:304-436 — the FFT pipeline, BPM autocorr, peak-normalize, and energy-curve helpers. Refactored from v4's `AudioBuffer` methods into free functions so tests can build a tiny synthetic buffer and pin the dict shape without standing up the whole audio package (RESEARCH.md A1)." Implement: `snapshot_features(buf: AudioBuffer, seconds: float = 5.0) -> dict` — read `n = int(buf._sr * seconds)`, `arr_int16 = buf.snapshot(n)`, early-out `if arr_int16.size < buf._sr // 4: return {"silent": True, "rms": 0.0}` (matches v4:335-336), `arr = arr_int16.astype(np.float32) / 32768.0`, then port v4:338-369 verbatim — RMS, 1/50-sec-window onset detection with `thr = max(0.005, deltas.mean() + deltas.std())`, 16384-sample Hanning-windowed rfft, five band-energy slices via `band_energy(lo, hi)` closure (use the exact frequency edges from v4:364-368: sub 20-100, low 100-300, mid_low 300-1000, mid_hi 1000-4000, high 4000-8000), total + 1e-9 denom. Return dict EXACTLY shaped per v4:371-379: `{"silent": rms < SILENT_RMS, "rms": round(rms, 4), "onsets_per_sec": round(onsets_per_sec, 1), "sub_share": round(sub/total, 2), "low_share": round(low/total, 2), "mid_share": round((mid_low+mid_hi)/total, 2), "high_share": round(high/total, 2)}`. Implement `snapshot_wav(buf: AudioBuffer, seconds: float, normalize_peak_dbfs: float | None = -3.0) -> bytes` — `n = int(buf._sr * seconds)`, `pcm = buf.snapshot(n)`. Peak-normalize block must port v4:313-321 VERBATIM: `peak = int(np.abs(pcm).max())`; if peak > 0, `target = int(32767 * (10 ** (normalize_peak_dbfs / 20.0)))`, `scale = target / peak`; if scale > 1.0: `pcm = np.clip(pcm.astype(np.float32) * scale, -32768, 32767).astype(np.int16)` (CLIP BEFORE CAST — RESEARCH.md Pitfall 4); elif scale < 1.0: `pcm = (pcm.astype(np.float32) * scale).astype(np.int16)`. Then `bio = io.BytesIO()`, `wave.open(bio, "wb")` with setnchannels(1) / setsampwidth(2) / setframerate(buf._sr) / writeframes(pcm.tobytes()), return bio.getvalue(). Implement `energy_curve(buf: AudioBuffer, seconds: float = 12.0, hop: float = 1.0) -> list[float]` — verbatim port of v4:381-392 over `buf.snapshot(int(buf._sr * seconds))` divided by 32768 to float. Implement `long_arc_curve(buf: AudioBuffer, seconds: float = 120.0, hop: float = 10.0) -> list[float]` — verbatim port of v4:394-408. Implement `estimate_bpm(buf: AudioBuffer, seconds: float = 6.0) -> float` — verbatim port of v4:410-436: 6s window, 1/100-sec envelope frames, mean-subtract, full np.correlate, positive-lag slice `ac[ac.size // 2:]`, lo_lag=30 / hi_lag=60, return `round(60.0 * 100.0 / best_lag, 1)` or 0.0 on insufficient data. Add a focused docstring on each function pinning its v4 source line range. Run ruff check + format.</action>
  <verify>
    <automated>uv run python -c "
import numpy as np
from vibemix.audio import AudioBuffer
from vibemix.audio.features import snapshot_features, snapshot_wav, estimate_bpm, energy_curve, long_arc_curve

buf = AudioBuffer(seconds=10.0, sr=16000)
# Empty buffer → silent early-out (filled < sr/4 = 4000 samples)
buf.push(np.zeros(100, dtype=np.int16))
f = snapshot_features(buf, seconds=5.0)
assert set(f.keys()) == {'silent', 'rms'} and f['silent'] is True

# Push 5s of pseudo-music — should hit the full dict path
buf.push((np.random.randn(80000) * 8000).astype(np.int16))
f = snapshot_features(buf, seconds=5.0)
assert set(f.keys()) == {'silent', 'rms', 'onsets_per_sec', 'sub_share', 'low_share', 'mid_share', 'high_share'}, f

# snapshot_wav RIFF check
wav = snapshot_wav(buf, seconds=2.0)
assert wav[:4] == b'RIFF' and wav[8:12] == b'WAVE'

# Peak-normalize doesn't produce -32768
buf2 = AudioBuffer(seconds=1.0, sr=16000)
buf2.push(np.full(16000, 32767, dtype=np.int16))  # full-scale
wav2 = snapshot_wav(buf2, seconds=1.0, normalize_peak_dbfs=-3.0)
pcm = np.frombuffer(wav2[44:], dtype=np.int16)
assert pcm.min() > -32768, 'peak-normalize int16 overflow'

# energy_curve returns list of floats
ec = energy_curve(buf, seconds=8.0)
assert isinstance(ec, list) and all(isinstance(x, float) for x in ec)
"</automated>
  </verify>
  <done>features.py exposes the 5 free functions; smoke roundtrip confirms dict shape parity with v4, peak-normalize correctness, and RIFF header validity; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 3.2: Port VoiceRecorder + write feature/recorder tests + update __init__.py</name>
  <files>src/vibemix/audio/recorder.py, src/vibemix/audio/__init__.py, tests/audio/test_features.py, tests/audio/test_recorder.py</files>
  <action>Create `src/vibemix/audio/recorder.py` with SPDX header, future annotations, imports `import json`, `import threading`, `import time`, `import wave`, `from datetime import datetime`, `from pathlib import Path`, `from vibemix.audio.constants import INPUT_SR_TARGET, OUTPUT_SR`. Module docstring: "Per-session WAV/JSONL recorder. Lifted from cohost_v4.py:769-848 with two improvements: (1) configurable recording root (constructor `root: Path | None`) — fixes the v4:771 `Path(__file__).parent` anti-pattern that would write WAVs into site-packages on a packaged install; (2) session dir created with mode=0o700 (RESEARCH.md Security V8 — recordings contain Kaan's voice, matches the HARD privacy rule in ~/CLAUDE.md)." Implement `class VoiceRecorder`: `__init__(self, root: Path | None = None) -> None` — `rec_dir = root if root is not None else Path.cwd() / "recordings"`, then `rec_dir.mkdir(parents=True, exist_ok=True, mode=0o700)`. Compute `ts = datetime.now().strftime("%Y%m%d-%H%M%S")`. `self.session_dir = rec_dir / ts`. `self.session_dir.mkdir(mode=0o700)`. Defensively `os.chmod(self.session_dir, 0o700)` (some platforms ignore the mkdir `mode` umask — `import os` at top). `self.start_time = time.time()`. Open `voice.wav` via `wave.open` with channels=1, sampwidth=2, framerate=`OUTPUT_SR`. Open `input.wav` via `wave.open` with channels=1, sampwidth=2, framerate=`INPUT_SR_TARGET`. Open `events.jsonl` for append. `self._lock = threading.Lock()`. Write initial `session_start` event verbatim from v4:792-799 (use `datetime.now().astimezone()` for `wall_start`, fields `t=0.0`, `kind="session_start"`, `wall_clock_iso=wall_start.isoformat(timespec="milliseconds")`, `wall_clock_unix=round(wall_start.timestamp(), 3)`, `session_dir=str(self.session_dir.name)`). Then methods: `push_voice(pcm_bytes: bytes) -> None` (verbatim v4:803-810 with try/except: pass), `push_input(pcm_bytes: bytes) -> None` (verbatim v4:812-819), `_write_event_locked(rec: dict) -> None` (verbatim v4:821-827 — `json.dump(rec, self.events_f, ensure_ascii=False); self.events_f.write("\\n"); self.events_f.flush()` under try/except: pass), `log_event(kind: str, **fields) -> None` (verbatim v4:829-833 — compute `rel = time.time() - self.start_time`, build `{"t": round(rel, 3), "kind": kind, **fields}`, call `_write_event_locked` under `self._lock`), `close() -> None` (verbatim v4:835-848 — close all three handles under try/except). Keep the `print(f"-> recording session → {self.session_dir.name}/  (voice.wav + input.wav + events.jsonl)")` startup line from v4:801. Update `src/vibemix/audio/__init__.py` to add `from .features import snapshot_features, snapshot_wav, energy_curve, long_arc_curve, estimate_bpm` and `from .recorder import VoiceRecorder`, extending `__all__` accordingly. Create `tests/audio/test_features.py` with at least these cases (each docstring pins the v4 source line): (FEAT-01) `test_snapshot_features_empty_buffer_silent_early_out` — fresh AudioBuffer, push < sr/4 samples, snapshot_features returns exactly `{"silent": True, "rms": 0.0}` (v4:335-336). (FEAT-02) `test_snapshot_features_full_dict_shape` — push ≥ sr/4 samples of pseudo-music, returned dict has exactly the 7 v4 keys (v4:371-379). (FEAT-03) `test_snapshot_features_silent_flag_matches_silent_rms_constant` — push very-low-amplitude noise (RMS < SILENT_RMS), assert `f["silent"] is True`. (FEAT-04) `test_snapshot_wav_riff_header_valid` — RIFF + WAVE chunk IDs at offsets 0 + 8 (matches the standard 44-byte header). (FEAT-05) `test_snapshot_wav_peak_normalize_no_int16_overflow` — push full-scale `np.full(16000, 32767, dtype=np.int16)`, call snapshot_wav with normalize_peak_dbfs=-3.0, parse the PCM out of the RIFF body, assert min > -32768 (RESEARCH.md Pitfall 4 regression). (FEAT-06) `test_snapshot_wav_normalize_disabled` — pass `normalize_peak_dbfs=None`, assert the output PCM equals the input bytes exactly. (FEAT-07) `test_estimate_bpm_returns_float_in_range` — push 6s of a synthetic ~125 BPM pulse train (use the `int16_sine` helper from conftest at a low frequency modulated at 125 BPM), assert the returned BPM is a float, ≥0.0, ≤200.0. Loose tolerance — the v4 autocorr is not high-precision; the test pins ALIVENESS not accuracy. (FEAT-08) `test_estimate_bpm_short_buffer_returns_zero` — push <2s of audio, assert returns 0.0 (v4:413-415). (FEAT-09) `test_energy_curve_length_matches_seconds_over_hop` — 12s of data with hop=1.0 should yield 12 values. (FEAT-10) `test_long_arc_curve_length_matches_120s_over_10s_hop` — push 120s, hop=10.0, yields 12 values. Create `tests/audio/test_recorder.py` with at least: (REC-01) `test_voice_recorder_creates_session_dir_with_0700_perms` — instantiate VoiceRecorder with a `tmp_path` fixture root, assert the per-session subdir exists with `mode & 0o777 == 0o700` (use `tmp_path.iterdir()` to find the timestamped subdir). (REC-02) `test_voice_recorder_writes_input_wav_with_correct_header` — push some input PCM, close, open `input.wav` with `wave.open`, assert nchannels=1, sampwidth=2, framerate=INPUT_SR_TARGET (16000). (REC-03) `test_voice_recorder_writes_voice_wav_with_24khz_framerate` — same idea for voice.wav (OUTPUT_SR=24000). (REC-04) `test_events_jsonl_first_line_is_session_start` — open events.jsonl, parse line 1 as JSON, assert keys include `t=0.0, kind="session_start", wall_clock_iso, wall_clock_unix, session_dir`. (REC-05) `test_log_event_appends_jsonl_with_relative_t` — call log_event("trigger", reason="test"), parse the appended line, assert `kind == "trigger"`, `reason == "test"`, `t` is a float ≥ 0.0. (REC-06) `test_voice_recorder_configurable_root_writes_to_custom_dir` — call `VoiceRecorder(root=tmp_path / "custom")`, assert recordings land in `tmp_path/custom/<timestamp>/`. Use pytest `tmp_path` fixture throughout — never write to the project's real `recordings/` dir. Run `uv run pytest tests/audio/ -x -q` — full audio suite (Plans 01+02+03 tests) all green. Run ruff check + format.</action>
  <verify>
    <automated>uv run pytest tests/audio/ -x -q && uv run ruff check src/vibemix/audio tests/audio && uv run ruff format --check src/vibemix/audio tests/audio</automated>
  </verify>
  <done>features.py + recorder.py + 2 test files; 16+ test cases across the new files; full audio suite green; __init__.py re-exports the 6 new public names; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio thread → recorder | sounddevice callback writes via `push_input` / `push_voice` — wraps in try/except (best-effort, never blocks the live pipeline) |
| filesystem → user voice data | recordings/ holds Kaan's voice + room speech — privacy-sensitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Information Disclosure | recordings dir world-readable | mitigate | session dir created with `mode=0o700` + defensive `os.chmod` (some platforms ignore mkdir mode under umask). RESEARCH.md Security V8. |
| T-02-03-02 | Tampering | Path traversal in recording filename | mitigate | Timestamp generated internally via `datetime.now().strftime` — no caller-supplied filename input. RESEARCH.md V12. |
| T-02-03-03 | Information Disclosure | int16 overflow on peak-normalize → silent corruption | mitigate | FEAT-05 test pins the clip-before-cast invariant (RESEARCH.md Pitfall 4); v4:319 order preserved verbatim. |
| T-02-03-04 | Tampering | snapshot_features dict shape drift | mitigate | FEAT-02 + FEAT-01 assert the exact 7-key vs 2-key contracts. Phase 3 EventDetector consumes this shape. |
| T-02-03-05 | Denial of Service | Recorder.push_input on dead WAV handle blocks audio thread | accept | All writers wrapped in `try/except Exception: pass` per v4:807-810 — recording is best-effort. Failure mode is silent dropped frame, not stall. |
</threat_model>

<verification>
- `uv run pytest tests/audio/ -x -q` — Plans 01+02+03 tests all green (≥28 cases total).
- `uv run ruff check src/vibemix/audio tests/audio` and `uv run ruff format --check ...` exit 0.
- `uv run python -c 'from vibemix.audio import snapshot_features, snapshot_wav, estimate_bpm, VoiceRecorder, AudioBuffer'` succeeds.
- POC files untouched.
</verification>

<success_criteria>
features.py + recorder.py complete the domain-side audio package. Plan 04 can now build `AudioMacOS` on top of (a) the constants from Plan 01, (b) the buffers + Levels + BufferRegistry from Plans 01-02, and (c) the VoiceRecorder from this wave for the input-callback recording side-effect.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-core-port-ring-buffer-fix/02-03-SUMMARY.md`. Commit as `feat(02): wave 3 — features.py (DSP math) + VoiceRecorder (0o700 sessions + configurable root)`.
</output>
