---
phase: 02-audio-core-port-ring-buffer-fix
plan: 05
type: execute
wave: 5
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - ARCH-07
  - AUDIO-01
  - AUDIO-04
  - AUDIO-06
  - AUDIO-07
  - AUDIO-08
  - AUDIO-09
must_haves:
  truths:
    - "ALL Phase 2 acceptance criteria from the user prompt pass: zero `np.concatenate` in src/vibemix/audio (grep clean); tracemalloc zero-alloc test green; 14 tuning constants match v4 exactly; AudioMacOS satisfies AudioBackend isinstance; SampleRateMismatchError raises with actionable message on mismatch; all Phase 1 tests still green; 4+ atomic feat(02) commits exist; POC files diff-untouched."
    - "`uv run ruff check src/ tests/` AND `uv run ruff format --check src/ tests/` both exit 0."
    - "`uv run pytest -x -q --ignore=tests/test_audio_macos_live.py` is all green (Phase 1 + Phase 2 tests combined)."
    - "Live opt-in smoke test on Kaan's machine: `uv run pytest -m macos_audio -v` either passes (BlackHole at 48kHz) OR fails with the actionable SampleRateMismatchError message (BlackHole at wrong rate) — never an unhandled stack trace."
    - "Phase summary file `02-SUMMARY.md` exists at the phase root, follows the Phase 1 SUMMARY structure (Requirements Coverage table + Files + Architectural Decisions + Deviations + Dependent Phases Unlocked + Open Items + Verification Snapshot + Commit History)."
    - "ROADMAP.md Phase 2 row updated: status box checked OR phase marked complete in the standard convention; Phase 2 plan list shows all 5 plans by filename."
    - ".planning/STATE.md Current Position advanced to Phase 3."
  artifacts:
    - path: ".planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md"
      provides: "Phase-level summary — follows Phase 1's structure (see .planning/phases/01-platform-protocol-firewall/01-SUMMARY.md as template). Includes verification snapshot table covering all 8 acceptance criteria from the user prompt."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 row marked complete; plan list expanded to show 5 plans by filename"
    - path: ".planning/STATE.md"
      provides: "Current Position updated to Phase 3 (Sensing & State Port)"
  key_links:
    - from: ".planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md"
      to: "all 4 wave SUMMARYs (02-01-SUMMARY.md through 02-04-SUMMARY.md)"
      via: "phase-level rollup"
      pattern: "Wave 1 commit"
---

<objective>
Verification gate + phase rollup. This wave runs the full acceptance-criteria checklist from the user prompt, executes the live BlackHole smoke test on Kaan's machine, writes the phase-level SUMMARY.md, and advances ROADMAP.md + STATE.md. This is a checkpoint plan — autonomous gate execution PLUS one human-verify checkpoint at the end where Kaan confirms the audio actually works against BlackHole.

Purpose:
- Run the 8 acceptance criteria from the user prompt as a hard checklist; any failure → STOP, file gap, do not write SUMMARY.
- Execute the live smoke test on Kaan's hardware (BlackHole + headphones present) to validate the sample-rate guard against the real driver state — mocked tests pass on CI but the empirical 2026-05-11 bug was a CI-invisible misconfiguration.
- Roll up the four wave SUMMARYs into a single phase SUMMARY following Phase 1's structure.
- Advance the planning surface: ROADMAP.md Phase 2 row, STATE.md current position.

Output:
- `.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md` — phase rollup
- `.planning/ROADMAP.md` — Phase 2 marked complete + plan list expanded
- `.planning/STATE.md` — current position bumped to Phase 3
- Final commit: `docs(02): phase summary + roadmap/state update`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-CONTEXT.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-RESEARCH.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-PATTERNS.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-01-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-02-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-03-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-04-PLAN.md
@.planning/phases/01-platform-protocol-firewall/01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Run the 8 acceptance-criteria gate + draft 02-SUMMARY.md</name>
  <files>.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md</files>
  <action>Run each of the 8 acceptance criteria from the user prompt as a literal shell command and record pass/fail. (G1) `test -z "$(grep -rE 'np\\.concatenate' src/vibemix/audio/ 2>/dev/null)"` — zero matches → ✓. (G2) `uv run pytest tests/audio/test_buffers.py::test_audio_buffer_push_zero_alloc_tracemalloc -v` (or the exact name written in Plan 02) — pass → ✓. (G3) `uv run pytest tests/audio/test_constants.py -v` confirms all 14 constants match v4. (G4) `uv run python -c 'from vibemix.platform import AudioBackend, AudioMacOS; from vibemix.audio import BufferRegistry, AudioBuffer, MicBuffer, PassthroughBuffer, PlaybackQueue, Levels, VoiceRecorder; import tempfile, pathlib; l=Levels(); reg=BufferRegistry(audio=AudioBuffer(seconds=1), clean_audio=AudioBuffer(seconds=1), mic=MicBuffer(gain=1.0, levels=l), passthrough=PassthroughBuffer(), playback=PlaybackQueue(l), levels=l); rec=VoiceRecorder(root=pathlib.Path(tempfile.mkdtemp())); assert isinstance(AudioMacOS(registry=reg, recorder=rec), AudioBackend)'`. (G5) `uv run pytest tests/test_audio_macos.py::test_assert_device_sample_rate_raises_on_mismatch -v` (or exact name). (G6) `uv run pytest tests/test_platform.py tests/test_package.py tests/test_license.py tests/test_signpath_checklist.py -x -q` — all Phase 1 tests still green. (G7) `git log --oneline main..HEAD | grep -E '^[a-f0-9]{7,} feat\\(02\\)'` — confirm ≥4 atomic `feat(02)` commits (one per wave 1-4) plus the final docs commit. (G8) POC diff check: `git diff --name-only main..HEAD | grep -E '^(cohost[^/]*\\.py|run[^/]*\\.sh|mascot\\.html|sprite-.*\\.png|generate_bat\\.py|_test_.*\\.py|test_voice\\.py|fillers/|cohost\\.streaming\\.py\\.bak)$'` — must return empty. Record each gate's result. If ANY gate fails: STOP, do NOT write the SUMMARY, do NOT advance ROADMAP/STATE. Instead, return a `## ⚠ Source Audit: Gap Found` block to the orchestrator describing which gate failed and what's needed. If all 8 gates pass, continue. Draft `.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md` modeled on `.planning/phases/01-platform-protocol-firewall/01-SUMMARY.md`. Frontmatter: `phase: 02-audio-core-port-ring-buffer-fix`, `plan: rollup`, `type: summary`, `status: complete`, `completed_at: <today's date>`, `requirements_covered: [ARCH-07, AUDIO-01, AUDIO-04, AUDIO-06, AUDIO-07, AUDIO-08, AUDIO-09]`, `wave_commits: [<sha1>, <sha2>, <sha3>, <sha4>]` (extract from `git log --oneline main..HEAD` filtering for `feat(02): wave N` lines). Body sections (mirror Phase 1's structure): **What Phase 2 Delivered** (3-5 sentences: pre-allocated rings fix the np.concatenate-per-callback bug at v4:300+v4:462; macOS AudioBackend impl with non-negotiable sample-rate guard against the BlackHole 44100/48000 trap that bit Kaan on 2026-05-11; VoiceRecorder with 0o700 perms; the 14 LOAD-BEARING v4-tuned constants moved to importable module scope), **Requirements Coverage** table (ARCH-07 / AUDIO-01 / AUDIO-04 / AUDIO-06 / AUDIO-07 / AUDIO-08 / AUDIO-09 each with a "How Phase 2 satisfied it" cell — note AUDIO-02/03/05 are deferred to Phase 7/11), **Files** (Created list + Modified list + "POC files touched: 0"), **Architectural Decisions Locked** table (ring buffer storage = pre-allocated np.ndarray + write-pointer + threading.Lock; 14 constants live in vibemix.audio.constants; sample-rate guard is pre-open AND post-open; VoiceRecorder root is configurable + 0o700 perms; BufferRegistry as frozen dataclass; features.py separates DSP from buffer storage; AudioMacOS via composition not inheritance — runtime_checkable Protocol), **Deviations from Plan** (any small drifts — e.g., if any wave's task got combined or split; if Phase 1's Protocol needed an amendment for `open_mic_capture`), **Dependent Phases Unlocked** table (Phase 3 imports `vibemix.audio.snapshot_features` / `Levels.snapshot` / `BufferRegistry` for state_refresh_loop; Phase 4 imports `vibemix.audio.PlaybackQueue` / `MicBuffer` for LiveKit AgentSession wiring; Phase 6 imports `vibemix.audio.constants` for genre-aware overrides; Phase 7 reuses `BufferRegistry` + `VoiceRecorder` + the buffer/features modules verbatim for `_audio_windows.py`; Phase 11 catches `SampleRateMismatchError` in the calibration wizard; Phase 15 wraps `VoiceRecorder` with retention + browser UI), **Open Items Carried Forward** (BlackHole programmatic-config still Phase 11; AUDIO-02/03/05 — Windows + auto-detect + output destination picker — Phase 7 + Phase 11/12; the inline mic stream's mic.pull(len(music48)) cadence-discard pattern from v4:918 is still present in the v4 reference but Phase 2 ships it as an option, not the default — Phase 4 will resurrect mic-into-AgentSession when needed), **Verification Snapshot** (the 8-gate table populated with ✓/✗ for each gate from above + commands used), **Commit History** (the git log --oneline main..HEAD output trimmed to Phase 2 commits). Do not commit yet — Task 5.2 handles the verification checkpoint and Task 5.3 the final commit together.</action>
  <verify>
    <automated>test -f .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md && grep -q "Requirements Coverage" .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md && grep -q "Verification Snapshot" .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md && grep -q "Dependent Phases Unlocked" .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md && grep -cE "feat\(02\)" .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md</automated>
  </verify>
  <done>All 8 gates pass; 02-SUMMARY.md drafted following Phase 1 structure with frontmatter + Requirements Coverage + Files + Decisions + Verification Snapshot + Commit History sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.2: Kaan-verify live BlackHole audio + approve before ROADMAP/STATE advance</name>
  <files>(no files modified by Claude — Kaan-only verification step against live BlackHole hardware; reference artifacts: .planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md)</files>
  <action>
Pause for Kaan to run ONE live-hardware smoke test that mocked CI cannot cover, plus a quick SUMMARY proofread. Surface the verification steps below and wait for the resume signal before letting Task 5.3 advance ROADMAP + STATE + commit.

Do NOT skip the checkpoint. Do NOT advance ROADMAP/STATE or commit before Kaan types "approved". If Kaan describes issues instead, STOP and return the issue to the orchestrator for a gap-closure plan.
  </action>
  <what-built>
Phase 2 is code-complete: pre-allocated ring buffers replace the np.concatenate regression; AudioMacOS satisfies the Phase 1 Protocol; the sample-rate sanity guard catches the 2026-05-11 BlackHole bug pre-open AND post-open; VoiceRecorder writes to 0o700 sessions. All 8 acceptance gates passed in Task 5.1. The phase SUMMARY is drafted but uncommitted, and ROADMAP.md + STATE.md still show Phase 2 as in-progress. This checkpoint is Kaan running ONE live-hardware smoke test against the actual BlackHole driver state and either approving or asking for changes. After approval, Task 5.3 advances ROADMAP + STATE and creates the final commit.
  </what-built>
  <how-to-verify>
    1. **Live BlackHole smoke test (the bug Phase 2 exists to prevent):**
       Run `uv run pytest -m macos_audio -v`.
       - If BlackHole 2ch is configured at 48kHz (the correct setting) → test_blackhole_device_present_at_48khz_or_raises PASSES.
       - If BlackHole is at 44.1kHz (the bug Kaan hit on 2026-05-11) → the test FAILS with `SampleRateMismatchError` AND the error message includes the Audio MIDI Setup fix steps + Drift Correction note.
       Either outcome is correct behavior — what we're verifying is the guard FIRES with an actionable message, not that BlackHole is correctly configured right now.
       If the test stack-traces with an unhandled exception (not SampleRateMismatchError), Plan 04 has a bug — REJECT this checkpoint and the orchestrator routes to a gap-closure plan.
    2. **Manual sanity (optional but recommended):**
       From the project root, `uv run python -c "from vibemix.platform import AudioMacOS; from vibemix.audio import *; import pathlib, tempfile; l=Levels(); reg=BufferRegistry(audio=AudioBuffer(seconds=10), clean_audio=AudioBuffer(seconds=10), mic=MicBuffer(gain=1.0, levels=l), passthrough=PassthroughBuffer(), playback=PlaybackQueue(l), levels=l); rec=VoiceRecorder(root=pathlib.Path(tempfile.mkdtemp())); be=AudioMacOS(registry=reg, recorder=rec); idx=be.find_device('BlackHole', 'input'); print('BlackHole input @ index', idx)"`. Should print the BlackHole device index (probably 2). If RuntimeError fires, BlackHole isn't installed — orthogonal to Phase 2, but report it.
    3. **Read the SUMMARY** at `.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md`. Look for: every wave commit referenced; AUDIO-* requirements mapped to actual artifacts (not vague "see code"); the 8-gate verification snapshot showing ✓ on every gate; dependent phases section listing the Phase 3/4/6/7/11/15 import handoffs.
    4. **Confirm POC files untouched:** `git diff --name-only main..HEAD -- 'cohost*.py' 'run*.sh' mascot.html 'sprite-*.png' generate_bat.py '_test_*.py' test_voice.py fillers/ cohost.streaming.py.bak` returns empty.

    If all four checks read clean, type `approved`. If anything is off, describe what.
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a checkpoint:human-verify task; the verification IS the four manual steps in &lt;how-to-verify&gt; above. Resumption is gated on Kaan typing "approved".</automated>
  </verify>
  <done>Kaan typed `approved` (or equivalent). Step 1 live test either passed or raised SampleRateMismatchError with the actionable message (never an unhandled stack trace). SUMMARY draft is clean and POC files diff-untouched.</done>
  <resume-signal>Type "approved" once Steps 1-4 are complete, or describe what blocked.</resume-signal>
</task>

<task type="auto">
  <name>Task 5.3: After Kaan approval — update ROADMAP.md + STATE.md + final commit</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>This task ONLY runs after Task 5.2 returns "approved". Update `.planning/ROADMAP.md`: locate the `### Phase 2: Audio Core Port + Ring Buffer Fix` section and change the phase-list checkbox at the top from `- [ ] **Phase 2:** ...` to `- [x] **Phase 2:** Audio Core Port + Ring Buffer Fix - ...` (mirroring Phase 1's checked format including a trailing `_**Complete YYYY-MM-DD.**_` italic note). In the Phase 2 detail section, change the `**Plans**: TBD` line to a full list mirroring Phase 1's format: `**Plans:** 5 plans` followed by a `Plans:` block listing all 5 plan filenames with their objectives — `02-01-PLAN.md — Audio package skeleton + constants + Levels + errors + pytest-mock dev dep`, `02-02-PLAN.md — Pre-allocated ring buffers (fixes np.concatenate at v4:300 + v4:462) + BufferRegistry`, `02-03-PLAN.md — features.py DSP math + VoiceRecorder (0o700 sessions + configurable root)`, `02-04-PLAN.md — AudioMacOS impl + sample-rate sanity guard (pre + post-open)`, `02-05-PLAN.md — Verification gate + phase SUMMARY + roadmap/state advance`. Each checkbox state `[x]` (all 5 complete). Update `.planning/STATE.md`: change `**Phase:**` from `02 — Audio Core Port + Ring Buffer Fix (next).` to `03 — Sensing & State Port (next).`. Update `**Status:**` to mention Phase 2 ✅ complete + cite the merge commit (read from `git log --oneline -1`). Bump `**Progress:**` to `2/20 phases complete` and update the ASCII progress bar from `[█                   ] 5%` to `[██                  ] 10% (2/20 phases)`. Update the `Phases complete` row in the metrics table to `2 / 20`. Add a new top-level dated entry at the top of any "Recent activity" or "Decisions / Notes" section reading "2026-XX-XX: Phase 2 shipped. Ring-buffer regression (v4:300+v4:462) closed. Sample-rate guard active. Phase 3 (Sensing & State Port) is next." (use the actual current date). Stage the SUMMARY from Task 5.1 + the ROADMAP.md + STATE.md changes. Final commit via a HEREDOC commit message: `git commit -m "$(cat <<'EOF'` then `docs(02): phase 2 complete — audio core port + ring buffer fix` blank line, bulleted body (Pre-allocated rings fix np.concatenate-per-callback regression (v4:300, v4:462); AudioMacOS satisfies Phase 1 AudioBackend Protocol; Sample-rate sanity guard catches BlackHole 44100/48000 trap (pre + post-open); VoiceRecorder writes 0o700 sessions (Kaan voice = privacy-sensitive); 14 LOAD-BEARING v4-tuned constants importable at vibemix.audio.constants; POC files untouched), blank line, `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`, then `EOF` `)"`. Run `git status` after commit to confirm clean working tree (only untracked POC noise + `.venv/` permitted). Print the final summary including the merge commit SHA.</action>
  <verify>
    <automated>grep -q "^- \[x\] \*\*Phase 2:" .planning/ROADMAP.md && grep -q "02-01-PLAN.md" .planning/ROADMAP.md && grep -q "02-05-PLAN.md" .planning/ROADMAP.md && grep -q "Phase:.*03 — Sensing" .planning/STATE.md && grep -q "Progress.*2.*20" .planning/STATE.md && git log --oneline -5 | grep -q "docs(02): phase 2 complete"</automated>
  </verify>
  <done>ROADMAP.md Phase 2 row marked complete with all 5 plans listed; STATE.md advanced to Phase 3 with progress bumped to 2/20; final docs commit landed; working tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live hardware → automated test | macos_audio-marked tests read REAL Audio MIDI Setup state via sd.query_devices; outcome depends on Kaan's local config |
| Drafted SUMMARY → committed planning record | If any gate failed silently and a misleading SUMMARY landed, future phases plan on broken foundations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | SUMMARY claims pass when a gate failed | mitigate | Task 5.1 explicitly enumerates 8 gates and STOPs on any failure before writing SUMMARY. The human-verify checkpoint in 5.2 is the second-line defense. |
| T-02-05-02 | Information Disclosure | Phase 2 SUMMARY exposes recordings/ contents | accept | SUMMARY only references paths and structure, not actual recorded content. No bytes from input.wav/voice.wav included. |
| T-02-05-03 | DoS | Live BlackHole test stack-traces unhandled | mitigate | Task 5.2 explicitly distinguishes "SampleRateMismatchError = correct guard fired" from "unhandled stack trace = bug in Plan 04" — the latter rejects the checkpoint. |
</threat_model>

<verification>
- All 8 acceptance gates from the user prompt pass (Task 5.1).
- `uv run pytest -m macos_audio -v` either passes (BlackHole 48kHz) OR fails with SampleRateMismatchError carrying the actionable message (BlackHole misconfigured) — never an unhandled exception.
- `.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md` exists with the Phase 1 SUMMARY structure (Requirements Coverage + Files + Decisions + Deviations + Dependent Phases + Open Items + Verification Snapshot + Commit History).
- `.planning/ROADMAP.md` shows Phase 2 checked off with all 5 plans listed.
- `.planning/STATE.md` shows Phase 3 as the current position with 2/20 progress.
- Final `docs(02):` commit lands cleanly. `git log --oneline main..HEAD` shows 5+ commits (4 feat + 1 docs).
- `git diff --name-only main..HEAD -- 'cohost*.py' 'run*.sh' mascot.html 'sprite-*.png' generate_bat.py '_test_*.py' test_voice.py fillers/ cohost.streaming.py.bak` returns empty — POC immortality preserved.
</verification>

<success_criteria>
Phase 2 closes with a verified pass on all 8 acceptance gates, a human-approved live-hardware smoke confirmation, a phase-level SUMMARY following the Phase 1 template, ROADMAP/STATE advanced to Phase 3, and 4 atomic feat(02) commits + 1 docs commit on the main branch. Phase 3 (Sensing & State Port) can start by importing `vibemix.audio.{AudioBuffer, snapshot_features, BufferRegistry, Levels, constants}` directly — no scavenger hunt.
</success_criteria>

<output>
After Task 5.3 commit, no further per-wave SUMMARY needed (this wave IS the phase rollup). Print to the user the 5-line phase summary: feat commits SHAs, plan count, test count delta, BlackHole live-smoke verdict, ROADMAP/STATE position. Orchestrator picks up "Phase 3" as next.
</output>
