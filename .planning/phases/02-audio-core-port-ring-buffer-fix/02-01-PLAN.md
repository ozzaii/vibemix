---
phase: 02-audio-core-port-ring-buffer-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/vibemix/audio/__init__.py
  - src/vibemix/audio/constants.py
  - src/vibemix/audio/levels.py
  - src/vibemix/audio/errors.py
  - tests/audio/__init__.py
  - tests/audio/conftest.py
  - tests/audio/test_constants.py
  - tests/audio/test_levels.py
autonomous: true
requirements:
  - ARCH-07
  - AUDIO-07
must_haves:
  truths:
    - "`uv run python -c 'from vibemix.audio import Levels, SILENT_RMS, LOW_RMS, PEAK_RMS, MIN_EVENT_GAP_PER_TYPE'` succeeds with zero OS-specific module side effects (no sounddevice/Quartz/mss loaded by the import)."
    - "Every one of the 14 tuning constants listed in 02-PATTERNS.md (8 module-level + dict + 3 lifted-from-EventDetector + INVOKE_AUDIO_SECONDS for Phase 4 consumption) is importable from `vibemix.audio.constants` with values matching `cohost_v4.py` exactly."
    - "`Levels.update_music(int16_array)`, `update_voice(bytes)`, `update_mic(float32_array)`, `decay_voice()`, `snapshot()` all behave identically to v4:255-286 (verified by a side-by-side numeric test against the v4 source)."
    - "`uv add --dev pytest-mock` succeeded — pytest-mock appears in `[dependency-groups].dev` of `pyproject.toml` and `uv.lock` updated."
    - "`uv run ruff check src/vibemix/audio tests/audio` and `uv run ruff format --check src/vibemix/audio tests/audio` exit 0."
    - "`uv run pytest tests/audio/test_constants.py tests/audio/test_levels.py -x -q` passes (≥6 new test cases green)."
    - "POC files (cohost_v4.py, cohost_v3.py, cohost_v2.py, cohost_lk.py, cohost.py, cohost.streaming.py.bak, run*.sh, mascot.html, sprite-*.png, generate_bat.py, _test_*.py, test_voice.py, fillers/) untouched — `git diff --name-only` of this wave's commit excludes them."
  artifacts:
    - path: "src/vibemix/audio/__init__.py"
      provides: "Public re-exports for the audio package — Levels + every constant + SampleRateMismatchError (the typed errors module — buffer classes added in Plan 02)"
      exports: ["Levels", "SILENT_RMS", "LOW_RMS", "PEAK_RMS", "AUDIBLE_DEBOUNCE_SEC", "SILENCE_DEBOUNCE_SEC", "EVENT_GLOBAL_MIN_GAP", "HEARTBEAT_SEC", "MIN_EVENT_GAP_PER_TYPE", "TRACK_CHANGE_MIN_CONFIDENCE", "MUSIC_PRESENCE_MIN_SECONDS", "BPM_VALID_MIN", "BPM_VALID_MAX", "INVOKE_AUDIO_SECONDS", "INPUT_SR_NATIVE", "INPUT_SR_TARGET", "OUTPUT_SR", "INPUT_CHUNK_FRAMES", "OUTPUT_BLOCKSIZE", "VOICE_BLOCKSIZE", "PASSTHROUGH_GAIN", "MUSIC_GAIN_TO_GEMINI", "MIC_GAIN", "MIC_TALK_THRESHOLD", "MIC_GAIN_AT_AI_TALK", "MIC_HOLD_AFTER_AI_MS", "AI_TALK_THRESHOLD", "SampleRateMismatchError"]
    - path: "src/vibemix/audio/constants.py"
      provides: "All 14 LOAD-BEARING tuning constants lifted verbatim from cohost_v4.py — these are the v4 retuned French Touch / Daft Punk / Digitalism profile and must not be casually re-tuned without a new live-DJ-session validation"
      contains: "SILENT_RMS = 0.012"
    - path: "src/vibemix/audio/levels.py"
      provides: "Levels class — EMA RMS smoothing for music/voice/mic + threading.Lock — verbatim port of cohost_v4.py:255-286"
      contains: "class Levels"
    - path: "src/vibemix/audio/errors.py"
      provides: "Typed exceptions for the audio package — SampleRateMismatchError (consumed by Plan 04 macOS impl)"
      contains: "class SampleRateMismatchError"
    - path: "tests/audio/test_constants.py"
      provides: "Constant presence + value parity test — fails the build if any constant goes missing or drifts from v4"
    - path: "tests/audio/test_levels.py"
      provides: "EMA decay + RMS-from-int16 + lock-thread-safety tests for Levels"
    - path: "pyproject.toml"
      provides: "pytest-mock added to [dependency-groups].dev — only new dep this phase per RESEARCH.md Q3"
      contains: "pytest-mock"
  key_links:
    - from: "src/vibemix/audio/__init__.py"
      to: "src/vibemix/audio/constants.py + levels.py + errors.py"
      via: "wildcard re-exports"
      pattern: "from vibemix.audio.constants import"
    - from: "tests/audio/test_constants.py"
      to: "cohost_v4.py:127-143 (verbatim source)"
      via: "asserted equality"
      pattern: "assert SILENT_RMS == 0.012"
---

<objective>
Stand up the `src/vibemix/audio/` package skeleton with the LOAD-BEARING tuning constants, the verbatim `Levels` class, the typed exceptions module, and the only new dependency this phase needs (`pytest-mock` dev-only). Wave 1 has zero dependencies — it ships pure domain code with no sounddevice/CoreAudio surface, so Plans 02-04 can import constants and Levels freely.

Purpose: The 14 tuning constants (8 retuned in v4 for French Touch / Daft Punk / Digitalism, plus 3 lifted out of `EventDetector` class scope, plus the `MIN_EVENT_GAP_PER_TYPE` dict, plus `INVOKE_AUDIO_SECONDS` for Phase 4) are the engine's source of truth — Phase 3 EventDetector imports them, Phase 6 genre-aware override extends them. They MUST land before any code that consumes them.

Output:
- `src/vibemix/audio/{__init__.py, constants.py, levels.py, errors.py}` — first four files of the audio package
- `tests/audio/{__init__.py, conftest.py, test_constants.py, test_levels.py}` — test scaffolding for the whole phase
- `pyproject.toml` + `uv.lock` updated with `pytest-mock` dev dep
- `pyproject.toml` `[tool.pytest.ini_options]` gains the `markers = ["macos_audio: ..."]` registration so Plan 05's opt-in smoke test runs clean
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-CONTEXT.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-RESEARCH.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-PATTERNS.md
@.planning/phases/01-platform-protocol-firewall/01-SUMMARY.md
@cohost_v4.py
@pyproject.toml
@src/vibemix/__init__.py
@src/vibemix/platform/__init__.py

<interfaces>
<!-- Phase 1 contract — already shipped, do not modify. Plan 04 will satisfy this. -->

From src/vibemix/platform/audio.py (already committed):
```python
Kind = Literal["input", "output"]
AudioCallback = Callable[..., None]   # sounddevice-shaped: (indata, frames, time_info, status)

class AudioStream(Protocol):
    @property
    def latency_ms(self) -> float: ...
    def start(self) -> None: ...
    def stop(self) -> None: ...
    def close(self) -> None: ...

@runtime_checkable
class AudioBackend(Protocol):
    def find_device(self, name_substring: str, kind: Kind) -> int: ...
    def open_capture(self, device_index: int, *, sample_rate: int, channels: int,
                     block_size: int, callback: AudioCallback) -> AudioStream: ...
    def open_passthrough_output(self, device_index: int, *, sample_rate: int, channels: int,
                                block_size: int, callback: AudioCallback) -> AudioStream: ...
    def open_voice_output(self, device_index: int, *, sample_rate: int,
                          block_size: int, callback: AudioCallback) -> AudioStream: ...
```

From cohost_v4.py (the canonical port source — READ-ONLY reference):
- Tuning constants block: lines 96-143 (module-level) + lines 1176-1180 (class-attrs on EventDetector to lift OUT)
- `class Levels`: lines 255-286 (port verbatim — byte-identical between v3 and v4 per PATTERNS.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Create audio package skeleton + constants module + typed errors</name>
  <files>src/vibemix/audio/__init__.py, src/vibemix/audio/constants.py, src/vibemix/audio/errors.py</files>
  <action>Create `src/vibemix/audio/` package directory. Write `constants.py` with SPDX-License-Identifier header (Apache-2.0), `from __future__ import annotations`, a module docstring stating "LOAD-BEARING tuning constants — French Touch / Daft Punk / Digitalism profile, 125-128 BPM, tuned against real DJ sessions on 2026-05-11. Do not casually re-tune without a fresh live validation; Phase 6 will add genre-aware overrides on top.", and the following 14 constants/dict in this exact order (values lifted VERBATIM from cohost_v4.py — line anchors inline as comments): I/O constants `INVOKE_AUDIO_SECONDS = 18.0` (v4:100), `INPUT_SR_NATIVE = 48000` (v4:106), `INPUT_SR_TARGET = 16000` (v4:107), `OUTPUT_SR = 24000` (v4:108), `INPUT_CHUNK_FRAMES = 480` (v4:109), `OUTPUT_BLOCKSIZE = 256` (v4:110), `VOICE_BLOCKSIZE = 1024` (v4:111), `PASSTHROUGH_GAIN = 0.0` (v4:112), `MUSIC_GAIN_TO_GEMINI = 1.0` (v4:113 — v4 retuned from 8.0); mic gating `MIC_GAIN = 1.0` (v4:116), `MIC_TALK_THRESHOLD = 0.09` (v4:117), `MIC_GAIN_AT_AI_TALK = 0.0` (v4:118), `MIC_HOLD_AFTER_AI_MS = 350` (v4:119), `AI_TALK_THRESHOLD = 0.02` (v4:120); engine `SILENT_RMS = 0.012` (v4:127), `LOW_RMS = 0.040` (v4:128), `PEAK_RMS = 0.110` (v4:129), `AUDIBLE_DEBOUNCE_SEC = 0.6` (v4:130), `SILENCE_DEBOUNCE_SEC = 1.2` (v4:131), `EVENT_GLOBAL_MIN_GAP = 7.0` (v4:132), `HEARTBEAT_SEC = 45.0` (v4:133), then the `MIN_EVENT_GAP_PER_TYPE` dict verbatim from v4:134-142 with keys TRACK_CHANGE/PHASE/LAYER_ARRIVAL/MIX_MOVE/HEARTBEAT/MIC/MANUAL (use the existing `HEARTBEAT_SEC` reference for the HEARTBEAT value), `TRACK_CHANGE_MIN_CONFIDENCE = 0.5` (v4:143); and the three constants lifted OUT of `EventDetector` class scope per PATTERNS.md: `MUSIC_PRESENCE_MIN_SECONDS = 4.0` (v4:1176), `BPM_VALID_MIN = 100.0` (v4:1179), `BPM_VALID_MAX = 180.0` (v4:1180 — note CONTEXT.md said "assumed 175" but v4 source confirms 180.0). Type `MIN_EVENT_GAP_PER_TYPE` as `dict[str, float]` via PEP 585 inline annotation. Write `errors.py` with SPDX header, module docstring "Typed exceptions for the vibemix.audio package.", and a `SampleRateMismatchError(Exception)` class with a docstring explaining the BlackHole-44100-vs-48000 failure mode that bit Kaan on 2026-05-11 (Plan 04's macOS backend raises this; lives here so the Windows port reuses the same exception type in Phase 7). Write `__init__.py` with SPDX header, package docstring describing the audio package boundary ("Domain layer — pure Python audio primitives, no OS imports. The platform-specific I/O lives in `vibemix.platform._audio_*`."), and `from .constants import *` re-exports listing each constant explicitly in `__all__` plus `SampleRateMismatchError` from `.errors` and `Levels` from `.levels`. Match the existing `src/vibemix/__init__.py` and `src/vibemix/platform/__init__.py` style (SPDX header, blank line, future annotations import, docstring, code).</action>
  <verify>
    <automated>uv run python -c "from vibemix.audio import SILENT_RMS, LOW_RMS, PEAK_RMS, MIN_EVENT_GAP_PER_TYPE, MUSIC_PRESENCE_MIN_SECONDS, BPM_VALID_MIN, BPM_VALID_MAX, INVOKE_AUDIO_SECONDS, SampleRateMismatchError; assert SILENT_RMS == 0.012 and PEAK_RMS == 0.110 and BPM_VALID_MAX == 180.0 and INVOKE_AUDIO_SECONDS == 18.0 and MIN_EVENT_GAP_PER_TYPE['HEARTBEAT'] == 45.0 and issubclass(SampleRateMismatchError, Exception)"</automated>
  </verify>
  <done>constants.py contains all 14 constants + dict with values matching v4 verbatim; errors.py defines SampleRateMismatchError; __init__.py re-exports every public name; import smoke from `vibemix.audio` succeeds; ruff check + format pass on the new files.</done>
</task>

<task type="auto">
  <name>Task 1.2: Port Levels class verbatim from cohost_v4.py:255-286</name>
  <files>src/vibemix/audio/levels.py</files>
  <action>Create `src/vibemix/audio/levels.py` with SPDX-License-Identifier (Apache-2.0) header, `from __future__ import annotations`, imports `import threading; import numpy as np`, and the `Levels` class verbatim-shape from cohost_v4.py:255-286. Fields: `music: float`, `voice: float`, `mic: float`, `_lock: threading.Lock`. Methods preserve exact EMA coefficients (PATTERNS.md flags these as load-bearing): `update_music(mono_int16: np.ndarray) -> None` computes `float(np.sqrt(np.mean(mono_int16.astype(np.float32) ** 2))) / 32768.0` then `self.music = self.music * 0.6 + rms * 0.4` under lock; `update_voice(pcm_int16: bytes) -> None` early-returns on empty bytes, frombuffer as int16, float32 cast, RMS/32768 normalize, `self.voice = self.voice * 0.5 + rms * 0.5` under lock; `update_mic(samples_float: np.ndarray) -> None` computes float32 RMS directly (no 32768 normalize — samples already in -1..1), `self.mic = self.mic * 0.5 + rms * 0.5` under lock; `decay_voice() -> None` does `self.voice *= 0.7` under lock; `snapshot() -> dict[str, float]` returns `{"music": ..., "voice": ..., "mic": ...}` under lock. Add module docstring stating "EMA-smoothed RMS levels for music / AI voice / mic. Verbatim port of cohost_v4.py:255-286 (byte-identical between v3 and v4 per 02-PATTERNS.md). Thread-safe — audio-thread writers + asyncio readers share a single threading.Lock held only for the EMA arithmetic, not the RMS compute." Add class docstring restating the thread-safety contract. Type-hint all public methods. Do NOT add any features beyond v4 — pure port, even the EMA coefficient comments transfer (`# faster — music level shifts mid-mix`, `# 50/50 — needs to drop fast so mic-gate releases promptly`).</action>
  <verify>
    <automated>uv run python -c "
import numpy as np
from vibemix.audio import Levels
l = Levels()
assert l.music == 0.0 and l.voice == 0.0 and l.mic == 0.0
l.update_music(np.full(1024, 32767, dtype=np.int16))  # full-scale
assert l.music > 0.3  # EMA after one update with full-scale input
l.decay_voice()
assert l.voice == 0.0  # 0 * 0.7 = 0
snap = l.snapshot()
assert set(snap) == {'music', 'voice', 'mic'}
"</automated>
  </verify>
  <done>levels.py contains Levels class with identical method signatures and EMA coefficients to v4; import + smoke roundtrip works; ruff check + format pass.</done>
</task>

<task type="auto">
  <name>Task 1.3: Add pytest-mock dev dep + register macos_audio marker + write Wave 1 tests</name>
  <files>pyproject.toml, uv.lock, tests/audio/__init__.py, tests/audio/conftest.py, tests/audio/test_constants.py, tests/audio/test_levels.py</files>
  <action>Run `uv add --dev pytest-mock` from project root — this updates `[dependency-groups].dev` in pyproject.toml AND regenerates uv.lock atomically. Then manually edit `[tool.pytest.ini_options]` in pyproject.toml to add a `markers = ["macos_audio: requires BlackHole 2ch present (opt-in smoke test, Plan 05)"]` entry alongside the existing `testpaths`/`addopts` (strict-markers mode is already on per Phase 1 config — without this registration Plan 05's opt-in smoke would fail strict-markers). Create `tests/audio/__init__.py` (empty file). Create `tests/audio/conftest.py` with a shared `int16_sine(freq_hz: float, duration_sec: float, sample_rate: int = 16000, amplitude: float = 0.5) -> np.ndarray` fixture-factory (NOT a fixture itself — a plain helper function importable by every test module) that synthesizes a known-RMS int16 sine wave for buffer/level tests. Create `tests/audio/test_constants.py` with at least these test cases: `test_engine_constants_match_v4` asserts SILENT_RMS/LOW_RMS/PEAK_RMS/AUDIBLE_DEBOUNCE_SEC/SILENCE_DEBOUNCE_SEC/EVENT_GLOBAL_MIN_GAP/HEARTBEAT_SEC equal their v4 values; `test_io_constants_match_v4` asserts INVOKE_AUDIO_SECONDS/INPUT_SR_NATIVE/INPUT_SR_TARGET/OUTPUT_SR/INPUT_CHUNK_FRAMES/OUTPUT_BLOCKSIZE/VOICE_BLOCKSIZE/PASSTHROUGH_GAIN/MUSIC_GAIN_TO_GEMINI equal their v4 values; `test_mic_gating_constants_match_v4` covers MIC_GAIN/MIC_TALK_THRESHOLD/MIC_GAIN_AT_AI_TALK/MIC_HOLD_AFTER_AI_MS/AI_TALK_THRESHOLD; `test_event_gap_dict_shape_and_values` asserts MIN_EVENT_GAP_PER_TYPE has exactly the 7 keys (TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, HEARTBEAT, MIC, MANUAL) with values (5.0, 10.0, 10.0, 14.0, 45.0, 3.0, 1.5); `test_event_detector_constants_lifted_to_module_scope` asserts MUSIC_PRESENCE_MIN_SECONDS=4.0, BPM_VALID_MIN=100.0, BPM_VALID_MAX=180.0 are now module-level (not class-attrs of any EventDetector); `test_track_change_min_confidence_present` covers the v4-new TRACK_CHANGE_MIN_CONFIDENCE=0.5. Create `tests/audio/test_levels.py` with: `test_initial_state_zero` asserts a fresh Levels has all three fields at 0.0; `test_update_music_int16_full_scale_yields_nonzero` pushes a full-scale int16 sine, asserts `levels.music` > 0.3 after one update (EMA = 0.4 of full-scale RMS); `test_update_voice_bytes_path` confirms the bytes-typed input path normalizes correctly; `test_update_mic_float32_no_32768_divide` pushes a float32 array of value 0.5 and asserts the resulting `levels.mic` is in the 0.2-0.3 EMA range (NOT divided by 32768, which would yield ~7.6e-6); `test_decay_voice_multiplies_by_0_7` sets voice=1.0 via update then calls decay 5 times and asserts the result is `1.0 * 0.7**5` ± 0.01; `test_snapshot_returns_fresh_dict_not_view` asserts mutating the returned dict doesn't affect Levels state. Each test gets a focused docstring stating the v4 line it pins. Run `uv run ruff check tests/audio src/vibemix/audio` and `uv run ruff format tests/audio src/vibemix/audio` — fix any flagged issues. Final check: `uv run pytest tests/audio/ -x -q` all green.</action>
  <verify>
    <automated>uv run pytest tests/audio/test_constants.py tests/audio/test_levels.py -x -q && uv run ruff check src/vibemix/audio tests/audio && uv run ruff format --check src/vibemix/audio tests/audio && grep -q "pytest-mock" pyproject.toml && grep -q "macos_audio:" pyproject.toml</automated>
  </verify>
  <done>pytest-mock present in dev deps; macos_audio marker registered; ≥12 test cases across the two test files all green; ruff format + check clean; uv.lock regenerated and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-deps → build | `uv add --dev pytest-mock` introduces a new dependency tree — verify it resolves through pinned numpy/pytest without conflict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Tuning constants | mitigate | `test_constants.py` asserts equality with v4 source values — any drift fails CI before merge. The constants are LOAD-BEARING IP (real-DJ-session tuned); a silent typo is a regression. |
| T-02-01-02 | Information Disclosure | Levels.snapshot return | accept | snapshot returns floats — no PII or sensitive bytes leak through the API |
| T-02-01-03 | Denial of Service | Levels lock contention | accept | EMA arithmetic is ~5 instructions under lock — contention with the audio thread is sub-microsecond; not a real DoS surface |
</threat_model>

<verification>
- `uv run python -c 'from vibemix.audio import Levels, SILENT_RMS, MIN_EVENT_GAP_PER_TYPE, SampleRateMismatchError'` succeeds.
- `uv run pytest tests/audio/test_constants.py tests/audio/test_levels.py -x -q` — all green, ≥12 cases.
- `uv run ruff check src/vibemix/audio tests/audio` and `uv run ruff format --check src/vibemix/audio tests/audio` exit 0.
- `uv run pytest -x -q` — full suite still green (Phase 1 tests + Wave 1 tests).
- `grep -E "import sounddevice|import mss|import Quartz|import mido" src/vibemix/audio/` returns empty (audio domain has zero OS imports).
- POC files untouched: `git diff --name-only HEAD~1..HEAD -- cohost_v4.py cohost_v3.py cohost_v2.py cohost_lk.py cohost.py cohost.streaming.py.bak 'run*.sh' mascot.html 'sprite-*.png' generate_bat.py '_test_*.py' test_voice.py fillers/` returns empty.
</verification>

<success_criteria>
Wave 1 ships the constants + Levels + errors module + dev-dep + test scaffolding. Plans 02/03/04 can now `from vibemix.audio import <constant>` without circular deps and `from vibemix.audio.errors import SampleRateMismatchError` for Plan 04. No buffer code, no sounddevice surface — strictly pure-domain foundations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-core-port-ring-buffer-fix/02-01-SUMMARY.md` (small per-wave summary; the phase-level SUMMARY happens in Plan 05). Commit as `feat(02): wave 1 — audio package skeleton + constants + Levels + errors`.
</output>
