---
phase: 02-audio-core-port-ring-buffer-fix
plan: 04
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - src/vibemix/platform/_audio_macos.py
  - src/vibemix/platform/__init__.py
  - src/vibemix/platform/audio.py
  - tests/test_audio_macos.py
  - tests/test_audio_macos_live.py
autonomous: true
requirements:
  - ARCH-07
  - AUDIO-01
  - AUDIO-04
  - AUDIO-06
  - AUDIO-07
  - AUDIO-08
  - AUDIO-09
must_haves:
  truths:
    - "`isinstance(AudioMacOS(...), AudioBackend) is True` — the concrete macOS impl satisfies the Phase 1 `@runtime_checkable` Protocol structurally."
    - "`assert_device_sample_rate(idx, 48000)` raises `SampleRateMismatchError` when `sd.query_devices(idx)['default_samplerate']` is 44100 (RESEARCH.md Q2 — empirically verified against live BlackHole on 2026-05-11). Error message includes the actionable Audio MIDI Setup + Drift Correction fix per CONTEXT.md."
    - "Sample-rate guard is pre-open AND post-open: pre-open reads `sd.query_devices`, post-open reads `Stream.samplerate` — both must match the requested rate (RESEARCH.md Q2 two-layer pattern)."
    - "All four AudioBackend methods (`find_device`, `open_capture`, `open_passthrough_output`, `open_voice_output`) are implemented on AudioMacOS. Plus the inline mic stream factory from v4:1895-1908 is wrapped as `AudioMacOS.open_mic_capture(...)` (PATTERNS.md §AntiPatterns-5 — symmetry with the other stream factories)."
    - "Mocked sample-rate tests use `pytest-mock`'s `mocker.patch` on `vibemix.platform._audio_macos.sd.query_devices` per RESEARCH.md Q2 snippet — NO real audio device required for CI."
    - "Live smoke test (`test_audio_macos_live.py`) is marked `@pytest.mark.macos_audio` (registered in Plan 01's pyproject.toml change) — opts in to live BlackHole testing on Kaan's machine only; skipped by default."
    - "Each stream factory defines its sounddevice callback as an inner closure (POC pattern — PATTERNS.md §Established-Patterns); closure captures the registry's buffer references and the recorder."
    - "POC files (cohost*.py, run*.sh, mascot.html, sprite-*.png, generate_bat.py, _test_*.py, test_voice.py, fillers/) untouched — `git diff --name-only HEAD~1..HEAD` excludes them."
  artifacts:
    - path: "src/vibemix/platform/_audio_macos.py"
      provides: "AudioMacOS class — concrete `AudioBackend` Protocol impl wrapping sounddevice. find_device + open_capture + open_passthrough_output + open_voice_output + open_mic_capture (Plan-2-only addition for symmetry). Pre-open + post-open sample-rate sanity check (RESEARCH.md Q2 two-layer). _SoundDeviceStreamHandle adapter satisfies AudioStream Protocol."
      contains: "class AudioMacOS"
    - path: "src/vibemix/platform/__init__.py"
      provides: "Public re-export adds `AudioMacOS` alongside the existing Protocol exports — Plan 04+ callers do `from vibemix.platform import AudioMacOS`"
      contains: "AudioMacOS"
    - path: "src/vibemix/platform/audio.py"
      provides: "Phase 1 Protocol module — may gain a NEW protocol method `find_mic_device` (or equivalent) if Plan 04's port surfaces a shape gap. CRITICAL CONSTRAINT: change Phase 1 Protocol via an explicit amendment commit ONLY IF the v4 call sites can't be satisfied by the existing 4 methods. Default expectation: no change."
    - path: "tests/test_audio_macos.py"
      provides: "Mocked sounddevice unit tests — SampleRateMismatchError on mismatch, isinstance(AudioBackend), find_device by substring, find_device raises clear error when device not found, stream factories call sd.X with the right kwargs (RESEARCH.md test mocking pattern)"
    - path: "tests/test_audio_macos_live.py"
      provides: "Smoke test marked @pytest.mark.macos_audio — skipped by default, opt-in via `uv run pytest -m macos_audio` on Kaan's machine with BlackHole present"
  key_links:
    - from: "src/vibemix/platform/_audio_macos.py"
      to: "src/vibemix/platform/audio.py"
      via: "AudioBackend Protocol implemented structurally — no inheritance, runtime_checkable allows isinstance check"
      pattern: "AudioBackend"
    - from: "src/vibemix/platform/_audio_macos.py"
      to: "src/vibemix/audio/errors.py"
      via: "SampleRateMismatchError raised from assert_device_sample_rate"
      pattern: "raise SampleRateMismatchError"
    - from: "src/vibemix/platform/_audio_macos.py"
      to: "src/vibemix/audio/constants.py"
      via: "INPUT_SR_NATIVE / INPUT_SR_TARGET / OUTPUT_SR / INPUT_CHUNK_FRAMES / OUTPUT_BLOCKSIZE / VOICE_BLOCKSIZE / PASSTHROUGH_GAIN / MUSIC_GAIN_TO_GEMINI imported for stream kwargs + the resample_poly path"
      pattern: "from vibemix.audio.constants import"
    - from: "src/vibemix/platform/_audio_macos.py"
      to: "src/vibemix/audio/buffers.py + registry.py + recorder.py"
      via: "BufferRegistry handed in as ctor arg; callbacks push into the rings + recorder; v4:893-947 wiring"
      pattern: "registry.audio.push"
---

<objective>
Land the concrete macOS audio I/O impl (`_audio_macos.AudioMacOS`) that satisfies the Phase 1 `AudioBackend` Protocol. This wires the v4 `start_passthrough_stream` / `start_playback_stream` / `start_input_to_session` / inline-mic-stream choreography into the Phase 1 firewall shape and adds the non-negotiable sample-rate sanity guard.

Purpose:
- Convert v4's three module-level stream factories (v4:855-947) + the inline mic stream (v4:1895-1908) into methods on a single `AudioMacOS` class that owns the `sounddevice` import and exposes the cross-platform `AudioBackend` Protocol surface.
- Wrap `sd.InputStream` / `sd.OutputStream` / `sd.RawOutputStream` instances with a tiny `_SoundDeviceStreamHandle` adapter so they satisfy Phase 1's `AudioStream` Protocol (start/stop/close + `latency_ms` property).
- Add the LOAD-BEARING sample-rate guard `assert_device_sample_rate(idx, expected)` — pre-open it reads `sd.query_devices(idx)['default_samplerate']` (RESEARCH.md Q2 — empirically verified that this reflects Audio MIDI Setup's current setting); post-open it cross-checks `stream.samplerate`. Either mismatch raises `SampleRateMismatchError` with the actionable Audio MIDI Setup + Drift Correction message. This is the bug Kaan hit live on 2026-05-11 — Phase 2's job is to make it impossible to ship a build that doesn't catch it (CONTEXT.md "non-negotiable").
- Honor the v4 dual-buffer pattern (`audio_buf` at gain-boosted state level for engine math, `clean_audio_buf` at natural level for LLM Part snapshots) — both fed inside the same input callback per v4:893-947. The callback also drives `levels.update_music`, `recorder.push_input`, and `passthrough.push`.
- Drop the v4 `_HAS_VISION` / `_HAS_WS` / `_HAS_QUARTZ` feature-flag anti-pattern (PATTERNS.md §AntiPatterns-2) — if `sounddevice` fails to import, the macOS backend fails loud, no silent degradation.

Output:
- `src/vibemix/platform/_audio_macos.py` — AudioMacOS + _SoundDeviceStreamHandle + assert_device_sample_rate
- `src/vibemix/platform/__init__.py` — adds AudioMacOS re-export
- `tests/test_audio_macos.py` — ≥6 mocked-sounddevice unit tests
- `tests/test_audio_macos_live.py` — 1-2 opt-in smoke tests gated by `@pytest.mark.macos_audio`
- (Conditional) `src/vibemix/platform/audio.py` — Phase 1 Protocol amendment IF v4 port reveals a shape gap; default expectation is no change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-CONTEXT.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-RESEARCH.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-PATTERNS.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-01-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-02-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-03-PLAN.md
@.planning/phases/01-platform-protocol-firewall/01-SUMMARY.md
@cohost_v4.py
@src/vibemix/platform/audio.py
@src/vibemix/audio/constants.py
@src/vibemix/audio/errors.py
@src/vibemix/audio/registry.py

<interfaces>
<!-- Phase 1 contract (already shipped — do not modify unless gap found): -->

From src/vibemix/platform/audio.py:
```python
Kind = Literal["input", "output"]
AudioCallback = Callable[..., None]   # sounddevice-shaped: (indata, frames, time_info, status)

class AudioStream(Protocol):
    @property
    def latency_ms(self) -> float: ...
    def start(self) -> None: ...
    def stop(self) -> None: ...
    def close(self) -> None: ...

@runtime_checkable
class AudioBackend(Protocol):
    def find_device(self, name_substring: str, kind: Kind) -> int: ...
    def open_capture(self, device_index: int, *, sample_rate: int, channels: int,
                     block_size: int, callback: AudioCallback) -> AudioStream: ...
    def open_passthrough_output(self, device_index: int, *, sample_rate: int, channels: int,
                                block_size: int, callback: AudioCallback) -> AudioStream: ...
    def open_voice_output(self, device_index: int, *, sample_rate: int,
                          block_size: int, callback: AudioCallback) -> AudioStream: ...
```

<!-- v4 source for port (READ-ONLY): -->

cohost_v4.py:239-248  → find_device (substring + kind matching)
cohost_v4.py:855-875  → start_passthrough_stream (sd.OutputStream @ 48kHz stereo float32)
cohost_v4.py:878-890  → start_playback_stream (sd.RawOutputStream @ 24kHz mono int16)
cohost_v4.py:893-947  → start_input_to_session (sd.InputStream @ 48kHz stereo float32, callback resamples to 16kHz via scipy.signal.resample_poly, feeds dual AudioBuffer + Levels + passthrough + recorder)
cohost_v4.py:1895-1908 → inline mic stream (sd.InputStream @ 48kHz mono float32, callback does mic.push(mono.astype(np.float32)))

<!-- assert_device_sample_rate reference impl from RESEARCH.md Q2: -->

```python
def assert_device_sample_rate(device_index: int, expected: int) -> None:
    """Pre-open guard: reads sd.query_devices(idx)['default_samplerate']
    which on macOS CoreAudio reflects kAudioDevicePropertyNominalSampleRate
    (= whatever Audio MIDI Setup is configured to right now).
    NOT the same as Stream.samplerate after opening (which reports the
    PortAudio-negotiated rate and silently lies about device drift).
    """
    info = sd.query_devices(device_index)
    actual = int(info["default_samplerate"])
    if actual != expected:
        raise SampleRateMismatchError(
            f"BlackHole 2ch is configured at {actual}Hz but vibemix expects {expected}Hz.\n"
            f"Fix: open Audio MIDI Setup → BlackHole 2ch → Format → "
            f"{expected:,} Hz (2 ch, 32-bit float).\n"
            f"Also enable Drift Correction on BlackHole if you use a Multi-Output Device."
        )
```

<!-- The dual-buffer input callback flow (v4:893-947, condensed): -->

```python
# In open_capture callback (inner closure capturing registry + recorder):
# 1. status check → stderr if non-empty
# 2. passthrough.push(indata.tobytes() OR indata * PASSTHROUGH_GAIN.tobytes())
# 3. music48 = indata.mean(axis=1).astype(np.float32)   # stereo → mono
# 4. mic.pull(len(music48))    # cadence-align discard (v4:918 comment "keep cadence aligned, discard samples")
# 5. state48 = music48 * MUSIC_GAIN_TO_GEMINI     # gain-boosted for engine math
# 6. state_pcm_48k = np.clip(state48 * 32767.0, -32768, 32767).astype(np.int16)
# 7. Try block: resample state48 → 16kHz int16 → audio_buf.push + recorder.push_input
#               resample music48 (natural) → 16kHz int16 → clean_audio_buf.push
# 8. levels.update_music(state_pcm_48k)
```

<!-- _SoundDeviceStreamHandle adapter shape (RESEARCH.md): -->

```python
class _SoundDeviceStreamHandle:
    def __init__(self, stream: sd.RawInputStream | sd.RawOutputStream | sd.InputStream | sd.OutputStream) -> None:
        self._stream = stream

    @property
    def latency_ms(self) -> float:
        lat = self._stream.latency
        if isinstance(lat, tuple):
            lat = lat[0]
        return float(lat) * 1000.0

    def start(self) -> None: self._stream.start()
    def stop(self) -> None: self._stream.stop()
    def close(self) -> None: self._stream.close()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Implement assert_device_sample_rate + _SoundDeviceStreamHandle + AudioMacOS class (find_device + open_capture + open_passthrough_output + open_voice_output + open_mic_capture)</name>
  <files>src/vibemix/platform/_audio_macos.py, src/vibemix/platform/__init__.py</files>
  <action>Create `src/vibemix/platform/_audio_macos.py` with SPDX-License-Identifier (Apache-2.0) header, future annotations, module docstring stating "Concrete `AudioBackend` impl for macOS — owns all sounddevice / scipy imports. Wires the v4 stream factories (cohost_v4.py:855-947 + 1895-1908) into a class that satisfies the Phase 1 Protocol firewall. Sample-rate sanity guard (RESEARCH.md Q2) catches the BlackHole 44100-vs-48000 mismatch Kaan hit live on 2026-05-11 — Audio MIDI Setup misconfig is detected pre-open AND post-open." Imports: `import sys`, `import numpy as np`, `import sounddevice as sd`, `from scipy.signal import resample_poly`, `from typing import Literal`, `from collections.abc import Callable`, `from vibemix.platform.audio import AudioBackend, AudioStream, AudioCallback, Kind`, `from vibemix.audio.errors import SampleRateMismatchError`, `from vibemix.audio.registry import BufferRegistry`, `from vibemix.audio.recorder import VoiceRecorder`, `from vibemix.audio.constants import (INPUT_SR_NATIVE, INPUT_SR_TARGET, OUTPUT_SR, INPUT_CHUNK_FRAMES, OUTPUT_BLOCKSIZE, VOICE_BLOCKSIZE, PASSTHROUGH_GAIN, MUSIC_GAIN_TO_GEMINI)`. Implement free function `assert_device_sample_rate(device_index: int, expected: int) -> None` verbatim per the RESEARCH.md Q2 reference shown in the interfaces block — read `sd.query_devices(device_index)["default_samplerate"]`, int-cast, compare to `expected`, raise `SampleRateMismatchError` with the multi-line message including the device name (read from `info["name"]`) AND the Audio MIDI Setup fix steps AND the Drift Correction note. Implement `class _SoundDeviceStreamHandle` per the RESEARCH.md interface block — wraps any `sd.InputStream | sd.OutputStream | sd.RawInputStream | sd.RawOutputStream` and satisfies `AudioStream` Protocol with `latency_ms` property (handles the tuple-vs-scalar latency shape) + `start` / `stop` / `close`. Implement `class AudioMacOS`: `__init__(self, registry: BufferRegistry, recorder: VoiceRecorder) -> None` — store references, no work done at construction (per PATTERNS.md §Wiring-in-main analysis). `find_device(self, name_substring: str, kind: Kind) -> int` — verbatim port of v4:239-248 with one improvement: on miss, raise `RuntimeError(f"No {kind} device matching {name_substring!r}. Available {kind} devices: {[d['name'] for d in sd.query_devices() if d[max_field] > 0]}")` so the user sees the candidate list (PATTERNS.md §Anti-Patterns + RESEARCH.md threat 4 "BlackHole driver not installed → cryptic PortAudio error"). `open_capture(self, device_index: int, *, sample_rate: int, channels: int, block_size: int, callback: AudioCallback) -> AudioStream` — this is the v4:893-947 port. PRE-OPEN: call `assert_device_sample_rate(device_index, sample_rate)`. Open `sd.InputStream(device=device_index, samplerate=sample_rate, channels=channels, dtype="float32", blocksize=block_size, latency="low", callback=callback)`. POST-OPEN: `if int(stream.samplerate) != sample_rate: stream.close(); raise SampleRateMismatchError(f"PortAudio negotiated {int(stream.samplerate)}Hz vs requested {sample_rate}Hz on device {device_index!r}. Hardware drift detected.")`. Call `stream.start()`. Return `_SoundDeviceStreamHandle(stream)`. NOTE on the callback parameter — Phase 1's Protocol says `callback: AudioCallback` (caller provides the sounddevice-shaped callback). Phase 2's `AudioMacOS.open_capture` does NOT build the v4 dual-buffer callback internally — that lives in a CALLER FACTORY (Plan 04 of Phase 3 OR a Phase 2 demo `main()` if needed) that closes over `registry` + `recorder` + `levels` and is passed in by the caller. This keeps `_audio_macos.py` focused on the I/O / sample-rate / stream lifecycle, NOT the dual-buffer choreography. The closure pattern IS still used inside Plan 04 — see Task 4.2 for where the callback factory lives. `open_passthrough_output(self, device_index: int, *, sample_rate: int, channels: int, block_size: int, callback: AudioCallback) -> AudioStream` — pre-open assert at `sample_rate`, open `sd.OutputStream(device=device_index, samplerate=sample_rate, channels=channels, dtype="float32", blocksize=block_size, latency="low", callback=callback)`, post-open assert, start, return handle. `open_voice_output(self, device_index: int, *, sample_rate: int, block_size: int, callback: AudioCallback) -> AudioStream` — open `sd.RawOutputStream(device=device_index, samplerate=sample_rate, channels=1, dtype="int16", blocksize=block_size, latency="low", callback=callback)`, same pre/post guards, return handle. ALSO add `open_mic_capture(self, device_index: int, *, sample_rate: int, block_size: int, callback: AudioCallback) -> AudioStream` — wraps the v4:1895-1908 inline mic stream as a proper factory (PATTERNS.md §AntiPatterns-5 fix: every other stream has a factory, mic doesn't in v4 — Phase 2 makes the surface symmetric). Channels=1 mono, dtype="float32", same pre/post sample-rate guards. NOTE this method is NOT in the Phase 1 `AudioBackend` Protocol — it's an AudioMacOS-only extension. **Decision point:** if Plan 04 reveals that callers always need mic capture, propose amending the Phase 1 Protocol in a separate commit BEFORE finalizing this plan (per the prompt's Critical Constraint #3). Default action: ship as AudioMacOS-only public method; the Plan 1 amendment can happen in Phase 3 when mic-into-AgentSession is wired. Document this in the method's docstring. Update `src/vibemix/platform/__init__.py` to add `from ._audio_macos import AudioMacOS, assert_device_sample_rate` IF and ONLY IF doing so doesn't cause a side-effect import of `sounddevice` for non-macOS Linux / Windows imports — wrap in `if sys.platform == "darwin"` with a fallback `AudioMacOS = None  # type: ignore[assignment]` for other platforms, OR put the import behind a lazy proxy. Pragmatic call: keep the import at module top of `__init__.py` since Phase 2 is macOS-only by design (CLAUDE.md constraint: Linux excluded; Windows lands in Phase 7). The `import sounddevice as sd` itself works on all three platforms (sounddevice ships wheels for darwin/linux/win) — the firewall test (`tests/test_platform.py` from Phase 1) only forbids `import sounddevice` inside `platform/audio.py` (the typing-only module), NOT inside `_audio_macos.py`. Re-verify by running the Phase 1 firewall test after the change. If `tests/test_platform.py` flags `_audio_macos.py` as a violation, the test is over-broad and Phase 2 needs to refine its allowlist — bring this up via `## ⚠ Source Audit` if it surfaces. Run ruff check + format on the new module + __init__.py.</action>
  <verify>
    <automated>uv run python -c "
from vibemix.platform import AudioBackend, AudioMacOS
from vibemix.audio import BufferRegistry, AudioBuffer, MicBuffer, PassthroughBuffer, PlaybackQueue, Levels, VoiceRecorder
from vibemix.audio.errors import SampleRateMismatchError
import tempfile, pathlib

# AudioMacOS satisfies the @runtime_checkable AudioBackend Protocol
levels = Levels()
reg = BufferRegistry(
    audio=AudioBuffer(seconds=1.0), clean_audio=AudioBuffer(seconds=1.0),
    mic=MicBuffer(gain=1.0, levels=levels), passthrough=PassthroughBuffer(),
    playback=PlaybackQueue(levels), levels=levels,
)
with tempfile.TemporaryDirectory() as tmp:
    rec = VoiceRecorder(root=pathlib.Path(tmp))
    backend = AudioMacOS(registry=reg, recorder=rec)
    assert isinstance(backend, AudioBackend), 'AudioMacOS does not satisfy AudioBackend Protocol'
    # The Protocol methods exist on the instance
    for m in ('find_device', 'open_capture', 'open_passthrough_output', 'open_voice_output', 'open_mic_capture'):
        assert callable(getattr(backend, m)), f'missing method {m}'
    rec.close()
"</automated>
  </verify>
  <done>_audio_macos.py exists; AudioMacOS satisfies AudioBackend isinstance check; all 5 stream factory methods callable; assert_device_sample_rate raises SampleRateMismatchError with multi-line actionable text; ruff clean.</done>
</task>

<task type="auto">
  <name>Task 4.2: Write mocked sounddevice unit tests + opt-in live smoke test + verify Phase 1 firewall still holds</name>
  <files>tests/test_audio_macos.py, tests/test_audio_macos_live.py</files>
  <action>Create `tests/test_audio_macos.py` with SPDX header, future annotations, `import pytest`, `import numpy as np`, imports from `vibemix.audio` (BufferRegistry, AudioBuffer, MicBuffer, PassthroughBuffer, PlaybackQueue, Levels, VoiceRecorder) and `vibemix.audio.errors` (SampleRateMismatchError) and `vibemix.platform` (AudioBackend, AudioMacOS). Add a `@pytest.fixture` `make_backend` that returns a callable producing a fresh `AudioMacOS(registry=..., recorder=...)` instance using `tmp_path` for the recorder root — keeps each test isolated. Implement at least: (RATE-01) `test_assert_device_sample_rate_raises_on_mismatch` per RESEARCH.md Q2 snippet — `mocker.patch("vibemix.platform._audio_macos.sd.query_devices", return_value={"default_samplerate": 44100.0, "name": "BlackHole 2ch"})`, call `assert_device_sample_rate(device_index=2, expected=48000)`, assert raises `SampleRateMismatchError` with `match=r"44100Hz.*48000Hz"`. Assert the error message also contains the strings `"Audio MIDI Setup"` and `"Drift Correction"`. (RATE-02) `test_assert_device_sample_rate_passes_on_match` — mocker.patch returns `{"default_samplerate": 48000.0, "name": "BlackHole 2ch"}`, assert call returns None silently. (RATE-03) `test_open_capture_pre_open_guard_fires_before_stream_construction` — mock `sd.query_devices` to return 44100; mock `sd.InputStream` to raise an `AssertionError` if it's called at all (= the guard correctly short-circuits BEFORE PortAudio sees the device). Call `backend.open_capture(0, sample_rate=48000, channels=2, block_size=480, callback=lambda *a: None)`, assert `SampleRateMismatchError` raised, assert `sd.InputStream.call_count == 0`. (RATE-04) `test_open_capture_post_open_guard_fires_on_negotiated_drift` — mocker patches `sd.query_devices` to return 48000 (pre-open passes), patches `sd.InputStream` to return a mock with `.samplerate = 47999.0` (post-open mismatch); calls `backend.open_capture(...)`, asserts `SampleRateMismatchError` raised AND the stream's `.close()` was called (verify `stream.close.assert_called_once()`) — the post-open guard MUST close the stream on mismatch to avoid resource leak. (RATE-05) `test_find_device_returns_index_for_matching_input` — mocker patches `sd.query_devices()` (no args path) to return `[{"name": "BlackHole 2ch", "max_input_channels": 2, "max_output_channels": 0}, {"name": "Built-in Output", "max_input_channels": 0, "max_output_channels": 2}]`; calls `backend.find_device("BlackHole", "input")`, asserts returns 0. (RATE-06) `test_find_device_raises_with_candidate_list_on_miss` — same mock, calls `backend.find_device("nonexistent", "input")`, asserts `RuntimeError` raised with the device names in the message (the candidate-list improvement over v4). (RATE-07) `test_audio_macos_is_audio_backend` — instantiates AudioMacOS, asserts `isinstance(backend, AudioBackend) is True` (Phase 1 runtime_checkable contract). (RATE-08) `test_open_passthrough_output_pre_open_guard` and (RATE-09) `test_open_voice_output_pre_open_guard` — mirror RATE-03 for the other two stream openers (mock query_devices to wrong rate, assert guard fires before sd.OutputStream / sd.RawOutputStream is called). (RATE-10) `test_open_mic_capture_pre_open_guard` — same pattern for the new mic factory method. (RATE-11) `test_phase1_firewall_still_holds_with_audio_macos_present` — re-run a small replica of Phase 1's `tests/test_platform.py` OS-leak guard: `from vibemix.platform import audio; assert "sounddevice" not in getattr(audio, "__dict__", {})` AND verify `_audio_macos.py` is NOT imported by the bare `from vibemix.platform.audio import AudioBackend` path (use `sys.modules` snapshot before and after). This pins the typing-only-module guarantee Phase 1 shipped — Plan 04 must not regress it. Create `tests/test_audio_macos_live.py` with SPDX header, future annotations. Add `pytestmark = pytest.mark.macos_audio` at module top so the entire file opts in via `uv run pytest -m macos_audio`. Implement: (LIVE-01) `test_blackhole_device_present_at_48khz_or_raises` — calls `backend.find_device("BlackHole", "input")` then `assert_device_sample_rate(idx, 48000)`. If BlackHole is configured at 48kHz this passes; if at 44.1kHz it raises with the actionable message. This is the smoke test Kaan runs on his machine before declaring the phase shipped. (LIVE-02) `test_open_voice_output_completes_without_real_audio_device` — calls `backend.open_voice_output(...)` with the headphones device (use `backend.find_device("External Headphones", "output")` OR fall back to `find_device("Headphones", "output")`); asserts the returned handle satisfies `AudioStream` Protocol (has start/stop/close + latency_ms); immediately calls `handle.close()` to avoid leaking a real stream. This ONLY runs when Kaan invokes the macos_audio marker — CI default-skips. Run `uv run pytest tests/ -x -q` (full suite minus macos_audio): all green. Run `uv run pytest -m macos_audio --collect-only` to confirm the live tests are discovered correctly (Plan 05 will actually invoke them on Kaan's machine). Verify Phase 1 tests still green: `uv run pytest tests/test_platform.py tests/test_package.py -x -q`. Run ruff check + format.</action>
  <verify>
    <automated>uv run pytest tests/ -x -q --ignore=tests/test_audio_macos_live.py && uv run pytest -m macos_audio --collect-only -q 2>&1 | grep -E "test_blackhole_device_present|test_open_voice_output" && uv run ruff check src/vibemix tests && uv run ruff format --check src/vibemix tests</automated>
  </verify>
  <done>≥11 mocked unit tests in test_audio_macos.py all green; live smoke file collected under macos_audio marker; Phase 1 firewall tests still pass; AudioMacOS satisfies AudioBackend; sample-rate guard pre-open + post-open both pinned by tests; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sounddevice → audio thread | sd.InputStream callback runs on PortAudio thread — must not allocate (Plan 02 fix), must not block on asyncio |
| BlackHole device config → vibemix | Audio MIDI Setup is OS-level state outside the app's control — Phase 2 detects misconfig, Phase 11 calibration wizard fixes it |
| _audio_macos.py → vibemix.platform.audio Protocol module | typing-only Protocol module must NOT gain a sounddevice import path (Phase 1 firewall) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | BlackHole at 44.1kHz silently passes sd.InputStream | mitigate | `assert_device_sample_rate` pre-open guard via `sd.query_devices(idx)['default_samplerate']` (RESEARCH.md Q2 — empirically verified). Tests RATE-01..04 pin both pre-open and post-open paths. This is the bug Kaan hit on 2026-05-11; making it impossible to ship a build that doesn't catch it is non-negotiable (CONTEXT.md). |
| T-02-04-02 | DoS | Audio callback blocked on stderr write | accept | sounddevice's status param is `print(file=sys.stderr)` in v4 — fast (no I/O blocking), inherited as-is. Phase 4 may add a structured logger. |
| T-02-04-03 | Information Disclosure | BlackHole driver missing → cryptic PortAudio stack trace | mitigate | `find_device` raises with the candidate-name list in the message — user sees "available: ['MacBook Pro Speakers', ...]" not a PortAudioError. RESEARCH.md threat 4. |
| T-02-04-04 | Tampering | _audio_macos.py imports leaking into platform/audio.py typing module | mitigate | Test RATE-11 re-runs the Phase 1 firewall guard. Plan 04 must not regress it. |
| T-02-04-05 | DoS | Hardware sample-rate drift on Multi-Output Device | mitigate | Post-open `Stream.samplerate` check (RESEARCH.md Q2 "belt-and-suspenders") + "enable Drift Correction" instruction in the error message (RESEARCH.md Pitfall 3). |
| T-02-04-06 | Resource leak | Pre-open succeeds, post-open fails, stream not closed | mitigate | Test RATE-04 pins that `stream.close()` is called before raising the post-open SampleRateMismatchError. |
</threat_model>

<verification>
- `uv run pytest tests/ -x -q --ignore=tests/test_audio_macos_live.py` — full suite (Plans 01+02+03+04 tests) all green.
- `uv run pytest -m macos_audio --collect-only` — live smoke tests discovered (run on Kaan's machine in Plan 05).
- `uv run ruff check src/vibemix tests` and `uv run ruff format --check src/vibemix tests` exit 0.
- `uv run python -c 'from vibemix.platform import AudioBackend, AudioMacOS'` succeeds.
- `uv run python -c 'from vibemix.platform import AudioMacOS; from vibemix.audio import BufferRegistry, AudioBuffer, MicBuffer, PassthroughBuffer, PlaybackQueue, Levels, VoiceRecorder; import tempfile, pathlib; l=Levels(); reg=BufferRegistry(audio=AudioBuffer(seconds=1), clean_audio=AudioBuffer(seconds=1), mic=MicBuffer(gain=1.0, levels=l), passthrough=PassthroughBuffer(), playback=PlaybackQueue(l), levels=l); rec=VoiceRecorder(root=pathlib.Path(tempfile.mkdtemp())); from vibemix.platform import AudioBackend; assert isinstance(AudioMacOS(registry=reg, recorder=rec), AudioBackend)'` succeeds (runtime_checkable contract pinned).
- POC files untouched.
</verification>

<success_criteria>
The macOS audio backend is shipped and satisfies the Phase 1 Protocol. The non-negotiable sample-rate sanity guard is in place pre-open (`sd.query_devices` reading Audio MIDI Setup state) and post-open (`Stream.samplerate` for hardware drift). All five stream factories (capture / passthrough output / voice output / mic capture + find_device) work, with mocked unit tests pinning behavior and live opt-in tests ready for Plan 05 to invoke on Kaan's machine.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-core-port-ring-buffer-fix/02-04-SUMMARY.md`. Commit as `feat(02): wave 4 — AudioMacOS impl + sample-rate sanity guard (pre/post-open)`.
</output>
