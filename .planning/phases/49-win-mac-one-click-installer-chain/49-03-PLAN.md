---
phase: 49-win-mac-one-click-installer-chain
plan: 03
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - tauri/ui/src/wizard/step-forewarning.ts
  - tauri/ui/src/wizard/step-driver-fetch.ts
  - tauri/ui/src/wizard/step-48k-probe.ts
  - tauri/ui/src/wizard/router.ts
  - tauri/ui/src/wizard/onboarding-stopwatch.ts
  - tauri/ui/src/wizard/smoke-test.ts
  - tauri/ui/src/wizard/copy.ts
  - src/vibemix/install/blackhole_probe.py
  - tests/wizard/test_forewarning.test.ts
  - tests/wizard/test_driver_fetch.test.ts
  - tests/wizard/test_48k_probe.test.ts
  - tests/wizard/test_axe_a11y.test.ts
autonomous: true
requirements:
  - INSTALL-01
  - INSTALL-02
  - INSTALL-03
  - INSTALL-06
  - INSTALL-08
  - INSTALL-10
tags:
  - wizard
  - tauri-ui
  - forewarning
  - a11y
  - onboarding-stopwatch

must_haves:
  truths:
    - "`tauri/ui/src/wizard/step-forewarning.ts` renders 2-card OS forewarning surface (Mac BlackHole + Win VB-CABLE) reading strings from `installer/companion/onboarding_copy.json § steps.forewarning`; OS detection via `platform: \"darwin\" | \"win32\"` prop (Wave 3 fakes via navigator.userAgent, Wave 4 reads TAURI_PLATFORM)"
    - "`tauri/ui/src/wizard/step-driver-fetch.ts` invokes companion `fetch_drivers.{sh,ps1}` via Tauri shell.execute, subscribes to `audio.probe.*` events (existing event family from src/vibemix/install/blackhole_probe.py), renders progress bar + parallel probe rows (MIDI / TCC / Bravoh-proxy); emits `INSTALL_READY` event on success with `elapsed_ms` payload"
    - "`tauri/ui/src/wizard/step-48k-probe.ts` invokes `audio_config.py --probe-48k`, renders success or FAIL card per UI-SPEC; FAIL state shows fix-it CTA that invokes `audio_config.py --configure-routing`; final CTA `[ Start your set ]` on success advances to session"
    - "`tauri/ui/src/wizard/router.ts` registers 3 new step IDs: `forewarning`, `driver-fetch`, `format-check`; preserves existing step order (intro → permissions → output-device → controller → forewarning → driver-fetch → format-check → telemetry-consent → profile-consent → done); back-button support per Impeccable Wave 5.A"
    - "`tauri/ui/src/wizard/onboarding-stopwatch.ts` extended with `emitInstallReadyEvent()` function that writes elapsed ms + per-step breakdown to `audio.probe.install_ready` event payload (additive field `auto_install_attempted: bool`); existing stopwatch behavior preserved byte-identical"
    - "`tauri/ui/src/wizard/copy.ts` is a NEW typed module that loads `installer/companion/onboarding_copy.json` at module-init and exports a typed `copy` object; tests import from here, components import from here — single source-of-truth for wizard strings (substitution: copy.steps.welcome.primary_cta)"
    - "`src/vibemix/install/blackhole_probe.py` extends existing `BlackHoleProbeEvent` payload schema with new optional field `auto_install_attempted: bool` (defaults false); event TYPE unchanged (still `audio.probe.detected` / `audio.probe.missing` / `audio.probe.cta_fired`); IPC contract additive, ZERO new event types"
    - "`tauri/ui/src/wizard/smoke-test.ts` extended with axe-core run on the 3 new steps + uninstall dialog (test gated on dev mode; production build excluded via vite env check); produces report under `tauri/ui/dist/a11y-report.html`"
    - "`tests/wizard/test_axe_a11y.test.ts` is a vitest run that loads each new step, runs axe-core, asserts ZERO serious + ZERO critical violations (INSTALL-08 gate)"
    - "Every wizard string reads from `copy.ts` (which reads `onboarding_copy.json`); zero inline string literals in the 3 new step files (test asserts via AST grep)"
    - "Visual contract: every new step uses tokens from `tauri/ui/src/tokens.css` only — zero hex literal in the 3 new step files (test asserts via regex grep)"
    - "Step-0 worktree-sync invariant included in plan execution"
  artifacts:
    - path: "tauri/ui/src/wizard/step-forewarning.ts"
      provides: "Step 1 OS forewarning component (Mac BlackHole + Win VB-CABLE cards)"
      min_lines: 100
    - path: "tauri/ui/src/wizard/step-driver-fetch.ts"
      provides: "Step 2 companion-fetch orchestrator + parallel probes + progress bar + INSTALL_READY emit"
      min_lines: 200
    - path: "tauri/ui/src/wizard/step-48k-probe.ts"
      provides: "Step 3 BlackHole 48 kHz probe + done CTA"
      min_lines: 100
    - path: "tauri/ui/src/wizard/router.ts"
      provides: "Step registration for 3 new IDs; preserved existing routing"
    - path: "tauri/ui/src/wizard/onboarding-stopwatch.ts"
      provides: "emitInstallReadyEvent + per-step elapsed breakdown"
    - path: "tauri/ui/src/wizard/copy.ts"
      provides: "Typed loader for onboarding_copy.json"
      min_lines: 80
    - path: "src/vibemix/install/blackhole_probe.py"
      provides: "Payload schema additive field auto_install_attempted (zero new event types)"
    - path: "tests/wizard/test_axe_a11y.test.ts"
      provides: "axe-core gate on 3 new steps + uninstall dialog"
      min_lines: 60
  key_links:
    - from: "tauri/ui/src/wizard/step-driver-fetch.ts"
      to: "installer/companion/fetch_drivers.{sh,ps1}"
      via: "Tauri shell.execute spawn + audio.probe.* event subscription"
      pattern: "wizard orchestrates companion fetch + parallel probes"
    - from: "tauri/ui/src/wizard/step-48k-probe.ts"
      to: "installer/companion/audio_config.py"
      via: "Tauri shell.execute --probe-48k + --configure-routing CLI"
      pattern: "INSTALL-10 post-install probe per memory project_v4_canonical_baseline"
    - from: "tauri/ui/src/wizard/copy.ts"
      to: "installer/companion/onboarding_copy.json"
      via: "single-source-of-truth import; copy.steps.<step>.<field>"
      pattern: "no inline strings in step components"
---

<objective>
Land the 3 new wizard steps (forewarning / driver-fetch / format-check) reading strings from `onboarding_copy.json`, orchestrating companion scripts via Tauri shell.execute, emitting `INSTALL_READY` stopwatch event, passing axe-core a11y gate. Closes INSTALL-01 (Mac DMG → wizard ≤ 60s session-ready), INSTALL-02 (Win EXE → wizard ≤ 60s), INSTALL-03 (forewarning copy), INSTALL-06 (Tauri MSI target + stopwatch), INSTALL-08 (a11y), INSTALL-10 (48 kHz probe in wizard) at the UI surface.

Out of scope: companion script bodies (Plan 01), signing chain (Plan 02), Inno Setup `[Run]` (Plan 04), VM matrix (Plan 05), anti-slop + uninstall (Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-win-mac-one-click-installer-chain/49-CONTEXT.md
@.planning/phases/49-win-mac-one-click-installer-chain/49-UI-SPEC.md
@installer/companion/onboarding_copy.json
@tauri/ui/src/tokens.css
@tauri/ui/src/wizard/router.ts
@tauri/ui/src/wizard/step0-intro.ts
@tauri/ui/src/wizard/step1-permissions.ts
@tauri/ui/src/wizard/onboarding-stopwatch.ts
@src/vibemix/install/blackhole_probe.py

<interfaces>
v3.0 wizard is a vanilla TS/HTML/CSS surface (no React). Components live as factory functions returning HTMLElement. Style registry pattern: each step calls `registerStyle('step-foo', '<css>')` at module init; CSS is injected at first-use.

`audio.probe.*` events (existing): `audio.probe.detected`, `audio.probe.missing`, `audio.probe.cta_fired`. Phase 49 adds payload field `auto_install_attempted: bool` to ALL three. Zero new event types.

Tauri shell.execute spawns companion scripts under bundle ID `world.bravoh.vibemix` (per Tauri ACL — capabilities/default.json already allows `installer/companion/*` paths in v3.0 SHIP-04). Subscribe to stdout via `Command.spawn().stdout.on('data')`.

INSTALL_READY event payload: `{ elapsed_ms: number, per_step: { permissions, output_device, controller, forewarning, driver_fetch, format_check }, auto_install_attempted: bool }`. Consumed by `scripts/dist/install_vm_matrix.sh --check-60s` (Plan 05).

Step-0 worktree-sync invariant per memory `feedback_worktree_must_sync_main_first`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: copy.ts — typed loader for onboarding_copy.json</name>
  <files>tauri/ui/src/wizard/copy.ts</files>
  <action>
    Create `tauri/ui/src/wizard/copy.ts`:
    - Imports `onboarding_copy.json` via Vite's JSON import (already supported)
    - Defines TypeScript interfaces for every section (Welcome, Forewarning, DriverFetch, FormatCheck, Uninstall)
    - Exports `copy` const typed to the interfaces
    - Adds helper `interpolate(template: string, vars: Record<string, string | number>): string` that replaces `{name}` placeholders with values
    - Adds assertion at module init that every required field is present (throws on missing; loud-fail philosophy)
    - JSDoc header documenting: "ALL wizard user-facing strings flow from `installer/companion/onboarding_copy.json` via this module. Inline strings in step components are a lint-checked anti-pattern (see test_wizard_no_inline_strings.test.ts)."

    Note: Vite must be configured to import JSON from outside the `tauri/ui/` root. Add a `vite-plugin-json5` import or use Vite's built-in JSON import with absolute path. Document in JSDoc.
  </action>
  <check>cd tauri/ui && npx tsc --noEmit src/wizard/copy.ts 2>&1 | head -20</check>
  <on_check_fail>Add Vite JSON import support; if blocked, copy onboarding_copy.json to tauri/ui/src/wizard/copy.json and import locally as fallback (document deferred unification).</on_check_fail>
</task>

<task type="auto">
  <name>Task 2: step-forewarning.ts</name>
  <files>tauri/ui/src/wizard/step-forewarning.ts</files>
  <action>
    Create `step-forewarning.ts` mirroring the shape of `step1-permissions.ts`:
    - Export factory `createStepForewarning(state, callbacks): HTMLElement`
    - Renders 2 cards (Mac BlackHole + Win VB-CABLE) — only the platform-relevant card is visible, the other is hidden via `display: none` controlled by `state.platform`
    - Reads strings from `copy.steps.forewarning.*` (zero inline string literals)
    - Cards use `PrimaryPanel` + token-only styling (zero hex literals; `--glass-1`, `--silk`, `--glass-edge`)
    - Bottom row: `[ Got it — continue ]` primary CTA + `Back` secondary CTA
    - `registerStyle('step-forewarning', '<css>')` at module init
    - Emits `wizard.forewarning_continued` event on CTA click
    - aria-labels read from copy strings for screen-reader compat
  </action>
  <check>cd tauri/ui && npx tsc --noEmit src/wizard/step-forewarning.ts 2>&1 | head -10</check>
  <on_check_fail>Type errors fix; ensure imports match existing wizard primitives (Button, PrimaryPanel).</on_check_fail>
</task>

<task type="auto">
  <name>Task 3: step-driver-fetch.ts</name>
  <files>tauri/ui/src/wizard/step-driver-fetch.ts</files>
  <action>
    Create `step-driver-fetch.ts`:
    - Export factory `createStepDriverFetch(state, callbacks): HTMLElement`
    - On mount: invokes `Command.create('fetch_drivers', [...])` via Tauri shell API targeting `installer/companion/fetch_drivers.sh` or `.ps1` based on platform
    - Subscribes to script stdout JSON lines + maps to row state (idle / fetching / verifying / installing / done)
    - In parallel, fires probes for: MIDI controller (existing `controller_probe`), TCC permissions (existing `tcc_check`), Bravoh proxy reachability (existing `proxy_ping`)
    - Renders 4-row UI (driver / MIDI / TCC / proxy) with status icon + label + state copy from `copy.steps.driver_fetch.*`
    - Bottom: stopwatch readout in JetBrains Mono (`font-family: var(--type-mono)`); reads elapsed ms from `onboarding-stopwatch.ts`
    - Color tween on stopwatch: `--silk-65` (idle) → `--amber-65` (< 50s) → `--led-warn` (50-60s) → `--led-fault` (> 60s)
    - On all-rows-done + < 60s: enables `[ Continue ]` CTA which advances to step-48k-probe
    - On fetch error (companion exits non-zero): shows fallback heading + body from copy + retry button
    - Emits `wizard.driver_fetch_complete` event with `{ elapsed_ms, auto_install_attempted: true }` payload
    - aria-live="polite" on the row container so VO/NVDA announces state changes
  </action>
  <check>cd tauri/ui && npx tsc --noEmit src/wizard/step-driver-fetch.ts 2>&1 | head -10</check>
  <on_check_fail>Type errors fix; if Tauri shell API import path uncertain, search `tauri/ui/src` for an existing Command import.</on_check_fail>
</task>

<task type="auto">
  <name>Task 4: step-48k-probe.ts</name>
  <files>tauri/ui/src/wizard/step-48k-probe.ts</files>
  <action>
    Create `step-48k-probe.ts`:
    - On mount: invokes `audio_config.py --probe-48k` via Tauri shell
    - Parses JSON stdout: `{ ok: bool, measured_khz: number, expected_khz: number, reason?: string }`
    - On ok=true: renders success card (interpolate `copy.steps.format_check.success`), border-anim sweep (one-CDJ-one-light), `[ Start your set ]` CTA dispatches `wizard.complete` event
    - On ok=false: renders FAIL card (interpolate `copy.steps.format_check.fail`, {measured_khz: state.measured_khz}), `[ Fix it for me ]` CTA invokes `audio_config.py --configure-routing` then re-probes; manual link `Open Audio MIDI Setup` / `Open Sound settings` opens Audio MIDI Setup on Mac / `mmsys.cpl` on Win via Tauri shell
    - Token-only styling; aria-labels from copy
    - Emits `wizard.format_check_passed` or `wizard.format_check_failed` event
  </action>
  <check>cd tauri/ui && npx tsc --noEmit src/wizard/step-48k-probe.ts 2>&1 | head -10</check>
  <on_check_fail>Type errors fix; ensure Tauri shell.open API used for external links not direct file paths.</on_check_fail>
</task>

<task type="auto">
  <name>Task 5: router.ts registration + back-button</name>
  <files>tauri/ui/src/wizard/router.ts</files>
  <action>
    Update `tauri/ui/src/wizard/router.ts`:
    - Add 3 new step IDs to the step enum / type: `'forewarning'`, `'driver-fetch'`, `'format-check'`
    - Register the 3 factories with the router
    - Update step ordering: intro → permissions → output-device → controller → **forewarning** → **driver-fetch** → **format-check** → telemetry-consent → profile-consent → done
    - Each new step receives `onBack` callback per Impeccable Wave 5.A back arrow pattern (existing in router.ts)
    - PRESERVE all existing routing logic byte-identical
  </action>
  <check>cd tauri/ui && npx tsc --noEmit src/wizard/router.ts 2>&1 | head -10</check>
  <on_check_fail>Type errors fix; if step ordering breaks existing tests, check tests/wizard/test_router.test.ts and update step enum consistently.</on_check_fail>
</task>

<task type="auto">
  <name>Task 6: onboarding-stopwatch.ts — emitInstallReadyEvent</name>
  <files>tauri/ui/src/wizard/onboarding-stopwatch.ts</files>
  <action>
    Extend `tauri/ui/src/wizard/onboarding-stopwatch.ts`:
    - Add `emitInstallReadyEvent(perStep: Record<string, number>, autoInstallAttempted: bool): void` function
    - Invokes Tauri's `emit` to fire `audio.probe.install_ready` event with payload `{ elapsed_ms, per_step, auto_install_attempted }`
    - elapsed_ms = sum of perStep values
    - PRESERVE existing stopwatch tick logic byte-identical
  </action>
  <check>cd tauri/ui && npx tsc --noEmit src/wizard/onboarding-stopwatch.ts 2>&1 | head -10</check>
  <on_check_fail>Type errors fix; if `emit` import path different, find existing event emitter in tauri/ui/src for consistency.</on_check_fail>
</task>

<task type="auto">
  <name>Task 7: blackhole_probe.py — additive payload field</name>
  <files>src/vibemix/install/blackhole_probe.py</files>
  <action>
    Open `src/vibemix/install/blackhole_probe.py`:
    - Add `auto_install_attempted: bool = False` to the dataclass / TypedDict / Pydantic model for `BlackHoleProbeEvent` payload
    - PRESERVE existing event types `audio.probe.detected` / `audio.probe.missing` / `audio.probe.cta_fired` byte-identical — only the payload schema gets the new optional field
    - Update docstring noting Phase 49 additive extension
  </action>
  <check>python -c "from src.vibemix.install import blackhole_probe; e = blackhole_probe.BlackHoleProbeEvent(...)" 2>&1 | head -5 ; python -m pytest tests/ -k blackhole_probe -v 2>&1 | tail -10</check>
  <on_check_fail>If model shape differs from expected (dataclass vs Pydantic), inspect existing payload definition and add field in matching style.</on_check_fail>
</task>

<task type="auto">
  <name>Task 8: smoke-test.ts axe-core extension + test_axe_a11y.test.ts</name>
  <files>tauri/ui/src/wizard/smoke-test.ts, tests/wizard/test_axe_a11y.test.ts</files>
  <action>
    Extend `tauri/ui/src/wizard/smoke-test.ts` to run axe-core on the 3 new step components + uninstall dialog (when imported standalone).

    Create `tests/wizard/test_axe_a11y.test.ts` (vitest):
    - mounts each new step in JSDOM
    - runs `axe.run(container)` from `@axe-core/playwright` or `axe-core` vanilla
    - asserts `results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious').length === 0`
    - covers: step-forewarning, step-driver-fetch (idle state), step-48k-probe (success state + FAIL state), uninstall-dialog (deferred to Plan 06 — skip with @todo comment if file absent)

    Also create `tests/wizard/test_forewarning.test.ts`, `tests/wizard/test_driver_fetch.test.ts`, `tests/wizard/test_48k_probe.test.ts` — basic mount + interaction tests (CTA click → callback fires, copy strings present, no inline string literals).
  </action>
  <check>cd tauri/ui && npm test -- tests/wizard/test_axe_a11y.test.ts 2>&1 | tail -20</check>
  <on_check_fail>Install axe-core via npm if missing (check package.json first; axe-core is small + dev-only dep — acceptable per Phase 48 Decision 9 since it's test-only); if a violation found, fix the underlying component (aria-label / focus order / contrast) — DO NOT relax the gate.</on_check_fail>
</task>

</tasks>

<verification>
- 3 new wizard step files exist + compile under `tsc --noEmit`
- `copy.ts` exists + every wizard string reads through it (zero inline string literals — assertable via AST grep)
- Zero hex literals in the 3 new step files (assertable via regex grep `#[0-9a-fA-F]{3,8}`)
- `axe-core` run on 3 new steps + uninstall dialog: zero serious + zero critical violations
- `blackhole_probe.py` payload has new optional field; existing tests pass
- `router.ts` registers 3 new step IDs with correct ordering
- `onboarding-stopwatch.ts` emits `audio.probe.install_ready` event with full payload
</verification>

<commit>
gsd-sdk query commit "feat(49): wizard 3-step surface + axe a11y + INSTALL_READY emit (INSTALL-01/02/03/06/08/10)" --files tauri/ui/src/wizard/step-forewarning.ts tauri/ui/src/wizard/step-driver-fetch.ts tauri/ui/src/wizard/step-48k-probe.ts tauri/ui/src/wizard/router.ts tauri/ui/src/wizard/onboarding-stopwatch.ts tauri/ui/src/wizard/smoke-test.ts tauri/ui/src/wizard/copy.ts src/vibemix/install/blackhole_probe.py tests/wizard/test_forewarning.test.ts tests/wizard/test_driver_fetch.test.ts tests/wizard/test_48k_probe.test.ts tests/wizard/test_axe_a11y.test.ts
</commit>
