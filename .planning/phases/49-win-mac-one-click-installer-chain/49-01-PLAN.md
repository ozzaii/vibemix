---
phase: 49-win-mac-one-click-installer-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/companion/fetch_drivers.sh
  - installer/companion/fetch_drivers.ps1
  - installer/companion/driver_manifest.json
  - installer/companion/audio_config.py
  - installer/companion/onboarding_copy.json
  - installer/companion/README.md
  - tests/install/test_driver_manifest_schema.py
  - tests/install/test_audio_config.py
autonomous: true
requirements:
  - INSTALL-04
  - INSTALL-09
  - INSTALL-10
tags:
  - installer
  - companion
  - driver-fetch
  - sha-256-verify

must_haves:
  truths:
    - "installer/companion/driver_manifest.json declares both BlackHole 2ch (Mac) + VB-CABLE (Win) entries with: vendor, version, url, sha256, license_ack_text, vendor_signed (bool). SHA-256 placeholders are 64-hex-char placeholders documented as needing real-asset population at SignPath cert discharge"
    - "installer/companion/fetch_drivers.sh (Mac) reads manifest, downloads via curl -fsSL, verifies SHA-256 against manifest, runs `installer -pkg ... -target /` via sudo; exits non-zero on any verify mismatch + emits structured log line; honors --dry-run flag for CI rehearsal"
    - "installer/companion/fetch_drivers.ps1 (Win) reads manifest, downloads via Invoke-WebRequest, verifies SHA-256 via Get-FileHash, runs vendor installer with documented silent flag where EULA permits (VB-CABLE NSIS /S) or surfaces UAC modal where it doesn't; honors --dry-run flag for CI rehearsal"
    - "installer/companion/audio_config.py post-driver-install routine: macOS Multi-Output Device via AppleScript (CoreAudio Aggregate Device, BlackHole + system output), Win default-device routing via SetDefaultAudioPlaybackDevice COM; INSTALL-10 BlackHole 48 kHz format probe wired here (reads CoreAudio AudioDeviceGetProperty(kAudioDevicePropertyNominalSampleRate) on Mac / IPropertyStore for Win), returns FAIL with measured kHz if != 48000"
    - "installer/companion/onboarding_copy.json declares every wizard string from 49-UI-SPEC.md § Copywriting Contract verbatim (Step 0 / Step 1 / Step 2 / Step 3 / uninstall). JSON schema: { \"steps\": {...}, \"forewarning\": {...}, \"driver_rows\": {...}, \"probe\": {...}, \"uninstall\": {...} }"
    - "installer/companion/README.md documents the contract: NEVER inline AIza pattern (Pitfall-7 grep gate); SHA-256 placeholders require real-asset population at SignPath discharge; offline-installer fallback documented; companion scripts spawn under bundle ID world.bravoh.vibemix"
    - "tests/install/test_driver_manifest_schema.py validates driver_manifest.json against an inline JSON schema (vendor, version, url MUST match https://existential.audio/* or https://vb-audio.com/*; sha256 MUST be 64 hex chars; vendor_signed MUST be bool)"
    - "tests/install/test_audio_config.py mocks CoreAudio + WASAPI APIs and asserts: 48000 returns OK, 44100 returns FAIL with measured_khz=44.1, missing device returns FAIL with reason='no_blackhole_device'"
    - "Zero AIza-pattern literal in any companion file (greppable test in test_audio_config.py asserts the grep gate)"
  artifacts:
    - path: "installer/companion/driver_manifest.json"
      provides: "Single source of truth for driver fetch (BlackHole + VB-CABLE; SHA-256 + URL + license-ack)"
      min_lines: 30
    - path: "installer/companion/fetch_drivers.sh"
      provides: "Mac companion fetch + verify + install (BlackHole pkg via existential.audio)"
      min_lines: 80
    - path: "installer/companion/fetch_drivers.ps1"
      provides: "Win companion fetch + verify + install (VB-CABLE via vb-audio.com)"
      min_lines: 80
    - path: "installer/companion/audio_config.py"
      provides: "Multi-Output Device / WASAPI default-device + 48 kHz probe"
      min_lines: 120
    - path: "installer/companion/onboarding_copy.json"
      provides: "All wizard strings sourced as single grep target"
      min_lines: 60
    - path: "installer/companion/README.md"
      provides: "Contract + AIza ban + SHA-256 placeholder discharge instructions"
      min_lines: 40
    - path: "tests/install/test_driver_manifest_schema.py"
      provides: "JSON schema validation + URL allowlist + grep guards"
      min_lines: 60
    - path: "tests/install/test_audio_config.py"
      provides: "48 kHz probe mocked CoreAudio/WASAPI; AIza grep gate"
      min_lines: 80
  key_links:
    - from: "installer/companion/fetch_drivers.{sh,ps1}"
      to: "installer/companion/driver_manifest.json"
      via: "read SHA-256 + URL + version at runtime"
      pattern: "manifest-driven fetch"
    - from: "installer/companion/audio_config.py"
      to: "src/vibemix/install/blackhole_probe.py"
      via: "shares 48 kHz format probe contract; v3.0 probe stays canonical, this module adds post-install config"
      pattern: "INSTALL-10 BlackHole 48 kHz post-install probe per memory project_v4_canonical_baseline"
---

<objective>
Land the four companion script primitives and their JSON contract under `installer/companion/`. This is the foundation — Plans 02 (signing), 03 (wizard UI), 04 (Inno Setup + DMG hook), 05 (VM matrix), 06 (anti-slop + uninstall) all depend on these files.

Out of scope: release.yml `companion-sign` stage (Plan 02), wizard UI integration (Plan 03), Inno Setup `[Run]` modifications (Plan 04), VM matrix harness (Plan 05), anti-slop sibling-script + uninstall flow (Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/49-win-mac-one-click-installer-chain/49-CONTEXT.md
@.planning/phases/49-win-mac-one-click-installer-chain/49-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@scripts/audit/dep_ratings.yaml
@src/vibemix/install/blackhole_probe.py

<interfaces>
The companion scripts spawn under bundle ID `world.bravoh.vibemix` (invariant). They write logs ONLY to `~/Library/Application Support/vibemix/install.log` (Mac) / `%APPDATA%\vibemix\install.log` (Win). They NEVER write to `~/.hermes/`, `~/hermes-rig/logs/`, `~/.lmstudio/` per memory `feedback_privacy_scope_narrow`.

Driver manifest entries pinned from Phase 46 AUDIT.md vendor versions + Phase 48 `opportunity_evaluations` Green-rated subset. Phase 48 confirmed BlackHole + VB-CABLE Green; opportunity scan asserts no other driver is in scope.

Step-0 worktree-sync invariant (per memory `feedback_worktree_must_sync_main_first`): if this plan executes in a worktree, the executor MUST run `git fetch origin main && git merge origin/main --no-edit` before any edits, then verify `git rev-parse origin/main == merge-base with HEAD`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: driver_manifest.json + schema test</name>
  <files>installer/companion/driver_manifest.json, tests/install/test_driver_manifest_schema.py</files>
  <action>
    Create `installer/companion/driver_manifest.json` with this exact shape (SHA-256 placeholders for now — discharge at SignPath cert time):

    ```json
    {
      "$schema": "https://github.com/ozzaii/vibemix/blob/main/installer/companion/driver_manifest.schema.json",
      "version": "1.0.0",
      "drivers": {
        "blackhole_2ch": {
          "platform": "darwin",
          "vendor": "ExistentialAudio",
          "version": "0.6.0",
          "url": "https://existential.audio/blackhole/BlackHole2ch.v0.6.0.pkg",
          "sha256": "PLACEHOLDER_REAL_SHA256_TO_BE_POPULATED_AT_SIGNPATH_DISCHARGE_64HEX",
          "vendor_signed": true,
          "license": "GPL-3.0",
          "license_ack_text": "BlackHole is licensed GPL-3.0. By installing, you agree to its terms — see https://existential.audio/blackhole/."
        },
        "vb_cable": {
          "platform": "win32",
          "vendor": "VB-Audio",
          "version": "1.0.4.3",
          "url": "https://vb-audio.com/Cable/VBCABLE_Driver_Pack43.zip",
          "sha256": "PLACEHOLDER_REAL_SHA256_TO_BE_POPULATED_AT_SIGNPATH_DISCHARGE_64HEX",
          "vendor_signed": true,
          "license": "Donationware",
          "license_ack_text": "VB-CABLE is freeware-with-attribution from VB-Audio. By installing, you agree to its terms — see https://vb-audio.com/Cable/.",
          "silent_flag": "/S"
        }
      }
    }
    ```

    Create `tests/install/test_driver_manifest_schema.py` validating:
    - JSON parses
    - both `blackhole_2ch` and `vb_cable` present
    - `url` starts with `https://existential.audio/` or `https://vb-audio.com/`
    - `sha256` is 64-char hex OR matches placeholder literal (acceptable until discharge)
    - `vendor_signed` is bool true
    - `license_ack_text` non-empty
  </action>
  <check>python -m pytest tests/install/test_driver_manifest_schema.py -v</check>
  <on_check_fail>Inspect manifest JSON syntax and URL allowlist regex; do not relax allowlist; fix manifest to match.</on_check_fail>
</task>

<task type="auto">
  <name>Task 2: fetch_drivers.sh (Mac)</name>
  <files>installer/companion/fetch_drivers.sh</files>
  <action>
    Create executable bash script:
    - `set -euo pipefail`
    - parses `--dry-run` flag
    - reads `installer/companion/driver_manifest.json` via `jq`
    - extracts `blackhole_2ch.url`, `.sha256`, `.version`
    - checks if BlackHole already installed (probe via `system_profiler SPAudioDataType` or by calling out to `audio_config.py --probe-only`); if present and version matches, exit 0 with `state=already_installed`
    - else: downloads via `curl -fsSL -o /tmp/blackhole.pkg "$URL"`
    - verifies SHA-256 via `shasum -a 256 /tmp/blackhole.pkg | awk '{print $1}'` vs manifest
    - on dry-run: skip actual install, exit 0 with `state=dry_run_complete`
    - on real-run: invoke `sudo installer -pkg /tmp/blackhole.pkg -target /` via osascript with sudo prompt
    - logs every stage to `~/Library/Application Support/vibemix/install.log` (JSONL with timestamp + stage + state)
    - cleans up `/tmp/blackhole.pkg`
    - emits structured JSON to stdout: `{"state": "...", "version": "...", "verified_sha256": "..."}`
    - placeholder SHA-256 path: when manifest sha256 starts with `PLACEHOLDER_`, log a WARNING and SKIP verification (development convenience; real builds discharge SHA-256 at SignPath time and re-run verification)
  </action>
  <check>bash -n installer/companion/fetch_drivers.sh && installer/companion/fetch_drivers.sh --dry-run 2>&1 | head -20</check>
  <on_check_fail>Syntax errors fix in-place; dry-run failures inspect log.</on_check_fail>
</task>

<task type="auto">
  <name>Task 3: fetch_drivers.ps1 (Win)</name>
  <files>installer/companion/fetch_drivers.ps1</files>
  <action>
    Create PowerShell script mirroring Task 2's contract:
    - parses `-DryRun` switch
    - reads manifest via `Get-Content | ConvertFrom-Json`
    - extracts `vb_cable.url`, `.sha256`, `.version`, `.silent_flag`
    - checks if VB-CABLE installed via registry probe `HKLM:\SOFTWARE\WOW6432Node\Microsoft\Windows\CurrentVersion\Uninstall` for `VB-Audio`; if present + version matches, exit 0 with `state=already_installed`
    - else: downloads via `Invoke-WebRequest -Uri $url -OutFile $tmp -UseBasicParsing`
    - verifies SHA-256 via `Get-FileHash -Algorithm SHA256` vs manifest (skip with WARNING when sha256 starts with `PLACEHOLDER_`)
    - on dry-run: skip actual install, exit 0
    - on real-run: extract VB-CABLE zip via `Expand-Archive`, then invoke `VBCABLE_Setup_x64.exe /S` (silent flag from manifest)
    - logs every stage to `$env:APPDATA\vibemix\install.log` (JSONL)
    - emits structured JSON stdout
  </action>
  <check>powershell.exe -NoProfile -Command "Test-Path installer/companion/fetch_drivers.ps1" 2>/dev/null || python -c "import pathlib; assert pathlib.Path('installer/companion/fetch_drivers.ps1').exists()" </check>
  <on_check_fail>If PowerShell not available on this dev host (Mac), confirm file exists and syntactically scans via shellcheck-equivalent.</on_check_fail>
</task>

<task type="auto">
  <name>Task 4: audio_config.py — routing + 48 kHz probe</name>
  <files>installer/companion/audio_config.py, tests/install/test_audio_config.py</files>
  <action>
    Create `installer/companion/audio_config.py`:
    - CLI: `python -m installer.companion.audio_config [--probe-48k | --configure-routing | --probe-only] [--platform darwin|win32]`
    - `--probe-48k`: reads BlackHole nominal sample rate via CoreAudio (Mac) / IPropertyStore (Win); returns JSON `{"ok": bool, "measured_khz": float, "expected_khz": 48.0}`; on Mac uses `pyobjc-framework-CoreAudio` if available, falls back to subprocess `system_profiler SPAudioDataType` regex parse; on Win uses `pycaw` if available, falls back to PowerShell `Get-PnpDevice` parse
    - `--configure-routing`: on Mac, creates a Multi-Output Device via AppleScript (osascript with Audio MIDI Setup automation) bundling BlackHole + current default output; on Win, sets VB-CABLE as default playback device via COM (PolicyConfig::SetDefaultEndpoint)
    - `--probe-only`: detects whether BlackHole/VB-CABLE driver is installed (no routing change)
    - all output is single-line JSON to stdout (machine-parseable)
    - logs to `~/Library/Application Support/vibemix/install.log` / `%APPDATA%\vibemix\install.log`
    - module-level constant `LOG_PATH` defined platform-aware
    - NEVER writes outside the per-OS log path; assert at module scope (raise on bad path)
    - NEVER inlines `AIza` literal (assert: `assert 'AIza' not in open(__file__).read()` in test)

    Create `tests/install/test_audio_config.py` covering:
    - `probe_48k` mocked at 48000 returns `{"ok": true, "measured_khz": 48.0}`
    - probe at 44100 returns `{"ok": false, "measured_khz": 44.1}`
    - missing device returns `{"ok": false, "reason": "no_device"}`
    - grep gate: source contains zero `AIza` literals
    - grep gate: source contains zero off-limits path strings (`.hermes`, `hermes-rig/logs`, `.lmstudio`)
    - log path resolves to `~/Library/Application Support/vibemix/install.log` on darwin, `%APPDATA%\vibemix\install.log` on win32
  </action>
  <check>python -m pytest tests/install/test_audio_config.py -v</check>
  <on_check_fail>Fix module syntax; check pyobjc/pycaw imports are optional via try/except.</on_check_fail>
</task>

<task type="auto">
  <name>Task 5: onboarding_copy.json — all wizard strings</name>
  <files>installer/companion/onboarding_copy.json</files>
  <action>
    Create `installer/companion/onboarding_copy.json` with every string from `.planning/phases/49-win-mac-one-click-installer-chain/49-UI-SPEC.md` § Copywriting Contract VERBATIM. Schema:

    ```json
    {
      "version": "1.0.0",
      "steps": {
        "welcome": { "hero_line_1": "SET UP", "hero_line_2": "VIBEMIX", "hero_line_3": "— ONE TAP, NO TERMINAL —", "primary_cta": "Let's go" },
        "forewarning": {
          "section_heading": "Two prompts you'll see",
          "mac_title": "BlackHole — system audio driver",
          "mac_body": "macOS will ask: Allow BlackHole in System Settings → Privacy & Security. This is one click — vibemix can't grant it for you, and it's the only step Apple keeps user-driven.",
          "win_title": "VB-CABLE — virtual audio cable",
          "win_body": "Windows will ask permission to install an audio driver — click Yes. The driver is signed by VB-Audio; vibemix verifies the SHA-256 before running it.",
          "continue_cta": "Got it — continue",
          "back_cta": "Back"
        },
        "driver_fetch": {
          "heading": "Setting up audio",
          "row_idle": "Checking…",
          "row_fetching": "Downloading from {vendor_host}…",
          "row_verifying": "Verifying SHA-256…",
          "row_installing": "Running vendor installer…",
          "row_done": "Installed · {version}",
          "midi_probe": "MIDI controller: optional",
          "tcc_probe": "Mic + screen permissions: ready",
          "bravoh_probe": "Bravoh proxy: reachable",
          "stopwatch": "{ms} ms",
          "fallback_heading": "BlackHole couldn't auto-install",
          "fallback_body": "Run this in Terminal, then click retry: brew install blackhole-2ch",
          "continue_cta": "Continue"
        },
        "format_check": {
          "heading": "Format check",
          "success": "BlackHole is set to 48 kHz · ready",
          "fail": "BlackHole is at {measured_khz} kHz — vibemix needs 48 kHz",
          "fix_cta": "Fix it for me",
          "mac_manual": "Open Audio MIDI Setup",
          "win_manual": "Open Sound settings",
          "final_cta": "Start your set"
        }
      },
      "uninstall": {
        "title": "Uninstall vibemix",
        "body": "Your recordings and debriefs will stay on your machine. The app, audio routing, and caches will be removed.",
        "default_cta": "Uninstall vibemix",
        "clean_opt_in": "Also remove recordings and debriefs",
        "clean_body": "Removes ~{mb} MB of recordings, debriefs, ghost calibration. This can't be undone.",
        "clean_confirm_cta": "Remove vibemix and all data",
        "cancel_cta": "Cancel"
      }
    }
    ```
  </action>
  <check>python -c "import json; d=json.load(open('installer/companion/onboarding_copy.json')); assert 'steps' in d and 'uninstall' in d; print('ok')"</check>
  <on_check_fail>JSON syntax error fix in-place.</on_check_fail>
</task>

<task type="auto">
  <name>Task 6: README.md — companion contract + AIza ban + SHA-256 discharge instructions</name>
  <files>installer/companion/README.md</files>
  <action>
    Create `installer/companion/README.md` documenting:
    - **Purpose**: post-install vendor-driver fetch + audio routing + 48 kHz probe
    - **Bundle ID**: scripts spawn under `world.bravoh.vibemix` (invariant)
    - **AIza ban**: NEVER inline AIza pattern — Pitfall-7 grep gate enforced by `tests/install/test_audio_config.py`
    - **SHA-256 placeholder discharge**: how to populate real SHA-256 at SignPath cert discharge time (run `shasum -a 256 BlackHole*.pkg`, write to manifest, re-run `tests/install/test_driver_manifest_schema.py`)
    - **Offline-installer fallback**: documented at `installer/companion/onboarding_copy.json § driver_fetch.fallback_body` — user runs `brew install blackhole-2ch` or downloads VB-CABLE manually
    - **Privacy invariant**: scripts write ONLY to `~/Library/Application Support/vibemix/install.log` / `%APPDATA%\vibemix\install.log`. NEVER `~/.hermes/`, `~/hermes-rig/logs/`, `~/.lmstudio/`
    - **Signing chain hand-off**: see `.github/workflows/release.yml § companion-sign` job (Plan 49-02 owns)
    - **Wizard UI hand-off**: see `tauri/ui/src/wizard/` (Plan 49-03 owns)
  </action>
  <check>test -s installer/companion/README.md && wc -l installer/companion/README.md</check>
  <on_check_fail>Re-run if file empty.</on_check_fail>
</task>

</tasks>

<verification>
- All 8 files exist at declared paths
- `pytest tests/install/` returns 0 failures
- `grep -r "AIza" installer/companion/` returns zero matches
- `grep -r "\\.hermes\\|hermes-rig\\|\\.lmstudio" installer/companion/` returns zero matches
- `python -m json.tool installer/companion/driver_manifest.json > /dev/null` exits 0
- `python -m json.tool installer/companion/onboarding_copy.json > /dev/null` exits 0
</verification>

<commit>
gsd-sdk query commit "feat(49): companion fetch scripts + manifest + audio config (INSTALL-04/09/10 scaffold)" --files installer/companion/fetch_drivers.sh installer/companion/fetch_drivers.ps1 installer/companion/driver_manifest.json installer/companion/audio_config.py installer/companion/onboarding_copy.json installer/companion/README.md tests/install/test_driver_manifest_schema.py tests/install/test_audio_config.py
</commit>
