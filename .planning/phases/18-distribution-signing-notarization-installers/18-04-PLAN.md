---
phase: 18-distribution-signing-notarization-installers
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - tauri/src-tauri/tauri.conf.json5
  - tauri/src-tauri/src/updater.rs
  - tauri/src-tauri/src/main.rs
  - tauri/src-tauri/src/config.rs
  - tauri/src-tauri/keys/.gitkeep
  - tauri/src-tauri/keys/README.md
  - docs/updater.md
autonomous: true
requirements:
  - DIST-06
tags: [dist, updater, tauri, opt-out, manifest, minisign]
must_haves:
  truths:
    - "Tauri's updater plugin is configured `active: true` with a real `endpoints` URL pointing at `https://api.altidus.world/vibemix/updates/{{target}}/{{arch}}/{{current_version}}` and a non-empty `pubkey` (real minisign public key OR a placeholder constant of the documented shape with a TODO calling Plan 18-05 to populate via `TAURI_UPDATER_PUBLIC_KEY`)"
    - "A Rust-side `update_check_on_launch: bool` flag is persisted in `tauri-plugin-store` under `config.json` with default `true`; user opt-out at the Settings layer flips it; the boot-time updater fire-and-forget honours it (false ⇒ no fetch)"
    - "`tauri-plugin-updater` capability identifier (`updater:default`) is present in `tauri/src-tauri/capabilities/default.json` (it already is from Phase 11 W2 — this plan verifies non-regression, not addition)"
    - "`docs/updater.md` documents the manifest endpoint shape (200 / 204 contract + signed `latest.json` JSON keys), the rollback recipe, key generation procedure (`npx @tauri-apps/cli signer generate -w …` NEVER committed), and the boot-time check_on_launch opt-out"
    - "Zero new pip / npm deps; no new Rust crates beyond what Cargo.toml already pins (tauri-plugin-updater 2.10 is on disk since Phase 11 W2)"
    - "POC files (cohost*.py, mascot.html, mocks/) diff-untouched"
  artifacts:
    - path: tauri/src-tauri/tauri.conf.json5
      provides: "Replaces the Phase 11 STUB stanza (active:false, endpoints:[], pubkey:\"\") with active:true + real endpoint + populated pubkey"
      contains: "active: true, endpoints: [\"https://api.altidus.world/vibemix/updates/{{target}}/{{arch}}/{{current_version}}\"], pubkey: \"<base64-minisign-public-key>\", dialog: true"
    - path: tauri/src-tauri/src/updater.rs
      provides: "Boot-time updater fire-and-forget — reads update_check_on_launch from tauri-plugin-store, dispatches an async fetch + download + install prompt via the plugin's UpdaterExt; default true; respects user opt-out"
      contains: "fn check_on_launch_enabled, async fn run_update_check_if_enabled, UpdaterExt::updater"
      min_lines: 80
    - path: docs/updater.md
      provides: "End-user + Kaan documentation for the manifest endpoint, key generation, opt-out, and rollback recipe"
      min_lines: 100
    - path: tauri/src-tauri/keys/README.md
      provides: "Procedure for generating + storing the Tauri updater minisign key pair OUTSIDE the repo (the .key + .key.pub files NEVER get committed; only the base64-encoded public half is pasted into tauri.conf.json5)"
      min_lines: 25
  key_links:
    - from: tauri/src-tauri/tauri.conf.json5
      to: api.altidus.world
      via: "plugins.updater.endpoints[0] URL"
      pattern: "api\\.altidus\\.world/vibemix/updates"
    - from: tauri/src-tauri/src/updater.rs
      to: tauri/src-tauri/src/config.rs
      via: "reads update_check_on_launch via tauri-plugin-store"
      pattern: "update_check_on_launch"
    - from: tauri/src-tauri/src/main.rs
      to: tauri/src-tauri/src/updater.rs
      via: "spawn(updater::run_update_check_if_enabled(app_handle))"
      pattern: "updater::"
---

<objective>
Replace the Phase 11 W2 updater **stub** in `tauri.conf.json5` with a real,
active, signed-manifest auto-updater wired to `api.altidus.world/vibemix/updates/`.
Add a Rust-side `update_check_on_launch` opt-out bool that defaults to `true`
and is honoured by the boot-time updater fire-and-forget so users who never
want auto-update prompts can turn it off via Settings (UI surface for the
toggle is a small Phase 12 Settings drawer extension — deferred to Plan 18-04
follow-up OR Phase 19 polish, since CONTEXT D-Area-4 explicitly says: "Plan
18-04 includes a 1-task subtask to wire this if Phase 12 didn't already;
otherwise marked DEFERRED"). Phase 12 did NOT wire it — `grep -rn
"update_check_on_launch\|check_for_update\|updater" src/vibemix/ tauri/ui/src/`
returns zero hits. This plan ships the **back-end primitive** (config key +
default + Rust boot-time read); the **UI toggle in the Settings drawer** is
deferred to a Phase 19 polish item (one row in PerformanceGroup or a new
UpdateGroup; tracked in `STATE.md` Open To-dos by the SUMMARY).

The plan does NOT add a new IPC settings field — that would require schema
changes + dataclass changes + codegen rerun + drift-gate regeneration, which
is disproportionate for a single bool. Instead, the Rust shell reads/writes
`update_check_on_launch` DIRECTLY via `tauri-plugin-store` to the same
`config.json` file the Python sidecar reads (top-level key, additive — the
sidecar's `_PHASE12_FIELDS` allowlist already preserves unknown top-level
keys per `config_store.py` lines 14-22 "both sides preserve unknown top-level
keys on round-trip"). When a Settings-drawer toggle ships in Phase 19 it
calls a new `core:store:default` write through the existing
`tauri-plugin-store` permission; no new capability needed.

Purpose:
- DIST-06 satisfied: tag-pushed releases trigger update prompts on running
  installs (Plan 18-05 publishes the manifest; this plan teaches the Tauri
  shell to fetch + verify + install).
- Anti-anti-AI-friction: signed updater is the OSS-standard distribution
  trust signal (Sentry-equivalent for desktop apps).
- Future-proof rollback: signed manifest endpoint + tag-deletion recipe gives
  Kaan a one-line "republish v0.1.4 manifest as latest" rollback without a
  client-side push (deferred-rollback-automation per CONTEXT §deferred).

Output:
- tauri.conf.json5 updater stanza flipped from STUB to live config
- Rust `updater.rs` module with boot-time check + opt-out plumbing
- main.rs wired to call updater on setup
- docs/updater.md as the manifest contract reference
- keys/README.md as the key-generation playbook
- POC files diff-untouched; zero new deps
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-distribution-signing-notarization-installers/18-CONTEXT.md
@.planning/phases/11-tauri-shell-calibration-wizard/11-SUMMARY.md
@.planning/phases/05-fastapi-proxy-install-uuid-jwt/05-SUMMARY.md
@tauri/src-tauri/tauri.conf.json5
@tauri/src-tauri/capabilities/default.json
@tauri/src-tauri/Cargo.toml
@tauri/src-tauri/src/main.rs
@tauri/src-tauri/src/config.rs
@src/vibemix/runtime/config_store.py

<interfaces>
<!-- The updater plugin is already pinned in Cargo.toml at version 2.10 and
     registered in main.rs at line 47 (`.plugin(tauri_plugin_updater::Builder
     ::new().build())`). The capability allowlist at
     capabilities/default.json line 16 already grants `updater:default`. The
     only thing missing is real config + a boot-time check call. Plan 18-04
     does NOT add new dependencies — it activates dormant infrastructure. -->

From tauri/src-tauri/tauri.conf.json5 (lines 109-122) — the STUB to replace:
```
"plugins": {
  "updater": {
    "active": false,
    "endpoints": [],
    "pubkey": "",
    "dialog": false
  },
  ...
}
```

From tauri/src-tauri/Cargo.toml line 34 — the plugin is on disk:
`tauri-plugin-updater = "2.10"  # STUBBED in Phase 11`

From tauri/src-tauri/capabilities/default.json line 16:
`"updater:default"` permission already granted (covers the plugin's
default-allowlisted commands: `plugin:updater|check`, `plugin:updater|download_and_install`).

From tauri/src-tauri/src/main.rs lines 47 + 77-150:
- `.plugin(tauri_plugin_updater::Builder::new().build())` already registered
- `.setup(|app| { ... })` is where the boot-time fire-and-forget will live
- `tauri::async_runtime::spawn(async move { ... })` is the established
  pattern for non-blocking boot work (sidecar supervisor lines 96-103, ws
  client lines 106-109)

From tauri/src-tauri/src/config.rs lines 21-26 — tauri-plugin-store wrapper:
- `STORE_PATH: &str = "config.json"` — file the Python sidecar's ConfigStore
  also reads (per src/vibemix/runtime/config_store.py header lines 14-22)
- Pattern: `Serialize/Deserialize` struct, `read_<key>` + `write_<key>` Tauri
  commands. Plan 18-04 follows this pattern for `update_check_on_launch`,
  but exposes NO commands (read happens internally during boot; user write
  comes from a future Phase 19 Settings UI calling
  `tauri-plugin-store`'s built-in `set` command via the existing
  `store:default` permission — no new app command needed).

From src/vibemix/runtime/config_store.py (lines 14-22, 80-87 + 168-205) —
the sidecar's view of `config.json`:
- Both sides "preserve unknown top-level keys on round-trip so neither
  stomps the other"
- `update_check_on_launch: bool` will be a new top-level key OWNED by the
  Rust shell; the sidecar's `_PHASE11_FIELDS` and `_PHASE12_FIELDS`
  allowlists do not need updating because the merge-preserve behaviour is
  unrestricted on unknown keys

From .planning/phases/18-distribution-signing-notarization-installers/18-CONTEXT.md §Area 4:
- `endpoints`: `https://api.altidus.world/vibemix/updates/{{target}}/{{arch}}/{{current_version}}`
- `pubkey`: base64 of the public half of `TAURI_UPDATER_PRIVATE_KEY`. Committed to git (public).
- `dialog: true` — standard updater prompt
- Server returns 204 if current is latest, 200 with `{ url, version, notes, pub_date, signature }` if update available

From .planning/phases/05-fastapi-proxy-install-uuid-jwt/05-SUMMARY.md:
- `api.altidus.world` runs the Bravoh FastAPI proxy on `:8443` behind nginx.
- New `/vibemix/updates/{target}/{arch}/{current_version}` endpoint is
  conceptually a sibling of the existing JWT-protected upstream paths but
  this plan does NOT touch the proxy crate — the Bravoh-side endpoint is
  out-of-scope per CONTEXT §domain "What CAN be shipped autonomously"
  excludes "Actually serving the manifest on api.altidus.world (requires
  Kaan's Bravoh proxy deployment)". Plan 18-05's CI workflow `curl POST`s
  the signed manifest to the Bravoh proxy endpoint when (if) it ships;
  until then, the updater's GET will receive a 404 in production. That's
  OK for v0.1.0: the updater simply prints "no update available" / "update
  server unreachable" and the user keeps the version they have.

The Tauri updater config keys we MUST set, per tauri-plugin-updater 2.10
config schema (https://docs.rs/tauri-plugin-updater/2.10.1/tauri_plugin_updater/):
- `active: bool` — enable the plugin
- `endpoints: Vec<String>` — URL templates with `{{target}}`, `{{arch}}`,
  `{{current_version}}` placeholders
- `pubkey: String` — base64-encoded minisign public key (NOT the URL —
  the manifest's `signature` field is what gets verified against this)
- `dialog: bool` — show built-in update prompt vs developer-driven UI
- `windows`: per-platform installer hints (NSIS args, etc.) — optional;
  not configured here (CI's MSI handles silent install)
</interfaces>

<key_generation_procedure>
<!-- The TAURI_UPDATER_PRIVATE_KEY is generated ONCE per project on Kaan's
     local machine. The .key file is stored OUTSIDE the repo (e.g.
     ~/.tauri/vibemix_updater.key); the base64 of the .key.pub file is
     committed to tauri.conf.json5; the .key file's base64 is stored as
     GitHub repo secret TAURI_UPDATER_PRIVATE_KEY; the passphrase for the
     .key (set during generation) is stored as TAURI_UPDATER_KEY_PASSWORD.

     Plan 18-04 does NOT actually run `tauri signer generate` — that
     requires Kaan's machine + npx + storing the .key locally + pasting
     the .key.pub into tauri.conf.json5 manually. Instead, this plan:
       (a) Ships the PROCEDURE in tauri/src-tauri/keys/README.md
       (b) Ships a placeholder pubkey in tauri.conf.json5 with a clearly
           marked TODO calling Kaan to replace before the v0.1.0 tag
       (c) Plan 18-05's release.yml refuses to build with the placeholder
           still in place (grep gate on the placeholder sentinel string).
     Rationale: the .key.pub belongs in tauri.conf.json5 as plain text
     (it's PUBLIC), but generating it requires npx + Kaan's manual key
     storage — out of scope for an autonomous plan.

     Placeholder sentinel that Plan 18-05 will check for:
       `dW50cnVzdGVkIGNvbW1lbnQ6IFRBVVJJX1VQREFURVJfUExBQ0VIT0xERVI=`
       (base64 of "untrusted comment: TAURI_UPDATER_PLACEHOLDER") —
       a syntactically-valid base64 string that is NEVER a real minisign
       public key (real keys are 56 bytes after the comment block). The
       placeholder is documented in tauri.conf.json5 with a TODO and
       in keys/README.md as "the value to replace on Kaan's first
       v0.1.0 prep run."
-->
</key_generation_procedure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tauri.conf.json5 updater stanza + ship placeholder pubkey + keys/README.md</name>
  <files>tauri/src-tauri/tauri.conf.json5, tauri/src-tauri/keys/.gitkeep, tauri/src-tauri/keys/README.md</files>
  <read_first>
    - tauri/src-tauri/tauri.conf.json5 (lines 109-122 — the STUB stanza to replace; preserve every other field byte-for-byte)
    - .planning/phases/18-distribution-signing-notarization-installers/18-CONTEXT.md §Area 4 (endpoints / pubkey / dialog locked decisions)
    - https://v2.tauri.app/plugin/updater/#configuration — Tauri 2.x updater config schema reference (already verified at Cargo.toml plugin pin)
    - tauri/src-tauri/Cargo.toml line 34 (plugin version 2.10 — config schema corresponds)
  </read_first>
  <action>
    Edit `tauri/src-tauri/tauri.conf.json5` lines 109-122 to replace the
    Phase 11 STUB stanza with the live config per D-Area-4. KEEP all other
    fields in the file BYTE-IDENTICAL — this is a surgical edit; the only
    change is the contents of `plugins.updater.*`.

    The new updater block must read (JSON5 — comments + trailing commas allowed):

      "updater": {
        // Phase 18 Plan 18-04 — live updater config.
        //
        // `active: true` enables the plugin's plugin:updater|check command
        // (capability already granted via `updater:default` in
        // capabilities/default.json line 16).
        //
        // `endpoints` URL template — `{{target}}` becomes `darwin`/`windows`,
        // `{{arch}}` becomes `aarch64`/`x86_64`, `{{current_version}}`
        // becomes the running app's version. The server returns 204
        // No Content if the running version is the latest; 200 with a
        // `{ version, url, signature, notes, pub_date }` JSON envelope
        // if an update is available. See docs/updater.md for the full
        // contract.
        //
        // `pubkey` — base64-encoded minisign public key. CURRENT VALUE
        // IS A PLACEHOLDER. Kaan must replace before tagging v0.1.0 by:
        //   1. `npx @tauri-apps/cli signer generate -w ~/.tauri/vibemix_updater.key`
        //   2. `cat ~/.tauri/vibemix_updater.key.pub | base64`
        //   3. Paste the result here, overwriting the placeholder.
        //   4. Store the matching private-key file's base64 contents
        //      as GitHub secret TAURI_UPDATER_PRIVATE_KEY and the
        //      passphrase as TAURI_UPDATER_KEY_PASSWORD (Plan 18-05).
        // Plan 18-05's release.yml refuses to build while the placeholder
        // string `TAURI_UPDATER_PLACEHOLDER` remains in this file.
        //
        // `dialog: true` — built-in update prompt (standard Tauri UX:
        // notify user, show changelog from `notes`, ask "Update now?",
        // show progress bar, restart). Update prompts are gated by
        // `update_check_on_launch` (default true) stored in
        // tauri-plugin-store; opt-out flips it to false.
        "active": true,
        "endpoints": [
          "https://api.altidus.world/vibemix/updates/{{target}}/{{arch}}/{{current_version}}"
        ],
        "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IFRBVVJJX1VQREFURVJfUExBQ0VIT0xERVI=",
        "dialog": true
      }

    Then create the `keys/` directory:

    `tauri/src-tauri/keys/.gitkeep`: empty file (preserves the directory
    in git for the README.md neighbour).

    `tauri/src-tauri/keys/README.md` (≥25 lines): Key generation playbook
    for Kaan. Required sections:

    # vibemix — Tauri Updater Key Generation

    > ONE-TIME setup, run on Kaan's local machine before the v0.1.0 release tag.
    > The private key NEVER gets committed. Only the PUBLIC half (base64-encoded)
    > goes into `tauri/src-tauri/tauri.conf.json5` as `plugins.updater.pubkey`.

    ## What lives here

    NOTHING. The `keys/` directory exists so this README sits next to the
    crate root for discoverability. The actual `.key` + `.key.pub` files
    live OUTSIDE the repo at `~/.tauri/vibemix_updater.key{,.pub}` (or
    Kaan's preferred path). The `.gitkeep` is what holds this empty
    directory in git.

    ## Generation procedure

    ```
    # 1. Generate the keypair (interactive — prompts for a passphrase).
    npx @tauri-apps/cli signer generate -w ~/.tauri/vibemix_updater.key

    # 2. Verify outputs.
    ls -la ~/.tauri/vibemix_updater.key ~/.tauri/vibemix_updater.key.pub

    # 3. Extract the public half as base64 for tauri.conf.json5.
    cat ~/.tauri/vibemix_updater.key.pub | base64 | tr -d '\n'

    # 4. Paste the result into tauri/src-tauri/tauri.conf.json5,
    #    replacing the TAURI_UPDATER_PLACEHOLDER sentinel.
    ```

    ## CI / GitHub secrets to set (Plan 18-05)

    | Secret | Value source |
    |--------|--------------|
    | `TAURI_UPDATER_PRIVATE_KEY` | `cat ~/.tauri/vibemix_updater.key \| base64 \| tr -d '\\n'` |
    | `TAURI_UPDATER_KEY_PASSWORD` | The passphrase chosen at step 1 |

    ## Rotating the key

    If the private key is ever compromised:
      1. Generate a new keypair.
      2. Update `tauri.conf.json5` `pubkey` to the new public half.
      3. Update GitHub secrets to the new private key + passphrase.
      4. Cut a new release. Existing installs WILL NOT auto-update to it
         (signature verification fails against the OLD pubkey baked into
         their installed binary). Communicate the issue + ask users to
         re-download from GitHub Releases manually.

    This is the same trade-off as any signed-updater scheme. Document the
    incident in `.planning/STATE.md` Open To-dos when rotating.

    ## Why we use minisign

    Tauri 2.x updater uses `minisign-verify` for manifest signature
    checking (see Cargo.lock for `minisign_verify`). Minisign is a small,
    audited, OpenBSD-derived signing tool; the `.key` file is Ed25519-based.

    Also add `tauri/src-tauri/keys/` to `.gitignore` for any `*.key` and
    `*.key.pub` files (defense-in-depth — the keys SHOULD never live here,
    but if Kaan ever accidentally drops a copy of the keypair into the
    directory, git refuses to track it).

    Add to `.gitignore` at repo root (append, do not rewrite the file):
      ```
      # Phase 18 Plan 18-04 — Tauri updater keys never live in repo.
      tauri/src-tauri/keys/*.key
      tauri/src-tauri/keys/*.key.pub
      ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('/Users/ozai/projects/dj-set-ai/tauri/src-tauri/tauri.conf.json5','utf8');const s=c.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm,'').replace(/,(\s*[}\]])/g,'$1');const j=JSON.parse(s);const u=j.plugins.updater;if(u.active!==true)throw new Error('updater not active');if(!u.endpoints[0].includes('api.altidus.world/vibemix/updates'))throw new Error('endpoint missing');if(!u.pubkey||u.pubkey==='')throw new Error('pubkey empty');if(u.dialog!==true)throw new Error('dialog not true');console.log('updater config valid:', JSON.stringify(u,null,2));" && test -f /Users/ozai/projects/dj-set-ai/tauri/src-tauri/keys/README.md && test -f /Users/ozai/projects/dj-set-ai/tauri/src-tauri/keys/.gitkeep && grep -q 'TAURI_UPDATER_PLACEHOLDER\|tauri-apps/cli signer generate' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/keys/README.md && grep -q 'tauri/src-tauri/keys/\*\.key' /Users/ozai/projects/dj-set-ai/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `tauri/src-tauri/tauri.conf.json5` `plugins.updater.active` is `true`
    - `plugins.updater.endpoints[0]` contains literal `api.altidus.world/vibemix/updates`
    - `plugins.updater.pubkey` is non-empty (the placeholder counts — empty string would crash Tauri builder at startup per RESEARCH note)
    - `plugins.updater.dialog` is `true`
    - The placeholder pubkey value is the documented sentinel string `dW50cnVzdGVkIGNvbW1lbnQ6IFRBVVJJX1VQREFURVJfUExBQ0VIT0xERVI=` so Plan 18-05's grep gate can detect "not yet replaced"
    - Every other top-level field in tauri.conf.json5 byte-identical to pre-edit state (verify by reading the file before + after; use `git diff --stat` after the edit — exactly 1 file changed)
    - `tauri/src-tauri/keys/.gitkeep` exists
    - `tauri/src-tauri/keys/README.md` ≥25 lines, contains `npx @tauri-apps/cli signer generate`, contains `TAURI_UPDATER_PRIVATE_KEY`, contains `TAURI_UPDATER_KEY_PASSWORD`, contains the `TAURI_UPDATER_PLACEHOLDER` sentinel reference
    - `.gitignore` appended with `tauri/src-tauri/keys/*.key` + `tauri/src-tauri/keys/*.key.pub` entries
  </acceptance_criteria>
  <done>Updater stanza is live; placeholder + procedure documented; keys never committable.</done>
</task>

<task type="auto">
  <name>Task 2: Add Rust updater.rs module + wire boot-time check_on_launch in main.rs + config.rs accessor</name>
  <files>tauri/src-tauri/src/updater.rs, tauri/src-tauri/src/main.rs, tauri/src-tauri/src/config.rs</files>
  <read_first>
    - tauri/src-tauri/src/main.rs (full file — the `.setup` closure structure + `tauri::async_runtime::spawn` pattern at lines 96-109)
    - tauri/src-tauri/src/config.rs (full file — the `tauri-plugin-store` wrapper pattern: `STORE_PATH = "config.json"`, `KEY_*` constants, `read_<key>` / `write_<key>` functions using `app.store(STORE_PATH)?.get(KEY)`)
    - tauri/src-tauri/Cargo.toml (verify `tauri-plugin-updater = "2.10"` + `tauri-plugin-store = "2.4"` already pinned — no new crates needed)
    - https://docs.rs/tauri-plugin-updater/2.10.1/tauri_plugin_updater/trait.UpdaterExt.html — `UpdaterExt::updater()` returns an Updater builder; `.check().await` returns `Result<Option<Update>>`; `.download_and_install(progress_cb, ready_cb).await` runs the install. We use the `dialog: true` config so the plugin handles UX; this module just gates the .check() call on the user opt-out.
  </read_first>
  <action>
    Create `tauri/src-tauri/src/updater.rs` (~80-120 lines). Top-of-file
    SPDX-License-Identifier: Apache-2.0 banner + module docstring
    describing:
      - Phase 18 Plan 18-04 — Tauri updater boot-time fire-and-forget
      - Reads `update_check_on_launch` from `tauri-plugin-store`'s
        `config.json` (top-level bool key, default true)
      - If true: spawns a tokio task that calls
        `app.updater()?.check().await` — the plugin's `dialog: true`
        config handles the UX from there
      - If false: logs `update check skipped (user opt-out)` and exits
      - On error (network unreachable, manifest 404, signature
        mismatch): logs at WARN level and exits without UI

    Module contents:
      - `use tauri::{AppHandle, Manager};`
      - `use tauri_plugin_updater::UpdaterExt;`
      - `use tauri_plugin_store::StoreExt;` (for reading the
        update_check_on_launch key — `StoreExt::store(path)` is the
        established 2.4 API used in config.rs)
      - `const KEY_UPDATE_CHECK_ON_LAUNCH: &str = "update_check_on_launch";`
      - `const STORE_PATH: &str = "config.json";` (mirrors config.rs)
      - `pub fn check_on_launch_enabled(app: &AppHandle) -> bool` — reads
        the bool from the store; returns `true` if absent (default ON);
        returns `false` if present and `false`; logs at debug level on
        read error (return `true` so opt-out failure doesn't silently
        disable updates).
      - `pub async fn run_update_check_if_enabled(app: AppHandle)`:
        early return if `!check_on_launch_enabled(&app)` with a
        `tracing::info!("updater: skipped (user opt-out)")`. Otherwise
        call `let updater = app.updater().map_err(|e| ...)?` then
        `match updater.check().await { Ok(Some(update)) => { ... let _ =
        update.download_and_install(|_, _| {}, || {}).await; } Ok(None)
        => tracing::info!("updater: no update available"), Err(e) =>
        tracing::warn!("updater: check failed: {e}") }`. The `dialog:
        true` plugin config means the user sees the standard prompt;
        this function's job is just gate + dispatch + structured logging.
      - Wrap the entire async body in `tokio::task::spawn` semantics
        when called from main.rs (consistent with the sidecar +
        ws_client spawn pattern at main.rs lines 96-109).

    Edit `tauri/src-tauri/src/main.rs`:
      - Add `mod updater;` at the module declarations near the top of the
        file (alongside the existing `mod config;`, `mod sidecar;`, etc.
        declarations — find them and insert in lexical order).
      - Inside `.setup(|app| { ... })`, AFTER the `tray::install_tray_state_listener(&app_handle);`
        call near line 148 (so the updater fire happens AFTER tray + ws
        client + sidecar are already spawned — non-critical path, lowest
        priority of all boot tasks), insert:
          ```rust
          // Phase 18 Plan 18-04 — Tauri updater boot-time fire-and-forget.
          // Gated by `update_check_on_launch` (default true); when true the
          // plugin's `dialog: true` config drives the prompt UX. Failures
          // are logged but never bail setup — the user keeps the version
          // they have if the manifest server is unreachable.
          let updater_app = app_handle.clone();
          tauri::async_runtime::spawn(async move {
              updater::run_update_check_if_enabled(updater_app).await;
          });
          ```
        Preserve every other line in main.rs byte-for-byte.

    Edit `tauri/src-tauri/src/config.rs`:
      - Add a new constant near the existing `KEY_*` consts:
          ```rust
          /// Phase 18 Plan 18-04 — bool. Default `true`. When `false`,
          /// `updater::run_update_check_if_enabled` returns early without
          /// hitting the manifest endpoint. The Settings UI surface for
          /// flipping this is deferred to Phase 19 polish (one row in
          /// the existing PerformanceGroup or a new UpdateGroup).
          pub const KEY_UPDATE_CHECK_ON_LAUNCH: &str = "update_check_on_launch";
          ```
      - DO NOT add new pub fn `read_update_check_on_launch` / `write_*`
        wrappers — the Rust-side reader lives in `updater.rs` (single
        consumer); a future Settings UI calls `tauri-plugin-store`'s
        built-in `set` command directly through the existing
        `store:default` capability permission (no new app command). The
        constant in config.rs is purely a single-source-of-truth for the
        key string so updater.rs + future Settings UI both reference
        `config::KEY_UPDATE_CHECK_ON_LAUNCH` rather than two stringly-
        typed literals that could drift.

    Compile-validate: `cd tauri/src-tauri && cargo check --frozen` must
    exit 0. (CI's `cargo build --release` will run downstream.)
  </action>
  <verify>
    <automated>test -f /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/updater.rs && grep -q 'pub fn check_on_launch_enabled' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/updater.rs && grep -q 'pub async fn run_update_check_if_enabled' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/updater.rs && grep -q 'UpdaterExt' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/updater.rs && grep -q '^mod updater;' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/main.rs && grep -q 'updater::run_update_check_if_enabled' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/main.rs && grep -q 'KEY_UPDATE_CHECK_ON_LAUNCH' /Users/ozai/projects/dj-set-ai/tauri/src-tauri/src/config.rs && cd /Users/ozai/projects/dj-set-ai/tauri/src-tauri && cargo check --frozen --offline 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `tauri/src-tauri/src/updater.rs` exists, ≥80 lines
    - Contains `use tauri_plugin_updater::UpdaterExt;`
    - Contains `use tauri_plugin_store::StoreExt;`
    - Defines `pub fn check_on_launch_enabled(app: &AppHandle) -> bool` that returns `true` on absent / read-error (default-ON is the locked invariant per CONTEXT)
    - Defines `pub async fn run_update_check_if_enabled(app: AppHandle)`
    - Module-level constant `const KEY_UPDATE_CHECK_ON_LAUNCH: &str = "update_check_on_launch";` OR imports it from config.rs
    - `tauri/src-tauri/src/main.rs` contains `mod updater;` declaration
    - `tauri/src-tauri/src/main.rs` `.setup` closure spawns `updater::run_update_check_if_enabled(updater_app)` via `tauri::async_runtime::spawn`
    - `tauri/src-tauri/src/config.rs` exports `pub const KEY_UPDATE_CHECK_ON_LAUNCH: &str = "update_check_on_launch";`
    - `cargo check --frozen` in `tauri/src-tauri/` exits 0
    - No new entries in `Cargo.toml` `[dependencies]` (the plugin + store crates are already pinned)
    - No new top-level keys in `capabilities/default.json` (the `updater:default` permission already covers the plugin's commands; the store key read uses `store:default` which is already granted)
  </acceptance_criteria>
  <done>Rust shell honours update_check_on_launch on boot; cargo check green; no new deps.</done>
</task>

<task type="auto">
  <name>Task 3: docs/updater.md — manifest contract + rollback recipe + opt-out documentation</name>
  <files>docs/updater.md</files>
  <read_first>
    - tauri/src-tauri/tauri.conf.json5 (just edited; the live endpoint URL + pubkey placement)
    - tauri/src-tauri/src/updater.rs (just written; the boot-time fire-and-forget semantics)
    - tauri/src-tauri/keys/README.md (just written; the key-gen procedure to cross-reference)
    - .planning/phases/18-distribution-signing-notarization-installers/18-CONTEXT.md §Area 4 (manifest contract spec) + §deferred (rollback automation explicitly deferred to v2)
    - docs/signing-macos.md / docs/signing-windows.md (sibling Phase 18 docs — style + rollback section parity)
    - .planning/phases/05-fastapi-proxy-install-uuid-jwt/05-SUMMARY.md (api.altidus.world infra + how Plan 18-05 will POST manifests there)
  </read_first>
  <action>
    Write `docs/updater.md` (≥100 lines). Required sections:

    # vibemix — Auto-Updater Reference

    > Tauri's built-in updater plugin (`tauri-plugin-updater` 2.10) checks
    > a signed manifest at every launch (if the user has not opted out)
    > and prompts to install if a newer signed binary is available. This
    > doc explains the manifest contract, how Kaan publishes a new release,
    > and how to roll back a bad release.

    ## How it works (user perspective)

    1. App launches.
    2. Within ~2s of boot, the Tauri shell fires a `GET` against
       `https://api.altidus.world/vibemix/updates/<target>/<arch>/<current_version>`.
    3. Server responds `204 No Content` (you're on the latest) — no UI.
    4. Or server responds `200 OK` with a signed manifest — Tauri shows a
       dialog: "Update available: vibemix 0.1.4 (release notes). Install
       now?".
    5. On Yes: download → verify signature against `pubkey` baked into
       the running app → install → restart. On No: dismissed for this
       launch; check happens again next launch.

    ## Opt-out

    `update_check_on_launch` (bool, default `true`) lives in the OS-standard
    config file (`~/Library/Application Support/vibemix/config.json` on
    macOS, `%APPDATA%\vibemix\config.json` on Windows). Set to `false` to
    disable boot-time checking. The Phase 19 Settings drawer is expected to
    surface a toggle for this; meanwhile, end users can edit the file
    manually:

    ```json
    {
      "first_run_completed": true,
      "voice": "kore",
      "...": "...",
      "update_check_on_launch": false
    }
    ```

    Save → restart app → no more update prompts.

    ## Manifest endpoint contract

    `GET /vibemix/updates/{target}/{arch}/{current_version}` on
    `api.altidus.world`.

    | Path param | Possible values |
    |------------|-----------------|
    | `target` | `darwin`, `windows` |
    | `arch` | `aarch64`, `x86_64` |
    | `current_version` | running app's semver, e.g. `0.1.0` |

    Server logic:
      - Look up the latest signed manifest for `(target, arch)`.
      - If `latest.version <= current_version`: respond `204`.
      - Else respond `200 OK` with the signed manifest JSON:
        ```json
        {
          "version": "0.1.4",
          "url": "https://github.com/<owner>/vibemix/releases/download/v0.1.4/vibemix-0.1.4-aarch64-apple-darwin.dmg",
          "signature": "<minisign-signature-base64-of-the-dmg>",
          "notes": "Fixes mic gating regression. Improves Windows DDJ-FLX4 hot-plug.",
          "pub_date": "2026-06-15T18:00:00Z"
        }
        ```
      - `signature` is the minisign signature of the `url` payload, signed
        with the private half of the keypair documented in
        `tauri/src-tauri/keys/README.md`. Tauri verifies the signature
        against the `pubkey` baked into the running app (committed to
        `tauri.conf.json5` at build time). Mismatched / forged signatures
        are rejected silently and the running app keeps its version.

    ## Publishing a release (Plan 18-05)

    The `.github/workflows/release.yml` workflow does the following on `v*`
    tag push:

      1. Build + sign + notarize + DMG / MSI (Plans 18-01..03).
      2. Upload the signed DMG / MSI to the GitHub Release at the matching
         tag.
      3. Sign the manifest with `npx @tauri-apps/cli signer sign` (using
         `TAURI_UPDATER_PRIVATE_KEY` + `TAURI_UPDATER_KEY_PASSWORD` from
         GitHub secrets).
      4. `curl POST` the signed manifest to a Bravoh-proxy endpoint on
         `api.altidus.world` (TODO: endpoint not yet implemented on the
         proxy side — out of scope for Phase 18; Bravoh ops adds the
         endpoint in a separate hand-off).

    Until the Bravoh proxy endpoint ships, the updater will receive HTTP
    `404 Not Found` and silently keep the running version. This is OK for
    v0.1.0 — manual `https://github.com/<owner>/vibemix/releases` downloads
    are the primary acquisition path.

    ## Rolling back a bad release

    Per CONTEXT §deferred, full rollback automation is v2. For v1, the
    manual procedure:

      1. Identify the bad tag (e.g. `v0.1.4`).
      2. Delete the GitHub Release + tag:
         ```
         gh release delete v0.1.4 --yes
         git tag -d v0.1.4
         git push origin :refs/tags/v0.1.4
         ```
      3. Republish the previous release's manifest as `latest` on the
         api.altidus.world side (procedure owned by Bravoh ops; for now
         this means re-tagging `v0.1.3` after deleting v0.1.4 from the
         proxy's manifest cache).
      4. Communicate the rollback in the GitHub Releases page (the old
         release notes for v0.1.4 stay deleted; users who already
         installed 0.1.4 keep it until the next published release —
         there is no client-side push to downgrade them).
      5. Ship the FIX in `v0.1.5` ASAP so the affected users get a real
         upgrade path.

    ## Key generation

    See `tauri/src-tauri/keys/README.md` for the one-time setup.

    ## Why minisign?

    Tauri 2.x updater uses `minisign-verify` (Ed25519). Minisign is small,
    audited, OpenBSD-derived. The public key is 32 bytes; the manifest
    signature is 64 bytes. Both fit comfortably in HTTP JSON.

    ## Day-1 reputation

    SmartScreen + Gatekeeper reputation builds across the first ~few
    thousand installs of a new cert. The updater does NOT need separate
    reputation — Tauri verifies the manifest signature locally before
    download, then re-checks the installer's OS-level signature
    (codesign / SignPath) on install. Two independent verification layers.

    ## Troubleshooting

    | Symptom | Likely cause | Fix |
    |---------|--------------|-----|
    | No update prompt ever | `update_check_on_launch: false` in config.json | Edit to `true`, restart |
    | "Update available" but install fails silently | Signature mismatch (rotated key) | Manual download from GitHub Releases |
    | "Update check failed" log line in sidecar.log | Manifest server unreachable | Check `https://api.altidus.world/healthz` |
    | Update appears too aggressively | Future Phase 19 Settings UI will let you disable | For now edit config.json |

    ## Related docs

    - `docs/signing-macos.md` — codesign + notarize bench (Plan 18-02)
    - `docs/signing-windows.md` — SignPath OV signing (Plan 18-03)
    - `tauri/src-tauri/keys/README.md` — minisign key generation
    - `.github/workflows/release.yml` — orchestrator (Plan 18-05)
  </action>
  <verify>
    <automated>test -f /Users/ozai/projects/dj-set-ai/docs/updater.md && wc -l /Users/ozai/projects/dj-set-ai/docs/updater.md | awk '$1 >= 100 {exit 0} {exit 1}' && grep -q 'api.altidus.world/vibemix/updates' /Users/ozai/projects/dj-set-ai/docs/updater.md && grep -q 'update_check_on_launch' /Users/ozai/projects/dj-set-ai/docs/updater.md && grep -q 'minisign' /Users/ozai/projects/dj-set-ai/docs/updater.md && grep -q 'Rolling back\|rollback\|gh release delete' /Users/ozai/projects/dj-set-ai/docs/updater.md && grep -q 'tauri/src-tauri/keys/README.md' /Users/ozai/projects/dj-set-ai/docs/updater.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/updater.md` with ≥100 lines
    - Documents the GET endpoint shape with target/arch/current_version path params
    - Shows the 200 / 204 contract + the signed manifest JSON envelope (version / url / signature / notes / pub_date)
    - Documents the `update_check_on_launch` opt-out + the config.json path on each OS
    - Cross-references `tauri/src-tauri/keys/README.md` (key generation), `docs/signing-macos.md`, `docs/signing-windows.md`
    - Rollback recipe section with the `gh release delete` + `git push origin :refs/tags/...` procedure
    - Documents that the Bravoh proxy endpoint on api.altidus.world is the hand-off boundary (not implemented in Phase 18; future Bravoh-side delivery)
    - Day-1 reputation paragraph + troubleshooting table
  </acceptance_criteria>
  <done>Doc is the canonical reference for the updater contract + opt-out + rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running app → manifest server | Updater fetches an HTTPS manifest. TLS verification + minisign signature verification (independent of TLS) protect against compromise of the manifest server. |
| manifest signature → installed binary | Minisign-verified manifest carries the URL of a binary AND a separate signature of that binary. If signature mismatches the locally-baked pubkey, the install is rejected and the user keeps the running version. |
| user opt-out → config.json | The opt-out lives in plaintext config.json. A local-machine attacker could flip it to `false` to suppress security updates; threat is out-of-scope (any local attacker has higher-impact options). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Tampering | Manifest server compromise → forged update | mitigate | Minisign signature on the manifest verified against `pubkey` baked into the running app at build time. Forged manifests with valid TLS but invalid signatures are rejected. The pubkey is committed to `tauri.conf.json5` and baked into the signed binary; a compromised manifest server cannot reach into installed binaries to swap the pubkey. |
| T-18-10 | Tampering | Pubkey placeholder remains in v0.1.0 build | mitigate | Plan 18-05's release.yml refuses to build any tagged release while the literal placeholder sentinel `TAURI_UPDATER_PLACEHOLDER` (base64 `dW50cnVzdGVkIGNvbW1lbnQ6IFRBVVJJX1VQREFURVJfUExBQ0VIT0xERVI=`) is present in tauri.conf.json5. Mock-build CI runs on PRs detect the placeholder and skip the sign/publish steps but produce a green build; tagged releases hard-fail with a clear error. |
| T-18-11 | Information Disclosure | Private key in repo | mitigate | `.gitignore` blocks `tauri/src-tauri/keys/*.key`. Key generation procedure in `keys/README.md` instructs Kaan to store the key OUTSIDE the repo. CI receives the key as a GitHub Actions secret (auto-masked in logs). |
| T-18-12 | Denial of Service | Manifest endpoint unreachable | accept | Updater logs at WARN and silently exits — user keeps running version. No retry storm (single check per launch). |
| T-18-13 | Repudiation | Bad release pushed to all users | mitigate (manual) | Rollback procedure in docs/updater.md walks Kaan through tag deletion + manifest republish. Full automation is v2-deferred per CONTEXT. |
</threat_model>

<verification>
- `tauri/src-tauri/tauri.conf.json5` JSON5 parses cleanly (node strip-comments script) and `plugins.updater.{active,endpoints,pubkey,dialog}` match the spec
- `tauri/src-tauri/src/updater.rs` exists, ≥80 lines, references `UpdaterExt` + `tauri-plugin-store`
- `cargo check --frozen` in `tauri/src-tauri/` exits 0
- `grep -q 'mod updater;' tauri/src-tauri/src/main.rs` succeeds
- `grep -q 'updater::run_update_check_if_enabled' tauri/src-tauri/src/main.rs` succeeds
- `grep -q 'KEY_UPDATE_CHECK_ON_LAUNCH' tauri/src-tauri/src/config.rs` succeeds
- `docs/updater.md` exists, ≥100 lines, contains the endpoint URL + manifest shape + rollback recipe
- `tauri/src-tauri/keys/README.md` exists, ≥25 lines, contains npx signer generate command + GitHub secret names
- `.gitignore` blocks `tauri/src-tauri/keys/*.key` + `*.key.pub`
- No new entries in `Cargo.toml` `[dependencies]` (delta limited to Rust source files + config + docs)
- No new entries in `capabilities/default.json` (the `updater:default` + `store:default` permissions already cover the plugin's commands)
- POC files (cohost*.py, mascot.html, mocks/) diff-untouched (`git diff --stat` after edit covers only the 7 files listed)
</verification>

<success_criteria>
- DIST-06 satisfied at the Tauri-shell layer (boot-time check + opt-out + signed-manifest config)
- Phase 11 W2 STUB replaced with live config; pubkey placeholder + procedure documented so v0.1.0 prep is one Kaan task (key generation + paste)
- Zero new pip / npm / cargo deps
- Opt-out plumbed end-to-end via `tauri-plugin-store` without IPC schema changes (avoids regenerating codegen + drift gate)
- POC files diff-untouched
- Bravoh-side `/vibemix/updates/` endpoint hand-off documented (NOT shipped here — separate Bravoh ops deliverable)
</success_criteria>

<output>
After completion, create `.planning/phases/18-distribution-signing-notarization-installers/18-04-SUMMARY.md` documenting:
- tauri.conf.json5 updater stanza: STUB → live (active:true, real endpoint, placeholder pubkey)
- Pubkey placeholder sentinel (`TAURI_UPDATER_PLACEHOLDER`) — Plan 18-05's release.yml refuses to build with this still in place
- `update_check_on_launch` default-true persistence via `tauri-plugin-store` (no new IPC schema entry)
- Boot-time fire-and-forget in `tauri::async_runtime::spawn` from `main.rs .setup` — lowest-priority boot task, errors logged but never bail
- Cross-plan hand-off:
  - To Plan 18-05: TAURI_UPDATER_PRIVATE_KEY + TAURI_UPDATER_KEY_PASSWORD secrets + placeholder grep gate
  - To Bravoh ops (NOT in Phase 18): `/vibemix/updates/{target}/{arch}/{current_version}` endpoint on api.altidus.world
  - To Phase 19 polish: Settings UI surface for the opt-out toggle (one row in PerformanceGroup or new UpdateGroup)
- Open follow-up: end-to-end fresh-machine rehearsal of updater (Phase 20)
</output>
