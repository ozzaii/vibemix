---
phase: 99-harden-retry
plan: 05
type: execute
wave: 5
depends_on:
  - 99-04
files_modified:
  - tests/library/test_toolset_starvation.py
  - tests/repo/test_no_seen_relaxation.py
autonomous: true
requirements:
  - HARDEN-RETRY-05

must_haves:
  truths:
    - "A `tool_starvation` termination short-circuits the `create_playlist` handler BEFORE its library re-validation runs — proven by an explicit test scenario."
    - "Cardinal Invariant #2 (citation grounding) holds structurally: the count of `self.seen.add(` occurrences in `src/vibemix/library/toolset.py` is unchanged from the pre-Phase-99 baseline."
    - "No new code outside `src/vibemix/library/toolset.py` reads or writes `self._consecutive_empties` / `self.stop_reason` (single-writer analog of Invariant #1)."
    - "`create_playlist` library re-validation (in `library/create_playlist.py`) is untouched by Phase 99."
  artifacts:
    - path: "tests/library/test_toolset_starvation.py"
      provides: "starvation-short-circuits-create-playlist test (the load-bearing Invariant-#2 acid test)"
      exports: ["test_starvation_short_circuits_create_playlist"]
    - path: "tests/repo/test_no_seen_relaxation.py"
      provides: "AST/grep gate pinning seen-write count + single-writer analog of starvation state"
      exports: ["test_seen_writes_unchanged_from_baseline", "test_consecutive_empties_single_writer", "test_stop_reason_writes_confined_to_toolset"]
  key_links:
    - from: "tests/library/test_toolset_starvation.py::test_starvation_short_circuits_create_playlist"
      to: "dispatch terminal short-circuit blocks create_playlist re-validation"
      via: "set self.stop_reason directly, then call dispatch('create_playlist', ...) — assert no library lookups happen, no partial playlist persists"
      pattern: "dispatch.*create_playlist"
    - from: "tests/repo/test_no_seen_relaxation.py"
      to: "src/vibemix/library/toolset.py grep count"
      via: "subprocess grep -c invocation against current source"
      pattern: "self\\.seen\\.add\\("
---

<objective>
Pin Cardinal Invariant #2 (citation grounding) and the single-writer analog of Invariant #1 with explicit gates. This plan adds two failing-then-passing test scaffolds that ride forward as regression-pins for Phase 100 + every future change to `library/toolset.py`:

1. A scenario test in `tests/library/test_toolset_starvation.py` that proves the `tool_starvation` terminal short-circuit (from Plan 99-03) blocks `create_playlist`'s library re-validation — no partial playlist write.

2. An AST/grep gate test file `tests/repo/test_no_seen_relaxation.py` that pins:
   - The `seen.add` write-site count in `library/toolset.py` is unchanged from baseline.
   - `_consecutive_empties` is read/written ONLY inside `library/toolset.py`.
   - `self.stop_reason` is read/written ONLY inside `library/toolset.py` + the wrapper-side READ in `library/codex_curate.py` (the side-channel propagation seam from Plan 99-04 — that file READ is allowed, but no other module touches the attribute).

Purpose: Make the invariants explicit + machine-enforceable. Future PRs that drift the surface (a curious dev decides to relax `seen` for "convenience", or sprinkle counter reads in `mcp_server.py`) trip the gate at PR time, not at customer time.

Output: One new scenario test in `test_toolset_starvation.py`; one new test file `tests/repo/test_no_seen_relaxation.py` with three gate tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-harden-retry/99-CONTEXT.md
@.planning/phases/99-harden-retry/99-RESEARCH.md
@.planning/phases/99-harden-retry/99-03-SUMMARY.md
@.planning/phases/99-harden-retry/99-04-SUMMARY.md
@CLAUDE.md

# Source files (READ-ONLY in this plan — pin baseline counts, do NOT modify)
@src/vibemix/library/toolset.py
@src/vibemix/library/create_playlist.py
@src/vibemix/library/codex_curate.py

# Existing repo-gate test posture (mirror this style)
@tests/repo/test_asset_manifest_shape.py

<interfaces>
<!-- The Invariant #2 surface this plan protects. -->

`create_playlist` handler entry (in LibraryToolset.create_playlist — verified at `toolset.py:431-444`):
```python
def create_playlist(self, args: dict[str, Any]) -> dict[str, Any]:
    # Existing two-gate validation:
    # 1. seen-set check: every track_id must be in self.seen (per-run grounding)
    # 2. library re-validation: create_playlist.create_playlist(...) walks the library
    #    again and rejects any track_id not in the real library.
    # ─── Plan 99-03 added the terminal short-circuit at the TOP of dispatch() ───
    # When self.stop_reason is set, dispatch() returns the terminal-echo BEFORE
    # invoking any handler — including create_playlist. THIS PLAN proves it.
```

Baseline `seen.add` count to pin (capture at plan-execute time):
```bash
# Run BEFORE this plan's edits, capture the integer:
grep -c "self\.seen\.add(" src/vibemix/library/toolset.py
# That number becomes the gate's reference.
# Plan 99-03 SUMMARY recorded this baseline — read it.
```
</interfaces>

<decisions_locked>
- D-01 (Decision 1): Counter location = `LibraryToolset` only. ← THIS PLAN gates.
- D-04 (Decision 4): `self.stop_reason` is an instance attribute on `LibraryToolset` only. ← THIS PLAN gates.
- Cardinal Invariant #2 (citation grounding, CLAUDE.md § Cardinal Invariants): no Phase-99 change touches `seen` write sites. ← THIS PLAN gates.
- The wrapper-side READ in `library/codex_curate.py` (Plan 99-04) reads the side-channel FILE, NOT the `LibraryToolset.stop_reason` attribute directly (the toolset lives in a different process). ← THIS PLAN's gate respects this distinction.
</decisions_locked>

<antipatterns_to_avoid>
- Writing a gate test that hardcodes a count from this plan's run rather than reading the baseline from Plan 99-03's SUMMARY (the count must reflect pre-Phase-99 state, not post-99-01 state — though those are equal, the gate's docstring should say so).
- Using `grep` in a way that miscounts comments: `grep -c` counts every match line, including comments mentioning `self.seen.add`. To filter comments: `grep -v '^[[:space:]]*#' | grep -c "self\.seen\.add("`. RESEARCH.md notes this hygiene rule.
- Allowing the gate to live in `tests/library/` instead of `tests/repo/`. Repo-level gates (cross-cutting invariants) go in `tests/repo/` by convention (see existing files there).
- Pinning `stop_reason` to "only in toolset.py" — that's WRONG. Plan 99-04 added a wrapper-side READ in `codex_curate.py` of the side-channel FILE (not the attribute). The gate must distinguish: `LibraryToolset.stop_reason` ATTRIBUTE writes are toolset-only; the side-channel FILE read in `codex_curate.py` is allowed. The grep target is the ATTRIBUTE (`\.stop_reason\b` on a LibraryToolset instance), not the side-channel file references.
</antipatterns_to_avoid>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test_starvation_short_circuits_create_playlist scenario test</name>
  <files>tests/library/test_toolset_starvation.py</files>
  <behavior>
    Add one new test to `tests/library/test_toolset_starvation.py`:

    `test_starvation_short_circuits_create_playlist`:
    - Build toolset with a library that has 5 valid tracks (any titles).
    - Pre-load `toolset.seen = {"track_001", "track_002"}` (or whatever the fixture track-id shape is) to make a `create_playlist` call HYPOTHETICALLY succeed.
    - Manually set `toolset.stop_reason = {"reason": "tool_starvation", "hint": "test-set-up", "tool": "search_vibe", "consecutive": 3}` (bypass the threshold trip for direct testing of the short-circuit).
    - Call `toolset.dispatch("create_playlist", {"name": "test", "track_ids": ["track_001", "track_002"]})`.
    - Assert:
      - Return value is the terminal echo `{"error": "tool_starvation", "stop_reason": <dict>}`.
      - `toolset.created is None` (no PlaylistResult was created — the handler never ran).
      - No M3U file appeared in the fake `out_dir` / no `create_playlist.create_playlist` library re-validation ran (use a sentinel callable for `create_playlist.create_playlist` that raises if called — monkeypatch it).
    - This is the load-bearing test: it proves Invariant #2 holds because the `create_playlist` handler — which performs the library re-validation — is structurally unreachable after a `tool_starvation` trip.

    Test MUST fail RED on current source if the short-circuit from Plan 99-03 was somehow not landed (this is also a regression-pin for 99-03).
  </behavior>
  <action>
    Append the test. Use `monkeypatch.setattr` to inject a sentinel `create_playlist.create_playlist` that raises `RuntimeError("must not be called after starvation")` if invoked. Use the existing fake-`library`/`embedder`/`store` posture.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_starvation.py::test_starvation_short_circuits_create_playlist 2>&1 | tail -15</automated>
  </verify>
  <done>
    Test present; passes on post-99-03/99-04 source (the short-circuit landed in 99-03 already makes this GREEN on Task 1 — that's expected: 99-03's idempotence test partially covered this; THIS test is the explicit `create_playlist`-specific scenario that pins Invariant #2 + HARDEN-RETRY-06 contract `(starvation MUST short-circuit before partial-playlist write)`). If the test passes immediately on a fresh-from-99-04 tree, that's the contract — the gate now exists as a regression-pin going forward. If it FAILS unexpectedly, that's a real bug in Plan 99-03's short-circuit — investigate before continuing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/repo/test_no_seen_relaxation.py AST/grep gate file</name>
  <files>tests/repo/test_no_seen_relaxation.py</files>
  <behavior>
    Create new file `tests/repo/test_no_seen_relaxation.py` with three gate tests:

    - `test_seen_writes_unchanged_from_baseline`:
      - Read `BASELINE_SEEN_ADD_COUNT` constant from the top of the test file. Source the baseline from Plan 99-03's SUMMARY (which recorded the pre-99-01 `seen.add` count). Document the baseline value + provenance in a top-of-file comment.
      - Run `grep -v '^[[:space:]]*#' src/vibemix/library/toolset.py | grep -c "self\.seen\.add(" ` via subprocess (filtered to skip comment lines per RESEARCH.md hygiene rule).
      - Assert the count equals `BASELINE_SEEN_ADD_COUNT`.
      - If a future PR adds or removes a `seen.add` site, this test flags it — the developer must justify the change in a `seen` write site (Cardinal Invariant #2 is in scope) before bumping the baseline constant.

    - `test_consecutive_empties_single_writer`:
      - Grep `_consecutive_empties` across `src/vibemix/`. Assert ALL hits are in `src/vibemix/library/toolset.py`. No other source module is allowed to mention the attribute. Tests are excluded from this gate (test files MAY read it for assertions).

    - `test_stop_reason_writes_confined_to_toolset`:
      - Grep `\.stop_reason\b` (with word boundary) across `src/vibemix/`. Allowed locations:
        - `src/vibemix/library/toolset.py` — owner.
        - `src/vibemix/library/codex_curate.py` — READS `result.stop_reason` on the `CodexCurateResult` dataclass + DOES NOT touch `LibraryToolset.stop_reason` directly. The wrapper reads the side-channel FILE (not the attribute) — different surface.
        - `src/vibemix/__main__.py` — Plan 99-06 will add `result.stop_reason` reads at the CLI dispatch sites. (Whitelist this file too; Plan 99-06 will land the actual reads. The gate must not fight Plan 99-06.)
        - `src/vibemix/library/telegram_bridge.py` — Plan 99-07 will add `norm.get("stop_reason")` reads. (Whitelist.)
      - Assert NO hits in any OTHER source file (`agent/`, `intel/`, `runtime/`, etc.).
      - Test fails if any future PR sprinkles `stop_reason` reads outside the whitelisted set.

    Pattern after `tests/repo/test_asset_manifest_shape.py` style — `subprocess.run` with `grep`/`rg`, parse output, assert on counts.
  </behavior>
  <action>
    Create new test file. Source `BASELINE_SEEN_ADD_COUNT` from Plan 99-03's SUMMARY (read the SUMMARY first; if not yet recorded, run the grep yourself and document it in the top-of-file comment). Use `subprocess.run(["grep", ...], capture_output=True, text=True)` for the gates — match the existing posture in `tests/repo/`. The whitelist for `test_stop_reason_writes_confined_to_toolset` is exactly: `toolset.py`, `codex_curate.py`, `__main__.py`, `telegram_bridge.py` — encode as a list constant at the top of the file. If the gates fail because of an unexpected hit, document the offending file path in the failure message so the developer knows exactly where to look.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/repo/test_no_seen_relaxation.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    File `tests/repo/test_no_seen_relaxation.py` exists with three tests; all three pass on the current post-99-04 source; the whitelist comment is present and documents Plans 99-06 + 99-07's upcoming reads as expected and pre-approved. Failure messages name specific offending file paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-side AST/grep gates | Repo-level invariant guard — runs against current source on every test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04 | Tampering | Invariant #2 (`seen`-set grounding) leak via accidental `seen.add` addition | mitigate | `test_seen_writes_unchanged_from_baseline` grep gate fires on any new `seen.add` line in `toolset.py`. Comment lines are filtered (`grep -v '^[[:space:]]*#'`). |
| T-99-04b | Tampering | Counter / `stop_reason` sprinkled across modules | mitigate | `test_consecutive_empties_single_writer` + `test_stop_reason_writes_confined_to_toolset` gate against cross-module spread. Whitelist for `stop_reason` includes the four files that legitimately consume it (Plans 99-04/-06/-07 callers); any new read site outside whitelist fails the gate. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. Pure stdlib + existing tests. |
</threat_model>

<verification>
- `pytest -q tests/library/test_toolset_starvation.py::test_starvation_short_circuits_create_playlist` — green.
- `pytest -q tests/repo/test_no_seen_relaxation.py` — all three gate tests green.
- Manual grep sanity-check: `grep -rn "_consecutive_empties" src/vibemix/ --include='*.py' | grep -v __pycache__` returns hits ONLY in `src/vibemix/library/toolset.py`.
- Manual grep sanity-check: `grep -rn "stop_reason" src/vibemix/ --include='*.py' | grep -v __pycache__` returns hits ONLY in the whitelist (toolset.py, codex_curate.py, __main__.py — only at CLI dispatch sites once Plan 99-06 lands, telegram_bridge.py — only once Plan 99-07 lands).
- The `BASELINE_SEEN_ADD_COUNT` constant in the gate file matches the count recorded in Plan 99-03's SUMMARY.
- `pytest -q tests/library/ tests/repo/` regression-pin — full library + repo suites stay green.
</verification>

<success_criteria>
- `test_starvation_short_circuits_create_playlist` pins HARDEN-RETRY-06's `create_playlist` short-circuit contract.
- `test_seen_writes_unchanged_from_baseline` pins Invariant #2 (citation grounding) across Phase 99.
- `test_consecutive_empties_single_writer` pins Invariant #1 analog (counter is toolset-only).
- `test_stop_reason_writes_confined_to_toolset` pins the propagation surface to four whitelisted files.
- All four invariants documented in the gate file's top-of-file comment with file:line references to the locked decisions in `99-CONTEXT.md` + `CLAUDE.md`.
</success_criteria>

<output>
Create `.planning/phases/99-harden-retry/99-05-SUMMARY.md` when done.

SUMMARY must record:
- The `BASELINE_SEEN_ADD_COUNT` value pinned in the gate.
- The whitelist file list (4 files) for `stop_reason` reads.
- Note that Plans 99-06 / 99-07 will land the actual reads in `__main__.py` / `telegram_bridge.py` — the gates are PRE-AUTHORIZED for those files; if the gate fires unexpectedly, the failing file path will name the offender precisely.
- Confirmation that no source files were modified in this plan (test-only changes).
</output>
</content>
</invoke>