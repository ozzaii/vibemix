---
phase: 99-harden-retry
plan: 03
type: execute
wave: 3
depends_on:
  - 99-02
files_modified:
  - src/vibemix/library/toolset.py
  - tests/library/test_toolset_starvation.py
autonomous: true
requirements:
  - HARDEN-RETRY-02
  - HARDEN-RETRY-03

must_haves:
  truths:
    - "When `_consecutive_empties` reaches `TOOL_STARVATION_THRESHOLD` (default 3) AND `self.stop_reason is None`, the next `dispatch()` call sets `self.stop_reason` to a dict with shape `{\"reason\": \"tool_starvation\", \"hint\": <str>, \"tool\": <str>, \"consecutive\": <int>}`."
    - "Hint case A (zero-track library): when `self._library.tracks` is empty/falsy at trip time, `hint == \"library has 0 tracks — run \\`library ingest\\` first\"`."
    - "Hint case B (no theme match): when library has tracks AND last tool was `search_vibe`, `hint == f\"no tracks matched '{theme}' — try a broader theme or different BPM range\"` where `{theme}` is the `args.get(\"query\", \"\")` value."
    - "Hint case C (tool error): when last tool was NOT `search_vibe` and returned an error, `hint == f\"tool '{last_tool}' kept failing — try again or check codex installation\"`."
    - "Once `self.stop_reason` is set, subsequent `dispatch()` calls become idempotent no-ops: they return `{\"error\": \"tool_starvation\", \"stop_reason\": dict(self.stop_reason)}` WITHOUT executing the handler — the run is terminal."
    - "Counter does NOT reset after the trip — the run is terminal."
  artifacts:
    - path: "src/vibemix/library/toolset.py"
      provides: "Threshold trip detection + `_build_starvation_payload` helper + terminal short-circuit branch at top of `dispatch()`"
      contains: "_build_starvation_payload"
    - path: "tests/library/test_toolset_starvation.py"
      provides: "Threshold-trip + 3 hint cases + terminal-idempotence tests"
      exports:
        - "test_threshold_trip_sets_stop_reason"
        - "test_threshold_is_tunable"
        - "test_hint_zero_track_library"
        - "test_hint_no_theme_match"
        - "test_hint_tool_error"
        - "test_terminal_idempotence_after_starvation"
        - "test_starvation_short_circuits_subsequent_dispatch"
  key_links:
    - from: "src/vibemix/library/toolset.py::dispatch"
      to: "self.stop_reason terminal write"
      via: "threshold check immediately after counter increment, with `self.stop_reason is None` guard for idempotence"
      pattern: "if\\s+self\\._consecutive_empties\\s*>=\\s*.*TOOL_STARVATION_THRESHOLD"
    - from: "src/vibemix/library/toolset.py::dispatch top-of-method"
      to: "terminal-run short-circuit"
      via: "early-return when self.stop_reason is not None"
      pattern: "if self\\.stop_reason is not None:"
---

<objective>
Land the threshold-trip detection + the deterministic three-case hint generator + terminal short-circuit on `LibraryToolset`. After this plan, the toolset emits `self.stop_reason = {"reason": "tool_starvation", "hint": <case-specific>, "tool": <last>, "consecutive": <int>}` on threshold-trip and becomes a no-op for subsequent dispatch calls.

Purpose: Close the in-process side of Factor 9 — the toolset now terminates honestly. The cross-process propagation (side-channel file) lands in Plan 99-04; the wrapper + CLI + Telegram surfacing land in plans 99-04 / 99-06 / 99-07 / 99-08.

Output: A new `_build_starvation_payload(last_tool, args)` method on `LibraryToolset` with three deterministic case branches; threshold check + idempotent write in `dispatch()`; a top-of-`dispatch()` short-circuit; seven new tests covering trip + hint cases + idempotence.

Note: The hint copy is SEEDED per D-05 (Decision 5 in `99-CONTEXT.md`). KAAN-ACTION §HARDEN-PHASE-A-EAR-PASS (parked at milestone close per `PROJECT.md` § Current Milestone) will polish the wording. Tests pin STRUCTURE + key strings (`"library has 0 tracks"`, `"no tracks matched"`, `"kept failing"`), not exact full wording — this lets ear-pass refine without breaking tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-harden-retry/99-CONTEXT.md
@.planning/phases/99-harden-retry/99-RESEARCH.md
@.planning/phases/99-harden-retry/99-01-SUMMARY.md
@.planning/phases/99-harden-retry/99-02-SUMMARY.md
@CLAUDE.md

# Source file this plan touches
@src/vibemix/library/toolset.py

<interfaces>
<!-- Builds on Plans 99-01 + 99-02. Insertion sites verified against current source at research time. -->

From src/vibemix/library/toolset.py (after Plans 99-01 + 99-02 — this plan extends):
```python
class LibraryToolset:
    def __init__(self, *, library, embedder, store, ...):
        # ... (existing)
        self._consecutive_empties: int = 0    # added by 99-01
        self.stop_reason: dict[str, Any] | None = None  # added by 99-01

    def _is_empty_or_error(self, name, result):  # added by 99-02
        ...

    def dispatch(self, name, args):
        # ─── THIS PLAN INSERTS HERE (top of method): ─────────────────
        # if self.stop_reason is not None:
        #     return {"error": "tool_starvation", "stop_reason": dict(self.stop_reason)}

        handlers = {...}
        handler = handlers.get(name)
        if handler is None:
            return {"error": f"unknown tool {name!r}"}
        with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
            fut = ex.submit(handler, args)
            try:
                result = fut.result(timeout=TOOL_CALL_TIMEOUT_S)
            except concurrent.futures.TimeoutError:
                result = {"error": f"tool {name!r} timed out"}
            except Exception as e:
                result = {"error": f"tool {name!r} crashed: {type(e).__name__}"}

        # counter logic (from 99-02):
        if self._is_empty_or_error(name, result):
            self._consecutive_empties += 1
            # ─── THIS PLAN INSERTS HERE: threshold-trip ─────────────
            # if (self._consecutive_empties >= TOOL_STARVATION_THRESHOLD
            #         and self.stop_reason is None):
            #     self.stop_reason = self._build_starvation_payload(
            #         last_tool=name, args=args,
            #     )
            #     # NB: Plan 99-04 adds the side-channel file write here.
        else:
            self._consecutive_empties = 0

        return result
```

Library tracks attribute (used by case A of the hint generator — verified API):
```python
# From src/vibemix/library/rekordbox.py
class RekordboxLibrary:
    tracks: dict[str, TrackEntry]   # dict-id-keyed
```
The fake library used in tests provides the same `tracks` attribute (per `tests/library/test_toolset.py:148-170` posture).
</interfaces>

<decisions_locked>
- D-02 (Decision 2): Counter increment predicate (already wired in 99-02; this plan reuses).
- D-03 (Decision 3): Threshold = `TOOL_STARVATION_THRESHOLD` module constant (already added in 99-01; this plan references via `tool_mod.TOOL_STARVATION_THRESHOLD`, NOT a local copy — Pitfall 7).
- D-04 (Decision 4): Stop-reason surface = `self.stop_reason: dict | None` attribute (added in 99-01; this plan writes). Payload shape: `{"reason": "tool_starvation", "hint": <str>, "tool": <last_tool>, "consecutive": <int>}` per `99-CONTEXT.md` § Specifics.
- D-05 (Decision 5): Hint generation = deterministic, three named cases (zero-track library / no theme match / tool error). Copy is seeded; KAAN-ACTION ear-pass polishes wording. ← THIS PLAN.
- RESEARCH.md Open Question 2 recommendation: subsequent `dispatch()` calls return `{"error": "tool_starvation", "stop_reason": dict(self.stop_reason)}` — idempotent terminal echo. ← THIS PLAN.
</decisions_locked>

<antipatterns_to_avoid>
- Re-resetting the counter after the threshold trip — once `self.stop_reason` is set, the counter is FROZEN (the `else: self._consecutive_empties = 0` branch is unreachable because the short-circuit at top of `dispatch()` returns before reaching the handler call). Verify: the counter freezes at `>= TOOL_STARVATION_THRESHOLD` after a trip.
- Letting the threshold check run multiple times (writing `stop_reason` again) — the `self.stop_reason is None` guard makes the write idempotent. Tests pin this.
- LLM-generating the hint string. Hints MUST be deterministic case-based code (D-05). The `theme` user input enters via f-string interpolation ONLY into Case B's hint — that's safe because the hint is rendered to stderr (CLI) / `strip_leaks` (Telegram), never eval'd (T-99-02 mitigation).
- Inlining `3` in the comparison — must use `tool_mod.TOOL_STARVATION_THRESHOLD` so tests can monkeypatch (Pitfall 7).
- Forgetting that Case C ("tool error") only triggers when last_tool was NOT `search_vibe` — Case B claims the `search_vibe` empty path even if that empty was caused by an internal error in `search_vibe`. Verify with the test sequence: 3 search_vibe empties → Case B; 3 get_track_features errors → Case C.
</antipatterns_to_avoid>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing threshold-trip + hint + idempotence tests</name>
  <files>tests/library/test_toolset_starvation.py</files>
  <behavior>
    Add seven new tests to `tests/library/test_toolset_starvation.py` (all RED before Task 2):

    - `test_threshold_trip_sets_stop_reason`: After 3 consecutive empty `search_vibe` dispatches on a library that has SOME tracks (so Case B fires), assert `toolset.stop_reason is not None` AND `toolset.stop_reason["reason"] == "tool_starvation"` AND `toolset.stop_reason["tool"] == "search_vibe"` AND `toolset.stop_reason["consecutive"] == 3`.

    - `test_threshold_is_tunable`: `monkeypatch.setattr(tool_mod, "TOOL_STARVATION_THRESHOLD", 2)` → fire 2 empties → assert `stop_reason is not None`. Pins Pitfall 7.

    - `test_hint_zero_track_library`: Build toolset with a library whose `tracks` is empty (`{}` or `[]` depending on fake shape). Fire 3 empty `search_vibe` → assert `"library has 0 tracks" in toolset.stop_reason["hint"]` AND `"library ingest" in toolset.stop_reason["hint"]`. Substring-match on key strings, NOT full-string equality (D-05 ear-pass tolerance).

    - `test_hint_no_theme_match`: Library has 3+ tracks (any titles). Fire 3 empty `search_vibe` with `{"query": "uplifting 200 BPM ambient"}` → assert `"no tracks matched" in hint` AND `"'uplifting 200 BPM ambient'" in hint` AND `"BPM" in hint`.

    - `test_hint_tool_error`: Library has tracks. Fire 3 dispatches of `get_track_features` where the handler raises (caught by dispatch → `{"error": ...}`) → assert `"tool 'get_track_features'" in hint` AND `"kept failing" in hint`.

    - `test_terminal_idempotence_after_starvation`: After trip, call `dispatch("search_vibe", {"query": "x"})` again → return value is `{"error": "tool_starvation", "stop_reason": <dict matching self.stop_reason>}`. The `stop_reason` payload shape is the same on every subsequent call (idempotent). Counter does NOT advance past threshold (frozen).

    - `test_starvation_short_circuits_subsequent_dispatch`: After trip, register a sentinel `vibe_search` fake that raises `RuntimeError("should not run")`. Call `dispatch("search_vibe", ...)` — assert NO exception bubbles (handler is not invoked) AND return value is the terminal echo. This is the proof that the short-circuit at top of `dispatch()` runs BEFORE the `with ThreadPoolExecutor(...)` block.

    Use the same fake-`library`/`embedder`/`store` posture as Plans 99-01 / 99-02 (clone from `tests/library/test_toolset.py:148-170` fixture).

    All seven tests must FAIL RED on the post-99-02 source before Task 2.
  </behavior>
  <action>
    Append the seven tests to `tests/library/test_toolset_starvation.py` (next to existing tests). Use `monkeypatch.setattr(tool_mod, "TOOL_STARVATION_THRESHOLD", ...)` where needed. Substring-match hint assertions on key phrases (D-05 ear-pass tolerance). Do NOT modify `tests/library/test_toolset_starvation_concurrency.py`. Do NOT add `xfail` / `skip` markers — RED is the contract.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_starvation.py::test_threshold_trip_sets_stop_reason tests/library/test_toolset_starvation.py::test_threshold_is_tunable tests/library/test_toolset_starvation.py::test_hint_zero_track_library tests/library/test_toolset_starvation.py::test_hint_no_theme_match tests/library/test_toolset_starvation.py::test_hint_tool_error tests/library/test_toolset_starvation.py::test_terminal_idempotence_after_starvation tests/library/test_toolset_starvation.py::test_starvation_short_circuits_subsequent_dispatch 2>&1 | tail -20</automated>
  </verify>
  <done>
    Seven new tests in the file; all seven fail RED (counter trips but never writes `stop_reason` because Task 2's logic isn't there yet); Plans 99-01 / 99-02 tests remain GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire threshold detection + _build_starvation_payload + terminal short-circuit</name>
  <files>src/vibemix/library/toolset.py</files>
  <behavior>
    Three additive edits in `src/vibemix/library/toolset.py`:

    A. Add a new private method `_build_starvation_payload(self, last_tool: str, args: dict[str, Any]) -> dict[str, Any]` per RESEARCH.md Code Example. Three deterministic case branches (zero-track library / no theme match / tool error). Returns the payload dict with keys `reason`, `hint`, `tool`, `consecutive` (per `99-CONTEXT.md` § Specifics).

    B. In `dispatch()`, IMMEDIATELY after the existing `self._consecutive_empties += 1` line (added by 99-02), insert:
       ```
       if (
           self._consecutive_empties >= tool_mod.TOOL_STARVATION_THRESHOLD
           and self.stop_reason is None
       ):
           self.stop_reason = self._build_starvation_payload(last_tool=name, args=args)
       ```
       NB: reference via the module (`tool_mod.TOOL_STARVATION_THRESHOLD` — get the module via `import vibemix.library.toolset as tool_mod` at top of file OR via `sys.modules[__name__]`). Pitfall 7 — direct constant reference at module scope inside the class method would capture-at-import; the module attribute lookup honors `monkeypatch.setattr`. The RESEARCH.md Code Example uses `TOOL_STARVATION_THRESHOLD` directly; verify monkeypatch works with that form first — if test_threshold_is_tunable fails, switch to module-attribute lookup via `import sys; sys.modules[__name__].TOOL_STARVATION_THRESHOLD`. Document the chosen form in the SUMMARY.

       NB: Plan 99-04 will add a `self._write_side_channel(self.stop_reason)` call after the assignment — leave a one-line comment `# Side-channel write lands in Plan 99-04.` so the next plan's insertion point is unambiguous.

    C. At the TOP of `dispatch()` (BEFORE the `handlers = {...}` dict construction, BEFORE any handler lookup), insert:
       ```
       if self.stop_reason is not None:
           return {"error": "tool_starvation", "stop_reason": dict(self.stop_reason)}
       ```
       (verbatim per RESEARCH.md Code Example "Counter + threshold hook in dispatch()" lines 423-426). The `dict(self.stop_reason)` creates a shallow copy so callers cannot mutate the internal payload.

    All seven Task-1 tests turn GREEN. All Plans 99-01 + 99-02 tests stay GREEN. `tests/library/test_toolset.py` stays GREEN.
  </behavior>
  <action>
    Edit `src/vibemix/library/toolset.py`. Implement A (helper method), B (threshold check immediately after `_consecutive_empties += 1`), C (short-circuit at top of `dispatch()`). For the threshold-constant reference, prefer the direct symbol `TOOL_STARVATION_THRESHOLD` first; if `test_threshold_is_tunable` fails after Task 2 lands, switch to `sys.modules[__name__].TOOL_STARVATION_THRESHOLD` (Pitfall 7 patch). Add the inline comment `# Side-channel write lands in Plan 99-04.` so Plan 99-04's seam is signposted. Do NOT add any file I/O / env-var reads / `import os` use in this plan — those are Plan 99-04.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py tests/library/test_toolset.py 2>&1 | tail -25</automated>
  </verify>
  <done>
    `_build_starvation_payload` method present with three case branches; threshold-trip block inserted in `dispatch()`; terminal short-circuit at top of `dispatch()`; all seven Task-1 tests pass; Plans 99-01 + 99-02 tests stay green; `tests/library/test_toolset.py` stays green. Grep gate: `grep -c "self\.stop_reason" src/vibemix/library/toolset.py` returns exactly 5 (init + 2 reads in short-circuit + 1 read in threshold-guard + 1 write in trip). Grep gate: `grep -c "TOOL_STARVATION_THRESHOLD" src/vibemix/library/toolset.py` returns exactly 2 (constant declaration + threshold check). Inline comment `Side-channel write lands in Plan 99-04` present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-process Python | Hint string is constructed from `args.get("query")` (user input via Codex's tool call) — interpolated into stderr text (CLI) / `strip_leaks` (Telegram), never eval'd |
| `self.stop_reason` payload | Returned to callers as `dict(self.stop_reason)` (shallow copy) — prevents in-place mutation of internal state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02 | Tampering / Spoofing | Hint string with user-input `theme` interpolation (Case B) | mitigate | Hint is f-string interpolation only — never `exec` / `eval`. Output destinations: stderr (CLI, plain print) + `strip_leaks` (Telegram, scrub FS paths). Tests pin substring match, not exact string, so future ear-pass can refine without breaking tests. |
| T-99-01 | Tampering | Counter overflow past threshold | mitigate | Top-of-`dispatch()` short-circuit returns BEFORE the counter would advance, so the counter is FROZEN at `>= TOOL_STARVATION_THRESHOLD` after trip. `test_terminal_idempotence_after_starvation` pins this. |
| T-99-04 | Tampering | Invariant #2 (`seen`-set grounding) leak | mitigate | This plan does NOT touch any `seen` write site (verified by `grep -c "self\.seen\.add" src/vibemix/library/toolset.py` — count unchanged pre/post plan). Plan 99-05 adds the AST gate that pins this across the phase. |
| T-99-03 | Tampering (race) | Idempotent payload write under concurrency | mitigate | The `self.stop_reason is None` guard makes the write idempotent. Even if two threads raced into the threshold-trip block (which they cannot, per `toolset.py:407-411` serialization), only the first write would land. Plan 99-02's concurrency acid test stays GREEN. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. |
</threat_model>

<verification>
- `pytest -q tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py tests/library/test_toolset.py` — all green (15+ tests across files).
- `pytest -q tests/library/` — broader regression-pin; the existing library test suite stays green.
- Grep gate: `grep -c "self\.stop_reason" src/vibemix/library/toolset.py` returns exactly 5.
- Grep gate: `grep -c "TOOL_STARVATION_THRESHOLD" src/vibemix/library/toolset.py` returns exactly 2.
- Grep gate (Invariant #2 baseline for Plan 99-05): record the current count of `self.seen.add(` in `library/toolset.py` — must be unchanged from pre-99-01 baseline. This is the number Plan 99-05's AST/grep gate will pin.
- Grep gate: `grep -n "Side-channel write lands in Plan 99-04" src/vibemix/library/toolset.py` returns exactly 1 hit (Plan 99-04's insertion-point signpost).
</verification>

<success_criteria>
- Threshold trip on 3 consecutive empty `search_vibe` writes `self.stop_reason` with the correct shape AND the correct hint case.
- Threshold trip on 3 consecutive tool errors (non-search_vibe) writes Case C hint.
- Threshold trip on 3 consecutive empties against zero-track library writes Case A hint.
- After trip, subsequent `dispatch()` calls return idempotent terminal-echo `{"error": "tool_starvation", "stop_reason": <copy>}` WITHOUT executing the handler.
- `monkeypatch.setattr(tool_mod, "TOOL_STARVATION_THRESHOLD", 2)` actually changes runtime behavior (Pitfall 7 closed).
- Cardinal Invariant #2 holds: `seen.add` count unchanged from baseline; no new `seen` write site introduced.
- Inline signpost `Side-channel write lands in Plan 99-04` present at the correct location.
</success_criteria>

<output>
Create `.planning/phases/99-harden-retry/99-03-SUMMARY.md` when done.

SUMMARY must record:
- Line numbers of: `_build_starvation_payload` method, threshold-check insertion, top-of-`dispatch()` short-circuit (for Plan 99-04 to reference the side-channel write site).
- Which form was used for the threshold reference (direct `TOOL_STARVATION_THRESHOLD` symbol vs. `sys.modules[__name__].TOOL_STARVATION_THRESHOLD` module-attribute lookup) and whether `test_threshold_is_tunable` passed first try.
- The baseline `grep -c "self\.seen\.add"` count for Plan 99-05's gate.
- Confirmation that the inline signpost `Side-channel write lands in Plan 99-04` is present.
- **KAAN-ACTION reminder:** Hint copy is SEEDED per D-05; `§HARDEN-PHASE-A-EAR-PASS` (parked at milestone close per PROJECT.md) will polish wording. Tests pin substrings, not full strings — ear-pass refinements should not break tests.
</output>
</content>
</invoke>