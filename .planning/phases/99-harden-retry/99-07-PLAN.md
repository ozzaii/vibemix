---
phase: 99-harden-retry
plan: 07
type: execute
wave: 5
depends_on:
  - 99-04
files_modified:
  - src/vibemix/library/telegram_bridge.py
  - tests/library/test_telegram_bridge.py
autonomous: true
requirements:
  - HARDEN-RETRY-07

must_haves:
  truths:
    - "`format_reply({\"stop_reason\": \"tool_starvation\", \"hint\": \"<text>\"})` returns a string containing the hint, prefixed by a warning glyph, scrubbed through `strip_leaks`."
    - "`format_reply` on a non-starvation result (existing playlist branch / error branch) is BYTE-EQUIVALENT to pre-plan output — no regression."
    - "The starvation branch is rendered as a SINGLE line (not numbered choices — that's Phase 100's `clarification_needed` shape)."
    - "Any FS path in the hint (defensive — Decision 5 seed has none, but ear-pass might add one) is scrubbed by `strip_leaks` before display."
  artifacts:
    - path: "src/vibemix/library/telegram_bridge.py"
      provides: "`format_reply` tool_starvation branch (BEFORE the generic error branch)"
      contains: "tool_starvation"
    - path: "tests/library/test_telegram_bridge.py"
      provides: "Extended with starvation-branch + non-regression tests"
      exports: ["test_format_reply_starvation_branch", "test_format_reply_starvation_strips_leaks", "test_format_reply_existing_branches_unchanged"]
  key_links:
    - from: "src/vibemix/library/telegram_bridge.py::format_reply"
      to: "tool_starvation branch insertion BEFORE generic error branch"
      via: "if norm.get('stop_reason') == 'tool_starvation': return strip_leaks(f'⚠️ {hint}')"
      pattern: "stop_reason.*tool_starvation"
---

<objective>
Add the `tool_starvation` branch to `library/telegram_bridge.py::format_reply` (line 107 per RESEARCH.md) so the Telegram mobile surface renders the hint, not a generic error. The branch sits BEFORE the existing error branch (`if not norm.get("ok"):`) and routes through `strip_leaks` for privacy.

Purpose: Close the Telegram leg of HARDEN-RETRY-07 (uniform propagation). After Plan 99-06's `curate_fn` normalization, the Telegram bridge sees `{"ok": False, "stop_reason": "tool_starvation", "hint": "..."}` and `format_reply` renders it as a chat message with the actionable hint.

Output: A single 4-line insertion at the top of `format_reply` (per RESEARCH.md Code Example "Telegram `format_reply` branch"); three new tests in `tests/library/test_telegram_bridge.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-harden-retry/99-CONTEXT.md
@.planning/phases/99-harden-retry/99-RESEARCH.md
@.planning/phases/99-harden-retry/99-04-SUMMARY.md
@CLAUDE.md

# Source file this plan touches
@src/vibemix/library/telegram_bridge.py
@tests/library/test_telegram_bridge.py

<interfaces>
<!-- The existing format_reply structure that this plan extends. -->

From src/vibemix/library/telegram_bridge.py:107-124 (verified existing — this plan inserts at the TOP of the function body):
```python
def format_reply(norm: dict[str, Any]) -> str:
    """Render a normalized curation result into a chat message (leak-stripped)."""
    # ─── THIS PLAN INSERTS HERE (BEFORE the existing error branch): ───
    # if norm.get("stop_reason") == "tool_starvation":
    #     hint = norm.get("hint") or norm.get("error") or "Viber ran out of grounded options."
    #     return strip_leaks(f"⚠️ {hint}")

    if not norm.get("ok"):                          # ← existing generic error branch
        err = norm.get("error") or "no playlist could be built"
        return strip_leaks(f"⚠️ {err}")

    # ... existing happy-path (playlist rendering)
```

`strip_leaks` (existing scrubber at `telegram_bridge.py:102-104`) — unchanged by this plan.

The `curate_fn` adapter (Plan 99-06 lands the normalizer) — emits `{"ok": False, "stop_reason": "tool_starvation", "hint": "<hint string>"}`. This plan's `format_reply` branch consumes that shape.
</interfaces>

<decisions_locked>
- D-08 (Decision 8): Telegram propagation = explicit `tool_starvation` branch in `format_reply`, renders hint via `strip_leaks` (privacy preserved). ← THIS PLAN.
</decisions_locked>

<antipatterns_to_avoid>
- Inserting the branch AFTER the generic error branch — the generic branch matches `if not norm.get("ok")` which is True for the starvation payload (`ok=False`), so the generic branch would FIRE FIRST and the starvation branch would be dead code. Insertion order matters: starvation BEFORE generic.
- Using numbered-choices rendering for the starvation hint — that's Phase 100's `clarification_needed` shape, NOT starvation's. Starvation is a SINGLE-LINE hint.
- Bypassing `strip_leaks` — the hint copy seeded in Decision 5 has no FS paths, but ear-pass might add one. Defense-in-depth says pass through `strip_leaks` regardless (T-99-05 mitigation).
- Reading from `norm.get("error")` first instead of `norm.get("hint")` first — the curate_fn normalizer (Plan 99-06) emits `"hint": <hint>`. Falling back to `error` is defensive, but `hint` is the canonical key.
- Modifying the playlist happy-path branch — must stay byte-equivalent. Run a regression test that compares the rendered output of a normal playlist `norm` dict pre- and post-edit; output must be identical.
- Forgetting that `format_reply` is PURE LOGIC + dep-free per the module docstring at `telegram_bridge.py:33-43` — the `telegram` extra is NOT needed for this test. Use the existing test posture in `tests/library/test_telegram_bridge.py`.
</antipatterns_to_avoid>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing format_reply starvation tests</name>
  <files>tests/library/test_telegram_bridge.py</files>
  <behavior>
    Add three new tests to `tests/library/test_telegram_bridge.py` (RED on current source):

    - `test_format_reply_starvation_branch`: Call `format_reply({"ok": False, "stop_reason": "tool_starvation", "hint": "library has 0 tracks — run `library ingest` first"})`. Assert return is a non-empty string AND contains `"library has 0 tracks"` substring AND starts with the warning glyph `⚠️` (or whatever the existing convention uses — verify in the existing error branch).

    - `test_format_reply_starvation_strips_leaks`: Call with a hint that contains a fake FS path: `{"ok": False, "stop_reason": "tool_starvation", "hint": "library has 0 tracks at /Users/ozai/.cache/vibemix/library.pkl"}`. Assert the returned string does NOT contain the literal path `/Users/ozai/` (proven by `strip_leaks` scrub). The exact substring assertion depends on what `strip_leaks` substitutes — check the existing `tests/library/test_telegram_bridge.py` for the scrub convention (probably replaces with `<…>` or removes path components). Mirror the existing test's assertion style.

    - `test_format_reply_existing_branches_unchanged`: Call `format_reply` with the canonical PLAYLIST happy-path input (`{"ok": True, "name": "test", "titles": ["t1", "t2"]}` — verify exact shape from existing tests). Assert output matches the BASELINE captured before this plan's edits. Run the test on current source first; capture output as `EXPECTED_PLAYLIST_OUTPUT`; pin in the test as a string equality assertion. After Task 2, the same input must produce the same output (regression-pin).

    Use the existing test posture from `tests/library/test_telegram_bridge.py` (pure-logic tests, no Telegram SDK needed). Tests MUST fail RED on current source for the first two; the third is a regression-pin that must STAY GREEN through Task 2.
  </behavior>
  <action>
    Append the three tests. For `test_format_reply_existing_branches_unchanged`, run the playlist scenario on current source FIRST, capture stdout, and pin the exact string as the expected value — this is the regression-pin contract.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_telegram_bridge.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    Three new tests present; first two fail RED (no starvation branch yet); third passes (regression-pin baseline captured); existing telegram_bridge tests stay green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert tool_starvation branch in format_reply</name>
  <files>src/vibemix/library/telegram_bridge.py</files>
  <behavior>
    Single edit in `src/vibemix/library/telegram_bridge.py::format_reply` — insert as the FIRST branch (BEFORE the existing `if not norm.get("ok"):` block):

    ```
    if norm.get("stop_reason") == "tool_starvation":
        hint = norm.get("hint") or norm.get("error") or "Viber ran out of grounded options."
        return strip_leaks(f"⚠️ {hint}")
    ```

    No other lines change. The existing error branch + playlist happy-path branch are byte-equivalent.

    All three Task-1 tests turn GREEN. Existing `tests/library/test_telegram_bridge.py` stays GREEN.
  </behavior>
  <action>
    Edit `src/vibemix/library/telegram_bridge.py` — insert the 3-line branch at the top of `format_reply` body (immediately after the docstring, BEFORE the `if not norm.get("ok"):` block). Use the warning glyph `⚠️` matching the existing convention (check that the existing error branch uses the same emoji — adjust to match exactly). Diff envelope: ~5 lines added, 0 removed.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_telegram_bridge.py tests/library/test_cli_exit_codes.py 2>&1 | tail -25</automated>
  </verify>
  <done>
    Starvation branch present at top of `format_reply`; all three Task-1 tests pass; existing tests stay green; grep gate `grep -c "tool_starvation" src/vibemix/library/telegram_bridge.py` returns exactly 1 (the new branch). Diff envelope is ~5 lines added (verified via `git diff src/vibemix/library/telegram_bridge.py | grep -E "^[+-]" | wc -l`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram bot ↔ user chat | Hint rendered as plain text; no markdown injection vector beyond what existing branches expose |
| `format_reply` input ↔ output | `strip_leaks` scrubs FS paths defensively (T-99-05 mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05 | Information disclosure | FS path leak via hint in Telegram | mitigate | All hints passed through `strip_leaks` defensively. The seeded copy (D-05) has no FS paths, but the gate covers future ear-pass changes. `test_format_reply_starvation_strips_leaks` pins this. |
| T-99-05b | Information disclosure | Existing playlist branch regression | mitigate | `test_format_reply_existing_branches_unchanged` regression-pin — playlist rendering stays byte-equivalent. |
| T-99-02 | Tampering | Hint string injection from upstream | accept | Hint originates from the toolset's deterministic case-based code (Plan 99-03); never from arbitrary user input or LLM output. Theme query interpolated into Case B's hint is plain string display, not eval. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. |
</threat_model>

<verification>
- `pytest -q tests/library/test_telegram_bridge.py` — green (existing + 3 new).
- `pytest -q tests/library/test_cli_exit_codes.py` — green (regression-pin on Plan 99-06's Telegram `curate_fn` adapter — the contract bridge holds).
- Grep gate: `grep -c "tool_starvation" src/vibemix/library/telegram_bridge.py` returns exactly 1.
- Insertion-order check: `grep -n "tool_starvation\|if not norm.get..ok.." src/vibemix/library/telegram_bridge.py` — the starvation line appears BEFORE the generic error line.
- Plan 99-05's gate test `test_stop_reason_writes_confined_to_toolset` stays green (`telegram_bridge.py` is whitelisted for `stop_reason` reads).
- Regression-pin: `test_format_reply_existing_branches_unchanged` passes — playlist rendering byte-equivalent.
</verification>

<success_criteria>
- `format_reply` renders the `tool_starvation` hint as a single warning-glyph-prefixed line.
- Hint passes through `strip_leaks` (FS paths scrubbed defensively).
- Existing playlist branch + generic error branch are byte-equivalent (regression-pinned).
- Telegram bridge end-to-end: starvation result → curate_fn normalizes (Plan 99-06) → format_reply renders hint (this plan).
- Phase 100 forward-compat: a sibling `if norm.get("stop_reason") == "clarification_needed":` branch will sit BEFORE the existing error branch alongside this one; no refactor needed.
</success_criteria>

<output>
Create `.planning/phases/99-harden-retry/99-07-SUMMARY.md` when done.

SUMMARY must record:
- Exact line of the starvation branch insertion in `format_reply`.
- The captured `EXPECTED_PLAYLIST_OUTPUT` string from Task 1's regression-pin (so future plans can re-verify).
- Confirmation that the warning glyph matches the existing error branch's emoji.
- Note that Plan 99-08 will seal the uniform-propagation contract across all three surfaces (CLI / Telegram / wrapper dataclass).
</output>
</content>
</invoke>