---
phase: 99-harden-retry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/library/toolset.py
  - tests/library/test_toolset_starvation.py
autonomous: true
requirements:
  - HARDEN-RETRY-01
  - HARDEN-RETRY-05
user_setup: []

must_haves:
  truths:
    - "`TOOL_STARVATION_THRESHOLD = 3` is defined at module scope in `library/toolset.py`."
    - "`LibraryToolset.__init__` allocates `self._consecutive_empties: int = 0` and `self.stop_reason: dict | None = None`."
    - "Both attributes default-zero/None on a fresh instance and are settable on the same instance (no frozen/slot guard interferes)."
    - "No behavior change yet — `dispatch()` is byte-equivalent on success and on existing error paths."
    - "Single-writer analog (Invariant #1) preconditions hold by construction: no other module reads or writes `_consecutive_empties` / `stop_reason`."
  artifacts:
    - path: "src/vibemix/library/toolset.py"
      provides: "Threshold constant + per-instance counter + stop_reason attribute"
      contains: "TOOL_STARVATION_THRESHOLD"
    - path: "tests/library/test_toolset_starvation.py"
      provides: "Scaffolding tests for counter/threshold/stop_reason presence + initial state"
      exports: ["test_threshold_constant_present", "test_counter_initial_zero", "test_stop_reason_initial_none"]
  key_links:
    - from: "src/vibemix/library/toolset.py"
      to: "TOOL_STARVATION_THRESHOLD module constant"
      via: "module-level integer constant near existing TOOL_CALL_TIMEOUT_S"
      pattern: "TOOL_STARVATION_THRESHOLD\\s*=\\s*3"
    - from: "LibraryToolset.__init__"
      to: "per-instance counter + stop_reason attribute"
      via: "attribute assignment in __init__ alongside self.seen / self.created / self.exported"
      pattern: "self\\._consecutive_empties\\s*=\\s*0"
---

<objective>
Add the static scaffolding for Phase 99 — module constant `TOOL_STARVATION_THRESHOLD = 3` and the two per-instance attributes on `LibraryToolset` (`_consecutive_empties: int = 0` and `stop_reason: dict | None = None`).

Purpose: Lay the spine that plans 99-02 / 99-03 / 99-04 / 99-05 wire into, without any behavior change. This plan is reviewable in isolation as "pure scaffolding, zero runtime effect" — by design.

Output: Two attribute lines + one module constant in `library/toolset.py`, plus a new test file `tests/library/test_toolset_starvation.py` whose initial tests confirm the scaffolding exists with correct defaults. Future plans append more tests to this same file (see Wave 0 gaps in `99-RESEARCH.md` § Validation Architecture).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-harden-retry/99-CONTEXT.md
@.planning/phases/99-harden-retry/99-RESEARCH.md
@CLAUDE.md

# Source files this plan touches
@src/vibemix/library/toolset.py

# Test fixture posture to mirror (read; do not modify)
@tests/library/test_toolset.py

<interfaces>
<!-- The existing per-run state attributes on LibraryToolset that this plan extends. -->
<!-- Extracted from src/vibemix/library/toolset.py:94-111 (verified at research-time). -->

From src/vibemix/library/toolset.py (existing `__init__` per-run state pattern):
```python
class LibraryToolset:
    def __init__(self, *, library, embedder, store, ...):
        # Per-run state — one toolset instance == one curation run.
        self.seen: set[str] = set()
        self.seen_sections: dict[str, set[str]] = {}
        self.issued_search_ids: set[str] = set()
        self.issued_features: dict[str, ...] = {}
        self.created: PlaylistResult | None = None
        self.exported: ExportResult | None = None
        # ─── PHASE 99 INSERTION SITE (this plan): ───────────────────
        # self._consecutive_empties: int = 0
        # self.stop_reason: dict[str, Any] | None = None
```

The existing module-level timeout constant — model the threshold constant on this:
```python
# src/vibemix/library/toolset.py (near top of file)
TOOL_CALL_TIMEOUT_S: float = 30.0
# ─── PHASE 99 INSERTION SITE (this plan): ───────────────────────────
# TOOL_STARVATION_THRESHOLD: int = 3
```
</interfaces>

<decisions_locked>
<!-- From .planning/phases/99-harden-retry/99-CONTEXT.md — these are NON-NEGOTIABLE. -->
- D-01 (Decision 1): Counter location = `LibraryToolset` instance attribute. ← THIS PLAN.
- D-03 (Decision 3): `TOOL_STARVATION_THRESHOLD = 3` as a module constant. ← THIS PLAN.
- D-04 (Decision 4): Stop-reason surface = `self.stop_reason: dict | None` instance attribute (parallel to `self.created` / `self.exported`). ← THIS PLAN.
</decisions_locked>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing scaffolding tests in tests/library/test_toolset_starvation.py</name>
  <files>tests/library/test_toolset_starvation.py</files>
  <behavior>
    Three tests that fail today (module constant absent, attributes absent) and will pass after Task 2:
    - `test_threshold_constant_present`: `from vibemix.library import toolset as tool_mod; assert tool_mod.TOOL_STARVATION_THRESHOLD == 3` (int, exactly 3 per D-03).
    - `test_counter_initial_zero`: Construct a `LibraryToolset` with the canonical fixture posture from `tests/library/test_toolset.py:148-170` (fake `library`, `embedder`, `store`) — assert `toolset._consecutive_empties == 0` immediately after construction.
    - `test_stop_reason_initial_none`: Same fixture posture — assert `toolset.stop_reason is None` immediately after construction.
    Mirror the existing fixture style from `tests/library/test_toolset.py`; share helpers via pytest fixtures inside the new file (do NOT import private symbols from other test files — pytest convention).
  </behavior>
  <action>
    Create new file `tests/library/test_toolset_starvation.py`. Read `tests/library/test_toolset.py:1-200` for canonical fixture posture (`_make_track`, `library`, `toolset`, `_stub_search`) — clone the needed minimal subset into the new file (no cross-test imports). Implement the three tests above (per D-01, D-03, D-04). Tests MUST fail under `python3 -m pytest -q tests/library/test_toolset_starvation.py` on current source — confirm RED before proceeding to Task 2. Do NOT pre-emptively skip with `pytest.skip` or `xfail` — the failing run is the contract.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_starvation.py::test_threshold_constant_present tests/library/test_toolset_starvation.py::test_counter_initial_zero tests/library/test_toolset_starvation.py::test_stop_reason_initial_none 2>&1 | tail -20</automated>
  </verify>
  <done>
    File `tests/library/test_toolset_starvation.py` exists; the three tests are present, each fails with `AttributeError` (counter/stop_reason) or `ImportError`/`AttributeError` (`TOOL_STARVATION_THRESHOLD`); no `pytest.skip` / `xfail` markers; no other test files modified. RED state captured in commit message.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TOOL_STARVATION_THRESHOLD constant and __init__ attributes in library/toolset.py</name>
  <files>src/vibemix/library/toolset.py</files>
  <behavior>
    - `TOOL_STARVATION_THRESHOLD: int = 3` declared at module scope, located near the existing `TOOL_CALL_TIMEOUT_S` constant (top of file, after imports, before class definitions).
    - In `LibraryToolset.__init__`, two new attribute assignments are added on the lines immediately AFTER the existing `self.exported: ExportResult | None = None` line:
      - `self._consecutive_empties: int = 0`
      - `self.stop_reason: dict[str, Any] | None = None`
    - No other lines change; `dispatch()` and every handler stay byte-equivalent.
    - The three failing tests from Task 1 turn GREEN.
    - The full existing toolset test suite (`tests/library/test_toolset.py`) stays GREEN (regression-pin against accidental side-effects).
  </behavior>
  <action>
    Edit `src/vibemix/library/toolset.py`. Insert `TOOL_STARVATION_THRESHOLD: int = 3` at module scope adjacent to `TOOL_CALL_TIMEOUT_S`. Inside `LibraryToolset.__init__`, insert the two attribute assignments directly after the existing `self.exported` line (verified location per `99-RESEARCH.md` Pattern 1 + Code Examples). Do NOT touch `dispatch()` body in this plan — wiring lands in plan 99-02. Do NOT touch any handler. Do NOT touch `Any` import (already present per `from typing import Any` at module top — if absent, ADD `Any` to existing typing import; verify before editing). After the edit, the three Task-1 tests must turn GREEN and `tests/library/test_toolset.py` must stay GREEN.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset_starvation.py tests/library/test_toolset.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    Module constant present; the two instance attributes initialize to `0` and `None` respectively on a fresh `LibraryToolset`; the three Task-1 tests pass; `tests/library/test_toolset.py` is byte-equivalent in pass/fail count (no regressions). The line count delta in `toolset.py` is 3 lines added (constant + two attribute assignments), no lines removed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-process Python module scope | No external surface in this plan — constant + two attrs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | `_consecutive_empties` counter on `LibraryToolset` | mitigate | Type-hint `int`; initial value 0; this plan adds the attribute only — Plan 99-02 adds increment-only logic. Bounded by Plan 99-03 short-circuit (counter never advances past threshold once `stop_reason` is set). |
| T-99-03 | Tampering (race) | Counter writes | mitigate | Per `toolset.py:407-411` documented serialization, dispatch is single-threaded within a run. This plan adds the attribute on the construction thread — no concurrent access possible at `__init__`-time. Plan 99-02 inherits the same guarantee. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure stdlib + existing in-tree code. RESEARCH.md § Package Legitimacy Audit confirms "Not applicable — zero external packages." |
</threat_model>

<verification>
- `pytest -q tests/library/test_toolset_starvation.py` — 3 tests green (Task 1 → Task 2 transition).
- `pytest -q tests/library/test_toolset.py` — existing toolset tests stay green (no regression on the canonical fixture).
- Grep gate (manual): `grep -c "_consecutive_empties\|self\.stop_reason" src/vibemix/library/toolset.py` returns exactly 2 (the two `__init__` assignments). Any other count means a stray write or read appeared — investigate before committing. This is the single-writer precondition (Invariant #1 analog) that plan 99-02 will tighten.
- Grep gate: `grep -rn "_consecutive_empties\|TOOL_STARVATION_THRESHOLD" src/vibemix/` returns hits ONLY in `src/vibemix/library/toolset.py`. No other source module references the counter or threshold yet (per disjointness contract).
</verification>

<success_criteria>
- `TOOL_STARVATION_THRESHOLD == 3` at module scope.
- `LibraryToolset()._consecutive_empties == 0` on construction.
- `LibraryToolset().stop_reason is None` on construction.
- The three Task-1 tests turn from RED (Task 1) to GREEN (Task 2).
- `dispatch()` body and all handler bodies are byte-equivalent to pre-plan state.
- No other source module references the new symbols.
- Single-writer analog precondition holds: only `__init__` writes the two attributes in this plan; no read/write elsewhere yet (Plan 99-02 will add the single dispatch-site writer).
</success_criteria>

<output>
Create `.planning/phases/99-harden-retry/99-01-SUMMARY.md` when done.

SUMMARY must record:
- The exact line numbers where `TOOL_STARVATION_THRESHOLD` and the two attributes were inserted in `toolset.py` (for plan 99-02 to reference).
- Confirmation that `dispatch()` body is byte-equivalent (diff line count + which lines changed).
- The grep-gate counts from `<verification>` above (proof of single-writer precondition).
- Note that this is pure scaffolding — Plan 99-02 will wire `dispatch()` to the counter.
</output>
</content>
</invoke>