---
phase: 03-sensing-state-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/state/__init__.py
  - src/vibemix/state/music_state.py
  - src/vibemix/state/phase.py
  - src/vibemix/state/track_resolver.py
  - tests/state/__init__.py
  - tests/state/conftest.py
  - tests/state/test_music_state.py
  - tests/state/test_phase.py
  - tests/state/test_track_resolver.py
autonomous: true
requirements:
  - SENSE-01
  - SENSE-05
  - SENSE-09
must_haves:
  truths:
    - "`from vibemix.state import MusicState, classify_phase, derive_audible_deck, derive_audible_track` resolves."
    - "`MusicState()` constructs with v4's 22 fields (incl. `_lock: threading.Lock`) at v4 defaults; `state.set_seconds == 0.0` when `set_start_at == 0.0`; `state.time_in_phase == 0.0` when `phase_started_at == 0.0`."
    - "`classify_phase([0.06]*5, audible=True)` returns one of the seven v4 labels (`silent`, `low`, `groove`, `build`, `drop`, `peak`, `breakdown`); `classify_phase(curve, audible=False)` always returns `silent`; `classify_phase([], audible=True)` returns `silent`."
    - "`derive_audible_deck({...}, {...}, xfader, connected)` returns `(label, conf)` where label ∈ {A, B, mix, none} and conf ∈ [0.0, 1.0]; verbatim v4:1093-1135 ladder."
    - "`derive_audible_track(title, deck, deck_conf, audio_audible)` follows v4:1138-1159 ladder: `(None, 0.0)` when `not audio_audible OR not title`; `(title, 0.3)` when deck == 'none'; `(title, 0.4)` when deck == 'mix'; `(title, clamp(deck_conf, 0.5, 0.85))` otherwise."
    - "POC files diff-untouched after wave 1 commit (`git diff --name-only HEAD~1..HEAD -- 'cohost*.py' 'run*.sh' mascot.html fillers/` returns empty)."
  artifacts:
    - path: "src/vibemix/state/__init__.py"
      provides: "Re-exports `MusicState`, `classify_phase`, `derive_audible_deck`, `derive_audible_track` (Event / EventDetector / AICoach / state_refresh_loop added in later waves)."
      contains: "MusicState"
    - path: "src/vibemix/state/music_state.py"
      provides: "`@dataclass MusicState` — 22 fields verbatim from v4:1009-1062 + two `@property` computed accessors."
      contains: "class MusicState"
    - path: "src/vibemix/state/phase.py"
      provides: "Free function `classify_phase(curve: list[float], audible: bool) -> str` — verbatim port of v4:1065-1090. Separate module so Phase 6 (percentile-based) can swap impl."
      contains: "def classify_phase"
    - path: "src/vibemix/state/track_resolver.py"
      provides: "Two free functions: `derive_audible_deck(deck_a, deck_b, xfader, connected)` (v4:1093-1135) and `derive_audible_track(track_title, audible_deck, deck_confidence, audio_audible)` (v4:1138-1159)."
      contains: "def derive_audible_deck"
    - path: "tests/state/test_music_state.py"
      provides: "Dataclass field smoke (22 fields exist with v4 defaults) + property tests (`set_seconds`, `time_in_phase`) + single-writer doc-contract test (asserts `_lock` is a `threading.Lock` instance)."
    - path: "tests/state/test_phase.py"
      provides: "Seven-label fixture coverage — one canned `energy_curve` per phase label that classifies as expected. Includes `audible=False → 'silent'` guard and `curve=[] → 'silent'` guard."
    - path: "tests/state/test_track_resolver.py"
      provides: "Boundary coverage of both resolver ladders — xfader edge values (0/15/16/47/48/64/80/81/111/112/127), play=False → weight 0, vol < 0.1 → weight 0, single-dominant deck, mix/none/A/B classification, track resolver none/mix/dominant branches."
  key_links:
    - from: "src/vibemix/state/music_state.py"
      to: "vibemix.audio.constants"
      via: "import"
      pattern: "from vibemix.audio.constants"
    - from: "src/vibemix/state/phase.py"
      to: "vibemix.audio.constants"
      via: "SILENT_RMS / LOW_RMS / PEAK_RMS imports"
      pattern: "SILENT_RMS|LOW_RMS|PEAK_RMS"
---

<objective>
Port the load-bearing **pure-data** primitives of v4's sensing layer into `src/vibemix/state/`: the `MusicState` dataclass (22 fields, verbatim from v4:1009-1062), the free function `classify_phase` (v4:1065-1090), and the two `derive_audible_*` free functions (v4:1093-1159). These have zero external dependencies beyond `vibemix.audio.constants` and `threading.Lock`, so they belong in wave 1 — no I/O, no event detection, no Gemini prompt building. Later waves (02 → Event/EventDetector; 03 → AICoach; 04 → state_refresh_loop + macOS backends) build on this foundation.

Purpose: Establish the data shape EventDetector and AICoach read against. `MusicState` IS the contract — every later wave depends on its field names and types being right. Keeping it in its own wave means the contract is locked before any consumer is written.

Output: 4 source files (`__init__.py` + 3 modules) + 3 test files + a `tests/state/` package directory. ~250 LOC of source, ~150 LOC of tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sensing-state-port/03-CONTEXT.md
@.planning/phases/03-sensing-state-port/03-PATTERNS.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md
@src/vibemix/audio/constants.py
@src/vibemix/audio/__init__.py
@cohost_v4.py

<interfaces>
<!-- Phase 2 outputs that wave 1 consumes (verbatim from `vibemix.audio.constants`): -->

SILENT_RMS = 0.012      # used by classify_phase
LOW_RMS    = 0.040      # used by classify_phase
PEAK_RMS   = 0.110      # used by classify_phase

<!-- The v4 sources to port verbatim (line anchors verified 2026-05-11): -->
<!-- MusicState:          cohost_v4.py:1009-1062  (22 fields + 2 @property) -->
<!-- classify_phase:      cohost_v4.py:1065-1090  (free function) -->
<!-- derive_audible_deck: cohost_v4.py:1093-1135  (free function) -->
<!-- derive_audible_track:cohost_v4.py:1138-1159  (free function) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Create state package skeleton + MusicState dataclass</name>
  <files>
    src/vibemix/state/__init__.py,
    src/vibemix/state/music_state.py,
    tests/state/__init__.py,
    tests/state/conftest.py,
    tests/state/test_music_state.py
  </files>
  <behavior>
    - `MusicState()` with no kwargs constructs successfully and exposes all 22 fields from v4:1009-1062 at their default values.
    - Field defaults match v4 EXACTLY: `audible=False`, `rms=0.0`, `bands={'sub':0.0,'low':0.0,'mid':0.0,'high':0.0}`, `onset_density=0.0`, `bpm=0.0`, `energy_curve=[]`, `phase='silent'`, `phase_started_at=0.0`, `deck_a={}`, `deck_b={}`, `xfader=64`, `controller_connected=False`, `audible_deck='none'`, `deck_confidence=0.0`, `audible_track=None`, `audible_track_confidence=0.0`, `last_audible_track=None`, `recent_moves=[]`, `long_arc=[]`, `phase_history=[]`, `track_history=[]`, `set_start_at=0.0`, `last_kaan_spoke_at=0.0`, plus `_lock` (a `threading.Lock` instance).
    - `dict`/`list` defaults use `field(default_factory=...)` — verify by constructing two instances and confirming `id(state1.bands) != id(state2.bands)` (no shared mutable default).
    - `state.set_seconds` returns `0.0` when `set_start_at == 0.0`; returns `time.time() - set_start_at` otherwise.
    - `state.time_in_phase` returns `0.0` when `phase_started_at == 0.0`; returns `time.time() - phase_started_at` otherwise.
    - `state._lock` is a `threading.Lock` instance (assert via `type(state._lock).__name__ == 'lock'` or `hasattr(state._lock, 'acquire') and hasattr(state._lock, 'release')`).
    - `from vibemix.state import MusicState` resolves.
  </behavior>
  <action>
    Create `src/vibemix/state/` as a Python package (per D-01 / 03-CONTEXT decisions, layout matches 03-PATTERNS.md §File-Map). Create `src/vibemix/state/__init__.py` re-exporting `MusicState` (this wave) — leave room for `Event, EventDetector, AICoach, state_refresh_loop, classify_phase, derive_audible_deck, derive_audible_track` to be added in subsequent waves' `__init__.py` edits.

    Create `src/vibemix/state/music_state.py`:
    - SPDX header + docstring referencing v4:1009-1062 as the source.
    - `from __future__ import annotations`.
    - Imports: `import threading`, `import time`, `from dataclasses import dataclass, field`.
    - `@dataclass MusicState` with all 22 fields. Field-by-field copy from v4:1015-1054 — keep the section comments verbatim ("# Audio", "# Phase (derived from energy curve, only valid when audible)", "# Controller (snapshot from MIDI thread)", "# Audible deck inference — which deck is producing the sound NOW", "# Track (cross-referenced with audible deck)", "# Recent moves (within last 12s, deck-attributed)", "# Historical context — lets the AI reference set shape and continuity", "# Set timing"). They are load-bearing developer documentation per 03-PATTERNS.md.
    - Use Python 3.12 PEP 604 union syntax (`str | None`, not `Optional[str]`).
    - For `bands`, use `field(default_factory=lambda: {"sub": 0.0, "low": 0.0, "mid": 0.0, "high": 0.0})` — exact v4 default (do NOT switch to a dataclass-of-bands; v4 uses a plain dict and EventDetector reads `state.bands["mid"]` / `state.bands["high"]` directly).
    - `_lock: threading.Lock = field(default_factory=threading.Lock)` — note: this is a `field` with `default_factory`, NOT a class attribute.
    - Two `@property` methods (`set_seconds`, `time_in_phase`) byte-for-byte from v4:1056-1062.
    - Class docstring lifted verbatim from v4:1011-1013 ("Single source of truth. Refreshed at 10Hz from audio + MIDI + track poll. Everything the EventDetector and AICoach need — and nothing they don't. Read-only from the consumer side; only state_refresh_loop writes to it.").

    Create `tests/state/__init__.py` (empty).

    Create `tests/state/conftest.py` (empty — `int16_sine` in `tests/audio/conftest.py` is NOT auto-shared; if needed in a later wave, import directly).

    Create `tests/state/test_music_state.py` covering the eight bullet points in `<behavior>`:
    - `test_default_construction_exposes_22_fields` — asserts every field name is set on `MusicState()` and each scalar default matches v4.
    - `test_default_factories_not_shared` — constructs two instances, asserts `state1.bands is not state2.bands` and same for the four list defaults.
    - `test_set_seconds_zero_when_unset` and `test_set_seconds_progresses_with_time` (use `monkeypatch.setattr('time.time', lambda: 100.0)`).
    - `test_time_in_phase_zero_when_unset` and `test_time_in_phase_progresses`.
    - `test_lock_is_threading_lock` — `hasattr(state._lock, 'acquire')` + `hasattr(state._lock, 'release')` + `hasattr(state._lock, 'locked')`.
    - `test_state_imports_from_package` — `from vibemix.state import MusicState`.

    Do NOT add `Event`, `EventDetector`, `AICoach`, `state_refresh_loop`, `classify_phase`, `derive_audible_*` to `__init__.py` yet — they land in later waves. The `__init__.py` for this wave only exports `MusicState`.
  </action>
  <verify>
    <automated>uv run pytest tests/state/test_music_state.py -v</automated>
  </verify>
  <done>All 22 fields present at v4 defaults; `_lock` is a Lock; `set_seconds`/`time_in_phase` properties work; mutable defaults are per-instance; `from vibemix.state import MusicState` works; 8 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Port classify_phase (free function)</name>
  <files>
    src/vibemix/state/__init__.py,
    src/vibemix/state/phase.py,
    tests/state/test_phase.py
  </files>
  <behavior>
    - `classify_phase(curve=[], audible=True)` → `"silent"` (empty curve early-out, v4:1067).
    - `classify_phase([0.1, 0.1, 0.1], audible=False)` → `"silent"` (audible=False overrides, v4:1067).
    - `classify_phase([0.005], audible=True)` → `"silent"` (last < SILENT_RMS=0.012, v4:1070-1071).
    - `classify_phase([0.020], audible=True)` → `"low"` (SILENT_RMS ≤ last < LOW_RMS=0.040, v4:1072-1073).
    - `classify_phase([0.05, 0.05, 0.05], audible=True)` → `"groove"` (len(curve) < 5 fallback, v4:1074-1075).
    - **build:** monotonic climb of ≥3 positive diffs over last 5 with total delta > 0.020 → `"build"` (v4:1080-1083). Test: `[0.02, 0.04, 0.06, 0.08, 0.10, 0.12]` → expect `"build"`.
    - **drop:** `last >= PEAK_RMS (0.110)` AND any of `recent[:3]` < LOW_RMS (0.040) → `"drop"` (v4:1084-1085). Test: `[0.05, 0.03, 0.04, 0.05, 0.06, 0.12]` last=0.12 ≥ 0.110 and recent[:3]=[0.04,0.05,0.06] has 0.04 < 0.040? NO — 0.04 is NOT < 0.040. Use `[0.05, 0.02, 0.04, 0.05, 0.06, 0.12]` so `recent[:3] = [0.04, 0.05, 0.06]` — still 0.04 not < 0.040. Use `[0.05, 0.02, 0.03, 0.05, 0.06, 0.12]` → `recent[-5:] = [0.02, 0.03, 0.05, 0.06, 0.12]`, `recent[:3] = [0.02, 0.03, 0.05]`; 0.02 < 0.040 → drop fires.
    - **breakdown:** `earlier_max ≥ 0.040` AND `last < 0.5 * earlier_max` (v4:1086-1087). Test: `[0.08, 0.08, 0.08, 0.08, 0.08, 0.03, 0.03, 0.03, 0.03, 0.03]` — earlier (indices [-10:-5]) = [0.08]*5, earlier_max=0.08, last=0.03 < 0.04 → breakdown.
    - **peak:** `all(v >= 0.045 for v in recent)` (v4:1088-1089). Test: `[0.05]*10` — all five recent ≥ 0.045 → peak. (But verify the build/drop/breakdown branches don't trip first; with a flat curve diffs are 0, monotonic_climbs=0, no build/drop/breakdown match.)
    - **groove:** default fallback (v4:1090). Test: `[0.05, 0.05, 0.05, 0.04, 0.045, 0.043, 0.044]` — len ≥ 5, no build (climbs < 3), no drop (last=0.044 not ≥ 0.110), no breakdown (earlier_max=0.05, 0.044 not < 0.025), peak fails (0.043 < 0.045) → groove.
  </behavior>
  <action>
    Create `src/vibemix/state/phase.py`:
    - SPDX header + module docstring noting v4:1065-1090 verbatim port and that Phase 6 will introduce a percentile-based override.
    - `from __future__ import annotations`.
    - `from vibemix.audio.constants import SILENT_RMS, LOW_RMS, PEAK_RMS`.
    - `def classify_phase(curve: list, audible: bool) -> str:` — function body verbatim from v4:1065-1090.
    - Docstring lifted verbatim from v4:1066: `"""Phase tag from energy curve. Returns 'silent' if not audible."""`.
    - Do NOT inline the constant values — import them. Phase 6 will override at import time.

    Update `src/vibemix/state/__init__.py`:
    - Add `from vibemix.state.phase import classify_phase` and append `"classify_phase"` to `__all__`.

    Create `tests/state/test_phase.py`:
    - `test_empty_curve_returns_silent`
    - `test_not_audible_returns_silent`
    - `test_below_silent_rms_returns_silent`
    - `test_low_band_returns_low`
    - `test_short_curve_returns_groove` (len < 5)
    - `test_build_monotonic_climb` (use exact `[0.02, 0.04, 0.06, 0.08, 0.10, 0.12]` from behavior)
    - `test_drop_high_peak_with_recent_low` (use `[0.05, 0.02, 0.03, 0.05, 0.06, 0.12]`)
    - `test_breakdown_recent_below_half_earlier_max` (use `[0.08]*5 + [0.03]*5`)
    - `test_peak_all_recent_above_045` (use `[0.05]*10`)
    - `test_groove_fallback` (use `[0.05, 0.05, 0.05, 0.04, 0.045, 0.043, 0.044]`)
    - `test_constants_imported_not_inlined` — `import src.vibemix.state.phase as p` then assert `p.SILENT_RMS == 0.012` (imported, not hardcoded).
  </action>
  <verify>
    <automated>uv run pytest tests/state/test_phase.py -v</automated>
  </verify>
  <done>All seven phase labels reachable via canned fixtures; constants imported (not hardcoded); 11 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: Port derive_audible_deck + derive_audible_track (free functions)</name>
  <files>
    src/vibemix/state/__init__.py,
    src/vibemix/state/track_resolver.py,
    tests/state/test_track_resolver.py
  </files>
  <behavior>
    **derive_audible_deck (verbatim port of v4:1093-1135):**
    - `connected=False` → `("none", 0.0)` regardless of other args (v4:1097-1098).
    - Deck weight = 0 if `not play` (v4:1114-1115). Test: `deck_a={'play': False, 'vol': 127}`, deck_b same, xfader=64, connected=True → both weights 0 → `("none", 0.0)`.
    - Deck weight = 0 if `vol/127 < 0.1` (v4:1116-1117). Test: `deck_a={'play': True, 'vol': 10}` (vol/127 ≈ 0.079 < 0.1) → wa=0 → `("none", 0.0)` if same for B.
    - **xfader_factor(A):** xfader ≥ 112 → 0.0; ≥ 80 → 0.3; ≥ 48 → 0.7; else → 1.0.
    - **xfader_factor(B):** xfader < 16 → 0.0; < 48 → 0.3; ≤ 80 → 0.7; else → 1.0.
    - Both weights < 0.05 → `("none", 0.0)`. Test: deck_a play+vol=4 (low vol skip), deck_b play+vol=4 → `("none", 0.0)`.
    - wa > 0.3 AND wb < 0.1 → `("A", min(1.0, wa))`. Test: `deck_a={'play': True, 'vol': 127}` xfader=0, deck_b vol=0 → wa = 1.0 * 1.0 = 1.0 → `("A", 1.0)`.
    - wb > 0.3 AND wa < 0.1 → `("B", min(1.0, wb))`. Test: `deck_b={'play': True, 'vol': 127}` xfader=127, deck_a vol=0 → wb=1.0 → `("B", 1.0)`.
    - wa > 0.2 AND wb > 0.2 → `("mix", min(0.5, max(wa, wb)))`. Test: both decks vol=127, xfader=64 → wa = 1.0 * 0.7 = 0.7, wb = 0.7 → `("mix", 0.5)`.
    - Otherwise dominant side with conf `max(0.4, abs(wa-wb))`. Test: deck_a vol=80, deck_b vol=30, xfader=64 → wa = (80/127)*0.7 ≈ 0.441, wb = (30/127)*0.7 ≈ 0.165; wa > 0.3, wb > 0.1 (not < 0.1), wa > 0.2, wb < 0.2 → falls through to last branch → `("A", max(0.4, 0.441-0.165))` = `("A", max(0.4, 0.276))` = `("A", 0.4)`.

    **derive_audible_track (verbatim port of v4:1138-1159):**
    - `not audio_audible` → `(None, 0.0)` regardless of other args.
    - `track_title is None or ''` → `(None, 0.0)` (the `not track_title` guard).
    - `audible_deck == "none"` → `(track_title, 0.3)`.
    - `audible_deck == "mix"` → `(track_title, 0.4)`.
    - Single dominant deck (A or B): `(track_title, min(0.85, max(0.5, deck_confidence)))`.
      - `deck_confidence = 0.2` → conf clamped to 0.5
      - `deck_confidence = 0.6` → conf = 0.6
      - `deck_confidence = 0.95` → conf clamped to 0.85
  </behavior>
  <action>
    Create `src/vibemix/state/track_resolver.py`:
    - SPDX header + module docstring noting v4:1093-1159 verbatim port. State that `derive_audible_deck` depends on `deck['play']` (controller state); the Pioneer DDJ-FLX4 play-state propagation bug means `play` may stay `False` in live use → `audible_deck → "none"` → confidence capped at 0.3 → `TRACK_CHANGE_MIN_CONFIDENCE` (0.5) never satisfied. **Phase 9 fix** — document inline with a `# KNOWN ISSUE (Phase 9):` comment block at the top of `derive_audible_deck`.
    - `from __future__ import annotations`.
    - `def derive_audible_deck(deck_a: dict, deck_b: dict, xfader: int, connected: bool) -> tuple[str, float]:` — body verbatim from v4:1093-1135, including the two inner functions `xfader_factor` and `deck_weight` (do not refactor to module-level — v4 nests them; keep that shape so the source diff is trivial).
    - `def derive_audible_track(track_title: str | None, audible_deck: str, deck_confidence: float, audio_audible: bool) -> tuple[str | None, float]:` — body verbatim from v4:1138-1159.
    - Both docstrings lifted verbatim from v4.

    Update `src/vibemix/state/__init__.py`:
    - Add `from vibemix.state.track_resolver import derive_audible_deck, derive_audible_track` and append both names to `__all__`.

    Create `tests/state/test_track_resolver.py`:
    - `test_deck_not_connected_returns_none`
    - `test_deck_no_play_returns_none`
    - `test_deck_low_vol_returns_none` (vol=10, below 0.1*127)
    - `test_deck_xfader_factors_A` — parametrize xfader values [0, 15, 16, 47, 48, 79, 80, 81, 111, 112, 127] and verify the 4-tier mapping for side A
    - `test_deck_xfader_factors_B` — symmetric for side B
    - `test_deck_dominant_A` — deck_a full, deck_b silent → ("A", 1.0)
    - `test_deck_dominant_B` — deck_b full, deck_a silent → ("B", 1.0)
    - `test_deck_mix_branch` — both decks full center → ("mix", 0.5)
    - `test_deck_fallback_dominant` — wa=0.441, wb=0.165 → ("A", 0.4) per behavior table
    - `test_track_not_audible_returns_none_zero`
    - `test_track_no_title_returns_none_zero`
    - `test_track_deck_none_returns_03`
    - `test_track_deck_mix_returns_04`
    - `test_track_deck_dominant_clamps_to_min_05` (deck_conf=0.2 → 0.5)
    - `test_track_deck_dominant_passes_through` (deck_conf=0.6 → 0.6)
    - `test_track_deck_dominant_clamps_to_max_085` (deck_conf=0.95 → 0.85)
  </action>
  <verify>
    <automated>uv run pytest tests/state/test_track_resolver.py -v</automated>
  </verify>
  <done>All `derive_audible_*` branches reachable; xfader factor table verified; track resolver ladder verified at all four boundaries; ~16 tests green.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/state/ -v` — all wave 1 tests green (target ~35 tests across 3 files).
- `uv run pytest tests/ -x -q --ignore=tests/test_audio_macos_live.py` — full suite (Phase 1 + 2 + 3 wave 1) still green; no regressions.
- `uv run ruff check src/vibemix/state/ tests/state/ && uv run ruff format --check src/vibemix/state/ tests/state/` — lint + format clean.
- `python -c "from vibemix.state import MusicState, classify_phase, derive_audible_deck, derive_audible_track; print('OK')"` — public surface importable.
- `git diff --name-only HEAD~1..HEAD -- 'cohost*.py' 'cohost.streaming.py.bak' 'run*.sh' mascot.html 'sprite-*.png' generate_bat.py '_test_*.py' test_voice.py fillers/` — empty (POC untouched).
</verification>

<success_criteria>
1. `from vibemix.state import MusicState, classify_phase, derive_audible_deck, derive_audible_track` resolves with all four names exported via `__all__` in `src/vibemix/state/__init__.py`.
2. `MusicState()` exposes the 22 v4 fields at correct defaults; mutable defaults are per-instance; `_lock` is a `threading.Lock`; both `@property` accessors return correct values.
3. `classify_phase` returns one of the seven v4 labels for every canned fixture covering each branch; `audible=False` and `curve=[]` both short-circuit to `"silent"`.
4. `derive_audible_deck` and `derive_audible_track` ladders verified at every branch boundary, including xfader factor edges and confidence clamp boundaries.
5. POC files untouched; ruff clean; full suite green.
6. Atomic commit: `feat(03): wave 1 — MusicState + classify_phase + audible-deck/track resolvers (v4 verbatim)`.
</success_criteria>

<output>
After completion, do NOT create a per-wave SUMMARY. The Phase 3 SUMMARY is produced in wave 5 (`03-05-PLAN.md`) covering all 4 implementation waves.
</output>
