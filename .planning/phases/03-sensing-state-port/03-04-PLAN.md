---
phase: 03-sensing-state-port
plan: 04
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - src/vibemix/state/__init__.py
  - src/vibemix/state/refresh.py
  - src/vibemix/platform/__init__.py
  - src/vibemix/platform/_screen_macos.py
  - src/vibemix/platform/_midi_macos.py
  - src/vibemix/platform/_track_macos.py
  - tests/state/test_refresh.py
  - tests/test_screen_macos.py
  - tests/test_midi_macos.py
  - tests/test_track_macos.py
autonomous: true
requirements:
  - SENSE-01
  - SENSE-02
  - SENSE-05
  - SENSE-09
  - SCREEN-01
  - SCREEN-03
  - SCREEN-04
  - SCREEN-05
must_haves:
  truths:
    - "`from vibemix.state import state_refresh_loop` resolves; `state_refresh_loop` is an async function."
    - "`state_refresh_loop` cadence is 10Hz — `await asyncio.sleep(0.1)` at top of loop body (v4:1659 verbatim). Test pins this by `mocker.patch('asyncio.sleep')` and asserting the kwarg is 0.1."
    - "`state_refresh_loop` is the SINGLE writer to MusicState — its writes are wrapped in `with state._lock:` (v4:1684 verbatim)."
    - "`state_refresh_loop` calls `snapshot_features(audio_buf, seconds=4.0)`, `energy_curve(audio_buf, seconds=12.0, hop=1.0)`, `long_arc_curve(audio_buf, seconds=120.0, hop=10.0)`, `estimate_bpm(audio_buf, seconds=6.0)` from `vibemix.audio.features` as FREE FUNCTIONS (NOT methods on AudioBuffer — Phase 2 refactor)."
    - "BPM refresh gate: `estimate_bpm` is invoked only when `now - last_bpm_at > 3.0 AND currently_loud` (v4:1670-1672 verbatim)."
    - "Audible debouncing: edges sustained for `AUDIBLE_DEBOUNCE_SEC` / `SILENCE_DEBOUNCE_SEC` before flipping `state.audible` (v4:1674-1690 verbatim)."
    - "Phase transition handling: when `new_phase != state.phase`, appends `(now, old_phase, new_phase)` to `state.phase_history` (trim to last 6), updates `state.phase` and `state.phase_started_at` (v4:1703-1710 verbatim)."
    - "Track-history append gate: `(now, title)` is appended ONLY when `tt and tc >= 0.5 and tt != last_history_title` (v4:1734-1739 verbatim — the 0.5 threshold matches `TRACK_CHANGE_MIN_CONFIDENCE`, prevents jitter pollution)."
    - "Error wrap: entire tick body is `try / except Exception as e: print(f'[state refresh err] {e}', file=sys.stderr)` — loop NEVER exits on exception (v4:1750-1751)."
    - "`ScreenMacOS` implements `ScreenBackend` Protocol — `isinstance(ScreenMacOS(), ScreenBackend) is True`. Internal `ScreenBuffer` stores latest JPEG with `threading.Lock` (v4:759-772 verbatim). `find_djay_window_bounds()` lives as a private module function inside `_screen_macos.py` (v4:220-242 verbatim). `screen_capture_loop` is exposed as `async ScreenMacOS.run_capture_loop(state, stop_event)` and **pauses (sleeps 1s + continues) when `not state.audible`** (v4:993-1002 verbatim)."
    - "`MidiMacOS` implements `MidiBackend` Protocol — `isinstance(MidiMacOS(), MidiBackend) is True`. Module-private `_CC_MAP` and `_NOTE_MAP` BYTE-IDENTICAL to v4:582-598 (asserted by `==` comparison test). `_knob_label` and `_xfader_label` byte-identical to v4:601-615. Embeds `ControllerState` class (v4:618-727 verbatim). MIDI listener runs as a daemon `threading.Thread` (v4:730-756 verbatim) — NOT asyncio. Reproduces v4's DDJ-FLX4 play-state limitation verbatim (`play` defaults False, toggled on note_on); a KNOWN ISSUE docstring block at module top references Phase 9 fix."
    - "`TrackMacOS` implements `TrackInfoBackend` Protocol — `isinstance(TrackMacOS(), TrackInfoBackend) is True`. Internal `TrackInfo` class wraps `subprocess.check_output([cli, 'get', 'title', 'artist'], ...)` (v4:544-549 verbatim — NOT `get-raw`; outputs two newline-separated lines: title, artist). Format string `f'{artist} - {title}'` when both present (v4:554)."
    - "POC files diff-untouched after wave 4 commit."
  artifacts:
    - path: "src/vibemix/state/refresh.py"
      provides: "`async def state_refresh_loop(state, audio_buf, controller_state_backend, track_info_backend, stop_event)` — verbatim port of v4:1647-1751 with the four `audio_buf.X(...)` METHOD calls rewritten as `X(audio_buf, ...)` FREE FUNCTION calls per Phase 2 refactor. ONLY writer to MusicState. 10Hz cadence. Locks `state._lock` for batched writes."
      contains: "async def state_refresh_loop"
    - path: "src/vibemix/platform/_screen_macos.py"
      provides: "`ScreenMacOS` class — implements ScreenBackend Protocol via mss + Quartz. Embeds the v4 `ScreenBuffer` shape internally + a public `latest()` for Phase 4 consumers. `async run_capture_loop(state, stop_event)` ports v4:956-1002 verbatim with `state.audible` gating."
      contains: "class ScreenMacOS"
    - path: "src/vibemix/platform/_midi_macos.py"
      provides: "`MidiMacOS` class — implements MidiBackend Protocol via mido. Embeds `ControllerState` (v4:618-727), module-private `_CC_MAP` + `_NOTE_MAP` + `_knob_label` + `_xfader_label` (v4:582-615 verbatim). Spawns daemon `threading.Thread` (v4:730-756) for the polling listener."
      contains: "class MidiMacOS"
    - path: "src/vibemix/platform/_track_macos.py"
      provides: "`TrackMacOS` class — implements TrackInfoBackend Protocol via subprocess. Embeds `TrackInfo` class (v4:532-567 verbatim) with 1Hz poll cadence; exposes `snapshot()` and `start()`/`stop()` for the async polling task (v4:570-577)."
      contains: "class TrackMacOS"
    - path: "tests/state/test_refresh.py"
      provides: "Golden-tick test: feed canned `AudioBuffer` snapshot + fake `ControllerState` + fake `TrackInfo` to one iteration of `state_refresh_loop` body (extracted as `_tick_once` helper OR via patching `asyncio.sleep` to short-circuit) and assert final `MusicState` fields match expectations. Includes BPM-gate test, debouncing test, phase-history append test, track-history append-gate test, error-wrap test."
    - path: "tests/test_screen_macos.py"
      provides: "Mocked mss + Quartz tests — `find_djay_window_bounds` returns largest window matching `'djay'` substring; capture pipeline produces JPEG bytes within size budget. Marks live mss usage under `macos_audio` pytest marker (reuse the Phase 2 marker, or introduce a `macos_screen` marker if cleaner — planner discretion)."
    - path: "tests/test_midi_macos.py"
      provides: "Mocked mido tests — feed canned `mido.Message`-shaped namedtuples through `ControllerState.handle_msg`, assert deck/xfader/_moves updates. `_CC_MAP` and `_NOTE_MAP` byte-equality tests. `_knob_label` and `_xfader_label` boundary tests."
    - path: "tests/test_track_macos.py"
      provides: "Mocked `subprocess.check_output` tests — assert title parsing (title-only, title+artist, both empty, subprocess timeout, FileNotFoundError → graceful no-op). isinstance(TrackBackend) check."
  key_links:
    - from: "src/vibemix/state/refresh.py"
      to: "vibemix.audio.features"
      via: "snapshot_features / energy_curve / long_arc_curve / estimate_bpm (free function calls)"
      pattern: "snapshot_features\\(|energy_curve\\(|long_arc_curve\\(|estimate_bpm\\("
    - from: "src/vibemix/state/refresh.py"
      to: "vibemix.state.track_resolver"
      via: "derive_audible_deck + derive_audible_track"
      pattern: "derive_audible_deck|derive_audible_track"
    - from: "src/vibemix/state/refresh.py"
      to: "vibemix.state.phase"
      via: "classify_phase"
      pattern: "classify_phase"
    - from: "src/vibemix/platform/_midi_macos.py"
      to: "vibemix.platform.midi"
      via: "structural Protocol satisfaction"
      pattern: "MidiBackend"
    - from: "src/vibemix/platform/_screen_macos.py"
      to: "vibemix.platform.screen"
      via: "structural Protocol satisfaction"
      pattern: "ScreenBackend"
    - from: "src/vibemix/platform/_track_macos.py"
      to: "vibemix.platform.track"
      via: "structural Protocol satisfaction"
      pattern: "TrackInfoBackend"
---

<objective>
Land the **runtime layer** that ties waves 1-3 together: `state_refresh_loop` (the 10Hz single writer) plus the three macOS platform backend impls (`ScreenMacOS`, `MidiMacOS`, `TrackMacOS`) that satisfy the Phase 1 Protocols. After wave 4, a Phase 4 LiveKit agent can `from vibemix.state import state_refresh_loop, MusicState, EventDetector, AICoach` and `from vibemix.platform import ScreenMacOS, MidiMacOS, TrackMacOS` and wire a complete sensing stack.

This is the largest wave (4 source files + 4 test files, ~600 LOC source / ~400 LOC tests) and the only one with cross-thread concerns (sounddevice callbacks already in Phase 2 + MIDI daemon thread + asyncio refresh loop). Per CONTEXT decision: MIDI stays on a daemon `threading.Thread` (mido.poll is blocking-only); locks protect the cross-thread `ControllerState` surface.

**Critical alignment facts (verified against on-disk cohost_v4.py 2026-05-11):**

1. v4's `state_refresh_loop` calls `audio_buf.snapshot_features(seconds=4.0)`, `audio_buf.energy_curve(...)`, `audio_buf.estimate_bpm(...)`, `audio_buf.long_arc_curve(...)` as METHODS on AudioBuffer. **Phase 2 refactored these to FREE FUNCTIONS** in `vibemix.audio.features` (Phase 2 SUMMARY rationale: testability without standing up the whole audio package). Wave 4 must rewrite these four call sites as `snapshot_features(audio_buf, seconds=4.0)` etc. This is the one structural deviation from v4 — every other line is verbatim.

2. v4 has TWO 16kHz buffers (`audio_buf` 8x-gain-boosted, `clean_audio_buf` natural-level) populated by `start_input_to_session` (v4:899-953). Phase 2's `BufferRegistry` already exposes both via `registry.audio` and `registry.clean_audio`. `state_refresh_loop` reads from `registry.audio` (the gain-boosted one — per v4:1664 which uses the boosted buffer for state thresholds tuned in v2). Phase 4 will wire `registry.clean_audio` into `DJCoHostAgent.llm_node` via `snapshot_wav`. Wave 4 takes `audio_buf: AudioBuffer` as a parameter — the caller (Phase 4 `main()`) supplies `registry.audio`.

3. v4's `screen_capture_loop` GATES on `state.audible` (v4:993-997) — pauses capture when no music. Port verbatim.

4. v4's MIDI listener is a daemon `threading.Thread` (v4:730-756) with retry-every-2s. Port verbatim. Document the FLX4 play-state limitation (Phase 9 fix) in the module docstring per CONTEXT decision.

5. v4's `TrackInfo` uses `subprocess.check_output([cli, 'get', 'title', 'artist'], ...)` (v4:544-549) — TWO-line output, NOT JSON. Port verbatim. The Phase 1 Protocol `TrackInfoBackend.poll()` returns `NowPlayingSnapshot | None`; convert v4's dict-shape `snapshot()` to that dataclass.

Output: 4 source modules + 4 test modules. ~1000 LOC total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-sensing-state-port/03-CONTEXT.md
@.planning/phases/03-sensing-state-port/03-PATTERNS.md
@.planning/phases/03-sensing-state-port/03-01-PLAN.md
@.planning/phases/03-sensing-state-port/03-02-PLAN.md
@.planning/phases/03-sensing-state-port/03-03-PLAN.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md
@src/vibemix/audio/__init__.py
@src/vibemix/audio/features.py
@src/vibemix/audio/buffers.py
@src/vibemix/audio/registry.py
@src/vibemix/platform/audio.py
@src/vibemix/platform/screen.py
@src/vibemix/platform/midi.py
@src/vibemix/platform/track.py
@src/vibemix/state/music_state.py
@src/vibemix/state/event_detector.py
@src/vibemix/state/coach.py
@src/vibemix/state/phase.py
@src/vibemix/state/track_resolver.py
@cohost_v4.py

<interfaces>
<!-- Phase 1 Protocol contracts that wave 4 macOS impls must satisfy structurally: -->

# ScreenBackend (src/vibemix/platform/screen.py:38-57)
class ScreenBackend(Protocol):
    def is_available(self) -> bool: ...
    def find_window_bounds(self, app_name_substring: str) -> WindowBounds | None: ...
    def capture(self, bounds: WindowBounds | None, *, max_width: int = 1280, max_height: int = 800, jpeg_quality: int = 82) -> CapturedFrame: ...

# MidiBackend (src/vibemix/platform/midi.py:44-55)
class MidiBackend(Protocol):
    def list_input_ports(self) -> list[str]: ...
    def open_input(self, port_name: str) -> MidiPort: ...

# MidiPort (src/vibemix/platform/midi.py:33-41)
class MidiPort(Protocol):
    name: str
    def poll(self) -> MidiMessage | None: ...
    def close(self) -> None: ...

# TrackInfoBackend (src/vibemix/platform/track.py:34-49)
class TrackInfoBackend(Protocol):
    def is_available(self) -> bool: ...
    def poll(self) -> NowPlayingSnapshot | None: ...

# NowPlayingSnapshot (src/vibemix/platform/track.py:19-31)
@dataclass(frozen=True)
class NowPlayingSnapshot:
    title: str | None
    artist: str | None
    album: str | None
    duration_sec: float | None
    position_sec: float | None

<!-- Phase 2 audio surface (verbatim): -->
from vibemix.audio import (AudioBuffer, snapshot_features, energy_curve, long_arc_curve, estimate_bpm,
                          SILENT_RMS, LOW_RMS, PEAK_RMS, AUDIBLE_DEBOUNCE_SEC, SILENCE_DEBOUNCE_SEC,
                          BufferRegistry)

<!-- v4 line anchors (verified 2026-05-11): -->
<!-- state_refresh_loop:        cohost_v4.py:1647-1751 -->
<!-- ScreenBuffer:              cohost_v4.py:759-772 -->
<!-- find_djay_window_bounds:   cohost_v4.py:220-242 -->
<!-- screen_capture_loop:       cohost_v4.py:956-1002 -->
<!-- _CC_MAP:                   cohost_v4.py:582-590 (VERBATIM — copy-paste from 03-PATTERNS.md §8) -->
<!-- _NOTE_MAP:                 cohost_v4.py:591-598 (VERBATIM) -->
<!-- _knob_label / _xfader_label: cohost_v4.py:601-615 -->
<!-- ControllerState:           cohost_v4.py:618-727 -->
<!-- midi_listener_thread:      cohost_v4.py:730-756 -->
<!-- TrackInfo:                 cohost_v4.py:532-567 -->
<!-- track_poll_loop:           cohost_v4.py:570-577 -->

<!-- The Quartz import header for _screen_macos.py (will be inside the impl file, NOT in the typing-only Protocol module): -->
from Quartz import (
    CGWindowListCopyWindowInfo,
    kCGWindowListOptionOnScreenOnly,
    kCGNullWindowID,
)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: Port state_refresh_loop (the 10Hz single writer)</name>
  <files>
    src/vibemix/state/__init__.py,
    src/vibemix/state/refresh.py,
    tests/state/test_refresh.py
  </files>
  <behavior>
    **Signature (lifted from v4:1647-1649, adapted for backend Protocols):**
    ```python
    async def state_refresh_loop(
        state: MusicState,
        audio_buf: AudioBuffer,
        controller_state: ControllerState,  # macOS-specific concrete class from _midi_macos
        track_info: TrackInfo,               # macOS-specific concrete class from _track_macos
        stop_event: asyncio.Event,
    ) -> None: ...
    ```
    Note: parameter types are concrete v4 shapes (`ControllerState`, `TrackInfo`) rather than the Phase 1 Protocols. This preserves v4's tight coupling — `controller_state.deck_snapshot()` returns a dict with the exact `{A, B, xfader, connected}` shape; `track_info.snapshot()` returns a dict with `{title, prev_title, title_changed_at}`. Phase 4 will adapt these to the Protocols if cross-platform reuse forces a contract widening.

    **Loop body (verbatim from v4:1658-1751 except the four free-function rewrites):**

    Tests against an instrumented helper `_tick_once(state, audio_buf, controller_state, track_info, *, fake_now, _last_audible_high=0.0, _last_audible_low=0.0, _bpm_cache=0.0, _last_bpm_at=0.0) -> dict` extracted from the loop body. Extracting `_tick_once` is necessary so tests can drive single ticks deterministically with fake time and fake snapshots, without spinning up `asyncio`. The async loop calls `_tick_once` in its `while not stop_event.is_set():` body.

    1. **Free-function rewrites (the ONE structural deviation from v4):**
       - `feats = audio_buf.snapshot_features(seconds=4.0)` → `feats = snapshot_features(audio_buf, seconds=4.0)`
       - `curve = audio_buf.energy_curve(seconds=12.0, hop=1.0)` → `curve = energy_curve(audio_buf, seconds=12.0, hop=1.0)`
       - `bpm_cache = audio_buf.estimate_bpm(seconds=6.0)` → `bpm_cache = estimate_bpm(audio_buf, seconds=6.0)`
       - `state.long_arc = audio_buf.long_arc_curve(seconds=120.0, hop=10.0)` → `state.long_arc = long_arc_curve(audio_buf, seconds=120.0, hop=10.0)`

    2. **BPM refresh gate test:** call `_tick_once` with `currently_loud=False` (low RMS audio buffer) and assert `estimate_bpm` was NOT called. Call with `currently_loud=True` but `now - last_bpm_at = 2.5 < 3.0` and assert `estimate_bpm` was NOT called. Call with `currently_loud=True, now - last_bpm_at = 3.5` and assert `estimate_bpm` WAS called.

    3. **Audible-debouncing test:** drive 5 ticks of loud audio → after `AUDIBLE_DEBOUNCE_SEC=0.6` of sustained loud, `state.audible` flips to True. Drive 5 ticks of silent → after `SILENCE_DEBOUNCE_SEC=1.2` of sustained silent, flips to False. The edge timers `last_audible_high` / `last_audible_low` are reset on each polarity flip.

    4. **Phase transition test:** drive a tick where `classify_phase(curve, audible)` returns `"drop"` (use a canned curve) while `state.phase=="groove"`. After tick: `state.phase == "drop"`, `state.phase_started_at == fake_now`, `state.phase_history[-1] == (fake_now, "groove", "drop")`. After 7 phase transitions, `state.phase_history` is trimmed to last 6.

    5. **Track-history append gate test:** drive a tick where derive_audible_track returns `("Title", 0.6)` (>= 0.5). After tick: `state.track_history[-1] == (fake_now, "Title")`. Drive next tick with same title — NO new entry (dedupe on `tt != last_history_title`). Drive tick with `("Title", 0.4)` (< 0.5) — NO new entry. Drive tick with confidence 0.6 but title=`None` → NO new entry.

    6. **Audible-deck + audible-track writes test:** stub `controller_state.deck_snapshot()` to return `{'A': {'play': True, 'vol': 127}, 'B': {'play': False, 'vol': 0}, 'xfader': 0, 'connected': True}` → after tick, `state.audible_deck == "A"`, `state.deck_confidence > 0.5`. Stub `track_info.snapshot()` to return `{'title': 'Daft Punk - Around the World', 'prev_title': '', 'title_changed_at': 0.0}` → after tick, `state.audible_track == "Daft Punk - Around the World"`, `state.audible_track_confidence` in `[0.5, 0.85]`.

    7. **Recent moves write test:** stub `controller_state.moves_since(now - 12.0)` to return `[(2.0, 'A_play→ON'), (5.0, 'xfader→full-A')]` → after tick, `state.recent_moves == [(2.0, 'A_play→ON'), (5.0, 'xfader→full-A')]`.

    8. **Error-wrap test:** stub `snapshot_features` to raise `RuntimeError("test")` — `_tick_once` should NOT propagate; instead it logs to stderr (capturable via pytest's `capsys`) with the exact v4 format `[state refresh err] test`. Loop continues (next call works normally).

    9. **Single-writer / lock test:** monkeypatch `state._lock` to a `MagicMock` proxy that tracks `__enter__`/`__exit__` invocations — assert `_tick_once` enters and exits the lock exactly once per tick.

    10. **Async loop sleep cadence test:** instrument `asyncio.sleep` via `mocker.patch` and run `state_refresh_loop` for one iteration (set `stop_event` after first `sleep` returns) — assert `asyncio.sleep` was called with `0.1` (v4:1659).
  </behavior>
  <action>
    Create `src/vibemix/state/refresh.py`:
    - SPDX header + module docstring noting v4:1647-1751 verbatim port with ONE structural deviation: the four `audio_buf.METHOD(...)` calls become `FUNCTION(audio_buf, ...)` calls per Phase 2's free-function refactor (cite Phase 2 SUMMARY).
    - `from __future__ import annotations`.
    - `import asyncio`, `import sys`, `import time`.
    - Imports:
      ```python
      from vibemix.audio import (
          AudioBuffer,
          AUDIBLE_DEBOUNCE_SEC,
          SILENCE_DEBOUNCE_SEC,
          SILENT_RMS,
          energy_curve,
          estimate_bpm,
          long_arc_curve,
          snapshot_features,
      )
      from vibemix.state.music_state import MusicState
      from vibemix.state.phase import classify_phase
      from vibemix.state.track_resolver import derive_audible_deck, derive_audible_track
      ```
    - Type-hint imports for `ControllerState` and `TrackInfo` from the macOS impls. Since wave 4 also creates those, use TYPE_CHECKING guards to avoid circular imports:
      ```python
      from typing import TYPE_CHECKING
      if TYPE_CHECKING:
          from vibemix.platform._midi_macos import ControllerState
          from vibemix.platform._track_macos import TrackInfo
      ```
    - Define an inner helper `def _tick_once(state, audio_buf, controller_state, track_info, *, now, last_audible_high, last_audible_low, bpm_cache, last_bpm_at) -> tuple[float, float, float, float]:` that runs ONE tick of the body and returns the updated (last_audible_high, last_audible_low, bpm_cache, last_bpm_at). Body is v4:1660-1751 with the four free-function rewrites.
    - `state_refresh_loop` body is the `while not stop_event.is_set():` wrapper around `await asyncio.sleep(0.1)` then a try/except calling `_tick_once` and updating local state.

    Update `src/vibemix/state/__init__.py`:
    - Add `from vibemix.state.refresh import state_refresh_loop`; append `"state_refresh_loop"` to `__all__`.

    Create `tests/state/test_refresh.py` covering behaviors 1-10. Use:
    - `pytest-mock` for `mocker.patch('vibemix.state.refresh.time.time', return_value=fake_now)` and `mocker.patch('vibemix.state.refresh.snapshot_features', ...)`.
    - Build a real `AudioBuffer` with `int16_sine` helper (import from `tests/audio/conftest.py`) — produces a deterministic snapshot_features output.
    - Build fake `ControllerState` and `TrackInfo` stubs using `unittest.mock.Mock(spec=...)` — easier than spinning up real instances.
    - The "async loop sleep cadence" test (#10) uses `asyncio.run` with a `stop_event.set()` scheduled via `asyncio.get_running_loop().call_later(0.0, stop_event.set)` so the loop exits after one iteration.
  </action>
  <verify>
    <automated>uv run pytest tests/state/test_refresh.py -v</automated>
  </verify>
  <done>state_refresh_loop ports verbatim; 4 free-function rewrites verified by grep; 10 behavioral tests green incl. lock + sleep cadence + error wrap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: Port macOS Screen + MIDI + Track backends</name>
  <files>
    src/vibemix/platform/__init__.py,
    src/vibemix/platform/_screen_macos.py,
    src/vibemix/platform/_midi_macos.py,
    src/vibemix/platform/_track_macos.py,
    tests/test_screen_macos.py,
    tests/test_midi_macos.py,
    tests/test_track_macos.py
  </files>
  <behavior>
    **ScreenMacOS:**
    - Constructor takes no required args.
    - `screen.is_available() -> bool` returns True when both `mss` and `Quartz` import successfully; False otherwise (try/except at import time; cache the result on the instance).
    - `screen.find_window_bounds("djay") -> WindowBounds | None` — wraps the v4:220-242 `find_djay_window_bounds()` algorithm. Test: mock `Quartz.CGWindowListCopyWindowInfo` to return a list with two djay-named windows of different sizes; return the LARGER one. Mock to return zero windows; return None. Mock to return a window of width<200 or height<200; skip (return None).
    - `screen.capture(bounds, *, max_width=1280, max_height=800, jpeg_quality=82) -> CapturedFrame` — captures the screen via mss, crops to `bounds` if non-None, thumbnails to max_width/max_height, encodes JPEG at quality. Test: mock `mss.mss()` and `Image.frombytes` to return a 1920x1080 RGB image; assert returned `CapturedFrame.jpeg` has length > 0 and `CapturedFrame.width <= 1280`.
    - `async screen.run_capture_loop(state: MusicState, screen_buf, stop_event: asyncio.Event)` — async wrapper around the v4:956-1002 capture loop. **Pauses (`await asyncio.sleep(1.0); continue`) when `not state.audible`** — preserves the v4 CPU-save behavior. Internal `ScreenBuffer` (v4:759-772) lives in this module and is the destination of `push()`. Phase 4 reads via `screen.latest()`.
    - `isinstance(ScreenMacOS(), ScreenBackend) is True` (structural Protocol check).

    **MidiMacOS:**
    - Constructor takes no required args.
    - `midi.list_input_ports() -> list[str]` wraps `mido.get_input_names()`.
    - `midi.open_input(port_name) -> MidiPort` returns a `_MidoPortAdapter` that wraps `mido.open_input(port_name)` and satisfies the `MidiPort` Protocol (`.name` attribute + `.poll()` + `.close()`).
    - `midi.controller_state -> ControllerState` exposes the live ControllerState (v4:618-727 verbatim) — Phase 3 wave 4 wires it; Phase 4 / Phase 9 consume via `controller_state.deck_snapshot()` and `controller_state.moves_since(t)`.
    - `midi.start_listener_thread(controller_state, stop_event: threading.Event)` spawns the v4:730-756 daemon thread for the polling loop. Thread retries every 2s on disconnect/exception (verbatim).
    - **Module-private constants are byte-identical to v4:582-615:** Test reads `from vibemix.platform._midi_macos import _CC_MAP, _NOTE_MAP, _knob_label, _xfader_label` and asserts equality to literal dicts copied from 03-PATTERNS.md §8 (paste from PATTERNS.md verbatim — those bytes are LOAD-BEARING IP).
    - `_knob_label(v)` boundary tests: v=7→"killed", v=8→"deep-cut" (note v4:602 uses `< 8` not `<= 7`); v=29→"deep-cut", v=30→"cut"; v=54→"cut", v=55→"flat"; v=73→"flat", v=74→"boost"; v=100→"boost", v=101→"max".
    - `_xfader_label(v)` boundary tests: v=15→"full-A", v=16→"A-side"; v=47→"A-side", v=48→"center"; v=80→"center", v=81→"B-side"; v=112→"B-side", v=113→"full-B".
    - `ControllerState.handle_msg(msg)` decode behavior (verbatim from v4:654-712):
      - control_change on `(channel=0, control=0x13)` (A_vol) with value=80 then value=120 → records `("A_vol up (medium)", ts)` in `_moves` (delta=40, magnitude="medium").
      - control_change on `(channel=6, control=0x1F)` (xfader) from 64 to 0 → records `"xfader→full-A"` only because tier changed.
      - note_on on `(channel=0, note=0x0B)` (A_play) — toggles `deck['A']['play']` from False → True, records `"A_play→ON"`.
      - note_on on `(channel=0, note=0x10)` (A_loop_in) — SETS `deck['A']['play'] = True` (v4:700), records `"A_loop_in_hit (play=ON)"`.
      - note_on on `(channel=0, note=0x60)` (A_sync) — records `"A_sync_hit"`, no state change.
      - note_off on `(channel=0, note=0x36)` (A_jog_touch) — clears `deck['A']['jog_touched']` to False.
    - `ControllerState.deck_snapshot()` returns a dict with `{'A': {...copy}, 'B': {...copy}, 'xfader': int, 'connected': bool}` — A and B are deep-copy dicts (so caller can't mutate listener's state).
    - `ControllerState.moves_since(t)` returns list of `(seconds_ago_rounded_0.1, label)` for moves with `mt > t`. Verifies the time-relative conversion (v4:724-727).
    - **KNOWN ISSUE docstring at module top** documents the FLX4 play-state limitation and points to Phase 9 fix (per CONTEXT decision + user request 7).
    - `isinstance(MidiMacOS(), MidiBackend) is True`.

    **TrackMacOS:**
    - Constructor takes no required args; locates `nowplaying-cli` via `shutil.which()` with `/opt/homebrew/bin/nowplaying-cli` fallback (v4:542).
    - `track.is_available() -> bool` returns True iff the resolved CLI path exists (`Path(cli).is_file()`).
    - `track.poll() -> NowPlayingSnapshot | None`:
      - Calls `subprocess.check_output([cli, "get", "title", "artist"], timeout=1.5, stderr=subprocess.DEVNULL)` (v4:546-548 verbatim — **NOT `get-raw`**; outputs two newline-separated lines).
      - Parses: first line = title, second line = artist.
      - Returns `NowPlayingSnapshot(title=..., artist=..., album=None, duration_sec=None, position_sec=None)`.
      - On TimeoutExpired / CalledProcessError / FileNotFoundError / OSError → returns `None` (graceful — v4:550-551 verbatim catch list).
    - `track.snapshot()` is the legacy v4-dict-shape accessor that state_refresh_loop reads — returns `{'title': '...', 'prev_title': '...', 'title_changed_at': ts}`. Title is `f"{artist} - {title}"` when both present, else just title (v4:554). Internal `TrackInfo` class (v4:532-567 verbatim) holds the prev/current/timestamp state.
    - `async track.run_poll_loop(stop_event: asyncio.Event)` — wraps the v4:570-577 `track_poll_loop` as a method. Calls `poll_once` via `loop.run_in_executor` since subprocess is blocking, sleeps 1s between calls.
    - `isinstance(TrackMacOS(), TrackInfoBackend) is True`.

    **Tests use pytest-mock to stub `mss`, `Quartz`, `mido`, `subprocess.check_output`. The `tests/test_*_macos.py` tests do NOT run live (no real screen capture, no real MIDI port, no real subprocess) — all mocked. Live smoke tests (if any) are marked `@pytest.mark.macos_audio` (reuse the Phase 2 marker) and run only when Kaan opts in.**
  </behavior>
  <action>
    **Create `src/vibemix/platform/_screen_macos.py`:**
    - SPDX header + module docstring noting v4:220-242 + v4:759-772 + v4:956-1002 verbatim port. Note the `Quartz.CGWindowListCreateImageFromArray` deprecation in macOS 14+ with a `# Phase 8: ScreenCaptureKit migration replaces the Quartz path` comment.
    - `from __future__ import annotations`.
    - Imports: `import asyncio`, `import io`, `import sys`, `import threading`. Then try/except for `mss`, `PIL.Image`, `Quartz` — set `_HAS_MSS`, `_HAS_QUARTZ` module flags. These flags are PRIVATE to this file (the underscore-prefix discipline keeps Phase 1's no-feature-flag rule intact at the package boundary; here in the macOS impl we DO need the flags because is_available() needs to check the imports).
    - `from vibemix.platform.screen import CapturedFrame, ScreenBackend, WindowBounds`.
    - `from vibemix.state.music_state import MusicState`.
    - Private module function `_find_djay_window_bounds(app_name_substring: str) -> WindowBounds | None` — v4:220-242 verbatim, returning a `WindowBounds` dataclass instance instead of a tuple.
    - Class `_ScreenBuffer` (v4:759-772 verbatim, prefixed `_` to mark module-private).
    - Class `ScreenMacOS` with:
      - `__init__(self)` — instantiates an internal `_ScreenBuffer`.
      - `is_available(self) -> bool` — returns `_HAS_MSS and _HAS_QUARTZ`.
      - `find_window_bounds(self, app_name_substring: str) -> WindowBounds | None` — calls `_find_djay_window_bounds(app_name_substring)`.
      - `capture(self, bounds, *, max_width=1280, max_height=800, jpeg_quality=82) -> CapturedFrame` — refactor of v4 `grab()` (v4:973-991) into a synchronous method.
      - `latest(self) -> tuple[bytes | None, tuple[int, int]]` — delegates to internal `_ScreenBuffer.latest()`.
      - `async run_capture_loop(self, state: MusicState, stop_event: asyncio.Event)` — v4:956-1002 verbatim with `state.audible` gating; pushes JPEG bytes to internal `_ScreenBuffer`.

    **Create `src/vibemix/platform/_midi_macos.py`:**
    - SPDX header + module docstring with the **KNOWN ISSUE (Phase 9): Pioneer DDJ-FLX4 play-state propagation** block per CONTEXT decision. State: "v4's `_NOTE_MAP` DOES map note 0x0B → 'play'. `ControllerState.handle_msg` DOES toggle `deck[deck]['play']` on `note_on`. But when djay Pro is the active controlling app, the FLX4 firmware sometimes consumes play presses locally without forwarding `note_on` to other listeners → `deck['play']` stays at boot-default False → `derive_audible_deck` returns 'none' → `audible_track_confidence` capped at 0.3 → TRACK_CHANGE never gated past 0.5. Phase 3 reproduces v4 verbatim; Phase 9 fix is the docket."
    - `from __future__ import annotations`.
    - Imports: `import sys`, `import threading`, `import time`. Try/except for `mido` — set `_HAS_MIDO` flag.
    - `from vibemix.platform.midi import MidiBackend, MidiMessage, MidiPort`.
    - Module-private constants `_CC_MAP`, `_NOTE_MAP` (paste byte-for-byte from 03-PATTERNS.md §8 — the `(channel, control) -> (deck, field)` tuples). Module-private helpers `_knob_label(v)` and `_xfader_label(v)` (v4:601-615 verbatim).
    - Class `ControllerState` (v4:618-727 verbatim) — Phase 3 boundary: this class is module-private to `_midi_macos.py` for now. Phase 9 may promote it to a separate `vibemix.midi.controller_state` module when the controller library lands.
    - Class `_MidoPortAdapter` implementing `MidiPort` Protocol — wraps `mido.open_input(...)` to provide `.name`, `.poll()`, `.close()`.
    - Class `MidiMacOS` with:
      - `__init__(self)` — instantiates `ControllerState`.
      - `list_input_ports(self) -> list[str]` — calls `mido.get_input_names()`.
      - `open_input(self, port_name) -> MidiPort` — returns `_MidoPortAdapter(mido.open_input(port_name))`.
      - `controller_state` attribute exposes the ControllerState.
      - `start_listener_thread(self, stop_event: threading.Event) -> threading.Thread` — spawns the v4:730-756 daemon thread (port hint `"DDJ-FLX4"`), returns the Thread object (so caller can join on shutdown).

    **Create `src/vibemix/platform/_track_macos.py`:**
    - SPDX header + module docstring noting v4:532-567 + v4:570-577 verbatim port.
    - `from __future__ import annotations`.
    - Imports: `import asyncio`, `import shutil`, `import subprocess`, `import sys`, `import threading`, `import time`, `from pathlib import Path`.
    - `from vibemix.platform.track import NowPlayingSnapshot, TrackInfoBackend`.
    - Class `TrackInfo` (v4:532-567 verbatim) — module-private (no underscore prefix because callers from `state_refresh_loop` import it directly; it lives in `_track_macos` so the platform firewall is satisfied).
    - Class `TrackMacOS` with:
      - `__init__(self)` — instantiates `TrackInfo` and resolves CLI path via `shutil.which("nowplaying-cli") or "/opt/homebrew/bin/nowplaying-cli"`.
      - `is_available(self) -> bool` — `Path(self._cli).is_file()`.
      - `poll(self) -> NowPlayingSnapshot | None` — calls `TrackInfo.poll_once()` and converts the dict-shape to `NowPlayingSnapshot` (album/duration/position are always None for nowplaying-cli `get title artist` — that subcommand only returns title + artist).
      - `track_info` attribute exposes the `TrackInfo` instance (state_refresh_loop calls `track_info.snapshot()` directly, not via the Protocol).
      - `async run_poll_loop(self, stop_event: asyncio.Event)` — v4:570-577 verbatim; calls `TrackInfo.poll_once` via `loop.run_in_executor` every 1s.

    **Update `src/vibemix/platform/__init__.py`:**
    - Add re-exports for `ScreenMacOS`, `MidiMacOS`, `TrackMacOS`. Append all three names to `__all__`.

    **Create the three test files:**
    - `tests/test_screen_macos.py` — mocked tests for `find_window_bounds`, `capture`, `is_available`, `isinstance(ScreenBackend)`. Uses `mocker.patch('vibemix.platform._screen_macos.mss.mss')`, `mocker.patch('vibemix.platform._screen_macos.CGWindowListCopyWindowInfo')`, etc.
    - `tests/test_midi_macos.py` — `_CC_MAP` / `_NOTE_MAP` byte-equality, `_knob_label` / `_xfader_label` boundaries, `ControllerState.handle_msg` decode behavior for control_change + note_on + note_off, `deck_snapshot` deep-copy invariant, `moves_since` time-relative conversion. Mocked `mido`.
    - `tests/test_track_macos.py` — `poll()` parses two-line output, `poll()` returns None on each failure mode (TimeoutExpired, CalledProcessError, FileNotFoundError, OSError), `snapshot()` legacy dict shape, `is_available()` boolean, isinstance(TrackInfoBackend). Mocked `subprocess.check_output`.

    Note for the AST guard (`tests/test_platform.py::test_no_os_leaks`): the underscore-prefixed concrete impls (`_screen_macos.py`, `_midi_macos.py`, `_track_macos.py`) are exempt from the OS-leak ban — same exemption Phase 2 already established for `_audio_macos.py`. No change needed to the test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_screen_macos.py tests/test_midi_macos.py tests/test_track_macos.py -v</automated>
  </verify>
  <done>Three macOS backend classes satisfy their Phase 1 Protocols via isinstance; `_CC_MAP` / `_NOTE_MAP` byte-equal v4:582-598; `_knob_label` / `_xfader_label` boundaries verified; `ControllerState.handle_msg` decode verified for the 6 message types; mocked `mss` / `Quartz` / `mido` / `subprocess` tests all green; Phase 1 firewall test still passes; ~50 tests green across the three files.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/state/ tests/test_screen_macos.py tests/test_midi_macos.py tests/test_track_macos.py -v` — wave 4 tests green.
- `uv run pytest tests/ -x -q --ignore=tests/test_audio_macos_live.py` — full suite green (Phase 1 + 2 + 3 waves 1-4 ≈ 150 tests).
- `python -c "from vibemix.state import MusicState, Event, EventDetector, AICoach, state_refresh_loop, classify_phase, derive_audible_deck, derive_audible_track; print('state OK')"`
- `python -c "from vibemix.platform import ScreenMacOS, MidiMacOS, TrackMacOS, ScreenBackend, MidiBackend, TrackInfoBackend; assert isinstance(ScreenMacOS(), ScreenBackend); assert isinstance(MidiMacOS(), MidiBackend); assert isinstance(TrackMacOS(), TrackInfoBackend); print('platform OK')"`
- `grep -n "audio_buf\\.\\(snapshot_features\\|energy_curve\\|estimate_bpm\\|long_arc_curve\\)" src/vibemix/state/refresh.py` — returns ZERO matches (free-function rewrites done).
- `grep -n "snapshot_features\\(audio_buf\\|energy_curve\\(audio_buf\\|estimate_bpm\\(audio_buf\\|long_arc_curve\\(audio_buf" src/vibemix/state/refresh.py` — returns FOUR matches (one per rewrite).
- `uv run ruff check src/vibemix/state/ src/vibemix/platform/ tests/` — clean.
- POC files untouched.
</verification>

<success_criteria>
1. `from vibemix.state import state_refresh_loop` resolves; full state package surface importable in one line.
2. `state_refresh_loop` is the SINGLE writer (lock-protected, single async function, no parallel writer in the package — confirmed by grep + test).
3. The 4 v4-method calls are rewritten as free-function calls per Phase 2's refactor; rest of v4:1647-1751 body verbatim.
4. `ScreenMacOS`, `MidiMacOS`, `TrackMacOS` each satisfy their Phase 1 Protocols structurally (`isinstance` check).
5. `_CC_MAP` and `_NOTE_MAP` byte-identical to v4:582-598 (tested).
6. `ControllerState.handle_msg` decode logic verified for all 6 message types.
7. `screen_capture_loop` pauses when `not state.audible`.
8. MIDI listener runs on a daemon `threading.Thread` (not asyncio).
9. `TrackInfo.poll_once` uses `get title artist` (two-line output), NOT `get-raw`.
10. KNOWN ISSUE (Phase 9): FLX4 play-state limitation documented in `_midi_macos.py` module docstring.
11. Atomic commit: `feat(03): wave 4 — state_refresh_loop + macOS Screen/MIDI/Track backends`.
</success_criteria>

<output>
After completion, do NOT create a per-wave SUMMARY. Phase 3 SUMMARY produced in wave 5.
</output>
