---
phase: 03-sensing-state-port
plan: 05
type: execute
wave: 5
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - .planning/phases/03-sensing-state-port/03-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - SENSE-01
  - SENSE-02
  - SENSE-05
  - SENSE-09
  - SCREEN-01
  - SCREEN-03
  - SCREEN-04
  - SCREEN-05
must_haves:
  truths:
    - "All 11 verification gates below PASS (see <verification>) — `pytest`, `ruff`, `grep` invariants, `isinstance` Protocol checks, POC-untouched diff, atomic commit count."
    - "`.planning/phases/03-sensing-state-port/03-SUMMARY.md` exists and follows the template structure used by `.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md` (frontmatter + sections: What Phase 3 Delivered / Requirements Coverage / Files / Architectural Decisions Locked / Deviations from Plan / Dependent Phases Unlocked / Open Items Carried Forward / Verification Snapshot / Commit History / Self-Check)."
    - "`.planning/STATE.md` updated: Current Position phase advanced to `04 — LiveKit Cascade Agent Pivot (next)`. Progress bar reflects 3/20 phases complete (`[███               ] 15%`). Performance Metrics `Phases complete` shows 3/20. Phase 3 marked ✅ in the Last Session block with the four wave commit hashes."
    - "`.planning/ROADMAP.md` Phase 3 entry: `- [ ]` → `- [x]` with completion date. Plans list at the Phase 3 detail block: all 5 plan checkboxes flip to `- [x]`."
  artifacts:
    - path: ".planning/phases/03-sensing-state-port/03-SUMMARY.md"
      provides: "Phase 3 rollup SUMMARY — replicates the structure of 02-SUMMARY.md. Records what shipped, requirements covered, files created/modified, architectural decisions locked, deviations, downstream-unlocked phases, open items, verification table, commit history, self-check."
      contains: "Phase 3 — Sensing & State Port — Summary"
    - path: ".planning/STATE.md"
      provides: "Updated Current Position + Last Session + Next Session + Performance Metrics blocks reflecting Phase 3 completion."
      contains: "Phase 3 ✅"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 entry marked complete; Plans list 5/5 checked."
      contains: "Phase 3: Sensing & State Port"
  key_links:
    - from: ".planning/STATE.md"
      to: "wave commits"
      via: "git log"
      pattern: "feat\\(03\\):"
---

<objective>
Verification gate + phase SUMMARY for Phase 3. Runs after waves 1-4 have shipped (4 atomic `feat(03)` commits). Verifies the phase delivered against ROADMAP.md success criteria + the 11 acceptance checks below. On PASS, writes `03-SUMMARY.md`, advances STATE.md to Phase 4, marks ROADMAP entry complete.

This wave is `autonomous: false` — it has ONE checkpoint (`checkpoint:human-verify`) where Kaan can optionally run a live BlackHole + DDJ-FLX4 smoke test if his rig is set up. The checkpoint is OPTIONAL — the phase is shippable on mocked-test green alone (Phase 4 is the first true end-to-end LiveKit smoke; Phase 3 in isolation has no audible output).

Purpose: lock the Phase 3 state of the world, hand the baton to Phase 4 with a complete SUMMARY so the LiveKit-cascade-agent wave knows exactly what it can import.

Output: 1 SUMMARY + 2 updated planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md
@.planning/phases/03-sensing-state-port/03-CONTEXT.md
@.planning/phases/03-sensing-state-port/03-PATTERNS.md
@.planning/phases/03-sensing-state-port/03-01-PLAN.md
@.planning/phases/03-sensing-state-port/03-02-PLAN.md
@.planning/phases/03-sensing-state-port/03-03-PLAN.md
@.planning/phases/03-sensing-state-port/03-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Run full verification gate</name>
  <files>(no source files modified — verification only)</files>
  <action>
    Run the 11 checks below in order. ALL must pass. If any fails, do NOT proceed to Task 5.2 — instead, file a gap-closure task and report to the user with the failure detail.

    Use a single bash invocation per check to keep the audit trail tidy. The 11 checks are listed in `<verification>` at the bottom of this plan. Read them there and execute each one.

    On all-pass: print a green summary table to stdout and proceed.
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q --ignore=tests/test_audio_macos_live.py 2>&1 | tail -5</automated>
  </verify>
  <done>All 11 gates green; gap-closure not needed; ready to write SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 5.2: Write 03-SUMMARY.md</name>
  <files>.planning/phases/03-sensing-state-port/03-SUMMARY.md</files>
  <action>
    Write `.planning/phases/03-sensing-state-port/03-SUMMARY.md` following the structure of `.planning/phases/02-audio-core-port-ring-buffer-fix/02-SUMMARY.md` exactly. Frontmatter:

    ```yaml
    ---
    phase: 03-sensing-state-port
    plan: rollup
    type: summary
    status: complete
    completed_at: <today's date>
    requirements_covered:
      - SENSE-01
      - SENSE-02
      - SENSE-05
      - SENSE-09
      - SCREEN-01  # partial — Quartz baseline; ScreenCaptureKit migration is Phase 8
      - SCREEN-03  # partial — find_window_bounds groundwork; full picker UI is Phase 11
      - SCREEN-04
      - SCREEN-05
    wave_commits:
      - <wave 1 commit hash>
      - <wave 2 commit hash>
      - <wave 3 commit hash>
      - <wave 4 commit hash>
    ---
    ```

    Sections (mirror 02-SUMMARY.md):

    1. **# Phase 3 — Sensing & State Port — Summary**
    2. **## What Phase 3 Delivered** — 2-3 paragraphs naming the 6 v4 primitives ported, the single-writer 10Hz state contract, the 3 macOS backends satisfying Phase 1 Protocols, and the load-bearing IP preserved (verbatim _CC_MAP/_NOTE_MAP, verbatim AICoach task strings, MUSIC_PRESENCE gate, no-`phase=` in evidence_line).
    3. **## Requirements Coverage** — table mapping the 8 requirement IDs to artifacts. SCREEN-01 + SCREEN-03 explicitly marked "partial" with Phase 8/11 carry-forward.
    4. **## Files** — Created (count + list) / Modified (count + list) / POC files touched: 0.
    5. **## Architectural Decisions Locked** — bullet table:
       - MusicState as mutable @dataclass with `_lock: threading.Lock`; single-writer guaranteed by convention + state_refresh_loop being the only async writer
       - EventDetector imports MUSIC_PRESENCE / BPM thresholds from `vibemix.audio.constants` — class-attrs removed per CONTEXT decision
       - AICoach static-method-only; evidence_line OMITS `phase=` (anti-hallucination); track threshold at 0.3 for quoting (not 0.5 — that's the EventDetector firing gate)
       - classify_phase + derive_audible_deck + derive_audible_track as FREE FUNCTIONS (not methods) → Phase 6 can swap classify_phase impl without touching the class hierarchy
       - state_refresh_loop calls features as FREE FUNCTIONS — the one structural deviation from v4 (Phase 2 refactor)
       - MIDI listener on daemon `threading.Thread` (mido.poll is blocking); ControllerState cross-thread surface lock-protected
       - screen_capture_loop pauses when `not state.audible` (CPU save)
       - Pioneer DDJ-FLX4 play-state limitation reproduced verbatim from v4 — Phase 9 fix in docket
       - All three macOS backends (`_screen_macos.py`, `_midi_macos.py`, `_track_macos.py`) satisfy Phase 1 Protocols structurally (no inheritance) — AST guard already exempts underscore-prefixed concrete impls from Phase 2
       - SCREEN-01 partial completion: Phase 3 ships Quartz `CGWindowListCopyWindowInfo` path; Phase 8 migrates to ScreenCaptureKit (deprecation chase)
    6. **## Deviations from Plan** — list any plan amendments encountered during execution (Task 4.1's `_tick_once` helper extraction was planned; any other deviation gets called out here).
    7. **## Dependent Phases Unlocked** — table:
       | Phase | Depends on Phase 3 for | Imports |
       |-------|------------------------|---------|
       | 4 | DJCoHostAgent + AgentSession + llm_node override | `from vibemix.state import MusicState, Event, EventDetector, AICoach, state_refresh_loop`; `from vibemix.platform import ScreenMacOS, MidiMacOS, TrackMacOS` |
       | 6 | Percentile phase detector swap-in | `from vibemix.state.phase import classify_phase` (Phase 6 monkey-patches the module's `SILENT_RMS/LOW_RMS/PEAK_RMS` reads or supplies a replacement function) |
       | 7 | Windows Screen / MIDI / Track impls | Mirrors the 3 macOS classes in `_screen_windows.py` / `_midi_windows.py` / `_track_windows.py` |
       | 8 | macOS ScreenCaptureKit | Replaces `_screen_macos.py` Quartz path |
       | 9 | Curated 10-controller library | Extends `_midi_macos.py` with per-controller `_CC_MAP` / `_NOTE_MAP` sets + hot-plug rescan + FLX4 play-state fix |
       | 10 | Prompt template matrix | Wraps `AICoach.build_prompt` output with the full anti-slop stack |
    8. **## Open Items Carried Forward** — bullet list with phase-pointer per item (FLX4 play-state → Phase 9; ScreenCaptureKit → Phase 8; hot-plug MIDI rescan → Phase 9; etc.).
    9. **## Verification Snapshot** — table of 11 checks with commands + ✓/✗.
    10. **## Commit History (Phase 3)** — `git log --oneline | grep -E "^[a-f0-9]+ (feat|docs|plan)\\(03\\):"` output.
    11. **## Self-Check** — list every file referenced in `must_haves.artifacts` across plans 01-04 + verify each commit hash. Print `Self-Check: PASSED`.

    Write the file via the Write tool (not `cat << EOF`). Verify it parses by running `uv run python -c "import yaml; yaml.safe_load(open('.planning/phases/03-sensing-state-port/03-SUMMARY.md').read().split('---')[1])"` — frontmatter parseable.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-sensing-state-port/03-SUMMARY.md && grep -c "^## " .planning/phases/03-sensing-state-port/03-SUMMARY.md | awk '$1 >= 10 {exit 0} {exit 1}'</automated>
  </verify>
  <done>03-SUMMARY.md exists with all 10+ sections + parseable frontmatter + all wave commit hashes recorded.</done>
</task>

<task type="auto">
  <name>Task 5.3: Update STATE.md + ROADMAP.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    **STATE.md updates** (use the Edit tool — match existing format):
    1. `**Last updated:**` line → today's date with "(Phase 3 complete)".
    2. **Current Position** block:
       - `**Phase:**` → `04 — LiveKit Cascade Agent Pivot (next)`.
       - `**Status:**` → 1-2 sentences summarizing Phase 3 delivery (sensing & state primitives ported; single-writer MusicState at 10Hz; 3 macOS backends; ~150 tests green; POC untouched).
       - `**Progress:**` → `3/20 phases complete`.
       - Progress bar: `[███               ] 15% (3/20 phases)`.
    3. **Performance Metrics** table: `Phases complete` → `3 / 20`.
    4. **Session Continuity → Last Session**: append a new bullet for today recording Phase 3 ship: 4 wave commits, ~150 tests green, the 6 primitives ported, the architectural decisions locked.
    5. **Session Continuity → Next Session**: replace existing bullets with a single line: "Continue from Phase 4 (LiveKit Cascade Agent Pivot). Imports `from vibemix.state import MusicState, EventDetector, AICoach, state_refresh_loop` + `from vibemix.platform import ScreenMacOS, MidiMacOS, TrackMacOS` directly — no scavenger hunt."

    **ROADMAP.md updates** (use the Edit tool):
    1. Phase 3 line under `## Phases`: `- [ ] **Phase 3: Sensing & State Port** - ...` → `- [x] **Phase 3: Sensing & State Port** - ... _**Complete <today's date>.**_`
    2. Phase 3 detail block: `**Plans**: TBD` → `**Plans:** 5 plans`.
    3. Add Plans list under that with all 5 checkboxes flipped to `- [x]`:
       ```
       Plans:
       - [x] 03-01-PLAN.md — MusicState + classify_phase + audible-deck/track resolvers (v4 verbatim)
       - [x] 03-02-PLAN.md — Event + EventDetector (constants imported, no class-attrs)
       - [x] 03-03-PLAN.md — AICoach (evidence_line + task_for_event + build_prompt)
       - [x] 03-04-PLAN.md — state_refresh_loop + macOS Screen/MIDI/Track backends
       - [x] 03-05-PLAN.md — 11-gate verification + phase SUMMARY + ROADMAP/STATE advance
       ```
  </action>
  <verify>
    <automated>grep -E "^- \[x\] \*\*Phase 3" .planning/ROADMAP.md && grep -E "^\*\*Phase:\*\* 04" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md + ROADMAP.md reflect Phase 3 complete; Phase 4 is the next active phase.</done>
</task>

<task type="checkpoint:human-verify" gate="optional">
  <name>Task 5.4: Optional Kaan live smoke test (skip if rig not available)</name>
  <files>(no Claude-modified files — Kaan-only verification step against live DDJ-FLX4 + djay Pro hardware; reference artifact: src/vibemix/state/refresh.py + src/vibemix/platform/_midi_macos.py + src/vibemix/platform/_track_macos.py)</files>
  <action>
    Pause for Kaan to OPTIONALLY run one live-hardware smoke test that mocked CI cannot cover. Phase 3 in isolation produces NO audible output (no Gemini call yet — Phase 4 wires LiveKit). The smoke test verifies the MIDI decode + ControllerState + state_refresh_loop wiring against the actual DDJ-FLX4 + djay Pro setup.

    Surface the verification steps below to Kaan and wait for the resume signal before letting Task 5.5 commit. Do NOT skip; the checkpoint may be RESOLVED as "skipped" (rig not available) — that's a valid resume signal — but it must be explicitly resolved.

    **What was built (recap for Kaan):**
    - `MusicState` dataclass at 10Hz, single writer
    - `EventDetector` with 7-event taxonomy + MUSIC_PRESENCE gate + per-type cooldowns matching the live v4 tuning
    - `AICoach` building per-event evidence + task strings byte-for-byte from v4
    - macOS backends: ScreenMacOS (mss + Quartz), MidiMacOS (mido + DDJ-FLX4 maps), TrackMacOS (nowplaying-cli subprocess)

    **How to run the smoke test (optional):**
    1. Plug in DDJ-FLX4. Launch djay Pro. Start a track on deck A.
    2. From repo root:
       ```bash
       source .venv/bin/activate
       python -c "
       import asyncio, time, threading
       from vibemix.audio import AudioBuffer, INPUT_SR_TARGET
       from vibemix.state import MusicState, EventDetector, AICoach, state_refresh_loop
       from vibemix.platform import MidiMacOS, TrackMacOS

       state = MusicState(set_start_at=time.time())
       audio_buf = AudioBuffer(seconds=140, sr=INPUT_SR_TARGET)
       midi = MidiMacOS()
       track = TrackMacOS()
       stop_thread = threading.Event()
       midi.start_listener_thread(stop_thread)
       detector = EventDetector()

       async def main():
           stop_evt = asyncio.Event()
           task = asyncio.create_task(state_refresh_loop(
               state, audio_buf, midi.controller_state, track.track_info, stop_evt
           ))
           try:
               for _ in range(20):
                   await asyncio.sleep(1.0)
                   print(f'audible={state.audible} deck={state.audible_deck} '
                         f'track={state.audible_track!r} moves={len(state.recent_moves)}')
                   ev = detector.detect(state, kaan_just_spoke=False, manual=False)
                   if ev:
                       print(f'  EVENT: {ev.type} extra={ev.extra}')
                       print(f'  EVIDENCE: {AICoach.evidence_line(state)}')
           finally:
               stop_evt.set()
               stop_thread.set()
               await task

       asyncio.run(main())
       "
       ```
    3. Move the deck-A volume fader. Twist EQ knobs. Hit the crossfader. **Expected:** console prints `moves=N` rising as MIDI flows in; `audible_deck='A'` once `play=True` is registered (NOTE: due to the FLX4 play-state limitation reproduced from v4, you may need to press PLAY on the controller twice — first toggle may be swallowed locally by djay; that's a Phase 9 fix).
    4. **Expected:** NO EVENTs fire because there's no audio in the AudioBuffer (Phase 3 doesn't wire `AudioMacOS` — Phase 4 does). This is expected behavior — the MUSIC_PRESENCE gate blocks auto-events without sustained-audible RMS.
    5. `Ctrl+C` to exit.

    **Approve if:** console shows live MIDI moves + ControllerState updates. The lack of EventDetector fires is correct (no audio → MUSIC_PRESENCE gate blocks auto-events).

    **Skip if:** DDJ-FLX4 not connected or djay Pro not installed — that's fine; Phase 4 is the first true end-to-end smoke test anyway.

    Wait for Kaan's resume signal: "approved" / "skipped" / issue description.
  </action>
  <verify>
    <automated>echo "checkpoint pending Kaan signal"</automated>
  </verify>
  <done>Kaan typed "approved" or "skipped" — checkpoint resolved either way; proceed to Task 5.5.</done>
</task>

<task type="auto">
  <name>Task 5.5: Final docs commit</name>
  <files>.planning/phases/03-sensing-state-port/03-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Single commit:

    ```bash
    git add .planning/phases/03-sensing-state-port/03-SUMMARY.md .planning/STATE.md .planning/ROADMAP.md
    git commit -m "docs(03): phase 3 complete — sensing & state port"
    ```

    Verify: `git log --oneline -1` shows the docs commit on top of 4 `feat(03)` commits + the earlier `docs(03)` plan + research + patterns commits.
  </action>
  <verify>
    <automated>git log --oneline | head -10 | grep -c "^[a-f0-9]\+ \(feat\|docs\|plan\)(03):" | awk '$1 >= 7 {exit 0} {exit 1}'</automated>
  </verify>
  <done>Phase 3 is sealed: 4 feat commits + this docs commit + earlier plan/docs commits all visible in git log.</done>
</task>

</tasks>

<verification>
The 11 acceptance gates (Task 5.1 runs all of these):

1. **Full suite green:** `uv run pytest tests/ -x -q --ignore=tests/test_audio_macos_live.py` — exits 0; reports ~150 tests passed (Phase 1 ~10 + Phase 2 ~78 + Phase 3 ~60).
2. **Ruff clean:** `uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/` — exits 0.
3. **POC files untouched:** `git diff --name-only HEAD~6..HEAD -- 'cohost*.py' 'cohost.streaming.py.bak' 'run*.sh' mascot.html 'sprite-*.png' generate_bat.py '_test_*.py' test_voice.py fillers/` — empty output.
4. **state package import surface:** `python -c "from vibemix.state import MusicState, Event, EventDetector, AICoach, state_refresh_loop, classify_phase, derive_audible_deck, derive_audible_track; print('OK')"` — prints OK.
5. **platform package import surface:** `python -c "from vibemix.platform import ScreenMacOS, MidiMacOS, TrackMacOS; print('OK')"` — prints OK.
6. **Protocols satisfied:** `python -c "from vibemix.platform import ScreenMacOS, MidiMacOS, TrackMacOS, ScreenBackend, MidiBackend, TrackInfoBackend; assert isinstance(ScreenMacOS(), ScreenBackend); assert isinstance(MidiMacOS(), MidiBackend); assert isinstance(TrackMacOS(), TrackInfoBackend); print('OK')"`.
7. **All 6 primitives ported:** `grep -rE "^class ScreenBuffer|^class TrackInfo|^class ControllerState|^@dataclass$" src/vibemix/state/ src/vibemix/platform/_screen_macos.py src/vibemix/platform/_midi_macos.py src/vibemix/platform/_track_macos.py` finds: MusicState, Event, EventDetector, AICoach (in state/) + `class TrackInfo`, `class ControllerState`, `class _ScreenBuffer` (in platform/_*_macos.py). 6 v4 primitives accounted for (the 7th is `screen_capture_loop` which is a method on ScreenMacOS).
8. **EventDetector constants imported, NOT class-attrs:** `grep -n "MUSIC_PRESENCE_MIN_SECONDS\\|BPM_VALID_MIN\\|BPM_VALID_MAX" src/vibemix/state/event_detector.py | grep -v "from vibemix.audio.constants"` — empty (zero non-import matches).
9. **state_refresh_loop uses FREE FUNCTIONS:** `grep -nE "audio_buf\\.(snapshot_features|energy_curve|estimate_bpm|long_arc_curve)" src/vibemix/state/refresh.py` — empty. AND `grep -cE "(snapshot_features|energy_curve|estimate_bpm|long_arc_curve)\\(audio_buf" src/vibemix/state/refresh.py` returns 4.
10. **AICoach evidence_line has NO `phase=`:** `grep -nE 'append.*"phase=' src/vibemix/state/coach.py` — empty. The only "phase=" string in the file is in the inline comment explaining the removal.
11. **≥ 4 atomic `feat(03)` commits:** `git log --oneline | grep -cE "^[a-f0-9]+ feat\\(03\\):"` returns >= 4 (one per wave 1-4).

If all 11 pass → proceed to SUMMARY + STATE + ROADMAP updates.
</verification>

<success_criteria>
1. All 11 verification gates PASS.
2. `03-SUMMARY.md` written with all 10+ sections, parseable frontmatter, all wave commit hashes recorded.
3. `STATE.md` advanced to Phase 4 as current position; Phase 3 ✅ in Last Session.
4. `ROADMAP.md` Phase 3 entry marked `- [x]` with completion date; 5 plans checkboxes all `- [x]`.
5. Final commit: `docs(03): phase 3 complete — sensing & state port`.
6. Optional Kaan smoke test checkpoint resolved (approved / skipped / issues).
</success_criteria>

<output>
After this plan completes, Phase 3 is shipped. Phase 4 (LiveKit Cascade Agent Pivot) is next — it imports the entire Phase 3 surface and wires `DJCoHostAgent.llm_node` to feed `AICoach.build_prompt(event)` output to Gemini Flash multimodal.

Final summary printed to console:
- Plans complete: 5 / 5 (waves 1-5)
- Tests added: ~60 (state package + macOS backend tests)
- Total suite: ~150 tests green
- Atomic commits: 4 `feat(03)` + 1 `docs(03)`
- POC files diff: empty
- Phase 3 SUMMARY: `.planning/phases/03-sensing-state-port/03-SUMMARY.md`
</output>
