---
phase: 15-recording-browser-retention-enforcement
plan: 04
type: checkpoint
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md
autonomous: false
requirements:
  - REC-07
  - REC-08

must_haves:
  truths:
    - "Kaan opens vibemix → Settings → drawer scrolls to RECORDING group → sees a chronologically-ordered list of past sessions with date + duration + disk size"
    - "Clicking the reveal icon on a row opens Finder with the session dir selected"
    - "Clicking the replay icon expands the row + plays voice.wav inline (amber-accent native scrubber); collapsing tears down the audio decoder"
    - "Clicking the open-external icon opens input.wav in the OS default audio app"
    - "Clicking the delete icon optimistically removes the row + shows the bottom-right `deleted · undo?` toast for 4 seconds; clicking undo restores the row at its original position; letting the timer elapse fires the actual delete and the row stays gone after the next list-refresh"
    - "Disk-usage line at top shows `RECORDINGS · {N} SESSIONS · {SIZE} USED` reflecting actual disk truth (verify with `du -sh ~/Library/Application Support/vibemix/recordings`)"
    - "After ~6h of continuous sidecar runtime (or the test-mode shortened interval), the events.jsonl of the live session contains a `retention_pruned` line when sessions older than the retention window existed"
    - "Setting retention to ∞ (slider stop position 6) followed by reopening the drawer + waiting for next sweep does NOT delete anything (verifiable via `ls ~/Library/Application Support/vibemix/recordings`)"
  artifacts:
    - path: ".planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md"
      provides: "Kaan-signed structured pass/fail per success criterion + screenshots/screen-recording references"
      contains: "PASS"
  key_links:
    - from: "Plan 15-01 (audit) → Plan 15-02 (periodic + events.jsonl) → Plan 15-03 (reveal + open-external) → Plan 15-04 (this checkpoint)"
      to: "ROADMAP §Phase 15 Success Criteria 1–4"
      via: "Each criterion mapped to a sub-section of 15-EAR-TEST.md"
      pattern: "Criterion [1234]"
---

<objective>
Final user-verification checkpoint for Phase 15. After Plans 01 (audit + success-criteria gates) + 02 (periodic 6h sweep + events.jsonl logging) + 03 (reveal-in-OS + open-input-wav commands + UI buttons) ship, Kaan personally drives all four ROADMAP success criteria end-to-end on the dev build and signs each off as PASS / FAIL with notes.

Purpose: Phase 15 is a partially-shipped retroactive closure. The shipped surface (browser, row, retention slider, undo toast, boot/close/settings sweep) was verified by automated tests in Plans 01–03, but the FULL user journey (drawer-open → reveal → replay → open-external → delete-with-undo → retention-changes-and-takes-effect) was never end-to-end-verified by Kaan post the impeccable Wave 5.A pivot from modal confirm to undo toast. This checkpoint closes that gap and produces the structured EAR-TEST log that Phase 16 (calendar-blocking ear-test gate) consumes as its first input.

Output: `.planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md` — Kaan's signed pass/fail per criterion, with timestamps, screen-recording references (saved under `~/Movies/vibemix-ear-tests/15-XX.mp4` or equivalent), and any UX paper-cuts surfaced for an impeccable polish wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-recording-browser-retention-enforcement/15-CONTEXT.md
@.planning/phases/15-recording-browser-retention-enforcement/15-UI-SPEC.md
@.planning/phases/15-recording-browser-retention-enforcement/15-01-SUMMARY.md
@.planning/phases/15-recording-browser-retention-enforcement/15-02-SUMMARY.md
@.planning/phases/15-recording-browser-retention-enforcement/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint smoke + automated regression sweep</name>
  <files>.planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md</files>
  <action>
Before pausing for human verification, run the full automated suite to confirm Plans 01/02/03 left a green baseline:

```
pytest tests/recording/ tests/ui_bus/test_recordings_messages.py -x -q
( cd tauri/ui && npm run test -- src/settings/components/recording-row.spec.ts src/settings/components/recording-browser.spec.ts src/settings/components/recording-browser.success.spec.ts tests/session/ws-bridge.recordings.spec.ts )
( cd tauri/src-tauri && cargo test --lib reveal_in_os )
( cd tauri/src-tauri && cargo build --release 2>&1 | tail -10 )
```

If anything is red, STOP — do not proceed to the human checkpoint until prior plans are clean. Surface failures inline.

Then create `15-EAR-TEST.md` with the four-criterion checklist scaffold (filled by Kaan during the checkpoint):

```markdown
# Phase 15 Ear-Test Log

**Tested by:** Kaan
**Build:** dev (`npm run tauri dev`) — commit <SHA>
**Date:** <YYYY-MM-DD>
**Environment:** macOS <ver>, vibemix recordings root: ~/Library/Application Support/vibemix/recordings

## Criterion 1 — Chronological list with date + duration + disk size + reveal-in-Finder
- [ ] Drawer opens to RECORDING group
- [ ] Sessions sorted newest-first
- [ ] Date column reads `YYYY-MM-DD HH:MM` per row
- [ ] Duration column reads `Xm` or `Xh Ym` per row
- [ ] Disk-usage silkscreen line top of group reads `RECORDINGS · N SESSIONS · X MB|GB USED`
- [ ] Click reveal icon → Finder opens with the session dir selected
- **Result:** PASS / FAIL — <notes>

## Criterion 2 — Replay voice.wav inline + open input.wav externally
- [ ] Click replay (or row body) expands the row + native <audio> player appears
- [ ] Player scrubber accent is amber
- [ ] Audio plays
- [ ] Collapsing the row stops playback (decoder torn down per row contract)
- [ ] Opening a different row's player STOPS the previous row's audio (single-row playback discipline)
- [ ] Click open-external icon → OS default audio app launches input.wav
- **Result:** PASS / FAIL — <notes>

## Criterion 3 — Delete with undo (impeccable Wave 5.A pattern)
- [ ] Click delete icon → row vanishes immediately
- [ ] Bottom-right toast `deleted · undo?` appears
- [ ] Click `undo?` within 4s → row restores at original sort position
- [ ] Re-delete the same row → toast reappears
- [ ] Wait 4s without clicking undo → toast disappears, row stays gone, dir actually removed from disk (verify with `ls ~/Library/Application\ Support/vibemix/recordings/`)
- [ ] Disk-usage line updates after the real delete (sidecar emits ipc.recordings.usage push)
- **Result:** PASS / FAIL — <notes>

## Criterion 4 — Retention auto-prune 7d default + every 6h + events.jsonl logging
- [ ] First-install retention slider defaults to position 7d (3rd stop)
- [ ] Move slider to ∞ (rightmost stop) → setting saved → wait for next periodic sweep tick → no sessions deleted (verify via `ls`)
- [ ] Move slider to 1d → setting saved → settings-change sweep fires → sessions older than 1d are pruned NOW (verify via `ls` before/after)
- [ ] events.jsonl of the LIVE session contains a line with `"kind":"retention_pruned"` matching schema `{t, kind, count, bytes}` after at least one prune occurred (verify with `tail -5 ~/Library/Application\ Support/vibemix/recordings/<live-session>/events.jsonl`)
- [ ] To verify periodic 6h sweep WITHOUT waiting 6h: temporarily set `RETENTION_SWEEP_INTERVAL_S=10` via env override OR direct edit (revert before commit) — wait 30s — observe ≥2 periodic ticks via `pm2 logs` or the sidecar console
- **Result:** PASS / FAIL — <notes>

## Paper-cuts surfaced for polish wave (non-blocking)
- (free-form list — surfaces UX issues for a follow-up impeccable polish wave; does NOT gate Phase 15 closure)

## Sign-off
- **Phase 15 closure:** PASS / FAIL
- **Signed:** Kaan, <date>
```
  </action>
  <verify>
    <automated>pytest tests/recording/ tests/ui_bus/test_recordings_messages.py -x -q && (cd tauri/ui && npm run test -- src/settings/components/) && (cd tauri/src-tauri && cargo test --lib reveal_in_os && cargo build --release 2>&1 | tail -3)</automated>
  </verify>
  <done>
All automated suites green; `15-EAR-TEST.md` scaffold exists with the four-criterion checklist; ready for human verification checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Kaan ear-test sign-off — drive all four ROADMAP success criteria end-to-end on dev build</name>
  <action>Pause for Kaan's hands-on verification per the how-to-verify steps below; record results into 15-EAR-TEST.md and sign PASS or describe FAIL with concrete reproduction notes.</action>
  <files>.planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md</files>
  <verify>
    <automated>test -f .planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md && grep -q "Signed:" .planning/phases/15-recording-browser-retention-enforcement/15-EAR-TEST.md</automated>
  </verify>
  <done>15-EAR-TEST.md fully filled + signed by Kaan with all four criteria marked PASS.</done>
  <what-built>
Phase 15 surface end-to-end:
- Plans 01–03 closed the periodic-sweep + events.jsonl log gaps (Plan 02), the reveal-in-Finder + open-input.wav-externally gaps (Plan 03), and locked all four ROADMAP success criteria as automated tests (Plan 01).
- The shipped surface in `tauri/ui/src/settings/components/` (recording-browser.ts + recording-row.ts + retention-slider.ts) is now feature-complete against ROADMAP §Phase 15.
- All automated tests are green: pytest/recording (~28 cases), vitest/recording-* (~22 cases), cargo test reveal_in_os (4 cases).
  </what-built>
  <how-to-verify>
1. Pull the latest dev branch on Kaan's Mac and run `npm run tauri dev` from `tauri/ui/` (which spawns the sidecar via the existing tauri-plugin-shell sidecar binding).
2. Open the app → click Settings → wait for drawer to slide in → scroll to the RECORDING group.
3. Drive each criterion in `15-EAR-TEST.md` top-to-bottom:
   - Criterion 1 (visual + reveal-in-Finder): observe the list, click reveal on at least 2 rows, confirm Finder opens with the dir selected.
   - Criterion 2 (replay + open-external): click replay on a row, hear voice.wav (Avery's voice), let it play 5 seconds, collapse the row → audio stops. Open a different row → it plays, the previous one is silent. Click open-external on a row → default audio app opens input.wav.
   - Criterion 3 (undo-delete): click delete on a known-disposable session, observe the toast, click undo within 4s, verify row returns. Re-delete, let timer elapse 4s+, refresh the list, verify the dir is gone with `ls ~/Library/Application\ Support/vibemix/recordings/`.
   - Criterion 4 (retention): move slider to 1d, verify older-than-1d sessions are pruned within 1s of the slider release (settings-change sweep fires synchronously). Move to ∞ → verify no sweep deletes anything. For the periodic 6h proof, use the test-mode shortcut: set `VIBEMIX_RETENTION_INTERVAL_S=10` env (Plan 02 must support this override; if not, set the constant directly via a temp source-edit, observe ≥2 ticks in 30s, REVERT before commit).
4. For each checklist item, mark `[x]` PASS or `[ ]` FAIL with a one-line note.
5. Save a screen recording of the full session as `~/Movies/vibemix-ear-tests/phase-15-closure.mp4` and reference it in 15-EAR-TEST.md.
6. Sign + date the bottom of 15-EAR-TEST.md.

If ANY criterion fails: do NOT mark Phase 15 closed. Surface the failure as either (a) a polish-wave item if non-blocking OR (b) a blocking gap that demands a Plan 15-05 follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" when 15-EAR-TEST.md is fully filled + signed PASS, OR describe failures + request Plan 15-05 scaffolding.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human reviewer ↔ live app | Kaan drives the full UI; the app talks to the local sidecar over WS; no remote attack surface added |
| Test-mode env override `VIBEMIX_RETENTION_INTERVAL_S` | Only used during the checkpoint to avoid waiting 6h; reverted before commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Repudiation | EAR-TEST signed log | mitigate | The signed log + screen recording is the closure artifact. Phase 16 ear-test gate explicitly references this log as Phase 15's truth-of-record. |
| T-15-04-02 | Tampering | Test-mode interval override left in committed code | mitigate | The how-to-verify includes an explicit "REVERT before commit" instruction; the resume-signal cannot fire until the EAR-TEST is signed AND the working tree is clean (`git status` should not show the temp edit). |
</threat_model>

<verification>
- All four ROADMAP success criteria PASS in 15-EAR-TEST.md.
- Screen recording exists at the documented path.
- Working tree is clean post-checkpoint (no test-mode env overrides committed).
- Phase 16 entry condition satisfied: the EAR-TEST log exists for Phase 15 and Kaan can move on to scheduling the calendar-blocking DJ-set sessions for Phases 17–20.
</verification>

<success_criteria>
- 15-EAR-TEST.md exists with all four criterion sections marked PASS.
- Screen recording referenced.
- Kaan signed + dated the closure.
- ROADMAP.md Phase 15 checkbox can flip from `- [ ]` to `- [x]` in a follow-up commit.
</success_criteria>

<output>
After Kaan signs PASS, create `.planning/phases/15-recording-browser-retention-enforcement/15-04-SUMMARY.md` containing:

1. **Phase 15 closure status** — PASS / FAIL.
2. **Test artifacts** — paths to all spec/test files added across Plans 01/02/03 (consolidated count).
3. **Surface artifacts** — final list of shipped UI components + sidecar code paths.
4. **Open polish-wave items** — non-blocking UX paper-cuts surfaced during the EAR-TEST (handed to a future impeccable wave, NOT to Phase 16).
5. **Phase 16 hand-off** — confirm 15-EAR-TEST.md exists as the precondition for Phase 16's calendar-blocking ear-test scheduling.
</output>
