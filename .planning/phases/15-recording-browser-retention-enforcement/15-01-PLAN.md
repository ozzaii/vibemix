---
phase: 15-recording-browser-retention-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/recording/test_phase15_success_criteria.py
  - tauri/ui/src/settings/components/recording-browser.success.spec.ts
autonomous: true
requirements:
  - REC-07
  - REC-08

must_haves:
  truths:
    - "Existing recording-browser.spec.ts (8 cases) and recording-row.spec.ts (14 cases) pass on a clean checkout"
    - "Existing tests/recording/test_recordings_index.py (16 cases) and test_retention_sweep.py (7 cases) pass"
    - "A new pytest case proves run_retention_sweep prunes a >7d directory at default retention_days=7 and skips the live session dir"
    - "A new pytest case proves the ∞ sentinel (retention_days=36500) returns [] WITHOUT scanning"
    - "A new vitest case proves the disk-usage line renders chronological session count + GB/MB total + LOADING/UNAVAILABLE sentinels"
    - "A new vitest case proves single-row playback (only one <audio> mounted at a time across the row set)"
    - "An audit grep documents what IS shipped vs what is GAP for each of the 4 ROADMAP success criteria"
  artifacts:
    - path: "tests/recording/test_phase15_success_criteria.py"
      provides: "Pytest gate covering retention default, ∞ sentinel, live-session exclusion"
      min_lines: 80
    - path: "tauri/ui/src/settings/components/recording-browser.success.spec.ts"
      provides: "Vitest gate covering disk-usage formatting + single-row playback discipline"
      min_lines: 60
  key_links:
    - from: "tests/recording/test_phase15_success_criteria.py"
      to: "vibemix.runtime.recordings_index.run_retention_sweep"
      via: "import + direct call"
      pattern: "run_retention_sweep"
    - from: "tauri/ui/src/settings/components/recording-browser.success.spec.ts"
      to: "tauri/ui/src/settings/components/recording-browser.ts"
      via: "import + jsdom render"
      pattern: "renderRecordingBrowser"
---

<objective>
Audit Phase 15's already-shipped surface against ROADMAP's four success criteria and lock down the success-criteria proofs as new automated tests. Outcome: the executor produces (a) a written audit table mapping each ROADMAP criterion to shipped artifact + status (SHIPPED / GAP), and (b) two new test files that wedge the success criteria into CI so any regression on subsequent plan work fails red.

Purpose: Phase 15 is partially shipped. Before adding more code (Plans 02/03/04), prove what is already correct, surface the gaps, and bolt success-criteria coverage onto the existing test suites so subsequent plans ship against a green baseline.

Output:
- New pytest file: `tests/recording/test_phase15_success_criteria.py` (live-session exclusion + ∞ sentinel + default-7d prune).
- New vitest spec: `tauri/ui/src/settings/components/recording-browser.success.spec.ts` (disk-usage formatting + single-row playback discipline).
- An audit table written to the SUMMARY (per `<output>` below) mapping ROADMAP criterion → shipped artifact path → SHIPPED / GAP / status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-recording-browser-retention-enforcement/15-CONTEXT.md
@.planning/phases/15-recording-browser-retention-enforcement/15-UI-SPEC.md

# Source files under audit (read each ONCE; extract patterns in a single pass)
@src/vibemix/runtime/recordings_index.py
@src/vibemix/__main__.py
@src/vibemix/runtime/session_loop.py
@src/vibemix/runtime/settings.py
@tauri/ui/src/settings/components/recording-browser.ts
@tauri/ui/src/settings/components/recording-row.ts

# Existing tests — read for fixture conventions, do not duplicate cases
@tests/recording/test_recordings_index.py
@tests/recording/test_retention_sweep.py
@tauri/ui/src/settings/components/recording-browser.spec.ts
@tauri/ui/src/settings/components/recording-row.spec.ts

<interfaces>
<!-- Extracted from src/vibemix/runtime/recordings_index.py — executor uses these signatures directly -->

```python
SESSION_DIR_RE: re.Pattern[str]  # = re.compile(r"^\d{8}-\d{6}$")

class RecordingsIndex:
    def __init__(self, recordings_root: Path) -> None: ...
    def list(self) -> tuple[RecordingSummary, ...]: ...   # newest-first
    def compute_usage(self) -> tuple[int, int]: ...        # (sessions, bytes_total)
    def delete(self, session_dir_name: str) -> tuple[bool, Optional[str]]: ...
    def read_events(self, session_dir_name: str) -> tuple[Optional[list[dict]], Optional[str]]: ...

def run_retention_sweep(
    recordings_root: Path,
    retention_days: int,
    *,
    now: Optional[datetime] = None,
) -> list[str]:
    """Returns list of deleted dir names. Sentinel retention_days >= 36500 returns []."""
```

<!-- Extracted from tauri/ui/src/settings/components/recording-browser.ts -->
```typescript
export interface RecordingsUsage {
  sessions: number;
  bytes_total: number;  // sentinels: -1 LOADING, -2 UNAVAILABLE
}

export interface RecordingBrowserHandle {
  root: HTMLElement;
  setSessions(sessions: RecordingSummary[]): void;
  setUsage(usage: RecordingsUsage): void;
}

export function renderRecordingBrowser(opts: RecordingBrowserProps): RecordingBrowserHandle;
```

<!-- Extracted from tauri/ui/src/settings/components/recording-row.ts -->
```typescript
export interface RecordingSummary {
  session_dir: string;
  started_at_iso: string;
  duration_s: number;
  event_count: number;
  bytes_total: number;
  crashed: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing test suites + write audit table to SUMMARY</name>
  <files>tests/recording/test_phase15_success_criteria.py</files>
  <action>
Step 1 — Run the existing pytest + vitest suites that already cover Phase 15 surface and confirm they pass on a clean checkout:

```
pytest tests/recording/test_recordings_index.py tests/recording/test_retention_sweep.py tests/ui_bus/test_recordings_messages.py -x -q
( cd tauri/ui && npm run test -- src/settings/components/recording-browser.spec.ts src/settings/components/recording-row.spec.ts tests/session/ws-bridge.recordings.spec.ts )
```

If any pre-existing case fails on the clean checkout, STOP and surface the failures in the SUMMARY as a Pre-Audit Regression block — do NOT attempt to fix them in this plan (that is a separate concern from Phase 15 closure).

Step 2 — Build the audit table (4 rows, one per ROADMAP success criterion). For each row record: ROADMAP criterion text → shipped artifact path(s) → status (SHIPPED / PARTIAL / GAP) → gap closure plan reference (15-02 / 15-03 / 15-04). Use these inputs to fill it (extracted during planning):

  Criterion 1 (chronological list + reveal-in-Finder):
    SHIPPED: list rendering in tauri/ui/src/settings/components/recording-browser.ts (renderRecordingBrowser) + recording-row.ts (date + duration cells); newest-first sort enforced in recordings_index.py:296.
    GAP: reveal-in-Finder action — UI-SPEC §"Reveal-in-OS (DEFERRED to Phase 15 Plan 06+)" explicitly reserves the slot but ships no icon. → addressed by Plan 15-04.

  Criterion 2 (replay voice.wav inline + open input.wav externally):
    SHIPPED: inline <audio controls> for voice.wav via convertFileSrc(asset://) in recording-row.ts:436-444; native scrubber accent-color amber.
    GAP: open input.wav in OS default app — UI-SPEC §"Open-input.wav-in-default-app (DEFERRED to Phase 15 Plan 06+)" reserves the slot but ships no icon. → addressed by Plan 15-04.

  Criterion 3 (delete with confirm pattern):
    SHIPPED (alternate pattern per impeccable Wave 5.A 2026-05-14): optimistic-remove + 4s undo toast in recording-browser.ts:332-360 replaces the modal confirm flow declared in CONTEXT.md. Tests recording-browser.spec.ts cases 6-8 lock the new pattern. CONTEXT.md decision is preserved-by-reframing: the undo toast IS the confirm — see UI-SPEC §"Delete Flow" + §"Row delete (no modal)".

  Criterion 4 (retention auto-prune 7d default + every 6h + events.jsonl logging):
    SHIPPED: boot sweep in __main__.py:330-336 + session-close sweep in __main__.py:521-531 + settings-change sweep in runtime/settings.py:269-312; default 7d declared in retention-slider/config_store; ∞ sentinel short-circuit in recordings_index.py:482-483.
    GAPS:
      (a) periodic every-6h sweep — confirmed absent via `grep -rn "21600\|6 \* 3600\|hours=6\|sweep_loop" src/vibemix/` returns zero matches. → addressed by Plan 15-02.
      (b) events.jsonl `retention_pruned` log line — confirmed absent via `grep -rn "retention_pruned" src/vibemix/` returns zero matches. Current sweep emits `log.info()` only. → addressed by Plan 15-02.

Write this table to `tests/recording/test_phase15_success_criteria.py` as a top-of-file docstring (no code yet — Task 2 fills the test bodies). The table is canonical: subsequent plans cite row numbers in their SUMMARYs.

Step 3 — Verify the absence claims with grep:
```
grep -rn 'retention_pruned' src/vibemix/ || echo "GAP confirmed: retention_pruned absent"
grep -rn '21600\|hours=6\|sweep_loop\|sweep_task' src/vibemix/ || echo "GAP confirmed: periodic 6h sweep absent"
grep -rn 'reveal_in_os\|open_external\|recording.reveal' src/vibemix/ tauri/ || echo "GAP confirmed: reveal/open-external IPC absent"
```
Paste the verbatim grep output (or "no matches → GAP confirmed") into the SUMMARY's Gap Evidence block.
  </action>
  <verify>
    <automated>pytest tests/recording/ tests/ui_bus/test_recordings_messages.py -x -q && (cd tauri/ui && npm run test -- src/settings/components/recording-browser.spec.ts src/settings/components/recording-row.spec.ts tests/session/ws-bridge.recordings.spec.ts) && test -f tests/recording/test_phase15_success_criteria.py</automated>
  </verify>
  <done>
Existing test suites green on clean checkout; new file `tests/recording/test_phase15_success_criteria.py` exists with the 4-row audit table as a docstring; SUMMARY contains the audit table + Gap Evidence grep output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write pytest success-criteria gates (default 7d prune + ∞ sentinel + live-session exclusion)</name>
  <files>tests/recording/test_phase15_success_criteria.py</files>
  <behavior>
    - Test A — `test_default_retention_7d_prunes_old_session`: Build a tmp recordings root with two dirs: one named `20260101-120000` (≥7d old vs frozen now=2026-05-14) + one named `20260513-120000` (≤7d old vs frozen now). Call `run_retention_sweep(root, retention_days=7, now=datetime(2026, 5, 14, 12, 0, 0))`. Assert: returned list contains exactly `["20260101-120000"]`; `(root / "20260101-120000").exists()` is False; `(root / "20260513-120000").exists()` is True.
    - Test B — `test_infinite_sentinel_36500_short_circuits_without_scan`: Patch `os.scandir` with a MagicMock that raises if called. Call `run_retention_sweep(root, retention_days=36500)`. Assert: returns `[]`; the scandir mock's `assert_not_called()` passes.
    - Test C — `test_live_session_dir_excluded_from_sweep`: Build a tmp recordings root with one dir whose name parses to `now - 1 second`. Call `run_retention_sweep(root, retention_days=0, now=<frozen now>)`. Assert: returned list is `[]`; the dir still exists. (Validates the threat-model claim T-15-03-06 in recordings_index.py:476-481.)
    - Test D — `test_default_retention_days_is_7_in_config_store`: Import `vibemix.runtime.config_store.ConfigStore`. Construct fresh (no persisted file). Assert: `cs.retention_days == 7`. (Defends against ROADMAP success-criterion #4 "default 7d" drifting silently in config_store.)
  </behavior>
  <action>
Append the four test functions described in `<behavior>` to `tests/recording/test_phase15_success_criteria.py`. Keep the file's existing docstring (audit table from Task 1) at the top.

Use existing fixture conventions from `tests/recording/test_retention_sweep.py` (the `tmp_recordings_dir` pytest fixture pattern). Do NOT introduce a new conftest; reuse `tmp_path` for the root and `os.makedirs` for session dirs. Touch a `voice.wav` placeholder file inside each session dir so `bytes_total` is non-zero (and rmtree has something to delete).

For Test B, use `unittest.mock.patch` on `vibemix.runtime.recordings_index.os.scandir` — the `os` import inside the module is local to that name. The sentinel branch (recordings_index.py:482-483) returns `[]` BEFORE any os.scandir call, so the mock should never fire.

For Test C, the live-session-dir name MUST parse via `datetime.strptime(name, "%Y%m%d-%H%M%S")` to `now - timedelta(seconds=1)` so `session_start >= cutoff` evaluates True (cutoff = now - 0d = now; session_start = now - 1s; session_start < cutoff). The sweep skips it via the `if session_start >= cutoff: continue` branch — but for retention_days=0, cutoff=now, the active dir at started_at = now - epsilon falls JUST below cutoff, so the actual defense is the dir-name parse + frozen `now`. Reference recordings_index.py:476-481 docstring in a code comment so the executor can verify the behavior matches the threat-model claim.

For Test D, fall through to load_config() OR construct ConfigStore() fresh (whichever the codebase pattern shows). Read `src/vibemix/runtime/config_store.py` first (single Read call) to confirm the API; do NOT guess.

NEVER edit recordings_index.py or config_store.py in this task — these are PURE test additions. If a test is impossible to write without modifying source, that is a real gap and goes into Plan 15-02 / 15-03 / 15-04 instead.
  </action>
  <verify>
    <automated>pytest tests/recording/test_phase15_success_criteria.py -v</automated>
  </verify>
  <done>
All 4 pytest cases pass green; no source files modified; the test file documents which lines of recordings_index.py / config_store.py each test defends.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write vitest success-criteria gates (disk-usage formatting + single-row playback)</name>
  <files>tauri/ui/src/settings/components/recording-browser.success.spec.ts</files>
  <behavior>
    - Test E — `disk usage line is the single error/loading channel for the surface`: Render the browser with `initialUsage: { sessions: 0, bytes_total: -2 }`. Assert: usage line text === `"RECORDINGS · UNAVAILABLE"`; the empty-state body still renders ("No recordings yet…"). Then `setUsage({ sessions: 5, bytes_total: 524288000 })` and assert text becomes `"RECORDINGS · 5 SESSIONS · 500 MB USED"` AND the empty-state body still renders (no list refetch on usage push — matches UI-SPEC §State Management).
    - Test F — `chronological newest-first ordering preserved on setSessions`: Render with empty initial set, then call `setSessions([older, newer])` where `older.started_at_iso < newer.started_at_iso`. Read row text-content top-to-bottom (`.vmx-rec-row__ts` cells). Assert: the FIRST rendered row's timestamp matches the input list's index 0 (the browser does NOT re-sort — it trusts the sidecar's wire order, per recordings_index.py:296). This locks the UI side of ROADMAP success-criterion #1 ("chronologically-ordered list").
    - Test G — `single-row playback discipline`: Render 3 rows. Click row[0]'s replay button; assert exactly one `<audio>` element exists in the document. Click row[1]'s replay button; assert exactly one `<audio>` element STILL exists in the document (row[0]'s audio was torn down). The lazy-mount + collapse-teardown pattern in recording-row.ts:436-500 enforces this — the test proves the contract holds end-to-end across multiple rows.
  </behavior>
  <action>
Create `tauri/ui/src/settings/components/recording-browser.success.spec.ts` with the three vitest cases described in `<behavior>`. Mirror the imports + mocks + `FakeIntersectionObserver` setup from the existing `recording-browser.spec.ts` (read it ONCE for the boilerplate; do not re-read).

Use the same `vi.mock("@tauri-apps/api/core", () => ({ convertFileSrc: ... }))` + `vi.mock("../../ipc/client.js", () => ({ sendIpcRequest: vi.fn(...) }))` pattern. For Test G the IPC mock should `() => new Promise(() => {})` (never resolves) — the row only needs to MOUNT the audio element, not wait for events.jsonl.

For Test F, build the two summaries with distinct `started_at_iso` values (e.g. `"2026-05-13T21:04:10+02:00"` for older, `"2026-05-14T21:04:10+02:00"` for newer). Pass them in newest-first order (`[newer, older]`) and assert the FIRST `.vmx-rec-row__ts` matches `"2026-05-14 21:04"`. Then re-test with reversed input — the browser should render in the order it received (no client-side sort) so the FIRST row matches `"2026-05-13 21:04"`. This proves the wire order is the truth.

For Test G, click handlers fire toggle which invokes the mounted `<audio>` lazily. After clicking row[0]'s replay button, query `document.querySelectorAll("audio")` should be length 1. After clicking row[1]'s replay button, the count should still be 1 — the row[0] handle's `setExpanded(false)` was called by the browser's toggle indirection. Read recording-browser.ts lines 362-378 to confirm: on toggle, the parent calls `handle.setExpanded(!isOpen)` for the clicked row only — there is no automatic close-others discipline. **If after running the test you find row[0]'s audio is NOT torn down on row[1]'s click**, that is a real gap. In that case, the test should EXPECT FAIL with `it.fails(...)` AND the gap is added to the SUMMARY's Found Gaps block for closure in 15-04 (the UI-SPEC §Row replay claims "Single-row guarantee: only one row is open at a time" — if the code does not enforce this, the gap is real).

NEVER edit recording-browser.ts or recording-row.ts in this task — pure test additions only.
  </action>
  <verify>
    <automated>cd tauri/ui && npm run test -- src/settings/components/recording-browser.success.spec.ts</automated>
  </verify>
  <done>
Three vitest cases run; Tests E + F pass green; Test G either passes green (single-row discipline holds) OR is `it.fails(...)` with the gap recorded in SUMMARY for Plan 15-04 to close. No source files modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → tmp filesystem | Tests create real dirs in pytest's `tmp_path`; no untrusted input crosses |
| Vitest jsdom → mocked IPC | All IPC calls mocked via `vi.mock("../../ipc/client.js")`; no real WebSocket |
| Audit grep → repo source tree | Read-only; no shell injection vector (greps use literal string patterns) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | tests/recording/test_phase15_success_criteria.py | mitigate | Tests are PURE additions — never modify source files. The audit-table docstring is a documentation gate; CI compares the artifact list against the success criteria. |
| T-15-01-02 | Information disclosure | grep audit output | accept | Grep results are file paths + line numbers from the project tree (already public in git). No secrets surfaced. |
| T-15-01-03 | Denial of service | run_retention_sweep test (Test B with mocked scandir) | mitigate | Test B asserts the ∞ sentinel SHORT-CIRCUITS — proves an attacker cannot trigger expensive scandir by setting retention_days=36500. The mock-not-called assertion is the gate. |
</threat_model>

<verification>
- All four pytest cases in `tests/recording/test_phase15_success_criteria.py` pass.
- All three vitest cases in `tauri/ui/src/settings/components/recording-browser.success.spec.ts` pass (or Test G is `it.fails(...)` with documented gap).
- Audit table in SUMMARY maps each ROADMAP success criterion to a SHIPPED artifact OR a Plan 15-02/03/04 gap-closure pointer.
- Pre-existing test suites still pass on the same checkout.
</verification>

<success_criteria>
- `pytest tests/recording/ tests/ui_bus/test_recordings_messages.py -v` exits 0.
- `cd tauri/ui && npm run test -- src/settings/components/` exits 0.
- The SUMMARY contains: (a) audit table (4 rows), (b) Gap Evidence grep output, (c) Found Gaps list with explicit pointers to Plans 15-02 / 15-03 / 15-04.
- Zero source files modified (the audit is read-only; gap closure happens in subsequent plans).
</success_criteria>

<output>
After completion, create `.planning/phases/15-recording-browser-retention-enforcement/15-01-SUMMARY.md` containing:

1. **Audit Table** — 4 rows (one per ROADMAP success criterion) with shipped-artifact paths + status + plan-pointer.
2. **Gap Evidence** — verbatim grep output proving the absent surfaces.
3. **Found Gaps** — explicit list of gaps with their target closure plan (15-02 retention loop + events.jsonl log; 15-03 reveal-in-OS IPC + UI; 15-04 open-input.wav externally + single-row playback if Test G fails).
4. **Pre-existing Test Status** — green / red baseline before any code was changed.
5. **New Test Files Added** — paths + line counts + which ROADMAP criterion each defends.
</output>
