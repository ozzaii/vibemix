---
phase: 66-visible-copilot-move
plan: 02
type: execute
wave: 1
depends_on: [66-01]
files_modified:
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/state/coach.py
  - .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md
  - KAAN-ACTION-LEGAL.md
autonomous: true
requirements: [COPILOT-01, COPILOT-02, COPILOT-03]
tags: [phase-66, copilot, recall, chip, prompt-fragment, cooldown, kaan-action, ui-tier-1, wave-1, green]

must_haves:
  truths:
    - "A grounded [recall:<record_id>] citation in an AI reaction yields a chip with verb='recall' on the citation_strip — surfacing the copilot move on the existing Tier-1 ipc.session.cohost-reaction envelope (no new socket, no new port)"
    - "A fabricated [recall:<unregistered>] citation strips the WHOLE turn (Phase 65 anti-poisoning gate carries forward unchanged); a recall chip cannot surface without a real registered past moment"
    - "On TRACK_CHANGE / MIX_MOVE / LAYER_ARRIVAL with non-empty recall_moments and cooldown clear, the coach prompt body carries the transition-shape callback fragment (a past-tense, single-citation invitation)"
    - "On PHASE with non-empty recall_moments and cooldown clear, the coach prompt body carries the vocabulary callback fragment (echo your own past phrasing)"
    - "Two recall callbacks land at most once per 120s (≥120s between any two arms); a single turn produces at most ONE [recall:<id>] token (structural property of the fragment template + registry strict-subset)"
    - "The cooldown ARMS only when a recall chip REACHED the audience — `_last_recall_callback_at = time.time()` runs ONLY when `await self._ipc_bus.emit(...)` returned without exception (strict 'reached the audience' semantic per CONTEXT.md Area 1 Q3 + RESEARCH §Pitfall 2 + §Open Q5). A bus-emit failure does NOT arm the cooldown."
    - "Empty / None / below-floor / current-session-excluded recall_moments → cold path is BYTE-IDENTICAL to v5.0 baseline (the recall_fragment_for_event helper returns '' on falsy input — the load-bearing regression floor)"
    - "No 'you tend to' / 'you usually' / 'next track' / 'you should play' phrases reach the coach/prompt surface (static gate over coach.py + matrix.py stays GREEN)"
    - "Phase 65 anti-poisoning + cross-turn rescope invariants are UNCHANGED — clear_source('recall') stays unconditional at dj_cohost.py:706-713, CancelledError re-raise stays in _maybe_dispatch_recall, single in-flight discipline preserved"
    - "VIBEMIX_RECALL_ENABLED stays default OFF — Phase 66 ships engineering-green behind the Phase 65 flag; Kaan flips it on his real corpus"
    - "All 9 Wave 0 RED tests flip GREEN; zero NEW failures vs the documented 8-WIP live-tuning-or-brain baseline"
  artifacts:
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "Edited _build_citation_strip with 'recall' in allow-list + fixed-verb branch; new module constant RECALL_CALLBACK_COOLDOWN_S; new agent field _last_recall_callback_at; cooldown gate in llm_node; cooldown arm in `else:` branch on bus-emit success (strict reached-the-audience semantic)"
      contains: "RECALL_CALLBACK_COOLDOWN_S, _last_recall_callback_at"
    - path: "src/vibemix/state/coach.py"
      provides: "New module-scope TRANSITION_SHAPE_RECALL_FRAGMENT_TPL + VOCABULARY_RECALL_FRAGMENT_TPL string constants; new recall_fragment_for_event function with falsy-gate cold-path byte-identity; build_prompt edited to append fragment"
      contains: "recall_fragment_for_event, TRANSITION_SHAPE_RECALL_FRAGMENT_TPL, VOCABULARY_RECALL_FRAGMENT_TPL"
    - path: ".planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md"
      provides: "NEW HUMAN-UAT scaffold (clones 54-HUMAN-UAT.md shape) with Kaan-ear test items for the transition callback, the vocabulary callback, the cooldown discipline, and the absence of anti-feature phrases"
      contains: "Phase 66 — Visible Copilot Move HUMAN-UAT"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "Appended §RECALL-EAR entry naming VIBEMIX_RECALL_ENABLED=1 flip-after-ear-pass discharge (mirrors §HARMONIC-VETO / §SHIP / §SHIP-V4 / §RECALL precedents)"
      contains: "§RECALL-EAR"
  key_links:
    - from: "src/vibemix/agent/dj_cohost.py::_build_citation_strip allow-list"
      to: "src/vibemix/agent/dj_cohost.py::_build_citation_strip recall verb branch"
      via: "'recall' added to the tuple at line 215; new elif-branch at line ~230 mirrors the key branch"
      pattern: "source not in \\(\"ev\", \"mix\", \"midi\", \"key\", \"recall\"\\)"
    - from: "src/vibemix/agent/dj_cohost.py::DJCoHostAgent.llm_node cooldown gate"
      to: "src/vibemix/agent/dj_cohost.py::DJCoHostAgent.llm_node cooldown arm (on bus-emit success)"
      via: "time.time() comparison drops recall_moments=[] when cooldown active; the broad try/except wrapping the bus emit gains an `else:` branch (success-only) that scans citation_strip for source=='recall' and arms self._last_recall_callback_at — a bus-emit failure cannot arm the cooldown"
      pattern: "RECALL_CALLBACK_COOLDOWN_S"
    - from: "src/vibemix/state/coach.py::recall_fragment_for_event"
      to: "src/vibemix/state/coach.py::AICoach.build_prompt return statement"
      via: "build_prompt appends recall_frag to the existing f-string return; falsy-gate keeps cold path byte-identical"
      pattern: "recall_fragment_for_event"
    - from: "tests/repo/test_no_recall_antifeatures.py FORBIDDEN_RECALL_PHRASES"
      to: "src/vibemix/state/coach.py + src/vibemix/prompts/matrix.py"
      via: "static gate scans for forbidden English-prose patterns after tokenize-stripping comments/docstrings + lowercasing"
      pattern: "FORBIDDEN_RECALL_PHRASES"
---

<objective>
Wave 1 GREEN implementation for Phase 66 (Visible Copilot Move). Flip all 9 Wave 0 RED tests GREEN by:
(a) widening the citation-strip allow-list to include `recall` with a fixed letters-only verb (mirrors the Phase 59 `key` precedent);
(b) wiring the coach-tier cooldown (≥120s between callbacks, max 1/turn — structural property of fragment + registry), with the arm placed on the `else:` branch of the bus-emit try/except so the cooldown reflects "a recall callback REACHED the audience";
(c) adding the two prompt-fragment templates + the `recall_fragment_for_event` helper with the cold-path falsy gate;
(d) wiring the fragment into `AICoach.build_prompt` so a TRACK_CHANGE/MIX_MOVE/LAYER_ARRIVAL turn with survivors invites Gemini to emit ONE `[recall:<id>]` callback;
(e) landing the HUMAN-UAT scaffold + §RECALL-EAR KAAN-ACTION entry.

Purpose: deliver the two visible copilot moves named in COPILOT-01/02 (transition-shape callback + vocabulary callback) on the existing Tier-1 `citation_strip` surface, with no anti-features creeping in (COPILOT-03). The phase preserves the four cardinal invariants Phase 65 closed (citation-grounding, byte-identical cold path, off-loop dispatch, one-socket) and ships behind the same default-OFF `VIBEMIX_RECALL_ENABLED` flag — Kaan flips after the ear-pass discharge in §RECALL-EAR.

Output: 2 source files edited + 1 doc file created + 1 doc file appended. Every Wave 0 RED test flips GREEN; static-gate stays GREEN; v5.0 byte-identity goldens stay GREEN; zero NEW regressions in the full suite beyond the documented 8-WIP baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-visible-copilot-move/66-CONTEXT.md
@.planning/phases/66-visible-copilot-move/66-RESEARCH.md
@.planning/phases/66-visible-copilot-move/66-PATTERNS.md
@.planning/phases/66-visible-copilot-move/66-VALIDATION.md
@.planning/phases/66-visible-copilot-move/66-01-PLAN.md
@.planning/phases/65-memory-retrieval-seam/65-VERIFICATION.md
@.claude/skills/frontend-enforcement/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Key contracts the executor consumes. -->

From src/vibemix/agent/dj_cohost.py:

- Line 43: `import time` (already imported — no new import needed for the cooldown timestamp).
- Lines 80-145: module-scope constants (`SILENCE_TOKEN`, `SCREEN_SKIP_EVENTS`, `DIET_AUDIO_SECONDS`, `ENV_SKILL_LEVEL`, `DEFAULT_*`, `OVERLAY_DURATION_MS`, `SILENCE_PAD_MS/BYTES`, `CITATION_STRIP_MAX_CHIPS=3`, `CITATION_VERB_MAX_WORDS=3`). The new `RECALL_CALLBACK_COOLDOWN_S` sits alongside.
- Lines 148-251: `_build_citation_strip(*, reaction_text, registry)` — the chip allow-list edit site at :215 + the `key` verb branch at :230. The allow-list tuple becomes `("ev", "mix", "midi", "key", "recall")`. The new `elif source == "recall":` branch fixes verb to `"recall"` (mirrors the `key` precedent).
- Lines 277-460 area: `DJCoHostAgent.__init__` (constructor) — co-locate the new `self._last_recall_callback_at: float = 0.0` with the existing `_invoke_counter` / `_recall_task` / `_pending_event` per-agent scalars (research §Pattern F).
- Lines 670-783: `llm_node` recall path. The cooldown gate inserts INSIDE the existing `try:` at line 676, AFTER `recall_moments = self._recall.get_latest()` at line 715, AFTER the `if self._registry is None: recall_moments = []` check at line 752, and BEFORE the registry write loop at line 754. Shape: `if recall_moments and (time.time() - self._last_recall_callback_at) < RECALL_CALLBACK_COOLDOWN_S: recall_moments = []`. When cooldown trips, `recall_moments = []` makes the rest of the path byte-identical to the cold/feature-off path (the falsy-gate at coach.py:220 already short-circuits; the agent's existing `if recall_moments and self._registry is not None:` at :754 short-circuits the registry writes; the `if recall_moments and not diet:` at :827 omits the kwarg).
- Lines 1497-1515: the bus-emit path. Current shape (verified verbatim from the live tree):

```python
if self._ipc_bus is not None and citation_action in ("emit", "bypass"):
    try:
        strip = (
            _build_citation_strip(
                reaction_text=full_text,
                registry=self._registry,
            )
            if self._registry is not None
            else []
        )
        reaction_msg = SessionCohostReaction.make(
            text=full_text,
            event_id=ev_tag,
            citation_strip=strip,
        )
        await self._ipc_bus.emit(reaction_msg.to_dict())
    except Exception as e:  # noqa: BLE001 — best-effort telemetry
        print(f"\n[cohost-reaction publish err] {e}", file=sys.stderr)
```

The cooldown ARM site is the `else:` branch of the SAME try/except: success-only execution means `await self._ipc_bus.emit(...)` returned without raising — i.e. the chip REACHED the audience. This satisfies CONTEXT.md Area 1 Q3 + RESEARCH §Pitfall 2 + §Open Q5: the cooldown reflects "a recall callback REACHED the audience". An emit failure jumps to the `except` and the arm never runs; a `strip`-build failure also jumps to the `except`; only a clean pass arms the cooldown.

From src/vibemix/state/coach.py:

- Lines 35-41 area: TYPE_CHECKING import of `Record` already in place from Phase 65 — no new import needed.
- Lines 200-229: existing `evidence_line` recall block (Phase 65). DO NOT MODIFY this — it stays as-is.
- Lines 271-382: `task_for_event` — the 9-branch event-type dispatcher. The new fragments are NOT inlined into the branches; they live in a NEW helper `recall_fragment_for_event` defined OUTSIDE the AICoach class, near the module-scope constants.
- Lines 384-429: `build_prompt` — the integration site. The edited return statement reads `return f"[{evidence} | event={ev.type}] {task}{recall_frag}"` where `recall_frag = recall_fragment_for_event(ev, recall_moments)`.
- The diet path at :415-422 is UNTOUCHED — it never receives recall_moments per the existing `if recall_moments and not diet:` gate at dj_cohost.py:827.

From .planning/phases/66-visible-copilot-move/66-RESEARCH.md §Pattern 1 + §Pattern 2:

The fragment template strings (verbatim — lift these EXACTLY into Plan 02):

TRANSITION_SHAPE_RECALL_FRAGMENT_TPL (lifted from RESEARCH.md lines 258-275 — KEEP the leading space):
```
 A PAST MOMENT from a prior session is attached in the 'FROM A PAST SESSION' block above — a transition with similar shape you've run before. If — AND ONLY IF — that past moment matches what just happened in the live audio, you MAY add ONE short past-tense callback line referencing it, citing exactly [recall:{record_id}]. Compare what you heard NOW vs. what's in the past signature — the delta is the whole point. Examples of shape: "that blend sat longer than the same one you ran last set", "killed the bass earlier this time around", "cleaner cut than the version you ran before". Hard rules: cite [recall:{record_id}] EXACTLY ONCE (the registry validates it; a fabricated id strips the whole turn); do NOT invent a past moment, paraphrase the past signature, or describe it as live; do NOT recommend a NEXT track or move (no 'try X next time'); do NOT claim a tendency ('you usually do', 'you always') — narrate THIS one compared to THAT one. If the past moment doesn't match, OMIT the callback entirely — your normal reaction is the floor.
```

VOCABULARY_RECALL_FRAGMENT_TPL (lifted from RESEARCH.md lines 288-302 — KEEP the leading space):
```
 A PAST MOMENT from a prior session is attached in the 'FROM A PAST SESSION' block above — a phrasing or call you made before. If — AND ONLY IF — what you'd naturally say RIGHT NOW lines up with that past signature, you MAY echo your own past words, citing exactly [recall:{record_id}]. The past signature is YOUR voice from before; speak in the same register, not Gemini-paraphrased. Examples: "same call you made on the last drop like this", "your line from the last set still holds". Hard rules: cite [recall:{record_id}] EXACTLY ONCE; do NOT invent a past phrasing; do NOT claim it's a habit ('you always', 'you tend to'); do NOT recommend a next move. If your live reaction wouldn't naturally echo the past, OMIT the callback — a forced echo is the failure mode this phase guards.
```

**FORBIDDEN-PHRASE NOTE (load-bearing):** the templates above CONTAIN literal forbidden phrases inside QUOTED EXAMPLES (`"you usually do"`, `"you always"`, `"you tend to"`). These are inside Python STRING LITERALS — the static gate's `_strip_comments_and_docstrings` removes STRING tokens (replaces with `""`), so the forbidden phrases inside the example quotes are STRIPPED OUT before the substring scan. This means the static gate is GREEN even with the example phrases in the templates. The stripper is the load-bearing mechanism; the negative-control stripper test in Plan 01 already pinned this.

Verb format pin from tests/agent/test_citation_strip_emit.py:211-231: `^[a-z]+( [a-z]+){0,2}$`. The fixed `"recall"` verb is a single lowercase word — trivially matches.

From KAAN-ACTION-LEGAL.md (root): the file exists. The new §RECALL-EAR entry appends in the same shape as the Phase 60 §HARMONIC-VETO + Phase 65 §RECALL precedents. Locate the file and read the §RECALL header to confirm the in-file naming convention before appending §RECALL-EAR.

From .planning/phases/54-hype-mode-live/54-HUMAN-UAT.md: the HUMAN-UAT shape (front-matter YAML + numbered tests + Summary section). Phase 66 clones with 4 ear-test items.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the citation-strip recall chip + coach-tier cooldown wiring in dj_cohost.py</name>
  <files>src/vibemix/agent/dj_cohost.py</files>
  <read_first>
    src/vibemix/agent/dj_cohost.py lines 80-145 — the module-constants block where RECALL_CALLBACK_COOLDOWN_S sits (alongside CITATION_STRIP_MAX_CHIPS / CITATION_VERB_MAX_WORDS / DIET_AUDIO_SECONDS — same idiom, one-line knob).
    src/vibemix/agent/dj_cohost.py lines 148-251 — the FULL _build_citation_strip function so the executor sees the chip-building loop in context; the allow-list is at :215 and the `key` verb branch at :230.
    src/vibemix/agent/dj_cohost.py lines 277-460 — DJCoHostAgent class body + __init__; co-locate the new `_last_recall_callback_at: float = 0.0` with the existing per-agent scalars (research §Pattern F).
    src/vibemix/agent/dj_cohost.py lines 670-783 — the FULL llm_node recall path Phase 65 wired. The cooldown gate inserts AFTER line 752's `if self._registry is None: recall_moments = []` and BEFORE line 754's strict-subset registration loop.
    src/vibemix/agent/dj_cohost.py lines 1497-1515 — the bus-emit try/except where _build_citation_strip is called + `SessionCohostReaction.make(...)` + `await self._ipc_bus.emit(...)`. The arm site is the `else:` branch of this try/except (the success-only branch).
    src/vibemix/agent/dj_cohost.py lines 1572-1584 — the existing end-of-turn `self._recall.clear()` block. Confirm the cooldown arm runs BEFORE this clear (since clear() resets the recall service, not the cooldown).
    .planning/phases/66-visible-copilot-move/66-PATTERNS.md §"src/vibemix/agent/dj_cohost.py — chip allow-list + recall verb branch + cooldown wiring" — the verbatim analog + insertion-site map.
    .planning/phases/66-visible-copilot-move/66-RESEARCH.md §Pattern 3 (Coach-Tier Cooldown Wiring) + §Pitfall 2 (cooldown arm at emit-success, not dispatch) + §Open Q5 (the strict reached-the-audience semantic) + §Code Examples.
    .planning/phases/66-visible-copilot-move/66-01-PLAN.md — Plan 01's tasks pinned the RED contract; the cooldown tests in test_dj_cohost_linter.py import RECALL_CALLBACK_COOLDOWN_S — the new constant MUST be importable from `vibemix.agent.dj_cohost`.
  </read_first>
  <behavior>
    Four structural edits to src/vibemix/agent/dj_cohost.py, in this order, each behind an unambiguous Phase 66 comment marker:

    Edit 1 — chip allow-list + `recall` verb branch (`_build_citation_strip`):
    - Change the allow-list tuple `("ev", "mix", "midi", "key")` to `("ev", "mix", "midi", "key", "recall")` (at the existing source-not-in tuple line, near :215). Add an inline comment naming the Phase 66 / COPILOT-01 driver.
    - Insert a new `elif source == "recall":` branch immediately AFTER the existing `if source == "key":` branch and BEFORE the `else:` branch. Body: `verb = "recall"` with a multi-line comment naming the Phase 66 / COPILOT-01 driver + citing the `key` precedent + naming the body shape `<session_id>:<seq>` + naming the locked verb-format regex `^[a-z]+( [a-z]+){0,2}$` that `"recall"` trivially matches.

    Edit 2 — module-scope cooldown constant + per-agent timestamp field:
    - Add `RECALL_CALLBACK_COOLDOWN_S: float = 120.0` to the module-constants block (after `CITATION_VERB_MAX_WORDS`). Comment block above it: cite Phase 66 / COPILOT-02; cite the "≥120s between any two recall callbacks" CONTEXT.md Area 1 Q3 decision; note that this is a SEPARATE concern from EventDetector.MIN_EVENT_GAP_PER_TYPE (research §Anti-Patterns: "Putting the cooldown in EventDetector._cooldown_ok"); name the one-line-tunable contract for the Kaan-ear KAAN-ACTION.
    - Add `self._last_recall_callback_at: float = 0.0` to `DJCoHostAgent.__init__` co-located with the existing per-agent timestamp/counter scalars (research §Pattern F — alongside `_invoke_counter` / `_recall_task` / `_pending_event`; the EXACT line varies based on the existing __init__ shape — find the right spot near the recall-related fields already present). Comment block: cite Phase 66 / COPILOT-02; document that the timestamp is WALL-CLOCK time.time() not session-relative t (so a single Kaan session's gap is what matters, not multi-session).

    Edit 3 — cooldown gate in llm_node:
    - Insert AFTER line 752's `if self._registry is None: recall_moments = []` and BEFORE line 754's `if recall_moments and self._registry is not None:` strict-subset registration loop.
    - Shape: `if recall_moments and (time.time() - self._last_recall_callback_at) < RECALL_CALLBACK_COOLDOWN_S:` body: `recall_moments = []` with a multi-line comment block naming Phase 66 / COPILOT-02; cite the byte-identical-to-cold-path discipline (when recall_moments=[] the rest of the path is structurally identical to the empty-survivor case); name the arm-at-emit-success pattern (research §Pitfall 2 + §Open Q5); reference the test pin `test_cooldown_suppresses_back_to_back_recalls_COPILOT02`.

    Edit 4 — cooldown arm on bus-emit success (strict "REACHED the audience" semantic):
    - The existing try/except wraps `_build_citation_strip(...)` + `SessionCohostReaction.make(...)` + `await self._ipc_bus.emit(reaction_msg.to_dict())`. ADD an `else:` branch to this try/except that runs ONLY when the try body completes without raising — i.e. the bus emit returned cleanly, the chip reached the audience.
    - In the `else:` branch: scan `strip` for any chip with `event_id` starting `"recall:"` via `any(chip.get("event_id", "").startswith("recall:") for chip in strip)`. If True, set `self._last_recall_callback_at = time.time()`. The arm has its OWN narrow try/except wrapper for defense-in-depth (a fault in `chip.get` should not propagate), but the OUTER try/except's `else:` placement means a bus-emit FAILURE never reaches this code at all — the cooldown cannot arm on a failed emit. This satisfies the strict "REACHED the audience" semantic locked in CONTEXT.md Area 1 Q3 + RESEARCH §Pitfall 2 + §Open Q5.

    Final shape (in pseudocode — exact line anchors live in the file, do NOT rely on numeric anchors that drift between commits):

    ```
    if self._ipc_bus is not None and citation_action in ("emit", "bypass"):
        try:
            strip = (
                _build_citation_strip(
                    reaction_text=full_text,
                    registry=self._registry,
                )
                if self._registry is not None
                else []
            )
            reaction_msg = SessionCohostReaction.make(
                text=full_text,
                event_id=ev_tag,
                citation_strip=strip,
            )
            await self._ipc_bus.emit(reaction_msg.to_dict())
        except Exception as e:  # noqa: BLE001 — best-effort telemetry
            print(f"\n[cohost-reaction publish err] {e}", file=sys.stderr)
        else:
            # Phase 66 (COPILOT-02) — arm cooldown ONLY when the chip
            # reached the audience. The `else:` branch runs iff the try
            # body completed without raising — i.e. `_ipc_bus.emit(...)`
            # returned cleanly. A bus-emit failure jumps to the except
            # above and the arm never runs; this is the strict semantic
            # locked in CONTEXT.md Area 1 Q3 + RESEARCH §Pitfall 2 +
            # §Open Q5: cooldown reflects "a recall callback REACHED the
            # audience", not "a recall callback was MERELY ATTEMPTED".
            # One-way (arm only); no reset path.
            try:
                if any(chip.get("event_id", "").startswith("recall:") for chip in strip):
                    self._last_recall_callback_at = time.time()
            except Exception as _e:  # noqa: BLE001
                print(f"\n[recall cooldown arm err] {_e}", file=sys.stderr)
    ```

    Wave 0 tests pinned a specific test for `RECALL_CALLBACK_COOLDOWN_S` import path — confirm the constant is module-scope (importable as `from vibemix.agent.dj_cohost import RECALL_CALLBACK_COOLDOWN_S`). Confirm `self._last_recall_callback_at` is a single-underscore-prefixed instance attribute (NOT a class attribute — research §Pattern F mandates instance scope).

    Preserve all Phase 65 invariants verbatim — DO NOT touch lines 686-737 (the `clear_source("recall")` + `get_latest()` + WR-01 + WR-03 blocks). The cooldown gate's job is to DROP `recall_moments` AFTER those gates run; the registry writes at :754-778 and the prompt-kwarg threading at :826-836 are then short-circuited by the existing falsy gates.
  </behavior>
  <action>
    Open src/vibemix/agent/dj_cohost.py and make the four edits described in <behavior>, in this order:

    1. Edit `_build_citation_strip` — change the allow-list tuple to include `"recall"` as the fifth element. Add the `elif source == "recall": verb = "recall"` branch after the existing `if source == "key":` branch. The new branch must include the multi-line Phase 66 comment block citing the precedent + the body shape + the verb-format regex.

    2. Add `RECALL_CALLBACK_COOLDOWN_S: float = 120.0` after `CITATION_VERB_MAX_WORDS` (find the exact line by reading the existing constants block; place the new constant in idiom order — alongside the chip-related caps). Comment block above per <behavior>.

    3. In `DJCoHostAgent.__init__` add `self._last_recall_callback_at: float = 0.0` near the existing recall-related fields (`self._recall`, `self._recall_enabled`, `self._recall_task`). Find the exact spot by grep'ing for `self._recall = ` in the file; place the new field immediately after the existing recall-related initializations. Comment block per <behavior>.

    4. In `llm_node` (the existing try block at line 676), insert the cooldown gate AFTER line 752's `if self._registry is None: recall_moments = []` and BEFORE line 754's `if recall_moments and self._registry is not None:` strict-subset loop. Shape and comments per <behavior>.

    5. In the bus-emit try/except (locate by `grep -n "cohost-reaction publish err" src/vibemix/agent/dj_cohost.py`), add an `else:` branch to the try/except per the pseudocode in <behavior>. The `else:` branch:
       - runs ONLY when the try body (which contains `await self._ipc_bus.emit(reaction_msg.to_dict())`) completed without raising — strict "REACHED the audience" semantic per CONTEXT.md Area 1 Q3 + RESEARCH §Pitfall 2 + §Open Q5
       - scans `strip` (still in scope from the try block) for any chip with `event_id` starting `"recall:"`
       - arms `self._last_recall_callback_at = time.time()` if a recall chip is present
       - wraps the scan + arm in its OWN narrow `try/except Exception as _e: print(f"\n[recall cooldown arm err] {_e}", file=sys.stderr)` so a fault in `chip.get` cannot propagate; this inner wrapper is defense-in-depth, not a substitute for the `else:` placement
       - does NOT modify `strip`, does NOT call any other side-effecting function, does NOT add a reset path (the cooldown is one-way)

    Do NOT alter any of the Phase 65 wiring at lines 686-783 outside the single insertion described in Edit 3. Do NOT modify the end-of-turn `self._recall.clear()` — the cooldown is independent of the latch lifecycle.

    Quick sanity self-check after editing:
    - `grep -n "RECALL_CALLBACK_COOLDOWN_S\|_last_recall_callback_at" src/vibemix/agent/dj_cohost.py` → ≥4 hits (constant decl + __init__ assignment + llm_node read + emit-success arm)
    - `grep -n '("ev", "mix", "midi", "key", "recall")' src/vibemix/agent/dj_cohost.py` → exactly 1 hit (the edited allow-list)
    - `grep -nE "^\s+else:\s*$" src/vibemix/agent/dj_cohost.py | wc -l` → at least 1 new hit beyond the pre-edit count (the new `else:` branch on the bus-emit try/except)
    - `grep -n "\[recall cooldown arm err\]" src/vibemix/agent/dj_cohost.py` → exactly 1 hit (the inner arm wrapper)
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/agent/test_citation_strip_emit.py tests/agent/test_dj_cohost_linter.py -q --no-header 2>&1 | tail -20</automated>
    Expected: the 2 chip tests (test_recall_citation_yields_chip_with_registry_timestamp_COPILOT01 + test_fabricated_recall_yields_no_chip_COPILOT01) flip GREEN; the existing key-chip + verb-format pin tests stay GREEN; the 2 cooldown tests (test_cooldown_suppresses_back_to_back_recalls_COPILOT02 + test_max_one_recall_per_turn_COPILOT02) — the cooldown-suppresses test flips GREEN (cooldown gate is wired AND arm runs on emit-success); the max-one-recall test STAYS RED until Task 2 lands the fragment helper. The Phase 65 anti-poisoning tests (test_fabricated_recall_strips_turn + test_fabricated_recall_strips_turn_n_plus_1_with_empty_recall) MUST stay GREEN.
  </verify>
  <done>
    `RECALL_CALLBACK_COOLDOWN_S = 120.0` at module scope; `self._last_recall_callback_at` initialized in __init__; cooldown gate in llm_node at the documented insertion point; cooldown arm in the `else:` branch of the bus-emit try/except (success-only — strict reached-the-audience semantic); allow-list widened with `recall` + new verb branch; chip RED tests GREEN; cooldown-suppresses test GREEN; Phase 65 floor unchanged; existing key-chip + verb-format pin unchanged. Commit message: `feat(66-02): land recall chip allow-list + coach-tier cooldown wiring in dj_cohost.py (COPILOT-01/02)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land the prompt-fragment helper + build_prompt integration in coach.py</name>
  <files>src/vibemix/state/coach.py</files>
  <read_first>
    src/vibemix/state/coach.py lines 1-50 — module-scope imports + constants; confirm `Record` is already under TYPE_CHECKING (Phase 65 wired it). The new TRANSITION_SHAPE_RECALL_FRAGMENT_TPL + VOCABULARY_RECALL_FRAGMENT_TPL strings sit at module scope NEAR existing module constants but BEFORE the AICoach class definition.
    src/vibemix/state/coach.py lines 200-229 — the existing evidence_line recall block + falsy-gate at :220 (the analog Phase 66's recall_fragment_for_event helper clones).
    src/vibemix/state/coach.py lines 271-382 — the existing task_for_event 9-branch dispatcher (the SHAPE precedent for the fragment templates' "cited, narrate-only, do NOT invent" discipline — especially KEY_CLASH at :332 and TRANSITION_OPPORTUNITY at :363).
    src/vibemix/state/coach.py lines 384-429 — `build_prompt`. The integration site at :428 — change `return f"[{evidence} | event={ev.type}] {task}"` to `recall_frag = recall_fragment_for_event(ev, recall_moments); return f"[{evidence} | event={ev.type}] {task}{recall_frag}"` — diet branch at :415-422 stays untouched.
    .planning/phases/66-visible-copilot-move/66-RESEARCH.md §Pattern 1 + §Pattern 2 — the EXACT fragment template strings to lift verbatim into coach.py.
    .planning/phases/66-visible-copilot-move/66-RESEARCH.md §Code Examples (recall_fragment_for_event helper) — the helper signature, the TRACK_CHANGE/MIX_MOVE/LAYER_ARRIVAL → transition branch, PHASE → vocabulary branch, falsy-gate cold-path.
    .planning/phases/66-visible-copilot-move/66-PATTERNS.md §"src/vibemix/state/coach.py — recall_fragment_for_event helper + build_prompt integration".
  </read_first>
  <behavior>
    One structural change spanning two artifacts in coach.py:

    Edit A — module-scope fragment template constants + the helper function:
    - Add the TWO module-scope string constants TRANSITION_SHAPE_RECALL_FRAGMENT_TPL + VOCABULARY_RECALL_FRAGMENT_TPL — copy the EXACT text from RESEARCH.md §Pattern 1 lines 258-275 and §Pattern 2 lines 288-302 into the file. Place them at module scope AFTER any existing constants but BEFORE the AICoach class body. Each constant has a comment block citing: Phase 66 / COPILOT-01 (transition) or COPILOT-02 (vocabulary); the precedent (TRANSITION_OPPORTUNITY at :363-381 — research §Pattern 2 analog); the locked anti-feature rules baked in; the static-gate inviolability (forbidden phrases inside QUOTED EXAMPLES are stripped by `_strip_comments_and_docstrings` — this is why the gate stays GREEN; document explicitly).
    - Add the `recall_fragment_for_event(ev: Event, recall_moments: "list[Record] | None") -> str` function at module scope AFTER the two template constants and BEFORE the AICoach class. Signature + docstring per research §Code Examples (the helper section, RESEARCH.md lines 654-690). Body: `if not recall_moments: return ""` (the cold-path byte-identity guarantee — research §Pitfall 1 + Pattern A); then `strongest = recall_moments[0]` (cosine_topk sort order: highest first — Phase 65 contract); then `if ev.type in ("TRACK_CHANGE", "MIX_MOVE", "LAYER_ARRIVAL"): return TRANSITION_SHAPE_RECALL_FRAGMENT_TPL.format(record_id=strongest.record_id)`; then `if ev.type == "PHASE": return VOCABULARY_RECALL_FRAGMENT_TPL.format(record_id=strongest.record_id)`; final `return ""`. The TRACK_CHANGE overlap is resolved by FIRST-MATCH WINS: TRACK_CHANGE is in BOTH research §Pitfall 5 candidate gates, but the transition branch lists TRACK_CHANGE first so transition WINS (per research §Recommendation Q1 + CONTEXT.md decision area resolution).

    Edit B — `AICoach.build_prompt` integration:
    - Edit `AICoach.build_prompt` at line 423-429. Insert `recall_frag = recall_fragment_for_event(ev, recall_moments)` between the existing `task = AICoach.task_for_event(ev)` at :428 and the return statement; change the return to `return f"[{evidence} | event={ev.type}] {task}{recall_frag}"`. Do NOT touch the diet branch at :415-422.

    With these edits landed, all five recall-fragment + byte-identity Wave 0 RED tests in tests/state/test_coach.py flip GREEN; the max-one-recall test in tests/agent/test_dj_cohost_linter.py flips GREEN (the fragment template's "cite EXACTLY ONCE" + the strongest-survivor-only interpolation is the structural enforcement). The static gate at tests/repo/test_no_recall_antifeatures.py stays GREEN — the forbidden phrases inside QUOTED examples in TRANSITION_SHAPE_RECALL_FRAGMENT_TPL ("you usually do", "you always") are inside Python STRING LITERALS, so `_strip_comments_and_docstrings` removes them before the substring scan.
  </behavior>
  <action>
    1. Open src/vibemix/state/coach.py. Add the two module-scope string constants TRANSITION_SHAPE_RECALL_FRAGMENT_TPL + VOCABULARY_RECALL_FRAGMENT_TPL — copy the EXACT text from RESEARCH.md §Pattern 1 and §Pattern 2 (the literal text is reproduced in the <interfaces> block of this plan — lift it character-for-character INCLUDING the leading space at the start of each template). Place them at module scope after the existing module constants but before the AICoach class definition. Each constant carries a multi-line comment block per <behavior>. **Critical:** the leading space in each template is load-bearing — `build_prompt` concatenates the fragment directly onto the task string with no separator, so the leading space is the only delimiter; preserve it exactly.

    2. Add the `recall_fragment_for_event(ev, recall_moments)` function at module scope immediately after the two templates. Signature, docstring, and body per <behavior> + research §Code Examples (RESEARCH.md lines 654-690). The function lives OUTSIDE the AICoach class (research §Don't Hand-Roll: "prompt-fragment dispatch by event type ... use inline conditional appends in task_for_event ... a new dispatcher class is over-engineering" — but a top-level pure function is NOT a dispatcher class; it is the helper the research file names directly).

    3. Edit `AICoach.build_prompt` at the existing return statement (line ~428): insert `recall_frag = recall_fragment_for_event(ev, recall_moments)` between the `task = AICoach.task_for_event(ev)` and the `return` line; change the return to `return f"[{evidence} | event={ev.type}] {task}{recall_frag}"`. The diet path return at :415-422 stays untouched.

    Self-check after editing:
    - `grep -n "recall_fragment_for_event\|TRANSITION_SHAPE_RECALL_FRAGMENT_TPL\|VOCABULARY_RECALL_FRAGMENT_TPL" src/vibemix/state/coach.py` → ≥5 hits (2 constant declarations + 1 helper definition + 1 helper call in build_prompt + at least 1 docstring reference)
    - `grep -n "def recall_fragment_for_event" src/vibemix/state/coach.py` → exactly 1 hit
    - Run the static gate to confirm forbidden-phrase quotes inside templates are stripped: `pytest tests/repo/test_no_recall_antifeatures.py -q` → main scan GREEN, negative-control stripper GREEN

    Project-skill discipline: this task touches NO UI surface (the chip is a one-character allow-list edit in Task 1; the renderer is unchanged; no styling/CSS touched). The frontend-enforcement skill governs the chip's CDJ-Whisper material when a UI-SPEC chain runs — Phase 66 ships without distinct chip treatment (research §Open Q4: v6.0 ships the recall chip with the SAME visual treatment as `key`/`ev`/`mix`/`midi` chips; distinct treatment is a v6.1+ touch).
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/state/test_coach.py tests/agent/test_dj_cohost_linter.py tests/repo/test_no_recall_antifeatures.py -q --no-header 2>&1 | tail -25</automated>
    Expected: all 5 fragment + byte-identity tests in test_coach.py GREEN (test_transition_recall_fragment_appears, test_vocabulary_recall_fragment_appears, test_task_for_event_byte_identical_v5_baseline_no_recall, test_only_strongest_survivor_record_id_in_fragment, test_transition_wins_track_change_overlap); the max-one-recall test in test_dj_cohost_linter.py GREEN; the static gate main scan stays GREEN (string-literal stripping discipline) AND the negative-control stripper test stays GREEN; all Phase 65 anti-poisoning + cross-turn + byte-identity goldens stay GREEN.
  </verify>
  <done>
    Two module-scope fragment templates in coach.py with verbatim text from RESEARCH.md §Pattern 1 + §Pattern 2 (leading-space preserved); recall_fragment_for_event function defined at module scope with falsy gate + transition/vocabulary branches + TRACK_CHANGE-first-wins; build_prompt edited to append recall_frag in the non-diet path; all 5 fragment+byte-identity tests GREEN; max-one-recall test GREEN; Phase 65 floor unchanged; static gate GREEN (main scan + negative-control stripper). Commit message: `feat(66-02): land recall prompt fragments + recall_fragment_for_event helper (COPILOT-01/02/03)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Land HUMAN-UAT scaffold + §RECALL-EAR KAAN-ACTION entry</name>
  <files>.planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md, KAAN-ACTION-LEGAL.md</files>
  <read_first>
    .planning/phases/54-hype-mode-live/54-HUMAN-UAT.md — the HUMAN-UAT shape (front-matter + numbered ear-tests + Summary).
    KAAN-ACTION-LEGAL.md — read the existing §HARMONIC-VETO and §RECALL sections (if §RECALL exists from Phase 65 — confirm in-file before appending). The new §RECALL-EAR sits alongside as a sibling discharge.
    .planning/phases/66-visible-copilot-move/66-CONTEXT.md §Area 4 (Kaan-Ear Release Gate) — names the carry-forward pattern + the discharge surface.
    .planning/phases/66-visible-copilot-move/66-RESEARCH.md §Pitfall 4 (anti-paraphrase failure mode — the explicit ear-check for the vocabulary callback).
  </read_first>
  <behavior>
    Two documentation artifacts, each on its own review surface:

    Edit C — create .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md (NEW file):
    - Front-matter YAML cloning 54-HUMAN-UAT.md shape: `status: partial`, `phase: 66-visible-copilot-move`, `source: [66-VERIFICATION.md]`, `started: 2026-05-22`, `updated: 2026-05-22`.
    - `## Current Test`: "[awaiting human testing — Kaan-action on the live/real-corpus clock]"
    - `## Tests` section with 4 numbered ear-tests:
      1. **Transition-shape callback grounds on a real past move** — flip VIBEMIX_RECALL_ENABLED=1, run a live DJ set with at least one TRACK_CHANGE/MIX_MOVE/LAYER_ARRIVAL where memory should have a relevant past move. Expected: AI emits ONE callback line with `[recall:<id>]`; the recall chip surfaces; the past-tense framing is honest (compares NOW to THEN); no scripted feel; no "you tend to"/"you usually". Result: [pending].
      2. **Vocabulary callback echoes prior phrasing** — on a PHASE event where past phrasing is in the survivor list. Expected: the AI echoes Kaan's own words (quote-shape), not Gemini-paraphrased; cooldown rare; no nagging. Research §Pitfall 4 — anti-paraphrase failure mode is the explicit ear-check. Result: [pending].
      3. **Cooldown discipline by ear** — no more than 1 recall callback per ~2 minutes; never two in succession. If two callbacks land within 120s, that is a bug. Result: [pending].
      4. **Anti-feature absence by ear** — listen for "you tend to" / "you usually" / "you always" / "next track" / "you should play"; if any heard, that is a COPILOT-03 failure even if the static gate is green (the gate scans CODE; the ear catches Gemini drift). Result: [pending].
    - `## Summary` section: `total: 4`, `passed: 0`, `issues: 0`, `pending: 4`, `skipped: 0`, `blocked: 0`.

    Edit D — append §RECALL-EAR to KAAN-ACTION-LEGAL.md:
    - Locate the file at the repo root (`/Users/ozai/projects/dj-set-ai/KAAN-ACTION-LEGAL.md`). Read the existing §HARMONIC-VETO + §RECALL entries first (Phase 65's §RECALL surfaced the Kaan-ear veto for the retrieval seam; §RECALL-EAR is the Phase 66 sibling for the visible callbacks).
    - Append a new section `## §RECALL-EAR — Phase 66 Visible Copilot Move Kaan-Ear Discharge` mirroring the §HARMONIC-VETO / §RECALL prose shape: status field, what to listen for (the 4 ear-tests from 66-HUMAN-UAT.md), the env-var flip (`export VIBEMIX_RECALL_ENABLED=1`), the carry-forward rationale ("engineering-green per `gsd-autonomous fully`; Kaan-ear is veto, not blocker"), and a cross-reference to `66-HUMAN-UAT.md`. Do NOT alter any existing entry in the file.
  </behavior>
  <action>
    1. Create .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md with the front-matter + 4 ear-tests + Summary per <behavior>. Mirror the .planning/phases/54-hype-mode-live/54-HUMAN-UAT.md shape exactly — read it first to confirm the front-matter delimiters and section heading shapes.

    2. Open /Users/ozai/projects/dj-set-ai/KAAN-ACTION-LEGAL.md. Read the existing §HARMONIC-VETO and §RECALL sections to lock the in-file naming/section-header convention. APPEND a new `## §RECALL-EAR — Phase 66 Visible Copilot Move Kaan-Ear Discharge` section at the END of the file (or in the §RECALL adjacent position if the file has a sibling-section convention — confirm from the existing file structure). Body per <behavior>: status (open / pending Kaan-ear), what to listen for (4 ear-tests from 66-HUMAN-UAT.md), the env-var flip command, the carry-forward rationale, the cross-reference to 66-HUMAN-UAT.md. Do NOT alter any existing entry.

    Self-check after editing:
    - `ls -la .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md` → file resolves
    - `grep -n "§RECALL-EAR" KAAN-ACTION-LEGAL.md` → ≥1 hit
    - `grep -c "result: \[pending\]" .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md` → exactly 4 (one per ear-test)
  </action>
  <verify>
    <automated>ls -la .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md && grep -n "§RECALL-EAR" KAAN-ACTION-LEGAL.md && grep -c "result: \[pending\]" .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md</automated>
    Expected: the HUMAN-UAT file resolves; §RECALL-EAR hits at least once in KAAN-ACTION-LEGAL.md; the 4-pending-results grep returns `4`.
  </verify>
  <done>
    66-HUMAN-UAT.md created with 4 ear-tests + Summary block; §RECALL-EAR section appended to KAAN-ACTION-LEGAL.md mirroring the §HARMONIC-VETO / §RECALL precedent shape; no existing KAAN-ACTION entries altered. Commit message: `docs(66-02): add Phase 66 HUMAN-UAT scaffold + §RECALL-EAR KAAN-ACTION entry (COPILOT-01/02/03 carry-forward)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (Gemini) → agent reaction text | Untrusted reaction text crosses back into _build_citation_strip + the citation linter. Phase 66 widens the chip allow-list to include `recall` (existence-only); the registry strict-subset invariant (Phase 65) is the load-bearing security control. A fabricated [recall:<unregistered>] strips the WHOLE turn before any chip can surface. |
| Developer-authored prompt source → Gemini system instruction | Phase 66 adds two new fragment templates in coach.py; the static gate at tests/repo/test_no_recall_antifeatures.py is the boundary control that prevents forbidden-phrase regression. The Python string-literal stripping discipline means the templates can quote forbidden phrases as EXAMPLES (inside quotes) without tripping the gate — the gate only fires on identifier-shaped forbidden text, which is rare for English prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02-01 | Tampering | A fabricated `[recall:<unregistered>]` chip surfaces despite the Phase 65 linter (turn-strip) discipline | mitigate | The linter strips the WHOLE turn (not just the chip) when an unregistered recall id is present (Phase 65 test_fabricated_recall_strips_turn — verified GREEN before AND after Phase 66). The chip never reaches `_build_citation_strip` because `full_text` is replaced with the stripped reaction. Phase 66 inherits this by NOT touching the linter or the turn-strip path. |
| T-66-02-02 | Tampering | A turn with multiple survivors emits MORE THAN ONE `[recall:<id>]` token (max-1-per-turn violation) | mitigate | The fragment templates instruct "cite [recall:{record_id}] EXACTLY ONCE" (prompt-level); only the strongest survivor's record_id is interpolated into the fragment (helper-level); the registry strict-subset at dj_cohost.py:754-778 is the linter-level enforcement (any extra recall id in the reaction MUST be unregistered → turn strips). Three layers of defense; verified by test_max_one_recall_per_turn_COPILOT02. |
| T-66-02-03 | Tampering | A recall chip from turn N's emit-success cooldown-arm leaks into turn N+1's cooldown computation via a stale `_last_recall_callback_at` | accept | The cooldown is one-way (arm only, no reset); the stale value is the EXPECTED state — it represents "the last time a recall reached the audience". A future turn checks `time.time() - self._last_recall_callback_at` and the wall-clock progression naturally drains the cooldown over 120s. No mitigation needed; this is the design. |
| T-66-02-04 | Spoofing | A developer pastes a forbidden phrase into a fragment template (e.g. "you usually do" as plain prose not quoted) and the static gate fails to catch it | mitigate | The static gate at tests/repo/test_no_recall_antifeatures.py scans the post-stripped source for the FORBIDDEN_RECALL_PHRASES tuple. The stripper removes string literals + comments + docstrings, so the gate's coverage class is "forbidden phrases as identifier-adjacent code text" — which Python syntax forbids for multi-word phrases. The realistic developer paste-in mode is "phrase inside a quoted example" — which the templates explicitly use and the stripper removes. The DEFENSE-IN-DEPTH is the §RECALL-EAR Kaan-ear check (66-HUMAN-UAT.md item 4): if Gemini emits a forbidden phrase via prompt drift, Kaan's ear catches it. |
| T-66-02-05 | Tampering | The cooldown gate is bypassed by a TRACK_CHANGE that overlaps with a vocabulary-eligible PHASE event in quick succession (the gate is per-callback, not per-event-type) | mitigate | The cooldown is GLOBAL across all recall-eligible events (one timestamp field for all callback emissions); a TRACK_CHANGE arm at t=100 blocks BOTH a transition-shape candidate AND a vocabulary candidate at t=180. The test_cooldown_suppresses_back_to_back_recalls_COPILOT02 covers this by driving two consecutive TRACK_CHANGE events; the contract is checked. |
| T-66-02-06 | Repudiation | The recall chip's timestamp_s is wrong (shows past-record .ts instead of current turn's t_session) — the chip lies about WHEN the callback fired | mitigate | The chip's `timestamp_s` comes from the registry observation at `_build_citation_strip:228`. The registry write at dj_cohost.py:771 uses `t_session = getattr(ev.state, "set_seconds", 0.0)` (CURRENT turn's set-time). Phase 66 does NOT touch this — the chip honestly shows when the recall callback FIRED, not when the past moment originally happened (research §Pitfall 3). |
| T-66-02-07 | Tampering | A bus-emit failure leaves the cooldown unarmed but the chip's text MAY have partially reached the audience before the exception fired, so a subsequent recall callback at t+30s would not be suppressed | accept | The strict "REACHED the audience" semantic (CONTEXT.md Area 1 Q3 + RESEARCH §Pitfall 2 + §Open Q5) defines "REACHED" as "`_ipc_bus.emit(...)` returned cleanly". A partial-emit failure is treated as "did NOT reach" — the cooldown does not arm. This is the strictest correct semantic: arming on partial failure would mean cooldown reflects "we tried to emit", not "audience saw the chip". The accepted edge case is a vanishingly rare race where the IPC delivers but the await raises (e.g. the receiver disconnects mid-send); the asymmetric failure favors more callbacks, not fewer, which matches the "rare-and-earned but never accidentally suppressed" design goal. |
| T-66-02-SC | Tampering | Phase 66 installs no packages — no slopcheck surface | accept | N/A — pure code+test+doc phase with zero net-new dependencies. |
</threat_model>

<verification>
- Targeted phase-surface verify: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/agent/test_citation_strip_emit.py tests/agent/test_dj_cohost_linter.py tests/state/test_coach.py tests/repo/test_no_recall_antifeatures.py -q --no-header 2>&1 | tail -25` — expect ALL Wave 0 RED tests GREEN.
- Phase 65 floor regression check: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/agent/test_dj_cohost_linter.py::test_fabricated_recall_strips_turn tests/agent/test_dj_cohost_linter.py::test_fabricated_recall_strips_turn_n_plus_1_with_empty_recall tests/state/test_coach.py::test_evidence_line_silent_state_full_format tests/state/test_coach.py::test_evidence_line_audible_no_recall_byte_identical_v5_baseline -v` — expect ALL 4 GREEN.
- Phase 65 retrieval seam regression: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/memory/test_retrieval.py tests/coach/test_citation_linter.py tests/state/test_evidence_registry.py tests/prompts/test_matrix.py -q --no-header 2>&1 | tail -10` — expect zero failures.
- Full-suite regression: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q 2>&1 | tail -15` — expect 4147+ passed (the 9 newly-GREEN tests above the previous 4138), 8 pre-existing baseline failures (the documented `live-tuning-or-brain` WIP), zero NEW failures.
- Grep audits to prove the structural edits landed:
  - `grep -n "RECALL_CALLBACK_COOLDOWN_S" src/vibemix/agent/dj_cohost.py` → ≥4 hits (constant decl + cooldown gate read + at least one comment ref + import-availability)
  - `grep -n '("ev", "mix", "midi", "key", "recall")' src/vibemix/agent/dj_cohost.py` → exactly 1 hit (the edited allow-list)
  - `grep -n "elif source == \"recall\":" src/vibemix/agent/dj_cohost.py` → 1 hit (the new verb branch)
  - `grep -n "\[recall cooldown arm err\]" src/vibemix/agent/dj_cohost.py` → exactly 1 hit (the inner arm wrapper inside the else-branch)
  - `grep -n "recall_fragment_for_event" src/vibemix/state/coach.py` → ≥3 hits (definition + call from build_prompt + comment ref)
  - `grep -n "TRANSITION_SHAPE_RECALL_FRAGMENT_TPL\|VOCABULARY_RECALL_FRAGMENT_TPL" src/vibemix/state/coach.py` → ≥2 hits (the two template declarations)
  - `grep -n "§RECALL-EAR" KAAN-ACTION-LEGAL.md` → ≥1 hit
  - `ls -la .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md` → file resolves
- Phase 65 dormancy gate regression: the cooldown wiring touches `time.time()` reads only — no new imports added to retrieval.py or ingest.py; the dormancy gate at tests/memory/test_no_live_path_import.py stays GREEN (verify by running it explicitly).
</verification>

<success_criteria>
- All 9 Wave 0 RED tests flip GREEN (2 chip + 2 cooldown + 5 fragment/byte-identity)
- The negative-control stripper test in tests/repo/test_no_recall_antifeatures.py stays GREEN
- The main static-gate scan stays GREEN (no forbidden phrases in identifier-adjacent code text in coach.py / matrix.py; example quotes inside templates are stripped by `_strip_comments_and_docstrings`)
- All Phase 65 anti-poisoning + cross-turn + byte-identity goldens stay GREEN (the v5.0 floor is intact)
- The verb-format regex pin `^[a-z]+( [a-z]+){0,2}$` stays GREEN (`"recall"` is a single lowercase word, trivially matches)
- Zero NEW failures in the full suite beyond the documented 8-WIP `live-tuning-or-brain` baseline
- KAAN-ACTION-LEGAL.md carries a new §RECALL-EAR entry; .planning/phases/66-visible-copilot-move/66-HUMAN-UAT.md exists with 4 ear-tests
- VIBEMIX_RECALL_ENABLED env-var default OFF (no change from Phase 65); the Phase 66 visible callbacks fire ONLY when Kaan flips the flag — engineering ships green behind the gate
- The cooldown arm site is the `else:` branch of the bus-emit try/except — strict "REACHED the audience" semantic; a bus-emit failure cannot arm the cooldown
- The four cardinal invariants Phase 65 closed (single-writer, citation-grounding, trust-the-audio, one-socket) are PRESERVED — no new ports, no new wire formats, the citation_strip rides the existing ipc.session.cohost-reaction envelope
- All `requirements: [COPILOT-01, COPILOT-02, COPILOT-03]` in this plan's frontmatter map to landed observable behaviors (verifiable by the 9 GREEN tests + the 4 HUMAN-UAT ear-tests)
</success_criteria>

<output>
Create `.planning/phases/66-visible-copilot-move/66-02-SUMMARY.md` when done, listing:
- The exact line ranges of each edit in dj_cohost.py + coach.py (separately for Task 1 and Task 2)
- Confirmation that the cooldown arm landed in the `else:` branch of the bus-emit try/except (strict reached-the-audience semantic)
- The full literal text of TRANSITION_SHAPE_RECALL_FRAGMENT_TPL + VOCABULARY_RECALL_FRAGMENT_TPL as committed (so verify-work can confirm the Wave 0 substring choices match the deployed templates)
- The exact §RECALL-EAR section as appended to KAAN-ACTION-LEGAL.md (Task 3)
- The grep-audit numbers confirming each structural edit landed
- The full-suite pass/fail counts (expect 4147+ passed, 8 baseline failures, zero new)
- Phase 65 floor regression check: 4-test verify panel (all 4 GREEN)
- KAAN-ACTION carry-forward: §RECALL-EAR named + 66-HUMAN-UAT.md scaffolded; engineering-green per `gsd-autonomous fully`
</output>
