---
phase: 92-lesson-runtime-ai-highlight-contract
plan: 01
subsystem: ipc
tags: [ipc, schema, jsonschema, ajv, state-machine, lesson-runtime, foundation]

# Dependency graph
requires: []
provides:
  - "python-statemachine ^3.1.2 runtime dep (the LessonRuntime FSM engine)"
  - "learn_tutor route in vibemix.llm.model_router (Open Q1 resolution)"
  - "11 ipc.learn.* envelope schemas (start_course/start_lesson/complete_lesson/lesson_loaded/highlight/advance/ack/tutor_speak/exemplar_play/exemplar_stop/progress_state)"
  - "11 Python wrapper dataclasses + payload structs (frozen+slots, shared _VALIDATOR)"
  - "Regenerated pre-compiled ajv validator + TS types covering all 13 ipc.learn.* shapes (2 P91 + 11 P92)"
  - "Hardened count-parity heuristic in scripts/check_ipc_schema.py — filters on type-field Literal containing 'ipc.' to skip nested LearnExpectedAction sub-discriminator"
affects: [92-02, 92-03, 92-04, 92-05, 92-06, 92-07, 93, 94, 95, 96]

# Tech tracking
tech-stack:
  added:
    - "python-statemachine ^3.1.2 (MIT pure-Python ~30 KB, no transitives, no native code; PyPI provenance since 2015 — slopcheck OK)"
  patterns:
    - "Decoupled router paths: learn_tutor sits next to live_coach with the same model/tier but as a SEPARATE entry — future model swaps decouple the live co-host and lesson-tutor surfaces"
    - "Custom .to_json() drops None-valued optional payload fields before _VALIDATOR.validate — mirrors SessionMute.to_json pattern in messages.py:864-870; LearnProgressState uses it (the schema allows progress to be absent but not null)"
    - "Sub-domain discriminator dataclasses (e.g. LearnExpectedAction with type: Literal['cc', 'button']) are filtered out of count-parity introspection by an 'ipc.'-substring check on the type annotation"

key-files:
  created:
    - ".planning/phases/92-lesson-runtime-ai-highlight-contract/92-01-SUMMARY.md"
  modified:
    - "pyproject.toml (+1 dep: python-statemachine ^3.1.2)"
    - "uv.lock (regenerated by uv sync)"
    - "src/vibemix/llm/_router_config.py (+learn_tutor route entry)"
    - "tauri/ui/src/ipc/messages.schema.json (+11 oneOf $refs, +11 definitions)"
    - "tauri/ui/src/ipc/messages.ts (regenerated)"
    - "tauri/ui/src/ipc/validator.generated.mjs (regenerated)"
    - "src/vibemix/ui_bus/learn_messages.py (+11 envelope wrappers + payloads + LearnProgressDot + LearnExpectedAction nested types)"
    - "src/vibemix/ui_bus/__init__.py (re-export 24 new names — 11 envelopes + 11 payload structs + 2 nested helpers)"
    - "scripts/check_ipc_schema.py (+11 example entries; envelope-field heuristic tightened to filter on 'ipc.' substring; multi-module introspection retained)"
    - "tests/llm/test_model_router.py (ROUTER_PATHS count 9 → 10; expected set += learn_tutor)"
    - "tests/ui_bus/test_messages_schema.py (+11 examples; counts 66 → 77, 67 → 78; renamed _is_66 → _is_77)"
    - "tests/ui_bus/test_recordings_messages.py (renamed _at_66 → _at_77; count 66 → 77; envelope-field predicate)"
    - "tests/ui_bus/test_mood_change_envelope.py (count 66 → 77; envelope-field predicate)"
    - "tests/ui_bus/test_citation_schema.py (envelope-field predicate)"
    - "tests/ui_bus/test_overlay_schema.py (envelope-field predicate)"

key-decisions:
  - "learn_tutor is a NEW router path, not a reuse of live_coach — RESEARCH §Open Q1 ratified. Same model/tier today (gemini-3.5-flash STANDARD) but decoupled so future swaps don't drag both surfaces."
  - "11 envelopes added in ONE schema-side commit + 11 dataclasses in ONE python-side commit — atomic per task. Codegen output (messages.ts + validator.generated.mjs) commits alongside the schema edit per CLAUDE.md `feedback_schema_edit_needs_codegen_ipc` (the pre-compiled ajv validator silently rejects new fields when stale)."
  - "LearnExpectedAction is a NESTED dataclass with its own type field (cc/button sub-discriminator). It is NOT a top-level envelope — the count-parity script and 3 of the 5 test files needed an envelope-field heuristic upgrade to exclude it. The 5th test (tests/ipc/test_library_schemas.py) already filtered on `'payload' in __dataclass_fields__` which excludes LearnExpectedAction (no payload field) — no edit there."
  - "LearnProgressState ships its own .to_json that drops None-valued payload fields. The schema marks `progress` as optional but only permits type:'object' — never null. Reset / reset_ack envelopes omit `progress` entirely; the snapshot path emits it as a dict. SessionMute.to_json pattern from messages.py:864-870 reused verbatim."
  - "tests/ipc/test_library_schemas.py NOT modified — its existing predicate (`'payload' in obj.__dataclass_fields__`) is strictly stronger than the new envelope-field heuristic and already excludes nested dataclasses. Documenting this as a positive deviation."

patterns-established:
  - "Schema additions: append both oneOf $refs AND definitions in same commit; run npm run codegen:ipc; commit regenerated artefacts alongside (P91 precedent; P92 extends it to a 11-envelope batch)"
  - "Sub-domain discriminator dataclasses (nested type fields with non-ipc Literal values) require a tightened count-parity heuristic"
  - "Optional payload fields with no nullable schema type need a custom .to_json that strips None before validate"
  - "Hand-written introspection predicate replicated across 4 sites (script + 3 tests) deliberately — keep them in lockstep when the heuristic evolves"

requirements-completed: [LESSON-02, LESSON-06]

# Metrics
duration: 13min
completed: 2026-05-28
---

# Phase 92 Plan 01: Lesson Runtime + AI Highlight Contract Foundation Summary

**ONE new MIT pure-Python dep (python-statemachine ^3.1.2), ONE new router path (learn_tutor — Open Q1 resolution), 11 new ipc.learn.* envelope shapes wired end-to-end through schema → regenerated ajv/TS → Python dataclasses → count-parity tests, with shared-file edits committed atomically in 4 task commits.**

## Performance

- **Duration:** 13 min
- **Started:** 2026-05-28T00:40:35Z
- **Completed:** 2026-05-28T00:53:51Z
- **Tasks:** 4
- **Files modified:** 14 (1 dep + 1 router + 3 schema/codegen + 2 python + 1 script + 6 tests)
- **Files created:** 1 (this SUMMARY)
- **Commits:** 4 task commits + 1 metadata commit (this SUMMARY + STATE + ROADMAP + REQUIREMENTS)

## Accomplishments

- `python-statemachine ^3.1.2` declared in `pyproject.toml` and installed into `.venv` via `uv sync`; `from statemachine import State, StateMachine` succeeds — the LessonRuntime FSM engine is now available for Plans 92-03 onward.
- `learn_tutor` route added to `vibemix.llm.model_router._ROUTES` — resolves to `('gemini-3.5-flash', STANDARD)` today, decoupled from `live_coach` so future model swaps don't drag both surfaces (RESEARCH §Open Q1 ratified).
- 11 new `ipc.learn.*` envelopes (start_course / start_lesson / complete_lesson / lesson_loaded / highlight / advance / ack / tutor_speak / exemplar_play / exemplar_stop / progress_state) added to `messages.schema.json` with `additionalProperties: false` at envelope, payload, and nested-object levels.
- 11 Python wrapper dataclasses + their payload structs added to `src/vibemix/ui_bus/learn_messages.py`, mirroring the P91 pattern verbatim (`@dataclass(frozen=True, slots=True)` + `.make()` keyword-only factory + `.to_json()` via shared `_serialize` + `.to_dict()` round-trip). Two helper nested types ship alongside: `LearnProgressDot` (for `LearnLessonLoaded.payload.progress_dots[]`) and `LearnExpectedAction` (for `LearnHighlight.payload.expected_action`).
- Pre-compiled ajv validator (`validator.generated.mjs`) and TS types (`messages.ts`) regenerated via `npm run codegen:ipc`; running the codegen twice produces byte-identical output (idempotent — verified by shasum equality: `ae6161c357c96c9a2cc013b2e1ed09392395063e`).
- `scripts/check_ipc_schema.py` count-parity gate green at **77 / 77** (was 66/66 from P91; +11 envelopes from P92).
- `cd tauri/ui && npx tsc --noEmit` exits 0 — frontend types compile cleanly after the schema regen.
- Invariant #4 (one-socket) stays green: `tests/learn/test_no_new_ws_port.py` PASSED (no new `websockets.serve` introduced).
- Full Python suite for the affected scope: 293 passed, 1 skipped (the unrelated VCR cassette skip for `tests/llm/test_tts_3_1.py` — out of P92 scope).

## Task Commits

Each task was committed atomically by named paths only (concurrent-session discipline — never `git add -A`):

1. **Task 1: Add python-statemachine dep + learn_tutor route** — `2214911f` (feat) — `pyproject.toml` + `uv.lock` + `_router_config.py` + `tests/llm/test_model_router.py` (Rule 1 count bump)
2. **Task 2: Add 11 ipc.learn.* schemas + regenerate ajv validator** — `3020dfba` (feat) — `messages.schema.json` + regenerated `messages.ts` + `validator.generated.mjs`
3. **Task 3: 11 Python dataclasses + ui_bus re-exports** — `56c3a0be` (feat) — `learn_messages.py` + `ui_bus/__init__.py`
4. **Task 4: Restore 77/77 count parity across 5 tests + script** — `fcdda017` (test) — `check_ipc_schema.py` + 5 test files (envelope-field predicate added in 4 sites, count bumped 66→77 in 3 sites, function renamed in 2 sites)

**Plan metadata commit (this SUMMARY + STATE/ROADMAP/REQUIREMENTS):** see final commit below.

## Files Created/Modified

### Created

- `.planning/phases/92-lesson-runtime-ai-highlight-contract/92-01-SUMMARY.md` — this file.

### Modified (dependency + router)

- `pyproject.toml` — appended `"python-statemachine>=3.1.2,<4.0"` to `[project] dependencies` (alphabetised after `python-rtmidi`). MIT pure-Python ~30 KB; no transitives; slopcheck OK per RESEARCH §Package Legitimacy Audit.
- `uv.lock` — regenerated by `uv sync` to pin `python-statemachine==3.1.2`.
- `src/vibemix/llm/_router_config.py` — appended new entry `"learn_tutor": ("gemini-3.5-flash", "STANDARD")` to `_ROUTES`, between the `live_coach_tts_openrouter` block and the `debrief` block. Inline comment cites Open Q1 resolution.
- `tests/llm/test_model_router.py` — Rule 1 bug fix: bumped `len(ROUTER_PATHS) == 9` to `== 10` and added `"learn_tutor"` to the expected-set literal. Stale assertion that had to move when the route table grew.

### Modified (schema + codegen)

- `tauri/ui/src/ipc/messages.schema.json` — +11 `oneOf $ref` entries (between the P91 `LearnMidiPosition` ref and the closing `]`), +11 definitions blocks (each with `additionalProperties: false` at envelope, payload, and every nested-object level). Total: 77 oneOf entries, 78 definitions (the +1 is `LevelPair`, a shared helper).
- `tauri/ui/src/ipc/messages.ts` — regenerated via `npm run codegen:ipc`. SHA-256 `ff893b51ccd37f65ca2a7aca1ae94e80f50a8fcc`.
- `tauri/ui/src/ipc/validator.generated.mjs` — regenerated. SHA-256 `ae6161c357c96c9a2cc013b2e1ed09392395063e`. Running codegen twice is byte-equal.

### Modified (Python wrappers + re-exports)

- `src/vibemix/ui_bus/learn_messages.py` — +11 envelope wrapper classes (with their payload structs), +2 nested helpers (`LearnProgressDot`, `LearnExpectedAction`). One custom `.to_json` on `LearnProgressState` to strip None-valued optional `progress` field before validation (the schema permits absence but not null). All other 10 envelopes reuse the shared `_serialize` helper.
- `src/vibemix/ui_bus/__init__.py` — re-export 24 new names from `learn_messages` (11 envelopes + 11 payloads + 2 nested helpers) under a Phase 92 comment block; extended `__all__` accordingly.

### Modified (count-parity infrastructure)

- `scripts/check_ipc_schema.py` — +11 minimal-valid example tuples; **new `_is_envelope_type_field()` predicate** (filters dataclasses whose `type` annotation contains `'"ipc.'` — excludes nested sub-discriminator types like `LearnExpectedAction`); `_count_wrapper_dataclasses` updated to apply the predicate. Final tally line prints `OK: 77 dataclasses validate against schema; OK: count parity — 77 oneOf entries == 77 wrapper dataclasses`.
- `tests/ui_bus/test_messages_schema.py` — +11 example tuples to `_make_examples()`; counts bumped 66→77 / 67→78; renamed `test_schema_oneof_count_is_66` → `test_schema_oneof_count_is_77`; docstrings updated.
- `tests/ui_bus/test_recordings_messages.py` — renamed `test_count_parity_at_66` → `test_count_parity_at_77`; count bumped 66→77; envelope-field predicate added.
- `tests/ui_bus/test_mood_change_envelope.py` — count bumped 66→77 (docstring + assert); envelope-field predicate added.
- `tests/ui_bus/test_citation_schema.py` — envelope-field predicate added (no hard-coded count to bump).
- `tests/ui_bus/test_overlay_schema.py` — envelope-field predicate added (no hard-coded count to bump).
- `tests/ipc/test_library_schemas.py` — **NOT modified**. Its existing predicate (`"payload" in obj.__dataclass_fields__`) is strictly stronger than the new envelope-field heuristic (the nested `LearnExpectedAction` has no `payload` field). Documented as a positive deviation — the plan listed it in-scope, but the existing filter already excludes the new nested type.

## Decisions Made

- **`learn_tutor` is a NEW router-path entry, not a reuse of `live_coach`** — RESEARCH.md §Open Q1 ratified by the planner. Same model/tier today (Gemini Flash STANDARD) but a SEPARATE dict entry so future swaps decouple the live co-host (party/coach) and the lesson tutor surfaces.
- **Custom `to_json` on `LearnProgressState`** — the schema marks `progress` as optional and only allows `type: object` (no nullable union); reset / reset_ack envelopes don't carry `progress`. Mirrors the `SessionMute.to_json` pattern from `messages.py:864-870` verbatim.
- **`LearnExpectedAction.type: Literal["cc", "button"]` is a sub-domain discriminator inside `LearnHighlight.payload.expected_action`** — NOT a top-level envelope. Required upgrading the count-parity heuristic in 4 sites to filter on an `"ipc.`-substring check.
- **`tests/ipc/test_library_schemas.py` left untouched** — its `"payload" in obj.__dataclass_fields__` filter is strictly stronger than the new heuristic and already excludes `LearnExpectedAction`. The plan declared it in-scope as a precaution, but no edit was needed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Bumped `tests/llm/test_model_router.py::test_router_paths_is_frozen_tuple` count 9 → 10**
- **Found during:** Task 1 (post-route-add verify gate)
- **Issue:** `test_router_paths_is_frozen_tuple` hard-coded `len(ROUTER_PATHS) == 9` and an `expected` set without `learn_tutor`. Adding the new route flipped the assertion red.
- **Fix:** Bumped the count to 10 and added `"learn_tutor"` to the expected-set literal. Stale infrastructure-test pin that had to move when the route table legitimately grew.
- **Files modified:** `tests/llm/test_model_router.py`
- **Verification:** `pytest -q tests/llm/test_model_router.py` 13/13 passed.
- **Committed in:** `2214911f` (Task 1 commit, named-path staged)

**2. [Rule 1 - Bug] `LearnExpectedAction` nested dataclass false-positives the count-parity heuristic**
- **Found during:** Task 4 (first run of `scripts/check_ipc_schema.py` after Task 3 landed all 11 dataclasses)
- **Issue:** `LearnExpectedAction` (the nested object inside `LearnHighlight.payload.expected_action`) has its own `type: Literal["cc", "button"]` field — required for the schema mirror. The count-parity introspection (`"has a type field"`) caught it as a top-level envelope, breaking the 77 oneOf vs 78 wrapper drift gate (`FAIL: schema/dataclass drift — 77 oneOf entries vs 78 wrapper dataclasses`).
- **Fix:** Added a new `_is_envelope_type_field()` predicate to `scripts/check_ipc_schema.py` that checks whether the `type` field's annotation contains `"ipc.` (the envelope-discriminator literal namespace). Replicated the predicate verbatim in 3 of the 5 affected test files (`test_recordings_messages.py`, `test_mood_change_envelope.py`, `test_citation_schema.py`, `test_overlay_schema.py`). `test_library_schemas.py` already filters on `"payload" in __dataclass_fields__` which is strictly stronger — no edit needed.
- **Files modified:** `scripts/check_ipc_schema.py`, 4 of 6 listed test files (the 5th file `tests/ipc/test_library_schemas.py` left intact per analysis; the 6th file `tests/ui_bus/test_messages_schema.py` got the count bump 66→77 / 67→78 + rename `_is_66 → _is_77`).
- **Verification:** `python scripts/check_ipc_schema.py` exits 0 with `OK: count parity — 77 oneOf entries == 77 wrapper dataclasses`. All 6 tests pass.
- **Committed in:** `fcdda017` (Task 4 commit)

**3. [Rule 1 - Bug] `LearnProgressState.to_json` had to drop `progress=None`**
- **Found during:** Task 3 (round-trip smoke after adding the 11 dataclasses)
- **Issue:** The schema marks `LearnProgressState.payload.progress` as optional but `type: "object"` only — no `["object", "null"]` union. `LearnProgressState.make(action="reset")` emitted `{"progress": null}` which the validator rejected.
- **Fix:** Added a per-class `.to_json` on `LearnProgressState` that strips None-valued payload fields before `_validate` + `json.dumps` — mirrors the `SessionMute.to_json` pattern in `messages.py:864-870`. Only `LearnProgressState` needs this; the other 10 envelopes round-trip cleanly with the shared `_serialize`.
- **Files modified:** `src/vibemix/ui_bus/learn_messages.py`
- **Verification:** `LearnProgressState.make(action="reset").to_dict()` now returns `{"action": "reset", "was_recovered": false}` — `progress` field stripped; validator accepts.
- **Committed in:** `56c3a0be` (Task 3 commit)

**4. [Positive deviation — no edit needed] `tests/ipc/test_library_schemas.py` was listed in the plan's `files_modified` but doesn't need a change**
- **Found during:** Task 4 (analysis of all 6 listed count-parity tests)
- **Reason:** The test's existing predicate `("type" in obj.__dataclass_fields__ AND "payload" in obj.__dataclass_fields__)` is strictly stronger than the new envelope-field heuristic. `LearnExpectedAction` (the nested type that broke count parity elsewhere) has fields `[type, control, deck, direction, min_delta]` — no `payload` field — so the existing filter already excludes it. The test was already correct.
- **Action:** Documented in this Summary; no commit-time edit. The plan's `files_modified` list was a precaution; reality showed the test was robust.

**5. [Positive deviation — no AGENTS.md violation] No `git add -A` ever used; concurrent-session discipline upheld**
- **Concurrent context:** 3 Codex sessions touching `live-tuning-or-brain` plus an rc1 product-sweep with many unstaged files at session start (CLAUDE.md, src/vibemix/agent/_streaming_pipe.py, etc.). Every `git add` in this plan staged ONLY named paths from `files_modified` (verified with `git status --short <paths>` and `git diff --cached --name-only` before each commit). No co-located unstaged changes were stolen.

---

**Total deviations:** 5 (3 Rule 1 auto-fixes, 2 positive). All auto-fixes corrected stale infrastructure / fixed mirror-fidelity bugs. No deviation changed plan scope or invariants. All 4 task commits stayed named-path strict.

## Issues Encountered

- **`tests/llm/test_model_router_no_inline_literals.py` does not exist** — the plan's Task 1 verify step referenced this path but the actual model_router test file is `tests/llm/test_model_router.py` (no `_no_inline_literals` suffix). Used the real path. (Adding the `learn_tutor` model literal to `_router_config.py` was a non-issue — that file is allowlisted from the grep gate per the CI script's existing config.)
- **`gsd-sdk` CLI not on PATH for SDK queries beyond init** — `init.execute-phase` worked at start; later queries (`state.advance-plan`, `roadmap.update-plan-progress`) will run via SDK if available. Falls back to direct file edits otherwise (see Final Commit below).

## Self-Check: PASSED

- `pyproject.toml` declares `"python-statemachine>=3.1.2,<4.0"`: ✓ (verified by `grep -q "python-statemachine" pyproject.toml`)
- `uv.lock` regenerated with `python-statemachine==3.1.2`: ✓ (verified by `uv sync` output)
- `from statemachine import State, StateMachine` succeeds in `.venv`: ✓
- `_router_config._ROUTES` contains `"learn_tutor": ("gemini-3.5-flash", "STANDARD")`: ✓
- `vibemix.llm.model_router.resolve("learn_tutor")` returns `('gemini-3.5-flash', <ServiceTier.STANDARD: 'standard'>)`: ✓
- `tests/llm/test_model_router.py` all 13 tests passing (ROUTER_PATHS count 10 + expected set match): ✓
- `messages.schema.json` has 11 new `oneOf $ref` entries (Learn{StartCourse,StartLesson,CompleteLesson,LessonLoaded,Highlight,Advance,Ack,TutorSpeak,ExemplarPlay,ExemplarStop,ProgressState}): ✓
- Every new envelope + payload declares `additionalProperties: false`: ✓
- `npm run codegen:ipc` regenerates `messages.ts` + `validator.generated.mjs` idempotently (byte-equal across 2 runs): ✓ — SHA-256 `ae6161c357c96c9a2cc013b2e1ed09392395063e`
- `cd tauri/ui && npx tsc --noEmit` exits 0: ✓
- `src/vibemix/ui_bus/learn_messages.py` exports all 11 new wrapper classes + payloads + 2 nested helpers: ✓
- `src/vibemix/ui_bus/__init__.py` re-exports all 24 new names: ✓
- Round-trip validation succeeds for all 11 envelopes through `_VALIDATOR` (Draft-07): ✓
- Negative paths reject correctly: `cue_color="purple"` → `ValidationError`: ✓; `citations=["[bogus:not-allowed]"]` → `ValidationError`: ✓
- `scripts/check_ipc_schema.py` exits 0 and reports `OK: 77 dataclasses validate against schema; OK: count parity — 77 oneOf entries == 77 wrapper dataclasses`: ✓
- All 6 named count-parity tests pass: ✓ (152 total tests in the 6 files, all green)
- `tests/learn/test_no_new_ws_port.py` (Invariant #4) stays green: ✓
- Four task commits exist + verified:
  - `2214911f` feat(92-01): add python-statemachine dep + learn_tutor router path ✓
  - `3020dfba` feat(92-01): add 11 ipc.learn.* envelope schemas + regenerate ajv validator ✓
  - `56c3a0be` feat(92-01): land 11 ipc.learn.* Python dataclasses + ui_bus re-exports ✓
  - `fcdda017` test(92-01): restore 77/77 count parity gate across 5 test files + script ✓
- No git deletions in any task commit: ✓ (verified via `git diff --diff-filter=D --name-only HEAD~1 HEAD` empty after each commit)
- Full Python suite for the affected scope (`tests/ui_bus/ tests/ipc/ tests/llm/ tests/learn/`): 293 passed, 1 skipped (unrelated VCR cassette for tts_3_1) ✓

## Threat Flags

No new security-relevant surface flagged. All 11 envelopes ride the existing `127.0.0.1:8765` loopback ws bus (one-socket invariant #4 preserved) with `additionalProperties: false` on every payload, `minLength: 1` on identifier strings, integer range bounds on MIDI values, and an enum-or-regex constraint on every string field. T-92-01-01..T-92-01-SC mitigations from the plan's `<threat_model>` are all implemented:
- **T-92-01-01 (Tampering, schema concurrent edit):** Task 2 landed BOTH the 11 new oneOf entries + 11 new definitions + regenerated codegen output in ONE atomic commit (`3020dfba`). Concurrent Codex sessions never raced the schema edit (only `tauri/ui/src/ipc/*` files in that commit).
- **T-92-01-02 (Tampering, unvalidated ws frame after schema add):** `additionalProperties: false` at every envelope + payload + nested object level on every new envelope. Both ajv (TS) and jsonschema (Python) enforce.
- **T-92-01-03 (Information Disclosure, model identifier leak):** `_router_config._ROUTES` is the AUTHORIZED home for model literals; the `learn_tutor` route's model id is allowlisted per the existing grep gate. No new external attack surface.
- **T-92-01-04 (DoS, progress write spam):** Deferred to Plan 92-04 (atomic `os.replace` write path). Out of scope here.
- **T-92-01-05 (Tampering, stale ajv validator):** `npm run codegen:ipc` ran as a discrete step in Task 2; idempotence verified in same task via twice-run shasum equality.
- **T-92-01-SC (Tampering, package supply chain):** ONE new package added: `python-statemachine 3.1.2` (MIT, pure-Python, ~30 KB, PyPI provenance since 2015 — RESEARCH §Package Legitimacy Audit slopcheck OK). No blocking human checkpoint required.

## Next Phase Readiness

- **Plan 92-02 (test scaffolding wave)** unblocked — the 11 new envelopes are wire-locked + importable from `vibemix.ui_bus`. Plan 02 can write the dedicated round-trip test `tests/ipc/test_learn_envelope_parity_p92.py` referenced in 92-RESEARCH.md §Code Example 3.
- **Plan 92-03 (LessonRuntime FSM)** unblocked — `python-statemachine 3.1.2` is installed; `learn_tutor` route resolves; `LearnState` will sit alongside, sole-writer-enforced by `tests/learn/test_runtime_invariants.py` (lands in P92-03).
- **Plan 92-04 (progress persistence)** unblocked — `LearnProgressState` envelope is wire-locked with the tri-mode action enum (`snapshot` / `reset` / `reset_ack`) + schema-versioned `progress` payload + `was_recovered` corruption-flag.
- **Plan 92-05 (Learn webview wiring of the 11 envelopes)** unblocked — `validator.generated.mjs` (pre-compiled ajv) + `messages.ts` are regenerated and exported from `tauri/ui/src/ipc/`.
- **Plan 92-06 / 92-07 (tutor lens + hello-world end-to-end demo)** unblocked.
- **Concurrent sessions** continue safely — the 14 files this plan touched are now stable; the 3 Codex sessions on `live-tuning-or-brain` saw NO file collisions during execution (every commit was named-path staged and verified by `git diff --cached --name-only`).
- **No KAAN-ACTION items added** by this plan.

---
*Phase: 92-lesson-runtime-ai-highlight-contract*
*Plan: 01*
*Completed: 2026-05-28*
