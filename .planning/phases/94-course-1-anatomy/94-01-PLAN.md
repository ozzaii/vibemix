---
phase: 94-course-1-anatomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/learn/curriculum.py
  - src/vibemix/learn/transcripts/course_1_anatomy/01_welcome.json
  - src/vibemix/learn/transcripts/course_1_anatomy/02_meet_your_controller.json
  - src/vibemix/learn/transcripts/course_1_anatomy/03_channel_strip.json
  - src/vibemix/learn/transcripts/course_1_anatomy/04_crossfader.json
  - src/vibemix/learn/transcripts/course_1_anatomy/05_pitch_fader.json
  - src/vibemix/learn/transcripts/course_1_anatomy/06_transport_buttons.json
  - src/vibemix/learn/transcripts/course_1_anatomy/07_jog_wheel.json
  - src/vibemix/learn/transcripts/course_1_anatomy/08_headphone_cueing.json
  - src/vibemix/learn/transcripts/course_1_anatomy/09_master_booth_headphones.json
  - src/vibemix/learn/transcripts/course_1_anatomy/10_anatomy_of_a_song.json
  - src/vibemix/learn/transcripts/course_1_anatomy/11_counting_bars.json
  - src/vibemix/learn/transcripts/course_1_anatomy/12_spot_breakdown_by_ear.json
  - src/vibemix/learn/transcripts/course_1_anatomy/13_spot_breakdown_by_eye.json
  - src/vibemix/learn/transcripts/course_1_anatomy/14_eq_as_tutor.json
  - src/vibemix/learn/transcripts/course_1_anatomy/15_load_two_tracks.json
  - src/vibemix/learn/transcripts/course_1_anatomy/16_course_1_recital.json
autonomous: true
requirements:
  - TONE-01
  - CURR-1.01
  - CURR-1.02
  - CURR-1.03
  - CURR-1.04
  - CURR-1.05
  - CURR-1.06
  - CURR-1.07
  - CURR-1.08
  - CURR-1.09
  - CURR-1.10
  - CURR-1.11
  - CURR-1.12
  - CURR-1.13
  - CURR-1.14
  - CURR-1.15
  - CURR-1.16
tags: [course-1-anatomy, lesson-fixtures, hand-authored, tone-lock, curriculum-extension, eq-as-tutor]
must_haves:
  truths:
    - "16 hand-authored Course 1 lesson scripts exist under src/vibemix/learn/transcripts/course_1_anatomy/"
    - "L1.01 fixture carries the iconic 4-line opening dialog VERBATIM (byte-equality)"
    - "Every lesson script keys to a CURRICULUM[L1.NN] entry with course_id=\"course_1_anatomy\""
    - "Every lesson's system_instruction_addendum is ≤200 chars (composition-time cap from prompts.py:185 still holds)"
    - "Every lesson's tutor_speak[] is hand-authored prose (TONE-02 fixture gate stays green)"
    - "Every lesson's expected_action describes a real MIDI action the LessonRuntime FSM can gate on"
    - "L1.14 (EQ-as-Tutor) declares three band-cycles in its script for the runtime to drive ExemplarFinder.find()"
    - "L1.16 (Course 1 Recital) declares the 5-prompt subset pool for Plan 94-03's RecitalRuntime"
    - "COURSE_FRAMES contains a 'course_1_anatomy' entry describing the course context for the tutor system instruction"
  artifacts:
    - path: src/vibemix/learn/transcripts/course_1_anatomy/01_welcome.json
      provides: "L1.01 — iconic opening dialog verbatim-locked (TONE-01 binding)"
      contains: "Oh bestie, don't worry. You know why? Because I'm the beginner module of vibemix. Let's go."
    - path: src/vibemix/learn/transcripts/course_1_anatomy/14_eq_as_tutor.json
      provides: "L1.14 marquee EQ-as-Tutor lesson — 3-band cycle (low/mid/high) for the runtime to drive ExemplarFinder.find()"
      contains: "exemplar_cycle"
    - path: src/vibemix/learn/transcripts/course_1_anatomy/16_course_1_recital.json
      provides: "L1.16 recital — random 5-prompt mixed gate definition; replayable; honest grading copy"
      contains: "recital_pool"
    - path: src/vibemix/learn/curriculum.py
      provides: "Extended CURRICULUM with 16 L1.NN entries + extended COURSE_FRAMES with 'course_1_anatomy'"
      contains: "course_1_anatomy"
  key_links:
    - from: src/vibemix/learn/transcripts/course_1_anatomy/*.json
      to: src/vibemix/learn/curriculum.py
      via: LessonMeta.transcript_path
      pattern: "course_1_anatomy/"
    - from: src/vibemix/learn/curriculum.py
      to: src/vibemix/learn/prompts.py
      via: "CURRICULUM[lesson_id].system_instruction_addendum read by build_tutor_system_instruction"
      pattern: "CURRICULUM\\[lesson_id\\]"
    - from: src/vibemix/learn/transcripts/course_1_anatomy/14_eq_as_tutor.json
      to: src/vibemix/learn/exemplar.py
      via: "exemplar_cycle bands consumed by Plan 94-03's runtime hook calling ExemplarFinder.find(band)"
      pattern: "exemplar_cycle"
---

<objective>
Land the 16 hand-authored JSON lesson scripts for Course 1 Anatomy + extend
curriculum.py with COURSE_FRAMES["course_1_anatomy"] + 16 CURRICULUM entries
(L1.01..L1.16). The iconic 4-line opening dialog ships verbatim in
01_welcome.json so Plan 94-02's byte-equality gate can pin it. Every script
matches the P92 hello_world fixture shape so LessonRuntime can load them
without code changes.

Purpose: Course 1 is the BEGINNER module's first surface — the moment a
stranger sees "Oh bestie, don't worry…" verbatim and walks through 15 more
controller-anatomy lessons before unlocking Course 2. The scripts are the
SOURCE OF TRUTH for tutor_speak text (TONE-02 fixture binding from P92
stays green) — no live LLM writes ever generate this content.

Output: 16 JSON fixtures + curriculum.py extension. No runtime/UI code
changes (Plan 94-03 wires L1.14 + L1.16 backends).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/94-course-1-anatomy/94-CONTEXT.md
@src/vibemix/learn/curriculum.py
@src/vibemix/learn/prompts.py
@src/vibemix/learn/transcripts/hello_world/01_press_play.json

<interfaces>
<!-- Key contracts the executor uses. Lifted from P92 + P93 code; no codebase exploration needed. -->

From `src/vibemix/learn/curriculum.py` (P92-03 shipped):

```python
COURSE_FRAMES: dict[str, str] = {
    "course_0": "Course 0 is the hello-world tutorial — a one-lesson demo proving the runtime end-to-end.",
    # P94 adds course_1_anatomy (anatomy: decks, mixer, transport, EQ).
}

@dataclass(frozen=True)
class LessonMeta:
    title: str                          # lowercase, period-free (UI-SPEC)
    course_id: str                      # key into COURSE_FRAMES
    system_instruction_addendum: str    # ≤200 chars (composition-time cap, prompts.py:185)
    transcript_path: str                # rel to src/vibemix/learn/transcripts/
    # .script @property json-decodes the file lazily

CURRICULUM: dict[str, LessonMeta] = {
    "L0.00-press-play": LessonMeta(...),  # P92 hello-world
    # P94 adds L1.01..L1.16
}
```

From `src/vibemix/learn/prompts.py:171-200` (build_tutor_system_instruction):

```python
# Order of frames in the final system instruction:
#   1. base teacher persona (from prompts/matrix.py, runtime-only blocks OFF)
#   2. COURSE_FRAMES[course_id]
#   3. _controller_frame(controller_id)            # naming the user's hardware
#   4. CURRICULUM[lesson_id].system_instruction_addendum    # ≤200 chars
#   5. _FORBIDDEN_TUTOR_MOVES_LOCK                  # LAST for strongest recency
```

From `src/vibemix/learn/runtime.py:209-300` (action_matches gate — what
`expected_action` must shape itself to):

```python
# CC delta branch:
#   expected = {"type": "cc", "control": "<name>", "min_delta": <int default 38>}
#   midi must carry: {"type": "cc", "control": "<name>", "value": int, "prev_value": int}
#   gate: abs(cur - prev) >= min_delta

# Button branch:
#   expected = {"type": "button", "control": "<name>", "direction": "down" | "up",
#               "deck": "A" | "B" | None}     # deck enforced when both sides declared
#   midi must carry: {"type": "button", "control": "<name>", "direction": ..., "deck": ...}
```

From `src/vibemix/learn/transcripts/hello_world/01_press_play.json`
(canonical P92 shape executors mirror EXACTLY for every lesson script):

```json
{
  "lesson_id": "L0.00-press-play",
  "title": "press play",
  "system_instruction_addendum": "HELLO WORLD ADDENDUM: ... ≤200 chars ...",
  "tutor_speak": [
    { "beat": 0, "text": "...", "tts_marker": "L000.beat0", "citations": [] }
  ],
  "expected_action": { "type": "button", "control": "play", "deck": "A", "direction": "down" },
  "hints": [
    { "strike": 1, "text": "...", "tts_marker": "L000.hint1" },
    { "strike": 2, "text": "...", "tts_marker": "L000.hint2" },
    { "strike": 3, "text": "...", "tts_marker": "L000.hint3" }
  ]
}
```

From the iconic dialog (STATE.md verbatim + CONTEXT.md §decisions —
BYTE-EQUALITY LOCK; reproduce these 4 lines EXACTLY in 01_welcome.json):

```
user:    Hello vibemix, what are you?
vibemix: I'm the best DJ app in the world.
user:    If you are the best, then who the fuck am I?
vibemix: Oh bestie, don't worry. You know why? Because I'm the beginner module of vibemix. Let's go.
```

Tone discipline carried from P92 _FORBIDDEN_TUTOR_MOVES_LOCK (every
lesson script honors these — verified by Plan 94-02's blocklist):

- NO compliments ("nice", "great", "you crushed it", any praise-tic).
- NO summarizing what happened ("you just did", "you've now learned").
- NO previewing what's next ("now let's", "next we'll").
- NO upbeat hook closers ("exciting, right?", "this is where it gets fun").
- Lowercase, period-terminated. ONE exclamation in the whole 36-lesson
  curriculum: "Let's go." in L1.01 (CONTEXT.md §decisions §iconic dialog).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand-author the 16 Course 1 lesson JSON fixtures</name>
  <files>
    src/vibemix/learn/transcripts/course_1_anatomy/01_welcome.json,
    src/vibemix/learn/transcripts/course_1_anatomy/02_meet_your_controller.json,
    src/vibemix/learn/transcripts/course_1_anatomy/03_channel_strip.json,
    src/vibemix/learn/transcripts/course_1_anatomy/04_crossfader.json,
    src/vibemix/learn/transcripts/course_1_anatomy/05_pitch_fader.json,
    src/vibemix/learn/transcripts/course_1_anatomy/06_transport_buttons.json,
    src/vibemix/learn/transcripts/course_1_anatomy/07_jog_wheel.json,
    src/vibemix/learn/transcripts/course_1_anatomy/08_headphone_cueing.json,
    src/vibemix/learn/transcripts/course_1_anatomy/09_master_booth_headphones.json,
    src/vibemix/learn/transcripts/course_1_anatomy/10_anatomy_of_a_song.json,
    src/vibemix/learn/transcripts/course_1_anatomy/11_counting_bars.json,
    src/vibemix/learn/transcripts/course_1_anatomy/12_spot_breakdown_by_ear.json,
    src/vibemix/learn/transcripts/course_1_anatomy/13_spot_breakdown_by_eye.json,
    src/vibemix/learn/transcripts/course_1_anatomy/14_eq_as_tutor.json,
    src/vibemix/learn/transcripts/course_1_anatomy/15_load_two_tracks.json,
    src/vibemix/learn/transcripts/course_1_anatomy/16_course_1_recital.json
  </files>
  <read_first>
    src/vibemix/learn/transcripts/hello_world/01_press_play.json (canonical lesson-script shape),
    src/vibemix/learn/prompts.py (TONE-04 lock to honor),
    .planning/phases/94-course-1-anatomy/94-CONTEXT.md (locked decisions),
    .planning/REQUIREMENTS.md (CURR-1.01..1.16 wording)
  </read_first>
  <action>
    Hand-author the 16 JSON fixtures under `src/vibemix/learn/transcripts/course_1_anatomy/`. Each file mirrors the
    hello_world precedent (`tutor_speak[]` + `expected_action` + `hints[]` + `system_instruction_addendum` etc.). Tone
    discipline binding: every text field is lowercase, period-terminated, and honors the 4-forbidden-moves lock (no
    compliments / no summaries / no previews / no upbeat closers per D-CONTEXT §decisions §tone). The lone exclamation
    permitted across the entire curriculum is "Let's go." in L1.01 (the iconic dialog closer).

    **L1.01 — Opening Dialog (TONE-01 + CURR-1.01) — VERBATIM BYTE-LOCK.** This is the iconic opening. Per CONTEXT.md
    §decisions §iconic-dialog and STATE.md "Pre-Milestone Direction (carried forward)", the 4-line dialog ships
    BYTE-EQUAL. Plan 94-02 lands the byte-equality test that pins this. The fixture's `tutor_speak[]` carries the 4
    lines IN ORDER (lines 1 + 3 are user-prompted reading; lines 2 + 4 are vibemix-spoken — model them as 4
    sequential beats so the runtime narrates the dialog as a single linear cinematic):

    ```
    beat 0 — speaker="user",    text="Hello vibemix, what are you?"
    beat 1 — speaker="vibemix", text="I'm the best DJ app in the world."
    beat 2 — speaker="user",    text="If you are the best, then who the fuck am I?"
    beat 3 — speaker="vibemix", text="Oh bestie, don't worry. You know why? Because I'm the beginner module of vibemix. Let's go."
    ```

    Reproduce the four `text` values EXACTLY — including the apostrophes, the lowercase "i'm" → "I'm" capitalization
    as shown, the question mark + period punctuation, the comma after "bestie", the period after "worry", and the
    final period after "Let's go." The byte-equality gate fails red on a single drift (smart-quote → straight quote,
    extra space, lost capital, etc.). Use the verbatim block in `<interfaces>` above as the literal source. Add a
    new `speaker` field on tutor_speak entries to distinguish user vs vibemix lines (the schema does not break — Plan
    94-02's byte-equality test reads `tutor_speak[i].text` by index; Plan 94-03's runtime hook reads `speaker` only
    on this one lesson). The `expected_action` is a passive `continue` button (advance on user click, no MIDI gate —
    L1.01 is dialog read-through). Use `expected_action = { "type": "button", "control": "lesson_continue",
    "direction": "down" }` (a synthetic control name; LessonRuntime's button branch in runtime.py:289 already accepts
    any control name — the controller-side click maps to this when no real MIDI control is bound for "dialog
    continue"). `system_instruction_addendum` = "OPENING DIALOG ADDENDUM: Read the four scripted lines verbatim. Do
    not improvise, paraphrase, or add an extra line." (97 chars, under the 200-char cap). `hints[]` = 3 minimal "tap
    to continue" reminders — keep them obvious because this is the first lesson the user sees.

    **L1.02 — Meet Your Controller (CURR-1.02).** The rendered SVG fires in on lesson load (P91 ipc.learn.highlight
    glow already wired). Tutor introduces the controller by name (the `{display_name}` token from `_controller_frame`
    in prompts.py:111 lets the LLM substitute "DDJ-FLX4" at runtime — but tutor_speak.text is hand-authored, so
    write a generic-but-grounded line that names "your controller" and pointers to "the three big sections" — left
    deck / mixer / right deck — without depending on a specific model). `expected_action` = button press on any
    transport-row control (use `expected_action = { "type": "button", "control": "play", "deck": "A", "direction":
    "down" }` since play:A is universal across all 10 controllers). 2 tutor beats: beat 0 names the controller
    sections; beat 1 says "press the play button on the left deck when you've spotted it." `system_instruction_addendum`
    ≤200: "MEET CONTROLLER ADDENDUM: Name the rendered controller's three sections — left deck / mixer / right deck.
    Do not narrate features beyond the three sections."

    **L1.03 — Channel Strip (CURR-1.03).** The deck-A channel-fader sweep. `expected_action` = cc-drop on the
    channel_fader_a control: `{ "type": "cc", "control": "channel_fader_a", "min_delta": 95 }` (95 = ~75% of 127,
    forces a full or near-full sweep, not a tiny twitch). Tutor beats 2-3 lines: beat 0 says "the channel fader
    controls how loud deck A is in the mix — push it all the way up, then all the way down." Hints walk the user
    toward the leftmost vertical fader on the controller body. `system_instruction_addendum` ≤200: "CHANNEL STRIP
    ADDENDUM: Name the channel fader as the level for one deck. Do not conflate with the master fader. Stay quiet
    while the user is sweeping."

    **L1.04 — Crossfader (CURR-1.04).** User moves crossfader center → left → right. `expected_action` = cc-drop on
    the crossfader: `{ "type": "cc", "control": "crossfader", "min_delta": 100 }` (full-throw). Tutor beats 2-3 lines
    explaining left = deck A only, right = deck B only, center = both. Hints point to the horizontal slider between
    the channel faders. `system_instruction_addendum` ≤200: "CROSSFADER ADDENDUM: Crossfader is the A/B blend slider.
    State only the mechanical behavior; the transition mechanics come in Course 2."

    **L1.05 — Pitch Fader (CURR-1.05).** User shifts pitch ±4% (call out the percentage display on the controller).
    `expected_action` = cc-drop on tempo_fader_a: `{ "type": "cc", "control": "tempo_fader_a", "min_delta": 50 }`
    (smaller delta — pitch faders have ±8% / ±16% / ±50% ranges that mean ±4% is roughly a quarter throw).
    `system_instruction_addendum` ≤200: "PITCH FADER ADDENDUM: Pitch fader is a percentage adjustment to the BPM.
    Reference the tempo display on the user's deck. Sync is a separate lesson."

    **L1.06 — Transport Buttons (CURR-1.06).** Play / cue / sync — but advance on FIRST press of any of the three
    (the user can experiment with each). `expected_action` = `{ "type": "button", "control": "cue", "deck": "A",
    "direction": "down" }` — cue is the one users underuse and the lesson centers it. `system_instruction_addendum`
    ≤200: "TRANSPORT BUTTONS ADDENDUM: Play / cue / sync are deck transport. Cue plays-from-cue-while-held. Do not
    pre-empt the sync explanation; Course 2 owns sync."

    **L1.07 — Jog Wheel (CURR-1.07).** Nudge mode only (scratching deferred to v9.x per CURR-1.07 + ROADMAP defer
    list). `expected_action` = cc-drop on jog_a: `{ "type": "cc", "control": "jog_a", "min_delta": 10 }` (jog wheels
    emit rapid CC deltas; 10 is one obvious nudge). Tutor narrates "nudge forward to push the track 1ms ahead, nudge
    back to pull it back." `system_instruction_addendum` ≤200: "JOG WHEEL ADDENDUM: Jog wheel = nudge mode only.
    Scratching is a separate art form, deferred to a later course. Do not narrate scratching."

    **L1.08 — Headphone Cueing (CURR-1.08).** User toggles the headphone-cue button on deck B (pre-listen while
    deck A plays). `expected_action` = `{ "type": "button", "control": "headphone_cue_b", "direction": "down" }`.
    `system_instruction_addendum` ≤200: "HEADPHONE CUEING ADDENDUM: Cue button routes the deck to headphones only.
    Master deck and headphone cue can differ — that is the point."

    **L1.09 — Master/Booth/Headphone Volumes + red-zone hygiene (CURR-1.09).** "Don't touch the master fader" hygiene
    + red-zone clipping awareness. `expected_action` = cc-drop on the master_volume knob: `{ "type": "cc", "control":
    "master_volume", "min_delta": 5 }` (small delta — we just want them to TOUCH it once so they know which knob it
    is; the lesson script then warns them to put it back to noon). `system_instruction_addendum` ≤200: "MASTER VOL
    ADDENDUM: Master sets venue volume — booth = monitor, headphone = cue. Red-zone clipping at >-6 dBFS. Do not
    move master mid-set."

    **L1.10 — Anatomy of a Song (CURR-1.10).** Intro / build / drop / breakdown / outro narrated over a playing
    track. This lesson does NOT gate on MIDI (it's a passive listening lesson — like L1.01). Use the same continue-
    button pattern: `expected_action = { "type": "button", "control": "lesson_continue", "direction": "down" }`.
    Tutor narration unrolls across 5 beats — one per section. `system_instruction_addendum` ≤200: "SONG ANATOMY
    ADDENDUM: Walk through intro / build / drop / breakdown / outro on the playing track. Reference the audio, not
    music-theory jargon."

    **L1.11 — Counting Bars (CURR-1.11).** AI counts 1-2-3-4 over playback; user counts along. Gate: tap any
    transport button on beat (use `expected_action = { "type": "button", "control": "play", "deck": "A",
    "direction": "down" }` as a generic "you tapped along" signal — runtime doesn't enforce timing in v9.0).
    `system_instruction_addendum` ≤200: "COUNTING BARS ADDENDUM: Count 1-2-3-4 across 4 bars (16 beats). Tap on the
    1 each bar. Do not narrate microtiming."

    **L1.12 — Spot Breakdown By Ear (CURR-1.12).** User presses cue button when they hear the breakdown.
    `expected_action = { "type": "button", "control": "cue", "deck": "A", "direction": "down" }`. Tutor narrates
    "listen for the energy drop — the kick will stop, the chord will sustain, something will get quieter."
    `system_instruction_addendum` ≤200: "SPOT BREAKDOWN EAR ADDENDUM: Breakdown = energy drop, often kick out. User
    presses cue when they hear it. State the audible cue, not the bar number."

    **L1.13 — Spot Breakdown By Eye (CURR-1.13).** User identifies breakdown on the waveform. The Learn surface
    already paints a waveform on the deck (P91 controller render). `expected_action = { "type": "button", "control":
    "cue", "deck": "A", "direction": "down" }`. Tutor narrates "the breakdown shows up on the waveform as a thin
    section — the bass is gone, only the mids sustain." `system_instruction_addendum` ≤200: "SPOT BREAKDOWN EYE
    ADDENDUM: Breakdown on the waveform = thinner section, less low-end. Point to the waveform, not the audio."

    **L1.14 — EQ-as-Tutor Demo (CURR-1.14) — MARQUEE.** This is the moat lesson. Three-band cycle (low → mid → high).
    The runtime hook in Plan 94-03 reads a NEW top-level field `exemplar_cycle` from this fixture and drives
    ExemplarFinder.find(band) for each entry, plays via ExemplarPlayer, then waits for the user's EQ-knob sweep
    before advancing to the next band. Define `exemplar_cycle` as an ordered array of three entries:

    ```
    "exemplar_cycle": [
      { "band": "low",
        "expected_action": { "type": "cc", "control": "eq_low_a", "min_delta": 80 },
        "tutor_speak": "i picked a low-heavy track from your library. turn deck A's low EQ knob to hear the bass swell and fade."
      },
      { "band": "mid",
        "expected_action": { "type": "cc", "control": "eq_mid_a", "min_delta": 80 },
        "tutor_speak": "now a mid-heavy track. turn deck A's mid EQ knob to hear the vocals and snare move."
      },
      { "band": "high",
        "expected_action": { "type": "cc", "control": "eq_hi_a", "min_delta": 80 },
        "tutor_speak": "and a high-heavy track. turn deck A's high EQ knob to hear the hats and shimmer change."
      }
    ]
    ```

    Outer-shell fields: `expected_action` at the top level is the SAME as `exemplar_cycle[0].expected_action` (the
    runtime's existing action_matches gate from runtime.py:209 reads top-level expected_action; Plan 94-03 cycles
    forward through exemplar_cycle entries one at a time so the top-level value always matches the active band).
    `tutor_speak[]` outer array can have one introductory beat ("you've met every knob on your controller. now we
    use them on real music. for each EQ band, i'll play a track from your library where that band lives loudest.
    you turn the knob; you hear it swell or fade.") followed by 3 beats matching the cycle (just point at the
    cycle entries by index in the runtime). `system_instruction_addendum` ≤200: "EQ AS TUTOR ADDENDUM: Cycle three
    EQ bands — low / mid / high. Cite the exemplar track once per band. Stay quiet while the user is sweeping the
    knob."

    Citations: each `exemplar_cycle[i].tutor_speak` carries a `citations` array — leave it EMPTY in the fixture (the
    runtime emits the `[exemplar:<track_id>]` cite at narration time via Plan 94-03's wiring; pre-baking the
    citation here would lie about the track since the actual pick comes from the user's library at runtime).

    **L1.15 — Load Two Tracks (CURR-1.15).** User loads track to deck A and track to deck B. `expected_action` =
    button press on load_b: `{ "type": "button", "control": "load_b", "direction": "down" }` (the load on deck A is
    a hint-strike target; loading deck B is the advance gate — confirms both decks are loaded). Tutor narrates the
    browse-and-load flow on the controller. `system_instruction_addendum` ≤200: "LOAD TWO TRACKS ADDENDUM: User
    loads a track to deck A, then a track to deck B. Reference the load button on the controller, not the laptop
    keyboard."

    **L1.16 — Course 1 Recital (CURR-1.16) — random 5-prompt mixed gate.** This fixture declares the recital pool
    that Plan 94-03's RecitalRuntime reads. Use a NEW top-level field `recital_pool`:

    ```
    "recital_pool": [
      { "from_lesson": "L1.03",
        "prompt": "move deck A's channel fader all the way up.",
        "expected_action": { "type": "cc", "control": "channel_fader_a", "min_delta": 95 }
      },
      { "from_lesson": "L1.04",
        "prompt": "slide the crossfader fully to the right.",
        "expected_action": { "type": "cc", "control": "crossfader", "min_delta": 100 }
      },
      { "from_lesson": "L1.05",
        "prompt": "nudge deck A's pitch fader to +4%.",
        "expected_action": { "type": "cc", "control": "tempo_fader_a", "min_delta": 50 }
      },
      { "from_lesson": "L1.06",
        "prompt": "press cue on deck A.",
        "expected_action": { "type": "button", "control": "cue", "deck": "A", "direction": "down" }
      },
      { "from_lesson": "L1.07",
        "prompt": "nudge deck A's jog wheel forward.",
        "expected_action": { "type": "cc", "control": "jog_a", "min_delta": 10 }
      },
      { "from_lesson": "L1.08",
        "prompt": "tap the headphone cue button on deck B.",
        "expected_action": { "type": "button", "control": "headphone_cue_b", "direction": "down" }
      },
      { "from_lesson": "L1.09",
        "prompt": "touch the master volume knob.",
        "expected_action": { "type": "cc", "control": "master_volume", "min_delta": 5 }
      },
      { "from_lesson": "L1.12",
        "prompt": "press cue when you hear the breakdown.",
        "expected_action": { "type": "button", "control": "cue", "deck": "A", "direction": "down" }
      },
      { "from_lesson": "L1.13",
        "prompt": "press cue when you spot the breakdown on the waveform.",
        "expected_action": { "type": "button", "control": "cue", "deck": "A", "direction": "down" }
      }
    ]
    ```

    The pool draws from L1.03..L1.13 controls (per CONTEXT.md §decisions §recital + REQ CURR-1.16). RecitalRuntime
    in Plan 94-03 samples 5 entries deterministically (seed = day of replay so the user re-rolls a different subset
    on repeat). Honest-grading copy lives in tutor_speak[] outer beats: "5 prompts. perform each. if you nail all 5,
    course 2 unlocks. if not, the recital replays — no shame, no penalty." Closing copy ships TWO variants under a
    `recital_outcomes` field for Plan 94-03 to select between based on score:

    ```
    "recital_outcomes": {
      "pass":  "5 of 5. course 2 unlocked.",
      "fail":  "{score} of 5. replay when you're ready."
    }
    ```

    `system_instruction_addendum` ≤200: "COURSE 1 RECITAL ADDENDUM: Five mixed prompts from prior lessons. State the
    score honestly. Do not soften a fail; do not embellish a pass."

    Top-level `expected_action` on this fixture is a stub — RecitalRuntime takes over advancement once recital_pool
    sampling begins, but the fixture still needs a parseable shape for LessonRuntime.load. Use the lesson_continue
    button pattern: `{ "type": "button", "control": "lesson_continue", "direction": "down" }`.

    **Cross-cutting tone audit (every fixture):** lowercase, period-terminated. ONE exclamation in the whole 16
    files: "Let's go." in 01_welcome.json beat 3. No "great", "nice", "awesome", "perfect", "you crushed it",
    "amazing", "fantastic", "incredible", "way to go", "well done" — every one of these is on the Plan 94-02
    blocklist that will run against these files. No "now let's", "next we'll", "coming up", "later you'll", "in
    the next lesson" — preview-tics are blocklisted. No "exciting, right?", "this is where it gets fun", "you're
    going to love this" — upbeat hook closers are blocklisted.

    **All fixtures: hints[]** — exactly 3 hint entries per lesson at strikes 1 / 2 / 3 with `tts_marker` shaped
    `L1NN.hintM` (e.g. `L103.hint2`). Hints escalate from gentle pointer to obvious "press it now" instruction.
    Keep hints lowercase, period-terminated, same tone discipline.

    **All fixtures: tts_marker** — `L1NN.beatM` (e.g. `L101.beat2`). Use 0-padded lesson numbers so they sort
    deterministically.

    Constraint: keep each fixture under ~80 lines of JSON for readability — the goal is "hand-authored, ear-pass-able,
    Kaan can edit a typo without a 200-line context window."
  </action>
  <verify>
    <automated>
      cd /Users/ozai/projects/dj-set-ai && PYTHONPATH=src python3 -c "
import json
from pathlib import Path
base = Path('src/vibemix/learn/transcripts/course_1_anatomy')
expected = [
    '01_welcome.json', '02_meet_your_controller.json', '03_channel_strip.json',
    '04_crossfader.json', '05_pitch_fader.json', '06_transport_buttons.json',
    '07_jog_wheel.json', '08_headphone_cueing.json', '09_master_booth_headphones.json',
    '10_anatomy_of_a_song.json', '11_counting_bars.json', '12_spot_breakdown_by_ear.json',
    '13_spot_breakdown_by_eye.json', '14_eq_as_tutor.json', '15_load_two_tracks.json',
    '16_course_1_recital.json',
]
for name in expected:
    p = base / name
    assert p.exists(), f'MISSING: {p}'
    d = json.loads(p.read_text())
    assert 'lesson_id' in d, f'{name}: missing lesson_id'
    assert 'title' in d, f'{name}: missing title'
    assert 'system_instruction_addendum' in d, f'{name}: missing addendum'
    assert len(d['system_instruction_addendum']) &lt;= 200, f'{name}: addendum > 200 chars'
    assert 'tutor_speak' in d and isinstance(d['tutor_speak'], list), f'{name}: bad tutor_speak'
    assert 'expected_action' in d, f'{name}: missing expected_action'
    assert 'hints' in d and len(d['hints']) == 3, f'{name}: wrong hints count'
# Iconic dialog byte-equality (Plan 94-02 lands the formal test; smoke-check here).
w = json.loads((base / '01_welcome.json').read_text())
beats = w['tutor_speak']
assert beats[0]['text'] == 'Hello vibemix, what are you?', 'L1.01 beat 0 drift'
assert beats[1]['text'] == \"I'm the best DJ app in the world.\", 'L1.01 beat 1 drift'
assert beats[2]['text'] == 'If you are the best, then who the fuck am I?', 'L1.01 beat 2 drift'
assert beats[3]['text'] == \"Oh bestie, don't worry. You know why? Because I'm the beginner module of vibemix. Let's go.\", 'L1.01 beat 3 drift'
# L1.14 marquee shape
eq = json.loads((base / '14_eq_as_tutor.json').read_text())
assert 'exemplar_cycle' in eq, 'L1.14 missing exemplar_cycle'
assert len(eq['exemplar_cycle']) == 3, 'L1.14 exemplar_cycle must have 3 entries'
bands = [c['band'] for c in eq['exemplar_cycle']]
assert bands == ['low', 'mid', 'high'], f'L1.14 band order drift: {bands}'
# L1.16 recital shape
rc = json.loads((base / '16_course_1_recital.json').read_text())
assert 'recital_pool' in rc and len(rc['recital_pool']) &gt;= 5, 'L1.16 recital_pool too small'
assert 'recital_outcomes' in rc, 'L1.16 missing recital_outcomes'
print('PASS: 16 Course 1 fixtures present, schema-valid, addenda ≤200 chars, iconic dialog byte-equal, L1.14 + L1.16 extension fields present')
"
    </automated>
  </verify>
  <done>16 JSON fixtures exist under `src/vibemix/learn/transcripts/course_1_anatomy/`. Each parses as valid JSON, matches the P92 hello_world shape (lesson_id / title / addendum / tutor_speak / expected_action / hints×3), and addendum is ≤200 chars. L1.01 carries the iconic 4-line opening dialog byte-equal. L1.14 carries `exemplar_cycle` with 3 entries (low/mid/high). L1.16 carries `recital_pool` with ≥5 entries + `recital_outcomes` pass/fail copy.</done>
</task>

<task type="auto">
  <name>Task 2: Extend curriculum.py with COURSE_FRAMES + 16 CURRICULUM entries</name>
  <files>src/vibemix/learn/curriculum.py</files>
  <read_first>src/vibemix/learn/curriculum.py (the P92 shape we extend), src/vibemix/learn/prompts.py:171-200 (composition contract that reads CURRICULUM)</read_first>
  <action>
    Extend `src/vibemix/learn/curriculum.py` to register Course 1 — DO NOT rewrite the file, append. Two extensions
    only:

    **(a) `COURSE_FRAMES["course_1_anatomy"]`** — a 1-3 sentence course frame the tutor system instruction reads via
    prompts.py:194. Per CONTEXT.md §decisions §curriculum-metadata, write a frame that primes the LLM for the
    course context. Suggested wording (keep grounded, ≤350 chars; the COURSE_FRAMES.course_0 entry is the precedent
    at 102 chars):

    ```
    "course_1_anatomy": (
        "Course 1 is the anatomy walkthrough — a beginner meets their controller, "
        "names every section (decks / mixer / transport / EQ), then hears the EQ bands "
        "demonstrated on a track from their own library. The user has no DJ vocabulary yet; "
        "ground every observation in what their hands and ears are doing right now."
    ),
    ```

    **(b) 16 `CURRICULUM["L1.NN"]`** entries — one per lesson fixture. Each entry follows the LessonMeta dataclass:

    ```python
    "L1.01": LessonMeta(
        title="opening dialog",
        course_id="course_1_anatomy",
        system_instruction_addendum=(
            "OPENING DIALOG ADDENDUM: Read the four scripted lines verbatim. "
            "Do not improvise, paraphrase, or add an extra line."
        ),
        transcript_path="course_1_anatomy/01_welcome.json",
    ),
    "L1.02": LessonMeta(
        title="meet your controller",
        course_id="course_1_anatomy",
        system_instruction_addendum=(
            "MEET CONTROLLER ADDENDUM: Name the rendered controller's three sections — "
            "left deck / mixer / right deck. Do not narrate features beyond the three sections."
        ),
        transcript_path="course_1_anatomy/02_meet_your_controller.json",
    ),
    # ... 14 more, one per fixture under course_1_anatomy/
    ```

    **CRITICAL CONSISTENCY:** every entry's `system_instruction_addendum` MUST match (byte-equal) the addendum string
    inside the corresponding JSON fixture's `system_instruction_addendum` field. The fixture is the source of truth
    for tutor_speak text (TONE-02 fixture lock); curriculum.py duplicates the addendum so prompts.py can compose the
    system instruction without reading the JSON twice. (Plan 94-02's tutor-prompts byte-equality test pins this
    drift gate.) Use the same lowercase title strings as in the JSON `title` field.

    Naming convention: LESSON IDs are dotted: `L1.01`, `L1.02`, ..., `L1.16`. The dotted shape matches Plan 92-03's
    L0.00-press-play precedent and the REQ-ID prefix `CURR-1.01..1.16`. Inside the keys, use `"L1.01"` (string).

    Title field: `title` is the lowercase + period-free HUD label per UI-SPEC + LessonMeta docstring. Use these:

    - L1.01 = "opening dialog"
    - L1.02 = "meet your controller"
    - L1.03 = "channel strip"
    - L1.04 = "crossfader"
    - L1.05 = "pitch fader"
    - L1.06 = "transport buttons"
    - L1.07 = "jog wheel"
    - L1.08 = "headphone cueing"
    - L1.09 = "master · booth · headphones"
    - L1.10 = "anatomy of a song"
    - L1.11 = "counting bars"
    - L1.12 = "spot breakdown by ear"
    - L1.13 = "spot breakdown by eye"
    - L1.14 = "eq as tutor"
    - L1.15 = "load two tracks"
    - L1.16 = "course 1 recital"

    transcript_path is `"course_1_anatomy/<NN_filename>.json"` per Task 1's filenames.

    **Do NOT touch** the existing `"L0.00-press-play"` entry or the COURSE_FRAMES["course_0"] entry — they stay
    byte-identical (P92 invariant pins read them). Append the 16 new entries AFTER the existing one, keeping the
    existing trailing `# P94 adds L1.01..L1.16, P95 adds L2.01..L2.14, P96 adds L3.01..L3.07.` comment as the
    sentinel for P95 to find. Same for COURSE_FRAMES — drop "course_1_anatomy" between "course_0" and the P95/P96
    placeholder comment.

    Per CONTEXT.md §"Claude's Discretion": addendum prose can be polished within the lock — keep the SHOUTED PREFIX
    ("CHANNEL STRIP ADDENDUM:", "EQ AS TUTOR ADDENDUM:", etc.) since the test_lock_is_last_block_for_strongest_recency
    test (currently bound to "HELLO WORLD ADDENDUM") uses an addendum sentinel as the lock-position anchor. The
    Course 1 fixtures' SHOUTED prefixes give Plan 94-02's test a per-lesson anchor it can probe across the whole
    course without hard-coding wording.
  </action>
  <verify>
    <automated>
      cd /Users/ozai/projects/dj-set-ai && PYTHONPATH=src python3 -c "
import json
from pathlib import Path
from vibemix.learn.curriculum import CURRICULUM, COURSE_FRAMES, LessonMeta
# COURSE_FRAMES grew.
assert 'course_1_anatomy' in COURSE_FRAMES, 'COURSE_FRAMES missing course_1_anatomy'
assert 'course_0' in COURSE_FRAMES, 'course_0 entry deleted (must remain)'
# 17 CURRICULUM entries: 1 hello world + 16 Course 1.
expected_l1 = [f'L1.{i:02d}' for i in range(1, 17)]
for lesson_id in expected_l1:
    assert lesson_id in CURRICULUM, f'missing curriculum entry {lesson_id}'
    meta = CURRICULUM[lesson_id]
    assert isinstance(meta, LessonMeta)
    assert meta.course_id == 'course_1_anatomy', f'{lesson_id} wrong course_id'
    assert meta.transcript_path.startswith('course_1_anatomy/'), f'{lesson_id} bad path'
    assert len(meta.system_instruction_addendum) &lt;= 200, f'{lesson_id} addendum > 200 chars'
    # Addendum byte-equality with fixture (drift gate)
    base = Path('src/vibemix/learn/transcripts')
    script = json.loads((base / meta.transcript_path).read_text())
    assert script['system_instruction_addendum'] == meta.system_instruction_addendum, (
        f'{lesson_id} addendum mismatch — JSON: {script[\"system_instruction_addendum\"]!r}, '
        f'curriculum: {meta.system_instruction_addendum!r}'
    )
# Loadability check: every CURRICULUM[L1.NN].script @property resolves a real JSON file.
for lesson_id in expected_l1:
    s = CURRICULUM[lesson_id].script
    assert s['lesson_id'].startswith('L1.'), f'{lesson_id} script lesson_id mismatch: {s[\"lesson_id\"]!r}'
# P92 invariant: hello-world entry untouched.
assert 'L0.00-press-play' in CURRICULUM, 'P92 hello-world entry deleted'
assert CURRICULUM['L0.00-press-play'].course_id == 'course_0', 'hello-world course drift'
print('PASS: curriculum.py extended cleanly; 17 lessons total; addendum byte-equality holds across all 16 Course 1 entries')
" &amp;&amp; cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/learn/test_prompts.py tests/learn/test_tutor_system_instruction_lock.py tests/learn/test_scripts_are_fixtures.py -x
    </automated>
  </verify>
  <done>curriculum.py exports COURSE_FRAMES with "course_1_anatomy" + CURRICULUM with 17 entries (P92's L0.00-press-play + 16 new L1.01..L1.16). Every L1.NN entry's addendum is byte-equal to its fixture's addendum, ≤200 chars. P92's existing prompts / lock / fixtures test suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → fixture JSON | hand-authored copy is the source of tutor narration; any drift bypasses the runtime |
| filesystem → curriculum.py | LessonMeta values gate the FSM's lesson selection + composition path |
| user → published binary | strangers see the iconic dialog on first launch; drift here is a brand-defacement vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01-01 | Tampering | iconic dialog (L1.01 fixture) | mitigate | Plan 94-02 lands `test_tutor_prompts_byte_equality.py` — CI red on any whitespace / capitalization / smart-quote drift; Task 1 verify-script smoke-checks the 4 lines too |
| T-94-01-02 | Tampering | tutor narration text | mitigate | TONE-02 fixture gate (`test_scripts_are_fixtures.py` from P92) stays green — no live LLM writes; tutor_speak.text loaded verbatim from JSON |
| T-94-01-03 | Information Disclosure | lesson scripts in repo | accept | scripts are intentionally public — Apache-2.0 OSS distribution; no PII / no secrets |
| T-94-01-04 | Repudiation | author of lesson copy | accept | git blame is the audit log; no separate provenance store needed for v9.0 |
| T-94-01-05 | Denial of Service | malformed JSON wedges runtime | mitigate | Task 1 verify-script + Task 2 verify-script both json.loads every fixture; LessonMeta.script @property raises FileNotFoundError / JSONDecodeError on load with the existing P92 defensive try/except in on_enter_loaded swallowing the error to stderr — the FSM never wedges |
| T-94-01-06 | Tampering | system_instruction_addendum drift between fixture + curriculum.py | mitigate | Task 2 verify-script asserts byte-equality between every fixture's addendum and the corresponding CURRICULUM[lesson_id].system_instruction_addendum |
| T-94-01-SC | Tampering | npm/pip installs | accept | zero new dependencies — pure content + curriculum.py edit; no install-gate exposure |
</threat_model>

<verification>
- Every Course 1 JSON fixture parses as valid JSON.
- Every fixture has lesson_id / title / system_instruction_addendum / tutor_speak / expected_action / hints×3.
- Every addendum is ≤200 chars (composition-time cap from prompts.py:185 — would raise ValueError downstream).
- L1.01 byte-equal to the iconic 4-line dialog (TONE-01 binding; Plan 94-02 pins formally).
- L1.14 declares `exemplar_cycle` with 3 entries in order low → mid → high.
- L1.16 declares `recital_pool` with ≥5 entries (random subset drawn from L1.03..L1.13 controls) + `recital_outcomes`.
- CURRICULUM has 17 entries (P92's hello world + 16 Course 1).
- COURSE_FRAMES has the new "course_1_anatomy" entry.
- Every CURRICULUM[L1.NN].system_instruction_addendum is byte-equal to its fixture's addendum.
- P92's existing test suite (test_prompts.py, test_tutor_system_instruction_lock.py, test_scripts_are_fixtures.py) stays green.
- TONE discipline: no tutor-tic blocklist tokens (Plan 94-02's gate must pass against these fixtures).
</verification>

<success_criteria>
- 16 hand-authored JSON fixtures exist under `src/vibemix/learn/transcripts/course_1_anatomy/`.
- curriculum.py exports 17 CURRICULUM entries (one course_0 + 16 course_1_anatomy) + 2 COURSE_FRAMES entries.
- The verify-scripts above run clean.
- P92 invariant pins remain green (test_runtime_invariants.py + test_no_new_ws_port.py + test_tutor_system_instruction_lock.py + test_scripts_are_fixtures.py = 4 AST gates).
</success_criteria>

<output>
Create `.planning/phases/94-course-1-anatomy/94-01-SUMMARY.md` when done.
</output>
