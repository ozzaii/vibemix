---
phase: 11
plan: 1
type: execute
status: draft
wave: 0
depends_on: []
files_modified:
  - tauri/ui/src/ipc/messages.schema.json
  - tauri/ui/src/ipc/messages.ts
  - tauri/ui/src/ipc/validator.ts
  - tauri/ui/package.json
  - tauri/ui/tsconfig.json
  - tauri/ui/vitest.config.ts
  - tauri/ui/src/ipc/validator.spec.ts
  - src/vibemix/ui_bus/__init__.py
  - src/vibemix/ui_bus/messages.py
  - src/vibemix/ui_bus/validator.py
  - scripts/check_ipc_schema.py
  - tests/ui_bus/__init__.py
  - tests/ui_bus/test_messages_schema.py
  - tests/ui_bus/test_status_tick.py
  - pyproject.toml
autonomous: true
requirements:
  - ARCH-01
  - DIST-05
  - UX-11
tags:
  - tauri
  - ipc
  - schema
  - python
  - typescript
must_haves:
  truths:
    - "messages.schema.json is the single source-of-truth for every ipc.* message"
    - "Python dataclasses in vibemix.ui_bus.messages serialize to JSON that validates against the schema"
    - "TypeScript types in tauri/ui/src/ipc/messages.ts are regenerated from the schema, never hand-written"
    - "scripts/check_ipc_schema.py exits non-zero if any dataclass example fails schema validation OR if the schema oneOf count diverges from the wrapper dataclass count"
    - "npm run check:ipc inside tauri/ui regenerates types and runs tsc --noEmit; drift fails CI"
    - "Both jsonschema (Python) and ajv (TypeScript) reject malformed messages at runtime"
  artifacts:
    - path: "tauri/ui/src/ipc/messages.schema.json"
      provides: "JSON Schema source-of-truth for ipc.* messages"
      contains: "$id"
    - path: "src/vibemix/ui_bus/messages.py"
      provides: "Hand-written @dataclass IPC messages with .to_json() runtime validation"
      exports: ["StatusTick", "StatusTickPayload", "IpcBoot"]
    - path: "src/vibemix/ui_bus/validator.py"
      provides: "jsonschema runtime guard (validate_message)"
      exports: ["validate_message"]
    - path: "scripts/check_ipc_schema.py"
      provides: "Build-time CI gate: every dataclass example roundtrips through the schema + schema/wrapper count parity assertion"
    - path: "tauri/ui/src/ipc/validator.ts"
      provides: "ajv runtime guard parseIpcMessage(raw): IpcMessage"
      exports: ["parseIpcMessage"]
    - path: "tauri/ui/package.json"
      provides: "Vite + TypeScript + ajv + json-schema-to-typescript dev deps + check:ipc script"
      contains: "check:ipc"
  key_links:
    - from: "src/vibemix/ui_bus/messages.py"
      to: "tauri/ui/src/ipc/messages.schema.json"
      via: "_SCHEMA = json.loads(...) at module import + jsonschema.validate in to_json()"
      pattern: "messages\\.schema\\.json"
    - from: "scripts/check_ipc_schema.py"
      to: "src/vibemix/ui_bus.messages"
      via: "imports every @dataclass + asserts to_json() roundtrip + asserts oneOf count == wrapper count"
      pattern: "from vibemix.ui_bus"
    - from: "tauri/ui/package.json"
      to: "tauri/ui/src/ipc/messages.schema.json"
      via: "npm script codegen:ipc → json-schema-to-typescript writes messages.ts"
      pattern: "json-schema-to-typescript"
---

<objective>
Wave 0 of Phase 11: establish the IPC schema source-of-truth and the dual-language validation gate BEFORE any sidecar packaging, Tauri shell, or wizard UI work begins. This is the foundation every subsequent wave consumes — Python dataclasses, TypeScript types, runtime validators (jsonschema + ajv), and CI gates that fail loud on drift.

Purpose: Phase 11's existential risk is schema drift between Python sidecar and TypeScript webview at runtime (RESEARCH Pitfall 10). Locking the schema first + wiring both build-time and runtime gates means waves 1-4 can build against stable contracts. Honors locked decisions D-Area-1.3 (schema sync) and D-Area-1.1 (ipc.* namespace on existing ws_bus port 8765).

Output:
- `tauri/ui/src/ipc/messages.schema.json` (the source-of-truth)
- `src/vibemix/ui_bus/` Python package (messages + validator)
- `tauri/ui/` Node project scaffold (package.json, tsconfig, vitest config, ipc/validator.ts)
- `scripts/check_ipc_schema.py` (CI gate)
- Tests on both sides
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tauri-shell-calibration-wizard/11-CONTEXT.md
@.planning/phases/11-tauri-shell-calibration-wizard/11-RESEARCH.md
@.planning/phases/11-tauri-shell-calibration-wizard/11-UI-SPEC.md
@.claude/skills/frontend-enforcement/SKILL.md
@pyproject.toml
@src/vibemix/runtime/ws_bus.py

<interfaces>
<!-- Phase 4 already established ws_bus pattern at 127.0.0.1:8765. This plan only ADDS ipc.* message types — does NOT modify ws_bus.py (Wave 4 wires the WizardLoop handler). -->
<!-- Phase 6 convention: NO pydantic. Use @dataclass + jsonschema validation only. -->

Schema namespace (per RESEARCH §IPC Schema):
- ipc.boot                         — sidecar → shell, payload: {ready: bool}
- ipc.status.tick                  — sidecar → shell, payload: {livekit, gemini, midi, screen}
- ipc.permission.check             — shell → sidecar, payload: {kind: "screen_recording" | "microphone"}
- ipc.permission.state             — sidecar → shell, payload: {kind, status: "authorized" | "denied" | "notDetermined" | "restricted"}
- ipc.calibration.list_devices     — shell → sidecar, payload: {}
- ipc.calibration.device_list      — sidecar → shell, payload: {devices: [{id, name, is_blackhole, variant}], blackhole_present}
- ipc.calibration.probe_audio      — shell → sidecar, payload: {output_device_id: str, expected_rate: 44100|48000}
- ipc.calibration.audio_result     — sidecar → shell, payload: {playback_ok, audible_confirmed, programmatic_pass, actual_rate, error}
- ipc.calibration.user_heard_tone  — shell → sidecar, payload: {heard: bool}
- ipc.calibration.start_midi_listen — shell → sidecar, payload: {timeout_s: number}
- ipc.calibration.midi_event       — sidecar → shell, payload: {control_label: str, raw: str}
- ipc.calibration.midi_timeout     — sidecar → shell, payload: {}
- ipc.calibration.list_windows     — shell → sidecar, payload: {} (Wave 4 window-picker; OS-specific enumeration via Quartz on mac / EnumWindows on win lives in the Python platform layer per Phase 3+7+8 surfaces)
- ipc.calibration.window_list      — sidecar → shell, payload: {windows: [{id: str, app_name: str, title: str, dj_app_hint: str | null}]}
- ipc.calibration.smoke_test       — shell → sidecar, payload: {template: "HYPE_BEGINNER"}
- ipc.calibration.smoke_test_started — sidecar → shell, payload: {}
- ipc.calibration.smoke_test_done    — sidecar → shell, payload: {transcript: str}
- ipc.wizard.start                 — shell → sidecar, payload: {}
- ipc.wizard.done                  — shell → sidecar, payload: {output_device_id, controller_profile, target_window_id}

Total: **19 ipc.* messages** → 19 schema `oneOf` entries → 19 wrapper dataclasses. The CI gate asserts these three counts agree.

Project conventions (from CLAUDE.md + Phase 6/9 history):
- No pydantic anywhere in src/vibemix/. Use `@dataclass` + `jsonschema` validation only.
- Type hints with `from __future__ import annotations` (Phase 6 pattern).
- Hand-written validators (no codegen on Python side); jsonschema 4.23.x is install-only.
- Tests live under tests/<area>/test_<thing>.py per Phase 2-10 convention.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author messages.schema.json and Python ui_bus package</name>
  <files>tauri/ui/src/ipc/messages.schema.json, src/vibemix/ui_bus/__init__.py, src/vibemix/ui_bus/messages.py, src/vibemix/ui_bus/validator.py, pyproject.toml, tests/ui_bus/__init__.py, tests/ui_bus/test_messages_schema.py, tests/ui_bus/test_status_tick.py</files>
  <action>
Create the JSON Schema source-of-truth and the Python dataclass package that mirrors it. This is the foundation every later wave consumes.

1. Add `jsonschema>=4.23,<5` to `pyproject.toml` `[project] dependencies` (NOT dev — sidecar uses it at runtime). Keep the existing `requires-python = ">=3.12,<3.13"` pin per D-Area-4.4. Run `uv sync` (or `uv lock && uv sync`) to update the lockfile.

2. Create `tauri/ui/src/ipc/messages.schema.json` (draft-07). Use a `oneOf` discriminator over **19** `definitions` covering every `ipc.*` message listed in the `<interfaces>` block. Each definition is a closed object (`additionalProperties: false`) with three top-level fields: `type` (const string matching the ipc.* name), `ts` (date-time string), `payload` (nested object). Use enums for closed state fields (`livekit`, `gemini`, `screen`, `status`, `kind`). Use `["integer", "null"]` for `midi` count and `actual_rate` (nullable on error). For `ipc.calibration.window_list.payload.windows[].dj_app_hint`, allow `["string", "null"]` so the sidecar can return null when no known DJ-app name matches the window. For floats (e.g. magnitude in future), use `{"type": "number"}`. `$id`: `"vibemix.ipc.messages"`. Add `$comment` per definition referencing the originating RESEARCH section or D-XX decision ID for traceability.

3. Create `src/vibemix/ui_bus/__init__.py` re-exporting the public names: `StatusTick`, `StatusTickPayload`, `IpcBoot`, `CalibrationDeviceList`, `CalibrationDeviceListPayload`, `DeviceInfo`, `CalibrationProbeAudio`, `CalibrationAudioResult`, `CalibrationAudioResultPayload`, `PermissionCheck`, `PermissionState`, `CalibrationStartMidiListen`, `CalibrationMidiEvent`, `CalibrationMidiTimeout`, `CalibrationListWindows`, `CalibrationWindowList`, `WindowInfo`, `CalibrationSmokeTest`, `CalibrationSmokeTestStarted`, `CalibrationSmokeTestDone`, `WizardStart`, `WizardDone`, `validate_message`. Use `from __future__ import annotations` at top. Wrapper-dataclass count (excluding payload-only structs `*Payload` / `DeviceInfo` / `WindowInfo` and the `validate_message` symbol) MUST equal 19 — one per `oneOf` entry.

4. Create `src/vibemix/ui_bus/messages.py`. For each ipc.* message create a pair of frozen `@dataclass(frozen=True, slots=True)` classes: one for the payload and one wrapping `type`/`ts`/`payload`. Each wrapper class has a `@classmethod make(...) -> Self` factory that fills `type=` (const literal) and `ts=datetime.now(timezone.utc).isoformat()`, and a `to_json(self) -> str` method that calls `asdict(self)`, runs `jsonschema.validate(d, _SCHEMA)`, returns `json.dumps(d, separators=(",", ":"))`. Use `typing.Literal[...]` for closed-enum fields so type-checkers catch invalid values at write time. Load `_SCHEMA = json.loads(_SCHEMA_PATH.read_text())` once at module scope; resolve `_SCHEMA_PATH` via `Path(__file__).resolve().parents[3] / "tauri" / "ui" / "src" / "ipc" / "messages.schema.json"`. Compile the validator once with `_VALIDATOR = jsonschema.Draft7Validator(_SCHEMA)` and use `_VALIDATOR.validate(d)` (cheaper than module-level validate per call).

5. Create `src/vibemix/ui_bus/validator.py` with `validate_message(raw: dict) -> None` that raises `jsonschema.ValidationError` on schema violation, plus `parse_message(raw: dict | str) -> dict` that accepts a JSON string or dict and returns a validated dict (raises on type mismatch). This is the runtime guard the WS bus loop will call in Wave 4.

6. Create `tests/ui_bus/test_messages_schema.py`: parametrize over every wrapper dataclass — assert `.make(...)` constructs without error, asserts `.to_json()` returns a string that parses back to a dict that passes `jsonschema.validate` against `_SCHEMA`. Use minimal valid inputs (mock data) per type. Include the two new window-picker messages (`CalibrationListWindows.make()` empty payload + `CalibrationWindowList.make(windows=[WindowInfo(id="0", app_name="djay Pro AI", title="djay Pro — Main", dj_app_hint="djay")])`).

7. Create `tests/ui_bus/test_status_tick.py`: focused tests for the `ipc.status.tick` shape per UX-11 — `midi=null` valid, `midi=-1` invalid (minimum: 0), unknown enum values rejected, `additionalProperties` rejected. Cover all four state fields.

Per D-Area-4.3 (status badges scope): the `StatusTick` payload is `{livekit, gemini, midi, screen}` exactly — no extra fields tolerated. Tests assert `additionalProperties: false` blocks any drift.

Note about realistic dataclass examples in tests: per A10, every dataclass must have a minimal valid example that can be constructed without external services. For `CalibrationAudioResult` use `playback_ok=True, audible_confirmed=True, programmatic_pass=True, actual_rate=48000, error=None`.
  </action>
  <verify>
    <automated>uv run python -c "from vibemix.ui_bus.messages import StatusTick, WizardStart, CalibrationListWindows, CalibrationWindowList; m = StatusTick.make(livekit='ok', gemini='ok', midi=1, screen='ok'); print(m.to_json()); WizardStart.make(); CalibrationListWindows.make()" &amp;&amp; uv run pytest tests/ui_bus/ -x -q</automated>
  </verify>
  <done>messages.schema.json passes self-validation (Draft7Validator.check_schema). All 19 ipc.* message wrappers construct via .make() and roundtrip through .to_json() under jsonschema validation. `WizardStart`, `CalibrationListWindows`, and `CalibrationWindowList` are importable from `vibemix.ui_bus`. tests/ui_bus/ green. `grep -RE "pydantic" src/vibemix/ui_bus/` returns zero matches (Phase 6 convention).</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tauri/ui Node project + ajv validator + check_ipc CI gate</name>
  <files>tauri/ui/package.json, tauri/ui/tsconfig.json, tauri/ui/vitest.config.ts, tauri/ui/src/ipc/validator.ts, tauri/ui/src/ipc/messages.ts, tauri/ui/src/ipc/validator.spec.ts, scripts/check_ipc_schema.py, .gitignore</files>
  <action>
Stand up the TypeScript side of the schema sync gate. No Tauri Rust yet — Wave 2 owns that. This task only creates the Vite/TS scaffold needed to run codegen and `tsc --noEmit` against the schema.

1. Create `tauri/ui/package.json` with:
   - `"name": "vibemix-ui"`, `"private": true`, `"type": "module"`, `"version": "0.0.0"`
   - Scripts: `"codegen:ipc": "json-schema-to-typescript src/ipc/messages.schema.json --output src/ipc/messages.ts --unreachableDefinitions"`, `"check:ipc": "npm run codegen:ipc && tsc --noEmit"`, `"test": "vitest run --reporter=dot"`, `"dev": "vite"`, `"build": "tsc --noEmit && vite build"`
   - devDependencies (run `npm view <pkg> version` to confirm latest per RESEARCH versioning rule — pin to MAJOR.MINOR resolved at install time): `typescript ^5.7`, `vite ^6`, `vitest ^2`, `ajv ^8`, `ajv-formats ^3`, `json-schema-to-typescript ^15`, `@tauri-apps/api ^2`, `@tauri-apps/plugin-shell ^2`, `@tauri-apps/plugin-store ^2`
   - No runtime dependencies in Wave 0 (Tauri plugins land Wave 2).

2. Create `tauri/ui/tsconfig.json`: `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "bundler"`, `"strict": true`, `"noUncheckedIndexedAccess": true`, `"resolveJsonModule": true`, `"isolatedModules": true`, `"lib": ["ES2022", "DOM"]`, `"include": ["src/**/*"]`. Set `"types": []` to avoid pulling Node globals into webview code.

3. Create `tauri/ui/vitest.config.ts` minimal — defaults; environment `"node"` for now (DOM tests arrive in Wave 3).

4. Create `tauri/ui/src/ipc/validator.ts`:
   - Import ajv + ajv-formats, import schema JSON via `import schema from "./messages.schema.json"` (tsconfig `resolveJsonModule: true`).
   - Compile once with `const ajv = new Ajv({ allErrors: true, strict: false }); addFormats(ajv); const validate = ajv.compile(schema);`
   - Export `parseIpcMessage(raw: unknown): IpcMessage` that throws `Error` with `ajv.errorsText(validate.errors)` on failure, returns `raw as IpcMessage` on success.
   - Export `isIpcMessage(raw: unknown): raw is IpcMessage` for narrowing without throwing.

5. Run `npm install` then `npm run codegen:ipc` to produce the initial `tauri/ui/src/ipc/messages.ts`. Commit the generated file alongside the schema (per RESEARCH alternatives-considered: codegen output is committed so executors/Tauri builds don't depend on Node being available; CI regenerates and diffs).

6. Create `tauri/ui/src/ipc/validator.spec.ts`: vitest cases — valid `StatusTick` parses without throw, missing `payload` field throws, unknown enum value (`livekit: "wat"`) throws, `additionalProperties` throws, `midi: null` valid, `midi: -1` invalid, valid `ipc.calibration.window_list` payload with one window entry parses, malformed window entry (missing `id`) throws. Mirror the Python test_status_tick.py asserts so both sides catch the same drift.

7. Create `scripts/check_ipc_schema.py` — the build-time CI gate per RESEARCH Pattern 3:
   - Imports every wrapper dataclass from `vibemix.ui_bus.messages`.
   - For each, builds a minimal-valid example, calls `.to_json()`, parses, runs `jsonschema.validate(d, SCHEMA)`. Collects errors.
   - **Count-parity assertion (load-bearing):** after the per-dataclass loop, count the schema `oneOf` entries and the wrapper-dataclass classes in `vibemix.ui_bus.messages`, then assert they are equal. Drift here is the canonical "schema added without wrapper" or "wrapper added without schema entry" failure that Wave 0 must catch loud. Use this idiom (adjust if a cleaner one emerges — the assertion is the load-bearing piece):
     ```python
     import json
     from pathlib import Path
     from vibemix.ui_bus import messages

     SCHEMA = json.loads(Path("tauri/ui/src/ipc/messages.schema.json").read_text())
     oneof_count = len(SCHEMA["oneOf"])
     # Detect wrapper dataclasses: dataclasses with a `type` field whose value is a Literal[...] const.
     # Excludes payload-only structs (StatusTickPayload, DeviceInfo, WindowInfo, etc.) because they have no `type` field.
     wrapper_count = sum(
         1 for name in dir(messages)
         if isinstance(getattr(messages, name), type)
         and hasattr(getattr(messages, name), "__dataclass_fields__")
         and "type" in getattr(messages, name).__dataclass_fields__
     )
     assert oneof_count == wrapper_count, (
         f"schema/dataclass drift: {oneof_count} oneOf entries vs {wrapper_count} wrapper dataclasses. "
         f"Add the missing schema definition or wrapper class so both sides stay in sync."
     )
     print(f"OK: count parity — {oneof_count} oneOf entries == {wrapper_count} wrapper dataclasses")
     ```
   - Prints `OK: N dataclasses validate against schema` and the count-parity OK line, then exits 0 on success.
   - Prints each failure with class name and exits 1 on any error.
   - Uses `pathlib.Path("tauri/ui/src/ipc/messages.schema.json").read_text()` — fails loud if schema file moved.

8. Add to `.gitignore`: `tauri/ui/node_modules/`, `tauri/ui/dist/`, `tauri/ui/.vite/` (already covered by global node_modules but be explicit for the tauri/ subtree).

Anti-pattern guard: do NOT hand-write `messages.ts` — only edit `messages.schema.json`, then regenerate via `npm run codegen:ipc`. Add a comment header at the top of the committed `messages.ts`: `/* AUTO-GENERATED from messages.schema.json — do not edit. Run 'npm run codegen:ipc'. */`.
  </action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; npm install --no-audit --no-fund &amp;&amp; npm run check:ipc &amp;&amp; npx vitest run --reporter=dot src/ipc/validator.spec.ts &amp;&amp; cd ../.. &amp;&amp; uv run python scripts/check_ipc_schema.py</automated>
  </verify>
  <done>`npm run check:ipc` regenerates messages.ts and tsc --noEmit exits 0. Vitest validator suite green (≥8 cases including window_list happy/sad). `python scripts/check_ipc_schema.py` exits 0, prints both "OK: N dataclasses validate against schema" and "OK: count parity — 19 oneOf entries == 19 wrapper dataclasses". `head -1 tauri/ui/src/ipc/messages.ts` shows the AUTO-GENERATED banner. `grep "^export type IpcMessage" tauri/ui/src/ipc/messages.ts` finds the discriminated union.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python sidecar ↔ JSON Schema file | Untrusted disk read at module import; schema file controls what counts as valid IPC |
| TypeScript webview ↔ JSON Schema file | Same schema read via Vite import — same trust assumption |
| (No external network in Wave 0 — sidecar/webview/Rust not yet wired) | — |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W0-01 | Tampering | tauri/ui/src/ipc/messages.schema.json drift between Python and TS | mitigate | `scripts/check_ipc_schema.py` (per-dataclass roundtrip + oneOf/wrapper count parity) + `npm run check:ipc` both run in CI per RESEARCH Pattern 3; either failing breaks the gate |
| T-11-W0-02 | Information Disclosure | Schema file accidentally encoding secret-shaped fields (e.g. tokens in payload) | mitigate | Schema explicitly closes `additionalProperties: false` on every payload; Phase 5 JWT lives in keychain, never crosses ipc.* — assert in test |
| T-11-W0-03 | Tampering | Hand-edited `messages.ts` diverges from schema | mitigate | AUTO-GENERATED banner at file head + CI regenerates and diffs; any executor edit shows up as a diff in the next CI run |
| T-11-W0-04 | Denial of Service | Malformed inbound message crashes sidecar at parse | mitigate | `validate_message` raises `jsonschema.ValidationError` (recoverable); Wave 4 WizardLoop catches + logs + drops the frame |
</threat_model>

<verification>
- `uv run pytest tests/ui_bus/ -x -q` — green
- `cd tauri/ui && npm run check:ipc` — exits 0
- `cd tauri/ui && npx vitest run --reporter=dot` — green
- `uv run python scripts/check_ipc_schema.py` — exits 0, prints OK count + "OK: count parity — 19 oneOf entries == 19 wrapper dataclasses"
- `grep -RE "pydantic" src/vibemix/ui_bus/` — zero matches
- `head -1 tauri/ui/src/ipc/messages.ts` — AUTO-GENERATED banner
- `uv run python -c "import jsonschema, json; from pathlib import Path; s = json.loads(Path('tauri/ui/src/ipc/messages.schema.json').read_text()); jsonschema.Draft7Validator.check_schema(s); print('schema OK')"` — prints `schema OK`
</verification>

<success_criteria>
1. `tauri/ui/src/ipc/messages.schema.json` exists, is Draft-07 valid, defines the full **19-message** `ipc.*` namespace with closed payloads (including `ipc.wizard.start`, `ipc.calibration.list_windows`, `ipc.calibration.window_list`).
2. `src/vibemix/ui_bus/` package importable from anywhere in the project; every wrapper dataclass roundtrips through schema validation.
3. `npm run check:ipc` is the canonical TS-side gate; `python scripts/check_ipc_schema.py` is the canonical Python-side gate. Both run in <10s. The Python gate includes the oneOf/wrapper count-parity assertion.
4. Zero pydantic imports in `src/vibemix/ui_bus/`.
5. No Tauri Rust code yet — Wave 1/2 own that.
</success_criteria>

<anti_patterns>
- DO NOT introduce pydantic for the schema layer (Phase 6 convention; D-Area-4.4 reinforces hand-validated JSON loaders).
- DO NOT hand-edit `tauri/ui/src/ipc/messages.ts` — it is codegen output; the schema is the source.
- DO NOT add Tauri Rust crates yet (Wave 2 owns Cargo.toml).
- DO NOT pin npm packages to exact versions — use caret ranges (`^X.Y`) so security patches land via `npm install`; lockfile (`package-lock.json`) pins exact.
- DO NOT add `messages.schema.json` to multiple locations — the path is `tauri/ui/src/ipc/messages.schema.json` and only that. Python resolves via parents[3].
- DO NOT skip `additionalProperties: false` — open payloads silently absorb drift (RESEARCH Pitfall 10).
- DO NOT drop the count-parity assertion from `check_ipc_schema.py` — it is the canonical drift-detector for "schema added without wrapper" (and vice versa).
</anti_patterns>

<commit_template>
chore(11): wave 0 — IPC schema source-of-truth + dual-language validator gate

- Add tauri/ui/src/ipc/messages.schema.json (Draft-07, 19 ipc.* message types, closed payloads)
- Add src/vibemix/ui_bus/{messages.py, validator.py} (@dataclass + jsonschema; no pydantic)
- Scaffold tauri/ui/ Node project (vite + vitest + ajv + json-schema-to-typescript)
- Add scripts/check_ipc_schema.py CI gate (per-dataclass roundtrip + oneOf/wrapper count parity)
  + tauri/ui `check:ipc` npm script
- Tests: tests/ui_bus/ + tauri/ui/src/ipc/validator.spec.ts assert drift fails loud

Closes Wave 0 of Phase 11. Foundation for Waves 1-4.
</commit_template>

<output>
After completion, the executor reports back to the orchestrator. SUMMARY consolidation happens at end-of-phase per project convention (see Phase 10 pattern). For this plan, append a short status note to the next session's STATE update.
</output>
</output>
