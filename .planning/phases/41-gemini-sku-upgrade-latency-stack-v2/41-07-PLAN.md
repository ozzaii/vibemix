---
phase: 41-gemini-sku-upgrade-latency-stack-v2
plan: 07
type: execute
wave: 3
depends_on: [41-01, 41-02, 41-03, 41-04, 41-05, 41-06]
files_modified:
  - scripts/eval/replay_harness.py
  - tests/e2e/test_phase_41_latency_stack_integration.py
  - .planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md
autonomous: true
requirements: [LAT-01, LAT-02, LAT-03, LAT-04, LAT-05, LAT-06, LAT-07, LAT-08, LAT-09]
must_haves:
  truths:
    - "End-to-end integration test exercises a full session through ModelRouter-resolved SKUs (live coach Standard 3-Flash; debrief Flex 3-Pro; embedding Flex Embedding-2) via VCR cassettes — no real Gemini spend"
    - "replay_harness.py extended with --print-llm-to-tts-delta flag — surfaces average + p50 + p95 delta_ms from events.jsonl across a synthetic-session replay"
    - "Measured LLM→TTS delta savings over pre-refactor baseline: 200-400ms per CONTEXT.md LAT-04 success criterion (recorded in integration report)"
    - "cache_hit telemetry observed > 0 on ≥60% of live_coach turns in a 30-turn synthetic session (Open Q3 conservative initial threshold)"
    - "thinking_gate runtime validator confirmed: agent boots cleanly with default config; synthetic FLEX-tier-on-live config raises LiveCoachConfigError"
    - "Embedding 2 probe result observed + logged: which candidate the developer machine resolves to (A1 confirmation surface)"
    - "INTEGRATION-REPORT.md captures all measurements + assumption verification + pitfall observations into one phase-level artifact"
  artifacts:
    - path: "tests/e2e/test_phase_41_latency_stack_integration.py"
      provides: "Full-session integration test pinning end-to-end ModelRouter + streaming + cache + thinking-gate behavior"
    - path: "scripts/eval/replay_harness.py"
      provides: "Extended harness — --print-llm-to-tts-delta + --print-cache-hit-rate + --print-router-resolves CLI flags"
    - path: ".planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md"
      provides: "Phase 41 integration measurements + assumption-log resolution + observed pitfall behaviors"
  key_links:
    - from: "scripts/eval/replay_harness.py"
      to: "events.jsonl llm_to_tts_delta_ms + cache_hit + embedding_model_probe events"
      via: "harness CLI flags surface aggregate statistics"
      pattern: "llm_to_tts_delta_ms|cache_hit|embedding_model_probe"
    - from: "tests/e2e/test_phase_41_latency_stack_integration.py"
      to: "Plans 41-01..06 surfaces"
      via: "VCR-cassette-driven full-session simulation"
      pattern: "resolve|validate_live_config|find_sentence_end|LLMToTTSDeltaMeter|GeminiContextCache"
---

<objective>
Land the Phase 41 integration test + extend `scripts/eval/replay_harness.py` to surface the latency-stack metrics that Plans 41-01..06 introduce. Produces `41-INTEGRATION-REPORT.md` documenting:
- Measured LLM→TTS delta savings against pre-refactor baseline (target 200-400ms per CONTEXT.md)
- Observed cache_hit rate on a 30-turn synthetic session (Open Q3 conservative threshold ≥60%)
- Embedding 2 probe result (Assumption A1 verification — `gemini-embedding-002` vs `gemini-embedding-2`)
- thinking_gate runtime validator behavior under synthetic FLEX-on-live and bad-thinking-level overrides
- Any Pitfall 8 (LiveKit cancel race) observations from end-to-end runs

This plan is the goal-backward gate for Phase 41: it confirms the orthogonal Plan 41-01..06 changes compose into a working session without behavior drift. Wave 3 — runs after all six prior plans complete.

Output: Integration test exercising all 9 REQ-IDs in one synthetic session run; extended replay harness; integration report capturing phase-level measurements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-CONTEXT.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-RESEARCH.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-01-PLAN.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-02-PLAN.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-03-PLAN.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-04-PLAN.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-05-PLAN.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-06-PLAN.md

@scripts/eval/replay_harness.py

<interfaces>
<!-- Surfaces from Plans 41-01..06 that this integration plan composes. -->

From Plan 41-01:
- `vibemix.llm.model_router.resolve(path) -> (model_id, ServiceTier | None)`
- 8 router paths: live_coach, live_coach_tts, live_coach_tts_fallback, live_coach_tts_openrouter, debrief, debrief_tts, library_auto_tag, embedding

From Plan 41-02:
- `GeminiContextCache(ttl_s=3600.0)` — no refresh_loop method
- `EvidenceRegistry(on_mutation=...)` — debounced refresh callback
- events.jsonl `cache_hit` event with `cached_tokens` payload

From Plan 41-03:
- `vibemix.llm.thinking_gate.validate_live_config(cfg)` raises `LiveCoachConfigError` on bad thinking_level or FLEX-on-live

From Plan 41-04:
- `vibemix.agent._streaming_pipe.find_sentence_end(text)` + `passes_head_gate(head)`
- `vibemix.runtime.llm_to_tts_delta_meter.LLMToTTSDeltaMeter` — records `llm_to_tts_delta_ms` event
- Dual-phase gate in `dj_cohost.llm_node` — head streams + cancel-on-trailing-slop

From Plan 41-05:
- LibraryEmbedder probes `gemini-embedding-002` then falls back to `gemini-embedding-2`
- Emits `embedding_model_probe` event
- Bit-identical 768-dim top-K via cosine_topk

From Plan 41-06:
- spikes/ scaffolding (NOT in src/vibemix/ runtime path — integration test confirms boundary)

Existing replay harness:
- `scripts/eval/replay_harness.py` already drives the eval corpus through cascade for proxy gate
- Phase 40-04 added `--print-cooldowns` flag — same extension pattern
- Reads events.jsonl line-by-line; aggregates per session

VCR fixtures (existing v2.1 convention):
- Cassettes under `tests/e2e/cassettes/` or per-test directory
- `VCR_RECORD_MODE=none` in CI (replay only); `=new_episodes` to record
- Re-recording requires real GEMINI_API_KEY + acknowledged cost
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend replay_harness.py with Phase 41 metric surfaces</name>
  <files>scripts/eval/replay_harness.py, tests/eval/test_replay_harness_phase_41.py</files>
  <action>
    Read the current `scripts/eval/replay_harness.py` to identify the existing CLI flag pattern (Phase 40-04 added `--print-cooldowns`). Add three new flags following the same pattern:

    1. `--print-llm-to-tts-delta`: reads events.jsonl from a session, filters `llm_to_tts_delta_ms` events, prints aggregate stats: count, mean, median, p50, p95, p99, min, max. If no events, print "no LLMToTTSDeltaMeter events found — pre-Phase-41 session or no first-sentence emissions".

    2. `--print-cache-hit-rate`: reads events.jsonl, counts `cache_hit` events vs total `llm_invoke` events; prints "cache hit rate: {X}/{Y} = {pct}%; mean cached_tokens on hit: {Z}". Used to verify Open Q3 ≥60% threshold.

    3. `--print-router-resolves`: walks src/vibemix/ via subprocess `grep -rn "resolve(" src/vibemix/` and prints which router paths are referenced + reference count. Used as a quick audit during dev: "did anyone add a new SDK call site without going through resolve?"

    Each flag adds ~30 LOC; combine into a small helper module `scripts/eval/_phase_41_summaries.py` if total flag-handler code exceeds 100 LOC.

    Add CLI tests in `tests/eval/test_replay_harness_phase_41.py`:
    - test_print_llm_to_tts_delta_empty_session: synthetic events.jsonl with no delta events → expected "no LLMToTTSDeltaMeter events found" output
    - test_print_llm_to_tts_delta_aggregates: synthetic events.jsonl with 10 deltas (mix of 200-500ms) → aggregate stats parse correctly
    - test_print_cache_hit_rate: synthetic events.jsonl with 30 llm_invoke + 18 cache_hit → output "18/30 = 60%"
    - test_print_router_resolves_no_violations: subprocess invocation against current src/vibemix/ → exits 0; reports ≥9 resolve() call sites (1 per migration site from Plan 41-01)
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/eval/test_replay_harness_phase_41.py -x -v &amp;&amp; uv run python scripts/eval/replay_harness.py --help 2&gt;&amp;1 | grep -E "print-llm-to-tts-delta|print-cache-hit-rate|print-router-resolves"</automated>
  </verify>
  <done>Three new CLI flags shipped + 4 tests green. `--help` renders all three flags. Existing harness Phase 40-04 functionality preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end integration test exercising all 9 REQ-IDs</name>
  <files>tests/e2e/test_phase_41_latency_stack_integration.py, tests/e2e/cassettes/phase_41_integration/*.yaml</files>
  <behavior>
    Integration scenarios (5 atomic tests sharing setup fixtures):
    - test_router_resolves_all_paths: imports `resolve` from `vibemix.llm.model_router`; calls each of the 8 paths; asserts SKU + tier per CONTEXT.md table (live_coach STANDARD 3-Flash, debrief FLEX 3-Pro, embedding FLEX Embedding-2, etc.)
    - test_agent_boots_with_validated_config: constructs a real-ish DJCoHostAgent (with mocked genai client) using production _gen_cfg; assert no exception (validates Plan 41-03 + 41-01 compose); follow with synthetic bad config (service_tier=FLEX) — assert LiveCoachConfigError
    - test_cache_does_not_spawn_refresh_loop_task: instantiate GeminiContextCache + EvidenceRegistry wired together (mock client); inspect running asyncio tasks via `asyncio.all_tasks()` — no task named matching `refresh_loop`; mutate registry once; await debounce delay; verify cache.refresh was called via spy
    - test_full_turn_streams_first_sentence_before_completion: VCR cassette of a single Gemini stream returning "Killer drop hits hard. The next track elevates the groove. Lock in." — assert first chunk `yield`-ed contains only the first sentence + arrives before the stream awaitable resolves; second + third sentences yield as they arrive
    - test_embedding_probe_runs_at_boot_and_logs: VCR cassette of probe response; construct LibraryEmbedder; assert `embedding_model_probe` event emitted exactly once with `chosen` field set to one of the two candidates
  </behavior>
  <action>
    Create `tests/e2e/test_phase_41_latency_stack_integration.py`. Use VCR cassettes for any actual Gemini interaction.

    Test fixtures:
    - `phase_41_recorder` — captures emitted events for inspection
    - `phase_41_genai_client` — VCR-recorded genai client (cassette dir `tests/e2e/cassettes/phase_41_integration/`)
    - `phase_41_evidence_registry` — fresh EvidenceRegistry with on_mutation hook wired to a spy

    5 tests above. The "agent boots with validated config" test exercises Plan 41-03's `validate_live_config` in the boot path; the "cache does not spawn refresh_loop" test exercises Plan 41-02's surgery; the "full turn streams first sentence" test exercises Plan 41-04's dual-phase gate; the "embedding probe" test exercises Plan 41-05's probe.

    The "router resolves all paths" test is the catch-all for Plan 41-01.

    Cassettes for the streaming + embedding-probe scenarios: record once via `VCR_RECORD_MODE=new_episodes uv run pytest tests/e2e/test_phase_41_latency_stack_integration.py` against a real GEMINI_API_KEY. Document the record procedure in the test docstring. After record, commit cassettes — CI replays from cassette only.

    Important: if cassette record fails for any specific scenario (e.g., embedding probe call format unexpectedly different), capture the actual failure in the integration report instead of forcing a fake cassette. The integration report is the surface for surfacing pre-deploy surprises.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/e2e/test_phase_41_latency_stack_integration.py -x -v</automated>
  </verify>
  <done>5 integration tests green. VCR cassettes committed under `tests/e2e/cassettes/phase_41_integration/`. All 9 REQ-IDs touched by at least one test in this file (mapping documented in test module docstring).</done>
</task>

<task type="auto">
  <name>Task 3: Integration report — phase-level measurements + assumption resolution</name>
  <files>.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md</files>
  <action>
    Run a synthetic-session replay using the existing eval corpus:
    ```
    uv run python scripts/eval/replay_harness.py \
        --corpus tests/eval/fixtures/synthetic_session/ \
        --print-llm-to-tts-delta \
        --print-cache-hit-rate \
        --print-router-resolves > /tmp/phase_41_replay_output.txt
    ```

    Then compose `.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md` with sections:

    1. **Summary** — One paragraph: did Phase 41 land its 9 REQ-IDs cleanly? Aggregate green/yellow/red verdict.

    2. **REQ-ID coverage** — Table: REQ-ID | Plan | Tests | Status
       - Each LAT-01..09 row checks off the closing plan + relevant test paths

    3. **Measurements**
       - LLM→TTS delta savings: pre-refactor baseline (estimated 1-2s; from research) vs post-refactor measured (from harness output); savings in ms + percent
       - Cache hit rate on synthetic session: X/Y = Z% (verify ≥60% Open Q3 conservative threshold)
       - Router resolve sites: count across src/vibemix/ (proves Plan 41-01 migration completeness)
       - Embedding probe result: which candidate the developer machine resolved to → A1 verification

    4. **Assumption resolutions** (8 assumptions from research)
       - A1 (Embedding 2 GA model ID): RESOLVED → which model ID actually returned
       - A2 (service_tier="flex" no extra account config): RESOLVED via integration test — no extra config needed
       - A3 (Pro supports FLEX tier): RESOLVED via debrief test invocation
       - A4 (sentence boundary regex on EN+TR): RESOLVED via streaming tests; document any Turkish edge cases observed
       - A5 (implicit cache auto-activates ≥1024 tokens): RESOLVED via cache hit rate measurement
       - A6 (LiveKit FallbackAdapter mid-chunk cancel): document observation from Plan 41-04 cancel-on-trailing test; mark as PARTIAL if cancel race surfaces
       - A7 (vibemix.llm package doesn't break agent.config import): RESOLVED via test_router_resolves_all_paths import success
       - A8 (OpenRouter route preserved through router): RESOLVED via Plan 41-01 monkey-patch test

    5. **Pitfall observations** (8 pitfalls)
       - Pitfall 1 (citation period collision): PASSED via test_pitfall_1_citation_period_no_premature_yield
       - Pitfall 2 (callback storm): PASSED via test_burst_of_mutations_only_fires_once_post_burst
       - Pitfall 3 (FLEX on live): PASSED via thinking_gate FLEX rejection test
       - Pitfall 4 (Live API 15-min cap): documented in spike framework (Plan 41-06); not exercised in v3.0 runtime
       - Pitfall 5 (implicit cache floor): pad block preserved per Plan 41-02; cache_hit rate measurement validates
       - Pitfall 6 (model ID preview vs GA): RESOLVED via probe logic (Plan 41-05); A1 verification surfaces actual outcome
       - Pitfall 7 (tts_chain test pinning OpenRouter literal): Plan 41-01 backward-compat re-export handles
       - Pitfall 8 (LiveKit mid-chunk cancel race): document any observation from Plan 41-04 + test_citation_failure_after_head_emits_cancel; mark partial/full mitigation status

    6. **Open Items** (anything that surfaced during execution that warrants follow-up)
       - Format: `- [ ] {item} ({severity}: blocker / nice-to-have)`
       - Examples to look out for: tag DSL surprises (LAT-05), VAD kwarg confirmation for LAT-09 (Plan 41-06 OPERATOR_NOTES), cancellation race specifics

    7. **Phase 41 ship verdict** — single line: GREEN / YELLOW / RED with one-sentence rationale.

    The report is meant for Kaan + future agent review. Be precise about numbers. Cite exact files + line numbers where measurements come from. Cross-link to plan SUMMARYs (e.g., "see 41-04-SUMMARY.md for tag DSL details").
  </action>
  <verify>
    <automated>test -f /Users/ozai/projects/dj-set-ai/.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md &amp;&amp; grep -q "REQ-ID coverage" /Users/ozai/projects/dj-set-ai/.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md &amp;&amp; grep -q "Assumption resolutions" /Users/ozai/projects/dj-set-ai/.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md &amp;&amp; grep -q "Pitfall observations" /Users/ozai/projects/dj-set-ai/.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md &amp;&amp; grep -qE "GREEN|YELLOW|RED" /Users/ozai/projects/dj-set-ai/.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-INTEGRATION-REPORT.md</automated>
  </verify>
  <done>41-INTEGRATION-REPORT.md exists with all 7 sections. Measurements pulled from real harness run. 8 assumptions + 8 pitfalls each have a status line. Verdict line ends GREEN / YELLOW / RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VCR cassettes → integration tests | Cassettes are committed; readers trust the recorded responses match real API contract |
| Synthetic session → measurement aggregation | Replay harness aggregates over a known-good corpus; not real-user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Information Disclosure | VCR cassettes leak API key in headers | mitigate | VCR config (existing v2.1) strips Authorization headers + GEMINI_API_KEY from cassettes; double-check on first record |
| T-41-07-02 | Tampering | Integration report says GREEN but a real pitfall is unmitigated | mitigate | Each pitfall section MUST cite a specific test or measurement; "GREEN" rationale must reference evidence |
| T-41-07-03 | Denial of Service | Harness flag aggregation blocks on huge events.jsonl | accept | Synthetic-session events.jsonl typically <100MB; streaming line read in harness, no full-file load |
</threat_model>

<verification>
- `uv run pytest tests/eval/test_replay_harness_phase_41.py tests/e2e/test_phase_41_latency_stack_integration.py -x -v` → all green
- `uv run python scripts/eval/replay_harness.py --help` → all three new flags documented
- `bash scripts/release/check_no_hardcoded_model.sh` (from Plan 41-01) → still passes
- Full Phase 41 + Phase 40 regression: `uv run pytest tests/ -x --ignore=tests/e2e/test_phase_41_latency_stack_integration.py -q && uv run pytest tests/e2e/test_phase_41_latency_stack_integration.py -x` → clean
- Integration report inspection: every REQ-ID row has a test reference; every assumption + pitfall has a resolution status
</verification>

<success_criteria>
1. `scripts/eval/replay_harness.py` extended with 3 new metric flags + 4 unit tests green.
2. `tests/e2e/test_phase_41_latency_stack_integration.py` exercises all 9 REQ-IDs via VCR-cassette session — 5 tests green.
3. `41-INTEGRATION-REPORT.md` shipped with: REQ-ID coverage / Measurements / Assumption resolutions (8) / Pitfall observations (8) / Open Items / Verdict.
4. LLM→TTS delta measurement recorded against pre-refactor baseline (target 200-400ms savings — verify or note shortfall).
5. Cache hit rate measurement recorded (conservative threshold ≥60% on a 30-turn synthetic session).
6. Embedding 2 probe outcome documented (A1 verification — primary vs fallback).
7. Phase 41 ship verdict: GREEN or YELLOW + documented rationale.
</success_criteria>

<output>
After completion, the `41-INTEGRATION-REPORT.md` IS the output of this plan (its contents serve as the phase-level summary). Additionally create `.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-07-SUMMARY.md` capturing:
- Reference to integration report
- VCR cassette inventory + record-mode timestamp
- Any open follow-ups flagged for Phase 42+
</output>
