---
phase: 41-gemini-sku-upgrade-latency-stack-v2
verified: 2026-05-17T06:09:06Z
status: human_needed
score: 4/6 must-haves verified; 2/6 human_needed
overrides_applied: 0
human_verification:
  - test: "TTFT p95 < 500ms on live coach"
    expected: "Real-DJ-session TTFTMeter logs show p95 < 500ms across ≥2 sessions ≥2 genres (Phase 16 ear-test protocol)"
    why_human: "Runtime perf SC; per memory project_phase_16_kaan_dj_testing, hallucination + perf gate satisfied by Kaan's personal DJ-set testing, not automated harness. TTFTMeter wiring is in place (src/vibemix/runtime/ttft.py + dj_cohost.py:267) but no real-session metrics in repo to confirm p95."
  - test: "3.1 Flash Live spike — verdict box checked"
    expected: "spikes/gemini-3-1-flash-live-music.md status flipped from `engineering-scaffolded` to `verdict-written`; one of [defer-to-v3.x / sealed-no] checkbox marked; recording filed under spikes/recordings/"
    why_human: "Scaffold + verdict template shipped. Per Plan 41-06 + KAAN-ACTION-PROXY §LAT-09, real-DJ-clip 5-min spike + offline listen is Kaan-action discharge under gsd-autonomous fully (memory feedback_autonomous_no_grey_area_pause — defer + continue). Expected discharge time 1-2h."
  - test: "Flex tier billing visible on next batch run + €/mo CI gate under €50"
    expected: "Run library re-index OR eval-corpus replay; confirm Gemini billing dashboard shows FLEX line items; €/mo CI gate green"
    why_human: "ServiceTier.FLEX is wired in _router_config.py for debrief/library_auto_tag/embedding; harness mocks don't exercise live API billing. Confirmation requires real batch run with GEMINI_API_KEY + billing dashboard inspection."
---

# Phase 41: Gemini SKU Upgrade + Latency Stack v2 — Verification Report

**Phase Goal:** Adopt 2026 Gemini deltas — ModelRouter seam, implicit caching, LLM→TTS pipe-through, 3.1 Flash TTS, Embedding 2 GA, Flex tier for batch paths. Target: end-to-end latency 5-10s → 3-5s; perceived latency 0-2s with P40 lookahead offset.
**Verified:** 2026-05-17T06:09:06Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| #   | Truth                                                                                                                              | Status         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ModelRouter config-driven — zero hardcoded `gemini-3-flash`; per-path SKU + tier wired                                              | ✓ VERIFIED     | `src/vibemix/llm/model_router.py` + `_router_config.py` ship. Grep gate `bash scripts/release/check_no_hardcoded_model.sh` exits 0 ("clean — no hardcoded Gemini model literals"). 8 router paths defined with correct tier dispatch (live_coach STANDARD, debrief/library_auto_tag/embedding FLEX, openrouter None sentinel). 10 resolve() call sites in `src/vibemix/`: agent.config (5), debrief.tldr (2), debrief.drills (1), library.embed (1), library.grounding (1 via re-export). Harness `--print-router-resolves` confirms. |
| 2   | TTFT p95 < 500ms on live coach (tracked via existing TTFTMeter)                                                                    | ? HUMAN_NEEDED | TTFTMeter exists in `src/vibemix/runtime/ttft.py`, wired in `dj_cohost.py:267`. No runtime p95 measurement artifact in repo. Per project memory `project_phase_16_kaan_dj_testing`, runtime perf is satisfied by Kaan's personal DJ-set testing (ear-test protocol GATE-05).                                                                                                                                                                                                                                                       |
| 3   | LLM first sentence streams to TTS with measured 200-400ms perceived savings                                                        | ✓ VERIFIED     | Harness reproduces 41-INTEGRATION-REPORT measurement byte-identically: `[llm-to-tts-delta] count=30 mean=299.7ms median=292ms p95=403ms`. Sits inside CONTEXT LAT-04 target band. `_streaming_pipe.py` ships `find_sentence_end` + `passes_head_gate`; `LLMToTTSDeltaMeter` wired at `dj_cohost.py:267`. Synthetic ≠ real-DJ-session, but the wiring + the measured baseline pin the contract. Real-session ear confirmation deferred to Kaan ear-test (covered by SC#2 human_needed slot).                                          |
| 4   | Embedding 2 GA + MRL 768-dim parity                                                                                                 | ✓ VERIFIED     | `EMBEDDING_DIM = 768` locked in `src/vibemix/library/_cosine.py:51`. `_probe_ga_model_id` in `library/embed.py:163` with `EMBEDDING_GA_CANDIDATES` from `_router_config.py` (GA-first, legacy-second). Parity tests green: 5/5 in `test_embeddings_parity.py` + 13/13 in `test_embedding_ga_probe.py`. `output_dimensionality=EMBEDDING_DIM` on embed_content calls (`embed.py:200, 526, 547`).                                                                                                                                     |
| 5   | Flex tier billing visible on next batch run; €/mo CI gate stays under €50                                                          | ? HUMAN_NEEDED | `ServiceTier.FLEX` correctly wired in `_router_config.py` for debrief / debrief_tts / library_auto_tag / embedding paths. Tests pin tier dispatch. Live API billing visibility requires real batch run with GEMINI_API_KEY + dashboard inspection — not a programmatically verifiable artifact.                                                                                                                                                                                                                                    |
| 6   | 3.1 Flash Live spike verdict written — go/no-go on optional v3.x toggle                                                            | ? HUMAN_NEEDED | Spike scaffold + verdict template shipped at `spikes/gemini-3-1-flash-live-music.md`. Status field reads `engineering-scaffolded` (NOT `verdict-written`). All Verdict boxes blank. Per Plan 41-06 + KAAN-ACTION-PROXY §LAT-09 + memory `feedback_autonomous_no_grey_area_pause`, the real-DJ-clip discharge is deliberately Kaan-action and was deferred (not paused) under `gsd-autonomous fully`. Engineering scaffold is the right deliverable for phase-close; verdict fill-in is a separate Kaan-action.                       |

**Score:** 4 / 6 truths verified (3 VERIFIED, 1 VERIFIED via harness with ear-test deferred, 3 HUMAN_NEEDED).

### Required Artifacts

| Artifact                                                       | Expected                                                            | Status     | Details                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------- | ------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/vibemix/llm/model_router.py`                              | `resolve(path) -> (model_id, ServiceTier)` + RouterPathError        | ✓ VERIFIED | 66 lines. Frozen tuple `ROUTER_PATHS`. KeyError-subclassed RouterPathError with full-list error msg. Used in 6 src files. 12 unit tests green.                                                                                                                       |
| `src/vibemix/llm/_router_config.py`                            | Sole literal-bearing `_ROUTES` dict (8 entries)                     | ✓ VERIFIED | All 8 paths shipped per spec. EMBEDDING_GA_CANDIDATES tuple added here per LAT-06 (grep-gate compliant placement).                                                                                                                                                  |
| `src/vibemix/llm/thinking_gate.py`                             | `validate_live_config` + `LiveCoachConfigError`                     | ✓ VERIFIED | 107 lines. Pure callable. Aggregates violations (both thinking_level + service_tier reported together). Wired at 3 call sites: `llm_factory.py:32, llm_factory.py:60, dj_cohost.py:319`. 18 tests green.                                                              |
| `src/vibemix/agent/cache.py`                                   | TTL=3600s, no refresh_loop, refresh() atomic-swap, pad invariant   | ✓ VERIFIED | `GEMINI_CACHE_TTL_S = 3600.0` (line 71). `refresh_loop` method gone (docstring references only). `async def refresh` at line 200. `_CACHE_PAD_BLOCK` preserved (line 88). 18 tests green.                                                                            |
| `src/vibemix/state/evidence_registry.py`                       | on_mutation hook + 5s debounce + 30s min-interval guard             | ✓ VERIFIED | `DEFAULT_MUTATION_DEBOUNCE_S = 5.0` (line 76), `DEFAULT_MIN_REFRESH_INTERVAL_S = 30.0` (line 82). `_schedule_refresh` + `_run_callback` exception-safe. Wired in `__main__.py:674`: `EvidenceRegistry(on_mutation=lambda: cache.refresh())`. 11 tests green.          |
| `src/vibemix/agent/_streaming_pipe.py`                         | bracket-depth sentence boundary + head gate                         | ✓ VERIFIED | `find_sentence_end` at line 84 (state-machine, bracket-depth-aware — Pitfall 1 mitigation). `passes_head_gate` at line 141. 16 unit tests green.                                                                                                                     |
| `src/vibemix/runtime/llm_to_tts_delta_meter.py`                | `LLMToTTSDeltaMeter` class                                          | ✓ VERIFIED | Class at line 42. Wired in `dj_cohost.py:267` (parallel to TTFTMeter pattern). 7 tests green.                                                                                                                                                                        |
| `src/vibemix/library/embed.py` GA probe                        | `_probe_ga_model_id` + `LibraryEmbedder` (768-dim MRL)              | ✓ VERIFIED | Probe at line 163. Consumes `EMBEDDING_GA_CANDIDATES` from `_router_config.py` (grep-gate-compliant). `LibraryEmbedder` at line 254. `output_dimensionality=EMBEDDING_DIM` (768).                                                                                     |
| `scripts/release/check_no_hardcoded_model.sh`                  | Bash grep gate scoped to src/vibemix/                                | ✓ VERIFIED | Executable. Exit 0 on current tree. POSIX extended regex matches the 5 banned patterns. Allowlists only `_router_config.py`.                                                                                                                                         |
| `.github/workflows/model-literal-check.yml`                    | CI workflow wrapping bash gate                                      | ✓ VERIFIED | Exists; mirrors `no-api-key-surface.yml` pattern.                                                                                                                                                                                                                    |
| `spikes/gemini-3-1-flash-live-music.md`                        | Spike verdict file                                                  | ⚠️ STUB    | File exists (107 lines, full template). Status: `engineering-scaffolded`. Verdict box NOT checked. Per Plan 41-06 + memory `feedback_autonomous_no_grey_area_pause`, the empty verdict is intentional (Kaan-action discharge). Scaffold-complete ≠ verdict-written. |
| `scripts/library/migrate_embeddings_2.py`                      | Migration script (audit / dry-run / re-embed-all)                   | ✓ VERIFIED | Exists. Plan 41-05 documents the 3 modes.                                                                                                                                                                                                                            |
| `docs/prompts/tts-tags.md`                                     | Public-facing audio-tag DSL doc                                     | ✓ VERIFIED | Exists. `TTS_TAGS` tuple + `TTS_TAG_DSL_BLOCK` in `src/vibemix/prompts/matrix.py:162-171`.                                                                                                                                                                            |

### Key Link Verification

| From                                                          | To                                                            | Via                                                              | Status      | Details                                                                                                                                                                                                                          |
| ------------------------------------------------------------- | ------------------------------------------------------------- | ---------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/vibemix/agent/config.py`                                  | `model_router.resolve`                                        | module-load-time `LLM_MODEL = resolve("live_coach")[0]`           | ✓ WIRED     | Lines 25-39 — 5 router-derived constants (LLM_MODEL, TTS_MODEL, TTS_FALLBACK_MODEL, OPENROUTER_TTS_MODEL, LIVE_COACH_SERVICE_TIER).                                                                                              |
| `src/vibemix/debrief/tldr.py`                                  | `model_router.resolve`                                        | `DEBRIEF_TLDR_MODEL = resolve("debrief")[0]`                     | ✓ WIRED     | Lines 49-50.                                                                                                                                                                                                                     |
| `src/vibemix/library/embed.py`                                 | `model_router.resolve`                                        | `GEMINI_EMBEDDING_MODEL = resolve("embedding")[0]`               | ✓ WIRED     | Line 71. Plus `EMBEDDING_GA_CANDIDATES` re-exported from `_router_config.py`.                                                                                                                                                    |
| `.github/workflows/model-literal-check.yml`                    | `scripts/release/check_no_hardcoded_model.sh`                 | GH Actions run step                                              | ✓ WIRED     | Workflow file exists; uses pattern mirroring `no-api-key-surface.yml`.                                                                                                                                                           |
| `src/vibemix/agent/llm_factory.py`                             | `validate_live_config`                                        | module-level call before LLM build                               | ✓ WIRED     | 2 call sites (lines 32, 60) — direct + proxy paths.                                                                                                                                                                              |
| `src/vibemix/agent/dj_cohost.py:__init__`                      | `validate_live_config`                                        | agent-init validation of `_gen_cfg`                              | ✓ WIRED     | Line 319.                                                                                                                                                                                                                        |
| `src/vibemix/state/evidence_registry.py:mutate/write/register` | `src/vibemix/agent/cache.py:refresh`                          | `on_mutation_callback` (debounced, cancel-and-reschedule)         | ✓ WIRED     | `EvidenceRegistry(on_mutation=lambda: cache.refresh())` in `__main__.py:674`. `_schedule_refresh` at line 357. Burst test pins single fire per burst.                                                                            |
| `src/vibemix/agent/dj_cohost.py:llm_node`                      | `_streaming_pipe.find_sentence_end`                           | per-chunk accumulator boundary scan                              | ✓ WIRED     | Streaming pipe block bracketed by `=== Plan 41-04 streaming pipe-through (LAT-04) ===` comments. 11 integration tests green.                                                                                                     |
| `src/vibemix/agent/dj_cohost.py:llm_node`                      | `LLMToTTSDeltaMeter.record_first_sentence`                    | called at head-emission point                                    | ✓ WIRED     | Meter instance at line 267. `record_first_sentence` + `log_turn` wired per plan.                                                                                                                                                 |
| `scripts/eval/replay_harness.py`                               | events.jsonl `llm_to_tts_delta_ms`                            | replay-harness aggregation flag                                  | ✓ WIRED     | `--print-llm-to-tts-delta` flag reproduces (mean=299.7ms / p95=403ms) byte-identically.                                                                                                                                          |

### Data-Flow Trace (Level 4)

| Artifact                                                       | Data Variable                       | Source                                                                          | Produces Real Data            | Status                                                  |
| -------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------- |
| `model_router.resolve()`                                        | `_ROUTES[path]` dict lookup          | `_router_config._ROUTES` literal                                                | Yes — real tuples              | ✓ FLOWING                                               |
| `EvidenceRegistry.on_mutation` callback                         | `cache.refresh` coroutine            | `__main__.py:674` lambda wires it                                               | Yes — real awaitable           | ✓ FLOWING                                               |
| `LLMToTTSDeltaMeter.delta_ms()`                                 | per-turn measurement                 | `start_turn` / `record_first_sentence` calls in dj_cohost.py                    | Yes — harness reproduces 30 samples (mean=299.7ms) | ✓ FLOWING                                               |
| `_probe_ga_model_id` candidate iteration                        | live API embed_content call          | `client.aio.models.embed_content(model=candidate, …)`                           | Synthetic via mocked tests; real-API run deferred to Phase 42 ear-test calibration (per INTEGRATION-REPORT §3.4 A1 PARTIAL) | ⚠️ STATIC (test path) / runtime path verified by mocked integration tests |
| `spikes/gemini-3-1-flash-live-music.md` verdict                 | verdict box                          | Kaan operator (Kaan-action discharge per §LAT-09)                                | No — template only             | ⚠️ HOLLOW_PROP (intentional — Kaan-action deferred)     |

### Behavioral Spot-Checks

| Behavior                                                       | Command                                                                                       | Result                                                                                          | Status   |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------- |
| CI grep gate runs clean                                         | `bash scripts/release/check_no_hardcoded_model.sh ; echo $?`                                  | "clean — no hardcoded Gemini model literals"; exit 0                                            | ✓ PASS   |
| Replay harness reproduces INTEGRATION-REPORT measurements       | `python scripts/eval/replay_harness.py --corpus tests/eval/fixtures/phase_41_synthetic --judges noop --print-llm-to-tts-delta --print-cache-hit-rate --print-router-resolves` | `[llm-to-tts-delta] count=30 mean=299.7ms median=292ms p95=403ms`; `[cache-hit-rate] 25/30 = 83.3%`; `[router-resolves] 10 call sites` — all byte-identical to report | ✓ PASS   |
| LLM tests green                                                 | `uv run pytest tests/llm/`                                                                    | 45 passed, 1 skipped (VCR-record-mode)                                                          | ✓ PASS   |
| Cache + repo tests green                                        | `uv run pytest tests/agent/test_cache.py tests/agent/test_cache_mutation_refresh.py tests/agent/test_dj_cohost_cache_hit.py tests/repo/test_model_literal_gate.py` | 43 passed                                                                                       | ✓ PASS   |
| Embedding + streaming pipe tests green                           | `uv run pytest tests/library/test_embedding_ga_probe.py tests/library/test_embeddings_parity.py tests/agent/test_dj_cohost_streaming_pipe.py tests/runtime/test_llm_to_tts_delta_meter.py tests/agent/test_streaming_pipe_helpers.py` | 52 passed                                                                                       | ✓ PASS   |
| Phase 41 integration + harness tests green                       | `uv run pytest tests/e2e/test_phase_41_latency_stack_integration.py tests/eval/test_replay_harness_phase_41.py` | 32 passed                                                                                       | ✓ PASS   |

### Requirements Coverage (LAT-01..LAT-09)

| Requirement | Source Plan                  | Description                                                                                              | Status        | Evidence                                                                                                                                                                                          |
| ----------- | ---------------------------- | -------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LAT-01      | 41-01                        | ModelRouter config-driven seam; CI grep gate; per-path SKU mapping                                       | ✓ SATISFIED   | `model_router.py` + `_router_config.py` + `check_no_hardcoded_model.sh` + workflow. 10 call sites confirmed via harness flag.                                                                     |
| LAT-02      | 41-02                        | Implicit caching default-on for static prefix; strip explicit cache-create                               | ✓ SATISFIED   | `padded_body()` retained for Pitfall 5 floor; wall-clock `refresh_loop` deleted; `cache_hit` telemetry. Harness reports 83.3% hit rate on synthetic corpus, ≥60% Open Q3 threshold cleared.       |
| LAT-03      | 41-02                        | TTL 60min; mutation-driven refresh; remove 4-min refresh machinery                                       | ✓ SATISFIED   | `GEMINI_CACHE_TTL_S = 3600.0`; `EvidenceRegistry.on_mutation` with 5s debounce + 30s guard; `cache_hit` events to `events.jsonl`.                                                                  |
| LAT-04      | 41-04                        | LLM→TTS streaming pipe-through                                                                            | ✓ SATISFIED   | `_streaming_pipe.find_sentence_end` (bracket-depth-aware — Pitfall 1) + `LLMToTTSDeltaMeter`. Measured median 292ms / p95 403ms — inside CONTEXT LAT-04 target band.                              |
| LAT-05      | 41-04                        | Migration to Gemini 3.1 Flash TTS + expressive-tag DSL                                                    | ✓ SATISFIED   | Router path `live_coach_tts` → `gemini-3.1-flash-tts-preview`. 6-tag DSL in `prompts/matrix.py`. Public-facing `docs/prompts/tts-tags.md`. OpenRouter Achird OPUS fallback preserved.              |
| LAT-06      | 41-05                        | Embedding 2 GA migration + MRL 768-dim                                                                    | ✓ SATISFIED   | `EMBEDDING_DIM = 768`; `_probe_ga_model_id` + `EMBEDDING_GA_CANDIDATES`; parity tests + migration script (`audit` / `dry-run` / `re-embed-all`).                                                  |
| LAT-07      | 41-01                        | Flex inference tier routing for batch paths                                                                | ✓ SATISFIED   | `ServiceTier.FLEX` wired for debrief / debrief_tts / library_auto_tag / embedding paths. STANDARD on live_coach + live_coach_tts. Live-billing dashboard inspection: see SC#5 human_needed.        |
| LAT-08      | 41-03                        | `thinking_level=MINIMAL` enforced on live path                                                           | ✓ SATISFIED   | `validate_live_config` at 3 call sites; aggregates `thinking_level` + `service_tier` violations. Pitfall 3 FLEX-on-live defense in same module. 18 tests.                                          |
| LAT-09      | 41-06                        | 1-2 day Gemini 3.1 Flash Live spike; verdict written                                                      | ? NEEDS HUMAN | Scaffold + verdict template + KAAN-ACTION-PROXY §LAT-09 runbook shipped. Status `engineering-scaffolded`; verdict box not checked. Per autonomous mode + memory, real-DJ-clip session is Kaan-action — defer + continue.   |

**Coverage:** 8 / 9 LAT requirements programmatically SATISFIED. 1 (LAT-09) deferred to Kaan-action per `gsd-autonomous fully` discharge protocol; scaffolding shipped.

### Anti-Patterns Found

| File                                                         | Line | Pattern                                                | Severity | Impact                                                                                                                                                                                            |
| ------------------------------------------------------------ | ---- | ------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `spikes/gemini-3-1-flash-live-music.md`                      | 8, 36-44, 50-65, 84-85, 92-97, 107 | Template placeholders (`___`, blank verdict, blank Rationale, blank Sign-off) | ℹ️ Info  | Intentional — scaffold for Kaan-action discharge (Plan 41-06 + KAAN-ACTION-PROXY §LAT-09). Not a stub of shipped runtime code. Status field accurately reads `engineering-scaffolded` to signal incompleteness. |
| `src/vibemix/agent/cache.py`                                 | various | Docstring mentions of removed `refresh_loop`            | ℹ️ Info  | Informational — explains the Plan 41-02 cleanup. Not a debt marker.                                                                                                                              |

No `TBD` / `FIXME` / `XXX` markers found in Phase-41-modified production files. No unreferenced debt markers. No stub code in runtime paths.

### Human Verification Required

#### 1. TTFT p95 < 500ms — real DJ session ear-test

**Test:** Run vibemix end-to-end through ≥2 real DJ sessions across ≥2 genres (per Phase 16 / GATE-05 ear-test protocol). Inspect `events.jsonl` `ttft_ms` events for that session; compute p95 across all turns.
**Expected:** p95 < 500ms; no perceived "AI thinking" delay between event and first audio.
**Why human:** Per memory `project_phase_16_kaan_dj_testing`, runtime perf gate is satisfied by Kaan's personal DJ-set testing, not automated harness. TTFTMeter wiring is in place (`runtime/ttft.py` + `dj_cohost.py:267`); no real-session p95 artifact in repo.

#### 2. 3.1 Flash Live spike — verdict box checked

**Test:** Set `GEMINI_API_KEY`, run `python -m spikes.scripts.run_live_spike --duration-s 300` against a real 5-min DJ clip with 2 transitions + 1 phrase shift. Listen to `spike_<UTC>.wav` offline. Fill verdict template per KAAN-ACTION-PROXY §LAT-09. Flip status to `verdict-written`.
**Expected:** One of [defer-to-v3.x toggle / sealed-no] checkbox marked; all 6 H2 sections filled; recording filed under `spikes/recordings/`.
**Why human:** Per Plan 41-06 + memory `feedback_autonomous_no_grey_area_pause`, the real-DJ-clip discharge is deliberately Kaan-action under `gsd-autonomous fully` (defer + continue). Engineering scaffold is the right phase-close deliverable; verdict fill-in is a separate Kaan-action ~1-2h discharge.

#### 3. Flex tier billing visible + €/mo CI gate

**Test:** Trigger a real batch run (library re-index via `scripts/library/migrate_embeddings_2.py --re-embed-all` OR eval-corpus replay with non-noop judge). Check Gemini billing dashboard for FLEX line items. Confirm €/mo CI gate stays under €50.
**Expected:** Gemini dashboard shows FLEX-tier usage on debrief / library / embedding paths. Spend < €50/mo.
**Why human:** ServiceTier.FLEX is correctly wired in `_router_config.py` and unit-tested. Live-API billing visibility requires real API spend + dashboard inspection — not a programmatically verifiable artifact.

### Deferred Items Summary

Per `gsd-autonomous fully` autonomy mode and project memory `feedback_autonomous_no_grey_area_pause`, the following items are intentionally deferred to human discharge — they do NOT block phase close:

- **LAT-09 verdict fill-in** — Kaan-action per KAAN-ACTION-PROXY §LAT-09. Scaffold + template shipped (Plan 41-06).
- **TTFT p95 real-session ear-test** — Kaan-action per Phase 16 protocol (GATE-05). TTFTMeter wired and logging.
- **FLEX tier live-billing confirmation** — first batch run with `GEMINI_API_KEY`; router wiring + tier dispatch proven by unit tests.

These three items are surfaced as `human_verification` slots in the frontmatter so the orchestrator (Phase 45 SHIP / Phase 16 ear-test) picks them up at the appropriate gate.

### Gaps Summary

**No engineering gaps.** All 7 plans (41-01..41-07) ship their planned artifacts; CI grep gate passes; 4 of 6 ROADMAP Success Criteria fully programmatically verified; remaining 2 require human ear-test / Kaan-action discharge per project autonomy contract.

Integration report `41-INTEGRATION-REPORT.md` claims (verdict GREEN; LLM→TTS mean=299.7ms; cache-hit 83.3%; 10 router resolve sites) reproduce byte-identically when the harness is run from the repo root. No SUMMARY.md narrative was found to contradict codebase evidence.

The phase achieves its engineering goal: ModelRouter seam locks SKU literals behind a CI gate; cache cleanup removes wall-clock refresh; LLM→TTS streaming pipe-through with bracket-depth-aware boundary detection shipped + measured; 3.1 Flash TTS audio-tag DSL shipped + documented; Embedding 2 GA probe + 768-dim MRL parity shipped; Flex tier wired for batch paths; thinking_level=MINIMAL + FLEX-on-live defense at 3 call sites; Live spike scaffold + Kaan-discharge runbook shipped.

---

_Verified: 2026-05-17T06:09:06Z_
_Verifier: Claude (gsd-verifier)_
