---
phase: 41-gemini-sku-upgrade-latency-stack-v2
plan: 06
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - spikes/gemini-3-1-flash-live-music.md
  - spikes/scripts/__init__.py
  - spikes/scripts/run_live_spike.py
  - spikes/scripts/recording_harness.py
  - spikes/recordings/.gitkeep
  - tests/repo/test_live_spike_scaffold.py
  - KAAN-ACTION-PROXY.md
autonomous: true
requirements: [LAT-09]
must_haves:
  truths:
    - "Verdict template scaffold at spikes/gemini-3-1-flash-live-music.md ships with: Setup / Measurements / Anti-Hallucination Behavior / Session Cap Workaround Status / Verdict / Rationale sections (per RESEARCH.md Live Spike Framework)"
    - "spikes/scripts/run_live_spike.py is a runnable script — ports cohost_lk.py:1670+ main() logic into a clean spike entry point (POC file is READ-ONLY per Phase 37-06 immutability gate; this is a fresh port, not an import)"
    - "spikes/scripts/recording_harness.py captures Gemini Live audio output to a wav file for offline analysis"
    - "Kaan-action discharge note in KAAN-ACTION-PROXY.md §LAT-09 documents: how to run the spike on a real DJ clip, what to fill in the verdict template, expected investigation time"
    - "The spike does NOT change default cascade behavior — gemini-3.1-flash-live-preview MUST NOT appear in src/vibemix/ (CI grep gate from Plan 41-01 catches accidental default-flip); it lives ONLY in spikes/"
    - "Spike script is non-blocking — if Kaan-action time isn't available autonomously, the framework + template + script + harness still ship green; the actual investigation defers"
  artifacts:
    - path: "spikes/gemini-3-1-flash-live-music.md"
      provides: "Verdict template with all 6 sections pre-structured; status field starts at engineering-scaffolded"
    - path: "spikes/scripts/run_live_spike.py"
      provides: "Runnable script wiring Gemini 3.1 Flash Live RealtimeModel + audio capture; fresh port from cohost_lk.py:1670+"
    - path: "spikes/scripts/recording_harness.py"
      provides: "Audio capture utility — writes Gemini Live output to spikes/recordings/<timestamp>.wav"
    - path: "tests/repo/test_live_spike_scaffold.py"
      provides: "Sanity tests: scaffolding files exist + template has required sections + spike script imports cleanly + CI gate enforces gemini-3.1-flash-live-preview NOT in src/vibemix/"
    - path: "KAAN-ACTION-PROXY.md"
      provides: "Section §LAT-09 — Kaan-action discharge runbook"
  key_links:
    - from: "spikes/scripts/run_live_spike.py"
      to: "livekit.plugins.google.realtime.RealtimeModel"
      via: "session.start + push_audio pipeline"
      pattern: "RealtimeModel"
    - from: "tests/repo/test_live_spike_scaffold.py"
      to: "scripts/release/check_no_hardcoded_model.sh"
      via: "positive assertion that the live model literal is NOT in src/vibemix/"
      pattern: "gemini-3.1-flash-live-preview"
---

<objective>
Land the Gemini 3.1 Flash Live spike framework — engineering scaffolding only per CONTEXT.md "Live spike = engineering scaffold (verdict template + spikes/scripts/run_live_spike.py cherry-picked from cohost_lk.py:1670+ + recording harness + discharge note). Real-DJ-clip investigation is Kaan-action discharge."

Scope per locked CONTEXT.md:
- Verdict template (`spikes/gemini-3-1-flash-live-music.md`) with all 6 sections pre-structured
- Runnable spike script (`spikes/scripts/run_live_spike.py`) that PORTS `cohost_lk.py:1670+ main()` logic — the POC file is READ-ONLY (Phase 37-06 immutability gate) so this is a CLEAN PORT
- Recording harness (`spikes/scripts/recording_harness.py`)
- Kaan-action discharge note in `KAAN-ACTION-PROXY.md` §LAT-09
- CI gate enforcement: `gemini-3.1-flash-live-preview` MUST NOT appear in `src/vibemix/`

Per CONTEXT.md: "default cascade unchanged for v3.0 regardless of spike outcome". This plan ships investigation INFRASTRUCTURE only.

Output: Verdict scaffold + spike script + recording harness + Kaan-action runbook + scaffold sanity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-CONTEXT.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-RESEARCH.md

@cohost_lk.py
@KAAN-ACTION-LEGAL.md

<interfaces>
POC source-of-truth (READ-ONLY — Phase 37-06 immutability gate):
- `cohost_lk.py` lines 1670+: `main()` establishes Gemini Live via `livekit.plugins.google.realtime.RealtimeModel`; session lifecycle; 48kHz audio push; ~1fps screen push; trigger detection

Port (clean new file): `spikes/scripts/run_live_spike.py` does NOT import from `cohost_lk.py`. Copies the architecture into a standalone spike entry point. Stripped to minimum viable: Live session + audio push + audio output capture. NO mascot, NO MIDI, NO event detector — pure latency + grounding observation.

KAAN-ACTION-LEGAL.md pattern (existing template):
- Section per discharge item: `## §<ID> — <title>` with sub-sections Why / How to discharge / Expected time / Acceptance criteria
- `KAAN-ACTION-PROXY.md` may not exist yet — create it if absent with a brief header explaining "Proxy-tier discharge items: not legal-capacity blocked (vs KAAN-ACTION-LEGAL.md); just require operator time/attention that wasn't autonomously available during planning execution."

Spike model:
- `gemini-3.1-flash-live-preview` — model ID lives ONLY in spikes/
- VAD: `realtimeInputConfig.automaticActivityDetection: low` (music-tolerant)
- Proactive Audio: enabled

CI gate boundary (from Plan 41-01):
- `scripts/release/check_no_hardcoded_model.sh` scopes to `src/vibemix/**/*.py`
- `spikes/` excluded from scope — literal in spike script is allowed
- This plan ADDS pytest sanity (`tests/repo/test_live_spike_scaffold.py`) that explicitly asserts the literal is allowed in spikes/ AND rejected in src/vibemix/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verdict template + recording harness scaffolding</name>
  <files>spikes/gemini-3-1-flash-live-music.md, spikes/scripts/__init__.py, spikes/scripts/recording_harness.py, spikes/recordings/.gitkeep</files>
  <action>
    Create `spikes/` directory if absent.

    Create `spikes/gemini-3-1-flash-live-music.md` per RESEARCH.md Live Spike Framework verdict template — copy the structure verbatim from research, with:
    - Status field at top: `**Status:** engineering-scaffolded` (subsequent states: kaan-action-discharge → verdict-written)
    - Run date field empty: `**Run date:** _____` (TBD)
    - Operator field: `**Operator:** Kaan (real-DJ-ear required)`
    - All 6 sections present as H2 headers: Setup / Measurements / Anti-Hallucination Behavior / Session Cap Workaround Status / Verdict / Rationale
    - Each measurement field is a fillable blank (e.g., `- TTFT (first audio chunk after event): ___ ms (target: <500ms)`)
    - Each verdict checkbox starts unchecked: `- [ ] **defer-to-v3.x toggle** — ...` / `- [ ] **sealed-no** — ...`

    Add top-of-file note: "This is a scaffolded verdict template — Phase 41 LAT-09 ships the framework; the real-DJ-clip session that fills the blanks is a Kaan-action discharge per KAAN-ACTION-PROXY.md §LAT-09. Until that session runs, status stays at engineering-scaffolded."

    Create `spikes/scripts/__init__.py` (empty — makes the package importable).

    Create `spikes/scripts/recording_harness.py`:
    - `class RecordingHarness` with methods:
      - `__init__(self, output_path: Path, sample_rate: int = 24000)` — sets up output wav using stdlib `wave`
      - `push_audio(self, pcm_bytes: bytes) -> None` — append 16-bit PCM
      - `close(self) -> None` — finalize wav header
    - Defaults: 16-bit PCM mono, 24kHz (typical Gemini Live audio output; the spike operator confirms format on first real run)
    - Pure stdlib (no extra deps)

    Create `spikes/recordings/.gitkeep` to commit the empty directory.
  </action>
  <verify>
    <automated>test -f /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "engineering-scaffolded" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "## Setup" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "## Measurements" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "## Anti-Hallucination Behavior" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "## Session Cap Workaround Status" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "## Verdict" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; grep -q "## Rationale" /Users/ozai/projects/dj-set-ai/spikes/gemini-3-1-flash-live-music.md &amp;&amp; test -f /Users/ozai/projects/dj-set-ai/spikes/scripts/recording_harness.py &amp;&amp; test -d /Users/ozai/projects/dj-set-ai/spikes/recordings</automated>
  </verify>
  <done>Verdict template exists with all 6 required sections + status field + measurement blanks. Recording harness ships with documented API. spikes/recordings/ committed via .gitkeep.</done>
</task>

<task type="auto">
  <name>Task 2: Spike runner — port cohost_lk.py:1670+ into clean entry point</name>
  <files>spikes/scripts/run_live_spike.py</files>
  <action>
    Create `spikes/scripts/run_live_spike.py` as a FRESH Python file. POC `cohost_lk.py:1670+` is READ-ONLY (Phase 37-06 immutability gate) — DO NOT modify or import from it. Port the architecture as a clean new file.

    Required elements (lifted as architecture; written fresh):

    Header docstring + banner comment:
    "PHASE 41 LAT-09 SPIKE — Gemini 3.1 Flash Live music co-host investigation. This is engineering scaffolding. The real-DJ-clip session that produces the verdict at spikes/gemini-3-1-flash-live-music.md is a Kaan-action discharge per KAAN-ACTION-PROXY.md §LAT-09. Default cascade behavior in src/vibemix/ is UNCHANGED regardless of spike outcome — this script lives in spikes/ only."

    Imports:
    - asyncio, os, argparse, json
    - pathlib.Path, datetime.datetime
    - livekit.rtc, livekit.agents (AgentSession or equivalent v1.5.8 entry)
    - livekit.plugins.google.realtime.RealtimeModel
    - spikes.scripts.recording_harness.RecordingHarness

    Constants:
    - `SPIKE_MODEL_ID = "gemini-3.1-flash-live-preview"`
    - `SAMPLE_RATE_IN = 48000` (BlackHole)
    - `SAMPLE_RATE_OUT = 24000` (Gemini Live output)

    `async def main(args)`:
    - Validate GEMINI_API_KEY — if absent, print informational message ("Spike requires GEMINI_API_KEY + real audio source — see KAAN-ACTION-PROXY.md §LAT-09") and `return 0` (NOT raise — lets the smoke test in Task 4 confirm the script imports + parses CLI cleanly without network calls)
    - Construct `RealtimeModel(model=SPIKE_MODEL_ID, ...)` with VAD config `low` + Proactive Audio enabled — name the kwargs in comments and reference the livekit-plugins-google v1.5.8 API; if exact kwarg names need tuning at spike-run time, document inline with a `# OPERATOR NOTE:` comment
    - Open session
    - Set up audio I/O via sounddevice (port BlackHole input pattern from cohost_lk minimal): 48kHz input → push to session as `rtc.AudioFrame`
    - Set up output capture via `RecordingHarness` writing to `spikes/recordings/spike_<UTC-timestamp>.wav`
    - Log latency markers: session-open timestamp, first audio out chunk timestamp → write to `spikes/recordings/spike_<UTC-timestamp>.metrics.json`
    - Run for configurable duration (default 60s; CLI arg `--duration-s`); session closed cleanly on exit

    CLI parsing via stdlib argparse:
    - `--duration-s INT` (default 60)
    - `--output-dir PATH` (default spikes/recordings)
    - `--screen-capture` (flag, default False — keep spike minimal; flag exists for future expansion)

    `if __name__ == "__main__":` runs `asyncio.run(main(args))`

    Module-level `OPERATOR_NOTES` constant (string) listing things the human operator should verify when actually running the spike: VAD kwarg names against installed livekit-plugins-google version, Proactive Audio enable mechanism, session-cap handling for runs > 15min.

    The script MUST exit cleanly (0) without GEMINI_API_KEY set — this is the "smoke-import" path that CI / scaffold tests use to confirm the file is well-formed without spending API quota.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run python -c "import importlib.util; spec = importlib.util.spec_from_file_location('spike', 'spikes/scripts/run_live_spike.py'); mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod); assert mod.SPIKE_MODEL_ID == 'gemini-3.1-flash-live-preview'; print('OK')" &amp;&amp; uv run python -m spikes.scripts.run_live_spike --help 2&gt;&amp;1 | grep -q "duration-s"</automated>
  </verify>
  <done>run_live_spike.py imports cleanly. CLI --help renders. `SPIKE_MODEL_ID` constant verified. Script runs to exit-0 without GEMINI_API_KEY (smoke-import path).</done>
</task>

<task type="auto">
  <name>Task 3: Kaan-action discharge runbook</name>
  <files>KAAN-ACTION-PROXY.md</files>
  <action>
    Check if `KAAN-ACTION-PROXY.md` exists at repo root. If not, create it with a brief header:

    ```
    # KAAN-ACTION-PROXY.md

    Proxy-tier discharge items: not legal-capacity blocked (vs KAAN-ACTION-LEGAL.md);
    just require operator time/attention that wasn't autonomously available during
    planning execution. Each item ships engineering scaffolding green; the discharge
    is operator-driven.

    ---
    ```

    Append (or insert in the appropriate alphabetical / phase-order location) the §LAT-09 section:

    ```
    ## §LAT-09 — Gemini 3.1 Flash Live spike investigation

    **Status:** scaffolded; discharge pending
    **Scaffold landed:** Phase 41 Plan 41-06 (2026-05-16)
    **Verdict file:** spikes/gemini-3-1-flash-live-music.md

    ### Why
    CONTEXT.md locks: "default cascade unchanged for v3.0 regardless of spike
    outcome." The spike validates whether Gemini 3.1 Flash Live + Proactive Audio
    is music-grounding-suitable enough to become a v3.x opt-in toggle. The
    research framework + verdict template are scaffolded; only a real DJ clip
    + Kaan's ear produces the verdict.

    ### How to discharge

    1. Ensure GEMINI_API_KEY is set in environment.
    2. Connect a real DJ source (djay Pro / Rekordbox / Mixxx) feeding BlackHole
       at 48kHz — same pre-flight as a normal vibemix session.
    3. Cue up a 5-min representative DJ clip: techno around 124 BPM, includes
       2 transitions + 1 phrase shift (matches research clip recommendation).
    4. Run:
       ```
       python -m spikes.scripts.run_live_spike --duration-s 300
       ```
       Output: spikes/recordings/spike_<UTC-timestamp>.wav + .metrics.json
    5. Listen to the recording offline. Note hallucinations, grounding failures,
       feel of latency vs cascade baseline. Compare against the v4 cascade
       "harikaydı" feel.
    6. Fill in spikes/gemini-3-1-flash-live-music.md:
       - Measurements section: paste numbers from .metrics.json
       - Anti-Hallucination Behavior: verbatim transcript samples
       - Session Cap Workaround Status: did session hit 15-min cap? — relevant
         only if running > 15min duration
       - Verdict: check ONE box (defer-to-v3.x toggle OR sealed-no)
       - Rationale: 2-3 sentence ear notes
    7. Flip status field from `engineering-scaffolded` →  `verdict-written`.
    8. Commit verdict file.

    ### Expected time
    1-2 hours total (5min spike + 30min offline listen + 30min verdict writeup).

    ### Acceptance criteria
    - spikes/gemini-3-1-flash-live-music.md status field == `verdict-written`
    - All 6 sections filled (no blanks)
    - Verdict box checked (exactly one of two options)
    - Recording file committed under spikes/recordings/ (git-LFS if > 50MB)
    ```

    If `KAAN-ACTION-PROXY.md` exists, append this §LAT-09 section without disturbing existing content.
  </action>
  <verify>
    <automated>test -f /Users/ozai/projects/dj-set-ai/KAAN-ACTION-PROXY.md &amp;&amp; grep -q "§LAT-09" /Users/ozai/projects/dj-set-ai/KAAN-ACTION-PROXY.md &amp;&amp; grep -q "run_live_spike" /Users/ozai/projects/dj-set-ai/KAAN-ACTION-PROXY.md &amp;&amp; grep -q "verdict-written" /Users/ozai/projects/dj-set-ai/KAAN-ACTION-PROXY.md</automated>
  </verify>
  <done>KAAN-ACTION-PROXY.md exists with §LAT-09 section. Runbook documents discharge steps + expected time + acceptance criteria. Operator can follow without additional context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Scaffold sanity tests + CI gate boundary assertion</name>
  <files>tests/repo/test_live_spike_scaffold.py</files>
  <behavior>
    - test_verdict_template_exists_with_all_sections: file at spikes/gemini-3-1-flash-live-music.md exists; reads all 6 required section headers via regex
    - test_verdict_template_status_starts_engineering_scaffolded: file contains `**Status:** engineering-scaffolded`
    - test_recording_harness_module_importable: `import spikes.scripts.recording_harness` succeeds; class `RecordingHarness` callable
    - test_run_live_spike_module_importable: `import spikes.scripts.run_live_spike` succeeds; module-level `SPIKE_MODEL_ID == "gemini-3.1-flash-live-preview"`
    - test_run_live_spike_help_renders: subprocess invokes `python -m spikes.scripts.run_live_spike --help`; exit code 0; stdout contains "duration-s"
    - test_run_live_spike_exits_cleanly_without_api_key: subprocess with GEMINI_API_KEY unset; exit code 0; stderr/stdout contains "GEMINI_API_KEY" hint
    - test_kaan_action_proxy_has_lat09: file at KAAN-ACTION-PROXY.md exists; contains `§LAT-09` header + step-by-step discharge instructions
    - test_live_model_id_NOT_in_src_vibemix: glob src/vibemix/**/*.py; assert literal `gemini-3.1-flash-live-preview` does NOT appear in any matched file (positive defense — CI gate boundary check)
    - test_live_model_id_allowed_in_spikes: glob spikes/**/*.py; assert literal IS present in run_live_spike.py (proves the spike script actually carries the literal — would fail if someone gutted the spike accidentally)
  </behavior>
  <action>
    Create `tests/repo/test_live_spike_scaffold.py`:
    - pytest-only; no external deps beyond subprocess + pathlib + re
    - 9 cases above
    - For the "exits cleanly without API key" subprocess test: use `env={**os.environ, "GEMINI_API_KEY": ""}` (or `pop("GEMINI_API_KEY", None)` from a copied env dict) to ensure absence; assert exit code 0; capture stdout+stderr and check for the informational message

    These tests run in standard CI; they protect against:
    - Accidental loss of the spike framework (someone deleting `spikes/`)
    - Accidental promotion of the spike model to src/vibemix/ (the literal-boundary check)
    - Drift between the spike script entry point and the documented Kaan-action runbook
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/repo/test_live_spike_scaffold.py -x -v</automated>
  </verify>
  <done>9 scaffold-sanity tests green. CI gate boundary assertion (positive: spike literal allowed in spikes/, rejected in src/vibemix/) green. Runbook structure pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spike script → Gemini Live API | Real API usage during spike run; consumes quota |
| spikes/ directory → src/vibemix/ | Spike artifacts must stay isolated; live model literal must NOT leak to runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Tampering | Spike model literal `gemini-3.1-flash-live-preview` accidentally pasted into src/vibemix/ at a future date | mitigate | Plan 41-01 CI grep gate rejects (literal pattern matches `gemini-3.1-flash`); Plan 41-06 test `test_live_model_id_NOT_in_src_vibemix` redundantly asserts |
| T-41-06-02 | Information Disclosure | Spike recording captures sensitive Kaan-DJ-session audio | accept | Recordings live in spikes/recordings/ under Kaan's control; not committed unless intentional; user decides what to commit/share |
| T-41-06-03 | Denial of Service | Spike runs past Gemini Live 15-min session cap mid-investigation | accept | Pitfall 4 documents the cap; verdict template's Session Cap Workaround section captures the finding for v3.x design |
| T-41-06-04 | Tampering | POC `cohost_lk.py` modified during port (violates Phase 37-06 immutability) | mitigate | Task 2 explicit instruction "DO NOT modify or import from it"; existing Phase 37-06 immutability gate catches accidental edits in CI |
</threat_model>

<verification>
- `uv run pytest tests/repo/test_live_spike_scaffold.py -x -v` → 9 tests green
- `bash scripts/release/check_no_hardcoded_model.sh` (from Plan 41-01) → still passes (spikes/ excluded from scope)
- `cat KAAN-ACTION-PROXY.md | grep -A 3 "§LAT-09"` → shows runbook header + first content line
- `python -m spikes.scripts.run_live_spike --help` → renders without crash
- POC `cohost_lk.py` byte-identical to v2.1 tag (Phase 37-06 immutability gate enforces; not this plan's job but verify-by-omission)
</verification>

<success_criteria>
1. `spikes/gemini-3-1-flash-live-music.md` shipped with 6 sections + engineering-scaffolded status.
2. `spikes/scripts/run_live_spike.py` shipped — runnable, ports cohost_lk.py:1670+ architecture cleanly, exits 0 without API key.
3. `spikes/scripts/recording_harness.py` shipped with documented API.
4. `KAAN-ACTION-PROXY.md` §LAT-09 documents discharge runbook + expected time + acceptance criteria.
5. 9 scaffold-sanity tests green.
6. Live model literal `gemini-3.1-flash-live-preview` NOT in `src/vibemix/` (positive defense via test_live_model_id_NOT_in_src_vibemix).
7. Default cascade behavior in `src/vibemix/` UNCHANGED — this plan ships zero runtime mutations.
</success_criteria>

<output>
After completion, create `.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-06-SUMMARY.md` capturing:
- Spike script LOC + which cohost_lk.py:1670+ elements were ported (architectural-shape mapping)
- KAAN-ACTION-PROXY.md created-vs-appended decision
- Status of POC immutability gate post-port (re-run Phase 37-06 gate)
- Open question for operator: actual VAD + Proactive Audio kwarg names against installed livekit-plugins-google v1.5.8 (verified at first real run)
</output>
