---
phase: 62-floating-pill-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/src-tauri/src/pill_window.rs
  - tauri/src-tauri/capabilities/default.json
autonomous: false
requirements: [PILL-01, PILL-04]
user_setup: []

must_haves:
  truths:
    - "A transparent, always-on-top, decoration-less pill window can be created at runtime (engineering: window builds; felt drag/focus = KAAN-ACTION)"
    - "The pill window persists x/y/w/h with a 200ms debounce and snaps back when its origin lands off-screen (cloned from the mascot, proven by cargo test)"
    - "The pill window re-clamps to the visible work area when the monitor topology changes (display-change WindowEvent hook)"
    - "The pill never calls set_ignore_cursor_events(true) — it is interactive, not click-through"
    - "macOS focus-non-steal is implemented at the Accessory-policy FLOOR (no new dep); the NSPanel non-activating swizzle is a clearly-bounded STRETCH that degrades to the floor"
    - "The pill window label 'pill' is in the capability allowlist, closing the v0.1.0-rc1 drag-capability debt"
  artifacts:
    - path: "tauri/src-tauri/src/pill_window.rs"
      provides: "Pill overlay window builder + geometry-persist/debounce/off-screen-fallback + display-change re-clamp + macOS focus-non-steal interop + cargo tests"
      contains: "PILL_WINDOW_LABEL"
      min_lines: 200
    - path: "tauri/src-tauri/capabilities/default.json"
      provides: "'pill' window label added to the windows scope so core:window:allow-start-dragging applies to the pill"
      contains: "pill"
  key_links:
    - from: "tauri/src-tauri/src/pill_window.rs"
      to: "tauri/src-tauri/capabilities/default.json"
      via: "PILL_WINDOW_LABEL constant == 'pill' in the windows allowlist"
      pattern: "PILL_WINDOW_LABEL.*=.*\"pill\""
    - from: "pill_window.rs builder"
      to: "WebviewWindowBuilder"
      via: "transparent(true) + always_on_top(true) + decorations(false) + focused(false), NO set_ignore_cursor_events"
      pattern: "WebviewWindowBuilder::new"

kaan_action_live_confirm:
  - "Drag-on-unfocused-window FEELS right: build, run, drag the pill while the DJ app is focused — pill follows cursor without a focus flash (tauri#11605/#10767)"
  - "Focus non-steal: click the pill, then type — keystrokes must land in the DJ app, not vibemix (NSWindow→NSPanel/Accessory residual first-click behavior is OS-runtime-only)"
---

<objective>
Build `pill_window.rs` — the third Tauri overlay window (after `mascot_window`, `debrief_window`)
— as a clone-with-subtraction of `mascot_window.rs`, and add the `"pill"` window label to the
capability allowlist. This plan is the ROADMAP-flagged **resolve-FIRST spike**: the drag /
focus-non-steal / transparency mechanism is the single most likely "looks done but feels broken"
failure, so the window mechanics land before any UI is built on them.

Purpose: PILL-01 (transparent / on-top / draggable / multi-monitor-safe pill window) and PILL-04
(never steals keyboard focus; closes the v0.1.0-rc1 drag-capability debt). The drag mechanism
itself is already proven in the codebase (the mascot uses `getCurrentWindow().startDragging()`);
the genuinely-new code is the macOS focus-non-steal `#[cfg(target_os="macos")]` block and the
display-change re-clamp hook.

Output: `pill_window.rs` (builder + geometry-persist + debounce + off-screen fallback +
re-clamp + macOS focus interop + cargo tests), one-line capability edit, and a blocking human
checkpoint that confirms the spike on the BUILT app (felt drag + focus-non-steal).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-floating-pill-ui/62-CONTEXT.md
@.planning/phases/62-floating-pill-ui/62-RESEARCH.md
@.planning/phases/62-floating-pill-ui/62-PATTERNS.md
@.planning/phases/62-floating-pill-ui/62-VALIDATION.md
@.claude/skills/frontend-enforcement/SKILL.md

<interfaces>
<!-- Clone source — pill_window.rs is mascot_window.rs minus one block, plus the macOS #[cfg] block. -->
From tauri/src-tauri/src/mascot_window.rs (263 lines — clone ~90% verbatim):
- `pub const MASCOT_WINDOW_LABEL: &str = "mascot";` (line 33) — pill's is `PILL_WINDOW_LABEL = "pill"`
- `const DEBOUNCE_MS: u64 = 200;` (line 47) — keep verbatim
- `pub fn create_mascot_window(app: &AppHandle) -> ... ` (line 58) — mirror as `create_pill_window`
- builder block (lines 101-128) including `window.set_ignore_cursor_events(true)?` (line 122) — DROP this line for the pill
- `fn primary_logical_size(app)` (line 135), `fn default_top_right(app, width)` (line 150) — clone verbatim
- `fn install_geometry_listener(app, window)` (lines 166-218) — matches `WindowEvent::Moved(_) | WindowEvent::Resized(_)` (line 174); sleeps DEBOUNCE_MS then writes (line 200) — clone + ADD a display-change arm
- `fn window_position(app)` (line 220), `fn window_size(app)` (line 227) — clone verbatim
- tests: `defaults_pin_context_decisions` (line 239), `debounce_is_not_zero_or_thrashy` (line 250), `label_constant_matches_capability_allowlist` (line 259) — clone with pill numbers/label
- comment at line 56: "the manual ObjC override goes here" — the reserved slot for the focus-non-steal interop

From tauri/src-tauri/capabilities/default.json:
- `"windows": ["main", "mascot", "overlay-*", "debrief"]` (line 5) — add `"pill"`
- `"core:window:allow-start-dragging"` is ALREADY in permissions (line 9) — no new permission identifier needed; adding the label is what makes the existing drag permission apply to the pill
- the long `description` string (line 4) names the registered windows + commands — update it to mention the pill window (capabilities-lint.yml checks this file)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 cargo tests for pill_window (RED) — defaults, debounce, label↔capability, clamp</name>
  <read_first>
    - tauri/src-tauri/src/mascot_window.rs lines 234-263 (the three existing tests to clone: `defaults_pin_context_decisions`, `debounce_is_not_zero_or_thrashy`, `label_constant_matches_capability_allowlist`)
    - .planning/phases/62-floating-pill-ui/62-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map — PILL-01/04 rows)
  </read_first>
  <behavior>
    - `label_constant_matches_capability_allowlist`: asserts `PILL_WINDOW_LABEL == "pill"` (the capability allowlist must include it).
    - `defaults_pin_context_decisions`: asserts the UI-SPEC collapsed dimensions 280.0 × 44.0 are the builder defaults (pin the CONTEXT/UI-SPEC numbers so a future edit is a deliberate test change).
    - `debounce_is_not_zero_or_thrashy`: asserts `DEBOUNCE_MS` is in `50..=500`.
    - `clamp_to_work_area_keeps_visible`: a pure clamp-math helper takes a candidate (x, y, w, h) + a work-area rect and returns a position whose rect is fully inside the work area; an off-screen candidate is snapped back inside, an on-screen candidate is returned unchanged.
  </behavior>
  <action>
    Create `tauri/src-tauri/src/pill_window.rs` with the module skeleton: `pub const PILL_WINDOW_LABEL: &str = "pill";`, `const DEBOUNCE_MS: u64 = 200;`, `const PILL_COLLAPSED_W: f64 = 280.0;`, `const PILL_COLLAPSED_H: f64 = 44.0;` (UI-SPEC §Pill Dimensions LOCKED 280×44), and a pure `fn clamp_to_work_area(x: i32, y: i32, w: u32, h: u32, area: WorkArea) -> (i32, i32)` helper (define a small `WorkArea { x, y, w, h }` struct so the clamp math is unit-testable WITHOUT a live monitor — this is the Leg-D math extracted for testability). Add a `#[cfg(test)] mod tests` cloning the three mascot tests (swap label → "pill", dimensions → 280/44) plus `clamp_to_work_area_keeps_visible`. Wire `mod pill_window;` is NOT done here (that is plan 62-02's main.rs edit) — but the file must compile standalone, so register the module temporarily under `#[cfg(test)]` is NOT needed; instead leave the create-fn as a stub returning `Ok(None)` so `cargo test pill_window::tests` compiles and the four tests pass GREEN once written (these are characterization tests, not RED-for-impl — they pin constants + pure math). Run `cd tauri/src-tauri && cargo test pill_window::tests` and confirm all four pass. Per the test-first discipline, commit the tests + skeleton in one commit before Task 2 fleshes the builder.
    NOTE: because Rust requires the module to be declared to compile its tests, declare `mod pill_window;` in `main.rs` in THIS task as a minimal one-line add (the full setup-branch wiring is plan 62-02) — guard the create-fn body so it is a no-op stub here.
  </action>
  <verify>
    <automated>cd tauri/src-tauri && cargo test pill_window::tests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test pill_window::tests` runs and the four named tests pass (`label_constant_matches_capability_allowlist`, `defaults_pin_context_decisions`, `debounce_is_not_zero_or_thrashy`, `clamp_to_work_area_keeps_visible`).
    - `grep -c "set_ignore_cursor_events" tauri/src-tauri/src/pill_window.rs` returns 0 (the pill is interactive — the click-through line must never appear).
    - `PILL_WINDOW_LABEL` equals the string `"pill"` (source assertion).
  </acceptance_criteria>
  <done>The pill_window module compiles, the four cargo tests pass, and the clamp helper is unit-testable without a live monitor.</done>
</task>

<task type="auto">
  <name>Task 2: Flesh the pill window builder + geometry-persist + display-change re-clamp + macOS focus-non-steal floor/stretch</name>
  <read_first>
    - tauri/src-tauri/src/mascot_window.rs lines 58-232 (the create fn, builder block, primary_logical_size, default_top_right, install_geometry_listener, window_position/window_size — the verbatim-clone body) AND line 56 (the reserved ObjC-override slot comment)
    - tauri/src-tauri/src/pill_window.rs (the skeleton from Task 1)
    - .planning/phases/62-floating-pill-ui/62-RESEARCH.md "The Load-Bearing Spike" Legs A + D + the "Don't Hand-Roll" macOS-non-activating row
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "Shared Patterns / macOS focus-non-steal (Leg A)" + "display-change re-clamp"
  </read_first>
  <action>
    Replace the Task-1 stub `create_pill_window` body with the cloned builder: `WebviewWindowBuilder::new(app, PILL_WINDOW_LABEL, WebviewUrl::App("pill.html".into()))` with `.title("vibemix")`, `.transparent(true)`, `.always_on_top(true)`, `.decorations(false)`, `.resizable(false)` (the pill is fixed-size — expand is CSS height, not a window resize), `.skip_taskbar(true)`, `.visible_on_all_workspaces(true)`, `.focused(false)`, `.inner_size(PILL_COLLAPSED_W, PILL_COLLAPSED_H)`, `.position(...)` (reuse `default_top_right` + persisted geometry exactly like the mascot), `.visible(true)`, `.build()?`. DO NOT clone mascot_window.rs lines 120-123 (`set_ignore_cursor_events`). Clone `primary_logical_size`, `default_top_right`, `install_geometry_listener` (with its 200ms debounce + the off-screen fallback), `window_position`, `window_size` verbatim, swapping `MASCOT_WINDOW_LABEL` → `PILL_WINDOW_LABEL` and the mascot store helpers → pill geometry persistence (reuse the same geometry struct keyed under a `pill_window` store key, OR a dedicated pill helper — your discretion per CONTEXT, keep the debounce machinery identical).

    Add the display-change re-clamp (Leg D — the one new event hook): in `install_geometry_listener`, also match `WindowEvent::ScaleFactorChanged { .. }` (and re-evaluate on monitor topology change), recompute against `current_monitor()` work area (use `Monitor::work_area()` if available; if the 2.11 signature differs, fall back to `size()/scale_factor()` clamp math per RESEARCH A2), and `set_position` back via `clamp_to_work_area` if the pill is off-screen.

    Add the macOS focus-non-steal interop after `.build()?`, `#[cfg(target_os="macos")]` only — implement BOTH tiers:
    - FLOOR (shipped baseline, no new dep): call `app.set_activation_policy(tauri::ActivationPolicy::Accessory)` so vibemix reads as a background/accessory app, and rely on `.focused(false)` from the builder. Document the residual first-click limitation (wry#637, tauri#14102 open) in a comment.
    - STRETCH (true non-activating, bounded): in a `with_webview` closure get `ns_window()` (cast to `objc2_app_kit::NSWindow`), swizzle the class to `NSPanel`, then set `styleMask |= NonactivatingPanel`. CAVEAT (RESEARCH Leg A, HIGH confidence): the NonactivatingPanel mask is a SILENT NO-OP on a plain NSWindow — it only takes effect after the class is an NSPanel subclass. Gate the stretch behind a small helper that, on any failure, falls back to the floor (never panics). If `objc2-app-kit` is added, run `cargo search objc2-app-kit` FIRST and add it as a macOS-only target dep in Cargo.toml (crates.io only — `tauri-nspanel` git dep is REJECTED). If the swizzle cannot land cleanly within this task, ship the FLOOR and leave a `// STRETCH: NSPanel swizzle deferred` marker — defer, do not pause (`gsd-autonomous fully`).

    Keep the pill setup non-fatal: `create_pill_window` returns `Result<Option<WebviewWindow>, _>` mirroring the mascot's "log but don't bail setup" discipline.
  </action>
  <verify>
    <automated>cd tauri/src-tauri && cargo build 2>&1 | tail -15 && cargo test pill_window:: 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` succeeds (the crate compiles with the fleshed builder + macOS `#[cfg]` block).
    - `cargo test pill_window::` passes (the Task-1 tests still green after the builder is real).
    - `grep -c "set_ignore_cursor_events" tauri/src-tauri/src/pill_window.rs` returns 0.
    - `grep -E "transparent\(true\)|always_on_top\(true\)|decorations\(false\)|focused\(false\)" tauri/src-tauri/src/pill_window.rs` matches all four builder flags.
    - The macOS `#[cfg(target_os="macos")]` block exists and references `ActivationPolicy::Accessory` (the floor) — `grep -c "ActivationPolicy::Accessory" tauri/src-tauri/src/pill_window.rs` >= 1.
    - If `objc2-app-kit` was added: it appears under a `[target.'cfg(target_os = "macos")'.dependencies]` block in Cargo.toml and resolves from crates.io (no git dep) — `grep -c "tauri-nspanel" tauri/src-tauri/Cargo.toml` returns 0.
  </acceptance_criteria>
  <done>The pill window builds with transparency/on-top/decoration flags, persists+re-clamps geometry, implements the focus-non-steal floor (and stretch if it landed), and never goes click-through.</done>
</task>

<task type="auto">
  <name>Task 3: Add "pill" to the capability allowlist (close the v0.1.0-rc1 drag-capability debt)</name>
  <read_first>
    - tauri/src-tauri/capabilities/default.json (the `"windows"` array line 5 + `core:window:allow-start-dragging` line 9 + the `description` string line 4)
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "capabilities/default.json" section (the one-line delta + the `app:allow-<command>` anti-pattern)
  </read_first>
  <action>
    Edit `tauri/src-tauri/capabilities/default.json`: add `"pill"` to the `"windows"` array (now `["main", "mascot", "overlay-*", "debrief", "pill"]`). Do NOT add any new permission identifier for drag — `core:window:allow-start-dragging` is already in `permissions` (line 9); adding the label is what scopes the existing drag permission to the pill window. Append a short sentence to the `description` string noting the Phase-62 pill window label was added so the existing start-dragging permission applies to it (capabilities-lint.yml checks the description). Do NOT add `app:allow-<command>` entries (Tauri 2.x auto-allows webview→app-command invocation; that namespace is reserved for the core-app plugin and would fail the build with "permission identifier not found").
  </action>
  <verify>
    <automated>cd tauri/src-tauri && python3 -c "import json; d=json.load(open('capabilities/default.json')); assert 'pill' in d['windows'], 'pill not in windows'; print('OK windows:', d['windows'])" && cargo build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `"pill"` is present in the `windows` array of `capabilities/default.json` (JSON-parsed assertion above).
    - The capability file still parses as valid JSON and `cargo build` succeeds (the ACL is accepted by the Tauri build).
    - No `app:allow-` permission identifier was added (`grep -c "app:allow-" tauri/src-tauri/capabilities/default.json` returns 0).
  </acceptance_criteria>
  <done>The pill window label is in the capability allowlist; the existing start-dragging permission now applies to the pill, closing the drag-capability debt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Spike checkpoint — confirm felt drag + focus-non-steal on the built app (KAAN-ACTION)</name>
  <action>Human-gated spike checkpoint (no code change). Pause for Kaan to confirm the felt drag-on-unfocused + focus-non-steal behavior on the BUILT app per the steps in &lt;how-to-verify&gt;. Per `gsd-autonomous fully`, a non-approval is RECORDED in the SUMMARY KAAN-ACTION list, not a blocker — engineering ships at the floor.</action>
  <what-built>
    A transparent, always-on-top, decoration-less pill overlay window (`pill_window.rs`) that builds at
    runtime, persists/re-clamps its geometry, implements the macOS focus-non-steal Accessory-policy floor
    (plus the NSPanel stretch if it landed), is interactive (NOT click-through), and is in the capability
    allowlist. This is the resolve-FIRST spike — the felt drag + focus behavior on the BUILT app is the
    one KAAN-ACTION carve-out per `gsd-autonomous fully` (surface, do not block engineering completion).
  </what-built>
  <how-to-verify>
    1. Build + run the app so the pill window appears (set `primary_surface = "pill"` once 62-02 lands, OR temporarily force-create the pill in setup for this spike check).
    2. DRAG check (tauri#11605/#10767): with a DJ app (or any other app) focused, grab the pill's top strip and drag — it should follow the cursor smoothly WITHOUT a focus flash, on the first grab (no "drag twice" bug).
    3. FOCUS-NON-STEAL check (PILL-04): focus a text field in the DJ app (or a Notes window), CLICK the pill, then TYPE — the keystrokes must still land in the DJ app, not in vibemix. (Floor: the first click may transfer focus — note it. Stretch/NSPanel: no transfer at all.)
    4. Report which tier is in effect (Accessory floor vs NSPanel stretch) and whether keystrokes stayed in the DJ app.
  </how-to-verify>
  <resume-signal>Type "approved" (engineering ships at the floor regardless), or describe the felt drag/focus behavior so it is recorded in the SUMMARY KAAN-ACTION list. Per `gsd-autonomous fully`, a non-approval here is RECORDED, not a blocker — the floor ships and the stretch is logged as follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS window manager → pill window | The pill is an always-on-top OS window; the only privilege it gains beyond defaults is window-drag (already granted app-wide via `core:window:allow-start-dragging`). No new listening port, no remote input. |
| Tauri capability ACL | The `"pill"` label scopes the existing drag permission to the new window — least privilege; no shell/fs/process perms added to the pill label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Elevation | `capabilities/default.json` `"pill"` entry | mitigate | Add ONLY the `"pill"` window label to the existing `windows` scope; grant NO new permission identifier (drag is already present). Do not add shell/fs/process perms to the pill label (V4 least-privilege). |
| T-62-02 | Denial (usability) | always-on-top pill covering critical deck UI | mitigate | Compact 280×44, top-right default placement out of the deck read zone; clamp-to-visible on display change; never `set_ignore_cursor_events` (pill stays interactive so the DJ can move it). |
| T-62-03 | Spoofing / Info disclosure | window/port surface | accept | NO new network surface introduced — this plan adds an OS window + a capability label only; the pill consumes the existing localhost bus in a later plan. No remote input is accepted. |
| T-62-SC | Tampering | cargo install of `objc2-app-kit` (stretch path only) | mitigate | Add only if the NSPanel stretch is taken; verify via `cargo search objc2-app-kit` (crates.io, canonical objc2/madsmtm set) before adding; `tauri-nspanel` git dep is REJECTED (reproducible-build / one-click-install discipline). No npm/PyPI install in this plan → slopcheck N/A. |
</threat_model>

<verification>
- `cargo build` (whole crate) succeeds with the new module + capability.
- `cargo test pill_window::` all green (defaults, debounce, label↔capability, clamp math).
- `grep -c "set_ignore_cursor_events" tauri/src-tauri/src/pill_window.rs` == 0.
- `grep -c "tauri-nspanel" tauri/src-tauri/Cargo.toml` == 0 (git dep never added).
- `python3 -c "import json; assert 'pill' in json.load(open('tauri/src-tauri/capabilities/default.json'))['windows']"` passes.
- KAAN-ACTION (recorded, not a blocker): felt drag + focus-non-steal on the built app.

## Dimension-8 Validation (per 62-VALIDATION.md)
| Task | Requirement | Test Type | Automated Command | KAAN-ACTION |
|------|-------------|-----------|-------------------|-------------|
| 1 | PILL-04 (label↔capability) + PILL-01 (clamp math) | unit (cargo) | `cargo test pill_window::tests` | — |
| 2 | PILL-01/04 (builder flags + focus floor + re-clamp) | unit (cargo) + build | `cargo build && cargo test pill_window::` | felt drag + focus-non-steal |
| 3 | PILL-04 (capability allowlist) | unit (json parse) + build | `python3 -c "..." && cargo build` | — |
| 4 | PILL-01/04 (felt drag + focus) | manual | n/a — checkpoint | drag-on-unfocused + keystroke routing |
Sampling: per-task `cargo test pill_window::`; per-wave `cargo test`. No 3 consecutive tasks without automated verify (Task 4 is the human checkpoint after 3 automated tasks).
</verification>

<success_criteria>
- PILL-01 (engineering): the pill window builds transparent / on-top / decoration-less / fixed-size, persists + re-clamps geometry (multi-monitor safe). Felt transparency on the `.dmg` = KAAN-ACTION (plan 62-04 owns the rgba surface; this plan owns the window flags).
- PILL-04 (engineering): focus-non-steal floor implemented (Accessory policy + `focused(false)`); the `"pill"` capability label closes the drag-capability debt; the pill is interactive (no click-through). Felt focus-non-steal = KAAN-ACTION.
- The resolve-FIRST spike is discharged: the drag mechanism (already-proven `startDragging`) + the focus floor are in place, and the felt result is recorded for Kaan.
</success_criteria>

<output>
Create `.planning/phases/62-floating-pill-ui/62-01-SUMMARY.md` when done — record which focus tier
shipped (Accessory floor vs NSPanel stretch), whether `objc2-app-kit` was added, and the KAAN-ACTION
felt-drag/focus result.
</output>
