---
phase: 62-floating-pill-ui
plan: 04
type: execute
wave: 3
depends_on: ["62-02"]
files_modified:
  - tauri/ui/pill.html
  - tauri/ui/src/pill/index.ts
  - tauri/ui/src/pill/state-machine.ts
  - tauri/ui/src/pill/waveform.ts
  - tauri/ui/src/pill/index.test.ts
  - tauri/ui/src/pill/state-machine.test.ts
  - tauri/ui/src/pill/waveform.test.ts
autonomous: false
requirements: [PILL-03]
user_setup: []

must_haves:
  truths:
    - "pill.html is a transparent-overlay webview that clones the mascot.html invariant (html,body transparent !important + body::before display:none) so tokens.css never paints a vignette over the desktop"
    - "The pill consumes the EXISTING ws:8765 frames via connectMascotBus (direct-WS, like the mascot) — no new port, no Python delivery change"
    - "The pill renders four states from wire data: idle / listening / speaking / expand-on-event (cohost_status → base state; cohost-reaction → expand)"
    - "The TTS waveform during speaking is driven by the REAL voice.rms signal on the bus (Levels.update_voice), read defensively flat OR nested — not a fake animation"
    - "The expand state renders the reaction text verbatim + the citation strip (renderCitationStrip reused verbatim, returns null on empty)"
    - "The state machine is pure (now-param, no wall-clock, no timers) so vitest cases are deterministic; expand-collapse is a data-driven collapseAt field, not a setTimeout"
    - "Pill code lives under tauri/ui/src/pill/ (NOT src/mascot/) and never references mascot.html — the mascot-audit fence stays green"
  artifacts:
    - path: "tauri/ui/pill.html"
      provides: "Transparent-overlay HTML entry for the pill window (mascot.html invariant cloned), loads /src/pill/index.ts"
      contains: "background: transparent"
    - path: "tauri/ui/src/pill/state-machine.ts"
      provides: "Pure 4-state derivation: cohost_status → idle/listening/speaking; cohost-reaction → expand with collapseAt"
      contains: "baseState"
    - path: "tauri/ui/src/pill/index.ts"
      provides: "Boot: connectMascotBus + drag handler (top 28px strip) + reader/writer handleFrame + waveform + citation strip wiring"
      contains: "connectMascotBus"
    - path: "tauri/ui/src/pill/waveform.ts"
      provides: "TTS waveform reusing meter.ts tokens + data-lit update path, driven by voice.rms"
      contains: "setMeterLevels"
  key_links:
    - from: "tauri/ui/src/pill/index.ts"
      to: "ws://127.0.0.1:8765"
      via: "connectMascotBus (direct-WS, imported/copied from the shipped client)"
      pattern: "connectMascotBus"
    - from: "tauri/ui/src/pill/index.ts"
      to: "tauri/ui/src/session/components/citation-strip.ts renderCitationStrip"
      via: "expand panel renders the citation strip verbatim"
      pattern: "renderCitationStrip"
    - from: "tauri/ui/src/pill/index.ts drag handler"
      to: "getCurrentWindow().startDragging()"
      via: "mousedown over the top 28px drag strip (chips tagged data-no-drag)"
      pattern: "startDragging"

kaan_action_live_confirm:
  - "Visual: the pill renders as a compact dark-glass lozenge (idle dim dot + IDLE label), grows DOWN on a reaction, and the waveform animates on real AI speech — confirmed on the running app (felt rendering = live-only)"
---

<objective>
Build the pill's UI surface — `pill.html` + the vanilla-TS state machine, frame consumer, waveform,
and citation strip — as a clone/reuse of the shipped mascot + session components. The pill consumes
the EXISTING ws:8765 frames (direct-WS like the mascot), renders four states (idle / listening /
speaking / expand-on-event), drives the waveform from the REAL `voice.rms` signal, and renders the
reaction text + citation strip verbatim in the expand panel.

Purpose: PILL-03 (the pill consumes existing frames and shows idle/listening/speaking/expand states
with the real TTS waveform + citation strip). This is the CORE pill UI; deck-context chips + the
transparency rgba surface land in plan 62-05.

Output: `pill.html` (transparent-overlay invariant), `src/pill/state-machine.ts` (pure 4-state),
`src/pill/index.ts` (boot + bus + drag + reader/writer + waveform + citation), `src/pill/waveform.ts`
(meter.ts reuse), and vitest fences. All under `tauri/ui/src/pill/` — mascot-audit fence stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-floating-pill-ui/62-CONTEXT.md
@.planning/phases/62-floating-pill-ui/62-RESEARCH.md
@.planning/phases/62-floating-pill-ui/62-PATTERNS.md
@.planning/phases/62-floating-pill-ui/62-UI-SPEC.md
@.planning/phases/62-floating-pill-ui/62-VALIDATION.md
@.claude/skills/frontend-enforcement/SKILL.md

<interfaces>
From tauri/ui/mascot.html (95 lines — the transparent-overlay invariant to clone VERBATIM):
- `<html ... style="background: transparent ...">` + `html,body { background: transparent !important; background-image: none !important }` + `body::before { display:none !important }` (lines 1-2, 66-78) — WITHOUT this, tokens.css paints a global vignette + film-grain over the desktop (RESEARCH Pitfall 4).
- `<script type="module" src="...">` boot entry (line 84) — pill loads `/src/pill/index.ts`.

From tauri/ui/src/mascot/index.ts (608 lines — clone the drag + bus + reader/writer, DROP the Three.js renderer):
- drag handler (lines 147-163): `getCurrentWindow()` then `addEventListener("mousedown", ...)` — `button !== 0` left-only, `closest("[data-no-drag]")` exempt, `startDragging().catch(...)`. DELTA: scope to the top 28px drag strip element, not `document` (UI-SPEC §Drag handle).
- bus wiring (lines 51, 291-292): `import { connectMascotBus } from "./ws-client.js"`; `bus = connectMascotBus("ws://127.0.0.1:8765")`; `bus.addMessageListener(handleMessage)`.
- `handleMessage` (lines 183-251) — the reader/writer discipline; the LIVE-05a flat-float read (lines 205-217): `voice` arrives FLAT on the direct WS frame, nested `meters.voice.rms` on the bridged snapshot → read `msg.voice ?? msg.meters?.voice?.rms ?? prev`.

From tauri/ui/src/mascot/state-machine.ts (lines 1-26 — the PURITY DISCIPLINE header to honor):
- No wall-clock reads (take `now: number` param), no timers (express scheduling as a data field the rAF loop fires), no heavy imports — so vitest is deterministic without clock mocking.

From tauri/ui/src/mascot/ws-client.ts (175 lines — connectMascotBus, reuse; do NOT re-implement):
- 1→2→4→8s backoff, reset-on-open, silent-drop of non-JSON/malformed frames (anti-slop), `addMessageListener`/`addStatusListener`/`close` surface.
- NOTE (PATTERNS): importing from `../mascot/ws-client.js` may path-trigger mascot-audit since it touches `tauri/ui/src/mascot/**` indirectly — COPY `ws-client.ts` into `tauri/ui/src/pill/ws-client.ts` to keep the pill fully decoupled and the fence un-triggered. (Planner's call → COPY for this plan.)

From tauri/ui/src/session/components/citation-strip.ts (174 lines — reuse renderCitationStrip VERBATIM):
- `export interface CitationChip { event_id; verb; timestamp_s }` (lines 37-47) — field-identical to the Python `CitationChipPayload`.
- `export function renderCitationStrip(props: { chips: CitationChip[]; onChipClick })` — returns `null` when `chips.length === 0` (line 147) → caller does `if (strip) panel.append(strip)`.
- `export const _CSS_FOR_TEST` (line 119) — the grep-able CSS for the no-hex/amber-token frontend-enforcement assert.

From tauri/ui/src/session/components/meter.ts (263 lines — reuse tokens + data-lit update path for the waveform):
- `export function renderMeter(props: MeterProps)` (line 186) + `export function setMeterLevels(el, {rms, peak})` (line 230) — single-attribute `data-lit-count`/`data-lit` write, zero-DOM-churn, amber-zone gradient tokens (lines 103-119).
- `export const _CSS_FOR_TEST` (line 263).

From src/vibemix/runtime/ws_bus.py (the wire shapes the pill reads — VERIFIED):
- flat 30Hz frame: `voice`/`music`/`mic` top-level floats (`**levels.snapshot()`), `cohost_status` derivation (`voice_rms>0.05`→TALKING, grounded→LISTENING, else IDLE — but the FLAT frame carries `audible`/`deck`, the bridged snapshot carries `cohost_status`). Read `cohost_status` defensively; derive from the flat frame's `voice`/`audible` if absent.
- cohost_status enum: `LISTENING | TALKING | IDLE` (messages.schema.json) → `speaking`/`listening`/`idle`.

From tauri/ui/src/ipc/messages.schema.json:
- `cohost_status` enum {LISTENING, TALKING, IDLE}; `ipc.session.cohost-reaction` payload {text, event_id, citation_strip[]}.

vitest: `vitest.config.ts` `include` already globs `src/**/*.test.ts` → pill tests under `tauri/ui/src/pill/*.test.ts` are auto-collected (NO config change needed).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — pure pill state machine + frame→state map + transparent pill.html scaffold</name>
  <read_first>
    - tauri/ui/src/mascot/state-machine.ts lines 1-26 (the PURITY DISCIPLINE header — now-param, no timers, no wall-clock) AND mascot state-machine.test.ts (the deterministic now-param test pattern to clone)
    - tauri/ui/mascot.html lines 1-2, 55, 66-84 (the transparent-overlay invariant to clone verbatim)
    - .planning/phases/62-floating-pill-ui/62-UI-SPEC.md "Pill States (PILL-03)" + "Color" (the 4 states + the --glass-3 surface contract)
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "state-machine.ts" + "pill.html" sections
  </read_first>
  <behavior>
    - `baseState("TALKING") === "speaking"`, `baseState("LISTENING") === "listening"`, `baseState("IDLE") === "idle"` (and unknown/absent → "idle").
    - A `cohost-reaction` frame transitions the machine to `expand` with `collapseAt === now + EXPAND_MS` (UI-SPEC ~6s); reaction text + chips are carried on the state.
    - With `now >= collapseAt`, the machine reverts to `baseState(currentStatus)` — DATA-DRIVEN (no setTimeout); a fresh reaction before collapseAt re-extends collapseAt.
    - Purity: every state function takes `now: number`; the file contains no `Date.now()`/`setTimeout`/`setInterval`/`new Date` (grep-asserted, mirroring the mascot purity verifier).
  </behavior>
  <action>
    Create `tauri/ui/src/pill/state-machine.ts` cloning the mascot's PURITY header + discipline (now-param, no timers, no wall-clock, no heavy imports) but with the pill's far-simpler 4-state model. Implement `baseState(s: CohostStatus): "idle"|"listening"|"speaking"`, an `initialPillState(now)`, a pure `applyFrame(state, frame, now)` that (a) updates `cohostStatus`/`voiceRms` from a snapshot frame WITHOUT a transition (reader), and (b) on a `cohost-reaction` sets `mode="expand"` + `collapseAt = now + EXPAND_MS` + carries `reactionText`/`chips` (writer), and a pure `tickCollapse(state, now)` that reverts `mode` to `baseState(cohostStatus)` once `now >= collapseAt`. Export an `EXPAND_MS` const (UI-SPEC ~6000). Create `tauri/ui/pill.html` cloning the mascot.html transparent-overlay invariant VERBATIM (`html,body { background: transparent !important; background-image: none !important }` + `body::before { display:none !important }`, `overflow:hidden`, full-viewport), linking `/src/tokens.css` and booting `/src/pill/index.ts`; lay out a compact DOM skeleton: a top 28px drag strip (with a `--silk-22` 2px×20px grab-bar affordance), a collapsed row (state dot + state label + waveform mount), and an expand panel mount (reaction text + citation strip mount). Do NOT reference mascot.html anywhere. Create `tauri/ui/src/pill/state-machine.test.ts` cloning the mascot deterministic now-param cases for the behaviors above, plus a purity grep test asserting no `Date.now`/`setTimeout`/`setInterval` in state-machine.ts. Run `cd tauri/ui && npx vitest run src/pill/state-machine.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai/tauri/ui && npx vitest run src/pill/state-machine.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pill/state-machine.test.ts` passes (baseState mapping, expand-with-collapseAt, data-driven revert, re-extend, purity grep).
    - `grep -E "Date\.now|setTimeout|setInterval|new Date" tauri/ui/src/pill/state-machine.ts` returns nothing (purity preserved).
    - `pill.html` contains `background: transparent !important` and `body::before` `display:none` (the overlay invariant) and does NOT contain the string `mascot.html` (`grep -c "mascot.html" tauri/ui/pill.html` == 0).
    - `pill.html` boots `/src/pill/index.ts` (`grep -c "src/pill/index.ts" tauri/ui/pill.html` >= 1).
  </acceptance_criteria>
  <done>The pure 4-state pill state machine + the transparent pill.html scaffold exist and are GREEN; expand-collapse is data-driven; the mascot.html invariant is cloned without referencing it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pill boot — copy ws-client, wire connectMascotBus + reader/writer handleFrame + drag handler</name>
  <read_first>
    - tauri/ui/src/mascot/index.ts lines 51, 147-163 (drag handler), 183-251 (handleMessage reader/writer), 205-217 (LIVE-05a flat-float read), 288-292 (bus wiring)
    - tauri/ui/src/mascot/ws-client.ts (the whole 175-line client to COPY into src/pill/ws-client.ts — do NOT re-implement)
    - tauri/ui/src/pill/state-machine.ts (Task 1 — applyFrame/tickCollapse/baseState)
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "index.ts" + "Frame fan-out" + "Direct-WS bus client" sections
  </read_first>
  <behavior>
    - A snapshot frame with `cohost_status: "TALKING"` (or flat `voice > threshold` when cohost_status is absent on the flat frame) drives the collapsed view to `speaking`; `LISTENING` → `listening`; `IDLE`/none → `idle` (READER — no expand).
    - `voiceRms` is read defensively: `msg.voice ?? msg.meters?.voice?.rms ?? prev` (flat OR nested — LIVE-05a).
    - A `cohost-reaction` frame (`type === "cohost-reaction"` OR `"ipc.session.cohost-reaction"`) drives `expand` with the reaction text + `citation_strip ?? []` (WRITER).
    - The drag handler fires `startDragging()` only on left-mousedown over the top 28px drag strip, and is exempted on `[data-no-drag]` targets (chips/controls).
  </behavior>
  <action>
    COPY `tauri/ui/src/mascot/ws-client.ts` to `tauri/ui/src/pill/ws-client.ts` verbatim (keeps the pill decoupled from `src/mascot/**` so the mascot-audit fence is not path-triggered — PATTERNS recommendation). Create `tauri/ui/src/pill/index.ts`: import `connectMascotBus` from `./ws-client.js`, the state machine from `./state-machine.js`, and (Task 3) the waveform. Wire `bus = connectMascotBus("ws://127.0.0.1:8765"); bus.addMessageListener(handleFrame); bus.addStatusListener((s) => {/* CONTEXT: no error UI on the pill — silence is the honest empty state; bus-health surfaces in the main window, not here */})`. Implement `handleFrame(msg)` cloning the mascot reader/writer discipline: snapshot/flat frames are READERS (update `view.cohostStatus`/`view.voiceRms` via `applyFrame`, no transition), `cohost-reaction` is the WRITER (call `applyFrame` to enter expand with text+chips). Read `voiceRms` defensively flat-or-nested (LIVE-05a). Clone the drag handler (mascot lines 147-163) but scope `addEventListener("mousedown", ...)` to the top 28px drag-strip element (not `document`), keep `button !== 0` left-only + `closest("[data-no-drag]")` exempt + `startDragging().catch(...)`. Add an rAF loop that calls `tickCollapse(state, performance.now())` to revert the expand and (Task 3) repaints the waveform; render the collapsed view (state dot pulse cadence per UI-SPEC: dim/no-pulse idle, 1.4s listening, 0.9s speaking) and the state label (IDLE/LISTENING/SPEAKING). Create `tauri/ui/src/pill/index.test.ts` (jsdom) asserting the frame→state map (TALKING→speaking, LISTENING→listening, IDLE→idle), the defensive flat-or-nested voice read, and that a cohost-reaction enters expand with the verbatim text. Run `cd tauri/ui && npx vitest run src/pill/index.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai/tauri/ui && npx vitest run src/pill/index.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pill/index.test.ts` passes (frame→state map, defensive voice read, reaction→expand).
    - `tauri/ui/src/pill/ws-client.ts` exists and exports `connectMascotBus` (the copied client — `grep -c "connectMascotBus" tauri/ui/src/pill/ws-client.ts` >= 1).
    - `index.ts` imports the bus from `./ws-client.js` (the pill-local copy), NOT from `../mascot/` (`grep -c "../mascot/" tauri/ui/src/pill/index.ts` == 0 — keeps the fence un-triggered).
    - The drag handler references `startDragging` and is scoped to the drag strip (`grep -c "startDragging" tauri/ui/src/pill/index.ts` >= 1).
  </acceptance_criteria>
  <done>The pill boots: it consumes the existing ws:8765 frames via a pill-local connectMascotBus, derives the four collapsed states with a defensive voice read, expands on a reaction, and drags from the top strip — all decoupled from src/mascot/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TTS waveform (meter.ts reuse, real voice.rms) + citation strip (renderCitationStrip verbatim) in expand</name>
  <read_first>
    - tauri/ui/src/session/components/meter.ts lines 103-119 (amber-zone gradient tokens), 186-254 (renderMeter + setMeterLevels data-lit update), 263 (_CSS_FOR_TEST)
    - tauri/ui/src/session/components/citation-strip.ts lines 37-47 (CitationChip), 119 (_CSS_FOR_TEST), 144-174 (renderCitationStrip, null-on-empty), 128-137 (formatMmSs)
    - tauri/ui/src/session/components/meter.test.ts + citation-strip.test.ts (the _CSS_FOR_TEST no-hex/amber-token grep pattern to clone)
    - .planning/phases/62-floating-pill-ui/62-UI-SPEC.md "Waveform spec" + "Color" (amber reserved for state dot / waveform / chips / deck key only — 20/80 rule)
    - .claude/skills/frontend-enforcement/SKILL.md (20/80 rule, amber accent discipline, no hand-rolled canvas)
  </read_first>
  <behavior>
    - During `speaking`, the waveform is driven by the REAL `voice.rms` from the bus (not a fake animation): feeding a sequence of rms values updates the lit-bar count via the same `data-lit` single-attribute path meter.ts uses (zero per-frame DOM construction).
    - When `voice.rms` is silent between phrases, bars settle to a flat low baseline (not zero) — UI-SPEC.
    - The expand panel renders the reaction text verbatim (max 3 lines, ellipsis after) and, BELOW it, `renderCitationStrip({chips, onChipClick})`; when `chips` is empty, `renderCitationStrip` returns `null` and the panel shows no hanging strip.
    - frontend-enforcement: the waveform + chip CSS use `var(--token)` only (no hex, no non-black rgba); amber appears ONLY on the state dot, waveform fill, and chips (20/80 rule).
  </behavior>
  <action>
    Create `tauri/ui/src/pill/waveform.ts` reusing meter.ts: import `renderMeter`/`setMeterLevels` (or fork the geometry to horizontal 12-16 bars per UI-SPEC while reusing the amber gradient tokens + the `data-lit-count` update pattern — your discretion, but REUSE the tokens, do NOT hand-roll a canvas/SVG waveform). Expose a `renderWaveform()` mount + a `setWaveform(el, rms, peak?)` updater that calls the meter's single-attribute update path. Wire it into `index.ts`'s rAF loop: during `speaking`, call `setWaveform(wfEl, view.voiceRms, view.voicePeak ?? null)` per frame; settle to a low baseline (not zero) when silent. Wire the citation strip into the expand panel in `index.ts`: `import { renderCitationStrip } from "../session/components/citation-strip.js"`; on expand, build `const strip = renderCitationStrip({ chips: view.reactionChips, onChipClick: () => {/* v1 no-op or open_debrief deep-link later */} }); if (strip) expandPanel.append(strip);`. Render the reaction text verbatim above it (no "AI:" prefix, no framing — anti-slop, UI-SPEC Copywriting). Tag the citation chips/controls `[data-no-drag]` so a chip click does not start a window drag. Create `tauri/ui/src/pill/waveform.test.ts` cloning the meter `_CSS_FOR_TEST` no-hex/amber-token grep + asserting the rms→lit-count update path (a higher rms lights more bars) + the silent→baseline behavior. Run `cd tauri/ui && npx vitest run src/pill/ && npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai/tauri/ui && npx vitest run src/pill/ 2>&1 | tail -15 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pill/` passes (state-machine + index + waveform suites all green).
    - `npx tsc --noEmit` is clean (the pill TS compiles against the reused meter + citation-strip types).
    - `index.ts` imports `renderCitationStrip` from the shipped session component (`grep -c "renderCitationStrip" tauri/ui/src/pill/index.ts` >= 1) — NOT a re-implemented chip component.
    - `waveform.ts` reuses the meter path (`grep -E "setMeterLevels|data-lit" tauri/ui/src/pill/waveform.ts` matches) and contains NO `<canvas`/`createElement("canvas")` (no hand-rolled waveform).
    - frontend-enforcement: `waveform.test.ts` asserts the `_CSS_FOR_TEST`-style no-hex/amber-token contract (grep-based, mirroring meter.test.ts).
  </acceptance_criteria>
  <done>The speaking-state waveform is driven by the real voice.rms via the reused meter tokens, the expand panel renders the verbatim reaction text + the verbatim citation strip (null-on-empty), and the 20/80 amber discipline holds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Pill UI checkpoint — confirm the four states + real-voice waveform + expand render live (KAAN-ACTION)</name>
  <action>Human-gated UI checkpoint (no code change). Pause for Kaan to confirm the four pill states + the real-voice.rms waveform + the expand render on the running app per the steps in &lt;how-to-verify&gt;. Per `gsd-autonomous fully`, a non-approval is RECORDED in the SUMMARY KAAN-ACTION list, not a blocker — engineering ships at the green-test bar.</action>
  <what-built>
    The pill's core UI: a transparent-overlay webview (`pill.html`) that consumes the existing
    ws:8765 frames via a pill-local connectMascotBus, renders idle/listening/speaking/expand states
    from real wire data, drives the speaking-waveform from the real voice.rms (Levels.update_voice),
    and shows the verbatim reaction text + citation strip in the expand panel. Built entirely under
    `tauri/ui/src/pill/` (mascot-audit fence un-triggered). Per `gsd-autonomous fully`, the felt
    rendering on the running app is the recorded KAAN-ACTION carve-out — engineering ships at the
    automated-test bar regardless.
  </what-built>
  <how-to-verify>
    1. Run the app with `primary_surface = "pill"` (62-02 default) so the pill window appears.
    2. IDLE: with no session, the pill shows a compact dark lozenge — dim dot + `IDLE` label, no waveform, no filler chatter (silence is the honest empty state).
    3. LISTENING/SPEAKING: start a session; the state dot pulses (slow listening / fast speaking) and the waveform animates on REAL AI speech (not a loop) — it tracks the voice.
    4. EXPAND: on a co-host reaction, the pill grows DOWN to show the reaction text + citation chips, then collapses back after ~6s.
    5. Confirm the reaction text is rendered verbatim (no "AI:" prefix / no added framing) and the chips match the grounded citation strip.
  </how-to-verify>
  <resume-signal>Type "approved", or describe any rendering/state issue so it is recorded in the SUMMARY KAAN-ACTION list. Per `gsd-autonomous fully`, a non-approval here is RECORDED, not a blocker — the engineering ships at the green-test bar and visual notes become follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ws:8765 frame → pill webview | The pill is a pure CONSUMER of the existing localhost bus. Frame content (reaction text, citation chips, meter floats) is rendered as structured text/DOM — NEVER `eval`'d, never `innerHTML`'d from untrusted content. The pill sends nothing back to Python (consume-only for v1). |
| webview → OS (drag) | The only privilege the pill webview exercises is `startDragging()` (granted via the `"pill"` capability label from 62-01). No fs/shell/process access from the pill surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-11 | Tampering / XSS | reaction text + chip verb rendered into the pill DOM | mitigate | Render frame text via `textContent` (NOT `innerHTML`); the reaction text is set verbatim as a text node, the citation strip is the contract-tested `renderCitationStrip` (text-node chips). No frame content is `eval`'d or HTML-injected. |
| T-62-12 | Information disclosure | a fabricated/orphaned citation chip on the pill | mitigate | The pill renders `citation_strip[]` exactly as the backend (post-CitationLinter) emits it — it does NOT fabricate or add chips. Reaction text is verbatim, no pill-side framing (anti-slop / Phase-59 citation grounding carried to the surface). |
| T-62-13 | Denial of service | a malformed/non-JSON frame crashing the pill | mitigate | Reuse the shipped `connectMascotBus` (copied verbatim) which silently drops non-JSON/malformed frames and self-heals with 1→2→4→8s backoff — a bad frame can never crash the always-on-top surface. |
| T-62-14 | Spoofing / scope creep | a new ws port or a pill→Python command channel | mitigate | The pill imports the existing bus client pointed at the existing 8765 socket and registers NO message-send path — consume-only (deferred: pill→Python channel). `grep` proves no new socket/port and no `forward_ipc`-style send from the pill. |
| T-62-SC | Tampering | npm/cargo installs | accept | This plan adds NO new runtime npm dependency (reuses shipped meter.ts / citation-strip.ts / ws-client.ts; vanilla TS). slopcheck N/A — no new package in scope. |
</threat_model>

<verification>
- `npx vitest run src/pill/` all green (state-machine purity + frame→state map + reaction→expand + waveform rms→lit-count + citation-strip reuse).
- `npx tsc --noEmit` clean (pill compiles against reused component types).
- pill.html clones the transparent-overlay invariant and never references mascot.html.
- The pill is decoupled from `src/mascot/**` (ws-client copied into `src/pill/`) — mascot-audit fence un-triggered.
- KAAN-ACTION (recorded, not a blocker): the felt rendering / waveform-on-real-speech / expand on the running app.

## Dimension-8 Validation (per 62-VALIDATION.md)
| Task | Requirement | Test Type | Automated Command | KAAN-ACTION |
|------|-------------|-----------|-------------------|-------------|
| 1 | PILL-03 (pure 4-state machine + transparent pill.html) | unit (vitest) | `npx vitest run src/pill/state-machine.test.ts` | — |
| 2 | PILL-03 (consume ws:8765 frames → state map + drag) | unit (vitest/jsdom) | `npx vitest run src/pill/index.test.ts` | — |
| 3 | PILL-03 (real-voice.rms waveform + citation strip reuse) | unit (vitest) + tsc | `npx vitest run src/pill/ && npx tsc --noEmit` | — |
| 4 | PILL-03 (felt rendering) | manual | n/a — checkpoint | visual states + waveform-on-real-speech + expand |
Sampling: per-task targeted vitest; per-wave `npx vitest run && npx tsc --noEmit`. No 3 consecutive tasks without automated verify (Task 4 is the human checkpoint after 3 automated tasks).
</verification>

<success_criteria>
- PILL-03 (core): the pill consumes the EXISTING ws:8765 frames (no new port, no Python delivery
  change) and renders idle / listening / speaking / expand-on-event with the real TTS waveform
  (Levels.update_voice via voice.rms) + the citation strip (renderCitationStrip verbatim).
- The pill is a transparent-overlay webview that never paints a vignette over the desktop, drags
  from its top strip, and is fully decoupled from src/mascot/ — the mascot-audit fence stays green.
- Deck-context chips + the explicit rgba glass surface + the DMG/transparency KAAN-ACTION land in
  plan 62-05 (this plan's expand panel leaves a deck-chips mount for 62-05 to populate).
</success_criteria>

<output>
Create `.planning/phases/62-floating-pill-ui/62-04-SUMMARY.md` when done — record the four states
wired, the real-voice.rms waveform reuse, the citation-strip verbatim reuse, the src/mascot/
decoupling (ws-client copied), and the KAAN-ACTION felt-rendering note. Note 62-05 owns deck chips + rgba.
</output>
