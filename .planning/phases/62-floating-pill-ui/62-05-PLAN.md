---
phase: 62-floating-pill-ui
plan: 05
type: execute
wave: 4
depends_on: ["62-03", "62-04"]
files_modified:
  - tauri/ui/src/pill/deck-chips.ts
  - tauri/ui/src/pill/deck-chips.test.ts
  - tauri/ui/src/pill/index.ts
  - tauri/ui/src/pill/pill.css
autonomous: false
requirements: [PILL-03, PILL-01]
user_setup: []

must_haves:
  truths:
    - "The expand panel renders one deck-context chip per resolved deck — <deck> · <key> · <bpm> (e.g. 'a · 8a · 128') — sourced from the 62-03 deck_state wire field"
    - "Honest unknown is preserved end-to-end: a deck whose camelot is null on the wire renders 'unknown' in --silk-40 (never a fabricated key); if NO deck resolves, a single 'decks · unknown' chip shows"
    - "The deck-key glyph is amber ONLY when a real key resolved (--silk-40 when unknown) — the 20/80 amber discipline holds"
    - "The pill's visible surface is painted by an explicit --glass-3 rgba fill (rgba(2,3,6,0.88)), NOT OS vibrancy — guarantees mac+win parity (no opaque white box on Windows) and dodges the DMG transparency regression (tauri#13415)"
    - "backdrop-filter is a progressive enhancement layered ON TOP of the opaque-enough rgba — never the sole opacity source"
  artifacts:
    - path: "tauri/ui/src/pill/deck-chips.ts"
      provides: "deck_state → deck-context chips, honest unknown, amber-only-when-resolved key glyph"
      contains: "deckChipText"
    - path: "tauri/ui/src/pill/pill.css"
      provides: "Explicit --glass-3 rgba pill surface + token-only styling (no hex), backdrop-filter as enhancement only"
      contains: "--glass-3"
  key_links:
    - from: "tauri/ui/src/pill/deck-chips.ts"
      to: "the 62-03 deck_state wire field (per-deck camelot/key/bpm)"
      via: "render one chip per resolved deck from the frame's deck_state"
      pattern: "camelot"
    - from: "tauri/ui/src/pill/pill.css"
      to: "the visible pill surface"
      via: "explicit --glass-3 rgba fill (not OS vibrancy)"
      pattern: "var\\(--glass-3\\)"

kaan_action_live_confirm:
  - "Transparency parity on the .dmg (tauri#13415, OPEN no upstream fix): build the .dmg, open it (NOT tauri dev), confirm the pill renders as a floating dark-glass lozenge over the desktop, not a white box. On Windows confirm the explicit-glass renders (no opaque white box)."
  - "Deck chips show real key/bpm with honest unknown: with two decks loaded + a resolved key, the chip shows 'a · 8a · 128'; with an unresolved deck the key reads 'unknown' in dim ink — confirmed live."
---

<objective>
Add the pill's deck-context chips (consuming the 62-03 `deck_state` wire field) and lock the
explicit-rgba glass surface that guarantees mac+win transparency parity. The deck chips render one
`<deck> · <key> · <bpm>` chip per resolved deck with honest `unknown` when a deck/key is unresolved
(never a fabricated key). The pill's visible surface is painted by an explicit `--glass-3` rgba fill
(not OS vibrancy), which dodges the DMG transparency regression (tauri#13415) and avoids an opaque
white box on Windows.

Purpose: PILL-03 (deck-context chips with honest unknown) + PILL-01 (mac+win transparency parity via
the explicit `--glass-*` fallback). The deck chips are the soft-dependency on Phase 59 the ROADMAP
flagged — Phase 59 is COMPLETE and 62-03 puts deck_state on the wire, so they land here.

Output: `src/pill/deck-chips.ts` (deck_state → chips, honest unknown), the deck-chips mount wired
into the expand panel in `index.ts`, `src/pill/pill.css` (the explicit rgba glass surface, token-only),
vitest fences, and the DMG-transparency KAAN-ACTION checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-floating-pill-ui/62-CONTEXT.md
@.planning/phases/62-floating-pill-ui/62-RESEARCH.md
@.planning/phases/62-floating-pill-ui/62-PATTERNS.md
@.planning/phases/62-floating-pill-ui/62-UI-SPEC.md
@.planning/phases/62-floating-pill-ui/62-VALIDATION.md
@.claude/skills/frontend-enforcement/SKILL.md

<interfaces>
From the 62-03 deck_state wire field (the source the chips consume):
- The flat 30Hz mascot frame carries `deck_state: { "A": {title, camelot, key, bpm, confidence}, "B": {...} }` (per-deck, read-only). `camelot`/`key` are `null` when the deck is unresolved (honest-null contract — NEVER a fabricated key). Empty `deck_state == {}` when no deck resolved.

From tauri/ui/src/session/components/citation-strip.ts (the chip-strip skeleton to clone):
- the `vmx-citation-strip` flex layout (lines 56-68), the mono tabular-nums chip styling (lines 69-96), the resting-glow-OFF / hover-glow discipline (lines 84-103), and the return-`null`-when-empty contract (line 147) → `if (strip) panel.append(strip)`.
- `export const _CSS_FOR_TEST` (line 119) — the grep-able no-hex/amber-token assert pattern to clone.

From .planning/phases/62-floating-pill-ui/62-UI-SPEC.md:
- "Deck-context chips (expand only)": one chip per resolved deck `<deck> · <key> · <bpm>` ("a · 8a · 128"); key glyph amber when resolved, `--silk-40` when `unknown`; NO deck resolves → single `decks · unknown` chip in `--silk-40`. The chip key MUST come from registry-observed `key:` evidence, never fabricated.
- "Color": `--glass-3 = rgba(2,3,6,0.88)` is the dominant ~80% surface; amber reserved EXCLUSIVELY for the state dot, waveform fill, citation chips, and the deck-resolved key glyph (4-item list — 20/80 rule). "Glass transparency parity (PILL-04)": the visible surface MUST use the explicit `--glass-3` rgba fallback, NOT pure OS vibrancy; `backdrop-filter: var(--blur-glass)` is a progressive enhancement ON TOP, never the sole opacity source.

From .planning/phases/62-floating-pill-ui/62-PATTERNS.md:
- "deck-chips.ts" section: `deckChipText(d)` honest-unknown helper; "No Analog Found" — the deck-state wire gap (now closed by 62-03).
- "Code Examples / Deck-context chip from snapshot": `key = d.camelot ?? "unknown"`, `bpm = d.bpm != null ? Math.round(d.bpm) : "unknown"`, `${d.deck.toLowerCase()} · ${key.toLowerCase()} · ${bpm}`.

From tauri/ui/src/pill/index.ts (62-04 — the expand panel + the deck-chips mount left for this plan):
- the expand panel renders reaction text + citation strip (62-04); this plan appends the deck-chips strip below the citation strip.
- the view ref carries the latest frame (62-04's reader) — read `view.deckState` (the 62-03 wire field) here.

From tauri/ui/src/tokens.css:
- `--glass-3`, `--silk`, `--silk-40`, `--amber`, `--blur-glass`, `--rad-lg`, `--sp-*` — the only color/spacing source (no hex in pill CSS).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deck-chips.ts — deck_state → chips with honest unknown + amber-only-when-resolved (RED then GREEN)</name>
  <read_first>
    - tauri/ui/src/session/components/citation-strip.ts lines 56-103 (the chip-strip flex layout + mono chip styling + glow discipline), 119 (_CSS_FOR_TEST), 144-174 (null-on-empty)
    - .planning/phases/62-floating-pill-ui/62-UI-SPEC.md "Deck-context chips" + "Color" (amber-only-when-resolved, --silk-40 unknown)
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "deck-chips.ts" + "Code Examples / Deck-context chip"
    - .claude/skills/frontend-enforcement/SKILL.md (20/80 amber discipline)
  </read_first>
  <behavior>
    - A resolved deck `{deck:"A", camelot:"8A", bpm:128}` renders chip text `a · 8a · 128` (lowercase deck + lowercase camelot + rounded bpm).
    - An unresolved key `{deck:"A", camelot:null, bpm:128}` renders `a · unknown · 128` with the key glyph in `--silk-40` (NOT amber — amber is reserved for a resolved key, 20/80 rule).
    - An unresolved bpm `{deck:"A", camelot:"8A", bpm:null}` renders `a · 8a · unknown`.
    - NO deck resolves (empty deck_state) → a SINGLE chip `decks · unknown` in `--silk-40` (honest, never a fake key).
    - The chip strip returns `null` only in a degenerate/absent case the caller can `if (strip)`-guard; otherwise it always shows at least the `decks · unknown` chip when deck_state is present-but-empty (UI-SPEC: present-but-unresolved still shows the honest chip).
    - frontend-enforcement: the deck-chip CSS uses `var(--token)` only (no hex, no non-black rgba); amber appears ONLY on a resolved key glyph.
  </behavior>
  <action>
    Create `tauri/ui/src/pill/deck-chips.ts` cloning the `citation-strip.ts` chip-strip skeleton (flex layout, mono tabular-nums chip styling, resting-glow-OFF discipline, token-only CSS via a `_CSS_FOR_TEST` export). Implement `deckChipText(d: {deck; camelot: string|null; bpm: number|null})` per PATTERNS: `const key = d.camelot ?? "unknown"; const bpm = d.bpm != null ? Math.round(d.bpm) : "unknown"; return \`${d.deck.toLowerCase()} · ${key.toLowerCase()} · ${bpm}\`;`. Implement `renderDeckChips(deckState: Record<string, {title; camelot; key; bpm; confidence}>)`: map each resolved deck to a chip, set the key-glyph element class to amber ONLY when `camelot != null` (else `--silk-40`); when `deckState` is empty (or all unresolved with no decks), render a single `decks · unknown` chip in `--silk-40`. NEVER fabricate a key — pass `camelot` through, render `unknown` on null (anti-slop, the Phase-59 honest-null contract carried to the UI). Create `tauri/ui/src/pill/deck-chips.test.ts` covering: resolved chip text, unknown-key dim glyph (assert the unknown-key element does NOT carry the amber class), unknown-bpm, empty→`decks · unknown`, and the `_CSS_FOR_TEST` no-hex/amber-token grep (mirroring citation-strip.test.ts). Run `cd tauri/ui && npx vitest run src/pill/deck-chips.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai/tauri/ui && npx vitest run src/pill/deck-chips.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pill/deck-chips.test.ts` passes (resolved text, unknown-key dim, unknown-bpm, empty→`decks · unknown`, no-hex/amber-token CSS grep).
    - The unknown-key chip's key glyph does NOT carry the amber class (asserted in the test) — amber is reserved for a resolved key (20/80 rule).
    - `deckChipText({deck:"A",camelot:null,bpm:128})` returns `a · unknown · 128` (honest-null, never a fabricated key) — source/test assertion.
    - `grep -E "#[0-9a-fA-F]{3,6}" tauri/ui/src/pill/deck-chips.ts` returns no raw hex in the styling (token-only, frontend-enforcement).
  </acceptance_criteria>
  <done>deck-chips.ts renders honest deck-context chips from deck_state — real key/bpm when resolved (amber key glyph), `unknown` in dim ink otherwise, `decks · unknown` when nothing resolves — token-only, never fabricating a key.</done>
</task>

<task type="auto">
  <name>Task 2: Wire deck chips into the expand panel + lock the explicit --glass-3 rgba pill surface</name>
  <read_first>
    - tauri/ui/src/pill/index.ts (62-04 — the expand panel; the view ref carrying the latest frame's deck_state; the citation-strip mount the deck chips sit below)
    - tauri/ui/src/pill/deck-chips.ts (Task 1 — renderDeckChips)
    - .planning/phases/62-floating-pill-ui/62-UI-SPEC.md "Color" (the --glass-3 explicit-rgba surface contract + backdrop-filter-as-enhancement) + "Pill Dimensions" (--rad-lg)
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "Transparency parity (Leg C)" (explicit rgba, never OS vibrancy)
    - tauri/ui/mascot.html lines 66-78 (the transparent-overlay invariant — pill.css must NOT re-introduce an opaque body background)
  </read_first>
  <action>
    In `tauri/ui/src/pill/index.ts`: read `view.deckState` from the latest frame (the 62-03 `deck_state` wire field — read defensively `msg.deck_state ?? {}` in the reader; surface it on the view ref). In the expand panel, AFTER the citation strip, append `renderDeckChips(view.deckState)` (the deck chips sit below the chips per UI-SPEC §States). Tag the deck chips `[data-no-drag]` so a chip area does not start a window drag. Create `tauri/ui/src/pill/pill.css`: paint the visible pill surface with an EXPLICIT `background: var(--glass-3)` (= `rgba(2,3,6,0.88)`) — the near-opaque dark glass that is what the user actually sees — NOT OS vibrancy. Add the hairline border (`--glass-edge`) + top inner-light seam (`--glass-top`) for the sealed-glass read, `--rad-lg` (10px) corner radius, `--sp-*` spacing, the `--silk`/`--silk-65`/`--silk-40` ink scale, and the Saira + JetBrains Mono type roles per UI-SPEC §Typography. Add `backdrop-filter: var(--blur-glass)` ONLY as a progressive enhancement layered ON TOP of the opaque rgba (never the sole opacity source — Windows has no vibrancy; DMG #13415). Do NOT set an opaque `html`/`body` background (the transparent-overlay invariant from pill.html must survive — the rgba fill is on the PILL element, not the body). Token-only: no hex anywhere in pill.css. Link `pill.css` from `pill.html` (or import it in index.ts per the project's CSS-loading convention — match how the session components load their CSS). Run vitest + tsc to confirm no regression.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai/tauri/ui && npx vitest run src/pill/ 2>&1 | tail -12 && npx tsc --noEmit 2>&1 | tail -8 && (grep -qE "#[0-9a-fA-F]{3,6}" tauri/ui/src/pill/pill.css && echo "FAIL: raw hex in pill.css" && exit 1 || echo "OK: pill.css is token-only")</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/pill/` all green; `npx tsc --noEmit` clean.
    - `pill.css` paints the pill surface with `var(--glass-3)` (`grep -c "var(--glass-3)" tauri/ui/src/pill/pill.css` >= 1) — explicit rgba, not OS vibrancy.
    - `backdrop-filter` in pill.css (if present) is layered on a surface that ALSO sets `var(--glass-3)` — it is NOT the sole opacity source (inspectable: the `--glass-3` fill is on the same/parent element).
    - `pill.css` is token-only — `grep -E "#[0-9a-fA-F]{3,6}" tauri/ui/src/pill/pill.css` returns nothing (frontend-enforcement no-hex rule).
    - `index.ts` appends `renderDeckChips` in the expand panel (`grep -c "renderDeckChips" tauri/ui/src/pill/index.ts` >= 1) and reads `deck_state` from the frame (`grep -c "deck_state" tauri/ui/src/pill/index.ts` >= 1).
    - pill.css does NOT set an opaque `body { background: ... }` that would defeat the transparent-overlay invariant (`grep -E "body\s*\{[^}]*background\s*:\s*(?!transparent)" tauri/ui/src/pill/pill.css` returns nothing opaque).
  </acceptance_criteria>
  <done>The expand panel shows the honest deck-context chips below the citation strip, and the pill paints its own explicit --glass-3 rgba surface (token-only, body stays transparent) so transparency is mac+win parity-safe and DMG-#13415-resistant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Transparency + deck-chips checkpoint — confirm DMG transparency parity + live deck key/bpm (KAAN-ACTION)</name>
  <action>Human-gated checkpoint (no code change). Pause for Kaan to confirm DMG transparency parity (tauri#13415) + Windows parity + live deck-chip key/bpm with honest unknown per the steps in &lt;how-to-verify&gt;. Per `gsd-autonomous fully`, a non-approval is RECORDED in the SUMMARY KAAN-ACTION list, not a blocker — engineering ships at the green-test bar.</action>
  <what-built>
    The pill's deck-context chips (consuming the 62-03 deck_state wire field, honest `unknown` when
    unresolved) and the explicit `--glass-3` rgba glass surface that the user actually sees. Engineering
    ships at the green-vitest/tsc bar; the felt transparency on the built `.dmg` (tauri#13415, OPEN, no
    upstream fix) and the live deck-chip values are the recorded KAAN-ACTION carve-outs per
    `gsd-autonomous fully` — surface, do not pause.
  </what-built>
  <how-to-verify>
    1. TRANSPARENCY ON THE .dmg (tauri#13415 — the genuine build-only check): build the `.dmg` (`cargo tauri build` / the repo's bundle command), OPEN the built app (NOT `tauri dev`), and confirm the pill renders as a floating dark-glass lozenge over the desktop — NOT a solid white box. The explicit rgba should read correctly regardless; note the chrome-edge transparency outcome.
    2. WINDOWS PARITY (if a Windows build is available): confirm the pill is dark glass, not an opaque white box (no reliance on OS vibrancy).
    3. DECK CHIPS LIVE: load two decks in the DJ app with a resolved key on one; on a co-host reaction, confirm the expand panel shows a chip like `a · 8a · 128` for the resolved deck and `unknown` (dim) for an unresolved deck/key — never a fabricated key.
    4. Report the DMG transparency outcome + whether deck chips showed real key/bpm with honest unknown.
  </how-to-verify>
  <resume-signal>Type "approved", or describe the DMG transparency / deck-chip behavior so it is recorded in the SUMMARY KAAN-ACTION list. Per `gsd-autonomous fully`, a non-approval here is RECORDED, not a blocker — engineering ships at the green-test bar and the felt notes become follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deck_state wire field → pill DOM | The deck_state values (camelot/key/bpm) are rendered as text-node chips, NEVER `eval`'d or `innerHTML`'d. The chip key is passed through verbatim (honest-null), never computed or fabricated pill-side. |
| CSS surface → OS compositor | The pill paints its own rgba surface; it relies on no OS-vibrancy privilege. No new capability is needed beyond the `"pill"` label from 62-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-15 | Information disclosure / Spoofing | a fabricated key on a deck chip (false-confident harmonic claim on the always-on-top surface) | mitigate | `deckChipText` renders `unknown` whenever `camelot` is null — it NEVER fabricates a key (anti-slop, Phase-59 honest-null carried to the UI). A Task-1 test asserts the unknown-key glyph is dim (not amber) and the text reads `unknown`. The amber key glyph appears ONLY when a real registry-observed key resolved. |
| T-62-16 | Tampering / XSS | deck title/key rendered into the chip DOM | mitigate | Chip text is set via `textContent` (text node), not `innerHTML`; the values come from the contract-shaped 62-03 wire field. No HTML injection path. |
| T-62-17 | Denial (usability) / parity | opaque white box on Windows or on the DMG build (#13415) | mitigate | The visible surface is an explicit `var(--glass-3)` rgba fill (not OS vibrancy), so the content reads correctly on mac+win regardless of vibrancy support; `backdrop-filter` is enhancement-only. The body stays transparent (overlay invariant). The chrome-edge DMG outcome is the recorded KAAN-ACTION build check. |
| T-62-18 | Tampering | a raw hex / non-token color breaking the 20/80 frontend-enforcement contract | mitigate | pill.css + deck-chips.ts are token-only (no-hex grep gate in the verify); amber is reserved for the 4-item accent list (state dot / waveform / citation chips / resolved key glyph). |
| T-62-SC | Tampering | npm/cargo installs | accept | This plan adds NO new dependency (reuses the citation-strip skeleton + tokens.css; vanilla TS + CSS). slopcheck N/A — no new package in scope. |
</threat_model>

<verification>
- `npx vitest run src/pill/` all green (deck-chips honest-unknown + amber-only-when-resolved + the full pill suite); `npx tsc --noEmit` clean.
- deck-chips.ts + pill.css are token-only (no-hex grep); amber reserved for the resolved-key glyph (20/80).
- The pill surface is an explicit `var(--glass-3)` rgba (not OS vibrancy); body stays transparent (overlay invariant intact).
- index.ts reads `deck_state` from the frame and mounts `renderDeckChips` in the expand panel below the citation strip.
- KAAN-ACTION (recorded, not a blocker): DMG transparency (#13415) + Windows parity + live deck-chip key/bpm with honest unknown.

## Dimension-8 Validation (per 62-VALIDATION.md)
| Task | Requirement | Test Type | Automated Command | KAAN-ACTION |
|------|-------------|-----------|-------------------|-------------|
| 1 | PILL-03 (deck chips, honest unknown, amber-only-when-resolved) | unit (vitest) | `npx vitest run src/pill/deck-chips.test.ts` | — |
| 2 | PILL-03 (deck chips wired) + PILL-01 (explicit rgba transparency parity) | unit (vitest) + tsc + no-hex grep | `npx vitest run src/pill/ && npx tsc --noEmit && grep no-hex` | — |
| 3 | PILL-01 (DMG transparency) + PILL-03 (live deck chips) | manual | n/a — checkpoint | DMG #13415 + Windows parity + live deck key/bpm |
Sampling: per-task targeted vitest; per-wave `npx vitest run && npx tsc --noEmit`. No 3 consecutive tasks without automated verify (Task 3 is the human checkpoint after 2 automated tasks).
</verification>

<success_criteria>
- PILL-03 (deck polish): the expand panel renders honest deck-context chips (`<deck> · <key> · <bpm>`)
  from the 62-03 deck_state wire field — real key/bpm when resolved (amber key glyph), `unknown` in
  dim ink otherwise, `decks · unknown` when nothing resolves; never a fabricated key.
- PILL-01 (transparency parity): the pill's visible surface is an explicit `--glass-3` rgba fill (not
  OS vibrancy) → mac+win parity (no opaque white box on Windows) + DMG-#13415 resistance; body stays
  transparent (overlay invariant). The felt DMG transparency is the recorded KAAN-ACTION.
</success_criteria>

<output>
Create `.planning/phases/62-floating-pill-ui/62-05-SUMMARY.md` when done — record the deck-chips
honest-unknown wiring (consuming 62-03's deck_state), the explicit-rgba glass surface (token-only,
body transparent), and the DMG-transparency + live-deck-chip KAAN-ACTION notes.
</output>
