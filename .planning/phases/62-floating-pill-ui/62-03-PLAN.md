---
phase: 62-floating-pill-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/runtime/ws_bus.py
  - tests/runtime/test_ws_bus_deck_state.py
autonomous: true
requirements: [PILL-03]
user_setup: []

must_haves:
  truths:
    - "The flat 30Hz mascot frame on ws://127.0.0.1:8765 carries an additive deck_state field — per-deck {title, camelot, key, bpm, confidence} read from MusicState.deck_state"
    - "Honest unknown is preserved on the wire: a deck with no resolved key serializes camelot=null and key=null (never a fabricated key)"
    - "When MusicState.deck_state is empty (no decks resolved), deck_state serializes as an empty object — golden-equivalence: existing subscribers are undisturbed (additive-only)"
    - "No new port, no new socket, no new delivery mechanism — the deck_state field rides the EXISTING frame on the EXISTING socket; the single-writer rule is untouched (ws_bus only READS state.deck_state)"
    - "The deck_state values are derived from the registry-observed Phase-59 DeckTrack fields, not recomputed — ws_bus is a pure read/serialize boundary"
  artifacts:
    - path: "src/vibemix/runtime/ws_bus.py"
      provides: "Additive deck_state field on the flat 30Hz mascot_frame dict, read-only from MusicState.deck_state"
      contains: "deck_state"
    - path: "tests/runtime/test_ws_bus_deck_state.py"
      provides: "Serialization tests: populated deck → wire shape; unresolved deck → null camelot/key; empty deck_state → {} (golden-equivalence)"
      contains: "deck_state"
  key_links:
    - from: "src/vibemix/runtime/ws_bus.py mascot_frame"
      to: "MusicState.deck_state.decks"
      via: "additive read-only serialization of per-deck DeckTrack fields into the flat frame"
      pattern: "deck_state"
    - from: "deck_state wire field"
      to: "DeckTrack.camelot / DeckTrack.key (honest None)"
      via: "null on the wire when the deck is unresolved (never fabricate)"
      pattern: "camelot"
---

<objective>
Serialize Phase-59 per-deck `deck_state` onto the EXISTING flat 30Hz frame on the EXISTING
`ws://127.0.0.1:8765` socket so the pill's deck-context chips have real per-deck title/key/BPM to
render. This closes the load-bearing wire-source gap PATTERNS flagged: `MusicState.deck_state`
exists in-process (Phase 59) but is NOT on either WS frame today, so deck chips have nothing to read.

This is the resolved grey-area #1 decision baked in: ADD a `deck_state` field to the EXISTING
snapshot frame on the EXISTING socket (additive serialization, read-only, single-writer respected).
"No Python delivery change" = no NEW port / NO new socket / NO transport rework — an additive field
on the existing frame is the same delivery path and is REQUIRED for the deck-chip key value the
UI-SPEC promises.

Purpose: PILL-03 (deck-context chips need per-deck camelot/key/bpm on the wire). This plan is the
PRODUCER half; plan 62-04's `deck-chips.ts` is the CONSUMER half (wave-ordered AFTER this).

Output: an additive `deck_state` field on the flat mascot frame in `ws_bus.py`, with honest-null
key handling and golden-equivalence on empty deck_state, plus a pytest serialization fence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-floating-pill-ui/62-CONTEXT.md
@.planning/phases/62-floating-pill-ui/62-RESEARCH.md
@.planning/phases/62-floating-pill-ui/62-PATTERNS.md
@.planning/phases/62-floating-pill-ui/62-VALIDATION.md

<interfaces>
From src/vibemix/state/deck_state.py (Phase 59 — the in-process source, ALREADY shipped):
- `@dataclass class DeckTrack` — fields: `title: str|None`, `track_id: str|None`, `bpm: float=0.0`,
  `key: str|None` (RAW tag "Am"/"F#m"), `camelot: str|None` (normalized "8A"), `open_key: str|None`,
  `energy: int|None`, `loaded_at: float`, `confidence: float=0.0`, `source: str="unknown"`.
  Honest-default contract: a bare DeckTrack has NO key/camelot/bpm and source="unknown" — never a
  fabricated key BY CONSTRUCTION.
- `@dataclass class DeckState` — `decks: dict[str, DeckTrack]` (e.g. {"A": DeckTrack, "B": ...});
  `updated_at: float`. Empty `decks` dict = no decks resolved.
- `MusicState.deck_state` is a default-empty field of MusicState (Plan 59-01) — single-writer
  `_tick_once` is the ONLY writer; ws_bus only READS it.

From src/vibemix/runtime/ws_bus.py (the serialization site):
- `ws_broadcast(...)` loop builds `mascot_frame = { **levels.snapshot(), "audible": state.audible,
  "deck": state.audible_deck, "phase": state.phase, "bpm": state.bpm, ... }` (lines 266-291) — a
  PLAIN DICT (not the strict `additionalProperties:false` SessionSnapshot schema) → the natural
  additive site. Already carries flat `voice`/`music`/`mic` (line 266 `**levels.snapshot()`) and
  flat `deck = state.audible_deck` (line 269). The pill reads THIS frame via `connectMascotBus`
  (direct-WS, per RESEARCH A4).
- Emit-boundary guard (lines 297-307): never serialize an empty/meter-less frame — the additive
  deck_state field must not break the `("music","voice","mic")` key-presence check.
- `payload = json.dumps(mascot_frame)` (line 308) then broadcast to clients.
- The `ipc.session.snapshot` path (the strict schema, ~15Hz, lines ~310-342) is NOT the pill's path
  — do NOT touch the strict SessionSnapshot schema. The pill reads the flat frame.

From tests/runtime/ (existing serialization-test analogs to clone):
- tests/runtime/test_ws_bus_genre_fields.py — the canonical "additive field on the flat mascot_frame"
  test pattern (asserts a new state attr appears on the serialized frame). Clone its harness shape.
- tests/runtime/test_ws_bus_empty_frames.py — the emit-boundary guard test (empty/meter-less frame
  is skipped). Confirm the additive field does not regress this.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 RED — deck_state serialization fence (populated / unresolved-null / empty-equivalence)</name>
  <read_first>
    - tests/runtime/test_ws_bus_genre_fields.py (the additive-field-on-flat-frame test harness to clone — how it constructs a MusicState/Levels and asserts the serialized frame)
    - src/vibemix/state/deck_state.py (DeckTrack/DeckState shape — the honest-default contract)
    - .planning/phases/62-floating-pill-ui/62-VALIDATION.md (PILL-03 deck-state-on-wire row)
  </read_first>
  <behavior>
    - Populated deck: a MusicState whose `deck_state.decks = {"A": DeckTrack(title="Strobe", camelot="8A", key="Am", bpm=128.0, confidence=0.8)}` serializes a `deck_state` entry for "A" carrying `title="Strobe"`, `camelot="8A"`, `key="Am"`, `bpm=128.0` on the flat mascot frame.
    - Unresolved deck (honest unknown): a DeckTrack with `camelot=None`, `key=None` serializes `camelot: null` and `key: null` on the wire — NEVER a fabricated key string.
    - Empty deck_state (golden-equivalence): a MusicState with `deck_state.decks == {}` serializes `deck_state` as an empty object `{}` (or the agreed empty shape) and does NOT remove/alter any existing flat key (`music`/`voice`/`mic`/`deck`/`bpm`/`phase` unchanged) — additive-only.
    - Emit-boundary intact: a frame carrying deck_state still passes the `("music","voice","mic")` key-presence guard (the additive field never trips the malformed-frame skip).
  </behavior>
  <action>
    Create `tests/runtime/test_ws_bus_deck_state.py` cloning the construction harness from `test_ws_bus_genre_fields.py` (build a minimal `MusicState` + a fake `Levels` whose `snapshot()` returns the three meter keys, call the frame-builder or drive one `ws_broadcast` tick, capture the serialized `mascot_frame` dict / its `json.dumps` round-trip). Write the four behaviors above as RED tests against the not-yet-present `deck_state` field. Use the Phase-59 `DeckTrack`/`DeckState` constructors directly (import from `vibemix.state.deck_state`). Assert the honest-null contract explicitly: `frame["deck_state"]["A"]["camelot"] is None` for an unresolved deck (anti-slop — the wire must carry null, never a guess). Assert golden-equivalence: build the SAME frame with empty `deck_state.decks` and confirm every pre-existing flat key is byte-identical to a pre-deck_state baseline frame. Run under `.venv` (system python3.14 has incompatible google.genai per STATE) — `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/runtime/test_ws_bus_deck_state.py`. These tests are RED until Task 2 adds the field.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/runtime/test_ws_bus_deck_state.py 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/runtime/test_ws_bus_deck_state.py` exists and collects 4 tests under pytest (populated, unresolved-null, empty-equivalence, emit-boundary).
    - The tests are RED (fail) before Task 2 — they reference a `deck_state` key not yet on the frame (this is the RED-first proof the field is genuinely absent today, matching PATTERNS' wire-gap finding).
    - The test file imports `DeckTrack`/`DeckState` from `vibemix.state.deck_state` (uses the real Phase-59 model, not a mock shape).
  </acceptance_criteria>
  <done>The deck_state serialization fence exists and is RED — proving the wire-gap and pinning the honest-null + golden-equivalence contract before any production change.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — add the additive deck_state field to the flat mascot frame (read-only, honest-null)</name>
  <read_first>
    - src/vibemix/runtime/ws_bus.py lines 266-308 (the `mascot_frame` dict construction + the emit-boundary guard + the `json.dumps` send)
    - src/vibemix/state/deck_state.py (DeckTrack/DeckState — the read-only source fields)
    - tests/runtime/test_ws_bus_deck_state.py (the RED fence from Task 1 — make it GREEN)
    - .planning/phases/62-floating-pill-ui/62-PATTERNS.md "No Analog Found" (the deck-state-on-wire gap + the safe additive reading of "no Python delivery change")
  </read_first>
  <action>
    In `src/vibemix/runtime/ws_bus.py`, add ONE additive key to the flat `mascot_frame` dict (lines 266-291): `"deck_state": _serialize_deck_state(state.deck_state)` where `_serialize_deck_state` is a small pure helper (define it module-level near the frame builder) that maps `DeckState.decks` → `{side: {"title": dt.title, "camelot": dt.camelot, "key": dt.key, "bpm": dt.bpm, "confidence": dt.confidence}}`. Read `getattr(state, "deck_state", None)` defensively (a state without deck_state → empty `{}`) so the field is robust against older state objects. CRITICAL honest-null contract: pass `dt.camelot` and `dt.key` THROUGH as-is — when they are `None`, the JSON carries `null`; NEVER substitute a placeholder/fabricated key (anti-slop, Phase-59 contract). CRITICAL single-writer: this is a pure READ of `state.deck_state` at the serialize boundary — do NOT write to MusicState, do NOT recompute camelot (the writer-side `_tick_once` already normalized it via `harmonics.to_camelot`). CRITICAL golden-equivalence: when `decks` is empty, emit `deck_state: {}` and leave every other flat key untouched — additive-only, no reordering of existing keys. Do NOT touch the strict `ipc.session.snapshot` schema path (the pill reads the flat frame). Confirm the emit-boundary guard (lines 297-307) still keys only on `("music","voice","mic")` — the additive deck_state field does not change that check. Run the Task-1 fence to GREEN, then run the existing ws_bus suite to confirm no regression on the empty-frame / genre-field tests.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/runtime/test_ws_bus_deck_state.py tests/runtime/test_ws_bus_empty_frames.py tests/runtime/test_ws_bus_genre_fields.py 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `tests/runtime/test_ws_bus_deck_state.py` is GREEN (all 4 behaviors pass): populated deck carries title/camelot/key/bpm; unresolved deck carries `camelot: null` + `key: null`; empty deck_state → `{}` with all existing flat keys byte-identical; emit-boundary guard still passes.
    - `tests/runtime/test_ws_bus_empty_frames.py` and `tests/runtime/test_ws_bus_genre_fields.py` still pass (no regression — additive-only).
    - `grep -c "deck_state" src/vibemix/runtime/ws_bus.py` >= 1 (the field is on the flat frame); `grep -c "deck_state.*=" src/vibemix/runtime/ws_bus.py` shows NO assignment INTO `state.deck_state` (read-only — single-writer respected).
    - No new port/socket: `grep -nE "websockets.serve|new.*socket|:[0-9]{4}" src/vibemix/runtime/ws_bus.py` shows no NEW listening port beyond the existing 8765 bus (delivery path unchanged).
  </acceptance_criteria>
  <done>The flat 30Hz frame carries an additive read-only deck_state field with honest-null keys and golden-equivalence on empty; the existing ws_bus suite is regression-free; no delivery-mechanism change.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate (no NEW failures beyond the 7-WIP baseline)</name>
  <read_first>
    - .planning/phases/62-floating-pill-ui/62-VALIDATION.md ("Before /gsd:verify-work: repo pytest at the documented 7-WIP baseline (no NEW failures)")
    - .planning/STATE.md (the documented 7 pre-existing live-tuning-or-brain WIP failures — release/readme-matrix/anti-slop-wiring/main-smoke; NONE touch ws_bus deck serialization)
  </read_first>
  <action>
    Run the full pytest suite under `.venv` and confirm the additive deck_state field introduces ZERO new failures — the count must hold at the documented 7 pre-existing `live-tuning-or-brain` WIP failures (release/readme-matrix/anti-slop-wiring/main-smoke), none of which touch ws_bus or deck serialization. If the count exceeds 7, the additive field regressed something — fix before completing. Record the exact failed/passed/skipped counts in the SUMMARY and re-confirm in the phase `deferred-items.md` that the 7 are the same pre-existing WIP set (grep the failing test names to prove they are NOT deck_state/ws_bus-related). This task adds no production change; it is the regression fence the test-discipline section requires.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest run completes; the FAILED count is <= 7 (the documented pre-existing `live-tuning-or-brain` WIP baseline) — ZERO new failures introduced by the deck_state field.
    - The failing test names match the documented 7-WIP set (release / readme-matrix / anti-slop-wiring / main-smoke) — none reference `deck_state` / `ws_bus` / deck serialization (grep-proven in the SUMMARY).
    - The new `test_ws_bus_deck_state.py` tests are GREEN within that run (counted in passed).
  </acceptance_criteria>
  <done>The additive deck_state serialization holds the full-suite baseline at 7-WIP failures with zero new regressions; the deck-chip wire source is ready for plan 62-04's consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MusicState (in-process) → ws:8765 frame | ws_bus reads `state.deck_state` and serializes it onto the existing localhost frame. The boundary is read-only at the serialize edge; the single writer (`_tick_once`) is upstream and untouched. |
| ws:8765 frame → webview consumer (pill) | The frame is broadcast as JSON text over the EXISTING localhost socket to localhost-only clients. The pill renders deck_state values as structured text/chips, never `eval`'d. No remote input is accepted on the bus (consume-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Tampering | single-writer rule (a serialize-side write into MusicState) | mitigate | The deck_state serialization is a PURE READ of `state.deck_state` at the frame-build boundary; the acceptance grep proves no `state.deck_state =` assignment in ws_bus. `_tick_once` remains the only writer (Phase-59 invariant preserved). |
| T-62-08 | Information disclosure / Spoofing | fabricated key on the wire (false-confident harmonic claim) | mitigate | Honest-null contract: `camelot`/`key` pass through as-is, serializing `null` when unresolved — the wire NEVER carries a fabricated key. A Task-1 RED test pins `camelot is None` for an unresolved deck (anti-slop, Phase-59 uncitable-by-construction guarantee carried to the UI surface). |
| T-62-09 | Denial of service | additive field breaking the emit-boundary guard / golden-equivalence | mitigate | The emit-boundary guard keys only on `("music","voice","mic")`; the additive field cannot trip it. Empty deck_state → `{}` with all existing flat keys byte-identical (golden-equivalence test) — existing subscribers (mascot.html) are undisturbed. |
| T-62-10 | Elevation / scope creep | a new ws port or transport rework masquerading as "delivery" | mitigate | The field rides the EXISTING frame on the EXISTING 8765 socket; the acceptance grep proves no new listening port. "No Python delivery change" (one-socket invariant) is honored — additive field, not a new mechanism. |
| T-62-SC | Tampering | npm/pip/cargo installs | accept | This plan adds NO package (pure Python serialization of an existing in-process model). slopcheck N/A — no new dependency in scope. |
</threat_model>

<verification>
- `test_ws_bus_deck_state.py` GREEN: populated deck on the wire; unresolved deck → `camelot:null`/`key:null`; empty deck_state → `{}` with golden-equivalent flat keys; emit-boundary intact.
- `test_ws_bus_empty_frames.py` + `test_ws_bus_genre_fields.py` still GREEN (additive-only, no regression).
- ws_bus is read-only on `state.deck_state` (no assignment-into grep) — single-writer preserved.
- No new ws port — delivery mechanism unchanged (one-socket invariant honored).
- Full pytest at the documented 7-WIP baseline; zero new failures.

## Dimension-8 Validation (per 62-VALIDATION.md)
| Task | Requirement | Test Type | Automated Command | KAAN-ACTION |
|------|-------------|-----------|-------------------|-------------|
| 1 | PILL-03 (deck-state-on-wire contract, RED) | unit (pytest) | `pytest -q tests/runtime/test_ws_bus_deck_state.py` | — |
| 2 | PILL-03 (additive serialization, honest-null, GREEN) | unit (pytest) | `pytest -q test_ws_bus_deck_state.py test_ws_bus_empty_frames.py test_ws_bus_genre_fields.py` | — |
| 3 | PILL-03 (no full-suite regression) | repo (pytest) | `pytest -q` (full) | — |
Sampling: per-task targeted pytest; per-wave full pytest. All three tasks carry an `<automated>` command (no 3 consecutive un-verified tasks). Run under `.venv` (system python3.14 incompatible).
</verification>

<success_criteria>
- PILL-03 (producer half): the flat 30Hz frame on the EXISTING ws:8765 socket carries an additive
  read-only `deck_state` field with per-deck title/camelot/key/bpm so the pill's deck-context chips
  have a real source — honest `null` when a deck/key is unresolved (never fabricated).
- One-socket / single-writer / "trust the audio" invariants all preserved: no new port, no
  delivery-mechanism change, no write into MusicState, no recomputed key, golden-equivalence on empty.
</success_criteria>

<output>
Create `.planning/phases/62-floating-pill-ui/62-03-SUMMARY.md` when done — record the wire shape of
the deck_state field, confirm honest-null + golden-equivalence + single-writer-respected, and the
full-suite count (held at the 7-WIP baseline). Note this is the PRODUCER for 62-04's deck-chips consumer.
</output>
