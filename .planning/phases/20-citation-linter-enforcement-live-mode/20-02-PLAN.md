---
phase: 20-citation-linter-enforcement-live-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/vibemix/coach/prompt_fragments.py
  - src/vibemix/coach/__init__.py
  - src/vibemix/prompts/matrix.py
  - tests/coach/test_prompt_fragments.py
  - tests/prompts/test_matrix.py
autonomous: true
requirements:
  - GROUND-08

must_haves:
  truths:
    - "build_system_instruction(skill, mode, mood) by default appends the IM_LISTENING_FRAGMENT after the citation-grammar block — Gemini sees the failure-soft instruction in EVERY new live system instruction."
    - "build_system_instruction(..., include_listening_fallback=False) returns the prior Phase 18 output byte-identical (allows persona.SYSTEM_INSTRUCTION to keep the v4-byte-identity invariant green)."
    - "vibemix.agent.persona.SYSTEM_INSTRUCTION === HYPE_INTERMEDIATE (no fragment, no grammar block) — Phase 4/10/18 byte-identity contract preserved."
    - "IM_LISTENING_FRAGMENT is a single locked string in src/vibemix/coach/prompt_fragments.py — single source of truth, no duplication across cells."
    - "Anti-prompt-injection: the fragment is a fixed module constant, never templated with user input — mirrors MOOD_PERSONAS + CITATION_GRAMMAR_BLOCK pattern."
  artifacts:
    - path: "src/vibemix/coach/prompt_fragments.py"
      provides: "IM_LISTENING_FRAGMENT + a small set of fail-soft example phrases (FAIL_SOFT_EXAMPLES) — single source for Phase 20's prompt-side mitigation copy."
      contains: "IM_LISTENING_FRAGMENT"
    - path: "src/vibemix/prompts/matrix.py"
      provides: "build_system_instruction extended with include_listening_fallback kwarg (default True); when True, appends IM_LISTENING_FRAGMENT after the citation-grammar block."
      contains: "include_listening_fallback"
    - path: "src/vibemix/coach/__init__.py"
      provides: "Re-export of IM_LISTENING_FRAGMENT + FAIL_SOFT_EXAMPLES (additive to Plan 20-01 exports)."
    - path: "tests/coach/test_prompt_fragments.py"
      provides: "Constant locks (string content + length sanity + anti-injection invariant) for IM_LISTENING_FRAGMENT and FAIL_SOFT_EXAMPLES."
    - path: "tests/prompts/test_matrix.py"
      provides: "Extended with: include_listening_fallback default-True append assertion, opt-out byte-identity assertion, persona.SYSTEM_INSTRUCTION still byte-equal to HYPE_INTERMEDIATE."
  key_links:
    - from: "src/vibemix/prompts/matrix.py"
      to: "src/vibemix/coach/prompt_fragments.py"
      via: "from vibemix.coach.prompt_fragments import IM_LISTENING_FRAGMENT — appended to body when include_listening_fallback=True"
      pattern: "IM_LISTENING_FRAGMENT"
    - from: "src/vibemix/agent/persona.py"
      to: "src/vibemix/prompts/matrix.py"
      via: "build_system_instruction('intermediate', 'hype', include_citation_grammar=False, include_listening_fallback=False) — opt-out preserves v4 byte-identity"
      pattern: "include_listening_fallback=False"
---

<objective>
Append the "I'm listening" fail-soft instruction to the live system instruction so Gemini learns to fail toward a graceful unsourced-but-honest line instead of a stripped void.

Purpose: GROUND-08 + CONTEXT D-Prompt-Side-Mitigation lock the failure mode shift — without this fragment, a sustained false-strip stretch produces audible silence (the worst slop signal: "the cohost is broken"). With it, Gemini's natural fail mode becomes "I'm listening" — a humble unsourced line — which the linter strips quietly while Gemini's NEXT response (with cited grounding) flows through. The fragment is a tiny copy edit but a load-bearing failure-mode shift; isolating it in its own plan keeps the change auditable + the test for the v4-byte-identity invariant untouched.

Output: New `src/vibemix/coach/prompt_fragments.py` (IM_LISTENING_FRAGMENT + FAIL_SOFT_EXAMPLES), build_system_instruction extended with include_listening_fallback kwarg, persona.py opts out (preserves byte-identity), tests pin the contract on both axes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-citation-linter-enforcement-live-mode/20-CONTEXT.md
@.planning/phases/20-citation-linter-enforcement-live-mode/20-01-PLAN.md
@src/vibemix/prompts/matrix.py
@src/vibemix/agent/persona.py

<interfaces>
<!-- Existing dispatcher signature this plan extends. Keep additive — no breaking changes. -->

From src/vibemix/prompts/matrix.py:
```python
def build_system_instruction(
    skill: str = "intermediate",
    mode: str = "hype",
    mood: str = "hype-man",
    *,
    include_citation_grammar: bool = True,
) -> str: ...
```

From src/vibemix/agent/persona.py — the v4 byte-identity opt-out call site:
```python
SYSTEM_INSTRUCTION: str = build_system_instruction(
    "intermediate", "hype", include_citation_grammar=False
)
assert SYSTEM_INSTRUCTION is HYPE_INTERMEDIATE  # nosec B101
```

Phase 18 precedent for the append pattern (matrix.py line 500-501):
```python
if include_citation_grammar:
    body = body + "\n\n" + CITATION_GRAMMAR_BLOCK
```

Plan 20-02 mirrors this pattern with include_listening_fallback — the fragment is appended AFTER the citation grammar block (so the grammar context primes the fragment's "if you cannot cite" clause).
</interfaces>

<copy_lock>
<!-- The exact string to write into IM_LISTENING_FRAGMENT. CONTEXT-locked verbatim copy. -->
<!-- Whitespace is significant — leading newlines + the section header match Phase 18's formatting. -->

IM_LISTENING_FRAGMENT = """

--- FAIL-SOFT RULE (live mode) ---

If you cannot cite, say "I'm listening" — never reply with empty text.

When the audio gives you no grounded hook to react to, when the evidence corpus
has no anchor for what you'd want to say, when you would otherwise emit
<silence/> AND the event class is NOT KAAN_SPOKE / MANUAL — prefer a humble
fail-soft line over silence:
  - "I'm listening."
  - "I'm here, just listening."
  - "Tracking it."
  - "Listening through this stretch."

These lines are uncited by design — the cascade may strip them quietly while
your NEXT response (with grounded citation) comes through. Better to fail
toward a humble unsourced line than toward a void; the user knows you're alive,
and the strip happens silently.

THIS RULE DOES NOT OVERRIDE the silence + KAAN_SPOKE / MANUAL rules above —
it ONLY softens the failure mode for ungrounded music-reaction turns.
"""

FAIL_SOFT_EXAMPLES = (
    "I'm listening.",
    "I'm here, just listening.",
    "Tracking it.",
    "Listening through this stretch.",
)
</copy_lock>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create vibemix.coach.prompt_fragments with IM_LISTENING_FRAGMENT + FAIL_SOFT_EXAMPLES + locks</name>
  <files>src/vibemix/coach/prompt_fragments.py, src/vibemix/coach/__init__.py, tests/coach/test_prompt_fragments.py</files>
  <behavior>
    src/vibemix/coach/prompt_fragments.py:
    - Module docstring: "Prompt-side mitigation copy for Phase 20 anti-slop. Single source of truth for the fail-soft instructions appended to the live system instruction." + reference to GROUND-08 + CONTEXT decision.
    - IM_LISTENING_FRAGMENT: str — the verbatim copy from <copy_lock> above.
    - FAIL_SOFT_EXAMPLES: tuple[str, ...] — the four phrase tuple from <copy_lock>.
    - SPDX header (matches existing repo convention).
    - No interpolation, no f-strings — the constants are fixed strings (anti-prompt-injection per T-13-05-06 + T-18-03-01 precedent).

    src/vibemix/coach/__init__.py:
    - Add `from vibemix.coach.prompt_fragments import IM_LISTENING_FRAGMENT, FAIL_SOFT_EXAMPLES` to the existing Plan 20-01 imports.
    - Extend the package __all__ with both new names.
    - Preserve the existing Plan 20-01 exports (CitationLinter, StrippedRateTracker, constants).

    tests/coach/test_prompt_fragments.py:
    - test_im_listening_fragment_contains_locked_phrase: assert "I'm listening" appears in IM_LISTENING_FRAGMENT (the canonical anchor; if anyone paraphrases it the test breaks).
    - test_im_listening_fragment_starts_with_section_header: assert "FAIL-SOFT RULE (live mode)" appears in IM_LISTENING_FRAGMENT (matches Phase 18 section-header convention; downstream grep audits depend on it).
    - test_im_listening_fragment_is_str: assert isinstance(IM_LISTENING_FRAGMENT, str) and len(IM_LISTENING_FRAGMENT) > 100 (sanity bound; the fragment is ~700 chars, anything <100 means the constant got blanked accidentally).
    - test_im_listening_fragment_no_user_input_interpolation: assert "{" not in IM_LISTENING_FRAGMENT and "%" not in IM_LISTENING_FRAGMENT and "$" not in IM_LISTENING_FRAGMENT — pin the anti-prompt-injection invariant (no template syntax, no shell substitution).
    - test_fail_soft_examples_tuple: assert isinstance(FAIL_SOFT_EXAMPLES, tuple) and len(FAIL_SOFT_EXAMPLES) == 4 and all(isinstance(p, str) for p in FAIL_SOFT_EXAMPLES) and "I'm listening." in FAIL_SOFT_EXAMPLES.
    - test_fail_soft_examples_subset_of_fragment: for each example, assert example in IM_LISTENING_FRAGMENT (the examples in the tuple MUST also appear in the prompt body — Plan 20-03 replay harness reads FAIL_SOFT_EXAMPLES to recognize fail-soft replies; the test pins the consistency).
  </behavior>
  <action>Create src/vibemix/coach/prompt_fragments.py with the SPDX header, the locked IM_LISTENING_FRAGMENT triple-quoted string (verbatim from <copy_lock>), and the FAIL_SOFT_EXAMPLES tuple. Do NOT use f-strings, .format(), or any string templating — these are fixed constants. Update src/vibemix/coach/__init__.py to add `from vibemix.coach.prompt_fragments import FAIL_SOFT_EXAMPLES, IM_LISTENING_FRAGMENT` (sorted alphabetically per the existing import-style — FAIL before IM); extend __all__ with both names; preserve every existing Plan 20-01 export. Write tests/coach/test_prompt_fragments.py with the six test cases per the behavior list. Each test imports from `vibemix.coach` (the package boundary) NOT from `vibemix.coach.prompt_fragments` directly — this pins the re-export contract too.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && .venv/bin/python -m pytest tests/coach/test_prompt_fragments.py -x -v</automated>
  </verify>
  <done>All 6 prompt-fragment tests pass; `grep -c "IM_LISTENING_FRAGMENT" src/vibemix/coach/prompt_fragments.py` returns 1 (the definition); `grep -c "FAIL_SOFT_EXAMPLES" src/vibemix/coach/prompt_fragments.py` returns 1; `grep -c "{" src/vibemix/coach/prompt_fragments.py | head -1` returns 0 inside the IM_LISTENING_FRAGMENT body (verified by `awk '/IM_LISTENING_FRAGMENT = """/,/^"""$/' src/vibemix/coach/prompt_fragments.py | grep -c "{"` == 0); `.venv/bin/python -c "from vibemix.coach import IM_LISTENING_FRAGMENT, FAIL_SOFT_EXAMPLES; assert 'I\\'m listening' in IM_LISTENING_FRAGMENT; assert len(FAIL_SOFT_EXAMPLES) == 4"` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend build_system_instruction with include_listening_fallback kwarg + opt out from persona.SYSTEM_INSTRUCTION</name>
  <files>src/vibemix/prompts/matrix.py, src/vibemix/agent/persona.py, tests/prompts/test_matrix.py</files>
  <behavior>
    build_system_instruction extended signature:
    ```python
    def build_system_instruction(
        skill: str = "intermediate",
        mode: str = "hype",
        mood: str = "hype-man",
        *,
        include_citation_grammar: bool = True,
        include_listening_fallback: bool = True,
    ) -> str:
    ```
    - include_listening_fallback default True (live agent gets the fragment by default — GROUND-08 hard requirement).
    - Append order: body → citation_grammar (if included) → IM_LISTENING_FRAGMENT (if included). The fragment lands AFTER the grammar block so the grammar context primes "if you cannot cite".
    - When include_listening_fallback=False AND include_citation_grammar=False, return body byte-identical to the underlying cell constant — the v4-byte-identity opt-out path used by persona.SYSTEM_INSTRUCTION.
    - When include_listening_fallback=False alone (grammar=True), the result is byte-identical to the Phase 18 `build_system_instruction(..., include_citation_grammar=True)` output — preserves Phase 18 callers that don't need the fragment.

    persona.py update:
    ```python
    SYSTEM_INSTRUCTION: str = build_system_instruction(
        "intermediate", "hype",
        include_citation_grammar=False,
        include_listening_fallback=False,
    )
    assert SYSTEM_INSTRUCTION is HYPE_INTERMEDIATE  # nosec B101
    ```
    - The double-opt-out preserves the byte-identity-with-v4 invariant. The existing assert is the gate; if the dispatcher ever drifts the import-time check fires.

    DJCoHostAgent path is UNCHANGED in this plan — the agent's _resolve_prompt_cell call already uses the dispatcher's defaults; the new include_listening_fallback=True default is what makes the live agent get the fragment automatically. NO change needed in dj_cohost.py for Plan 20-02 (Plan 20-01 already wires the linter; the fragment append is dispatcher-side).
  </behavior>
  <action>Modify src/vibemix/prompts/matrix.py: (1) Add import `from vibemix.coach.prompt_fragments import IM_LISTENING_FRAGMENT` at the top imports block (alongside the existing NEGATIVE_PHRASES import). (2) Extend build_system_instruction signature with the new keyword-only `include_listening_fallback: bool = True` AFTER include_citation_grammar (preserves keyword-only ordering convention). (3) Update the docstring's Args block — copy the include_citation_grammar paragraph as a template, swap names + reference IM_LISTENING_FRAGMENT + GROUND-08. (4) After the existing `if include_citation_grammar: body = body + "\n\n" + CITATION_GRAMMAR_BLOCK` block, append a parallel `if include_listening_fallback: body = body + IM_LISTENING_FRAGMENT` (no extra "\n\n" — the IM_LISTENING_FRAGMENT constant already starts with "\n\n--- FAIL-SOFT RULE..." per the locked copy). Modify src/vibemix/agent/persona.py: update the build_system_instruction call to pass `include_listening_fallback=False` per the behavior list; update the comment block above the call to reference Plan 20-02 alongside the existing Plan 18-03 reference. The `assert SYSTEM_INSTRUCTION is HYPE_INTERMEDIATE` line stays unchanged — it remains the byte-identity gate. Extend tests/prompts/test_matrix.py with: (a) test_default_path_includes_im_listening_fragment (`build_system_instruction()` result contains IM_LISTENING_FRAGMENT); (b) test_default_path_includes_grammar_block_too (same call also contains CITATION_GRAMMAR_BLOCK — both defaults active); (c) test_grammar_after_cell_body_then_fragment (assert the order: cell body chars come BEFORE CITATION_GRAMMAR_BLOCK chars come BEFORE IM_LISTENING_FRAGMENT chars in the returned string — use .index() for ordering); (d) test_opt_out_listening_fallback_alone (build_system_instruction(include_listening_fallback=False) result does NOT contain IM_LISTENING_FRAGMENT; result == build_system_instruction with citation grammar still appended); (e) test_double_opt_out_byte_identical_to_cell (build_system_instruction("intermediate", "hype", include_citation_grammar=False, include_listening_fallback=False) === HYPE_INTERMEDIATE — pin the persona.py path); (f) test_invalid_skill_still_raises (sanity: existing ValueError on bad skill still fires with the new kwarg present); (g) test_persona_system_instruction_still_byte_equal_to_hype_intermediate (re-import SYSTEM_INSTRUCTION + HYPE_INTERMEDIATE; assert SYSTEM_INSTRUCTION == HYPE_INTERMEDIATE). All seven tests live in test_matrix.py — append at the end of the file under a `# Plan 20-02 — IM_LISTENING_FRAGMENT integration` section divider.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && .venv/bin/python -m pytest tests/prompts/test_matrix.py tests/agent/test_persona.py tests/coach/test_prompt_fragments.py -x -v</automated>
  </verify>
  <done>All seven new test_matrix.py cases pass; pre-existing tests/prompts/test_matrix.py + tests/agent/test_persona.py suite stays green (v4-byte-identity invariant intact); `grep -c "include_listening_fallback" src/vibemix/prompts/matrix.py` returns ≥3 (signature + docstring reference + the if-block); `grep -c "include_listening_fallback=False" src/vibemix/agent/persona.py` returns 1 (the single opt-out call); `.venv/bin/python -c "from vibemix.prompts import build_system_instruction; from vibemix.coach import IM_LISTENING_FRAGMENT; assert IM_LISTENING_FRAGMENT in build_system_instruction()"` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IM_LISTENING_FRAGMENT → Gemini system instruction | A fixed module constant flows into Gemini's system-instruction context. No user input crosses this boundary — the fragment is hard-coded and cannot be modified at runtime. |
| build_system_instruction → DJCoHostAgent prompt body | The dispatcher renders the prompt body once per agent instantiation; the kwargs (skill, mode, mood, include_*) are env-var-derived (skill, mode) or session-state-derived (mood) — never raw user text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Tampering | IM_LISTENING_FRAGMENT | mitigate | Fixed string constant (no interpolation, no f-string, no .format()); test_im_listening_fragment_no_user_input_interpolation pins the no-template-syntax invariant. Mirrors T-13-05-06 + T-18-03-01 precedent. |
| T-20-02-02 | Spoofing | bypass-of-fragment via flag | accept | include_listening_fallback=False is a legitimate opt-out used by persona.py for v4 byte-identity. The DJCoHostAgent never passes False (it relies on the dispatcher default True). The flag is a Python kwarg, not network-exposed. Accept — internal API surface only. |
| T-20-02-03 | Repudiation | persona.SYSTEM_INSTRUCTION drift | mitigate | The `assert SYSTEM_INSTRUCTION is HYPE_INTERMEDIATE` import-time guard fires at agent construction if the dispatcher ever drifts away from byte-identity. Backed by tests/agent/test_persona.py and the new test_persona_system_instruction_still_byte_equal_to_hype_intermediate. |
</threat_model>

<verification>
- `pytest tests/prompts/test_matrix.py tests/agent/test_persona.py tests/coach/test_prompt_fragments.py -x` passes.
- `pytest tests/prompts/ tests/agent/ tests/coach/ -x` shows no regression beyond Plan 20-02's deliberate additions.
- `grep -c "IM_LISTENING_FRAGMENT" src/vibemix/coach/prompt_fragments.py` is 1 (definition).
- `grep -c "IM_LISTENING_FRAGMENT" src/vibemix/prompts/matrix.py` is ≥2 (import + use).
- `grep -c "include_listening_fallback=False" src/vibemix/agent/persona.py` is 1.
- `.venv/bin/python -c "from vibemix.agent.persona import SYSTEM_INSTRUCTION; from vibemix.prompts.matrix import HYPE_INTERMEDIATE; assert SYSTEM_INSTRUCTION == HYPE_INTERMEDIATE"` exits 0.
- POC files (cohost*.py, mascot.html) UNTOUCHED.
</verification>

<success_criteria>
- IM_LISTENING_FRAGMENT lives in vibemix.coach.prompt_fragments as the single source of truth.
- Live agent (default kwargs) gets the fragment + the grammar block in the system instruction.
- persona.SYSTEM_INSTRUCTION === HYPE_INTERMEDIATE byte-identity invariant intact.
- Six tests pin the fragment content + anti-injection invariant; seven tests pin the dispatcher integration; one test pins persona byte-identity.
- No POC file touched, no third-party dep added.
</success_criteria>

<output>
After completion, create `.planning/phases/20-citation-linter-enforcement-live-mode/20-02-SUMMARY.md`
</output>
