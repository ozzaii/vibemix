---
phase: 47-mascot-real-glb-land-full-emotion-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mascot/retarget_to_neon_rebel.py
  - scripts/mascot/MIXAMO-CLIP-SOURCES.md
  - assets/mascot/source/MANIFEST.yaml
  - .gitignore
  - tests/mascot/__init__.py
  - tests/mascot/test_retarget_cli_slots.py
autonomous: true
requirements:
  - MASCOT-02
tags:
  - mascot
  - retarget-cli
  - mixamo

must_haves:
  truths:
    - "scripts/mascot/retarget_to_neon_rebel.py accepts --slot with 28 valid values (5 legacy prep_* slots + 23 new family slots: 3 base_*, 5 emotion_*, 5 prep_kick/breakdown/drop/layer/mix, 10 react_*)"
    - "scripts/mascot/retarget_to_neon_rebel.py asserts the output GLB's per-family size band (Base 200-600 KB / Emotion 300-900 KB / Anticipation+Reaction 400-1200 KB) and exits non-zero if outside the band"
    - "scripts/mascot/MIXAMO-CLIP-SOURCES.md extends Kaan's selection-guidance table with 18 new slot rows + per-family aesthetic guardrails citing memory project_visual_direction_cdj_whisper"
    - "assets/mascot/source/MANIFEST.yaml is the manifest schema (in-repo); assets/mascot/source/*.fbx remains gitignored"
  artifacts:
    - path: "scripts/mascot/retarget_to_neon_rebel.py"
      provides: "Extended slot whitelist + per-family size-band assertion + --slot-family flag"
      min_lines: 200
    - path: "scripts/mascot/MIXAMO-CLIP-SOURCES.md"
      provides: "Extended selection-guidance with 18 new rows + per-family aesthetic guardrails"
      min_lines: 150
    - path: "assets/mascot/source/MANIFEST.yaml"
      provides: "Schema-bootstrap with 5 legacy + 23 new placeholder rows pending Kaan discharge"
      min_lines: 80
    - path: "tests/mascot/test_retarget_cli_slots.py"
      provides: "Asserts every slot in the 28-slot enumeration is accepted by --slot; rejects unknown slots"
      min_lines: 60
  key_links:
    - from: "scripts/mascot/retarget_to_neon_rebel.py"
      to: "assets/mascot/source/MANIFEST.yaml"
      via: "CLI appends a row on each --really run; manifest is the in-repo provenance log"
      pattern: "MANIFEST.yaml"
    - from: ".gitignore"
      to: "assets/mascot/source/"
      via: "raw .fbx sources gitignored; only MANIFEST.yaml committed"
      pattern: "assets/mascot/source/*.fbx"
---

<objective>
Extend the Phase 43-05 retarget CLI from 5 → 28 slots (covering the 5 legacy `prep_*` Phase-22-02 placeholders PLUS the 23 new family slots from Phase 47), add per-family size-band assertion, write the in-repo source-asset manifest schema, and extend `MIXAMO-CLIP-SOURCES.md` with 18 new selection-guidance rows for Kaan's eventual §VIS-04 discharge.

The CLI is the single workhorse for Kaan-action discharge: `uv run python scripts/mascot/retarget_to_neon_rebel.py --slot <slot> --source <path> --really` per slot. Plan 01 makes it slot-aware for the new family taxonomy without touching state-machine wiring (that lands in Plan 03). Source `.fbx` files stay gitignored (license — Mixamo blanket-licenses Adobe-account users; no redistribution); the YAML manifest is the in-repo provenance log.

Out of scope: placeholder GLB stubs (Plan 05); state-machine wiring (Plan 03); bundle-gate per-family extension (Plan 04); pools.ts mapping (Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-mascot-real-glb-land-full-emotion-coverage/47-CONTEXT.md
@.planning/phases/47-mascot-real-glb-land-full-emotion-coverage/47-UI-SPEC.md
@scripts/mascot/retarget_to_neon_rebel.py
@scripts/mascot/MIXAMO-CLIP-SOURCES.md
@tauri/ui/assets/mascot/animations/PLACEHOLDER_NOTE.md
@.gitignore

<interfaces>
<!-- The current 5-slot CLI surface; extension target. -->

scripts/mascot/retarget_to_neon_rebel.py (Phase 43-05):
- Existing `--slot` whitelist (5 entries): prep_settle, prep_head_turn_left, prep_head_turn_right, prep_lean_in_hyped, prep_lean_in_neutral
- Existing draco compression pipeline (calls `npx @gltf-transform/cli draco`)
- Existing per-clip size assertion: 400-1200 KB band
- Existing target rig: tauri/ui/assets/mascot/character.glb

assets/mascot/source/ — directory does NOT yet exist; create it; gitignore the *.fbx + *.glb raw contents but commit MANIFEST.yaml.

.gitignore — currently does NOT exclude assets/mascot/source/; add the rule.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend retarget_to_neon_rebel.py slot whitelist + per-family size bands</name>
  <files>scripts/mascot/retarget_to_neon_rebel.py</files>
  <read_first>
    - scripts/mascot/retarget_to_neon_rebel.py (read entire file — preserve existing 5-slot semantics; extend, do not replace)
    - .planning/phases/47-mascot-real-glb-land-full-emotion-coverage/47-CONTEXT.md (§ Bundle-Gate Strategy — per-family bands)
  </read_first>
  <action>
    Edit `scripts/mascot/retarget_to_neon_rebel.py` to extend the slot taxonomy:

    1. Replace the existing `VALID_SLOTS = {...}` set (5 entries) with a structured map `SLOT_FAMILIES`:
       ```python
       SLOT_FAMILIES = {
           "legacy_prep": {  # Phase 22-02 placeholders (preserved verbatim)
               "slots": ["prep_settle", "prep_head_turn_left", "prep_head_turn_right",
                         "prep_lean_in_hyped", "prep_lean_in_neutral"],
               "size_band_kb": (400, 1200),
           },
           "base": {  # Phase 47 — 3 looping baseline clips
               "slots": ["base_idle", "base_breathe", "base_sway"],
               "size_band_kb": (200, 600),
           },
           "emotion": {  # Phase 47 — 5 priority-60 emotion clips
               "slots": ["emotion_joy", "emotion_trust", "emotion_surprise",
                         "emotion_anticipation", "emotion_focus"],
               "size_band_kb": (300, 900),
           },
           "anticipation": {  # Phase 47 — 5 event-class-specific prep clips (NEW, separate from legacy_prep)
               "slots": ["prep_kick", "prep_breakdown", "prep_drop", "prep_layer", "prep_mix"],
               "size_band_kb": (400, 1200),
           },
           "reaction": {  # Phase 47 — 10 priority-80 reaction one-shots
               "slots": ["react_kick_swap", "react_sub_layer", "react_breakdown",
                         "react_reentry", "react_phrase_boundary", "react_distortion_climb",
                         "react_acid_line", "react_mix_in", "react_mix_out", "react_hype_peak"],
               "size_band_kb": (400, 1200),
           },
       }
       VALID_SLOTS = {s: family for family, info in SLOT_FAMILIES.items() for s in info["slots"]}
       ```

    2. Update the `--slot` argument parser `choices=` to use `VALID_SLOTS.keys()` (28 entries).

    3. Add a new optional flag `--slot-family {base,emotion,anticipation,reaction,legacy_prep}` that, when given without `--slot`, runs the retarget against EVERY slot in that family in sequence (assumes one `--source` per slot in `~/Downloads/mixamo_<slot>.glb` convention — matches Kaan's existing flow).

    4. Replace the hard-coded `(400, 1200)` size-band check with a per-family lookup:
       ```python
       family = VALID_SLOTS[args.slot]
       min_kb, max_kb = SLOT_FAMILIES[family]["size_band_kb"]
       ```

    5. Add a post-step manifest append: on successful retarget, append a YAML row to `assets/mascot/source/MANIFEST.yaml` capturing `{slot, mixamo_search_term, downloaded_at_iso, sha256_source, draco_level, output_bytes}`. If the file doesn't exist, create it with the header block.

    6. Preserve every existing Phase 43-05 invariant — comment block at top, `--really` confirmation, draco pipeline, output path `tauri/ui/assets/mascot/animations/<slot>.glb`.

    Add docstring at top referencing Phase 47 + REQ MASCOT-02. Cite memory `project_mascot_as_vtuber_personality_surface` in the comment that justifies the 23 new slots.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; python3 -c "import ast; tree = ast.parse(open('scripts/mascot/retarget_to_neon_rebel.py').read()); print('parsed ok')" &amp;&amp; python3 -c "src = open('scripts/mascot/retarget_to_neon_rebel.py').read(); assert 'SLOT_FAMILIES' in src; assert 'base_idle' in src; assert 'react_hype_peak' in src; assert 'emotion_joy' in src; assert 'prep_kick' in src; assert 'prep_settle' in src; assert '(200, 600)' in src; assert '(300, 900)' in src; assert '(400, 1200)' in src; print('OK 28 slots + 3 size bands')"</automated>
  </verify>
  <done>retarget_to_neon_rebel.py exports SLOT_FAMILIES with 28 total slots across 5 families with per-family size bands; --slot accepts all 28; --slot-family multi-slot batch mode added.</done>
</task>

<task type="auto">
  <name>Task 2: Write assets/mascot/source/MANIFEST.yaml schema + 28 placeholder rows</name>
  <files>assets/mascot/source/MANIFEST.yaml, .gitignore</files>
  <read_first>
    - .gitignore (read full file — append a new rule, don't replace)
    - scripts/mascot/retarget_to_neon_rebel.py (the CLI appends rows to this manifest — schema must match)
  </read_first>
  <action>
    Create `assets/mascot/source/` directory and write `MANIFEST.yaml`:

    ```yaml
    # Mascot source-asset provenance manifest (Phase 47 / MASCOT-02).
    #
    # Tracks the original .fbx files downloaded from Mixamo per slot.
    # Raw .fbx files are gitignored (Mixamo license: Adobe-account-only,
    # no redistribution); this manifest is in-repo for the audit trail.
    #
    # Schema:
    #   slots:
    #     - slot: <slot stem matching scripts/mascot/retarget_to_neon_rebel.py SLOT_FAMILIES>
    #       family: <legacy_prep | base | emotion | anticipation | reaction>
    #       mixamo_search_term: <Mixamo search query Kaan used>
    #       downloaded_at: <ISO-8601 UTC timestamp or null when still placeholder>
    #       sha256_source: <hex digest of source .fbx, or null when placeholder>
    #       draco_level: <0-10 compression level used by retarget CLI>
    #       output_bytes: <size of final .glb, or null when placeholder>
    #       status: <placeholder | retargeted>
    #
    # Status transitions: placeholder → retargeted (when Kaan runs the CLI with --really)
    # and the CLI updates the row in-place.
    schema_version: 1
    slots:
      # Legacy prep_* family (Phase 22-02 placeholders — pre-existing, Kaan may discharge separately)
      - slot: prep_settle
        family: legacy_prep
        mixamo_search_term: "Standing Idle or Breathing Idle"
        downloaded_at: null
        sha256_source: null
        draco_level: 7
        output_bytes: null
        status: placeholder
      - slot: prep_head_turn_left
        family: legacy_prep
        mixamo_search_term: "Talking or Talking Casually"
        downloaded_at: null
        sha256_source: null
        draco_level: 7
        output_bytes: null
        status: placeholder
      - slot: prep_head_turn_right
        family: legacy_prep
        mixamo_search_term: "Talking 2 or Explaining"
        downloaded_at: null
        sha256_source: null
        draco_level: 7
        output_bytes: null
        status: placeholder
      - slot: prep_lean_in_hyped
        family: legacy_prep
        mixamo_search_term: "Salute or Cheering (Pioneer-CDJ headbob aesthetic)"
        downloaded_at: null
        sha256_source: null
        draco_level: 7
        output_bytes: null
        status: placeholder
      - slot: prep_lean_in_neutral
        family: legacy_prep
        mixamo_search_term: "Headbob or Macarena Dance first 4s"
        downloaded_at: null
        sha256_source: null
        draco_level: 7
        output_bytes: null
        status: placeholder
      # Base family (3 looping baseline clips — Phase 47 NEW)
      - slot: base_idle
        family: base
        mixamo_search_term: "Standing Idle Relaxed"
        downloaded_at: null
        sha256_source: null
        draco_level: 9
        output_bytes: null
        status: placeholder
      - slot: base_breathe
        family: base
        mixamo_search_term: "Breathing Idle"
        downloaded_at: null
        sha256_source: null
        draco_level: 9
        output_bytes: null
        status: placeholder
      - slot: base_sway
        family: base
        mixamo_search_term: "Subtle Sway"
        downloaded_at: null
        sha256_source: null
        draco_level: 9
        output_bytes: null
        status: placeholder
      # Emotion family (5 priority-60 emotion clips — Phase 47 NEW)
      - slot: emotion_joy
        family: emotion
        mixamo_search_term: "Happy Idle (restrained — no clapping)"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: emotion_trust
        family: emotion
        mixamo_search_term: "Nodding Confidently"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: emotion_surprise
        family: emotion
        mixamo_search_term: "Surprised Mild"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: emotion_anticipation
        family: emotion
        mixamo_search_term: "Listening Forward Lean"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: emotion_focus
        family: emotion
        mixamo_search_term: "Concentrated Idle"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      # Anticipation family (5 event-class-specific prep clips — Phase 47 NEW)
      - slot: prep_kick
        family: anticipation
        mixamo_search_term: "Ready Stance Slight Headbob"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: prep_breakdown
        family: anticipation
        mixamo_search_term: "Settling Calm"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: prep_drop
        family: anticipation
        mixamo_search_term: "Coiled Tension Forward"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: prep_layer
        family: anticipation
        mixamo_search_term: "Listening Head Tilt"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: prep_mix
        family: anticipation
        mixamo_search_term: "Subtle Sway Anticipation"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      # Reaction family (10 priority-80 one-shots — Phase 47 NEW)
      - slot: react_kick_swap
        family: reaction
        mixamo_search_term: "Short Head Nod"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_sub_layer
        family: reaction
        mixamo_search_term: "Eyebrow Raise Subtle"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_breakdown
        family: reaction
        mixamo_search_term: "Slow Bow Head"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_reentry
        family: reaction
        mixamo_search_term: "Head Lift Slight Smile"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_phrase_boundary
        family: reaction
        mixamo_search_term: "Quick Headbob 8-count"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_distortion_climb
        family: reaction
        mixamo_search_term: "Building Tension Shoulder Roll"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_acid_line
        family: reaction
        mixamo_search_term: "Wry Smile Tilt"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_mix_in
        family: reaction
        mixamo_search_term: "Approving Nod Forward"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_mix_out
        family: reaction
        mixamo_search_term: "Glance Aside Subtle"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
      - slot: react_hype_peak
        family: reaction
        mixamo_search_term: "Reserved Hype Headbang"
        downloaded_at: null
        sha256_source: null
        draco_level: 8
        output_bytes: null
        status: placeholder
    ```

    Append to `.gitignore` (preserve all existing rules; add a NEW block):
    ```
    # Phase 47 / MASCOT-02 — Mixamo source .fbx files (Adobe-account license, no redistribution)
    assets/mascot/source/*.fbx
    assets/mascot/source/*.glb
    !assets/mascot/source/MANIFEST.yaml
    ```
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f assets/mascot/source/MANIFEST.yaml &amp;&amp; python3 -c "import yaml; d = yaml.safe_load(open('assets/mascot/source/MANIFEST.yaml')); assert d['schema_version'] == 1; assert len(d['slots']) == 28; families = {s['family'] for s in d['slots']}; assert families == {'legacy_prep', 'base', 'emotion', 'anticipation', 'reaction'}; print('OK 28 slots / 5 families')" &amp;&amp; grep -q "assets/mascot/source/\*.fbx" .gitignore</automated>
  </verify>
  <done>MANIFEST.yaml schema established with 28 placeholder rows across 5 families; .gitignore rules added.</done>
</task>

<task type="auto">
  <name>Task 3: Extend scripts/mascot/MIXAMO-CLIP-SOURCES.md with 18 new selection-guidance rows</name>
  <files>scripts/mascot/MIXAMO-CLIP-SOURCES.md</files>
  <read_first>
    - scripts/mascot/MIXAMO-CLIP-SOURCES.md (read entire file — extend, don't replace)
    - .planning/phases/47-mascot-real-glb-land-full-emotion-coverage/47-CONTEXT.md (Brand Compliance section in UI-SPEC.md cited)
  </read_first>
  <action>
    Edit `scripts/mascot/MIXAMO-CLIP-SOURCES.md`:

    1. Update the document title from "Mixamo Clip Sources — VIS-04 (Phase 43, Plan 43-05)" to "Mixamo Clip Sources — VIS-04 (Phase 43 → Phase 47 extension)".

    2. Update the preamble: "Per CONTEXT §VIS-04: 5 Mixamo source clips → Neon Rebel rig → 5 prep_*.glb slot replacements" → expand to "5 legacy slots + 23 new Phase 47 slots (3 Base + 5 Emotion + 5 Anticipation + 10 Reaction) — 28 total Mixamo retargets at full discharge".

    3. After the existing "## Source clip selections" 5-row table, ADD 4 new H2 sections — one per family — with per-family selection-guidance tables. Reuse the existing column shape (`# | Mixamo asset (search term) | vibemix slot | Purpose | Notes`).

    4. For each new family section, include an aesthetic-guardrail subsection that cites memory `project_visual_direction_cdj_whisper` and the Phase 47 UI-SPEC § Brand Compliance rules verbatim (Pioneer-CDJ headbob; hands close to body; static-foot-grounded; head as primary expressive surface; ~120 BPM equivalent loop).

    5. Per-family notable guardrails to add (in addition to the shared CDJ Whisper rules):
       - **Base family:** "Loops MUST be < 4s. Breath visible at the chest; no exaggerated chest rise. Sway = full-body shift, not waist twist."
       - **Emotion family:** "Face + upper body only. Hands hover near belt; no above-shoulder gestures. Emotion clips are 2-4s loops."
       - **Anticipation family:** "Same aesthetic as the 5 legacy prep_*; this family adds event-class-specific posture (e.g., prep_kick = ready stance, weight forward; prep_breakdown = settling, weight back)."
       - **Reaction family:** "One-shot, NOT loops. Each clip is 1-2.5s. NO peak-energy dance moves — even react_hype_peak is reserved headbob, not full-body convulsion. react_hype_peak is the README hero render asset — get this one right."

    6. Update the "## Download checklist (Kaan's flow)" → add a sub-bullet: "For Phase 47 new families: each slot's source file at `~/Downloads/mixamo_<slot>.glb` (e.g., `mixamo_react_hype_peak.glb`)."

    7. Update the "## Retarget run (one-liner per clip)" → add a `--slot-family` example: `uv run python scripts/mascot/retarget_to_neon_rebel.py --slot-family reaction --really` (batch-mode runs all 10 reaction clips in sequence).

    8. Update the "## Verification" section → bundle gate now checks 5 families × varying per-family bands; ls outputs 28 GLBs after full discharge.

    Total new line count target: ~150 lines added.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; grep -q "Phase 47 extension" scripts/mascot/MIXAMO-CLIP-SOURCES.md &amp;&amp; grep -q "base_idle" scripts/mascot/MIXAMO-CLIP-SOURCES.md &amp;&amp; grep -q "react_hype_peak" scripts/mascot/MIXAMO-CLIP-SOURCES.md &amp;&amp; grep -q "emotion_joy" scripts/mascot/MIXAMO-CLIP-SOURCES.md &amp;&amp; grep -q "prep_kick" scripts/mascot/MIXAMO-CLIP-SOURCES.md &amp;&amp; grep -q "project_visual_direction_cdj_whisper" scripts/mascot/MIXAMO-CLIP-SOURCES.md &amp;&amp; lines=$(wc -l &lt; scripts/mascot/MIXAMO-CLIP-SOURCES.md) &amp;&amp; [ "$lines" -ge 150 ] &amp;&amp; echo "OK MIXAMO-CLIP-SOURCES.md extended ($lines lines)"</automated>
  </verify>
  <done>MIXAMO-CLIP-SOURCES.md extended with 18 new selection-guidance rows across 4 family sections + per-family aesthetic guardrails.</done>
</task>

<task type="auto">
  <name>Task 4: Smoke test asserting CLI accepts all 28 slots</name>
  <files>tests/mascot/__init__.py, tests/mascot/test_retarget_cli_slots.py</files>
  <read_first>
    - scripts/mascot/retarget_to_neon_rebel.py (created in Task 1 — test reads its module attributes)
  </read_first>
  <action>
    Create `tests/mascot/__init__.py` as an empty file.

    Write `tests/mascot/test_retarget_cli_slots.py`:

    ```python
    """MASCOT-02 smoke test — assert scripts/mascot/retarget_to_neon_rebel.py
    knows about all 28 slots across 5 families. Pure static-import test;
    does NOT invoke gltf-pipeline or Mixamo."""

    import importlib.util
    from pathlib import Path

    REPO_ROOT = Path(__file__).resolve().parents[2]
    SCRIPT = REPO_ROOT / "scripts" / "mascot" / "retarget_to_neon_rebel.py"


    def _load_module():
        spec = importlib.util.spec_from_file_location("retarget_to_neon_rebel", SCRIPT)
        mod = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(mod)
        return mod


    def test_script_exists():
        assert SCRIPT.is_file()


    def test_slot_families_has_5_families():
        mod = _load_module()
        assert set(mod.SLOT_FAMILIES.keys()) == {
            "legacy_prep", "base", "emotion", "anticipation", "reaction"
        }


    def test_28_total_slots():
        mod = _load_module()
        all_slots = [s for fam in mod.SLOT_FAMILIES.values() for s in fam["slots"]]
        assert len(all_slots) == 28


    def test_per_family_slot_counts():
        mod = _load_module()
        counts = {f: len(info["slots"]) for f, info in mod.SLOT_FAMILIES.items()}
        assert counts == {
            "legacy_prep": 5, "base": 3, "emotion": 5, "anticipation": 5, "reaction": 10,
        }


    def test_legacy_prep_slots_preserved_verbatim():
        mod = _load_module()
        assert set(mod.SLOT_FAMILIES["legacy_prep"]["slots"]) == {
            "prep_settle", "prep_head_turn_left", "prep_head_turn_right",
            "prep_lean_in_hyped", "prep_lean_in_neutral",
        }


    def test_new_anticipation_family_distinct_from_legacy():
        """Phase 47 anticipation family adds NEW prep_* slots, NOT aliasing legacy_prep."""
        mod = _load_module()
        assert set(mod.SLOT_FAMILIES["anticipation"]["slots"]) == {
            "prep_kick", "prep_breakdown", "prep_drop", "prep_layer", "prep_mix",
        }
        legacy = set(mod.SLOT_FAMILIES["legacy_prep"]["slots"])
        new = set(mod.SLOT_FAMILIES["anticipation"]["slots"])
        assert legacy.isdisjoint(new)


    def test_reaction_family_includes_hype_peak():
        """react_hype_peak is the README hero render anchor — MUST exist."""
        mod = _load_module()
        assert "react_hype_peak" in mod.SLOT_FAMILIES["reaction"]["slots"]


    def test_per_family_size_bands():
        mod = _load_module()
        assert mod.SLOT_FAMILIES["base"]["size_band_kb"] == (200, 600)
        assert mod.SLOT_FAMILIES["emotion"]["size_band_kb"] == (300, 900)
        assert mod.SLOT_FAMILIES["anticipation"]["size_band_kb"] == (400, 1200)
        assert mod.SLOT_FAMILIES["reaction"]["size_band_kb"] == (400, 1200)
        assert mod.SLOT_FAMILIES["legacy_prep"]["size_band_kb"] == (400, 1200)
    ```
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/mascot/test_retarget_cli_slots.py -x --tb=short 2>&amp;1 | tail -10</automated>
  </verify>
  <done>All 8 tests pass; CLI slot taxonomy locked at 28 slots / 5 families with per-family size bands.</done>
</task>

</tasks>

<verification>
- `python3 -c "..."` confirms SLOT_FAMILIES has 28 slots / 5 families with bands (200,600) / (300,900) / (400,1200).
- `MANIFEST.yaml` schema_version=1 with exactly 28 placeholder rows.
- `.gitignore` excludes `assets/mascot/source/*.fbx` + `*.glb` but preserves `MANIFEST.yaml`.
- `MIXAMO-CLIP-SOURCES.md` ≥ 150 lines with new family sections + aesthetic guardrails citing `project_visual_direction_cdj_whisper`.
- `pytest tests/mascot/test_retarget_cli_slots.py` green.
</verification>

<summary_template>
Use @$HOME/.claude/get-shit-done/templates/summary.md verbatim; write to `47-01-SUMMARY.md`.
</summary_template>