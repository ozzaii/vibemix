---
phase: 50-end-to-end-macbook-os-matrix-pass
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - scripts/audit/check_no_slop_e2e.py
  - scripts/audit/test_check_no_slop_e2e.py
  - .github/workflows/check-slop-e2e.yml
autonomous: true
requirements:
  - E2E-10
tags:
  - anti-slop
  - sibling-script
  - ci-gate

must_haves:
  truths:
    - "scripts/audit/check_no_slop_e2e.py is a SIBLING script of scripts/launch/check_no_ai_slop.py, NOT a redesign — mirrors the Phase 47 (scripts/mascot/check_no_ai_slop_phase47.py) and Phase 48 (scripts/audit/check_no_slop_opp.py) precedent."
    - "scripts/audit/check_no_slop_e2e.py imports the 15-token blocklist + regex from scripts/launch/check_no_ai_slop.py (NEVER redefines them). Single source of truth preserved."
    - "Grep target is constrained to dist/e2e-macbook-runs/**/report.html ONLY. Does NOT re-scan source files (that's the canonical script's job). Avoids double-coverage."
    - "Exit code 0 = green; exit code 1 = found banned token, with file + line + token in stderr."
    - "scripts/audit/test_check_no_slop_e2e.py uses tmp_path to synthesize a fake dist/e2e-macbook-runs/<UTC>/report.html with: (a) clean content → exit 0; (b) content containing 'deeply' → exit 1 with location reported; (c) content with no banned tokens but containing 'deep' (substring of 'deeply') → exit 0 (word-boundary respected)."
    - ".github/workflows/check-slop-e2e.yml wires the sibling script into CI on pull_request + push to main. Runs AFTER e2e harness tests so dist/e2e-macbook-runs/ exists; gracefully no-ops if no runs present (exit 0 with explanatory log)."
    - "Workflow uses pinned SHA per Phase 46 pinact precedent; checkout@<SHA>, setup-python@<SHA>."
  artifacts:
    - path: "scripts/audit/check_no_slop_e2e.py"
      provides: "Anti-slop gate scoped to e2e report.html artifacts; imports canonical blocklist"
      min_lines: 70
    - path: "scripts/audit/test_check_no_slop_e2e.py"
      provides: "tmp_path-based test verifying 3 cases: clean / banned / word-boundary"
      min_lines: 70
    - path: ".github/workflows/check-slop-e2e.yml"
      provides: "CI wire; pinned SHA per Phase 46; no-op on missing dist/"
      min_lines: 35
  key_links:
    - from: "scripts/audit/check_no_slop_e2e.py"
      to: "scripts/launch/check_no_ai_slop.py"
      via: "from scripts.launch.check_no_ai_slop import BLOCKLIST, REGEX_PATTERNS"
      pattern: "single-source blocklist via sibling-script reuse"
    - from: ".github/workflows/check-slop-e2e.yml"
      to: "scripts/audit/check_no_slop_e2e.py"
      via: "ci step: python scripts/audit/check_no_slop_e2e.py"
      pattern: "CI sibling-gate wire"
---

<objective>
Wire anti-slop coverage to the new e2e `report.html` artifact path via a sibling script — mirrors the Phase 47/48 sibling precedent. Single source of truth on the blocklist remains `scripts/launch/check_no_ai_slop.py`.

Out of scope: editing the canonical blocklist (forbidden), scanning source code (existing gate handles that), changes to existing CI workflows.
</objective>

<execution_context>
- `scripts/launch/check_no_ai_slop.py` — canonical blocklist source (import only)
- `scripts/mascot/check_no_ai_slop_phase47.py` — sibling precedent
- `scripts/audit/check_no_slop_opp.py` — sibling precedent
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-CONTEXT.md` § Anti-Slop Coverage
- `.planning/REQUIREMENTS.md` § E2E-10
</execution_context>

<steps>
1. Read existing sibling at `scripts/audit/check_no_slop_opp.py` for shape.
2. Write `scripts/audit/check_no_slop_e2e.py`: `argparse` with `--root dist/e2e-macbook-runs` default; glob `**/report.html`; import `BLOCKLIST` + `REGEX_PATTERNS` from `scripts.launch.check_no_ai_slop` (add `__init__.py` stubs if needed for import path); per-file scan; report tuple (file, line, token); exit 1 on first hit.
3. Write `scripts/audit/test_check_no_slop_e2e.py` — 3 test cases via `tmp_path` fixture + `subprocess.run`. Verify exit codes + stderr content.
4. Write `.github/workflows/check-slop-e2e.yml` — triggers on push/PR; setup-python; run `python -m pytest tests/e2e/macbook/test_report_render.py` first to generate sample report; then run the sibling gate; pinned SHAs.
5. Run `pytest scripts/audit/test_check_no_slop_e2e.py -v`. Green.
6. Commit: `feat(50-02): anti-slop sibling script for e2e report.html`.
</steps>

<verification>
- Sibling script lints clean (`python -m py_compile`)
- 3 test cases green
- CI workflow YAML validates (`actionlint` if available; else manual `yq` parse)
- Grep confirms NO redefinition of `BLOCKLIST` — only `import` from canonical
- Step-0 worktree invariant compliance documented in commit
</verification>
