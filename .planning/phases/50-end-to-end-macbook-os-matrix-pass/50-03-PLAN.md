---
phase: 50-end-to-end-macbook-os-matrix-pass
plan: 03
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - tests/e2e/macbook/playwright.config.ts
  - tests/e2e/macbook/snapshots/persona_smoke.spec.ts
  - tests/e2e/macbook/snapshots/library_page.spec.ts
  - tests/e2e/macbook/snapshots/live_session.spec.ts
  - tests/e2e/macbook/__snapshots__/.gitkeep
  - tests/e2e/macbook/snapshots/README.md
  - package.json
autonomous: true
requirements:
  - E2E-03
tags:
  - visual-regression
  - playwright
  - pixelmatch
  - mascot-snapshots

must_haves:
  truths:
    - "tests/e2e/macbook/playwright.config.ts pins: viewport 1024×768, deviceScaleFactor 2, colorScheme 'dark', reducedMotion 'reduce', screenshot { mode: 'only-on-failure', fullPage: true }. Visual-comparison via @playwright/test toHaveScreenshot with maxDiffPixelRatio: 0.02 (REQ E2E-03 verbatim)."
    - "tests/e2e/macbook/snapshots/persona_smoke.spec.ts navigates to tauri://localhost/mascot-persona-smoke (existing Phase 47 route), captures one PNG per emotion state, baselines saved at tests/e2e/macbook/__snapshots__/persona_smoke--<platform>.png."
    - "tests/e2e/macbook/snapshots/library_page.spec.ts captures the library-mode UI (CDJ Whisper direction). One snapshot for empty state, one for populated state (mock fixture)."
    - "tests/e2e/macbook/snapshots/live_session.spec.ts captures the live-session route with mock mascot state. ONE snapshot — Tier-1 only; Tier-2 surfaces deferred per CONTEXT.md."
    - "Baseline GLBs source = tauri/ui/assets/mascot/animations/ (Phase 47 placeholders). Re-baseline trigger (§VIS-04 real-asset land) documented in tests/e2e/macbook/snapshots/README.md."
    - "package.json gets new devDependencies: @playwright/test, pixelmatch, playwright versions per .planning/research/STACK.md § Bucket 4 (@playwright/test==1.50.x, pixelmatch==7.1.0). New npm script test:e2e:visual added."
    - "tests/e2e/macbook/snapshots/README.md documents: how to re-baseline (npx playwright test --update-snapshots), the §VIS-04 re-baseline trigger, platform tag convention (darwin-arm64 / darwin-x64 / win32-x64), and the maxDiffPixelRatio 0.02 lock."
    - "Snapshot diff outputs land at dist/e2e-macbook-runs/<UTC>/diffs/ — referenced by report_template.html § Visual details. Test setup writes diffs there on failure."
    - "Tests are CI-tolerant per PITFALLS § 8: if tauri-plugin-playwright maturity blocks macOS WKWebView, falls back to WebView2-only Win e2e + manual Mac walk. Spec files include skip/marker logic."
    - "mascot.html NEVER referenced (POC immutability + Phase 47 grep gate)."
  artifacts:
    - path: "tests/e2e/macbook/playwright.config.ts"
      provides: "Playwright base config: viewport + DPR + colorScheme + reducedMotion + diff threshold"
      min_lines: 50
    - path: "tests/e2e/macbook/snapshots/persona_smoke.spec.ts"
      provides: "Mascot emotion-state PNG baselines via tauri:// route from Phase 47"
      min_lines: 60
    - path: "tests/e2e/macbook/snapshots/library_page.spec.ts"
      provides: "Library page empty + populated baselines"
      min_lines: 50
    - path: "tests/e2e/macbook/snapshots/live_session.spec.ts"
      provides: "Live session Tier-1 baseline"
      min_lines: 40
    - path: "tests/e2e/macbook/__snapshots__/.gitkeep"
      provides: "Placeholder so empty dir tracks in git pre-baseline"
      min_lines: 1
    - path: "tests/e2e/macbook/snapshots/README.md"
      provides: "Re-baseline workflow + §VIS-04 trigger + platform tag convention"
      min_lines: 40
  key_links:
    - from: "tests/e2e/macbook/snapshots/persona_smoke.spec.ts"
      to: "tauri/ui/src/persona-smoke-route.tsx"
      via: "navigates to tauri://localhost/mascot-persona-smoke (existing Phase 47 surface)"
      pattern: "production-surface visual baseline"
    - from: "tests/e2e/macbook/snapshots/*.spec.ts"
      to: "tauri/ui/assets/mascot/animations/"
      via: "Three.js loads GLB → playwright captures pixels"
      pattern: "Phase 47 placeholder GLB → §VIS-04 re-baseline"
    - from: "tests/e2e/macbook/playwright.config.ts"
      to: ".planning/research/STACK.md § Bucket 4"
      via: "version pins"
      pattern: "STACK-locked tooling"
---

<objective>
Wire Playwright + pixelmatch visual-regression against Tauri+Three.js production surfaces. Baselines target Phase 47 placeholder GLBs; re-baseline at §VIS-04 real-asset discharge.

Out of scope: real-GLB asset bake (§VIS-04 Kaan-action), audio-loopback testing (Plan 04), CI workflow wire (handled by Plan 02's workflow + extended in Plan 05).
</objective>

<execution_context>
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-CONTEXT.md` § Visual Regression
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-UI-SPEC.md` § 6 Design System Decisions
- `.planning/research/STACK.md` § Bucket 4 — version pins
- `.planning/research/PITFALLS.md` § 8 — tauri-plugin-playwright maturity fallback
- `tauri/ui/assets/mascot/animations/` — Phase 47 placeholder GLBs
- `.planning/phases/47-mascot-real-glb-land-full-emotion-coverage/47-VERIFICATION.md` — persona-smoke route reference
</execution_context>

<steps>
1. Read `.planning/research/STACK.md` § Bucket 4 to confirm version pins.
2. Add to `package.json` devDependencies: `@playwright/test`, `playwright`, `pixelmatch` at locked versions. Add npm script `test:e2e:visual`: `playwright test -c tests/e2e/macbook/playwright.config.ts`.
3. Write `tests/e2e/macbook/playwright.config.ts`. Use TypeScript per project convention (Tauri UI is TS).
4. Write `tests/e2e/macbook/snapshots/persona_smoke.spec.ts` — iterates over emotion states from Phase 47 EVENT_LAYER_PRIORITY_MAP; uses `await page.goto('tauri://localhost/mascot-persona-smoke')`; per-state `await expect(page).toHaveScreenshot({ maxDiffPixelRatio: 0.02 })`.
5. Write `tests/e2e/macbook/snapshots/library_page.spec.ts` + `live_session.spec.ts` similarly.
6. Write `tests/e2e/macbook/snapshots/README.md` documenting re-baseline workflow + §VIS-04 trigger + maxDiffPixelRatio lock.
7. Add `tests/e2e/macbook/__snapshots__/.gitkeep` so the snapshot dir is tracked pre-first-baseline.
8. Run `npx playwright install --with-deps chromium webkit` (CI dev step).
9. Run `npx playwright test -c tests/e2e/macbook/playwright.config.ts --reporter=list` to generate first baselines. If `tauri-plugin-playwright==0.1.0` blocks on macOS, mark tests as `test.fixme` with Pitfall-8 reference comment (CI-tolerant fallback per CONTEXT.md).
10. Commit: `feat(50-03): visual regression baselines vs Phase 47 placeholders`.
</steps>

<verification>
- `npx playwright test --list -c tests/e2e/macbook/playwright.config.ts` lists all 3 spec files
- First baseline run produces PNGs at `tests/e2e/macbook/__snapshots__/`
- README.md documents §VIS-04 trigger explicitly
- Grep for `mascot.html` in tests/e2e/macbook/ → zero matches (POC immutability)
- maxDiffPixelRatio: 0.02 appears in config (REQ E2E-03 verbatim)
- Step-0 worktree invariant documented in commit
</verification>
