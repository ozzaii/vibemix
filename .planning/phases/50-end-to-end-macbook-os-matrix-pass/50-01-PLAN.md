---
phase: 50-end-to-end-macbook-os-matrix-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/macbook/__init__.py
  - tests/e2e/macbook/conftest.py
  - tests/e2e/macbook/report_template.html
  - tests/e2e/macbook/render_report.py
  - tests/e2e/macbook/dimensions.py
  - tests/e2e/macbook/test_report_render.py
  - tests/e2e/macbook/test_privacy_fixture.py
autonomous: true
requirements:
  - E2E-01
  - E2E-09
tags:
  - e2e
  - harness-foundation
  - report-html
  - privacy-fixture

must_haves:
  truths:
    - "tests/e2e/macbook/ is the canonical harness root per ARCHITECTURE.md § 4. __init__.py + conftest.py + report_template.html + render_report.py + dimensions.py land here. No imports from src/vibemix/ — harness is sibling, not subordinate."
    - "tests/e2e/macbook/dimensions.py declares 5 dimension dataclasses: Functional, Visual, Aesthetic, Usability, Hallucination. Each has fields status: Literal['PASS','FAIL','PARTIAL','SKIPPED'], total: int, passed: int, summary: str (≤120 chars), details: list[dict]. Status pill computed from worst dimension status."
    - "tests/e2e/macbook/render_report.py emits dist/e2e-macbook-runs/<UTC>/report.html using Jinja2 + report_template.html. UTC stamp format: %Y-%m-%dT%H-%M-%SZ. Writes ZERO files outside dist/e2e-macbook-runs/ and tests/e2e/macbook/__snapshots__/ — fixture asserts this."
    - "report_template.html implements 50-UI-SPEC.md verbatim: Geist Mono stack, 10-token color palette, 4-step type scale, ONE accent (--accent #F2A03D), section labels Functional/Visual/Aesthetic/Usability/Hallucination locked. Inline <style> block only — no external CSS, no JS, no build step."
    - "tests/e2e/macbook/conftest.py declares pytest fixture e2e_run that yields an EeRun dataclass with run_id (UTC), out_dir (dist/e2e-macbook-runs/<UTC>/), and the 5 dimension recorders. yields → finalizes → renders report.html on teardown."
    - "tests/e2e/macbook/conftest.py declares pytest fixture _privacy_guard (autouse=True, scope='session') that snapshots mtime + file count of ~/.hermes/, ~/hermes-rig/logs/, ~/.lmstudio/ pre-run, re-snapshots post-run, asserts zero delta. Skips on missing dirs with explicit comment that absence is OK (treated as zero baseline). Per memory feedback_privacy_scope_narrow."
    - "tests/e2e/macbook/test_privacy_fixture.py verifies _privacy_guard fixture itself: synthesizes a test that intentionally writes to /tmp/fake-hermes/ (NOT real off-limits paths — uses a mocked env override), confirms guard fires AssertionError. Never touches real off-limits paths."
    - "tests/e2e/macbook/test_report_render.py renders a sample EeRun with all 5 dimensions PASS, asserts: file exists at dist/e2e-macbook-runs/<UTC>/report.html, contains exact section labels, no banned anti-slop tokens, status pill PASS."
    - "Step-0 worktree invariant included in any spawned subagent prompt: cd <worktree> && git fetch origin main && git merge origin/main --no-edit per memory feedback_worktree_must_sync_main_first."
  artifacts:
    - path: "tests/e2e/macbook/__init__.py"
      provides: "Package marker; empty"
      min_lines: 1
    - path: "tests/e2e/macbook/conftest.py"
      provides: "e2e_run fixture + _privacy_guard session-autouse fixture"
      min_lines: 120
    - path: "tests/e2e/macbook/dimensions.py"
      provides: "5 dimension dataclasses + status-pill computation"
      min_lines: 90
    - path: "tests/e2e/macbook/render_report.py"
      provides: "Jinja2 renderer; emits report.html with full UI-SPEC token compliance"
      min_lines: 80
    - path: "tests/e2e/macbook/report_template.html"
      provides: "Single Jinja2 template; inline CSS per 50-UI-SPEC.md"
      min_lines: 140
    - path: "tests/e2e/macbook/test_privacy_fixture.py"
      provides: "Self-test for _privacy_guard correctness"
      min_lines: 50
    - path: "tests/e2e/macbook/test_report_render.py"
      provides: "Renderer smoke test + section-label assertions + anti-slop probe"
      min_lines: 60
  key_links:
    - from: "tests/e2e/macbook/render_report.py"
      to: "tests/e2e/macbook/dimensions.py"
      via: "imports 5 dimension dataclasses + EeRun container"
      pattern: "dataclass-driven render"
    - from: "tests/e2e/macbook/conftest.py"
      to: "tests/e2e/macbook/render_report.py"
      via: "e2e_run fixture teardown calls render_report()"
      pattern: "pytest yield → render"
    - from: "tests/e2e/macbook/conftest.py::_privacy_guard"
      to: "memory feedback_privacy_scope_narrow"
      via: "asserts zero writes to ~/.hermes/, ~/hermes-rig/logs/, ~/.lmstudio/"
      pattern: "session-autouse privacy fixture"
---

<objective>
Land the e2e harness foundation: `tests/e2e/macbook/` package, dimension dataclasses, `report.html` renderer, and the privacy fixture that asserts zero off-limits writes on every test run.

Out of scope: anti-slop sibling script (Plan 02), visual snapshots (Plan 03), audio-loopback VCR fixture (Plan 04), Gate 6b + cut_release wire (Plan 05), 50a checklist + screencast rig (Plan 06).
</objective>

<execution_context>
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-CONTEXT.md`
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-UI-SPEC.md`
- `.planning/REQUIREMENTS.md` § E2E-01, E2E-09
- `.planning/research/ARCHITECTURE.md` § 4 — tests/e2e/macbook/ placement, dist/e2e-macbook-runs path
- `scripts/launch/check_no_ai_slop.py` — anti-slop blocklist (consumed by Plan 02)
</execution_context>

<steps>
1. Create `tests/e2e/macbook/__init__.py` (empty marker).
2. Write `tests/e2e/macbook/dimensions.py` with 5 dataclasses (`Functional`, `Visual`, `Aesthetic`, `Usability`, `Hallucination`) + `EeRun` container + `compute_overall_status()` helper (worst-of). Use `@dataclass` + `Literal` types.
3. Write `tests/e2e/macbook/report_template.html` per 50-UI-SPEC.md § 1–6. Inline `<style>` block; Jinja2 `{% for dim in dimensions %}` loop. No JS. No emojis. Banned-vocab-free.
4. Write `tests/e2e/macbook/render_report.py` — pure stdlib + Jinja2. `render(ee_run: EeRun, out_dir: Path) -> Path` returns path to written report.html. UTC stamp format `%Y-%m-%dT%H-%M-%SZ`.
5. Write `tests/e2e/macbook/conftest.py` with:
   - `e2e_run` fixture (function-scope) — yields `EeRun` with empty dimensions; teardown calls `render()`.
   - `_privacy_guard` fixture (session-scope, `autouse=True`) — snapshots `~/.hermes/`, `~/hermes-rig/logs/`, `~/.lmstudio/` pre/post; assert zero delta.
6. Write `tests/e2e/macbook/test_privacy_fixture.py` — uses `monkeypatch` to redirect off-limits paths to `tmp_path` subdirs; verifies guard fires on intentional write, passes on clean run.
7. Write `tests/e2e/macbook/test_report_render.py` — constructs sample `EeRun` all-PASS, calls `render()`, asserts file exists + contains `Functional`, `Visual`, `Aesthetic`, `Usability`, `Hallucination` exactly + no banned token.
8. Run `pytest tests/e2e/macbook/test_privacy_fixture.py tests/e2e/macbook/test_report_render.py -v`. Both green.
9. Commit: `feat(50-01): e2e harness foundation + privacy fixture`.
</steps>

<verification>
- All 7 files exist with min-lines bands met
- `pytest tests/e2e/macbook/ -v` → all tests pass
- `report.html` rendered from test run contains all 5 section labels verbatim
- `_privacy_guard` fixture confirmed via self-test
- Grep `tests/e2e/macbook/` for banned tokens (`deeply`, `seamlessly`, `crafted`) → zero matches
- Step-0 worktree invariant present in commit (no worktree spawn this plan, but compliance documented)
</verification>
