---
phase: 50-end-to-end-macbook-os-matrix-pass
plan: 06
type: execute
wave: 4
depends_on: [50-01, 50-02, 50-03, 50-04, 50-05]
files_modified:
  - tests/e2e/macbook/50a_kaan_walk_checklist.md
  - tests/e2e/macbook/nielsen_10_checklist.json
  - scripts/e2e/record_50a_walk.sh
  - docs/e2e/README.md
  - docs/e2e/.gitkeep
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - E2E-06
  - E2E-07
tags:
  - 50a-kaan-walk
  - nielsen-10
  - screencast-rig
  - kaan-action-surface

must_haves:
  truths:
    - "tests/e2e/macbook/50a_kaan_walk_checklist.md is Kaan's executable walk script: BlackHole connected → real DJ-set audio playing → launch signed .dmg → walk through library page → start live session → trigger 3 mascot reactions → close cleanly. Each step has PASS/FAIL checkbox + space for time-to-react note. NOT a 30-session formal harness per memory project_phase_16_kaan_dj_testing."
    - "tests/e2e/macbook/nielsen_10_checklist.json declares the 10 Nielsen heuristics as machine-readable items with: id (1-10), name, prompt (per-surface), tier (1=primary surface, 2=secondary). Phase 50 50a evaluates Tier-1 surfaces ONLY (library page, live session, settings) per REQ E2E-06."
    - "scripts/e2e/record_50a_walk.sh is the screencast capture rig: macOS uses `screencapture -v` or QuickTime CLI; outputs raw .mov; offers a one-line ffmpeg invocation to transcode to .webm < 25 MB; emits final path docs/e2e/2026-05-walk.webm per REQ E2E-07. ZERO live recording in CI — script is for Kaan's local execution."
    - "docs/e2e/README.md documents: §E2E-50A-WALK discharge procedure (read checklist → playback real DJ audio → record screencast → save to docs/e2e/2026-05-walk.webm), file-size budget (<25 MB or git-lfs track), and the Nielsen checklist scoring rubric."
    - "docs/e2e/.gitkeep placeholder so dir is tracked pre-screencast land; .webm itself NOT committed by Phase 50 (Kaan-action)."
    - ".planning/STATE.md updated with two Phase 50 Kaan-action items appended to the Accumulated Context section: §E2E-50A-WALK (Kaan + MacBook + real DJ audio walk) and §INSTALL-VM-RUN downstream (carry-forward from Phase 49 — real-VM execution on all 5 OS configs)."
    - ".planning/REQUIREMENTS.md E2E rows updated: E2E-01..E2E-10 marked [x] for engineering-green deliverables shipped this phase, with parenthetical (§E2E-50A-WALK pending) on E2E-06, E2E-07; and (§INSTALL-VM-RUN downstream) on E2E-02's full real-VM execution."
    - "Anti-slop blocklist absolute on every file landed (no banned tokens in checklist.md, README.md, JSON labels, shell script comments). Verification grep runs as part of plan."
  artifacts:
    - path: "tests/e2e/macbook/50a_kaan_walk_checklist.md"
      provides: "Kaan's executable walk checklist (per project_phase_16_kaan_dj_testing memory)"
      min_lines: 60
    - path: "tests/e2e/macbook/nielsen_10_checklist.json"
      provides: "Machine-readable Nielsen 10 heuristics with Tier-1 prompts"
      min_lines: 60
    - path: "scripts/e2e/record_50a_walk.sh"
      provides: "Screencast capture rig for Kaan's local walk"
      min_lines: 50
    - path: "docs/e2e/README.md"
      provides: "§E2E-50A-WALK discharge procedure + Nielsen rubric"
      min_lines: 40
    - path: "docs/e2e/.gitkeep"
      provides: "Dir placeholder pre-screencast"
      min_lines: 1
    - path: ".planning/STATE.md"
      provides: "MODIFIED — Kaan-action surface updated with §E2E-50A-WALK + §INSTALL-VM-RUN downstream"
      min_lines: 0
    - path: ".planning/REQUIREMENTS.md"
      provides: "MODIFIED — E2E REQ status checkbox updates"
      min_lines: 0
  key_links:
    - from: "tests/e2e/macbook/50a_kaan_walk_checklist.md"
      to: "memory project_phase_16_kaan_dj_testing"
      via: "Kaan-ear NOT formal 30-session harness"
      pattern: "subjective discharge per memory anchor"
    - from: "scripts/e2e/record_50a_walk.sh"
      to: "docs/e2e/2026-05-walk.webm (KAAN-ACTION-LEGAL)"
      via: "screencast output destination"
      pattern: "engineering scaffolds capture rig; Kaan discharges recording"
    - from: ".planning/STATE.md § Accumulated Context"
      to: "§E2E-50A-WALK + §INSTALL-VM-RUN downstream"
      via: "Kaan-action surface registration"
      pattern: "autonomous-mode defer-blockers protocol"
---

<objective>
50a Kaan-ear subjective pass scaffold: walk checklist + Nielsen 10 Tier-1 prompts + screencast capture rig. Update STATE.md Kaan-action surface and REQUIREMENTS.md E2E checkboxes.

Engineering ships scaffold; Kaan executes the walk + records `docs/e2e/2026-05-walk.webm` at §E2E-50A-WALK discharge.

Out of scope: 30-session formal hallucination harness (memory `project_phase_16_kaan_dj_testing` explicit deferral), the screencast .webm itself (Kaan-action), quantified SUS/NASA-TLX (out of v3.1 scope per REQUIREMENTS § Future).
</objective>

<execution_context>
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-CONTEXT.md` § 50a Subjective Kaan-Ear Pass
- `.planning/REQUIREMENTS.md` § E2E-06, E2E-07
- `.planning/STATE.md` § Accumulated Context (target for Kaan-action surface)
- Memory: `project_phase_16_kaan_dj_testing` — Kaan-ear NOT formal harness
- Memory: `feedback_autonomous_no_grey_area_pause` — defer blockers, don't pause
</execution_context>

<steps>
1. Write `tests/e2e/macbook/50a_kaan_walk_checklist.md` — markdown checklist with 8–12 steps. Each step has `[ ] PASS / [ ] FAIL` + free-text note. Steps reflect real DJ workflow: BlackHole route → audio playback → mascot reactions × 3 → close.
2. Write `tests/e2e/macbook/nielsen_10_checklist.json` — JSON array of 10 heuristic objects. Per-surface prompts focus on Tier-1: library page + live session + settings.
3. Write `scripts/e2e/record_50a_walk.sh` — bash with `set -euo pipefail`. macOS-only (per project constraints; Win 50a walk deferred). Uses `screencapture -v` invocation example + ffmpeg transcode command. Emits final path.
4. Write `docs/e2e/README.md` — §E2E-50A-WALK discharge procedure + Nielsen scoring rubric + git-lfs note for >25 MB recordings.
5. Add `docs/e2e/.gitkeep`.
6. Edit `.planning/STATE.md` — append to Accumulated Context section:
   - `§E2E-50A-WALK` — Kaan's MacBook walk per memory `project_phase_16_kaan_dj_testing`; engineering ships harness scaffold + Nielsen checklist + screencast capture rig; Kaan discharges walk + records `docs/e2e/2026-05-walk.webm`.
   - `§INSTALL-VM-RUN downstream` — carry-forward from Phase 49; 50b real-VM execution on all 5 OS configs (engineering ships dry-run + 2 reachable configs; full execution waits on §INSTALL-VM-RUN + §INSTALL-COMPANION-SIGN).
7. Edit `.planning/REQUIREMENTS.md` § E2E — mark E2E-01..10 as `[x]` for engineering-green; annotate E2E-06 / E2E-07 with `(§E2E-50A-WALK pending Kaan discharge)`; annotate E2E-02 with `(§INSTALL-VM-RUN downstream — full real-VM execution waits)`.
8. Grep all 5 files (md/json/sh/README) for banned tokens (`deeply`, `seamlessly`, `crafted`, `unleashes`, `delight`, `magic`, `experience` as full word). Zero matches.
9. Commit: `feat(50-06): 50a Kaan-walk scaffold + Nielsen 10 + Kaan-action surface`.
</steps>

<verification>
- All 5 deliverable files exist with min-lines met
- STATE.md contains both Kaan-action surface entries verbatim
- REQUIREMENTS.md E2E section reflects engineering-green status for E2E-01..10 with annotations
- Grep banned tokens across new files → zero matches
- `bash -n scripts/e2e/record_50a_walk.sh` → syntactically valid
- `python -c "import json; json.load(open('tests/e2e/macbook/nielsen_10_checklist.json'))"` → valid JSON
- Step-0 worktree invariant documented in commit
</verification>
