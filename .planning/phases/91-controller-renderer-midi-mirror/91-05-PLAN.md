---
phase: 91-controller-renderer-midi-mirror
plan: 05
type: execute
wave: 3
depends_on: [91-01, 91-02, 91-03, 91-04]
files_modified:
  - tauri/ui/src/learn/learn-window.ts
  - tauri/ui/src/learn/ws-client.ts
  - tauri/ui/src/learn/controllers/_aria-labels.ts
  - tauri/ui/src/learn/controllers/_generic.svg.ts
  - tauri/ui/src/learn/controllers/pioneer_ddj_flx4.svg.ts
  - tauri/ui/src/learn/components/controller-stage.ts
  - tauri/ui/src/learn/components/titlebar.ts
  - tauri/ui/src/learn/components/empty-state.ts
  - tauri/ui/src/learn/components/status-bar.ts
  - tauri/ui/src/learn/components/unplugged-toast.ts
  - tauri/ui/src/learn/styles/learn.css
autonomous: true
requirements: [RENDER-01, RENDER-02, RENDER-03, RENDER-05, RENDER-06]
must_haves:
  truths:
    - "The Learn webview at `learn.html` opens a ws client to ws://127.0.0.1:8765 (the SAME socket the live deck uses — Invariant #4 preserved)."
    - "On receiving `ipc.learn.controller_detected` with `connected: true` and `controller_id: pioneer_ddj_flx4`, the FLX4 SVG mounts at `#learn-root` within 2 seconds."
    - "On receiving `ipc.learn.controller_detected` with `controller_id` matching no known SVG (or low-confidence detection), the generic fallback SVG renders with labeled DECK A / MIXER / DECK B zones."
    - "On receiving `ipc.learn.midi_position` frames, the SVG controls (knobs rotate, faders translate, button cap fills swap) update via `transform`-only or `data-active` swaps — no full SVG repaint — at synthetic P95 ≤ 50 ms (jsdom) / red guardrail at 80 ms."
    - "Every `<g data-control-id>` in the FLX4 SVG has `role=\"button\"`, a non-empty `aria-label` derived from `_aria-labels.ts`, AND two empty `<g class=\"cue-color\"></g>` + `<g class=\"cue-shape\"></g>` child slots (RENDER-05 scaffolding for P92)."
    - "The FLX4 SVG passes the bi-directional parity gate against `src/vibemix/midi/profiles/pioneer_ddj_flx4.json` — every `data-control-id` in the SVG has a profile binding and vice versa (RENDER-06)."
    - "No `#FF7F00` / `pioneer dj` / hex literal `fill=` or `stroke=` appears anywhere in the 2 shipped SVG files (brand-safety + currentColor discipline)."
  artifacts:
    - path: tauri/ui/src/learn/learn-window.ts
      provides: Webview entrypoint — mounts `LearnWindow` root, opens ws client, dispatches `controller_detected`/`midi_position` envelopes
      min_lines: 100
    - path: tauri/ui/src/learn/controllers/pioneer_ddj_flx4.svg.ts
      provides: Inline SVG of DDJ-FLX4 (hand-authored from manufacturer hardware-diagram PDFs, currentColor + var(--silk-22) strokes only)
      min_lines: 80
    - path: tauri/ui/src/learn/controllers/_generic.svg.ts
      provides: Labeled-zone generic fallback (DECK A / MIXER / DECK B large rectangles with silk-22 dashed borders)
      min_lines: 30
    - path: tauri/ui/src/learn/controllers/_aria-labels.ts
      provides: Hand-authored lookup `Record<string, string>` of (field, deck) → human-readable aria-label
      contains: ARIA_LABELS
  key_links:
    - from: tauri/ui/src/learn/learn-window.ts
      to: tauri/ui/src/ipc/validator.generated.mjs
      via: ws onmessage ajv validate
      pattern: validator\\.generated
    - from: tauri/ui/src/learn/learn-window.ts
      to: ws://127.0.0.1:8765
      via: new WebSocket(...)
      pattern: 8765
    - from: tauri/ui/src/learn/controllers/pioneer_ddj_flx4.svg.ts
      to: src/vibemix/midi/profiles/pioneer_ddj_flx4.json
      via: every data-control-id matches a profile field+deck binding
      pattern: data-control-id
---

<objective>
Land the full frontend half of P91 — the webview entry (`learn-window.ts`, `ws-client.ts`), 5 small components (titlebar, controller-stage, empty-state, status-bar, unplugged-toast), the ARIA-label lookup, the FLX4 inline SVG, the generic labeled-zone SVG fallback, and the styles file. Make the FLX4 + generic test stubs from Plan 02 GO GREEN — parity gate, aria-labels-present, dual-cue-slots-present, ws-client-uses-8765, controller-detected-mounts-svg, generic-fallback, latency harness, brand-safety greps. The FLX4 SVG is the canonical golden — the moment this plan lands, Kaan can plug an FLX4 and see it mirror on screen (Plan 07 is the actual ear-pass checkpoint, but the artifact is operable from here).

Purpose: This is where the user-facing artifact lights up. Plans 01-04 prepared the wire contract, the Python emitter, and the Rust window-spawn primitive. Plan 05 paints the actual pixels.

Output: 11 new TS files under `tauri/ui/src/learn/` (1 entry + 1 ws-client + 5 components + 3 controllers + 1 styles). After this plan, `cd tauri/ui && npm run build` emits the Learn bundle; `cd tauri/ui && npm test` passes — every TS test from Plan 02 that depends on FLX4+generic source goes green; the 9 not-yet-shipped controller specs stay `it.todo`/skipped pending Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-controller-renderer-midi-mirror/91-CONTEXT.md
@.planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md
@.planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md
@.planning/phases/91-controller-renderer-midi-mirror/91-VALIDATION.md
@tauri/ui/src/debrief/debrief-window.ts
@tauri/ui/src/debrief/ws-client.ts
@tauri/ui/src/wizard/controllers/ddj-flx4.svg.ts
@tauri/ui/src/lib/tokens.css
@src/vibemix/midi/profiles/pioneer_ddj_flx4.json
@.claude/skills/frontend-enforcement/SKILL.md

<interfaces>
Frontend stack: vanilla TS + DOM (no React/Vue per UI-SPEC §Design System). Inline-SVG strings via Vite `?raw` import (precedent: `tauri/ui/src/wizard/controllers/ddj-flx4.svg.ts`).

Plan 01 envelopes (Python -> webview):
- `ipc.learn.controller_detected` payload: `{ connected: boolean, controller_id: string, display_name: string, port_name: string }`.
- `ipc.learn.midi_position` payload: `{ controller_id: string, positions: Record<string, number> }` where keys are `<field>:<deck>` or bare `<field>` and values are 0..127 integers.

ws-client pattern (research §Architecture Diagram):
- `new WebSocket("ws://127.0.0.1:8765")` — SAME port as live deck.
- onmessage → JSON.parse → ajv `validator.generated.mjs` validate → `CustomEvent(envelope.type, { detail: envelope.payload })` dispatch on `window`.
- learn-window.ts subscribes to `window.addEventListener("ipc.learn.controller_detected", ...)` etc.

SVG hit-region pattern (research §Pattern 3):
- Every interactive control is `<g data-control-id="<field>:<deck>" role="button" aria-label="..." tabindex="0">`.
- `<field>:<deck>` exactly matches the `field` value in `src/vibemix/midi/profiles/pioneer_ddj_flx4.json` with the deck suffix appended.
- Each `<g>` contains the factual geometry (`<circle>`, `<rect>`, etc. with `stroke="currentColor"` + `var(--silk-22)` default) PLUS two empty cue slots `<g class="cue-color"></g>` + `<g class="cue-shape"></g>` (P91 scaffolding for P92 highlight).

Existing tokens (UI-SPEC §Color):
- `var(--void)`, `var(--silk)`, `var(--silk-22)`, `var(--silk-65)`, `var(--amber)`, `var(--led-ok)`, `var(--led-fault)`, `var(--learn-highlight)` (new — declared in `learn.css` pointing at `var(--amber)` so P92 can flip via CSS-variable swap).
- ALL SVG strokes/fills use `currentColor` or `var(--silk-22)`/`var(--silk)`/`var(--learn-highlight)`/`none` — never hex literals (UI-SPEC §Color forbidden-list).

UI-SPEC §Component Inventory enumerates every TS file in this plan and its visual contract. Use it as the spec for empty-state / status-bar / titlebar / unplugged-toast layout.

FLX4 profile (`src/vibemix/midi/profiles/pioneer_ddj_flx4.json`) — control list (must produce a matching `<g data-control-id>` for each):
- Deck A + B controls: `vol_a`/`vol_b`, `eq_hi_a`/`eq_hi_b`, `eq_mid_a`/`eq_mid_b`, `eq_low_a`/`eq_low_b`, `tempo_a`/`tempo_b`, `filter_a`/`filter_b`.
- Master: `xfader`.
- Buttons per deck: `play_a`/`play_b`, `cue_a`/`cue_b`, `sync_a`/`sync_b`, `jog_a`/`jog_b`, `loop_in_a`/`loop_in_b` (+ `loop_out_*` if present in the profile).
- Wire shape (Plan 03 `_read_current_positions` mapping): deck-suffixed `<field>:<deck>` keys — e.g. `eq_hi:A`, `vol:B`, `play:A`. Master uses bare `xfader`.
- The SVG `<g data-control-id>` value MUST match this wire-shape exactly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the FLX4 inline SVG, the generic fallback SVG, and the ARIA-label lookup</name>
  <files>tauri/ui/src/learn/controllers/pioneer_ddj_flx4.svg.ts, tauri/ui/src/learn/controllers/_generic.svg.ts, tauri/ui/src/learn/controllers/_aria-labels.ts</files>
  <read_first>
    - src/vibemix/midi/profiles/pioneer_ddj_flx4.json (the full control list — every entry needs a matching `<g data-control-id>`)
    - tauri/ui/src/wizard/controllers/ddj-flx4.svg.ts (the precedent for inline-SVG `?raw` exports — copy the export-string-literal pattern + the SPDX header style)
    - .planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md §Pattern 3 (SVG hit regions + `data-control-id` convention) + §Pattern 4 (ARIA label lookup shape) + §Pattern 5 (dual-cue slot scaffolding)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Asset Pipeline (the 9-rule SVG authoring contract: hand-authored from manufacturer PDF, no Pioneer logo, no #FF7F00, no photo-lift, viewBox-normalised 0 0 1280 720, `<g data-control-id>` per interactive control, currentColor everywhere, _generic.svg.ts labeled-zone layout)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Color forbidden list + §Asset Pipeline rule 7 (currentColor everywhere)
    - tauri/ui/tests/learn/test_svg_profile_parity.spec.ts (Plan 02 stub — its parameterised assertion is the executor's done-when-green gate for FLX4 + generic; the other 9 controllers stay skipped pending Plan 06)
  </read_first>
  <behavior>
    - `pioneer_ddj_flx4.svg.ts` exports a const `PIONEER_DDJ_FLX4_SVG` (string template literal containing the inline SVG). Starts with `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1280 720" role="img" aria-label="Pioneer DDJ-FLX4 schematic" ...>` and ends with `</svg>`.
    - Every interactive control listed in `pioneer_ddj_flx4.json` has a corresponding `<g data-control-id="<wire-key>" role="button" aria-label="<from ARIA_LABELS lookup>" tabindex="0">` element wrapping the factual geometry.
    - The wire-key convention is `<field>:<deck>` per Plan 03 `_read_current_positions` — so `eq_hi_a` profile binding becomes `data-control-id="eq_hi:A"`, `vol_b` becomes `data-control-id="vol:B"`, etc.; `xfader` (no deck) becomes the bare `data-control-id="xfader"`.
    - Every `<g data-control-id>` contains: (a) factual geometry (one or more `<circle>` / `<rect>` / `<line>` elements with `stroke="currentColor"` only — NO hex `fill=` or `stroke=` literals); (b) two empty child slots `<g class="cue-color"></g>` and `<g class="cue-shape"></g>` (P91 scaffolding for P92 highlight per UI-SPEC §Color dual-channel cue baseline).
    - Knob geometry: a circle with a center marker line (the index line that rotates on CC change). Fader geometry: a rectangular slot + a draggable thumb `<rect>` translated by `data-active`. Button geometry: a rounded `<rect>` with `data-active="0|1"` swapping fill between `none` (off) and `var(--silk)` (on).
    - The data-`cx` / data-`cy` attributes are populated on knob `<g>` elements so the latency harness's `getCenterX`/`getCenterY` helpers (research §Code Example 3) can read pivot coordinates.
    - File header: SPDX-Apache-2.0 + a provenance comment naming the publisher (e.g. "Pioneer DJ DDJ-FLX4 Operating Instructions, ©AlphaTheta Corp., 2023 revision") + retrieval date (placeholder OK — Kaan ratifies). NO Pioneer logo. NO `#FF7F00` or any literal in the forbidden palette.
    - `_generic.svg.ts` exports `GENERIC_CONTROLLER_SVG` (string) — a labeled-zone layout: three large rectangles (`DECK A`, `MIXER`, `DECK B`) with `stroke="currentColor" stroke-dasharray="8 6"` (the "we don't recognise this controller" visual), each containing a JetBrains-Mono text label centered. No specific control geometry. NO `<g data-control-id>` entries (this is the explicit "unmapped" surface; the parity gate per Plan 02 should skip the generic file or assert empty set equality on both sides).
    - `_aria-labels.ts` exports `export const ARIA_LABELS: Record<string, string>` covering every (field, deck) combination across all 10 controller profiles — populated per research §Code Example §Pattern 4 (e.g. `"eq_hi:A": "EQ-HI knob, deck A"`, `"xfader": "crossfader"`, etc.). Hand-authored once; Plan 06's new SVGs will reference the same lookup.
    - All test stubs from Plan 02 that scan SVG files pass: `test_no_pioneer_orange.spec.ts`, `test_svg_currentcolor_only.spec.ts`, `test_no_pioneer_brand_marks.py` stay green. `test_svg_profile_parity.spec.ts` is green for the FLX4 row. `test_aria_labels_present.spec.ts` is green for FLX4. `test_dual_cue_slots_present.spec.ts` is green for FLX4. `test_generic_fallback.spec.ts` is green.
  </behavior>
  <action>Create `tauri/ui/src/learn/controllers/_aria-labels.ts` FIRST per research §Pattern 4 — SPDX header, then `export const ARIA_LABELS: Record<string, string> = { "eq_hi:A": "EQ-HI knob, deck A", "eq_hi:B": "EQ-HI knob, deck B", "eq_mid:A": "EQ-MID knob, deck A", "eq_mid:B": "EQ-MID knob, deck B", "eq_low:A": "EQ-LOW knob, deck A", "eq_low:B": "EQ-LOW knob, deck B", "vol:A": "channel fader, deck A", "vol:B": "channel fader, deck B", "tempo:A": "pitch fader, deck A", "tempo:B": "pitch fader, deck B", "filter:A": "filter knob, deck A", "filter:B": "filter knob, deck B", "xfader": "crossfader", "play:A": "play button, deck A", "play:B": "play button, deck B", "cue:A": "cue button, deck A", "cue:B": "cue button, deck B", "sync:A": "sync button, deck A", "sync:B": "sync button, deck B", "jog_touched:A": "jog wheel, deck A", "jog_touched:B": "jog wheel, deck B", "loop_in:A": "loop-in button, deck A", "loop_in:B": "loop-in button, deck B", "loop_out:A": "loop-out button, deck A", "loop_out:B": "loop-out button, deck B" }` plus any additional (field, deck) pairs that Plan 06's controllers will need (DDJ-1000 / SX3 have hotcue pads, FX section — add `hotcue:A:1`..`hotcue:A:8`, `hotcue:B:1`..`hotcue:B:8`, `tap_tempo`, `master_vol`, `headphone_cue` etc. as you find them in the 10 profile JSONs). Use research §Pattern 4 baseline + a profile grep to surface additional fields; if a profile uses a field not yet in ARIA_LABELS, add it with a sensible human label. Then create `tauri/ui/src/learn/controllers/pioneer_ddj_flx4.svg.ts` — SPDX header + provenance comment + `export const PIONEER_DDJ_FLX4_SVG = \`<svg ...>...</svg>\`;`. Hand-author the SVG following UI-SPEC §Asset Pipeline + research §Pattern 3 + §Pattern 5: viewBox `0 0 1280 720`, all strokes `currentColor`, all fills `none` or `var(--silk)` / `var(--void-3)` / `var(--silk-12)` per UI-SPEC §Color; each interactive element wrapped in `<g data-control-id="<wire-key>" role="button" aria-label="${ARIA_LABELS[wire-key]}" tabindex="0">` with `data-cx` / `data-cy` on knobs for the latency harness pivot reads; each `<g>` ends with `<g class="cue-color"></g><g class="cue-shape"></g>` (the dual-cue empty slots). DDJ-FLX4 layout: two decks left+right (each with jog wheel circle, play/cue/sync buttons, hotcue pads if profile lists them), central mixer (EQ knobs per deck, channel faders, crossfader, tempo faders, filter knobs). Use research §Pattern 3 SVG example block as the per-element template. Inline the entire SVG as a string literal — DO NOT use `<image href=...>` (Apache-clean photo-lift forbidden). Then create `tauri/ui/src/learn/controllers/_generic.svg.ts` — labeled-zone layout per UI-SPEC §Component Inventory `_generic` row: three large rounded rectangles centered on the 1280×720 grid labeled `DECK A`, `MIXER`, `DECK B` (JetBrains Mono text via `<text font-family="\"JetBrains Mono\", monospace" font-size="32" letter-spacing="0.18em" fill="currentColor">DECK A</text>`), each rectangle with `stroke="currentColor" stroke-dasharray="8 6"` to convey the "unmapped" affordance. NO `<g data-control-id>` entries in `_generic.svg.ts`. NO Pioneer logo, NO #FF7F00, NO hex literals anywhere. Reference: UI-SPEC §Asset Pipeline 9-rule list (rule 8 specifies the generic-fallback shape) + research §Pattern 3.</action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; npx vitest run tests/learn/test_svg_profile_parity.spec.ts tests/learn/test_aria_labels_present.spec.ts tests/learn/test_dual_cue_slots_present.spec.ts tests/learn/test_no_pioneer_orange.spec.ts tests/learn/test_svg_currentcolor_only.spec.ts tests/learn/test_generic_fallback.spec.ts --reporter=basic 2>&amp;1 | tail -30 &amp;&amp; PYTHONPATH=src python3 -m pytest tests/learn/test_no_pioneer_brand_marks.py -v</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist: `pioneer_ddj_flx4.svg.ts`, `_generic.svg.ts`, `_aria-labels.ts` under `tauri/ui/src/learn/controllers/`.
    - `pioneer_ddj_flx4.svg.ts` contains `export const PIONEER_DDJ_FLX4_SVG`; the SVG string starts with `<svg` and contains `viewBox="0 0 1280 720"`.
    - `_generic.svg.ts` contains `export const GENERIC_CONTROLLER_SVG`; the SVG string contains the strings `DECK A`, `MIXER`, `DECK B` and at least one `stroke-dasharray` attribute.
    - `_aria-labels.ts` exports `ARIA_LABELS: Record<string, string>` containing AT LEAST the 25 baseline (field, deck) keys from research §Pattern 4.
    - `cd tauri/ui && npx vitest run tests/learn/test_svg_profile_parity.spec.ts` exits 0 with the FLX4 row PASSED (other 9 may be skipped).
    - `npx vitest run tests/learn/test_aria_labels_present.spec.ts` exits 0 with FLX4 PASSED.
    - `npx vitest run tests/learn/test_dual_cue_slots_present.spec.ts` exits 0 with FLX4 PASSED (every `<g data-control-id>` in the FLX4 SVG has the 2 empty cue-color + cue-shape slots).
    - `npx vitest run tests/learn/test_no_pioneer_orange.spec.ts` exits 0 PASSED (zero brand-orange hex literals).
    - `npx vitest run tests/learn/test_svg_currentcolor_only.spec.ts` exits 0 PASSED (zero `fill="#..."`/`stroke="#..."` hex literals).
    - `pytest tests/learn/test_no_pioneer_brand_marks.py -v` exits 0 PASSED (no "Pioneer DJ" or "Pioneer ®" string).
    - `npx vitest run tests/learn/test_generic_fallback.spec.ts` exits 0 PASSED.
    - `git grep "<image " tauri/ui/src/learn/controllers/*.svg.ts` returns zero matches (no raster photo-lift).
  </acceptance_criteria>
  <done>FLX4 inline SVG + generic fallback SVG + ARIA-label lookup all ship; 8 of Plan 02's TS test stubs go GREEN (parity, aria-labels, dual-cue slots, brand-orange, currentColor, brand-marks, generic-fallback, all but the latency harness which Task 2 lights).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the webview entry (learn-window.ts, ws-client.ts), the 5 components, the styles file — making the remaining Plan 02 test stubs go green</name>
  <files>tauri/ui/src/learn/learn-window.ts, tauri/ui/src/learn/ws-client.ts, tauri/ui/src/learn/components/controller-stage.ts, tauri/ui/src/learn/components/titlebar.ts, tauri/ui/src/learn/components/empty-state.ts, tauri/ui/src/learn/components/status-bar.ts, tauri/ui/src/learn/components/unplugged-toast.ts, tauri/ui/src/learn/styles/learn.css</files>
  <read_first>
    - tauri/ui/src/debrief/debrief-window.ts (the webview entrypoint precedent — root mount, ws subscribe, custom-event dispatch)
    - tauri/ui/src/debrief/ws-client.ts (the ajv-validated ws client precedent — `new WebSocket("ws://127.0.0.1:8765")`, JSON.parse, validator.generated.mjs validate, CustomEvent dispatch)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Component Inventory (the verbatim visual contract for each of the 5 components — titlebar, controller-stage, empty-state, status-bar, unplugged-toast)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Spacing Scale (use `var(--sp-*)` tokens only — DO NOT introduce new spacing tokens)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Typography + §Color (forbidden fonts list + the reserved-for amber 5-element hardcap)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Motion + Interaction (transform-only knob rotation, transform: translateY on faders, data-active swap on buttons, breathing under-stroke on title, focus-ring inheritance from tokens.css)
    - .planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md §Architecture Diagram + §Pitfall 6 (frame-flood pattern — keep only the latest midi_position frame between repaint slots)
    - tauri/ui/src/lib/tokens.css (the existing token table — body background, focus ring, motion durations; this plan does NOT modify it)
    - .claude/skills/frontend-enforcement/SKILL.md (20/80 rule, retro-futurist hardware vocabulary, motion is intentional, no AI slop)
    - tauri/ui/tests/learn/highlight-latency.test.ts (Plan 02 stub — needs `PIONEER_DDJ_FLX4_SVG` import to resolve; flips green after Task 1 lands the SVG; this task also makes the harness verifiable end-to-end in the wired runtime)
    - tauri/ui/tests/learn/test_controller_detected_mounts_svg.test.ts (Plan 02 stub — green when this task lands the `learn-window.ts` mount logic)
    - tauri/ui/tests/learn/test_ws_client_uses_8765.spec.ts (Plan 02 grep gate — green when `ws-client.ts` ships with `:8765`)
    - tauri/ui/tests/learn/test_sr_announcement.spec.ts (Plan 02 stub — green when the `aria-live="polite"` element is mounted in `learn-window.ts`)
  </read_first>
  <behavior>
    - `learn-window.ts` is the webview entrypoint imported by `tauri/ui/learn.html`'s `<script src="/src/learn/learn-window.ts">`. It mounts a `LearnWindow` root into `#learn-root` (the element id from Plan 01's `learn.html`), opens a `LearnWsClient` connection, and dispatches `controller_detected`/`midi_position` envelopes to the mounted components.
    - `ws-client.ts` opens `new WebSocket("ws://127.0.0.1:8765")` (the SAME port — Invariant #4), JSON-parses each frame, ajv-validates via `import("../ipc/validator.generated.mjs")`, and dispatches `CustomEvent(envelope.type, { detail: envelope.payload })` on `window` (mirroring `tauri/ui/src/debrief/ws-client.ts`). On parse/validate failure, logs `[learn:ws]` to console and drops the frame (defensive — never crashes the webview).
    - `learn-window.ts` registers a single `requestAnimationFrame` drainer for `midi_position` frames per research §Pitfall 6: a module-level `let pendingPositions: object | null = null;` is set on every `midi_position` event; the next rAF callback consumes it (LWW — last write wins; tab-resume floods only the most-recent frame).
    - `controller-stage.ts` mounts the inline SVG corresponding to the most recent `controller_detected` envelope's `controller_id`. Uses dynamic import (`await import("../controllers/<id>.svg")`) per research §Pitfall 5 (lazy SVG chunks). Falls back to `_generic.svg.ts` on `find_mapping` no-match OR an unknown controller_id. The mounted SVG is the only child of the stage; subsequent `midi_position` frames update existing elements via `transform`/`data-active` mutation only (no re-mount).
    - The `applyPositionFrame(stage, frame)` helper (research §Code Example 3) walks `frame.positions`, for each `<field>:<deck>` key locates the `<g data-control-id="...">` element, and: for knobs (presence of `data-cx`/`data-cy`), sets `transform="rotate(${degrees} ${cx} ${cy})"` with `degrees = ((value / 127) * 270) - 135`; for faders (those `<g>`s with class `fader`), sets `transform="translateY(${(1 - value/127) * faderRange}px)"`; for buttons (those with class `button` or `data-control-id^="play|cue|sync|jog_touched"`), sets `data-active="${value > 0 ? 1 : 0}"`. The CSS handles the visual response to `data-active`.
    - `empty-state.ts` renders the "no controller detected" surface per UI-SPEC §Component Inventory empty-state row: centered single column, 64×64 plug glyph (silk-22 currentColor), 28px Saira heading `no controller detected`, 16px body `plug one in to begin.`, no CTA.
    - `titlebar.ts` renders the 56px-tall titlebar per UI-SPEC: left `LEARN` wordmark (Saira wdth 85, wght 700, 18px, silk + 12px amber text-shadow); center controller display name (JetBrains Mono, silk-65, 11px UPPERCASE letter-spacing 0.18em); right clock (JetBrains Mono 11px silk-40, updates once per second).
    - `status-bar.ts` renders the 40px bottom rail per UI-SPEC: 3 segments with silk-22 hairline `|` dividers — (1) controller display name + mirror status; (2) latency probe `P95: NN ms` with green/amber/red pip per UI-SPEC §Open Decisions #5 (≤50ms green, 50-80ms amber, >80ms red — the user-visible early-warning for §LEARN-LATENCY-CONTINGENCY); (3) `cmd+tab to deck` hint. Latency is calculated from a rolling 240-sample window of `(performance.now() - envelope.ts_decoded)` deltas.
    - `unplugged-toast.ts` is mounted as a top-floating element by `learn-window.ts` on `controller_detected.connected === false`; auto-dismisses after 4s; uses the `--led-fault` border + silk text per UI-SPEC §Color destructive row.
    - An `aria-live="polite"` element exists in `learn-window.ts`'s DOM tree; on first `controller_detected.connected === true`, its text content becomes `<display_name> connected.`; on disconnect, `controller disconnected.`.
    - `learn.css` declares `.learn-controller-schematic` styling (default `color: var(--silk-22)` for `currentColor` propagation), defines the `--learn-highlight: var(--amber)` variable (Plan 92 will flip this dynamically on highlighted groups), implements the 1fr-grid window layout `display: grid; grid-template-rows: var(--titlebar-h) 1fr var(--statusbar-h);`, and the title under-stroke breathing animation per UI-SPEC §Motion.
    - All UI-SPEC §Copywriting Contract strings are byte-equivalent (lowercase per CONTEXT.md tone discipline + UI-SPEC forbidden-list).
    - Frontend-enforcement skill compliance: NO Inter / Roboto / Arial fonts; the 20/80 rule holds (amber accent reserved to 5 elements per UI-SPEC §Color reserved-for list); motion is intentional (breathing under-stroke = "sign of life", not decoration).
  </behavior>
  <action>Create `tauri/ui/src/learn/ws-client.ts` as a near-verbatim mirror of `tauri/ui/src/debrief/ws-client.ts`: SPDX header, `export class LearnWsClient { constructor(public url = "ws://127.0.0.1:8765") {...} }` opening a `WebSocket`, onmessage handler that does `JSON.parse(event.data)` → ajv validate via `import("../ipc/validator.generated.mjs")` (lazy import) → dispatch `window.dispatchEvent(new CustomEvent(env.type, { detail: env.payload }))` → on parse/validate fail log `[learn:ws]` and drop. Implement reconnect-on-close with 1s backoff (matches debrief precedent). Create `tauri/ui/src/learn/components/controller-stage.ts`: exports `class ControllerStage { mount(rootEl: HTMLElement) {} render(controllerId: string) { /* dynamic import + innerHTML the SVG */ } applyPositionFrame(positions: Record<string,number>) { /* the research §Code Example 3 applyPositionFrame helper */ } }`. The render function uses `await import("../controllers/" + safeId + ".svg")` with a hard-coded mapping `controllerId -> safeId` (defends against runtime import-string injection — only known controller_ids resolve). On unknown id or `connected: false`, renders `_generic.svg.ts`. Create `tauri/ui/src/learn/components/titlebar.ts`, `empty-state.ts`, `status-bar.ts`, `unplugged-toast.ts` each per UI-SPEC §Component Inventory verbatim — small modules, each exporting one render function or class. The status bar's latency probe uses `performance.now()` deltas captured by the ws-client on each midi_position envelope; the rolling 240-sample window computes P95 once per second. Create `tauri/ui/src/learn/learn-window.ts` as the orchestrator: SPDX header + import all components + bind `LearnWsClient` + wire `window.addEventListener("ipc.learn.controller_detected", e => ...)` and `window.addEventListener("ipc.learn.midi_position", e => { pendingPositions = e.detail.positions; })` + `requestAnimationFrame` drainer that consumes `pendingPositions` (research §Pitfall 6 frame-flood pattern). Mount the layout into `#learn-root`: grid of `titlebar` / `controller-stage` (or `empty-state` if no controller yet) / `status-bar`. Inject the `aria-live="polite"` element and update its textContent on connect/disconnect. Create `tauri/ui/src/learn/styles/learn.css` with the grid layout + `--learn-highlight` variable + `.learn-controller-schematic { color: var(--silk-22); }` + title under-stroke breathing animation (`@keyframes breathe` using `--motion-led-pulse` 1400ms ease-in-out infinite). All copy strings come from UI-SPEC §Copywriting Contract byte-equivalent. All token references via `var(--*)` from existing `tokens.css` — DO NOT add new tokens. Reference: research §Architecture Diagram (learn webview block) + §Code Example 3 (applyPositionFrame helper shape) + §Pitfall 6 (frame-flood handling) + UI-SPEC §Component Inventory + §Motion.</action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10 &amp;&amp; npm run build 2>&amp;1 | tail -10 &amp;&amp; npx vitest run tests/learn/ --reporter=basic 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files exist under `tauri/ui/src/learn/`: `learn-window.ts`, `ws-client.ts`, `components/{controller-stage,titlebar,empty-state,status-bar,unplugged-toast}.ts`, `styles/learn.css`.
    - `cd tauri/ui && npx tsc --noEmit` exits 0 (typechecks clean).
    - `cd tauri/ui && npm run build` exits 0 and emits `tauri/ui/dist/learn.html` + lazy SVG chunk(s) for the FLX4 + generic.
    - `git grep "127\\.0\\.0\\.1:8765" tauri/ui/src/learn/` returns at least 1 match (`ws-client.ts`).
    - `git grep -E "Inter|Roboto|Arial|system-ui" tauri/ui/src/learn/` returns zero matches (forbidden-fonts gate).
    - `cd tauri/ui && npx vitest run tests/learn/test_ws_client_uses_8765.spec.ts` exits 0 PASSED.
    - `cd tauri/ui && npx vitest run tests/learn/test_controller_detected_mounts_svg.test.ts` exits 0 PASSED.
    - `cd tauri/ui && npx vitest run tests/learn/test_sr_announcement.spec.ts` exits 0 PASSED.
    - `cd tauri/ui && npx vitest run tests/learn/highlight-latency.test.ts` exits 0 PASSED with P95 ≤ 80 ms (red guardrail).
    - `cd tauri/ui && npx vitest run tests/learn/test_learn_window_label.spec.ts` exits 0 PASSED (depends on Plan 04's Rust label).
    - `cd tauri/ui && npx vitest run tests/learn/test_all_11_svgs_present.spec.ts` shows 2 of 11 PASSED (FLX4 + generic) — the other 9 are still `it.todo` pending Plan 06.
    - `git grep "image href=" tauri/ui/src/learn/` returns zero matches.
    - `git grep -E "fill=\"#[0-9a-fA-F]" tauri/ui/src/learn/` returns zero matches (currentColor discipline — UI-SPEC forbidden list).
  </acceptance_criteria>
  <done>Webview entry + ws-client + 5 components + styles all ship; tsc and build pass; Plan 02's TS test stubs for FLX4+generic+latency+ws-port all go GREEN; the 9 not-yet-shipped controller specs stay properly skipped/todo for Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ws frame (Python) → webview JS | Every frame ajv-validated against `validator.generated.mjs`; invalid frames dropped + logged. |
| envelope → SVG `innerHTML` mount | `controllerId` from envelope is mapped through a hard-coded allowlist of known controller IDs; never used as a raw import path. |
| webview DOM ← latency probe | The P95 number is display-only — no consumer of the latency value triggers app behaviour beyond the visual pip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-05-01 | Tampering | XSS via SVG `<script>` injection in dynamic SVG strings | mitigate | All SVGs are hand-authored at build time (string literals in `.svg.ts` files); no runtime concatenation. CI gate `test_svg_no_script_tag.spec.ts` (optional follow-up — research recommended) greps for `<script` in `learn/**.svg.ts`; for P91 a manual grep at code review covers it. |
| T-91-05-02 | Tampering | spoofed ws frame with unknown `controller_id` triggers a dynamic-import path-traversal | mitigate | The `controller-stage.ts` `render` function maps `controllerId` through a hardcoded allowlist (`{"pioneer_ddj_flx4": pioneer_ddj_flx4.svg, ...}`); unknown ids fall back to `_generic.svg` — never used as a raw dynamic-import string. |
| T-91-05-03 | DoS | rapid `controller_detected` flood remounts SVG repeatedly | mitigate | `controller-stage.ts.render` early-returns if the new id matches the currently-mounted id; reconnects on the same controller are no-ops. |
| T-91-05-04 | DoS | tab-resume midi_position backlog flood | mitigate | Research §Pitfall 6 rAF-drainer pattern — only the LATEST frame between repaint slots is consumed. |
| T-91-05-SC | Tampering | npm/pip installs | accept | NONE — uses pre-existing vitest, playwright, jsdom; zero new packages. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd tauri/ui && npx tsc --noEmit
cd tauri/ui && npm run build 2>&1 | tail -5
cd tauri/ui && npx vitest run tests/learn/ --reporter=basic
PYTHONPATH=src python3 -m pytest tests/learn/ tests/ipc/test_learn_envelope_parity.py tests/runtime/test_ws_broadcast_30hz_under_learn_load.py -v
```

Expected:
- tsc + build exit 0.
- All Plan 02 TS test stubs except the 9-of-11 controllers-present rows are GREEN.
- All Plan 02 Python tests (5 of them) are GREEN.
- The 9 not-yet-shipped controllers remain `it.todo` pending Plan 06.
</verification>

<success_criteria>
- 11 new frontend files exist under `tauri/ui/src/learn/`: 1 entry, 1 ws-client, 5 components, 3 SVG/lookup files, 1 styles file.
- FLX4 + generic SVGs are bidirectionally parity-clean against `pioneer_ddj_flx4.json`.
- ws client connects to `:8765` (NOT a new port).
- Latency harness P95 ≤ 80 ms in jsdom (red guardrail not triggered).
- ARIA + dual-cue + brand-safety + currentColor gates all GREEN for FLX4 + generic.
- The 9 remaining controller specs are properly `it.todo`/skipped pending Plan 06.
- tsc + build + vitest + pytest all exit 0.
</success_criteria>

<output>
Create `.planning/phases/91-controller-renderer-midi-mirror/91-05-SUMMARY.md` recording: (a) the 11 frontend file paths + line counts; (b) the FLX4 SVG `data-control-id` set vs the FLX4 profile `field`+`deck` set (must be equal); (c) the latency harness P95 measurement; (d) Plan 02 test stub pass/skip/todo breakdown after this plan lands (e.g. "9 PASSED / 5 SKIPPED for 9 non-FLX4 controllers / 0 FAILED"); (e) screenshot path of a `cargo tauri dev` Learn window open with FLX4 SVG mounted (or a note that screenshot is a Kaan-action per `feedback_verify_live_app_not_just_tests`); (f) confirmation that `cd tauri/ui && npm run build` emits `dist/learn.html` + the FLX4 chunk. Note any UI-SPEC §Component Inventory item that didn't ship exactly as specified, with rationale.
</output>
