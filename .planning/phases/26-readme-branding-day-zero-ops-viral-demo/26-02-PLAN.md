---
phase: 26
plan: "02"
type: docs
autonomous: true
wave: 2
depends_on: ["26-01"]
requirements: [GH-19, GH-20, GH-21]
---

# Phase 26 Plan 02: GitHub Issue Templates + CONTRIBUTING Refinement

## Objective

Existing `.github/ISSUE_TEMPLATE/` already has bug_report.yml, feature_request.yml, new_controller.yml. Missing: `ai_misbehavior.yml` — the hallucination / anti-slop misbehavior template, which is the load-bearing "vibemix integrity" report channel for users.

Existing `CONTRIBUTING.md` is solid (DCO sign-off + 3 contribution paths). Refinement: add explicit sniff_controller.py invocation example, link to the new ai_misbehavior issue template, and confirm CODE_OF_CONDUCT.md is present (it is — Contributor Covenant 2.1, already in repo per `ls`).

## Context

@CLAUDE.md
@.planning/phases/26-readme-branding-day-zero-ops-viral-demo/26-CONTEXT.md
@CONTRIBUTING.md
@CODE_OF_CONDUCT.md

## Tasks

<task id="1" type="auto">
<name>Add ai_misbehavior issue template</name>
<behavior>
A new issue template `.github/ISSUE_TEMPLATE/ai_misbehavior.yml` exists, labeled appropriately for AI quality triage. Captures: what the AI said, what the actual musical event was (grounding evidence), which mode/level was active, whether the user can attach the events.jsonl excerpt.
</behavior>
<implementation>
1. Create `.github/ISSUE_TEMPLATE/ai_misbehavior.yml` with:
   - title prefix `[ai-slop]`
   - labels `["ai-quality", "triage"]`
   - markdown intro explaining that anti-slop is a P0 product priority and this template is the fastest path to a fix
   - inputs: vibemix version (required), OS (dropdown, required), skill level (Beginner/Intermediate/Pro), mode (Hype-man / Coach), what-the-AI-said (textarea required), what-actually-happened (textarea required — the grounding-evidence ask), events.jsonl excerpt (textarea jsonl), recording context (optional — were you mixing what?), anything-else (textarea)
   - matches the existing template tone (concise, no fluff, "your specifics make fixes faster")
2. Verify the auto-labels list matches existing patterns (`bug` + `triage` for bug_report; `ai-quality` + `triage` for misbehavior).
</implementation>
<files>.github/ISSUE_TEMPLATE/ai_misbehavior.yml</files>
<verification>
- `[ -f .github/ISSUE_TEMPLATE/ai_misbehavior.yml ]` returns 0.
- YAML parses: `python3 -c "import yaml; yaml.safe_load(open('.github/ISSUE_TEMPLATE/ai_misbehavior.yml'))"` exits 0.
- `grep -c "labels:" .github/ISSUE_TEMPLATE/ai_misbehavior.yml` returns 1.
- `grep "ai-quality" .github/ISSUE_TEMPLATE/ai_misbehavior.yml` returns at least one hit.
</verification>
<done>
ai_misbehavior.yml exists, YAML-valid, follows existing template conventions, labels correctly.
</done>
</task>

<task id="2" type="auto">
<name>CONTRIBUTING refinement: sniff tool invocation + ai_misbehavior link</name>
<behavior>
CONTRIBUTING.md gets two targeted refinements: (a) the controller-mapping path includes the literal `python3 scripts/sniff_controller.py` invocation example with expected output shape, (b) a new "Reporting AI misbehavior / hallucinations" section links to the new ai_misbehavior issue template and explains why this report channel matters more than a generic bug.
</behavior>
<implementation>
1. Edit `CONTRIBUTING.md`:
   - In the "New controller mapping" section, add a sub-step showing the literal sniff tool invocation: `python3 scripts/sniff_controller.py --device "Your Controller Name"` plus a one-line note about the JSON output shape.
   - Add a new top-level section "Reporting AI misbehavior" between "What we don't accept" and "License", explaining:
     - Anti-slop is the product's central thesis — broken AI reactions matter more than feature requests.
     - Use the [ai_misbehavior issue template](.github/ISSUE_TEMPLATE/ai_misbehavior.yml).
     - The most useful thing is the events.jsonl excerpt + "what actually happened" context, which lets us replay the moment and verify the grounding chain.
2. Cross-link from the bug-report path: a one-line "for hallucinated reactions or anti-slop violations, use the AI misbehavior template instead".
</implementation>
<files>CONTRIBUTING.md</files>
<verification>
- `grep "sniff_controller.py" CONTRIBUTING.md` returns at least one hit.
- `grep -i "ai misbehavior\|ai_misbehavior\|hallucin" CONTRIBUTING.md` returns at least one hit.
- `grep -c "^## " CONTRIBUTING.md` shows section count increased by 1.
- DCO sign-off section intact.
</verification>
<done>
CONTRIBUTING.md shows the sniff_controller.py invocation explicitly, has an AI-misbehavior reporting section linking to the new template, DCO section unchanged.
</done>
</task>

## Success Criteria

- 4 issue templates exist: bug_report.yml + feature_request.yml + new_controller.yml + ai_misbehavior.yml (the 4th is the new one).
- CONTRIBUTING.md shows literal sniff_controller.py invocation.
- CONTRIBUTING.md has AI-misbehavior reporting section.
- CODE_OF_CONDUCT.md (already shipped, Contributor Covenant 2.1) referenced from CONTRIBUTING.
- No regression: `pytest -q` failures don't increase.

## Output

- Created: `.github/ISSUE_TEMPLATE/ai_misbehavior.yml`
- Modified: `CONTRIBUTING.md`
- Commit: `feat(26-02): ai_misbehavior issue template + CONTRIBUTING refinement`
- Summary: `.planning/phases/26-readme-branding-day-zero-ops-viral-demo/26-02-SUMMARY.md`
