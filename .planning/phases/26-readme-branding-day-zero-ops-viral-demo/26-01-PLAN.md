---
phase: 26
plan: "01"
type: docs
autonomous: true
wave: 1
depends_on: []
requirements: [GH-19, GH-20, GH-21]
---

# Phase 26 Plan 01: README + Branding Asset Slot

## Objective

Audit + polish the existing README.md so it lands the anti-slop thesis as the load-bearing hook, then add a `BRANDING.md` documenting the CDJ Whisper visual direction with a placeholder SVG logo under `docs/branding/`. Existing README (already drafted, hero PNG + architecture SVG already shipped per Phase 19) gets the surgical anti-slop opener + community PR call refinement — not a full destructive rewrite.

## Context

@CLAUDE.md
@.planning/phases/26-readme-branding-day-zero-ops-viral-demo/26-CONTEXT.md
@README.md
@CONTRIBUTING.md

## Tasks

<task id="1" type="auto">
<name>README anti-slop hook + community PR call</name>
<behavior>
README opens with the "real DJ friend in your ear, no AI slop" hook prominently surfaced (above the install table) and adds an explicit community PR call to the controller compatibility section.
</behavior>
<implementation>
1. Edit `README.md`:
   - Tagline (line 7) is currently functional. Strengthen the lede paragraph (line 24) so the anti-slop thesis is the FIRST claim, not buried inside a long sentence.
   - Below the controller grid (line 67-69 area), add an explicit "Don't see your controller?" subsection that calls out `scripts/sniff_controller.py` + the new-controller issue template as the contribution path.
   - Audit all placeholder URLs: any GitHub Releases / Discord / demo GIF placeholder gets an explicit `<TBD>` marker comment if it doesn't already have one. Don't fabricate any URL.
   - Keep all existing structure (hero, install, feature matrix, controllers, screenshots, how-it-works, FAQ, Bravoh footer). Surgical edits only.
2. Lint check: visual review (markdownlint-cli unavailable on PATH; npx markdownlint-cli works — run `npx --yes markdownlint-cli README.md` for any catchable issues, ignore false positives that touch hero HTML).
</implementation>
<files>README.md</files>
<verification>
- `grep -i "AI slop" README.md` returns a hit in the lede paragraph (first 30 lines).
- `grep "sniff_controller" README.md` returns at least one hit.
- `git diff README.md` shows additive/surgical edits only (no section removals).
- Existing badges, feature matrix, FAQ all intact.
</verification>
<done>
README leads with anti-slop thesis prominently. Community controller PR path documented. No fabricated URLs introduced.
</done>
</task>

<task id="2" type="auto">
<name>BRANDING.md + placeholder logo SVG</name>
<behavior>
`BRANDING.md` at repo root documents the CDJ Whisper visual direction (palette, typography, tactility principles, what to avoid). Placeholder logo SVG lives at `docs/branding/logo.svg` — simple text-based wordmark for now, with explicit note that a designer-finalized logo is a Kaan-action.
</behavior>
<implementation>
1. Create `BRANDING.md` covering:
   - Visual direction summary (CDJ Whisper — Pioneer-grade hardware in library mode).
   - Color palette (5 warm blacks + single amber accent with 4 intensities). Reference `mocks/vibemix-direction-final.html` as the canonical source.
   - Typography (Geist for UI, Fraunces for display moments).
   - Tactility principles (faint glow over faux-3D bevels).
   - What to avoid (generic AI gradients, neon cyan/magenta, glass-morphism, faux 3D, anything resembling generic "AI assistant" branding).
   - Logo status: placeholder shipped, pro logo design = Kaan-action (post-v2.0).
   - Usage examples: where the wordmark appears (README hero, app titlebar, OG image).
2. Create `docs/branding/logo.svg`: simple text-based wordmark using Geist-like font-family and the amber accent color (#FF8A3D pulled from existing README badge). Black background, amber wordmark "vibemix". Marked as placeholder via SVG `<desc>` element.
3. Cross-link: `BRANDING.md` → `mocks/vibemix-direction-final.html` and `docs/branding/logo.svg`. README footer gets a one-line BRANDING.md reference (optional — only add if it fits cleanly in the existing footer line).
</implementation>
<files>BRANDING.md,docs/branding/logo.svg</files>
<verification>
- `[ -f BRANDING.md ]` returns 0.
- `[ -f docs/branding/logo.svg ]` returns 0.
- `grep -i "CDJ Whisper" BRANDING.md` returns at least one hit.
- `grep -i "placeholder" docs/branding/logo.svg` returns at least one hit.
- SVG is valid: `xmllint --noout docs/branding/logo.svg` exits 0 (or accept if xmllint unavailable — visual inspect).
</verification>
<done>
BRANDING.md exists, references CDJ Whisper, palette, typography. Placeholder SVG exists at docs/branding/logo.svg with explicit placeholder marker.
</done>
</task>

<task id="3" type="auto">
<name>Verify install instructions match real artifact paths</name>
<behavior>
Install instructions in README reflect the actual Phase 21 release matrix (DMG + MSI from GitHub Releases). Where the release page URL is not yet active, an explicit `<TBD>` marker is present.
</behavior>
<implementation>
1. Cross-reference `README.md` install table (lines 30-39) against Phase 21 deliverables in `.planning/phases/21-sign-notarize-github-release-matrix/`.
2. If the URLs in README point to `github.com/bravoh/vibemix/releases/latest` (the literal owner/repo from existing README), audit whether `bravoh/vibemix` is the actual final GitHub org/repo slug. If unclear, leave as-is with a TBD comment noting "verify org slug before v2.0 launch".
3. Add (or confirm exists) a one-line note under the install table: "Install URLs go live with the first signed release — see Phase 21 deliverables."
4. No fabricated artifact names. If the actual artifact name from Phase 21 is e.g. `vibemix_0.1.0_aarch64.dmg`, prefer the unversioned `latest` redirect (which is what's there).
</implementation>
<files>README.md</files>
<verification>
- `grep "releases/latest" README.md` returns the install URLs.
- No URL like `vibemix.app/download` or any fabricated direct-download URL.
- Either Phase 21 path matches OR a TBD comment is present.
</verification>
<done>
Install instructions are honest: they either point to the real Phase 21 release URL pattern or carry an explicit TBD marker.
</done>
</task>

## Success Criteria

- README opens with anti-slop thesis above-the-fold.
- Community controller PR path documented (sniff tool + issue template referenced).
- BRANDING.md exists with CDJ Whisper visual direction.
- Placeholder logo SVG exists at `docs/branding/logo.svg` (marked as placeholder).
- Install URLs are honest — either real or TBD-marked, never fabricated.
- No regression: `pytest -q` failures don't increase (baseline: 29 collection errors, 1 pre-existing scripts failure — out of scope for this plan).

## Output

- Modified: `README.md`
- Created: `BRANDING.md`, `docs/branding/logo.svg`
- Commit: `feat(26-01): README anti-slop hook + BRANDING.md + placeholder logo`
- Summary: `.planning/phases/26-readme-branding-day-zero-ops-viral-demo/26-01-SUMMARY.md`
