# Plan 104-02 — Frontend: what-remains render + quiet-fill pin + dual-cue a11y

**Phase:** 104 (Skill-Tree Surface + Earned Celebration)
**Wave:** 2 of 2 (frontend — schema/codegen + SkillWall render + a11y + vitest pins)
**Requirements:** SURF-01 (render what-remains) · SURF-02 (quiet-fill pin) · SURF-04 (dual-cue + keyboard-nav)
**Mode:** schema → codegen → render → pins; `frontend-enforcement` (CDJ-Whisper / 20-80 / no slop).

## Tasks

### T1 — Schema + codegen for `what_remains` (SURF-01)
- `tauri/ui/src/ipc/messages.schema.json`: add `what_remains: { type: string }` to the
  `skill_wall` array-item object (stays inside `learn.*` — no new envelope family).
- `tauri/ui/src/ipc/messages.ts`: add `what_remains: string` to the skill_wall row type
  (hand-mirror, as `6d87c632` did).
- `cd tauri/ui && npm run codegen:ipc` → regenerates `validator.generated.mjs`
  (pre-compiled ajv — stale validator rejects the new field).

### T2 — Render the what-remains line (SURF-01)
- `SkillWall.ts`: add `what_remains: string` to `SkillWallRow`; render a
  `.skill-wall__remains` line per row when `what_remains` is non-empty (hidden for
  Mastered, which already shows its cited proof). Plain text, no slop.
- `skill-wall.css`: a quiet muted style for `.skill-wall__remains` (small, `--text-tertiary`).

### T3 — Quiet Competent-fill cue + its pin (SURF-02)
- Confirm the fill animates once + settles (`transition: width …ease` already present;
  add a `prefers-reduced-motion` guard so motion-sensitive users get no animation —
  no-time-pressure spirit).
- **Pin** `tauri/ui/tests/learn/skill-tree-quiet-fill.spec.ts` (NEW): rendering/updating a
  Competent row dispatches NO `cohost-reaction` window event and creates NO `[role=dialog]`/
  `.modal`/celebration node; the fill is a width style only. Regression-locks "no slop".

### T4 — Dual-channel cue (color + SHAPE) + full keyboard-nav (SURF-04)
- `SkillWall.ts`: add a per-stage shape glyph `.skill-wall__glyph` (`aria-hidden`,
  decorative — e.g. ○ locked / ◑ competent / ★ mastered) so stage is NOT color-only.
  Make EVERY row keyboard-focusable (`tabindex=0`) with an `aria-label` summarizing
  name + stage + what-remains (browsable without hardware); only Mastered rows keep
  `role=button` + Enter/Space toggle (activatable proof).
- `skill-wall.css`: glyph per `data-stage`; `:focus-visible` ring on every row.
- **Pin** `tauri/ui/tests/learn/skill-tree-a11y.spec.ts` (NEW): each row has a non-color
  stage indicator (glyph + text label both present); every row is focusable (`tabindex=0`);
  no timed gate (no `disabled`+timer). Dual-cue distinguishable for color-blind.
- **Evolve** `tauri/ui/tests/learn/test_skill_wall.spec.ts`: the non-mastered row is now
  browsable (`tabindex=0`) but still NOT a button (`role` null) — update that one
  assertion; add a what-remains assertion. Called out in SUMMARY (learn-island, requirement-driven).

## Verification (Wave 2)
- `cd tauri/ui && npm run build && npm test` — `tsc --noEmit && vite build` clean; vitest
  green (new pins + evolved test + the rest of the ~886 suite unregressed).
- **Live-app gate** (`feedback_verify_live_app_not_just_tests`): confirm the payload rides
  `ipc.learn.progress_state` on `:8765` through `IpcRouterBus` and the wall paints
  what-remains + glyph (read `ui.log` `[vmx:ipc>]/[vmx:ipc<]` round-trip; or document the
  `?dev` mock path if the live sidecar is frozen). Mounted at `shell/app.ts:72`.

## Out of scope
The Mastered vocal hardware ear-pass + live-mastered hardware verify are parked KAAN-ACTION.
