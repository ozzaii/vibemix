---
phase: 96-course-3-play-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 96-01
files_modified:
  - src/vibemix/state/evidence_registry.py
  - src/vibemix/prompts/matrix.py
  - src/vibemix/agent/dj_cohost.py
  - tests/learn/test_cue_citation_schema_mirror.py
  - tests/state/test_evidence_registry.py
  - tests/prompts/test_matrix.py
autonomous: true
requirements:
  - CURR-3.07
tags: [cue-evidence-source, 4-site-mirror, atomic-landing, schema-mirror-lock, evidence-sources-11, invariant-2-bind]
must_haves:
  truths:
    - "EVIDENCE_SOURCES frozenset gains 'cue' as the 11th source identifier — count flips 10 → 11"
    - "_SOURCE_ALT regex alternation includes 'cue' so parse_citations() matches [cue:<anchor_id>] atoms"
    - "CITATION_GRAMMAR_BLOCK in prompts/matrix.py lists [cue:<anchor_id>] alongside the existing 10 forms"
    - "_build_citation_strip in agent/dj_cohost.py adds 'cue' to the chip-derivation allow-list, mirroring the v6 [recall:] and P93 [exemplar:] precedent (the sixth/seventh allow-list entry)"
    - "ALL FOUR SITES land in ONE COMMIT — atomic mirror lock prevents the silent-poisoning hole (a [cue:<id>] that joins EVIDENCE_SOURCES but NOT _SOURCE_ALT regex would never get parsed/stripped — fabricated cites ride through un-validated)"
    - "tests/learn/test_cue_citation_schema_mirror.py exists and pins all 4 sites independently (one test per site, mirrors test_exemplar_citation_schema_mirror.py from P93-05)"
    - "test_evidence_11_sources_constant_locked_GROUND02 in tests/state/test_evidence_registry.py renames to test_evidence_12_sources_constant_locked_GROUND02 (or evolves to assert the 11-source frozenset) — the GROUND-02 lock holds at 11 sources"
    - "[cue:<anchor_id>] is an EXISTENCE-ONLY source (like [track:], [key:], [recall:], [exemplar:]) — body is the anchor_id, NOT a timestamp; the citation linter's _TIME_KEYED_SOURCES set stays UNCHANGED (cue does NOT join it; cue joins the existence-only set by being IN EVIDENCE_SOURCES + ABSENT from _TIME_KEYED_SOURCES)"
    - "Existing P92+P93+P94+P95 invariant pins stay GREEN (test_runtime_invariants / test_no_new_ws_port / test_tutor_system_instruction_lock / test_scripts_are_fixtures / test_exemplar_citation_schema_mirror)"
  artifacts:
    - path: src/vibemix/state/evidence_registry.py
      provides: "'cue' added to EVIDENCE_SOURCES frozenset (site 1) AND _SOURCE_ALT regex (site 2)"
      contains: "cue"
    - path: src/vibemix/prompts/matrix.py
      provides: "'cue:<anchor_id>' form added to CITATION_GRAMMAR_BLOCK (site 3)"
      contains: "[cue:<anchor_id>]"
    - path: src/vibemix/agent/dj_cohost.py
      provides: "'cue' added to _build_citation_strip source allow-list (site 4)"
      contains: "cue"
    - path: tests/learn/test_cue_citation_schema_mirror.py
      provides: "4-site schema-mirror lock test for the [cue:] evidence source — mirrors P93's test_exemplar_citation_schema_mirror.py exactly"
      contains: "cue"
  key_links:
    - from: src/vibemix/state/evidence_registry.py:EVIDENCE_SOURCES
      to: src/vibemix/state/evidence_registry.py:_SOURCE_ALT
      via: "schema-mirror lock — both MUST contain 'cue' for parse_citations to match [cue:<id>] atoms"
      pattern: "cue"
    - from: src/vibemix/prompts/matrix.py:CITATION_GRAMMAR_BLOCK
      to: src/vibemix/state/evidence_registry.py:EVIDENCE_SOURCES
      via: "documented-in-grammar matches the runtime regex — Plan 96-03's [cue:<anchor_id>] emissions land in both"
      pattern: "[cue:<anchor_id>]"
    - from: src/vibemix/agent/dj_cohost.py:_build_citation_strip
      to: src/vibemix/state/evidence_registry.py:EVIDENCE_SOURCES
      via: "chip-derivation allow-list mirror — 'cue' chips ride the WS bus to the UI just like 'exemplar' and 'recall'"
      pattern: "cue"
    - from: tests/learn/test_cue_citation_schema_mirror.py
      to: "all four mirror sites"
      via: "one test per site — site 1 (EVIDENCE_SOURCES), site 2 (_SOURCE_ALT), site 3 (CITATION_GRAMMAR_BLOCK), site 4 (_build_citation_strip)"
      pattern: "test_site_"
---

<objective>
Land the `[cue:<anchor_id>]` evidence source via the locked 4-site atomic
mirror pattern (CONTEXT.md §Decisions §order-of-operations step 4 — same
shape as P93-05's `[exemplar:]` landing). This source is the
proof-of-grounding for Course 3 proactive count-ins: when the tutor narrates
"breakdown in 16 beats — get ready to bring in track 2," it MUST cite
`[cue:<anchor_id>]` against a CueAnchor / phrase-boundary observation
already in the EvidenceRegistry. Without it, the count-in cannot ride
through the citation linter — a fabricated `[cue:bogus]` strips the whole
turn.

After this plan lands, `EVIDENCE_SOURCES` count flips from 10 → 11. The
existing 10 sources are unchanged. The asymmetry with
`memory/ingest.py`'s 8-source extractor (CONTEXT precedent: `recall` and
`exemplar` are retrieval-time, never ingest-time) is preserved — `cue` is
also retrieval-time (a stored past reaction never cited a cue itself; the
cue is sampled from CueAnchor at narration time), so the ingest extractor
must NOT whitelist it.

The mirror is ATOMIC — all 4 sites land in ONE commit so the silent-
poisoning hole (one site updated, three stale → fabricated `[cue:bogus]`
rides through un-validated) never opens. P93-05's same-shape landing is the
direct precedent + the test pattern lifted verbatim.

Purpose: Plan 96-03's proactive tutor lens emits `[cue:<anchor_id>]`
citations on every forward-looking count-in. Without this 4-site mirror,
those citations parse as gibberish, the linter strips them, and the lens
can either emit raw count-ins (Invariant #2 violation — uncited claim) or
go silent. The mirror IS the evidence-source surface; Plan 96-03 cannot
land its lens wiring until this site mirror is in place.

Output: 3 file edits (evidence_registry.py + matrix.py + dj_cohost.py) +
1 new test file (test_cue_citation_schema_mirror.py) + 1 evolved test
(test_evidence_*_sources_constant_locked_GROUND02 renamed/updated for 11
sources) + 1 grammar-block test update in tests/prompts/test_matrix.py.
ZERO new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/96-course-3-play-mode/96-CONTEXT.md
@.planning/phases/93-exemplar-engine-evidence-source/93-02-PLAN.md
@src/vibemix/state/evidence_registry.py
@src/vibemix/prompts/matrix.py
@src/vibemix/agent/dj_cohost.py
@tests/learn/test_exemplar_citation_schema_mirror.py

<interfaces>
<!-- Lifted from the 3 mirror sites + the P93-05 test precedent. Executor
     does NOT need to spelunk; the contracts are below. -->

From `src/vibemix/state/evidence_registry.py:120-122` (Site 1 — the
frozenset; 10 entries today, 11 after this plan):

```python
EVIDENCE_SOURCES: frozenset[str] = frozenset(
    {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar"}
)
# → AFTER Plan 96-02:
# EVIDENCE_SOURCES: frozenset[str] = frozenset(
#     {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue"}
# )
```

From `src/vibemix/state/evidence_registry.py:155` (Site 2 — the regex
alternation):

```python
_SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key|recall|exemplar"
# → AFTER Plan 96-02:
# _SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key|recall|exemplar|cue"
```

From `src/vibemix/prompts/matrix.py:111-150` (Site 3 — CITATION_GRAMMAR_BLOCK
docstring — current form, P93-05 added the [exemplar:] line at 129):

```
Forms (each is a single citation; the linter accepts any of these):
  [ev:<TYPE>@<t>]     event citation, e.g. [ev:KICK_SWAP@45.2]
  ...
  [recall:<record_id>]  past-moment reference, e.g. [recall:20260520-2200:7]
  [exemplar:<track_id>]  band-exemplar track, e.g. [exemplar:library:Marlon Hoffstadt - Atlas]
# → AFTER Plan 96-02 add:
#  [cue:<anchor_id>]    cue/phrase anchor reference, e.g. [cue:phrase_boundary@45.2]
```

CITATION_GRAMMAR_BLOCK comment header at line 106-107:
```python
# The 10 source forms are kept in lock-step with EVIDENCE_SOURCES (Plan 18-01,
# + `key` added Phase 59 / DECK-03, + `recall` added Phase 65 / RECALL-01,
# + `exemplar` added Phase 93 / EXEMPLAR-05) via Test R cross-validation in
# tests/prompts/test_matrix.py.
# → AFTER Plan 96-02:
# The 11 source forms ... + `cue` added Phase 96 / CURR-3.07 ...
```

From `src/vibemix/agent/dj_cohost.py:176-260` (Site 4 — chip-derivation
allow-list inside `_build_citation_strip`). The current P93-05 form
already added `"exemplar"` to the allow-list; Plan 96-02 adds `"cue"` as
the seventh allow-list entry.

The `_build_citation_strip` already iterates `parse_citations(reaction_text)`
and looks each (source, body) pair up in the registry snapshot. Adding
`"cue"` to the source allow-list means a grounded `[cue:<anchor_id>]`
emission produces a chip in the UI strip (like `[exemplar:]` does); an
unregistered `[cue:bogus]` falls through the existing `if not timestamps:
continue` guard with no chip (same final state as the linter's whole-turn
strip — defense in depth).

From `tests/learn/test_exemplar_citation_schema_mirror.py` (P93-05 ship —
this is the EXACT shape to mirror; only the source name changes):

```python
def test_site_1_evidence_sources_frozenset_contains_exemplar() -> None:
    from vibemix.state.evidence_registry import EVIDENCE_SOURCES
    assert "exemplar" in EVIDENCE_SOURCES, (
        "EVIDENCE_SOURCES drift — `exemplar` missing from the frozenset. "
        "Phase 93 4-site mirror is broken; fabricated [exemplar:<id>] rides "
        "through un-validated."
    )

def test_site_2_source_alt_regex_includes_exemplar() -> None:
    from vibemix.state.evidence_registry import _SOURCE_ALT
    assert "exemplar" in _SOURCE_ALT.split("|"), (
        "_SOURCE_ALT regex drift — `exemplar` missing from the alternation."
    )
# ... etc for site 3 (matrix.py CITATION_GRAMMAR_BLOCK) and site 4 (dj_cohost.py)
```

From `tests/state/test_evidence_registry.py:210-229` (the existing GROUND-02
lock test — currently asserts 10 sources). After Plan 96-02 the assertion
flips to 11 sources. The constant test is the **source-of-truth** lock;
if any of the 4 sites drift, multiple tests fire red (the 4-site mirror
test + the GROUND-02 constant test + the grammar-coherence test
test_evidence_12_registry_grammar_coherence_GROUND01_GROUND02 which iterates
EVIDENCE_SOURCES to confirm every source is matchable AND writable).

From the orchestrator brief — body shape lock for `[cue:<anchor_id>]`:

```
[cue:<anchor_id>]    body = <anchor_id> (a CueAnchor identifier emitted
                     by state/refresh.py + CueAnchor; existence-only,
                     mirrors [exemplar:] / [recall:] / [key:] / [track:]).

Existing semantic precedent (CONTEXT.md §domain): the body is the cue
anchor identifier, NOT a timestamp. The Plan 96-03 narration emits
[cue:phrase_boundary@45.2] only if "phrase_boundary@45.2" was REGISTERED
in the EvidenceRegistry under source="cue" via state/refresh.py BEFORE
the LLM emission. A fabricated [cue:bogus] is uncitable-by-construction.
```

Anchor ID body grammar: `<non-bracket, non-whitespace, non-comma>` —
inherits from `_INNER_ATOM = rf"(?:{_SOURCE_ALT}):[^\s,\]]+"` unchanged.
Inner colons in the body are PERMITTED (mirrors `recall:20260520-2200:7`
and `exemplar:library:Marlon`); `parse_citations` splits on the FIRST `:`
so the inner anchor body survives intact.

`_TIME_KEYED_SOURCES` set in `citation_linter.py` — `cue` must STAY OUT
of this set (existence-only, like exemplar/recall/key/track). The Plan
96-03 wiring registers `("cue", anchor_id, t_session)` BEFORE the LLM
emits the cite — at narration time, the linter validates by snapshot
existence, not by timestamp comparison.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomic 4-site mirror commit — add 'cue' to EVIDENCE_SOURCES + _SOURCE_ALT + CITATION_GRAMMAR_BLOCK + _build_citation_strip</name>
  <files>src/vibemix/state/evidence_registry.py, src/vibemix/prompts/matrix.py, src/vibemix/agent/dj_cohost.py</files>
  <read_first>
    src/vibemix/state/evidence_registry.py:90-160 (the two site-1+site-2 surfaces — frozenset + _SOURCE_ALT regex with their P93-05 update precedent comments),
    src/vibemix/prompts/matrix.py:94-150 (the site-3 surface — CITATION_GRAMMAR_BLOCK with P93-05's [exemplar:] line shape),
    src/vibemix/agent/dj_cohost.py:176-280 (the site-4 surface — _build_citation_strip with P93-05's "exemplar" allow-list precedent),
    tests/learn/test_exemplar_citation_schema_mirror.py (the precedent the new test mirrors verbatim)
  </read_first>
  <behavior>
    Test 1: `"cue" in EVIDENCE_SOURCES` (site 1 — frozenset).
    Test 2: `"cue" in _SOURCE_ALT.split("|")` (site 2 — regex alternation).
    Test 3: `"[cue:<anchor_id>]" in CITATION_GRAMMAR_BLOCK` (site 3 — grammar docs).
    Test 4: `"cue" in <allow-list set inside _build_citation_strip>` (site 4 — chip derivation). Use AST grep or substring scan: assert the literal `"cue"` appears as an element in the source-allow-list condition (the existing pattern is `if source in {"ev", "mix", "midi", "key", "recall", "exemplar"}:` — Plan 96-02 adds `"cue"`).
    Test 5: `parse_citations("[cue:phrase_boundary@45.2]")` returns `[("cue", "phrase_boundary@45.2")]` — the regex + EBNF semantic round-trip.
    Test 6: EvidenceRegistry.write(source="cue", key="phrase_boundary@45.2", t_session=45.2) round-trips through .snapshot() — `snap["cue"]["phrase_boundary@45.2"] == (45.2,)`.
    Test 7: `EVIDENCE_CITATION_RE.fullmatch("[cue:phrase_boundary@45.2]")` matches (inherits from _INNER_ATOM via _SOURCE_ALT).
    Test 8: The GROUND-02 constant test now asserts 11 sources — `EVIDENCE_SOURCES == frozenset({"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue"})`.
  </behavior>
  <action>
    **CRITICAL: ATOMIC — all 4 sites must land in ONE git commit per CONTEXT.md
    §Decisions step 4 ("atomic 4-site mirror"). If any site lands without
    the others, the silent-poisoning hole opens. Stage all file edits, run
    pytest, then commit ALL FILES in one named-path commit.**

    **Site 1 — `src/vibemix/state/evidence_registry.py` EVIDENCE_SOURCES frozenset (line 120-122).**

    Add `"cue"` to the existing frozenset. Update the source-list docstring
    comment block at lines 89-119 mentioning Phase 96 / CURR-3.07:

    ```python
    #: The 11 EBNF source identifiers — 7 locked in 18-CONTEXT.md §EBNF Grammar
    #: plus ``key`` added Phase 59 (DECK-03), ``recall`` added Phase 65 (RECALL-01),
    #: ``exemplar`` added Phase 93 (EXEMPLAR-05), ``cue`` added Phase 96 (CURR-3.07).
    # ... existing source-list explanatory comments unchanged ...
    #: ``cue`` = CueAnchor / phrase-boundary reference, body ``<anchor_id>`` —
    #: Phase 96 (CURR-3.07). Existence-only retrieval-time source that makes a
    #: fabricated ``[cue:<id>]`` uncitable-by-construction: it joins the
    #: linter's EXISTENCE-ONLY set purely by being IN this frozenset and
    #: ABSENT from ``citation_linter.py::_TIME_KEYED_SOURCES`` (mirrors
    #: ``key``/``track``/``recall``/``exemplar``). state/refresh.py writes
    #: ``("cue", anchor_id, t_session)`` BEFORE the LLM emits the cite so
    #: unregistered ids strip the turn (Plan 96-03 wiring).
    #: SCHEMA-MIRROR: this frozenset is the source-of-truth — keep ``_SOURCE_ALT``,
    #: the EBNF docstring, ``prompts/matrix.py::CITATION_GRAMMAR_BLOCK`` and
    #: ``agent/dj_cohost.py::_build_citation_strip`` in lock-step.
    EVIDENCE_SOURCES: frozenset[str] = frozenset(
        {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue"}
    )
    ```

    **Site 2 — same file, _SOURCE_ALT regex (line 155) + EBNF docstring (line 163-169).**

    ```python
    # ... existing comment block including the recall + exemplar narrative ...
    # ``cue`` (Phase 96 / CURR-3.07) is the same silent-poisoning-hole pair
    # situation as ``recall``/``exemplar``: it MUST join this alternation in the
    # SAME commit it joins EVIDENCE_SOURCES — otherwise a fabricated
    # ``[cue:<id>]`` is never matched by parse_citations, never stripped, and
    # rides through un-validated.
    # ``_INNER_ATOM`` stays UNCHANGED — a ``cue:phrase_boundary@45.2`` body has
    # no whitespace/comma/bracket and the inner colon survives as the
    # anchor_id body (same posture as recall/key/exemplar).
    #
    # ASYMMETRY (intentional, do NOT "fix"): ``memory/ingest.py``'s copy of
    # this alternation stays at 8 sources — ``recall``, ``exemplar``, AND
    # ``cue`` are RETRIEVAL-time, never ingest-time (a stored past reaction
    # never cited cue itself; cue is sampled from CueAnchor at narration
    # time), so the ingest-time extractor must not whitelist any of them.
    _SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key|recall|exemplar|cue"
    ```

    Also update the EBNF docstring (around lines 163-169) — add a `cue-body`
    line:

    ```python
    #: Grammar (informal EBNF):
    #:
    #:   source := 'ev' | 'aud' | 'midi' | 'track' | 'screen' | 'mix' | 'tend'
    #:             | 'key' | 'recall' | 'exemplar' | 'cue'
    #:   ...
    #:   recall-body := <record_id>          # e.g. "20260520-2200:7"
    #:   exemplar-body := <track_id>         # e.g. "library:Marlon Hoffstadt - Atlas"
    #:   cue-body := <anchor_id>             # e.g. "phrase_boundary@45.2" or "drop@180.0"
    ```

    **Site 3 — `src/vibemix/prompts/matrix.py` CITATION_GRAMMAR_BLOCK + header comment.**

    Update lines 106-107 header (the source-count narrative):

    ```python
    # The 11 source forms are kept in lock-step with EVIDENCE_SOURCES (Plan 18-01,
    # + `key` added Phase 59 / DECK-03, + `recall` added Phase 65 / RECALL-01,
    # + `exemplar` added Phase 93 / EXEMPLAR-05, + `cue` added Phase 96 / CURR-3.07)
    # via Test R cross-validation in tests/prompts/test_matrix.py.
    ```

    Then add a new line inside CITATION_GRAMMAR_BLOCK (around line 129, right
    after `[exemplar:<track_id>]`):

    ```
    Forms (each is a single citation; the linter accepts any of these):
      ...
      [recall:<record_id>]  past-moment reference, e.g. [recall:20260520-2200:7]
      [exemplar:<track_id>]  band-exemplar track, e.g. [exemplar:library:Marlon Hoffstadt - Atlas]
      [cue:<anchor_id>]      cue/phrase anchor reference, e.g. [cue:phrase_boundary@45.2]
    ```

    The line slots BETWEEN the `[exemplar:]` line and the multi-citation
    paragraph that follows. Keep the alignment style (the existing forms
    use two-space gutter between the form bracket and the description; the
    new line follows the same width).

    **Site 4 — `src/vibemix/agent/dj_cohost.py` _build_citation_strip allow-list (around line 240-270).**

    Add `"cue"` to the source allow-list set. The current form (P93-05
    shipped) reads something like:

    ```python
    if source not in {"ev", "mix", "midi", "key", "recall", "exemplar"}:
        continue  # quiet sources don't surface as chips
    ```

    Update to include `"cue"`:

    ```python
    # Phase 96 (CURR-3.07) — ``cue`` added as the seventh allow-list entry
    # (mirrors v6 commit 0bfc8bd0 for [recall:] and P93-05 commit for [exemplar:]).
    # state/refresh.py registers ``("cue", anchor_id, t_session)`` BEFORE
    # the LLM emits the cite so a grounded ``[cue:<id>]`` resolves like any
    # other source and an unregistered (fabricated) ``[cue:<unregistered>]``
    # falls through the existing ``if not timestamps: continue`` guard with
    # strip == [] (same final state as the linter whole-turn strip — defense
    # in depth at two tiers).
    if source not in {"ev", "mix", "midi", "key", "recall", "exemplar", "cue"}:
        continue
    ```

    Read the actual current shape of the allow-list condition first — the
    pseudocode above is the contract; the exact form (set literal /
    explicit if-chain / etc.) is whatever P93-05 last shipped. Read
    dj_cohost.py:240-270 to confirm before editing.

    For the chip `verb` derivation: cue's verb should be a fixed `"cue"`
    label (mirroring `key`'s pattern at lines 215-222 — `verb` is a fixed
    label since the body is an anchor_id, not a clear "DJ action"). The
    detail of which anchor lives in `event_id` for the UI deep-link.

    **Land Atomic Test File (sub-step E below) + run pytest + commit ALL
    FOUR SITES in ONE commit named-path:**

    ```bash
    git add \
      src/vibemix/state/evidence_registry.py \
      src/vibemix/prompts/matrix.py \
      src/vibemix/agent/dj_cohost.py \
      tests/learn/test_cue_citation_schema_mirror.py \
      tests/state/test_evidence_registry.py \
      tests/prompts/test_matrix.py
    git commit -m "feat(state,prompts,agent): add [cue:] citation source via atomic 4-site mirror (P96 CURR-3.07)"
    ```

    NEVER `git add -A`. NEVER `git stash`. The mirror IS the atomicity
    contract — one missing site = silent poisoning hole.

    **Sub-step E — Land tests/learn/test_cue_citation_schema_mirror.py.**

    Create the new file mirroring the P93-05 test verbatim, swapping
    `exemplar` → `cue` and the message text:

    ```python
    # SPDX-License-Identifier: Apache-2.0
    """Phase 96 — 4-site schema-mirror lock for the `[cue:]` evidence source.

    Mirrors the v6 [recall:] and P93 [exemplar:] precedents EXACTLY. Each
    site MUST carry the ``cue`` token; if ANY site drifts, the grammar
    lock breaks and a fabricated [cue:<id>] can ride through un-validated
    (the silent-poisoning hole that the multi-site lock-step contract
    exists to prevent).

    REQ-ID: CURR-3.07 (citation source schema-mirror lock).
    Flipped GREEN by Plan 96-02 — the atomic 4-site mirror commit landed
    ``"cue"`` in ``EVIDENCE_SOURCES``, ``_SOURCE_ALT``,
    ``CITATION_GRAMMAR_BLOCK``, and ``dj_cohost._build_citation_strip``.
    """
    from __future__ import annotations
    import re
    from pathlib import Path

    _REPO = Path(__file__).resolve().parent.parent.parent


    def test_site_1_evidence_sources_frozenset_contains_cue() -> None:
        """Site 1: EVIDENCE_SOURCES frozenset @ state/evidence_registry.py."""
        from vibemix.state.evidence_registry import EVIDENCE_SOURCES
        assert "cue" in EVIDENCE_SOURCES, (
            "EVIDENCE_SOURCES drift — `cue` missing from the frozenset. "
            "Phase 96 4-site mirror is broken; fabricated [cue:<id>] rides "
            "through un-validated."
        )


    def test_site_2_source_alt_regex_includes_cue() -> None:
        """Site 2: _SOURCE_ALT regex alternation @ state/evidence_registry.py."""
        from vibemix.state.evidence_registry import _SOURCE_ALT
        assert "cue" in _SOURCE_ALT.split("|"), (
            "_SOURCE_ALT regex drift — `cue` missing from the alternation. "
            "parse_citations() will not match [cue:<id>] atoms; the linter "
            "never sees them."
        )


    def test_site_3_citation_grammar_block_documents_cue() -> None:
        """Site 3: CITATION_GRAMMAR_BLOCK @ prompts/matrix.py — documented
        grammar includes the [cue:<anchor_id>] form so Gemini learns it."""
        from vibemix.prompts.matrix import CITATION_GRAMMAR_BLOCK
        assert "[cue:<anchor_id>]" in CITATION_GRAMMAR_BLOCK, (
            "CITATION_GRAMMAR_BLOCK drift — the [cue:<anchor_id>] form is "
            "missing. Gemini's system instruction does not teach the source; "
            "Plan 96-03's proactive lens emits ungrounded count-ins."
        )


    def test_site_4_dj_cohost_citation_strip_allows_cue() -> None:
        """Site 4: agent/dj_cohost.py _build_citation_strip — `cue` joins
        the chip-derivation allow-list (the seventh entry after the v6
        addition of recall and P93's addition of exemplar)."""
        source_path = _REPO / "src" / "vibemix" / "agent" / "dj_cohost.py"
        text = source_path.read_text(encoding="utf-8")
        # The contract is: `cue` appears in the source-allow-list set or
        # alternation inside _build_citation_strip. Two acceptable shapes:
        #   1. set literal:  if source in {..., "cue", ...}:
        #   2. tuple/list:    if source in ("ev", ..., "cue"):
        # Substring match on the literal `"cue"` (quoted, comma-delimited
        # context) is sufficient — the test does NOT need to parse the AST.
        assert '"cue"' in text or "'cue'" in text, (
            "agent/dj_cohost.py drift — the `cue` token does not appear in "
            "source. Add to the _build_citation_strip allow-list set."
        )
        # Soft-guard: confirm we're not just matching a stray `cue` substring
        # in a comment that mentions the deferred source. Grep for the
        # specific allow-list context within +/- 80 chars of the literal.
        # Pattern: `cue` must appear within 80 chars of either "exemplar"
        # or "_build_citation_strip" (the surrounding context).
        idx = text.find('"cue"')
        if idx < 0:
            idx = text.find("'cue'")
        # The allow-list is inside _build_citation_strip — match the function
        # header (or 'exemplar' which is the immediate-preceding entry).
        window = text[max(0, idx - 200) : idx + 200]
        assert "exemplar" in window or "_build_citation_strip" in window, (
            "the `cue` token appears in source but NOT inside the "
            "_build_citation_strip allow-list context — possibly a "
            "comment-only reference. Wire to the actual allow-list."
        )


    def test_parse_citations_round_trips_cue_atom() -> None:
        """End-to-end parsing: a [cue:phrase_boundary@45.2] atom round-trips
        through parse_citations to ('cue', 'phrase_boundary@45.2')."""
        from vibemix.state.evidence_registry import parse_citations
        atoms = parse_citations("[cue:phrase_boundary@45.2]")
        assert atoms == [("cue", "phrase_boundary@45.2")], (
            f"parse_citations failed to round-trip [cue:] atom: {atoms!r}"
        )


    def test_evidence_registry_writes_and_reads_cue_source() -> None:
        """The registry accepts source='cue' for write + snapshot read."""
        from vibemix.state.evidence_registry import EvidenceRegistry
        reg = EvidenceRegistry()
        reg.write(source="cue", key="phrase_boundary@45.2", t_session=45.2)
        snap = reg.snapshot()
        assert snap["cue"]["phrase_boundary@45.2"] == (45.2,)


    def test_evidence_citation_re_matches_cue_atom() -> None:
        """The compiled regex matches the [cue:<anchor_id>] form."""
        from vibemix.state.evidence_registry import EVIDENCE_CITATION_RE
        assert EVIDENCE_CITATION_RE.fullmatch("[cue:phrase_boundary@45.2]") is not None
        assert EVIDENCE_CITATION_RE.fullmatch("[cue:drop@180.0]") is not None
        # Inner colon survives (mirrors recall/exemplar precedent)
        assert EVIDENCE_CITATION_RE.fullmatch("[cue:phrase:boundary:45.2]") is not None
    ```

    **Sub-step F — Evolve tests/state/test_evidence_registry.py GROUND-02 lock.**

    The existing test `test_evidence_11_sources_constant_locked_GROUND02`
    currently asserts the 10-source frozenset (P93-05 ship). Plan 96-02
    flips the count to 11. Two acceptable evolution paths:

    1. **Rename** the function to `test_evidence_12_sources_constant_locked_GROUND02`
       (the count-suffix has been used per-source for P93-05; the existing
       test is the "11th" assertion landed; the new "12th" assertion adds
       cue) and update the body to assert 11 sources.
    2. **Update in place** — keep the function name (it's a constant lock,
       not numbered by source count) and update the assertion body.

    **Choose option 2** — the existing test name's "11" is a test-number
    suffix (matching the file's test_evidence_NN naming convention), not
    a source-count suffix. The test function landed as the 11th test in
    the file (test_evidence_11_*), not because there were 11 sources at
    the time. Read the test file's overall structure (test_01..test_NN
    sequence) before deciding. If "_11_" is the file's sequential test
    number, RENAME-WITH-COUNT may be wrong; KEEP-NAME-AND-UPDATE-BODY is
    correct. If "_11_" was a count signal, RENAME-TO-_12_ is correct.

    Edit the assertion body:

    ```python
    def test_evidence_11_sources_constant_locked_GROUND02() -> None:
        """EVIDENCE_SOURCES is a frozenset of exactly the 11 source identifiers.

        Phase 59 (DECK-03) added ``key``. Phase 65 (RECALL-01) added ``recall``.
        Phase 93 (EXEMPLAR-05) added ``exemplar``. Phase 96 (CURR-3.07) adds
        ``cue`` — CueAnchor / phrase-boundary reference that grounds Course 3
        proactive count-ins.

        The frozenset is the schema-mirror source-of-truth — the regex
        alternation, EBNF docstring, prompt grammar block and citation-strip
        whitelist all carry ``cue`` in lock-step.
        """
        assert isinstance(EVIDENCE_SOURCES, frozenset)
        assert EVIDENCE_SOURCES == frozenset(
            {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue"}
        )
    ```

    The companion test `test_evidence_12_registry_grammar_coherence_GROUND01_GROUND02`
    iterates `EVIDENCE_SOURCES` to assert every source is matchable AND
    writable. It will gain a `cue` iteration AUTOMATICALLY — no manual
    update needed (verify by running it; it should still pass against the
    grown frozenset).

    **Sub-step G — Update tests/prompts/test_matrix.py.**

    Locate the test that asserts CITATION_GRAMMAR_BLOCK contains every
    source form (the file has a "Test R" cross-validation that iterates
    EVIDENCE_SOURCES and asserts each appears in the block). If such a
    test exists, it self-updates (loops over the frozenset). If a separate
    test asserts source-count = 10 or hard-codes the source names, evolve
    it like the GROUND-02 lock above. Read `tests/prompts/test_matrix.py`
    first; only edit if a hardcoded assertion needs updating.

    The most likely hit pattern (verbatim from the existing P93-05 ship):
    `assert "[exemplar:" in CITATION_GRAMMAR_BLOCK` — Plan 96-02 adds a
    sibling assertion: `assert "[cue:" in CITATION_GRAMMAR_BLOCK`.

    **Concurrent-session discipline**: this plan touches FIVE shared files
    (evidence_registry.py + matrix.py + dj_cohost.py + test_evidence_registry.py
    + test_matrix.py) + adds ONE new file (test_cue_citation_schema_mirror.py).
    The atomic 4-site mirror requires ONE commit covering all sites — see
    the commit recipe above. Never split into multiple commits; the
    silent-poisoning hole opens on every site that's out-of-step. Use
    named-path `git add` only (NEVER `git add -A`; NEVER `git stash`).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/learn/test_cue_citation_schema_mirror.py tests/state/test_evidence_registry.py tests/prompts/test_matrix.py tests/agent/test_dj_cohost_linter.py tests/coach/test_citation_linter.py tests/learn/test_exemplar_citation_schema_mirror.py tests/learn/test_runtime_invariants.py tests/learn/test_no_new_ws_port.py -x</automated>
  </verify>
  <done>Four sites carry "cue" in lock-step. EVIDENCE_SOURCES has 11 entries. _SOURCE_ALT regex includes "cue". CITATION_GRAMMAR_BLOCK documents `[cue:<anchor_id>]`. _build_citation_strip allow-list includes "cue". tests/learn/test_cue_citation_schema_mirror.py exists with 7 tests; all pass. GROUND-02 constant lock evolves to assert 11 sources; passes. P93-05 [exemplar:] schema-mirror lock STAYS GREEN (defense in depth — both source mirrors coexist). All P92+P93+P94+P95 invariant pins stay green. Single commit landed all four sites + tests atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → 4 mirror sites | each site is the structural surface enforcing schema lock; one drift = silent poisoning hole |
| EvidenceRegistry → CitationLinter | the registry's source-of-truth lock (EVIDENCE_SOURCES) must equal the regex alternation (_SOURCE_ALT); divergence breaks parse + strip semantics |
| LLM emission → citation strip | a fabricated [cue:bogus] must strip the whole turn via the linter's existence-only branch; non-registration is the safety net |
| memory/ingest.py → 8-source asymmetry | retrieval-time sources (recall + exemplar + cue) MUST NOT appear in the ingest extractor — preserves the architectural rule that stored reactions never cite retrieval sources themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-02-01 | Spoofing | fabricated [cue:bogus] rides through un-validated | mitigate | atomic 4-site mirror: site 1 (EVIDENCE_SOURCES) makes the source nameable; site 2 (_SOURCE_ALT) makes it parseable; site 3 (CITATION_GRAMMAR_BLOCK) makes Gemini emit it correctly; site 4 (_build_citation_strip) makes the UI chip-derive it. Drift on any ONE site = the linter strips Plan 96-03's grounded count-ins (false negative) OR lets a fabricated cite through (false positive). The 4-site mirror lock test pins all 4 simultaneously |
| T-96-02-02 | Tampering | one site lands without the others | mitigate | atomic single-commit landing; the mirror lock test fires red on the next pytest run if any site is out of step. Defense in depth: tests/state/test_evidence_registry.py::test_evidence_12_registry_grammar_coherence_GROUND01_GROUND02 iterates EVIDENCE_SOURCES and asserts EVIDENCE_CITATION_RE matches each — if site 2 lags, the new "cue" iteration fails |
| T-96-02-03 | Tampering | memory/ingest.py extractor whitelists cue | mitigate | the explanatory comment block in evidence_registry.py:152-154 already pins the ASYMMETRY contract (cue/recall/exemplar stay OUT of ingest); any future planner who tries to widen ingest must pass the explicit `# allow-tutor-prompt-without-coach`-style review at the comment site. ASSUMPTION (not enforced by test): no Plan 96 / 97 / 98 task touches memory/ingest.py — verify post-merge |
| T-96-02-04 | Information Disclosure | citation grammar block leaks `[cue:]` examples in repo | accept | Apache-2.0 OSS; no PII / no secrets; the grammar is the source-of-truth |
| T-96-02-05 | Denial of Service | malformed regex in _SOURCE_ALT crashes parse_citations | mitigate | tests/state/test_evidence_registry.py:test_evidence_12 confirms EVIDENCE_CITATION_RE compiles AND matches every EVIDENCE_SOURCES entry; a broken regex fails pytest at module load time. The Plan 96-02 edit is a single `|cue` append to a known-good alternation — minimal blast radius |
| T-96-02-06 | Repudiation | atomic commit signature unclear | accept | git history is the audit trail; one commit message naming all 4 sites is the recommended pattern; PR-time review confirms |
| T-96-02-SC | Tampering | npm/pip installs | accept | zero new dependencies — pure additive edit to existing modules + one new test file |
</threat_model>

<verification>
- `EVIDENCE_SOURCES` count flips 10 → 11; the new frozenset equals `{"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue"}`.
- `_SOURCE_ALT` regex equals `"ev|aud|midi|track|screen|mix|tend|key|recall|exemplar|cue"`.
- `CITATION_GRAMMAR_BLOCK` contains the literal `"[cue:<anchor_id>]"` form.
- `_build_citation_strip` source allow-list contains `"cue"`.
- `tests/learn/test_cue_citation_schema_mirror.py` exists with 7 tests; all pass.
- `tests/state/test_evidence_registry.py::test_evidence_11_sources_constant_locked_GROUND02` updated body — assertion holds at 11 sources.
- `tests/learn/test_exemplar_citation_schema_mirror.py` (P93-05 ship) STAYS GREEN — defense-in-depth (each source has its own mirror lock).
- `tests/state/test_evidence_registry.py::test_evidence_12_registry_grammar_coherence_GROUND01_GROUND02` STAYS GREEN — automatically iterates the larger EVIDENCE_SOURCES set.
- `tests/agent/test_dj_cohost_linter.py` STAYS GREEN — the new allow-list entry is additive; existing entries unchanged.
- `tests/coach/test_citation_linter.py` STAYS GREEN — _TIME_KEYED_SOURCES unchanged; cue joins the existence-only set by being IN EVIDENCE_SOURCES + ABSENT from _TIME_KEYED_SOURCES.
- All P92+P93+P94+P95 invariant pins STAY GREEN: test_runtime_invariants.py, test_no_new_ws_port.py, test_tutor_system_instruction_lock.py, test_scripts_are_fixtures.py, test_no_tutor_slop_blocklist.py.
- Plan 96-01's two AST gates + the runtime confidence gate STAY GREEN.
- Single atomic commit landed all 4 sites + the test files together.
</verification>

<success_criteria>
- `[cue:<anchor_id>]` evidence source landed via atomic 4-site mirror commit (CONTEXT.md §Decisions step 4).
- `EVIDENCE_SOURCES` count flips 10 → 11.
- Schema-mirror lock test (`test_cue_citation_schema_mirror.py`) makes future drift on any of the 4 sites fail CI red.
- Plan 96-03 can emit `[cue:<anchor_id>]` citations from the proactive tutor lens with confidence the linter will accept grounded cites and strip fabricated ones.
- Defense-in-depth: existing source mirror locks (P93-05 [exemplar:], P65-04 [recall:]) stay green — every source has its own dedicated lock test.
- Verifies live by exiting 0 on the test command in Task 1 verify.
</success_criteria>

<output>
Create `.planning/phases/96-course-3-play-mode/96-02-SUMMARY.md` when done.
</output>
