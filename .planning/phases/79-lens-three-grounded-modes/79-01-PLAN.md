---
phase: 79-lens-three-grounded-modes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/prompts/test_lens.py
  - tests/runtime/test_settings_apply.py
  - tests/library/test_curator_persona_seam.py
  - tests/agent/test_dj_cohost.py
autonomous: true
requirements: [LENS-01, LENS-02]

must_haves:
  truths:
    - "Every LENS-01/02 acceptance criterion has a failing xfail(strict=True) test that flips to a real pass when its implementation plan lands"
    - "The default (hype) lens cold path is pinned byte-identical to today's co-host default — a REAL-GREEN pin, not xfail"
    - "A silent early-pass of any scaffold becomes a HARD failure (strict xfail)"
  artifacts:
    - path: "tests/prompts/test_lens.py"
      provides: "LENS-01 + LENS-02 xfail-strict scaffolds + default-lens byte-identity real-green pin"
      contains: "build_lens_instruction"
    - path: "tests/runtime/test_settings_apply.py"
      provides: "_apply_lens + SettingsState lens snapshot xfail scaffolds (additive to existing file)"
      contains: "apply_lens"
    - path: "tests/library/test_curator_persona_seam.py"
      provides: "curator-reads-shared-lens xfail scaffold (additive)"
      contains: "lens"
    - path: "tests/agent/test_dj_cohost.py"
      provides: "lens→(mode,mood) cell resolution + cold-path byte-identity xfail scaffolds (additive)"
      contains: "lens"
  key_links:
    - from: "tests/prompts/test_lens.py"
      to: "src/vibemix/prompts/matrix.py::build_system_instruction"
      via: "default-lens byte-identity assertion against the untouched builder"
      pattern: "build_system_instruction"
    - from: "tests/prompts/test_lens.py"
      to: "src/vibemix/coach/citation_linter.py::CitationLinter.check"
      via: "three-lenses-through-the-gate strip-decision proof"
      pattern: "CitationLinter"
---

<objective>
Install the Nyquist safety net for Phase 79 BEFORE any implementation: every LENS-01/02 acceptance criterion gets a failing `xfail(strict=True)` test that flips to a real pass when its plan lands (02 = LENS-01, 03 = LENS-02). Plus the one REAL-GREEN pin that must KEEP passing through both implementation plans: the default-lens cold-path byte-identity.

Purpose: A silent early-pass (an xfail that quietly starts passing) becomes a HARD failure, so we cannot ship a half-wired lens. The byte-identity pin guards the v4 golden contract — the default lens prompt must equal today's co-host default byte-for-byte.
Output: 1 new test file (`tests/prompts/test_lens.py`) + additive scaffolds in 3 existing test files. No `src/` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-lens-three-grounded-modes/79-RESEARCH.md
@.planning/phases/79-lens-three-grounded-modes/79-PATTERNS.md
@.planning/phases/79-lens-three-grounded-modes/79-VALIDATION.md

<interfaces>
<!-- Contracts the scaffolds assert against. These are TARGET signatures the implementation plans (02/03) will create — the xfail scaffolds reference them so they fail until built. -->

To be ADDED by Plan 02 (src/vibemix/prompts/matrix.py):
  LENS_TO_MODE_MOOD: dict[str, tuple[str, str]]  # {"hype":("hype","hype-man"), "critique":("coach","coach"), "tutor":("coach","teacher")}
  def build_lens_instruction(lens: str = "hype", skill: str = "intermediate", **kw) -> str  # validate→delegate to build_system_instruction

To be ADDED by Plan 03 (src/vibemix/runtime/settings.py):
  _VALID_LENSES: frozenset[str]  # {"hype","critique","tutor"}
  async def _apply_lens(self, value) -> tuple[bool, str | None]  # mirrors _apply_skill; persists ConfigStore.extra["lens"]

EXISTING — DO NOT MODIFY (matrix.py, VERIFIED this session):
  def build_system_instruction(skill="intermediate", mode="hype", mood="hype-man", *, include_citation_grammar=True, include_listening_fallback=True, include_tag_dsl=True) -> str
  _CURATOR_LENS_TO_MOOD: dict[str, str]  # matrix.py:834 — {"tutor":"teacher","hype":"hype-man","critique":"coach"}
  def build_curator_instruction(lens: str = "tutor") -> str  # matrix.py:841

EXISTING — the lens-blind gate (coach/citation_linter.py, VERIFIED):
  class CitationLinter: def check(self, text, registry_snapshot, *, mode="live") -> LintResult  # citation_linter.py:94
  # LintResult.valid depends ONLY on EvidenceRegistry membership — never reads the prompt cell.

EXISTING — co-host cell resolution (agent/dj_cohost.py, VERIFIED):
  def _resolve_prompt_cell(mood: str | None = None) -> str  # dj_cohost.py:309 — reads env, delegates to build_system_instruction
  DEFAULT_SKILL_LEVEL="intermediate", DEFAULT_MODE="hype", DEFAULT_MOOD="hype-man"  # dj_cohost.py:115-117
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New tests/prompts/test_lens.py — LENS-01 scaffolds + LENS-02 shared-flow scaffold + the byte-identity REAL-GREEN pin</name>
  <files>tests/prompts/test_lens.py</files>
  <read_first>
    - tests/library/test_curator_persona_seam.py (the Phase-77 Wave-0 scaffold — exact xfail-strict-until-built shape; imports modules directly, NO network/genai.Client/API key)
    - tests/prompts/test_matrix.py (the v4 byte-identity goldens this pin sits beside: `test_q_v4_byte_identity_preserved_at_constant_level`, `test_prompt_01_hype_intermediate_byte_identical_to_persona`)
    - src/vibemix/coach/citation_linter.py:94 (the lens-blind `CitationLinter.check` signature for the three-lenses-through-the-gate proof)
    - 79-PATTERNS.md "Test Pattern Assignments → tests/prompts/test_lens.py" + "Lens-blind citation-grounding gate" (NOTE the ack-bank-retired caveat: assert "strips", behavior is strip-to-`<silence/>`)
  </read_first>
  <action>
    Create `tests/prompts/test_lens.py` with `from __future__ import annotations`. NO genai.Client, NO GEMINI_API_KEY — pure prompt-shape + strip-decision assertions (mirror test_curator_persona_seam.py's no-network discipline).

    Add these tests, each `@pytest.mark.xfail(strict=True, reason="LENS-0x — Plan 0N not landed")` EXCEPT the byte-identity pin which is REAL GREEN (no xfail):

    (1) `test_lens_map_covers_three_canonical_lenses` — xfail: import `LENS_TO_MODE_MOOD` from `vibemix.prompts.matrix`; assert keys == {"hype","critique","tutor"} and every value is a `(mode, mood)` tuple where mode ∈ {"hype","coach"} and mood ∈ MOOD_PERSONAS. (LENS-01 map.)
    (2) `test_lens_prompt_shape_per_lens` — xfail: `build_lens_instruction("hype")` contains hype-man persona markers; `build_lens_instruction("critique")` contains coach-cell markers; `build_lens_instruction("tutor")` contains teacher persona markers (grep the actual MOOD_PERSONAS["teacher"]/["coach"]/["hype-man"] text fragments — read matrix.py:51 to pick stable substrings). Asserts each lens yields a DISTINCT grounded prompt over the same builder. (LENS-01 prompt-shape.)
    (3) `test_unknown_lens_raises_value_error` — xfail: `build_lens_instruction("bogus")` raises ValueError (fail-loud, mirrors mood/skill guards). (LENS-01.)
    (4) `test_default_lens_byte_identical_to_cohost_default` — **REAL GREEN, NO xfail** is NOT possible here because build_lens_instruction does not exist yet; mark this one `xfail(strict=True)` too, asserting `build_lens_instruction("hype","intermediate") == build_system_instruction("intermediate","hype")`. (Flips green in Plan 02.) ADD a SEPARATE genuinely-real-green companion test `test_v4_golden_anchor_present` that imports `build_system_instruction` and asserts `build_system_instruction("intermediate","hype")` is non-empty and contains the hype-man persona — this REAL-GREEN test documents the byte-identity anchor exists today and must never regress.
    (5) `test_three_lenses_pass_the_same_gate` — xfail: build a system instruction under each of the 3 lenses (via `build_lens_instruction`); construct a fixed `EvidenceRegistry` snapshot with one known track id; run a fixed CITED reply (cites the known id) and a fixed UN-CITED reply (cites an absent id) through `parse_citations` + `CitationLinter.check(...)`; assert `LintResult.valid` is identical across all three lenses for the cited case (all valid) AND identical across all three for the un-cited case (all invalid → strips). The strip DECISION depends only on registry membership, never on the lens. Phrase the assertion/comment as "strips to silence" (ack-bank retired 2026-05-19). (LENS-01 + invariant #2.)
    (6) `test_shared_selection_flows_to_both_builders` — xfail: this one references the Plan-03 shared-read seam; set `ConfigStore.extra["lens"]="critique"` (build a ConfigStore in a tmp path), assert the co-host resolution AND the curator seam both pick up "critique". Since the seam wiring lands in Plan 03, keep the import path pointing at the TARGET (the shared-read helper Plan 03 adds); xfail-strict until then.

    Use the linter/registry imports from their real modules (`vibemix.state.evidence_registry`, `vibemix.coach.citation_linter`) — read those files for exact constructor + `parse_citations` signature before writing the gate test.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/prompts/test_lens.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/prompts/test_lens.py` collects with NO import-time error and NO genai.Client / GEMINI_API_KEY dependency.
    - Exactly one REAL-GREEN test (`test_v4_golden_anchor_present`) passes; all LENS-01/02 scaffolds report `xfailed` (strict).
    - `pytest -q tests/prompts/test_lens.py` exits 0 (xfails are not failures); no `xpassed`.
  </acceptance_criteria>
  <done>test_lens.py exists; 5 xfail-strict scaffolds + 1 real-green anchor; suite green (xfailed, not failed).</done>
</task>

<task type="auto">
  <name>Task 2: Additive scaffolds in the 3 existing test files (settings / curator-seam / dj_cohost)</name>
  <files>tests/runtime/test_settings_apply.py, tests/library/test_curator_persona_seam.py, tests/agent/test_dj_cohost.py</files>
  <read_first>
    - tests/runtime/test_settings_apply.py:315-341 (the `test_skill_happy_path` / `test_skill_invalid_value_rejected` / `test_skill_non_string_rejected` trio + the `_redirect_config_path` fixture — mirror EXACTLY for lens)
    - tests/library/test_curator_persona_seam.py (existing seam-tier tests + how it resets the module-global caches `_SYSTEM_INSTRUCTION_CACHE`/`_INTERACTIVE_SYSTEM_INSTRUCTION_CACHE`/`_SYSTEM_PROMPT_CACHE`)
    - tests/agent/test_dj_cohost.py (existing `_resolve_prompt_cell` tests — how they monkeypatch ENV_MODE/ENV_MOOD/ENV_SKILL_LEVEL)
    - 79-PATTERNS.md "Test Pattern Assignments" (the 3 additive blocks + analogs)
  </read_first>
  <action>
    Append (do NOT rewrite existing tests) the following xfail-strict scaffolds:

    `tests/runtime/test_settings_apply.py` — mirror the skill trio for lens:
      - `test_apply_lens_happy_path` (xfail): a SettingsApplier with a tmp ConfigStore; `await applier.apply("lens","critique")` returns `(True, None)` and persists `store.extra["lens"]=="critique"` to disk.
      - `test_apply_lens_invalid_value_rejected` (xfail): `apply("lens","bogus")` returns `(False, ...)`.
      - `test_apply_lens_non_string_rejected` (xfail): `apply("lens", 123)` returns `(False, ...)`.
      Use the SAME `_redirect_config_path` / construction fixture the skill tests use.

    `tests/library/test_curator_persona_seam.py` — ADD:
      - `test_curator_seam_reads_shared_lens` (xfail): set `ConfigStore.extra["lens"]="hype"` (tmp), reset the module-global caches, call `library.agent._system_instruction()` (and codex_curate seam), assert the built voice reflects the shared lens (hype-man persona markers) — NOT the hardcoded tutor/teacher.
      - `test_curator_seam_defaults_to_tutor_when_lens_unset` (xfail): with NO `extra["lens"]`, the seam still builds the tutor voice (per-surface default-when-unset = byte-identity preserved). Reset caches between cases.

    `tests/agent/test_dj_cohost.py` — ADD:
      - `test_resolve_prompt_cell_uses_shared_lens` (xfail): with a shared lens "critique" available, `_resolve_prompt_cell` resolves `(mode,mood)=("coach","coach")` → a coach-cell prompt.
      - `test_resolve_prompt_cell_cold_path_byte_identical` (xfail UNTIL Plan 03, then flips green): with NO shared lens set and default env, `_resolve_prompt_cell()` equals today's `build_system_instruction("intermediate","hype","hype-man")` byte-for-byte. This is the co-host cold-path guard.

    Every scaffold imports modules directly; NO genai.Client, NO API key.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/runtime/test_settings_apply.py tests/library/test_curator_persona_seam.py tests/agent/test_dj_cohost.py</automated>
  </verify>
  <acceptance_criteria>
    - All NEW lens scaffolds report `xfailed` (strict); ALL pre-existing tests in the three files still pass (no regression).
    - No `xpassed`, no import errors.
    - Full suite still green: `PYTHONPATH=src python3 -m pytest -q` shows the prior baseline pass count + the new xfails (no new failures).
  </acceptance_criteria>
  <done>3 files carry additive lens xfail scaffolds; existing tests untouched + green; full suite green with new xfails counted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → src modules | Tests import real modules read-only; no untrusted input, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | xfail scaffold silently passing early (a half-wired lens ships green) | mitigate | `strict=True` on every scaffold — an early pass becomes `xpassed` → HARD failure; the byte-identity anchor is a real-green pin. |
| T-79-01-02 | Information Disclosure | a test accidentally requiring a live key/network | accept | Tests are pure prompt-shape/strip-decision; no genai.Client, no GEMINI_API_KEY — mirrors the verified no-network curator-seam scaffold. |
| T-79-01-SC | Tampering | npm/pip/cargo installs | mitigate | None — this phase installs no packages (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/prompts/test_lens.py` — 1 real green + 5 xfailed.
- `PYTHONPATH=src python3 -m pytest -q` — full suite green: prior baseline passes + 1 new real green + 9 new xfailed, zero new failures, zero xpassed.
</verification>

<success_criteria>
- Every LENS-01/02 acceptance criterion has a failing xfail-strict test (will flip in 02/03).
- The default-lens / cold-path byte-identity guard exists (the real-green v4 anchor + the xfail byte-identity assertions that flip green when the builders land).
- No `src/` change; no API key; no new package.
</success_criteria>

<output>
Create `.planning/phases/79-lens-three-grounded-modes/79-01-SUMMARY.md` when done
</output>
