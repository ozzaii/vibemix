---
phase: 40-anti-slop-audio-port
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - docs/security/pgp-public-key.txt
  - tauri/src-tauri/tauri.conf.json5
  - .github/workflows/release.yml
  - KAAN-ACTION-LEGAL.md
  - tests/security/test_pgp_published.py
  - tests/tauri/test_updater_key_rotated.py
autonomous: true
requirements: [AUDIO-05, AUDIO-06]
tags: [security, pgp, tauri-updater, kaan-action, pre-stage, gate-tests]

must_haves:
  truths:
    - "Engineering scaffolding for AUDIO-05 (PGP) and AUDIO-06 (Tauri updater key) ships green so the only remaining work is Kaan's autonomous discharge step (key gen + secret upload + publish)."
    - "`docs/security/pgp-public-key.txt` exists as the slot file (placeholder armor block during scaffolding; real key armor on Kaan discharge)."
    - "`tauri.conf.json5` placeholder dev-key remains until Kaan rotates (the release.yml `placeholder-pubkey-gate` still passes for non-tag pushes; tagged releases require rotation)."
    - "`KAAN-ACTION-LEGAL.md` documents the exact two discharge sequences (PGP gen+publish; Tauri keypair gen + secret upload + commit)."
    - "Gate-style tests fail loudly when Kaan completes discharge but leaves placeholder text behind."
  artifacts:
    - path: "docs/security/pgp-public-key.txt"
      provides: "Slot file for the real PGP public key armor block. Initially contains a clearly-marked placeholder header explaining how to populate."
      contains: "PGP PUBLIC KEY BLOCK"
    - path: "SECURITY.md"
      provides: "Updated PGP section pointing at `docs/security/pgp-public-key.txt` (not the placeholder asc file); placeholder fingerprint table preserved until discharge."
      contains: "docs/security/pgp-public-key.txt"
    - path: "tauri/src-tauri/tauri.conf.json5"
      provides: "Comment block documenting the production-key rotation runbook (per RESEARCH Code Examples §Tauri ed25519 key rotation procedure). Placeholder dev key value kept until Kaan discharges."
      contains: "Plan 40-05 — production key rotation"
    - path: ".github/workflows/release.yml"
      provides: "Confirmed `placeholder-pubkey-gate` job still gates tagged releases; comment updated to reference Plan 40-05."
      contains: "placeholder-pubkey-gate"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "New entries for AUDIO-05 (PGP discharge runbook) and AUDIO-06 (Tauri key rotation runbook) with copy-pasteable commands."
      contains: "AUDIO-05"
    - path: "tests/security/test_pgp_published.py"
      provides: "Gate test — passes when the placeholder strings are present (engineering pre-stage state); fails when discharge is partial (real key but placeholder text not removed)."
      min_lines: 40
    - path: "tests/tauri/test_updater_key_rotated.py"
      provides: "Gate test — passes when the placeholder dev key is present (engineering pre-stage state); fails when discharge is partial (real key but old comment block left behind)."
      min_lines: 30
  key_links:
    - from: "SECURITY.md PGP section"
      to: "docs/security/pgp-public-key.txt"
      via: "explicit hyperlink with hkps://keys.openpgp.org lookup hint"
      pattern: "docs/security/pgp-public-key\\.txt"
    - from: ".github/workflows/release.yml placeholder-pubkey-gate"
      to: "tauri/src-tauri/tauri.conf.json5 pubkey value"
      via: "grep for `TAURI_UPDATER_PLACEHOLDER` sentinel on tagged-release push"
      pattern: "TAURI_UPDATER_PLACEHOLDER"
    - from: "KAAN-ACTION-LEGAL.md"
      to: "RESEARCH Code Examples (AUDIO-05, AUDIO-06)"
      via: "embedded runbook with exact gpg + tauri signer commands"
      pattern: "gpg --quick-gen-key\\|tauri-apps/cli signer generate"
---

<objective>
Pre-stage the two AUDIO-05 + AUDIO-06 KAAN-ACTION items so all engineering scaffolding ships green and Kaan's autonomous discharge is the only remaining step (per `gsd-autonomous fully` mode + CONTEXT.md §Pre-stage KAAN-ACTION Items).

AUDIO-05 (PGP key for `security@bravoh.com`): Today `SECURITY.md` points at `KAAN-PGP-PLACEHOLDER.asc` (a placeholder file at repo root) with `PLACEHOLDER-FINGERPRINT-NOT-REAL` fingerprint. This plan creates the slot file `docs/security/pgp-public-key.txt`, updates `SECURITY.md` to reference it, and ships a `KAAN-ACTION-LEGAL.md` entry with the discharge runbook. The placeholder remains until Kaan runs the runbook.

AUDIO-06 (Tauri ed25519 updater key): Today `tauri.conf.json5:153` contains the 2026-05-13 dev key. The `release.yml::placeholder-pubkey-gate` job already gates tagged releases against the `TAURI_UPDATER_PLACEHOLDER` sentinel. This plan adds a `KAAN-ACTION-LEGAL.md` entry with the rotation runbook (per RESEARCH Code Examples §AUDIO-06).

**Hallucination class closed:** None directly — this plan is about pre-stage scaffolding, not Gemini grounding. But it ships the green-state required for AUDIO-05/06 traceability and for `gsd-autonomous fully` to mark them dischargeable.

Purpose: Pre-stage scaffolding for the two no-external-clock KAAN-ACTION items so Plan 45 (External Discharge) is unblocked.

Output:
- `docs/security/pgp-public-key.txt` slot file (placeholder until Kaan publishes).
- `SECURITY.md` updated to reference the slot.
- `KAAN-ACTION-LEGAL.md` runbook entries.
- 2 gate-style tests pinning the pre-stage state AND providing a clear failure mode when Kaan discharges partially.
- `tauri.conf.json5` annotated with the rotation runbook comment (no value change until Kaan rotates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-anti-slop-audio-port/40-CONTEXT.md
@.planning/phases/40-anti-slop-audio-port/40-RESEARCH.md
@CLAUDE.md

@SECURITY.md
@tauri/src-tauri/tauri.conf.json5
@.github/workflows/release.yml
@KAAN-ACTION-LEGAL.md

<interfaces>
Current PGP state (`SECURITY.md:26-39`):
- Line 28-29: references `KAAN-PGP-PLACEHOLDER.asc` (placeholder file at repo root).
- Line 31-35: explanatory note "This is a placeholder key. Kaan Özkan generates and signs the real key on first vulnerability report..."
- Line 37-39: table with `PLACEHOLDER-FINGERPRINT-NOT-REAL` fingerprint and `placeholder — Kaan-action` status.

Target post-Plan-40-05 state (ENGINEERING phase, before Kaan discharge):
- Reference path updated to `docs/security/pgp-public-key.txt` (new file).
- The explanatory note carries through verbatim — engineering still has the placeholder; Kaan's discharge replaces text + fingerprint atomically.
- `KAAN-ACTION-LEGAL.md` entry under §AUDIO-05 has the exact gpg commands from RESEARCH Code Examples.

Target post-Kaan-discharge state (Kaan completes runbook later, NOT in this plan):
- `docs/security/pgp-public-key.txt` contains real ASCII-armored ed25519 PGP public key.
- `SECURITY.md` table shows real fingerprint, status "published".
- `KAAN-PGP-PLACEHOLDER.asc` removed (`git rm`).
- `KAAN-ACTION-LEGAL.md` entry marked discharged.

Current Tauri updater state (`tauri/src-tauri/tauri.conf.json5:120-160`):
- Line 153: pubkey is the 2026-05-13 dev key (base64-encoded minisign public key starting `dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDk0QThGNkNFNDJFNjQ4N0Q...`).
- Comment block at lines 136-152 references "Phase 18-04 placeholder" generation runbook.
- `release.yml::placeholder-pubkey-gate` job (line 224-241) gates tagged releases against the `TAURI_UPDATER_PLACEHOLDER` sentinel string in the pubkey value. The 2026-05-13 dev key does NOT contain that sentinel, so the gate currently passes for non-tag pushes; tagged releases require the sentinel removed AND real key in place.

Target post-Plan-40-05 state (ENGINEERING phase):
- `tauri.conf.json5` comment block updated to reference the Plan 40-05 rotation runbook (no pubkey value change).
- `KAAN-ACTION-LEGAL.md` entry under §AUDIO-06 has the exact `tauri signer generate` + `gh secret set TAURI_UPDATER_PRIVATE_KEY` commands.
- The dev key sentinel `94A8F6CE42E6487D` (the dev key's fingerprint embedded in its base64-encoded comment) is treated as the placeholder identifier; the gate-test (Task 2) recognizes it.

`release.yml::placeholder-pubkey-gate` (already shipped in Phase 18-05): line 236 greps for `TAURI_UPDATER_PLACEHOLDER\|dW50cnVzdGVkIGNvbW1lbnQ6IFRBVVJJX1VQREFURVJfUExBQ0VIT0xERVI=` — the literal "TAURI_UPDATER_PLACEHOLDER" sentinel string. The 2026-05-13 dev key does NOT contain this sentinel; the gate is currently inert for the dev key (it only catches the original Phase 18 placeholder string). This is FINE for the engineering pre-stage but means the CI gate alone won't catch "dev key left in place at tag time" — the gate-test in Task 2 fills that hole.

`KAAN-ACTION-LEGAL.md` schema (load from existing file):
- Categorized list of pending discharges with `Item / Status / Discharge runbook` columns or sections.
- Plan 40-05 adds entries under existing categories: `post_approval_mechanical` (SEC-06-PGP → renamed AUDIO-05) and `post_approval_mechanical` (TAURI-UPDATER-KEY → renamed AUDIO-06). The Phase 39 STATE.md already lists these as deferred — this plan upgrades them with concrete runbooks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PGP slot file + SECURITY.md update + KAAN-ACTION-LEGAL runbook + gate test</name>
  <files>docs/security/pgp-public-key.txt, SECURITY.md, KAAN-ACTION-LEGAL.md, tests/security/test_pgp_published.py</files>
  <behavior>
    - `docs/security/pgp-public-key.txt` exists with a clear placeholder body that explains the slot:
      ```
      -----BEGIN PGP PUBLIC KEY BLOCK-----
      PLACEHOLDER-VIBEMIX-AUDIO-05-PGP-NOT-YET-GENERATED

      This is a placeholder. The real ed25519 PGP public key for
      security@bravoh.com is generated and published via the runbook
      in KAAN-ACTION-LEGAL.md §AUDIO-05. Until then, plaintext email
      to security@bravoh.com is accepted (mark sensitive details
      [ENCRYPT ON REPLY]).
      -----END PGP PUBLIC KEY BLOCK-----
      ```
    - `SECURITY.md` PGP section (lines 26-39) updated:
      - Line 28-29 text changes from `[KAAN-PGP-PLACEHOLDER.asc](./KAAN-PGP-PLACEHOLDER.asc)` to `[docs/security/pgp-public-key.txt](./docs/security/pgp-public-key.txt)`.
      - Line 31-35 explanatory note preserved (engineering is still pre-discharge).
      - Line 37-39 table preserved exactly (`PLACEHOLDER-FINGERPRINT-NOT-REAL` stays).
      - Add a one-line lookup hint after the table: `> After discharge, the key is also searchable via \`gpg --keyserver hkps://keys.openpgp.org --search security@bravoh.com\`.`
    - `KAAN-ACTION-LEGAL.md` gets a new section `## AUDIO-05 — Real PGP key generation + publish (post-Plan 40-05 discharge)` containing:
      1. The 5-step runbook from RESEARCH Code Examples §AUDIO-05 (`gpg --quick-gen-key ed25519`, fingerprint extraction, armor export, `gpg --send-keys --keyserver hkps://keys.openpgp.org`, SECURITY.md fingerprint update + `git rm KAAN-PGP-PLACEHOLDER.asc`).
      2. A clearly-marked "Discharge checklist" listing what changes when Kaan completes the runbook: real armor in `docs/security/pgp-public-key.txt`, real fingerprint in SECURITY.md table, `KAAN-PGP-PLACEHOLDER.asc` removed, placeholder explanatory paragraph removed.
    - `tests/security/test_pgp_published.py` is a GATE test with TWO modes via a sentinel check:
      - **Pre-discharge mode** (current state): if `docs/security/pgp-public-key.txt` contains `PLACEHOLDER-VIBEMIX-AUDIO-05-PGP-NOT-YET-GENERATED`, the test passes (engineering pre-stage is the expected state).
      - **Post-discharge mode** (after Kaan runs the runbook): the placeholder string is absent. The test then validates: (a) the file parses as a valid OpenPGP block (starts with `-----BEGIN PGP PUBLIC KEY BLOCK-----`, ends with `-----END PGP PUBLIC KEY BLOCK-----`, has at least 200 chars of base64 body), (b) `SECURITY.md` does NOT contain `PLACEHOLDER-FINGERPRINT-NOT-REAL`, (c) `SECURITY.md` does NOT reference `KAAN-PGP-PLACEHOLDER.asc`, (d) `KAAN-PGP-PLACEHOLDER.asc` file no longer exists at repo root.
      - **Partial-discharge failure mode**: if `SECURITY.md` lacks `PLACEHOLDER-FINGERPRINT-NOT-REAL` but `docs/security/pgp-public-key.txt` still has the placeholder string (or vice versa), the test fails with a clear message — Kaan finished half the runbook.
  </behavior>
  <action>
    Create `docs/security/pgp-public-key.txt` with the placeholder body specified above. Ensure the file is plain ASCII (no BOM, no trailing whitespace beyond a single final newline).

    Modify `SECURITY.md`:
    - Change the link target at line 29 from `KAAN-PGP-PLACEHOLDER.asc` to `docs/security/pgp-public-key.txt`. Update the surrounding sentence wording minimally to preserve clarity.
    - Update the table at line 37-39: change the first column from `` `KAAN-PGP-PLACEHOLDER.asc` `` to `` `docs/security/pgp-public-key.txt` `` (the fingerprint and status columns are preserved verbatim).
    - Add the hkps lookup hint line after the table.

    Append a new section to `KAAN-ACTION-LEGAL.md` titled `## AUDIO-05 — Real PGP key generation + publish` with the runbook + discharge checklist. Use the exact gpg commands from RESEARCH Code Examples §AUDIO-05.

    Create `tests/security/test_pgp_published.py` with the dual-mode gate logic. Implement via a simple read of `docs/security/pgp-public-key.txt` at test-start; if the placeholder sentinel is present → assert pre-discharge invariants (file exists, SECURITY.md still references it); else → assert post-discharge invariants (real PGP block format, no placeholder strings anywhere, KAAN-PGP-PLACEHOLDER.asc absent). Include a clear `assert` message for the partial-discharge failure mode.

    Reference: RESEARCH Code Examples §AUDIO-05.
  </action>
  <verify>
    <automated>python -m pytest tests/security/test_pgp_published.py -x -q</automated>
  </verify>
  <done>
    - `docs/security/pgp-public-key.txt` exists with placeholder body.
    - `SECURITY.md` references `docs/security/pgp-public-key.txt` (grep confirms 1+ match) and no longer references `KAAN-PGP-PLACEHOLDER.asc` in the live PGP section text. (Note: `KAAN-PGP-PLACEHOLDER.asc` may still EXIST at repo root — `git rm` is part of Kaan's discharge runbook, not this plan.)
    - `KAAN-ACTION-LEGAL.md` has a new `## AUDIO-05` section with the 5-step runbook.
    - `tests/security/test_pgp_published.py` passes in pre-discharge mode (3+ assertions on placeholder presence + file structure).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tauri updater key rotation scaffolding + KAAN-ACTION-LEGAL runbook + gate test</name>
  <files>tauri/src-tauri/tauri.conf.json5, KAAN-ACTION-LEGAL.md, tests/tauri/__init__.py, tests/tauri/test_updater_key_rotated.py</files>
  <behavior>
    - `tauri/src-tauri/tauri.conf.json5` comment block at lines 136-152 updated to mention Plan 40-05 production-key rotation. The pubkey value at line 153 is UNCHANGED (the 2026-05-13 dev key stays until Kaan rotates). Specifically, add a new comment near the existing block:
      ```jsonc
      // Plan 40-05 — production key rotation
      // The pubkey above is the 2026-05-13 dev key (Phase 18-04). Production
      // rotation runbook lives in KAAN-ACTION-LEGAL.md §AUDIO-06. After
      // discharge: this line is replaced with the prod key armor and the
      // `gh secret set TAURI_UPDATER_PRIVATE_KEY` step uploads the private
      // half. The placeholder-pubkey-gate in release.yml fires for tagged
      // pushes ONLY (the dev key passes the gate today since it lacks the
      // TAURI_UPDATER_PLACEHOLDER sentinel — tests/tauri/test_updater_key_rotated.py
      // adds the dev-key-still-present gate for tagged releases).
      ```
    - `KAAN-ACTION-LEGAL.md` gets a new section `## AUDIO-06 — Tauri ed25519 updater key rotation` containing:
      1. The 4-step runbook from RESEARCH Code Examples §AUDIO-06 (`npx @tauri-apps/cli signer generate`, paste pubkey into tauri.conf.json5, base64-encode private + `gh secret set TAURI_UPDATER_PRIVATE_KEY`, rehearse via workflow_dispatch).
      2. Discharge checklist: dev-key fingerprint `94A8F6CE42E6487D` (encoded in the dev-key base64 comment) no longer appears in `tauri.conf.json5`; `gh secret set` output confirms upload; `release.yml` rehearsal run completes with signature step green.
    - `tests/tauri/` is created (new dir per RESEARCH §Wave 0 Gaps). Add `__init__.py`.
    - `tests/tauri/test_updater_key_rotated.py` is a GATE test with TWO modes:
      - **Pre-discharge mode** (current state): if `tauri.conf.json5` contains the dev-key fingerprint sentinel `94A8F6CE42E6487D` (substring of the dev-key base64 body), the test passes (engineering pre-stage is the expected state).
      - **Post-discharge mode**: the dev-key sentinel is absent. The test then validates: (a) `pubkey` field in `tauri.conf.json5` is non-empty and base64-decodes successfully (no JSON5 parser needed — regex extraction is fine), (b) the pubkey starts with `dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6` (the minisign comment prefix in base64), (c) the pubkey is NOT the literal `TAURI_UPDATER_PLACEHOLDER` Phase-18 sentinel.
    - The gate test also enforces the CRITICAL invariant from RESEARCH Pitfall 6: there's no regression on the `placeholder-pubkey-gate` CI gate semantics. Specifically: grep `release.yml` for the `placeholder-pubkey-gate` job name; assert it still exists. This guards against accidental removal during the rotation.
  </behavior>
  <action>
    Modify `tauri/src-tauri/tauri.conf.json5`: insert the new `// Plan 40-05 — production key rotation` comment block immediately above the existing pubkey line (~line 152). Do NOT modify the pubkey value itself — that's Kaan's discharge step.

    Append `## AUDIO-06 — Tauri ed25519 updater key rotation` section to `KAAN-ACTION-LEGAL.md` with the 4-step runbook from RESEARCH Code Examples + discharge checklist.

    Create `tests/tauri/__init__.py` (empty).

    Create `tests/tauri/test_updater_key_rotated.py` with the dual-mode gate. Implement:
    1. Open `tauri/src-tauri/tauri.conf.json5` as text; extract the pubkey value via regex `r'"pubkey"\s*:\s*"([^"]+)"'`.
    2. Decode the pubkey via `base64.b64decode(pubkey)`; the first ~50 bytes are the human-readable minisign comment.
    3. Sentinel detection: if the dev-key fingerprint `94A8F6CE42E6487D` appears in the decoded comment string → pre-discharge mode → assert: (a) pubkey decodes successfully, (b) file path exists, (c) `release.yml` still contains `placeholder-pubkey-gate` job name.
    4. Post-discharge mode (sentinel absent): assert: (a) pubkey is not empty, not `TAURI_UPDATER_PLACEHOLDER`, (b) decodes successfully, (c) decoded comment starts with `untrusted comment: minisign public key:` (the conventional minisign header — line `dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6` in base64), (d) `release.yml::placeholder-pubkey-gate` job still exists.

    Reference: RESEARCH Code Examples §AUDIO-06 + Pitfall 6.

    Do NOT touch `.github/workflows/release.yml::placeholder-pubkey-gate` itself — the gate already works correctly per RESEARCH §State of the Art "Pitfall 6 prevention" — only update its comment reference if needed (currently fine).
  </action>
  <verify>
    <automated>python -m pytest tests/tauri/test_updater_key_rotated.py tests/security/test_pgp_published.py -x -q</automated>
  </verify>
  <done>
    - `tests/tauri/` dir exists with `__init__.py` and `test_updater_key_rotated.py`.
    - Gate test passes in pre-discharge mode (4+ assertions per branch).
    - `tauri.conf.json5` contains the new Plan 40-05 comment block (grep confirms: `grep -c "Plan 40-05" tauri/src-tauri/tauri.conf.json5` ≥ 1).
    - `KAAN-ACTION-LEGAL.md` has a new `## AUDIO-06` section.
    - The dev-key fingerprint `94A8F6CE42E6487D` STILL appears in `tauri.conf.json5` (engineering pre-stage state — confirmed by `grep -c "94A8F6CE42E6487D" tauri/src-tauri/tauri.conf.json5` ≥ 1 once the dev key is decoded; equivalently grep for the literal base64 substring).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaan local gpg keyring → repo PGP armor | Trusted (Kaan's local machine generates ed25519 keypair, only public half published) |
| Kaan local tauri signer → repo + GH secret | Trusted (Kaan's local machine generates keypair; private half via `gh secret set`, never on disk in repo) |
| Pre-discharge placeholder state → post-discharge real-key state | Gate tests fail-loud on partial-discharge (key in but text not removed, or vice versa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-05-01 | Tampering | Half-completed PGP discharge | mitigate | `test_pgp_published.py` partial-discharge failure mode — if SECURITY.md has real fingerprint but `pgp-public-key.txt` still has placeholder (or vice versa), test fails with clear message. |
| T-40-05-02 | Tampering | Half-completed Tauri key rotation | mitigate | `test_updater_key_rotated.py` partial-discharge failure mode — if pubkey changed but dev-key fingerprint still present (string-replace error), test fails. Also asserts `release.yml::placeholder-pubkey-gate` job still exists (Pitfall 6 regression guard). |
| T-40-05-03 | Information Disclosure | Private key leak in tauri.conf.json5 | mitigate | RESEARCH §Security Domain: only pubkey lives in `tauri.conf.json5`; private half goes through `gh secret set TAURI_UPDATER_PRIVATE_KEY`. Runbook is explicit about this; gate test confirms only base64-decodable pubkey present. |
| T-40-05-04 | Information Disclosure | Private PGP key in repo | mitigate | RESEARCH §Security Domain: only ASCII-armored PUBLIC key in `docs/security/pgp-public-key.txt`. Test asserts file starts with `-----BEGIN PGP PUBLIC KEY BLOCK-----` (not `-----BEGIN PGP PRIVATE KEY BLOCK-----`). |
| T-40-05-05 | Repudiation | Key rotation history | mitigate | Audit-trail: `KAAN-ACTION-LEGAL.md §AUDIO-05` and `§AUDIO-06` runbooks include "discharged on YYYY-MM-DD by Kaan" checklist line. Git blame on `tauri.conf.json5` + `docs/security/pgp-public-key.txt` completes the trail. |
</threat_model>

<verification>
- Gate tests: `python -m pytest tests/security/test_pgp_published.py tests/tauri/test_updater_key_rotated.py -x -q`.
- Cross-file consistency: `grep -c "docs/security/pgp-public-key.txt" SECURITY.md` ≥ 1.
- Runbook presence: `grep -c "AUDIO-05\|AUDIO-06" KAAN-ACTION-LEGAL.md` ≥ 2.
- CI gate preserved: `grep -c "placeholder-pubkey-gate" .github/workflows/release.yml` ≥ 2 (job name + comment reference).
- POC immutability: `git status cohost_v4*.py cohost.py cohost_v2.py cohost_lk.py mascot.html` clean.
- No tauri secret leak: `grep -v "^#\|^//" tauri/src-tauri/tauri.conf.json5 | grep -c "PRIVATE KEY"` returns 0 (defense-in-depth — no private key should ever land here).
</verification>

<success_criteria>
1. `docs/security/pgp-public-key.txt` exists as a slot file with a clearly-marked placeholder body.
2. `SECURITY.md` references the new path; placeholder fingerprint preserved until Kaan's discharge.
3. `tauri.conf.json5` has the new Plan 40-05 comment block; dev pubkey value unchanged.
4. `KAAN-ACTION-LEGAL.md` has `## AUDIO-05` and `## AUDIO-06` sections with copy-pasteable runbooks.
5. Two gate tests pass in pre-discharge mode AND have clear failure modes for partial-discharge AND validate full-discharge structure when Kaan completes the runbook later.
6. `placeholder-pubkey-gate` CI job preserved (no Pitfall 6 regression).
7. AUDIO-05 + AUDIO-06 requirements pre-staged (engineering green; Kaan-action discharge documented).
</success_criteria>

<output>
After completion, create `.planning/phases/40-anti-slop-audio-port/40-05-SUMMARY.md` documenting:
- Files created/modified (slot file, SECURITY.md edits, KAAN-ACTION-LEGAL.md sections, gate tests).
- The pre-discharge / post-discharge invariants encoded in each gate test.
- Explicit note: "Kaan-action: run the runbooks in KAAN-ACTION-LEGAL.md §AUDIO-05 + §AUDIO-06 to discharge. The gate tests flip from pre-discharge to post-discharge mode automatically — no code change needed when Kaan completes."
</output>
