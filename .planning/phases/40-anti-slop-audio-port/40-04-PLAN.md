---
phase: 40-anti-slop-audio-port
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/vibemix/audio/constants.py
  - tests/audio/test_constants.py
  - tests/state/test_event_detector.py
  - tests/audio/test_phase17_constants.py
  - scripts/eval/replay_harness.py
  - tests/eval/test_replay_harness_cooldowns.py
autonomous: true
requirements: [AUDIO-03]
tags: [cooldowns, event-detector, replay-harness, anti-slop, v4-baseline]

must_haves:
  truths:
    - "`MIN_EVENT_GAP_PER_TYPE` dict matches v4 chat-tested 2026-05-11 baseline: TRACK_CHANGE 5, PHASE 10, LAYER_ARRIVAL 10, MIX_MOVE 14, HEARTBEAT 45."
    - "`HEARTBEAT_SEC` module-level constant kept in sync (`HEARTBEAT_SEC = 45.0`)."
    - "`scripts/eval/replay_harness.py --print-cooldowns` emits measured inter-event gaps per type with delta vs locked value; warns when delta > ±1s."
    - "Existing tests that pinned the OLD values (18/16/20/70/6) are updated to the NEW values — no compat shims, no preserved-old-test-as-warning, the change IS the point (per RESEARCH Pitfall 3)."
  artifacts:
    - path: "src/vibemix/audio/constants.py"
      provides: "Re-tuned `MIN_EVENT_GAP_PER_TYPE` dict and `HEARTBEAT_SEC` constant with audit-trail comments referencing v4 chat-test 2026-05-11."
      contains: "PHASE.*10\\.0"
    - path: "scripts/eval/replay_harness.py"
      provides: "New `--print-cooldowns` flag that drives the existing replay loop and prints per-type measured inter-event median gaps with delta-from-locked report."
      contains: "--print-cooldowns"
    - path: "tests/eval/test_replay_harness_cooldowns.py"
      provides: "Test exercising the `--print-cooldowns` mode on a synthetic-event fixture session; asserts median gap calculation matches expected values."
      min_lines: 50
  key_links:
    - from: "src/vibemix/state/event_detector.py::_cooldown_ok"
      to: "src/vibemix/audio/constants.py::MIN_EVENT_GAP_PER_TYPE"
      via: "module-level dict lookup, no scattered constants"
      pattern: "MIN_EVENT_GAP_PER_TYPE\\["
    - from: "scripts/eval/replay_harness.py"
      to: "EventDetector firing records"
      via: "in-loop accumulator: `measured_gaps[ev.type].append(now - last_per_type_at.get(ev.type, now))`"
      pattern: "measured_gaps\\["
---

<objective>
Reduce `MIN_EVENT_GAP_PER_TYPE` cooldowns from v3-era ship-cut values to the v4 chat-tested baseline (per AUDIO-03 + locked decision in CONTEXT.md). The current shipped values (PHASE 18, LAYER_ARRIVAL 16, MIX_MOVE 20, HEARTBEAT 70, TRACK_CHANGE 6) are too conservative — they let real moments pass silently. The new values (10 / 10 / 14 / 45 / 5) match the "harikaydı" baseline ear-test from real DJ session 2026-05-11 (per memory `project_v4_canonical_baseline`).

**Hallucination class closed:** "AI didn't react when it should have" — tight cooldowns mean Gemini fires on real moments instead of being silently gagged by the 18s PHASE-cooldown across a track that has 3 phase shifts.

This plan also extends `scripts/eval/replay_harness.py` with a `--print-cooldowns` mode that measures actual inter-event gaps from replayed sessions, so the locked values can be validated empirically (and re-locked with audit trail if measured drift > ±1s).

Purpose: Anti-slop reactivity tuning + empirical validation tooling.

Output:
- 5 reduced constants in `audio/constants.py` with audit-trail comments.
- 3 test files updated to pin NEW values (test pins are intentionally updated, not preserved).
- New `--print-cooldowns` flag on `replay_harness.py` with a delta-report formatter.
- New test exercising the cooldown-report mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-anti-slop-audio-port/40-CONTEXT.md
@.planning/phases/40-anti-slop-audio-port/40-RESEARCH.md
@CLAUDE.md

@src/vibemix/audio/constants.py
@src/vibemix/state/event_detector.py
@tests/audio/test_constants.py
@tests/audio/test_phase17_constants.py
@tests/state/test_event_detector.py
@scripts/eval/replay_harness.py

<interfaces>
Current locked constants block (src/vibemix/audio/constants.py:43-92):
```python
HEARTBEAT_SEC = 70.0  # v4:133 — retuned post-chat-log
MIN_EVENT_GAP_PER_TYPE: dict[str, float] = {
    "TRACK_CHANGE": 6.0,
    "PHASE": 18.0,
    "LAYER_ARRIVAL": 16.0,
    "MIX_MOVE": 20.0,
    "HEARTBEAT": HEARTBEAT_SEC,
    "MIC": 3.0,
    "MANUAL": 1.5,
    # ... Phase 17/30 detector cooldowns left UNCHANGED ...
}
```

Target values (locked per AUDIO-03 + CONTEXT.md §Event Cooldowns):
- `TRACK_CHANGE: 5.0` (was 6.0)
- `PHASE: 10.0` (was 18.0)
- `LAYER_ARRIVAL: 10.0` (was 16.0)
- `MIX_MOVE: 14.0` (was 20.0)
- `HEARTBEAT_SEC: 45.0` (was 70.0) — and `MIN_EVENT_GAP_PER_TYPE["HEARTBEAT"]` continues to reference `HEARTBEAT_SEC`
- `MIC: 3.0` (unchanged)
- `MANUAL: 1.5` (unchanged)
- All Phase 17 SENSE-12 / Phase 30 SENSE-17 detector cooldowns UNCHANGED (KICK_SWAP 14, SUB_LAYER_ARRIVAL 16, KICK_DENSITY_SHIFT 18, BREAKDOWN_KICK_KILL 20, REENTRY_KICK_LAND 12, PHRASE_BOUNDARY 24, DISTORTION_CLIMB 6, ACID_LINE_ENTRY 8).

Existing test pins to UPDATE (per RESEARCH Pitfall 3 — search confirmed by `grep -rn "MIN_EVENT_GAP_PER_TYPE\|18\.0\|70\.0" tests/audio/ tests/state/`):
- `tests/audio/test_constants.py:52` — `assert HEARTBEAT_SEC == 70.0` → `45.0`
- `tests/audio/test_constants.py:105-111` — 5 assertions, update to new values
- `tests/audio/test_constants.py:118` — `assert MIN_EVENT_GAP_PER_TYPE["HEARTBEAT"] == HEARTBEAT_SEC` — keep (identity, not value).
- `tests/state/test_event_detector.py:14-16` — docstring/constants in a test-internal cooldown table; update to new values.
- `tests/audio/test_phase17_constants.py` — likely pins Phase 17 detector cooldowns (UNCHANGED) and may pin HEARTBEAT/PHASE alongside; only update HEARTBEAT/PHASE/LAYER_ARRIVAL/MIX_MOVE/TRACK_CHANGE references.

The audit-trail comments in constants.py MUST cite the v4 chat-test date (2026-05-11) and the project memory `project_v4_canonical_baseline` per RESEARCH §State of the Art table. Format:
```python
HEARTBEAT_SEC = 45.0  # Plan 40-04 / AUDIO-03 — was 70.0; v4 chat-tested 2026-05-11
                     # baseline ("harikaydı" session). See project memory
                     # project_v4_canonical_baseline.

MIN_EVENT_GAP_PER_TYPE: dict[str, float] = {
    "TRACK_CHANGE": 5.0,    # Plan 40-04 — was 6.0; v4 2026-05-11 baseline
    "PHASE": 10.0,          # Plan 40-04 — was 18.0; v4 2026-05-11 baseline
    "LAYER_ARRIVAL": 10.0,  # Plan 40-04 — was 16.0; v4 2026-05-11 baseline
    "MIX_MOVE": 14.0,       # Plan 40-04 — was 20.0; v4 2026-05-11 baseline
    "HEARTBEAT": HEARTBEAT_SEC,
    # ...
}
```

`scripts/eval/replay_harness.py` extension surface (per RESEARCH Code Examples §`--print-cooldowns`):
- New argparse flag: `parser.add_argument("--print-cooldowns", action="store_true", help="Emit measured inter-event gaps per type; warns when measured median >±1s from MIN_EVENT_GAP_PER_TYPE.")`
- New accumulator in the main replay loop: `measured_gaps: dict[str, list[float]] = defaultdict(list)`; `last_per_type_at: dict[str, float] = {}`; on each event fire `measured_gaps[ev.type].append(now - last_per_type_at.get(ev.type, now)); last_per_type_at[ev.type] = now`.
- New post-loop report: iterate `measured_gaps`, compute median (sorted list middle element), compare to `MIN_EVENT_GAP_PER_TYPE.get(ev_type, EVENT_GLOBAL_MIN_GAP)`, print line with `ev_type`, `median_gap`, `expected_min`, `delta`. Warn (print, don't exit-non-zero) when `abs(delta) > 1.0`.
- Output format (stderr): `{ev_type:24s} median_gap={median:6.2f}s expected_min={expected:5.2f}s delta={delta:+5.2f}s`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cooldown constants update + existing test pin updates</name>
  <files>src/vibemix/audio/constants.py, tests/audio/test_constants.py, tests/audio/test_phase17_constants.py, tests/state/test_event_detector.py</files>
  <behavior>
    - `HEARTBEAT_SEC` literal changes from `70.0` to `45.0` in `src/vibemix/audio/constants.py:52`.
    - The 4 changed entries in `MIN_EVENT_GAP_PER_TYPE` (TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE) update to (5, 10, 10, 14) with `# Plan 40-04 — was N; v4 2026-05-11 baseline` audit-trail comments.
    - Phase 17/30 detector cooldowns (KICK_SWAP, SUB_LAYER_ARRIVAL, KICK_DENSITY_SHIFT, BREAKDOWN_KICK_KILL, REENTRY_KICK_LAND, PHRASE_BOUNDARY, DISTORTION_CLIMB, ACID_LINE_ENTRY) and MIC/MANUAL are UNCHANGED (per CONTEXT §Event Cooldowns).
    - `tests/audio/test_constants.py` updates 6 assertions (HEARTBEAT_SEC == 45.0; TRACK_CHANGE == 5.0; PHASE == 10.0; LAYER_ARRIVAL == 10.0; MIX_MOVE == 14.0; HEARTBEAT == 45.0). Identity assertion `MIN_EVENT_GAP_PER_TYPE["HEARTBEAT"] == HEARTBEAT_SEC` preserved.
    - `tests/state/test_event_detector.py` updates the docstring/constants table at lines 14-16 to reflect the new values. If the test logic depends on specific numeric values (e.g. "wait 18s before next PHASE"), update those magic numbers too — locate via `grep -n "18\.0\|70\.0\|16\.0\|20\.0\|6\.0" tests/state/test_event_detector.py`.
    - `tests/audio/test_phase17_constants.py` — if it pins HEARTBEAT/PHASE/LAYER_ARRIVAL/MIX_MOVE/TRACK_CHANGE numbers (RESEARCH Pitfall 3 calls this out), update them. Phase 17 detector cooldowns themselves stay as-is.
    - No compat shim, no "soft warning" old-value preservation — the change IS the point (RESEARCH Pitfall 3 + CONTEXT §Event Cooldowns "match v4 chat-tested baseline literally").
  </behavior>
  <action>
    Modify `src/vibemix/audio/constants.py`:
    1. Change line 52 from `HEARTBEAT_SEC = 70.0  # v4:133 — heartbeat event cadence (retuned post-chat-log)` to `HEARTBEAT_SEC = 45.0  # Plan 40-04 / AUDIO-03 — was 70.0; v4 chat-tested 2026-05-11 baseline ("harikaydı" session). project memory project_v4_canonical_baseline.`
    2. Change the 4 entries in `MIN_EVENT_GAP_PER_TYPE` (lines 55-58):
       - `"TRACK_CHANGE": 6.0,` → `"TRACK_CHANGE": 5.0,  # Plan 40-04 — was 6.0; v4 2026-05-11 baseline`
       - `"PHASE": 18.0,` → `"PHASE": 10.0,  # Plan 40-04 — was 18.0; v4 2026-05-11 baseline`
       - `"LAYER_ARRIVAL": 16.0,` → `"LAYER_ARRIVAL": 10.0,  # Plan 40-04 — was 16.0; v4 2026-05-11 baseline`
       - `"MIX_MOVE": 20.0,` → `"MIX_MOVE": 14.0,  # Plan 40-04 — was 20.0; v4 2026-05-11 baseline`
    3. `"HEARTBEAT": HEARTBEAT_SEC,` line is unchanged (the value flows from the updated module constant).
    4. Verify Phase 17/30 detector cooldowns and MIC/MANUAL are byte-identical to before.

    Modify `tests/audio/test_constants.py`:
    - Line 52: `assert HEARTBEAT_SEC == 70.0` → `assert HEARTBEAT_SEC == 45.0  # Plan 40-04`.
    - Lines 105-109 (5 assertions): update to new values per the spec above.
    - Line 117 identity check preserved.

    Modify `tests/state/test_event_detector.py`:
    - Line 14-16 docstring/test-constants block: update numeric values to (5/10/10/14/45/3/1.5).
    - Search for any other magic-number assertions (e.g. `time.sleep(18.0)` or `assert gap > 18.0`) and update to the new minima. RESEARCH Pitfall 3 specifically calls out this file — be thorough; run `grep -n "18\.0\|16\.0\|20\.0\|70\.0\|6\.0" tests/state/test_event_detector.py` to enumerate.

    Modify `tests/audio/test_phase17_constants.py`:
    - Same approach: grep for the old values, update where they refer to PHASE/LAYER_ARRIVAL/MIX_MOVE/HEARTBEAT/TRACK_CHANGE. Phase 17 detector cooldowns (KICK_SWAP 14.0, SUB_LAYER_ARRIVAL 16.0, KICK_DENSITY_SHIFT 18.0, etc.) stay UNCHANGED — distinguish by event-type name.

    NEVER touch `cohost_v4.py:134-142` — POC file, Phase 37-06 immutability gate. The v4 chat-test values diverge from the literal `cohost_v4.py` shipped file per RESEARCH §Key Findings "the values in CONTEXT.md (10/10/14/45/5) are the TARGET; the values shipped in `src/vibemix/audio/constants.py:54-90` are still the OLD values (18/16/20/70/6) verbatim from v4:134-142" — the locked target is the chat-tested baseline, not the v4 file.
  </action>
  <verify>
    <automated>python -m pytest tests/audio/test_constants.py tests/audio/test_phase17_constants.py tests/state/test_event_detector.py -x -q</automated>
  </verify>
  <done>
    - All 3 affected test files pass with new value assertions.
    - `grep -n "PHASE.*10\.0\|HEARTBEAT_SEC = 45\.0\|TRACK_CHANGE.*5\.0" src/vibemix/audio/constants.py | wc -l` ≥ 3.
    - `grep -v '^#' src/vibemix/audio/constants.py | grep -c "70\.0\|18\.0" ` returns 0 hits in the cooldown block region (post-edit). Phase 17 SENSE-12 KICK_DENSITY_SHIFT = 18.0 line is NOT a regression — it's intentionally unchanged. Validate by structured assertion `MIN_EVENT_GAP_PER_TYPE["KICK_DENSITY_SHIFT"] == 18.0` in the test file (verifying it survived the edit) rather than greppingsource for `18.0`.
    - POC immutability: `git status cohost_v4*.py cohost.py cohost_v2.py cohost_lk.py mascot.html` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: replay_harness `--print-cooldowns` flag + cooldown-report test</name>
  <files>scripts/eval/replay_harness.py, tests/eval/test_replay_harness_cooldowns.py</files>
  <behavior>
    - `replay_harness.py` gains a `--print-cooldowns` argparse flag (store_true, default False).
    - When flag is set, the main replay loop accumulates `measured_gaps: dict[str, list[float]]` keyed by event type. On each EventDetector fire, append `now - last_per_type_at.get(ev.type, now)` and update `last_per_type_at[ev.type] = now`.
    - After all sessions are processed, emit a per-type report to stderr: `{ev_type:24s} median_gap={median:6.2f}s expected_min={expected:5.2f}s delta={delta:+5.2f}s`. Where `expected = MIN_EVENT_GAP_PER_TYPE.get(ev_type, EVENT_GLOBAL_MIN_GAP)` and `delta = median - expected`.
    - When `abs(delta) > 1.0`, additionally emit: `WARNING: {ev_type} measured gap outside ±1s of locked value`. Do NOT exit non-zero — the flag is observational, not a CI gate (per CONTEXT §Replay harness validation, the ±1s gate is informational for now; harder enforcement happens in Phase 42 GATE-04).
    - Cooldown-report block is the FIRST line emitted post-replay; the existing scorecard output still follows.
    - Test `tests/eval/test_replay_harness_cooldowns.py`: synthesize a small replay fixture (in-memory or via existing fixture session) with 4-5 events at known timestamps; invoke `replay_harness.main(["--corpus", str(fixture_dir), "--judges", "noop", "--output", str(tmp_path), "--print-cooldowns"])` (or equivalent argparse invocation); capture stderr; assert the report contains expected ev_type rows with median_gap values matching the synthesized event spacing.
  </behavior>
  <action>
    Modify `scripts/eval/replay_harness.py`:
    1. Add `parser.add_argument("--print-cooldowns", action="store_true", help=...)` to the existing argparse setup.
    2. Add `from collections import defaultdict` + `from vibemix.audio.constants import MIN_EVENT_GAP_PER_TYPE, EVENT_GLOBAL_MIN_GAP` imports if not already present.
    3. Initialize accumulators near the top of the main replay loop: `measured_gaps: dict[str, list[float]] = defaultdict(list)` and `last_per_type_at: dict[str, float] = {}`.
    4. After each `EventDetector` fire (locate via `grep -n "detector.detect\|detector.fire\|event_detector" scripts/eval/replay_harness.py`), append the gap calculation per the behavior spec. Gate on `if args.print_cooldowns:` so the accumulator overhead is zero when the flag is off.
    5. After the session-replay loop completes and before the scorecard call, add the report-emission block per RESEARCH Code Examples §`--print-cooldowns`. Wrap in `if args.print_cooldowns:`.

    Create `tests/eval/test_replay_harness_cooldowns.py`:
    - Use a small synthetic events.jsonl fixture OR mock the EventDetector to yield a controlled event sequence (e.g. 5 PHASE events spaced 12s apart, 4 MIX_MOVE events spaced 15s apart). The simpler path: monkeypatch the in-loop `detector.detect` to return a pre-scripted sequence indexed by tick number.
    - Invoke `replay_harness.main` (or the orchestrating function) with `--print-cooldowns` enabled and a `tmp_path` output dir; capture stderr via `capsys.readouterr().err`.
    - Assert the stderr output contains: a line for `PHASE` with `median_gap=12.00s` (or close) and `delta=+2.00s` (relative to locked 10.0); a line for `MIX_MOVE` with `median_gap=15.00s` and `delta=+1.00s`; the `WARNING` line for PHASE (delta > 1.0) is present; the WARNING line for MIX_MOVE is NOT present (delta == 1.0, edge case treated as not-exceeding).
  </action>
  <verify>
    <automated>python -m pytest tests/eval/test_replay_harness_cooldowns.py -x -q</automated>
  </verify>
  <done>
    - `tests/eval/test_replay_harness_cooldowns.py` passes (≥3 assertions on the stderr-captured report).
    - `python -m scripts.eval.replay_harness --help 2>&1 | grep -- "--print-cooldowns"` returns a hit.
    - Running the harness against an existing fixture session with `--print-cooldowns` produces a sensible report (manual sanity check; not gated in CI).
    - Existing `tests/eval/test_replay_harness.py` still passes — the new flag is additive, default off.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test value pins → CI gate | If old values survive in any test, CI silently keeps the old cooldowns logically green even though source flipped — slow rollback risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04-01 | Tampering | Test pin drift | mitigate | All 3 known pin sites (`tests/audio/test_constants.py`, `tests/audio/test_phase17_constants.py`, `tests/state/test_event_detector.py`) updated in this plan. A grep audit at end of plan confirms no `70.0\|18\.0` for HEARTBEAT/PHASE survives outside the SENSE-12 KICK_DENSITY_SHIFT block. |
| T-40-04-02 | Repudiation | Cooldown change history | mitigate | Audit-trail comments in `audio/constants.py` cite `Plan 40-04`, the old value, the v4 chat-test date (2026-05-11), and the project memory. Git blame + commit message complete the trail. |
| T-40-04-03 | Information Disclosure | Replay harness metrics in CI logs | accept | Cooldown report contains only event-type names + numeric gaps; no DJ content / track titles. Safe for CI stderr. |
</threat_model>

<verification>
- Constants tests: `python -m pytest tests/audio/test_constants.py tests/audio/test_phase17_constants.py -x -q`.
- EventDetector tests: `python -m pytest tests/state/test_event_detector.py -x -q`.
- Cooldown-report test: `python -m pytest tests/eval/test_replay_harness_cooldowns.py -x -q`.
- Full regression on impacted dirs: `python -m pytest tests/audio/ tests/state/ tests/eval/ -x -q`.
- POC immutability: `git status cohost_v4*.py cohost.py cohost_v2.py cohost_lk.py mascot.html` clean.
- Manual eval (optional): `python -m scripts.eval.replay_harness --corpus tests/eval/fixtures --judges noop --output /tmp/eval-out --print-cooldowns 2>&1 | grep -E "median_gap"` — emits real measurements from fixture corpus.
</verification>

<success_criteria>
1. `MIN_EVENT_GAP_PER_TYPE` and `HEARTBEAT_SEC` reflect the v4 chat-tested baseline (5/10/10/14/45) with audit-trail comments.
2. All existing test pins on the OLD values are updated to the NEW values — no compat shims.
3. `replay_harness.py --print-cooldowns` emits per-type measured median gaps + delta-from-locked + warnings when delta > ±1s. The flag is additive (default off; no impact on existing replay paths).
4. Phase 17/30 detector cooldowns are byte-identical to before (KICK_SWAP 14, etc.).
5. AUDIO-03 requirement closed.
</success_criteria>

<output>
After completion, create `.planning/phases/40-anti-slop-audio-port/40-04-SUMMARY.md` documenting:
- Before/after table of the 5 changed constants.
- Test pin sites updated (file:line list).
- Sample `--print-cooldowns` output from running against fixture corpus.
- One-line note: "Phase 42 GATE-04 will harden the ±1s observational warning into a CI exit-non-zero gate once the real-corpus baseline is signed."
</output>
