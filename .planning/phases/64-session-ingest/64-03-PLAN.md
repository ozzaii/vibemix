---
phase: 64-session-ingest
plan: 03
type: execute
wave: 3
depends_on: [64-02]
files_modified:
  - src/vibemix/runtime/session_loop.py
  - tests/memory/test_ingest_wiring.py
autonomous: true
requirements: [INGEST-02]

must_haves:
  truths:
    - "On session close, the runtime enqueues ingest_session for the just-finished session via loop.run_in_executor (off the reaction loop)"
    - "At boot, the runtime enqueues run_ingest_sweep for crashed/missed sessions via loop.run_in_executor"
    - "The wiring is one-way (runtime imports memory.ingest; memory.ingest never imports the runtime live path) and best-effort/never-raise"
  artifacts:
    - path: "src/vibemix/runtime/session_loop.py"
      provides: "session-close ingest enqueue + boot ingest sweep, both via run_in_executor"
      contains: "run_ingest_sweep"
    - path: "tests/memory/test_ingest_wiring.py"
      provides: "asserts the seams dispatch through run_in_executor (not inline) and import memory.ingest"
      contains: "run_in_executor"
  key_links:
    - from: "src/vibemix/runtime/session_loop.py"
      to: "vibemix.memory.ingest.ingest_session / run_ingest_sweep"
      via: "loop.run_in_executor(None, fn, ...)"
      pattern: "run_in_executor"
    - from: "session_loop.on_session_close / run_boot_sweeps"
      to: "the ingest enqueue"
      via: "the session-end + boot seams"
      pattern: "ingest_session|run_ingest_sweep"
---

<objective>
Wire the ingest module into the runtime at the two seams that already dispatch off-hot-path FS work: session-close (`on_session_close`, :709) enqueues a single-session `ingest_session`, and boot (`run_boot_sweeps`, :685) enqueues `run_ingest_sweep` for crashed/missed sessions — both via `loop.run_in_executor(None, ...)`, both best-effort and never-raising, matching the existing retention-sweep dispatch idiom. The arrow is one-way: the runtime imports `vibemix.memory.ingest`; the ingest module never imports the runtime live path (the no-live-path gate stays green by construction).

Purpose: INGEST-02 — ingest runs OFF the hot path, triggered at the session-end and boot seams, never inline in the reaction loop, never touching MusicState or holding state._lock.
Output: edits to `src/vibemix/runtime/session_loop.py` + a new `tests/memory/test_ingest_wiring.py` proving the seams dispatch through `run_in_executor`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-session-ingest/64-RESEARCH.md
@.planning/phases/64-session-ingest/64-PATTERNS.md

<interfaces>
<!-- The seams (SHIPPED) and the ingest surface (lands in 64-02). -->

From src/vibemix/runtime/session_loop.py (SHIPPED — the seams):
- async def run_boot_sweeps(self) -> None:  (:685 — boot seam; currently delegates to _fire_one_retention_sweep("boot"))
- async def on_session_close(self) -> None: (:709 — session-end seam; called from __main__ AFTER recorder.close() finalizes session.json)
- self.recordings_root: Path | None   (early-return guard when None)
- the dispatch idiom (_fire_one_retention_sweep, :725-756):
    loop = asyncio.get_running_loop()
    result = await loop.run_in_executor(None, run_retention_sweep, self.recordings_root, self.config_store.retention_days)

From src/vibemix/memory/ingest.py (lands in 64-02):
- def ingest_session(session_dir: Path, store: MemoryStore, embedder) -> IngestResult
- def run_ingest_sweep(recordings_root: Path, store: MemoryStore, embedder) -> ...

From src/vibemix/memory/store.py (SHIPPED):
- MemoryStore(db_path=None, prefer_sqlite_vec=True)  (db_path=None → lazy app_data_dir)
</interfaces>

<read_first>
- src/vibemix/runtime/session_loop.py  (lines 685-760 — run_boot_sweeps, on_session_close, _fire_one_retention_sweep dispatch idiom; the import block at the top of the file; how self.recordings_root + self.config_store are constructed ~:170-190)
- src/vibemix/memory/ingest.py  (the ingest_session / run_ingest_sweep signatures from 64-02 — what you enqueue)
- src/vibemix/memory/store.py  (MemoryStore construction — how to obtain the store + embedder to pass; check whether a shared embedder/store instance already exists on the loop or must be constructed lazily)
- src/vibemix/library/embed.py  (LibraryEmbedder construction — the embedder argument; construct lazily/best-effort so a missing proxy never breaks boot)
- .planning/phases/64-session-ingest/64-RESEARCH.md §Off-Hot-Path Execution (the two seams + the gate-safe one-way arrow) + Pitfall 5 (boot sweep must not block startup)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire session-close + boot ingest enqueue via run_in_executor</name>
  <files>src/vibemix/runtime/session_loop.py</files>
  <behavior>
    - on_session_close enqueues ingest_session(session_dir, store, embedder) for the just-finished session through loop.run_in_executor (NOT inline), AFTER the existing retention sweep, best-effort.
    - run_boot_sweeps enqueues run_ingest_sweep(recordings_root, store, embedder) through loop.run_in_executor, best-effort, never blocking IPC.
    - Both early-return when self.recordings_root is None (mirror the existing guard).
    - Any ingest exception is logged + swallowed (never raises into the session loop).
    - The store + embedder are constructed lazily/best-effort so a missing sqlite-vec extension or proxy never breaks session close or boot.
  </behavior>
  <action>Import `from vibemix.memory.ingest import ingest_session, run_ingest_sweep` at module level in session_loop.py (this is the gate-safe one-way arrow — runtime→ingest; the no-live-path gate forbids the reverse, not this). Add a helper (e.g. `_fire_ingest(self, trigger, session_dir=None)`) mirroring `_fire_one_retention_sweep`'s shape: early-return if recordings_root is None; construct a MemoryStore (db_path=None → lazy app_data_dir) + a LibraryEmbedder lazily inside the helper wrapped in try/except so construction failure (no extension / no proxy) logs + returns without raising; then `await loop.run_in_executor(None, run_ingest_sweep, self.recordings_root, store, embedder)` for boot or `await loop.run_in_executor(None, ingest_session, session_dir, store, embedder)` for close. Call the boot variant from `run_boot_sweeps` (sibling to the existing retention sweep — order does not matter; keep it best-effort). Call the close variant from `on_session_close` for the just-finished session dir (derive it the same way the close path knows the session — from the active recorder's session dir; if unavailable, fall back to the boot-style sweep which the marker makes a near-no-op). Wrap the whole thing in try/except + log; NEVER raise. Do NOT touch the reaction loop, MusicState, or state._lock. Do NOT inline any embed/FS work — everything heavy goes through run_in_executor.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && grep -q "from vibemix.memory.ingest import" src/vibemix/runtime/session_loop.py && grep -q "run_in_executor" src/vibemix/runtime/session_loop.py && grep -Eq "ingest_session|run_ingest_sweep" src/vibemix/runtime/session_loop.py && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from vibemix.memory.ingest import" src/vibemix/runtime/session_loop.py`
    - `grep -c "run_ingest_sweep\|ingest_session" src/vibemix/runtime/session_loop.py` >= 2 (both seams)
    - The ingest calls are arguments to `loop.run_in_executor(None, ...)` (not direct calls) — verified by reading the helper
    - `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_no_live_path_import.py -x` exits 0 (the one-way arrow did NOT break the dormancy gate — runtime importing ingest is allowed; ingest importing runtime is not)
  </acceptance_criteria>
  <done>on_session_close + run_boot_sweeps enqueue ingest through run_in_executor, best-effort/never-raise, lazily constructing store+embedder; the no-live-path gate stays GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the wiring dispatches off-hot-path (not inline)</name>
  <files>tests/memory/test_ingest_wiring.py</files>
  <behavior>
    - The session_loop source wires ingest through run_in_executor at both the boot and close seams (assert by source inspection + an exercised dispatch with a monkeypatched ingest that records it ran on an executor thread, not the loop thread).
    - When recordings_root is None, neither seam attempts ingest (early-return guard).
    - An ingest failure (monkeypatched to raise) is swallowed — the seam coroutine completes without raising.
  </behavior>
  <action>Create tests/memory/test_ingest_wiring.py. Add a static assertion that session_loop.py contains `from vibemix.memory.ingest import` and that the ingest calls appear as run_in_executor arguments (source-text check mirroring the style of existing wiring tests). Add a behavioral test: construct a SessionLoop-equivalent (or monkeypatch the helper's `run_ingest_sweep`/`ingest_session` to a recorder that captures `threading.get_ident()`), run the seam coroutine under asyncio, and assert the recorded thread id != the event-loop thread id (proves run_in_executor, not inline). Add a test that with recordings_root=None the seam does not call ingest. Add a test that a raising ingest stub does not propagate (the seam coroutine returns normally). Mark live-API-needing paths out — use a fake embedder/store; no network. If constructing a full SessionLoop is heavy, prefer the monkeypatch-the-helper approach to keep the test sub-second.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_ingest_wiring.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "run_in_executor" tests/memory/test_ingest_wiring.py`
    - `grep -q "recordings_root" tests/memory/test_ingest_wiring.py` (the None-guard test)
    - `pytest tests/memory/test_ingest_wiring.py -x` exits 0
    - The thread-identity (off-loop) assertion is present (`get_ident` or equivalent executor-thread proof)
  </acceptance_criteria>
  <done>test_ingest_wiring.py proves both seams dispatch off the loop thread, honor the recordings_root None guard, and swallow ingest failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime live path → memory.ingest | One-way import arrow; the runtime (already in the live path) imports ingest, never the reverse — keeps the dormancy gate green |
| session-end / boot seam → executor thread | Heavy FS+embed work crosses onto a worker thread via run_in_executor; never runs on the asyncio reaction loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-06 | Tampering (invariant break) | Live-path coupling via the wiring | mitigate | The import is one-way (runtime→ingest); ingest.py imports no runtime live path (enforced by the no-live-path static + ingest-subprocess dormancy gates). Verified GREEN after this plan. |
| T-64-08 | DoS / availability | Boot sweep blocking startup (Pitfall 5) | mitigate | Dispatched via run_in_executor; best-effort + never-raise; the memory_ingested marker makes steady-state boot ingest 0 sessions. |
| T-64-09 | Availability | A failing ingest crashing session close / boot | mitigate | Whole helper wrapped in try/except + log; the seam coroutine never raises (test_ingest_wiring asserts this). Store/embedder constructed lazily so a missing extension/proxy degrades gracefully. |
| T-64-10 | Single-writer invariant | Ingest touching MusicState / state._lock | mitigate | Ingest reads dead on-disk artifacts only on an executor thread; never imports MusicState, never holds state._lock (no-live-path gate + by construction). |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new dependencies; stdlib + shipped in-repo modules only (no install step). |
</threat_model>

<verification>
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory` → all GREEN (the 6 ingest tests, the wiring tests, no-extraction, no-live-path static + both subprocess, parity).
- `grep -rn "import" src/vibemix/memory/ingest.py | grep -E "session_loop|runtime.session|state.coach|ws_bus|MusicState"` → empty (the arrow stays one-way).
- `git diff --name-only` shows ONLY src/vibemix/runtime/session_loop.py + tests/memory/test_ingest_wiring.py.
- Full suite shows no NEW failures beyond the documented 8-failure live-tuning-or-brain baseline.
</verification>

<success_criteria>
- on_session_close enqueues ingest_session for the just-finished session via run_in_executor.
- run_boot_sweeps enqueues run_ingest_sweep via run_in_executor.
- Both best-effort, never-raise, recordings_root-None-guarded, lazily constructing store+embedder.
- The one-way runtime→ingest arrow holds; no-live-path gate GREEN.
- test_ingest_wiring proves off-loop dispatch + the None guard + failure-swallowing.
</success_criteria>

<output>
Create `.planning/phases/64-session-ingest/64-03-SUMMARY.md` when done
</output>
