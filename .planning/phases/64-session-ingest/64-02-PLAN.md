---
phase: 64-session-ingest
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - src/vibemix/memory/ingest.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "build_coach_line_signature produces a deterministic, byte-reproducible text string from raw fields with NO model in the path"
    - "ingest_session emits one coach_line record per EMITTED ai_text line and SKIPS citation_strip (silenced) lines"
    - "Re-ingesting a marked session costs 0 embed calls and writes 0 new records (marker + moments-count idempotency)"
    - "A signature-keyed content-hash embed cache makes an identical signature re-embed 0-cost (the embed_query no-cache fix, Finding 2)"
    - "Every record is session_id + ts + kind=='coach_line' tagged via MemoryStore.add_record"
    - "run_ingest_sweep mirrors recordings_index path-traversal defense (SESSION_DIR_RE + is_relative_to) and is best-effort per session"
    - "ingest.py imports NO live reaction path; the no-extraction + no-live-path + model-literal gates stay green"
  artifacts:
    - path: "src/vibemix/memory/ingest.py"
      provides: "build_coach_line_signature + ingest_session + run_ingest_sweep + memory_ingested marker + signature-keyed embed cache"
      contains: "def build_coach_line_signature("
      min_lines: 120
  key_links:
    - from: "src/vibemix/memory/ingest.py"
      to: "vibemix.memory.store.MemoryStore.add_record"
      via: "compose the shipped write target (no ad-hoc DB open)"
      pattern: "\\.add_record\\("
    - from: "src/vibemix/memory/ingest.py"
      to: "vibemix.library.embed.LibraryEmbedder.embed_query"
      via: "the ONLY model call, cache-guarded"
      pattern: "embed_query"
    - from: "src/vibemix/memory/ingest.py"
      to: "memory_ingested marker + embed_cache tables"
      via: "sqlite idempotency layer"
      pattern: "memory_ingested|embed_cache"
---

<objective>
Ship `src/vibemix/memory/ingest.py` — the post-session ingest module. It reads a finished session's `events.jsonl`, builds ONE deterministic `coach_line` text signature per EMITTED `ai_text` reaction (skipping silenced `citation_strip` lines), embeds each via the shipped `LibraryEmbedder.embed_query` behind a NEW signature-keyed content-hash cache, and writes tagged records through the shipped `MemoryStore.add_record`. It is marker-gated idempotent (re-ingest = 0 embeds, 0 writes), exposes a best-effort boot sweep, and never imports the live reaction path. Flips the 64-01 RED contract GREEN.

Purpose: This is the entire INGEST capability — INGEST-01 (deterministic text signatures, no LLM-extraction, no audio embed), INGEST-02 (off-hot-path-safe, gate-clean import boundary), INGEST-03 (the resolved ONE-kind `coach_line` taxonomy, fully tagged for later exclusion + deletion).
Output: `src/vibemix/memory/ingest.py` (NEW), with the 6 `test_ingest.py` tests + both ingest dormancy/no-extraction gates GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-session-ingest/64-RESEARCH.md
@.planning/phases/64-session-ingest/64-PATTERNS.md
@.planning/phases/63-memory-store/63-03-SUMMARY.md

<interfaces>
<!-- Everything this module composes already ships. Use directly — no codebase exploration needed. -->

From src/vibemix/memory/store.py (SHIPPED — the write target, do NOT re-open the DB):
- MemoryStore.add_record(record_id, session_id, ts, kind, signature, embedding) -> None  (vector-first; validates session_id)
- MemoryStore.query_topk(query, k, *, exclude_session=None)
- MemoryStore.delete_session(session_id) -> int   (do NOT modify — see A4 marker coupling below)

From src/vibemix/library/embed.py (SHIPPED):
- LibraryEmbedder.embed_query(query: str) -> np.ndarray   (768-dim, L2-normalized, FLEX, proxy-only; NO built-in cache — Finding 2)
- LibraryEmbedder._model   (probe-derived model id — NEVER a literal; this is the cache-key model component)
- EMBED_CACHE_DB_PATH = Path.home()/".cache"/"vibemix"/"embeddings.db"
- the embed_cache table shape to CLONE: CREATE TABLE IF NOT EXISTS embed_cache (key TEXT PRIMARY KEY, vector BLOB NOT NULL, ts REAL NOT NULL)  [embed.py:146-157]
- _cache_get/_cache_put round-trip [embed.py:605-623]: np.frombuffer(blob, dtype=np.float32).copy() ; INSERT OR REPLACE

From src/vibemix/library/_cosine.py (SHIPPED):
- EMBEDDING_DIM = 768, l2_normalize

From src/vibemix/state/evidence_registry.py:103-135 (the locked grammar — COPY the constant, do NOT import the module):
- EVIDENCE_SOURCES = {"ev","aud","midi","track","screen","mix","tend","key"}
- EVIDENCE_CITATION_RE = re.compile(rf"\[{_INNER_ATOM}(?:,{_INNER_ATOM})*\]") where _INNER_ATOM = rf"(?:{_SOURCE_ALT}):[^\s,\]]+"
</interfaces>

<read_first>
- src/vibemix/memory/store.py  (MemoryStore.add_record + the CREATE TABLE IF NOT EXISTS moments idiom at :252-265 to mirror for memory_ingested; the lazy app_data_dir import discipline at :71-83)
- src/vibemix/memory/index_sqlite_vec_memory.py  (how the backend/db connection is owned — for where the marker + cache tables live)
- src/vibemix/library/embed.py  (embed_query :362-369 NO-cache; embed_cache table :146-157; _cache_get/_cache_put :605-623; _track_hash keying idiom :583-601; self._model)
- src/vibemix/debrief/session_loader.py  (the ~12-line malformed-tolerant _read_events at :74-88 to MIRROR; the InvalidSessionDir/is_dir guard at :130-136; do NOT inherit the 5-min SessionTooShort floor at :140 or read the absent evidence_registry.json at :143-156)
- src/vibemix/runtime/recordings_index.py  (SESSION_DIR_RE at :78; the two-layer path-traversal gate at :388-401 to COPY; the run_retention_sweep scandir/best-effort shape at :490-524)
- src/vibemix/state/evidence_registry.py  (lines 103-135 — the EVIDENCE_CITATION_RE constant to COPY into ingest.py with a lock-step source comment)
- .planning/phases/64-session-ingest/64-RESEARCH.md §Signature Template Spec + §Off-Hot-Path Execution + Findings 1/2/3 + Pitfalls 1-5 + §Assumptions Log A4
- .planning/phases/64-session-ingest/64-PATTERNS.md  (every analog file:line; the A4 marker↔retention coupling note)
- tests/memory/test_ingest.py  (the RED contract from 64-01 — the exact identifiers + behaviors you implement against)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signature builder + reader + embed cache + marker schema</name>
  <files>src/vibemix/memory/ingest.py</files>
  <behavior>
    - build_coach_line_signature(reaction_text, ctx) returns the exact template "coach_line | track={track} | phase={phase} | deck={deck} | event={event_type} | cite={citation_tokens} | said: {reaction_text}" with missing→"unknown"/"unknown"/"none"/"MANUAL" defaults, citation tokens sorted+",".join, leading [emotion] tag stripped, NO `t`.
    - The signature is byte-identical for identical inputs (powers the content-hash cache).
    - A malformed-tolerant JSONL reader skips bad lines (mirrors session_loader._read_events) with NO 5-min floor.
    - The signature-keyed embed cache returns a cached vector for an identical (signature, model, SIG_TEMPLATE_VERSION) key at 0 API cost.
  </behavior>
  <action>Create src/vibemix/memory/ingest.py. Define `SIG_TEMPLATE_VERSION = "v1-coach_line"`. COPY the `EVIDENCE_CITATION_RE` constant verbatim from state/evidence_registry.py:133 (add a comment: "lock-step source-of-truth: state/evidence_registry.py:133") plus `_EMOTION_TAG_RE = re.compile(r"^\[[a-z]+\]\s*")`. Implement `build_coach_line_signature(reaction_text, ctx)` as a PURE function (string assembly + regex only — no client, no genai, no I/O): track/phase/deck/event_type from ctx with the missing-value defaults, citation tokens via `sorted(m.group(0).strip("[]") for m in EVIDENCE_CITATION_RE.finditer(reaction_text))` joined ",", said = `_EMOTION_TAG_RE.sub("", reaction_text).strip()`. MIRROR session_loader._read_events as a private `_read_events_jsonl(path)` (json.loads per line, skip+log on JSONDecodeError) — do NOT import session_loader, do NOT inherit the 5-min floor. Add the embed-cache layer: `_embed_cache_key(signature, model_id) = SHA256(signature.encode()||b"||"||model_id.encode()||b"||"||SIG_TEMPLATE_VERSION.encode()).hexdigest()`; CLONE the `embed_cache(key TEXT PRIMARY KEY, vector BLOB NOT NULL, ts REAL NOT NULL)` table + `_cache_get`/`_cache_put` round-trip from embed.py:146-157,605-623 (do NOT modify library/embed.py — this is a clone living in the memory layer). Cache DB: a sibling under app_data_dir() OR the shared EMBED_CACHE_DB_PATH (A2 says shared is safe; planner discretion — prefer a memory-owned sibling for isolation). Add the `memory_ingested(session_id TEXT PRIMARY KEY, ingested_at REAL NOT NULL, sig_template_version TEXT NOT NULL)` table DDL mirroring store.py's CREATE TABLE IF NOT EXISTS idiom. CRITICAL: any `vibemix.runtime.config_store`/`app_data_dir` import MUST be function-local (mirror store.py:71-83) or the dormancy gate fails. The model id for the cache key comes from `LibraryEmbedder._model` — NEVER a literal.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_ingest.py::test_signature_deterministic tests/memory/test_ingest.py::test_embed_cache_hit -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def build_coach_line_signature(" src/vibemix/memory/ingest.py`
    - `grep -q 'SIG_TEMPLATE_VERSION = "v1-coach_line"' src/vibemix/memory/ingest.py`
    - `grep -q "memory_ingested" src/vibemix/memory/ingest.py` and `grep -q "embed_cache" src/vibemix/memory/ingest.py`
    - `grep -vE '^\s*#|"""' src/vibemix/memory/ingest.py | grep -qE "gemini-embedding" && echo FAIL || echo NO-LITERAL`  (no model literal)
    - `pytest tests/memory/test_ingest.py::test_signature_deterministic tests/memory/test_ingest.py::test_embed_cache_hit -x` exits 0
  </acceptance_criteria>
  <done>The pure signature builder, the malformed-tolerant reader, the signature-keyed embed cache, and the memory_ingested marker schema all exist; the determinism + cache-hit tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest_session + run_ingest_sweep (emit, idempotency, tagging, sweep path-defense)</name>
  <files>src/vibemix/memory/ingest.py</files>
  <behavior>
    - ingest_session(session_dir, store, embedder): marker-check first → already-ingested (marker present AND moments exist for the session) short-circuits before any read/embed (0 embeds, 0 writes). Otherwise reads events.jsonl, and for each EMITTED ai_text line (kind=="ai_text"; SKIP citation_strip) finds the nearest preceding `event` line for context, builds the signature, embeds (cache-guarded), and writes via store.add_record with record_id=f"{session_id}:{seq}" (seq=0-based index of the emitted ai_text line), session_id=dir basename, ts=line's t, kind="coach_line". Writes the memory_ingested marker last. Returns IngestResult(embeds_made, records_written, ...).
    - Idempotency is gated on COUNT(*) FROM moments WHERE session_id=? (cheap) so an evicted session's stale marker does not block legitimate re-ingest (A4 — WITHOUT editing Phase-63 delete_session).
    - run_ingest_sweep(recordings_root, store, embedder): os.scandir the root, SESSION_DIR_RE + is_relative_to(root.resolve()) gate before reading any dir (skip + never read on failure), skip already-ingested sessions, ingest_session the rest; best-effort per session (one failure logs + continues, never raises).
  </behavior>
  <action>Add `ingest_session` and `run_ingest_sweep` plus the `IngestResult` result type to ingest.py. ingest_session: KEEP the InvalidSessionDir/is_dir + events.jsonl-exists guard idiom (session_loader.py:130-136); idempotency check = marker row with matching SIG_TEMPLATE_VERSION AND `COUNT(*) FROM moments WHERE session_id=?` > 0 (the A4 fix — reconcile against moments existence so a retention-evicted session re-ingests; do NOT touch store.delete_session). For each EMITTED `ai_text` line only (skip `citation_strip` — embedding a silenced line is confabulation, Pitfall 2; `citation_bypass` out of scope per A3), find nearest preceding `event` line → ctx dict (track/phase/deck/type), build signature, compute cache key from `embedder._model`, cache-get → hit reuses vector (0 API) / miss calls `embedder.embed_query(signature)` then `_cache_put`, then `store.add_record(f"{sid}:{seq}", sid, ts, "coach_line", signature, vector)`. Write the memory_ingested marker after the loop. run_ingest_sweep: COPY SESSION_DIR_RE + the two-layer path-traversal gate from recordings_index.py:388-401 (regex match THEN is_relative_to(recordings_root.resolve()) THEN refuse-root) before reading any session dir; mirror the run_retention_sweep best-effort scandir loop (recordings_index.py:490-524); never-raise per session. Both functions are pure/executor-safe (no asyncio, no live state). Keep all runtime imports function-local.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_ingest.py tests/memory/test_no_extraction.py tests/memory/test_no_live_path_import.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def ingest_session(" src/vibemix/memory/ingest.py` and `grep -q "def run_ingest_sweep(" src/vibemix/memory/ingest.py`
    - `grep -q "SESSION_DIR_RE" src/vibemix/memory/ingest.py` and `grep -q "is_relative_to" src/vibemix/memory/ingest.py`
    - `grep -q "citation_strip" src/vibemix/memory/ingest.py`  (the skip is explicit)
    - `grep -q "COUNT(\*) FROM moments\|count.*moments\|FROM moments WHERE session_id" src/vibemix/memory/ingest.py`  (A4 moments-count idempotency)
    - `pytest tests/memory/test_ingest.py tests/memory/test_no_extraction.py tests/memory/test_no_live_path_import.py -x` exits 0 (all 6 ingest tests + both gates GREEN, incl. the new ingest-dormancy subprocess assertion)
    - `pytest tests/repo/test_model_literal_gate.py -x` exits 0
  </acceptance_criteria>
  <done>ingest_session + run_ingest_sweep exist; all 6 test_ingest.py tests GREEN; no-extraction, no-live-path (static + both subprocess), and model-literal gates GREEN; store.delete_session UNMODIFIED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk session dir name → boot sweep | Untrusted-shaped input (a crafted/garbage dir name); crosses into FS reads — defended by SESSION_DIR_RE + is_relative_to |
| events.jsonl bytes → reader | Crash-truncated / malformed lines; per-line json.loads + skip-on-error, no eval, no schema-trust |
| signature → embedding API (FLEX, via proxy) | Outbound; the ONLY external call; identical to the shipped library import path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Spoofing (fabricated facts) | Confabulation via a generation call in the ingest path (HEADLINE) | mitigate | ingest.py's ONLY model call is `embedder.embed_query`; no `generate_content`/`generate_reply`/`.chats.`/`GenerateContentConfig`. Enforced by the shipped no-extraction static gate (globs memory/*.py — auto-covers ingest.py). |
| T-64-02 | Spoofing | Embedding a silenced (never-heard) reaction | mitigate | Ingest `kind=="ai_text"` (emitted/heard) lines ONLY; explicitly SKIP `citation_strip`. The coach can only ever "remember" what the DJ actually heard. |
| T-64-03 | Tampering / EoP | Path traversal via crafted session dir name (boot sweep) | mitigate | COPY SESSION_DIR_RE (`^\d{8}-\d{6}$`) + `is_relative_to(recordings_root.resolve())` + refuse-root from recordings_index.py:388-401 before any read; MemoryStore.add_record re-validates session_id on write. |
| T-64-04 | DoS / integrity | Malformed/oversized events.jsonl (crash-truncated last line) | mitigate | Per-line json.loads + skip-on-error (mirror session_loader._read_events); per-session best-effort in the sweep — one bad session never blocks others or boot. |
| T-64-05 | Information disclosure | Memory writes leaking outside the sanctioned dir | mitigate | All paths derive from app_data_dir(); reads scoped to recordings_root; NEVER the off-limits Hermes/LM-Studio privacy paths. |
| T-64-06 | Tampering (invariant break) | Live-path coupling via ingest import | mitigate | No-live-path static + the new ingest-subprocess dormancy gate (64-01 Task 2); function-local runtime imports only; one-way runtime→ingest arrow. |
| T-64-07 | Budget integrity | Re-ingest cost (embed_query no-cache trap, Finding 2) | mitigate | Two-layer: memory_ingested marker (skip whole session) + signature-keyed embed cache (skip the API call on any miss/template-bump). Re-ingest = 0 API calls. |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new dependencies (RESEARCH §Package Legitimacy Audit — no install step); stdlib (sqlite3, hashlib, json, re) + shipped in-repo modules only. |
</threat_model>

<verification>
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory` → all GREEN incl. the 6 test_ingest.py tests, no-extraction (+ positive control), no-live-path (static + both subprocess), parity.
- `PYTHONPATH=src python3 -m pytest -q tests/repo/test_model_literal_gate.py` → GREEN.
- `grep -rn "import" src/vibemix/memory/ingest.py | grep -E "agent|prompts|state.coach|state.refresh|ws_bus|MusicState|EventDetector|session_loop"` → empty (no live-path import).
- `git diff --name-only` shows ONLY src/vibemix/memory/ingest.py touched (store.py / embed.py / delete_session UNMODIFIED).
- Full suite shows no NEW failures beyond the documented 8-failure live-tuning-or-brain baseline (63-03-SUMMARY).
</verification>

<success_criteria>
- src/vibemix/memory/ingest.py exists with build_coach_line_signature, ingest_session, run_ingest_sweep, the memory_ingested marker schema, and the signature-keyed embed cache.
- ONE kind only: "coach_line" (no `moment`, no `audio_moment`).
- Re-ingest of a marked session = 0 embed calls + 0 new records; an evicted session (0 moments) re-ingests (A4).
- Records carry session_id + ts + kind=="coach_line".
- No-extraction, no-live-path (incl. the new ingest dormancy), and model-literal gates all GREEN.
- Phase-63 store.py and library/embed.py UNMODIFIED.
</success_criteria>

<output>
Create `.planning/phases/64-session-ingest/64-02-SUMMARY.md` when done
</output>
