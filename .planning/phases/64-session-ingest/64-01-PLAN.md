---
phase: 64-session-ingest
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/memory/test_ingest.py
  - tests/memory/test_no_live_path_import.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]

must_haves:
  truths:
    - "A RED test contract pins build_coach_line_signature determinism, coach_line emission (skip citation_strip), idempotent re-ingest, record tagging, embed-cache hit, and executor dispatch BEFORE ingest.py exists"
    - "A synthetic events.jsonl fixture mirrors the real on-disk shapes (event lines + emitted ai_text + a citation_strip line) generated in-test"
    - "The no-live-path subprocess dormancy gate covers `import vibemix.memory.ingest` (closes the PATTERNS gap where it only imported memory.store)"
  artifacts:
    - path: "tests/memory/test_ingest.py"
      provides: "RED-first contract for the ingest module + synthetic fixture"
      contains: "test_signature_deterministic"
      min_lines: 120
    - path: "tests/memory/test_no_live_path_import.py"
      provides: "parallel subprocess dormancy assertion for vibemix.memory.ingest"
      contains: "vibemix.memory.ingest"
  key_links:
    - from: "tests/memory/test_ingest.py"
      to: "vibemix.memory.ingest"
      via: "import (RED — ModuleNotFoundError until 64-02)"
      pattern: "from vibemix.memory.ingest import|import vibemix.memory.ingest"
    - from: "tests/memory/test_ingest.py"
      to: "vibemix.memory.store.MemoryStore"
      via: "write-target round-trip assertions"
      pattern: "from vibemix.memory.store import MemoryStore"
---

<objective>
Author the RED-first test contract for the Phase 64 session-ingest module BEFORE any production code exists. This pins the entire ingest surface — the deterministic `coach_line` signature, the emitted-only filter (skip `citation_strip`), idempotent zero-cost re-ingest, record tagging, the signature-keyed embed-cache hit, and off-hot-path executor dispatch — as failing tests, plus a synthetic `events.jsonl` fixture mirroring the real on-disk corpus shapes. Also close the PATTERNS-flagged dormancy gap by extending the no-live-path subprocess gate to `vibemix.memory.ingest`.

Purpose: This is the deliverable of Wave 0 — the contract, not green tests. Plan 64-02 flips it GREEN. The contract removes interpretation: the executor of 64-02 builds against these exact identifiers and behaviors.
Output: `tests/memory/test_ingest.py` (new, fails with `ModuleNotFoundError: No module named 'vibemix.memory.ingest'` under `.venv`/Python 3.12), plus an added parallel subprocess assertion in `tests/memory/test_no_live_path_import.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-session-ingest/64-RESEARCH.md
@.planning/phases/64-session-ingest/64-PATTERNS.md
@.planning/phases/64-session-ingest/64-VALIDATION.md

<interfaces>
<!-- Contracts the test pins. These are the identifiers 64-02 implements against. -->
<!-- The MemoryStore write target (Phase 63, SHIPPED) — do NOT re-open the DB ad hoc. -->

From src/vibemix/memory/store.py (SHIPPED):
- class MemoryStore — constructed `MemoryStore(db_path=None, prefer_sqlite_vec=True)` (db_path=None → lazy app_data_dir resolution; tests pass tmp_path/"memory.db")
- def add_record(self, record_id: str, session_id: str, ts: float, kind: str, signature: str, embedding: np.ndarray) -> None
- def query_topk(self, query: np.ndarray, k: int, *, exclude_session: str | None = None) -> list  # returns Record-shaped rows
- def delete_session(self, session_id: str) -> int
- Record fields: record_id, session_id, ts, kind, signature, score

From src/vibemix/library/_cosine.py (SHIPPED):
- EMBEDDING_DIM = 768
- def l2_normalize(vec: np.ndarray) -> np.ndarray
- def cosine_topk(query, vectors, k) -> ...

From src/vibemix/library/embed.py (SHIPPED):
- class LibraryEmbedder — has `embed_query(self, query: str) -> np.ndarray` (NO content-hash cache — Finding 2), `self._model` (probe-derived id, never a literal), `EMBED_CACHE_DB_PATH`

<!-- The NEW module surface the test pins (does NOT exist yet → RED). 64-02 implements verbatim. -->
NEW in src/vibemix/memory/ingest.py (pinned by this test, built in 64-02):
- SIG_TEMPLATE_VERSION: str = "v1-coach_line"
- def build_coach_line_signature(reaction_text: str, ctx: dict | None) -> str
    returns "coach_line | track={track} | phase={phase} | deck={deck} | event={event_type} | cite={citation_tokens} | said: {reaction_text}"
    rules: track→"unknown" if absent, phase→"unknown", deck→"none", event_type→"MANUAL", citation tokens sorted+joined ",", leading [emotion] tag stripped, NO `t` in the string
- def ingest_session(session_dir: Path, store: MemoryStore, embedder) -> IngestResult
    reads events.jsonl, emits one coach_line record per EMITTED ai_text line (skip citation_strip), marker-gated idempotent
- def run_ingest_sweep(recordings_root: Path, store: MemoryStore, embedder) -> ...  # boot sweep, best-effort
- IngestResult — has at least `.embeds_made` (int) and `.records_written` (int) so the test asserts 0-cost re-ingest
</interfaces>

<read_first>
- tests/memory/test_store.py  (shape analog: `_vec(seed)` deterministic-vector helper at :40-43, tmp_path fixture, prefer_sqlite_vec=False numpy path at :53)
- tests/memory/test_retention.py  (shape analog: store construction + sweep call surface)
- tests/memory/test_no_live_path_import.py  (the file you EXTEND — the subprocess block at :95-130 imports ONLY vibemix.memory.store; add a parallel one for ingest)
- src/vibemix/memory/store.py  (MemoryStore.add_record / query_topk signatures — the write target, lines 215-345)
- src/vibemix/debrief/session_loader.py  (the on-disk events.jsonl line shapes the synthetic fixture mirrors — event lines: {t, kind:"event", type, track, phase, deck}; ai_text lines: {t, kind:"ai_text", text}; citation_strip: {t, kind:"citation_strip", raw_text})
- .planning/phases/64-session-ingest/64-RESEARCH.md §Signature Template Spec + §Validation Architecture (the Test Map is authoritative)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write RED test_ingest.py with synthetic fixture covering all 7 INGEST behaviors</name>
  <files>tests/memory/test_ingest.py</files>
  <behavior>
    - test_signature_deterministic: build_coach_line_signature(same fields) → byte-identical twice; missing track/phase/deck/event → "unknown"/"unknown"/"none"/"MANUAL"; citation tokens sorted; leading [chill]/[hype] emotion tag stripped from said:; `t` never appears in the returned string.
    - test_ingest_emits_coach_lines: ingest_session over the synthetic fixture writes ONE coach_line record per EMITTED ai_text line and SKIPS the citation_strip line (assert via store.query_topk / a moments count == number of ai_text lines, NOT including citation_strip).
    - test_reingest_is_noop: a second ingest_session on the same marked session makes 0 embed calls (assert on a fake embedder's call counter) and 0 new records.
    - test_records_tagged: every written record carries session_id == dir basename, a numeric ts, and kind == "coach_line".
    - test_embed_cache_hit: embedding the SAME signature twice → second call hits the signature-keyed cache → 0 new API calls (assert fake embedder counter unchanged across the cache boundary).
    - test_sweep_uses_executor: run_ingest_sweep over a recordings root with two session dirs ingests both; a session dir name failing SESSION_DIR_RE (e.g. "..", "evil/") is skipped (path-traversal defense) and never read.
  </behavior>
  <action>Create tests/memory/test_ingest.py mirroring tests/memory/test_store.py shape. Import `from vibemix.library._cosine import EMBEDDING_DIM, l2_normalize`, `from vibemix.memory.store import MemoryStore`, and `from vibemix.memory.ingest import build_coach_line_signature, ingest_session, run_ingest_sweep, SIG_TEMPLATE_VERSION` (this import is the RED edge — it raises ModuleNotFoundError until 64-02 lands). Clone the `_vec(seed)` deterministic-vector helper. Build a synthetic events.jsonl fixture IN-TEST (write to tmp_path/session_dir/events.jsonl) mirroring real shapes: at least two `{"t":.., "kind":"event", "type":"PHASE"|"TRACK_CHANGE", "track":..., "phase":"groove", "deck":"B"}` lines, two `{"t":.., "kind":"ai_text", "text":"[chill] ... [aud:rms@96.0] ..."}` lines (one with an inline citation, one with only the [chill] tag), and one `{"t":.., "kind":"citation_strip", "raw_text":"..."}` line that MUST be skipped. Use a FakeEmbedder stub with a call counter returning `_vec(seed)` from `embed_query` (no live API). Provide both prefer_sqlite_vec=False (numpy) construction for the store. Cover the 6 named test fns above. Per VALIDATION the citation_strip-skip assertion lives inside test_ingest_emits_coach_lines. Do NOT implement any production logic here — tests only. No fenced code in the production module; this task writes test code only.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_ingest.py 2>&1 | grep -qE "ModuleNotFoundError.*vibemix.memory.ingest|No module named 'vibemix.memory.ingest'" && echo RED-CONTRACT-PINNED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_signature_deterministic\|def test_ingest_emits_coach_lines\|def test_reingest_is_noop\|def test_records_tagged\|def test_embed_cache_hit\|def test_sweep_uses_executor" tests/memory/test_ingest.py` returns 6
    - `grep -q "from vibemix.memory.ingest import" tests/memory/test_ingest.py` (the RED import edge)
    - `grep -q "citation_strip" tests/memory/test_ingest.py` (the skip assertion exists)
    - Collection fails ONLY with ModuleNotFoundError on vibemix.memory.ingest (verified under .venv/Python 3.12 — the authoritative runner; system Python 3.14 masks unrelated import gaps)
  </acceptance_criteria>
  <done>tests/memory/test_ingest.py exists with all 6 test functions + the in-test synthetic fixture; the suite is RED with exactly the ModuleNotFoundError edge.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the no-live-path subprocess dormancy gate to vibemix.memory.ingest</name>
  <files>tests/memory/test_no_live_path_import.py</files>
  <action>Per the PATTERNS gap (64-PATTERNS.md §No-live-path import boundary): the existing subprocess dormancy test (`test_importing_memory_loads_no_coach_loop`) imports ONLY `vibemix.memory.store`. Add a parallel test function `test_importing_ingest_loads_no_coach_loop` that clones the same subprocess sys.modules leak-scan block verbatim but swaps the import line to `import vibemix.memory.ingest`. Same leak predicate (startswith vibemix.state.coach / vibemix.state.refresh / vibemix.agent / vibemix.prompts / 'ws_bus' in m), same `assert result.stdout.strip() == "CLEAN"`. This is RED-first too: until 64-02 lands, the subprocess import fails non-zero and stdout != "CLEAN". The static AST gate (which globs memory/*.py) already auto-covers ingest.py — do NOT duplicate that; only add the runtime-dormancy subprocess parity.</action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && grep -q "import vibemix.memory.ingest" tests/memory/test_no_live_path_import.py && grep -c "def test_importing.*loads_no_coach_loop" tests/memory/test_no_live_path_import.py | grep -q 2 && echo INGEST-DORMANCY-ADDED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_importing_memory_loads_no_coach_loop\|def test_importing_ingest_loads_no_coach_loop" tests/memory/test_no_live_path_import.py` returns 2
    - `grep -q "import vibemix.memory.ingest" tests/memory/test_no_live_path_import.py`
    - The new test asserts `result.stdout.strip() == "CLEAN"` (same contract shape as the store version)
  </acceptance_criteria>
  <done>A second subprocess dormancy assertion covering `import vibemix.memory.ingest` exists; RED until 64-02 makes the module importable, then it must print CLEAN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → ingest module | Synthetic events.jsonl is trusted test input; the REAL boundary (untrusted session dir names + crash-truncated JSONL) is exercised by the sweep/path-traversal test, pinning the defense 64-02 must implement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Spoofing | Confabulation via a generation call in ingest | mitigate | This plan does not author ingest.py; the no-extraction gate (shipped, globs memory/*.py) is pinned to fire once ingest.py lands. The RED contract here forbids any embedder beyond a FakeEmbedder.embed_query stub — no generation surface in tests. |
| T-64-02 | Tampering | Live-path coupling via ingest import | mitigate | Task 2 extends the subprocess dormancy gate to `import vibemix.memory.ingest` (closes the PATTERNS gap) — pins the one-way runtime→ingest arrow as a RED-then-CLEAN assertion. |
| T-64-03 | Tampering / EoP | Path traversal via crafted session dir name (boot sweep) | mitigate | test_sweep_uses_executor pins that a `..`/`evil/`-shaped session dir is skipped (SESSION_DIR_RE + is_relative_to) and never read — 64-02 must satisfy it. |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | Zero net-new dependencies (RESEARCH §Package Legitimacy Audit — no install step); stdlib + shipped in-repo modules only. |
</threat_model>

<verification>
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/memory/test_ingest.py` → RED with ModuleNotFoundError on vibemix.memory.ingest (the contract is pinned, NOT green — that is correct for Wave 0).
- `PYTHONPATH=src python3 -m pytest -q tests/memory/test_no_live_path_import.py` → the new ingest-dormancy test is RED (import fails) until 64-02; the existing store-dormancy test stays GREEN.
- No production module created in this plan.
</verification>

<success_criteria>
- tests/memory/test_ingest.py exists with the 6 named test functions + an in-test synthetic events.jsonl fixture (event + ai_text + citation_strip lines).
- The RED edge is exactly `ModuleNotFoundError: No module named 'vibemix.memory.ingest'` under .venv/Python 3.12.
- tests/memory/test_no_live_path_import.py carries a parallel subprocess dormancy assertion for `vibemix.memory.ingest`.
- No `gemini-embedding` literal, no generation-surface token in any test file (the FakeEmbedder exposes only `embed_query`).
</success_criteria>

<output>
Create `.planning/phases/64-session-ingest/64-01-SUMMARY.md` when done
</output>
