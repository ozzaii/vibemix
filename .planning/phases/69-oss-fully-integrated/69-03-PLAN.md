---
phase: 69-oss-fully-integrated
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/vibemix/agent/proxy_client.py
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/runtime/session_loop.py
  - tests/integration/test_proxy_fallback.py
  - KAAN-ACTION-LEGAL.md
requirements: [OSS-02]
autonomous: true

must_haves:
  truths:
    - "When the Bravoh proxy returns 5xx (502/503/504), times out, or connection is refused, the coach surface emits a one-shot 'Co-host unavailable this session' transcript line and skips LLM emission for the duration."
    - "When the proxy returns 4xx/429, the existing per-error messaging path is preserved (NOT conflated with 'unavailable')."
    - "When the proxy recovers (a periodic /health canary returns 200), a single one-shot 'Co-host back online' transcript line is emitted and LLM emission resumes."
    - "No retries on the 4 trigger classes — refusing to lie when grounding is missing is on-thesis (anti-slop, never silently retry)."
    - "Server-side hardening (per-install-UUID rate limit + token-bucket + Prom /metrics + Sentry) is documented in KAAN-ACTION-LEGAL.md §V7-PROXY with pre-staged spec; engineering closes when the client-side fallback test is GREEN."
  artifacts:
    - path: "src/vibemix/agent/proxy_client.py"
      provides: "ProxyUnavailable sentinel exception class + thin try/except wrapper helper that classifies the 4 trigger classes."
      contains: "class ProxyUnavailable(Exception)"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "Narrowly-scoped try/except around the generate_content_stream call (line ~1233) that catches ProxyUnavailable + raises through 4xx unchanged."
    - path: "src/vibemix/runtime/session_loop.py"
      provides: "Orchestration: proxy_unavailable flag + 60s /health canary helper + one-shot transcript line emissions."
    - path: "tests/integration/test_proxy_fallback.py"
      provides: "4 parametrized cases: 5xx / timeout / connect-refused / recovery."
      contains: "test_proxy_503_emits_unavailable_transcript"
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "§V7-PROXY cluster (NEW, 6-section template) with pre-staged server-side hardening spec for the Bravoh ops repo."
      contains: "## §V7-PROXY"
  key_links:
    - from: "src/vibemix/agent/dj_cohost.py"
      to: "src/vibemix/agent/proxy_client.py::ProxyUnavailable"
      via: "try/except around generate_content_stream"
      pattern: "except ProxyUnavailable"
    - from: "src/vibemix/runtime/session_loop.py"
      to: "src/vibemix/agent/proxy_client.py::probe_proxy_health"
      via: "60s canary call to proxy_base_url + /health"
      pattern: "probe_proxy_health"
    - from: "tests/integration/test_proxy_fallback.py"
      to: "src/vibemix/agent/proxy_client.py"
      via: "monkeypatch httpx transport + parametrize over 4 trigger classes"
      pattern: "@pytest.mark.parametrize"
---

<objective>
Ship the client-side graceful fallback for v7.0 OSS-02: when the Bravoh proxy is unreachable (5xx / timeout / connect-refused / non-JSON body), the coach surface emits a one-shot "Co-host unavailable this session" line, skips LLM emission for the duration, and recovers cleanly via a 60s /health canary. The server-side hardening (per-install-UUID rate limit, token-bucket, Prom /metrics, Sentry) routes to a new KAAN-ACTION-LEGAL.md §V7-PROXY cluster — the Bravoh ops repo's clock.

Purpose: When the proxy is offline, the brain refuses to lie. NOT a crash, NOT a hallucinated coach line. The brain refusing to lie when grounding is missing is on-thesis (anti-slop). 4xx / 429 paths are preserved unchanged (those have their own existing per-error messaging from prior v3.x SHIP-CUT work — must NOT be conflated with "unavailable").

Output: src/vibemix/agent/proxy_client.py extended (ProxyUnavailable sentinel + classification helper + probe_proxy_health helper); src/vibemix/agent/dj_cohost.py minimal try/except wrap; src/vibemix/runtime/session_loop.py orchestration; tests/integration/test_proxy_fallback.py (NEW, 4 parametrized cases, @pytest.mark.integration); KAAN-ACTION-LEGAL.md §V7-PROXY cluster (NEW, 6-section template, appended after the existing §V7-LIVE section). Wave 2, no inter-wave dependencies.

**Cardinal invariant compliance (CONTEXT.md zero-touch):** This is the ONE narrowly-scoped touch to `src/vibemix/agent/` + `src/vibemix/runtime/` permitted by CONTEXT.md ("the minimum needed for OSS-02 client-side fallback (likely 1 try/except in the proxy client + 1 pill emission string)"). No coach prompts modified, no evidence sources added, no new ws ports, no new IPC envelopes. The Literal["LISTENING", "TALKING", "IDLE"] cohost_status schema is NOT extended (out of scope — UNAVAILABLE surfaces via transcript_delta + an internal runtime flag, not via a new schema literal). The four cardinal invariants (single-writer / citation-grounding / trust-the-audio / one-socket) hold by construction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-oss-fully-integrated/69-CONTEXT.md
@src/vibemix/agent/proxy_client.py
@src/vibemix/agent/dj_cohost.py
@src/vibemix/runtime/session_loop.py
@src/vibemix/ui_bus/messages.py
@KAAN-ACTION-LEGAL.md
@tests/integration/test_hotplug_matrix.py
@tests/integration/test_audio_backends.py

<interfaces>
<!-- proxy_client.py current surface (read in full above): -->
<!--   - build_proxy_genai_client(jwt: str, proxy_base_url: str) -> genai.Client -->
<!--   - build_proxy_tts_chain(jwt: str, proxy_base_url: str, voice: str = VOICE) -> agents_tts.FallbackAdapter -->
<!-- Pre-existing top-level file: 68 lines. No exception classes defined yet — ProxyUnavailable is NEW. -->

<!-- dj_cohost.py call site (line ~1233): -->
<!--   stream = await self._genai_client.aio.models.generate_content_stream(model=LLM_MODEL, contents=contents, config=gen_cfg) -->
<!-- The Google genai SDK raises google.genai.errors.* on 5xx / 4xx / network. The httpx layer (used by genai under the hood) raises httpx.TimeoutException / httpx.ConnectError on network failures. -->

<!-- session_loop.py current state: -->
<!--   - Line 842: VIBEMIX_LLM_MODE env-var branch (direct vs proxy) — proxy mode is opt-in. -->
<!--   - cohost_status surface (lines 1075-1086): Literal["LISTENING", "TALKING", "IDLE"] — DO NOT extend the Literal. -->
<!--   - transcript_delta (line 1102): tuple[TranscriptLine, ...] — additive surface for one-shot "Co-host unavailable" line. -->
<!--   - The session loop's hot path is `run_one_turn` (per cohost.py mainline); the orchestration this plan adds is a 60s timer + a flag. -->

<!-- ui_bus/messages.py (line 308): cohost_status: Literal["LISTENING", "TALKING", "IDLE"]. -->
<!--   - TranscriptLine type at /Users/ozai/projects/dj-set-ai/src/vibemix/ui_bus/messages.py — read to find the exact factory signature for emitting a transcript line. -->

<!-- KAAN-ACTION-LEGAL.md current state: -->
<!--   - §SHIP-V4 at line 3389 -->
<!--   - §V7-LIVE section starts around line 3653 with 10 clusters (§V7-LIVE-01..§V7-LIVE-10) + Discharge tracking table + Sign-off block (last sign-off line: V7-LIVE-10 at line 4166) -->
<!--   - Plan 69-02 Wave 1 (if shipped first) adds §V7-LIVE-11 BYO walk after §V7-LIVE-10. -->
<!--   - §V7-PROXY is a NEW top-level section (mirroring §V7-LIVE / §SHIP-V4 shape). Place AFTER the §V7-LIVE section closes (after its Sign-off block) so it gets its own clean cluster surface. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend proxy_client.py with ProxyUnavailable + classification + /health probe</name>
  <files>src/vibemix/agent/proxy_client.py</files>
  <behavior>
    - class ProxyUnavailable(Exception): subclass of Exception with a single `reason: str` attribute (one of: "5xx", "timeout", "connection_refused", "bad_body").
    - classify_proxy_error(exc: Exception) -> ProxyUnavailable | None: takes a raised exception, returns a ProxyUnavailable instance if the exception matches one of the 4 trigger classes (5xx response, timeout, connection refused, non-JSON body when JSON expected), else returns None (so the caller raises the original exception unchanged — 4xx/429 fall through).
    - probe_proxy_health(proxy_base_url: str, timeout_s: float = 5.0) -> bool: makes a synchronous httpx GET to `{proxy_base_url.rstrip('/')}/health`, returns True on HTTP 200, False on anything else (including network errors). NEVER raises. Used as the 60s recovery canary.
    - All three additions ADD to proxy_client.py (do NOT replace anything; existing build_proxy_genai_client + build_proxy_tts_chain stay byte-unchanged).
    - Module-level docstring extended with one paragraph describing the OSS-02 fallback contract.
  </behavior>
  <read_first>
    - src/vibemix/agent/proxy_client.py (current 68-line file — read in full; the additions go AFTER existing functions, before EOF)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "Client-Side Proxy Fallback (OSS-02 — vibemix side)" section under <decisions> spells out the 4 trigger classes + the NOT-triggered-by 4xx/429 rule)
    - src/vibemix/agent/__init__.py (existing public-export list — add ProxyUnavailable + classify_proxy_error + probe_proxy_health if exported)
  </read_first>
  <action>
Open src/vibemix/agent/proxy_client.py. Keep lines 1-67 (existing build_proxy_genai_client + build_proxy_tts_chain) byte-unchanged. Append the following new surface AFTER line 67:

1. **`class ProxyUnavailable(Exception)`** — a sentinel exception with an `__init__(self, reason: str, original: Exception | None = None)` that stores `self.reason: str` and `self.original: Exception | None`. `__repr__` returns `f"ProxyUnavailable(reason={self.reason!r})"`. Module docstring or class docstring documents the 4 reason values: "5xx" / "timeout" / "connection_refused" / "bad_body".

2. **`def classify_proxy_error(exc: Exception) -> ProxyUnavailable | None:`** — a pure function that inspects the passed exception and returns a `ProxyUnavailable` instance if the exception matches one of the 4 trigger classes. Detection logic (executor consults `httpx` + `google.genai.errors` for exact class names; the genai SDK wraps httpx errors in its own `APIError` / `ClientError` hierarchy — check the imported genai version at runtime):
   - **5xx detection:** if exc is a `genai.errors.APIError` (or similar httpx-derived) with `.status_code` attribute in {500, 502, 503, 504} → return `ProxyUnavailable("5xx", exc)`. Critically: 429 and 4xx ranges OTHER than these MUST NOT match (return None — caller re-raises).
   - **Timeout detection:** if exc is `httpx.TimeoutException` (or its subclasses: `httpx.ConnectTimeout`, `httpx.ReadTimeout`, `httpx.WriteTimeout`, `httpx.PoolTimeout`) → return `ProxyUnavailable("timeout", exc)`.
   - **Connection refused:** if exc is `httpx.ConnectError` (typically wraps `ConnectionRefusedError`) → return `ProxyUnavailable("connection_refused", exc)`.
   - **Bad body:** if exc is `json.JSONDecodeError` OR a genai-SDK wrapper signaling non-JSON body when JSON expected (executor inspects the SDK shape at write time and picks the right class; if unsure, document the inference in a code comment and target the most-likely class) → return `ProxyUnavailable("bad_body", exc)`.
   - Anything else (including 4xx other than 5xx range): return None.
   - **CRITICAL:** any AssertionError or KeyboardInterrupt or SystemExit MUST NOT be matched — those are flow control, not network errors. The function ONLY classifies network/transport errors.

3. **`def probe_proxy_health(proxy_base_url: str, timeout_s: float = 5.0) -> bool:`** — synchronous best-effort canary. Uses `import httpx` (already a transitive dep via genai). Body builds `url = f"{proxy_base_url.rstrip('/')}/health"`, opens `httpx.Client(timeout=timeout_s)` in a `with` block, calls `client.get(url)`, returns True iff `resp.status_code == 200`. Wraps everything in `try/except Exception: return False` — NEVER raises (caller treats False as "still unavailable"). Add a docstring noting: "If the proxy has no `/health` endpoint yet (Bravoh ops repo work — see §V7-PROXY), this returns False perpetually and the recovery 'Co-host back online' line never fires; this is acceptable failure mode — the next real LLM call will succeed once the proxy is up, and the unavailable flag clears on success (NOT on /health). When §V7-PROXY ships /health on api.altidus.world, the recovery line fires automatically without any client-side change."

Update `src/vibemix/agent/__init__.py` to export the three new names (ProxyUnavailable, classify_proxy_error, probe_proxy_health) — append to existing `__all__` if present.

Module docstring of proxy_client.py: append one paragraph documenting the OSS-02 fallback contract: "Phase 69 Plan 69-03 (OSS-02): when the proxy returns 5xx, times out, or connection is refused, classify_proxy_error returns a ProxyUnavailable sentinel; the caller (dj_cohost.py) catches it and the runtime (session_loop.py) emits a one-shot 'Co-host unavailable this session' transcript line + skips LLM emission for the duration. 4xx and 429 are NOT classified — they fall through to existing per-error messaging."
  </action>
  <verify>
    <automated>uv run python -c "from vibemix.agent.proxy_client import ProxyUnavailable, classify_proxy_error, probe_proxy_health; import httpx; assert classify_proxy_error(httpx.ConnectError('refused')).reason == 'connection_refused'; assert classify_proxy_error(ValueError('not net')) is None; assert classify_proxy_error(httpx.ReadTimeout('slow')).reason == 'timeout'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/vibemix/agent/proxy_client.py contains `class ProxyUnavailable(Exception)` with `reason: str` attribute.
    - `classify_proxy_error(httpx.ConnectError('refused'))` returns a ProxyUnavailable with reason == "connection_refused".
    - `classify_proxy_error(httpx.ReadTimeout('slow'))` returns a ProxyUnavailable with reason == "timeout".
    - `classify_proxy_error(ValueError('not network'))` returns None.
    - `classify_proxy_error(<a 4xx error>)` returns None (4xx MUST NOT classify — verified in Task 3 test).
    - `probe_proxy_health` never raises; returns False on any network error or non-200.
    - Existing `build_proxy_genai_client` + `build_proxy_tts_chain` are byte-unchanged (`git diff` shows only additions after line 67).
    - `src/vibemix/agent/__init__.py` exports the 3 new names (importable as `from vibemix.agent import ProxyUnavailable, ...`).
    - Default `uv run pytest -q` baseline: 0 regressions (this task adds no tests yet; Task 3 adds them).
  </acceptance_criteria>
  <done>proxy_client.py exports ProxyUnavailable + classify_proxy_error + probe_proxy_health; the public smoke import + classify-4-trigger-classes round-trip works; existing builders are byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire dj_cohost.py + session_loop.py to honor ProxyUnavailable</name>
  <files>src/vibemix/agent/dj_cohost.py, src/vibemix/runtime/session_loop.py</files>
  <behavior>
    - dj_cohost.py: the generate_content_stream call at line ~1233 is wrapped in a try/except. On caught Exception, call `classify_proxy_error(exc)`; if it returns a ProxyUnavailable, RAISE the ProxyUnavailable (re-raise as a typed exception the caller can catch); if it returns None, re-raise the original exception unchanged (4xx / 429 / programming errors fall through as before).
    - session_loop.py: catches ProxyUnavailable around the dj_cohost call. On first catch in a session: (a) emits a one-shot transcript_delta line "Co-host unavailable this session" via the existing transcript-line mechanism (executor picks the exact code path — typically appending a `TranscriptLine` to `self._transcript_unsent`), (b) sets a session-local `self._proxy_unavailable: bool = True` flag, (c) starts (or continues) a 60s periodic `probe_proxy_health(proxy_url)` canary task.
    - While `self._proxy_unavailable` is True: every coach loop tick skips the LLM call (do NOT call dj_cohost.generate; just continue collecting state + meters + evidence packets). NO retries on the proxy. The session loop continues running otherwise (meters update, MIDI events flow, mascot frame still emits with cohost_status="IDLE" since no LLM voice is producing).
    - On the 60s canary returning True: emit a one-shot transcript_delta line "Co-host back online", clear the flag, resume normal LLM emission on the next tick.
    - The "Co-host unavailable this session" line is emitted EXACTLY ONCE per unavailable-streak; subsequent loop ticks while still unavailable do NOT spam the transcript.
    - The "Co-host back online" line is emitted EXACTLY ONCE per recovery; subsequent successful LLM calls do NOT spam.
    - NO new ws ports. NO new IPC envelope. NO new schema literal (cohost_status stays Literal["LISTENING", "TALKING", "IDLE"] — the unavailable state surfaces via transcript_delta only).
  </behavior>
  <read_first>
    - src/vibemix/agent/dj_cohost.py lines 1220-1260 (the existing generate_content_stream call site — find the exact try-able boundary; the stream is iterated `async for chunk in stream`, so the try block must wrap both the await + the iteration)
    - src/vibemix/runtime/session_loop.py lines 1050-1140 (the existing snapshot-emit + cohost_status assignment; the unavailable flag belongs as a self-attribute on the session loop instance)
    - src/vibemix/runtime/session_loop.py lines 840-860 (the existing VIBEMIX_LLM_MODE branch — the proxy_url for probe_proxy_health is the same URL the build_proxy_genai_client gets)
    - src/vibemix/agent/proxy_client.py (Task 1 deliverable — the executor must read this AFTER Task 1 to get the actual symbol names)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "Coach loop behavior on fallback" + "Fallback surface" decisions under <decisions> spell out the one-shot + 60s canary contract)
  </read_first>
  <action>
**Sub-task 2a — dj_cohost.py (narrow wrap):** Find the generate_content_stream call at line ~1233 (the executor greps `generate_content_stream` in the file to locate exactly; line numbers may have drifted). Wrap the `stream = await ...generate_content_stream(...)` + the entire `async for chunk in stream:` block in a single try/except. Imports: add `from vibemix.agent.proxy_client import classify_proxy_error, ProxyUnavailable` at the top of dj_cohost.py (in the appropriate import section per existing conventions). The except clause structure:

- `except Exception as exc:` — outer broad catch
- `unavail = classify_proxy_error(exc)` — call the classifier
- `if unavail is not None: raise unavail from exc` — re-raise as the typed sentinel; preserves original via `__cause__`
- else: `raise` — re-raise original exception unchanged (4xx/429/programming errors fall through)

Do NOT add any other behavior to dj_cohost.py — the runtime decides what to do with the ProxyUnavailable. dj_cohost stays a thin call site.

**Sub-task 2b — session_loop.py (orchestration):** Find the call that invokes dj_cohost's coach turn (likely in `run_one_turn` or `coach_loop` — the executor greps for the call site). Add 4 instance attributes to the session-loop class's `__init__`:
- `self._proxy_unavailable: bool = False`
- `self._proxy_unavailable_message_emitted: bool = False` (one-shot guard)
- `self._proxy_recovery_message_emitted: bool = False` (one-shot guard for recovery)
- `self._last_proxy_health_probe: float = 0.0` (monotonic-time stamp for the 60s canary; use `time.monotonic()`)
- `self._proxy_base_url: str | None = None` (resolved from env at the same point the existing proxy_url is resolved at line ~851; None in direct mode)

Wrap the dj_cohost coach-call in a `try/except ProxyUnavailable as unavail:` block. On catch: if not already armed (`not self._proxy_unavailable`), arm the flag + reset both one-shot guards; if "Co-host unavailable this session" hasn't been emitted in this unavailable-streak, call `self._emit_transcript_line("Co-host unavailable this session")` and set the emitted guard; log warning to stderr + events.jsonl per existing patterns; do NOT retry.

Add a guard BEFORE the coach call: if `self._proxy_unavailable` is True AND `self._proxy_base_url is not None`, check whether 60s has elapsed since `self._last_proxy_health_probe` via `time.monotonic()`. If yes, fire `probe_proxy_health(self._proxy_base_url)` (synchronously — it's a 5s-timeout best-effort canary). On True: emit "Co-host back online" exactly once via the one-shot recovery guard, clear `self._proxy_unavailable`, clear the unavailable-message guard, log "[proxy] recovered" to events.jsonl. Either way (True or False), update `self._last_proxy_health_probe = now`. If `self._proxy_unavailable` is True at the end of the guard, `return` early — skip the coach call this tick.

Define `_emit_transcript_line(self, text: str) -> None` as a thin helper that constructs a `TranscriptLine` (or equivalent type — executor checks `vibemix.ui_bus.messages` for the exact factory) with the text and appends to `self._transcript_unsent` so the next snapshot picks it up via the existing `transcript_delta` mechanism (line ~1103). Reuse any existing helper if one is already present in session_loop.py.

If `VIBEMIX_LLM_MODE != "proxy"` (i.e. direct mode / BYO), the unavailable flag should never arm — store `self._proxy_base_url = None` at init time and gate every flag-arming branch on `self._proxy_base_url is not None`. In direct mode, network errors to `generativelanguage.googleapis.com` fall through unchanged (BYO users see their own quota / network errors directly per existing behavior).
  </action>
  <verify>
    <automated>uv run pytest -q --tb=short 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - dj_cohost.py: the `generate_content_stream` call site is wrapped in a try/except that calls `classify_proxy_error` and re-raises ProxyUnavailable; 4xx / 429 / non-network errors fall through unchanged.
    - dj_cohost.py: `from vibemix.agent.proxy_client import classify_proxy_error, ProxyUnavailable` added (no other imports changed).
    - session_loop.py: 5 new instance attributes (`_proxy_unavailable`, `_proxy_unavailable_message_emitted`, `_proxy_recovery_message_emitted`, `_last_proxy_health_probe`, `_proxy_base_url`) initialized in `__init__`.
    - session_loop.py: coach-call site wraps ProxyUnavailable, emits the one-shot transcript line, skips LLM emission for the duration.
    - session_loop.py: 60s canary path uses `probe_proxy_health(self._proxy_base_url)`; on True clears flag + emits one-shot "Co-host back online" line.
    - session_loop.py: in direct mode (`VIBEMIX_LLM_MODE != "proxy"`), the unavailable flag NEVER arms (BYO users see their own network errors per existing behavior).
    - cohost_status Literal["LISTENING", "TALKING", "IDLE"] schema in ui_bus/messages.py is UNCHANGED.
    - Default `uv run pytest -q` baseline: 0 regressions (existing tests for dj_cohost, session_loop, snapshot shape all pass).
    - `git diff --stat -- src/vibemix/coach/ src/vibemix/state/ src/vibemix/memory/ src/vibemix/recall/ src/vibemix/decks/ src/vibemix/grounding/` is empty (cardinal invariant zero-touch on those modules verified).
  </acceptance_criteria>
  <done>dj_cohost.py + session_loop.py both honor ProxyUnavailable; existing default pytest grid passes with 0 regressions; the unavailable state surfaces via transcript_delta one-shot (NOT via schema change).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/integration/test_proxy_fallback.py with the 4 trigger classes + recovery + 4xx-fall-through + direct-mode-bypass</name>
  <files>tests/integration/test_proxy_fallback.py</files>
  <behavior>
    - Unit-level classifier tests (5 functions): test_classify_proxy_error_5xx_returns_unavailable, test_classify_proxy_error_timeout_returns_unavailable, test_classify_proxy_error_connection_refused_returns_unavailable, test_classify_proxy_error_4xx_returns_none, test_classify_proxy_error_random_exception_returns_none.
    - Probe-health tests (2 functions): test_probe_proxy_health_returns_false_on_network_error, test_probe_proxy_health_returns_true_on_200.
    - SessionLoop-level integration tests (3 functions, one parametrized over 3 trigger reasons): test_session_loop_emits_unavailable_one_shot[5xx/timeout/connection_refused], test_session_loop_recovery_emits_back_online_one_shot, test_session_loop_direct_mode_does_not_arm_unavailable.
    - All tests carry `@pytest.mark.integration` decorator (opt-in marker — keeps default grid unaffected).
    - File follows tests/integration/ idiom: SPDX header, `from __future__ import annotations`, monkeypatch fixture, helpers prefixed `_`, total ≤ 280 lines.
  </behavior>
  <read_first>
    - tests/integration/test_hotplug_matrix.py (existing integration-test idiom — `from __future__ import annotations`, SPDX header, helpers, `@pytest.mark.integration`, parametrize pattern)
    - tests/integration/test_audio_backends.py (existing pattern for monkeypatching with sys.modules + fake-class injection — mirror this for the genai client mock)
    - src/vibemix/agent/proxy_client.py (Task 1 deliverable — read for the public surface)
    - src/vibemix/runtime/session_loop.py (Task 2 deliverable — read for the SessionLoop class shape + the helper `_emit_transcript_line`)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "Test shape" bullet under <decisions> lists the 4 required cases + the 4xx-not-triggered rule)
  </read_first>
  <action>
Create `tests/integration/test_proxy_fallback.py` (NEW, ≤280 lines) with this structure:

Top-of-file (lines 1-30): SPDX header + module docstring + `from __future__ import annotations` + imports (`import json`, `import time`, `from unittest.mock import MagicMock, patch`, `import httpx`, `import pytest`) + imports from vibemix (`from vibemix.agent.proxy_client import ProxyUnavailable, classify_proxy_error, probe_proxy_health`).

The 5 classifier tests (lines 30-110):

- `def test_classify_proxy_error_5xx_returns_unavailable():` — synthesize mock exceptions with `status_code` attribute set to 500, 502, 503, 504 in turn (executor picks the most realistic exception class — likely a `genai.errors.APIError` subclass with `code` or `status_code` attribute; if the genai SDK shape is unclear, use `httpx.HTTPStatusError` constructed from a `httpx.Response(status_code=503)`). Assert `classify_proxy_error(exc).reason == "5xx"` for each.

- `def test_classify_proxy_error_timeout_returns_unavailable():` — assert each of `httpx.TimeoutException("t")`, `httpx.ConnectTimeout("t")`, `httpx.ReadTimeout("t")` is classified with `reason == "timeout"`.

- `def test_classify_proxy_error_connection_refused_returns_unavailable():` — assert `httpx.ConnectError("refused")` classifies with `reason == "connection_refused"`.

- `def test_classify_proxy_error_4xx_returns_none():` — build mock exceptions with status_code=401, 403, 404, 429 — each MUST return None from `classify_proxy_error`. **CRITICAL anti-regression test** — this pins the "do NOT conflate with unavailable" boundary.

- `def test_classify_proxy_error_random_exception_returns_none():` — assert `ValueError("x")`, `RuntimeError("y")`, `KeyError("z")`, `AssertionError("a")` all return None. (KeyboardInterrupt + SystemExit are flow control — do not test directly with pytest as they tear down the runner; document the contract in a comment.)

The 2 probe-health tests (lines 110-160):

- `@pytest.mark.integration` `def test_probe_proxy_health_returns_false_on_network_error(monkeypatch):` — Monkeypatch `httpx.Client.__enter__` (or use `httpx.MockTransport` with a handler that raises `httpx.ConnectError`) so `.get` raises. Assert `probe_proxy_health("https://example.invalid") is False` and NEVER raises.

- `@pytest.mark.integration` `def test_probe_proxy_health_returns_true_on_200(monkeypatch):` — Use `httpx.MockTransport` with a handler that returns `httpx.Response(status_code=200)`. Patch `httpx.Client` constructor to inject the mock transport. Assert `probe_proxy_health("https://example.invalid") is True`.

The 3 SessionLoop-level tests (lines 160-280):

Build a `_build_session_loop_for_test(monkeypatch, mode="proxy")` helper that:
- Sets `VIBEMIX_LLM_MODE=mode` (default "proxy")
- Sets `VIBEMIX_PROXY_BASE_URL=https://example.invalid` + `VIBEMIX_PROXY_JWT=fake-jwt`
- Constructs the SessionLoop instance with minimum stub dependencies (executor consults session_loop.py's `__init__` signature and provides MagicMock stubs for anything required)
- Returns the SessionLoop instance + a list to capture emitted transcript lines (by monkeypatching `_emit_transcript_line` to append to the list)

Test 1: `@pytest.mark.integration` `@pytest.mark.parametrize("trigger_reason,exc_factory", [("5xx", _make_5xx_exc), ("timeout", lambda: httpx.ReadTimeout("t")), ("connection_refused", lambda: httpx.ConnectError("r"))])` `async def test_session_loop_emits_unavailable_one_shot(monkeypatch, trigger_reason, exc_factory):`
- Build SessionLoop fixture in proxy mode.
- Stub the dj_cohost coach call to raise `classify_proxy_error(exc_factory())` (a ProxyUnavailable) on every invocation.
- Drive ONE coach tick.
- Assert: emitted transcript captures contains exactly `["Co-host unavailable this session"]`.
- Drive a SECOND tick under the same conditions.
- Assert: capture STILL `["Co-host unavailable this session"]` (one-shot — no duplicate).

Test 2: `@pytest.mark.integration` `async def test_session_loop_recovery_emits_back_online_one_shot(monkeypatch):`
- Build SessionLoop fixture in proxy mode.
- Stub dj_cohost to raise ProxyUnavailable on tick 1; monkeypatch `time.monotonic` to advance by 61.0s between ticks; monkeypatch `probe_proxy_health` to return True on tick 2.
- Drive tick 1: assert `["Co-host unavailable this session"]`.
- Drive tick 2: assert `["Co-host unavailable this session", "Co-host back online"]`.
- Drive tick 3 (successful coach call): assert NO new entries (recovery is one-shot).

Test 3: `@pytest.mark.integration` `async def test_session_loop_direct_mode_does_not_arm_unavailable(monkeypatch):`
- Build SessionLoop fixture in `mode="direct"` (BYO).
- Stub dj_cohost to raise a network error that WOULD classify as ProxyUnavailable.
- Assert: the error propagates (or the call site handles per existing direct-mode logic — but the unavailable flag is NEVER set; transcript captures is empty for "Co-host unavailable").

File totals ≤ 280 lines; every test under `@pytest.mark.integration`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_proxy_fallback.py -m integration -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File tests/integration/test_proxy_fallback.py exists with `# SPDX-License-Identifier: Apache-2.0` on line 1.
    - File length ≤ 280 lines.
    - Every test carries `@pytest.mark.integration` decorator.
    - `uv run pytest tests/integration/test_proxy_fallback.py -m integration -q` exits 0 with at least 10 passed (5 classifier + 2 probe + 3 session-loop, with parametrize on test 8 counting as 3 rows).
    - Default-grid `uv run pytest -q` baseline: integration-marker tests are opt-in → default grid unaffected by Task 3.
    - `uv run pytest -m integration -q` baseline gains at least 10 tests (was 16 from P68; new total ≥ 26).
    - All 4 trigger classes covered (5xx / timeout / connection-refused + recovery via /health).
    - The anti-regression test (4xx does NOT trigger) is present and GREEN.
    - The direct-mode (BYO) test asserts the flag never arms — defends the boundary against future refactors.
  </acceptance_criteria>
  <done>tests/integration/test_proxy_fallback.py exists, all 10+ tests GREEN under `pytest -m integration`, default grid unaffected, the 4-trigger contract + recovery + 4xx-fall-through + direct-mode-bypass all pinned.</done>
</task>

<task type="auto">
  <name>Task 4: Append §V7-PROXY cluster to KAAN-ACTION-LEGAL.md</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <read_first>
    - KAAN-ACTION-LEGAL.md §V7-LIVE-10 (lines 4052-4123) — read for the 6-section template shape that §V7-PROXY mirrors
    - KAAN-ACTION-LEGAL.md §SHIP-V4 (lines 3389-3520) — read for the top-level cluster shape (§V7-PROXY is a NEW top-level cluster, like §SHIP-V4, NOT a sub-entry under §V7-LIVE)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "Server-side hardening route" bullet under OSS-02 decisions spells out the 4 pre-staged server-side hardening items + the verification curl + the 429-trigger contract)
  </read_first>
  <action>
Append a new top-level cluster `## §V7-PROXY — Bravoh proxy production hardening (OSS-02 server-side)` to KAAN-ACTION-LEGAL.md. Place it AFTER the entire §V7-LIVE section closes (after its Sign-off block + Cross-references, after line ~4170) and BEFORE any subsequent existing top-level cluster (if none, append at EOF). §V7-PROXY is a top-level `##` cluster, NOT a `###` sub-entry — it mirrors §SHIP-V4 / §V7-LIVE's top-level shape.

The cluster MUST contain these 6 subsections (mirror §V7-LIVE-10 verbatim shape for the subsection types, scoped to the proxy-hardening domain):

1. **Header line:** `## §V7-PROXY — Bravoh proxy production hardening (OSS-02 server-side)`

2. **Frontmatter block (mirror §SHIP-V4):**

   - `**REQ-ID:**` OSS-02 (server-side half — client-side closed via Plan 69-03 Tasks 1-3)
   - `**Owner:**` Kaan (founder) + Bravoh ops repo coordination
   - `**Status:**` ☐ pending  ☐ rate limit live  ☐ token bucket live  ☐ /metrics endpoint live  ☐ Sentry DSN wired  ☐ /health endpoint live  ☐ verified

3. **"Why this lives in a separate repo" (1 paragraph):**

   The Bravoh-side Gemini proxy at `api.altidus.world` is closed-source by design (per `CONTRIBUTING.md` carveout — Plan 69-01). Source edits live in the separate Bravoh ops repo. This §V7-PROXY cluster pre-stages the server-side hardening spec so when Kaan opens the Bravoh ops repo to land the work, every decision is already made: which endpoint shape, which env vars, which metric names, which Prom labels. The client-side fallback (Plan 69-03 Tasks 1-3) ships engineering-complete in vibemix independent of this cluster — when /metrics or /health goes live on api.altidus.world, NO client-side change is needed. The 60s recovery canary auto-detects /health and the recovery line fires the first time the next response returns 200.

4. **"Pre-staged server-side spec" (5 sub-sections, ordered):**

   **a. Per-install-UUID rate limit (token-bucket middleware):**
   - Recommended library: `slowapi` (FastAPI-native, ~5 LOC integration) or in-house FastAPI `Depends()` token-bucket.
   - Bucket parameters (defensible defaults — tune via post-launch metrics): 60 tokens / install-UUID, refill 1 token/sec (i.e. 60 reqs/min steady-state, burst-tolerant).
   - install-UUID comes from the existing `vibemix.agent.install_uuid` module (client-side; sent as a header by the existing proxy_client).
   - On bucket-empty: return HTTP 429 with `Retry-After: <seconds>` header AND a JSON body `{"error": "rate_limited", "install_uuid": "...", "retry_after_seconds": N}`. NOT a 5xx — the 429 fall-through path in vibemix client (Plan 69-03 Task 1 classify_proxy_error returns None for 429) is the contract.

   **b. Prom `/metrics` endpoint:**
   - Endpoint path: `GET /metrics` (standard prometheus_client surface; no auth, internal scrape only — `permissions: read-only` + IP-allowlist Bravoh's monitoring stack).
   - Required metric names (snake_case, label-cardinality bounded):
     - `rate_limit_hits_total{install_uuid="<uuid>"}` (counter)
     - `tokens_remaining_bucket{install_uuid="<uuid>"}` (gauge)
     - `proxy_request_duration_seconds{route="<route>", status="<status>"}` (histogram)
     - `proxy_upstream_errors_total{kind="<5xx|timeout|connect>"}` (counter — mirrors the client-side trigger classes)

   **c. Sentry DSN env var:**
   - Environment variable: `SENTRY_DSN` (the canonical Sentry SDK name; set in the Bravoh service deployment env, NOT in vibemix client).
   - Sample rate: 0.1 for traces, 1.0 for errors (defensible defaults; tune post-launch).
   - PII scrubbing: enable Sentry's default `send_default_pii=False`; install-UUID is the only request-identifier emitted (no IP, no user email).

   **d. `/health` endpoint:**
   - Endpoint path: `GET /health` (the contract the client-side `probe_proxy_health` from Plan 69-03 Task 1 already polls every 60s).
   - Response: HTTP 200 + JSON `{"status": "ok", "upstream_gemini": "<reachable|degraded|down>"}` when the service itself is up.
   - Response: HTTP 503 if the upstream Gemini is unreachable (which the client treats as 5xx-trigger → fallback stays armed; this is the correct cascade).
   - NO auth on /health (canary endpoint; intentionally cheap; no per-install-UUID rate limit applies).

   **e. Coordination point with Bravoh ops repo:**
   - Bravoh ops repo branch: `feat/oss-02-proxy-hardening` (suggested; Kaan picks the actual branch name).
   - File targets: `app/middleware/rate_limit.py`, `app/middleware/metrics.py`, `app/routes/health.py`, `app/config/sentry.py` (per the existing FastAPI app layout in the Bravoh repo; executor consults Bravoh's own CONVENTIONS.md if needed).
   - Migration cadence: each sub-section (a-d) can land independently; the client-side fallback (vibemix Plan 69-03) is GREEN today regardless of which sub-section ships first.

5. **"Verification" (fenced bash block, mirrors §SHIP-V4's Verification):**

   The block contains 5 numbered shell snippets, executor copies verbatim:

   - Snippet 1: `curl -s -o /dev/null -w '%{http_code}\n' https://api.altidus.world/health` — expect: 200
   - Snippet 2: `curl -s https://api.altidus.world/metrics | grep -E '^rate_limit_hits_total|^tokens_remaining_bucket|^proxy_request_duration_seconds|^proxy_upstream_errors_total'` — expect: 4 matching lines
   - Snippet 3: `for i in {1..120}; do curl -s -o /dev/null -w '%{http_code}\n' -H "X-Install-UUID: test-abuse-uuid" https://api.altidus.world/v1/<some-endpoint>; done | sort | uniq -c` — expect: at least one "429" in output
   - Snippet 4: server-side `echo "${SENTRY_DSN:0:20}..."` — expect: non-empty prefix
   - Snippet 5: client-side recovery walk — `uv run python -m vibemix` with `VIBEMIX_LLM_MODE=proxy`, induce 503 cascade (firewall block), then unblock — within 60s the "Co-host back online" transcript line MUST appear

6. **"Sign-off block":**

   Six lines (mirror §V7-LIVE-NN's Sign-off line shape):
   - `§V7-PROXY rate limit live on:                _________   (date — Kaan, Bravoh ops repo SHA ____)`
   - `§V7-PROXY /metrics endpoint live on:         _________   (date — Kaan, SHA ____)`
   - `§V7-PROXY Sentry DSN wired on:               _________   (date — Kaan, deployment env confirmed)`
   - `§V7-PROXY /health endpoint live on:          _________   (date — Kaan, SHA ____)`
   - `§V7-PROXY synthetic 429 trigger verified:    _________   (date — Kaan, command output captured)`
   - `§V7-PROXY client-side recovery verified:     _________   (date — Kaan, transcript line observed)`
   - `Sign-off by:                                    _________   (Kaan)`

Total cluster length: ≤ 180 lines (terse, the spec is the value; engineering happens in the other repo).

Do NOT touch any pre-existing §SHIP-V4 / §V7-LIVE / §V7-LIVE-NN content. §V7-PROXY is a NEW top-level cluster appended after all existing §V7-LIVE content (after the discharge-tracking table at line ~4139 AND after the Sign-off block at line ~4167 AND after the Cross-references at line ~4170).
  </action>
  <verify>
    <automated>grep -c "^## §V7-PROXY — Bravoh proxy production hardening" KAAN-ACTION-LEGAL.md | grep -q "^1$" &amp;&amp; grep -q "rate_limit_hits_total" KAAN-ACTION-LEGAL.md &amp;&amp; grep -q "/health endpoint" KAAN-ACTION-LEGAL.md &amp;&amp; grep -q "§V7-PROXY rate limit live on:" KAAN-ACTION-LEGAL.md &amp;&amp; awk '/^## §V7-PROXY/{print NR}' KAAN-ACTION-LEGAL.md | head -1 | awk '{if ($1 &gt; 4167) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## §V7-PROXY — Bravoh proxy production hardening" KAAN-ACTION-LEGAL.md` returns 1.
    - The §V7-PROXY cluster contains all 6 required subsections (Frontmatter / Why this lives in a separate repo / Pre-staged server-side spec / Verification / Sign-off block + the top-level header).
    - Pre-staged spec subsections name all 4 required Prom metric families (`rate_limit_hits_total`, `tokens_remaining_bucket`, `proxy_request_duration_seconds`, `proxy_upstream_errors_total`).
    - Pre-staged spec names `SENTRY_DSN` env var and the `/health` + `/metrics` endpoint paths.
    - Verification block contains 5 numbered shell snippets ending with the client-side recovery walk.
    - Sign-off block contains exactly 7 sign-off lines (6 §V7-PROXY rows + Sign-off by).
    - The §V7-PROXY cluster appears AFTER line 4167 (after the existing §V7-LIVE Sign-off block; line-number check).
    - Pre-existing §SHIP-V4 / §V7-LIVE / §V7-LIVE-NN content is byte-unchanged (`git diff KAAN-ACTION-LEGAL.md` shows only the additive §V7-PROXY cluster insertion).
    - Default-grid baseline preserved (KAAN-ACTION-LEGAL.md is not pytest-collected).
  </acceptance_criteria>
  <done>KAAN-ACTION-LEGAL.md gains §V7-PROXY top-level cluster (≤180 lines) with all 6 required subsections + 4 Prom metric names + Sentry DSN + /health + Verification + Sign-off; pre-existing §SHIP-V4 + §V7-LIVE content byte-unchanged.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete, run from repo root:

```bash
# 1. Classifier + probe smoke (Tasks 1+3, default grid is unaffected because integration marker is opt-in):
uv run pytest tests/integration/test_proxy_fallback.py -m integration -q
#   Expected: at least 10 passed in &lt; 5s

# 2. Default-grid baseline preserved (Task 3's integration tests are opt-in; Tasks 1+2's surface gains 0 default-grid tests):
uv run pytest -q
#   Expected: 4147 passed (or +5 / +10 if Wave 0 / Wave 1 have already shipped); 0 regressions; 26 skipped + 4 xpassed unchanged

# 3. Integration-marker baseline gains 10+ tests:
uv run pytest -m integration -q
#   Expected: was 16 from P68 → new total ≥ 26

# 4. Classifier round-trip smoke verified by hand:
uv run python -c "
from vibemix.agent.proxy_client import ProxyUnavailable, classify_proxy_error, probe_proxy_health
import httpx
assert classify_proxy_error(httpx.ConnectError('refused')).reason == 'connection_refused'
assert classify_proxy_error(httpx.ReadTimeout('slow')).reason == 'timeout'
assert classify_proxy_error(ValueError('not network')) is None
assert probe_proxy_health('https://nonexistent.invalid', timeout_s=1.0) is False
print('OK — Plan 69-03 classifier + probe surface verified')
"

# 5. §V7-PROXY cluster verified by hand-grep:
grep -c "^## §V7-PROXY — Bravoh proxy production hardening" KAAN-ACTION-LEGAL.md   # == 1
grep -c "rate_limit_hits_total" KAAN-ACTION-LEGAL.md                                # >= 1
grep -c "^§V7-PROXY rate limit live on:" KAAN-ACTION-LEGAL.md                        # == 1

# 6. Cardinal invariant zero-touch verified — only agent/proxy_client.py + agent/dj_cohost.py + runtime/session_loop.py touched in src/vibemix/:
git diff --stat -- src/vibemix/coach/ src/vibemix/state/ src/vibemix/memory/ src/vibemix/recall/ src/vibemix/decks/ src/vibemix/grounding/
#   Expected: empty (no edits to any of those subsystems)

# 7. Build smoke — vibemix still imports cleanly with the new exception surface:
uv run python -c "import vibemix.runtime.session_loop; import vibemix.agent.dj_cohost; print('OK')"
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vibemix client → Bravoh proxy | The client sends evidence packets + JWT-auth headers to `api.altidus.world`. The proxy can be 5xx-down, timeout, or refuse the connection. The OSS-02 contract: the brain refuses to lie when the grounding (LLM call) is missing. NOT a crash, NOT a hallucinated coach line. |
| client → exception classifier | The `classify_proxy_error` function must not silently swallow non-network errors (programming bugs, AssertionErrors, etc.). It only matches the 4 documented trigger classes; anything else falls through to caller (re-raised). |
| classifier → 4xx-distinction | 401 / 403 / 404 / 429 MUST NOT be classified as "unavailable" — those have their own existing per-error messaging from prior v3.x SHIP-CUT work. The test_classify_proxy_error_4xx_returns_none anti-regression test pins this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69P03-01 | Tampering (silent fallback) | classify_proxy_error 4xx boundary | mitigate | test_classify_proxy_error_4xx_returns_none anti-regression test pins 401/403/404/429 to None; a future refactor that "just retries on any error" fails CI red. |
| T-69P03-02 | Denial of Service (retry storm) | session_loop fallback path | mitigate | NO retries on the 4 trigger classes; 60s canary cadence enforced via `time.monotonic` delta; anti-slop "refuse to lie" semantics. |
| T-69P03-03 | Information Disclosure (transcript leak) | transcript_delta "Co-host unavailable" line | accept | The transcript line is shown to the user ONLY — it's the intended user-facing message; no PII or proxy internals leaked. The exception's `original` field is logged to events.jsonl (existing log path) but not surfaced via the UI. |
| T-69P03-04 | Tampering (schema drift) | cohost_status Literal["LISTENING", "TALKING", "IDLE"] | mitigate | The plan explicitly does NOT extend the Literal — the unavailable state surfaces via transcript_delta only. Any future PR that adds "UNAVAILABLE" to the Literal triggers Phase 19 oss_hygiene + readme_shape gates + ui_bus schema regen (separate review surface). |
| T-69P03-05 | Spoofing (proxy-imposter) | probe_proxy_health canary | accept | /health is a best-effort canary; an attacker who can MITM `api.altidus.world` to return 200 on /health while serving 503 on real endpoints can falsely clear the unavailable flag. Mitigation: the next real LLM call's 5xx will re-arm the flag within one tick. ASVS L1 mTLS for the proxy is out of scope (v7.0 acid test — no new product capability). |
| T-69P03-SC | Supply Chain | `httpx` / `slowapi` / no new deps in this plan | accept | This plan ships ZERO new package-install tasks (httpx is already a transitive dep via google-genai per pyproject.toml). `slowapi` is referenced ONLY in §V7-PROXY as a Bravoh-side recommendation (separate repo); no vibemix-side install. No Package Legitimacy Gate required. |
</threat_model>

<success_criteria>
- [ ] src/vibemix/agent/proxy_client.py: ProxyUnavailable + classify_proxy_error + probe_proxy_health surfaces shipped; existing build_proxy_genai_client + build_proxy_tts_chain are byte-unchanged.
- [ ] src/vibemix/agent/dj_cohost.py: narrowly-scoped try/except around generate_content_stream that re-raises ProxyUnavailable on the 4 trigger classes; 4xx/429 fall through unchanged.
- [ ] src/vibemix/runtime/session_loop.py: 4 new instance attributes + coach-call try/except + 60s /health canary + one-shot transcript line emissions for unavailable + back-online; direct-mode (BYO) bypass.
- [ ] cohost_status Literal in ui_bus/messages.py is UNCHANGED (anti-schema-drift verified).
- [ ] tests/integration/test_proxy_fallback.py: 10+ tests GREEN under `pytest -m integration`; default grid unaffected.
- [ ] KAAN-ACTION-LEGAL.md §V7-PROXY cluster appended (≤180 lines, 6 subsections, 4 Prom metric families, SENTRY_DSN, /health + /metrics, 5-snippet Verification block, 7-line Sign-off block); pre-existing §SHIP-V4 + §V7-LIVE byte-unchanged.
- [ ] Default `uv run pytest -q` baseline: 0 regressions; integration-marker baseline gains 10+ tests.
- [ ] Cardinal invariant zero-touch verified: `git diff --stat -- src/vibemix/coach/ src/vibemix/state/ src/vibemix/memory/ src/vibemix/recall/ src/vibemix/decks/ src/vibemix/grounding/` is empty.
- [ ] All commits on `live-tuning-or-brain` follow the existing commit-message style.
</success_criteria>

<output>
Create `.planning/phases/69-oss-fully-integrated/69-03-SUMMARY.md` when done. Record:
- The five artifacts shipped (paths + line counts).
- Baseline delta: default grid +0 tests / 0 regressions; integration-marker +10 tests.
- The §V7-PROXY cluster line-number anchor + grep-count proofs.
- Confirm OSS-02 closed engineering-side (client-side); server-side rides §V7-PROXY (separate Bravoh ops repo + Kaan-clock).
- Confirm cohost_status Literal unchanged + the cardinal invariants held by zero-touch on `coach/state/memory/recall/decks/grounding`.
- Note that Wave 2 is independent of Wave 0/1/3/4 — execution order is parallelizable.
</output>
