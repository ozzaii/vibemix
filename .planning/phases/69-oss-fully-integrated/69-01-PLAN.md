---
phase: 69-oss-fully-integrated
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - MAINTAINERS.md
  - CONTRIBUTING.md
  - README.md
  - tests/repo/test_oss_presence.py
requirements: [OSS-01]
autonomous: true

must_haves:
  truths:
    - "Repo root contains all four OSS-required files (CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, MAINTAINERS.md), each non-empty."
    - "CONTRIBUTING.md contains the exact Bravoh carveout sentinel 'the Bravoh proxy is closed-source by design'."
    - "README.md links all four OSS files at least once."
    - "Deletion or rename of any of the four files, or removal of the carveout sentinel, fails CI via tests/repo/test_oss_presence.py."
  artifacts:
    - path: "MAINTAINERS.md"
      provides: "Active-maintainer / decision-process / release-cadence / on-call surface for the OSS project."
      min_lines: 30
    - path: "CONTRIBUTING.md"
      provides: "Existing contributor doc + new 'Scope: vibemix vs Bravoh' section with the carveout sentinel."
      contains: "the Bravoh proxy is closed-source by design"
    - path: "README.md"
      provides: "Top-level entry point; links all four OSS files in a Community section."
    - path: "tests/repo/test_oss_presence.py"
      provides: "Presence test pinning all four files + the carveout sentinel + README backlinks."
      contains: "test_required_oss_files_exist"
  key_links:
    - from: "tests/repo/test_oss_presence.py"
      to: "MAINTAINERS.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md"
      via: "Path.exists + stat().st_size >= 200 byte check"
      pattern: "for fname in REQUIRED_OSS_FILES"
    - from: "tests/repo/test_oss_presence.py"
      to: "CONTRIBUTING.md"
      via: "grep for carveout sentinel string"
      pattern: "the Bravoh proxy is closed-source by design"
    - from: "tests/repo/test_oss_presence.py"
      to: "README.md"
      via: "substring search confirming each filename appears >=1x in README"
      pattern: "fname in readme_text"
---

<objective>
Ship the repo-presence baseline for v7.0 OSS-01: the four OSS-required files exist at repo root, CONTRIBUTING.md carries the Bravoh proxy carveout sentinel, README links all four, and a single presence test pins the contract so future deletions or renames fail CI red.

Purpose: Strangers landing on the repo see the four files they expect from any Apache-2.0 OSS project (MAINTAINERS.md is the only missing one today — CONTRIBUTING/CoC/SECURITY already exist), and contributors are told up-front via CONTRIBUTING.md's carveout section that the Bravoh proxy is closed-source by design so they don't waste cycles filing proxy bugs in the wrong repo.

Output: MAINTAINERS.md (NEW, repo root); CONTRIBUTING.md amended with a "Scope: vibemix vs Bravoh" section; README.md amended with a Community section linking all four OSS files; tests/repo/test_oss_presence.py NEW (two parametrized tests). One atomic commit pattern (per the 68P01 precedent), wave 0, no inter-wave dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-oss-fully-integrated/69-CONTEXT.md
@CONTRIBUTING.md
@CODE_OF_CONDUCT.md
@SECURITY.md
@README.md
@tests/repo/test_no_silent_skips.py
@tests/repo/test_oss_hygiene.py

<interfaces>
<!-- README.md currently links three of four OSS files (CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md); MAINTAINERS.md is NOT yet linked. -->
<!-- CONTRIBUTING.md grep for "the Bravoh proxy is closed-source by design" currently returns 0 (sentinel must be added). -->
<!-- File-size baseline: CONTRIBUTING.md ~5.0KB / 112 lines, SECURITY.md ~4.0KB / 107 lines, CODE_OF_CONDUCT.md ~1.6KB / 36 lines. -->
<!-- Existing presence-test idiom (tests/repo/test_oss_hygiene.py): REQUIRED_FILES list + @pytest.mark.parametrize per filename + Path.exists assert. -->
<!-- tests/repo/test_oss_hygiene.py covers (CONTRIBUTING, CoC, SECURITY, NOTICE, TRADEMARKS, LICENSE) — does NOT cover MAINTAINERS.md or the carveout sentinel. test_oss_presence.py is the v7.0-specific new gate. -->
<!-- Convention: tests/repo/ files use REPO_ROOT = Path(__file__).resolve().parent.parent.parent. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MAINTAINERS.md at repo root</name>
  <files>MAINTAINERS.md</files>
  <read_first>
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "MAINTAINERS.md" section under <decisions> spells out the required sections + sentinel content)
    - SECURITY.md (existing tone / Apache-2.0 voice baseline; mirror length and prose register)
    - CODE_OF_CONDUCT.md (existing terse voice baseline)
  </read_first>
  <action>
Create MAINTAINERS.md at repo root (≤80 lines, Apache-2.0-aligned voice mirroring SECURITY.md's prose register) with these five sections in this exact order:

1. **# vibemix — Maintainers** (title)
2. **## Active Maintainers** — single bulleted entry: "Kaan Özkan (@bravoh-ai) — `kaan@bravoh.tech`". One-line bio: "Founder of Bravoh; vibemix is Bravoh's first open-source release."
3. **## How to Reach Us** — three bullets: (a) "General questions / bug reports / feature requests: open a GitHub issue", (b) "Security vulnerabilities: follow `SECURITY.md` — do NOT open a public issue", (c) "Contribution questions: follow `CONTRIBUTING.md`".
4. **## Decision Process** — four bullets: (a) "License: Apache-2.0 (see `LICENSE` and `NOTICE`); SPDX-License-Identifier on every source file.", (b) "Governance: lazy consensus among active maintainers.", (c) "Merge bar: 1 maintainer approval + CI green (Full Test Matrix workflow + repo-presence gates).", (d) "Bravoh carveout: this repo (vibemix) is Apache-2.0; the Bravoh proxy at `api.altidus.world` is closed-source by design — see `CONTRIBUTING.md` 'Scope: vibemix vs Bravoh' for what belongs here vs there."
5. **## Release Cadence** — one-paragraph: "RC tags follow the `v0.1.0-rcN` pattern per `docs/release-process.md`; pre-flight gates are validated by `scripts/launch/cut_release.sh`; the final `gh release create` is a maintainer-action per `KAAN-ACTION-LEGAL.md §SHIP-V4`."
6. **## On-Call / Response Time** — one-paragraph: "Best-effort. vibemix is Kaan's OSS side-project alongside Bravoh's main product — issues are triaged in batches, typically within 7 days. Security reports per `SECURITY.md` get priority response."

Start with a `<!-- SPDX-License-Identifier: Apache-2.0 -->` HTML comment on line 1 to mirror the other root-doc Apache-2.0 markers. No emojis. No fenced code blocks. No external badges.
  </action>
  <verify>
    <automated>test -f MAINTAINERS.md && [ "$(wc -c &lt; MAINTAINERS.md)" -ge 200 ] && grep -c "the Bravoh proxy is closed-source by design" MAINTAINERS.md | grep -q "^1$" && grep -q "kaan@bravoh.tech" MAINTAINERS.md && grep -q "## Active Maintainers" MAINTAINERS.md && grep -q "## Decision Process" MAINTAINERS.md && grep -q "## Release Cadence" MAINTAINERS.md</automated>
  </verify>
  <acceptance_criteria>
    - File MAINTAINERS.md exists at repo root.
    - File size ≥ 200 bytes (typical: 1.5–3 KB).
    - All five required H2 sections present (Active Maintainers / How to Reach Us / Decision Process / Release Cadence / On-Call).
    - Contains the maintainer email `kaan@bravoh.tech` exactly once.
    - Contains the carveout sentinel `the Bravoh proxy is closed-source by design` exactly once (in the Decision Process bullet referencing CONTRIBUTING.md).
    - Contains the SPDX-License-Identifier marker on line 1.
    - Zero emoji characters.
  </acceptance_criteria>
  <done>MAINTAINERS.md exists at repo root and satisfies every acceptance criterion above.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Scope: vibemix vs Bravoh" carveout section to CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (current 112-line file — read in full to pick the correct insertion point; the new section belongs near the top, after the intro and before the per-contribution-type sections)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "CONTRIBUTING.md" section under <decisions> dictates exact sentinel + paragraph shape)
  </read_first>
  <action>
Insert a new top-level section into CONTRIBUTING.md titled `## Scope: vibemix vs Bravoh`. Place it AFTER the existing introductory section and BEFORE the first contribution-type section (the executor picks the exact line — typically after the first 1-2 paragraphs and before any `## 1. ...` or `## Bug fixes` header). The section content is one paragraph:

```
vibemix is Apache-2.0. The Bravoh-side Gemini proxy at `api.altidus.world` is closed-source by design — the Bravoh proxy is closed-source by design — it is the commercial wedge that funds Bravoh's main product. Contributions to vibemix should target the client-side code in this repo (`src/vibemix/`, `tauri/`, `scripts/`, `tests/`, `docs/`). Proxy bugs, quota issues, or rate-limit reports are not in scope here; route them via `SECURITY.md` for security-sensitive matters or open a GitHub issue describing the symptom and they will be triaged onto the Bravoh ops side by the maintainers. If you would rather bypass the Bravoh proxy entirely, see `docs/byo-key.md` for the Bring-Your-Own-Key path.
```

The exact sentinel string `the Bravoh proxy is closed-source by design` MUST appear verbatim (lowercase "the", no comma after, no rephrasing). The Task 4 grep gate is exact-match. Do NOT touch any other section of CONTRIBUTING.md — this is an additive insertion only.
  </action>
  <verify>
    <automated>grep -c "the Bravoh proxy is closed-source by design" CONTRIBUTING.md | grep -q "^1$" && grep -q "^## Scope: vibemix vs Bravoh" CONTRIBUTING.md && grep -q "docs/byo-key.md" CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md contains the H2 header `## Scope: vibemix vs Bravoh` exactly once.
    - CONTRIBUTING.md contains the exact-match sentinel `the Bravoh proxy is closed-source by design` exactly once (grep -c returns 1).
    - The new section references `docs/byo-key.md` (forward reference to 69-02 Wave 1 deliverable).
    - No pre-existing CONTRIBUTING.md content is removed or reordered — `git diff CONTRIBUTING.md` shows only an additive section insertion.
    - File still passes `tests/repo/test_oss_hygiene.py::test_hygiene_file_exists[CONTRIBUTING.md]`.
  </acceptance_criteria>
  <done>CONTRIBUTING.md carries the carveout section + sentinel verbatim, with no regressions to existing content.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Community" section to README.md linking all four OSS files</name>
  <files>README.md</files>
  <read_first>
    - README.md (read in full to find the existing footer-style links section near line 267 — `[CONTRIBUTING](CONTRIBUTING.md)` already appears there; MAINTAINERS.md is the only missing link)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "README link audit" decision specifies the minimum: ensure all four are referenced; pick the cleanest insertion shape)
  </read_first>
  <action>
README.md currently links CONTRIBUTING.md (lines 169, 245, 257, 267), CODE_OF_CONDUCT.md (line 267), SECURITY.md (lines 53, 267), but does NOT link MAINTAINERS.md anywhere. Add a `## Community` H2 section near the end of README (placed just before or after the existing license/footer at line 267). The section contains a short paragraph followed by a bulleted link list naming all four:

```
## Community

- **Contributing:** [CONTRIBUTING.md](CONTRIBUTING.md) — how to file bugs, add controller mappings, propose new prompt templates.
- **Code of Conduct:** [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md) — community standards for contributors and maintainers.
- **Security:** [SECURITY.md](SECURITY.md) — how to report vulnerabilities privately.
- **Maintainers:** [MAINTAINERS.md](MAINTAINERS.md) — who maintains vibemix, decision process, release cadence.
```

Do NOT delete the existing footer line at 267 — leave it as a redundant short-form link row; the Community section is the canonical surface, the footer row is a quick-link convenience. Do NOT reorder any other README content.
  </action>
  <verify>
    <automated>grep -c "MAINTAINERS.md" README.md | awk '{if ($1 &gt;= 1) exit 0; else exit 1}' && grep -q "^## Community" README.md && grep -c "CONTRIBUTING.md" README.md | awk '{if ($1 &gt;= 1) exit 0; else exit 1}' && grep -c "CODE_OF_CONDUCT.md" README.md | awk '{if ($1 &gt;= 1) exit 0; else exit 1}' && grep -c "SECURITY.md" README.md | awk '{if ($1 &gt;= 1) exit 0; else exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the H2 header `## Community` exactly once.
    - README.md contains a markdown link `[MAINTAINERS.md](MAINTAINERS.md)` at least once.
    - README.md still contains references to CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md (counts unchanged or increased — never decreased).
    - The pre-existing footer link row at line ~267 is preserved verbatim (no reordering / no deletion).
    - The pre-existing security email + hash sentinel surfaces (lines 53, readme-hero-hash gates) are untouched.
  </acceptance_criteria>
  <done>README.md links all four OSS files via a new Community section; no pre-existing README content is removed or reordered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create tests/repo/test_oss_presence.py with two presence tests</name>
  <files>tests/repo/test_oss_presence.py</files>
  <behavior>
    - test_required_oss_files_exist: parametrized over the four filenames (MAINTAINERS.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md). For each: file exists at repo root, file size ≥ 200 bytes, filename appears ≥ 1x in README.md.
    - test_contributing_has_bravoh_carveout: opens CONTRIBUTING.md, asserts the exact-match sentinel string `the Bravoh proxy is closed-source by design` appears exactly once (rename or removal triggers a failed assert with a helpful message pointing at the CONTRIBUTING.md "Scope: vibemix vs Bravoh" section).
    - Both tests must run under the default `uv run pytest -q` grid (no opt-in marker — these are presence gates that always run).
    - Test file follows the existing tests/repo/ idiom: `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`, `from __future__ import annotations`, `SPDX-License-Identifier: Apache-2.0` header, no opt-in marker, total file size ≤ 120 lines.
  </behavior>
  <read_first>
    - tests/repo/test_oss_hygiene.py (existing presence-test pattern — `REQUIRED_FILES` list + `@pytest.mark.parametrize("filename", REQUIRED_FILES)` + `Path.exists` assert; mirror this shape)
    - tests/repo/test_no_silent_skips.py (existing presence-test idiom — top-of-file SPDX header, `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`, module-level constants, docstring describing the gate's intent)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "Presence test shape" bullet under <decisions> specifies the two test function names)
  </read_first>
  <action>
Create `tests/repo/test_oss_presence.py` (NEW, ≤120 lines) following the established tests/repo/ idiom:

- Line 1: `# SPDX-License-Identifier: Apache-2.0`
- Module docstring describing the OSS-01 gate intent (1 paragraph; reference Phase 69 Plan 69-01).
- `from __future__ import annotations`
- Imports: `from pathlib import Path` + `import pytest`.
- `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`
- Module constant `REQUIRED_OSS_FILES: list[str] = ["MAINTAINERS.md", "CONTRIBUTING.md", "CODE_OF_CONDUCT.md", "SECURITY.md"]`
- Module constant `_MIN_BYTES: int = 200` (per CONTEXT.md decisions).
- Module constant `_BRAVOH_CARVEOUT_SENTINEL: str = "the Bravoh proxy is closed-source by design"`.

Function 1: `@pytest.mark.parametrize("filename", REQUIRED_OSS_FILES)` over `def test_required_oss_files_exist(filename: str) -> None:`. Per-row, assert (in order, with assert-messages that name the offender):
1. `(REPO_ROOT / filename).exists()` — message: f"Missing required OSS file: {filename}. v7.0 OSS-01 requires all four at repo root."
2. `(REPO_ROOT / filename).stat().st_size >= _MIN_BYTES` — message: f"OSS file {filename} is suspiciously small ({size} bytes &lt; {_MIN_BYTES}); placeholder?"
3. README.md mentions the filename: read `(REPO_ROOT / "README.md").read_text()` once into the function body, assert `filename in readme_text` — message: f"OSS file {filename} is not linked from README.md. Add it to the Community section (Plan 69-01 Task 3)."

Function 2: `def test_contributing_has_bravoh_carveout() -> None:`. Read CONTRIBUTING.md once, assert the sentinel `_BRAVOH_CARVEOUT_SENTINEL` appears exactly once via `contributing_text.count(_BRAVOH_CARVEOUT_SENTINEL) == 1` — message that points the executor at CONTRIBUTING.md's "Scope: vibemix vs Bravoh" section (Plan 69-01 Task 2).

No opt-in marker (this is a default-grid presence gate, like test_oss_hygiene.py). No fixtures needed.
  </action>
  <verify>
    <automated>uv run pytest tests/repo/test_oss_presence.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File tests/repo/test_oss_presence.py exists with `# SPDX-License-Identifier: Apache-2.0` on line 1.
    - File contains both `def test_required_oss_files_exist(` and `def test_contributing_has_bravoh_carveout(`.
    - `uv run pytest tests/repo/test_oss_presence.py -q` exits 0 with 5 passed (4 parametrize rows + 1 sentinel test).
    - File length ≤ 120 lines.
    - No opt-in pytest marker (runs under the default grid).
    - Default `uv run pytest -q` baseline increases by exactly 5 tests (4147 → 4152 passed); 26 skipped + 4 xpassed unchanged; 0 failed.
    - Negative-control verified manually: temporarily removing the sentinel from CONTRIBUTING.md → `test_contributing_has_bravoh_carveout` fails red with the executor-pointing assert message (then restore).
  </acceptance_criteria>
  <done>tests/repo/test_oss_presence.py exists with 5 GREEN tests under the default pytest grid; default-grid baseline increases by exactly 5; deletion of any of the 4 OSS files or removal of the carveout sentinel turns CI red on this gate.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete, run from repo root:

```bash
# 1. The new presence gate runs green on its own (5 tests):
uv run pytest tests/repo/test_oss_presence.py -q
#   Expected: 5 passed in &lt; 1s

# 2. Existing oss-hygiene gate (separate, predates this plan) unchanged:
uv run pytest tests/repo/test_oss_hygiene.py -q
#   Expected: same pass count as pre-plan baseline (no regressions)

# 3. Default-grid baseline preserved (+5 tests, 0 regressions):
uv run pytest -q
#   Expected: 4152 passed / 26 skipped / 4 xpassed / 0 failed (vs Wave 4 baseline 4147)

# 4. The four sentinels verified by hand-grep:
grep -c "the Bravoh proxy is closed-source by design" CONTRIBUTING.md   # == 1
grep -c "the Bravoh proxy is closed-source by design" MAINTAINERS.md    # == 1
grep -c "## Community" README.md                                         # == 1
grep -c "MAINTAINERS.md" README.md                                       # >= 1
test -f MAINTAINERS.md && echo OK                                        # OK

# 5. No src/vibemix/ regressions (cardinal invariant zero-touch):
git diff --stat -- src/vibemix/   # must be empty
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor → repo | New contributor reads CONTRIBUTING.md / MAINTAINERS.md to figure out where bug reports / security reports / proxy-related issues go. Misrouting (proxy bug filed in vibemix) wastes maintainer cycles AND leaks intent that vibemix can debug Bravoh proxy internals — a privacy/IP boundary leak. |
| presence-test → repo | The test asserts the sentinel string verbatim; a contributor rephrasing the sentinel to be "cleaner prose" without understanding it's load-bearing → CI goes red → deliberate review → no silent rot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69P01-01 | Information Disclosure | CONTRIBUTING.md carveout language | mitigate | Sentinel is exact-match grep-pinned by test_oss_presence.py::test_contributing_has_bravoh_carveout; any rewording fails CI and forces deliberate review. |
| T-69P01-02 | Tampering | MAINTAINERS.md / README.md presence | mitigate | test_required_oss_files_exist parametrize over all four files + README-backlink assert; deletion or rename fails CI. |
| T-69P01-03 | Repudiation | maintainer-of-record claim | accept | MAINTAINERS.md names @bravoh-ai / kaan@bravoh.tech; future maintainer additions land via PR with git-history audit trail (no in-test signature check needed). |
</threat_model>

<success_criteria>
- [ ] MAINTAINERS.md exists at repo root with all 5 required H2 sections and the carveout sentinel (≥ 200 bytes, no emojis).
- [ ] CONTRIBUTING.md contains the H2 `## Scope: vibemix vs Bravoh` section and the exact-match carveout sentinel exactly once; no pre-existing content removed/reordered.
- [ ] README.md contains the `## Community` section linking all four OSS files; pre-existing footer link row preserved verbatim.
- [ ] tests/repo/test_oss_presence.py exists, ≤ 120 lines, runs under default grid, 5/5 GREEN.
- [ ] Default `uv run pytest -q` baseline: 4147 → 4152 passed (exactly +5); 26 skipped + 4 xpassed unchanged; 0 failed.
- [ ] Zero `src/vibemix/` edits (cardinal invariant zero-touch verified by `git diff --stat -- src/vibemix/` empty).
- [ ] Negative-control: removing the carveout sentinel from CONTRIBUTING.md (then restoring) flips test_contributing_has_bravoh_carveout red with a helpful assert message.
- [ ] All commits on the `live-tuning-or-brain` branch follow the existing `git log` commit-message style (e.g. `docs(69-01): ship MAINTAINERS.md + CONTRIBUTING.md carveout + README community section + presence test`).
</success_criteria>

<output>
Create `.planning/phases/69-oss-fully-integrated/69-01-SUMMARY.md` when done. Record:
- The four artifacts shipped (paths + line counts).
- Baseline delta (4147 → 4152, 0 regressions).
- The negative-control verification result (carveout-removal → red, restore → green).
- Confirm OSS-01 closed.
- Note that Waves 1/2/3/4 (Plans 69-02..69-05) are unblocked and can run in parallel (no inter-wave deps).
</output>
