---
phase: 17-hard-tek-detectors-v1-genrerouter-musicstate-extension
plan: 06
type: execute
wave: 6
depends_on: [17-05]
files_modified:
  - scripts/tune_detectors.py
  - scripts/README.md
  - tests/scripts/__init__.py
  - tests/scripts/test_tune_detectors.py
  - tests/scripts/fixtures/__init__.py
  - tests/scripts/fixtures/synth_kick_pattern.py
autonomous: true
requirements: [SENSE-16]

must_haves:
  truths:
    - "`scripts/tune_detectors.py` accepts one or more reference WAV files via CLI, plays them through the FULL detector chain (GenreRouter + baseline EventDetector + 6 Wave-2 detectors), and writes a per-fire CSV consumable by Kaan's Phase 16 ear audit."
    - "CSV schema is locked per CONTEXT D: `track, t_seconds, bar_index, detector_name, score, threshold, fired` — one row per detector evaluation that fired (NOT every-tick rows; that would explode CSV size)."
    - "Harness runs against synthetic 130/150/170 BPM 4-on-floor patterns end-to-end without crashing — proves the full pipeline survives in a non-runtime context (no LiveKit, no asyncio, no Tauri shell)."
    - "Hard Tek anchor track ingestion is Kaan-action — the harness logs a clear 'no anchor tracks at .planning/phases/17-.../anchor_tracks/' WARN when called with no input files; STATE.md outstanding to-do is surfaced via `tune_detectors.py --help` epilog."
    - "Harness imports detector classes via the public package surface (`from vibemix.state.detectors import ...` + `from vibemix.events.genres import GENRE_REGISTRY`), not via internal paths — exercises the same import surface a community contributor would use."
  artifacts:
    - path: "scripts/tune_detectors.py"
      provides: "CLI tuning harness — `python scripts/tune_detectors.py <wav> [<wav> ...] --csv tuning_runs/<timestamp>.csv`"
      contains: "def main"
    - path: "scripts/README.md"
      provides: "Updated to document tune_detectors.py usage + CSV schema"
      contains: "tune_detectors.py"
    - path: "tests/scripts/test_tune_detectors.py"
      provides: "Smoke tests — synthetic WAV in, CSV out, schema validation"
      contains: "tune_detectors"
    - path: "tests/scripts/fixtures/synth_kick_pattern.py"
      provides: "Reusable synthetic 4-on-floor WAV generator (mirrors Plan 04 helper)"
      contains: "def write_synth_kick_wav"
  key_links:
    - from: "scripts/tune_detectors.py"
      to: "vibemix.state.GenreRouter"
      via: "instantiates a real GenreRouter + per-tick chain dispatch"
      pattern: "GenreRouter\\(|active_chain"
    - from: "scripts/tune_detectors.py"
      to: "vibemix.state.detectors._phrase_dsp.lock_downbeat_phase"
      via: "computes bar_index column from sample-time + BPM-locked downbeat phase"
      pattern: "lock_downbeat_phase"
---

<objective>
Ship the `scripts/tune_detectors.py` reference-WAV tuning harness per SENSE-16. The harness reads one or more WAV files, drives them through the full Phase 17 detector pipeline (GenreRouter + EventDetector + 6 Wave-2 detectors), and emits a per-fire CSV that Kaan consumes during Phase 16 ear-audit to validate / re-tune detector thresholds against real Hard Tek tracks.

Purpose: Phase 16 (Hallucination Verification Gate) needs a feedback loop — Kaan plays a real DJ session, hears a misfire, wants to know "what did the detector think it heard". This harness lets him replay any track through the same detector chain in a deterministic, offline way and compare CSV → ear truth. It's also the regression net: when Plan 02-04 thresholds get tuned, the same WAV → same CSV proves the change.

Output: 1 CLI script + smoke tests + Hard Tek anchor track Kaan-action surfaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-hard-tek-detectors-v1-genrerouter-musicstate-extension/17-CONTEXT.md
@.planning/phases/17-hard-tek-detectors-v1-genrerouter-musicstate-extension/17-01-SUMMARY.md
@.planning/phases/17-hard-tek-detectors-v1-genrerouter-musicstate-extension/17-04-SUMMARY.md
@.planning/phases/17-hard-tek-detectors-v1-genrerouter-musicstate-extension/17-05-SUMMARY.md
@src/vibemix/state/__init__.py
@src/vibemix/state/refresh.py
@src/vibemix/state/detectors/__init__.py
@src/vibemix/audio/buffers.py
@src/vibemix/audio/features.py
@src/vibemix/events/genres/__init__.py
@scripts/gen_sine.py

<interfaces>
<!-- Public surfaces the harness imports. Plans 17-01 through 17-05 all
     ship before this plan can run. -->

From src/vibemix/state/__init__.py (post Plan 17-05):
```
from vibemix.state import (
    EventDetector,        # GenreRouter-wrapped after Plan 17-05
    GenreRouter,
    MusicState,
    state_refresh_loop,   # the live-runtime loop — NOT used by the harness
)
```

From src/vibemix/state/refresh.py (post Plan 17-01):
```
def _tick(state, audio_buf, controller_state, track_info, now, ...) -> tuple[float, float, float, float]
```
The harness calls `_tick` directly with synthetic controller_state / track_info stubs — bypasses the asyncio loop.

From src/vibemix/audio/buffers.py:
```
class AudioBuffer:
    def __init__(self, sample_rate: int, max_seconds: float = 12.0)
    def push(self, frames: np.ndarray) -> None
    def snapshot(self, n: int) -> np.ndarray
    _sr: int
```

From src/vibemix/state/detectors/_phrase_dsp.py (post Plan 17-04):
```
def lock_downbeat_phase(samples, bpm, sample_rate) -> tuple[float, float]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synthetic WAV fixture helper + CSV schema validation</name>
  <files>tests/scripts/__init__.py, tests/scripts/fixtures/__init__.py, tests/scripts/fixtures/synth_kick_pattern.py, tests/scripts/test_tune_detectors.py</files>
  <behavior>
    - Test 1 (`test_synth_kick_wav_generates_correct_duration_and_sr`): `write_synth_kick_wav(path, bpm=130, duration_s=8.0, sample_rate=16000)` produces a 16kHz mono WAV of length 8.0s±10ms; soundfile.read confirms.
    - Test 2 (`test_synth_kick_wav_pulses_match_bpm`): generated 130 BPM WAV's onset count over 8s ≈ (130/60)*8 = 17.3 onsets ±2 (allows for first/last-pulse boundary).
    - Test 3 (`test_synth_kick_wav_supports_breakdown_section`): `write_synth_kick_wav(..., breakdown_at_s=4.0, breakdown_duration_s=2.0)` produces a WAV where samples in [4.0, 6.0] have RMS < 0.01 (silent) and samples elsewhere have full kick energy. Used to drive BREAKDOWN_KICK_KILL + REENTRY_KICK_LAND in tune_detectors smoke tests.
  </behavior>
  <action>
    Create test scaffolding directories with empty `__init__.py` markers: `tests/scripts/`, `tests/scripts/fixtures/`.

    Create `tests/scripts/fixtures/synth_kick_pattern.py`:
      `def write_synth_kick_wav(path: Path, *, bpm: float = 130.0, duration_s: float = 8.0, sample_rate: int = 16000, breakdown_at_s: float | None = None, breakdown_duration_s: float = 2.0) -> Path`:
        - Use `soundfile` (already a project dep — check `pip show soundfile`; if absent fall back to `scipy.io.wavfile.write` which IS in deps).
        - Generate brief 60Hz sine kick pulses every `60.0/bpm` seconds — 10ms attack + ~80ms exponential decay. Same envelope shape as Plan 17-04's `_synth_kick_pattern` helper (port the function — DRY).
        - If `breakdown_at_s` is set, ZERO out the sample range `[breakdown_at_s * sample_rate, (breakdown_at_s + breakdown_duration_s) * sample_rate]` after generation.
        - Write to `path`; return `path`.

    Write the 3 behavior tests in `tests/scripts/test_tune_detectors.py` (the helper tests live alongside the harness tests for locality). Use `tmp_path` fixture; clean up between tests automatically.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/python -m pytest tests/scripts/test_tune_detectors.py::test_synth_kick_wav_generates_correct_duration_and_sr tests/scripts/test_tune_detectors.py::test_synth_kick_wav_pulses_match_bpm tests/scripts/test_tune_detectors.py::test_synth_kick_wav_supports_breakdown_section -x -q</automated>
  </verify>
  <done>Synthetic WAV fixture helper works for full-kick + breakdown variants; 3 fixture tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tune_detectors.py CLI harness — WAV in, CSV out, end-to-end pipeline</name>
  <files>scripts/tune_detectors.py, tests/scripts/test_tune_detectors.py</files>
  <behavior>
    - Test 4 (`test_harness_processes_synthetic_wav_emits_csv`): given a 16s 130 BPM synthetic kick WAV with breakdown at 8s, run `tune_detectors.main(["test.wav", "--csv", "out.csv"])`. Asserts:
      - exit code 0
      - `out.csv` exists
      - CSV header row is exactly `track,t_seconds,bar_index,detector_name,score,threshold,fired` (matches CONTEXT D-locked schema).
      - At least one row exists with detector_name in `{KICK_SWAP, SUB_LAYER_ARRIVAL, KICK_DENSITY_SHIFT, BREAKDOWN_KICK_KILL, REENTRY_KICK_LAND, PHRASE_BOUNDARY, baseline}` (genre chain detectors).
      - All `t_seconds` values are within [0.0, 16.0].
      - All `bar_index` values are non-negative integers.
    - Test 5 (`test_harness_no_input_files_logs_anchor_tracks_to_do`): run `tune_detectors.main([])` — exits non-zero, stderr contains the string `anchor_tracks` AND `Phase 16` (so Kaan greps the script for the to-do).
    - Test 6 (`test_harness_processes_multiple_wavs`): pass two WAVs; CSV has rows tagged with both track filenames in the `track` column.
    - Test 7 (`test_harness_uses_real_eventdetector_with_genre_router`): mock-spy on `vibemix.state.GenreRouter.swap` → after a tick where the harness writes `state.active_genre = "techno"`, `GenreRouter.swap("techno")` is called at least once. (Proves the harness is using the SAME router-wired EventDetector the runtime uses, not a private copy.)
    - Test 8 (`test_harness_breakdown_wav_fires_kick_kill_then_reentry`): 16s synthetic 150 BPM techno-band WAV with a 2s breakdown at 8s. CSV contains a `BREAKDOWN_KICK_KILL` row near t≈8.0 and (if 150 BPM is in the techno BPM band) a `REENTRY_KICK_LAND` row near t≈10.0 (within ±0.5s). End-to-end pair-detection contract holds in offline mode too.
  </behavior>
  <action>
    Create `scripts/tune_detectors.py` with shebang `#!/usr/bin/env python3` and SPDX header.

    Module docstring documents the SENSE-16 contract + CSV schema + Kaan-action note. The note explicitly references STATE.md's outstanding to-do "Collect Hard Tek + 9 SKU reference tracks for P17 detector tuning harness. Hard Tek 7-10 anchor tracks especially — Kaan-owned." so anyone reading the file knows where the missing input lives.

    Required imports + structure:
      - argparse for CLI
      - csv + pathlib + datetime for output
      - numpy for sample math
      - soundfile (fallback scipy.io.wavfile if soundfile absent — same approach as Task 1)
      - `from vibemix.state import EventDetector, MusicState, GenreRouter`
      - `from vibemix.state.refresh import _tick`
      - `from vibemix.audio.buffers import AudioBuffer`
      - `from vibemix.audio.constants import GENRE_BPM_BANDS`
      - `from vibemix.state.detectors._phrase_dsp import lock_downbeat_phase`
      - `import logging` + module-level logger

    `def main(argv: list[str] | None = None) -> int`:
      1. Parse argv (default `sys.argv[1:]`):
         - Positional: one or more WAV file paths
         - `--csv PATH` (default: `tuning_runs/<UTC-iso>.csv` under repo root)
         - `--bpm-override FLOAT` (default: None — derive via `estimate_bpm` from the audio)
         - `--genre-override STR` (default: None — derive via the same heuristic state_refresh_loop uses)
         - Help epilog: "Hard Tek anchor tracks expected at .planning/phases/17-.../anchor_tracks/. STATE.md outstanding to-do — Kaan-owned."
      2. If no positional args: log ERROR `"tune_detectors: no input WAV files. Hard Tek anchor tracks Kaan-action — see Phase 16 ear-audit + STATE.md outstanding to-do `Collect Hard Tek + 9 SKU reference tracks`. Suggested anchor location: .planning/phases/17-hard-tek-detectors-v1-genrerouter-musicstate-extension/anchor_tracks/"`. Return exit code 2 (UNIX convention for usage error).
      3. For each input WAV:
         - Read samples via soundfile/wavfile; resample to 16kHz mono if needed (use `scipy.signal.resample_poly` — already a project dep per CLAUDE.md).
         - Construct fresh `MusicState`, `AudioBuffer(sample_rate=16000, max_seconds=12.0)`, `EventDetector(audio_buf=audio_buf)`.
         - Walk the WAV in 100ms hops (1600 samples @ 16kHz — matches state_refresh_loop tick rate). For each hop:
           - Push the hop into the audio_buf via `audio_buf.push(hop)`.
           - Construct synthetic `controller_state` + `track_info` stubs with `recent_moves=[]` and `snapshot()` returning `(None, None, None)` (the live runtime stubs — no MIDI/track input in offline harness).
           - Drive one `_tick(state, audio_buf, controller_state_stub, track_info_stub, now=current_t, ...)`.
           - Apply CLI overrides: `if args.bpm_override: state.bpm = args.bpm_override` (BEFORE event_detector.detect); same for genre.
           - Call `event = event_detector.detect(state, kaan_just_spoke=False, manual=False)`.
           - Compute `bar_index` from `current_t * state.bpm / 60.0 / 4.0` (same formula PhraseBoundaryDetector uses).
           - If event is not None: append a CSV row with track basename, current_t, bar_index, event.type, `event.extra` serialized to a `score` field (json.dumps the extra dict — single column keeps schema fixed-width; threshold = the active threshold for that detector type via a small lookup table this script defines from `vibemix.audio.constants`), fired=1.
      4. Write the CSV with header row.
      5. Return 0 on success.

    Update `scripts/README.md` (or create if missing) with a section:
      ```
      ## tune_detectors.py — Phase 17 reference-WAV harness
      Usage: `python scripts/tune_detectors.py track1.wav [track2.wav ...] --csv out.csv`
      CSV schema: track, t_seconds, bar_index, detector_name, score, threshold, fired
      Anchor track location: .planning/phases/17-.../anchor_tracks/ (Kaan-owned, STATE.md to-do).
      ```

    Write Tests 4-8 in `tests/scripts/test_tune_detectors.py`. Tests use `tmp_path` for input WAV + output CSV. Test 7 uses `mocker.spy` on the GenreRouter class (or monkeypatch the swap method).

    Anti-pattern guards:
      - DO NOT touch any `cohost_*.py` POC file — port-from only (per CLAUDE.md "POC = reference, devour it").
      - DO NOT call `state_refresh_loop` (the asyncio coroutine) — call the synchronous `_tick` directly.
      - DO NOT instantiate sounddevice / LiveKit / Tauri — this is a pure offline harness.
      - DO NOT silently degrade if anchor tracks are missing — log loud, exit 2 (Test 5).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; .venv/bin/python -m pytest tests/scripts/ -x -q</automated>
  </verify>
  <done>tune_detectors.py runs end-to-end on synthetic WAVs; CSV schema matches CONTEXT-D locked spec; missing-input case logs Kaan-action; pair-detection (kill→reentry) verified in offline mode; no POC files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied WAV file → soundfile/scipy reader | Untrusted file content; malformed WAV could crash the harness or trigger memory pressure. |
| Resampling 48kHz → 16kHz on arbitrary input | Large stereo input (~100 MB / 5 min @ 48kHz wave) takes ~50 MB RAM during resample. |
| Output CSV path | User can supply any path; harness should refuse to overwrite without --force? — defer; Kaan-only tool, not a public-facing surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | Denial of Service | Multi-hour input WAV exhausts memory | accept | Documented as a Kaan-only offline tool; he supplies his own anchor tracks (60-90s typical Hard Tek loops). Bullet in scripts/README "Recommended track length 60-300s" — no enforcement. |
| T-17-06-02 | Tampering | Crafted WAV with extreme sample values (NaN / inf) corrupts state_refresh_loop math | mitigate | numpy operations in the upstream Phase 6 / Phase 17 code already short-circuit on NaN BPM (`_bpm_max_valid` rejects). soundfile/scipy_wavfile reject NaN/inf samples on decode. |
| T-17-06-03 | Repudiation | Harness silently runs with default thresholds while user thinks they tuned them — no audit trail | mitigate | The CSV's `threshold` column captures the threshold value at fire-time. Run timestamp + git SHA could go in a header comment row — defer to Plan 06's first user feedback. |
| T-17-06-04 | Information Disclosure | Output CSV path defaults to `tuning_runs/` — could be committed to git accidentally | mitigate | Add `tuning_runs/` to `.gitignore` as part of this task (single line append; verify gitignore exists first). Documented in scripts/README. |
| T-17-06-05 | Tampering | Tester runs harness against a non-WAV file (mp3/flac) | accept | soundfile supports many formats; scipy.io.wavfile is WAV-only. Doc the supported list. Failure mode is clean exception, not corruption. |
</threat_model>

<verification>
1. `pytest tests/scripts/ -x -q` → 8 tests pass (3 fixtures + 5 harness).
2. `pytest tests/state/ tests/audio/ tests/runtime/ tests/scripts/ -x -q` → entire phase-relevant suite green.
3. `python scripts/tune_detectors.py --help` → exits 0, help text mentions `anchor_tracks` + `Phase 16` + the CSV schema.
4. `python scripts/tune_detectors.py` (no args) → exits 2, stderr contains the Kaan-action message.
5. `grep -n "cohost_v" scripts/tune_detectors.py` → 0 matches (POC files untouched + not imported).
6. `cat .gitignore | grep tuning_runs` → 1 match (output dir gitignored).
</verification>

<success_criteria>
- CLI tool exists at `scripts/tune_detectors.py` with documented `--csv` output
- CSV schema locked + verified to match CONTEXT D
- End-to-end pipeline (WAV → state → detector chain → fire → CSV row) works on synthetic inputs
- Missing-input case surfaces Kaan-action loudly (exit 2 + clear stderr)
- Hard Tek anchor track location documented in 3 places: scripts/README, scripts/tune_detectors.py docstring, --help epilog
- POC files untouched, runtime asyncio not invoked
- `tuning_runs/` gitignored
</success_criteria>

<output>
After completion, create `.planning/phases/17-hard-tek-detectors-v1-genrerouter-musicstate-extension/17-06-SUMMARY.md` documenting:
- The CLI surface + CSV schema (final values)
- The Kaan-action surface (anchor tracks outstanding — STATE to-do reaffirmed)
- The chain-imports-from-public-package contract (no internal paths used) — proves community contributors can build their own harness variants on top
</output>
