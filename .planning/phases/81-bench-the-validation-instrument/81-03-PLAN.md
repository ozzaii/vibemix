---
phase: 81-bench-the-validation-instrument
plan: 03
type: execute
wave: 3
depends_on: ["81-02"]
files_modified:
  - src/vibemix/bench/eval.py
autonomous: true
requirements: [BENCH-02]
must_haves:
  truths:
    - "Groundedness REUSES CitationLinter.check(output, dsp_snapshot, mode=\"debrief\") verbatim — a cell citing an atom present in its snapshot scores high; a fabricated atom absent from the snapshot scores 0"
    - "Specificity is a deterministic heuristic over NEGATIVE_PHRASES (penalize slop) + concrete measures (reward numbers/named elements)"
    - "Lens-fidelity scores per-lens vocabulary anchors deterministically (a hype line scores higher under hype than a flat line)"
    - "The eval RANKS cells; it never decides a winner (no verdict, no winner field)"
  artifacts:
    - path: "src/vibemix/bench/eval.py"
      provides: "score_cell(result) -> CellScore (groundedness/specificity/lens_fidelity) + a rank-only aggregate; pure, offline"
      exports: ["score_cell"]
  key_links:
    - from: "src/vibemix/bench/eval.py"
      to: "coach/citation_linter.py CitationLinter.check"
      via: "linter.check(result.output, result.dsp_snapshot, mode=\"debrief\")"
      pattern: "CitationLinter|\\.check\\("
    - from: "src/vibemix/bench/eval.py"
      to: "prompts/negative_dict.py NEGATIVE_PHRASES"
      via: "slop-penalty over the ban-list"
      pattern: "NEGATIVE_PHRASES"
---

<objective>
Build the BENCH-02 automated first-pass eval: three PURE scoring functions over a recorded `BenchResult`. Groundedness REUSES the existing `CitationLinter` against the cell's OWN DSP-fact snapshot (the linter IS the product's anti-slop gate — Invariant #2 — so reuse keeps the bench honest to the shipped behavior). Specificity + lens-fidelity are deterministic heuristics over the existing slop ban-list + per-lens vocabulary anchors. The auto-score produces a RANKING, never a verdict — Kaan's ear decides.

Purpose: A fast-lane first-pass that sorts cells so Kaan reads the strongest first. The decisive no-audio cell's groundedness is the empirical heart — if Gemini grounds a sharp reaction on the DSP snapshot alone, the intelligence resided in the EAR.
Output: `src/vibemix/bench/eval.py`; flips the Plan-01 BENCH-02 xfails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/81-bench-the-validation-instrument/81-RESEARCH.md
@.planning/phases/81-bench-the-validation-instrument/81-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Reused verbatim — do NOT re-implement groundedness. -->
CitationLinter().check(text, registry_snapshot, *, mode="live") -> LintResult      # coach/citation_linter.py:94
  # mode "debrief" = ±2.0s tolerance (offline-appropriate, RESEARCH A3); reason ∈ {valid, no_citations, malformed_atom, invalid_atoms}
LintResult(valid, citations_found, missing, reason)  frozen                         # coach/citation_linter.py:56
NEGATIVE_PHRASES: tuple[str, ...]                                                   # prompts/negative_dict.py:24

From Plan 02 (src/vibemix/bench/cell.py):
  BenchResult(cell, prompt, output, dsp_snapshot, usage, error)  frozen
  BenchCell(... lens: str ...)
From Plan 02 (src/vibemix/bench/matrix.py):
  LENS_ANCHORS: dict[str, tuple[str,...]]   # per-lens vocab anchors (hype/critique/tutor)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: eval.py — groundedness (reused linter) + specificity + lens-fidelity scorers</name>
  <files>src/vibemix/bench/eval.py</files>
  <behavior>
    - groundedness: a `BenchResult` whose `output` cites an atom PRESENT in its `dsp_snapshot` → high (1.0 for reason=="valid"); a fabricated `[aud:...]` absent from the snapshot → 0.0 (reason in {malformed_atom, invalid_atoms}); no citations → 0.5
    - an ERRORED result (`error` non-None) scores groundedness 0.0 and is flagged errored — never silently high
    - specificity: a line with a `NEGATIVE_PHRASES` hit scores LOWER than the same line without; a line with a concrete measure ("128 bpm") scores higher than a vague one — deterministic, repeatable
    - lens-fidelity: a hype-vocab line scores higher under `lens="hype"` than a flat line; tutor/critique anchors likewise
    - score_cell is PURE (no I/O, no API) and RANK-ONLY — there is no `winner`/`decision` output anywhere
  </behavior>
  <read_first>
    - 81-PATTERNS.md §"bench/eval.py" (groundedness REUSES `CitationLinter.check(..., mode="debrief")` verbatim — lines 186-196; the specificity + lens-fidelity heuristic shapes; "the eval RANKS, it does NOT decide" Pitfall 5)
    - 81-RESEARCH.md §"Pattern 2" (the exact groundedness mapping: 1.0 valid / 0.5 no_citations / 0.0 else) + §"Pattern 3" (the specificity + lens_fidelity heuristic sketches — keep simple + deterministic, do NOT over-engineer into a pseudo-judge)
    - src/vibemix/coach/citation_linter.py:56-173 (LintResult fields + check() reason values — what to map from; read once)
    - src/vibemix/prompts/negative_dict.py:24 (NEGATIVE_PHRASES content — the slop signal)
    - src/vibemix/bench/cell.py + matrix.py (the BenchResult fields + LENS_ANCHORS — Plan 02 outputs this plan consumes)
  </read_first>
  <action>
    Create `eval.py` with `@dataclass(frozen=True) CellScore` carrying `groundedness: float`, `specificity: float`, `lens_fidelity: float`, `errored: bool`, and a derived `aggregate: float` (a simple weighted mean for SORTING only — never a verdict). `score_cell(result: BenchResult) -> CellScore`: if `result.error` is not None → `CellScore(0.0, 0.0, 0.0, errored=True, aggregate=0.0)` (errored cells sink in the rank, never fabricated-high). Else: groundedness via a module-level `CitationLinter()` → `res = linter.check(result.output, result.dsp_snapshot or {}, mode="debrief")` → `1.0 if res.reason=="valid" else 0.5 if res.reason=="no_citations" else 0.0`. Specificity = pure heuristic: penalize `sum(p.lower() in text.lower() for p in NEGATIVE_PHRASES)`, reward `len(re.findall(r"\d+\s?(?:hz|bpm|db|bar|s)\b", text.lower()))` concrete measures + named-element hits, clamped `[0,1]` (RESEARCH §264). Lens-fidelity = `clamp(sum(a in text.lower() for a in LENS_ANCHORS[result.cell.lens]) / 2.0, 0.0, 1.0)`. Add a `rank_cells(scored: list[tuple[BenchResult, CellScore]]) -> list[...]` that SORTS by aggregate descending — explicitly a sort, with a docstring stating it does NOT decide a winner (Pitfall 5). Keep everything pure + deterministic; no winner field, no verdict, no I/O. Flip the Plan-01 `test_eval.py` xfails to real-green.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/bench/test_eval.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/bench/test_eval.py -x` passes real-green (groundedness/specificity/lens-fidelity xfails flipped, no xpass)
    - A fabricated-atom cell scores groundedness 0.0; a valid-citation cell scores 1.0 — via the reused `CitationLinter`, NOT a re-implementation (grep confirms `CitationLinter` import, no bespoke atom parser in eval.py)
    - `grep -n "winner\|verdict\|decide" src/vibemix/bench/eval.py` returns nothing (or only in a docstring stating it does NOT decide)
    - score_cell has no network/file I/O (deterministic on fixture cells)
    - `test_no_model_literal.py` stays green (no literal introduced)
  </acceptance_criteria>
  <done>eval.py exists; groundedness reuses the linter; specificity + lens-fidelity are deterministic; the eval ranks but never decides; eval xfails flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval.py → recorded cells | pure transform over in-memory `BenchResult`s; no network, no file I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-07 | Repudiation / integrity | the auto-score creeping into a verdict (pre-empting Kaan's ear) | mitigate | `rank_cells` is a SORT with a docstring stating it does not decide; no `winner`/`verdict` field exists; the grep gate in acceptance criteria enforces (RESEARCH Pitfall 5) |
| T-81-08 | Tampering | drifting from the product's real anti-slop gate by re-implementing groundedness | mitigate | reuse `CitationLinter.check` verbatim (Invariant #2); no bespoke citation resolver in `bench/` |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | zero new packages |
</threat_model>

<verification>
- `pytest -q tests/bench/test_eval.py` → green (BENCH-02 xfails flipped)
- `grep -n "winner\|verdict" src/vibemix/bench/eval.py` → none (or docstring-only)
- `grep -n "CitationLinter" src/vibemix/bench/eval.py` → present (reuse, not re-implement)
- Full suite `pytest -q` exit 0, no new failures
</verification>

<success_criteria>
- BENCH-02 met: groundedness/specificity/lens-fidelity scored per cell, unit-testable on fixtures with zero API
- Groundedness reuses the existing `CitationLinter`; the no-audio cell's groundedness is meaningful
- The eval RANKS, never decides (no winner/verdict)
- Full suite green additively
</success_criteria>

<output>
Create `.planning/phases/81-bench-the-validation-instrument/81-03-SUMMARY.md` when done
</output>
