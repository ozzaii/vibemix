---
phase: 81-bench-the-validation-instrument
plan: 04
type: execute
wave: 4
depends_on: ["81-03"]
files_modified:
  - src/vibemix/bench/review.py
  - docs/bench.md
autonomous: true
requirements: [BENCH-03]
must_haves:
  truths:
    - "A pure JSON→Markdown renderer lays ranked cells out by study/dimension, each with its prompt + output + auto-scores"
    - "The VERDICT section is present but EMPTY — a 'Kaan fills this' placeholder, NO winner ever written by code (THE HARD HUMAN GATE)"
    - "An errored cell renders as 'ERRORED — parked', never as fake output"
    - "The documented live e2e produce path (vibemix bench run) is recorded — bounded, fail-safe, parks gracefully on API error"
  artifacts:
    - path: "src/vibemix/bench/review.py"
      provides: "render_review(results, scores) -> markdown str; pure, deterministic, empty verdict"
      exports: ["render_review"]
    - path: "docs/bench.md"
      provides: "The documented produce step + the KAAN-ACTION parking (verdict + taste-rubric wording)"
      contains: "KAAN-ACTION"
  key_links:
    - from: "src/vibemix/bench/review.py"
      to: "bench/eval.py rank_cells"
      via: "ranked cells grouped by study/dimension"
      pattern: "rank_cells|aggregate"
---

<objective>
Build the BENCH-03 review surface — a PURE JSON→Markdown renderer that lays the ranked bench cells out by study and dimension, each with its prompt, output, and auto-scores, plus an explicit EMPTY **VERDICT (Kaan fills this)** section. This is the KAAN-ACTION artifact: autonomous produces the surface + the auto-ranking; it does NOT fill the verdict, does NOT pick the winner, does NOT fake any cell. Plus the `docs/bench.md` that documents the bounded, fail-safe live produce command and explicitly parks the verdict + the final taste-rubric wording as KAAN-ACTION.

Purpose: Give Kaan a clean, ranked surface to read + judge "did it click / is it slop / which architecture + model wins" — the Phase-16 rule, the hard human gate. The auto-score ranks; Kaan's ear decides.
Output: `src/vibemix/bench/review.py` + `docs/bench.md`; flips the Plan-01 BENCH-03 xfails; parks the verdict + rubric as KAAN-ACTION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/81-bench-the-validation-instrument/81-RESEARCH.md
@.planning/phases/81-bench-the-validation-instrument/81-PATTERNS.md
@.planning/phases/81-bench-the-validation-instrument/81-CONTEXT.md
@CLAUDE.md

<interfaces>
From Plan 02 (src/vibemix/bench/cell.py): BenchResult(cell, prompt, output, dsp_snapshot, usage, error)
From Plan 03 (src/vibemix/bench/eval.py): CellScore(groundedness, specificity, lens_fidelity, errored, aggregate); score_cell; rank_cells
Dev-instrument report-render precedent: src/vibemix/runtime/soak.py
The produce command (Plan 02): `uv run python -m vibemix bench run --study A`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: review.py — the ranked KAAN-ACTION review surface (empty verdict)</name>
  <files>src/vibemix/bench/review.py</files>
  <behavior>
    - render_review(results, scores) returns Markdown: cells ranked by aggregate descending, grouped by study/dimension, each showing prompt + output + the three auto-scores
    - the VERDICT section is present and EMPTY — contains the "Kaan fills this" placeholder and NO winner/winner: line written by code
    - an errored cell renders as "ERRORED — parked", never as fake output
    - the render is deterministic + pure (same inputs → byte-identical Markdown; no clock, no randomness, no I/O)
  </behavior>
  <read_first>
    - 81-PATTERNS.md §"bench/review.py" (the verified empty-VERDICT block: `## VERDICT (Kaan fills this)` + `> _Kaan fills this in — the auto-rank is a sort, not a decision._`; "Error cells render as ERRORED — parked, never as fake output"; Pitfall 5 — no code path writes a winner)
    - 81-RESEARCH.md §"Pattern" review.py + §"Anti-Patterns" (the auto-score must never decide) + §Security "Fabricated cell filling a gap" (errored cells render parked)
    - src/vibemix/runtime/soak.py (the dev-instrument report-render precedent — pure str builder, no I/O surprises)
    - src/vibemix/bench/eval.py (rank_cells + CellScore — Plan 03 output this consumes)
  </read_first>
  <action>
    Create `review.py` with `render_review(results: list[BenchResult], scores: dict | list) -> str` — a pure Markdown builder. Header section names the study + run metadata (cell count, errored count). Group cells by study/dimension; within each group sort by `CellScore.aggregate` descending (call `rank_cells` from eval.py). For each cell render a section with: the 6-D coordinates (model_path/grounding/prompting/contexting/lens/taste), the three auto-scores (groundedness/specificity/lens_fidelity + aggregate), the assembled prompt (truncate sensibly), and the output. An errored cell (`result.error` not None) renders `**ERRORED — parked:** {error}` instead of output — NEVER a fabricated line. End with the verdict block EXACTLY: `## VERDICT (Kaan fills this)` followed by `> _Kaan fills this in — the auto-rank is a sort, not a decision._` and NOTHING that names a winner. No code path may write a winner/decision. Keep it deterministic + pure (no timestamps that vary run-to-run unless explicitly passed in; no randomness; no file write — the caller writes the artifact). Flip the Plan-01 `test_review.py` xfails to real-green.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/bench/test_review.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/bench/test_review.py -x` passes real-green (xfails flipped, no xpass)
    - The rendered Markdown contains the exact `## VERDICT (Kaan fills this)` heading + the "Kaan fills this in" placeholder
    - `grep -in "winner\|the winner is\|recommended:" src/vibemix/bench/review.py` returns nothing that assigns a winner (docstring/heading mentions of "winner" as a forbidden concept are acceptable only if they don't write one)
    - An errored cell renders "ERRORED — parked" (asserted in the test)
    - render_review is pure — calling it twice on the same inputs yields byte-identical output
  </acceptance_criteria>
  <done>review.py renders a ranked surface with an EMPTY verdict; errored cells park; the render is pure; review xfails flipped.</done>
</task>

<task type="auto">
  <name>Task 2: docs/bench.md — documented produce step + KAAN-ACTION parking</name>
  <files>docs/bench.md</files>
  <read_first>
    - 81-CONTEXT.md §"Real-cell run (the produce step)" + §"BENCH-03" (the produce-and-park contract: autonomous produces the surface + ranking, NEVER the verdict / winner / faked cell)
    - 81-VALIDATION.md §"Manual-Only Verifications" (the three parked KAAN-ACTION rows: the real run, Kaan's verdict, the taste-rubric wording)
    - 81-RESEARCH.md §"The real-run command" + §"Cost reality" (the documented command + the low-single-digit-euro-cents bound) + §"Common Pitfalls" Pitfall 2 (the 429 fail-safe)
    - docs/library.md (the project's docs idiom — how a CLI workflow is documented; match the tone/structure)
  </read_first>
  <action>
    Write `docs/bench.md` documenting the bench dev-instrument. Sections: (1) What it is — the multi-dimensional validation instrument (model × grounding × prompting × contexting × lens × taste), the two focused studies (A architecture-axis / B model-axis) + the decisive no-audio cell; the harness expresses the full matrix, the runs are the two studies. (2) Offline gate — `pytest -q tests/bench/` proves the harness/eval/review with a fake client, zero API (the honest-green proof). (3) The produce step (KAAN-ACTION) — the exact command `uv run python -m vibemix bench run --study A` on the funded key (`.env`, `load_dotenv(override=True)` from Phase 77); cost-bounded (low single-digit euro-cents on Flash per `SessionMeter.summary()`); fail-safe — a 429/auth/network error parks the cell as `{"error": ...}`, never fabricates, never aborts, never blocks the milestone. (4) The review surface — `bench_run.json` → `render_review` → a KAAN-ACTION Markdown artifact with the EMPTY verdict. (5) THE HARD HUMAN GATE (KAAN-ACTION, explicit) — three parked rows: the real run, Kaan's-ear VERDICT (which architecture + model wins; feeds GROUND-02's `live_coach` alias + the default architecture as a follow-up), and the final TASTE_RUBRIC wording (Kaan's IP — replace the placeholder). State plainly: autonomous produces + ranks; Kaan decides; nothing is faked. Cross-reference: `81-VALIDATION.md` parked rows. Match `docs/library.md`'s structure/tone.
  </action>
  <verify>
    <automated>test -f docs/bench.md && grep -c "KAAN-ACTION" docs/bench.md && grep -q "bench run" docs/bench.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/bench.md` exists and documents the offline gate, the produce command, the cost bound, the fail-safe, and the review surface
    - It contains an explicit KAAN-ACTION section parking: (a) the real run, (b) Kaan's verdict, (c) the taste-rubric wording — `grep -c "KAAN-ACTION" docs/bench.md` ≥ 1 and all three parked items are named
    - It states autonomous never fills the verdict / never picks a winner / never fakes a cell
    - The documented command matches the Plan-02 CLI (`vibemix bench run --study A`)
  </acceptance_criteria>
  <done>docs/bench.md documents the produce-and-park contract; the live run + verdict + rubric are explicitly parked KAAN-ACTION.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review.py → recorded cells + scores | pure JSON/in-memory → Markdown transform; no network, no API; caller owns the file write |
| docs → human reader | documents the produce-and-park contract; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-09 | Repudiation / integrity | the review surface naming a winner (pre-empting the hard human gate) | mitigate | the VERDICT section is literally empty (`> _Kaan fills this in…_`); no code path writes a winner; the grep gate in acceptance criteria enforces (RESEARCH Pitfall 5 — THE cardinal rule) |
| T-81-10 | Integrity | an errored cell rendered as fake output | mitigate | errored cells render `ERRORED — parked`; the renderer never synthesizes a line for a missing output (asserted in test_review.py) |
| T-81-11 | Information disclosure | the review surface or docs leaking the API key / personal track paths | mitigate | the renderer emits prompt+output+scores only (no key); path-scrub track filenames (mirror the Telegram path-scrub); docs reference the key by `.env` only, never a value |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | zero new packages |
</threat_model>

<verification>
- `pytest -q tests/bench/test_review.py` → green (BENCH-03 xfails flipped)
- `grep -in "winner" src/vibemix/bench/review.py` → no code writes a winner
- `docs/bench.md` exists with the KAAN-ACTION parking section + the produce command
- Full suite `pytest -q` exit 0, no new failures
</verification>

<success_criteria>
- BENCH-03 met: a ranked, human-readable KAAN-ACTION review surface generated from cells + scores; the VERDICT stays EMPTY
- The hard human gate honored: autonomous produces + ranks, never decides, never fakes
- The live produce path is documented (bounded, fail-safe); the verdict + taste-rubric wording are explicitly parked KAAN-ACTION
- Full suite green additively
</success_criteria>

<output>
Create `.planning/phases/81-bench-the-validation-instrument/81-04-SUMMARY.md` when done
</output>
