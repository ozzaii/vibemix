---
phase: 24-djay-pro-mac-overlay-highlight
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md
  - tauri/src-tauri/spike/Cargo.toml
  - tauri/src-tauri/spike/src/main.rs
  - tauri/src-tauri/spike/sign-and-test.sh
autonomous: true
requirements:
  - OVERLAY-02
must_haves:
  truths:
    - "WAVE-0-AX-SPIKE.md exists with explicit verdict: PASS or FAIL"
    - "Verdict was reached on a CODE-SIGNED bundle (not `tauri dev`)"
    - "AXUIElementCopyAttributeValue + kCGWindowBounds both tested against running djay Pro 5"
    - "Verdict explicitly answers: can the Rust parent read djay window rect AFTER user grants AX in System Settings, on the installed signed bundle, without re-prompt?"
    - "If PASS: 24-03 ships AX-precise positioning. If FAIL: 24-03 ships window-rect-fallback only. Phase still ships either way (per CONTEXT decision: spike FAIL is NOT a phase block)."
  artifacts:
    - path: ".planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md"
      provides: "Verdict doc with PASS/FAIL + raw test logs + screenshot evidence"
      contains: "Verdict:"
    - path: "tauri/src-tauri/spike/src/main.rs"
      provides: "Standalone Rust spike binary that calls AX + Quartz APIs against djay Pro"
      min_lines: 60
    - path: "tauri/src-tauri/spike/sign-and-test.sh"
      provides: "Bash script: cargo build --release → codesign with entitlements → install bundle → relaunch from Applications → log AX call result"
      min_lines: 20
  key_links:
    - from: "tauri/src-tauri/spike/src/main.rs"
      to: "macOS HIServices framework"
      via: "objc2-app-kit + objc2-core-foundation crates (or core-graphics + accessibility-sys equivalents)"
      pattern: "AXUIElementCopyAttributeValue|kAXFocusedApplicationAttribute|CGWindowListCopyWindowInfo"
    - from: "tauri/src-tauri/spike/sign-and-test.sh"
      to: "tauri/src-tauri/entitlements.plist"
      via: "codesign --entitlements"
      pattern: "codesign.*--entitlements"
---

<objective>
Wave 0 day-1 spike (per ROADMAP + CONTEXT D-Wave-0): verify the kyleawayan/djay-pro-bridge AX pattern works from a Tauri Rust parent process on a CODE-SIGNED installed bundle (not just `tauri dev`).

Pitfall 3 prevention. Output is a verdict doc that gates Plan 24-03's implementation strategy:
- **PASS** → 24-03 ships AX-driven precise knob-level positioning.
- **FAIL** → 24-03 degrades to percentage-of-window-rect coord_map fallback (still ships, accuracy degrades from "knob-precise" to "EQ-region-approximate").

Per CONTEXT D-Wave-0: spike FAIL is NOT a phase block. Per `feedback_autonomous_no_grey_area_pause`: degraded ship is acceptable; do not pause for permission, run the spike and record the verdict.

Purpose: closes Pitfall 3 (Tauri #8329 — sidecar AX permission inheritance broken; AX from Python sidecar would silently return empty data on installed binaries).

Output: `WAVE-0-AX-SPIKE.md` with verdict + standalone spike binary kept under `tauri/src-tauri/spike/` for future re-runs after macOS / Tauri updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-djay-pro-mac-overlay-highlight/24-CONTEXT.md
@.planning/research/PITFALLS.md
@tauri/src-tauri/Cargo.toml
@tauri/src-tauri/entitlements.plist
@tauri/src-tauri/tauri.conf.json5
@cohost_v4.py

<interfaces>
<!-- Reference Python pattern from cohost_v4.py (DO NOT MODIFY — POC reference per CLAUDE.md). -->
<!-- Equivalent must be reimplemented in Rust for this spike. -->

From cohost_v4.py (Python POC, ports to Rust for spike):
```python
from Quartz import (
    CGWindowListCopyWindowInfo,
    kCGWindowListOptionOnScreenOnly,
    kCGNullWindowID,
)

def find_djay_window_bounds():
    infos = CGWindowListCopyWindowInfo(kCGWindowListOptionOnScreenOnly, kCGNullWindowID)
    for w in infos:
        owner = (w.get("kCGWindowOwnerName") or "").lower()
        if "djay" not in owner:
            continue
        b = w.get("kCGWindowBounds")
        return (int(b["X"]), int(b["Y"]), int(b["Width"]), int(b["Height"]))
```

From entitlements.plist (LOCKED — already includes hardenedRuntime + disable-library-validation; AX requires no extra entitlement on Rust parent — TCC handles it):
- `com.apple.security.cs.disable-library-validation = true`
- `com.apple.security.cs.allow-unsigned-executable-memory = true`
- Bundle ID: `world.bravoh.vibemix` (LOCKED — TCC permission tied to this exact ID)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build standalone Rust spike binary that exercises AX + kCGWindowBounds against djay Pro</name>
  <files>tauri/src-tauri/spike/Cargo.toml, tauri/src-tauri/spike/src/main.rs, tauri/src-tauri/spike/.gitignore</files>
  <action>Create a NEW standalone Cargo binary crate at `tauri/src-tauri/spike/` (separate from the main `vibemix` crate so it does not pollute the shipping binary). Use ONE of these dep paths (research the Cargo registry — pick whichever has the cleaner API surface for AXUIElement on Rust 1.77, document the choice in the spike README header comment): (a) `objc2` + `objc2-app-kit` + `objc2-core-foundation` (modern objc2 family, currently 0.5.x), or (b) `core-graphics` + `core-foundation` + bindgen-style FFI to `HIServices` for `AXUIElementCreateApplication` / `AXUIElementCopyAttributeValue`. Spike main.rs MUST: (1) call `CGWindowListCopyWindowInfo(kCGWindowListOptionOnScreenOnly, kCGNullWindowID)` and print every window with owner containing "djay" — its kCGWindowBounds (X, Y, Width, Height). (2) Resolve djay Pro's running PID, build an `AXUIElementCreateApplication(pid)`, walk to the focused window via `kAXFocusedWindowAttribute`, then `kAXChildren` and recursively scan for `AXSlider` roles — print each slider's `kAXPositionAttribute` + `kAXSizeAttribute` + `kAXTitle`/`kAXDescription` if present. (3) Print "AX_PASS" or "AX_FAIL" on stdout with reason. Use `println!` for everything — no tracing infra in the spike. NO Python sidecar involvement — this is the Rust parent isolation test. Per D-Wave-0 the spike binary stays in repo under `tauri/src-tauri/spike/` for future re-runs after macOS / Tauri updates; add to `tauri/src-tauri/spike/.gitignore` only `target/`. NEVER edit `cohost_v4.py` (POC reference per CLAUDE.md) — the Python pattern is read-only inspiration.</action>
  <verify>
    <automated>cd tauri/src-tauri/spike && cargo build --release 2>&1 | tail -5 | grep -E "Compiling|Finished" && ls target/release/vibemix-ax-spike 2>/dev/null && echo "spike binary built"</automated>
  </verify>
  <done>Standalone spike crate compiles to a release binary that calls both `CGWindowListCopyWindowInfo` and `AXUIElement*` APIs. Binary runs from CLI without panicking when djay is NOT running (logs "djay not found, AX skipped").</done>
</task>

<task type="auto">
  <name>Task 2: Codesign the spike binary with entitlements + run on installed-bundle equivalent</name>
  <files>tauri/src-tauri/spike/sign-and-test.sh, tauri/src-tauri/spike/spike-entitlements.plist</files>
  <action>Create `tauri/src-tauri/spike/sign-and-test.sh` (chmod +x) that simulates the installed-binary AX inheritance chain Pitfall 3 warns about. Steps the script must execute in order: (1) cargo build --release in spike/ dir. (2) Build a minimal `.app` bundle wrapper around the spike binary at `spike/build/vibemix-ax-spike.app/Contents/MacOS/vibemix-ax-spike` with an Info.plist whose CFBundleIdentifier is `world.bravoh.vibemix.spike` (NEVER `world.bravoh.vibemix` — that ID is LOCKED per STATE; using it would corrupt the real app's TCC entries). (3) Copy `tauri/src-tauri/entitlements.plist` to `spike/spike-entitlements.plist` AS-IS (per Pitfall 3 — the spike must use the SAME entitlements the shipped app will use; AX needs no special entitlement, TCC governs it). (4) Codesign the .app with: `codesign --force --deep --options runtime --entitlements spike-entitlements.plist --sign "-" spike/build/vibemix-ax-spike.app` (ad-hoc sign with `-` is sufficient for the AX-inheritance test on Kaan's dev machine; full Developer ID notarization is Phase 21's job). (5) `cp -R spike/build/vibemix-ax-spike.app /Applications/` so it runs from /Applications/ (mirrors the real install location TCC keys against). (6) `open /Applications/vibemix-ax-spike.app --args --probe` and tail the Console.app log via `log stream --predicate 'process == "vibemix-ax-spike"'` for 10 s, redirect to `spike/probe.log`. (7) Print one of three terminal verdicts based on probe.log: "VERDICT_PASS" (kCGWindowBounds returned non-zero rect AND AX returned position), "VERDICT_PARTIAL" (kCGWindowBounds works but AX returned empty/null — fallback path), "VERDICT_FAIL" (both empty — Tauri #8329 confirmed). Script must be idempotent: rm -rf spike/build at top.</action>
  <verify>
    <automated>test -x tauri/src-tauri/spike/sign-and-test.sh && grep -q "world.bravoh.vibemix.spike" tauri/src-tauri/spike/sign-and-test.sh && grep -q "VERDICT_" tauri/src-tauri/spike/sign-and-test.sh && echo "sign-and-test.sh validated"</automated>
  </verify>
  <done>Script is executable, refuses to use the locked production bundle ID, and emits one of three explicit verdicts. Script runs end-to-end on Kaan's macOS rig (manual validation in Task 3 below).</done>
</task>

<task type="auto">
  <name>Task 3: Run the spike against djay Pro + write verdict doc with raw evidence</name>
  <files>.planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md, tauri/src-tauri/spike/probe.log</files>
  <action>Run the spike end-to-end on Kaan's rig: (1) Launch djay Pro 5 (windowed mode, NOT fullscreen — Pitfall 4 affects fullscreen separately and is Plan 24-05's job, not the spike's). (2) Execute `bash tauri/src-tauri/spike/sign-and-test.sh` → captures `spike/probe.log`. (3) If the spike's first run fails with "AX permission denied", grant `vibemix-ax-spike` AX permission in System Settings → Privacy & Security → Accessibility, then re-run ONCE. (4) Author `WAVE-0-AX-SPIKE.md` per this template — front-matter `verdict: pass | partial | fail`, sections: `## Verdict` (one paragraph), `## Test Setup` (macOS version, djay Pro version, signed/installed location, AX granted yes/no, single vs multi-monitor), `## Raw Evidence` (paste full probe.log inline OR reference `spike/probe.log` if >100 lines), `## What Plan 24-03 Must Do` (PASS = use AX-precise path with kCGWindowBounds as fallback when AX returns empty; PARTIAL = use kCGWindowBounds + percentage-of-window-rect coord_map only, skip AX entirely; FAIL = same as PARTIAL but log a louder warning + add a doc note in 24-05 toast referencing Tauri #8329). (5) Per `feedback_autonomous_no_grey_area_pause` + CONTEXT D-Wave-0: ship the verdict no matter which way it goes — do NOT pause for Kaan to "approve" the FAIL path; the fallback IS the approved path. Surface the FAIL outcome only as a Kaan-action-surfaced note in the Plan 24-05 README docs (Plan 26 README rewrite consumes that note). The spike Python `--probe` arg from Task 2 may need a `cohost_v4.py`-style "is djay running?" pre-check — if djay is not running, the spike skips AX testing and writes verdict `inconclusive_djay_not_running` (Kaan re-runs after launching djay).</action>
  <verify>
    <automated>test -f .planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md && grep -E "^verdict: (pass|partial|fail|inconclusive_djay_not_running)$" .planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md && grep -q "## Raw Evidence" .planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md && grep -q "## What Plan 24-03 Must Do" .planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md && echo "verdict doc complete"</automated>
  </verify>
  <done>WAVE-0-AX-SPIKE.md committed with explicit verdict, raw probe.log evidence (inline or referenced), and a directive paragraph telling Plan 24-03 which implementation path to take. If verdict = `inconclusive_djay_not_running`, Plan 24-03 begins with the PARTIAL path (safer default) and Kaan re-runs the spike at first available djay session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → spike binary | User-launched, runs in user session; AX call goes through TCC |
| Spike → djay Pro process | AX calls inspect another app's window tree; TCC-gated |
| Spike → Console.app log | Probe stdout/stderr captured to /tmp via os_log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | spike binary CFBundleIdentifier | mitigate | Hard-code `world.bravoh.vibemix.spike` (NEVER `world.bravoh.vibemix`); script greps for the locked ID before signing — if production ID detected, refuse to sign (avoids corrupting real app's TCC keychain entries) |
| T-24-01-02 | Information Disclosure | probe.log captures djay window titles | accept | Window titles include track names; logs are local to dev rig, never committed if >100 lines (referenced from spike/, gitignored) |
| T-24-01-03 | Elevation of Privilege | spike requesting AX permission | accept | TCC explicit grant; revocable by user; only reads window geometry (no input injection, no AXPress) |
| T-24-01-04 | Spoofing | Ad-hoc codesign vs Developer ID | accept | Spike is dev-machine only, never shipped; Phase 21 governs production signing |
</threat_model>

<verification>
- WAVE-0-AX-SPIKE.md exists with verdict in `pass | partial | fail | inconclusive_djay_not_running`.
- Spike binary is reproducible: `cd tauri/src-tauri/spike && cargo build --release` rebuilds without errors.
- `sign-and-test.sh` refuses production bundle ID `world.bravoh.vibemix`.
- Plan 24-03 has a directive paragraph telling it which implementation path to take.
- POC files (`cohost_v4.py`, etc.) UNTOUCHED — verified by `git status -- cohost_v4.py cohost_v3.py cohost_lk.py cohost_v2.py cohost.py mascot.html` showing no modifications.
</verification>

<success_criteria>
The phase has a binding answer to "does AX-from-Rust-parent work on a code-signed installed bundle?" Plan 24-03 can implement the AX bridge or the fallback without re-running discovery. The spike binary stays in-repo so future macOS / Tauri updates can re-validate the assumption with one bash invocation.
</success_criteria>

<output>
After completion, create `.planning/phases/24-djay-pro-mac-overlay-highlight/24-01-SUMMARY.md` summarizing:
- Spike verdict (pass/partial/fail/inconclusive)
- One-line directive for Plan 24-03 ("Use AX-precise path" / "Use window-rect-fallback only")
- Any blockers surfaced for Plan 24-05 (e.g., FAIL verdict → louder README note)
- POC files verified untouched
</output>
