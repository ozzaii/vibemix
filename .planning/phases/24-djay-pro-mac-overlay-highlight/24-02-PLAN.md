---
phase: 24-djay-pro-mac-overlay-highlight
plan: 02
type: execute
wave: 1
depends_on: [24-01]
autonomous: true
files_modified:
  - tauri/src-tauri/Cargo.toml
  - tauri/src-tauri/src/djay_ax.rs
  - tauri/src-tauri/src/overlay.rs
  - tauri/src-tauri/src/main.rs
  - tauri/src-tauri/capabilities/default.json
  - tauri/ui/src/overlay/overlay-highlight.ts
  - tauri/ui/src/overlay/overlay.html
  - tauri/ui/src/overlay/overlay-runtime.ts
  - tauri/ui/src/ipc/messages.schema.json
  - src/vibemix/ui_bus/schemas/overlay.py
  - src/vibemix/ui_bus/messages.py
  - src/vibemix/ui_bus/__init__.py
  - src/vibemix/agent/dj_cohost.py
  - tests/ui_bus/test_overlay_schema.py
  - tests/agent/test_overlay_publish.py
  - scripts/check_ipc_schema.py
requirements:
  - OVERLAY-01
must_haves:
  truths:
    - "AX queries live ONLY in tauri/src-tauri/src/djay_ax.rs (Rust parent — NEVER Python sidecar)"
    - "Overlay window is transparent + always-on-top + click-through (set_ignore_cursor_events(true))"
    - "ipc.session.overlay-highlight schema entry exists in messages.schema.json + Python wrapper + count-parity hold"
    - "[screen:<element>] citation in cohost output → ipc.session.overlay-highlight envelope published"
    - "Rust modules are macOS-guarded (cfg(target_os=macos)) — Windows still cargo-builds via stub"
    - "Tests deterministic — AX queries mocked, Tauri invoke mocked"
---

<objective>
Ship the overlay-highlight feature end-to-end: Rust AX module + Tauri overlay command + IPC schema + frontend handler + sidecar publish point.

When Gemini emits a valid `[screen:<element>]` citation, the Python sidecar publishes an `ipc.session.overlay-highlight` envelope; the Tauri frontend receives it and invokes the Rust `show_overlay_highlight` command, which queries djay Pro AX for the element's screen bounds and opens a transparent click-through always-on-top WebviewWindow that renders an amber ring CSS animation for the requested duration before closing.

Plan 24-01 verdict gating (PARTIAL fallback by default — promotes to AX-precise when verdict flips to PASS) is internal to `djay_ax.rs`: the module exposes a single function `query_element_bounds` that returns `Option<Rect>` regardless of which path it took.
</objective>

<context>
@.planning/phases/24-djay-pro-mac-overlay-highlight/24-CONTEXT.md
@.planning/phases/24-djay-pro-mac-overlay-highlight/24-01-SUMMARY.md
@.planning/phases/24-djay-pro-mac-overlay-highlight/WAVE-0-AX-SPIKE.md
@tauri/src-tauri/Cargo.toml
@tauri/src-tauri/src/mascot_window.rs
@src/vibemix/ui_bus/messages.py
@src/vibemix/agent/dj_cohost.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust AX query module — query_element_bounds + macOS guard</name>
  <files>tauri/src-tauri/Cargo.toml, tauri/src-tauri/src/djay_ax.rs, tauri/src-tauri/src/main.rs</files>
  <action>Create `tauri/src-tauri/src/djay_ax.rs` exposing `pub fn query_element_bounds(element_id: &str) -> Option<Rect>`. On macOS: walk djay Pro's AX tree to find the named element (mid_eq_a, low_eq_a, etc) — use accessibility-sys + core-graphics crates. Return `None` if djay Pro not running OR element not found OR AX denied. On Windows: stub returns `None` always. Add `accessibility-sys = "0.1"` + `core-graphics = "0.24"` + `core-foundation = "0.10"` to `tauri/src-tauri/Cargo.toml` under `[target.'cfg(target_os = "macos")'.dependencies]`. Wire `mod djay_ax;` into `main.rs`. Include `#[cfg(test)]` mod with one unit test that asserts `query_element_bounds("nonexistent")` returns `None` without panicking when djay Pro is absent. Implementation default = PARTIAL path (window-rect + percentage-of-window-rect coord_map for the 12 hand-mapped elements) — auto-promotes to AX-precise when WAVE-0-AX-SPIKE.md verdict flips to PASS (Plan 24-03 work). For 24-02 the percentage coord_map is the shipping path.</action>
  <verify>
    <automated>cd tauri/src-tauri && cargo check 2>&1 | tail -3 | grep -E "Finished|error" | grep -v error && echo "cargo-check OK"</automated>
  </verify>
  <done>djay_ax.rs compiles macOS + Windows; query_element_bounds returns Option&lt;Rect&gt;; one unit test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tauri overlay command — show_overlay_highlight</name>
  <files>tauri/src-tauri/src/overlay.rs, tauri/src-tauri/src/main.rs, tauri/src-tauri/capabilities/default.json, tauri/ui/src/overlay/overlay.html, tauri/ui/src/overlay/overlay-runtime.ts</files>
  <action>Create `tauri/src-tauri/src/overlay.rs` exposing `#[tauri::command] pub async fn show_overlay_highlight(app: AppHandle, element_id: String, color: String, duration_ms: u32) -> Result<(), String>`. Calls `djay_ax::query_element_bounds(&element_id)` to get the rect; if `None`, returns early `Ok(())` (graceful no-op). Otherwise opens a second `WebviewWindow` (label = stable per-element key) sized to the rect + small padding, with `.transparent(true)`, `.always_on_top(true)`, `.decorations(false)`, `.skip_taskbar(true)`, `.visible_on_all_workspaces(true)`, `.resizable(false)`. After build call `window.set_ignore_cursor_events(true)` so it's click-through. Spawns a Tokio task that sleeps `duration_ms` ms then closes the window. Loads `overlay.html` (transparent body, single absolutely-positioned amber ring div animated via CSS keyframe). Add the bare overlay HTML + tiny TS runtime that reads URL query params for color/duration. Wire `mod overlay;` into main.rs, register `overlay::show_overlay_highlight` in invoke_handler, add the label-prefix glob `"overlay-*"` to `capabilities/default.json` windows list. macOS-only: guard the AX-coord branch with `#[cfg(target_os = "macos")]` — the Tauri command itself stays cross-platform (no-op fallback on Windows for v2.0).</action>
  <verify>
    <automated>cd tauri/src-tauri && cargo check 2>&1 | tail -3 | grep -E "Finished|error" | grep -v error && echo "cargo-check OK"</automated>
  </verify>
  <done>show_overlay_highlight registered in invoke_handler; capability allowlist updated; cargo check clean.</done>
</task>

<task type="auto">
  <name>Task 3: IPC schema + Python wrapper for ipc.session.overlay-highlight</name>
  <files>src/vibemix/ui_bus/schemas/overlay.py, src/vibemix/ui_bus/messages.py, src/vibemix/ui_bus/__init__.py, tauri/ui/src/ipc/messages.schema.json, scripts/check_ipc_schema.py, tauri/ui/src/overlay/overlay-highlight.ts</files>
  <action>Schema-first: add `SessionOverlayHighlight` definition to `tauri/ui/src/ipc/messages.schema.json` — `type: const "ipc.session.overlay-highlight"`, payload `{element_id: string, color: string, duration_ms: integer ≥ 0}`. Add `{"$ref": "#/definitions/SessionOverlayHighlight"}` to the top-level oneOf. Then create `src/vibemix/ui_bus/schemas/overlay.py` with `SessionOverlayHighlightPayload` frozen+slots dataclass. Add `SessionOverlayHighlight` wrapper (with `.make(element_id, color, duration_ms)` + `.to_json()`) to `src/vibemix/ui_bus/messages.py`; re-export from `__init__.py`. Add the wrapper to `scripts/check_ipc_schema.py` `_minimal_examples` list so the count-parity gate stays green. Add a tiny TS handler at `tauri/ui/src/overlay/overlay-highlight.ts` that subscribes to `ipc.session.overlay-highlight` envelopes on the existing ws bus and calls `invoke('show_overlay_highlight', { element_id, color, duration_ms })`.</action>
  <verify>
    <automated>.venv/bin/python -c "from vibemix.ui_bus import SessionOverlayHighlight; m=SessionOverlayHighlight.make(element_id='waveform_a', color='amber', duration_ms=1300); print(m.to_json())" && .venv/bin/python scripts/check_ipc_schema.py 2>&1 | tail -3</automated>
  </verify>
  <done>SessionOverlayHighlight is publishable through the same validator pipeline as every other ipc.* message; schema count-parity gate passes.</done>
</task>

<task type="auto">
  <name>Task 4: Sidecar publish — wire dj_cohost.py to emit overlay-highlight on valid [screen:*] citations</name>
  <files>src/vibemix/agent/dj_cohost.py</files>
  <action>In `DJCoHostAgent.llm_node`, after the citation linter completes (post-stream, after the silence/slop suppression branch resolves, BEFORE the per-invocation dump at line ~617), parse `full_text` via `parse_citations()` (already imported). For each `(source, body)` pair where `source == "screen"`, extract the element_id (the body itself, no `@t` suffix per evidence_registry grammar). Skip if `citation_action != "emit"` (don't fire overlay for stripped/bypass/skip turns — the user didn't hear the citation, so the ring would be ghost-firing). Best-effort publish via `ipc_bus.emit(SessionOverlayHighlight.make(element_id, color="amber", duration_ms=1300).to_json_dict())` — wrap in try/except (telemetry path must never break LLM response path; pattern matches Plan 18-04 citation_count). Add a new `ipc_bus` kwarg to the agent's `__init__` (default None preserves backward compat); when None, the publish is a no-op. Threading the bus into the agent happens at the runtime/session_loop.py construction site, but that wiring is out of scope for this plan (Plan 24-03 work) — the kwarg slot is what 24-02 ships.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/agent/test_overlay_publish.py tests/ui_bus/test_overlay_schema.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>When Gemini emits a valid response containing `[screen:waveform_a]` AND ipc_bus is wired AND citation_action == "emit", the bus receives an ipc.session.overlay-highlight envelope with element_id="waveform_a".</done>
</task>

<task type="auto">
  <name>Task 5: Tests — overlay schema + overlay publish + djay_ax unit test</name>
  <files>tests/ui_bus/test_overlay_schema.py, tests/agent/test_overlay_publish.py</files>
  <action>`tests/ui_bus/test_overlay_schema.py` mirrors `test_citation_schema.py` pattern: schema oneOf entry exists, payload shape locked (3 fields, additionalProperties false), Python wrapper round-trip, extra-field rejection, count-parity invariant. `tests/agent/test_overlay_publish.py` — drives the agent post-stream gate with a fake ipc_bus (records emits), asserts: (1) valid [screen:el_a] citation + emit branch → 1 emit with element_id=el_a; (2) skip/strip branch → 0 emits; (3) no [screen:*] citation → 0 emits; (4) ipc_bus=None → no errors, no emits. Reuse the `_FakeRecorder` + agent-build helper pattern from `tests/agent/test_dj_cohost_linter.py`. Rust djay_ax test lives in-source via `#[cfg(test)] mod tests` from Task 1.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ui_bus/test_overlay_schema.py tests/agent/test_overlay_publish.py -q 2>&1 | tail -10 && cd tauri/src-tauri && cargo check 2>&1 | tail -3 | grep -E "Finished|error" | grep -v "error\[" && echo OK</automated>
  </verify>
  <done>All overlay-related tests pass deterministically (no live AX, no live Tauri invoke); cargo check stays clean.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | Information Disclosure | element_id parsing | mitigate | strict allowlist check in djay_ax.rs (element_id ∈ known 12 elements); unknown returns None (no AX call) |
| T-24-02-02 | Elevation of Privilege | overlay window click-through | mitigate | set_ignore_cursor_events(true) called AFTER build, before window.show(); transparent layer is read-only |
| T-24-02-03 | Tampering | overlay window label collision | mitigate | label prefix "overlay-*" reserved; legacy "main"/"mascot" labels untouched |
| T-24-02-04 | DoS | ring-spam from citation flood | accept | per-element 8s cooldown lives in Plan 24-03 ring-renderer scope; 24-02 ships the publish path only |
</threat_model>

<verification>
- `cd tauri/src-tauri && cargo check` clean.
- `.venv/bin/python -m pytest tests/ui_bus/test_overlay_schema.py tests/agent/test_overlay_publish.py -q` — all pass.
- `.venv/bin/python scripts/check_ipc_schema.py` — count-parity gate passes.
- POC files untouched (`git status -- cohost_v4.py cohost_v3.py cohost_lk.py cohost_v2.py cohost.py mascot.html`).
- Final `.venv/bin/python -m pytest -q` shows no NEW failures vs pre-plan baseline (1885 passed / 10 pre-existing failures).
</verification>

<success_criteria>
- AX queries live ONLY in Rust parent (`tauri/src-tauri/src/djay_ax.rs`).
- ipc.session.overlay-highlight is a first-class IPC envelope (schema + wrapper + tests).
- Sidecar publishes on valid [screen:*] citation only when the citation action is "emit".
- Rust + Python both compile cleanly; pytest deterministic; no regressions.
</success_criteria>

<output>
After completion: `.planning/phases/24-djay-pro-mac-overlay-highlight/24-02-SUMMARY.md` documenting Rust AX API, Tauri overlay command, IPC schema, sidecar publish point, and Kaan-action smoke test (live djay Pro run).
</output>
