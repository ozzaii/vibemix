---
phase: 98-live-audit-ear-pass
plan: 02
type: execute
wave: 2
depends_on: [98-01]
autonomous: true
requirements: [AUDIT-04]
tags: [smoke, sidecar, rc1-regression, envelope-namespace]
---

<objective>
Write `scripts/smoke/sidecar_bundle_smoke.sh` — rc1 standalone sidecar
smoke regression check. Verifies that v9.0's `learn.*` additions did NOT
regress the rc1 bundle/launchd fixes (`patch_livekit_agents_init.py` +
`sidecar.rs` std::process + spec blocklist), and that the mascot
envelope-namespace audit from P92
(`tauri/ui/tests/mascot/learn-envelope-doesnt-break-mascot.spec.ts`)
still passes.

Script must be runnable standalone (no GUI, no Tauri runtime) on Kaan's
Mac, exit 0 on green, exit non-zero with actionable diagnostics on red.
Documents the rc1-regression gate for future milestone close-outs.
</objective>

<context>
@.planning/phases/98-live-audit-ear-pass/98-CONTEXT.md
@.planning/handoffs/2026-05-27-session-end.md
</context>

<tasks>

<task id="98-02-01" type="auto">
<what>Create `scripts/smoke/` directory + `scripts/smoke/sidecar_bundle_smoke.sh` rc1 regression smoke script.</what>
<why>rc1 ship landed 3 critical fixes (livekit-agents circular-import patch + sidecar.rs std::process spawn rewrite + PyInstaller spec blocklist). v9.0 added 13 `learn.*` IPC envelopes + ExemplarPlayer's second sd.OutputStream + new learn-window WebviewWindow + new learn_messages.py module. None of those should regress rc1, but no single script verifies that today. AUDIT-04 closes that gap: one script that future milestones can re-run.</why>
<how>
1. Create `scripts/smoke/` directory.
2. Create `scripts/smoke/sidecar_bundle_smoke.sh` bash script that does the following IN ORDER, exiting non-zero on first failure with a clear `[smoke] FAIL: <reason>` line to stderr:
   a. **Pre-flight**: assert `tauri/src-tauri/binaries/vibemix-core-*-apple-darwin/` exists on macOS (or `*-pc-windows-msvc/` on Windows). If not, suggest `uv run python scripts/build_sidecar.py --spec vibemix-core.macos.spec` and exit 2.
   b. **livekit patch check**: invoke `uv run python scripts/dist/patch_livekit_agents_init.py --check` (or equivalent — verify by reading the patch script; if no --check flag, just re-run it and confirm idempotent no-op on second run).
   c. **Frontend mascot envelope namespace test**: `cd tauri/ui && npx vitest run tests/mascot/learn-envelope-doesnt-break-mascot.spec.ts --reporter=verbose`. Must exit 0.
   d. **Frontend IPC schema parity**: `cd tauri/ui && npm run codegen:ipc` should be no-op (already regenerated). Then `python scripts/check_ipc_schema.py` exits 0 (77/77 oneOf parity).
   e. **Standalone sidecar boot test**: run `tauri/src-tauri/binaries/vibemix-core-<triple>/vibemix-core-<triple> --help </dev/null` with `timeout 10s`. Must exit 0 within timeout (proves the livekit-agents circular import is patched + spec blocklist doesn't dead-strip CLI). Catch the exit code and surface; print first 20 lines of stdout.
   f. **Standalone wizard boot**: same as (e) with `--wizard --help`. Some flags may exit 0; the goal is "process boots and prints something within 10s" not "completes a real wizard flow."
   g. **Standalone session probe**: same as (e) with `--session --help` or `--session --dry-run` if such flag exists; otherwise just check the binary recognizes the flag without an ImportError stack.
   h. **rc1 spec blocklist still in place**: grep `tauri/src-tauri/vibemix-core.macos.spec` for `excludes = [` block; assert it contains key entries (e.g. `'jupyter'`, `'matplotlib'`, `'pandas'`) — confirms spec wasn't accidentally reset.
   i. **rc1 sidecar.rs std::process pin**: grep `tauri/src-tauri/src/sidecar.rs` for `std::process::Command` (not `tokio::process` or `app.shell().command()`). Confirms the rc1 fix posture.
   j. **`learn.*` namespace audit**: grep `tauri/ui/src/ipc/messages.schema.json` for occurrences of `ipc.learn.` — count must be ≥13 (one per envelope: controller_detected + midi_position + start_course + start_lesson + complete_lesson + lesson_loaded + highlight + advance + ack + tutor_speak + exemplar_play + exemplar_stop + progress_state).
3. Make script executable: `chmod +x scripts/smoke/sidecar_bundle_smoke.sh`.
4. Surface in `scripts/smoke/README.md` (new — 1-paragraph "what this is + when to run").
5. Run the script once to confirm it exits 0 on the current tree. If any sub-check is genuinely red (e.g. `tests/sidecar/test_build_sidecar_rename.py` baseline drift carried from P93), document the SCRIPT-VS-PRE-EXISTING-DRIFT distinction in the script comments and either skip the check or flag with `[smoke] WARN: pre-existing baseline drift — non-blocking` rather than failing.
6. Verify the mascot envelope test passes alone: `cd tauri/ui && npx vitest run tests/mascot/learn-envelope-doesnt-break-mascot.spec.ts`.
</how>
<files>
  - scripts/smoke/sidecar_bundle_smoke.sh
  - scripts/smoke/README.md
</files>
<verify>
test -x scripts/smoke/sidecar_bundle_smoke.sh
test -f scripts/smoke/README.md
bash scripts/smoke/sidecar_bundle_smoke.sh; echo "exit: $?"
</verify>
<done>Script exists, executable, exits 0 on the current tree, all 10 sub-checks pass or are clearly documented as pre-existing-drift WARN.</done>
</task>

</tasks>

<success_criteria>
- `scripts/smoke/sidecar_bundle_smoke.sh` exists and is executable.
- `scripts/smoke/README.md` documents usage.
- Script exits 0 on the current tree (or clearly logs WARNs for pre-existing baseline drift).
- Mascot envelope namespace test passes alone.
- 10 sub-checks documented inline.
</success_criteria>

<output_spec>
SUMMARY.md documents:
- Sub-check pass/fail per item.
- Pre-existing baseline drift findings (if any).
- Confirmation that the rc1 fixes (livekit patch + sidecar.rs std::process + spec blocklist) are intact.
- Confirmation that the mascot envelope namespace audit from P92 still passes.
</output_spec>
