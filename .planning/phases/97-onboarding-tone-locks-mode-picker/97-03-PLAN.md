---
phase: 97-onboarding-tone-locks-mode-picker
plan: 03
type: execute
wave: 2
depends_on: [97-01, 97-02]
autonomous: true
requirements:
  - ONBOARD-04
  - ONBOARD-05
  - ONBOARD-06
  - ONBOARD-07
  - RENDER-08
tags: [wizard, headphone-picker, audio-routing-doc, progress-list-ui, disclaimer, trademarks]
---

<objective>
Land four surfaces in one plan (they share a frontend test gate and the
trademark disclaimer crosses two of them):

1. **Headphone device picker** in the existing wizard — extends `step2-output-device.ts`
   with a dedicated headphone-output dropdown that writes `learn.headphone_device_index`
   via `ipc.settings.set` (the field landed in P93-03).
2. **Advanced audio-routing doc** at `docs/audio-routing.md` (BlackHole + Multi-Output Device
   recipes for the headphones-via-DJ-software flow) + park
   `§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` KAAN-ACTION.
3. **Lesson progress list UI** at `tauri/ui/src/learn/lesson/progress-list.ts` — picks up
   where the user left off, replay any completed lesson, keyboard-nav-able.
4. **Disclaimer copy** (RENDER-08 + ONBOARD-07) — app footer + repo README
   "Trademarks" section, gated by `tests/learn/test_disclaimer_present.py`.
</objective>

<context>
@.planning/phases/97-onboarding-tone-locks-mode-picker/97-CONTEXT.md
@tauri/ui/src/wizard/step2-output-device.ts
@tauri/ui/src/learn/learn-window.ts
@tauri/ui/src/learn/lesson/hud.ts
@src/vibemix/runtime/config_store.py
@README.md
</context>

<tasks>

<task id="97-03-01" type="auto">
<what>Extend `step2-output-device.ts` with a headphone-device-picker row.</what>
<why>Beginners do exemplar listening on headphones; the existing wizard already picks the master OUTPUT device. The headphone picker is a SECOND device-dropdown that writes `learn.headphone_device_index` independently. Default = "system default" (null on the wire).</why>
<how>
1. Open `tauri/ui/src/wizard/step2-output-device.ts`.
2. Extend `Step2State` with `selectedHeadphoneDeviceIndex: number | null;` (null = system default).
3. Extend `Step2Callbacks` with `onSelectHeadphoneDevice: (idx: number | null) => void;`.
4. In `renderStep2`, after the AudioTestButton block (before the WindowPicker), append a new sub-section:
   - Subheading: `"Tutor exemplar playback (headphones)"` (lowercase tone-disciplined).
   - Helper text: `"Beginner lessons play short audio examples — pick where they should come out."`
   - DropdownDevice mirror — same component, distinct callback, prefixed by a "[ system default ]" pseudo-option that maps to `null`.
5. In the parent (likely `router.ts` or `onboarding-flow.ts`) wire the callback to emit `ipc.settings.set { field: "learn.headphone_device_index", value: idx }` (the envelope ALREADY accepts this field per the schema enum landed in P93).
6. Add a vitest test `tauri/ui/tests/wizard/test_step2_headphone_picker.spec.ts` covering: picker visible, system-default default, click emits the right ipc envelope.
</how>
<files>
  - tauri/ui/src/wizard/step2-output-device.ts
  - tauri/ui/src/wizard/router.ts  # OR onboarding-flow.ts — whichever owns Step2Callbacks plumbing
  - tauri/ui/tests/wizard/test_step2_headphone_picker.spec.ts
</files>
<verify>cd tauri/ui && npm test -- test_step2_headphone_picker</verify>
<done>Headphone picker visible; click writes the right envelope field.</done>
</task>

<task id="97-03-02" type="auto">
<what>Write `docs/audio-routing.md` covering BlackHole + Multi-Output Device recipes.</what>
<why>Beginner DJs who want tutor exemplars on headphones while the master output drives club PA need an advanced macOS audio-routing setup. The wizard's headphone picker is the v1 surface; this doc covers the deeper setup that the wizard does not automate (Multi-Output Device, BlackHole loopback, Aggregate Device).</why>
<how>
1. Create `docs/audio-routing.md`.
2. Sections:
   - **Goal** — the user has 1 master output (CDJ / DJ software speaker) and a SEPARATE tutor-exemplar headphones output.
   - **Recipe 1 (simple, default)** — pick headphones in the wizard's headphone-device row. Tutor exemplars route there directly. DJ software keeps owning the master.
   - **Recipe 2 (BlackHole loopback for vibemix to listen)** — same as Recipe 1, BUT vibemix also listens to the master via BlackHole 2ch (already documented for the live co-host). Cross-link to the BlackHole install in step 2 of the wizard.
   - **Recipe 3 (advanced — Multi-Output Device)** — for users who want headphones to ALSO carry the master mix (cue-listen pattern). Walks Audio MIDI Setup → create Aggregate / Multi-Output Device → pick that as the system default.
3. End with a `§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` callout: "The wizard does not yet automate Recipe 3. If we ship a guided Multi-Output Device setup later, this doc will reflect the shipped flow."
4. Park the KAAN-ACTION in `.planning/REQUIREMENTS.md` (or wherever the queue lives).
</how>
<files>
  - docs/audio-routing.md
  - .planning/REQUIREMENTS.md
</files>
<verify>ls -la docs/audio-routing.md && grep -n "LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE" .planning/REQUIREMENTS.md</verify>
<done>Doc reads cleanly; KAAN-ACTION parked.</done>
</task>

<task id="97-03-03" type="auto">
<what>Build the lesson progress list UI at `tauri/ui/src/learn/lesson/progress-list.ts`.</what>
<why>Users need to see all 36 lessons (16+14+6), pick up where they left off, and replay any completed lesson. Empty-state copy "no controller? plug one in" (lowercase, tone-disciplined). Keyboard-nav for browsing without hardware.</why>
<how>
1. Create `tauri/ui/src/learn/lesson/progress-list.ts`.
2. Export `renderProgressList(progress: ProgressStatePayload, callbacks: { onPickLesson: (lessonId: string) => void; onReset: () => void; })`.
3. Consume `ipc.learn.progress_state` — the payload structure is in `messages.schema.json::LearnProgressState.payload.progress` (lessons / courses dicts).
4. Render per-course groupings: Course 1 (Anatomy, 16), Course 2 (Transitions, 14), Course 3 (Play Mode, 6). Each lesson gets a dot: empty / in-progress / completed (existing tokens — `--silk-22`, `--silk-40`, `--amber`).
5. Click a lesson → emit `ipc.learn.start_lesson { lesson_id, level: "fresh" | "replay" }` (replay if already completed).
6. Keyboard-nav: arrow keys move focus, enter activates, Esc closes if mounted in a popup. Make every dot a `<button>` for accessibility.
7. Empty-state: when `progress.lessons` is empty AND no controller detected, mount a single line "no controller? plug one in" — lowercase verbatim.
8. Mount the list in `tauri/ui/src/learn/learn-window.ts` — recommended: append after the `LessonHud` (collapses when a lesson is loaded; expands when none is active).
9. Add a vitest test `tauri/ui/tests/learn/test_progress_list.spec.ts` covering: dot states, click → start_lesson emit, empty-state text verbatim, keyboard nav.
</how>
<files>
  - tauri/ui/src/learn/lesson/progress-list.ts
  - tauri/ui/src/learn/learn-window.ts
  - tauri/ui/tests/learn/test_progress_list.spec.ts
</files>
<verify>cd tauri/ui && npm test -- test_progress_list</verify>
<done>List renders, click → start_lesson, empty-state copy verbatim, keyboard-nav works.</done>
</task>

<task id="97-03-04" type="auto">
<what>Add the disclaimer copy to the app footer (frontend) and the repo README (Trademarks section).</what>
<why>RENDER-08 + ONBOARD-07 — distributing controller render images requires a nominative-fair-use posture. The disclaimer crosses both the app and the project page.</why>
<how>
1. Locked copy (VERBATIM, do NOT paraphrase): `Visual representation for instructional use. DDJ-FLX4, XDJ-RX3, etc. are trademarks of AlphaTheta / Pioneer DJ. Inpulse is a trademark of Hercules. Numark is a trademark of inMusic Brands. vibemix is not affiliated with or endorsed by these manufacturers.`
2. **App footer surface:** find the appropriate footer / about / first-launch acknowledgement slot. Recommended location: a small `.learn-footer` block at the bottom of the Learn window OR in the about / settings drawer. Render as static text, no link, font-size small. CSS via existing tokens (`--silk-40` color, JetBrains Mono).
   - Likely files: `tauri/ui/src/learn/learn-window.ts` (mount a footer slot) OR `tauri/ui/src/learn/styles/learn.css` (style the footer).
3. **README surface:** open `/Users/ozai/projects/dj-set-ai/README.md`. Add a `## Trademarks` section near the end (after the license / contributing section). Same verbatim copy.
4. The disclaimer is required to be present in BOTH surfaces — the test in the next task gates this.
</how>
<files>
  - tauri/ui/src/learn/learn-window.ts  # mount footer
  - tauri/ui/src/learn/styles/learn.css  # style footer (optional — could be inline)
  - README.md
</files>
<verify>grep -n "Visual representation for instructional use" README.md tauri/ui/src/learn/learn-window.ts</verify>
<done>Copy present in both surfaces, verbatim.</done>
</task>

<task id="97-03-05" type="auto">
<what>Add `tests/learn/test_disclaimer_present.py` to gate the disclaimer presence.</what>
<why>If a future refactor removes the copy from either surface, the test reds — keeps the nominative-fair-use posture intact.</why>
<how>
1. Create `tests/learn/test_disclaimer_present.py`.
2. The test:
   ```python
   from pathlib import Path

   DISCLAIMER_FRAGMENT = "Visual representation for instructional use."

   def test_disclaimer_in_readme():
       readme = Path("README.md").read_text(encoding="utf-8")
       assert DISCLAIMER_FRAGMENT in readme, "README.md missing Trademarks disclaimer"

   def test_disclaimer_in_learn_window():
       # Walk tauri/ui/src/learn/ for ANY .ts file containing the fragment.
       learn_root = Path("tauri/ui/src/learn")
       hit = False
       for ts_file in learn_root.rglob("*.ts"):
           if DISCLAIMER_FRAGMENT in ts_file.read_text(encoding="utf-8"):
               hit = True
               break
       assert hit, "Disclaimer fragment not found in any tauri/ui/src/learn/**/*.ts"
   ```
3. Repo-root invocation: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/learn/test_disclaimer_present.py -v`
</how>
<files>
  - tests/learn/test_disclaimer_present.py
</files>
<verify>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/learn/test_disclaimer_present.py -v</verify>
<done>Both tests pass.</done>
</task>

<task id="97-03-06" type="auto">
<what>Run the full Python + frontend gate.</what>
<why>P97 ship-ready check.</why>
<how>
1. `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/learn/ tests/midi/ tests/runtime/ -q`
2. `cd tauri/ui && npm run build && npm test`
3. `cd tauri/src-tauri && cargo check`
4. Address tsc / vitest / pytest failures inline (Rule 1 / Rule 3 deviations as discovered).
</how>
<files></files>
<verify>see commands above; all green</verify>
<done>All tests pass; cargo green; phase shippable.</done>
</task>

</tasks>

<success_criteria>
- Headphone picker visible in wizard; click emits `ipc.settings.set { field: "learn.headphone_device_index" }`.
- `docs/audio-routing.md` covers 3 recipes; `§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` parked.
- Lesson progress list UI mounted; dots / replay / keyboard-nav work.
- Disclaimer copy verbatim in README + Learn window footer; `test_disclaimer_present.py` green.
- Full suite green.
</success_criteria>

<output_spec>
SUMMARY.md documents:
- Where the headphone-picker callback was wired (router.ts vs onboarding-flow.ts).
- Where the disclaimer footer was mounted (file + element).
- Test counts (vitest + pytest) before vs after.
- KAAN-ACTION parks.
</output_spec>
