---
phase: 57-sexify-finish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/ui/src/wizard/__tests__/first-run-continuity.spec.ts
  - tauri/ui/src/wizard/router.ts
  - tauri/ui/src/wizard/copy.json
  - tauri/ui/src/wizard/copy.ts
  - docs/internal/first-run-friction-audit.md
autonomous: true
requirements: [POLISH-03]
must_haves:
  truths:
    - "Driving the wizard STEP_ORDER chain from the first step to smoke-test never hits a dead-end (every step renders and exposes a next action until the terminal step)"
    - "The fresh-account first-run path (intro → … → first-session-with-audio) is mapped, and every code-fixable friction point found is either fixed or documented with a reason"
    - "The forewarning/driver-fetch/48k-probe component question (present in tree but absent from STEP_ORDER) is resolved: either wired in if it removes friction, or documented as installer-only/intentional"
    - "Any new or edited wizard copy passes the anti-slop blocklist"
  artifacts:
    - path: "tauri/ui/src/wizard/__tests__/first-run-continuity.spec.ts"
      provides: "Vitest continuity smoke driving router STEP_ORDER intro→…→smoke-test with no dead-end"
      contains: "STEP_ORDER"
    - path: "docs/internal/first-run-friction-audit.md"
      provides: "The fresh-account walk audit: friction points, fixes applied, deferrals with reasons, the forewarning/driver-fetch/48k-probe resolution"
      contains: "forewarning"
  key_links:
    - from: "tauri/ui/src/wizard/__tests__/first-run-continuity.spec.ts"
      to: "tauri/ui/src/wizard/router.ts"
      via: "import STEP_ORDER / advanceTo / currentStep and drive the chain"
      pattern: "STEP_ORDER|advanceTo|currentStep"
  kaan_action:
    - "Real fresh macOS account first-run → first-session-with-audio walk on Kaan's Mac: the felt 'no friction / no dead-ends' sign-off (engineering proves continuity headlessly + fixes code-fixable friction; the lived walk is Kaan's)"
    - "Sidecar binary staleness (bundled binary predates line-buffer/watchdog fixes) is a Phase 58 ship-artifact dependency, NOT fixed here — documented in the audit as a dependency to surface if the live walk hits log lag"
---

<objective>
Audit the fresh-account first-run → first-session flow for friction, fix the code-fixable points (copy / step-order / dead-ends / missing guidance), resolve the forewarning/driver-fetch/48k-probe question, and pin a continuity smoke that proves the wizard chain reaches its terminal step with no dead-end.

Purpose: POLISH-03. A fresh user must reach a live session without a confusing or dead-end step before audio is live. Engineering closes the code-fixable friction and proves continuity headlessly; the felt "no friction" walk on a real fresh account is KAAN-ACTION.
Output: a continuity smoke vitest, a friction-audit doc, and any in-place copy/step-order fixes the audit surfaces (all anti-slop-clean).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-sexify-finish/57-RESEARCH.md

<scope_guardrails>
- Do NOT touch Kaan's WIP files (`tauri/src-tauri/src/mascot_window.rs`, `tauri/src-tauri/tauri.conf.json5`, `src/vibemix/__main__.py`, `agent/dj_cohost.py`, `audio/constants.py`, `prompts/matrix.py`, `tests/agent/test_*`). This plan is UI-wizard + docs only — naturally satisfied.
- This is a CLIENT-tier flow audit (per RESEARCH Responsibility Map): step order, copy, dead-ends are pure `tauri/ui/src/wizard/*` concerns. Do NOT change the sidecar IPC contract (`runtime/wizard.py` handlers stay fixed).
- ANY new/edited wizard copy MUST pass the anti-slop blocklist. NEVER relax the gate (STATE.md Risk #5). Tune the copy. Banned-token reference: `docs/internal/copy-substitutions.md`. Edit copy via `copy.json` + `copy.ts` (the typed single-source loader) — no inline strings (`test_no_inline_strings_install.py` enforces this).
- Sidecar binary rebuild is OUT of scope (Phase 58 ship-artifact territory per RESEARCH Open Question 1 / Assumption A1). Note it as a dependency in the audit; do NOT run `scripts/build_sidecar.py`.
- This plan MAY use a worktree (shares zero files with Kaan's Tauri/python WIP) per CONTEXT. If a worktree is used, sync main first (`git fetch origin main && git merge origin/main --no-edit`) per the worktree invariant.
- Prefer minimal, surgical edits. If the audit finds NO code-fixable friction beyond the continuity smoke, that is a valid outcome — document the clean walk; do not invent edits.
</scope_guardrails>

<interfaces>
<!-- Wizard navigation surface, read this session — tauri/ui/src/wizard/router.ts -->
const STEP_ORDER: WizardStep[] = [          // line 152
  "permissions", "audio", "controller",
  "profile-consent", "telemetry-consent", "smoke-test",
];
// "intro" and "smoke-test" own the full surface (no step strip) — router.ts:316-323
export function advanceTo(next: WizardStep): void   // line 203
export function currentStep(): WizardStep           // line 256
// goNext()-style chain: each step's onContinue/onBegin calls advanceTo(nextStep) — router.ts:329+
//   intro.onBegin -> advanceTo("permissions"); permissions.onContinue -> advanceTo("audio"); ...
//   smoke-test -> advanceTo("telemetry-consent")  (note: the back/forward ordering — verify in router)
// Wizard done -> emitIpc ipc.wizard.done + invoke write_first_run_state (router.ts:19)

<!-- Components present in tree but ABSENT from STEP_ORDER (Phase 49 installer companion) -->
//   tauri/ui/src/wizard/step-forewarning.ts
//   tauri/ui/src/wizard/step-driver-fetch.ts
//   tauri/ui/src/wizard/step-48k-probe.ts
//   RESEARCH Open Question 3: are these meant to be in the in-app flow,
//   or installer-only? The audit resolves this by reading the Phase 49
//   wiring + installer flow.

<!-- Existing wizard specs (homes/patterns) -->
//   tauri/ui/src/wizard/__tests__/onboarding-flow.spec.ts  (tests the OnboardingFlow class — a SEPARATE 4-step abstraction, NOT router STEP_ORDER)
//   tauri/ui/src/wizard/__tests__/blackhole-step.spec.ts, windows-smartscreen-step.spec.ts, tcc-permissions.spec.ts
//   copy loader: tauri/ui/src/wizard/copy.json + copy.ts (typed); tests/wizard/test_no_inline_strings_install.py enforces no inline copy
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Friction audit of fresh-account first-run → first-session</name>
  <files>docs/internal/first-run-friction-audit.md, tauri/ui/src/wizard/router.ts, tauri/ui/src/wizard/copy.json, tauri/ui/src/wizard/copy.ts</files>
  <read_first>
    - tauri/ui/src/wizard/router.ts (FULL — STEP_ORDER, advanceTo, the per-step onContinue/onBegin chain at ~329+, the back()/esc handling, the wizard-done path)
    - tauri/ui/src/wizard/step0-intro.ts, step1-permissions.ts, step2-output-device.ts, step3-controller.ts, step-profile-consent.ts, step-telemetry-consent.ts, smoke-test.ts (each step's continue/next affordance + any dead-end branch where a user could get stuck before audio is live)
    - tauri/ui/src/wizard/step-forewarning.ts, step-driver-fetch.ts, step-48k-probe.ts (the absent-from-STEP_ORDER components — determine intent)
    - tauri/ui/src/wizard/copy.json + copy.ts (the typed copy source; how strings are keyed)
    - docs/internal/copy-substitutions.md (the anti-slop banned-token list)
    - 57-RESEARCH.md Open Questions 1+3, Assumptions A1+A5, Pitfall 6, Runtime State Inventory (the `visible:false` mascot persisted-state dead-end note + tray recovery)
  </read_first>
  <action>
Walk the fresh-account path on paper from `intro` through each STEP_ORDER step to `smoke-test` → wizard done → first session. For each step, identify: (1) is there always a forward affordance (a way to continue), or can a fresh user dead-end before audio is live? (2) is the copy clear and non-confusing, and does it pass the anti-slop blocklist? (3) are there dead links / missing guidance?

Resolve RESEARCH Open Question 3 (the forewarning/driver-fetch/48k-probe components absent from STEP_ORDER): read their wiring + the Phase 49 installer flow. Decide and DOCUMENT one of: (a) they are installer-companion-only (separate from the in-app wizard) — record the boundary; or (b) one or more removes real first-run friction and should be wired into STEP_ORDER — if so, wire it in surgically (add to STEP_ORDER + the advanceTo chain + the step strip config) and note WHY. Default to NOT adding steps unless the audit shows a concrete friction the step removes (adding steps is itself friction).

Resolve the persisted `visible:false` mascot dead-end (RESEARCH Runtime State Inventory): confirm the tray "Toggle Mascot" recovery exists and is discoverable; code-edit ONLY if it is not discoverable (otherwise document that recovery exists).

Apply any code-fixable friction fixes found:
- Copy fixes go through `copy.json` + `copy.ts` (no inline strings); every changed/added string must pass the anti-slop blocklist (run `python3 scripts/audit/check_no_slop_install.py` mentally against the banned-token list, then for real in verify).
- Step-order / dead-end fixes go in `router.ts` (minimal, surgical — preserve the `intro`/`smoke-test` full-surface special-casing and the step-strip denominator logic at lines 165-201).

Write `docs/internal/first-run-friction-audit.md`: the mapped flow (step-by-step), each friction point found with disposition (FIXED / DEFERRED-with-reason / NOT-A-GAP), the forewarning/driver-fetch/48k-probe resolution, the sidecar-binary-staleness Phase-58 dependency note, and the KAAN-ACTION real-walk carveout. Use NO anti-slop banned tokens in the doc itself.
  </action>
  <acceptance_criteria>
    - [ ] `docs/internal/first-run-friction-audit.md` exists and maps the full intro→smoke-test→first-session flow with a disposition for every friction point.
    - [ ] The forewarning/driver-fetch/48k-probe question is explicitly resolved in the doc (installer-only OR wired-in-with-reason).
    - [ ] The sidecar-binary-staleness Phase-58 dependency is noted in the doc (NOT fixed here).
    - [ ] Any copy change is in `copy.json`/`copy.ts` (no inline strings); any step-order/dead-end change is a minimal router.ts edit.
    - [ ] `python3 scripts/audit/check_no_slop_install.py` passes (no banned tokens in new/edited copy OR the audit doc).
    - [ ] No Kaan WIP file touched; the sidecar IPC contract (`runtime/wizard.py`) is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>source .venv/bin/activate && python3 scripts/audit/check_no_slop_install.py && cd tauri/ui && npm test -- wizard</automated>
  </verify>
  <done>The fresh-account flow is mapped; every code-fixable friction is fixed or documented with a reason; the absent-step question is resolved; anti-slop is green; the real-account walk is recorded as KAAN-ACTION (must_haves).</done>
</task>

<task type="auto">
  <name>Task 2: First-run continuity smoke (vitest)</name>
  <files>tauri/ui/src/wizard/__tests__/first-run-continuity.spec.ts</files>
  <read_first>
    - tauri/ui/src/wizard/router.ts (STEP_ORDER, advanceTo, currentStep, the per-step advance chain, the rerender/switch at ~329+; identify how to drive a step transition headlessly in jsdom)
    - tauri/ui/src/wizard/__tests__/onboarding-flow.spec.ts (existing spec STYLE; note it tests OnboardingFlow not router — your spec targets router STEP_ORDER specifically)
    - tauri/ui/src/wizard/__tests__/tcc-permissions.spec.ts (pattern for rendering a wizard surface in jsdom + asserting affordances)
    - tauri/ui/vitest.config.ts (jsdom env confirm)
  </read_first>
  <action>
Create `tauri/ui/src/wizard/__tests__/first-run-continuity.spec.ts` — a vitest continuity smoke proving the wizard chain reaches its terminal step with no dead-end (POLISH-03's "no dead-end before audio live", per RESEARCH Test Map).

Drive the router from the first STEP_ORDER step through to `smoke-test`:
- Mount the wizard surface in jsdom (follow the render/mount idiom the existing wizard specs use; mock IPC/invoke where a step calls into Tauri so the smoke runs headless without a sidecar — the existing specs already establish how IPC is stubbed).
- For each step in STEP_ORDER, assert: (a) the step renders into the primary mount, and (b) a forward affordance exists (the continue/next control that calls `advanceTo` to the next step) — i.e., the user is never stuck. Drive the chain forward (invoke each step's continue path or call `advanceTo(nextStep)`) and assert `currentStep()` advances accordingly until `smoke-test` is reached.
- Assert the chain terminates at `smoke-test` (the terminal full-surface step) and that the wizard-done affordance ("Open vibemix" / `ipc.wizard.done` emit) is reachable from there.
- Keep it a SMOKE: assert continuity + no dead-end, not deep per-step behavior (those are covered by the existing step specs). It must run in the standard `npm test` (no watch flag, no opt-in marker).
If a step legitimately gates (e.g., permissions must be granted before continue), drive it into the satisfied state via the stubbed IPC, then assert the affordance unlocks — a gated-then-unlockable step is continuity, a permanently-blocked step is a dead-end and must fail the test.
  </action>
  <acceptance_criteria>
    - [ ] `tauri/ui/src/wizard/__tests__/first-run-continuity.spec.ts` exists and runs under the default `npm test` (jsdom, no watch flag, no opt-in marker).
    - [ ] It drives the router across the full STEP_ORDER and asserts each step renders + exposes a forward affordance (no dead-end).
    - [ ] It asserts the chain reaches `smoke-test` and the wizard-done / "Open vibemix" affordance is reachable.
    - [ ] Gated steps are driven into their satisfied state via stubbed IPC and asserted to unlock (a permanently-blocked step would fail the test).
    - [ ] The full 711+ baseline (now including this spec and Task 1's additions) is green.
  </acceptance_criteria>
  <verify>
    <automated>cd tauri/ui && npm test -- first-run-continuity.spec.ts && npm test</automated>
  </verify>
  <done>A headless smoke proves the wizard reaches its terminal step with a forward affordance at every step (no dead-end); the felt fresh-account walk stays KAAN-ACTION.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard webview → sidecar (WS bus) | The wizard calls sidecar IPC handlers; this plan does NOT change that contract (client-tier only). |
| new/edited copy → anti-slop gate | All wizard copy crosses the anti-slop quality contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Tampering (quality contract) | wizard copy | mitigate | Every new/edited string passes `check_no_slop_install.py`; the gate is NEVER relaxed (STATE.md Risk #5); copy goes through `copy.json`/`copy.ts` (no inline strings, enforced by `test_no_inline_strings_install.py`). |
| T-57-05 | Information Disclosure | first-run debugging | mitigate | The audit reads ONLY vibemix's own wizard source; never Kaan's OZ/Hermes/LM Studio paths (CLAUDE.md privacy rule, absolute). No log reads — the walk is on-paper + headless. |
| T-57-06 | Information Disclosure | window picker / list_windows | mitigate | If the audit touches the window-picker step, preserve `_on_list_windows` logging COUNT only (never titles, per RESEARCH Security Domain) — but this plan does NOT edit the sidecar handler. |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (UI-test + docs + surgical copy/router edits over existing infra). Legitimacy gate vacuously satisfied. |
</threat_model>

<verification>
- `cd tauri/ui && npm test` — full UI suite green including the new continuity smoke (count rises, none fail).
- `source .venv/bin/activate && python3 scripts/audit/check_no_slop_install.py` — no banned tokens in any new/edited copy or the audit doc.
- `cd tauri/ui && npm test -- wizard` + `test_no_inline_strings_install.py` (via the full pytest run) — wizard copy stays in the typed loader, no inline strings.
- `git diff --name-only` shows ONLY the five files in `files_modified`; no Kaan WIP file touched.
</verification>

<success_criteria>
- The fresh-account first-run → first-session flow is mapped and audited; every code-fixable friction is fixed or documented with a reason.
- The forewarning/driver-fetch/48k-probe component question is resolved.
- A continuity smoke proves the wizard chain reaches its terminal step with no dead-end.
- All new/edited copy is anti-slop-clean; no inline strings; sidecar IPC contract unchanged.
- The real fresh-account walk and the sidecar-binary-staleness Phase-58 dependency are surfaced (must_haves), not auto-completed.
</success_criteria>

<output>
Create `.planning/phases/57-sexify-finish/57-02-SUMMARY.md` when done.
</output>
