---
phase: 57-sexify-finish
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - tauri/ui/src/session/SessionLayout.ts
  - tauri/ui/src/session/components/_style-registry.ts
  - tauri/ui/src/session/components/cohost.ts
  - tauri/ui/src/session/components/meter.ts
  - tauri/ui/src/session/components/timecode.ts
  - tauri/ui/src/session/components/status-bar.ts
  - tauri/ui/src/session/components/event-ribbon.ts
  - tauri/ui/src/session/components/phase-tape.ts
  - tauri/ui/mascot.html
  - tauri/ui/src/mascot/chrome.css
  - tauri/ui/src/tokens.css
  - docs/internal/impeccable-pass-57.md
autonomous: false
requirements: [POLISH-01]
must_haves:
  truths:
    - "The session view (SessionLayout + its components) and the mascot overlay (mascot.html + chrome.css) hold the CDJ-Whisper direction: 5 warm blacks, single amber accent at ≤4 intensities (20/80 rule), tactility via faint glow not faux-3D bevels, Saira + JetBrains Mono typography"
    - "A paired ui-checker + ui-auditor pass on the two Tier-1 surfaces returns ZERO HIGH findings"
    - "The 711+ vitest baseline (including the Plan 01 regression assertions on chrome.css + drag) stays green after the visual edits"
    - "No Geist or Fraunces font is introduced; no renderer.ts / Three.js scene / GLB edit; the chrome strip stays display:none"
  artifacts:
    - path: "docs/internal/impeccable-pass-57.md"
      provides: "The impeccable critique→polish record: findings, edits applied per surface, before/after notes, the zero-HIGH ui-checker/ui-auditor result, and the Kaan felt-sign-off carveout"
      contains: "zero HIGH"
  key_links:
    - from: "tauri/ui/src/session/SessionLayout.ts"
      to: "tauri/ui/src/tokens.css"
      via: "CDJ-Whisper design tokens (warm blacks, amber intensities, glow, type)"
      pattern: "var\\(--"
    - from: "tauri/ui/mascot.html"
      to: "tauri/ui/src/mascot/chrome.css"
      via: "overlay chrome wrapper styling held to CDJ-Whisper"
      pattern: "mascot-window"
  kaan_action:
    - "The felt 'looks peak / sexy' visual sign-off on the session view + mascot overlay is Kaan's eye. Engineering ships the impeccable pass + zero-HIGH paired ui-checker/ui-auditor; the beauty call is his (review the before/after)."
---

<objective>
Run an `impeccable`-driven CDJ-Whisper visual pass on the two Tier-1 live surfaces — the session view (`SessionLayout.ts` + its components) and the mascot overlay (`mascot.html` + `chrome.css`) — and converge to ZERO HIGH findings on a paired ui-checker + ui-auditor pass. This is a presentation/consistency polish: hold the locked direction, do not redesign and do not touch the 3D rig pipeline.

Purpose: POLISH-01. The surfaces a user touches must look peak. Engineering proves the objective gate (zero HIGH, CDJ-Whisper consistency, 20/80, no AI-slop type held to Saira + JetBrains Mono); the felt "sexy" call is Kaan's eye (KAAN-ACTION).
Output: visual refinements on the named surfaces (CSS/markup only), an impeccable-pass record doc, and a clean paired-audit result — with the regression baseline (Plan 01) and 711 vitest still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@.claude/skills/frontend-enforcement/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-sexify-finish/57-RESEARCH.md
@DESIGN.md
@PRODUCT.md

<scope_guardrails>
- Depends on Plan 57-01: the chrome-strip + drag regression assertions must exist FIRST, so the visual pass cannot silently regress them. Re-run them after any `chrome.css` edit.
- TYPOGRAPHY: hold the IN-TREE direction — **Saira (variable) + JetBrains Mono** per `tokens.css` (`--type-display`/`--type-body` = Saira; `--type-mono` = JetBrains Mono). Do NOT introduce Geist or Fraunces (CONTEXT carried a STALE pairing; DESIGN.md + tokens.css + memory are authoritative — RESEARCH Pitfall 5). No italic anywhere. If any impeccable suggestion proposes Geist/Fraunces, REJECT it and flag to Kaan.
- 20/80 ACCENT RULE (frontend-enforcement hard rule): amber is the SINGLE accent. Do not spread amber across the surface. Demote any competing amber to silk (precedent: `step1-permissions.ts` amber→silk demotion). Tactility via `--glow-faint` hover-glow, NOT faux-3D bevels.
- DO NOT touch the 3D rig: no `renderer.ts`, no Three.js scene code, no GLB assets (RESEARCH Pitfall 3 + 6). The mascot visual work is the OVERLAY CHROME ONLY (`mascot.html` markup styling + `chrome.css`), targeting the shipped Three.js rig surface — NOT the dead root sprite overlay.
- Keep `.mascot-window__top-label` / `__state-caption` / `.border-anim` IN the markup but `display:none` (HMR/tests rely on the markup existing — RESEARCH anti-pattern). Do not delete them.
- Do NOT touch Kaan's WIP files (`mascot_window.rs`, `tauri.conf.json5`, the python persona files). The window-level flags in `mascot_window.rs` are already correct (`transparent(true)`, `decorations(false)`) — the overlay look is a CSS-tier concern here.
- Non-colliding visual work MAY use a worktree (shares zero files with Kaan's Tauri/python WIP). If a worktree is used, sync main first (`git fetch origin main && git merge origin/main --no-edit`) per the worktree invariant.
- This plan is `autonomous: false`: it ends with a human checkpoint to present the before/after for Kaan's felt sign-off.
</scope_guardrails>

<interfaces>
<!-- CDJ-Whisper tokens (read this session) — tauri/ui/src/tokens.css -->
  --type-display: 'Saira', system-ui, sans-serif;     /* line 128 */
  --type-body:    'Saira', system-ui, sans-serif;     /* line 129 */
  --type-mono:    'JetBrains Mono', ui-monospace, monospace;  /* line 130 */
  /* 5 warm blacks + amber accent (4 intensities) + --glow-faint live in tokens.css */
<!-- Tier-1 surfaces -->
//   tauri/ui/src/session/SessionLayout.ts (33k — session view composer; presentation-only, no IPC/state per file header)
//   tauri/ui/src/session/components/*.ts (meter, timecode, cohost, status-bar, event-ribbon, phase-tape, citation-strip, drop-chip, panel, titlebar, rocker, muted-banner — each renders + styles a deck element)
//   tauri/ui/src/session/components/_style-registry.ts (where component styles are registered/composed)
//   tauri/ui/mascot.html + tauri/ui/src/mascot/chrome.css (the transparent overlay chrome wrapper)
<!-- impeccable skill — ~/.claude/plugins/cache/impeccable/.../3.1.0/ -->
//   sub-commands available: polish, critique, audit, layout, typeset (reads PRODUCT.md + DESIGN.md, both at repo root)
<!-- Gate: paired ui-checker + ui-auditor (agent gate, governed by .claude/skills/frontend-enforcement) -->
<!-- Regression floor (Plan 01): tauri/ui/tests/mascot.chrome.test.ts pins chrome strip display:none -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: impeccable critique + polish the session view</name>
  <files>tauri/ui/src/session/SessionLayout.ts, tauri/ui/src/session/components/_style-registry.ts, tauri/ui/src/session/components/cohost.ts, tauri/ui/src/session/components/meter.ts, tauri/ui/src/session/components/timecode.ts, tauri/ui/src/session/components/status-bar.ts, tauri/ui/src/session/components/event-ribbon.ts, tauri/ui/src/session/components/phase-tape.ts, tauri/ui/src/tokens.css</files>
  <read_first>
    - DESIGN.md + PRODUCT.md (impeccable context — the locked CDJ-Whisper direction + product voice)
    - .claude/skills/frontend-enforcement/SKILL.md (20/80 rule, textured material feel, no-AI-slop typography — the enforced checklist)
    - tauri/ui/src/tokens.css (the warm-black palette, amber intensities, --glow-faint, --type-* tokens — every edit references these tokens, never raw hex except the documented resolveCssColor fallback sites)
    - tauri/ui/src/session/SessionLayout.ts (FULL — the 3-col deck composition)
    - tauri/ui/src/session/components/*.ts (each Tier-1 component's markup + inline/registered styles)
    - mocks/vibemix-direction-final.html (the CDJ-Whisper visual baseline reference)
    - 57-RESEARCH.md Pitfalls 5 (typography) + the anti-patterns (amber spread, no renderer.ts)
  </read_first>
  <action>
Run `impeccable critique` on the session view (SessionLayout + components) against DESIGN.md/PRODUCT.md + the frontend-enforcement checklist to enumerate findings. Then `impeccable polish` (and `typeset`/`layout` as needed at your discretion within the zero-HIGH gate) to apply refinements. Hold CDJ-Whisper:
- 5 warm blacks + single amber accent at ≤4 intensities — demote any competing amber to silk so 20/80 holds (precedent: the `step1-permissions.ts` amber→silk fix).
- Tactility via `--glow-faint` hover-glow, never faux-3D bevels.
- Typography: Saira (display+body) + JetBrains Mono (numerics/mono). Do NOT introduce Geist/Fraunces; no italic. Reject any impeccable typeset suggestion that swaps the family.
- All color/space/type via `tokens.css` vars (no raw hex outside the documented resolveCssColor fallback sites; frontend-enforcement gates this).
This is presentation polish — refine spacing, hierarchy, contrast, glow, type scale, alignment. Do NOT redesign the layout, do NOT change component behavior/props, do NOT alter what the components render (the 711 vitest contract must hold). If a finding requires a token addition (e.g. a new glow intensity), add it to `tokens.css` consistent with the existing 4-intensity amber scale — do not invent a competing palette.
Record each finding + the edit applied in the impeccable-pass doc (created in Task 3).
  </action>
  <acceptance_criteria>
    - [ ] `impeccable critique` findings on the session view are enumerated and each is addressed (fixed or justified) in the pass record.
    - [ ] No Geist/Fraunces introduced; no italic; all type uses `--type-display`/`--type-body`/`--type-mono`.
    - [ ] Amber holds 20/80 (single accent, ≤4 intensities); any competing amber demoted to silk; tactility via `--glow-faint` not bevels.
    - [ ] All new color/space/type values reference `tokens.css` vars (no raw hex outside documented fallback sites).
    - [ ] Component render contracts unchanged — the existing session-component vitest (`meter.test.ts`, `citation-strip.test.ts`, `hype-mode-indicator.test.ts`) stays green.
  </acceptance_criteria>
  <verify>
    <automated>cd tauri/ui && npm test</automated>
  </verify>
  <done>The session view is polished to the CDJ-Whisper direction with Saira + JetBrains Mono held, 20/80 amber held, and the full vitest baseline still green.</done>
</task>

<task type="auto">
  <name>Task 2: impeccable critique + polish the mascot overlay chrome</name>
  <files>tauri/ui/mascot.html, tauri/ui/src/mascot/chrome.css</files>
  <read_first>
    - tauri/ui/mascot.html (the overlay markup loaded by the shipped app per RESEARCH — the Three.js rig surface, NOT the dead root sprite overlay)
    - tauri/ui/src/mascot/chrome.css (FULL — the transparent wrapper; the strip is hidden at lines 42-46; the wrapper anatomy uses --glass-3 / --blur-glass-display / --glass-edge per the existing chrome spec)
    - tauri/ui/tests/mascot.chrome.test.ts (the Plan 01 regression floor — your edits must keep these green: `.mascot-window` root, `.border-anim` first child, transparent body, display:none on the strip)
    - 57-RESEARCH.md Pitfall 3 (edit the SHIPPED overlay surface, not renderer.ts/sprites) + the "keep markup, hide via CSS" anti-pattern
    - DESIGN.md + .claude/skills/frontend-enforcement/SKILL.md (overlay must read as the same CDJ-Whisper material as the session view)
  </read_first>
  <action>
Run `impeccable critique` then `polish` on the mascot OVERLAY CHROME (`mascot.html` markup styling + `chrome.css`) — the transparent wrapper that composites the Three.js rig over the desktop. Hold CDJ-Whisper consistency with the session view (same warm-black/amber/glow/type language). Refinements may include: the wrapper edge/glow treatment, transparency/blur tuning, any visible chrome that should read as Pioneer-grade restraint.
HARD CONSTRAINTS:
- Do NOT touch `renderer.ts`, the Three.js scene, or GLB assets — overlay chrome only.
- Keep `.mascot-window__top-label` / `__state-caption` / `.border-anim` in the markup, `display:none` (the Plan 01 regression test pins this; HMR/tests rely on the markup). Do not delete them or un-hide the strip.
- No Geist/Fraunces; type via the `--type-*` tokens.
- If a finding suggests window-level changes (decorations/transparency), that is `mascot_window.rs` territory (Kaan's WIP, already correct) — do NOT edit it; document the observation instead.
Record findings + edits in the pass doc.
  </action>
  <acceptance_criteria>
    - [ ] `impeccable critique` findings on the overlay chrome are enumerated + addressed in the pass record.
    - [ ] `renderer.ts` / Three.js scene / GLB assets are untouched (git diff confirms).
    - [ ] The chrome strip stays `display:none`; the strip markup elements remain present; `mascot.chrome.test.ts` (Plan 01 floor) stays green.
    - [ ] The overlay reads as the same CDJ-Whisper material as the session view; no Geist/Fraunces; tokens-driven.
    - [ ] No edit to `mascot_window.rs` / `tauri.conf.json5` (Kaan WIP).
  </acceptance_criteria>
  <verify>
    <automated>cd tauri/ui && npm test -- mascot.chrome.test.ts && npm test</automated>
  </verify>
  <done>The mascot overlay chrome is polished to CDJ-Whisper, the chrome strip stays hidden, the 3D rig is untouched, and the Plan 01 regression floor + full vitest baseline stay green.</done>
</task>

<task type="auto">
  <name>Task 3: paired ui-checker + ui-auditor gate to zero HIGH + record</name>
  <files>docs/internal/impeccable-pass-57.md</files>
  <read_first>
    - .claude/skills/frontend-enforcement/SKILL.md (the auditor/checker checklist the gate runs against)
    - DESIGN.md (the CDJ-Whisper acceptance bar)
    - the Task 1 + Task 2 edits (the surfaces under audit)
  </read_first>
  <action>
Run the paired ui-checker + ui-auditor pass (governed by `frontend-enforcement`) over the two Tier-1 surfaces (session view + mascot overlay). Drive findings to ZERO HIGH: for each HIGH finding, apply the fix on the relevant surface (re-entering Task 1/Task 2 file scope as needed) and re-run until zero HIGH remain. MEDIUM/LOW findings: fix where cheap, otherwise record with a reason.
Write `docs/internal/impeccable-pass-57.md`: the per-surface critique findings, the edits applied, before/after notes (describe the change; no need to embed screenshots), the final zero-HIGH ui-checker/ui-auditor result, the typography reconciliation note (held Saira + JetBrains Mono; rejected the stale Geist/Fraunces CONTEXT pairing), and the explicit KAAN-ACTION carveout (the felt "looks peak / sexy" sign-off is Kaan's eye). Use no anti-slop banned tokens in the doc.
  </action>
  <acceptance_criteria>
    - [ ] The paired ui-checker + ui-auditor pass on both Tier-1 surfaces returns ZERO HIGH findings.
    - [ ] `docs/internal/impeccable-pass-57.md` records findings, edits, the zero-HIGH result, the typography reconciliation, and the Kaan felt-sign-off carveout.
    - [ ] The full vitest baseline + the Plan 01 regression assertions are green after all gate-driven fixes.
    - [ ] The doc contains no anti-slop banned tokens.
  </acceptance_criteria>
  <verify>
    <automated>cd tauri/ui && npm test</automated>
  </verify>
  <done>Both Tier-1 surfaces are at zero HIGH on the paired audit; the pass is recorded; the regression floor + baseline are green. The felt sign-off is queued for Kaan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Kaan felt-quality sign-off on the two Tier-1 surfaces</name>
  <what-built>An impeccable-driven CDJ-Whisper visual pass on the session view + mascot overlay, converged to zero HIGH on the paired ui-checker/ui-auditor, holding Saira + JetBrains Mono, 20/80 amber, and the chrome strip hidden — with the 711+ vitest baseline + Plan 01 regression assertions green.</what-built>
  <how-to-verify>
    1. Build/run the app on your Mac (`cargo tauri dev` or the dev launch) and open a live session.
    2. Look at the session view (the 3-col deck) and the mascot overlay floating over the desktop.
    3. Read `docs/internal/impeccable-pass-57.md` for the before/after notes + the zero-HIGH result.
    4. Felt call: does it look peak / sexy? Does the CDJ-Whisper restraint hold (warm blacks, single amber, faint glow, no slop)? Is the mascot overlay clean (no chrome strip)?
  </how-to-verify>
  <resume-signal>Type "approved" if it looks peak, or describe what still feels off (specific surface + what's wrong) so the visual pass can iterate.</resume-signal>
  <action>PAUSE for Kaan. Present the before/after of the session view + mascot overlay (and `docs/internal/impeccable-pass-57.md`) and wait for the felt sign-off per <how-to-verify>. Do NOT auto-complete — this is the KAAN-ACTION felt-quality call. If Kaan reports issues, iterate the visual pass (re-enter Task 1/Task 2 scope) and re-present.</action>
  <done>Kaan typed "approved" on the felt visual quality, OR his specific feedback was applied and re-presented to approval. Engineering zero-HIGH gate (Task 3) is already green independent of this felt call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visual edit → regression floor | CSS/markup edits on `chrome.css` could regress the Plan 01 chrome-strip assertion — re-run it after every chrome edit. |
| capability allowlist | Untouched in this plan — no window/shell perm changes (visual-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-07 | Tampering (quality contract) | CDJ-Whisper typography | mitigate | Hold Saira + JetBrains Mono; reject any Geist/Fraunces swap; frontend-enforcement gates AI-slop type. The pass doc records the reconciliation. |
| T-57-08 | Tampering | chrome strip regression | mitigate | Plan 01's `mascot.chrome.test.ts` pins the strip `display:none`; re-run after every `chrome.css` edit (Task 2 verify does this explicitly). |
| T-57-09 | Elevation of Privilege | Tauri capabilities | accept | Visual-only plan; no capability/permission edits; the snapshot gate stays green untouched. |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (CSS/markup/docs over existing infra). Legitimacy gate vacuously satisfied. |
</threat_model>

<verification>
- `cd tauri/ui && npm test` — full UI suite green (component render contracts + Plan 01 regression assertions held through the visual edits).
- `cd tauri/ui && npm test -- mascot.chrome.test.ts` — the chrome-strip regression floor green after the overlay polish.
- Paired ui-checker + ui-auditor on session view + mascot overlay → ZERO HIGH (recorded in `docs/internal/impeccable-pass-57.md`).
- `git diff --name-only` shows ONLY the files in `files_modified`; no `renderer.ts` / GLB / Kaan-WIP file touched.
- KAAN-ACTION: Kaan's felt "looks peak" sign-off via the human-verify checkpoint.
</verification>

<success_criteria>
- The session view + mascot overlay hold CDJ-Whisper (warm blacks, single amber 20/80, faint-glow tactility, Saira + JetBrains Mono — no Geist/Fraunces, no italic).
- Paired ui-checker + ui-auditor on both surfaces returns zero HIGH.
- The 3D rig (renderer.ts/GLB) is untouched; the chrome strip stays hidden; no Kaan WIP file touched.
- The 711+ vitest baseline + Plan 01 regression assertions stay green.
- The felt "sexy" sign-off is surfaced as KAAN-ACTION (must_haves), confirmed via the human-verify checkpoint.
</success_criteria>

<output>
Create `.planning/phases/57-sexify-finish/57-03-SUMMARY.md` when done.
</output>
