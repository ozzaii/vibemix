---
phase: 57-sexify-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/security/test_drag_capability_present.py
  - tests/security/test_tcc_prime_path_wired.py
  - tauri/ui/tests/mascot.chrome.test.ts
  - tauri/ui/src/mascot/__tests__/drag.spec.ts
autonomous: true
requirements: [POLISH-02]
must_haves:
  truths:
    - "The drag capability `core:window:allow-start-dragging` can no longer silently vanish from capabilities/default.json without a test failing"
    - "The mascot chrome strip elements being hidden (display:none) can no longer silently regress without a test failing"
    - "The JS-API drag fallback (document mousedown → startDragging) can no longer silently vanish from mascot/index.ts without a test failing"
    - "The TCC prime-registration path (boot → _prime_tcc_registration; request_* on notDetermined) can no longer silently vanish without a test failing"
    - "The permission deep-link using direct Command::new(\"open\") (not the deprecated shell().open) can no longer silently regress without a test failing"
  artifacts:
    - path: "tests/security/test_drag_capability_present.py"
      provides: "Static pytest gate pinning the drag capability + the permissions.rs direct-open deep-link"
      contains: "core:window:allow-start-dragging"
    - path: "tests/security/test_tcc_prime_path_wired.py"
      provides: "Static pytest gate pinning the TCC prime-registration path in runtime/wizard.py"
      contains: "_prime_tcc_registration"
    - path: "tauri/ui/src/mascot/__tests__/drag.spec.ts"
      provides: "Vitest assertion that mascot/index.ts wires the document-mousedown startDragging() handler"
      contains: "startDragging"
    - path: "tauri/ui/tests/mascot.chrome.test.ts"
      provides: "Extended chrome spec asserting the strip elements are display:none"
      contains: "display"
  key_links:
    - from: "tests/security/test_drag_capability_present.py"
      to: "tauri/src-tauri/capabilities/default.json"
      via: "fs read + JSON parse, assert permission present"
      pattern: "core:window:allow-start-dragging"
    - from: "tests/security/test_tcc_prime_path_wired.py"
      to: "src/vibemix/runtime/wizard.py"
      via: "fs read + source-string assert that boot() calls _prime_tcc_registration"
      pattern: "_prime_tcc_registration"
  kaan_action:
    - "Real-app confirm on Kaan's Mac: window actually drags, no mascot chrome strip renders, the macOS Privacy list populates with vibemix after a fresh wizard boot (interaction/visual confirms — engineering only pins the code is present + regression-gated)"
---

<objective>
Pin the three v0.1.0-rc1 carryover-bug fixes (closed in commit `fac4c4a`, intact in the working tree) with headless regression assertions so they cannot silently regress. This is VERIFY-AND-HARDEN, not re-implement — every fix already exists at a cited file:line. The only new code is the missing regression assertions.

Purpose: POLISH-02's code is shipped; what is missing is the safety net that proves it stays shipped. After this plan, a stray edit that drops the drag capability, un-hides the chrome strip, removes the JS drag fallback, or breaks the TCC prime path turns a test red instead of shipping broken.
Output: 2 new pytest gates (drag-cap + deep-link; TCC prime-path), 1 new vitest spec (JS drag handler), 1 extended vitest spec (chrome strip display:none).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-sexify-finish/57-RESEARCH.md

<scope_guardrails>
- This plan touches ONLY test files. It edits ZERO production code. It does NOT touch Kaan's WIP files (`mascot_window.rs`, `tauri.conf.json5`, `src/vibemix/__main__.py`, `agent/dj_cohost.py`, `audio/constants.py`, `prompts/matrix.py`, `tests/agent/test_*`).
- Do NOT re-implement any fix. If an assertion you write fails, that means a real regression exists — STOP and surface it; do not "fix" working production code in this plan.
- Do NOT edit `capabilities/default.json` (the drag cap is already present at line 9). If you do not edit it, the capability-snapshot gate stays green and no `SNAPSHOT.json` regen is needed.
- Test files only: pure-read assertions over source text + (for the vitest drag spec) jsdom. No new packages.
</scope_guardrails>

<interfaces>
<!-- The fixes to pin, verified present in working tree this planning session. -->
<!-- (a) Drag capability — tauri/src-tauri/capabilities/default.json:9 -->
    "core:window:allow-start-dragging"
<!-- (a) JS-API drag fallback — tauri/ui/src/mascot/index.ts:148-163 -->
    document.addEventListener("mousedown", (ev) => { ... tauriWin.startDragging().catch(...) })
    // guarded by: me.button !== 0 (left-only) and target.closest("[data-no-drag]")
<!-- (b) Chrome strip hidden — tauri/ui/src/mascot/chrome.css:42-46 -->
    .mascot-window > .border-anim,
    .mascot-window__top-label,
    .mascot-window__state-caption { display: none; }
<!-- (c) Deep-link direct open — tauri/src-tauri/src/permissions.rs:29 -->
    std::process::Command::new("open").arg(url).spawn()   // NOT app.shell().open()
<!-- (c) TCC prime path — src/vibemix/runtime/wizard.py -->
    async def boot(self):  # line 137
        asyncio.create_task(asyncio.to_thread(self._prime_tcc_registration))  # line 153
    def _prime_tcc_registration(self):  # line 155
        # request_microphone_permission() on notDetermined  (line ~165)
        # request_screen_recording_permission() if not authorized  (line ~179)
<!-- Existing test homes (read this session): -->
<!--   tests/security/test_capability_snapshot.py — pattern for fs-read static gate over default.json -->
<!--   tests/security/test_tauri_plugin_macos_permissions_wired.py — pattern for static config-intent gate -->
<!--   tauri/ui/tests/mascot.chrome.test.ts — already asserts .mascot-window markup; EXTEND it for display:none -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin drag capability + JS drag handler + chrome strip (vitest + pytest)</name>
  <files>tests/security/test_drag_capability_present.py, tauri/ui/src/mascot/__tests__/drag.spec.ts, tauri/ui/tests/mascot.chrome.test.ts</files>
  <read_first>
    - tauri/src-tauri/capabilities/default.json (confirm `core:window:allow-start-dragging` present @ line 9)
    - tauri/src-tauri/src/permissions.rs (confirm `Command::new("open")` @ line 29; confirm NO `shell().open` remains)
    - tauri/ui/src/mascot/index.ts lines 140-165 (confirm document-mousedown → startDragging block)
    - tauri/ui/src/mascot/chrome.css lines 25-46 (confirm `.border-anim` / `__top-label` / `__state-caption { display: none }`)
    - tauri/ui/tests/mascot.chrome.test.ts (existing chrome spec — read fully; EXTEND, do not rewrite)
    - tests/security/test_capability_snapshot.py (pattern for the fs-read static-gate style: REPO_ROOT = parents[2], Path read_text, plain asserts)
  </read_first>
  <action>
Create `tests/security/test_drag_capability_present.py` — a static, fs-read pytest gate (never runs cargo, mirrors the style of `test_capability_snapshot.py`):
- Resolve `REPO_ROOT = Path(__file__).resolve().parents[2]`.
- Assert `core:window:allow-start-dragging` is in the parsed `permissions` array of `tauri/src-tauri/capabilities/default.json` (parse JSON, check membership — not a substring match, so reordering/whitespace can't false-pass). This pins POLISH-02a's capability (per RESEARCH: required for `startDragging()` to be permitted; symptom without it is "window.start_dragging not allowed").
- Assert `tauri/src-tauri/src/permissions.rs` source text contains `Command::new("open")` AND does NOT contain `shell().open` / `.shell().open(` (the deprecated path removed in `fac4c4a` — silently failed on macOS 26.3). This pins POLISH-02c's deep-link form.
- Add a docstring crediting commit `fac4c4a` and noting this is a regression pin for an already-shipped fix.

Create `tauri/ui/src/mascot/__tests__/drag.spec.ts` — a vitest spec that fs-reads `tauri/ui/src/mascot/index.ts` text and asserts the JS-API drag fallback is wired (POLISH-02a, per RESEARCH Pattern 3, since `data-tauri-drag-region` alone is unreliable on transparent+decorations:false macOS windows):
- Assert the source contains a `document.addEventListener("mousedown"` registration.
- Assert it calls `startDragging()` inside that handler.
- Assert the left-click guard (`button !== 0`) and the `[data-no-drag]` opt-out are present (these are load-bearing — drag must not fire on right-click or no-drag regions).
Use `readFileSync` + `resolve(__dirname, ...)` exactly like the existing `mascot.chrome.test.ts` does (it already resolves `MASCOT_INDEX_TS_PATH`); follow that file's path-resolution idiom.

Extend `tauri/ui/tests/mascot.chrome.test.ts` — add it() cases that read `chrome.css` text and assert the three strip elements are hidden (POLISH-02b). The existing spec asserts the `.mascot-window` markup + `.border-anim` first child but does NOT assert `display:none`. Add: assert the `chrome.css` source contains a rule hiding `.mascot-window > .border-anim`, `.mascot-window__top-label`, and `.mascot-window__state-caption` with `display: none`. Match the rule robustly (normalize whitespace; assert all three selectors appear in a `display: none` block) so a reorder can't false-fail and a deletion of any selector fails. Do NOT delete or rewrite the existing it() cases — append.
  </action>
  <acceptance_criteria>
    - [ ] `tests/security/test_drag_capability_present.py` exists and is pytest-collected (no syntax errors, importable).
    - [ ] It asserts `core:window:allow-start-dragging` is a member of the parsed default.json permissions array (JSON membership, not substring).
    - [ ] It asserts `permissions.rs` contains `Command::new("open")` and contains no `shell().open` form.
    - [ ] `tauri/ui/src/mascot/__tests__/drag.spec.ts` exists, asserts the `mousedown` listener + `startDragging()` call + `button !== 0` guard + `[data-no-drag]` opt-out are all present in `mascot/index.ts`.
    - [ ] `mascot.chrome.test.ts` has NEW it() cases asserting `display: none` covers all three strip selectors; the pre-existing it() cases are unchanged and still present.
    - [ ] No production file was edited (git diff shows only the test files above).
  </acceptance_criteria>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/security/test_drag_capability_present.py && cd tauri/ui && npm test -- mascot.chrome.test.ts drag.spec.ts</automated>
  </verify>
  <done>All three new/extended test surfaces pass green; the drag cap, JS drag handler, deep-link form, and chrome strip display:none are each pinned by an assertion; zero production files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the TCC prime-registration path (pytest)</name>
  <files>tests/security/test_tcc_prime_path_wired.py</files>
  <read_first>
    - src/vibemix/runtime/wizard.py lines 137-220 (confirm `boot()` schedules `_prime_tcc_registration`; confirm `_prime_tcc_registration` calls `request_microphone_permission()` on notDetermined and `request_screen_recording_permission()` when not authorized; confirm `_on_permission_check` fires `request_*` on `notDetermined`)
    - tests/security/test_tauri_plugin_macos_permissions_wired.py (pattern for a static config-intent gate over source text; REPO_ROOT = parents[2])
    - 57-RESEARCH.md "TCC prime-registration" code example + Open Question 2 (the "list does not populate" symptom is owned by `_prime_tcc_registration`; real confirmation is KAAN-ACTION)
  </read_first>
  <action>
Create `tests/security/test_tcc_prime_path_wired.py` — a static, source-text pytest gate (no live macOS calls; the real Privacy-list populate is KAAN-ACTION per RESEARCH Open Question 2). This pins POLISH-02c's "list-population" fix: the OS only lists vibemix AFTER a capture-API request fires, and `_prime_tcc_registration` is what fires it on wizard boot.
- Resolve `REPO_ROOT = Path(__file__).resolve().parents[2]`; read `src/vibemix/runtime/wizard.py` text.
- Assert `boot()` schedules the prime path: the source contains both `async def boot` and a call to `_prime_tcc_registration` (e.g. `asyncio.to_thread(self._prime_tcc_registration)` or `self._prime_tcc_registration(`). Prefer asserting the `_prime_tcc_registration` reference appears within the `boot` method body (slice the source from `async def boot` to the next `def `/`async def ` at the same indent and assert the call is in that slice) so moving the call out of boot fails the test.
- Assert `_prime_tcc_registration` is defined and its body references both `request_microphone_permission` and `request_screen_recording_permission` (the two capture-API requests that register the app in the Privacy list).
- Assert `_on_permission_check` references `request_screen_recording_permission` and `request_microphone_permission` (the on-demand grant path).
- Prefer AST parsing (`ast.parse` + walk for the `boot` / `_prime_tcc_registration` / `_on_permission_check` FunctionDef nodes and check their body source) over brittle line slicing where practical; fall back to indentation-bounded text slicing if AST is awkward for the async method bodies. Either way, the assertion must fail if the call is moved OUT of `boot()`.
- Docstring: credit `fac4c4a`/the wizard prime path; note this is a regression pin and the real fresh-account Privacy-list confirm is KAAN-ACTION.
  </action>
  <acceptance_criteria>
    - [ ] `tests/security/test_tcc_prime_path_wired.py` exists and is pytest-collected.
    - [ ] It fails if `_prime_tcc_registration` is no longer invoked from `boot()` (verify by reasoning about the slice/AST scoping — the call-site must be inside boot, not merely anywhere in the file).
    - [ ] It asserts `_prime_tcc_registration` body references both `request_microphone_permission` and `request_screen_recording_permission`.
    - [ ] It asserts `_on_permission_check` references both `request_*` permission functions.
    - [ ] No production file edited; the test reads `runtime/wizard.py` only.
  </acceptance_criteria>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/security/test_tcc_prime_path_wired.py</automated>
  </verify>
  <done>The TCC prime-registration path is pinned: a test goes red if the prime call leaves `boot()` or either capture-API request is removed; the real-Mac Privacy-list populate is recorded as KAAN-ACTION (must_haves), not auto-completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → source text | Tests read in-tree source as untrusted text; no execution of app code, no shell-out to cargo. |
| (none new) | This plan adds zero runtime/IPC surface — it only adds assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | Tauri capability allowlist (default.json) | mitigate | New pytest pins the EXACT scoped perm `core:window:allow-start-dragging`; the existing snapshot gate (`test_capability_snapshot.py`) catches any broadening. This plan does NOT edit default.json, so no snapshot regen needed. |
| T-57-02 | Elevation of Privilege | drag capability scope | accept | The drag cap is the minimal scoped window perm; no broadening introduced. Assertion pins membership, not a wildcard. |
| T-57-03 | Information Disclosure | first-run/TCC debugging | mitigate | Tests read ONLY vibemix's own source (`runtime/wizard.py`, `permissions.rs`); never touch Kaan's OZ/Hermes/LM Studio paths (CLAUDE.md privacy rule, absolute). No log reads. |
| T-57-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (test-only plan over existing infra). Package legitimacy gate vacuously satisfied per RESEARCH. |
</threat_model>

<verification>
- `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/security/test_drag_capability_present.py tests/security/test_tcc_prime_path_wired.py` — both new pytest gates green.
- `cd tauri/ui && npm test` — full 711 baseline still green (no regression from the extended chrome spec + new drag spec; total count rises by the added cases).
- `git diff --name-only` shows ONLY the four files in `files_modified` — confirms zero production edits and zero touches to Kaan's WIP files.
</verification>

<success_criteria>
- POLISH-02's three carryover-bug fixes are each pinned by at least one headless regression assertion (drag cap + JS handler; chrome strip display:none; deep-link form + TCC prime path).
- All new/extended tests pass; the 711-test UI baseline is preserved (count increases, none fail).
- No production code edited; no Kaan WIP file touched; no new package installed.
- The three real-app interaction confirmations stay surfaced as KAAN-ACTION (must_haves.kaan_action), not auto-completed.
</success_criteria>

<output>
Create `.planning/phases/57-sexify-finish/57-01-SUMMARY.md` when done.
</output>
