---
phase: 58-ship-readiness
plan: 04
type: execute
wave: 2
depends_on: ["58-01", "58-02"]
files_modified:
  - scripts/launch/cut_release.sh
  - tests/repo/test_cut_release_tag_regex.py
  - tests/repo/test_cut_release_dry_run.py
  - tests/repo/test_cut_release_no_autonomous_publish.py
autonomous: true
requirements: [REL-01, REL-03]
must_haves:
  truths:
    - "Gate 1 accepts the v0.1.0-rc public tag (TAG_REGEX re-pointed from ^v2\\.1\\.0-rc[0-9]+$ to ^v0\\.1\\.0-rc[0-9]+$), test-pinned"
    - "Gate 4 reads .planning/v4.0-MILESTONE-AUDIT.md (path re-pointed from v2.1) and goes green on the real WIRED audit from Plan 01"
    - "All GREEN-NOW gates (1, 3, 4, 5, 6) run green on real artifacts; Gate 5b classified ENG-but-server-dependent"
    - "cut_release.sh --dry-run stubs the Gate-2 signature check (drops --require-signed) + treats Gate 2b/6b pending-Kaan-input as PASS-for-dry-run-with-loud-log, runs all other gates for real, and exits 0 = everything-but-the-signature-is-ready"
    - "The hard guard is absolute: gh release create is NEVER executed, including under --dry-run — regression-pinned"
  artifacts:
    - path: "scripts/launch/cut_release.sh"
      provides: "v0.1.0/v4.0-aware driver with a --dry-run signature-stub mode; hard guard intact"
      pattern: "v0\\\\.1\\\\.0-rc|DRY_RUN|v4.0-MILESTONE-AUDIT"
    - path: "tests/repo/test_cut_release_dry_run.py"
      provides: "Pins --dry-run exits 0 against the current tree + the stub log lines appear"
    - path: "tests/repo/test_cut_release_no_autonomous_publish.py"
      provides: "Pins gh release create only appears inside the printed heredoc, never executed (even under --dry-run)"
    - path: "tests/repo/test_cut_release_tag_regex.py"
      provides: "Pins the v2.1->v0.1.0 regex change (accepts v0.1.0-rc1, rejects v2.1.0-rc1 + bare v1.0.0)"
  key_links:
    - from: "cut_release.sh Gate 1"
      to: "the v0.1.0-rc tag arg"
      via: "TAG_REGEX match"
      pattern: "\\^v0\\\\.1\\\\.0-rc"
    - from: "cut_release.sh Gate 4"
      to: ".planning/v4.0-MILESTONE-AUDIT.md"
      via: "AUDIT path + WIRED grep"
      pattern: "v4.0-MILESTONE-AUDIT"
    - from: "cut_release.sh --dry-run"
      to: "verify_signed.py (WITHOUT --require-signed)"
      via: "DRY_RUN guard drops the signed-binary requirement"
      pattern: "DRY_RUN"
  kaan_action:
    - "Gate 2 real signature (Apple/SignPath) — EXTERNAL. The dry-run stubs it; the real cut needs the signature. NEVER attempt it (P46)."
    - "Gate 2b ear-test leg (54+55 HUMAN-UAT) + Gate 6b recorded walk — KAAN-ACTION. The dry-run loudly logs these as wired-but-pending; it does NOT silently green them in a REAL (non-dry-run) cut."
---

<objective>
The load-bearing fix: re-point `cut_release.sh` from v2.1 to the v0.1.0-rc public tag + the
v4.0 milestone audit, add a `--dry-run` mode that stubs ONLY the external signature gate and
exits green ("everything but the signature is ready"), keep the `gh release create` hard guard
absolute (regression-pinned), and run the GREEN-NOW gates green on the real artifacts from
Plans 01+02. Pin every surgical change with a test.

Purpose: REL-01 (gates green on real artifacts) + REL-03 (one-button SHIP-CUT pre-verified via
a green dry-run). This is the proof that the release is one-button-after-signatures.
Output: a v0.1.0/v4.0-aware `cut_release.sh` with `--dry-run`, 3 new repo tests, and a green
`bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-ship-readiness/58-RESEARCH.md
@.planning/phases/58-ship-readiness/58-CONTEXT.md
@.planning/phases/58-ship-readiness/58-01-SUMMARY.md
@.planning/phases/58-ship-readiness/58-02-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

scripts/launch/cut_release.sh (verified, full):
  - L42 TAG="$1"; L45 TAG_REGEX='^v2\.1\.0-rc[0-9]+$'   <- RE-POINT to ^v0\.1\.0-rc[0-9]+$
  - L62-67 Gate 1: [[ "${TAG}" =~ ${TAG_REGEX} ]]
  - L71-90 Gate 2: verify_signed.py --artifact <art> --require-signed for every dist/*.{dmg,pkg,msi,exe}  <- DRY_RUN drops --require-signed
  - L94-99 Gate 2b: bash scripts/release/check_gate.sh  (nightly proxy + ear-test; KAAN-gated leg)
  - L104-108 Gate 6b: bash scripts/e2e/check_e2e_report.sh  (exit 2 = no run; KAAN-gated feed)
  - L113-117 Gate 3: pytest tests/repo/test_readme_hero_hash_sync.py
  - L121-132 Gate 4: AUDIT="${REPO_ROOT}/.planning/v2.1-MILESTONE-AUDIT.md"  <- RE-POINT to v4.0-MILESTONE-AUDIT.md ; grep -E '^(overall_verdict|status):\s*(WIRED|passed)\s*$'
  - L137-141 Gate 5: pytest tests/repo/test_g5_poc_files_untouched.py
  - L146-150 Gate 5b: bash scripts/release/check_bravoh_server_ready.sh --quiet  (ENG-but-server-dependent)
  - L155-159 Gate 6: pytest tests/security/test_bundle_id_locked.py  (reads tauri.conf.json5 — never written here)
  - L175-189 SUCCESS: cat <<EOF ... gh release create ... EOF  <- printed, NEVER executed (HARD GUARD, L32-33)
  - L191-202 reminder echoes (GATE-06 / P83 / SHIP-CUT)

scripts/dist/verify_signed.py (verified): exits 0 with ::notice:: when signing artifacts absent UNLESS --require-signed.

Existing test pattern: tests/repo/test_cut_release_invokes_check_gate.py uses _echo_lines() to scope assertions to user-visible echo output; mirror its Path/regex style.

NOTE: the `v0.1.0-rc1` git TAG already exists in this repo — irrelevant to the dry-run (which never runs gh release create), but the cookbook (Plan 03) flags it for the real cut.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point Gate 1 (tag regex) + Gate 4 (milestone audit path) to v0.1.0-rc / v4.0 + pin the regex</name>
  <read_first>scripts/launch/cut_release.sh L42-67 + L121-132; 58-RESEARCH.md Pitfall 1 + CONTEXT public-tag decision; tests/repo/test_cut_release_invokes_check_gate.py (pattern)</read_first>
  <files>scripts/launch/cut_release.sh, tests/repo/test_cut_release_tag_regex.py</files>
  <action>
    Surgically update `cut_release.sh`: change `TAG_REGEX` (L45) from `'^v2\.1\.0-rc[0-9]+$'` to `'^v0\.1\.0-rc[0-9]+$'` (the public OSS tag, per CONTEXT decision — v4.0 stays the INTERNAL milestone identity). Change the Gate-4 `AUDIT` path (L122) from `.planning/v2.1-MILESTONE-AUDIT.md` to `.planning/v4.0-MILESTONE-AUDIT.md`. Update the header comment block (L13, L17) + the usage example (L25, L38) + the Gate-1/Gate-4 echo labels to say v0.1.0-rc / v4.0 so the user-visible output is consistent. Leave the WIRED/passed grep regex (L127) and every other gate verbatim. Do NOT touch `tauri.conf.json5` (Gate 6 only READS it).
    Write `tests/repo/test_cut_release_tag_regex.py` (mirror `test_cut_release_invokes_check_gate.py`'s read-script + regex style): assert the script contains `^v0\.1\.0-rc[0-9]+$` and NOT the old `^v2\.1\.0-rc`; assert the Gate-4 path is `v4.0-MILESTONE-AUDIT.md` and NOT `v2.1-MILESTONE-AUDIT.md`. If feasible, also drive Gate 1 directly: extract TAG_REGEX and assert `v0.1.0-rc1` matches while `v2.1.0-rc1` and bare `v1.0.0` do not.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/repo/test_cut_release_tag_regex.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `TAG_REGEX` is `^v0\.1\.0-rc[0-9]+$`; old `^v2\.1\.0-rc` gone
    - [ ] Gate-4 `AUDIT` path is `.planning/v4.0-MILESTONE-AUDIT.md`; old v2.1 path gone
    - [ ] Header/usage/echo labels reflect v0.1.0-rc / v4.0 consistently
    - [ ] `test_cut_release_tag_regex.py` green; accepts `v0.1.0-rc1`, rejects `v2.1.0-rc1` + `v1.0.0`
    - [ ] No edit to `tauri.conf.json5` or any `src/vibemix/**` / `tauri/src-tauri/src/**`
  </acceptance_criteria>
  <done>Gates 1+4 re-pointed to the v0.1.0-rc public tag + v4.0 audit; regex change pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Add the --dry-run signature-stub mode + the hard-guard regression test</name>
  <read_first>scripts/launch/cut_release.sh L35-90 + L94-108 + L162-203; 58-RESEARCH.md "One-Button SHIP-CUT Dry-Run" recommendation + Pitfall 2 + Pitfall 4</read_first>
  <files>scripts/launch/cut_release.sh, tests/repo/test_cut_release_no_autonomous_publish.py</files>
  <action>
    Add a `--dry-run` flag (parse it as an optional arg alongside the tag; set `DRY_RUN=1`). Behavior under `DRY_RUN=1`:
    - Gate 2: run `verify_signed.py --artifact <art>` WITHOUT `--require-signed` (checksum-only; exits 0 on the unsigned local .dmg from Plan 01, or the whl fallback). Print `  --    DRY-RUN: signature gate stubbed (EXTERNAL — Apple/SignPath)`.
    - Gate 2b + Gate 6b: if the real inputs are absent (check_gate.sh / check_e2e_report.sh non-zero), print `  --    DRY-RUN: <gate> wired; awaiting Kaan input (54/55 ear-pass | E2E walk)` and treat as PASS-FOR-DRY-RUN (do NOT set FAIL). Loudly log the pending dependency. NEVER do this when DRY_RUN is unset (a real cut must still fail these).
    - All other gates (1, 3, 4, 5, 5b, 6) run for real.
    - On success print: `DRY-RUN GREEN — everything but the signature is ready. Real cut blocked only on: [Apple Dev Agreement, SignPath cert, 54/55 ear-pass, E2E walk].`
    HARD GUARD (absolute, Pitfall 4): even under `--dry-run`, `gh release create` must remain inside the printed `cat <<EOF ... EOF` heredoc and NEVER be executed. Do not add any code path that runs it.
    Write `tests/repo/test_cut_release_no_autonomous_publish.py`: read the script text, assert the literal `gh release create` appears ONLY within the heredoc block (between the `cat <<EOF` and `EOF` lines) and NEVER as a bare executed command line, and assert no `--dry-run` branch invokes it. (Static-analysis pin; mirror the _echo_lines/regex approach.)
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/repo/test_cut_release_no_autonomous_publish.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `--dry-run` flag parsed; `DRY_RUN=1` drops `--require-signed` on Gate 2 + prints the stub log line
    - [ ] Gate 2b/6b under dry-run print the wired-but-pending log + PASS-for-dry-run; a NON-dry-run cut still FAILS them
    - [ ] All other gates run for real under dry-run
    - [ ] `gh release create` stays inside the heredoc, never executed (even under --dry-run)
    - [ ] `test_cut_release_no_autonomous_publish.py` green
  </acceptance_criteria>
  <done>--dry-run stubs ONLY the signature, loudly logs pending-Kaan gates, exits green; hard guard regression-pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Run the green-now gates on real artifacts + pin the green dry-run</name>
  <read_first>the re-pointed cut_release.sh (Tasks 1+2); 58-01-SUMMARY.md (audit + dmg) + 58-02-SUMMARY.md (e2e report); 58-RESEARCH.md classification rollup + Pitfall 5 (Gate 5b server dependency)</read_first>
  <files>tests/repo/test_cut_release_dry_run.py</files>
  <action>
    Run the GREEN-NOW gates individually on the real artifacts to confirm each is green: Gate 3 (`pytest tests/repo/test_readme_hero_hash_sync.py`), Gate 4 (the v4.0 audit from Plan 01 reads WIRED), Gate 5 (`pytest tests/repo/test_g5_poc_files_untouched.py`), Gate 6 (`pytest tests/security/test_bundle_id_locked.py`), Gate 1 (regex accepts the tag). Note Gate 5b's result (Bravoh server) — if the server is down, classify it ENG-but-server-dependent + record it as a real-cut precondition (do NOT weaken the gate). Capture the green-now / KAAN-gated / EXTERNAL classification table in the SUMMARY.
    Write `tests/repo/test_cut_release_dry_run.py`: invoke `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` via subprocess from the repo root and assert exit code 0, AND assert the stub log lines appear in stdout (`DRY-RUN: signature gate stubbed`, the `DRY-RUN GREEN` summary, and the pending-input lines for any KAAN-gated gate). If Gate 5b makes the dry-run flaky on server state, gate the assertion on the server-dependent leg accordingly (skip-with-reason if Bravoh unreachable), but the signature + 2b/6b stub assertions must always hold.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/repo/test_cut_release_dry_run.py -q --no-header && bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1; echo "dryrun-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Gates 1, 3, 4, 5, 6 confirmed green on real artifacts (Gate 4 reads WIRED from the Plan-01 audit)
    - [ ] Gate 5b result recorded + classified ENG-but-server-dependent (precondition, not weakened)
    - [ ] `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` exits 0
    - [ ] `test_cut_release_dry_run.py` asserts exit 0 + the stub/summary log lines
    - [ ] Classification table (green-now / KAAN-gated / EXTERNAL) captured in the SUMMARY
  </acceptance_criteria>
  <done>Green-now gates verified on real artifacts; the signature-stubbed dry-run exits green and is pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --dry-run mode | Could be abused to silently green a REAL cut without a signature |
| autonomous agent → publish | gh release create must never auto-run |
| untrusted gate JSON | scorecard/ear-test JSON parsed by the gate scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-08 | Elevation/Spoofing | gh release create hard guard | mitigate | `test_cut_release_no_autonomous_publish.py` pins it stays inside the heredoc, never executed — even under --dry-run (Pitfall 4 / P46). |
| T-58-09 | Tampering | --dry-run stubbing in a real cut | mitigate | The stub behavior is gated on `DRY_RUN=1`; a non-dry-run cut still FAILS Gate 2/2b/6b without real inputs. Pinned by the dry-run test asserting the stub lines only under the flag. |
| T-58-10 | Tampering | untrusted scorecard/ear-test JSON | accept | Gate scripts (`check_gate.sh`) parse via `jq` only, never `eval`/`$(...)` (T-42-04-01); this plan does not change that. |
| T-58-11 | Spoofing | wrong public tag cut | mitigate | Gate 1 regex pins `^v0\.1\.0-rc[0-9]+$`; `test_cut_release_tag_regex.py` rejects a mis-versioned tag. |
| T-58-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `pytest tests/repo/test_cut_release_tag_regex.py tests/repo/test_cut_release_dry_run.py tests/repo/test_cut_release_no_autonomous_publish.py -q` all green.
- `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` exits 0 with the stub + DRY-RUN GREEN log lines.
- Gates 1/3/4/5/6 green on real artifacts; Gate 5b classified.
- `gh release create` never executed; NO edit to `tauri.conf.json5` / `src/vibemix/**` / `tauri/src-tauri/src/**`.
</verification>

<success_criteria>
cut_release.sh is v0.1.0-rc/v4.0-aware, the GREEN-NOW gates pass on real artifacts, `--dry-run` exits green with ONLY the signature stubbed (KAAN-gated gates loudly logged wired-but-pending), and the hard guard is absolute + regression-pinned.
</success_criteria>

<output>
Create `.planning/phases/58-ship-readiness/58-04-SUMMARY.md` when done.
</output>
