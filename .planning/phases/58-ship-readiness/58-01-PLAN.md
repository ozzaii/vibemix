---
phase: 58-ship-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dist/vibemix-core/
  - dist/*.dmg
  - tauri/src-tauri/binaries/
  - .planning/v4.0-MILESTONE-AUDIT.md
  - tests/repo/test_v4_milestone_audit_present.py
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "The Python sidecar is rebuilt + confirmed current (AIza-leak scan passes), staged for both Apple-Silicon + Intel triples"
    - "A real (unsigned) macOS .dmg exists in dist/ for Gate 2's verify_signed.py to inspect — OR the headless-build blocker is documented with the CI/real-Mac requirement (RESEARCH Open-Q3)"
    - "A real .planning/v4.0-MILESTONE-AUDIT.md exists, generated by integration_audit.py, with frontmatter overall_verdict: WIRED"
    - "Gate 4's input file exists and reads WIRED so the re-pointed Gate 4 (Plan 04) goes green on a real artifact"
  artifacts:
    - path: ".planning/v4.0-MILESTONE-AUDIT.md"
      provides: "Gate 4 input — milestone audit with WIRED verdict (generated, NOT hand-written)"
      contains: "overall_verdict: WIRED"
    - path: "tests/repo/test_v4_milestone_audit_present.py"
      provides: "Pins the v4.0 audit exists + reads WIRED + was generator-produced"
  key_links:
    - from: "scripts/integration_audit.py --write-milestone-audit"
      to: ".planning/v4.0-MILESTONE-AUDIT.md"
      via: "generator CLI"
      pattern: "overall_verdict:\\s*WIRED"
    - from: "dist/*.dmg (unsigned)"
      to: "scripts/dist/verify_signed.py"
      via: "Gate 2 inspects a real file under the dry-run stub (Plan 04)"
      pattern: "dist/.*\\.dmg"
  kaan_action:
    - "The SIGNED/notarized .dmg (Apple Dev Agreement + SignPath cert) — EXTERNAL. This plan builds UNSIGNED only. Never attempt the signature (P46 hard rule)."
---

<objective>
Produce the REAL artifacts the release gates inspect: rebuild + verify the Python sidecar
(deferred from Phase 57), build a local UNSIGNED macOS `.dmg` so Gate 2's `verify_signed.py`
has a real file to inspect under the dry-run, and generate the `v4.0-MILESTONE-AUDIT.md`
(Gate 4 input) via the existing generator — never hand-written.

Purpose: Gates can only be honest-green on REAL artifacts. This plan supplies them. It is the
artifact-prep half of REL-01; Plan 04 runs the gates against these artifacts.
Output: rebuilt sidecar (staged triples), an unsigned `dist/*.dmg` (or a documented headless
blocker), `.planning/v4.0-MILESTONE-AUDIT.md` with `overall_verdict: WIRED`, and a repo test
pinning the audit's presence + verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-ship-readiness/58-RESEARCH.md
@.planning/phases/58-ship-readiness/58-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no exploration needed. -->

scripts/build_sidecar.py (header, verified):
  Run via: `uv run python scripts/build_sidecar.py --spec vibemix-core.macos.spec`
  - Refuses --onefile; uses `rustc -vV` for the target triple.
  - Runs an AIza-leak scan (build_sidecar.py:13-16) over the bundle automatically; aborts non-zero on any `AIza[A-Za-z0-9_-]{35}` match.
  - Output: tauri/src-tauri/binaries/vibemix-core-<triple>/vibemix-core-<triple>
  - Already built 2026-05-20 (dist/vibemix-core/ 33 MB onedir + both triples staged); this re-run CONFIRMS currency vs any post-2026-05-20 source change.

scripts/integration_audit.py main(argv) (verified L731-770):
  `python scripts/integration_audit.py --write-milestone-audit <PATH> [--force]`
  - Composes the audit; writes frontmatter `overall_verdict: WIRED` iff all seams green (integration_audit.py:613,650).
  - --force allows overwriting an existing file.

scripts/dist/verify_signed.py (header, verified):
  - Checksum (sha256) validator runs unconditionally.
  - Exits 0 with `::notice::` when signing artifacts are absent UNLESS `--require-signed` is passed.
  - P46: NEVER POST/PUT to apple/signpath. Read-only file inspection only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild + verify the sidecar; build an unsigned local .dmg (or document the headless blocker)</name>
  <read_first>scripts/build_sidecar.py (header), 58-RESEARCH.md "Real Artifact Build" table + Code Examples</read_first>
  <files>dist/ (build outputs only — never src/vibemix/** or tauri/src-tauri/src/**)</files>
  <action>
    Rebuild the sidecar to confirm currency vs Kaan's WIP source edits: `uv run python scripts/build_sidecar.py --spec vibemix-core.macos.spec`. Confirm the auto AIza-leak scan passes (non-zero abort = a leaked key — STOP and surface, do not paper over). Confirm both triples land at `tauri/src-tauri/binaries/vibemix-core-{aarch64,x86_64}-apple-darwin/`.
    Then ATTEMPT an unsigned local `.dmg`: `cargo tauri build` (consumes the staged sidecar via externalBin) → copy the produced `target/release/bundle/dmg/*.dmg` into `dist/`. Do NOT sign, do NOT notarize, do NOT touch any APPLE_*/SIGNPATH_* secret (P46 — EXTERNAL/KAAN-ACTION).
    If `cargo tauri build` fails for signing-config reasons (RESEARCH Open-Q3), do NOT force it: document the exact failure + the CI/real-signed-Mac requirement in the SUMMARY, and note that Gate 2's dry-run path will fall back to the existing `dist/vibemix-0.1.0.dev0-py3-none-any.whl` as the file-to-inspect. The dry-run (Plan 04) must still be able to run.
    READ-ONLY constraint: do NOT edit `tauri.conf.json5` (Kaan WIP + Gate 6 reads it) or any `src/vibemix/**` / `tauri/src-tauri/src/**` file.
  </action>
  <verify>
    <automated>uv run python scripts/build_sidecar.py --spec vibemix-core.macos.spec 2>&1 | tail -5; test -x tauri/src-tauri/binaries/vibemix-core-aarch64-apple-darwin/vibemix-core-aarch64-apple-darwin && echo SIDECAR_OK</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `build_sidecar.py` exits 0; AIza-leak scan clean (no abort)
    - [ ] Both triples present + executable under `tauri/src-tauri/binaries/`
    - [ ] EITHER an unsigned `.dmg` exists in `dist/`, OR the SUMMARY documents the headless-build blocker + the CI/real-Mac requirement + the whl fallback for the dry-run
    - [ ] NO edit to `tauri.conf.json5`, `src/vibemix/**`, or `tauri/src-tauri/src/**` (git status shows none in this plan's scope)
  </acceptance_criteria>
  <done>Sidecar rebuilt + AIza-clean + both triples staged; a real unsigned .dmg in dist/ OR the headless blocker documented.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the v4.0 milestone audit (Gate 4 input) + pin it with a test</name>
  <read_first>scripts/integration_audit.py main() (L731-770) + the existing `.planning/v2.1-MILESTONE-AUDIT.md` frontmatter shape; 58-RESEARCH.md Gate 4 row + Code Examples</read_first>
  <files>.planning/v4.0-MILESTONE-AUDIT.md, tests/repo/test_v4_milestone_audit_present.py</files>
  <action>
    Generate the audit (do NOT hand-write it — Don't-Hand-Roll): `python scripts/integration_audit.py --write-milestone-audit .planning/v4.0-MILESTONE-AUDIT.md`. The generator writes frontmatter `overall_verdict: WIRED` iff all seams are green. If it does NOT emit WIRED, do NOT edit the file to force it — investigate the failing seam, surface it as a blocker in the SUMMARY, and stop (a faked WIRED is a Pitfall-2 violation).
    Then write `tests/repo/test_v4_milestone_audit_present.py` (follow the structural-pin style of `tests/repo/test_kaan_action_ship_runbooks.py`): assert the audit file exists, its frontmatter matches the same `^(overall_verdict|status):\s*(WIRED|passed)\s*$` regex `cut_release.sh:127` greps, and that it carries the generator's `**Generated:** by ... --write-milestone-audit` marker line (integration_audit.py:656) so a hand-written audit cannot satisfy the pin.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/repo/test_v4_milestone_audit_present.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `.planning/v4.0-MILESTONE-AUDIT.md` exists, generator-produced (carries the `**Generated:** by ... --write-milestone-audit` marker)
    - [ ] Frontmatter matches `^(overall_verdict|status):\s*(WIRED|passed)\s*$` (the regex Gate 4 greps at cut_release.sh:127)
    - [ ] `test_v4_milestone_audit_present.py` passes and would FAIL on a hand-written (non-generator) audit
    - [ ] `.planning/v2.1-MILESTONE-AUDIT.md` left untouched (no deletion — it is archived history)
  </acceptance_criteria>
  <done>v4.0 audit generated, reads WIRED, pinned by a test that rejects a hand-written substitute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → shipped binary | The sidecar bundle could leak an embedded API key into a distributed artifact |
| audit generator → Gate 4 | A fabricated WIRED verdict would falsely green a release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Information Disclosure | sidecar bundle | mitigate | `build_sidecar.py` AIza-leak scan runs automatically; non-zero abort STOPS the build. Never ship a key (Bravoh-proxy model). |
| T-58-02 | Tampering | v4.0 milestone audit | mitigate | Audit MUST be generator-produced; `test_v4_milestone_audit_present.py` greps the generator marker so a hand-written WIRED cannot pass (Pitfall 2). |
| T-58-03 | Elevation/Spoofing | .dmg signature | accept (defer) | This plan builds UNSIGNED only; the real signature is EXTERNAL/KAAN-ACTION (P46). No autonomous POST/PUT to apple/signpath. |
| T-58-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 58 installs ZERO new packages (RESEARCH: Package Legitimacy Audit N/A); builds from pinned `uv.lock`. No new install task exists. |
</threat_model>

<verification>
- Sidecar rebuild exits 0 + AIza scan clean + both triples staged.
- `.planning/v4.0-MILESTONE-AUDIT.md` exists, generator-marked, reads WIRED.
- `pytest tests/repo/test_v4_milestone_audit_present.py -q` green.
- `git status` shows NO edits to `src/vibemix/**`, `tauri/src-tauri/src/**`, or `tauri.conf.json5`.
</verification>

<success_criteria>
Real artifacts exist for the gates: sidecar current + AIza-clean, an unsigned .dmg (or documented blocker), and a generator-produced v4.0 milestone audit reading WIRED, pinned by a test.
</success_criteria>

<output>
Create `.planning/phases/58-ship-readiness/58-01-SUMMARY.md` when done.
</output>
