---
phase: 58-ship-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - KAAN-ACTION-LEGAL.md
  - tests/repo/test_kaan_action_v4_surface.py
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "ONE consolidated v4.0 ship surface exists in KAAN-ACTION-LEGAL.md (the single source of truth), in the canonical 8-block format, dated 2026-05 v4.0"
    - "It lists EVERY open external-clock + Kaan-action discharge item: Apple Dev Agreement (Francesco), SignPath OSS cert (Kaan, shared with v3.x), the §E2E-50A-WALK recording, the Gate-2b ear-passes (54+55 HUMAN-UAT), + still-open v3.0/v3.1 carry-forwards"
    - "It states the EXACT one-button SHIP-CUT sequence and cross-references the existing §SHIP-CUT runbook (does not re-write it)"
    - "It surfaces the public-tag Kaan-confirm (v0.1.0-rc1 recommended; v4.0.0-rc1 is a one-line override) and the Bravoh-server-up + healthz-fresh precondition for Gate 5b"
    - "A repo test pins that the consolidated surface lists every open carry-forward so the cookbook cannot drift away from STATE"
  artifacts:
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "Consolidated v4.0 ship surface section (e.g. §SHIP-V4) in canonical 8-block format"
      contains: "SHIP-V4"
    - path: "tests/repo/test_kaan_action_v4_surface.py"
      provides: "Completeness pin — every open carry-forward + the public-tag confirm + the Gate-5b precondition appear"
  key_links:
    - from: "§SHIP-V4 (new consolidated section)"
      to: "§SHIP-CUT runbook (existing, KAAN-ACTION-LEGAL.md:307)"
      via: "cross-reference (do not duplicate the 9-step runbook)"
      pattern: "SHIP-CUT"
    - from: "the consolidated surface"
      to: "DIST-09 (Apple) + DIST-11 (SignPath) sign-off blocks"
      via: "links each external signature to its existing sign-off block"
      pattern: "DIST-09|DIST-11"
  kaan_action:
    - "This plan DOCUMENTS the discharge items; it NEVER attempts a signature (P46 hard rule — no autonomous POST/PUT to apple.com/signpath.io). All listed items are Kaan/Francesco/external-clock actions."
---

<objective>
Consolidate ALL open external-clock + Kaan-action discharge items into ONE v4.0 ship surface in
the canonical `KAAN-ACTION-LEGAL.md` cookbook (single source of truth), in the existing 8-block
format, cross-referencing — never re-writing — the existing §SHIP-CUT runbook. Pin completeness
with a test so the surface cannot silently drift away from STATE's open items.

Purpose: REL-03 — surface the external-clock items as KAAN-ACTION with the exact one-button
SHIP-CUT sequence documented. The dry-run (Plan 04) is the proof; this is the human-discharge map.
Output: a `§SHIP-V4 — Consolidated v4.0 Ship Surface` section + a completeness-pin test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/58-ship-readiness/58-RESEARCH.md
@.planning/phases/58-ship-readiness/58-CONTEXT.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

KAAN-ACTION-LEGAL.md (verified structure, 3385 lines):
  - Canonical 8-block section format: header / who / pre-req / commands / verification / post-discharge / unblocks / fenced Sign-off block.
  - "Section SHIP" — Phase 39 Public RC Cut + Ship (L301); SHIP-CUT 9-step runbook (L307-339):
      python scripts/launch/populate_changelog.py --tag TAG
      bash scripts/launch/cut_release.sh TAG   # prints (never runs) gh release create
      # Kaan copies + runs the printed `gh release create ... --draft`
      # then: gh release edit TAG --draft=false --repo bravoh/vibemix
  - DIST-09 Apple Dev Program Agreement (Francesco, L156) + Sign-off block (L191).
  - DIST-11 SignPath OSS Foundation cert (Kaan, ~1wk SLA, L202) + Sign-off block (L255).
  - P46 hard rule: autonomous agents NEVER POST/PUT to apple.com/signpath.io (CI greps enforce).

tests/repo/test_kaan_action_ship_runbooks.py (verified, the pattern to mirror):
  - Structural pin: H2 headers present in order, each carries the 8 canonical blocks, cited scripts EXIST on disk,
    AI-slop blocklist clean (16 tokens + no `deeply \w+`), sign-off blocks carry `_____` + `Sign-off by`.

Open v4.0 carry-forwards to roll in (STATE.md:236-254 + RESEARCH carry-forward table):
  - Apple Dev Agreement (DIST-09, Francesco) · SignPath OSS cert (DIST-11 + INSTALL-COMPANION-SIGN — SAME shared cert)
  - E2E-50A-WALK recording → docs/e2e/2026-05-walk.webm (REL-02 feed of Gate 6b)
  - Gate-2b ear-passes: 54-HUMAN-UAT + 55-HUMAN-UAT (live hype + coach ear-pass >=2 genres)
  - INSTALL-VM-RUN (Tart VM 5-OS matrix; gated on COMPANION-SIGN — surface, not a v4.0 blocker)
  - VIS-04 / VIS-05 Mixamo retargets (parallel asset-discharge, not ship-blocking)
  - SHIP-CONTACT-VBAUDIO (email VB-Audio; ship-optimization)
  - DEPS-08 (livekit-plugins-openai cull; tech-debt, post-v3.1)
  - Public-tag confirm: v0.1.0-rc1 recommended; v4.0.0-rc1 = one-line regex override (CONTEXT decision)
  - Gate 5b precondition: Bravoh prod server up + healthz fresh (<=10 min)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the consolidated SHIP-V4 ship surface to KAAN-ACTION-LEGAL.md</name>
  <read_first>KAAN-ACTION-LEGAL.md SHIP / SHIP-CUT (L301-339) + DIST-09 (L156-201) + DIST-11 (L202-261) for the 8-block format + sign-off shape; STATE.md "Deferred Items" + "Blockers"; 58-RESEARCH.md "Open v3.0/v3.1 KAAN-ACTION carry-forwards" table</read_first>
  <files>KAAN-ACTION-LEGAL.md</files>
  <action>
    Append a new dated section "## SHIP-V4 — Consolidated v4.0 Ship Surface (2026-05)" AFTER the last existing section (do NOT touch any pre-existing content — append-only here; mirror the byte-preservation invariant test_kaan_action_ship_runbooks.py enforces for prior content). Use the canonical 8-block format. The section must:
    - State the EXACT one-button sequence and CROSS-REFERENCE the existing SHIP-CUT 9-step runbook (point to it; do NOT duplicate the 9 steps).
    - Enumerate every open discharge item from the interfaces list, each with owner (Kaan/Francesco/external), status, and a pointer to its existing Sign-off block (DIST-09, DIST-11) where one exists.
    - Surface the public-tag Kaan-confirm: recommend v0.1.0-rc1 (vibemix's first public OSS release, per CONTEXT decision); note v4.0.0-rc1 is a one-line TAG_REGEX override Kaan can pick — surface as a confirm, NOT a blocker.
    - Note SignPath cert is the SAME shared cert across v3.x INSTALL-COMPANION-SIGN + DIST-11 + this ship (consolidate, do not double-count).
    - List the Gate 5b precondition: "Bravoh prod server up + /vibemix/healthz fresh (<=10 min)" before a real cut.
    - Carry a fenced Sign-off block with the `_____` placeholders + a `Sign-off by` line, matching the canonical shape.
    Honor the anti-slop blocklist (16 tokens + no `deeply <word>`) in all new copy. NEVER write a curl/gh/POST to apple.com/signpath.io (P46).
  </action>
  <verify>
    <automated>grep -q "SHIP-V4" KAAN-ACTION-LEGAL.md && grep -q "SHIP-CUT" KAAN-ACTION-LEGAL.md && python3 scripts/launch/check_no_ai_slop.py KAAN-ACTION-LEGAL.md; echo "slop-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] A new "SHIP-V4" section appended; all pre-existing content byte-preserved
    - [ ] Cross-references the existing SHIP-CUT runbook (does NOT duplicate the 9 steps)
    - [ ] Lists Apple (DIST-09), SignPath (DIST-11, shared cert), E2E-50A-WALK, 54+55 ear-passes, INSTALL-VM-RUN, VIS-04/05, VBAUDIO, DEPS-08
    - [ ] States the public-tag confirm (v0.1.0-rc1 recommended) + the Gate-5b Bravoh precondition
    - [ ] Anti-slop check exits 0; carries a canonical sign-off block; NO POST/PUT to apple/signpath
  </acceptance_criteria>
  <done>One consolidated v4.0 ship surface in the canonical cookbook, anti-slop clean, cross-referencing SHIP-CUT.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the consolidated surface's completeness with a repo test</name>
  <read_first>tests/repo/test_kaan_action_ship_runbooks.py (the structural-pin pattern); the new SHIP-V4 section from Task 1; STATE.md open-items list</read_first>
  <files>tests/repo/test_kaan_action_v4_surface.py</files>
  <action>
    Write `tests/repo/test_kaan_action_v4_surface.py` mirroring the `test_kaan_action_ship_runbooks.py` style. Assert: (a) the "SHIP-V4" section exists; (b) it names every open carry-forward token — DIST-09, DIST-11, INSTALL-COMPANION-SIGN, E2E-50A-WALK / 2026-05-walk.webm, 54-HUMAN-UAT, 55-HUMAN-UAT, INSTALL-VM-RUN, VIS-04, VIS-05, SHIP-CONTACT-VBAUDIO, DEPS-08; (c) it cross-references SHIP-CUT (the literal token appears); (d) it states the public-tag confirm (`v0.1.0-rc1`) + the Gate-5b Bravoh precondition (`healthz`); (e) the appended copy is anti-slop clean (import AI_SLOP_BLOCKLIST from scripts/launch/check_no_ai_slop.py and assert no banned token + no `deeply \w+`); (f) it carries a sign-off block (`_____` + `Sign-off by`). The test must FAIL if a new open item is added to STATE but not reflected here (drift guard) — at minimum hard-code the current open-item token list so a missing token fails.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/repo/test_kaan_action_v4_surface.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Test asserts the SHIP-V4 section + all current open-item tokens are present
    - [ ] Test asserts SHIP-CUT cross-reference + public-tag confirm + Gate-5b precondition
    - [ ] Test asserts anti-slop clean (imports the canonical blocklist) + a sign-off block
    - [ ] `pytest tests/repo/test_kaan_action_v4_surface.py -q` green
  </acceptance_criteria>
  <done>Completeness + anti-slop + sign-off pinned; the cookbook cannot drift away from STATE's open items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cookbook → human discharge | A missing item leaves a real ship-blocker undocumented (silent gap) |
| autonomous agent → external signing endpoints | P46 — an agent must never attempt the signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Repudiation/Tampering | KAAN-ACTION cookbook | mitigate | `test_kaan_action_v4_surface.py` pins every open carry-forward token + sign-off block; drift = test failure. |
| T-58-07 | Elevation/Spoofing | external signing endpoints | mitigate | The section DOCUMENTS DIST-09/DIST-11; it contains no curl/gh/POST to apple/signpath. CI P46 grep + the anti-slop/structure test enforce. |
| T-58-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `grep "SHIP-V4" KAAN-ACTION-LEGAL.md` matches; anti-slop check exits 0.
- `pytest tests/repo/test_kaan_action_v4_surface.py -q` green.
- All pre-existing KAAN-ACTION-LEGAL.md content byte-preserved (append-only diff).
- NO edits to `src/vibemix/**`, `tauri/src-tauri/src/**`, or `tauri.conf.json5`.
</verification>

<success_criteria>
One consolidated, anti-slop-clean v4.0 ship surface in the canonical cookbook lists every open external-clock + Kaan-action item, cross-references SHIP-CUT, and is pinned against drift by a completeness test.
</success_criteria>

<output>
Create `.planning/phases/58-ship-readiness/58-03-SUMMARY.md` when done.
</output>
