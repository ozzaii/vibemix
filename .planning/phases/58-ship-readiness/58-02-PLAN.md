---
phase: 58-ship-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/e2e/record_50a_walk.sh
  - tests/repo/test_record_50a_walk_paths.py
  - tests/e2e/macbook/test_report_render.py
  - dist/e2e-macbook-runs/
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "record_50a_walk.sh resolves OUT_DIR/OUT_WEBM correctly regardless of the cwd it is invoked from (the latent REPO_ROOT double-`..` bug is fixed)"
    - "render_report.py produces a REAL dist/e2e-macbook-runs/<UTC>/report.html from an EeRun that check_e2e_report.sh (Gate 6b) accepts (no FAIL dimension)"
    - "The §E2E-50A-WALK recipe (record → transcode → land at docs/e2e/2026-05-walk.webm) is documented + path-correct so Kaan's recorded walk drops straight in"
    - "The Gate 6b producer→consumer path is proven green on a REAL (non-faked) run"
  artifacts:
    - path: "scripts/e2e/record_50a_walk.sh"
      provides: "Path-correct recording rig (REPO_ROOT two-levels-up; absolute OUT_WEBM)"
      pattern: "BASH_SOURCE.*\\.\\./\\.\\."
    - path: "tests/repo/test_record_50a_walk_paths.py"
      provides: "Pins OUT_DIR/OUT_WEBM resolve to repo-root/docs/e2e regardless of cwd"
    - path: "dist/e2e-macbook-runs/"
      provides: "A real rendered report.html Gate 6b consumes"
      contains: "report.html"
  key_links:
    - from: "tests/e2e/macbook/render_report.py render(EeRun)"
      to: "dist/e2e-macbook-runs/<run_id>/report.html"
      via: "Jinja render of a real EeRun"
      pattern: "dist/e2e-macbook-runs/.*/report\\.html"
    - from: "scripts/e2e/check_e2e_report.sh"
      to: "the rendered report.html"
      via: "Gate 6b parses the 5 dimension statuses; exits 0 on no-FAIL"
      pattern: "Hallucination|Functional|Visual|Aesthetic|Usability"
  kaan_action:
    - "The actual recorded docs/e2e/2026-05-walk.webm on the real Mac with real DJ-set audio (Gate 6b feed + E2E-06 zero-HIGH Nielsen). Engineering proves the path + produces a real report; the .webm itself is KAAN-ACTION — do NOT fake it."
    - "The Hallucination-dimension qualitative pass in the report is Kaan's live ear; engineering proves the producer→consumer wiring on a real minimal run, not a fabricated all-PASS."
---

<objective>
Make the §E2E-50A-WALK dischargeable: fix the latent path bug in the recording rig so the recipe
works from any cwd, and prove the Gate-6b producer→consumer path green on a REAL rendered report
(not a faked one). The actual recorded walk on the Mac is KAAN-ACTION — engineering ships the
working rig + a real report path + the documented recipe.

Purpose: REL-02 — discharge the §E2E-50A-WALK by driving the real app end-to-end. Engineering
proves the harness/app path is green; the .webm recording is Kaan's.
Output: path-correct `record_50a_walk.sh` (+ a path-pin test), a real `dist/e2e-macbook-runs/<UTC>/report.html`
produced by `render_report.py` (+ a render test), and the documented walk recipe in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-ship-readiness/58-RESEARCH.md

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

scripts/e2e/record_50a_walk.sh (verified — THE BUG, L20-22):
  REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"   # resolves to scripts/ (one ../ too few — file is in scripts/e2e/)
  OUT_DIR="${REPO_ROOT}/../docs/e2e"                            # accidentally lands right via double-`..`, but REPO_ROOT is mislabeled
  OUT_WEBM="docs/e2e/2026-05-walk.webm"                         # relative to cwd — only correct if run from repo root
  Modes: --record (screencapture -v -a), --transcode <mov> (ffmpeg VP9 + opus 32k, crf 32, <25MB).

tests/e2e/macbook/render_report.py (verified, full):
  from tests.e2e.macbook.dimensions import EeRun
  from tests.e2e.macbook.render_report import render
  render(run: EeRun, out_root: Path | None = None) -> Path
    # writes <out_root or dist/e2e-macbook-runs>/<run.run_id>/report.html ; returns the Path

scripts/e2e/check_e2e_report.sh (verified, head):
  REPO_ROOT="${REPO_ROOT:-...}"; RUN_ROOT="${VIBEMIX_E2E_RUN_ROOT:-${REPO_ROOT}/dist/e2e-macbook-runs}"
  Exit 0 = latest report all PASS/PARTIAL/SKIPPED; 1 = a dimension FAIL; 2 = no run found.
  Parses the 5 locked row labels: Functional / Visual / Aesthetic / Usability / Hallucination.

tests/e2e/macbook/dimensions.py (per RESEARCH): EeRun dataclass + Hallucination dimension at dimensions.py:61.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the record_50a_walk.sh path bug + pin it</name>
  <read_first>scripts/e2e/record_50a_walk.sh (full) + 58-RESEARCH.md §E2E-50A-WALK "BUG to fix"</read_first>
  <files>scripts/e2e/record_50a_walk.sh, tests/repo/test_record_50a_walk_paths.py</files>
  <action>
    Fix `REPO_ROOT` to resolve two levels up (`"$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"`) since the script lives in `scripts/e2e/`. Set `OUT_DIR="${REPO_ROOT}/docs/e2e"` and make `OUT_WEBM` an ABSOLUTE path under `OUT_DIR` (`OUT_WEBM="${OUT_DIR}/2026-05-walk.webm"`) so the recipe works regardless of the cwd it is invoked from. Keep the final `.webm` target path EXACTLY `docs/e2e/2026-05-walk.webm` (relative to repo root) — it must match CONTEXT + README + Gate 6b expectations. Preserve --record / --transcode behavior, the screencapture flags, and the ffmpeg VP9/opus params verbatim; this is a path-only surgical fix.
    Write `tests/repo/test_record_50a_walk_paths.py`: assert (a) the script's resolved `OUT_WEBM` equals `<repo_root>/docs/e2e/2026-05-walk.webm` when the script is sourced/dry-parsed from an ARBITRARY cwd (e.g. spawn a subprocess from `/tmp` and echo the resolved path via a `--print-paths`-style harness OR grep the literal `../..` + `${OUT_DIR}/2026-05-walk.webm` shape), and (b) `ffmpeg` presence is checked before transcode (the existing guard stays).
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/repo/test_record_50a_walk_paths.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `REPO_ROOT` resolves two levels up (`../..`); `OUT_WEBM` is absolute under `OUT_DIR`
    - [ ] Final target path is exactly `docs/e2e/2026-05-walk.webm` (repo-root-relative)
    - [ ] `test_record_50a_walk_paths.py` passes and would FAIL against the old single-`..` form
    - [ ] --record / --transcode behavior + ffmpeg params unchanged (path-only diff)
  </acceptance_criteria>
  <done>Recording rig is path-correct from any cwd; pinned by a test that rejects the old bug.</done>
</task>

<task type="auto">
  <name>Task 2: Produce a REAL Gate-6b report + prove the producer→consumer path green</name>
  <read_first>tests/e2e/macbook/render_report.py (full), tests/e2e/macbook/dimensions.py (EeRun + Hallucination), scripts/e2e/check_e2e_report.sh (full); 58-RESEARCH.md "wired to flip" + Pitfall 2</read_first>
  <files>tests/e2e/macbook/test_report_render.py, dist/e2e-macbook-runs/</files>
  <action>
    Write/extend `tests/e2e/macbook/test_report_render.py` to build a REAL `EeRun` (populate the 5 dimensions with honest non-FAIL statuses for the engineering-provable legs; the Hallucination dimension is PARTIAL/SKIPPED-pending-Kaan-ear, never a fabricated PASS), call `render(run)` to write `dist/e2e-macbook-runs/<run_id>/report.html`, then invoke `scripts/e2e/check_e2e_report.sh` (via subprocess, pointing `VIBEMIX_E2E_RUN_ROOT` at the rendered run dir) and assert it exits 0. This proves the producer→consumer path end-to-end on a real (rendered, not hand-written) report.
    Pitfall 2: do NOT write a `report.html` by hand — it MUST come from `render_report.render()`. Do NOT mark Hallucination PASS without a real ear-pass; PARTIAL/SKIPPED is the honest pre-Kaan state and Gate 6b accepts it (exit 0 on PASS/PARTIAL/SKIPPED).
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest tests/e2e/macbook/test_report_render.py -q --no-header</automated>
  </verify>
  <acceptance_criteria>
    - [ ] A real `dist/e2e-macbook-runs/<run_id>/report.html` is produced by `render_report.render()` (not hand-written)
    - [ ] `scripts/e2e/check_e2e_report.sh` exits 0 against the rendered run (all dimensions PASS/PARTIAL/SKIPPED, no FAIL)
    - [ ] Hallucination dimension is PARTIAL/SKIPPED-pending-Kaan-ear, NOT a fabricated PASS (Pitfall 2)
    - [ ] `test_report_render.py` passes
  </acceptance_criteria>
  <done>Gate 6b producer→consumer path proven green on a real rendered report; Hallucination honestly pending Kaan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render_report → Gate 6b | A fabricated report.html could falsely green the e2e gate |
| recording rig → committed .webm | A path bug silently writes the walk artifact to the wrong location |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | Tampering | Gate 6b report.html | mitigate | Report MUST come from `render_report.render()`; the test invokes the real renderer + the real `check_e2e_report.sh`. Hallucination dimension never fabricated-PASS (Pitfall 2). |
| T-58-05 | Tampering | recorded walk path | mitigate | Path-bug fix + `test_record_50a_walk_paths.py` pins the resolved target so the .webm lands at the exact Gate-6b-expected location. |
| T-58-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `pytest tests/repo/test_record_50a_walk_paths.py -q` green; resolved target is `docs/e2e/2026-05-walk.webm` from any cwd.
- `pytest tests/e2e/macbook/test_report_render.py -q` green; a real report renders + `check_e2e_report.sh` exits 0 on it.
- `git status` shows NO edits to `src/vibemix/**`, `tauri/src-tauri/src/**`, or `tauri.conf.json5`.
</verification>

<success_criteria>
The §E2E walk rig is path-correct + pinned; the Gate-6b producer→consumer path is proven green on a real report; the recorded .webm + the Hallucination ear-pass remain the KAAN-ACTION carveout (not faked).
</success_criteria>

<output>
Create `.planning/phases/58-ship-readiness/58-02-SUMMARY.md` when done.
</output>
