# Handoff — New-user / "neat package" robustification (2026-05-29)

**Goal:** deep code cleanup + robustification so a new user downloads the packaged
app and it works neatly. Backend island (`src/vibemix/**` minus `learn/`).

## Shipped this session (7 commits, branch `live-tuning-or-brain`, NOT pushed)

| Commit | What | Why it matters for a new user |
|--------|------|-------------------------------|
| `ef849e01` | track `library/prepared_pool.py` | **P0** — committed `__main__.py`/`suggestion.py` imported an untracked module → fresh `git clone` / PyInstaller sidecar ImportError'd → app would not start. |
| `beac3886` | output device → OS-default fallback | **P1** — `main()` hardcoded `OUTPUT_DEVICE="MacBook Pro Speakers"` → crash on any non-MacBook Mac / external output / localized macOS, with a misleading "install BlackHole" banner. |
| `8362e37b` | output resolver rejects loopback | **P2** — a stale persisted device index landing on BlackHole would route the AI voice into the capture device (feedback loop → AI hears itself = anti-slop hazard). |
| `1614910d` | library-CLI model-gap hint | **P1** — `library search/similar` dumped a raw traceback with no model installed; now an actionable `library models --install clap` hint. |
| `810b3e2d` | drop dead imports in `dj_cohost.py` | cleanup (AST-confirmed dead `find_sentence_end`/`passes_head_gate`). |
| `92c0df54` | clean-checkout integrity CI gate | **prevents the `prepared_pool` class** from recurring (catches a committed top-level import of an untracked vibemix module). |
| `62b0cbac` | CLAUDE.md milestone currency | docs (v9.0→v11.0). |

Verified: fresh `git archive HEAD` imports every entry path (`__main__`, debrief,
wizard, library, intel, agent). Suite green (2855 passed). Two workflows (7-agent
recon + 4-agent adversarial verify) confirmed the fixes + invariants.

## DEFERRED — needs another session / owner / a frozen build

### Cross-session landmines (HIGH — now guarded by `92c0df54`)
- **~4 untracked modules imported by concurrent sessions' DIRTY edits** (e.g.
  `intel/move_grade.py`, plus learn modules). Clean at HEAD today, but the moment
  those sessions commit their importers WITHOUT the module, the clean checkout
  re-breaks. The new `tests/repo/test_clean_checkout_imports.py` gate will fail
  the commit — each session must `git add` its module alongside its importer (the
  `prepared_pool` lesson).

### My-island, deferred with rationale
- **B1 — ws `:8765` bind unguarded** (`runtime/ws_bus.py:592`, `ws_broadcast`): a
  port collision (zombie sidecar) hangs `main()` forever instead of exiting with
  the port-in-use sentinel (wizard.py:562 / session_loop.py:1355 have the guard to
  mirror: `except OSError → sys.exit(2)`). Deferred: `ws_bus.py` carries 118 lines
  of another session's active course3-lens WIP and it's the single-socket file —
  not worth absorbing mid-edit for a restart-edge (the watch-parent guard prevents
  new-user zombies). Land it solo when `ws_bus.py` settles.
- **B3 / output-open guards** (`__main__.py` open_capture/voice/passthrough): a
  `SampleRateMismatchError` (fresh BlackHole @44.1k) or PortAudio open failure is
  uncaught → exit 1 → watchdog retries. Deliberately NOT changed: the retry +
  `assert_device_sample_rate` auto-opening Audio MIDI Setup is an *intentional
  recovery flow* (the user fixes the rate between retries). Converting to a
  no-retry exit-3 alone regresses UX + shows a wrong banner. Land it PAIRED with
  E1 (a sample-rate-specific crash banner).

### Cross-island (UI / learn)
- **E1 (UI)** — `tauri/ui/src/crash-banner.ts`: exit-3 always says "install
  BlackHole", even for an output-device miss or (future) sample-rate mismatch.
  Branch the banner on a sub-reason from the `[FATAL]` last-log line.
- **E2 (learn)** — `learn/__init__.py` imports untracked `graduation`/`lesson_flow`/
  `teaching_loop` + 4 exemplar `.wav`s (same untracked-import class; the new gate
  guards the `.py` side).
- **D4 (learn)** — band-exemplar `.wav`s are untracked (only `.gitkeep`+MANIFEST
  committed; generated by `scripts/audition_learn_exemplars.py`) AND the spec
  collects only `**/*.json,**/*.txt` → the frozen app's EQ-band feature has no
  wavs. Learn owns: commit-or-generate-at-build + add `**/*.wav` to both specs.

### Packaging (needs a real frozen build to confirm — could not verify statically)
- **D3** — `_rtmidi` native extension: no bundled PyInstaller hook +
  `collect_dynamic_libs("rtmidi")` returns 0. MAY be pulled via the import graph;
  only a frozen `pyinstaller vibemix-core.macos.spec` run + a MIDI smoke confirms.
  If missing, MIDI controllers are silently absent in the shipped app.
- **VERIFIED-OK (not gaps):** spec subpackage collection (`collect_submodules('vibemix')`
  recurses into all 19 subpackages — the explicit 9-entry list is redundant
  Phase-2-10 leftover, NOT a missing-half bug); PortAudio (PyInstaller auto-bundles
  `hook-sounddevice`); `demo_mode.py` + `emote_parser.py` are tested features, NOT
  orphans (do not delete).

### Follow-up nicety
- Output `output_device_id` is a positional index (reorders on replug). Persisting
  the device NAME alongside it (re-validate identity) would fully close the
  stale-index→wrong-output path; `8362e37b` already closed the dangerous
  feedback-loop subset.
