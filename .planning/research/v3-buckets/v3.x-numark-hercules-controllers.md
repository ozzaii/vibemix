# v3.x — Numark / Hercules controller library expansion

**Status:** Yellow-defer (carried forward from v3.1 opportunity scan)
**Originating scan:** `docs/dep-opportunities/2026-05-scan.md` § DEP-OPP-22
**YAML row:** `scripts/audit/dep_ratings.yaml::opportunity_evaluations[id=DEP-OPP-22]`
**Memory anchor:** `project_v2_open_candidates`
**Date deferred:** 2026-05-18

## Why deferred

Not a new dependency — just JSON map files. The path to expansion is the controller-map-transpiler (DEP-OPP-18). v3.1 keeps the controller library at the baseline 10 per memory `project_v2_open_candidates`.

## Re-eval trigger

- DEP-OPP-17 (Mixxx OSC) and DEP-OPP-18 (transpiler) both land.
- A v3.x phase explicitly takes "controller library expansion" as a goal.

## Context

Numark and Hercules are common entry-level controller brands. Adding maps without a transpiler path means per-device manual work; with the transpiler, both brands flow through the same import pipeline as any other Mixxx-corpus controller.
