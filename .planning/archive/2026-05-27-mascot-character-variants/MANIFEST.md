# Mascot Character Variants Archive - 2026-05-27

Reason: untracked alternate mascot character GLBs pushed the committed mascot
asset tree over the 25 MB bundle cap (`41.79 MB` total). The active product
contract is a single locked `character.glb` with mood handled by material/color
and animation state, so the variants were removed from the active manifest and
archived here.

Archived payload:

- `characters/midnight_raver.glb`
- `characters/noir_byte.glb`

Active source retained:

- `tauri/ui/assets/mascot/character.glb`
- `tauri/ui/assets/mascot/animations/*.glb`
- `tauri/ui/assets/mascot/manifest.json`

Local bundle: `.planning/archive/2026-05-27-mascot-character-variants.zip`.
