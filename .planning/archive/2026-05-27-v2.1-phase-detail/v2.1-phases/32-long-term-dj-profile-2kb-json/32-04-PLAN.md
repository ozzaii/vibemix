# Plan 32-04 — Wizard consent toggle (PROFILE-05)

**REQ-IDs:** PROFILE-05
**Wave:** 2 (depends on 32-01 for field-set strings)
**Status:** ready

## Files

- **NEW** `tauri/ui/src/wizard/components/profile-consent.ts` — vanilla TS factory `renderProfileConsentCard({consent, onToggle}) -> HTMLElement`.
- **NEW** `tauri/ui/src/wizard/step-profile-consent.ts` — full step surface, mounted by router between `controller` and `smoke-test`.
- **EDIT** `tauri/ui/src/wizard/router.ts` — add `"profile-consent"` to `WizardStep`, `STEP_ORDER`, `DEFAULT_STATE`, `back()`, `renderCurrentStep()` switch.
- **EDIT** `tauri/ui/src/ipc/messages.schema.json` — add `ProfileSetConsent` + `ProfileConsentState` definitions.
- **EDIT** `src/vibemix/ui_bus/schemas/profile.py` (NEW file) — `ProfileSetConsentPayload` + `ProfileConsentStatePayload`.
- **EDIT** `src/vibemix/ui_bus/messages.py` — wrap with `.make()` factory.
- **EDIT** `src/vibemix/runtime/wizard_loop.py` — register handler `ipc.profile.set_consent` → write `profile_consent: bool` into `state.json`.

## Wizard step surface

UI-SPEC §New Step:
```
STEP 4 / 4 · PROFILE  (note: step indicator updated to 4 steps)
let vibemix learn your style — coaching gets sharper over time.

[ ] BUILD A PROFILE TO PERSONALIZE COACHING?   <default OFF>

stored locally only — never uploaded:
  - preferred_genre       (hard_tek, techno, house, …)
  - avg_session_duration  (minutes)
  - mix_style_tags        (long_blends, quick_cuts, …)
  - tempo_preference_bin  (110-120, 120-128, …)
  - response_preferences  (per event type)

NO track titles, NO library contents, NO free-form text.

[ ← Back ]                       [ continue ]
```

Default state: `profile_consent: false`. Toggle off → checkbox unchecked → Continue advances without enabling.

## IPC additions

```json
"ProfileSetConsent": {
  "$comment": "Phase 32 — Shell → sidecar. Persists user's profile-build consent to ~/.config/vibemix/state.json. Default-OFF (PROFILE-05). Sidecar acks via ProfileConsentState.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "ts", "payload"],
  "properties": {
    "type": {"const": "ipc.profile.set_consent"},
    "ts": {"type": "string", "format": "date-time"},
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["consent"],
      "properties": {"consent": {"type": "boolean"}}
    }
  }
},
"ProfileConsentState": {
  "$comment": "Phase 32 — Sidecar → shell. Reflects current profile_consent state from disk.",
  "type": "object",
  "additionalProperties": false,
  "required": ["type", "ts", "payload"],
  "properties": {
    "type": {"const": "ipc.profile.consent_state"},
    "ts": {"type": "string", "format": "date-time"},
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["consent"],
      "properties": {"consent": {"type": "boolean"}}
    }
  }
}
```

Plus `oneOf` entries (top of schema).

## Storage extension

`storage.py` adds `consent_path()` + `load_consent() / save_consent(bool)` writing to `~/.config/vibemix/state.json` key `profile_consent`.

## Tests (Gate)

Python:
1. `test_consent_default_off`: fresh state.json → `load_consent()` returns `False`.
2. `test_consent_persisted`: `save_consent(True)` → file `profile_consent: true`.
3. `test_ipc_profile_set_consent_validates`: schema accepts well-formed envelope.
4. `test_ipc_profile_set_consent_rejects_extra`: `additionalProperties: false` blocks extra fields.

TS (vitest):
5. `profile-consent.spec.ts`: card factory renders with `checked=false` by default; clicking toggle fires `onToggle(true)`.
6. `step-profile-consent.spec.ts`: continue CTA armed regardless of toggle state (user can decline AND continue).

## Verification

- `python -m pytest tests/profile/test_consent.py tests/ipc/test_profile_schemas.py -v`
- `cd tauri/ui && npm run check:ipc && npm test -- profile-consent`

## Atomic commit

```
feat(32-04): wizard consent toggle (PROFILE-05, default-OFF)

- wizard/components/profile-consent.ts: vanilla TS card factory
- wizard/step-profile-consent.ts: new step surface inserted between
  controller and smoke-test
- wizard/router.ts: STEP_ORDER + back() + renderCurrentStep() updated
- ipc.profile.{set_consent,consent_state} schemas with additionalProperties:false
- src/vibemix/ui_bus/schemas/profile.py + messages.py wrappers
- runtime/wizard_loop.py: handler writes profile_consent: bool to state.json
- 6 tests: default-OFF, persistence, schema reject-extra, TS render path

P51 default-OFF non-negotiable; field-set disclosed inline.
```
