# Plan 35-04 — Doctrine Docs + AI-VO Grep Gate

**REQ-IDs:** ASSETS-01, ASSETS-02, ASSETS-06
**Pitfall:** P58

## Goal

Ship the doctrine docs that govern asset generation + demo film production. Add a grep gate that prevents any AI-VO service from sneaking into `scripts/demo_film/`.

## Tasks

1. `docs/asset_pipeline.md` — Meshy v6 vs Hunyuan3D 3.0 A/B, Mixamo auto-rig, Rokoko fallback (P61), gltfpack DRACO L7 + KTX2, SkeletonHelper QA.
2. `scripts/demo_film/3beat_structure.md` — Beat A (overlay highlight) → Beat B (mascot lean-in BEFORE voice) → Beat C (cited reaction).
3. `scripts/demo_film/recording_protocol.md` — Kaan/Francesco live session capture, 3min+ raw, Quartz screen + BlackHole audio.
4. `scripts/demo_film/vo_policy.md` — NO AI-VO, Kaan/Francesco-written OR no-VO. Enumerates forbidden services.
5. `tests/scripts/test_demo_film_no_ai_vo.py`:
   - `test_no_ai_vo_in_scripts` — grep `scripts/demo_film/**/*.{sh,py,json,ts,js}` for forbidden tokens (elevenlabs, openai, gemini-tts, tts.googleapis, synth.voice, ai-voiceover, synthesize_speech) case-insensitive. Fail with file list if any match.
   - `test_vo_policy_doc_exists` — `scripts/demo_film/vo_policy.md` exists + mentions "NO AI" or "no VO".

## Verification

- `pytest tests/scripts/test_demo_film_no_ai_vo.py -v` → 2 pass.
- Manual: `grep -ri "elevenlabs\|openai\|gemini-tts" scripts/demo_film/ --include="*.sh" --include="*.py"` → no hits.

## Commit

`feat(35-04): doctrine docs (3-beat / recording / VO) + AI-VO grep gate`

