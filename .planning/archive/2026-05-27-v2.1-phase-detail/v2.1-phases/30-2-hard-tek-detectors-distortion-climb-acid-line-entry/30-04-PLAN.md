# Plan 30-04 — tune_detectors.py Hard Tek extension + reference corpus README (SENSE-20)

**REQ:** SENSE-20
**Wave:** 2
**Status:** ready

## Goal
Extend `scripts/tune_detectors.py` so the new DISTORTION_CLIMB / ACID_LINE_ENTRY thresholds are recognised + scored against Kaan-curated Hard Tek reference tracks. Ship `eval/corpus/hard_tek/README.md` documenting curation policy + the (Kaan-action-pending) anchor track list.

## Files

1. `scripts/tune_detectors.py` — extend `_DETECTOR_THRESHOLDS` with new event types, document `--genre=hard_tek` use-case (uses existing `--genre-override`).
2. `eval/corpus/hard_tek/README.md` — new file. Documents curation source policy + acquisition checklist + per-track-meta template.
3. `KAAN-ACTION-LEGAL.md` — append "Phase 30 — Hard Tek reference tracks" line if not already there.

## tune_detectors.py extension

Append to `_DETECTOR_THRESHOLDS`:
```python
"DISTORTION_CLIMB": DISTORTION_FLATNESS_DELTA_MIN,
"ACID_LINE_ENTRY": ACID_SWEEP_SLOPE_MIN_OCT_PER_S,
```

The existing `--genre-override` flag handles `--genre-override=hard_tek` to force the router into the hard_tek chain. No new CLI flag needed.

Add docstring section "Phase 30 extension":
- Lists the 2 new detectors.
- Points at `eval/corpus/hard_tek/README.md` for the canonical curated set.
- Documents the `--genre-override=hard_tek` invocation.

## `eval/corpus/hard_tek/README.md`

```markdown
# Hard Tek reference corpus

Curated set used by `scripts/tune_detectors.py --genre-override=hard_tek <wav...>`
to validate `DISTORTION_CLIMB` + `ACID_LINE_ENTRY` detector thresholds against
real Hard Tek tracks.

## Acquisition policy

Per Phase 27 corpus policy (`eval/corpus/LICENSES.md`):

- **Archive.org** — CC-BY / CC-BY-SA Hard Tek mixes + tracks.
- **CCMixter** — CC-BY remixable tracks.
- **FMA (Free Music Archive)** — CC-licensed electronic + hard techno.

Tracks ship with full attribution + license file alongside the WAV.

## Curated set (KAAN-ACTION)

Status: pending. Kaan to commit 5 anchor tracks covering:

| # | Source | Title | Artist | BPM | Length | License | Why |
|---|--------|-------|--------|-----|--------|---------|-----|
| 1 | TBD | TBD | TBD | 150 | TBD | TBD | Baseline distortion-climb anchor |
| 2 | TBD | TBD | TBD | 160 | TBD | TBD | Acid-line entry mid-track |
| 3 | TBD | TBD | TBD | 170 | TBD | TBD | Distorted-kick + acid stack |
| 4 | TBD | TBD | TBD | 175 | TBD | TBD | Clean hard tek (negative — neither detector should fire) |
| 5 | TBD | TBD | TBD | 180 | TBD | TBD | Edge BPM upper-bound |

WAVs commit to `eval/corpus/hard_tek/audio/<slug>.wav` once acquired. Per-track
JSON sidecar at `eval/corpus/hard_tek/<slug>.json` carries expected fire
timestamps for the F1-scoring step (Phase 27 EVAL-03 matrix).

## Tuning run

```bash
python scripts/tune_detectors.py \
    --genre-override=hard_tek \
    --bpm-override=150 \
    eval/corpus/hard_tek/audio/*.wav \
    --csv eval/tuning/hard_tek_$(date +%Y-%m-%d).csv
```

Output: per-fire CSV with `detector_name`, `t_seconds`, `score`, `threshold`.
Cross-check against per-track JSON sidecar in Phase 16 ear-audit.

## Synthetic fallback

Until the 5 anchor tracks land, `tests/state/detectors/test_distortion_climb.py`
+ `test_acid_line_entry.py` ship deterministic synthetic fixtures that exercise
both detectors in CI. Real-track scoring waits on Kaan's curation pass.
```

## KAAN-ACTION-LEGAL.md append

Find file (likely `.planning/KAAN-ACTION-LEGAL.md` or root) and append item:

> ### Phase 30 — Hard Tek reference tracks (SENSE-20)
> 
> Commit 5 CC-licensed Hard Tek anchor tracks to `eval/corpus/hard_tek/audio/`
> with per-track JSON sidecar + attribution per `eval/corpus/LICENSES.md`.
> Tuning harness + F1 scoring blocked on this acquisition.

If the file doesn't exist, create it with this single item.

## Tests

1. `tests/scripts/test_tune_detectors_phase30.py` — assert `_DETECTOR_THRESHOLDS` contains both new keys.
2. Smoke: run `python scripts/tune_detectors.py --help` exits 0 and mentions Phase 30 wording (smoke test via subprocess in same test file).

## Verification
- `eval/corpus/hard_tek/README.md` exists.
- `KAAN-ACTION-LEGAL.md` mentions Phase 30.
- Threshold table covers new event types.
- Help string renders.
