---
phase: 27-eval-harness-v2-0-carry-forward-close-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/eval/__init__.py
  - scripts/eval/replay_harness.py
  - scripts/eval/f1.py
  - scripts/eval/corpus_manifest.py
  - scripts/eval/scorecard.py
  - src/vibemix/audio/buffers.py
  - tests/eval/__init__.py
  - tests/eval/conftest.py
  - tests/eval/test_replay_harness.py
  - tests/eval/test_f1_math.py
  - tests/eval/test_scorecard.py
  - tests/eval/fixtures/synthetic_session/events.jsonl
  - tests/eval/fixtures/synthetic_session/input.wav
  - tests/eval/fixtures/synthetic_session/responses/.gitkeep
  - pyproject.toml
autonomous: true
requirements:
  - EVAL-01
  - EVAL-08
tags:
  - eval-harness
  - replay
  - f1-math

must_haves:
  truths:
    - "A synthetic recorded session under tests/eval/fixtures/synthetic_session/ can be replayed end-to-end via `uv run python -m scripts.eval.replay_harness --corpus tests/eval/fixtures --judges noop` and produces an eval_report.json + scorecard.md"
    - "AudioBuffer exposes a new fill_from_wav(path) helper that loads recorded mono/stereo WAV into the 16kHz int16 ring without mutating the live runtime callback path"
    - "F1 computation tolerates ±2s event-timestamp drift and produces precision/recall/f1 per detector AND per detector-x-genre cell"
    - "Scorecard renders both markdown (PR comment ready) and JSON (machine-readable .planning/eval-runs artifact) outputs"
    - "Running `uv run pytest tests/eval/test_replay_harness.py tests/eval/test_f1_math.py tests/eval/test_scorecard.py -x` exits 0"
  artifacts:
    - path: "scripts/eval/replay_harness.py"
      provides: "Single-binary deterministic replay CLI entry point (--corpus, --judges, --output, --threshold-lock)"
      min_lines: 180
    - path: "scripts/eval/f1.py"
      provides: "Pure-Python F1/precision/recall on event timestamp lists with ±tolerance windowing + per-detector + per-genre matrix"
      min_lines: 80
    - path: "scripts/eval/scorecard.py"
      provides: "Markdown + JSON scorecard renderer with per-detector-per-genre matrix + threshold status block"
      min_lines: 120
    - path: "scripts/eval/corpus_manifest.py"
      provides: "Corpus diversity validator (Hard Tek ≤ 70%, ≥3 genres, ≥6 sessions) — referenced by Plan 03"
      min_lines: 60
    - path: "tests/eval/conftest.py"
      provides: "Synthetic 5s WAV + events.jsonl fixture factory + corpus manifest temp dir helper"
      min_lines: 60
    - path: "src/vibemix/audio/buffers.py"
      contains: "def fill_from_wav"
      provides: "Additive helper — does not touch sounddevice callback path"
  key_links:
    - from: "scripts/eval/replay_harness.py"
      to: "src/vibemix/audio/buffers.py::AudioBuffer.fill_from_wav"
      via: "import + invoke during session load"
      pattern: "audio_buf.fill_from_wav"
    - from: "scripts/eval/replay_harness.py"
      to: "src/vibemix/state/evidence_registry.py::EvidenceRegistry"
      via: "construct REAL primitive, NOT a mock"
      pattern: "EvidenceRegistry\\("
    - from: "scripts/eval/replay_harness.py"
      to: "src/vibemix/state/event_detector.py::EventDetector"
      via: "tick at 1Hz over fill_from_wav-loaded buffer"
      pattern: "EventDetector\\("
    - from: "scripts/eval/replay_harness.py"
      to: "scripts/eval/f1.py + scorecard.py"
      via: "function call after replay loop"
      pattern: "compute_f1\\(|render_scorecard\\("
---

<objective>
Lay the foundation for the autonomous hallucination-proxy gate: a deterministic, single-binary replay harness CLI plus the supporting math (F1/precision/recall) and reporting (scorecard.md + eval_report.json) that downstream plans (02 judge + 03 corpus + 04 CI gate) build on. Covers EVAL-01 (replay) and EVAL-08 (per-run scorecard artifact). No Gemini API calls in this plan — judges arrive in Plan 02; this plan stubs them via `--judges noop` so the orchestrator + math + scorecard are testable in isolation.

Purpose: every other Phase 27 EVAL plan imports from `scripts/eval/` or extends `tests/eval/`. This plan defines the contracts the rest of the phase consumes.

Output:
- `scripts/eval/` package with `replay_harness.py` CLI + `f1.py` + `scorecard.py` + `corpus_manifest.py`
- Additive `AudioBuffer.fill_from_wav(path)` helper (no live-runtime change)
- `tests/eval/conftest.py` shared synthetic-session fixture factory
- Unit tests for replay loop, F1 math, scorecard rendering
- Dev dependency adds: `vcrpy>=8.0`, `pytest-recording>=0.13` in `[dependency-groups] dev` of `pyproject.toml` (used in Plan 02; declared here so the manifest is set once)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-eval-harness-v2-0-carry-forward-close-out/27-CONTEXT.md
@.planning/phases/27-eval-harness-v2-0-carry-forward-close-out/27-RESEARCH.md
@src/vibemix/audio/buffers.py
@src/vibemix/state/evidence_registry.py
@src/vibemix/state/event_detector.py
@scripts/replay_linter.py

<interfaces>
<!-- Key contracts the executor consumes (extracted from codebase). No exploration needed. -->

From src/vibemix/audio/buffers.py (canonical AudioBuffer that already powers the live runtime):
- `class AudioBuffer` with `__init__(sample_rate: int = 16000, window_seconds: float = 7.0)`
- `push(pcm_int16: np.ndarray)` — appends to ring; held by audio-thread lock
- `snapshot_features(t_session: float | None = None) -> dict` — returns RMS/FFT/BPM features
- Audio callback path uses `push()` only; `fill_from_wav` must NOT call into the live callback machinery (it operates on the ring directly via a private append helper or the same `push()` path, but the test surface must guarantee thread-safe single-shot bulk load).

From src/vibemix/state/evidence_registry.py:
- `class EvidenceRegistry` — `register_evidence(...)`, `register_library(library)` (Plan 25-02, currently unwired — Plan 05 fixes that), `lookup_citation(tag)`
- Constructor takes no required args; safe to instantiate offline

From src/vibemix/state/event_detector.py:
- `class EventDetector` — accepts `evidence_registry=` kwarg + cooldown config
- `detect(snapshot: dict, t_session: float) -> list[Event]` — pure function of snapshot + t_session

From scripts/replay_linter.py (Phase 20-03 scaffold to reference — 270 LOC):
- Existing pattern for argparse + jsonl walk + report rendering
- DO NOT modify replay_linter.py; the new harness lives at scripts/eval/replay_harness.py

Recording format (existing v2.0 convention — DO NOT change):
- `recordings/<session>/events.jsonl` — one event per line: `{id, type, t_session, payload, ...}`
- `recordings/<session>/input.wav` — mono or stereo 48kHz master capture
- `recordings/<session>/voice.wav` — mono Gemini reply audio
- `recordings/<session>/responses/<event_id>.txt` — Gemini text response per ground-truth event (used by Plan 02 judge, stubbed by `--judges noop` in this plan)

Eval recording format (NEW for corpus, EXTENDS the existing one):
- `eval/corpus/sessions/<session>/` — same as recordings/ shape PLUS:
  - `genre.txt` — single-line genre identifier (`hard_tek`, `techno`, `house`, `dnb`); used by F1 per-genre matrix
  - `source.txt` — public-domain URL + license; used by Plan 03 corpus_manifest validator

VCR.py + pytest-recording API (used in Plan 02 — this plan only adds them to pyproject.toml dev group):
- `@pytest.mark.vcr` decorator; cassettes default to `tests/<dir>/cassettes/`
- `record_mode` configured via pytest CLI / conftest; default `"none"` in PR CI
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AudioBuffer.fill_from_wav additive helper + scripts/eval scaffold + F1 math</name>
  <files>src/vibemix/audio/buffers.py, scripts/eval/__init__.py, scripts/eval/f1.py, scripts/eval/corpus_manifest.py, tests/eval/__init__.py, tests/eval/conftest.py, tests/eval/test_f1_math.py</files>
  <behavior>
    - AudioBuffer.fill_from_wav(path: Path | str) — Test 1: loads a synthetic 16kHz mono 5s WAV (1s of silence + 4s of 440Hz sine); after the call, `audio_buf.snapshot_features(t_session=4.0)` returns non-zero `rms` and a `bpm` field that is either `None` or a float. Test 2: gracefully resamples a 48kHz stereo WAV down to 16kHz mono via scipy.signal.resample_poly (the live runtime pattern). Test 3: does NOT block on or replace the sounddevice callback path — running fill_from_wav inside a unit test with no audio device available must succeed.
    - f1.compute_f1(predicted: list[dict], ground_truth: list[dict], tolerance_s: float = 2.0) -> dict: Test 4: perfect match returns {f1: 1.0, precision: 1.0, recall: 1.0}. Test 5: a predicted event at t=10.5 matches a ground-truth event at t=12.0 (within ±2s) — counts as TP. Test 6: events with different `type` fields never match even within tolerance. Test 7: returns a `per_detector` dict keyed by event type with the same shape. Test 8: returns a `per_detector_per_genre` matrix when `genre_lookup` callable is provided (sessions map detector_type x genre to a cell {tp, fp, fn, precision, recall, f1}).
    - corpus_manifest.validate_manifest(manifest_path: Path) -> dict: Test 9: rejects a manifest where hard_tek_pct > 70 with `{"valid": False, "errors": ["hard_tek_pct exceeds 70..."]}`. Test 10: rejects fewer than 3 distinct genres. Test 11: returns `{"valid": True, "manifest_hash": "<sha256-12char>"}` on a 6-session 3-genre manifest. (Manifest schema is the contract — Plan 03 produces the real eval/corpus/manifest.json. This plan only defines + tests the validator.)
  </behavior>
  <action>
    Edit src/vibemix/audio/buffers.py: add `fill_from_wav(self, path)` instance method on AudioBuffer. Use `wave` (stdlib) to open the file, `numpy.frombuffer(..., dtype=np.int16)` to read samples, downmix stereo→mono via `samples.reshape(-1, 2).mean(axis=1).astype(np.int16)` only when nchannels==2, resample to 16kHz via `scipy.signal.resample_poly` ONLY when the source rate ≠ 16000. Acquire the existing internal threading.Lock that protects the ring (same pattern as `push`), then bulk-write the entire decoded sample array into the ring (truncate to ring capacity if needed). Add a module-level docstring sentence noting "EVAL-01 helper — offline replay only; not called from the sounddevice callback." This is additive; do not change any existing AudioBuffer signature.

    Create scripts/eval/__init__.py (empty marker), scripts/eval/f1.py implementing compute_f1 with the tolerance-window matching algorithm (greedy nearest-neighbor pairing per detector type — see RESEARCH §F1 math; pure stdlib + numpy only, NO scikit-learn), and scripts/eval/corpus_manifest.py implementing validate_manifest (reads JSON, asserts schema fields {sessions: list, hard_tek_pct: float, genre_distribution: dict, sessions_min: 6}; computes SHA-256 of canonical-sorted JSON for the manifest_hash; returns the structured dict above).

    Create tests/eval/__init__.py (empty marker) and tests/eval/conftest.py with two fixtures: `synthetic_session(tmp_path)` (builds events.jsonl + a 5s 16kHz mono WAV via `wave` + `numpy` to tmp_path; returns Path), and `cassettes_dir` (placeholder for Plan 02 — points at tests/eval/cassettes/ which is created on demand). Test files implement the 11 tests behavior block above using pytest. Use `numpy` for sine generation: `np.int16(np.sin(2*np.pi*440*t) * 0.3 * 32767)` over `t = np.arange(0, 5, 1/16000)`. Per autonomous mode + decision "Test discipline" in 27-CONTEXT: NO new test runners, NO pytest-asyncio (use `asyncio.run()` in test body when needed); pytest + pytest-mock already in dev deps.

    Add to pyproject.toml `[dependency-groups]` dev list: `"vcrpy>=8.0"`, `"pytest-recording>=0.13"`. Do NOT add comtypes here (gated to LATENCY-14 plan 27-07). Run `uv sync --group dev` is the executor's responsibility post-edit.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv sync --group dev &amp;&amp; uv run pytest tests/eval/test_f1_math.py -x --tb=short &amp;&amp; uv run python -c "from pathlib import Path; from vibemix.audio.buffers import AudioBuffer; import wave, numpy as np; p=Path('/tmp/_t.wav'); w=wave.open(str(p),'wb'); w.setnchannels(1); w.setsampwidth(2); w.setframerate(16000); w.writeframes((np.sin(2*np.pi*440*np.arange(0,1,1/16000))*0.3*32767).astype(np.int16).tobytes()); w.close(); b=AudioBuffer(sample_rate=16000); b.fill_from_wav(p); s=b.snapshot_features(); assert s.get('rms',0) > 0, 'fill_from_wav did not populate ring'; print('OK', s.get('rms'))"</automated>
  </verify>
  <done>fill_from_wav loads a synthetic WAV and snapshot_features returns rms > 0; f1.compute_f1 passes all 8 unit tests; corpus_manifest.validate_manifest rejects bad manifests and accepts good ones; vcrpy + pytest-recording are listed in pyproject.toml dev group; no live-runtime AudioBuffer signature changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/eval/replay_harness.py CLI + scripts/eval/scorecard.py + integration test</name>
  <files>scripts/eval/replay_harness.py, scripts/eval/scorecard.py, tests/eval/test_replay_harness.py, tests/eval/test_scorecard.py, tests/eval/fixtures/synthetic_session/events.jsonl, tests/eval/fixtures/synthetic_session/input.wav, tests/eval/fixtures/synthetic_session/responses/.gitkeep, tests/eval/fixtures/synthetic_session/genre.txt</name>
  <behavior>
    - replay_harness.main() — Test 1: invoked with `--corpus tests/eval/fixtures --judges noop --output /tmp/eval-out` runs without raising, writes /tmp/eval-out/eval_report.json (valid JSON, contains key `sessions` with len &gt;= 1), and /tmp/eval-out/scorecard.md (markdown starting with `# vibemix Eval Scorecard`).
    - Test 2: `--judges noop` substitutes a stub judge that returns `{"pro": {"verdict":"pass","substance":0.7,"f1_contribution":1.0},"flash":{"pass":true}}` per ground-truth event, so the harness exercises the full loop without any Gemini API call.
    - Test 3: `replay_one_session()` constructs REAL EvidenceRegistry + EventDetector + CitationLinter (not mocks); time-warps the state refresh tick at 1Hz across the synthetic WAV duration; returns `{session, predicted_events: list, f1: dict, verdicts: list, useful_response_ratio: float, bypass_rate: float}`.
    - Test 4: scorecard.render_scorecard(results: list[dict], thresholds: dict) -> tuple[str, dict] emits markdown with a per-detector-per-genre F1 matrix table, a "Threshold Status" block showing each threshold + actual + pass/fail, and returns the same data as JSON for eval_report.json.
    - Test 5: when `--threshold-lock <path>` is omitted, harness uses an internal default-thresholds dict matching CONTEXT EVAL-06 (f1_min=0.80, substance_min=0.65, cited_cosine_min=0.4, bypass_max=0.15, per_genre_f1_min=0.70). The actual THRESHOLD-LOCK.md frontmatter parser lives in Plan 04; this plan only consumes the dict.
    - Test 6: harness exits 1 when any session falls below threshold; exits 0 otherwise. Tested via subprocess invocation.
  </behavior>
  <action>
    Create scripts/eval/replay_harness.py implementing the CLI structure shown in 27-RESEARCH §Pattern 1 + §Code Examples "Replay harness — minimal session loop":
    - argparse: --corpus (Path, default eval/corpus/sessions), --judges (str, default "gemini-3-flash"; comma-separated; "noop" stub is recognized), --output (Path, required), --threshold-lock (Path | None, default None), --vcr-mode (str, default "none"; passed through to Plan 02 judge module via env var VCR_RECORD_MODE).
    - `async def replay_one_session(session_dir, judges)` exactly as research §Pattern 1: AudioBuffer.fill_from_wav → real EvidenceRegistry + EventDetector + CitationLinter → 1Hz manual tick over session duration → per-event judge calls (skipped/stubbed when judge name == "noop") → returns dict.
    - `def call_judges_stub(...)` for `noop` mode: returns the deterministic verdicts in behavior Test 2.
    - main(): gathers all sessions under --corpus glob `*/`, runs replay_one_session asyncio.gather, calls scorecard.render_scorecard with default-or-loaded thresholds, writes eval_report.json + scorecard.md to --output, exits 1 on any sub-threshold session per CONTEXT EVAL-06.
    - Defensive: if `--corpus` has zero session dirs (only fixtures pattern), print "no sessions found — empty corpus" and exit 0 (Plan 04 CI gate decides whether empty corpus is allowed for `[skip-eval]` PRs).

    Create scripts/eval/scorecard.py with render_scorecard(results, thresholds) -> (md: str, data: dict). Markdown layout:
    ```
    # vibemix Eval Scorecard

    **Phase:** 27 (autonomous proxy gate)
    **Sessions:** N
    **Generated:** <iso8601>

    ## Threshold Status

    | Metric | Threshold | Actual | Status |
    |--------|-----------|--------|--------|
    | min(pro_f1, flash_f1) | ≥ 0.80 | 0.83 | PASS |
    | useful_response_ratio | ≥ 0.65 | 0.71 | PASS |
    | cited_cosine | ≥ 0.4  | 0.52 | PASS |
    | bypass_rate          | ≤ 0.15 | 0.09 | PASS |

    ## Per-Detector-Per-Genre F1 Matrix
    (rows = detector type, cols = genre)

    ## Per-Session Results
    ```
    Per Pitfall P43, the per-detector-per-genre matrix MUST be present in the scorecard even when only 1 genre is in the corpus (cells for missing genres render `—`).

    Create the synthetic test fixture under tests/eval/fixtures/synthetic_session/: generate input.wav (5s of 440Hz sine at 16kHz mono via numpy + wave stdlib — same generation as Task 1 verify command); write events.jsonl with 3 entries (one TRACK_CHANGE at t=1.0, one PHRASE_BOUNDARY at t=2.5, one MIX_MOVE at t=4.0); create responses/.gitkeep (empty); create genre.txt containing "techno". Create the WAV at fixture creation time inside conftest.py if it does not exist (so a fresh clone regenerates it; do not commit large WAVs).

    Write tests/eval/test_replay_harness.py with the 6 behavior tests. Use `subprocess.run([sys.executable, "-m", "scripts.eval.replay_harness", "--corpus", str(fixtures_dir), "--judges", "noop", "--output", str(tmp_path)])` for Test 1 + Test 6 (exit code assertion). Use direct `replay_one_session()` import for Tests 2-3. Use a parametrized scorecard fixture for Test 4. tests/eval/test_scorecard.py covers render_scorecard pure-function tests independently.

    Add `tests/eval/fixtures/synthetic_session/.gitkeep`-style stubs so the fixture path resolves; the WAV regenerates via conftest.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/eval/test_replay_harness.py tests/eval/test_scorecard.py -x --tb=short &amp;&amp; uv run python -m scripts.eval.replay_harness --corpus tests/eval/fixtures --judges noop --output /tmp/p27-01-eval-out &amp;&amp; test -f /tmp/p27-01-eval-out/eval_report.json &amp;&amp; test -f /tmp/p27-01-eval-out/scorecard.md &amp;&amp; uv run python -c "import json; r=json.loads(open('/tmp/p27-01-eval-out/eval_report.json').read()); assert 'sessions' in r and len(r['sessions']) >= 1, r; print('OK', len(r['sessions']))"</automated>
  </verify>
  <done>replay_harness CLI runs end-to-end against tests/eval/fixtures/synthetic_session with --judges noop, exits 0 on synthetic happy path, writes eval_report.json + scorecard.md; integration tests pass; scorecard contains per-detector-per-genre matrix; CLI exits 1 when injected threshold violation occurs (verified in Test 6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus → harness | Untrusted WAV + events.jsonl content from public-domain sources (Plan 03 sources real corpus); harness must validate-then-parse, never `eval()` |
| harness → file system | --output path receives JSON + markdown; CLI must not write outside --output or accept `--output=/` |
| CI → harness env | GEMINI_API_KEY available in Plan 02+; this plan's `--judges noop` path MUST NOT touch the env var so smoke tests run offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Information Disclosure | Eval report json (Plan 04 commits to .planning/eval-runs/) | mitigate | Scorecard generator MUST NOT echo response text from per-event responses/<id>.txt files into eval_report.json — only metric values + verdicts (CONTEXT V7 + RESEARCH §Security Domain). Test scorecard.py output for the strings "AIza" + first 60 chars of the synthetic-session prompt. |
| T-27-01-02 | Tampering | corpus_manifest.json hash | mitigate | validate_manifest returns SHA-256 of canonical-sorted JSON; Plan 04 CI step asserts computed hash == manifest declaration; this plan provides the validator function. |
| T-27-01-03 | Tampering | scorecard.md PR comment (Plan 04 posts) | accept | Plan 04 owns the GitHub-script post; this plan only generates markdown — sanitization deferred to Plan 04 (low risk: input is internal eval results, not user input). |
| T-27-01-04 | Denial of Service | replay_harness `--corpus` walking | mitigate | Hard cap on per-session WAV size = 300 MB (defensive; corpus is 6×30min ≈ 50 MB each); harness skips sessions exceeding cap with a warning. |
| T-27-01-05 | Information Disclosure | Synthetic session WAV regenerated in tmp | accept | Sine wave only — no PII, no real audio; regeneration on every test run via conftest. |
| T-27-01-06 | Tampering | AudioBuffer.fill_from_wav inside live runtime | mitigate | Module docstring + comment "EVAL-01 helper — offline replay only"; method does NOT register with the audio callback registry; Phase 37 AUDIT-07 grep ensures __main__.py never calls fill_from_wav. |
</threat_model>

<verification>
**Phase-level eval gate readiness check (run after both tasks complete):**

```bash
cd /Users/ozai/projects/dj-set-ai

# Tests
uv run pytest tests/eval/ -x --tb=short

# CLI smoke
uv run python -m scripts.eval.replay_harness \
  --corpus tests/eval/fixtures \
  --judges noop \
  --output /tmp/p27-01-final

# Artifacts exist
test -s /tmp/p27-01-final/eval_report.json
test -s /tmp/p27-01-final/scorecard.md

# fill_from_wav is additive — live AudioBuffer signature unchanged
grep -q "def fill_from_wav" src/vibemix/audio/buffers.py
grep -c "def __init__" src/vibemix/audio/buffers.py | grep -q 1   # single __init__ — no overload break

# Dev deps wired for Plan 02
grep -q "vcrpy" pyproject.toml
grep -q "pytest-recording" pyproject.toml

# G5 POC files untouched
git diff --stat cohost.py cohost_v2.py cohost_lk.py cohost_v3.py cohost_v4.py mascot.html 2>&1 | grep -E "^\\s*0 files changed" || \
  ( echo "G5 VIOLATION: POC files modified" && exit 1 )

# Full suite green (regression guard)
uv run pytest -ra -x --tb=line
```
</verification>

<success_criteria>
1. `scripts/eval/replay_harness.py` CLI exists, accepts `--corpus`, `--judges`, `--output`, `--threshold-lock`, and exits 0 against the synthetic happy-path fixture under `--judges noop`.
2. `AudioBuffer.fill_from_wav(path)` exists, additive, loads WAV at 16kHz/48kHz mono/stereo, populates the ring, and `snapshot_features()` returns non-zero rms after the call.
3. `scripts/eval/f1.py::compute_f1` returns precision/recall/F1 with ±2s tolerance and `per_detector_per_genre` matrix.
4. `scripts/eval/scorecard.py::render_scorecard` produces markdown + JSON with threshold status + per-detector-per-genre matrix.
5. `scripts/eval/corpus_manifest.py::validate_manifest` rejects Hard Tek > 70% and < 3 genres; returns SHA-256 manifest hash.
6. `pyproject.toml` `[dependency-groups] dev` lists vcrpy>=8.0 + pytest-recording>=0.13.
7. POC files (`cohost*.py`, `mascot.html`) untouched (G5 rule).
8. `uv run pytest tests/eval/ -x` exits 0; full `uv run pytest -ra` shows zero new failures.
</success_criteria>

<output>
After completion, create `.planning/phases/27-eval-harness-v2-0-carry-forward-close-out/27-01-SUMMARY.md`.
</output>
