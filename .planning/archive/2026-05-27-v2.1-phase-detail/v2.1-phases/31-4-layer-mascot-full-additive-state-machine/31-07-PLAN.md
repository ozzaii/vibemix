# Plan 31-07 — GLB ≤ 25 MB CI gate

**REQ:** MASCOT-27
**Pitfall:** P52 (real GLBs push bundle past 350 MB hard cap)

## Goal
CI script that sums every `.glb` file under the mascot asset dirs and fails if total > 25 MB.

## Files
- NEW: `scripts/check_mascot_glb_size.sh`
- NEW: `tests/install/test_mascot_glb_size_gate.py` (Python wrapper for pytest-driven CI)

## Verification
- Script runs successfully against current asset state (no real GLBs yet → 0 bytes ≤ 25 MB).
- Test asserts total ≤ 25 MB.
