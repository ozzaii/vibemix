---
phase: 29-post-session-debrief-mvp-ui
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - tauri/src-tauri/capabilities/default.json
  - src/vibemix/runtime/recorder.py
  - src/vibemix/__main__.py
  - tests/capabilities/test_debrief_arg_allowlist.py
  - tests/runtime/test_evidence_registry_snapshot_written.py
  - tests/main/test_debrief_short_circuits_audio_init.py
  - tests/debrief/__init__.py
  - tests/debrief/conftest.py
  - tests/debrief/fixtures/sample_events.jsonl
  - tests/debrief/fixtures/sample_evidence_registry.json
  - tests/debrief/fixtures/sample_voice.wav
  - .planning/phases/29-post-session-debrief-mvp-ui/29-WAVE0-PROBES.md
autonomous: true
requirements:
  - DEBRIEF-08
  - DEBRIEF-09
tags: [debrief, capability, evidence-registry, wave0, unblock]

user_setup: []

must_haves:
  truths:
    - "Tauri capability allowlist permits sidecar arg `--debrief` AND a session-dir positional matching `^\\d{8}-\\d{6}$`"
    - "Every recorded session writes `evidence_registry.json` next to `events.jsonl` at `VoiceRecorder.close()`"
    - "`__main__.py --debrief <session_dir>` short-circuits BEFORE any audio device probe / LiveKit init / sounddevice import"
    - "Wave 0 probe report (`29-WAVE0-PROBES.md`) records verdicts for A1 (gemini-3-pro model id), A3 (PyAV libmp3lame), A5 (Achird 60-90s quality), A7 (proxy responseSchema passthrough)"
  artifacts:
    - path: "tauri/src-tauri/capabilities/default.json"
      provides: "Sidecar args validator regex extended for `--debrief` + session-dir positional + `windows` array includes `debrief` label"
      contains: "debrief"
    - path: "src/vibemix/runtime/recorder.py"
      provides: "`VoiceRecorder.close()` serializes EvidenceRegistry snapshot to `<session_dir>/evidence_registry.json` (idempotent, atomic)"
      contains: "evidence_registry.json"
    - path: "src/vibemix/__main__.py"
      provides: "`--debrief` dispatch lifted ABOVE audio init in `cli_entry()` / `main()` so debrief mode never touches sounddevice/LiveKit"
      contains: "debrief"
    - path: "tests/capabilities/test_debrief_arg_allowlist.py"
      provides: "Unit test that loads capabilities/default.json, regex-matches `--debrief` literal + sample `20260515-112139` session-dir"
    - path: "tests/runtime/test_evidence_registry_snapshot_written.py"
      provides: "Test that VoiceRecorder.close() produces a non-empty `evidence_registry.json` with at least one event_id key"
    - path: "tests/main/test_debrief_short_circuits_audio_init.py"
      provides: "Test that `python -m vibemix --debrief <fixture_session_dir>` runs without invoking sounddevice / LiveKit imports (monkeypatch import asserts)"
    - path: "tests/debrief/conftest.py"
      provides: "Shared pytest fixtures: `sample_session_dir`, `sample_events_jsonl`, `sample_evidence_registry_json`, `sample_voice_wav` for all Phase 29 tests"
    - path: ".planning/phases/29-post-session-debrief-mvp-ui/29-WAVE0-PROBES.md"
      provides: "Verdict log for assumptions A1, A3, A5, A7 with command output, dates, and pass/fail"
  key_links:
    - from: "tauri/src-tauri/capabilities/default.json"
      to: "shell:allow-execute -> validator regex"
      via: "JSON `permissions[].allow[].args[].validator` field"
      pattern: "\\^--\\(wizard\\|session\\|debrief\\)\\$"
    - from: "src/vibemix/runtime/recorder.py"
      to: "<session_dir>/evidence_registry.json"
      via: "VoiceRecorder.close() -> serialize EvidenceRegistry.snapshot()"
      pattern: "evidence_registry\\.json"
    - from: "src/vibemix/__main__.py"
      to: "args.debrief is not None -> _run_debrief_sidecar()"
      via: "early dispatch in cli_entry() / main()"
      pattern: "args\\.debrief"
---

<objective>
Wave 0 unblock — fix the four pre-requisites that block every other Phase 29 plan: capability allowlist for `--debrief`, EvidenceRegistry per-session persistence, `__main__.py` short-circuit, and Wave 0 assumption probes (A1/A3/A5/A7). Without this plan landing first, Plans 01-08 will fail at runtime.

Purpose: De-risk all four CRITICAL gaps surfaced by 29-RESEARCH.md before any backend or frontend work begins. Capability rejection at runtime is silent failure; missing `evidence_registry.json` breaks citation tooltips; audio init in --debrief mode wastes resources and crashes on machines without BlackHole.

Output: Capability JSON updated + recorder writes snapshot + `__main__.py` ordered correctly + 8 pytest fixture files + Wave 0 probe report committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-CONTEXT.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-RESEARCH.md

@tauri/src-tauri/capabilities/default.json
@src/vibemix/runtime/recorder.py
@src/vibemix/__main__.py
@src/vibemix/state/evidence_registry.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from shipped code. -->

From src/vibemix/state/evidence_registry.py:
- `EVIDENCE_CITATION_RE` — regex (locked Phase 18) matching `[(ev|track|mix|aud|midi|screen|tend):<id>(@<t>)?]`
- `EvidenceRegistry.snapshot() -> dict` (or equivalent serializer — confirm at execute time; if missing, add a method that returns `{event_id: {evidence_text, timestamp, source}}`)
- `EvidenceRegistry.clear()` — currently called from `VoiceRecorder.close()`; serialization MUST happen BEFORE clear

From src/vibemix/runtime/recorder.py:
- `VoiceRecorder.close()` — writes `events.jsonl` flush + closes `voice.wav` / `input.wav`. Add: serialize EvidenceRegistry snapshot to `<session_dir>/evidence_registry.json` BEFORE clear.

From src/vibemix/__main__.py:
- `cli_entry()` — argparse setup, then dispatches by mode
- `_run_debrief_sidecar(args)` — currently a banner-only no-op (lines ~199-247 per research)
- The short-circuit MUST happen BEFORE `sounddevice` / `livekit` / `mido` import path executes

From tauri/src-tauri/capabilities/default.json:
- `windows: ["main", "mascot", "overlay-*"]` — add `"debrief"` to allowlist
- `shell:allow-execute` validator: `"^--(wizard|session)$"` → `"^--(wizard|session|debrief)$"`
- ADD second args entry for the session-dir positional: `{ "validator": "^\\\\d{8}-\\\\d{6}$" }` (Tauri allows multiple args entries; second matches the positional)

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capability allowlist regex + windows list extension</name>
  <files>tauri/src-tauri/capabilities/default.json, tests/capabilities/test_debrief_arg_allowlist.py, tests/capabilities/__init__.py</files>
  <behavior>
    - Test 1: Loading `capabilities/default.json` and regex-matching `--debrief` against the validator returns True.
    - Test 2: Regex matches the literal `20260515-112139` (canonical session-dir form) against the second args entry.
    - Test 3: `--malicious` arg does NOT match (negative case — allowlist still narrow).
    - Test 4: `windows` array contains `"debrief"` literal.
    - Test 5: `--wizard` and `--session` still match (no regression for v2.0 callers).
  </behavior>
  <action>
    Write `tests/capabilities/test_debrief_arg_allowlist.py` first (RED). Tests load the JSON, locate the `shell:allow-execute` permission for `binaries/vibemix-core`, extract the `args` array, and assert per the behavior block. Then update `capabilities/default.json`:
    1. Replace validator `"^--(wizard|session)$"` with `"^--(wizard|session|debrief)$"`.
    2. Append a SECOND args entry with `{ "validator": "^\\\\d{8}-\\\\d{6}$" }` so the session-dir positional passes (Tauri matches sequential args, each against its own validator).
    3. Extend `windows: ["main", "mascot", "overlay-*"]` → `["main", "mascot", "overlay-*", "debrief"]` so the new WebviewWindow label is in scope for capability inheritance.
    4. Update the long doc-comment `description` field to add a "Phase 29-00:" entry briefly describing the change.
    Tests then pass (GREEN).
  </action>
  <verify>
    <automated>pytest tests/capabilities/test_debrief_arg_allowlist.py -x</automated>
  </verify>
  <done>capabilities/default.json updated, all 5 tests pass, no v2.0 regression (wizard/session still match).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EvidenceRegistry per-session snapshot + __main__ debrief short-circuit + Wave 0 probes</name>
  <files>src/vibemix/runtime/recorder.py, src/vibemix/__main__.py, src/vibemix/state/evidence_registry.py, tests/runtime/test_evidence_registry_snapshot_written.py, tests/main/test_debrief_short_circuits_audio_init.py, tests/debrief/__init__.py, tests/debrief/conftest.py, tests/debrief/fixtures/sample_events.jsonl, tests/debrief/fixtures/sample_evidence_registry.json, tests/debrief/fixtures/sample_voice.wav, .planning/phases/29-post-session-debrief-mvp-ui/29-WAVE0-PROBES.md</files>
  <behavior>
    - Test 1 (`test_evidence_registry_snapshot_written.py`): Run a synthetic VoiceRecorder lifecycle (open + push 3 events with citations + close); assert `<session_dir>/evidence_registry.json` exists, parses as JSON, contains at least 3 event_id keys with `evidence_text` + `timestamp` fields.
    - Test 2 (snapshot is idempotent): Calling close() twice does not crash and overwrites cleanly.
    - Test 3 (snapshot is atomic): Mid-write process kill leaves either old file or new file, never partial (write to temp + rename).
    - Test 4 (`test_debrief_short_circuits_audio_init.py`): Monkeypatch `sounddevice` and `livekit` modules to raise on import; run `cli_entry(["--debrief", str(fixture_session_dir)])`; assert no ImportError raised (because debrief mode never imports them).
  </behavior>
  <action>
    PART A — EvidenceRegistry snapshot:
    1. Audit `src/vibemix/state/evidence_registry.py` for an existing `snapshot()` / `to_dict()` method. If absent, add `def snapshot(self) -> dict[str, dict[str, Any]]` returning `{event_id: {evidence_text, timestamp, source}}` for every registered event.
    2. In `src/vibemix/runtime/recorder.py`, locate `VoiceRecorder.close()`. BEFORE the existing `EvidenceRegistry.clear()` call (or before recorder teardown), add:
       ```
       snapshot = self._evidence_registry.snapshot()
       tmp_path = self._session_dir / "evidence_registry.json.tmp"
       tmp_path.write_text(json.dumps(snapshot, indent=2, sort_keys=True))
       tmp_path.rename(self._session_dir / "evidence_registry.json")
       ```
       (Pseudocode — adapt to actual instance attrs.) Atomic via tmp+rename pattern.
    3. If `_evidence_registry` is not held on VoiceRecorder, accept it as a constructor arg (kwarg-only, default to module singleton) — preserves backward compat.

    PART B — __main__ debrief short-circuit:
    1. Trace current `src/vibemix/__main__.py:main()` / `cli_entry()` to find the FIRST `import sounddevice` or `from livekit import` or audio device probe.
    2. Move the `if args.debrief is not None:` dispatch BEFORE that point. The current `_run_debrief_sidecar(args)` body stays as-is for now (Plan 02 fills it); just ensure the early-exit happens before any heavy import.
    3. If imports are top-of-module (not inside functions), wrap the heavy ones in deferred-import helper functions called only by non-debrief code paths.
    4. Add docstring comment: `# Phase 29-00: --debrief MUST short-circuit before audio init (Pitfall: sounddevice on machines without BlackHole crashes)`.

    PART C — Test fixtures:
    1. Create `tests/debrief/conftest.py` with pytest fixtures `sample_session_dir`, `sample_events_jsonl`, `sample_evidence_registry_json`, `sample_voice_wav`. Source data: copy `recordings/20260515-112139/events.jsonl` (real session), generate a tiny 5-second silent `voice.wav` via numpy + scipy.io.wavfile, and synthesize `evidence_registry.json` from the events.jsonl event_ids.
    2. Files committed under `tests/debrief/fixtures/` so all subsequent Phase 29 plans use them.

    PART D — Wave 0 probes:
    1. Create `.planning/phases/29-post-session-debrief-mvp-ui/29-WAVE0-PROBES.md` with sections:
       - **A1: Gemini 3 Pro model ID** — run `python -c "from google import genai; c = genai.Client(); r = c.models.generate_content(model='gemini-3-pro', contents='hi'); print('OK')"`. Record verdict (correct id / rename to gemini-3-pro-001 / etc).
       - **A3: PyAV libmp3lame** — run `python -c "import av; print('libmp3lame' in [c.name for c in av.codec.codecs_available])"`. Record True/False; if False, document the system ffmpeg subprocess fallback path Plan 01 will use.
       - **A5: Achird 60-90s quality** — generate a 60-second sample TTS clip via `scripts/generate_ack_audio.py` template (extend with longer text). Listen, record verdict (acceptable / monotone / fallback to Kore needed).
       - **A7: Proxy responseSchema passthrough** — POST a tiny structured-output request through `api.altidus.world/vibemix/gemini` (proxy endpoint per STATE.md) with a Pydantic responseSchema. If proxy strips the field, document `mode=direct` fallback for debrief.
    2. Each section ends with **Verdict:** `PASS` / `FAIL` / `FALLBACK NEEDED` and a one-line action for downstream plans.
    3. If A1, A5, or A7 returns FALLBACK, defer to KAAN-ACTION-PROXY.md (autonomous mode = continue with documented fallback, not pause).
  </action>
  <verify>
    <automated>pytest tests/runtime/test_evidence_registry_snapshot_written.py tests/main/test_debrief_short_circuits_audio_init.py tests/debrief/conftest.py -x</automated>
  </verify>
  <done>EvidenceRegistry snapshot written atomically per session, __main__.py short-circuits debrief before audio init, conftest.py + fixtures committed, 29-WAVE0-PROBES.md committed with verdicts for A1/A3/A5/A7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→Tauri command | `open_debrief_window` receives `session_dir` from JS; Phase 29-04 owns canonicalization. This plan only sets the capability allowlist. |
| capability JSON→sidecar args | Validator regex constrains what args the renderer-driven Tauri command can pass. This is the load-bearing security gate. |
| VoiceRecorder→filesystem | EvidenceRegistry snapshot writes to `<session_dir>/evidence_registry.json`. session_dir already validated by VoiceRecorder construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-00-01 | Tampering | sidecar args via capability bypass | mitigate | Validator regex `^--(wizard\|session\|debrief)$` is exhaustive — any other flag is rejected by Tauri runtime. Test asserts `--malicious` does NOT match. |
| T-29-00-02 | Elevation of Privilege | session-dir positional could be `../../../etc/passwd` | mitigate | Second args validator `^\\d{8}-\\d{6}$` rejects path-traversal forms; only timestamp-style session dirs pass. Plan 04 adds Rust-side `validate_under_root` defense-in-depth. |
| T-29-00-03 | Information Disclosure | EvidenceRegistry snapshot contains citation text from session | accept | Citation text is locally-derived from the user's own session; written under user-private `recordings/<session>/`. No new disclosure surface. |
| T-29-00-04 | Denial of Service | Atomic snapshot write fails mid-flight | mitigate | tmp+rename pattern — partial write never readable as `evidence_registry.json`. Test 3 asserts. |
| T-29-00-05 | Tampering | Renderer escapes capability and spawns arbitrary args | mitigate | Capability JSON is the single source of truth; Tauri runtime enforces validator on every spawn. Out of scope: developer error mutating JSON — Phase 34 SEC-09 capability lint catches drift. |
| T-29-00-06 | Spoofing | Fake `--debrief` from another process | accept | `binaries/vibemix-core` only spawned by Tauri parent; fork from outside doesn't go through capability gate (it's a Tauri-runtime construct, not a kernel one). Local-only app, single-user, low-value target. |
</threat_model>

<verification>
- `pytest tests/capabilities tests/runtime/test_evidence_registry_snapshot_written.py tests/main/test_debrief_short_circuits_audio_init.py -x` — full Wave 0 unit gate.
- Manual smoke: run `python -m vibemix --debrief recordings/20260515-112139/` on a machine without BlackHole connected; assert no sounddevice ImportError, sidecar exits cleanly (banner-only body still present, will be filled in Plan 02).
- `cat .planning/phases/29-post-session-debrief-mvp-ui/29-WAVE0-PROBES.md` — verdicts present for A1/A3/A5/A7.
</verification>

<success_criteria>
1. `pytest tests/capabilities/test_debrief_arg_allowlist.py tests/runtime/test_evidence_registry_snapshot_written.py tests/main/test_debrief_short_circuits_audio_init.py -x` exits 0.
2. Running `python -m vibemix --debrief <real-session-dir>` does NOT trigger sounddevice / livekit imports (verified via monkeypatch test).
3. After running a real DJ session, `<session_dir>/evidence_registry.json` exists and parses as a non-empty JSON dict.
4. `29-WAVE0-PROBES.md` records a clear verdict for each of A1/A3/A5/A7 with command output and PASS/FAIL/FALLBACK marker.
5. `tauri/src-tauri/capabilities/default.json` validator regex includes `debrief` and `windows` array includes `"debrief"`.
6. No regressions: `pytest -x` full suite stays green vs prior baseline (1961+).
</success_criteria>

<output>
After completion, create `.planning/phases/29-post-session-debrief-mvp-ui/29-00-SUMMARY.md`
</output>
