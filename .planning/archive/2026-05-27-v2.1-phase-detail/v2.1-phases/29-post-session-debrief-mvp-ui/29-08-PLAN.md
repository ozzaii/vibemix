---
phase: 29-post-session-debrief-mvp-ui
plan: 08
type: execute
wave: 6
depends_on: ["29-00", "29-01", "29-02", "29-03", "29-04", "29-05", "29-06", "29-07"]
files_modified:
  - tests/e2e/test_debrief_e2e_open_close_cycle.py
  - tests/e2e/test_debrief_e2e_cache_hit.py
  - tests/e2e/test_debrief_e2e_short_session_disabled.py
  - tests/e2e/test_debrief_e2e_macos_smoke.md
  - tests/e2e/test_debrief_e2e_windows_smoke.md
  - .planning/phases/29-post-session-debrief-mvp-ui/29-CROSS-PLATFORM-VERDICT.md
  - .planning/phases/29-post-session-debrief-mvp-ui/screenshots/macos-debrief-window.png
  - .planning/phases/29-post-session-debrief-mvp-ui/screenshots/windows-debrief-window.png
autonomous: false
requirements:
  - DEBRIEF-03
  - DEBRIEF-04
  - DEBRIEF-05
  - DEBRIEF-06
  - DEBRIEF-07
  - DEBRIEF-08
  - DEBRIEF-09
  - DEBRIEF-10
  - DEBRIEF-11
tags: [debrief, e2e, cross-platform, verification, smoke]

user_setup: []

must_haves:
  truths:
    - "End-to-end open-close cycle test passes: real Tauri app launched headlessly, Open Debrief clicked, window appears, all 4 IPC frames received, click region → tooltip, close window → no orphan PID"
    - "Cache hit test passes: re-open same session returns within 1s (no Gemini calls — verified by VCR cassette empty)"
    - "Short session disabled test: open Settings → Recordings, locate a < 5min session, assert button.disabled === true and tooltip text matches"
    - "macOS WKWebView smoke verdict captured (screenshot + observations): MP3 plays, waveform renders, regions clickable, fonts rendered"
    - "Windows WebView2 smoke verdict captured (same checklist): MP3 plays, waveform parity with Mac, regions clickable"
    - "29-CROSS-PLATFORM-VERDICT.md committed with PASS/FAIL per Pitfall 5 (WaveSurfer rendering disparity) and final phase release decision"
  artifacts:
    - path: "tests/e2e/test_debrief_e2e_open_close_cycle.py"
      provides: "Full lifecycle e2e: Tauri spawn → click → spawn debrief sidecar → window appears → IPC frames → close → cleanup"
      min_lines: 60
    - path: "tests/e2e/test_debrief_e2e_cache_hit.py"
      provides: "Cache hit verification: second open returns < 1s, no Gemini calls"
    - path: "tests/e2e/test_debrief_e2e_short_session_disabled.py"
      provides: "DEBRIEF-11 disable gate verified end-to-end"
    - path: ".planning/phases/29-post-session-debrief-mvp-ui/29-CROSS-PLATFORM-VERDICT.md"
      provides: "Manual smoke verdict for Mac+Win with screenshots; closes Pitfall 5"
  key_links:
    - from: "tests/e2e/test_debrief_e2e_open_close_cycle.py"
      to: "Tauri Rust shell + Plan 02 sidecar + Plan 05 UI"
      via: "subprocess + websocket + DOM scraping"
      pattern: "open_debrief_window"
---

<objective>
End-to-end verification phase. Real Tauri launch + real sidecar + real WebviewWindow + real IPC traffic + real WaveSurfer render — for both macOS WKWebView and Windows WebView2. Closes every Phase 29 REQ-ID with cross-platform evidence. The hard gate before `/gsd-verify-work`.

Purpose: Phase 29's two non-negotiable pitfalls (P81 MP3 parity + P82 schema lock) only fully resolve when verified on BOTH webview engines. This plan delivers the cross-platform smoke evidence + automated e2e tests for the open-close cycle that captures every REQ-ID's behavior in one test run.

Output: 3 pytest e2e files + 2 smoke checklist md files + 2 screenshots + 1 cross-platform verdict document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-CONTEXT.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-RESEARCH.md

<!-- All prior plan summaries -->
@.planning/phases/29-post-session-debrief-mvp-ui/29-00-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-01-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-02-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-03-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-04-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-05-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-06-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-07-SUMMARY.md

<interfaces>
<!-- E2E driver pattern (existing tests/e2e/ scaffolding from prior phases) -->
Look at `tests/e2e/test_seam_*.py` if it exists (v2.1 Phase 37 plan), or use the v2.0 pattern (recall main.rs + subprocess + websocket test pattern).

Approach for headless e2e:
- `subprocess.Popen(["cd tauri && npm run tauri build --debug && ./target/debug/vibemix"])` to launch app.
- Use the existing WS test harness from Phase 28 to connect to 8765 (main) for Settings IPC.
- For debrief: spawn the sidecar directly via `subprocess.Popen([sys.executable, "-m", "vibemix", "--debrief", session_dir])` and connect to 8766 to verify IPC contracts.
- Skip the actual Tauri WebviewWindow render in CI (requires display); manual smoke handles that on developer machine.

Manual smoke runs locally on Mac + Windows VM.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Automated headless e2e — sidecar lifecycle + IPC contract + cache hit + disabled gate</name>
  <files>tests/e2e/__init__.py, tests/e2e/test_debrief_e2e_open_close_cycle.py, tests/e2e/test_debrief_e2e_cache_hit.py, tests/e2e/test_debrief_e2e_short_session_disabled.py</files>
  <behavior>
    test_debrief_e2e_open_close_cycle.py:
    - Test 1: Spawn `python -m vibemix --debrief <fixture_session_dir>` as subprocess. Connect ws to 127.0.0.1:8766. Receive frames in order: session-loaded → chapter-list → drills → tldr-audio. Send citation-tooltip-request → receive citation-tooltip. Close ws + send SIGTERM to subprocess. Assert subprocess exits within 2s. Assert no leftover `debrief_tldr.mp3.tmp` files in session_dir.
    - Test 2: Concurrent spawn — two sidecars on 8766. Second one exits with `port_in_use` error (verified via stderr capture).

    test_debrief_e2e_cache_hit.py:
    - Test 3: Pre-create session_debrief.json + debrief_tldr.mp3 with valid sha256 in fixture session_dir. Spawn sidecar. Connect ws. Assert all 4 IPC frames arrive within 1 second total (cache hit path — no Gemini wall time). VCR cassette must remain unaccessed.

    test_debrief_e2e_short_session_disabled.py:
    - Test 4: Generate a fixture session with `duration_s=120` events.jsonl. Spawn sidecar. Assert it emits `ipc.debrief.error { reason: "session_too_short" }` within 500ms then exits cleanly (code 0).
    - Test 5: Same with missing events.jsonl → `ipc.debrief.error { reason: "events_missing" }`.
  </behavior>
  <action>
    Write tests first (RED) using `pytest-asyncio` + `websockets` for the WS round-trips. Use the existing test fixtures from Plan 00 (`tests/debrief/conftest.py`).

    Test scaffolding:
    1. Async fixture: `async def debrief_sidecar(session_dir)` — yields a Popen + ws connection; cleanup kills subprocess.
    2. Helper `async def receive_kinds(ws, expected: list[str], timeout: float = 60.0)` — collects frames by kind, returns in arrival order.
    3. Mark heavy tests `@pytest.mark.e2e` so they can be selectively skipped in fast CI lanes.

    Implementation per test:
    - Test 1: Use fixtures sample_session_dir + sample_events_jsonl + sample_evidence_registry_json. Spawn subprocess. asyncio.wait_for(receive_kinds(ws, ["session-loaded", "chapter-list", "drills", "tldr-audio"]), timeout=120). Sleep 200ms. Send {kind: "citation-tooltip-request", event_id: <known id>}. Assert reply. Close ws. SIGTERM subprocess. Wait + assert exit.
    - Test 2: spawn first sidecar, wait for binding, spawn second; capture second's stderr; assert contains "port_in_use".
    - Test 3: pre-write fixture cache files (use Plan 01 persistence.write_debrief). Spawn. asyncio.wait_for(receive_kinds(ws, all 4 kinds), timeout=1.0). VCR cassette in offline mode — if Gemini called, cassette raises.
    - Tests 4-5: Use fixtures with intentionally too-short events.jsonl and absent events.jsonl. Spawn. Assert error frame received within 500ms.
  </action>
  <verify>
    <automated>pytest tests/e2e/test_debrief_e2e_open_close_cycle.py tests/e2e/test_debrief_e2e_cache_hit.py tests/e2e/test_debrief_e2e_short_session_disabled.py -x --timeout=180</automated>
  </verify>
  <done>5 e2e test assertions pass, sidecar lifecycle proven, cache hit < 1s, error frames surface for both edge cases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cross-platform manual smoke (Mac + Windows VM) + verdict document</name>
  <files>tests/e2e/test_debrief_e2e_macos_smoke.md, tests/e2e/test_debrief_e2e_windows_smoke.md, .planning/phases/29-post-session-debrief-mvp-ui/29-CROSS-PLATFORM-VERDICT.md, .planning/phases/29-post-session-debrief-mvp-ui/screenshots/macos-debrief-window.png, .planning/phases/29-post-session-debrief-mvp-ui/screenshots/windows-debrief-window.png</files>
  <what-built>
    Claude wrote two smoke-test checklist documents (`tests/e2e/test_debrief_e2e_macos_smoke.md` + `test_debrief_e2e_windows_smoke.md`) and a verdict template (`29-CROSS-PLATFORM-VERDICT.md`). Kaan runs each checklist on his Mac (already running) and on a Windows 11 VM (Parallels / UTM / equivalent), captures one screenshot per platform, fills in the verdict.

    Checklist contents (per platform):
    1. Build app: `cd tauri && npm run tauri build --debug`
    2. Launch app: `./target/debug/vibemix` (Mac) / `target\debug\vibemix.exe` (Win).
    3. Navigate Settings → Recordings.
    4. Locate a real session (recordings/20260515-112139 or equivalent — must be > 5 min, > 5 events).
    5. Hover row → confirm 5 buttons visible. Confirm Open Debrief button visible.
    6. Click Open Debrief.
    7. Verify second window opens, label "Debrief — 20260515-112139", 1280×720, standard chrome.
    8. Verify chapter list renders in left sidebar.
    9. Verify WaveSurfer waveform renders for voice.wav (no blank canvas).
    10. Verify TLDR audio plays when clicked (no silent fail).
    11. Verify clicking a region → cursor moves AND citation tooltip appears with evidence text.
    12. Verify 3 drills render with SBI/STAR-AR fields + citation chip.
    13. Take screenshot (full window).
    14. Close window.
    15. Verify no orphan vibemix-core process via Activity Monitor / Task Manager.
    16. Re-open same session → confirm < 1s load (cache hit).
    17. Verify on a short (< 5min) session → button disabled with tooltip.

    Verdict doc captures: PASS/FAIL per step + visual rendering parity verdict (Pitfall 5) + final Phase 29 release verdict.
  </what-built>
  <action>Claude pre-builds Task 1 e2e tests (already complete). For Task 2, Claude writes the two smoke checklist markdown files and the verdict template stub. Kaan executes the manual checklists on both platforms and fills in PASS/FAIL + verdict. No automated action — checkpoint pauses on human verification per the build above.</action>
  <how-to-verify>
    1. Run the macOS checklist on Kaan's primary Mac (steps 1-17 above).
    2. Save screenshot at `.planning/phases/29-post-session-debrief-mvp-ui/screenshots/macos-debrief-window.png`.
    3. Fill in `tests/e2e/test_debrief_e2e_macos_smoke.md` with PASS/FAIL per step + notes.
    4. Boot Windows VM. Build vibemix.exe via `cargo build` or copy CI artifact. Run the same checklist.
    5. Save screenshot at `.planning/phases/29-post-session-debrief-mvp-ui/screenshots/windows-debrief-window.png`.
    6. Fill in `tests/e2e/test_debrief_e2e_windows_smoke.md`.
    7. Fill in `29-CROSS-PLATFORM-VERDICT.md` with:
       - Per-platform PASS/FAIL summary
       - Pitfall 5 verdict (WaveSurfer rendering parity): MATCH / DRIFT / BROKEN
       - Pitfall 1 verdict (MP3 plays on both): YES / NO
       - Final Phase 29 release verdict: SHIP / BLOCK / REWORK
       - If REWORK, enumerate gaps in plain English so a gap-closure plan can be generated.
    8. Comment to resume: type "approved" if all PASS, OR paste gap list for `/gsd-plan-phase 29 --gaps`.
  </how-to-verify>
  <resume-signal>Type "approved" if both platforms PASS and no gaps; OR paste a gap list (one bullet per failed step) to trigger gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e subprocess → ws_server | Tests connect to local sidecar over WS; same trust model as production. |
| Manual smoke → user environment | Tests run on Kaan's actual machine (Mac) + a clean Windows VM; environment-specific quirks surface here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08-01 | Tampering | Test fixtures could leak into production builds | mitigate | All fixtures live under tests/ — not bundled by PyInstaller spec. Phase 21 build config already excludes tests/. |
| T-29-08-02 | Denial of Service | E2e tests slow CI to crawl | mitigate | Mark heavy e2e tests `@pytest.mark.e2e`; default CI lane skips; nightly lane runs. |
| T-29-08-03 | Information Disclosure | Smoke screenshots may contain user data | accept | Use fixture session for screenshots, not Kaan's real personal sessions. Document in checklist. |
| T-29-08-04 | Repudiation | Manual smoke verdict could drift from reality | mitigate | Verdict doc includes timestamps + git SHA + screenshots; auditable per Phase 37. |
| T-29-08-05 | Spoofing | Verdict could be fudged | accept | Single-user solo-dev workflow; trust model assumes Kaan acts in good faith on his own product. |
</threat_model>

<verification>
- `pytest tests/e2e/ -x --timeout=180` — automated headless e2e green.
- `cat .planning/phases/29-post-session-debrief-mvp-ui/29-CROSS-PLATFORM-VERDICT.md` — verdict committed with SHIP/BLOCK/REWORK.
- `ls .planning/phases/29-post-session-debrief-mvp-ui/screenshots/` — both PNGs present.
- Full Phase 29 test suite green: `pytest tests/debrief tests/e2e tests/ui_bus tests/capabilities tests/runtime tests/main -x && cd tauri/ui && npm test && cd tauri/src-tauri && cargo test`.
</verification>

<success_criteria>
1. 3 automated e2e tests pass (open-close cycle, cache hit, short-session disabled).
2. macOS smoke checklist completed with screenshot + PASS for steps 1-17.
3. Windows VM smoke checklist completed with screenshot + PASS for steps 1-17.
4. `29-CROSS-PLATFORM-VERDICT.md` records SHIP/BLOCK/REWORK final verdict.
5. Pitfall 1 (MP3 cross-webview parity) and Pitfall 5 (WaveSurfer rendering parity) both validated and documented PASS.
6. Full repo-wide test suite green (no regressions).
7. All 9 DEBRIEF REQ-IDs (DEBRIEF-03..11) traceable to passing tests OR checklist items.
8. If REWORK verdict → run `/gsd-plan-phase 29 --gaps` from the gap list.
</success_criteria>

<output>
After completion, create `.planning/phases/29-post-session-debrief-mvp-ui/29-08-SUMMARY.md` which also serves as the phase-completion summary linking to the verdict doc.
</output>
