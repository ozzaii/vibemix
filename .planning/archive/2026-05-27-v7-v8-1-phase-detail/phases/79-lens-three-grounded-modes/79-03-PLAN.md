---
phase: 79-lens-three-grounded-modes
plan: 03
type: execute
wave: 3
depends_on: ["79-01", "79-02"]
files_modified:
  - src/vibemix/runtime/settings.py
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/library/agent.py
  - src/vibemix/library/codex_curate.py
autonomous: true
requirements: [LENS-02]

must_haves:
  truths:
    - "ONE shared lens-selection source (ConfigStore.extra['lens']) is read by BOTH the live co-host (build_system_instruction path) and the curator (build_curator_instruction seam) — choose 'tutor' once → flows to both"
    - "_apply_lens validates the lens enum + persists to extra + survives reload (mirrors _apply_skill); unknown lens rejected (False, ...) — no silent fallback"
    - "the default (lens-unset) cold path is byte-identical to today on BOTH surfaces — co-host→hype, curator→tutor (per-surface default-when-unset)"
    - "no new ws port / no new IPC envelope / no messages.schema.json edit — lens rides ConfigStore.extra only"
  artifacts:
    - path: "src/vibemix/runtime/settings.py"
      provides: "_apply_lens handler + _VALID_LENSES enum + 'lens' dispatch case (mirrors _apply_skill)"
      contains: "_apply_lens"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "_resolve_prompt_cell reads the shared lens → (mode,mood) when set; cold path unchanged"
      contains: "lens"
    - path: "src/vibemix/library/agent.py"
      provides: "curator seam reads the shared lens (was hardcoded 'tutor'); cache invalidated on lens change"
      contains: "extra"
    - path: "src/vibemix/library/codex_curate.py"
      provides: "codex curator seam reads the shared lens (was hardcoded 'tutor')"
      contains: "extra"
  key_links:
    - from: "src/vibemix/runtime/settings.py::_apply_lens"
      to: "ConfigStore.extra['lens']"
      via: "validate enum → persist → save_config (mirror _apply_skill)"
      pattern: "extra\\[.lens.\\]"
    - from: "src/vibemix/agent/dj_cohost.py::_resolve_prompt_cell"
      to: "src/vibemix/prompts/matrix.py::LENS_TO_MODE_MOOD"
      via: "shared lens read → (mode,mood) → build_system_instruction"
      pattern: "LENS_TO_MODE_MOOD|build_lens_instruction"
    - from: "src/vibemix/library/agent.py::_system_instruction"
      to: "ConfigStore.extra['lens']"
      via: "shared lens read replaces hardcoded 'tutor'"
      pattern: "build_curator_instruction"
---

<objective>
Wire LENS-02: ONE shared lens-selection source of truth (`ConfigStore.extra["lens"]`, mirroring `mood`/`skill`) read by BOTH surfaces. Add `_apply_lens` to the settings bus (mirrors `_apply_skill` — validate→persist→next-build, no live ws emit, no schema bump). The co-host `_resolve_prompt_cell` reads the shared lens → `(mode,mood)` via the Plan-02 `LENS_TO_MODE_MOOD` when set; cold path (no lens) stays byte-identical. Both curator seams (`library/agent.py` ×2, `codex_curate.py` ×1) swap the hardcoded `build_curator_instruction("tutor")` for the shared read (default `"tutor"` when unset → curator cold path byte-identical), with cache invalidation on lens change.

Purpose: "Choose the lens once → it flows to both surfaces." One being, three voices, one selection. No new ws port, no new IPC envelope, no `messages.schema.json` edit (extra-only path — Planner Decision Flag #1: smallest additive diff; a UI selector is the deferred follow-up).
Output: 4 source edits. Flips the LENS-02 xfail scaffolds (test_lens.py shared-selection, settings apply_lens trio, curator-seam lens-read, dj_cohost lens cell + cold-path byte-identity) to real green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-lens-three-grounded-modes/79-RESEARCH.md
@.planning/phases/79-lens-three-grounded-modes/79-PATTERNS.md

<interfaces>
<!-- The exact existing surfaces to mirror / edit. VERIFIED this session. -->

src/vibemix/runtime/settings.py (VERIFIED):
  _VALID_MOODS: frozenset = {"hype-man","teacher","coach"}      # :49
  _VALID_SKILLS: frozenset = {"beginner","intermediate","pro"}  # :56
  # apply() dispatch ladder :171-191 — add the "lens" case alongside "skill"/"mood"/"click_through"
  async def _apply_skill(self, value) -> tuple[bool, str|None]   # :409-436 — the EXACT analog (validate enum → set env → extra["skill"] → save_config → (True,None))
  # NOTE: lens does NOT need an env export (skill sets VIBEMIX_SKILL_LEVEL); the co-host reads extra["lens"] directly.

src/vibemix/runtime/config_store.py (VERIFIED):
  class ConfigStore: extra: dict[str,Any] = field(default_factory=dict)  # :192 — untyped, survives relaunch, no schema bump
  def load_config(path=None) -> ConfigStore   # :287
  def save_config(store, path=None) -> Path    # :306

src/vibemix/agent/dj_cohost.py (VERIFIED):
  def _resolve_prompt_cell(mood: str | None = None) -> str  # :309 — skill=env, mode=env(DEFAULT_MODE), mood=arg or env; → build_system_instruction(skill,mode,mood)
  DEFAULT_SKILL_LEVEL="intermediate", DEFAULT_MODE="hype", DEFAULT_MOOD="hype-man"  # :115-117
  # called at agent build dj_cohost.py:452 with mood=live_mood (MusicState.mood override — keep)

src/vibemix/prompts/matrix.py (added by Plan 02):
  LENS_TO_MODE_MOOD: dict[str, tuple[str,str]]
  def build_lens_instruction(lens="hype", skill="intermediate", **kw) -> str

src/vibemix/library/agent.py (VERIFIED — the seam to edit):
  _SYSTEM_INSTRUCTION_CACHE / _INTERACTIVE_SYSTEM_INSTRUCTION_CACHE = None  # :98-99 module globals
  def _system_instruction() -> str   # :102 — lazy: build_curator_instruction("tutor") + _RULES_BLOCK, cached  (:109)
  def _interactive_system_instruction() -> str  # :114 — build_curator_instruction("tutor") + _INTERACTIVE_FLOW_BLOCK (:121)

src/vibemix/library/codex_curate.py (VERIFIED):
  _SYSTEM_PROMPT_CACHE = None  # :85
  def _system_prompt() -> str  # :90 — build_curator_instruction("tutor") + _RULES_BLOCK (:94); exposed via PEP 562 __getattr__
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings bus — _apply_lens handler + _VALID_LENSES + dispatch case (mirror _apply_skill)</name>
  <files>src/vibemix/runtime/settings.py</files>
  <behavior>
    - applier.apply("lens","critique") → (True, None) and ConfigStore.extra["lens"]=="critique", persisted to disk (survives reload)
    - applier.apply("lens","bogus") → (False, "<reason listing valid lenses>"); ConfigStore.extra unchanged
    - applier.apply("lens", 123) → (False, ...) (non-string rejected)
    - the valid lens enum == {"hype","critique","tutor"} (the shared charter vocabulary, same as matrix LENS_TO_MODE_MOOD)
  </behavior>
  <read_first>
    - src/vibemix/runtime/settings.py:409-436 (`_apply_skill` — copy its body shape: validate `isinstance(value,str) and value in _VALID_LENSES` → persist `extra["lens"]` → `save_config` → `(True,None)`. Lens does NOT set an env var — drop the `os.environ[...]` line skill has.)
    - src/vibemix/runtime/settings.py:49 + :56 (the `_VALID_MOODS`/`_VALID_SKILLS` frozenset declarations — add `_VALID_LENSES` beside them)
    - src/vibemix/runtime/settings.py:185-191 (the apply() dispatch if-ladder — add the `"lens"` case before the unknown-field fallthrough)
    - 79-PATTERNS.md "settings.py — _apply_lens + _VALID_LENSES" + Pitfall 3 (route lens through ONE consistent extra read; do NOT touch the orphaned VIBEMIX_MODE gap)
  </read_first>
  <action>
    Add `_VALID_LENSES: frozenset[str] = frozenset({"hype", "critique", "tutor"})` next to `_VALID_SKILLS` (settings.py:56). It MUST equal the matrix `LENS_TO_MODE_MOOD` keys (the shared vocabulary) — add a comment pointing at `vibemix.prompts.matrix.LENS_TO_MODE_MOOD` as the source of truth.

    Add `async def _apply_lens(self, value: Any) -> tuple[bool, str | None]:` mirroring `_apply_skill` body: reject `not isinstance(value, str) or value not in _VALID_LENSES` with `(False, f"lens must be one of {sorted(_VALID_LENSES)}, got {value!r}")`; on valid, `self.config_store.extra["lens"] = value` then `save_config(self.config_store)` then `(True, None)`. Docstring: lens is the canonical persona axis; it persists in `extra` like mood/skill (no schema bump), takes effect on the next agent build (co-host) / next curator process (curator). Unlike skill, lens does NOT export an env var — the co-host reads `extra["lens"]` directly at `_resolve_prompt_cell` (Pitfall 3: one consistent read, not the orphaned VIBEMIX_MODE env).

    Add the dispatch case `if field == "lens": return await self._apply_lens(value)` in the apply() ladder before the unknown-field fallthrough (settings.py:~191).

    Flip the Plan-01 `_apply_lens` trio in `tests/runtime/test_settings_apply.py` (happy/invalid/non-string) xfail→green.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/runtime/test_settings_apply.py -k "lens or skill or mood"</automated>
  </verify>
  <acceptance_criteria>
    - `_apply_lens` validates the enum + persists `extra["lens"]` + survives reload; the 3 apply_lens scaffolds flip xfail→green.
    - The existing skill/mood apply tests stay green (no regression to the dispatch ladder).
    - `_VALID_LENSES == {"hype","critique","tutor"}` (matches matrix `LENS_TO_MODE_MOOD` keys).
  </acceptance_criteria>
  <done>settings.py carries _apply_lens + _VALID_LENSES + dispatch case; apply_lens trio green; existing settings tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shared-read wiring — co-host _resolve_prompt_cell + both curator seams read ConfigStore.extra["lens"]</name>
  <files>src/vibemix/agent/dj_cohost.py, src/vibemix/library/agent.py, src/vibemix/library/codex_curate.py</files>
  <behavior>
    - With shared lens "critique" set: _resolve_prompt_cell resolves (mode,mood)=("coach","coach") → a coach-cell prompt; the curator seams build the critique (coach persona) voice
    - With NO shared lens (cold path): _resolve_prompt_cell() == build_system_instruction("intermediate","hype","hype-man") byte-for-byte (co-host default unchanged); the curator seams build the tutor voice (curator default unchanged) — both cold paths byte-identical to today
    - Setting extra["lens"]="tutor" once flows to BOTH surfaces (the shared-selection-flows-to-both proof)
    - The curator module-global instruction caches are invalidated when the lens differs from the cached build (a lens change is reflected, not stale-cached within a process)
  </behavior>
  <read_first>
    - src/vibemix/agent/dj_cohost.py:309-329 (`_resolve_prompt_cell` — the shared-lens read slots in at the same precedence point as the existing `mood` override; keep env/DEFAULT_* resolution as the cold-path fallback)
    - src/vibemix/library/agent.py:98-123 (the two lazy seams + their module-global caches `_SYSTEM_INSTRUCTION_CACHE`/`_INTERACTIVE_SYSTEM_INSTRUCTION_CACHE` — swap "tutor", add cache-invalidation-on-lens-change)
    - src/vibemix/library/codex_curate.py:85-101 (the third seam + PEP 562 `__getattr__` exposing `_SYSTEM_PROMPT` — swap "tutor")
    - src/vibemix/runtime/config_store.py:192,287 (`extra` field + `load_config` — the read source; config_store is a pure dataclass + file I/O, safe to lazy-import from library)
    - 79-PATTERNS.md "dj_cohost.py — _resolve_prompt_cell reads shared lens" + "library/agent.py + codex_curate.py — swap hardcoded tutor" (the GOTCHA: module-global cache invalidation) + Planner Decision Flag #1 (extra-only, NO schema bump) + Flag #2 (per-surface default-when-unset) + Flag #3 (cache invalidation)
  </read_first>
  <action>
    Add a single small shared-lens reader and use it on both surfaces. Per Planner Decision Flag #1: persist lens in `ConfigStore.extra["lens"]` ONLY — do NOT add `lens` to the wire snapshot, `SettingsState.make`, `messages.py`, or `messages.schema.json` (that would force `npm run codegen:ipc` + risk a stale pre-compiled ajv validator — avoid). Per Flag #2: per-surface default-when-unset (co-host→`hype`, curator→`tutor`) so both cold paths stay byte-identical; once the user sets a lens, that one value drives BOTH.

    CO-HOST (`dj_cohost.py::_resolve_prompt_cell`): read the shared lens (lazy `load_config().extra.get("lens")`, guarded — config-read failures fall through to the existing env/DEFAULT_* path so the cold path NEVER regresses). When a lens IS present, resolve `(mode, mood) = LENS_TO_MODE_MOOD[lens]` (or call `build_lens_instruction(lens, skill)`) and use those — but the existing `mood` arg override (MusicState.mood live path, dj_cohost.py:452) must still WIN when explicitly passed (keep current precedence: explicit mood arg > shared lens > env/DEFAULT). When NO lens is set, the function is byte-identical to today (Pitfall 3: route through the one `extra["lens"]` read, NOT the orphaned VIBEMIX_MODE env; do not fix the legacy mode gap).

    CURATOR (`library/agent.py` ×2 + `codex_curate.py` ×1): replace the hardcoded `build_curator_instruction("tutor")` with `build_curator_instruction(_shared_lens())` where `_shared_lens()` lazily reads `ConfigStore.extra["lens"]` and DEFAULTS to `"tutor"` when unset (per-surface default → curator cold path byte-identical). GOTCHA (Flag #3): the seams cache the built instruction in a module global. Add cache invalidation — at the START of each seam fn, read the current shared lens; if it differs from the lens the cache was built under, rebuild (store the lens-the-cache-was-built-under alongside the cache, or null the cache when the lens changes). Reset these globals between test cases (the existing seam test already imports the modules directly).

    Keep `model_router.resolve(...)` untouched (no model literal). The shared-lens reader is a tiny dep-free helper (lazy config_store import) — verify it does NOT regress `tests/memory/test_no_live_path_import.py` (config_store is NOT a forbidden surface; if any import leak appears, lazy-import inside the function like the Phase-77 seam did).

    Flip the remaining LENS-02 xfail scaffolds to green: `tests/prompts/test_lens.py::test_shared_selection_flows_to_both_builders`; `tests/library/test_curator_persona_seam.py` lens-read + default-when-unset; `tests/agent/test_dj_cohost.py::test_resolve_prompt_cell_uses_shared_lens` + `test_resolve_prompt_cell_cold_path_byte_identical`.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/prompts/test_lens.py tests/library/test_curator_persona_seam.py tests/agent/test_dj_cohost.py tests/memory/test_no_live_path_import.py</automated>
  </verify>
  <acceptance_criteria>
    - Setting `ConfigStore.extra["lens"]="critique"` flows to BOTH `_resolve_prompt_cell` (coach cell) AND the curator seams (coach persona) — `test_shared_selection_flows_to_both_builders` green.
    - Cold path (no lens): `_resolve_prompt_cell()` byte-identical to `build_system_instruction("intermediate","hype","hype-man")`; curator seams build the tutor voice — both byte-identity scaffolds green.
    - The curator cache reflects a lens change within a process (no stale-cache; Flag #3).
    - `tests/memory/test_no_live_path_import.py` stays green (no forbidden-surface import leak).
    - NO edit to `messages.py` / `messages.schema.json` / `SettingsState.make` (grep-confirm `lens` absent from those); no `npm run codegen:ipc` needed.
  </acceptance_criteria>
  <done>Both surfaces read the one shared extra["lens"]; cold paths byte-identical; cache invalidates on lens change; LENS-02 scaffolds all green; no schema/IPC edit; no-live-path boundary green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings bus (ws 127.0.0.1:8765) → _apply_lens | the `lens` field value crosses here from the UI/IPC — untrusted string. |
| persisted config → both builders | `ConfigStore.extra["lens"]` read at build time — must be re-validated by the builder's enum guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Tampering | a bad lens string arriving on the settings bus | mitigate | `_apply_lens` validates `value in _VALID_LENSES` at the trust boundary, fail-loud `(False, ...)` — no silent fallback (mirrors `_apply_skill`/`_apply_mood`). Downstream `build_lens_instruction` re-guards (defense in depth). |
| T-79-03-02 | Tampering | a corrupt persisted `extra["lens"]` poisoning the prompt | mitigate | Both builders re-validate the lens against the fixed enum; an invalid persisted lens raises ValueError at build (surfaces the corruption) rather than silently degrading the voice. |
| T-79-03-03 | Information Disclosure | adding `lens` to the wire snapshot forcing a stale pre-compiled ajv validator path | accept (avoid) | Persist `lens` in `ConfigStore.extra` ONLY; no `messages.schema.json` edit, no `codegen:ipc` — the extra-only path keeps the IPC envelope unchanged (invariant #4: one socket, zero new envelope). |
| T-79-03-SC | Tampering | npm/pip/cargo installs | mitigate | None — no package install (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/prompts/ tests/runtime/ tests/library/ tests/agent/test_dj_cohost.py` — all LENS-02 scaffolds green; v4 golden green.
- `PYTHONPATH=src python3 -m pytest -q tests/memory/test_no_live_path_import.py tests/repo/test_model_literal_gate.py` — boundary + model-literal gates green.
- `grep -rn "lens" tauri/ui/src/ipc/messages.schema.json src/vibemix/runtime/messages.py` — `lens` ABSENT (no schema/envelope edit).
- `PYTHONPATH=src python3 -m pytest -q` — full suite green: ALL Phase-79 xfails now flipped to real passes, zero remaining LENS xfails, zero new failures.
</verification>

<success_criteria>
- LENS-02 satisfied: ONE shared `ConfigStore.extra["lens"]` read by both the co-host and the curator — choose once, flows to both.
- `_apply_lens` validates + persists + survives reload; unknown lens rejected.
- Both cold paths byte-identical (co-host→hype, curator→tutor when unset).
- No new ws port, no new IPC envelope, no `messages.schema.json` edit, no model literal, no managed framework.
- The no-live-path boundary + v4 golden + model-literal gate stay green.
</success_criteria>

<output>
Create `.planning/phases/79-lens-three-grounded-modes/79-03-SUMMARY.md` when done
</output>
