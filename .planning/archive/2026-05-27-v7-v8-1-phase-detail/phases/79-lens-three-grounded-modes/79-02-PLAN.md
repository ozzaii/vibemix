---
phase: 79-lens-three-grounded-modes
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - src/vibemix/prompts/matrix.py
autonomous: true
requirements: [LENS-01]

must_haves:
  truths:
    - "hype / critique / tutor exist as three grounded prompt lenses over the SAME structured state — switching lens changes voice/intent, never the grounded facts"
    - "the default (hype) lens prompt is byte-identical to today's co-host default — the v4 golden stays green"
    - "an unknown lens fails loud with ValueError (mirrors mood/skill guards) — no silent fallback"
    - "all three lenses pass through the same lens-blind citation gate — strip decision depends only on EvidenceRegistry membership, not the lens"
  artifacts:
    - path: "src/vibemix/prompts/matrix.py"
      provides: "LENS_TO_MODE_MOOD map + build_lens_instruction wrapper over the UNTOUCHED build_system_instruction"
      contains: "def build_lens_instruction"
  key_links:
    - from: "src/vibemix/prompts/matrix.py::build_lens_instruction"
      to: "src/vibemix/prompts/matrix.py::build_system_instruction"
      via: "LENS_TO_MODE_MOOD[lens] → (mode, mood) → delegate (builder unchanged)"
      pattern: "build_system_instruction"
---

<objective>
Add the LENS-01 selector layer ABOVE the untouched `build_system_instruction`: a pure `LENS_TO_MODE_MOOD` map (mirroring the Phase-77 `_CURATOR_LENS_TO_MOOD` vocabulary, adding the co-host `mode` half) + a thin `build_lens_instruction(lens, skill)` wrapper that validates the lens then delegates. `hype`→`(hype,hype-man)` (= today's default → byte-identical by construction), `critique`→`(coach,coach)` (the charter `critique`==matrix coach alias, no rename), `tutor`→`(coach,teacher)` (the genuinely-new live teaching voice).

Purpose: Three real grounded voices over ONE structured state, NOT three brains. The lens is a SELECTOR over the existing tuned-IP 6-cell matrix + 3-mood persona dict — never a builder fork.
Output: `src/vibemix/prompts/matrix.py` gains a map + one wrapper function. Flips the LENS-01 xfail scaffolds from Plan 01 to real green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-lens-three-grounded-modes/79-RESEARCH.md
@.planning/phases/79-lens-three-grounded-modes/79-PATTERNS.md

<interfaces>
<!-- The exact existing surfaces to mirror / delegate to. VERIFIED this session. DO NOT MODIFY any of these. -->

src/vibemix/prompts/matrix.py (UNTOUCHED — the lens layer sits above):
  MOOD_PERSONAS: dict[str, str]                         # matrix.py:51 — keys: "hype-man","teacher","coach"
  _CELLS: dict[tuple[str,str], str]                      # matrix.py:685 — (skill, mode) → body
  _VALID_SKILLS = frozenset({"beginner","intermediate","pro"})   # matrix.py:694
  _VALID_MODES  = frozenset({"hype","coach"})                     # matrix.py:695
  def build_system_instruction(skill="intermediate", mode="hype", mood="hype-man", *, include_citation_grammar=True, include_listening_fallback=True, include_tag_dsl=True) -> str  # matrix.py:698 — has fail-loud skill/mode/mood guards
  _CURATOR_LENS_TO_MOOD: dict[str,str] = {"tutor":"teacher","hype":"hype-man","critique":"coach"}  # matrix.py:834 — the canonical lens VOCABULARY to mirror
  def build_curator_instruction(lens="tutor") -> str    # matrix.py:841 — copy its lower().strip()→guard→ValueError shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LENS_TO_MODE_MOOD + build_lens_instruction to matrix.py (selector over the untouched builder)</name>
  <files>src/vibemix/prompts/matrix.py</files>
  <behavior>
    - build_lens_instruction("hype", "intermediate") == build_system_instruction("intermediate", "hype") (byte-identical default — the v4 golden anchor)
    - build_lens_instruction("critique") yields a coach-cell prompt with the coach persona; build_lens_instruction("tutor") yields a coach-cell prompt with the teacher persona; build_lens_instruction("hype") yields a hype cell with hype-man — three DISTINCT prompts over the same builder
    - build_lens_instruction("bogus") raises ValueError listing the valid lenses (fail-loud, mirrors mood/skill guards)
    - LENS_TO_MODE_MOOD keys == set(_CURATOR_LENS_TO_MOOD) (shared lens vocabulary across surfaces); every value is a (mode, mood) tuple valid against _VALID_MODES + MOOD_PERSONAS
  </behavior>
  <read_first>
    - src/vibemix/prompts/matrix.py:827-895 (the `_CURATOR_LENS_TO_MOOD` map + `build_curator_instruction` — the EXACT analog: copy its lower().strip() normalize → `if lens_norm not in <map>: raise ValueError(...)` guard shape)
    - src/vibemix/prompts/matrix.py:698-766 (the `build_system_instruction` signature + its skill/mode/mood guards — the builder to DELEGATE to, NOT modify)
    - 79-PATTERNS.md "Pattern Assignments → matrix.py" (the validate-and-delegate wrapper recipe + the Critical/Pitfall-1 do-not-touch list)
  </read_first>
  <action>
    Add to `src/vibemix/prompts/matrix.py`, placed directly AFTER `build_curator_instruction` (keep the lens vocabulary co-located with the curator's). DO NOT touch `build_system_instruction`, `_CELLS`, `MOOD_PERSONAS`, `_CURATOR_LENS_TO_MOOD`, or any cell constant (Pitfall 1 — the v4 golden asserts on them).

    1. Module constant `LENS_TO_MODE_MOOD: dict[str, tuple[str, str]]` mapping `"hype"→("hype","hype-man")`, `"critique"→("coach","coach")`, `"tutor"→("coach","teacher")`. Add a comment: this mirrors `_CURATOR_LENS_TO_MOOD`'s lens vocabulary, adding the co-host `mode` half (which `_CELLS` cell to pick); the lens is a validated-enum selector, never sourced from user input (anti-injection, per T-77-02-01). Add an assertion or comment that `set(LENS_TO_MODE_MOOD) == set(_CURATOR_LENS_TO_MOOD)` so the two surfaces can never drift apart.

    2. `def build_lens_instruction(lens: str = "hype", skill: str = "intermediate", **kw) -> str:` — `from __future__ import annotations` is already at module top. Normalize `lens_norm = lens.lower().strip()`; if `lens_norm not in LENS_TO_MODE_MOOD` raise `ValueError(f"unknown lens {lens!r} — must be one of {sorted(LENS_TO_MODE_MOOD)}")` (copy `build_curator_instruction`'s guard shape verbatim in spirit). Unpack `mode, mood = LENS_TO_MODE_MOOD[lens_norm]` and `return build_system_instruction(skill, mode, mood, **kw)`. Docstring records the DEFAULT-LENS BYTE-IDENTITY contract: `build_lens_instruction("hype","intermediate")` resolves to `build_system_instruction("intermediate","hype","hype-man")`, byte-identical to today's default because the builder is unchanged. Note `critique` is the charter alias onto the matrix `coach` mode/mood (no rename), and `tutor` is the new live teaching voice (`coach` cell + `teacher` persona) — the "does tutor teach well" judgment is Phase-81 BENCH + Kaan's ear (parked, NOT this phase; do NOT build a bespoke TUTOR_* cell — Pitfall 4).

    Flip the Plan-01 LENS-01 xfail scaffolds in `tests/prompts/test_lens.py` to real passes by removing their `@pytest.mark.xfail` decorators: `test_lens_map_covers_three_canonical_lenses`, `test_lens_prompt_shape_per_lens`, `test_unknown_lens_raises_value_error`, `test_default_lens_byte_identical_to_cohost_default`, `test_three_lenses_pass_the_same_gate`. (LENS-02's `test_shared_selection_flows_to_both_builders` stays xfail — Plan 03.)
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/prompts/test_lens.py tests/prompts/test_matrix.py</automated>
  </verify>
  <acceptance_criteria>
    - `build_lens_instruction("hype","intermediate") == build_system_instruction("intermediate","hype")` (asserted in test_lens.py, now green).
    - The v4 byte-identity golden in `tests/prompts/test_matrix.py` stays fully GREEN (builder/_CELLS/MOOD_PERSONAS untouched).
    - The 5 LENS-01 scaffolds flip xfail→pass; the LENS-02 scaffold stays xfailed; no `xpassed`.
    - Model-literal gate stays green: `PYTHONPATH=src python3 -m pytest -q tests/repo/test_model_literal_gate.py` (no hardcoded model name introduced).
  </acceptance_criteria>
  <done>matrix.py carries LENS_TO_MODE_MOOD + build_lens_instruction; 5 LENS-01 scaffolds real-green; v4 golden + model-literal gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build_lens_instruction | `lens` arg crosses here — must be a validated enum, never interpolated free text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | prompt-injection — user input mutating the persona via the lens arg | mitigate | `build_lens_instruction` only KEYS the fixed `LENS_TO_MODE_MOOD` dict; an unknown lens raises ValueError (fail-loud). User input never enters the prompt text (extends the verified anti-injection invariant T-13-05-06 / T-77-02-01). |
| T-79-02-02 | Tampering | breaking the v4 byte-identity golden by editing the builder | mitigate | Lens layer is strictly additive ABOVE the untouched `build_system_instruction`; the golden asserts on the builder/_CELLS/MOOD_PERSONAS which are not touched (Pitfall 1). Verified by running `tests/prompts/test_matrix.py` green. |
| T-79-02-03 | Spoofing | a lens smuggling un-cited slop past the citation gate | mitigate | The gate is lens-blind (linter validates against EvidenceRegistry, not the prompt). `test_three_lenses_pass_the_same_gate` pins the strip decision is identical across lenses (invariant #2). |
| T-79-02-SC | Tampering | npm/pip/cargo installs | mitigate | None — no package install (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/prompts/` — test_lens.py LENS-01 scaffolds green; test_matrix.py v4 golden green.
- `PYTHONPATH=src python3 -m pytest -q tests/repo/test_model_literal_gate.py` — no model literal introduced.
- `PYTHONPATH=src python3 -m pytest -q` — full suite green (one fewer xfail than after Plan 01; the 5 LENS-01 scaffolds now pass).
</verification>

<success_criteria>
- LENS-01 satisfied: hype/critique/tutor are three grounded prompt lenses over the same builder; switching lens changes voice, not the builder/facts.
- Default-lens byte-identity holds; v4 golden untouched + green.
- Unknown lens fails loud; all three lenses provably pass the same lens-blind gate.
- Zero new package, zero model literal, zero builder/cell/persona edit.
</success_criteria>

<output>
Create `.planning/phases/79-lens-three-grounded-modes/79-02-SUMMARY.md` when done
</output>
