---
phase: 82-curate-unify-curator-co-host
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - src/vibemix/library/toolset.py
  - src/vibemix/library/agent.py
  - src/vibemix/library/codex_curate.py
autonomous: true
requirements: [CURATE-01, CURATE-02]
must_haves:
  truths:
    - "The curator's get_track_features resolves genre via the SAME genre_prototypes mechanism the co-host reads — both surfaces derive genre from ONE source"
    - "An abstaining classify yields honest-None genre (never a fabricated label)"
    - "Both curator backends (gemini agent + interactive + codex) curate 'for this DJ' by lazy-reading the shared profile/ taste layer"
    - "Cold path (no/consent-OFF profile, abstaining genre) is byte-identical to today's curator behavior"
    - "The import-boundary gate stays CLEAN — the new reads are lazy and the curator never touches MusicState"
  artifacts:
    - path: "src/vibemix/library/toolset.py"
      provides: "get_track_features genre resolved via genre_prototypes (SEAM #1), reaching gemini + codex + Telegram via the shared toolset"
      contains: "genre_prototypes"
    - path: "src/vibemix/library/agent.py"
      provides: "_taste_hint() lazy+guarded profile read appended to both _system_instruction and _interactive_system_instruction (SEAM #2)"
      contains: "_taste_hint"
    - path: "src/vibemix/library/codex_curate.py"
      provides: "_taste_hint() appended to _system_prompt (SEAM #2 twin)"
      contains: "_taste_hint"
  key_links:
    - from: "src/vibemix/library/toolset.py"
      to: "vibemix.library.genre_prototypes"
      via: "lazy-import + GenrePrototypeLookup/classify on the track's cached embedding"
      pattern: "genre_prototypes|GenrePrototypeLookup|classify"
    - from: "src/vibemix/library/agent.py"
      to: "vibemix.profile.render_profile_for_cache"
      via: "lazy+guarded _taste_hint() appended to the curator instruction"
      pattern: "render_profile_for_cache|load_profile"
    - from: "src/vibemix/library/codex_curate.py"
      to: "vibemix.profile.render_profile_for_cache"
      via: "lazy+guarded _taste_hint() appended to the codex system prompt"
      pattern: "render_profile_for_cache|load_profile"
---

<objective>
Land the two thin additive seams that close the v8.1 diamond — the curator and co-host become two facets of one mind:
- SEAM #1 (CURATE-01): `library/toolset.py.get_track_features` resolves `genre` via the SAME `genre_prototypes` mechanism the co-host reads (`refresh.py:254`) — one shared perception, both surfaces. ONE edit reaches gemini + codex/MCP + Telegram (all build the shared `LibraryToolset`).
- SEAM #2 (CURATE-02 taste half): a lazy+guarded `_taste_hint()` (mirroring the Phase-79 lens seam) reads the co-host-only `profile/` so the curator curates "for this DJ". TWO edits — `agent.py` (gemini + interactive) and `codex_curate.py` (codex) — because each backend builds its own instruction string.

Flips the Plan 01 xfail scaffolds to real-green. The persona/lens half is ALREADY shared (Phase 79) — regression-pin only, NOT re-implemented.

Purpose: Ship the concrete shared wiring (the win is the connection + the regression proving both surfaces read one source), not a grand abstraction. NO `PerceptionContract` dataclass — the existing functions ARE the contract.
Output: 3 modified `src/` files; the CURATE-01/02 scaffolds green; the cold path byte-identical; the import-boundary gate CLEAN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-curate-unify-curator-co-host/82-CONTEXT.md
@.planning/phases/82-curate-unify-curator-co-host/82-RESEARCH.md
@.planning/phases/82-curate-unify-curator-co-host/82-PATTERNS.md

<interfaces>
<!-- Live contracts extracted 2026-05-26. Use these directly — no codebase exploration needed. -->

SEAM #1 target — src/vibemix/library/toolset.py:108-125 (genre is hardcoded None at :124; self._store is the LibraryStore):
```
def get_track_features(self, args) -> dict:
    entry = self._library.lookup_by_id(track_id)
    camelot = harmonics.to_camelot(entry.key) if entry.key else None
    return {"track_id":..., "title":..., "artist":..., "bpm":..., "key": camelot,
            "duration_s":..., "genre": None}   # <-- THE SEAM
```

The ONE shared mechanism — src/vibemix/library/genre_prototypes.py (Phase 78; co-host reads it):
```
class GenrePrototypeLookup:
    def __init__(self, store=None) -> None          # store=self._store from the toolset
    def classify_playing(self, track_id: str) -> tuple[str, float]   # ("unknown", 0.0) abstain when no cached embedding
    def _cached_embedding(self, track_id) -> np.ndarray | None       # €0
def classify(emb, protos, labels, centroid, *, floor=0.25, margin=0.05) -> tuple[str, float]
def load_or_build_prototypes(store) -> tuple[np.ndarray, list[str], np.ndarray | None]
```
NOTE: NEVER re-roll centering (Pitfall 2) — the centroid contract lives inside genre_prototypes. Reuse classify_playing / classify directly.

SEAM #2 — the EXISTING lazy+guarded lens seam to MIRROR (src/vibemix/library/agent.py:128-134):
```
def _shared_lens() -> str:
    try:
        from vibemix.runtime.config_store import load_config
        from vibemix.runtime.settings import read_shared_lens
        return read_shared_lens(load_config(), default="tutor") or "tutor"
    except Exception:
        return "tutor"
```

SEAM #2 attach sites — gemini backend (agent.py):
```
def _system_instruction() -> str:           # :143-151
    ... _SYSTEM_INSTRUCTION_CACHE = build_curator_instruction(lens) + "\n" + _RULES_BLOCK
def _interactive_system_instruction() -> str:   # :159-170
    ... _INTERACTIVE_SYSTEM_INSTRUCTION_CACHE = build_curator_instruction(lens) + "\n" + _INTERACTIVE_FLOW_BLOCK
```
SEAM #2 attach site — codex backend (codex_curate.py:121-127):
```
def _system_prompt() -> str:
    ... _SYSTEM_PROMPT_CACHE = build_curator_instruction(lens) + " " + _RULES_BLOCK
```

The privacy-safe taste vehicle — src/vibemix/profile/cache_render.py:31-57 + profile/__init__.py:
```
def load_profile() -> dict | None            # None when absent/invalid/consent-OFF (default OFF)
def render_profile_for_cache(profile: dict | None) -> str   # "" when None; only 5 allowlisted fields, NO track titles
```

ALREADY SHARED — Phase 79 lens (do NOT touch): read_shared_lens + build_curator_instruction in both backends.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEAM #1 — get_track_features resolves genre via the ONE genre_prototypes mechanism (CURATE-01)</name>
  <read_first>
    - src/vibemix/library/toolset.py:108-125 (the `get_track_features` SEAM — `genre: None` at :124; `self._store` available at :68; grounding gate at :134-147 stays UNTOUCHED)
    - src/vibemix/library/genre_prototypes.py:299-335 (`GenrePrototypeLookup.__init__(store)`, `_cached_embedding`, `classify_playing` → `("unknown", 0.0)` abstain) + :145-176 (`classify` signature)
    - src/vibemix/state/refresh.py:248-258 (how the co-host calls the SAME mechanism — the reference for "one mechanism, both surfaces")
    - 82-PATTERNS.md SEAM #1 section (the pattern to copy: reuse GenrePrototypeLookup, honest-null on abstain, NEVER re-roll centering, lazy-import, grounding gate untouched)
  </read_first>
  <behavior>
    - Test (Plan 01, flips green): `get_track_features({"track_id": "t000"})["genre"]` returns the prototype-classified label (e.g. "techno") when the mechanism classifies the track — NOT None.
    - Test (Plan 01, flips green): when the mechanism abstains (`("unknown", 0.0)` — no cached embedding / degenerate table), `genre` is `None` (honest-null, same class as today's `genre: None` and the Camelot honest-null) — NEVER a fabricated label.
    - The genre is a deterministic library-side fact derived from genre_prototypes, never LLM-supplied; the seen-set grounding gate is unchanged.
  </behavior>
  <action>
    In `get_track_features` (`toolset.py`), replace the hardcoded `"genre": None` with a resolution through the ONE `genre_prototypes` mechanism. Lazy-import `genre_prototypes` INSIDE the function (Pattern 1 — keep the import-boundary discipline even though it's already in `library/`). Construct/reuse a `GenrePrototypeLookup(self._store)` and call `classify_playing(track_id)` (it reads the track's cached library embedding at €0 and abstains `("unknown", 0.0)` for unknown-to-library tracks). Map the result to the feature dict: a real label with confidence above the mechanism's own abstain → that label; `("unknown", _)` → `None` (honest-null, byte-identical class to today). Per Pitfall 2: do NOT add any `np.mean`/cosine to `toolset.py` — delegate ALL centroid/cosine math to `genre_prototypes`. Per Pitfall 3 / invariant #2: leave the seen-set grounding gate (`:134-147`) and `create_playlist` re-validation exactly as-is — genre is a deterministic fact, the model never supplies it. Consider caching a single `GenrePrototypeLookup` on the toolset instance (lazy-built on first `get_track_features` call) so repeated feature lookups in one run reuse the built prototype table; if you add an instance attr, keep it lazy (no work in `__init__`, the import-boundary stays clean). Implements CURATE-01 per D (CURATE-01). NO fenced code in this prose.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_toolset.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The two Plan-01 CURATE-01 scaffolds (`test_genre_via_prototypes_when_classified`, `test_genre_honest_none_on_abstain`) flip to real PASS (no longer xfail; no xpass).
    - The 6 existing `test_toolset.py` tests stay green (grounding gate + Camelot determinism untouched).
    - `grep -n "np.mean\|cosine" src/vibemix/library/toolset.py` returns NOTHING (centering not re-rolled — Pitfall 2).
    - `grep -n "genre_prototypes" src/vibemix/library/toolset.py` shows the lazy import INSIDE `get_track_features` (not at module top).
    - No `genai.Client`, no `GEMINI_API_KEY` on the path (genre is €0 cached-vector classify).
  </acceptance_criteria>
  <done>`get_track_features` resolves genre via `genre_prototypes` (one mechanism, both surfaces), honest-`None` on abstain; the grounding gate is untouched; the CURATE-01 scaffolds are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SEAM #2 — _taste_hint() reads the shared profile, appended to both backends' instructions (CURATE-02 taste half)</name>
  <read_first>
    - src/vibemix/library/agent.py:114-170 (`_shared_lens` lazy+guarded analog :128-134 — COPY THIS SHAPE; `_system_instruction` build :147; `_interactive_system_instruction` build :166; the `_CACHE_LOCK` + lens-keyed cache invalidation at :143-150/:159-170)
    - src/vibemix/library/codex_curate.py:95-127 (twin `_shared_lens` :107-113; `_system_prompt` build :125; `_CACHE_LOCK` + lens-keyed cache)
    - src/vibemix/profile/cache_render.py:31-57 (`render_profile_for_cache` — "" on None, only 5 allowlisted fields, NO titles) + profile/__init__.py (`load_profile` → None when consent-OFF default)
    - 82-PATTERNS.md SEAM #2 section (the NEW `_taste_hint` shape; the attach points; the cold-path identity rule; the `_interactive` second site note; Anti-pattern: never raw JSON, never `[...]` key access)
  </read_first>
  <behavior>
    - Test (Plan 01, flips green): with `load_profile()` returning a populated allowlisted profile, the built `_system_instruction()` (gemini) CONTAINS the profile-derived taste hint (a `render_profile_for_cache` field/header token).
    - Test (Plan 01, flips green): the SAME holds for `codex_curate._system_prompt()` — SEAM #2 reaches BOTH backends (the CURATE acid test).
    - Test (Plan 01 real-green, must STAY green): with `load_profile()` → None (consent-OFF), the built instruction is byte-identical to today's `build_curator_instruction(lens) + sep + RULES` (cold path additive).
    - The taste hint NEVER contains track titles / free-form (only the 5 allowlisted fields, via `render_profile_for_cache`).
  </behavior>
  <action>
    Add a `_taste_hint()` helper to BOTH `agent.py` and `codex_curate.py` (or, to avoid drift, define it once and import lazily — but per the established per-backend twin convention each module already carries its own `_shared_lens`, so a small per-module `_taste_hint` mirroring `_shared_lens` is acceptable and consistent). Shape: lazy-import `load_profile` + `render_profile_for_cache` from `vibemix.profile` INSIDE the function, guarded with a bare `except Exception: return ""` (mirrors `_shared_lens` exactly — Pattern 1 + Pattern 2). `render_profile_for_cache(load_profile())` returns `""` when the profile is None/consent-OFF → cold-path byte-identical.
    Attach the hint at all THREE build sites: `agent._system_instruction` (:147), `agent._interactive_system_instruction` (:166 — do NOT orphan interactive curation), and `codex_curate._system_prompt` (:125). Append `_taste_hint()` to the built instruction string (after the RULES/FLOW block, or before — pick one consistent placement; the hint is empty on the cold path so placement only matters when set). CRITICAL — cache correctness: the instruction is cached keyed by lens; the profile can change between requests too. Either (a) extend the cache key to include a cheap profile signature so a profile change rebuilds, or (b) recompute `_taste_hint()` outside the lens-keyed cache and concatenate it per call (simplest; the hint render is cheap). Choose (b) unless it breaks an existing cache test — recompute the hint per call, keep the lens-keyed cache for the `build_curator_instruction(lens) + RULES` base only. Per Pitfall 4: ONLY the allowlisted fields via `render_profile_for_cache`; never raw JSON, never `profile[...]` key access (it uses `.get` internally). Implements CURATE-02 taste half per D (CURATE-02). NO fenced code in this prose.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_curate_unify.py tests/library/test_curator_persona_seam.py 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The Plan-01 CURATE-02 taste scaffolds (`test_curator_taste_hint_present_when_profile_set`, `test_taste_hint_reaches_codex_backend`) flip to real PASS (no xfail, no xpass).
    - The cold-path-identical real-green pin stays green (no profile → instruction byte-identical to today).
    - `test_curator_persona_seam.py` (Phase-79 lens) stays green — persona NOT re-implemented (Pitfall 1).
    - `grep -n "_taste_hint" src/vibemix/library/agent.py src/vibemix/library/codex_curate.py` shows the helper + its use at all 3 build sites; the lazy `from vibemix.profile import` is INSIDE the helper (not module-top).
    - `grep -rn "import.*profile" src/vibemix/library/agent.py src/vibemix/library/codex_curate.py | grep -v "def \|#"` shows NO module-top profile import (lazy only).
  </acceptance_criteria>
  <done>`_taste_hint()` lazy-reads the shared profile and is appended at all 3 build sites (gemini + interactive + codex); profile-set → taste in instruction, profile-absent → byte-identical cold path; the lens persona pin stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the remaining pins green + full-suite + import-boundary regression gate</name>
  <read_first>
    - tests/library/test_curate_unify.py (the Plan-01 scaffolds + real-green pins — all must now be green, no xfail/xpass)
    - tests/memory/test_no_live_path_import.py (the import-boundary gate — must stay CLEAN after the two new lazy reads)
    - tests/library/test_agent.py, tests/library/test_telegram_bridge.py, tests/library/test_next_suggestion.py (the existing surfaces — regression pins, must stay byte-green)
    - 82-VALIDATION.md (the per-task verification map to fill + flip nyquist_compliant)
  </read_first>
  <action>
    Run the regression gate and confirm the unification is complete with neither backend orphaned and no invariant broken. Confirm: (1) all Plan-01 scaffolds in `test_curate_unify.py` + `test_toolset.py` are real-green (no remaining `xfailed`/`xpassed` for CURATE); (2) `test_no_live_path_import.py` stays CLEAN (the lazy genre + profile reads did not leak the live path into `sys.modules`); (3) the existing curate/Telegram/pill/MCP surfaces (`test_agent.py`, `test_telegram_bridge.py`, `test_next_suggestion.py`) stay green — both seams reach both backends via the shared `toolset.py`/`ViberAgent` core, neither orphaned; (4) no new ws port / IPC envelope was added (grep the diff for socket/port/messages.schema changes — there must be none); (5) the full suite is green at-or-above the phase baseline. Then update `82-VALIDATION.md`: fill the Per-Task Verification Map (one row per task with its automated command + ✅ status) and flip `nyquist_compliant: true` + `wave_0_complete: true` in the frontmatter. If any regression surfaces, fix the seam (do NOT weaken a test). NO fenced code in this prose.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/ tests/profile/ tests/memory/test_no_live_path_import.py 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/library/ tests/profile/ tests/memory/test_no_live_path_import.py` is fully green — zero CURATE xfails remain, zero xpasses, `test_no_live_path_import` CLEAN.
    - `pytest -q` (full suite) is green at-or-above the phase-start baseline (4523 passed) with no NEW failures.
    - `git diff --stat` touches only `src/vibemix/library/{toolset,agent,codex_curate}.py` + the two test files + `82-VALIDATION.md` — NO `messages.schema.json`, NO ws_bus/port change, NO new IPC envelope (invariant #4), NO `state/music_state.py` touch (invariant #1).
    - `82-VALIDATION.md` has `nyquist_compliant: true` and a filled per-task map.
  </acceptance_criteria>
  <done>All CURATE-01/02 scaffolds green; both backends reach both seams (neither orphaned); the four invariants hold (no new port/envelope, MusicState untouched, grounding gate intact, import-boundary CLEAN); full suite green; VALIDATION signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.json → curator system instruction | Local 2KB allowlisted DJ profile crosses into the curator prompt via `_taste_hint`. Only allowlisted fields may cross; NO track titles / free-form. |
| library cached embedding → curator genre fact | Deterministic library-side genre crosses into the tool surface; the model never supplies genre. |
| library/ curator package → live state path | The curator must NOT import/read `MusicState` (single-writer, invariant #1) — the shared perception is the offline LIBRARY representation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Information Disclosure | `_taste_hint` profile read into curator instruction | mitigate | Use `render_profile_for_cache` ONLY (Task 2) — emits the 5 allowlisted fields, NO titles/free-form; never raw JSON, never `profile[...]`; consent-default-OFF + schema `additionalProperties:false` enforce it upstream. Plan 01 Task 2(d) no-leak pin stays green. |
| T-82-02 | Tampering | curator-supplied genre (hallucination) | mitigate | Task 1: genre is a deterministic `genre_prototypes` library fact (not LLM-computed), honest-`None` on abstain; the seen-set grounding gate (`toolset.py:137-147`) + `create_playlist` re-validation are UNTOUCHED (invariant #2). |
| T-82-03 | Tampering | import-boundary breach (live path leaks via the curator's new reads) | mitigate | Both new reads (genre + profile) are LAZY (inside the function), guarded; Task 3 keeps `tests/memory/test_no_live_path_import.py` CLEAN; the no-MusicState pin (Plan 01 Task 2f) stays green. |
| T-82-04 | Tampering | new ws port / IPC envelope (invariant #4) | accept→verify | Task 3 grep proves the diff adds NO socket/port/`messages.schema.json` change — this phase is pure internal wiring over existing surfaces. |
| T-82-SC | Tampering | npm/pip/cargo installs | accept | ZERO packages installed (RESEARCH Package Legitimacy Audit — pure internal wiring across in-tree modules); no slopcheck needed; no `[ASSUMED]`/`[SUS]` packages. |
</threat_model>

<verification>
- `pytest -q tests/library/test_toolset.py` — SEAM #1 CURATE-01 scaffolds green; grounding gate + Camelot determinism untouched.
- `pytest -q tests/library/test_curate_unify.py tests/library/test_curator_persona_seam.py` — SEAM #2 CURATE-02 taste scaffolds green; lens persona pin green (Phase 79 not re-implemented).
- `pytest -q tests/library/ tests/profile/ tests/memory/test_no_live_path_import.py` — surfaces regression + import-boundary CLEAN.
- `pytest -q` full suite green at-or-above baseline (4523 passed).
- Diff touches only the 3 `src/` files + 2 tests + VALIDATION — no new port/envelope, no MusicState touch.
</verification>

<success_criteria>
- CURATE-01: `get_track_features` resolves genre via the ONE `genre_prototypes` mechanism (both surfaces, one source); honest-`None` on abstain.
- CURATE-02: both curator backends + interactive + transports curate "for this DJ" via the shared `profile/` taste layer; persona/lens confirmed already shared (regression-pinned).
- Cold path byte-identical; the four cardinal invariants hold by additive design; honest green (no API key); neither backend orphaned.
</success_criteria>

<output>
Create `.planning/phases/82-curate-unify-curator-co-host/82-02-SUMMARY.md` when done.
</output>
