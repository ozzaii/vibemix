---
phase: 69-oss-fully-integrated
plan: 05
type: execute
wave: 4
depends_on: []
files_modified:
  - KAAN-ACTION-LEGAL.md
  - docs/release-process.md
  - tests/repo/test_ship_v4_section_exists.py
requirements: [OSS-04]
autonomous: true

must_haves:
  truths:
    - "`bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` exits 0 today on the current tree (re-verifies the P39-shipped pre-flight gate after Phase 67/68 source changes; NO edits to cut_release.sh)."
    - "KAAN-ACTION-LEGAL.md §SHIP-V4 contains a v7.0 OSS-04 autonomous-mode-route sub-section with the exact pre-staged `bash scripts/launch/cut_release.sh v0.1.0-rc1` invocation + the post-pre-flight Kaan `gh release create` command + the hard-guard reminder + the v4.0-closes-alongside note."
    - "docs/release-process.md contains an 'Autonomous-mode release path' section describing the §SHIP-V4 deferral contract."
    - "tests/repo/test_ship_v4_section_exists.py pins the §SHIP-V4 section + the v7.0 OSS-04 sub-section + the exact pre-staged invocation string."
  artifacts:
    - path: "KAAN-ACTION-LEGAL.md"
      provides: "Existing §SHIP-V4 cluster (line 3389) extended with v7.0 OSS-04 sub-section."
      contains: "v7.0 OSS-04 autonomous-mode route"
    - path: "docs/release-process.md"
      provides: "Existing doc extended with 'Autonomous-mode release path' H2 section."
      contains: "Autonomous-mode release path"
    - path: "tests/repo/test_ship_v4_section_exists.py"
      provides: "Anti-rot presence test pinning §SHIP-V4 + the v7.0 OSS-04 sub-section + the pre-staged invocation."
      contains: "test_ship_v4_v7_oss_04_subsection_exists"
  key_links:
    - from: "KAAN-ACTION-LEGAL.md §SHIP-V4 v7.0 OSS-04 sub-section"
      to: "scripts/launch/cut_release.sh"
      via: "exact pre-staged invocation string for the real cut"
      pattern: "bash scripts/launch/cut_release.sh v0.1.0-rc1"
    - from: "tests/repo/test_ship_v4_section_exists.py"
      to: "KAAN-ACTION-LEGAL.md"
      via: "section-header + invocation-string grep asserts"
      pattern: "test_ship_v4_v7_oss_04_subsection_exists"
    - from: "docs/release-process.md Autonomous-mode release path section"
      to: "KAAN-ACTION-LEGAL.md §SHIP-V4"
      via: "doc cross-link explaining the deferral"
      pattern: "KAAN-ACTION-LEGAL.md §SHIP-V4"
---

<objective>
Ship the §SHIP-V4 wiring for v7.0 OSS-04 (the actual release publish): re-verify `cut_release.sh --dry-run v0.1.0-rc1` exits 0 today on the current tree (catches drift from Phase 67/68 source changes), append a v7.0 OSS-04 autonomous-mode-route sub-section to the existing `KAAN-ACTION-LEGAL.md §SHIP-V4` cluster with the exact pre-staged `cut_release.sh v0.1.0-rc1` invocation + the post-pre-flight Kaan command, extend `docs/release-process.md` with an autonomous-mode-route section, and add a presence test pinning the sub-section + the invocation string.

Purpose: When the Apple Dev Agreement (Francesco) + SignPath OSS Foundation cert (Kaan, ~1-week SLA) signatures land, the actual cut is one command. Under `gsd-autonomous fully`, the engineering side closes engineering-complete in v7.0 without waiting for the signatures; the cut itself rides Kaan's clock via the existing §SHIP-V4 cluster. v4.0 SHIP closes alongside this requirement when the cut fires for real. **The cut does NOT fire in this plan** — only the wiring + the deferral contract surface here.

Output: 3 files touched — KAAN-ACTION-LEGAL.md (extend §SHIP-V4 cluster), docs/release-process.md (append autonomous-mode-route section), tests/repo/test_ship_v4_section_exists.py (NEW). Wave 4, final plan. No edits to `scripts/launch/cut_release.sh` (P39-verified + §SHIP-CUT-locked — touching it requires its own dedicated phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-oss-fully-integrated/69-CONTEXT.md
@KAAN-ACTION-LEGAL.md
@scripts/launch/cut_release.sh
@docs/release-process.md
@tests/repo/test_no_silent_skips.py
@tests/repo/test_kaan_action_v4_surface.py

<interfaces>
<!-- KAAN-ACTION-LEGAL.md §SHIP-V4 cluster at line 3389 (read in detail above): -->
<!--   - Frontmatter: REQ-ID REL-03 / Owner Kaan+Francesco+external clock / Status checkbox list -->
<!--   - Pre-requisites subsection (DIST-09, DIST-11, Gate-2b ear-passes, §E2E-50A-WALK, Gate 5b precondition) -->
<!--   - Open discharge items table -->
<!--   - Discharge commands fenced block -->
<!--   - Public-tag confirm sub-section -->
<!--   - Verification fenced block -->
<!--   - Post-discharge subsection -->
<!--   - What unblocks subsection -->
<!--   - Sign-off block (lines 3500-3520ish) -->
<!--   The v7.0 OSS-04 sub-section is appended AFTER the existing Sign-off block + BEFORE the next top-level cluster (whatever that is — possibly the §V7-LIVE section starting at ~3653). The insertion is purely additive to the §SHIP-V4 cluster's "What unblocks" or after the Sign-off block. -->

<!-- scripts/launch/cut_release.sh (258 lines, P39-verified, §SHIP-CUT-locked): -->
<!--   - Usage: `bash scripts/launch/cut_release.sh v0.1.0-rc1` (REAL) or `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` (DRY-RUN; signature-stub mode) -->
<!--   - Output on PASS: prints the exact `gh release create` command Kaan should run; does NOT execute it -->
<!--   - The post-pre-flight Kaan command (from cut_release.sh's stdout block — executor must read the script to extract verbatim, NOT type from memory): `gh release create v0.1.0-rc1 --repo bravoh/vibemix --title "vibemix v0.1.0-rc1" --notes-file CHANGELOG-v0.1.0-rc1.md --draft --target main dist/*.dmg dist/*.msi dist/*.pkg dist/*.exe` (executor verifies this against the actual script output at task time; the exact form may have drifted from CONTEXT.md's lift) -->
<!--   - Pre-flight gates (6): tag-regex / verify_signed.py / check_gate.sh / readme_hero_hash_sync / v4.0-MILESTONE-AUDIT / no-poc-files / check_bravoh_server_ready / bundle-id-lock -->

<!-- docs/release-process.md (180 lines, 8 H2 sections — see Plan 69-04 Wave 3 for the full list). The "Autonomous-mode release path" H2 is a NEW section; place it near the end (e.g. after "## Hand-offs" but BEFORE "## Release-day checklist" — OR at the very end before "## Cross-references"). Coexist with Wave 3's "Homebrew + Scoop publish — split rationale" section (different anchor; both are additive). -->

<!-- tests/repo/test_kaan_action_v4_surface.py (existing): pins the §SHIP-V4 surface against drift — the new test_ship_v4_section_exists.py is COMPLEMENTARY (pins the v7.0 OSS-04 sub-section specifically), NOT a replacement. Both run under the default grid. -->

<!-- The dry-run command is the canonical health check for Wave 4: `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` is expected to exit 0 (per State.md / Phase 39 ledger). If it doesn't exit 0 today, drift has crept in (likely from Phase 67/68 source changes — e.g. the deleted POC files, the new test files, the §V7-LIVE clusters, the catalog reconciliation). Wave 4 either fixes that drift OR documents it as a Wave 4 follow-on (per CONTEXT.md "if any gate has drifted, fix the drift"). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify `cut_release.sh --dry-run v0.1.0-rc1` exits 0 today; fix or document any drift</name>
  <files>(read-only check; no edits unless drift detected)</files>
  <read_first>
    - scripts/launch/cut_release.sh (read at least lines 1-100 to understand the 6 pre-flight gates + the dry-run vs real-cut branching)
    - .planning/STATE.md (the v7.0 phase progress section for which Phase 67/68 plans have shipped — drift candidates: the deleted POC files, the new test files, the catalog reconciliation, the §V7-LIVE-NN additions)
    - tests/repo/test_kaan_action_v4_surface.py (existing surface gate — read to understand what's already covered)
    - tests/repo/test_cut_release_dry_run.py (if it exists — Phase 39 left a test for this; check if it's still green)
  </read_first>
  <action>
**Sub-task 1a — Run the dry-run:** From repo root, execute:

```bash
bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1
```

Capture the full stdout + stderr + the exit code.

**Sub-task 1b — Branch on exit code:**

If exit code == 0 (HAPPY PATH — expected per State.md / Phase 39 ledger):
- Record the script's stdout (in particular the exact `gh release create` command it prints — this is the post-pre-flight Kaan command that Task 2 lifts verbatim into KAAN-ACTION-LEGAL.md §SHIP-V4).
- Confirm in the Wave 4 SUMMARY: "cut_release.sh --dry-run v0.1.0-rc1 exits 0 on commit <SHA>; gh release create command captured verbatim from stdout for Task 2."
- Proceed to Task 2 with the captured stdout `gh release create` line.

If exit code != 0 (DRIFT DETECTED):
- Identify which pre-flight gate failed (read stderr carefully — the script prints a clear gate-failure message).
- For EACH failed gate, decide:
  - **Fix the drift** if it's a trivial fix-now: e.g. a removed file path that the gate references → the gate's allow-list needs updating. Make the fix as a sub-commit of Wave 4. Do NOT edit `scripts/launch/cut_release.sh` itself (P39-verified + §SHIP-CUT-locked); the fix lands in whatever ancillary file the gate consults (e.g. `tests/repo/test_g5_poc_files_untouched.py` for POC-files-stay-gone, or a README hash recompute if the hero hash sentinel drifted).
  - **Document the drift** if it's non-trivial (e.g. SBOM generation broken, signature shim refactor required): file the drift as a Wave 4 follow-on issue + add it as a §SHIP-V4 sub-discharge item in Task 2; the v7.0 OSS-04 sub-section explicitly calls out which gates still need a follow-on land before the real cut.
- Re-run `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` after each fix attempt until exit 0 OR the remaining failures are documented as Wave 4 follow-on items.

**Sub-task 1c — Confirm the post-pre-flight `gh release create` command:** Read the script's stdout block (or the `cut_release.sh` source itself — find the line that prints the `gh release create` command template; likely near the end of the script after the gate-passed block). Capture the EXACT command string verbatim (this becomes the Task 2 pre-staged invocation line).

**Sub-task 1d — Sanity-check `cut_release.sh` source untouched:** Run `git diff --stat -- scripts/launch/cut_release.sh` — MUST be empty. The script is §SHIP-CUT-locked; this plan touches NO source under scripts/launch/ except the new sync_packaging.sh from Plan 69-04 / Wave 3.

If Task 1 ends with the dry-run NOT exit 0 + drift NOT fixable inline, the Wave 4 SUMMARY documents the open drift items as §SHIP-V4 sub-discharge entries and Task 2 still proceeds (the v7.0 OSS-04 sub-section calls out the open drift as "DRIFT-NN" items the real cut must land first).
  </action>
  <verify>
    <automated>bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1 2&gt;&amp;1 | tail -20 &amp;&amp; git diff --stat -- scripts/launch/cut_release.sh | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` exits 0 (after any drift fixes applied) OR each non-zero exit gate is documented as a Wave 4 follow-on item.
    - `git diff --stat -- scripts/launch/cut_release.sh` is empty (the script is byte-unchanged; the §SHIP-CUT lock is honored).
    - The post-pre-flight `gh release create` command is captured verbatim from the script's stdout block (recorded in Wave 4 SUMMARY for use in Task 2).
    - If drift was fixed inline, the fix is committed as a sub-commit before Task 2 with a clear commit message like `fix(69-05): re-baseline <gate-name> after Phase 67/68 source changes`.
  </acceptance_criteria>
  <done>cut_release.sh --dry-run v0.1.0-rc1 exits 0 today (or drift is documented), the post-pre-flight gh release create command is captured for Task 2, and cut_release.sh source is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Append v7.0 OSS-04 autonomous-mode-route sub-section to KAAN-ACTION-LEGAL.md §SHIP-V4</name>
  <files>KAAN-ACTION-LEGAL.md</files>
  <read_first>
    - KAAN-ACTION-LEGAL.md lines 3389-3520 (the existing §SHIP-V4 cluster — read fully; the new sub-section appends to it, NOT a new top-level cluster)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "§SHIP-V4 update" decision under OSS-04 spells out the 4 required elements of the sub-section)
    - scripts/launch/cut_release.sh (the post-pre-flight `gh release create` command — Task 1 captured this verbatim from the script's stdout; this task lifts it into the sub-section)
    - .planning/STATE.md (the v4.0 SHIP entry — the "v4.0 closes alongside" note in this sub-section must match the State.md framing)
  </read_first>
  <action>
Insert a new sub-section `### v7.0 OSS-04 autonomous-mode route` into the existing `## §SHIP-V4` cluster in KAAN-ACTION-LEGAL.md. Place it AFTER the existing "### Sign-off block" sub-section (around line ~3520) and BEFORE the next top-level cluster (likely the §V7-LIVE section header at ~line 3653 OR the §V7-PROXY cluster added in Plan 69-03 if that shipped first). The sub-section is an EXTENSION of the §SHIP-V4 cluster, not a new top-level cluster.

The sub-section content (~30 lines):

```markdown
### v7.0 OSS-04 autonomous-mode route

Under `gsd-autonomous fully` (v7.0 milestone mode), the engineering-side of OSS-04 ships green via Plan 69-05 (this plan) WITHOUT waiting for the external signature clock (Apple Dev Agreement + SignPath OSS Foundation cert). The actual public-RC cut remains Kaan's hand on the trigger — this sub-section is the pre-staged invocation surface so when both signatures land, no engineering discovery step is left.

**Pre-staged invocation:**
```bash
bash scripts/launch/cut_release.sh v0.1.0-rc1
```

That command runs all 6 pre-flight gates (tag-regex, verify_signed.py, check_gate.sh, readme_hero_hash_sync, v4.0-MILESTONE-AUDIT, no-poc-files, check_bravoh_server_ready, bundle-id-lock). It does NOT invoke `gh release create` — that's intentional. cut_release.sh's hard guard: the script NEVER calls `gh release create` itself. The post-pre-flight Kaan command (printed verbatim by cut_release.sh's stdout block when gates pass):

```bash
# [Executor: paste the exact line captured from Task 1's cut_release.sh --dry-run v0.1.0-rc1 stdout here.
#  Likely shape, per CONTEXT.md (verify against actual script output before committing):]
gh release create v0.1.0-rc1 --repo bravoh/vibemix --title "vibemix v0.1.0-rc1" --notes-file CHANGELOG-v0.1.0-rc1.md --draft --target main dist/*.dmg dist/*.msi dist/*.pkg dist/*.exe
```

**Hard-guard reminder:** cut_release.sh NEVER invokes `gh release create` itself — the hand-on-trigger is Kaan, post-pre-flight. This guard is regression-pinned by `tests/repo/test_cut_release_no_autonomous_publish.py` (existing Phase 39 surface).

**v4.0 closes alongside:** When this cut fires for real, `MILESTONES.md` v4.0 entry flips from "engineering-complete (publish on signature clock)" to "SHIPPED" with the published GitHub release URL. v4.0 has been kept OPEN per Kaan directive specifically to close alongside OSS-04 — do NOT archive v4.0 until the cut is observable as a public release at `gh release view v0.1.0-rc1 --repo bravoh/vibemix`.

**v7.0 milestone close behavior:** Per CONTEXT.md decisions, v7.0 closes when OSS-04 either:
- (a) fires for real (signatures landed; cut command executed; gh release view shows the public RC), OR
- (b) has its discharge artifact accepted in §SHIP-V4 (this sub-section + the engineering-side wiring shipped; the cut itself rides forward on Kaan's clock).

Both outcomes are valid v7.0 closes under `gsd-autonomous fully`. The OSS-01/02/03/05 reqs ship unblocked regardless of OSS-04's signature-clock status (none depend on signatures).

**Sign-off block (v7.0 OSS-04 extension to the existing §SHIP-V4 Sign-off above):**

```
v7.0 OSS-04 engineering-side closed via Plan 69-05 on: _________   (date — Plan 69-05 SHA ____)
v7.0 OSS-04 cut fired for real on:                     _________   (date — Kaan, signatures landed)
v4.0 SHIPPED alongside (MILESTONES.md flip) on:        _________   (date — Kaan, gh release URL ____)
```
```

**CRITICAL — Executor verification:** Task 1 captured the exact `gh release create` line from `cut_release.sh --dry-run v0.1.0-rc1`'s stdout. Replace the placeholder comment `# [Executor: paste the exact line captured from Task 1...]` with that captured line VERBATIM. If the captured line differs from the CONTEXT.md best-guess (likely — the v3.x runbook may have drifted), the captured line wins. The Task 3 presence test pins the captured line, not the CONTEXT.md guess.

Do NOT touch the existing §SHIP-V4 cluster's Pre-requisites, Open discharge items table, Discharge commands, Public-tag confirm, Verification, Post-discharge, What unblocks, or Sign-off block. The v7.0 OSS-04 sub-section is purely additive AFTER the existing Sign-off block.
  </action>
  <verify>
    <automated>grep -c "^### v7.0 OSS-04 autonomous-mode route" KAAN-ACTION-LEGAL.md | grep -q "^1$" &amp;&amp; grep -q "bash scripts/launch/cut_release.sh v0.1.0-rc1" KAAN-ACTION-LEGAL.md &amp;&amp; grep -q "gh release create v0.1.0-rc1" KAAN-ACTION-LEGAL.md &amp;&amp; grep -q "Hard-guard reminder" KAAN-ACTION-LEGAL.md &amp;&amp; grep -q "v4.0 closes alongside" KAAN-ACTION-LEGAL.md &amp;&amp; grep -q "v7.0 OSS-04 engineering-side closed via Plan 69-05 on:" KAAN-ACTION-LEGAL.md &amp;&amp; awk '/^## §SHIP-V4/{ship=NR} /^### v7.0 OSS-04 autonomous-mode route/{sub=NR} END{if(sub &gt; ship &amp;&amp; sub &gt; 0 &amp;&amp; ship &gt; 0) exit 0; else exit 1}' KAAN-ACTION-LEGAL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### v7.0 OSS-04 autonomous-mode route" KAAN-ACTION-LEGAL.md` returns 1.
    - The sub-section line number is GREATER than the line number of `## §SHIP-V4` (i.e. the sub-section is correctly nested inside the §SHIP-V4 cluster, not a top-level header).
    - The sub-section contains the exact pre-staged invocation `bash scripts/launch/cut_release.sh v0.1.0-rc1`.
    - The sub-section contains the post-pre-flight `gh release create v0.1.0-rc1` command captured verbatim from Task 1's cut_release.sh --dry-run stdout (the placeholder comment is REPLACED with the captured line, not left as a placeholder).
    - The sub-section contains the "Hard-guard reminder" + "v4.0 closes alongside" + "v7.0 milestone close behavior" subsections.
    - The sub-section has a 3-line Sign-off extension naming Plan 69-05 + the real-cut date slot + the v4.0 SHIPPED flip slot.
    - The pre-existing §SHIP-V4 Pre-requisites / Open discharge items / Discharge commands / Public-tag confirm / Verification / Post-discharge / What unblocks / Sign-off block subsections are byte-unchanged.
  </acceptance_criteria>
  <done>KAAN-ACTION-LEGAL.md §SHIP-V4 gains the v7.0 OSS-04 autonomous-mode-route sub-section with the exact pre-staged invocation + the captured gh release create command + hard-guard reminder + v4.0-alongside note + 3-line Sign-off extension; pre-existing §SHIP-V4 content byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Append "Autonomous-mode release path" section to docs/release-process.md</name>
  <files>docs/release-process.md</files>
  <read_first>
    - docs/release-process.md (current 180-line file — find the existing H2 sections; the new section is placed at the end before "## Cross-references" — distinct anchor from Wave 3's "Homebrew + Scoop publish — split rationale" which sits between "Hand-offs" and "Release-day checklist" OR after Hand-offs depending on Wave 3 executor's choice)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "docs/release-process.md update" decision under OSS-04 spells out the content for the autonomous-mode-route section)
  </read_first>
  <action>
Append a new H2 section `## Autonomous-mode release path` to docs/release-process.md. Place it near the end of the file, AFTER any Wave 3 insertion (the "Homebrew + Scoop publish — split rationale" section) and BEFORE the existing `## Cross-references` section. Section content (~15 lines):

```markdown
## Autonomous-mode release path

Under `gsd-autonomous fully` (the autonomous overnight-run mode used for v7.0 milestone execution), the engineering side of OSS-04 — the actual v0.1.0-rc1 public release publish — ships green via Plan 69-05 WITHOUT blocking on the external signature clock (Apple Dev Agreement signed by Francesco + SignPath OSS Foundation cert granted to Kaan, ~1-week SLA).

The contract is:
1. The pre-flight script `scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` is re-verified GREEN on every v7.0 milestone close (Plan 69-05 / Wave 4). If a Phase 67/68 source change drifts a pre-flight gate, Plan 69-05 fixes it inline or routes the drift to `KAAN-ACTION-LEGAL.md §SHIP-V4` as a Wave 4 follow-on item.
2. The exact pre-staged real-cut invocation + post-pre-flight `gh release create` command live in `KAAN-ACTION-LEGAL.md §SHIP-V4` "v7.0 OSS-04 autonomous-mode route" sub-section. Kaan runs the cut when the signatures land.
3. cut_release.sh NEVER invokes `gh release create` itself — the hand-on-trigger is Kaan, regression-pinned by `tests/repo/test_cut_release_no_autonomous_publish.py`.
4. v4.0 "SHIP" milestone (engineering-complete since 2026-05-21) closes alongside OSS-04 when the real cut fires — `MILESTONES.md` v4.0 entry flips to SHIPPED with the public release URL.

Under non-autonomous mode (e.g. an interactive Kaan-driven session where Kaan wants to walk through `cut_release.sh v0.1.0-rc1` in real time), the contract is unchanged — the pre-flight script + the `gh release create` invocation + the hard-guard split between engineering-pre-flight and Kaan-trigger is the same.

See `KAAN-ACTION-LEGAL.md §SHIP-V4` for the full SHIP runbook + sign-off block.
```

Do NOT modify any existing section in docs/release-process.md. The insertion is purely additive. The section is independent of Wave 3's "Homebrew + Scoop publish — split rationale" insertion (different anchor point, different content); the two can coexist if Wave 3 has already shipped (or land independently if Wave 4 ships first).
  </action>
  <verify>
    <automated>grep -q "^## Autonomous-mode release path" docs/release-process.md &amp;&amp; grep -q "scripts/launch/cut_release.sh --dry-run v0.1.0-rc1" docs/release-process.md &amp;&amp; grep -q "KAAN-ACTION-LEGAL.md §SHIP-V4" docs/release-process.md &amp;&amp; grep -q "test_cut_release_no_autonomous_publish.py" docs/release-process.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/release-process.md contains the H2 header `## Autonomous-mode release path` exactly once.
    - Section references `scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` (the pre-flight invocation).
    - Section references `KAAN-ACTION-LEGAL.md §SHIP-V4` (cross-link to the cluster).
    - Section references `test_cut_release_no_autonomous_publish.py` (the hard-guard regression pin).
    - Section mentions the v4.0 SHIP closes-alongside contract.
    - Pre-existing docs/release-process.md sections are byte-unchanged (`git diff docs/release-process.md` shows only the additive insertion; if Wave 3 already added its section, both additive inserts coexist).
    - Section length ≤ 20 lines.
  </acceptance_criteria>
  <done>docs/release-process.md gains the Autonomous-mode release path section; pre-existing content byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create tests/repo/test_ship_v4_section_exists.py</name>
  <files>tests/repo/test_ship_v4_section_exists.py</files>
  <behavior>
    - test_ship_v4_section_exists: KAAN-ACTION-LEGAL.md contains `## §SHIP-V4 — Consolidated v4.0 Ship Surface` exactly once.
    - test_ship_v4_v7_oss_04_subsection_exists: KAAN-ACTION-LEGAL.md contains `### v7.0 OSS-04 autonomous-mode route` exactly once, and it appears AFTER the §SHIP-V4 header in the file.
    - test_ship_v4_pre_staged_invocation_pinned: KAAN-ACTION-LEGAL.md contains the exact pre-staged invocation `bash scripts/launch/cut_release.sh v0.1.0-rc1` at least once.
    - test_ship_v4_gh_release_create_command_pinned: KAAN-ACTION-LEGAL.md contains the post-pre-flight `gh release create v0.1.0-rc1` command (substring check — exact form depends on Task 1's captured stdout, but the `gh release create v0.1.0-rc1` prefix is invariant).
    - test_release_process_doc_has_autonomous_mode_path: docs/release-process.md contains `## Autonomous-mode release path` exactly once.
    - All tests run under the default `uv run pytest -q` grid (no opt-in marker — anti-drift presence gates).
    - Test file follows tests/repo/ idiom: SPDX header, REPO_ROOT three-parent walk, ≤120 lines.
  </behavior>
  <read_first>
    - tests/repo/test_kaan_action_v4_surface.py (existing surface gate — read for the existing §SHIP-V4 pinning pattern; the new test is COMPLEMENTARY)
    - tests/repo/test_no_silent_skips.py (top-of-file SPDX + REPO_ROOT pattern)
    - .planning/phases/69-oss-fully-integrated/69-CONTEXT.md (the "test surface for the deferral" bullet under OSS-04 decisions specifies the 4 required pins)
    - KAAN-ACTION-LEGAL.md (post-Task-2 state — the new sub-section's exact strings inform the asserts)
  </read_first>
  <action>
Create `tests/repo/test_ship_v4_section_exists.py` (NEW, ≤120 lines). Top of file:

```python
# SPDX-License-Identifier: Apache-2.0
"""Phase 69 Plan 69-05 — anti-rot presence test for §SHIP-V4 + v7.0 OSS-04 sub-section.

OSS-04 invariant: the §SHIP-V4 cluster + the v7.0 OSS-04 autonomous-mode-route
sub-section + the exact pre-staged `cut_release.sh v0.1.0-rc1` invocation
must not silently drift between v7.0 milestone close and the eventual real
cut. The existing `tests/repo/test_kaan_action_v4_surface.py` covers the
broader §SHIP-V4 surface; this gate pins specifically the v7.0 OSS-04
autonomous-mode-route extension Plan 69-05 ships.

When the real cut fires (Apple Dev + SignPath landed, Kaan runs the
invocation), this gate stays green — it pins the documentation contract,
not the cut outcome.
"""
from __future__ import annotations

from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent.parent

KAAN_ACTION = REPO_ROOT / "KAAN-ACTION-LEGAL.md"
RELEASE_PROCESS = REPO_ROOT / "docs" / "release-process.md"

_SHIP_V4_HEADER = "## §SHIP-V4 — Consolidated v4.0 Ship Surface"
_V7_OSS_04_SUBSECTION = "### v7.0 OSS-04 autonomous-mode route"
_PRE_STAGED_INVOCATION = "bash scripts/launch/cut_release.sh v0.1.0-rc1"
_GH_RELEASE_CREATE_PREFIX = "gh release create v0.1.0-rc1"
_RELEASE_PROCESS_HEADER = "## Autonomous-mode release path"
```

Then 5 test functions:

1. `def test_ship_v4_section_exists() -> None:` — assert `KAAN_ACTION.exists()`; assert the §SHIP-V4 header appears exactly once via `text.count(_SHIP_V4_HEADER) == 1`.

2. `def test_ship_v4_v7_oss_04_subsection_exists() -> None:` — read KAAN-ACTION-LEGAL.md once; assert the v7.0 OSS-04 sub-section header appears exactly once. Additionally, verify the sub-section position is AFTER the §SHIP-V4 header (line-number check): split the text into lines, find the line indices of both headers, assert `subsection_line > ship_v4_line`.

3. `def test_ship_v4_pre_staged_invocation_pinned() -> None:` — assert the exact pre-staged invocation `_PRE_STAGED_INVOCATION` appears at least once in KAAN-ACTION-LEGAL.md.

4. `def test_ship_v4_gh_release_create_command_pinned() -> None:` — assert `_GH_RELEASE_CREATE_PREFIX` appears at least once in KAAN-ACTION-LEGAL.md (substring check — the exact post-pre-flight form depends on Task 1's captured stdout; only the prefix is invariant across cut_release.sh versions).

5. `def test_release_process_doc_has_autonomous_mode_path() -> None:` — assert `RELEASE_PROCESS.exists()`; assert `_RELEASE_PROCESS_HEADER` appears exactly once in docs/release-process.md via `text.count(...) == 1`.

Every test has a helpful assert message pointing the executor at the originating plan + task. No opt-in marker. Total ≤ 120 lines.
  </action>
  <verify>
    <automated>uv run pytest tests/repo/test_ship_v4_section_exists.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File tests/repo/test_ship_v4_section_exists.py exists with `# SPDX-License-Identifier: Apache-2.0` on line 1.
    - File contains all 5 test function definitions named as listed in <action>.
    - `uv run pytest tests/repo/test_ship_v4_section_exists.py -q` exits 0 with exactly 5 passed.
    - File length ≤ 120 lines.
    - No opt-in pytest marker.
    - Default `uv run pytest -q` baseline increases by exactly 5 tests vs the pre-Wave-4 baseline.
    - Negative-control verified manually: temporarily renaming `### v7.0 OSS-04 autonomous-mode route` to `### v7 OSS-04 ...` in KAAN-ACTION-LEGAL.md → `test_ship_v4_v7_oss_04_subsection_exists` flips red with executor-pointing assert message; restore → green.
  </acceptance_criteria>
  <done>tests/repo/test_ship_v4_section_exists.py exists, 5/5 GREEN under default grid; deletion or rename of §SHIP-V4, the v7.0 OSS-04 sub-section, the pre-staged invocation, the gh release create prefix, or the docs/release-process.md autonomous-mode header turns CI red.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete, run from repo root:

```bash
# 1. New presence gate runs green on its own (5 tests):
uv run pytest tests/repo/test_ship_v4_section_exists.py -q
#   Expected: 5 passed in &lt; 1s

# 2. Default-grid baseline +5 tests, 0 regressions:
uv run pytest -q
#   Expected: prior baseline + 5; 26 skipped + 4 xpassed unchanged; 0 failed

# 3. cut_release.sh --dry-run still GREEN (re-verified after any inline drift fixes):
bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1
#   Expected: exit 0 + the gh release create command in stdout

# 4. §SHIP-V4 sub-section + docs section verified by hand:
grep -c "^### v7.0 OSS-04 autonomous-mode route" KAAN-ACTION-LEGAL.md   # == 1
grep -c "bash scripts/launch/cut_release.sh v0.1.0-rc1" KAAN-ACTION-LEGAL.md   # >= 1
grep -c "^## Autonomous-mode release path" docs/release-process.md   # == 1

# 5. cut_release.sh source byte-unchanged (§SHIP-CUT lock honored):
git diff --stat -- scripts/launch/cut_release.sh   # must be empty

# 6. No src/vibemix/ regressions (cardinal invariant zero-touch):
git diff --stat -- src/vibemix/   # must be empty

# 7. The existing test_kaan_action_v4_surface.py still passes (not broken by the additive sub-section):
uv run pytest tests/repo/test_kaan_action_v4_surface.py -q   # exit 0
```
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §SHIP-V4 sub-section → real cut | The pre-staged invocation + the captured `gh release create` command are documentation; the actual cut runs under Kaan's account. If the documented commands drift from the real cut_release.sh output, Kaan runs the wrong command. Mitigation: Task 1 re-verifies the dry-run + captures the exact stdout, and Task 4 pins the captured command. |
| cut_release.sh source → §SHIP-CUT lock | The script is P39-verified + §SHIP-CUT-locked; this plan touches NO source under scripts/launch/cut_release.sh. Any inline drift fix from Task 1 lands in ancillary files (e.g. a test allow-list), not in the script itself. |
| KAAN-ACTION-LEGAL.md §SHIP-V4 surface → audit trail | The §SHIP-V4 cluster is the historical audit trail for the v4.0 SHIP discharge. The v7.0 OSS-04 sub-section is an extension; the pre-existing audit trail is byte-unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69P05-01 | Tampering | cut_release.sh hard-guard (NEVER invokes gh release create) | mitigate | Existing `tests/repo/test_cut_release_no_autonomous_publish.py` (Phase 39 surface) regression-pins the hard guard; this plan does NOT touch that test or the script. The new Task 4 test pins the documentation contract complementarily. |
| T-69P05-02 | Spoofing | gh release create command in §SHIP-V4 sub-section | mitigate | Task 1 captures the command VERBATIM from `cut_release.sh --dry-run v0.1.0-rc1` stdout; Task 2 pastes it; Task 4 substring-pins the `gh release create v0.1.0-rc1` prefix. A future cut_release.sh refactor that changes the printed command without updating §SHIP-V4 fails the Task 4 test red. |
| T-69P05-03 | Repudiation | v4.0 SHIPPED flip in MILESTONES.md | accept | The sub-section calls out that v4.0 closes alongside OSS-04 when the cut fires; the actual `MILESTONES.md` flip is a manual edit at cut-time, NOT pinned by this plan's tests. Accepting because the milestone-close audit trail is the Wave 4 SUMMARY + the eventual `gh release view` URL, both human-verifiable. |
| T-69P05-04 | Tampering | docs/release-process.md autonomous-mode section | mitigate | Task 4's `test_release_process_doc_has_autonomous_mode_path` pins the section header exactly-once; deletion/rename fails CI red. |
| T-69P05-SC | Supply Chain | gh CLI used in the cut command | accept | `gh` CLI is GitHub's official tooling, installed on developer machines + GH Actions runners. v7.0 does NOT pin a specific gh version. Acceptable: the gh CLI is part of the trust base for any GitHub-hosted OSS project. |
</threat_model>

<success_criteria>
- [ ] `bash scripts/launch/cut_release.sh --dry-run v0.1.0-rc1` exits 0 on the current tree (after any inline drift fixes); any non-trivial drift documented as a §SHIP-V4 sub-discharge item.
- [ ] KAAN-ACTION-LEGAL.md §SHIP-V4 gains the v7.0 OSS-04 autonomous-mode-route sub-section with the exact pre-staged invocation + the captured gh release create command + Hard-guard reminder + v4.0-alongside note + 3-line Sign-off extension.
- [ ] Pre-existing §SHIP-V4 Pre-requisites / Open discharge items / Discharge commands / Public-tag confirm / Verification / Post-discharge / What unblocks / Sign-off block are byte-unchanged.
- [ ] docs/release-process.md gains `## Autonomous-mode release path` H2 section cross-linking §SHIP-V4 + the hard-guard test; pre-existing content byte-unchanged.
- [ ] tests/repo/test_ship_v4_section_exists.py exists, ≤ 120 lines, 5/5 GREEN under default grid.
- [ ] Default `uv run pytest -q` baseline: +5 tests, 0 regressions; 26 skipped + 4 xpassed unchanged.
- [ ] `git diff --stat -- scripts/launch/cut_release.sh` is empty (§SHIP-CUT lock honored).
- [ ] Zero `src/vibemix/` edits (cardinal invariant zero-touch).
- [ ] Existing `tests/repo/test_kaan_action_v4_surface.py` still passes (the additive sub-section does not break the existing pin).
- [ ] Negative-control verified: temporarily renaming the v7.0 OSS-04 sub-section header in KAAN-ACTION-LEGAL.md flips the matching presence test red; restore → green.
- [ ] All commits on `live-tuning-or-brain` follow the existing commit-message style.
</success_criteria>

<output>
Create `.planning/phases/69-oss-fully-integrated/69-05-SUMMARY.md` when done. Record:
- The dry-run result (exit code + any drift fixed inline + any drift documented as follow-on).
- The captured `gh release create v0.1.0-rc1 ...` command verbatim (from Task 1's stdout) — for posterity + audit.
- The three artifacts shipped (§SHIP-V4 sub-section addition + docs/release-process.md section + test file).
- Baseline delta (+5 tests, 0 regressions).
- The negative-control verification result.
- Confirm OSS-04 closed engineering-side; the real cut rides Kaan's clock via §SHIP-V4.
- Confirm cut_release.sh source byte-unchanged (§SHIP-CUT lock honored).
- Confirm v4.0 SHIP stays OPEN (NOT archived) — closes alongside OSS-04 when the real cut fires.
- Confirm Phase 69 ENGINEERING-COMPLETE if Waves 0/1/2/3 have all also shipped — Phase 70 (GH pillar) is unblocked.
- Note that Wave 4 is independent of Wave 0/1/2/3 — execution order is parallelizable.
</output>
