---
phase: 77-wire-connect-the-islands
plan: 04
type: execute
wave: 2
depends_on: [77-01, 77-03]
files_modified:
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/__main__.py
autonomous: true
requirements: [WIRE-01, WIRE-05]

must_haves:
  truths:
    - "DJCoHostAgent accepts a grounding kwarg/setter; with grounding=None the cold path is byte-identical (existing tests green unchanged)"
    - "On a track-aware event the agent dispatches Grounding.on_event OFF the asyncio loop (executor thread), never inline-awaits the embed"
    - "A cited [track:<id>] from grounding is injected into the prompt and resolves in EvidenceRegistry (survives the citation linter — invariant #2)"
    - "Grounding is cleared at turn end alongside the recall clear"
    - "On the live main() path, with VIBEMIX_RECALL_ENABLED on, memory.db ingest fires at boot + session-close via _fire_ingest; with the flag off, NO ingest call (cold path)"
    - "Retention is NOT doubled — run_boot_sweeps/on_session_close are NOT called; _fire_ingest only"
  artifacts:
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "grounding kwarg + _maybe_dispatch_grounding + llm_node pull/inject + turn-end clear"
      contains: "_maybe_dispatch_grounding"
    - path: "src/vibemix/__main__.py"
      provides: "grounding passed into DJCoHostAgent + gated _fire_ingest boot/close on _session_ipc"
      contains: "_fire_ingest"
  key_links:
    - from: "src/vibemix/agent/dj_cohost.py::set_next_event"
      to: "Grounding.on_event"
      via: "_maybe_dispatch_grounding off-loop run_in_executor on TRACK_AWARE_EVENTS"
      pattern: "_maybe_dispatch_grounding"
    - from: "src/vibemix/agent/dj_cohost.py::llm_node"
      to: "EvidenceRegistry"
      via: "get_latest_citation -> inject [track:<id>] -> register_library-seeded id resolves"
      pattern: "get_latest_citation"
    - from: "src/vibemix/__main__.py::main"
      to: "SessionLoop._fire_ingest"
      via: "gated boot + close ingest on the already-built _session_ipc instance"
      pattern: "_session_ipc._fire_ingest"
---

<objective>
The two structural wires. WIRE-01 (highest leverage): the live co-host references what is actually playing by mirroring the proven Phase-65 MemoryRecall 4-point seam — pass the already-armed `Grounding` into `DJCoHostAgent`, dispatch `Grounding.on_event` off-loop on track-aware events, pull `get_latest_citation()` in `llm_node` and inject `[track:<id>]` (which resolves via the already-registered library ids), clear at turn end. WIRE-05: fill `memory.db` on the live `main()` path by calling `_fire_ingest` directly on the already-built `_session_ipc` SessionLoop, gated on `VIBEMIX_RECALL_ENABLED`, WITHOUT double-running retention.

Purpose: closes connection-map wires #1 (grounding→agent, orphaned today) and #5 (memory.db never fills on the live path). Everything in v8.1 downstream enriches this wired surface.
Output: the grounding seam in dj_cohost.py + the grounding pass-in and gated ingest calls in __main__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-wire-connect-the-islands/77-RESEARCH.md
@.planning/phases/77-wire-connect-the-islands/77-PATTERNS.md

<interfaces>
<!-- The exact contracts — extracted + line-verified from live source 2026-05-26. Mirror the recall seam; do not re-explore. -->

WIRE-01 — the recall seam to MIRROR (src/vibemix/agent/dj_cohost.py):
- Constructor (:340), recall kwargs precedent at `:420` (`recall: "MemoryRecall | None" = None`, `recall_enabled: bool = False`); stored `self._recall` at `:517`.
- `set_next_event(ev)` (:826) calls `self._maybe_dispatch_recall(ev)`.
- `_maybe_dispatch_recall(ev)` (:855) — the template: gate (return if disabled/None) → event-gate → `loop=asyncio.get_running_loop()` (return on RuntimeError = no loop, unit tests) → `asyncio.create_task` wrapping `asyncio.wait_for(loop.run_in_executor(None, svc.on_event, ...), timeout=...)` → cancel+replace prior in-flight task → on timeout/exc call `svc.clear()` + stderr log.
- `llm_node` (:967): recall pull/register block around `:1008-1085`; per-turn `snapshot = self._registry.snapshot()` happens after. `self._clean_audio_buf` (:449); WAV built via `snapshot_wav(self._clean_audio_buf, audio_seconds)` (:1180).
- Turn-end recall clear at `:2118` (`if self._recall_enabled and self._recall is not None: try: self._recall.clear()`).

WIRE-01 — the source object (src/vibemix/library/grounding.py — READ-ONLY, do not modify):
- `TRACK_AWARE_EVENTS: frozenset[str]` (:46) = {"TRACK_CHANGE","LAYER_ARRIVAL","MIX_MOVE"} — import this as the event gate.
- `Grounding.on_event(event_type, audio_bytes, *, event_id=None, mime_type="audio/wav")` (:188) — off-loop entry; stores latest CITED citation under a threading.Lock.
- `Grounding.get_latest_citation() -> Citation | None` (:211); `Grounding.clear()` (:216).
- `Citation.is_cited` property; `Citation.track_id` set only when decision=="cited" (cosine >= 0.7). `identify_playing` tolerates audio_bytes=None → below-threshold Citation, never raises.

WIRE-01 — the citation gate (already wired):
- `register_library` (state/evidence_registry.py) already seeds EVERY library track id as a "track" observation — called on the live path at `__main__.py:1101`. So `[track:<id>]` resolves in `EvidenceRegistry.has("track", id, t)` → CitationLinter does NOT strip the turn. NO linter change.

WIRE-01 — the build sites (src/vibemix/__main__.py):
- agent built at `:1047` (DJCoHostAgent(...) with recall=recall_svc, recall_enabled=recall_enabled at the end of the kwargs block ~:1089).
- grounding built at `:1134-1162` (`grounding = _Grounding(_library_embedder, _library_store)` when library_cache.exists()), AFTER the agent. Deps (genai_client/library_cache) available before :1047.

WIRE-05 (src/vibemix/__main__.py + runtime/session_loop.py):
- `recall_enabled` resolved at `:1024` (from VIBEMIX_RECALL_ENABLED; default OFF).
- `_session_ipc = SessionLoop(...)` built inside try: at `:1210`, `register_handlers()` at `:1221`, NEVER `run()`. On the except path (:1226) only `ipc_router` is set None — guard `_session_ipc is not None`.
- `_fire_ingest(self, trigger, *, session_dir=None)` (session_loop.py:862) — ingest-only; None-guards on recordings_root. Triggers: "boot" (run_ingest_sweep) / "close" (ingest_session). All heavy work inside loop.run_in_executor; best-effort never-raise.
- DO NOT call run_boot_sweeps (:689) / on_session_close (:725) — they ALSO run retention. main() already runs retention at boot (:632-646) and close (:1366-1377). Calling them = double retention.
- close region: the finally/close block ends near `:1377` (retention sweep close); place the close ingest there, after recorder.close(), guarded by recall_enabled AND _session_ipc not None.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WIRE-01 — mirror the recall 4-point seam for Grounding in dj_cohost.py</name>
  <files>src/vibemix/agent/dj_cohost.py, tests/agent/test_dj_cohost_grounding.py</files>
  <read_first>
    - src/vibemix/agent/dj_cohost.py:340-460 (constructor + self._clean_audio_buf), 826-965 (set_next_event + _maybe_dispatch_recall template), 967-1180 (llm_node + recall pull + snapshot_wav), 2095-2123 (turn-end recall clear)
    - src/vibemix/library/grounding.py:46-48, 169-220 (TRACK_AWARE_EVENTS + Grounding contract)
    - tests/agent/test_dj_cohost_grounding.py (Wave-0 stub — un-xfail as each beat lands)
  </read_first>
  <behavior>
    - DJCoHostAgent(..., grounding=None) → cold path byte-identical; no dispatch, no injection, existing dj_cohost tests green unchanged.
    - DJCoHostAgent(..., grounding=<fake>) + set_next_event(TRACK_CHANGE) → fake.on_event runs on an executor thread (thread id != event-loop thread id), not inline.
    - A non-track-aware event (e.g. HEARTBEAT) → no grounding dispatch.
    - llm_node with grounding returning a cited Citation(track_id=X) → prompt text contains "[track:X]"; X is registered → EvidenceRegistry.has("track", X, t) True → linter keeps the turn.
    - Turn end → grounding.clear() called when grounding is not None.
  </behavior>
  <action>
    Mirror `_maybe_dispatch_recall` exactly at 4 points (per PATTERNS Pattern 1 / Points 1-4):
    POINT 1 — Constructor (:340): add `grounding: "Grounding | None" = None` to the kwargs-only block (next to recall); store `self._grounding = grounding` in the __init__ body next to `self._recall`. Default None → cold path byte-identical. (Match the recall kwarg style; no separate enabled flag needed — `grounding is not None` IS the gate, since the live build already conditions grounding creation on library presence.)
    POINT 2 — Add `_maybe_dispatch_grounding(self, ev: Event) -> None` modeled on `_maybe_dispatch_recall` (:855): return if `self._grounding is None`; event-gate on `ev.type not in TRACK_AWARE_EVENTS` (import the frozenset from `vibemix.library.grounding`); `loop = asyncio.get_running_loop()` with RuntimeError→return; create a cancellable task wrapping `asyncio.wait_for(loop.run_in_executor(None, self._grounding.on_event, ev.type, audio_bytes, ...), timeout=<reuse a deadline const like the recall one>)`; on timeout/exception call `self._grounding.clear()` + stderr log `[grounding dispatch err]`; cancel+replace any prior in-flight grounding task (separate task attr `self._grounding_task`). AUDIO SOURCE: reuse the existing `self._clean_audio_buf` WAV snapshot (snapshot_wav, as llm_node does at :1180) — do NOT add a second capture path (A3). `on_event`'s mime is "audio/wav"; pass `event_id` analogous to the recall call. Call `self._maybe_dispatch_grounding(ev)` from `set_next_event` (:826) right alongside the existing `self._maybe_dispatch_recall(ev)` call.
    POINT 3 — In `llm_node` (:967), BEFORE the prompt text Part is finalized (near where recall pulls at ~:1037, before the per-turn `snapshot` at the registry line): if `self._grounding is not None`, `cit = self._grounding.get_latest_citation()`; if `cit is not None and cit.is_cited`, append ` [track:{cit.track_id}]` into the prompt text. NO per-turn registration needed (register_library already seeded the id). Keep this strictly read-only; never write MusicState (invariant #1).
    POINT 4 — At turn end (:2118, alongside the recall clear): add `if self._grounding is not None: try: self._grounding.clear() except Exception as _e: print(f"[grounding clear err] {_e}", file=sys.stderr)`.
    NEVER inline-await the embed in llm_node (TTFT regression — the entire reason the pre-dispatch seam exists). `from __future__ import annotations` present; use the string forward-ref `"Grounding | None"`. As each behavior lands, remove the corresponding xfail(strict) marker in the Wave-0 test.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/agent/test_dj_cohost_grounding.py tests/agent/test_dj_cohost.py</automated>
  </verify>
  <acceptance_criteria>
    - grounding kwarg present; `self._grounding` stored; default None.
    - `_maybe_dispatch_grounding` dispatches off-loop (executor thread != loop thread) only on TRACK_AWARE_EVENTS; the Wave-0 off-loop + source-text `run_in_executor` gates pass (xfail removed).
    - cited `[track:<id>]` injected + resolves in EvidenceRegistry (citation-survives test green).
    - turn-end grounding.clear() called.
    - Cold path: existing `tests/agent/test_dj_cohost.py` byte-identity/golden tests GREEN unchanged.
    - No model literal introduced (grounding resolves its own embed model); `tests/repo/test_model_literal_gate.py` green.
  </acceptance_criteria>
  <done>4-point grounding seam mirrors the recall pattern; off-loop dispatch + cited injection + turn-end clear; cold path byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass grounding into the agent + gated _fire_ingest on the live path (WIRE-01 wiring + WIRE-05)</name>
  <files>src/vibemix/__main__.py, src/vibemix/agent/dj_cohost.py, tests/memory/test_ingest_wiring.py</files>
  <read_first>
    - src/vibemix/__main__.py:632-660 (boot retention), 1024 (recall_enabled), 1047-1090 (agent build), 1098-1103 (register_library), 1129-1162 (grounding build), 1199-1228 (SessionLoop build site), 1340-1380 (close region + retention)
    - src/vibemix/runtime/session_loop.py:689-743, 862-924 (_fire_ingest vs the combined sweep methods — confirm the difference)
    - tests/memory/test_ingest_wiring.py (the main()-path xfail to un-xfail)
  </read_first>
  <behavior>
    - main() builds grounding (when library_cache exists) and passes it into DJCoHostAgent; when no library, grounding stays None and the agent cold path is unchanged.
    - With VIBEMIX_RECALL_ENABLED set, main() calls _session_ipc._fire_ingest("boot") at boot and _fire_ingest("close", session_dir=...) at session close; with the flag off, neither fires.
    - run_boot_sweeps / on_session_close are NOT called from main() (no double retention).
    - _session_ipc is None-guarded on both ingest call sites.
  </behavior>
  <action>
    WIRE-01 wiring: make the armed `Grounding` reach the agent. Per RESEARCH Open Q1 + PATTERNS anti-pattern note, prefer the smallest additive diff: EITHER move the grounding build (`:1134-1162`) ABOVE the agent build (`:1047`) — its deps (genai_client, library_cache/_library_embedder/_library_store) are available earlier — and add `grounding=grounding` to the `DJCoHostAgent(...)` kwargs block (alongside `recall=recall_svc`), OR if reordering is not a clean diff, add a post-construction `agent.attach_grounding(grounding)` setter called after both are built (add the trivial setter to dj_cohost.py in this task if chosen). Pick whichever is the smaller, cleaner diff; document the choice in the SUMMARY. Either way the cold path (no library → grounding None) is byte-identical.
    WIRE-05: on the already-built `_session_ipc` SessionLoop, add gated ingest calls — DO NOT call `run_boot_sweeps`/`on_session_close` (double-retention). BOOT: after the `_session_ipc.register_handlers()` block (~:1224, inside or right after the try), add `if recall_enabled and _session_ipc is not None: asyncio.create_task(_session_ipc._fire_ingest("boot"))` (mirror SessionLoop.run's `_boot_ingest_task` at session_loop.py:723). CLOSE: in the finally/close region near the close retention sweep (~:1366-1377), after `recorder.close()`, add `if recall_enabled and _session_ipc is not None: await _session_ipc._fire_ingest("close", session_dir=recorder.session_dir)` (the close path is async; confirm it is — if the surrounding close is sync, schedule it the same way the boot task is, or run it via the loop). Reuse `recall_enabled` already resolved at `:1024`. Confirm `_session_ipc` is in scope in the finally block (it is defined in the outer try at :1210); guard `is not None` since the except path leaves it unset. Default flag OFF → additive no-op, cold path byte-identical.
    Remove the main()-path xfail(strict) markers in `tests/memory/test_ingest_wiring.py` so they assert real passes.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/memory/test_ingest_wiring.py tests/agent/test_dj_cohost_grounding.py</automated>
  </verify>
  <acceptance_criteria>
    - `DJCoHostAgent(...)` in main() receives `grounding=grounding` (or via attach_grounding setter); grounding build deps satisfied; cold path (no library) unchanged.
    - Source: `grep -n "_session_ipc._fire_ingest" src/vibemix/__main__.py` shows boot + close calls, both gated on `recall_enabled` and `_session_ipc is not None`.
    - Source: `run_boot_sweeps(` / `on_session_close(` do NOT appear in `__main__.py` (anti-double-retention) — assert in the Wave-0 main()-path test.
    - Wave-0 main()-path ingest test passes (flag-on fires _fire_ingest boot+close, flag-off no-op, no double-retention); xfail removed.
    - Full suite green; no API key (fakes); retention not doubled.
  </acceptance_criteria>
  <done>grounding wired into the agent on the live path; gated _fire_ingest boot+close fire without doubling retention; main()-path test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live audio → grounding embed → prompt citation | The grounding cosine result becomes a citation injected into a Gemini turn — must resolve in the registry. |
| session recordings → memory.db ingest | Session artifacts are ingested into the local recall store on the live path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04-01 | Spoofing/Tampering | Hallucinated/fabricated track citation reaching a reaction | mitigate | Grounding only emits a cited Citation at cosine>=0.7; the injected `[track:<id>]` resolves ONLY against register_library-seeded ids; an unresolvable citation strips the turn (citation-grounding gate, invariant #2). Read-only consult — never writes MusicState (invariant #1). |
| T-77-04-02 | Denial of Service | Inline-awaiting the embed wedges the reaction hot path (TTFT regression) | mitigate | Dispatch is off-loop via run_in_executor + wait_for(timeout); a slow/failed embed clears grounding (late worse than none) and never blocks llm_node — mirrors the proven recall seam. |
| T-77-04-03 | Tampering | Double retention sweep prunes recordings twice | mitigate | Call `_fire_ingest` ONLY, never run_boot_sweeps/on_session_close; Wave-0 test asserts those combined methods are absent from __main__.py. |
| T-77-04-04 | Information Disclosure | memory.db ingest fires unexpectedly for the default user | mitigate | Both ingest calls gated on VIBEMIX_RECALL_ENABLED (default OFF) — additive no-op; recall stays opt-in (RECALL-EAR is a separate Kaan-ear clock). |
| T-77-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/agent tests/memory` green.
- Cold-path: `PYTHONPATH=src python3 -m pytest -q tests/agent/test_dj_cohost.py` (existing byte-identity/golden) green UNCHANGED.
- Model-literal gate green.
- Full suite `PYTHONPATH=src python3 -m pytest -q` green (4449 + new, 0 fail), no API key.
- LIVE e2e (KAAN-ACTION, never faked): `uv run python -m vibemix`, play a known library track, confirm a real `[track:<id>]` citation in a reaction; and with `VIBEMIX_RECALL_ENABLED=1` confirm memory.db fills.
</verification>

<success_criteria>
- WIRE-01: agent grounds reactions on the actual playing track via the off-loop seam; cold path byte-identical.
- WIRE-05: memory.db fills on the live path when recall is enabled, without doubling retention.
- All invariants hold by additive design; honest green offline; live e2e parked as KAAN-ACTION.
</success_criteria>

<output>
Create `.planning/phases/77-wire-connect-the-islands/77-04-SUMMARY.md` when done. Record: (1) the WIRE-01 wiring approach chosen (build-reorder vs attach_grounding setter) and why; (2) confirmation the close-path `_fire_ingest` is awaited/scheduled correctly; (3) the KAAN-ACTION live-e2e item for the milestone surface.
</output>
