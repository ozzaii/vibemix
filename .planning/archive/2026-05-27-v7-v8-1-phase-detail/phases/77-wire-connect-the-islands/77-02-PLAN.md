---
phase: 77-wire-connect-the-islands
plan: 02
type: execute
wave: 1
depends_on: [77-01]
files_modified:
  - src/vibemix/prompts/matrix.py
  - src/vibemix/library/agent.py
  - src/vibemix/library/codex_curate.py
autonomous: true
requirements: [WIRE-04]

must_haves:
  truths:
    - "Both curator backends (gemini library/agent.py + codex library/codex_curate.py) source their persona voice from prompts/matrix.py, not a hardcoded literal"
    - "Each curator's non-negotiable grounding RULES (seen-set / no-invented-id) are preserved verbatim after the voice swap"
    - "The matrix v4-byte-identity golden for build_system_instruction is unaffected (curator helper is a NEW sibling function)"
    - "mcp_server.py is confirmed to inherit voice via the codex path (or swapped) — not a silent persona-blind orphan"
  artifacts:
    - path: "src/vibemix/prompts/matrix.py"
      provides: "build_curator_instruction(lens='tutor') -> str — the shared curator voice seam"
      contains: "def build_curator_instruction"
    - path: "src/vibemix/library/agent.py"
      provides: "ViberAgent voice sourced from build_curator_instruction"
    - path: "src/vibemix/library/codex_curate.py"
      provides: "codex _SYSTEM_PROMPT voice sourced from build_curator_instruction"
  key_links:
    - from: "src/vibemix/library/agent.py"
      to: "vibemix.prompts.matrix.build_curator_instruction"
      via: "import + string compose with verbatim RULES block"
      pattern: "build_curator_instruction"
    - from: "src/vibemix/library/codex_curate.py"
      to: "vibemix.prompts.matrix.build_curator_instruction"
      via: "import + string compose with verbatim RULES block"
      pattern: "build_curator_instruction"
---

<objective>
WIRE-04: unify the persona voice across surfaces. Add a NEW `build_curator_instruction` sibling in `prompts/matrix.py` (the single source of truth for voice), then make BOTH curator backends consume it instead of hardcoding their persona opener — while preserving their grounding RULES verbatim. This builds only the persona SEAM; the three-lens-as-modes propagation is Phase 79.

Purpose: closes connection-map wire #3 ("unify persona/lens across surfaces"). The curator stops speaking a divergent hardcoded voice; the co-host's matrix vocabulary becomes the shared origin.
Output: `build_curator_instruction` in matrix.py + voice swap in agent.py + codex_curate.py + a verified note on mcp_server.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-wire-connect-the-islands/77-RESEARCH.md
@.planning/phases/77-wire-connect-the-islands/77-PATTERNS.md

<interfaces>
<!-- Extracted from live source 2026-05-26. Do not re-explore. -->

From src/vibemix/prompts/matrix.py:
- `MOOD_PERSONAS: dict[str,str]` (:51) — fixed anti-injection dict, keys {"hype-man","teacher","coach"}. Charter lens "tutor" maps onto "teacher".
- `_CELLS: dict[tuple[str,str],str]` (:685) and `build_system_instruction(...)` (:698) — the FULL co-host instruction. It appends CITATION_GRAMMAR_BLOCK (:789), IM_LISTENING_FRAGMENT, TTS_TAG_DSL_BLOCK/COACH_TAG_DSL_BLOCK (:808-822). These are co-host-runtime ONLY — they MUST NOT appear in the curator voice.
- v4-byte-identity golden: tests/prompts/test_matrix.py pins build_system_instruction('intermediate','hype'). Do NOT modify _CELLS or MOOD_PERSONAS.

From src/vibemix/library/agent.py:
- `_SYSTEM_INSTRUCTION` (:58-71) — used at `:264` (system_instruction= in _run_loop). RULES block at `:61-70` (seen-set / never-invent-id / keys-from-get_track_features / create_playlist-once) — PRESERVE VERBATIM.
- `_INTERACTIVE_SYSTEM_INSTRUCTION` (:189-202) — used at `:295`; has a `FLOW:` section (:192-202) — preserve verbatim, only swap the persona opener.
- Model resolution at `:224` `model_router.resolve("library_agent")` — DO NOT TOUCH (no model literal).

From src/vibemix/library/codex_curate.py:
- `_SYSTEM_PROMPT` (:60-74) — RULES at `:64-73` (codex rule #3 returns final JSON {name,track_ids,rationale}, differs from agent.py — keep codex's). `_OUTPUT_SCHEMA` (:81-90) preserve verbatim.
- `build_prompt(theme)` (:126-127) = `f"{_SYSTEM_PROMPT}\n\nTheme: {theme.strip()}"` — keep this shape.

From src/vibemix/library/mcp_server.py:
- Research grep (A2): NO system prompt of its own; grounding is at the tool boundary. Verify it inherits voice via the codex path — document the finding.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add build_curator_instruction sibling in prompts/matrix.py</name>
  <files>src/vibemix/prompts/matrix.py</files>
  <read_first>
    - src/vibemix/prompts/matrix.py:46-95, 685-823 (MOOD_PERSONAS, _CELLS, build_system_instruction structure)
    - tests/prompts/test_matrix.py (the v4-byte-identity golden — confirm it pins only build_system_instruction)
    - tests/library/test_curator_persona_seam.py (the Wave-0 stub this task makes green — un-xfail the seam tier)
  </read_first>
  <behavior>
    - build_curator_instruction("tutor") returns a string containing the teacher/tutor persona vocabulary (drawn from MOOD_PERSONAS["teacher"]).
    - The returned string does NOT contain the co-host-only blocks: no TTS tag DSL header, no "CITATION GRAMMAR" header, no IM_LISTENING fragment.
    - Default lens = "tutor" (maps to "teacher"); accepts an override lens; unknown lens raises ValueError (mirror build_system_instruction's unknown-mood guard at matrix.py:761-763).
    - build_system_instruction output is byte-identical to before (NEW function does not touch shared constants).
  </behavior>
  <action>
    Add `def build_curator_instruction(lens: str = "tutor") -> str:` to `prompts/matrix.py` as a sibling of `build_system_instruction`. Map the charter lenses onto the existing matrix vocabulary: tutor→teacher, hype→hype-man, critique→coach (per RESEARCH Open Q2). Compose the persona CHARACTER from `MOOD_PERSONAS[<mapped>]` (the fixed anti-injection dict — NEVER user input) plus a short curator-context framing (this is a text playlist curator, not a live voice co-host). Do NOT append CITATION_GRAMMAR_BLOCK, IM_LISTENING_FRAGMENT, TTS_TAG_DSL_BLOCK, COACH_TAG_DSL_BLOCK, or COACH_CLOSING_BLOCK — those are co-host-runtime concerns wrong for a text curator (per RESEARCH Pattern 2). Guard unknown lens with a ValueError matching the existing unknown-mood pattern. `from __future__ import annotations` already present. Add a docstring explaining WHY this is a separate function (curator voice = persona only, no TTS/citation/co-host blocks). Do NOT modify `_CELLS` or `MOOD_PERSONAS` (protects the v4 golden). Export via the module's existing `__all__` if one is present for these helpers (grep for `__all__`).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/prompts/test_matrix.py tests/library/test_curator_persona_seam.py -k "matrix or curator_instruction or absent or persona"</automated>
  </verify>
  <acceptance_criteria>
    - `build_curator_instruction` importable from `vibemix.prompts.matrix`; default "tutor" returns teacher-vocabulary string.
    - Output asserts: contains teacher persona substring; ABSENT of TTS-tag + CITATION-GRAMMAR headers.
    - `tests/prompts/test_matrix.py` v4-byte-identity golden still GREEN (build_system_instruction unchanged).
    - The Wave-0 "build_curator_instruction exists" xfail flips to a real pass (executor removes that xfail marker).
  </acceptance_criteria>
  <done>build_curator_instruction added; matrix golden intact; curator-voice-exists test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap both curator backends to the shared seam (gemini + codex) + verify mcp_server</name>
  <files>src/vibemix/library/agent.py, src/vibemix/library/codex_curate.py, src/vibemix/library/mcp_server.py</files>
  <read_first>
    - src/vibemix/library/agent.py:58-71, 189-228, 255-322 (voice constants, RULES, call sites :264/:295)
    - src/vibemix/library/codex_curate.py:60-127 (_SYSTEM_PROMPT, RULES, build_prompt, _OUTPUT_SCHEMA)
    - src/vibemix/library/mcp_server.py (full — confirm NO own system prompt; locate where it invokes codex)
    - tests/library/test_curator_persona_seam.py + tests/library/test_agent.py + tests/library/test_codex_curate.py
  </read_first>
  <action>
    In `library/agent.py`: replace the persona-opener body of `_SYSTEM_INSTRUCTION` (:58-71) with `build_curator_instruction("tutor") + "\n" + <the existing verbatim RULES block (agent.py:61-70)>`. Keep the RULES text byte-for-byte. Do the same for `_INTERACTIVE_SYSTEM_INSTRUCTION` (:189-202): swap ONLY the persona opener (:190-191) for the curator seam; keep the `FLOW:` section (:192-202) verbatim. Import `build_curator_instruction` from `vibemix.prompts.matrix`. Call sites at `:264`/`:295` unchanged. Do NOT touch `model_router.resolve("library_agent")` at `:224`.
    In `library/codex_curate.py`: replace the persona opener of `_SYSTEM_PROMPT` (:60-74) the same way — `build_curator_instruction("tutor") + "\n" + <verbatim codex RULES block (:64-73)>`. Keep codex rule #3 (final JSON output) and `_OUTPUT_SCHEMA` (:81-90) verbatim. `build_prompt` (:126-127) shape unchanged.
    In `library/mcp_server.py`: VERIFY (per RESEARCH A2 / PATTERNS verify-only) that it has no own system prompt and inherits voice via the codex path. If confirmed, add a one-line code comment noting "voice inherited via codex_curate seam (WIRE-04 verified — no own prompt)" and document the finding in the SUMMARY. If it DOES carry a hidden voice string, swap it to the seam the same way (do NOT leave it persona-blind — CURATE acid test).
    Keep persona sourced from the FIXED matrix seam, never user input (anti-prompt-injection, V5).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/library/test_agent.py tests/library/test_codex_curate.py tests/library/test_curator_persona_seam.py</automated>
  </verify>
  <acceptance_criteria>
    - `library.agent._SYSTEM_INSTRUCTION` + `_INTERACTIVE_SYSTEM_INSTRUCTION` voice sourced from `build_curator_instruction`; RULES + FLOW preserved verbatim.
    - `library.codex_curate._SYSTEM_PROMPT` voice sourced from the seam; codex RULES + `_OUTPUT_SCHEMA` verbatim.
    - Wave-0 voice-from-seam xfail flips to real pass; RULES-preserved pin stays green.
    - mcp_server.py: documented verified-no-change (or swapped). Not a silent orphan.
    - Model-literal CI gate `tests/repo/test_model_literal_gate.py` still green (no model name introduced).
  </acceptance_criteria>
  <done>Both curator backends read the shared seam; RULES intact; mcp_server status documented; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user theme/persona → curator prompt | Theme is user input; persona must come from the fixed matrix dict, never user-controllable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-02-01 | Tampering | Prompt injection via curator persona | mitigate | build_curator_instruction draws ONLY from the fixed MOOD_PERSONAS dict (matrix.py:46 anti-injection note); theme stays in build_prompt's data slot, never the system voice. |
| T-77-02-02 | Spoofing/Tampering | Hallucinated track id smuggled into a playlist | mitigate | Curator grounding RULES (seen-set / never-invent-id) preserved verbatim after the voice swap — the Wave-0 RULES-preserved pin enforces this (V5 input validation). |
| T-77-02-03 | Tampering | Curator backend left persona-blind (orphan) | mitigate | mcp_server.py explicitly verified/swapped; both backends provably read the seam (CURATE acid test). |
| T-77-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs (RESEARCH Package Legitimacy Audit = N/A). |
</threat_model>

<verification>
- Quick: `PYTHONPATH=src python3 -m pytest -q tests/prompts tests/library`.
- Model-literal gate: `PYTHONPATH=src python3 -m pytest -q tests/repo/test_model_literal_gate.py`.
- Full suite stays green (no API key; curator tests use existing fakes).
</verification>

<success_criteria>
- `build_curator_instruction` is the single curator-voice source; both backends consume it.
- Grounding RULES + codex output schema preserved verbatim.
- matrix v4 golden untouched; cold path byte-identical.
- mcp_server.py persona-status documented (verified or swapped).
</success_criteria>

<output>
Create `.planning/phases/77-wire-connect-the-islands/77-02-SUMMARY.md` when done. Record the lens→mood mapping chosen and the mcp_server.py verification finding (A2 resolution).
</output>
