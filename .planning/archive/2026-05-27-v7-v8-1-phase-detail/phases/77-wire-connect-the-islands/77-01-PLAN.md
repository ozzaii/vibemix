---
phase: 77-wire-connect-the-islands
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/agent/test_dj_cohost_grounding.py
  - tests/memory/test_ingest_wiring.py
  - tests/library/test_curator_persona_seam.py
  - tests/runtime/test_load_env_override.py
  - tests/repo/test_wire_regression_pins.py
autonomous: true
requirements: [WIRE-01, WIRE-02, WIRE-03, WIRE-04, WIRE-05, WIRE-06]

must_haves:
  truths:
    - "Every WIRE wire (01/04/05/06) has a failing test that turns green only when the wire is implemented"
    - "WIRE-02/03 already-shipped behavior is locked by a regression pin that fails if the detector evidence or detected_genre surfacing silently regresses"
    - "All new tests run with NO Gemini API key (honest green — fakes + source-text gates only)"
  artifacts:
    - path: "tests/agent/test_dj_cohost_grounding.py"
      provides: "WIRE-01 failing stubs: grounding kwarg/setter, off-loop dispatch, cited [track:<id>] injection, cold-path byte-identity"
    - path: "tests/library/test_curator_persona_seam.py"
      provides: "WIRE-04 failing stubs: curator voice sourced from matrix seam, grounding RULES preserved"
    - path: "tests/runtime/test_load_env_override.py"
      provides: "WIRE-06 failing stub: decoy GEMINI_API_KEY env var overridden by .env"
    - path: "tests/repo/test_wire_regression_pins.py"
      provides: "WIRE-02/03 regression pins (lock shipped behavior)"
  key_links:
    - from: "tests/memory/test_ingest_wiring.py"
      to: "vibemix.__main__ main() boot/close ingest"
      via: "source-text + behavioral assertion of _fire_ingest on the live path"
      pattern: "_fire_ingest"
---

<objective>
Create the failing test scaffolds (Wave 0) that the four implementation plans (02/03/04) make green, plus the WIRE-02/03 regression pins. Tests are written FIRST so each wire has an automated proof before any production code changes — and so the cold-path byte-identity guarantee is captured as an assertion, not a hope.

Purpose: Nyquist compliance — every WIRE-0x requirement maps to an automated check that runs without a Gemini API key. WIRE-02/03 are already shipped (`ccf4930`/`a9979b8`); this plan pins them so they cannot silently regress.
Output: 4 new/extended test files + 1 regression-pin file. ALL must be RED (or xfail-marked) at end of this plan for the unimplemented wires, and GREEN for the WIRE-02/03 pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-wire-connect-the-islands/77-RESEARCH.md
@.planning/phases/77-wire-connect-the-islands/77-PATTERNS.md
@.planning/phases/77-wire-connect-the-islands/77-VALIDATION.md

<interfaces>
<!-- Contracts the test author needs — extracted from live source 2026-05-26. No codebase exploration needed. -->

From src/vibemix/library/grounding.py:
- `TRACK_AWARE_EVENTS: frozenset[str]` (matrix.py:46) = {"TRACK_CHANGE","LAYER_ARRIVAL","MIX_MOVE"}
- `class Grounding` (:169): `on_event(event_type, audio_bytes, *, event_id=None, mime_type="audio/wav")` (:188), `get_latest_citation() -> Citation | None` (:211), `clear()` (:216)
- `Citation.is_cited` property; `Citation.track_id` populated only when cosine >= 0.7

From src/vibemix/agent/dj_cohost.py:
- `DJCoHostAgent.__init__` (:340) — kwargs-only; recall precedent at `recall=`/`recall_enabled=` (:420)
- `set_next_event(ev)` (:826) calls `self._maybe_dispatch_recall(ev)`
- `_maybe_dispatch_recall(ev)` (:855) — the off-loop dispatch template
- `self._clean_audio_buf` (:449) — WAV source already held by the agent
- turn-end recall clear at `:2118` (`if self._recall_enabled and self._recall is not None: self._recall.clear()`)

From src/vibemix/__main__.py:
- `_load_env_robust()` (:117-182) — two `load_dotenv(..., override=False)` at `:173` + `:182`
- `recall_enabled` resolved at `:1024` from `VIBEMIX_RECALL_ENABLED`
- `_session_ipc = SessionLoop(...)` built at `:1210`, `register_handlers()` at `:1221`, NEVER `run()`

From src/vibemix/runtime/session_loop.py:
- `_fire_ingest(self, trigger: str, *, session_dir: Path | None = None)` (:862) — ingest-only; None-guards on `recordings_root is None`
- `run_boot_sweeps` (:689) + `on_session_close` (:725) — DO NOT call (double-retention)

From src/vibemix/prompts/matrix.py:
- `MOOD_PERSONAS: dict[str,str]` (:51) keys = {"hype-man","teacher","coach"}
- `build_system_instruction(...)` (:698) — the FULL co-host instruction (NOT for curator)
- NEW (this phase): `build_curator_instruction(lens: str = "tutor") -> str` to be added in Plan 02

From src/vibemix/library/agent.py:
- `_SYSTEM_INSTRUCTION` (:58), `_INTERACTIVE_SYSTEM_INSTRUCTION` (:189); RULES block at `:61-70`

From src/vibemix/library/codex_curate.py:
- `_SYSTEM_PROMPT` (:60), `build_prompt(theme)` (:126); RULES at `:64-73`, `_OUTPUT_SCHEMA` (:81)
</interfaces>

<!-- Test template to mirror — read it in full before writing the WIRE-01/05 off-loop tests. -->
@tests/memory/test_ingest_wiring.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: WIRE-01 + WIRE-05 off-loop wiring test scaffolds</name>
  <files>tests/agent/test_dj_cohost_grounding.py, tests/memory/test_ingest_wiring.py</files>
  <read_first>
    - tests/memory/test_ingest_wiring.py (the two-tier template: source-text gate + behavioral fakes, off-loop thread-id proof)
    - src/vibemix/agent/dj_cohost.py:340-460, 826-965, 2095-2123 (constructor, set_next_event, _maybe_dispatch_recall, turn-end clear)
    - src/vibemix/library/grounding.py:46-48, 169-220 (TRACK_AWARE_EVENTS, Grounding contract)
    - src/vibemix/__main__.py:1199-1228, 1340-1380 (SessionLoop build site, close region)
  </read_first>
  <action>
    Create `tests/agent/test_dj_cohost_grounding.py` (WIRE-01), mirroring the two-tier structure of `tests/memory/test_ingest_wiring.py`:
    (a) SOURCE-TEXT GATE tier — read `dj_cohost.py` source, assert it references `grounding`/`get_latest_citation` AND that the grounding dispatch appears as a `run_in_executor` argument (off-loop static proof — NO inline embed). This tier RUNS GREEN ONLY after Plan 04 lands; mark the body `@pytest.mark.xfail(reason="WIRE-01 not yet wired (Plan 04)", strict=True)` so the suite stays green now and flips to a real pass after implementation (executor removes xfail in Plan 04).
    (b) BEHAVIORAL tier — construct `DJCoHostAgent` with a fake `Grounding` (duck-typed: records the thread-id its `on_event` runs on; `get_latest_citation` returns a fake cited Citation with a known `track_id`). Drive `set_next_event` with a TRACK_CHANGE Event under a running loop; assert the fake's `on_event` ran on an executor thread (id != loop thread id). Add a cold-path test: agent built with `grounding=None` behaves byte-identical (no dispatch attempted). Also `@pytest.mark.xfail(strict=True)` the dispatch-fires assertions until Plan 04.
    (c) CITATION-GATE tier — assert that a `[track:<id>]` for an id seeded via `register_library` resolves in `EvidenceRegistry.has("track", id, t)` (this can be GREEN now — it tests the existing registry contract, proving the injection target survives the linter). NO API key — use fakes only; never construct `genai.Client`.
    Extend `tests/memory/test_ingest_wiring.py` (WIRE-05): add a NEW test class/function asserting the LIVE `main()` path (vibemix.__main__ source-text tier) — that `_session_ipc._fire_ingest("boot")` and a close-path `_fire_ingest("close", session_dir=...)` call appear in `__main__.py` source AND are gated behind `recall_enabled`, AND that `run_boot_sweeps(`/`on_session_close(` do NOT appear (anti-double-retention guard). Mark `@pytest.mark.xfail(strict=True, reason="WIRE-05 main()-path ingest not yet wired (Plan 04)")`. Keep the existing SessionLoop-level tests untouched and green.
    Use `from __future__ import annotations`; SPDX header `# SPDX-License-Identifier: Apache-2.0`.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/agent/test_dj_cohost_grounding.py tests/memory/test_ingest_wiring.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/test_dj_cohost_grounding.py` exists; xfail(strict) markers on the not-yet-wired assertions; the registry-resolves-track-id test passes green.
    - `tests/memory/test_ingest_wiring.py` gains a main()-path xfail(strict) test asserting `_fire_ingest` boot+close gated on `recall_enabled` and NO `run_boot_sweeps`/`on_session_close`.
    - Suite is GREEN now (xfail counts as pass; strict ensures it flips to a hard failure if accidentally implemented wrong). No `genai.Client` constructed; runs with no API key.
  </acceptance_criteria>
  <done>WIRE-01 + WIRE-05 failing scaffolds committed; xfail(strict) on unimplemented assertions; registry + cold-path checks green.</done>
</task>

<task type="auto">
  <name>Task 2: WIRE-04 curator-seam + WIRE-06 env-override test scaffolds</name>
  <files>tests/library/test_curator_persona_seam.py, tests/runtime/test_load_env_override.py</files>
  <read_first>
    - src/vibemix/library/agent.py:58-71, 189-228, 255-322 (hardcoded voice constants + RULES + call sites)
    - src/vibemix/library/codex_curate.py:60-127 (system prompt + build_prompt + RULES)
    - src/vibemix/prompts/matrix.py:46-95, 685-823 (MOOD_PERSONAS, _CELLS, build_system_instruction)
    - src/vibemix/__main__.py:117-185 (_load_env_robust + diagnostic print)
  </read_first>
  <action>
    Create `tests/library/test_curator_persona_seam.py` (WIRE-04):
    (a) Assert (source-text + import) that after Plan 02, `prompts.matrix` exposes a callable `build_curator_instruction` (import inside an xfail(strict) block until Plan 02). Assert its output (when implemented) contains the teacher/tutor persona vocabulary AND does NOT contain the co-host-only blocks (no TTS tag DSL marker, no CITATION GRAMMAR marker — assert those substrings are ABSENT). Use stable substrings from `MOOD_PERSONAS["teacher"]` and from the TTS/citation block headers.
    (b) Assert the curator GROUNDING RULES survive: `library.agent._SYSTEM_INSTRUCTION` and `library.codex_curate._SYSTEM_PROMPT` still contain the verbatim "RULES (non-negotiable)" / seen-set / "Never invent a track_id" contract substrings. This tier can be GREEN now (the rules exist today) — it LOCKS that Plan 02's voice swap preserves them.
    (c) Assert (xfail(strict) until Plan 02) that the curator voice is SOURCED from the matrix seam — e.g. `library.agent._SYSTEM_INSTRUCTION` starts with / contains the `build_curator_instruction("tutor")` output prefix.
    Create `tests/runtime/test_load_env_override.py` (WIRE-06): set `os.environ["GEMINI_API_KEY"]="DECOY"`, write a temp `.env` with `GEMINI_API_KEY=REAL_FUNDED` at a path `_load_env_robust` probes (use monkeypatch/chdir to a tmp dir or patch the candidate path list), call `_load_env_robust()`, assert `os.environ["GEMINI_API_KEY"] == "REAL_FUNDED"`. Mark `@pytest.mark.xfail(strict=True, reason="WIRE-06 override=True not yet flipped (Plan 03)")`. Add a SECOND green test asserting the diagnostic print path NEVER emits the key VALUE (capture stdout/stderr, assert the literal key string is absent) — this is the security pin (V7 secrets) and is true today and must stay true.
    Restore `os.environ` in teardown (fixture). SPDX header + `from __future__ import annotations`.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/library/test_curator_persona_seam.py tests/runtime/test_load_env_override.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/library/test_curator_persona_seam.py` exists: RULES-preserved tier green; voice-from-seam tier xfail(strict).
    - `tests/runtime/test_load_env_override.py` exists: override tier xfail(strict); key-not-logged security tier green.
    - Suite green (xfail=pass). Env mutated only inside fixtures with teardown restore. No API key needed.
  </acceptance_criteria>
  <done>WIRE-04 + WIRE-06 scaffolds committed; security key-not-logged pin green; unimplemented assertions xfail(strict).</done>
</task>

<task type="auto">
  <name>Task 3: WIRE-02 + WIRE-03 regression pins (lock shipped behavior — no new logic)</name>
  <files>tests/repo/test_wire_regression_pins.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md (WIRE-02/03 shipped commits ccf4930 + a9979b8)
    - src/vibemix/state/event_detector.py + src/vibemix/state/coach.py (genre-chain detector evidence + evidence_line genre field) — grep for the detector evidence + `detected_genre` / `genre=` surfacing to locate the existing assertions
    - existing tests under tests/state/ touching genre + detector evidence (grep `genre`/`detected_genre`)
  </read_first>
  <action>
    Create `tests/repo/test_wire_regression_pins.py`. This is a PIN, not new functionality — it locks the two already-shipped wires against silent regression:
    (a) WIRE-02 pin: assert the 8 genre-chain detectors' measured evidence surfaces to the prompt AND registers in `EvidenceRegistry`. Locate the existing mechanism (grep the codebase for the detector evidence surfacing — likely `coach.py task_for_event` branch + `event_detector.py` `_fire` for the genre-chain types). Write an assertion that proves a genre-chain detected event produces a non-empty task/evidence (NOT the "React naturally." fallback) and that its evidence registers. Reuse existing test fixtures/helpers — do NOT rebuild the detector; just assert its output is wired.
    (b) WIRE-03 pin: assert `coach.py` `evidence_line` (or the equivalent prompt-evidence builder) emits a `genre=<name>(conf)` field when `MusicState.detected_genre` is set above the confidence floor, and OMITS it (no `genre=unknown` spam) when below floor / unset. Use the existing confidence-gating logic — assert both branches.
    These tests MUST be GREEN immediately (the behavior is shipped). If grep reveals an existing test already covers this exactly, this file adds a thin guard that imports/asserts the public surface so a future refactor that drops it fails here. SPDX header + `from __future__ import annotations`. No API key.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/repo/test_wire_regression_pins.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/repo/test_wire_regression_pins.py` exists and is GREEN (shipped behavior holds).
    - WIRE-02 pin: a genre-chain event yields real evidence/task (not "React naturally.") and registers in EvidenceRegistry.
    - WIRE-03 pin: `genre=<name>(conf)` present above floor, ABSENT below floor (no `genre=unknown`).
    - No re-implementation of WIRE-02/03 logic — assertions only against the existing public surface.
  </acceptance_criteria>
  <done>WIRE-02/03 regression pins committed and green; both shipped behaviors locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → repo source | Tests read source text; no untrusted input. No network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01-01 | Tampering | xfail markers hiding a real regression | mitigate | Use `xfail(strict=True)` so an accidentally-passing xfail becomes a hard failure — the marker cannot silently mask a wired/mis-wired wire. |
| T-77-01-02 | Information Disclosure | test fixtures writing a real API key to a temp `.env` | mitigate | Use the literal string `REAL_FUNDED` (a decoy), never the real funded key; teardown restores `os.environ`. The WIRE-06 security pin asserts the key VALUE is never logged. |
| T-77-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this phase (RESEARCH.md Package Legitimacy Audit = N/A); no slopcheck required. |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest -q tests/agent/test_dj_cohost_grounding.py tests/memory/test_ingest_wiring.py tests/library/test_curator_persona_seam.py tests/runtime/test_load_env_override.py tests/repo/test_wire_regression_pins.py` — all green (xfail=pass, pins=pass).
- Full suite `PYTHONPATH=src python3 -m pytest -q` stays green (4449 + new, 0 fail).
- No `genai.Client` constructed anywhere; runs with no `GEMINI_API_KEY`.
</verification>

<success_criteria>
- 4 test files created/extended + 1 regression-pin file.
- Every unimplemented wire (01/04/05/06) has an `xfail(strict=True)` assertion that flips to a real pass when its implementation plan lands.
- WIRE-02/03 pins are green now and fail loudly on regression.
- Honest green: zero API dependency.
</success_criteria>

<output>
Create `.planning/phases/77-wire-connect-the-islands/77-01-SUMMARY.md` when done. Record which assertions are xfail (to be un-xfailed by Plans 02/03/04) and the exact grep findings for the WIRE-02/03 existing mechanism.
</output>
