---
phase: 81-bench-the-validation-instrument
plan: 02
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - src/vibemix/bench/__init__.py
  - src/vibemix/bench/cell.py
  - src/vibemix/bench/matrix.py
  - src/vibemix/bench/fixtures.py
  - src/vibemix/bench/assemble.py
  - src/vibemix/bench/run.py
  - src/vibemix/__main__.py
autonomous: true
requirements: [BENCH-01]
must_haves:
  truths:
    - "A BenchCell (one 6-D matrix point) assembles into ONE prompt + call config by reusing the REAL product seams — never re-authoring prompt text"
    - "The no-audio (dsp_only) cell sends ZERO audio Parts but a fully populated evidence body — the milestone's empirical-heart cell"
    - "A full study sweep records N cells via an INJECTED client; the fake client makes it offline-green with zero API"
    - "A per-cell API error records {error:...} and the sweep continues — never fabricated, never aborted (the 429 fail-safe)"
    - "Every model reference resolves via model_router.resolve(alias) — zero hardcoded Gemini literals under bench/"
  artifacts:
    - path: "src/vibemix/bench/cell.py"
      provides: "BenchCell (6-D point) + BenchResult (recorded output + dsp_snapshot + error) frozen dataclasses"
      contains: "class BenchCell"
    - path: "src/vibemix/bench/matrix.py"
      provides: "STUDY_A (arch-axis, 1 model) + STUDY_B (model-axis) + NO_AUDIO_CELL + TASTE_RUBRIC placeholder + LENS_ANCHORS"
      contains: "TASTE_RUBRIC"
    - path: "src/vibemix/bench/assemble.py"
      provides: "build_cell_prompt(cell) -> (system, contents, model, tier) composing the real seams"
      exports: ["build_cell_prompt"]
    - path: "src/vibemix/bench/fixtures.py"
      provides: "fixture_state_for(contexting, grounding) -> (MusicState, snapshot)"
      exports: ["fixture_state_for"]
    - path: "src/vibemix/bench/run.py"
      provides: "run_study(cells, client, ...) -> list[BenchResult] + the `vibemix bench` CLI + per-cell fail-safe + SessionMeter feed"
      exports: ["run_study"]
  key_links:
    - from: "src/vibemix/bench/assemble.py"
      to: "model_router.resolve"
      via: "model, tier = resolve(cell.model_path)"
      pattern: "model_router\\.resolve|resolve\\(cell"
    - from: "src/vibemix/bench/run.py"
      to: "library/budget.py SessionMeter"
      via: "record(cell.model_path, prompt=, cached=, output=)"
      pattern: "SessionMeter|get_session_meter|\\.record\\("
    - from: "src/vibemix/bench/assemble.py"
      to: "prompts/matrix.py + state/coach.py"
      via: "build_lens_instruction + build_prompt + build_parts_description"
      pattern: "build_lens_instruction|build_prompt|build_parts_description"
---

<objective>
Build the BENCH-01 harness as THIN COMPOSITION over the real Phase-77→80 product seams: a `BenchCell` (one point in the model × grounding × prompting × contexting × lens × taste matrix) → an assembler that builds ONE prompt by calling `build_lens_instruction` / `build_system_instruction` flags / `AICoach.build_prompt` / `build_parts_description` / `model_router.resolve` (NEVER re-authoring prompt text) → a runner that issues one injected-client `generate_content` per cell with a per-cell 429 fail-safe → a recorder that writes each cell verbatim. The harness EXPRESSES the full 6-D matrix; the documented RUNS are the two focused studies + the decisive no-audio cell.

Purpose: Measure the ACTUAL shipped product across the architecture/model axes, not a toy — the empirical instrument that proves the One Mind thesis.
Output: `src/vibemix/bench/` (cell/matrix/fixtures/assemble/run) + the `vibemix bench` CLI; flips the Plan-01 BENCH-01 xfails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/81-bench-the-validation-instrument/81-RESEARCH.md
@.planning/phases/81-bench-the-validation-instrument/81-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The seams the assembler composes — verified live (PATTERNS.md). REUSE; never re-author. -->
build_lens_instruction(lens="hype", skill="intermediate", **kw) -> str            # prompts/matrix.py:964
build_system_instruction(skill, mode, mood, *, include_citation_grammar=True,
    include_listening_fallback=True, include_tag_dsl=True) -> str                 # prompts/matrix.py:737
build_parts_description(audio_seconds, has_mic_part, has_lookahead_part,
    secondary_ear=False) -> str                                                   # prompts/matrix.py:599
LENS_TO_MODE_MOOD: dict[str, tuple[str,str]]                                       # prompts/matrix.py:950
AICoach.build_prompt(ev, *, registry_snapshot=None, recall_moments=None, diet=False) -> str  # state/coach.py:730
AICoach.evidence_line(state, *, registry_snapshot=None) -> str                     # state/coach.py:257
EvidenceRegistry().snapshot() -> dict[str, dict[str, tuple[float,...]]]            # evidence_registry.py:322
model_router.resolve(path) -> tuple[str, ServiceTier|None]                        # llm/model_router.py:44
SessionMeter.record(path, *, prompt=0, cached=0, output=0); .summary(); get_session_meter()  # library/budget.py:206/276/329

Frozen-dataclass convention (copy): coach/citation_linter.py:56 LintResult(@dataclass(frozen=True))
Injected-client + wall-clock timeout idiom: library/agent.py:306/335 _gemini_call
Fail-safe + JSON-record pattern to fold in: /tmp/truthtest/bench_models.py:31-61 (`except → {"error": repr(e)[:160]}`)
CLI dispatch precedent: __main__.py:1533 _build_library_subparsers / :1669 _run_library_cli
</interfaces>

@/tmp/truthtest/run_test.py
@/tmp/truthtest/bench_models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cell.py + matrix.py + fixtures.py + assemble.py — the prompt composer</name>
  <files>src/vibemix/bench/__init__.py, src/vibemix/bench/cell.py, src/vibemix/bench/matrix.py, src/vibemix/bench/fixtures.py, src/vibemix/bench/assemble.py</files>
  <behavior>
    - A hype `BenchCell` assembles a system instruction == `build_lens_instruction("hype", skill)` output (+ TASTE_RUBRIC iff taste=="with_rubric") — reused, not re-authored
    - The `dsp_only` (no-audio) cell: `contents` has ZERO audio Parts but a NON-EMPTY evidence body (from a populated MusicState + snapshot)
    - A `structured` cell passes `registry_snapshot=snap` to `build_prompt`; a `generic` cell passes `None` and uses `include_*=False` (or the documented bare /tmp/truthtest PROMPT — the ONLY allowed literal, labeled the no-structure control)
    - The model is `model_router.resolve(cell.model_path)` — NO Gemini literal anywhere under bench/
    - `fixture_state_for("snapshot", g)` → a cold MusicState (trajectory_narrative="", no phase_history/long_arc); `fixture_state_for("trajectory", g)` → the same with PERCEIVE-02 multi-scale fields populated
  </behavior>
  <read_first>
    - 81-PATTERNS.md §"bench/assemble.py — THE HEART" (the verified composition order, lines 108-122) + §"bench/cell.py" (BenchResult MUST carry `dsp_snapshot: dict[str,dict[str,tuple[float,...]]] | None`, `prompt`, `output`, `usage`, `error`) + §"bench/fixtures.py" (build a REAL MusicState, not dicts) + §"Shared Patterns: Fixed-constant prompt fragments" (TASTE_RUBRIC + LENS_ANCHORS are FIXED module constants — anti-injection)
    - src/vibemix/prompts/matrix.py:599-737, :950-990 (build_parts_description / build_system_instruction flags / LENS_TO_MODE_MOOD / build_lens_instruction — the seams; read once, extract signatures)
    - src/vibemix/state/coach.py:257-340 (evidence_line gating) + :730-800 (build_prompt) — the contexting/grounding seam
    - src/vibemix/state/music_state.py (the MusicState dataclass fields, esp. trajectory_narrative / phase_history / long_arc / detected_genre / genre_confidence — what the fixtures populate)
    - src/vibemix/state/evidence_registry.py:322 (snapshot shape) + how events register so the fixture snapshot is non-empty
  </read_first>
  <action>
    `__init__.py`: empty package marker (mirror `runtime/__init__.py`). `cell.py`: `@dataclass(frozen=True) BenchCell` with fields `model_path: str`, `grounding: str` (raw_audio|dsp_only|audio+dsp|audio+dsp+traj+genre), `prompting: str` (generic|structured), `contexting: str` (snapshot|trajectory), `lens: str` (hype|critique|tutor), `taste: str` (with_rubric|without_rubric), `skill: str = "intermediate"`, `track: str | None = None`; and `@dataclass(frozen=True) BenchResult` carrying `cell: BenchCell`, `prompt: str`, `output: str`, `dsp_snapshot: dict | None`, `usage: dict`, `error: str | None`. `matrix.py`: define `STUDY_A` (architecture-axis — sweep grounding × prompting × contexting × lens × taste at ONE fixed `model_path` alias, e.g. "live_coach"; ~12-24 cells), `STUDY_B` (model-axis — the best-arch placeholder cell swept over the plausible reaction-model router aliases "live_coach"/"library_auto_tag"), and `NO_AUDIO_CELL` (the single decisive dsp_only cell). Add a TINY FIXED `TASTE_RUBRIC` PLACEHOLDER string with a clear marker comment `# KAAN-ACTION: placeholder taste rubric — final wording is Kaan's IP (Open Q1). Do NOT author the real reward-signal wording here.` and `LENS_ANCHORS: dict[str, tuple[str,...]]` (hype/critique/tutor vocab anchors per RESEARCH §271). NO model literal — `STUDY_*` stores router ALIAS strings only. `fixtures.py`: `fixture_state_for(contexting, grounding) -> (MusicState, snapshot)` building a real `MusicState` and registering events into a fresh `EvidenceRegistry` so `.snapshot()` is non-empty; populate trajectory fields only when `contexting=="trajectory"`, genre only when grounding includes `+genre`. `assemble.py`: `build_cell_prompt(cell, *, coach, audio_seconds=80.0) -> tuple[str, list, str, object]` composing in the verified order — `system = build_lens_instruction(cell.lens, cell.skill, include_citation_grammar=(cell.prompting=="structured"), ...) + (TASTE_RUBRIC if cell.taste=="with_rubric" else "")`; build the Event + `user_body = coach.build_prompt(ev, registry_snapshot=(snap if structured else None))`; `parts_suffix = build_parts_description(audio_seconds, False, False, secondary_ear=(cell.grounding in {"audio+dsp","audio+dsp+traj+genre"}))`; `contents = [system + user_body + parts_suffix] + ([audio_part] if cell.grounding in AUDIO_GROUNDINGS else [])`; `model, tier = model_router.resolve(cell.model_path)`. The dsp_only branch appends NO audio Part. Flip the Plan-01 `test_assemble.py` xfails to real-green.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/bench/test_assemble.py tests/bench/test_no_model_literal.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/bench/test_assemble.py -x` passes real-green (xfails flipped, no xpass)
    - `pytest tests/bench/test_assemble.py::test_no_audio_cell -x` passes: dsp_only cell `contents` has zero audio Parts + non-empty evidence body
    - `test_no_model_literal.py` stays green AND `bash scripts/release/check_no_hardcoded_model.sh` (or `tests/repo/test_model_literal_gate.py`) reports zero literals under `bench/`
    - `grep -rn` for any Gemini model-id literal (e.g. `gemini-`) under `src/vibemix/bench/` returns nothing except inside an alias-resolution comment
    - `TASTE_RUBRIC` carries the KAAN-ACTION placeholder marker comment
  </acceptance_criteria>
  <done>cell/matrix/fixtures/assemble exist; the assembler composes the real seams; the no-audio cell is correct; zero model literals; assemble xfails flipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run.py — injected-client runner, per-cell fail-safe, recorder, SessionMeter, CLI</name>
  <files>src/vibemix/bench/run.py, src/vibemix/__main__.py</files>
  <behavior>
    - `run_study(cells, client=fake)` records one BenchResult per cell with zero network (fake client) — prompt/output/dsp_snapshot/usage populated, error None
    - With a raising client, a per-cell error sets `result.error` (non-None), `output==""`, and the sweep CONTINUES to the next cell (no abort, no fabricated output)
    - The recorder writes the cells verbatim to a JSON artifact (`json.dumps(..., indent=2, ensure_ascii=False)`)
    - On a successful real call, `SessionMeter.record(cell.model_path, prompt=usage.prompt_token_count, cached=..., output=...)` is fed (cost bounding)
  </behavior>
  <read_first>
    - 81-PATTERNS.md §"bench/run.py" (Analog A injected-client+timeout `library/agent.py:335`; Analog B the `except → {"error": repr(e)[:160]}` per-cell fail-safe from `/tmp/truthtest/bench_models.py:155-171` — MANDATORY; the SessionMeter feed)
    - /tmp/truthtest/bench_models.py (the for-model/for-track loop + `Part.from_bytes(mime_type="audio/mp3")` audio-Part construction + the JSON write)
    - src/vibemix/library/budget.py:206/276/329 (record / summary / get_session_meter signatures)
    - src/vibemix/__main__.py:1533-1673 (`_build_library_subparsers` + `_run_library_cli` — the argparse-subcommand precedent + how argv routes; the `argv[0] == "library"` dispatch guard at :200-201)
  </read_first>
  <action>
    `run.py`: `run_study(cells, *, client, coach, data_dir=None, results_path=None) -> list[BenchResult]`. For each cell: `system, contents, model, tier = build_cell_prompt(cell, coach=coach)`; if the grounding uses audio, load the cell's track `.mp3` from the env-overridable `bench_data_dir` and append a `types.Part.from_bytes(data=..., mime_type="audio/mp3")` (mirror `/tmp/truthtest`); wrap the single `client.models.generate_content(model=model, contents=contents, config=...)` in `try/except Exception as e:` → on success record `BenchResult(..., output=resp.text, usage={...from resp.usage_metadata...}, error=None)` AND feed `get_session_meter().record(cell.model_path, prompt=usage.prompt_token_count, cached=usage.cached_content_token_count, output=usage.candidates_token_count)`; on error record `BenchResult(..., output="", usage={}, error=repr(e)[:160])` and CONTINUE (never abort, never fabricate). Write all results to `results_path` (default `bench_run.json` under a results dir) via `json.dumps(..., indent=2, ensure_ascii=False)`. Add a `bench` argparse subcommand to `__main__.py`: extend the `argv[0]` dispatch guard to also recognize `"bench"` (mirror the `"library"` guard at :200-201 so no startup banner is prepended), add a `_build_bench_subparsers` + `_run_bench_cli(argv)` mirroring `_run_library_cli`, with `bench run --study {A|B|no-audio} [--tracks ...]` that constructs a REAL `genai.Client(api_key=os.environ["GEMINI_API_KEY"])` (reuse the `_library_genai_client` direct-first idiom), runs the study, and prints `get_session_meter().summary()`. Document the produce command in a module docstring: `uv run python -m vibemix bench run --study A`. Per-cell fail-safe wraps the live call so a 429/auth/network error parks the cell as KAAN-ACTION and never blocks. Flip the Plan-01 `test_run_fake.py` xfails to real-green.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/bench/test_run_fake.py -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/bench/test_run_fake.py -x` passes real-green (sweep with fake client records N cells, zero network)
    - The fail-safe test passes: a raising client yields a result with `error` set, `output==""`, and the sweep completes all cells
    - `python3 -m vibemix bench --help` exits 0 and lists `run` (CLI wired; no startup banner — argv guard extended)
    - No Gemini literal under `bench/` (`test_no_model_literal.py` green); the real client is built only inside the CLI path, never in `run_study` (which takes the client by injection)
    - The recorder writes valid JSON parseable by `json.loads`
  </acceptance_criteria>
  <done>run_study + the fail-safe + recorder + SessionMeter feed + the `vibemix bench` CLI exist; offline sweep green; the real run is one documented command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bench/run → google-genai (real run only) | the only network surface; reuses the existing funded `.env` key; the injected fake client makes the test gate network-free |
| bench code → filesystem | reads read-only `.mp3` excerpts (non-PII music); writes a fresh `bench_run.json` (prompt+output+usage only) |
| matrix.py constants → system prompt | TASTE_RUBRIC + LENS_ANCHORS are FIXED module constants, never user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03 | Tampering | prompt-injection via a dynamic taste/lens string | mitigate | TASTE_RUBRIC + LENS_ANCHORS are fixed module constants (mirror MOOD_PERSONAS / build_curator_instruction T-77-02-01); the studies are code-defined cells, no user input |
| T-81-04 | Information disclosure | API key leaking into `bench_run.json` | mitigate | the recorder writes prompt+output+usage_metadata ONLY — never the key; path-scrub track filenames if they carry personal paths (mirror the Telegram path-scrub) |
| T-81-05 | Repudiation / integrity | a fabricated cell filling a 429 gap (the cardinal sin) | mitigate | per-cell `except → {error: repr(e)[:160]}` records the parked error; the sweep never fabricates output, never aborts (RESEARCH Pitfall 2) |
| T-81-06 | Tampering | a Gemini model literal slipping into bench/ | mitigate | every model via `model_router.resolve(alias)`; `bench/` NOT in the gate allowlist; `test_no_model_literal.py` + the CI grep enforce |
| T-81-SC | Tampering | npm/pip/cargo installs | accept | zero new packages (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pytest -q tests/bench/test_assemble.py tests/bench/test_run_fake.py tests/bench/test_no_model_literal.py` → green (BENCH-01 xfails flipped)
- `bash scripts/release/check_no_hardcoded_model.sh` → 0 literals (bench/ clean)
- `python3 -m vibemix bench --help` → exit 0
- Full suite `pytest -q` exit 0, no new failures
</verification>

<success_criteria>
- BENCH-01 met: the harness expresses the 6-D matrix; STUDY_A/STUDY_B/NO_AUDIO_CELL defined; the assembler reuses the real seams; the no-audio cell is correct
- Offline-green: fake-client sweep records cells with zero API; the 429 fail-safe parks gracefully
- Zero model literals under `bench/`; the real run is one documented `vibemix bench run` command
- Full suite green additively
</success_criteria>

<output>
Create `.planning/phases/81-bench-the-validation-instrument/81-02-SUMMARY.md` when done
</output>
