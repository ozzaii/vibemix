---
phase: 78-perceive-deeper-generalized-ear
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - src/vibemix/state/music_state.py
  - src/vibemix/state/deltas.py
  - src/vibemix/state/refresh.py
  - src/vibemix/state/coach.py
autonomous: true
requirements: [PERCEIVE-01, PERCEIVE-02]

must_haves:
  truths:
    - "When a prior snapshot exists and a scalar changed above the floor, the prompt renders Δ-phrasing ('kick density rose 18%') instead of (or alongside) the bare scalar."
    - "Below the confidence floor, the fact is OMITTED (abstain), never asserted as a 0% delta."
    - "The prompt carries one bounded multi-scale trajectory narrative composed from phrase / energy-arc / recent-moves."
    - "A cold MusicState (no prior, empty trajectory) renders byte-identical to the v8.0 baseline."
  artifacts:
    - path: "src/vibemix/state/music_state.py"
      provides: "prev_perceive + trajectory_narrative additive fields (falsy defaults)"
      contains: "prev_perceive"
    - path: "src/vibemix/state/deltas.py"
      provides: "pure render_delta + calibration helpers (no state write)"
      exports: ["render_delta"]
    - path: "src/vibemix/state/refresh.py"
      provides: "prev_perceive capture (last in lock) + trajectory compose (single-writer)"
      contains: "trajectory_narrative"
    - path: "src/vibemix/state/coach.py"
      provides: "Δ render branch + trajectory[...] gated render"
      contains: "trajectory["
  key_links:
    - from: "src/vibemix/state/refresh.py::_tick_once"
      to: "MusicState.prev_perceive / trajectory_narrative"
      via: "single-writer assignment inside `with state._lock:`"
      pattern: "state\\.prev_perceive|state\\.trajectory_narrative"
    - from: "src/vibemix/state/coach.py::evidence_line"
      to: "render_delta + state.trajectory_narrative"
      via: "gated render branches"
      pattern: "render_delta|trajectory\\["
---

<objective>
PERCEIVE-01 (deltas + calibrated confidence) and PERCEIVE-02 (multi-scale trajectory narrative) — both as additive single-writer state + gated render branches.

Purpose: Make the EAR speak in CHANGE, not snapshots. Gemini reads "kick density rose 18%; 3rd phrase of an energy build, last move bass-swap 20s ago" instead of a bare scalar dump — the documented fix for the felt shallowness. Abstain below floor (anti-slop, invariants #2/#3).

Output: 2 additive MusicState fields, 1 new pure-helper module (deltas.py), single-writer writes in refresh.py, gated render branches in coach.py. Flips the PERCEIVE-01/02 xfail scaffolds from Plan 01 to real green; keeps the cold-path byte-identity pin + all v8.0 coach goldens green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-RESEARCH.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md

<interfaces>
<!-- Exact anchors from live source this session. -->

MusicState additive-field precedent (music_state.py:47-69) — copy the comment idiom:
```
# Phase 52 (GENRE-01) — ... SINGLE-WRITER (_tick_once only) ...
# Additive defaults preserve golden-equivalence ...
detected_genre: str = "unknown"
genre_confidence: float = 0.0
```
Source scalar fields already present (PERCEIVE-01 deltas read these): `rms`, `bands` (sub/low/mid/high), `onset_density`, `bpm`, `crest_factor`.
Source trajectory fields already present + BOUNDED (PERCEIVE-02 reads these, no new buffers): `phase_history` (capped 6, music_state.py:127), `recent_moves` (12s window, :123), `long_arc` (~120s/10s hop, :126), `buildup_score` (:102), `phase`/`phase_started_at` (:38-39), `time_in_phase` property (:150-152).

refresh.py single-writer batch: `with state._lock:` at refresh.py:306. All this-tick scalar writes are at :314-352. The genre DSP write is at :339-352. The recent_moves write at :504, long_arc at :508, phase_history append at :408-413.

coach.py evidence_line render gates (coach.py:256-432):
- audible bare-scalar block at :284-289 (`hearing[rms=… bpm=…]`) — PERCEIVE-01 prefers Δ-phrasing over (or alongside) this when a prior exists + change clears floor.
- recent_moves[8s] gated `if recent_8s: ... else: NONE` idiom at :346-353 — the exact precedent for the trajectory gate.
- genre= gate at :334-335 (UNCHANGED by this plan).

deltas.py analog: state/genre/genre_autodetect.py — pure scoring module, NO state write, module constants `_`-prefix/UPPER_SNAKE, `from __future__ import annotations`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive MusicState fields + pure delta/calibration helpers (music_state.py, deltas.py)</name>
  <files>src/vibemix/state/music_state.py, src/vibemix/state/deltas.py</files>
  <behavior>
    - `MusicState()` default has `prev_perceive == {}` and `trajectory_narrative == ""` (falsy → cold-path byte-identity holds).
    - `render_delta(label, cur, prev, *, floor, fmt)` returns `None` when `prev is None` or `prev == 0.0` (cold path → abstain).
    - `render_delta` returns a phrasing string (e.g. `"<label> rose 18%"` / `"<label> fell 12%"`) when `abs((cur-prev)/abs(prev)) >= floor`.
    - `render_delta` returns `None` when the relative change magnitude is below `floor` (abstain, not a 0% line).
    - The calibration helper is a deterministic monotone map (logistic over normalized delta magnitude, or bucketed high/med/low) — pure, no library, no training.
  </behavior>
  <read_first>
    - src/vibemix/state/music_state.py:23-153 (the dataclass + the GENRE-01 additive-field comment block at :47-56)
    - src/vibemix/state/genre/genre_autodetect.py:50-159 (pure-module convention: constants, score_genre returns a tuple, no state write)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (deltas.py analog, lines 109-114; music_state field block, lines 24-46)
  </read_first>
  <action>
    In src/vibemix/state/music_state.py, add TWO additive fields with falsy defaults, mirroring the GENRE-01 comment block idiom (PERCEIVE-01 single-writer, _tick_once only; additive defaults preserve golden-equivalence):
    - `prev_perceive: dict = field(default_factory=dict)` — prior-tick scalar snapshot. Comment: written LAST inside the lock so the next tick reads a consistent prior; default {} means the first tick abstains.
    - `trajectory_narrative: str = ""` — composed multi-scale narrative; default "" so the render branch omits on cold path.

    Create src/vibemix/state/deltas.py (pure helpers, NO state write — analog genre_autodetect.py). `from __future__ import annotations`. Define module-level floor constant(s) (`_`-prefixed internal, UPPER_SNAKE public floor) and:
    - `render_delta(label, cur, prev, *, floor, fmt="+.0%")` per the behavior block — returns the phrasing string or `None` to abstain. Choose a `verb` ("rose"/"fell") by sign; format the relative magnitude.
    - a `calibrate_confidence(...)` (or bucketed) pure helper expressing the calibrated per-fact confidence the abstain gate uses. Keep it deterministic; document the chosen calibration (logistic vs bucket) in a comment with the WHY.
    Export both via `__all__`. NO fenced code in the module beyond normal Python; NO model literal; NO API call.

    TDD: the PERCEIVE-01 render tests from Plan 01 (currently xfail) become the RED spec — write deltas.py + the fields to make them pass. Add focused unit tests for `render_delta` cold/abstain/rendered branches in the existing test_coach_perceive.py if not already covered.
  </action>
  <acceptance_criteria>
    - `from vibemix.state.deltas import render_delta` imports; `render_delta("kick", 0.59, 0.5, floor=0.1)` returns a string containing `rose`; `render_delta("kick", 0.5, None, floor=0.1)` returns `None`; `render_delta("kick", 0.501, 0.5, floor=0.1)` returns `None` (below floor).
    - `MusicState().prev_perceive == {}` and `MusicState().trajectory_narrative == ""`.
    - The existing `tests/state/test_coach.py` goldens stay green (fields are additive, unrendered until coach.py changes in Task 2).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_coach.py -q && PYTHONPATH=src python3 -c "from vibemix.state.deltas import render_delta as r; assert 'rose' in r('kick',0.59,0.5,floor=0.1); assert r('kick',0.5,None,floor=0.1) is None; assert r('kick',0.501,0.5,floor=0.1) is None; from vibemix.state.music_state import MusicState as M; s=M(); assert s.prev_perceive=={} and s.trajectory_narrative==''; print('ok')"</automated>
  </verify>
  <done>Two additive falsy-default fields exist; deltas.py exports a pure render_delta + calibration helper with cold/abstain/rendered semantics; v8.0 coach goldens unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single-writer prev_snapshot capture + trajectory compose + gated render (refresh.py, coach.py)</name>
  <files>src/vibemix/state/refresh.py, src/vibemix/state/coach.py</files>
  <behavior>
    - After `_tick_once`, `state.prev_perceive` holds this tick's scalar set (rms, sub/low/mid/high band shares, onset_density, bpm, crest) — captured as the LAST step inside `with state._lock:` (single-writer invariant #1).
    - After `_tick_once` on a warm state, `state.trajectory_narrative` is a bounded non-empty string composed from phase_history (phrase chain) + buildup_score (energy arc) + recent_moves (newest move age); recomputed each tick, never accumulated.
    - `coach.evidence_line` renders Δ-phrasing for a changed scalar when `prev_perceive` has the prior AND change clears the floor; abstains (omits) below floor.
    - `coach.evidence_line` appends `trajectory[<narrative>]` only when `trajectory_narrative` is non-empty (cold → omit).
    - A cold default `MusicState()` renders byte-identical to v8.0 (the Plan-01 pin + all test_coach.py goldens stay green).
  </behavior>
  <read_first>
    - src/vibemix/state/refresh.py:306-352 (the `with state._lock:` batch; scalar writes; genre write) and :408-413,:504,:508 (phase_history/recent_moves/long_arc writes — read these for trajectory compose)
    - src/vibemix/state/coach.py:284-296 (the bare-scalar hearing block) and :334-353 (genre gate + recent_moves[8s] gated branch — the render idiom to copy)
    - src/vibemix/state/deltas.py (the helpers from Task 1)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (refresh.py single-writer section lines 49-77; coach.py render section lines 80-105)
  </read_first>
  <action>
    In src/vibemix/state/refresh.py `_tick_once`, INSIDE the existing `with state._lock:` batch:
    - PERCEIVE-02: compose `state.trajectory_narrative` by reading the already-bounded fields (`state.phase_history` → last-3 phase chain; `state.buildup_score` → "building"/"settled" arc label; `state.recent_moves` → newest move + age). Join into ONE compact bounded string (e.g. "build→drop→groove; building; last move: bass-swap 20s ago"). Place this AFTER the phase_history/recent_moves/long_arc writes so it reads consistent values. No new accumulation — recomputed each tick from bounded fields.
    - PERCEIVE-01: as the LAST writes before lock release, capture `state.prev_perceive = {...}` with this tick's scalar set (rms, sub/low/mid/high band shares, onset_density, bpm, crest). This becomes the NEXT tick's prior. (Capture last so the delta the NEXT tick renders is prior-vs-current consistent.)
    Both are pure reads of just-written fields + dict/string assignment — no new I/O, no second writer.

    In src/vibemix/state/coach.py `evidence_line`:
    - PERCEIVE-01: where the bare `hearing[...]` scalars render (:284-289), add a gated Δ branch: when `state.prev_perceive` is non-empty, call `render_delta(...)` per relevant scalar (kick/onset density, RMS, etc.) and PREFER the returned phrasing (append it; the contract per CONTEXT keeps the raw scalar too — raw NOT removed). When `render_delta` returns None (cold or below-floor), append nothing extra (abstain). Mirror the existing `if <field>:`-gated style exactly so a cold state appends ZERO new bytes.
    - PERCEIVE-02: mirror the recent_moves[8s] gate — `if state.trajectory_narrative: e.append(f"trajectory[{state.trajectory_narrative}]")`. Cold ("") → omit.
    Do NOT touch the genre= gate (:334-335) or any existing branch. Every new branch is `if <falsy>:`-guarded so cold-path byte-identity holds.

    TDD: flip the PERCEIVE-01/02 render + single-writer xfail scaffolds (test_coach_perceive.py, test_refresh_perceive.py prev_snapshot/trajectory) from xfail to real pass by removing their `@pytest.mark.xfail`. Keep the byte-identity pin + test_coach.py goldens green.
  </action>
  <acceptance_criteria>
    - `pytest tests/state/test_refresh_perceive.py -k "prev_snapshot or trajectory" -q` PASSES (no longer xfail).
    - `pytest tests/state/test_coach_perceive.py -q` — delta-rendered + abstain + empty-trajectory-omitted + byte-identity all PASS.
    - `pytest tests/state/test_coach.py -q` (v8.0 goldens) stays fully green.
    - Trajectory narrative is bounded: repeated `_tick_once` calls do not grow `state.trajectory_narrative` length unboundedly (assert a cap).
    - No `state.prev_perceive =` or `state.trajectory_narrative =` assignment exists outside `_tick_once` (single-writer; grep-checkable: `grep -rn "state\.prev_perceive\s*=\|state\.trajectory_narrative\s*=" src/vibemix | grep -v refresh.py` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_coach_perceive.py tests/state/test_refresh_perceive.py tests/state/test_coach.py -q -k "prev_snapshot or trajectory or delta or genre or byte_identical or recall or audible or silent" && test -z "$(grep -rn 'state\.prev_perceive[[:space:]]*=\|state\.trajectory_narrative[[:space:]]*=' src/vibemix | grep -v 'refresh.py')"</automated>
  </verify>
  <done>prev_perceive + trajectory_narrative written ONLY in _tick_once (single-writer); Δ-phrasing + trajectory[...] render gated; PERCEIVE-01/02 scaffolds flipped green; v8.0 byte-identity intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refresh loop → MusicState | The only writer of MusicState (invariant #1). A second writer of the new fields would race the tick. |
| coach render → prompt string | Untrusted-feel surface: a low-confidence fact asserted here is the anti-slop failure (invariant #2/#3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02-01 | Tampering (consistency) | refresh.py prev_perceive / trajectory write | mitigate | Both writes live inside the existing `with state._lock:` batch; acceptance grep confirms no assignment outside `_tick_once`. |
| T-78-02-02 | Tampering (truth/anti-slop) | coach.py Δ render branch | mitigate | `render_delta` abstains (returns None → omit) below floor; never emits a 0% delta. Pinned by `test_delta_abstains_below_floor`. |
| T-78-02-03 | Information disclosure (cold drift) | coach.py new render branches | mitigate | Every new branch `if <falsy>:`-gated; cold MusicState byte-identical to v8.0 (Plan-01 pin + test_coach.py goldens). |
| T-78-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (numpy + stdlib only; RESEARCH audit vacuous). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/ -q` — all state tests green; PERCEIVE-01/02 scaffolds flipped from xfail to pass; v8.0 goldens unchanged.
- Full suite (`source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q`) green; baseline ~4451/0 holds (minus the now-passing xfails).
- Single-writer grep gate (in Task 2 verify) returns empty.
</verification>

<success_criteria>
- PERCEIVE-01: Δ-phrasing rendered above floor, abstains below floor, raw scalars retained; calibrated confidence is a pure helper.
- PERCEIVE-02: one bounded trajectory narrative composed in refresh, rendered gated in coach.
- Cold-path byte-identical to v8.0 (golden tests green).
- Single-writer invariant #1 held (no second writer; grep-verified).
- Honest green: no `genai.Client`, no `GEMINI_API_KEY`.
</success_criteria>

<output>
Create `.planning/phases/78-perceive-deeper-generalized-ear/78-02-SUMMARY.md` when done
</output>
