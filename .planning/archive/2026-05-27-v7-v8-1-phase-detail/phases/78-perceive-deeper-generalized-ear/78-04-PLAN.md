---
phase: 78-perceive-deeper-generalized-ear
plan: 04
type: execute
wave: 3
depends_on: [78-02, 78-03]
files_modified:
  - src/vibemix/state/refresh.py
  - src/vibemix/state/genre/genre_reconcile.py
autonomous: true
requirements: [PERCEIVE-03]

must_haves:
  truths:
    - "On TRACK_CHANGE, the off-loop GenrePrototypeLookup result feeds the single-writer refresh loop's detected_genre / genre_confidence write — never a second writer."
    - "Embedding-genre and DSP-genre are reconciled into ONE coherent detected_genre per tick: embedding wins when its (normalized) confidence clears the floor, else DSP fallback; the committed label runs through apply_genre_hysteresis (no flicker)."
    - "The centered-cosine confidence is normalized into the same band the coach.py render gate (>= 0.5) expects — pinned by a unit test that maps a known centered cosine to an above/below-gate render decision (the flagged risk)."
    - "The coach.py genre= render line (coach.py:334-335) and its v8.0 goldens are byte-identical — PERCEIVE-03 changes only the SOURCE of the value."
  artifacts:
    - path: "src/vibemix/state/genre/genre_reconcile.py"
      provides: "pure reconciliation + centered-cosine→render-band normalization helper"
      exports: ["reconcile_genre", "normalize_embedding_confidence"]
    - path: "src/vibemix/state/refresh.py"
      provides: "off-loop holder read + reconciled single-writer genre write"
      contains: "get_latest"
  key_links:
    - from: "src/vibemix/library/genre_prototypes.py::GenrePrototypeLookup.get_latest"
      to: "src/vibemix/state/refresh.py::_tick_once"
      via: "off-loop holder read inside the lock, then single-writer detected_genre write"
      pattern: "get_latest|reconcile_genre"
    - from: "src/vibemix/state/refresh.py"
      to: "MusicState.detected_genre / genre_confidence"
      via: "single-writer assignment after reconciliation + hysteresis"
      pattern: "state\\.detected_genre|state\\.genre_confidence"
---

<objective>
PERCEIVE-03 wiring: feed the embedding-genre lookup (Plan 03) into the single-writer refresh loop, reconciled with the existing DSP `score_genre` path into ONE coherent `detected_genre`/`genre_confidence` per tick — AND solve THE flagged risk: the centered-cosine confidence scale (~0.25) must normalize into the `>= 0.5` render-gate band (coach.py:334).

Purpose: This is the connection that makes 86.5%-accurate embedding genre actually drive the prompt. The render line + its goldens stay byte-identical (source changes, render doesn't). Single-writer invariant #1 is held via the off-loop-holder→`_tick_once`-read pattern (mirrors the deck-snapshot copy-in).

Output: a pure `genre_reconcile.py` (reconciliation + confidence normalization, fully unit-testable) + the refresh.py wiring (holder read + reconciled write). Flips the PERCEIVE-03 single-writer + reconciliation xfail scaffolds from Plan 01 to real green; pins the confidence-band reconciliation with a dedicated unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-RESEARCH.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-03-SUMMARY.md

<interfaces>
<!-- Exact anchors from live source + Plan 03 output. -->

From src/vibemix/state/refresh.py — the genre DSP write to reconcile/feed (refresh.py:339-352), inside `with state._lock:` (:306):
```
raw_genre, raw_genre_conf = score_genre(bpm_cache, {...band shares...}, smoothed_crest, _cached_profiles())
committed_genre = apply_genre_hysteresis(raw_genre, genre_hysteresis)
state.detected_genre = committed_genre
state.genre_confidence = round(raw_genre_conf, 2)
```
`_tick_once` signature: `def _tick_once(state, audio_buf, controller_state, track_info, *, now, ..., genre_hysteresis=None, deck_source=None)` (refresh.py:191-210). The deck-snapshot copy-in idiom to mirror for the off-loop holder read is at refresh.py:472-481 (`deck_source.snapshot()` read; `state.deck_state.decks =` assigned ONLY here).

From src/vibemix/state/genre/genre_autodetect.py:
```
GENRE_CONFIDENCE_MIN = 0.55   # DSP-scale floor
GENRE_TIE_MARGIN = 0.08
def score_genre(...) -> tuple[str, float]
class GenreHysteresis
def apply_genre_hysteresis(raw: str, hs: GenreHysteresis) -> str
```

From src/vibemix/library/genre_prototypes.py (Plan 03):
```
PROTO_FLOOR  # centered-cosine-scale floor (~0.25)
PROTO_MARGIN
class GenrePrototypeLookup:
    def classify_playing(track_id) -> tuple[str, float]   # off-loop worker
    def get_latest(self) -> tuple[str, float] | None      # refresh reads THIS
    def clear(self) -> None
```

From src/vibemix/state/coach.py:334-335 (the render gate — UNCHANGED by this plan):
```
if state.detected_genre != "unknown" and state.genre_confidence >= 0.5:
    e.append(f"genre={state.detected_genre}")
```
THE RISK: `genre_confidence` written by this plan must be on the SAME [0,1] scale the `>= 0.5` gate expects. A raw centered cosine of ~0.25 would never clear 0.5 (over-suppress) — so embedding confidence is normalized into the render band by `normalize_embedding_confidence` before it is written.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure reconciliation + centered-cosine→render-band normalization (genre_reconcile.py)</name>
  <files>src/vibemix/state/genre/genre_reconcile.py, tests/state/test_genre_reconcile.py</files>
  <behavior>
    - `normalize_embedding_confidence(cosine)` maps a centered-cosine score (natural floor ~PROTO_FLOOR ≈ 0.25) into the [0,1] render band such that a cosine clearing PROTO_FLOOR by a meaningful margin maps to ≥ 0.5 (clears the coach gate) and a sub-PROTO_FLOOR cosine maps to < 0.5 (suppressed). Deterministic monotone map (e.g. affine rescale anchored at PROTO_FLOOR→~0.5, 1.0→1.0; clamp to [0,1]).
    - `reconcile_genre(emb_label, emb_conf_raw, dsp_label, dsp_conf, *, emb_floor)` returns `(label, confidence)`: prefer embedding when `emb_label != "unknown"` AND its NORMALIZED confidence clears the floor; else fall back to the DSP `(dsp_label, dsp_conf)`. The returned confidence is on the render-band scale.
    - Agree case (emb and dsp same label) → that label with the higher-confidence source's value.
    - Disagree + confident embedding → embedding label wins.
    - Embedding below floor → DSP fallback (its native confidence).
    - Both unknown → `("unknown", 0.0)`.
  </behavior>
  <read_first>
    - src/vibemix/state/genre/genre_autodetect.py:50-203 (GENRE_CONFIDENCE_MIN, GENRE_TIE_MARGIN, score_genre unknown fallback, apply_genre_hysteresis — the pure-module convention)
    - src/vibemix/library/genre_prototypes.py (PROTO_FLOOR, PROTO_MARGIN — the centered-cosine scale this normalizes from)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-RESEARCH.md (Pitfall 4 confidence-floor mismatch; Open Q1 reconciliation policy + recommendation)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (refresh.py genre reconcile, lines 55-62)
  </read_first>
  <action>
    Create src/vibemix/state/genre/genre_reconcile.py (pure module, NO state write — analog genre_autodetect.py). `from __future__ import annotations`. Import `PROTO_FLOOR` from `vibemix.library.genre_prototypes` (or accept it as a kwarg to keep state→library coupling minimal — planner's call; if importing creates a layering concern, pass the floor in).

    `normalize_embedding_confidence(cosine, *, floor=PROTO_FLOOR) -> float` per the behavior block — affine rescale so `floor → ~0.5`, `1.0 → 1.0`, clamped to [0,1]; below floor → maps below 0.5. Document the anchor choice + WHY in a comment (this is THE confidence-band reconciliation — the flagged risk).

    `reconcile_genre(emb_label, emb_conf_raw, dsp_label, dsp_conf, *, emb_floor=PROTO_FLOOR) -> tuple[str, float]` — normalize the embedding confidence, apply the embedding-wins-when-confident policy (RESEARCH Open Q1 recommendation), else DSP fallback. Return `(label, render_band_confidence)`.

    Export both via `__all__`. NO model literal, NO API call, NO state write.

    TDD: write focused unit tests for both functions (agree / disagree-confident-embedding / embedding-below-floor / both-unknown for reconcile; and a known-cosine→above/below-0.5 map for normalize — THE pinning test the flagged risk demands). These pin the contract independently of refresh wiring.
  </action>
  <acceptance_criteria>
    - `from vibemix.state.genre.genre_reconcile import reconcile_genre, normalize_embedding_confidence` imports.
    - `normalize_embedding_confidence(0.40)` (clearing the ~0.25 floor) returns a value ≥ 0.5; `normalize_embedding_confidence(0.20)` (below floor) returns a value < 0.5 — pins the centered-cosine→render-band reconciliation.
    - `reconcile_genre("hard_techno", 0.40, "house", 0.6, ...)` returns `("hard_techno", >=0.5)` (confident embedding wins); `reconcile_genre("unknown", 0.10, "house", 0.6, ...)` returns `("house", 0.6)` (DSP fallback); `reconcile_genre("unknown",0.0,"unknown",0.0,...)` returns `("unknown", 0.0)`.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/ -q -k "reconcile or normalize" && PYTHONPATH=src python3 -c "from vibemix.state.genre.genre_reconcile import normalize_embedding_confidence as n, reconcile_genre as r; assert n(0.40)>=0.5; assert n(0.20)<0.5; assert r('hard_techno',0.40,'house',0.6)[0]=='hard_techno' and r('hard_techno',0.40,'house',0.6)[1]>=0.5; assert r('unknown',0.10,'house',0.6)==('house',0.6); assert r('unknown',0.0,'unknown',0.0)==('unknown',0.0); print('ok')"</automated>
  </verify>
  <done>genre_reconcile.py pins the centered-cosine→render-band normalization (the flagged risk) + the embedding-wins-when-confident reconciliation, fully unit-tested, no state write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire GenrePrototypeLookup holder into the single-writer refresh genre write</name>
  <files>src/vibemix/state/refresh.py, tests/state/test_refresh_perceive.py</files>
  <behavior>
    - `_tick_once` accepts an optional `genre_lookup: GenrePrototypeLookup | None = None` kwarg (default None → byte-identical to v8.0: pure DSP path, no embedding).
    - When `genre_lookup` is provided, inside the existing `with state._lock:` batch the loop READS `genre_lookup.get_latest()` (the off-loop holder — never the loop embedding inline) and reconciles `(emb_label, emb_conf)` with the DSP `(raw_genre, raw_genre_conf)` via `reconcile_genre`, then runs the COMMITTED label through `apply_genre_hysteresis`, then writes `state.detected_genre` / `state.genre_confidence` ONCE.
    - The off-loop `classify_playing` dispatch is debounced on TRACK_CHANGE (not per-tick) — fired from the existing track-change detection path, off the loop (mirrors the deck-snapshot / Grounding off-loop dispatch). The loop only READS the holder.
    - `detected_genre` / `genre_confidence` are written ONLY in `_tick_once` (single-writer invariant #1).
    - Cold (genre_lookup=None OR holder empty) → DSP-only path → coach.py genre= render + its v8.0 goldens byte-identical.
  </behavior>
  <read_first>
    - src/vibemix/state/refresh.py:191-210 (the `_tick_once` signature), :306-352 (the lock batch + DSP genre write), :451-501 (track-change detection + the deck-snapshot off-loop-holder copy-in idiom to mirror), :532-599 (state_refresh_loop — where genre_lookup would be constructed/passed + where TRACK_CHANGE dispatch fires)
    - src/vibemix/state/genre/genre_reconcile.py (Task 1)
    - src/vibemix/library/genre_prototypes.py (GenrePrototypeLookup.get_latest / classify_playing / clear)
    - src/vibemix/state/coach.py:334-335 (the genre render gate — must stay byte-identical)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (refresh.py single-writer + holder section, lines 49-77; Pitfall 1 second-writer)
  </read_first>
  <action>
    In src/vibemix/state/refresh.py:
    - Add `genre_lookup: GenrePrototypeLookup | None = None` to `_tick_once` (and thread it through from `state_refresh_loop` where the loop is set up). Default None preserves v8.0 behavior exactly.
    - Inside the existing `with state._lock:` batch, at the DSP genre write (refresh.py:339-352): keep `raw_genre, raw_genre_conf = score_genre(...)`. When `genre_lookup is not None`, read `latest = genre_lookup.get_latest()`; `emb_label, emb_conf = latest if latest else ("unknown", 0.0)`; `committed_raw, committed_conf = reconcile_genre(emb_label, emb_conf, raw_genre, raw_genre_conf)`. Else fall back to the pure DSP `(raw_genre, raw_genre_conf)`. Then `committed = apply_genre_hysteresis(committed_raw, genre_hysteresis)`; `state.detected_genre = committed`; `state.genre_confidence = round(committed_conf, 2)`. This stays ONE write, inside the lock, single-writer.
    - TRACK_CHANGE dispatch: where the loop detects a track change (the `track_history.append` path / audible_track change at refresh.py:456-463), dispatch `genre_lookup.classify_playing(track_id)` OFF the loop (mirror the Grounding/deck off-loop dispatch — `run_in_executor` or the existing off-loop seam; do NOT block the 10Hz tick, do NOT write state from the worker). On TRACK_CHANGE also `genre_lookup.clear()` first so a superseded in-flight lookup is discarded (generation-token contract from Plan 03). Keep the dispatch best-effort/try-guarded so a lookup failure never wedges the tick.
    - Do NOT modify coach.py — the genre= render gate is unchanged; this plan only changes the SOURCE of the values.

    TDD: flip the PERCEIVE-03 `test_genre_fed_single_writer` + `test_genre_reconciliation` xfail scaffolds (test_refresh_perceive.py) to real pass by injecting a fake `genre_lookup` whose `get_latest()` returns a known `(label, conf)` and asserting the reconciled single-writer write. Keep coach.py genre goldens (test_coach.py:260-296) green.
  </action>
  <acceptance_criteria>
    - `pytest tests/state/test_refresh_perceive.py -k "genre_fed_single_writer or genre_reconciliation" -q` PASSES (no longer xfail).
    - With `genre_lookup=None`, `_tick_once` produces the SAME `detected_genre`/`genre_confidence` as the v8.0 DSP-only path (byte-identical cold path) — assert via a tick test.
    - With an injected holder returning a confident embedding genre, `_tick_once` writes that label (embedding-wins) at a render-band confidence; with a sub-floor holder it writes the DSP genre.
    - No `state.detected_genre =` / `state.genre_confidence =` write outside `_tick_once`: `grep -rn "state\.detected_genre\s*=\|state\.genre_confidence\s*=" src/vibemix | grep -v "refresh.py"` returns nothing.
    - coach.py genre goldens green: `pytest tests/state/test_coach.py -k genre -q`.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/state/test_refresh_perceive.py tests/state/test_coach.py -q -k "genre or reconcil or byte_identical or silent or audible" && test -z "$(grep -rn 'state\.detected_genre[[:space:]]*=\|state\.genre_confidence[[:space:]]*=' src/vibemix | grep -v 'refresh.py')"</automated>
  </verify>
  <done>Embedding genre feeds the single-writer refresh write via the off-loop holder, reconciled with DSP + hysteresis; TRACK_CHANGE dispatch is off-loop + generation-token-guarded; coach render + v8.0 goldens byte-identical; single-writer invariant grep-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-loop genre worker → MusicState | The lookup runs off-loop on TRACK_CHANGE; ONLY `_tick_once` writes `detected_genre` (invariant #1). |
| two confidence scales → one render gate | DSP confidence (~0.55 floor) vs centered-cosine (~0.25 floor) reconciled into the single `>= 0.5` render gate (the flagged risk). |
| genre label → prompt | A wrong genre is more priming than a wrong title — anti-slop demands the abstain/floor + hysteresis (invariant #2/#3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04-01 | Tampering (consistency) | refresh.py genre write / off-loop dispatch | mitigate | Worker fills the Plan-03 holder; `_tick_once` reads `get_latest()` and is the ONLY writer (grep-verified). `clear()` on TRACK_CHANGE discards a superseded in-flight lookup (generation token). |
| T-78-04-02 | Tampering (truth/anti-slop) | confidence-band reconciliation | mitigate | `normalize_embedding_confidence` maps centered cosine into the `>= 0.5` band; pinned by the known-cosine→above/below-0.5 unit test. Below floor → DSP fallback or unknown; committed label runs through `apply_genre_hysteresis` (no flicker). |
| T-78-04-03 | Information disclosure (cold drift) | coach.py genre= render | mitigate | coach.py UNCHANGED; `genre_lookup=None` / empty holder → DSP-only path → v8.0 goldens byte-identical (test_coach.py genre goldens green). |
| T-78-04-04 | Denial of service (tick wedge) | off-loop classify_playing dispatch | mitigate | Dispatch is off-loop + best-effort try-guarded; a lookup failure logs and never wedges the 10Hz tick or the `in_flight` gate (mirrors the per-loop error catch). |
| T-78-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (numpy + stdlib only; RESEARCH audit vacuous). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/state/ tests/library/test_genre_prototypes.py -q` — all PERCEIVE-03 scaffolds flipped green; coach genre goldens + cold-path byte-identity intact.
- Full suite (`source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q`) green; every PERCEIVE xfail from Plan 01 now a real pass; baseline ~4451/0 holds.
- Single-writer grep gate returns empty; the confidence-band pinning test (Task 1) is the flagged-risk proof.
</verification>

<success_criteria>
- PERCEIVE-03 fully wired: embedding genre feeds the single-writer write, reconciled with DSP into one coherent label + hysteresis.
- The centered-cosine→`>= 0.5`-render-band reconciliation is pinned by a dedicated unit test (the flagged risk closed).
- Single-writer invariant #1 held (grep-verified); off-loop dispatch generation-token-guarded; tick never wedged.
- coach.py genre render + v8.0 goldens byte-identical (source changed, render unchanged).
- Honest green: no `genai.Client`, no `GEMINI_API_KEY` (cached-vector path).
</success_criteria>

<output>
Create `.planning/phases/78-perceive-deeper-generalized-ear/78-04-SUMMARY.md` when done
</output>
