---
phase: 78-perceive-deeper-generalized-ear
plan: 03
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - src/vibemix/library/genre_prototypes.py
autonomous: true
requirements: [PERCEIVE-03]

must_haves:
  truths:
    - "A genre-prototype table (one centered-mean vector per folder-label) is built from the cached library.db vectors using the SAME corpus centroid the ranking path uses."
    - "Classifying a track's cached embedding returns the nearest-prototype genre label, or 'unknown' below the cosine floor or within the tie-margin (anti-slop abstain)."
    - "The prototype table persists snapshot-hash-keyed and self-heals when library.db changes — €0, no new API call."
    - "The module NEVER writes MusicState (it returns values / fills a thread-safe holder for the single-writer refresh loop to read)."
  artifacts:
    - path: "src/vibemix/library/genre_prototypes.py"
      provides: "build_prototypes + classify + thread-safe holder, composing centering + _cosine + rekordbox"
      exports: ["build_prototypes", "classify", "GenrePrototypeLookup"]
  key_links:
    - from: "src/vibemix/library/genre_prototypes.py"
      to: "vibemix.library.centering / _cosine"
      via: "load_or_compute_centroid + center_and_renorm + cosine_topk (reused verbatim, no duplicate math)"
      pattern: "load_or_compute_centroid|center_and_renorm|cosine_topk"
    - from: "src/vibemix/library/genre_prototypes.py"
      to: "RekordboxLibrary.lookup_by_id → TrackEntry.filepath"
      via: "genre proxy = parent folder of filepath"
      pattern: "lookup_by_id|filepath"
---

<objective>
PERCEIVE-03 mechanism: the mean-centered nearest-prototype genre lookup as a SELF-CONTAINED library module (`library/genre_prototypes.py`) — build, persist, classify, hold. NO state writes, NO refresh wiring (that is Plan 04). 86.5%-validated, €0 (cached vectors).

Purpose: Replace the coarse 3-band DSP genre proxy with the embedding-driven genre the research validated. This plan delivers the pure mechanism + the thread-safe holder; Plan 04 feeds it into the single-writer refresh loop.

Output: one new `library/genre_prototypes.py` composing `centering.py` + `_cosine.py` + `rekordbox.py` (NO hand-rolled math). Flips the PERCEIVE-03 prototype-build + classify xfail scaffolds from Plan 01 to real green. Runs in parallel with Plan 02 (zero file overlap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-RESEARCH.md
@.planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md

<interfaces>
<!-- Exact signatures from live source this session — reuse verbatim, do NOT duplicate. -->

From src/vibemix/library/centering.py (reuse VERBATIM — the anisotropy fix chokepoint):
```
CENTROID_PATH = Path.home()/".cache"/"vibemix"/"library_centroid.npy"
CENTROID_META_PATH = Path.home()/".cache"/"vibemix"/"library_centroid.meta.json"
def compute_centroid(vectors: np.ndarray) -> np.ndarray | None        # None for N<2
def center_and_renorm(vectors: np.ndarray, centroid: np.ndarray) -> np.ndarray   # (D,) or (N,D)
def load_or_compute_centroid(vectors, snapshot_hash, *, centroid_path=None, meta_path=None) -> np.ndarray | None
```

From src/vibemix/library/_cosine.py:
```
EMBEDDING_DIM = 1536
def l2_normalize(vec: np.ndarray) -> np.ndarray
def cosine_topk(query: np.ndarray, vectors: np.ndarray, track_ids: list[str], k: int = 10) -> list[tuple[str, float]]
    # query MUST be (EMBEDDING_DIM,) L2-normalized; vectors (N, EMBEDDING_DIM) L2-normalized; asserts shapes.
    # returns [(label, sim)] DESC sim, ASC id (deterministic tie-break).
```

From src/vibemix/library/store.py:
```
def open_store(prefer_sqlite_vec: bool = True) -> LibraryStore
class LibraryStore:
    def __init__(self, backend: _Backend)
    def snapshot_hash(self) -> str
    # backend: load_all() -> tuple[list[str], np.ndarray]  (ids, (N,1536) float32); snapshot_hash() -> str
```
(store.py:82-90 is the canonical center-BOTH-with-SAME-centroid sequence to copy.)

From src/vibemix/library/rekordbox.py:
```
class TrackEntry: ... filepath: str   # line 93
class RekordboxLibrary:
    CACHE_PATH = Path.home()/".cache"/"vibemix"/"library.pkl"   # line 123
    def try_load_cache(self) -> bool                            # line 185
    def lookup_by_id(self, track_id: str) -> TrackEntry | None  # line 233
```
Genre proxy: `te = lib.lookup_by_id(tid); label = Path(te.filepath).parent.name if te else "unknown"`.

Thread-safe holder analog — src/vibemix/library/grounding.py:169-265 (`Grounding`): off-loop worker stores result under `threading.Lock`; per-dispatch generation token (`_inflight_gen`, bumped in `clear()`) discards a stale write if superseded; `get_latest_citation()` snapshots; the refresh loop READS, never the worker writing state.

Anti-slop floor + margin: state/genre/genre_autodetect.py — `GENRE_CONFIDENCE_MIN=0.55`, `GENRE_TIE_MARGIN=0.08`; below floor OR within margin → `("unknown", best_conf)`. NOTE (the flagged risk): centered-cosine confidence scale (~0.25 floor) differs from DSP's 0.55 — this module returns the RAW centered cosine; the normalization into the `>= 0.5` render band is a Plan-04 reconciliation concern. This module's floor/margin are its own internal cosine-scale params (planner discretion: floor≈0.25, margin≈0.05 per RESEARCH Pattern 3) — keep them named constants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_prototypes + classify (compose centering + _cosine + rekordbox)</name>
  <files>src/vibemix/library/genre_prototypes.py, tests/library/test_genre_prototypes.py</files>
  <behavior>
    - `build_prototypes` over a synthetic `(ids, vectors, label_of)` corpus returns `(protos, labels)` where `protos` is shape `(n_labels, 1536)` float32, each row L2-normalized, one row per distinct label; the centroid used is `load_or_compute_centroid(vectors, snapshot_hash)`.
    - When the corpus is degenerate (N<2 → centroid None), `build_prototypes` returns an empty `(0,1536)` array + `[]` (no crash).
    - `classify(track_embedding, protos, labels, centroid, *, floor, margin)` centers the query with the SAME centroid, ranks via `cosine_topk`, returns `(best_label, best_sim)` when `best_sim >= floor` AND `(best_sim - second_sim) >= margin`.
    - `classify` returns `("unknown", conf)` when below floor OR within the tie-margin (anti-slop abstain).
    - Both prototype-build and classify use the SAME corpus centroid (Pitfall 2 — centroid divergence = garbage cosine).
  </behavior>
  <read_first>
    - src/vibemix/library/centering.py (full — load_or_compute_centroid, center_and_renorm, compute_centroid)
    - src/vibemix/library/store.py:56-178 (open_store, snapshot_hash, the search_centered center-both sequence at :63-90)
    - src/vibemix/library/_cosine.py:56-130 (EMBEDDING_DIM, l2_normalize, cosine_topk + its shape asserts)
    - src/vibemix/library/rekordbox.py:70-93,123,185,233 (TrackEntry.filepath, CACHE_PATH, try_load_cache, lookup_by_id)
    - src/vibemix/library/grounding.py:74-79 (the `_decide` floor/margin idiom)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (genre_prototypes section, lines 117-154)
  </read_first>
  <action>
    Create src/vibemix/library/genre_prototypes.py. `from __future__ import annotations`. Import (library-tier convention):
    `numpy as np`; `EMBEDDING_DIM, cosine_topk, l2_normalize` from `vibemix.library._cosine`; `center_and_renorm, load_or_compute_centroid` from `vibemix.library.centering`; `open_store` from `vibemix.library.store`; `RekordboxLibrary` from `vibemix.library.rekordbox`; `Path`, `threading`.

    `build_prototypes(*, store=None, lib=None)` — load `ids, vectors` via `store._backend.load_all()` (or accept an injected `(ids, vectors, label_of)` for tests); compute the centroid via `load_or_compute_centroid(vectors, store.snapshot_hash())` (degenerate guard: None → return `(np.empty((0, EMBEDDING_DIM), np.float32), [])`); `center_and_renorm(vectors, centroid)`; derive `label_of[tid] = Path(te.filepath).parent.name if te else "unknown"` via `RekordboxLibrary().try_load_cache()` + `lookup_by_id`. For each sorted distinct label, take the centered rows for that label, `l2_normalize(rows.mean(axis=0))` = prototype. Return `(np.stack(protos), labels)`. Make the function injectable (accept the corpus + label_of directly) so the Plan-01 synthetic test drives it without touching disk.

    `classify(track_embedding, protos, labels, centroid, *, floor=PROTO_FLOOR, margin=PROTO_MARGIN) -> tuple[str, float]` — `q = center_and_renorm(track_embedding, centroid)`; `ranked = cosine_topk(q, protos, labels, k=2)`; empty → `("unknown", 0.0)`; unpack best + second; `if s0 < floor or (s0 - second) < margin: return ("unknown", float(max(0.0, s0)))`; else `return (best, float(s0))`. Mirror the `score_genre` unknown-fallback shape (genre_autodetect.py:153-159).

    Module constants: `PROTO_FLOOR` and `PROTO_MARGIN` (named, documented — the centered-cosine-scale floor/margin; comment that the render-gate normalization is Plan 04's reconciliation job). Do NOT hand-roll centroid/normalize/cosine — reuse the imports.

    Persistence (mirror centering.CENTROID_PATH sidecar): a `PROTOTYPES_PATH` (`~/.cache/vibemix/genre_prototypes.npy`) + meta sidecar, snapshot-hash-keyed, atomic tmp→replace, best-effort. Lazy-build helper `load_or_build_prototypes(store)` that returns cached prototypes when the snapshot_hash matches, else rebuilds + persists. Paths resolved AT CALL TIME so tests monkeypatch.

    TDD: the PERCEIVE-03 prototype-build + classify xfail scaffolds from Plan 01 (test_genre_prototypes.py) are the RED spec — implement until they pass. Keep their tmp_path cache monkeypatches intact.
  </action>
  <acceptance_criteria>
    - `pytest tests/library/test_genre_prototypes.py -k "build_centered_means or classify_floor_and_margin" -q` PASSES (no longer xfail).
    - `build_prototypes` on a synthetic 2-label corpus returns shape `(2, 1536)` float32, each row L2-normalized (norm ≈ 1.0).
    - `classify` returns the correct label above floor and `("unknown", _)` below floor / within margin.
    - No duplicate centroid/normalize/cosine math: `grep -n "def compute_centroid\|np.linalg.norm\|argpartition\|argsort" src/vibemix/library/genre_prototypes.py` returns nothing (math delegated to centering/_cosine).
    - Real `~/.cache/vibemix/` untouched by the test run (tmp_path monkeypatches).
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/library/test_genre_prototypes.py -q && test -z "$(grep -n 'def compute_centroid\|np\.linalg\.norm\|argpartition\|argsort' src/vibemix/library/genre_prototypes.py)"</automated>
  </verify>
  <done>genre_prototypes.py builds centered-mean prototypes + classifies with floor/margin abstain, composing centering/_cosine/rekordbox (no duplicate math); PERCEIVE-03 mechanism scaffolds flipped green; real cache untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Thread-safe holder + lazy build-on-TRACK_CHANGE entry point</name>
  <files>src/vibemix/library/genre_prototypes.py, tests/library/test_genre_prototypes.py</files>
  <read_first>
    - src/vibemix/library/grounding.py:169-265 (the `Grounding` holder + `_inflight_gen` + `clear()` discard idiom to mirror)
    - src/vibemix/library/store.py (how a cached embedding is fetched by track_id — load_all / backend lookup)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-PATTERNS.md (thread-safe holder, line 150)
    - .planning/phases/78-perceive-deeper-generalized-ear/78-RESEARCH.md (Pitfall 1 second-writer creep; Open Q2 lazy-build recommendation; Assumption A1 unknown-track path)
  </read_first>
  <action>
    In src/vibemix/library/genre_prototypes.py, add a `GenrePrototypeLookup` class mirroring `Grounding` (grounding.py:169-265): `__init__(store=None)` holds the lazy-built prototypes + centroid + a `threading.Lock` + `_latest: tuple[str, float] | None` + `_inflight_gen: int`. Methods:
    - `classify_playing(track_id)` (off-loop worker entry): bump `_inflight_gen` under lock, capture `my_gen`; lazy-build prototypes (cached, snapshot-hash-keyed) on first call; fetch the track's CACHED embedding by id from the store (library tracks = €0); if the track is not in the library, return `("unknown", 0.0)` WITHOUT a live embed (the abstain-safe path, RESEARCH A1); `classify(...)`; under lock, write `_latest` ONLY if `my_gen == _inflight_gen`.
    - `get_latest() -> tuple[str, float] | None` — lock-guarded snapshot (the single-writer refresh loop reads THIS; Plan 04 wires it).
    - `clear()` — bump `_inflight_gen`, set `_latest = None` (discards a superseded in-flight write).
    Document that the holder is the anti-second-writer seam (Pitfall 1): the worker fills the holder, `refresh._tick_once` reads it and is the only MusicState writer.

    Add `__all__` exporting `build_prototypes`, `classify`, `load_or_build_prototypes`, `GenrePrototypeLookup`, `PROTO_FLOOR`, `PROTO_MARGIN`, `PROTOTYPES_PATH`.

    Add a focused unit test (in test_genre_prototypes.py) for the holder: a late write after `clear()` is discarded (generation-token race), and `classify_playing` on an unknown track returns `("unknown", 0.0)` with no API call. NO `genai.Client`, NO `GEMINI_API_KEY`.
  </action>
  <acceptance_criteria>
    - `from vibemix.library.genre_prototypes import GenrePrototypeLookup` imports.
    - Holder test: a stale write after `clear()` does not overwrite a cleared latch (generation token); `classify_playing(<unknown id>)` returns `("unknown", 0.0)`.
    - `get_latest()` returns the holder's snapshot; no code path in this module writes `MusicState` (`grep -n "MusicState\|state\.detected_genre" src/vibemix/library/genre_prototypes.py` returns nothing).
    - No `genai.Client` / `GEMINI_API_KEY` in the module or its tests for the cached-vector path.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/library/test_genre_prototypes.py -q && test -z "$(grep -n 'MusicState\|state\.detected_genre' src/vibemix/library/genre_prototypes.py)" && test -z "$(grep -n 'genai.Client\|GEMINI_API_KEY' src/vibemix/library/genre_prototypes.py)"</automated>
  </verify>
  <done>GenrePrototypeLookup holder mirrors Grounding (generation-token discard); classify_playing is €0 for library tracks and abstains on unknown tracks; the module never writes MusicState; honest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-loop genre worker → MusicState | The lookup runs off-loop; it must NEVER write MusicState (invariant #1). It fills a holder; refresh reads it. |
| test → real cache (`~/.cache/vibemix/`) | A prototype-build test without the path monkeypatch clobbers the real `library.pkl` / `genre_prototypes.npy` (dev-data tampering). |
| filesystem folder name → genre label | Folder names are read as labels (V5 input-validation, light) — stay inside the library cache, no PII, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03-01 | Tampering (consistency) | GenrePrototypeLookup off-loop write | mitigate | Per-dispatch generation token (mirrors Grounding._inflight_gen); a stale write after `clear()` is discarded. Holder never writes MusicState (grep-verified). |
| T-78-03-02 | Tampering (truth/anti-slop) | classify floor/margin | mitigate | Below floor OR within margin → `("unknown", conf)`; unknown-to-library track abstains without a live embed (invariant #3, RESEARCH A1). |
| T-78-03-03 | Tampering (dev data) | tests/library/test_genre_prototypes.py | mitigate | Monkeypatch `RekordboxLibrary.CACHE_PATH` + `centering.CENTROID_PATH`/`CENTROID_META_PATH` + `PROTOTYPES_PATH` to tmp_path; acceptance spot-checks real `library.pkl` mtime unchanged. |
| T-78-03-04 | Tampering (stale cache) | genre_prototypes.npy | mitigate | snapshot-hash-keyed sidecar (mirrors centering) — auto-rebuilds on library.db change. |
| T-78-03-V5 | Input validation | folder-name → label | mitigate | `EMBEDDING_DIM` shape assert (via cosine_topk) + N<2 degenerate guard (via centering); folder names stay inside the local cache, no untrusted network input. |
| T-78-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (numpy + stdlib only; RESEARCH Package Legitimacy Audit vacuous). |
</threat_model>

<verification>
- `PYTHONPATH=src python3 -m pytest tests/library/test_genre_prototypes.py tests/library/test_centering.py -q` — prototype build + classify + holder green; centering goldens unchanged.
- Full suite (`source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q`) green; baseline ~4451/0 holds.
- No-duplicate-math grep + no-MusicState-write grep (in task verifies) return empty.
</verification>

<success_criteria>
- PERCEIVE-03 mechanism: centered-mean prototypes built from cached vectors; classify with floor/margin abstain; €0 for library tracks.
- Reuses centering/_cosine/rekordbox verbatim (no duplicate centroid/normalize/cosine math).
- Thread-safe holder mirrors Grounding (generation-token discard); never writes MusicState.
- Real `~/.cache/vibemix/` untouched by tests.
- Honest green: no `genai.Client`, no `GEMINI_API_KEY`.
</success_criteria>

<output>
Create `.planning/phases/78-perceive-deeper-generalized-ear/78-03-SUMMARY.md` when done
</output>
