---
phase: quick-260525-gz2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/library/_cosine.py
  - src/vibemix/library/grounding.py
  - src/vibemix/library/embed.py
  - src/vibemix/library/folder_ingest.py
  - src/vibemix/library/__init__.py
  - src/vibemix/__main__.py
  - tests/library/fixtures/_generate.py
  - tests/library/fixtures/parity_corpus_100/_generate.py
  - tests/library/fixtures/synthetic_embeddings.npy
  - tests/library/fixtures/synthetic_queries.json
  - tests/library/fixtures/parity_corpus_100/vectors.npz
  - tests/library/fixtures/parity_corpus_100/track_ids.json
  - tests/library/test_embed.py
  - tests/library/test_search.py
  - tests/library/test_importer.py
  - tests/library/test_grounding.py
  - tests/library/test_migrate_embeddings_2.py
  - tests/library/test_folder_ingest.py
autonomous: true
requirements: [LIBRARY-EMBED-REAL]
must_haves:
  truths:
    - "Library embeddings are 1536-dim end to end (embed -> store -> search), L2-normalized before persistence on both backends"
    - "sqlite-vec <-> numpy top-K parity still holds at 1536-dim, and the €50 ceiling + SessionMeter tests still pass"
    - "No hardcoded output_dimensionality literal survives in src/ — grounding.py uses EMBEDDING_DIM"
    - "Tracks longer than the conservative single-call cap take the proven 3-excerpt path proactively (no failed full-track upload on slow connections)"
    - "Kaan can run `vibemix library embed-folder <dir>` against a raw audio folder and it embeds every supported file, resumable + per-track progress + running cost"
    - "A bad/too-long/corrupt file logs + skips without aborting the run, and never produces a faked embedding"
    - "Kaan can run a similarity query (free-text vibe via `search`, or seed track via `similar`) and see top-K filenames + scores for tracks he ingested via the folder path"
  artifacts:
    - path: "src/vibemix/library/folder_ingest.py"
      provides: "Folder-scan ingest: walk dir -> TrackEntry (ffprobe duration, filename title) -> embed -> store + try_load_cache-compatible pickle cache"
      min_lines: 120
    - path: "src/vibemix/library/_cosine.py"
      provides: "EMBEDDING_DIM bumped to 1536 (single source)"
      contains: "EMBEDDING_DIM = 1536"
    - path: "tests/library/test_folder_ingest.py"
      provides: "Unit tests for folder walk + TrackEntry derivation + resumable skip + partial-failure tolerance, embedder injected/faked (no network)"
      min_lines: 80
  key_links:
    - from: "src/vibemix/library/grounding.py"
      to: "EMBEDDING_DIM"
      via: "output_dimensionality must be EMBEDDING_DIM, not the literal 768"
      pattern: "output_dimensionality=EMBEDDING_DIM"
    - from: "src/vibemix/library/folder_ingest.py"
      to: "src/vibemix/library/embed.py (LibraryEmbedder.embed_track)"
      via: "embed each scanned TrackEntry, increment BudgetTelemetry via the existing embed path"
      pattern: "embed_track"
    - from: "src/vibemix/library/folder_ingest.py"
      to: "src/vibemix/library/store.py (open_store / add_batch)"
      via: "persist 1536-d vectors to the active backend"
      pattern: "add_batch"
    - from: "src/vibemix/__main__.py (_build_library_subparsers)"
      to: "folder_ingest + search/similar"
      via: "new `embed-folder` subcommand + folder-aware query entrypoint"
      pattern: "embed-folder"
---

<objective>
Embed Kaan's real, raw-folder DJ library and let him interrogate the embeddings.

Four coupled pieces, one plan:
1. Bump `EMBEDDING_DIM` 768 -> 1536 (single source in `_cosine.py`), fix the ONE
   hardcoded `output_dimensionality=768` literal in `grounding.py`, regenerate
   every dim-baked fixture, and confirm 1536-d vectors are L2-normalized before
   store/search on BOTH backends (the #1 correctness risk — Google does NOT
   auto-normalize sub-3072 MRL prefixes).
2. Harden the audio-cap assumption: take the proven 3-excerpt path proactively
   for tracks in the 80-180s gray zone instead of gambling a full-track upload
   (matters on Kaan's bad connection during the subset test).
3. Build `vibemix library embed-folder <dir>` — a resumable, progress-printing,
   partial-failure-tolerant folder-scan ingest (his library is a raw folder, NOT
   a Rekordbox collection.xml).
4. Make the existing `vibemix library search`/`similar` CLIs usable against the
   folder-ingested library (text vibe query OR seed-track), by persisting a
   try_load_cache-compatible library cache during ingest.

Purpose: Kaan points it at `/Users/ozai/Music/hardtechno` (~49 tracks), it embeds
them, and he eyeballs "does the techno embedding pull the techno out."
Output: dim-1536 pipeline, folder-ingest module + CLI, working similarity queries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<research_facts>
AUTHORITATIVE — do not re-research:
- emb-2 native dim 3072; valid MRL cut-points 3072 / 1536 / 768. Sub-3072 prefixes
  are NOT L2-normalized by Google — MUST normalize before cosine. vibemix already
  does via `_cosine.l2_normalize`; the dim bump must not break that path.
- Cost is per INPUT token, independent of output dim — 1536 costs the SAME per
  embed as 768 (just bigger storage + marginally slower cosine). No budget impact.
- Audio cap: Google docs CURRENTLY state 180s max (re-confirmed during planning at
  ai.google.dev/gemini-api/docs/embeddings on 2026-05-25). Kaan's research flagged a
  real-world cap of ~80s and single-call failures in the 80-180s band on emb-2.
  RESOLUTION (no need to settle the dispute): keep the hard-cap constant for the
  error heuristic, but lower the SINGLE-CALL threshold to a conservative 80s so
  80-180s tracks take the proven 3-excerpt path PROACTIVELY. The existing
  cap-error -> force_excerpts fallback stays as defense-in-depth.
- Target folder: /Users/ozai/Music — 1547 files, 9.3GB (mp3/m4a/wav), genre
  subfolders, NO collection.xml. Subset-first: Kaan embeds /Users/ozai/Music/hardtechno
  (~49 tracks) FIRST to validate quality + cost + UPLOAD SPEED on a bad connection.
- ~/.cache/vibemix/library.db currently has 0 rows -> recreating the vec0 table at
  1536 is a clean wipe, no real-data migration. But ingest must fail-loud or
  auto-recreate on a dim mismatch between a stored 768 table and EMBEDDING_DIM=1536.
- ffprobe + ffmpeg already on PATH (hard dep). Use ffprobe for duration. No new deps.
- CODEBASE FACTS (verified during planning):
  * The ONLY hardcoded `output_dimensionality=768` literal in src/ is
    grounding.py line ~125. Everywhere else (embed.py, ga-probe) already uses
    EMBEDDING_DIM. This literal MUST be switched or grounding queries will be
    768-d against a 1536-d store and dimension-mismatch in cosine_topk.
  * Both fixture corpora are dim-pinned: `fixtures/_generate.py` imports
    EMBEDDING_DIM (auto-adapts), but `fixtures/parity_corpus_100/_generate.py`
    hardcodes a LOCAL `EMBEDDING_DIM = 768` — that local literal must be replaced
    with an import.
  * tests with hardcoded 768 literals to fix: test_embed.py (`assert EMBEDDING_DIM
    == 768` line ~286 + `[0.1]*768`-style fakes), test_search.py:47, test_importer.py
    (~43, ~135), test_grounding.py:27, test_migrate_embeddings_2.py:108.
  * `parity` is an OPT-IN marker (not in default selection) — run `-m parity`
    explicitly to validate the regenerated fixtures.
  * memory/ (index_sqlite_vec_memory.py, ingest.py, store.py) imports EMBEDDING_DIM
    from `library._cosine` — so memory.db's vec0 table AUTO-recreates at 1536 with
    no edit. The feature is gated off by default; just run tests/memory to confirm.
</research_facts>

<interfaces>
From src/vibemix/library/_cosine.py:
```python
EMBEDDING_DIM = 768  # THE single source. l2_normalize(vec)->float32; cosine_topk(query, vectors, ids, k)
# cosine_topk asserts query.shape==(EMBEDDING_DIM,) and vectors.shape[1]==EMBEDDING_DIM.
```
From src/vibemix/library/grounding.py (line ~125 — the ONE hardcoded literal):
```python
config=_types.EmbedContentConfig(output_dimensionality=768)  # <-- MUST become EMBEDDING_DIM
```
From src/vibemix/library/rekordbox.py:
```python
@dataclass(frozen=True, slots=True)
class TrackEntry:
    track_id: str; title: str; artist: str; album: str
    bpm: float; key: str; duration_s: float
    cues: tuple[CuePoint, ...]; filepath: str
# RekordboxLibrary.CACHE_PATH = ~/.cache/vibemix/library.pkl ; _CacheBlob(version,xml_path,xml_mtime,tracks)
# try_load_cache() repopulates self.tracks from that pickle. search/similar CLIs read library.tracks for titles.
```
From src/vibemix/library/embed.py:
```python
class LibraryEmbedder:
    def __init__(self, client, cache_db=None, *, probe_on_init=True, recorder=None)
    def embed_track(self, track: TrackEntry) -> np.ndarray   # 1536-d float32 L2-normalized, content-hash cached
    def has_cached_embedding(self, track: TrackEntry) -> bool
AUDIO_CAP_SECONDS = 180          # hard cap, used by _is_audio_cap_error heuristic
EXCERPT_DURATION = 60
EXCERPT_STRATEGY_VERSION = "v1-3excerpt-mean"   # bump to invalidate ALL cached embeds
# _embed_audio: if duration_s <= AUDIO_CAP_SECONDS -> single call (try), else 3-excerpt(intro/mid/outro mean)
# output_dimensionality=EMBEDDING_DIM in every embed_content call (already imports the constant)
```
From src/vibemix/library/store.py:
```python
def open_store(prefer_sqlite_vec=True) -> LibraryStore
class LibraryStore:
    backend_name: str
    def add_batch(self, items: list[tuple[str, np.ndarray]]) -> None
    def search(self, query_vector: np.ndarray, k=10) -> list[tuple[str, float]]
    def snapshot_hash(self) -> str ; def close(self) -> None
# SqliteVecStore.vec0 table: FLOAT[{EMBEDDING_DIM}]; NumpyStore asserts vec.shape==(EMBEDDING_DIM,)
# NumpyStore._load() raises ValueError "vector dim mismatch: got 768, expected 1536" on a stale .npy
```
From src/vibemix/library/budget.py:
```python
def get_telemetry() -> BudgetTelemetry   # increment_audio_embed/text_embed/cache_hit auto-fire inside embed.py
# BudgetTelemetry.current_cost_estimate_eur() -> float ; .as_dict()
```
From src/vibemix/__main__.py (CLI):
```python
def _build_library_subparsers(parser)   # add new `embed-folder` subparser here; sub-dispatch via args.func
def _run_library_cli(argv: list[str]) -> int
# existing: search <query> --k ; similar <track_id> --k ; budget --dau --json
#   _cmd_library_search/_cmd_library_similar build proxy client via:
#     proxy_jwt = os.environ["VIBEMIX_PROXY_JWT"] (error+exit 1 if unset)
#     client = build_proxy_genai_client(proxy_jwt, os.environ.get("VIBEMIX_PROXY_BASE_URL","https://api.altidus.world"))
# `vibemix library <sub>` dispatched in cli_entry BEFORE _parse_args when raw_argv[0]=="library"
```
From src/vibemix/llm/model_router.py:
```python
def resolve(path: str) -> tuple[str, ServiceTier|None]   # resolve("embedding") -> ("gemini-embedding-2", FLEX)
# NEVER inline a gemini-* literal outside _router_config.py (CI grep gate). Folder ingest must not name a model.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump EMBEDDING_DIM 768->1536, fix the grounding literal, regenerate fixtures, prove L2-normalize + parity at the new dim</name>
  <files>src/vibemix/library/_cosine.py, src/vibemix/library/grounding.py, tests/library/fixtures/_generate.py, tests/library/fixtures/parity_corpus_100/_generate.py, tests/library/fixtures/synthetic_embeddings.npy, tests/library/fixtures/synthetic_queries.json, tests/library/fixtures/parity_corpus_100/vectors.npz, tests/library/fixtures/parity_corpus_100/track_ids.json, tests/library/test_embed.py, tests/library/test_search.py, tests/library/test_importer.py, tests/library/test_grounding.py, tests/library/test_migrate_embeddings_2.py</files>
  <behavior>
    - After the bump, `from vibemix.library._cosine import EMBEDDING_DIM` == 1536.
    - No `output_dimensionality=768` literal remains anywhere in src/ (grounding.py uses EMBEDDING_DIM).
    - tests/library/test_store_parity.py (numpy ground-truth + tie-break + float32 round-trip, plus the sqlite-vec parity test when the extension is present) passes against regenerated 1536-d fixtures under `-m parity`.
    - tests/library/test_embeddings_parity.py passes (its `vectors.shape == (100, EMBEDDING_DIM)` assert now expects 1536; its 3072-head-slice logic adapts automatically because it slices `[:EMBEDDING_DIM]`).
    - A freshly created SqliteVecStore + NumpyStore both build at FLOAT[1536]/(N,1536) and accept a 1536-d L2-normalized vector via add_batch without assertion error.
    - A 1536-d random vector run through l2_normalize then add_batch -> load_all round-trips bit-identically (no re-normalization drift on either backend).
    - The €50 ceiling test (test_budget.py) and SessionMeter tests (test_session_meter.py) still pass (dim is orthogonal to cost-per-token; this only guards against accidental regression).
  </behavior>
  <action>
Change `EMBEDDING_DIM = 768` to `EMBEDDING_DIM = 1536` in `src/vibemix/library/_cosine.py` — this is THE single source; every backend, embedder, search, and test reads it. Update the surrounding docstring/comment block (lines ~33-51) so the rollback note + "we ship 768" prose reflects 1536 (re-frame the rollback dims as 1536 -> 3072; these are documentation only — do NOT leave them claiming 768, it is misleading). Do NOT touch `embed.py`'s `output_dimensionality=EMBEDDING_DIM` calls — they already pull the constant.

Fix the ONE hardcoded literal that bypasses the constant: `src/vibemix/library/grounding.py` line ~125, `output_dimensionality=768` -> `output_dimensionality=EMBEDDING_DIM`. Confirm grounding.py imports EMBEDDING_DIM from `vibemix.library._cosine`; add the import if absent. This is the #1 correctness risk per RESEARCH — a 768-d grounding query against a 1536-d store dimension-mismatches in cosine_topk.

Fix the second-source fixture generator: `tests/library/fixtures/parity_corpus_100/_generate.py` hardcodes a LOCAL `EMBEDDING_DIM = 768`. Replace it with `from vibemix.library._cosine import EMBEDDING_DIM` and delete the local literal (keep SEED=42, N_TRACKS=100). The main `fixtures/_generate.py` already imports the constant — verify, no edit needed beyond the docstring.

Regenerate BOTH committed fixture corpora by running their own generators (they build from `rng.standard_normal((N, EMBEDDING_DIM))`, so they reshape to 1536 automatically and stay deterministic on the locked seeds):
  - `PYTHONPATH=src python3 tests/library/fixtures/_generate.py` -> rewrites synthetic_embeddings.npy + synthetic_queries.json.
  - `PYTHONPATH=src python3 tests/library/fixtures/parity_corpus_100/_generate.py` -> rewrites vectors.npz + track_ids.json.
Update the two generator docstrings that hardcode "768" / "(1000, 768)" / "(100, 768)" to say 1536 (cosmetic accuracy).

Update dim-pinned test assertions + hardcoded 768 literals (single-source them on EMBEDDING_DIM):
  - test_embed.py line ~286: `assert EMBEDDING_DIM == 768` -> `assert EMBEDDING_DIM == 1536`; fix `(768,)` docstrings to `(1536,)`.
  - test_search.py line ~47: `np.zeros(768, ...)` -> `np.zeros(EMBEDDING_DIM, ...)` (import EMBEDDING_DIM from vibemix.library).
  - test_importer.py lines ~43, ~135: `np.zeros(768, ...)` -> `np.zeros(EMBEDDING_DIM, ...)`.
  - test_grounding.py line ~27: `[0.1] * 768` -> `[0.1] * EMBEDDING_DIM`.
  - test_migrate_embeddings_2.py line ~108: `[0.1] * 768` -> `[0.1] * EMBEDDING_DIM`.
  Any other fake Gemini embedding response built with a literal-768 list: switch to EMBEDDING_DIM.

The L2-normalize invariant is the #1 risk: confirm (don't add new normalization) that the embed->store path normalizes 1536-d the same as 768-d. It does — `LibraryEmbedder._embed_audio`/text path and `embed_query` both call `l2_normalize` before returning, and `l2_normalize` is dim-agnostic. NumpyStore.add_batch trusts the caller's already-normalized vector (must NOT re-normalize — that breaks byte-parity). Verify by the behavior test, not by editing the store.

memory/ scope: do NOT change anything in src/vibemix/memory/ — it imports EMBEDDING_DIM from library._cosine, so memory.db auto-recreates at 1536. Just run tests/memory to confirm nothing pins a 768 literal; if a tests/memory/* test does, fix it the same way (EMBEDDING_DIM).

Do NOT touch int16 audio constants (32767/32768) anywhere. Do NOT add a gemini model literal outside _router_config.py.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -c "from vibemix.library._cosine import EMBEDDING_DIM; assert EMBEDDING_DIM==1536, EMBEDDING_DIM; import numpy as np; a=np.load('tests/library/fixtures/synthetic_embeddings.npy'); assert a.shape[1]==1536, a.shape; print('dim OK', a.shape)" && (grep -nE "output_dimensionality=768" src/vibemix/library/grounding.py && echo "FAIL: literal 768 still in grounding" && exit 1 || echo "grounding literal fixed") && PYTHONPATH=src python3 -m pytest tests/library/ tests/memory/ -q -m "parity or not parity" && bash scripts/release/check_no_hardcoded_model.sh</automated>
  </verify>
  <done>EMBEDDING_DIM==1536; grounding uses EMBEDDING_DIM (no literal 768 in src/); both fixture corpora regenerated at 1536; tests/library + tests/memory pass incl. the parity gate, €50 ceiling, and SessionMeter; model-literal grep gate clean; no memory dim edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proactive 3-excerpt path for 80-180s tracks (audio-cap hardening)</name>
  <files>src/vibemix/library/embed.py, tests/library/test_embed.py</files>
  <behavior>
    - A track with duration_s between the new single-call threshold (80s) and AUDIO_CAP_SECONDS (180s) takes the 3-excerpt path WITHOUT first attempting a single full-track upload.
    - A track with duration_s <= 80s still takes the single-call path (fast path preserved).
    - A track > 180s still takes the 3-excerpt path (unchanged).
    - The existing single-call -> cap-error -> force_excerpts fallback still works for <=80s tracks the API rejects (defense-in-depth, unchanged).
    - test_short_track_single_call and test_long_track_split_into_3_excerpts still pass (the long fixture is 480s, the short is 60s — both stay outside the gray zone, so only durations need a sanity check, no fixture rewrite expected).
  </behavior>
  <action>
In `src/vibemix/library/embed.py`, add a new locked constant near AUDIO_CAP_SECONDS:
`AUDIO_SINGLE_CALL_MAX_SECONDS = 80` with a why-comment citing the research note (emb-2's real-world single-call audio reliability degrades past ~80s on the GA SKU even though docs say 180s; we proactively excerpt to avoid a wasted full-track upload + retry on slow connections — matters for the subset bring-up). Keep `AUDIO_CAP_SECONDS = 180` as-is (it remains the hard-cap reference for `_is_audio_cap_error`). EXCERPT_DURATION stays 60.

In `_embed_audio`, change the single-call gate from `if duration_s <= AUDIO_CAP_SECONDS:` to `if duration_s <= AUDIO_SINGLE_CALL_MAX_SECONDS:`. Everything else (the cap-error catch -> `force_excerpts=True` -> excerpt path) stays. In `_extract_excerpts`, change the early-return guard `if duration_s <= AUDIO_CAP_SECONDS and not force:` to `if duration_s <= AUDIO_SINGLE_CALL_MAX_SECONDS and not force:` so a 100s track (now routed to excerpts) actually slices 3 clips instead of returning the whole file as one. Add `AUDIO_SINGLE_CALL_MAX_SECONDS` to `__all__`. Update the module docstring strategy section (currently ">180s -> excerpts") to state the new 80s single-call threshold.

Do NOT bump EXCERPT_STRATEGY_VERSION — a track that was single-call cached stays valid (the cache key already hashes file bytes + model + strategy version; nothing about a prior cached embed becomes wrong).

In test_embed.py: confirm the long-track fixture stays > 80 (it's 480) and the short-track fixture stays <= 80 (it's 60). Add `test_gray_zone_track_uses_excerpts` asserting a duration_s=120 track triggers `_extract_excerpts` (use the existing `fake_ffmpeg` fixture) and makes exactly 3 audio embed calls, with NO single-call attempt at 120s (call_count == 3, not 4).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -c "from vibemix.library.embed import AUDIO_SINGLE_CALL_MAX_SECONDS, AUDIO_CAP_SECONDS, EXCERPT_DURATION; assert AUDIO_SINGLE_CALL_MAX_SECONDS<=80 and AUDIO_CAP_SECONDS==180 and EXCERPT_DURATION==60, (AUDIO_SINGLE_CALL_MAX_SECONDS, AUDIO_CAP_SECONDS, EXCERPT_DURATION); print('cap fix OK')" && PYTHONPATH=src python3 -m pytest tests/library/test_embed.py -q</automated>
  </verify>
  <done>80-180s tracks take the 3-excerpt path with no single-call attempt; <=80s fast path + cap-error fallback both intact; test_embed.py green incl. new gray-zone test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: folder_ingest module — walk dir -> TrackEntry -> embed -> store + library.pkl cache</name>
  <files>src/vibemix/library/folder_ingest.py, src/vibemix/library/__init__.py, tests/library/test_folder_ingest.py</files>
  <behavior>
    - `scan_folder(path)` recursively yields supported audio files (.mp3 .m4a .wav .flac .aac), stdlib walk (Path.rglob), deterministic sorted order; ignores dotfiles and unsupported extensions; case-insensitive suffix match.
    - `folder_to_track_entry(file_path, duration_s)` returns a TrackEntry: track_id = stable namespaced hash of the absolute path; title = filename stem; artist/album="" ; bpm=0.0; key="" ; cues=() ; filepath=abspath; duration_s passed in.
    - `ingest_folder(root, embedder, store, *, persist_library=True, progress=None)` walks the folder, embeds each track via `embedder.embed_track`, calls `store.add_batch`, and returns an IngestReport (total, embedded, skipped_cached, failed; cost_estimate_eur from get_telemetry).
    - RESUMABLE: a file whose embedding is already cached (embedder.has_cached_embedding True) is counted skipped_cached and re-stored cheaply — re-running the same folder does ~0 API calls.
    - PARTIAL FAILURE: if embed_track raises (bad/too-long/corrupt file), the file is logged + counted failed and the loop CONTINUES; one bad file never aborts the run. NO faked embedding is ever stored for a failed file.
    - PROGRESS: the progress callback receives a per-track line (n/total, filename, ok|skip|err, running cost EUR).
    - The ingested TrackEntries are persisted to a try_load_cache-compatible pickle (RekordboxLibrary.CACHE_PATH / _CacheBlob shape) so `vibemix library search`/`similar` can resolve titles for folder-ingested tracks.
    - DIM-MISMATCH guard: if the active store already holds vectors at a different dim than EMBEDDING_DIM, ingest fails loud with an actionable message (recreate / wipe) rather than silently corrupting top-K.
    - ALL of the above is unit-testable with a FAKE embedder (inject an object exposing embed_track/has_cached_embedding) and a real NumpyStore in tmp_path — ZERO network, default pytest selection.
  </behavior>
  <action>
Create `src/vibemix/library/folder_ingest.py`. Mirror rekordbox.py conventions (SPDX header, `from __future__ import annotations`, module docstring describing data flow, snake_case, module logger). Reuse ffprobe (ships with the already-pinned ffmpeg dep): duration via `ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 <file>` through `subprocess.run` (capture, timeout, `shutil.which("ffprobe")` fail-loud like embed._require_ffmpeg). Make the probe injectable (default real ffprobe, tests pass a stub) so tests need no real binary.

Public surface:
  - `SUPPORTED_SUFFIXES = (".mp3", ".m4a", ".wav", ".flac", ".aac")`
  - `def scan_folder(root: Path) -> list[Path]` — recursive (rglob), filtered, sorted.
  - `def probe_duration_s(path: Path) -> float | None` — ffprobe wrapper; None on any failure.
  - `def folder_to_track_entry(path: Path, duration_s: float) -> TrackEntry` — track_id = `"folder:" + hashlib.sha1(str(path.resolve()).encode()).hexdigest()[:16]` (stable; prefix namespaces it from Rekordbox ids).
  - `@dataclass IngestReport`: total/embedded/skipped_cached/failed: int; cost_estimate_eur: float; failures: list[tuple[str,str]] (filepath, error); `as_dict()`.
  - `def ingest_folder(root, embedder, store, *, persist_library=True, progress=None) -> IngestReport`.

ingest_folder flow: scan -> for each file: probe duration (if None: log `[ingest err] <file>: unprobeable`, count failed, continue — honest: never embed something we can't probe); build TrackEntry; record `embedder.has_cached_embedding(entry)` for skip/embed accounting; call `embedder.embed_track(entry)` inside try/except (broad Exception -> log `[ingest err] <file>: <e>`, append to failures, count failed, continue — store nothing); on success `store.add_batch([(entry.track_id, vec)])` and count embedded (or skipped_cached if the pre-check was True); emit a progress line carrying `get_telemetry().current_cost_estimate_eur()`. Accumulate ALL successfully-handled entries (embedded + skipped_cached) for the library index. After the loop, if persist_library: build a `RekordboxLibrary`, set `.tracks = {entry.track_id: entry, ...}`, and write the pickle cache (replicate the `_CacheBlob` write to `RekordboxLibrary.CACHE_PATH`: version=RekordboxLibrary.SCHEMA_VERSION, xml_path="" / a folder marker, xml_mtime=time.time(), tracks=dict) so `try_load_cache()` later repopulates titles. If RekordboxLibrary exposes a private cache-writer, prefer reusing it; otherwise write the pickle directly with the same `_CacheBlob` shape.

DIM-mismatch guard (before the loop): probe via `store.search(np.zeros(EMBEDDING_DIM, dtype=np.float32), k=1)` inside a try — if it raises an assertion/shape/ValueError, the stored dim differs; raise `RuntimeError("Library store holds vectors at a different dimensionality than EMBEDDING_DIM=1536. The on-disk index is stale (likely a 768-dim build). Delete ~/.cache/vibemix/library.db (and library_vectors.npy/library_ids.json for the numpy backend) and re-run embed-folder.")`. (The DB has 0 rows so the happy path no-ops; this only fires against a stale 768 index.)

Export `ingest_folder`, `IngestReport`, `scan_folder`, `folder_to_track_entry`, `probe_duration_s`, `SUPPORTED_SUFFIXES` from `src/vibemix/library/__init__.py` (imports + `__all__`).

Tests in tests/library/test_folder_ingest.py (inject a fake embedder object exposing `embed_track` returning a fixed `l2_normalize(np.ones(EMBEDDING_DIM, dtype=np.float32))` and `has_cached_embedding`; use a real NumpyStore in tmp_path; monkeypatch `probe_duration_s` to a constant so no real ffprobe/files needed, OR write tiny placeholder files + monkeypatch probe): scan filtering + sort determinism + dotfile/unsupported exclusion; track_id stability + title-from-stem; resumable skip accounting (has_cached_embedding True -> skipped_cached, embed_track still gets a vector for re-store but counted skip); partial-failure-continues (one entry's embed_track raises -> failed==1, others succeed, store has N-1 rows, NO faked vector for the failed one); progress callback receives lines; library.pkl written so a fresh `RekordboxLibrary().try_load_cache()` repopulates the titles. NO network marker — default selection.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -c "from vibemix.library import ingest_folder, IngestReport, scan_folder, folder_to_track_entry, SUPPORTED_SUFFIXES; print('folder_ingest exports OK')" && PYTHONPATH=src python3 -m pytest tests/library/test_folder_ingest.py -q && bash scripts/release/check_no_hardcoded_model.sh</automated>
  </verify>
  <done>folder_ingest.py exists with scan/probe/entry/ingest surface; resumable + partial-failure-tolerant + honest (no faked embeds) + persists a try_load_cache-compatible library.pkl + dim-mismatch fail-loud; exported from the package; tests green with a fake embedder, zero network; model-literal gate clean.</done>
</task>

<task type="auto">
  <name>Task 4: CLI wiring — `embed-folder` subcommand + folder-aware query usability</name>
  <files>src/vibemix/__main__.py</files>
  <action>
In `_build_library_subparsers` (src/vibemix/__main__.py), add an `embed-folder` subparser:
  - positional `path` (folder to ingest, recursive)
  - `--json` flag (emit the IngestReport as JSON instead of human progress)
  - `set_defaults(func=_cmd_library_embed_folder)`.

Add `_cmd_library_embed_folder(args)` mirroring `_cmd_library_search`'s proxy-client boot (VIBEMIX_PROXY_JWT / VIBEMIX_PROXY_BASE_URL env, build_proxy_genai_client, fail with a clear stderr message + exit 1 if JWT unset). Then:
  - resolve + validate the folder path (Path(args.path); error+exit 1 if not a dir),
  - `embedder = LibraryEmbedder(client)` ; `store = open_store()`,
  - `from vibemix.library import ingest_folder` ; call it with a progress callback that prints `[n/total] <ok|skip|err> <filename>  ~€{running_cost:.4f}` to stdout (unless --json, in which case suppress per-line and dump the final IngestReport.as_dict()),
  - wrap in try/finally store.close(),
  - print a final summary line (embedded/skipped/failed/total + final cost estimate) and the library.pkl path so Kaan knows search/similar will now resolve titles.

Verify the existing `search` and `similar` subcommands work against the folder-ingested library WITHOUT modification: they read `RekordboxLibrary().try_load_cache()` for titles and `open_store()` for vectors — both of which folder_ingest now populates. Do NOT modify `_cmd_library_search`/`_cmd_library_similar`. Document in the SUMMARY that the two query modes are: free-text vibe via `vibemix library search "<text>"`, and seed-track via `vibemix library similar <track_id>` where the track_id is the `folder:<hash>` id that embed-folder prints (run embed-folder with `--json` to copy a track_id as a seed). Do NOT add a new `nearest <file>` subcommand — `search` + `similar` cover both modes per the research brief (planner's discretion: keeping the existing track_id contract avoids a redundant second embed of the seed file).

Keep the `library` dispatch in `cli_entry` untouched (it already routes `raw_argv[0]=="library"` before _parse_args). Do NOT inline any gemini model literal (CI grep gate) — the embedder resolves the model itself.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai && source .venv/bin/activate && PYTHONPATH=src python3 -c "
import contextlib, io
from vibemix.__main__ import _run_library_cli
buf = io.StringIO()
try:
    with contextlib.redirect_stdout(buf): _run_library_cli(['embed-folder', '--help'])
except SystemExit: pass
out = buf.getvalue()
assert 'embed-folder' in out or 'path' in out, out
print('embed-folder subcommand wired')
" && bash scripts/release/check_no_hardcoded_model.sh && PYTHONPATH=src python3 -m pytest tests/library/ tests/memory/ -q -m "parity or not parity"</automated>
  </verify>
  <done>`vibemix library embed-folder <dir>` is wired (positional path + --json), boots the proxy client, prints per-track n/total progress + running €, tolerates partial failure, writes the library.pkl, and search/similar resolve folder-ingested titles unchanged; model-literal gate clean; full library + memory suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local folder -> embedder | Arbitrary user files (any name/encoding/length) cross into ffprobe + Gemini upload |
| CLI -> Bravoh proxy -> Gemini | Audio bytes uploaded over network; cost incurred; JWT auth |
| stored vectors -> cosine_topk | Dim/dtype contract must hold or asserts fire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-gz2-01 | Tampering | dim bump (768->1536) silently breaking cosine_topk | mitigate | grounding.py literal -> EMBEDDING_DIM; both stores recreate at 1536; parity fixtures regenerated; cosine_topk shape-asserts guard mismatch; folder_ingest dim-mismatch fail-loud guard |
| T-gz2-02 | Denial of Service | one corrupt/huge/too-long file aborting the whole 1547-file run | mitigate | ingest_folder catches per-file (broad Exception + unprobeable), logs [ingest err], counts failed, continues (Task 3 fault-tolerance test) |
| T-gz2-03 | Information Disclosure | raw GEMINI_API_KEY in distributed binary | accept (out of scope) | proxy-only path enforced by LibraryEmbedder; CLI uses VIBEMIX_PROXY_JWT, never an AIza key — pre-existing invariant, unchanged here |
| T-gz2-04 | Denial of Service | €50 ceiling breached by a large folder embed | mitigate | BudgetTelemetry already auto-increments + warns at 90%; CLI surfaces running € per file so Kaan can abort if it spikes; €50 ceiling test still passes (Task 1) |
| T-gz2-05 | Spoofing | embedding a file we couldn't probe -> garbage/faked vector | mitigate | unprobeable files are counted failed + skipped, never embedded; failed embeds store nothing (Task 3 honesty contract) |
| T-gz2-06 | Tampering | hardcoded gemini model literal slipping into new code | mitigate | model resolved via model_router.resolve("embedding") only; scripts/release/check_no_hardcoded_model.sh run in every task's verify |
| T-gz2-SC | Tampering | npm/pip/cargo installs | accept | NO new dependencies added (ffprobe ships with the already-pinned ffmpeg; pure-stdlib folder walk) — no package-install task exists in this plan |
</threat_model>

<verification>
After all four tasks:
1. `EMBEDDING_DIM == 1536` everywhere (single source); no `output_dimensionality=768` literal remains in src/.
2. Default pytest selection green: `cd repo && source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q` (no markers) AND the opt-in parity gate `PYTHONPATH=src python3 -m pytest -m parity -q` green (fixtures regenerated at 1536).
3. €50 ceiling test (test_budget.py), sqlite-vec↔numpy parity, and SessionMeter tests all still pass.
4. `vibemix library embed-folder <dir>` wired (`--help` clean); folder_ingest runs against a tmp folder/store with a fake embedder and ZERO network in tests.
5. `vibemix library search "<text>"` and `vibemix library similar <folder:hash>` resolve titles for folder-ingested tracks (library.pkl written by ingest).
6. `scripts/release/check_no_hardcoded_model.sh` exits 0.
</verification>

<success_criteria>
- Kaan can run `vibemix library embed-folder /Users/ozai/Music/hardtechno`, watch n/total progress with a running € estimate, re-run it at ~0 cost (cache), and have it survive a bad file mid-run.
- Kaan can run `vibemix library search "techno"` (or `similar <folder:hash>`) and see top-K nearest tracks with similarity + filename, to judge "does the techno embedding pull the techno out".
- Embeddings are 1536-dim, L2-normalized, stored bit-identically across both backends; no quality/parity/budget test regressed.
- Audio tracks longer than 80s take the safe 60s 3-excerpt path; no raw >cap audio Part is ever sent.
</success_criteria>

<output>
Create `.planning/quick/260525-gz2-embed-real-music-library-dim-1536-folder/260525-gz2-SUMMARY.md` when done.
</output>
