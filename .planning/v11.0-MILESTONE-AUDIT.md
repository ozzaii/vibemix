---
milestone: v11.0
milestone_name: Earned
audited: 2026-05-30
status: passed
scores:
  requirements: 16/16
  phases: 3/3
  integration: 4/4
  flows: 1/1
gaps: {}
tech_debt: []
deferred_human_verification:
  - id: "§EARNED-MASTERED-VOCAL-EAR"
    severity: blocking
    phase: 104
    kind: ear_pass
    note: "Kaan ear-pass on the rare grounded Mastered unlock vocal tone (real friend vs gamification slop). The tone-gated, fixture-pinned, fire-once vocal is shipped + test-green; only the subjective tone verdict is parked."
  - id: "§EARNED-LIVE-MASTERED-VERIFY"
    severity: blocking
    phase: 103
    kind: hardware_verify
    note: "Real-FLX4 live verify: a cited event advances Mastered, an un-cited moment does not + the live-app GUI round-trip (cargo tauri dev). Engine is synthetic-verified across 102/103/104; the live hardware round-trip + the real Event→recognize time path is the parked obligation (deferred per the frozen dev-sidecar reality)."
  - id: "§EARNED-SURFACE-DESIGN-GATE"
    severity: non_blocking
    phase: 104
    kind: design_ratify
    note: "Kaan ratifies the final panel + celebration treatment. Default shipped frontend-enforcement-compliant (Earned Wall + per-stage shape glyph + quiet fill)."
  - id: "§EARNED-MASTERY-THRESHOLD-TUNE"
    severity: non_blocking
    phase: 103
    kind: tune
    note: "Per-skill N (grounded-demo count to flip Mastered) ships with a sane default (3); Kaan tunes after the live verify."
---

# v11.0 "Earned" — Milestone Audit

**Verdict: PASSED.** All 16 REQ-IDs satisfied across 3 phases, every phase VERIFICATION.md
`status: passed`, cross-phase integration wired + test-pinned, the end-to-end "earn a skill"
flow complete. Four KAAN-ACTION items (2 blocking ear/hardware passes, 2 non-blocking
ratify/tune) are parked for public-ship discharge — none is an engineering gap.

## Definition of done (charter)

> Turn the v9.0 "Lesson One" module into an evidence-grounded DJ skill-tree: ~6 real DJ
> competencies each level up through a two-stage, evidence-grounded mastery bar — lessons
> fill a skill to "Competent", and only a real, **cited** live demonstration unlocks
> "Mastered". A small, contained gamification layer that adds the felt reward of
> progression **without growing the product surface**.

**Met.** Six skills, two-stage bars; Competent is recital-gated (COMP-02, click-through
can't reach it); Mastered is citation-gated (MAST-03, un-cited/fabricated → zero credit);
the surface is a single read-only Earned Wall + one rare grounded vocal — zero new AI
provider / ws port / IPC envelope family / heavy dep / live detector (anti-creep held).

## Requirements coverage — 16/16 satisfied (3-source cross-reference)

| Group | REQ-IDs | Phase | VERIFICATION | SUMMARY | REQUIREMENTS | Final |
|-------|---------|-------|--------------|---------|--------------|-------|
| SKILL | 01·02·03 | 102 | passed | listed | `[x]` | **satisfied** |
| COMP | 01·02 | 102 | passed | listed | `[x]` | **satisfied** |
| DATA | 01·02·03 | 102 | passed | listed | `[x]` | **satisfied** |
| MAST | 01·02·03·04 | 103 | passed | listed | `[x]` | **satisfied** |
| SURF | 01·02·03·04 | 104 | passed | listed | `[x]` (updated this audit) | **satisfied** |

No unsatisfied requirements · no orphans (every REQ-ID resolves to a phase VERIFICATION).

## Phases — 3/3 passed

| Phase | Plans | VERIFICATION | Score |
|-------|-------|--------------|-------|
| 102 Skill-Tree Engine + Data Model + Competent Stage | 2/2 | passed | 8/8 must-haves + 3 invariant pins |
| 103 Live "Mastered" Grounding | 2/2 | passed | 14/14 must-haves |
| 104 Skill-Tree Surface + Earned Celebration | 2/2 | passed | 4/4 SURF REQs |

## Cross-phase integration — 4/4 wired (verified inline; built + test-pinned this run)

The 102→103→104 seam was authored and tested end-to-end in this run, so wiring is verified
inline against the test pins rather than via a separate checker:

1. **102 → 103 (engine → live writer).** `record_live_demo` (103) reads the DERIVED
   `SkillTree.compute(progress).competent` (102 MAST-01 gate) before any write, and writes
   the 102 `skills` live-portion block. Pinned: `test_coach_skill_credit.py` `_make_competent`
   (102) feeding `_credit_live_skill_demo` → `record_live_demo` (103).
2. **103 → 104 (credit → vocal).** `_credit_live_skill_demo` (103 live site) snapshots
   `mastered` before crediting and, on the not-mastered→mastered flip, calls
   `mastered_unlock_line` (104) once via the `speak` hook. Pinned:
   `test_mastered_flip_speaks_the_vocal_exactly_once` (+ non-flip/un-cited silent).
3. **102 + 103 → 104 (state → surface).** `skill_wall_payload` (104) folds `SkillTree.compute`
   (102) incl. the 103 live-portion + the single-source `what_remains` line; the seam is
   validator-verified (payload shape == schema `required[]`, codegen zero-diff) and rides the
   existing `ipc.learn.progress_state` on `:8765` (Inv #4). Pinned: `test_skill_wall_payload.py`
   + `test_skill_wall_what_remains.py` + `SkillWall.ts` vitest.
4. **Honesty seam.** `SkillSpec.live_creditable` (104) drift-pinned vs
   `_HONEST_UNCREDITABLE_V11` (103) — `what_remains` never promises a Mastered path a skill
   lacks (beatmatching). Pinned: `test_creditability_drift.py`.

## End-to-end flow — 1/1 complete

**"Earn a skill":** finish a skill's lessons + pass its recital → Competent (102) → play a
real set; a cited live event credits the skill → after N cited demos the bar flips to
Mastered (103) → the Earned Wall repaints the gold star + a single rare grounded vocal fires
once (104). Every junction is test-covered; the only live-hardware leg (`Event`→`recognize`
time round-trip on real FLX4) is the parked `§EARNED-LIVE-MASTERED-VERIFY`.

## Cardinal invariants — all held (additive design)
#1 single-writer (skill_tree sole skill-state writer, AST-pinned) · #2/#3 grounding (Mastered
credit + vocal fire ONLY on a resolvable citation) · #4 one socket (rides existing
`learn.*`/:8765) · privacy (skill data never in `profile.json`).

## Test evidence
`tests/learn` 751 · `tests/runtime` 344 · vitest 1430 (149 files) · ruff clean · vite build OK ·
codegen zero-diff. Engineering green; live-app GUI ear-pass parked per the honest-green rule.

## Tech debt
**None.** No stubs / TODOs / shortcuts introduced. The four parked items are human-verification
ratifications (ear-pass / hardware-verify / design-ratify / threshold-tune), not debt.
