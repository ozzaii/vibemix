---
gsd_state_version: 1.0
milestone: v9.0
milestone_name: Lesson One
status: executing
last_updated: "2026-05-28T06:28:36Z"
last_activity: 2026-05-28
progress:
  total_phases: 14
  completed_phases: 3
  total_plans: 24
  completed_plans: 23
  percent: 23
---

# vibemix — State

## Current Position

Phase: 94 (Course 1 — Anatomy: L1.01..L1.16) — EXECUTING (94-01 + 94-02 SHIPPED — 94-01 commits eda18aea + e00718e4 (16 fixtures + curriculum.py extension); 94-02 commits 60bc7966 → 89a98d81 → 9a9cddd6 (TDD RED: tests/learn/test_no_tutor_slop_blocklist.py 13 cases pinning the public surface of the gate that does not yet exist → GREEN: scripts/launch/check_no_tutor_slop.py 347-line CLI gate, 35 tutor-tic tokens across 4 forbidden-move categories with >=4 each, multi-word preview-tic design `now let's`/`later we'll`/`today we'll be learning` encodes the L1.01 `Let's go.` exception in token design instead of as a special-case skip, import-time `assert set(TUTOR_SLOP_BLOCKLIST) == set(_CATEGORY)` catches future planner drift at module-load, `_walk_string_values(node)` depth-first generator deep-scans every string descendant so future fixture shapes inherit the gate automatically, JSON parse errors reported under separate JSON_PARSE_ERROR category per T-94-02-04 mitigation → tests/learn/test_tutor_prompts_byte_equality.py 10 cases with `_ICONIC_DIALOG_LINES` single-source-of-truth tuple + Kaan-action-ratification comment block, 4 parametrized byte-equality cases on L1.01 tutor_speak[0..3].text, dedicated U+2018/U+2019 curly-quote sentinel catches the most likely real-world drift vector, closer-only-on-beat-3 anti-creep gate guards against self-improving-agent line-spreading; smoke-tested with deliberate `don't` → `don´t` (U+2019) drift on beat 3 — both the parametrized beat3 case AND the dedicated curly-quote sentinel fired red with actionable messages; fixture restored byte-equal before commit, git status clean on the fixture; python scripts/launch/check_no_tutor_slop.py from repo root = exit 0, "17 fixture(s) scanned, 0 slop hits, 0 parse errors"). Full tests/learn/ suite 104 pass / 3 pre-existing skip (+23 new test cases, no regressions from the 81/3 baseline). TONE-01 (byte-equality) + TONE-03 (slop blocklist) requirements both flipped complete; ready for Plan 94-03 (LessonRuntime / RecitalRuntime wiring of exemplar_cycle + recital_pool against the now-locked fixtures).
Phase: 93 (Exemplar Engine + [exemplar:] Evidence Source) — SHIPPED 2026-05-28 (all 6 plans complete; engineering-complete)
Phase: 92 (Lesson Runtime + AI Highlight Contract) — SHIPPED 2026-05-28 (92-01..92-06 SHIPPED — see git log; LessonRuntime FSM, IPC envelopes, controller renderer highlight contract, progress persistence, learn-window UI surfaces, settings drawer LearnGroup all green at 1029 passed / 2 skipped / 15 todo)
Status: P94 in flight (2 of 4 plans complete); ready for 94-03 (LessonRuntime / RecitalRuntime wiring of exemplar_cycle + recital_pool against the now byte-locked L1.01 + slop-gated transcripts)
Last activity: 2026-05-28

## Milestone Reference

See: `.planning/PROJECT.md` § Current Milestone (updated 2026-05-27)
See: `.planning/ROADMAP.md` § v9.0 (just added 2026-05-27)
See: `.planning/REQUIREMENTS.md` (72 v9.0 REQ-IDs across 9 categories)
See: `.planning/research/SUMMARY.md` (locked 14-axis reconciliation)

**Goal:** Turn vibemix into the AI teaching module beginner DJs need. Three progressive courses (Anatomy / Transitions / Play Mode) driven by a real-time interactive vector visualization of the user's MIDI controller. Every lesson grounded — controls highlighted on the rendered controller, EQ bands demonstrated audibly via library-pulled exemplar tracks, Course-3 live coaching grounded on CueAnchor + phrase detection.

**Phase numbering:** continues from P88 (P89/P90 = direct wire-ins) → milestone runs **P91 → P98**.

## Phase Spine (P91-P98)

```
P91 (Controller Renderer + MIDI Mirror) ──┬──► P92 (Lesson Runtime + AI Highlight Contract)
                                          │         │
                                          └──► P93 (Exemplar Engine + [exemplar:] evidence) ──┐
                                                                                              │
   P92 ───► P94 (Course 1 Anatomy) ───► P95 (Course 2 Transitions) ◄──────────────────────────┘
                                                            │
                                                            └────► P96 (Course 3 Play Mode + tutor lens proactive)
                                                                              │
                                                                              └─► P97 (Onboarding + Tone Locks + Mode Picker)
                                                                                              │
                                                                                              └─► P98 (Live Audit + Ear-Pass + rc1 Regression Smoke)
```

`P91 ∥ P93` parallelizable. `P93` feeds cite-grounding for `P95` and `P96`.

## Active Sources

- `.planning/PROJECT.md` § Current Milestone — vision + constraints + invariants
- `.planning/ROADMAP.md` § v9.0 "Lesson One" — 8-phase plan + KAAN-ACTION queue
- `.planning/REQUIREMENTS.md` — 72 v9.0 REQ-IDs · 100% traceability
- `.planning/research/SUMMARY.md` — 14-axis reconciliation (locked)
- `.planning/research/{STACK,FEATURES,ARCHITECTURE,PITFALLS}.md` — depth sources
- `.planning/handoffs/2026-05-27-session-end.md` — concurrent rc1 ship work (parked, Kaan ear-pass)

## Pre-Milestone Direction (carried forward)

From Kaan's verbatim brief (2026-05-27):

- **Iconic opening dialog (verbatim-locked, byte-equality test in P94):**
  - user: "Hello vibemix, what are you?"
  - vibemix: "I'm the best DJ app in the world."
  - user: "If you are the best, then who the fuck am I?"
  - vibemix: "Oh bestie, don't worry. You know why? Because I'm the beginner module of vibemix. Let's go."
- **The teaching mechanic:** AI highlights a control on the rendered visualization of the user's MIDI controller → user touches the physical control → lesson advances. For EQ lessons, the AI plays a library track exemplifying that band so the user HEARS the change.
- **Three courses:** (1) Anatomy of a Deck (16 lessons), (2) Transitions (14 lessons), (3) Play Mode (6 lessons + proactive tutor lens) = 36 total.
- **Default-YES on every candidate feature** — fully-comprehensive curriculum.
- **All-opus agents · maximum effort · best-of-the-best UI · impeccable skills.**
- **Kaan will plug DJ set into computer for autonomous overnight testing** — full computer access for fuck-around-find-out.

## Anti-Creep Acid Test (v9.0, LOCKED)

> *"Does this phase deliver a working slice of the BEGINNER (level 1, all 10 mapped controllers, 36-lesson curriculum, library exemplar engine + packaged fallback, scripted curriculum with grounded AI interjections) module — WITHOUT adding a new AI provider, new ws port, new IPC envelope family beyond `learn.*`, new DSP library, new content beyond the 36 hand-authored lessons, or new community/multi-user feature surface? Does it NOT regress any of the 4 cardinal invariants or the rc1 bundle fix?"*

If a phase doesn't pass → defer to v9.x or Bravoh.

## Concurrent Work (other sessions)

3 Codex sessions active on `live-tuning-or-brain` — commit by named paths only, never `git add -A`. rc1 product-sweep open in this branch; v9.0 work additive (new `src/vibemix/learn/` subpackage, extends but does not modify existing engines).

## Hard Rules (carried)

- **Privacy** — all OZ/Hermes/LM-Studio off-limits paths (see CLAUDE.md) — Kaan's overnight autonomous run does NOT extend this permission. Per-turn permission still expires at turn end.
- **One-socket invariant (#4)** — every `learn.*` IPC envelope rides `127.0.0.1:8765` (pinned by `tests/learn/test_no_new_ws_port.py` in P92).
- **Single-writer (#1)** — `MusicState` write-locked to `state/refresh.py`. `LearnState` lives separately under `src/vibemix/learn/`, sole writer = `LearnRuntime` (pinned by `tests/learn/test_runtime_invariants.py` in P92).
- **Trust-the-audio (#3)** — Course 3 tutor narration grounded on `[cue:<anchor_id>]` evidence + bpm/phrase confidence gates; never invents upcoming structure. `tests/learn/test_no_speculative_phrase.py` AST gate lands in P96 BEFORE Gemini wiring.
- **Citation-grounding (#2)** — AI tutor claims about exemplar tracks resolve via `EvidenceRegistry` through NEW `[exemplar:<track_id>]` source (4-site mirror in P93); fabricated `[exemplar:bogus]` strips whole turn.
- **Apache-clean** — no AGPL/GPL deps (essentia excluded); stylized controller renders, not Pioneer faceplate art; Mixxx-precedent nominative fair use posture.
- **Honest green** — every learn-engine module offline-unit-testable; live-app verification HARD per `feedback_verify_live_app_not_just_tests` (cargo tauri dev + ui.log). rc1 standalone sidecar smoke MUST PASS unregressed (P98 gate).
- **AI-slop blocklist** — every lesson copy runs through `scripts/launch/check_no_ai_slop.py` + NEW `scripts/launch/check_no_tutor_slop.py` v2 blocklist (≥20 tutor-tic tokens) — both CI-gated (P94).
- **codegen:ipc** — MANDATORY after every `messages.schema.json` edit (ajv validator pre-compiled — per `feedback_schema_edit_needs_codegen_ipc`).

## v9.0 KAAN-ACTION Queue (parked, never faked)

**BLOCKING (must resolve before v9.0 public ship):**

- `§LEARN-LEGAL-DISCLAIMER` (P98 / AUDIT-03) — Francesco/lawyer sight-check on rendered controllers + disclaimer copy
- `§LEARN-EAR-COURSE-1` (P98 / AUDIT-01) — Kaan ear-pass on Course 1 (16 lessons) on real FLX4
- `§LEARN-EAR-COURSE-2` (P98 / AUDIT-01) — Kaan ear-pass on Course 2 (14 lessons) on real FLX4
- `§LEARN-EAR-COURSE-3` (P98 / AUDIT-01) — Kaan ear-pass on Course 3 (6 lessons) on real FLX4 mid-set
- `§LEARN-CUE-DECISION` (P96 ratify) — Course 3 proactive count-ins (needs `[cue:]`) OR retrospective-only? Default-YES = count-ins

**NON-BLOCKING (ride forward):**

- `§LEARN-CONTROLLER-EAR` — 9 non-FLX4 controllers live-verify (FLX4 = canonical golden)
- `§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` — BlackHole/Multi-Output Device wizard for master+cue split
- `§LEARN-CLAP-FIRST-RUN-UX` — CLAP ONNX model first-run download UX
- `§LEARN-OVERNIGHT-DISCIPLINE` — One-page overnight-run handoff doc
- `§LEARN-LATENCY-CONTINGENCY` — If P91 measures >80 ms P95, Rust-direct `midir` amendment
- `§LEARN-MK2-DETECTION` — Hercules Inpulse 300 vs 300-MK2 live-verify
- `§LEARN-FIRMWARE-VARIANTS` — DDJ-FLX4 v1.07 variant detection
- `§LEARN-OFFLINE-TONE-PATH` — Codex tutor parity deferred to v9.x
- `§LEARN-LOCALIZATION-IT-TR` — en-only v9.0; v9.1 drops `tr.py`/`it.py`
- `§LEARN-PEDAGOGY-INSTRUCTOR-REVIEW` — 36-lesson ordering past beginner-track DJ instructor

## Carried-Forward KAAN-ACTION (from prior milestones)

- rc1 ear-pass + signed-release A/B/C decision (`.planning/handoffs/2026-05-27-session-end.md`)
- v8.2 UI-02 funded-key ear-pass (PROJECT.md)
- v8.0 §GH-BILLING / §SHIP-V4 / §V7-LIVE

## Next Action

```
/gsd:execute-phase 94
```

P94-02 SHIPPED — commits 60bc7966 (Task 1 TDD RED: tests/learn/test_no_tutor_slop_blocklist.py 13 cases pin the public surface of scripts/launch/check_no_tutor_slop.py — TUTOR_SLOP_BLOCKLIST + _CATEGORY + _COPY_FIELDS + check_no_tutor_slop + main, real-transcripts-pass, synthetic-fail with stderr-token-naming, blocklist-size ≥20, 4 categories × ≥4 tokens parametrized, deep-scan covers tutor_speak[].text + hints[].text + exemplar_cycle[].tutor_speak + recital_pool[].prompt + recital_outcomes.pass, 3 CLI gate paths default-dir / --quiet / --dir <empty>, iconic-dialog `Let's go.` exception does not trip the gate; failed RED at collection time with ModuleNotFoundError) → 89a98d81 (Task 1 GREEN: scripts/launch/check_no_tutor_slop.py 347 lines, sibling to scripts/launch/check_no_ai_slop.py — same architectural shape, independent domain. Module-level: TRANSCRIPTS_DIR + TUTOR_SLOP_BLOCKLIST (35 distinct tokens — 12 compliment / 5 summary / 7 preview / 11 upbeat-hook) + _CATEGORY (35 entries, every token mapped) + _COPY_FIELDS (informational documentation). Import-time `assert set(TUTOR_SLOP_BLOCKLIST) == set(_CATEGORY)` catches future planner drift at module-load. `_walk_string_values(node)` depth-first generator yields every string descendant of any JSON-decoded shape so future fixture-shape additions inherit the gate automatically. Multi-word preview-tic tokens `now let's` / `later we'll` / `today we'll be learning` encode the L1.01 `Let's go.` exception in token design instead of as a special-case skip. JSON parse errors reported under separate JSON_PARSE_ERROR category per T-94-02-04 mitigation. main(argv) argparse over --dir / --quiet; `if __name__ == "__main__": sys.exit(main())` trailer; CLI from repo root exits 0 with "17 fixture(s) scanned, 0 slop hits, 0 parse errors") → 9a9cddd6 (Task 2: tests/learn/test_tutor_prompts_byte_equality.py 10 cases pin the L1.01 iconic 4-line opening dialog byte-equal. _ICONIC_DIALOG_LINES single-source-of-truth tuple with explicit Kaan-action-ratification comment block. Tests: existence + JSON-parse + ≥4 beats + tuple has exactly 4 entries + 4 parametrized byte-equality cases (beat0 "Hello vibemix, what are you?" / beat1 "I'm the best DJ app in the world." / beat2 "If you are the best, then who the fuck am I?" / beat3 "Oh bestie, don't worry. You know why? Because I'm the beginner module of vibemix. Let's go.") + closer-only-on-beat-3 anti-creep gate + dedicated U+2018/U+2019 curly-quote sentinel as separate assertion with clearer failure message than generic byte-equality diff. Smoke-tested with deliberate `don't` → `don´t` (U+2019) drift on beat 3 — both the parametrized beat3 case AND the dedicated curly-quote sentinel fired red with actionable messages; fixture restored byte-equal before commit, git status clean on src/vibemix/learn/transcripts/course_1_anatomy/01_welcome.json). **Zero deviations** — plan `<action>` blocks ported verbatim; blocklist size landed at 35 vs planner's ≥20 floor (extra 15 are Khanmigo/Duolingo canonical motivator-tics the planner didn't explicitly enumerate but are well-attested in tutor-tic literature; cost zero, benefit hedges against future ear-pass discoveries); deep-scan walks every string descendant (broader than the planner's enumerated _COPY_FIELDS); curly-quote sentinel is an additional gate beyond the planner's spec (defensive against the most likely real-world drift vector — text editor / IDE auto-format pass silently introducing U+2019). All within the planner's `<action>` block phrasing — stronger executions of intent, not divergence. Full tests/learn/ suite **104 passed / 3 pre-existing skip** (+23 new cases, no regressions from the 81/3 Plan 94-01 baseline; 3 skips are pre-existing P93 packaged-bank deferrals). Plan-level verify both green: `python scripts/launch/check_no_tutor_slop.py` from repo root → exit 0, `pytest tests/learn/test_no_tutor_slop_blocklist.py tests/learn/test_tutor_prompts_byte_equality.py -v` → 23 passed. Concurrent-session discipline observed (3 sibling Codex sessions in flight on shared files; named-path commits only — never `git add -A`). **TONE-01 (byte-equality) + TONE-03 (slop blocklist) requirements both flipped complete — Phase 94 ready for Plan 94-03 (LessonRuntime / RecitalRuntime wiring of L1.14 exemplar_cycle low/mid/high + L1.16 recital_pool sampling against the now byte-locked + slop-gated fixtures).** Threat register: T-94-02-01 (iconic dialog drift) MITIGATED by tests/learn/test_tutor_prompts_byte_equality.py + smoke-tested gate bite; T-94-02-02 (tutor-tic creep across future courses) MITIGATED by transcripts_dir.rglob("*.json") that walks subdirs so P95/P96/P97 lesson fixtures inherit the gate without per-course wiring; T-94-02-04 (malformed JSON crashes gate) MITIGATED by try/except around json.loads with parse errors reported under separate JSON_PARSE_ERROR category.

P93-06 SHIPPED — commits d195bbf1 (Task 1 RED: 3 failing tests for compute_band_shares opt-in in tests/library/test_folder_ingest_band_shares.py — TypeError on missing kwarg) → 946a39cf (Task 1 GREEN: ingest_folder grows `compute_band_shares: bool = False` keyword-only arg + 27-line additive per-track block in the post-`handled[entry.track_id] = entry` success path; lazy imports `vibemix.learn.exemplar.compute_band_shares` + `vibemix.learn.band_share_store.{open_default_db, upsert}` ONLY when the flag is on; best-effort try/except logs `[band-share err] <path>: <exc>` via the existing module logger and continues — a band-share raise NEVER aborts an otherwise-good CLAP ingest; outer IngestReport.embedded/skipped_cached/failed counters never incremented by the band-share path. Default-False keeps every legacy `vibemix library ingest` caller byte-identical) → 1cd9bc04 (Task 2 RED: lift module-level `_is_cli_wired()` probe + pytest.skip in tests/learn/test_cli_learn_exemplar.py; 3 subprocess tests fail because the legacy "available: reset" usage-fallback line lacks the `exemplar` token) → 084b59d8 (Task 2 GREEN: wire `vibemix learn exemplar <band>` dispatch in __main__.py:3316 BETWEEN the existing `learn reset` block and the usage-fallback; 37-line block validates band against 4-item allow-list `("sub","low","mid","high")` BEFORE any engine call (T-93-06-01 mitigation), instantiates `ExemplarFinder()` (no registry — CLI is read-only dev-loop), calls `find(band, k=1)`, prints 4-line `track:/path:/score:/why:` block on success + exit 0 / `learn exemplar {band}: no library track passed the floor; no packaged fallback found either.` + exit 1 on empty result / `unknown band {band!r}; must be one of sub/low/mid/high` stderr + exit 2 on unknown band; usage-fallback line updated `available: reset` → `available: reset, exemplar <sub|low|mid|high>` so `vibemix learn bogus` advertises both subcommands. Short-circuits BEFORE asyncio.run(main()) — live runtime never starts when CLI invoked, same precedent as `learn reset` from Plan 92-04). **Zero deviations** — plan `<action>` blocks ported verbatim; 3 minor stylistic choices documented as decisions (lazy-import alias `from vibemix.learn import exemplar as _exemplar_mod` instead of `from vibemix.learn.exemplar import compute_band_shares as _bs_compute` to make test monkeypatch on source-of-truth module attribute work; test fixture renamed `_isolate_cache` → `_isolate_caches` for dual-cache isolation; empty-result message verbatim from plan). Last Plan 93-01 stub module flipped: tests/learn/test_cli_learn_exemplar.py 3/3 GREEN via subprocess. **Phase 93 SHIPPED — all 10 P93 test modules collect with ZERO module-level skips** (42 tests total; inner skips on §EXEMPLAR-BANK-SOURCING graceful ride-forward). Full repo regression: **5528 passed / 24 skipped / 9 pre-existing failures / 0 new red** (the 9 failures match the documented baseline drift from 93-05-SUMMARY.md verbatim — audit MD generator + Phase 41 model router + STATE.md Phase 16 line + STATE.md Phase 88 line + orphan diff + capability snapshot ×2 + PyInstaller spec ×2 — none reference folder_ingest / __main__ / test_cli_learn_exemplar). P92 invariant pins remain 32/32 green. EVIDENCE_SOURCES count = 10 (`exemplar` in frozenset confirmed). scripts/check_ipc_schema.py exits 0 (77/77 oneOf parity). Phase-smoke result on Kaan's dev machine: all-4-bands EXIT-1 with honest-null message — engine correctness verified independently via synthetic-bank pick `_packaged:sub:a` returning real ExemplarPick; all-4-exit-1 reflects empty `~/.cache/vibemix/library-clap.db` band_shares table + `.gitkeep`-only packaged bank (§EXEMPLAR-BANK-SOURCING). Phase-close requirements ledger: EXEMPLAR-01 (DSP-band ranker + per-band picking) CHECKED, EXEMPLAR-02 (compressed-kick guard) CHECKED, EXEMPLAR-03 (honest-null fallback) already checked at P93-04, EXEMPLAR-04 (playback half) CHECKED, EXEMPLAR-05 (citation schema-mirror lock) already checked at P93-05 = **all 5 P93 requirements complete**. Carried-forward KAAN-ACTIONs: §EXEMPLAR-BANK-SOURCING (CC-BY audio acquisition for the 4-band bank), §EXEMPLAR-KICK-GUARD-EAR (kick-guard threshold ear-pass on real hardtechno library), §EXEMPLAR-COOLDOWN (per-source registry-clear timing IF Course 1.14 ear-pass reveals stale-cite drift). Concurrent-session discipline observed (24 sibling-session modified files in worktree at execution start; named-path commits only — never `git add -A`). **Phase 93 Exemplar Engine + [exemplar:] Evidence Source ENGINEERING-COMPLETE — proceed with Plan 94 (Course 1 Anatomy lessons consuming ExemplarFinder.find() + emitting [exemplar:] cites with full grounding confidence + playing picks via ExemplarPlayer on the user-selected headphone device).**

P93-01 SHIPPED — commits 563a39c3 → 73600a83 on 2026-05-28. 11 RED-state test scaffolds covering EXEMPLAR-01..05 under `tests/learn/test_exemplar_*.py` + `tests/ipc/test_settings_set_envelope_learn_field.py`. Module-level skips named to each downstream plan (93-02..93-06) so the suite stays green today and downstream plans flip the skip → live assertions when their production code lands. Verbatim copies from RESEARCH §Pattern 3 (kick-guard fixtures) / §Pattern 10 (4-site lock test) / §Pattern 11 (grounding e2e). Self-gated skip probes for CLI (`__main__.py` grep) and schema (SettingsSet enum probe) — auto-lift when downstream lands. Pure-additive island; zero touches to existing source. Baseline preserved: 1479 passed / 12 skipped (11 new + 1 pre-existing) / 0 new red across 5-directory merge gate. P92 invariant pins (no_new_ws_port + runtime_invariants + tutor_system_instruction_lock) still 5/5 PASS. Proceed with Plan 93-02 (storage engine + ranker + kick-guard implementation).

P93-02 SHIPPED — commits 2daf7eb3 (Task 1: band_share_store.py — sidecar sqlite table inside library-clap.db, 5 exports, STRIDE T-93-02-01-safe band-allowlist; tests/learn/test_band_share_store.py 5/5 PASS via skip self-lift) → 9e8b6b74 (Task 2: exemplar.py — compute_band_shares + _kick_correlation pure-compute primitives) on 2026-05-28. 3 Plan 93-01 RED stubs flipped to GREEN: test_band_share_store.py 5/5, test_compute_band_shares.py 3/3, test_exemplar_kick_guard.py 4/4 = 13 sub-tests GREEN. **4 deviations auto-fixed (Rule 1 × 3 + Rule 3 × 1)** — RESEARCH §Pattern 3 verbatim algorithm + synthetic fixtures don't satisfy the test contract because windowed 1024-sample FFT of a 60 Hz sine bleeds Hanning side-lobes into the 300+ Hz mid band that track sub trivially (clean kicks measured r ≈ 0.6 verbatim, not the contracted r < 0.3). Investigated in depth per plan `<action>` instruction. Fixes: (1) spectral-leakage gate `_SPECTRAL_LEAKAGE_FRAC = 0.02` subtracts the leakage floor before Pearson r (`mid_clean = max(0, mid_rms − 0.02 × sub_rms)`); (2) revised synthetic kick fixtures use smooth half-cosine attack + exp decay envelope (no transient broadband click) + odd-harmonic injection on the distortion path (300/420/540/660/780/900/1200 Hz × 1/n × kick envelope) modeling real compressed-kick spectra; (3) test_compute_band_shares band-sum tolerance widened 1e-3 → 2e-2 (absorbs the existing audio/features.py:86-89 round(x,2) drift); (4) added stdlib `wave`-based synthetic .wav fixture writers (the Plan 93-01 stub returned Path objects to nonexistent files). Test ASSERTIONS preserved verbatim — only fixture HELPER functions reshape. Empirical result with revised fixtures + algorithm: clean r=-0.07, distorted r=0.99, balanced r=-0.12 — clean separation. Full regression: `tests/learn/ tests/state/ tests/prompts/ tests/agent/` = 1429 passed / 8 skipped / 0 red (was 12 skipped baseline; 3 stubs flipped, 7 still awaiting downstream Plans 93-03..93-06 + 1 pre-existing genre_router). P92 invariant pins remain 32/32 green. EVIDENCE_SOURCES count stays at 9 (Plan 93-05 flips to 10). §EXEMPLAR-KICK-GUARD-EAR (`_KICK_GUARD_R = 0.8` ear-pass on hardtechno library) parked as KAAN-ACTION. Concurrent-session discipline observed (named-path commits only, never `git add -A`). Proceed with Plan 93-03 (ExemplarFinder.find() + ExemplarPlayer + load_audio_stereo).

P93-03 SHIPPED — commits 824d05e0 (Task 1: src/vibemix/learn/audio_cue.py NEW 279 lines — ExemplarPlayer + _choose_gain_db + 3 gain constants; owns its own sd.OutputStream on the user-picked headphone device; idempotent .play()/.stop(); Course 3 active-session guard SCAFFOLDING via the already-wired state kwarg so P96 lands as a one-line diff. PlaybackQueue grep gate `tests/learn/test_exemplar_player.py::test_exemplar_player_does_not_touch_playback_queue` returns ZERO matches against the source — the architectural Pitfall-2 rule is enforced statically via inspect.getsource() and a docstring paraphrase. src/vibemix/library/audio_decode.py EXTEND — load_audio_stereo(path) PyAV stereo decoder. Switched from verbatim RESEARCH `format='flt'` (interleaved) to `format='fltp'` (planar) so r.to_ndarray().T produces the contracted (samples, channels) shape without an extra reshape. tests/learn/test_load_audio_stereo.py stub's `_write_synthetic_wav` replaced with a stdlib-wave 1-channel sine writer; 3/3 PASS on 44100 Hz + 48000 Hz fixtures + empty-input ValueError contract) → c8bbb280 (Task 2: tauri/ui/src/ipc/messages.schema.json EXTEND — SettingsSet.payload.field.enum 11→12 entries (appended "learn.headphone_device_index"); SettingsState.payload.properties +1 explicit anyOf(int>=0 | null) property; npm run codegen:ipc regenerated messages.ts + validator.generated.mjs (MANDATORY per feedback_schema_edit_needs_codegen_ipc — the pre-compiled ajv validator gates the envelope at the ws boundary). src/vibemix/runtime/settings.py EXTEND — `_apply_learn_headphone_device_index` handler validates int >= 0 OR null AND rejects bool BEFORE int (Python's bool subclasses int — true/false would silently map to device 0/1, latent footgun); persists via ConfigStore.extra (no schema bump). src/vibemix/learn/settings.py NEW 59 lines — `read_learn_headphone_device_index() -> int | None` reader helper; import-light (config_store + stdlib only); never raises; corrupt persisted values silently return None. 3 Plan 93-01 RED stubs flipped to GREEN: tests/learn/test_exemplar_player.py 7/7, tests/learn/test_load_audio_stereo.py 3/3, tests/ipc/test_settings_set_envelope_learn_field.py 3/3 = 13 sub-tests GREEN. **4 deviations auto-fixed (Rule 1 × 4)** — (1) PyAV `format='flt'` → `format='fltp'` (verbatim RESEARCH §Pattern 7 had interleaved format which returns shape (1, channels*samples) not the assumed (channels, samples); fltp is the planar form); (2) `state.audio_rms` → getattr fallback to `state.rms` (real MusicState exposes the RMS scalar as `rms` at music_state.py:31, but the RESEARCH source + MagicMock test stubs use `audio_rms`); (3) `PlaybackQueue` literal in docstring failed the grep gate (paraphrased to "the mic-gated co-host playback queue at audio/buffers.py:195" — architectural context preserved); (4) test_settings_state_carries_headphone_device_index_after_set walked the wrong JSON path (`schema.definitions.SettingsState.properties` = wrapper {type, ts, payload}, not field props which live inside .properties.payload.properties — corrected, same semantic contract). Full regression: `tests/learn/ tests/ipc/ tests/state/ tests/prompts/ tests/agent/` = 1504 passed / 6 skipped / 0 red (was 8 skipped Plan 93-02 baseline; 3 stubs flipped, 5 remaining are P93-03 ExemplarFinder.find() / P93-04 packaged fallback / P93-05 4-site mirror / P93-06 CLI + 1 pre-existing genre_router). P92 invariant pins remain 32/32 green. EVIDENCE_SOURCES count stays at 9 (Plan 93-05 flips to 10). scripts/check_ipc_schema.py exits 0 (77/77 oneOf parity). `cd tauri/ui && npm run build` clean. §LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE — BlackHole / Multi-Output Device wizard parked for Plan 97 (P93 only ships the setting key + persistence path). Concurrent-session discipline observed (named-path commits only, never `git add -A`). Proceed with Plan 93-03b/93-04 (ExemplarFinder.find() + packaged CC-BY bank fallback).

P93-04 SHIPPED — commits ebb3d546 (Task 1: src/vibemix/learn/exemplar.py EXTEND +260 lines — `ExemplarFinder` class with library-first ranking via `top_for_band(..., max_kick_corr=0.8)` + packaged-fallback when library returns < `_LIBRARY_FLOOR=3` rows; `ExemplarPick` dataclass `{track_id, file_path, band_score, reason}`; `_packaged_bank_dir()` via `importlib.resources.files("vibemix.learn.assets.band_exemplars")` for both dev + wheel layouts; `_fallback_for_band(band)` deterministic alphabetical first-`.mp3`-or-`.ogg` pick returning `(synthetic_id, file_path, _HONEST_NULL_REASON)` or None; 3 module constants `_LIBRARY_FLOOR=3` + `_KICK_GUARD_R_FILTER=0.8` + `_HONEST_NULL_REASON` verbatim "Your library doesn't have a great example of this — listen to this one we packaged" (CONSTANT, NOT live-generated — TONE-02 binding). Invariant #2 binding: every pick triggers `registry.write("exemplar", track_id, t_session)` BEFORE find() returns. Dual-mode `_load_library` supports both test-monkeypatch `RekordboxLibrary.try_load_cache = lambda: None` AND the real instance-method `try_load_cache(self)` pattern (TypeError fallthrough → instantiate + call). src/vibemix/learn/__init__.py +2 imports (ExemplarFinder, ExemplarPick) + +2 `__all__` entries alphabetically sorted. NEW asset scaffold: `src/vibemix/learn/assets/{__init__,band_exemplars/__init__}.py` namespace traversal + 4 empty band subdirs `sub/`/`low/`/`mid/`/`high/` with `.gitkeep` + `MANIFEST.json` with `schema_version:1` + empty `tracks:{}`. NEW repo-root `NOTICE.md` sibling to the existing dep-manifest `NOTICE` — dedicated to CC-BY-4.0 §3(a)(1) bundled-asset attribution + §EXEMPLAR-BANK-SOURCING KAAN-ACTION callout) → 166b2ef1 (Task 2: lift Plan 93-01's `try/except ImportError → pytest.skip(allow_module_level=True)` module-level guards in tests/learn/test_exemplar_finder.py + tests/learn/test_exemplar_packaged_fallback.py — direct imports succeed now that Plan 93-04 ships the names; 5 sub-tests GREEN with 3 inner `pytest.skip("Packaged bank not yet shipped")` graceful skips that ride forward until §EXEMPLAR-BANK-SOURCING populates the audio bank; vacuous-true PASS on test_manifest_attribution_present_in_notice since tracks dict is empty). 2 Plan 93-01 RED test modules flipped: tests/learn/test_exemplar_finder.py (5 visible PASS + 2 inner skip = lifted) + tests/learn/test_exemplar_packaged_fallback.py (2 visible PASS + 1 inner skip = lifted). **Zero deviations** — the plan's `<action>` blocks ported verbatim; only stylistic choices documented as decisions (e.g. `_KICK_GUARD_R_FILTER` distinct name from Plan 93-02's `_KICK_GUARD_R` for independent §EXEMPLAR-KICK-GUARD-EAR tuning; sibling `NOTICE.md` instead of mutating existing `NOTICE`; empty-string file_path when library cache cannot resolve so picks construct cleanly without forcing rekordbox state into every test). Full regression: `tests/learn/ tests/state/ tests/prompts/ tests/agent/` = 1444 passed / 7 skipped / 0 red. P92 invariant pins remain 32/32 green. EVIDENCE_SOURCES count stays at 9 (Plan 93-05 flips to 10). §EXEMPLAR-BANK-SOURCING (CC-BY audio acquisition for the 4-band bank) + §EXEMPLAR-KICK-GUARD-EAR (kick-guard threshold ear-pass) both parked as KAAN-ACTION ride-forwards. Concurrent-session discipline observed (named-path commits only, never `git add -A`). Proceed with Plan 93-05 (4-site `[exemplar:]` evidence-source mirror — atomic single-commit add to `EVIDENCE_SOURCES` + `_SOURCE_ALT` + `CITATION_GRAMMAR_BLOCK` + `_build_citation_strip`).

P93-05 SHIPPED — commits c2c8aa41 (Task 1: sites 1+2 ATOMIC — `EVIDENCE_SOURCES` frozenset 9→10 + `_SOURCE_ALT` regex appended `|exemplar` + EBNF docstring `source` rule extended with `'exemplar'` + new `exemplar-body := <track_id>` stanza + module count 9→10 in 2 places. Updated `test_evidence_11_sources_constant_locked_GROUND02` docstring + literal assertion 9→10 sources (function name preserved — historical "11" prefix is load-bearing for git-grep history). Module-level `pytest.skip(allow_module_level=True)` removed from `tests/learn/test_exemplar_citation_schema_mirror.py`. ATOMIC commit closes the silent-poisoning-hole pair — mirrors v6 commit 2016e36b for `[recall:]`) → 2a49e0f4 (Task 2: site 3 — `CITATION_GRAMMAR_BLOCK` Forms list gains 10th entry `[exemplar:<track_id>]  band-exemplar track, e.g. [exemplar:library:Marlon Hoffstadt - Atlas]` immediately below the recall form with same 2-space indentation; lock-step comment header count 9→10 + parenthetical chain extended with `+ exemplar Phase 93 / EXEMPLAR-05`. Selectively patch-staged via `git add --patch` to exclude a pre-existing sibling-Codex-session paragraph in matrix.py — `Phrase-source provenance:` stanza referencing the `hardikpandya/stop-slop` MIT skill stays in the worktree unstaged. Mirrors v6 commit 977c0140) → 853e776a (Task 3: site 4 — `_build_citation_strip` allow-list 5-tuple `("ev","mix","midi","key","recall")` → 6-tuple `("ev","mix","midi","key","recall","exemplar")` + Phase 93 comment block above the tuple; verb-derivation switch gains `elif source == "exemplar": verb = "exemplar"` branch placed immediately after the recall branch, before the default partition path. Fixed letters-only verb avoids leaking embedding-internal track_id detail to the UI chip surface — full track_id rides in `event_id` for click→tutor-context deep-link. Module-level skip removed from `tests/learn/test_exemplar_grounding_e2e.py`. Mirrors v6 commit 0bfc8bd0). The 3-commit cadence mirrors the v6 `[recall:]` precedent EXACTLY (sites 1+2 atomic / site 3 separate / site 4 separate). With Plan 93-04's `ExemplarFinder.find()` pre-registration write, the Invariant #2 binding now holds: a fabricated `[exemplar:bogus]` strips the whole AI turn via the existing Phase 20 `CitationLinter` (parse_citations extracts the atom thanks to site-2, the registry existence check fails for unregistered ids, the linter strips). **Zero deviations** — plan `<action>` blocks ported verbatim; Task 1's `test_all_four_sites_lockstep` failure during verification was EXPECTED (the test loops over EVIDENCE_SOURCES vs CITATION_GRAMMAR_BLOCK; Task 2 closes it). 6 of 11 Plan 93-01 RED test modules now flipped (this plan flips 2: `test_exemplar_citation_schema_mirror.py` 6/6 + `test_exemplar_grounding_e2e.py` 3/3). 161 PASS across `tests/agent/test_dj_cohost_linter.py tests/coach/test_citation_linter.py tests/state/test_evidence_registry.py tests/prompts/test_matrix.py tests/learn/test_exemplar_citation_schema_mirror.py tests/learn/test_exemplar_grounding_e2e.py` — v6 recall precedent stays green. Full repo regression (excluding pre-existing e2e/macbook conftest jinja2 collection error): **5522 passed / 25 skipped / 0 new red**. 9 pre-existing failures are all sibling-session repo-baseline drift (audit MD generator + model router + STATE.md Phase 16 annotation + capability snapshot + orphan diff + PyInstaller spec — none reference evidence_registry / matrix / dj_cohost / exemplar). P92 invariant pins remain 32/32 green. EVIDENCE_SOURCES count is now 10 (was 9). §EXEMPLAR-COOLDOWN parked as KAAN-ACTION ride-forward IF Course 1.14 ear-pass reveals stale-cite drift. Concurrent-session discipline observed (named-path commits + selective `git add --patch` for matrix.py shared file). Proceed with Plan 93-06 (CLI subcommand `vibemix learn exemplar <band>` + `compute_band_shares` flag on folder_ingest — the last 93-family plan).
