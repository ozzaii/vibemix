---
phase: 99-harden-retry
plan: 08
type: execute
wave: 6
depends_on:
  - 99-05
  - 99-06
  - 99-07
files_modified:
  - tests/library/test_codex_curate_stop_reason.py
autonomous: false
requirements:
  - HARDEN-RETRY-02
  - HARDEN-RETRY-03
  - HARDEN-RETRY-07

must_haves:
  truths:
    - "`CodexCurateResult.to_dict()` on a `tool_starvation` result contains `stop_reason == \"tool_starvation\"` AND `error == <hint>` AND empty `track_ids` / `playlist_name=None` / `m3u_path=None`."
    - "`CodexBuildSetResult.to_dict()` on a `tool_starvation` result has the same shape (uniform across set-prep + plain curation)."
    - "The same `tool_starvation` source-of-truth payload (side-channel file written by the toolset) propagates to ALL THREE consumer surfaces — CLI exit-code dispatch, Telegram `format_reply`, and the wrapper dataclass — with byte-equivalent hint string content."
    - "The HARDEN-PHASE-A-EAR-PASS KAAN-ACTION item is captured at milestone close: a checkpoint here surfaces the seeded hint copy for Kaan's ear-pass verdict (anti-slop release gate)."
    - "The HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY KAAN-ACTION item is captured at phase-close: a checkpoint here surfaces the `[viber-mcp] VIBEMIX_STOP_REASON_FILE=...` log line from Kaan's FIRST real Codex run, verifying Channel A's second-boundary passthrough (B1 Option A, per plan-checker)."
  artifacts:
    - path: "tests/library/test_codex_curate_stop_reason.py"
      provides: "Integration-seal tests covering the full propagation chain + the dataclass shape contract"
      exports:
        - "test_uniform_propagation_curate_path"
        - "test_uniform_propagation_build_set_path"
        - "test_to_dict_serializes_starvation_shape"
  key_links:
    - from: "tests/library/test_codex_curate_stop_reason.py::test_uniform_propagation_curate_path"
      to: "Toolset payload → side-channel file → wrapper dataclass → result.to_dict() consumed by callers"
      via: "end-to-end fake-runner harness that writes a real stop_reason.json and reads the wrapper output"
      pattern: "to_dict.*stop_reason.*tool_starvation"
---

<objective>
Land the integration-seal tests that prove uniform propagation across all three consumer surfaces (CLI / Telegram / wrapper dataclass). Plans 99-04 / 99-06 / 99-07 each wired their leg; this plan tests the WHOLE chain in one file so a future PR that breaks any leg fails this seal test loudly.

Add TWO MANDATORY checkpoints:
1. §HARDEN-PHASE-A-EAR-PASS — surfaces the seeded hint copy for Kaan's verdict (the anti-slop release gate parked at milestone close per `PROJECT.md` § Current Milestone).
2. §HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY (B1 Option A, plan-checker) — surfaces the MCP server's startup log line from Kaan's FIRST real Codex run to verify the env-var passthrough actually crosses the wrapper → Codex CLI → MCP server-child boundary. If the log says `<absent>`, the side-channel write silently no-ops in production and starvation never propagates — fix lands as HARDEN-FUTURE.

The checkpoints do NOT block plan completion — they capture the ear-pass + propagation-verify surfaces for the milestone-close / phase-close discharge.

Purpose: The integration seal closes HARDEN-RETRY-07 ("uniform propagation regardless of starvation origin") with a single, named, executable contract test. The ear-pass checkpoint surfaces the copy seam so it's not lost between phase-close and milestone-close. The env-propagation-verify checkpoint closes the B1 BLOCKER that unit tests CANNOT verify — only a live Codex run reveals whether the env var arrived.

Output: Three integration-seal tests appended to `tests/library/test_codex_curate_stop_reason.py`; two `checkpoint:human-verify` tasks surfacing the seeded hint copy + the env-propagation log line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-harden-retry/99-CONTEXT.md
@.planning/phases/99-harden-retry/99-RESEARCH.md
@.planning/phases/99-harden-retry/99-04-SUMMARY.md
@.planning/phases/99-harden-retry/99-06-SUMMARY.md
@.planning/phases/99-harden-retry/99-07-SUMMARY.md
@CLAUDE.md

# Source files (READ ONLY — pin contracts already landed)
@src/vibemix/library/codex_curate.py
@src/vibemix/library/telegram_bridge.py
@src/vibemix/library/mcp_server.py
@src/vibemix/__main__.py

<interfaces>
<!-- The full propagation chain that this plan seals. -->

```
toolset.dispatch() trips threshold
  ↓ writes self.stop_reason (Plan 99-03)
  ↓ writes stop_reason.json via env var (Plan 99-04)
─── subprocess boundary 1: MCP server child ← Codex CLI subprocess ───
                          (B1 risk: Codex CLI may strip env var here)
                          (Plan 99-04's mcp_server.build_toolset
                           startup log probe surfaces the verdict)
─── subprocess boundary 2: Codex CLI subprocess ← wrapper parent ───
wrapper reads stop_reason.json (Plan 99-04)
  ↓ returns CodexCurateResult(stop_reason="tool_starvation", error=<hint>)
  ↓ returns CodexBuildSetResult(stop_reason="tool_starvation", error=<hint>)
─── caller consumes result ───
  ├─ CLI: exit code 10 + bracket-tagged stderr (Plan 99-06)
  ├─ Telegram: curate_fn normalizes → format_reply renders hint (Plans 99-06 + 99-07)
  └─ GUI: result.to_dict() surfaces stop_reason + error to JSON consumer
```

`CodexCurateResult.to_dict()` (existing at `codex_curate.py:217-218`):
```python
def to_dict(self) -> dict[str, Any]:
    return asdict(self)
# → emits all dataclass fields including stop_reason="tool_starvation", error=<hint>,
#   theme=<theme>, playlist_name=None, track_ids=[], m3u_path=None, json_path=None,
#   rationale="", export_path=None
```
</interfaces>

<decisions_locked>
- HARDEN-RETRY-07: "uniform propagation regardless of whether starvation originated INSIDE the toolset dispatch or AFTER the Codex MCP harness exited." ← THIS PLAN seals.
- HARDEN-RETRY-02 + HARDEN-RETRY-03: seal tests exercise the terminal stop_reason payload + hint string carried through to ALL three consumer surfaces. ← THIS PLAN's frontmatter claims them per plan-checker WARNING W4.
- KAAN-ACTION §HARDEN-PHASE-A-EAR-PASS (PROJECT.md § Current Milestone): ear-pass on the `tool_starvation` user-facing message. Parked at milestone close. ← THIS PLAN surfaces.
- KAAN-ACTION §HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY (B1 Option A, plan-checker — this revision): verify the MCP server startup log line on Kaan's first real Codex run. ← THIS PLAN surfaces.
- All four Cardinal Invariants hold additively (verified by Plan 99-05's gate). ← THIS PLAN re-runs the gate as part of the seal.
</decisions_locked>

<antipatterns_to_avoid>
- Treating the ear-pass checkpoint as blocking — the seeded copy is fine for ship; ear-pass is at milestone close, not phase close. The checkpoint surfaces the surface but does NOT block plan-complete.
- Treating the env-propagation-verify checkpoint as blocking — the FIRST real Codex run is when the log line surfaces; the checkpoint records Kaan's read of that log + the verdict. If the verdict is `<absent>`, plan-close still proceeds; the HARDEN-FUTURE item rides forward to the next iteration.
- Adding a new seal test that duplicates Plan 99-04's tests verbatim. The seal tests are HIGHER-LEVEL: they exercise the WHOLE chain end-to-end (toolset payload generation → side-channel file → wrapper → consumer), not individual legs.
- Forgetting the `to_dict()` test — GUI consumers (future Tauri bridge) consume `result.to_dict()`. The dict shape is the API surface. Pin it.
- Asserting on EXACT hint copy in the seal tests — substring assertion (`"library has 0 tracks" in error`) per D-05 ear-pass tolerance. Plan 99-03 used the same posture; mirror it.
- Trying to test the LiveKit path. Disjointness contract: this plan touches ONLY tests under `tests/library/`. No `src/vibemix/agent/` code is exercised.
- Trying to AUTOMATE the env-propagation-verify check via a unit test — by definition this verification requires a real Codex subprocess run (which is the boundary we cannot mock). The checkpoint surfaces Kaan's read of the live log; tests CANNOT substitute.
</antipatterns_to_avoid>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration-seal tests for uniform propagation</name>
  <files>tests/library/test_codex_curate_stop_reason.py</files>
  <behavior>
    Append three integration-seal tests to `tests/library/test_codex_curate_stop_reason.py`:

    - `test_uniform_propagation_curate_path`:
      End-to-end: build a fake `_runner` that simulates the toolset's full payload pipeline — when called, it (1) writes a real `stop_reason.json` matching the exact shape `LibraryToolset._build_starvation_payload` would emit (using the actual `_build_starvation_payload` method called on a fake toolset to GENERATE the payload, NOT a hardcoded dict — this proves the payload-generation surface is the same one consumed downstream), (2) returns a `subprocess.CompletedProcess(returncode=0, ...)`. Call `curate_with_codex(theme="empty library test", library=zero_track_lib, _runner=fake_runner)`. Assert:
      - `result.stop_reason == "tool_starvation"`.
      - `result.error` contains the substring `"library has 0 tracks"` (Case A hint).
      - `result.playlist_name is None`, `result.track_ids == []`, `result.m3u_path is None`.
      - `result.to_dict()["stop_reason"] == "tool_starvation"` (GUI/JSON surface).
      - `result.to_dict()["error"]` contains `"library has 0 tracks"`.
      - This test exercises REQ-02 (terminal stop_reason discriminator surfaces through to dataclass), REQ-03 (hint carried through to `error` field), AND REQ-07 (uniform propagation seam from toolset → wrapper).

    - `test_uniform_propagation_build_set_path`:
      Same as above but invokes `build_set_with_codex`. Asserts on `CodexBuildSetResult.to_dict()`. Uses the same generated payload — proves the SAME source-of-truth payload reaches the build-set leg. This test exercises REQ-02 + REQ-03 + REQ-07 on the build-set leg.

    - `test_to_dict_serializes_starvation_shape`:
      Direct shape pin: construct `CodexCurateResult(theme="x", stop_reason="tool_starvation", error="test hint")` directly. Call `.to_dict()`. Assert all required keys are present (`theme`, `stop_reason`, `playlist_name`, `track_ids`, `m3u_path`, `json_path`, `rationale`, `error`, `export_path`); `stop_reason == "tool_starvation"`; `error == "test hint"`. Same for `CodexBuildSetResult`. This pins the dataclass shape so any future field addition / rename gets flagged. This test exercises REQ-02 (stop_reason discriminator is a structural dataclass field, not a stringly-typed leak) + REQ-03 (hint round-trips through `error`).

    Use the toolset's REAL `_build_starvation_payload` (via instantiating `LibraryToolset` on a fake zero-track library) to generate the payload — the seal test proves the payload that COMES OUT of the toolset is what GOES IN to the wrapper. Do NOT hardcode the payload dict.
  </behavior>
  <action>
    Append the three tests to `tests/library/test_codex_curate_stop_reason.py`. The trickiest piece is generating the payload via the actual `_build_starvation_payload` method — construct a fake `LibraryToolset` with a zero-track library, call `toolset._build_starvation_payload(last_tool="search_vibe", args={"query": "test"})` directly, and write the returned dict to the side-channel file. This proves the payload-shape contract: same generator, same consumer. After the tests pass, the seal is closed.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_codex_curate_stop_reason.py tests/library/test_toolset_starvation.py tests/library/test_toolset_starvation_concurrency.py tests/library/test_cli_exit_codes.py tests/library/test_telegram_bridge.py tests/repo/test_no_seen_relaxation.py 2>&1 | tail -25</automated>
  </verify>
  <done>
    Three seal tests present; all three pass on post-99-04/99-05/99-06/99-07 source; the full Phase 99 test surface stays green (toolset_starvation + concurrency + codex_curate_stop_reason + cli_exit_codes + telegram_bridge + no_seen_relaxation); grep gate `grep -c "stop_reason" tests/library/test_codex_curate_stop_reason.py` returns at least 10 (multiple tests reference it).
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression check</name>
  <files>(no file edits — pure regression check)</files>
  <action>
    Run the full Python test suite to confirm no Phase-99 plan introduced a regression in any other test file. Capture pass/fail counts in the SUMMARY.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    Full suite passes (or, if there are flaky markers, only marker-excluded tests fail — which is expected per `pyproject.toml [tool.pytest.ini_options]`). No NEW failures attributable to Phase 99. Pass count documented in SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Surface §HARDEN-PHASE-A-EAR-PASS seeded hint copy for Kaan's verdict</name>
  <files>(no file edits — KAAN-ACTION surface only)</files>
  <action>
    Present the seeded hint copy from Plan 99-03's `_build_starvation_payload` for Kaan's verdict per the §HARDEN-PHASE-A-EAR-PASS KAAN-ACTION item parked at milestone close (PROJECT.md § Current Milestone KAAN-ACTION queue).

    **What was built across Plans 99-01 → 99-07:**
    - Toolset trips on N=3 consecutive empty/error tool calls.
    - Side-channel file `stop_reason.json` propagates payload to the wrapper.
    - Wrapper returns `CodexCurateResult` / `CodexBuildSetResult` with `stop_reason="tool_starvation"`.
    - CLI exits 10 with `[viber/codex] tool_starvation: <hint>` on stderr.
    - Telegram `format_reply` renders the hint as a warning-glyph chat message.

    **The seeded hint copy in place per D-05 (`99-CONTEXT.md`):**
    - **Case A (zero-track library):** `library has 0 tracks — run \`library ingest\` first`
    - **Case B (no theme match):** `no tracks matched '<theme>' — try a broader theme or different BPM range`
    - **Case C (tool error):** `tool '<name>' kept failing — try again or check codex installation`

    **How Kaan verifies (this checkpoint, phase-close — NOT funded-key live ear-pass):**
    Live ear-pass with a funded Codex login is parked at MILESTONE CLOSE per PROJECT.md. For THIS phase-close checkpoint, Kaan reviews the SEEDED copy on the page:

    1. Read the three hint case strings above out loud.
    2. Ask the same question PROJECT.md asks: "Does it sound like a real friend telling you the library is empty, or like generic error text?"
    3. Verdict options:
       - **"approved"** → seeded copy ships in v10.0 release branch; KAAN-ACTION discharged. Phase 99 closes clean.
       - **"defer to milestone close"** → seeded copy ships INTO `live-tuning-or-brain`; ear-pass with real Codex output at milestone close before public release; KAAN-ACTION ITEM stays open in the queue. (Most likely choice per `gsd-autonomous fully` mode — phase ships, ear-pass at the right time.)
       - **"tweak now"** → name specific case + new wording; planner spins a tiny edit to `library/toolset.py::_build_starvation_payload` + updates the substring assertions in `test_toolset_starvation.py` to match new key phrases.

    NOTE: tests pin SUBSTRINGS (`"library has 0 tracks"`, `"no tracks matched"`, `"kept failing"`), not full strings — ear-pass tweaks that change wording AROUND those substrings won't break tests.

    This checkpoint is **non-blocking** per `gate="non-blocking"`: the executor records the verdict in the SUMMARY and proceeds to phase-close regardless. If the verdict is "defer to milestone close" (default per `gsd-autonomous fully`), the KAAN-ACTION item rides forward.
  </action>
  <verify>
    <automated>echo "KAAN-ACTION verdict required — checkpoint is human-gated, no automated check applicable. Executor records the verdict string in 99-08-SUMMARY.md."</automated>
    <human-check>Kaan types one of: "approved" / "defer to milestone close" / a wording tweak per case.</human-check>
  </verify>
  <done>
    Verdict string captured in `99-08-SUMMARY.md`. If verdict was "approved": KAAN-ACTION marked DISCHARGED. If "defer": item RIDES FORWARD. If "tweak now": planner emits a small follow-up edit (separate commit). Phase 99 close proceeds regardless.
  </done>
  <resume-signal>Type "approved" / "defer to milestone close" / describe a wording tweak per case.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 4: Surface §HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY MCP startup log for Kaan's first real Codex run (B1 Option A)</name>
  <files>(no file edits — KAAN-ACTION surface only)</files>
  <action>
    Per plan-checker BLOCKER B1 Option A: the side-channel propagation mechanism crosses TWO process boundaries:
    1. Wrapper Python process → Codex CLI subprocess (set via `subprocess.run(..., env=env)` in `codex_curate.py`). VERIFIED in Plan 99-04's unit tests via the `_runner` injection seam.
    2. Codex CLI subprocess → MCP server child (`python -m vibemix.library.mcp_server`). UNVERIFIED in unit tests because passthrough is owned by Codex CLI subprocess-spawning behavior, NOT vibemix code. If Codex strips env vars (sandbox / hygiene), the toolset's `_write_side_channel` finds the env var ABSENT, silently no-ops, wrapper reads no file, falls through — starvation NEVER propagates.

    Plan 99-04 Task 5 added a startup log line in `mcp_server.build_toolset()` recording the env var's presence:
    ```
    [viber-mcp] VIBEMIX_STOP_REASON_FILE=/var/folders/.../T/viber-codex-XXXXX/stop_reason.json
    ```
    or:
    ```
    [viber-mcp] VIBEMIX_STOP_REASON_FILE=<absent>
    ```

    **How Kaan verifies (this checkpoint, phase-close):**

    1. Make sure Codex is installed + logged in:
       ```
       codex --version
       codex login    # should report "Logged in as ..."
       ```

    2. Run a real curate against the empty library (the seam is the same whether the library is empty or full — empty just triggers starvation reliably so we exercise BOTH directions of the seam):
       ```
       export VIBEMIX_CODEX_ALLOW_SHELL=1
       uv run python -m vibemix library curate "any theme" 2>&1 | head -40
       ```

    3. Look for the FIRST stderr line emitted from inside the MCP child:
       ```
       [viber-mcp] VIBEMIX_STOP_REASON_FILE=...
       ```

    4. Read the value:
       - **A file path like `/var/folders/.../T/viber-codex-XXXXX/stop_reason.json`** → Codex CLI passes env vars through to MCP children verbatim. Channel A's second boundary is healthy. The side-channel seam works end-to-end on real Codex. **Verdict: "passthrough verified — env propagation works".** KAAN-ACTION discharged.
       - **`<absent>`** → Codex CLI strips env vars at the MCP child spawn boundary. The side-channel write inside the MCP subprocess no-ops; starvation does NOT propagate. **Verdict: "passthrough broken — defer fix to HARDEN-FUTURE".** Investigation paths (in priority order):
         1. Check Codex CLI docs / changelog for a `passthrough_env` config knob in `mcp_servers.<name>.*` overrides (the `-c` injection seam in `codex_curate.build_argv` at line ~342). If supported by current Codex version → ship Option B as HARDEN-FUTURE.
         2. Bump Codex CLI to latest (recent versions may have added env-passthrough).
         3. As fallback: spawn MCP via a wrapper shell script that re-exports `VIBEMIX_STOP_REASON_FILE` from a temp file the parent writes (more boundaries to cross, more failure modes — last resort).
       - **No `[viber-mcp]` line appears at all** → MCP boot failed (unrelated to this probe; existing failure mode). Look at preceding stderr for the actual cause and treat as a separate bug.

    5. **Verdict options:**
       - **"passthrough verified"** → env propagation works on real Codex. B1 risk closed. KAAN-ACTION discharged in SUMMARY.
       - **"passthrough broken — defer HARDEN-FUTURE"** → log shows `<absent>`. KAAN-ACTION item rides forward into HARDEN-FUTURE queue with the investigation paths above. Phase 99 closes regardless (other surfaces still ship; the seam fix is a separate phase).
       - **"MCP failed to boot"** → unrelated bug; log the actual stderr cause in SUMMARY and treat as a separate KAAN-ACTION item.
       - **"skip — Codex not installed locally"** → checkpoint deferred to milestone close (alongside §HARDEN-PHASE-A-EAR-PASS); KAAN-ACTION item stays open.

    This checkpoint is **non-blocking** per `gate="non-blocking"`: the executor records the verdict in the SUMMARY and proceeds to phase-close regardless. If the verdict is anything other than "passthrough verified", the HARDEN-FUTURE item rides forward.
  </action>
  <verify>
    <automated>echo "KAAN-ACTION verdict required — checkpoint requires a live Codex run, no automated check possible. Executor records Kaan's read of the [viber-mcp] log line + verdict in 99-08-SUMMARY.md."</automated>
    <human-check>Kaan runs `uv run python -m vibemix library curate "any theme"`, reads the `[viber-mcp] VIBEMIX_STOP_REASON_FILE=...` stderr line, types one of: "passthrough verified" / "passthrough broken — defer HARDEN-FUTURE" / "MCP failed to boot: <cause>" / "skip — Codex not installed locally".</human-check>
  </verify>
  <done>
    Verdict string captured in `99-08-SUMMARY.md` alongside the actual log line value Kaan observed. If verdict was "passthrough verified": B1 risk closed; KAAN-ACTION discharged. If "passthrough broken": HARDEN-FUTURE item filed with the investigation paths. If "MCP failed to boot": separate bug filed. If "skip": item deferred to milestone close. Phase 99 close proceeds regardless.
  </done>
  <resume-signal>Type "passthrough verified" / "passthrough broken — defer HARDEN-FUTURE" / "MCP failed to boot: <cause>" / "skip — Codex not installed locally".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Toolset payload generation ↔ Wrapper-side consumption | Seal test proves the SAME payload generator feeds the consumer (no shape drift) |
| Wrapper result.to_dict() ↔ GUI / JSON consumers | Pinned dataclass shape; future field additions surface here |
| Wrapper Python process → Codex CLI subprocess → MCP server child | Env-var passthrough verified by §HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY checkpoint on first real Codex run (B1 Option A) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-INT | Tampering | Inter-plan contract drift | mitigate | The seal tests use the real `_build_starvation_payload` generator + the real wrapper code path. Any future change to either side that drifts the contract fails the seal test loudly. |
| T-99-SHAPE | Tampering | Dataclass field rename / drop / reorder | mitigate | `test_to_dict_serializes_starvation_shape` pins all field keys. A rename or drop fails the test with a precise field-name diff. |
| T-99-EAR | Information disclosure | Anti-slop hint copy ships unreviewed | mitigate | KAAN-ACTION checkpoint (Task 3) surfaces the copy; default-action is "defer to milestone close" (per `gsd-autonomous fully` mode — phase ships, ear-pass at the right time). The KAAN-ACTION queue in PROJECT.md tracks the item. |
| T-99-PROP | Tampering | Env-var passthrough silently fails | mitigate (observe-then-defer) | KAAN-ACTION checkpoint (Task 4, B1 Option A) surfaces the `[viber-mcp]` startup log line from Kaan's first real Codex run. `<absent>` verdict files HARDEN-FUTURE item with investigation paths. Plan ships regardless — the propagation legs (Plans 99-04 toolset side + 99-04 wrapper side) are unit-tested against the `_runner` seam; only the second-boundary passthrough is observe-only. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. |
</threat_model>

<verification>
- `pytest -q tests/library/ tests/repo/test_no_seen_relaxation.py` — full Phase-99 surface green.
- `pytest -q` — full suite green (or only marker-excluded skips).
- Phase-99 gate count: `grep -rn "tool_starvation" src/vibemix/ tests/library/ tests/repo/` returns hits in exactly the expected files (toolset.py · codex_curate.py · mcp_server.py · __main__.py · telegram_bridge.py · the four test files + the gate file).
- All four Cardinal Invariants pinned by Plan 99-05's gate stay green.
- §HARDEN-PHASE-A-EAR-PASS KAAN-ACTION checkpoint surface documented in SUMMARY (Task 3).
- §HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY KAAN-ACTION checkpoint surface documented in SUMMARY (Task 4) with the actual log line value Kaan observed.
- Frontmatter claims HARDEN-RETRY-02 + HARDEN-RETRY-03 + HARDEN-RETRY-07 (the three REQs the seal tests exercise) per plan-checker WARNING W4.
</verification>

<success_criteria>
- All Phase 99 plans are integration-sealed: the toolset → side-channel → wrapper → CLI / Telegram / dataclass propagation chain is end-to-end tested.
- `CodexCurateResult.to_dict()` and `CodexBuildSetResult.to_dict()` shape is pinned (any future field drift surfaces here).
- The HARDEN-PHASE-A-EAR-PASS KAAN-ACTION item is presented to Kaan with three explicit verdict options.
- The HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY KAAN-ACTION item is presented to Kaan with four explicit verdict options (B1 Option A closure).
- Full suite green; no Phase-99 plan introduced a regression in unrelated tests.
- Phase 100 forward-compat: the seal-test posture (real generator + real wrapper) can be cloned for `clarification_needed` without refactor.
- Frontmatter claims REQ-02 + REQ-03 + REQ-07 (W4 fix).
</success_criteria>

<output>
Create `.planning/phases/99-harden-retry/99-08-SUMMARY.md` when done.

SUMMARY must record:
- The three seal-test pass counts.
- The full-suite pass / fail / skip counts (Task 2).
- The §HARDEN-PHASE-A-EAR-PASS verdict from Task 3 (one of: approved / deferred / tweaked).
  - If "approved" — note that the KAAN-ACTION item can be marked DISCHARGED in PROJECT.md.
  - If "deferred" — note that the item RIDES FORWARD to milestone close.
  - If "tweaked" — name the case(s) tweaked + the new wording + confirmation that tests still pass against the new substrings.
- The §HARDEN-PHASE-A-ENV-PROPAGATION-VERIFY verdict from Task 4 (one of: passthrough verified / passthrough broken — defer HARDEN-FUTURE / MCP failed to boot / skip):
  - Record the LITERAL `[viber-mcp] VIBEMIX_STOP_REASON_FILE=...` line Kaan observed (or "checkpoint skipped — Codex not installed locally" if skipped).
  - If "passthrough verified" — note B1 risk CLOSED; KAAN-ACTION DISCHARGED.
  - If "passthrough broken" — file HARDEN-FUTURE item with the three investigation paths from Task 4's action block.
  - If "MCP failed to boot" — file a separate bug item with the actual stderr cause.
  - If "skip" — item rides forward to milestone close alongside §HARDEN-PHASE-A-EAR-PASS.
- Phase-99 close summary: "All 7 HARDEN-RETRY requirement IDs implemented + tested + integration-sealed. B1 second-boundary passthrough observed (or deferred). Ready for `/gsd:verify-work 99`."
</output>
</content>
