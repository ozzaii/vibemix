---
phase: 99-harden-retry
plan: 06
type: execute
wave: 5
depends_on:
  - 99-04
files_modified:
  - src/vibemix/__main__.py
  - tests/library/test_cli_exit_codes.py
autonomous: true
requirements:
  - HARDEN-RETRY-04

must_haves:
  truths:
    - "`library curate <theme>` on a `CodexCurateResult` with `stop_reason == \"tool_starvation\"` exits with code 10."
    - "`library build-set <brief>` on a `CodexBuildSetResult` with `stop_reason == \"tool_starvation\"` exits with code 10."
    - "Both commands exit with code 1 on any OTHER non-success stop_reason (`timeout` / `error` / `codex_not_installed` / `codex_auth_required` / `empty_output` / `no_playlist`) — regression-pin on existing behavior."
    - "Both commands exit with code 0 on a successful curation (`stop_reason == \"created\"`) — regression-pin."
    - "The hint string is printed to stderr in the bracket-tagged form `[viber/codex] tool_starvation: <hint>` — consistent with the existing pattern at `__main__.py:2555`."
    - "Edits are SURGICAL — only the two hint dicts (`__main__.py:2547-2556` for curate, `:2591-2602` for build-set) + their `return 1` lines are touched. The LiveKit-session block at `__main__.py:1353` is structurally untouched (disjointness contract with the parallel LiveKit-upgrade handoff)."
  artifacts:
    - path: "src/vibemix/__main__.py"
      provides: "CLI exit-code dispatch: 10 for tool_starvation, 1 for other failures, 0 for success"
      contains: "tool_starvation"
    - path: "tests/library/test_cli_exit_codes.py"
      provides: "Exit-code dispatch tests for curate + build-set + telegram curate_fn adapter"
      exports:
        - "test_curate_exits_10_on_starvation"
        - "test_curate_exits_1_on_other_failures"
        - "test_curate_exits_0_on_success"
        - "test_build_set_exits_10_on_starvation"
        - "test_build_set_exits_1_on_other_failures"
        - "test_telegram_curate_fn_normalizes_starvation"
  key_links:
    - from: "src/vibemix/__main__.py::_cmd_library_curate_codex"
      to: "exit code 10 dispatch"
      via: "return 10 if result.stop_reason == 'tool_starvation' else 1"
      pattern: "return 10 if result\\.stop_reason == .tool_starvation. else 1"
    - from: "src/vibemix/__main__.py::_cmd_library_build_set_codex"
      to: "exit code 10 dispatch"
      via: "same surgical edit"
      pattern: "return 10 if result\\.stop_reason == .tool_starvation. else 1"
    - from: "src/vibemix/__main__.py::_cmd_library_telegram::curate_fn adapter"
      to: "normalized stop_reason payload for format_reply"
      via: "if result.stop_reason == 'tool_starvation': return {'ok': False, 'stop_reason': 'tool_starvation', 'hint': result.error or ''}"
      pattern: "stop_reason.*tool_starvation"
---

<objective>
Land the CLI exit-code dispatch for `tool_starvation` in BOTH CLI handlers (`_cmd_library_curate_codex` at `__main__.py:2545-2556` + `_cmd_library_build_set_codex` at `__main__.py:2591-2602`) AND prepare the Telegram bridge's `curate_fn` adapter at `__main__.py:2837-2850` to normalize the starvation payload before it reaches `format_reply` (Plan 99-07 will add the `format_reply` branch on the receiving end).

Purpose: Close the user-visible side of HARDEN-RETRY-04. After this plan, `library curate "anything" && echo "exit=$?"` against an empty library cache emits `exit=10` AND a one-line stderr `[viber/codex] tool_starvation: library has 0 tracks — run \`library ingest\` first`. Scripts can branch on the exit code.

Output: Three surgical edits to `__main__.py` — two are 2-line changes (add hint-dict entry + replace `return 1` with `return 10 if ... else 1`), one is a 4-line addition to the Telegram `curate_fn` adapter. Six new tests in a new `tests/library/test_cli_exit_codes.py` file.

**Disjointness reminder:** The LiveKit-session_loop block at `__main__.py:1353` is OWNED by the parallel LiveKit-upgrade handoff (see `STATE.md` § Concurrent Work + memory `project_streaming_pipe_speedfix_livekit_upgrade_handoff`). This plan touches ONLY the three named CLI sites — never line 1353 or anywhere near it. Commit by named path, never `git add -A`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-harden-retry/99-CONTEXT.md
@.planning/phases/99-harden-retry/99-RESEARCH.md
@.planning/phases/99-harden-retry/99-04-SUMMARY.md
@CLAUDE.md

# Source file this plan touches (READ ONLY the three target sections)
@src/vibemix/__main__.py

<interfaces>
<!-- Exact insertion sites verified at research time. -->

From src/vibemix/__main__.py:2547-2556 (existing `_cmd_library_curate_codex` tail — current state):
```python
if result.stop_reason != "created":
    print(_json.dumps(out, indent=2), file=sys.stderr)
    hint = {
        "codex_not_installed": (
            "Install Codex: `npm i -g @openai/codex` (or `brew install "
            "codex`), then `codex login`."
        ),
        "codex_auth_required": "Run `codex login` to connect your ChatGPT plan.",
        "timeout": "Codex took too long — try a narrower theme.",
        # ─── THIS PLAN INSERTS: "tool_starvation": result.error or "no playlist (tool starvation)",
    }.get(result.stop_reason, result.error or "no playlist created")
    print(f"[viber/codex] {result.stop_reason}: {hint}", file=sys.stderr)
    return 1  # ─── THIS PLAN REPLACES with: return 10 if result.stop_reason == "tool_starvation" else 1
```

Same shape at `__main__.py:2591-2602` for `_cmd_library_build_set_codex` (set-prep sibling).

From src/vibemix/__main__.py:2837-2850 (existing `curate_fn` adapter inside `_cmd_library_telegram`):
```python
def curate_fn(theme: str) -> dict:
    result = curate_with_codex(theme, lib)
    # ─── THIS PLAN INSERTS BEFORE the existing playlist-name check: ───
    # if result.stop_reason == "tool_starvation":
    #     return {"ok": False, "stop_reason": "tool_starvation", "hint": result.error or ""}
    if result.playlist_name is None or not result.track_ids:
        return {"ok": False, "error": result.error or f"no playlist ({result.stop_reason})"}
    # ... existing happy-path
```

`CodexCurateResult` / `CodexBuildSetResult` dataclasses (from Plan 99-04): both have `stop_reason: str` attribute. Plan 99-04 guarantees `"tool_starvation"` arrives as the value when starvation fires.
</interfaces>

<decisions_locked>
- D-06 (Decision 6): CLI exit code 10 for `tool_starvation`. Reserved range 10-19 for stop_reasons (11 reserved for Phase 100 `clarification_needed`). ← THIS PLAN.
- D-08 (Decision 8): Telegram `curate_fn` normalizes the starvation payload so `format_reply` (Plan 99-07) sees a uniform `{"ok": False, "stop_reason": "tool_starvation", "hint": "..."}` shape. ← THIS PLAN sets up; Plan 99-07 consumes.
</decisions_locked>

<antipatterns_to_avoid>
- Touching line 1353 of `__main__.py` (LiveKit-session_loop / turn_handling — owned by the parallel handoff). Commit by NAMED file paths only, never `git add -A`. Run `git diff src/vibemix/__main__.py` before commit; if any hunk near line 1353 appears, REVERT it surgically before committing.
- Replacing the entire hint dict block instead of adding ONE entry. The edit is additive: insert `"tool_starvation": result.error or "no playlist (tool starvation)"` as ONE new line in the existing dict literal. Diff should show 1 line added per CLI handler + 1 line modified (return statement).
- Using a `match`/`case` instead of the inline ternary for the return — the existing dispatch-dict pattern is the established convention here (per RESEARCH.md Pattern 2). Phase 100 review may refactor to a return-code dict-lookup if the count grows; for now, a single ternary is the minimal-diff edit.
- Forgetting the build-set sibling. The CLI exits 10 from BOTH handlers — `curate` and `build-set` — because HARDEN-RETRY-04 names both.
- Letting the Telegram `curate_fn` fall through into the generic `{"ok": False, "error": ...}` branch when starvation fires. That branch in Plan 99-07's `format_reply` would render as a generic error, NOT the hint. The `curate_fn` normalization is the CONTRACT bridge between Plan 99-06 and Plan 99-07.
- Calling `result.error` without the `or "..."` fallback — `error` may be `None` if the wrapper didn't populate it (defensive). The fallback `or "no playlist (tool starvation)"` provides a sane default. Same in the Telegram `curate_fn`'s `result.error or ""`.
</antipatterns_to_avoid>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing CLI exit-code tests in tests/library/test_cli_exit_codes.py</name>
  <files>tests/library/test_cli_exit_codes.py</files>
  <behavior>
    Create new file `tests/library/test_cli_exit_codes.py` with six tests (RED on current source):

    - `test_curate_exits_10_on_starvation`: Mock `curate_with_codex` to return `CodexCurateResult(theme="x", stop_reason="tool_starvation", error="library has 0 tracks")`. Invoke `_cmd_library_curate_codex` (or the CLI through `vibemix.__main__:main` with crafted `argv`; whichever is the least-intrusive test posture per existing `tests/library/` style — check what the existing CLI tests do, but note there may be none). Assert return code == 10 AND stderr contains `[viber/codex] tool_starvation:` AND stderr contains the hint substring `library has 0 tracks`.

    - `test_curate_exits_1_on_other_failures`: Mock `curate_with_codex` to return `CodexCurateResult(theme="x", stop_reason="timeout", error="Codex took too long")`. Assert return code == 1. Regression-pin on existing behavior.

    - `test_curate_exits_0_on_success`: Mock to return `CodexCurateResult(theme="x", stop_reason="created", playlist_name="test", track_ids=["t1"])`. Assert return code == 0. Regression-pin.

    - `test_build_set_exits_10_on_starvation`: Mock `build_set_with_codex` to return `CodexBuildSetResult(theme="x", stop_reason="tool_starvation", error="...")`. Assert return code == 10.

    - `test_build_set_exits_1_on_other_failures`: Mock to return non-starvation failure. Assert return code == 1.

    - `test_telegram_curate_fn_normalizes_starvation`: Build the `curate_fn` adapter using the actual code path in `_cmd_library_telegram` (or extract it to a unit-testable shape if the existing structure doesn't permit). Mock `curate_with_codex` to return starvation result. Assert the adapter returns `{"ok": False, "stop_reason": "tool_starvation", "hint": "<error string>"}`. This is the CONTRACT bridge that Plan 99-07's `format_reply` will consume.

    Use `unittest.mock.patch` or `monkeypatch` to inject the wrapper mocks. Tests MUST fail RED on current source.
  </behavior>
  <action>
    Create new test file. Decide test posture by inspecting `tests/library/` for existing CLI tests (e.g. is there a `test_cli.py` or `test_main.py`?) — if no precedent, use `unittest.mock.patch` directly. The CLI handlers live in `vibemix.__main__` — calling them directly via `from vibemix.__main__ import _cmd_library_curate_codex` is the cleanest test posture; if those are private and not importable, wrap via the CLI dispatcher pattern. Document the chosen posture in the file's top docstring. For the Telegram `curate_fn` test: if the adapter is defined inline inside `_cmd_library_telegram` (closure), the cleanest path is to extract it during Task 2 OR to test the behavior end-to-end by mocking and invoking the parent. Note any refactor needed; if refactor is too invasive for THIS plan, document a minimal-diff path: keep `curate_fn` as a closure but add a thin module-level helper `_normalize_codex_curate_result(result) -> dict` that `curate_fn` calls; Task 2 adds the helper + the test calls the helper directly.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_cli_exit_codes.py 2>&1 | tail -20</automated>
  </verify>
  <done>
    Six tests present; all six fail RED on current source. Test posture chosen + documented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surgical CLI exit-code edits in __main__.py + Telegram normalizer</name>
  <files>src/vibemix/__main__.py</files>
  <behavior>
    Three surgical edits in `src/vibemix/__main__.py`:

    A. In `_cmd_library_curate_codex` (line ~2547-2556):
       - Add to hint dict: `"tool_starvation": result.error or "no playlist (tool starvation)",`
       - Replace `return 1` with `return 10 if result.stop_reason == "tool_starvation" else 1`

    B. In `_cmd_library_build_set_codex` (line ~2591-2602):
       - Same two edits as A, mirrored for the build-set hint dict + return statement.

    C. In `_cmd_library_telegram` `curate_fn` adapter (line ~2837-2850):
       - Insert BEFORE the existing `if result.playlist_name is None or not result.track_ids:` line:
         ```
         if result.stop_reason == "tool_starvation":
             return {"ok": False, "stop_reason": "tool_starvation", "hint": result.error or ""}
         ```
       - If Task 1 chose the "extract to module-level helper" path (`_normalize_codex_curate_result`), add the helper here at module scope and refactor `curate_fn` to call it.

    All six Task-1 tests turn GREEN. No other CLI tests regress. Crucially: NO changes near line 1353 (LiveKit-session block — disjointness contract).
  </behavior>
  <action>
    Edit `src/vibemix/__main__.py` at the three named insertion sites. After editing, run `git diff src/vibemix/__main__.py | grep -E "^[+-]" | wc -l` — should be a small number (under 20 lines total across both `+` and `-`). If significantly larger, you've gone outside the surgical-edit envelope; revert and redo. Critically: `git diff src/vibemix/__main__.py | grep -B5 -A5 "1353\|turn_handling\|livekit"` should show NOTHING — those keywords belong to the LiveKit handoff. If any of those terms appear in your diff, REVERT immediately.
  </action>
  <verify>
    <automated>source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/library/test_cli_exit_codes.py tests/library/test_codex_curate_stop_reason.py 2>&1 | tail -25</automated>
  </verify>
  <done>
    All three edits present at the named locations; all six tests pass; the LiveKit block at line 1353 is structurally untouched (verified by `git diff` window inspection); diff envelope is small (<20 lines total). Grep gate: `grep -c "tool_starvation" src/vibemix/__main__.py` returns exactly 4 (2 hint dict entries + 2 return-ternary branches + possibly 1 normalizer = 4-5; document the exact count in SUMMARY).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI shell ↔ vibemix process | Exit code is the contract surface; scripts branch on `$?` |
| Telegram bridge `curate_fn` ↔ `format_reply` | Internal dict-shape contract; the curate_fn output is consumed by format_reply (Plan 99-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-08 | Spoofing | Exit-code masquerade | accept | Exit code 10 is unallocated in standard Unix conventions (verified — RESEARCH.md Pitfall 6). A script wrapper relying on exit codes trusts the vibemix CLI as much as any other tool. No new exposure. |
| T-99-05 | Information disclosure | Hint string printed to stderr (CLI) | accept | Hint copy is deterministic; user's `theme` query is the only user-supplied content interpolated. Stderr printed as plain text; no escape-sequence injection vector. The hint never includes FS paths (Decision 5 seed is path-free). |
| T-99-DJ | Tampering | Disjointness violation (touching LiveKit-session block) | mitigate | Diff-window grep check at Task 2 end. If `turn_handling` or line 1353 hunks appear in the diff, REVERT — that's the parallel-handoff's territory. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages; stdlib only. |
</threat_model>

<verification>
- `pytest -q tests/library/test_cli_exit_codes.py tests/library/test_codex_curate_stop_reason.py` — green.
- `pytest -q tests/library/` — broader regression-pin green.
- `git diff src/vibemix/__main__.py` envelope is small (<20 lines); no hunks near line 1353.
- Grep: `grep -c "tool_starvation" src/vibemix/__main__.py` returns 4-5 (per SUMMARY recording).
- Grep: `grep -n "return 10" src/vibemix/__main__.py` returns exactly 2 hits (one per CLI handler) — pin the exit-code contract.
- Bracket-tag pattern preserved: `grep "\[viber/codex\]" src/vibemix/__main__.py` shows the existing `print` lines are intact (consistency with CLAUDE.md § Conventions § Logging).
- Plan 99-05's gate test `test_stop_reason_writes_confined_to_toolset` stays GREEN because `__main__.py` was on the whitelist.
</verification>

<success_criteria>
- `library curate` and `library build-set` exit 10 on `stop_reason == "tool_starvation"`.
- Both exit 1 on other failures (regression-pinned).
- Both exit 0 on success (regression-pinned).
- Telegram `curate_fn` adapter emits the normalized `{"ok": False, "stop_reason": "tool_starvation", "hint": ...}` shape for Plan 99-07.
- LiveKit-session block at `__main__.py:1353` is byte-equivalent (disjointness contract preserved).
- Stderr line is bracket-tagged `[viber/codex] tool_starvation: <hint>` per CLAUDE.md § Logging.
- Phase 100 forward-compat: the inline ternary `return 10 if ... else 1` can be extended to `return 10 if ... else 11 if ... == "clarification_needed" else 1` (or refactored to dict-lookup) without breaking the test posture.
</success_criteria>

<output>
Create `.planning/phases/99-harden-retry/99-06-SUMMARY.md` when done.

SUMMARY must record:
- Exact line numbers of the three edits (curate hint, build-set hint, telegram curate_fn).
- The `grep -c "tool_starvation" src/vibemix/__main__.py` count.
- Confirmation that the LiveKit block at line 1353 is byte-equivalent (manual diff-window check: paste the 5-line context around 1353 from before-and-after).
- Confirmation that the test posture for `_cmd_library_curate_codex` / `_cmd_library_build_set_codex` / `curate_fn` is documented in the test file's top docstring.
- Note that Plan 99-07 will consume the `curate_fn`'s normalized starvation payload via `format_reply`.
</output>
</content>
</invoke>