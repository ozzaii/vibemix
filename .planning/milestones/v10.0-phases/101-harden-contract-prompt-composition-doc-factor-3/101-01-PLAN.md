---
phase: 101-harden-contract-prompt-composition-doc-factor-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PROMPT-COMPOSITION.md
autonomous: true
requirements:
  - HARDEN-CONTRACT-01
  - HARDEN-CONTRACT-02
  - HARDEN-CONTRACT-04
  - HARDEN-CONTRACT-07

must_haves:
  truths:
    - "docs/PROMPT-COMPOSITION.md exists at repo root and is byte-resolvable"
    - "Doc opens with a 1-paragraph Purpose section + 3-line Reading guide"
    - "Composite table enumerates every EventType currently emitted by event_detector.py via self._fire(...) — 9 string-literal types: KAAN_SPOKE, MANUAL, TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, KEY_CLASH, TRANSITION_OPPORTUNITY, HEARTBEAT"
    - "Each table row cites the file:line of the originating _fire() call (e.g. event_detector.py:273 for TRACK_CHANGE)"
    - "Each row shows the per-type cooldown by cross-reference to MIN_EVENT_GAP_PER_TYPE at audio/constants.py:77 with the concrete seconds value"
    - "Each row marks diet-mode eligibility — only KAAN_SPOKE, MIX_MOVE, LAYER_ARRIVAL, HEARTBEAT are eligible (per ACK_ELIGIBLE_EVENTS at coach.py:54)"
    - "Each row lists the citation sources from EVIDENCE_SOURCES (evidence_registry.py:129) the event may carry"
    - "A dedicated Cooldown Table section reproduces MIN_EVENT_GAP_PER_TYPE one row per key with the inline comment rationale preserved as a short note column"
  artifacts:
    - path: "docs/PROMPT-COMPOSITION.md"
      provides: "Sections 1-3 + Section 7 (Purpose · Reading guide · Composite table · Cooldown table)"
      contains: "## Composite Event Contract"
      contains_also: "EVIDENCE_SOURCES"
      contains_also: "MIN_EVENT_GAP_PER_TYPE"
      contains_also: "ACK_ELIGIBLE_EVENTS"
      min_lines: 80
  key_links:
    - from: "docs/PROMPT-COMPOSITION.md"
      to: "src/vibemix/state/event_detector.py"
      via: "file:line citation of each self._fire(...) call site"
      pattern: "event_detector\\.py:(239|243|273|289|312|340|383|443|469)"
    - from: "docs/PROMPT-COMPOSITION.md"
      to: "src/vibemix/audio/constants.py"
      via: "cite of MIN_EVENT_GAP_PER_TYPE dict header"
      pattern: "audio/constants\\.py:77"
    - from: "docs/PROMPT-COMPOSITION.md"
      to: "src/vibemix/state/evidence_registry.py"
      via: "cite of EVIDENCE_SOURCES frozenset"
      pattern: "evidence_registry\\.py:129"
    - from: "docs/PROMPT-COMPOSITION.md"
      to: "src/vibemix/state/coach.py"
      via: "cite of ACK_ELIGIBLE_EVENTS"
      pattern: "coach\\.py:54"
---

<objective>
Create `docs/PROMPT-COMPOSITION.md` (NEW file) and populate it with sections 1-3 + section 7 of the contract: Purpose, Reading guide, the load-bearing composite EventType × everything table, and the per-event-type cooldown table. These four sections satisfy REQs 01/02 (table column for citation sources) / 04 (diet-mode column) / 07 (cooldown cross-reference).

Purpose: ship the single named contract surface for live-prompt composition so new mood/lens contributors stop reverse-engineering `coach.py` + `event_detector.py` + `evidence_registry.py` each time. This plan delivers the quick-reference TABLE layer; subsequent plans deliver the depth sections.

Output: `docs/PROMPT-COMPOSITION.md` with sections 1, 2, 3, 7 fully written from grep-verified file:line anchors against current source.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-harden-contract-prompt-composition-doc-factor-3/101-CONTEXT.md

# Source files cited by the table (READ-ONLY references — this plan touches ZERO src/ code)
@src/vibemix/state/event_detector.py
@src/vibemix/state/evidence_registry.py
@src/vibemix/state/coach.py
@src/vibemix/audio/constants.py

<interfaces>
<!-- The shipped 9-event taxonomy (verified grep at write-time of this plan). -->
<!-- ALL event types are string literals, NOT an Enum. -->

From src/vibemix/state/event_detector.py (self._fire call sites):
  Line 239: "KAAN_SPOKE"          cooldown_key="MIC"
  Line 243: "MANUAL"
  Line 273: "TRACK_CHANGE"
  Line 289: "PHASE"
  Line 312: "LAYER_ARRIVAL"
  Line 340: "MIX_MOVE"
  Line 383: "KEY_CLASH"
  Line 443: "TRANSITION_OPPORTUNITY"
  Line 464: (re-emit branch — same ev.type as caller, not a new taxonomy entry)
  Line 469: "HEARTBEAT"

From src/vibemix/state/evidence_registry.py:129:
```python
EVIDENCE_SOURCES: frozenset[str] = frozenset(
    {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue"}
)
```
(11 sources — this is the locked surface; Phase 101 documents it, never extends it.)

From src/vibemix/state/coach.py:54:
```python
ACK_ELIGIBLE_EVENTS: frozenset[str] = frozenset(
    {"HEARTBEAT", "MIX_MOVE", "LAYER_ARRIVAL", "KAAN_SPOKE"}
)
```

From src/vibemix/audio/constants.py:77 (MIN_EVENT_GAP_PER_TYPE — partial listing,
read the file at write-time for the FULL dict including KICK_SWAP / SUB_LAYER_ARRIVAL /
KICK_DENSITY_SHIFT / BREAKDOWN_KICK_KILL / REENTRY_KICK_LAND / PHRASE_BOUNDARY etc.):
  "TRACK_CHANGE": 5.0
  "PHASE": 10.0
  "LAYER_ARRIVAL": 10.0
  "MIX_MOVE": 14.0
  "HEARTBEAT": HEARTBEAT_SEC  (45.0 — verify by grep)
  "MIC": 3.0
  "MANUAL": 1.5
  "KICK_SWAP": 14.0
  "SUB_LAYER_ARRIVAL": 16.0
  "KICK_DENSITY_SHIFT": 18.0
  "BREAKDOWN_KICK_KILL": 20.0
  "REENTRY_KICK_LAND": 12.0
  + further entries below line 103 (read the rest at write-time before tabulating)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Survey source anchors + draft composite table + write sections 1-3</name>
  <files>docs/PROMPT-COMPOSITION.md</files>
  <read_first>
    - src/vibemix/state/event_detector.py (lines 230-475 — read in ONE pass, extract every self._fire(...) call: event_type string, line number, cooldown_key argument if present, surrounding context for "evidence fields populated")
    - src/vibemix/state/evidence_registry.py (lines 125-180 — EVIDENCE_SOURCES frozenset at :129, inline comments at :144-167 documenting which sources are retrieval-time vs ingest-time, _SOURCE_ALT at :174)
    - src/vibemix/state/coach.py lines 50-70 — ACK_ELIGIBLE_EVENTS at :54
    - src/vibemix/audio/constants.py lines 70-130 — MIN_EVENT_GAP_PER_TYPE at :77 (read the FULL dict, not just the partial in the interfaces block — there are entries below the ones quoted)
  </read_first>
  <action>
    Create `docs/PROMPT-COMPOSITION.md` from scratch (file does NOT exist yet). Write sections 1 + 2 + 3 + 7 per the locked CONTEXT.md ordering — sections 4/5/6 + appendix 8 are deferred to Plans 101-02 / 101-03.

    Section structure to land in THIS plan:

    1. **Section 1 — Purpose** (1 paragraph, prose). State that this doc is the single named contract for what enters the LIVE CO-HOST prompt per event type, and that new mood/lens contributors must read it before editing the prompt path. Call out that the doc is SCOPED to the live co-host (NOT the Viber/library prompt surface, which lives separately).

    2. **Section 2 — Reading guide** (3-4 lines, bullet list). Pointers: "quick reference → §3 composite table", "citation grammar depth → §4 (Plan 101-02)", "recall-fragment depth → §5 (Plan 101-02)", "diet-mode depth → §6 (Plan 101-02)", "cooldown table → §7", "how to re-verify cites → §8 appendix (Plan 101-03)". Forward-link section anchors so the doc reads coherently even before §§4-6+8 land.

    3. **Section 3 — Composite Event Contract** (the load-bearing table). Heading MUST be the literal string `## Composite Event Contract` (must_haves grep gate). Markdown table with these columns:
       | EventType | Fires at | Cooldown (s) | Diet-mode eligible? | Citation sources |
       One row per shipped event type. Required rows (verify each by grep before writing): KAAN_SPOKE, MANUAL, TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, KEY_CLASH, TRANSITION_OPPORTUNITY, HEARTBEAT. Each "Fires at" cell cites `src/vibemix/state/event_detector.py:<line>`. Each "Cooldown" cell cites the concrete seconds from MIN_EVENT_GAP_PER_TYPE (or the cooldown_key indirection, e.g. KAAN_SPOKE uses cooldown_key="MIC" → 3.0s per the dict at :85). Each "Diet" cell is ✓ for events in ACK_ELIGIBLE_EVENTS (HEARTBEAT, MIX_MOVE, LAYER_ARRIVAL, KAAN_SPOKE), ✗ otherwise. Each "Citation sources" cell lists which of the 11 EVIDENCE_SOURCES the event may carry — base this on a STRUCTURAL read of the event_detector.py site (which evidence fields the event populates) cross-referenced with the EVIDENCE_SOURCES frozenset; do NOT invent associations. If unclear from grep, mark `(see §4)` and let Plan 101-02 fill the detail (acceptable — Plan 101-02 reads the same files and resolves any ambiguity).

       Below the table, a short paragraph naming the source-of-truth files for each column ("EventType + Fires at → event_detector.py", "Cooldown → audio/constants.py:77", "Diet → coach.py:54", "Citation sources → evidence_registry.py:129") so a reader can rebuild the table from source if the doc drifts.

    4. **Section 7 — Per-event-type Cooldowns** (the dedicated cooldown reference). Heading: `## Per-Event-Type Cooldowns`. Reproduce MIN_EVENT_GAP_PER_TYPE one row per dict key (full key set — KICK_SWAP, SUB_LAYER_ARRIVAL, KICK_DENSITY_SHIFT, BREAKDOWN_KICK_KILL, REENTRY_KICK_LAND, PHRASE_BOUNDARY, etc. — read the FULL dict at write-time, do not stop at TRACK_CHANGE..HEARTBEAT). Columns: `Key | Seconds | Source-of-truth comment`. The third column quotes the inline comment text from `audio/constants.py` for that row when present (short one-liner), preserving the rationale anchors (e.g. `# Plan 40-04 — v4 chat-tested 2026-05-11 baseline`). Reference `src/vibemix/audio/constants.py:77` (the dict header line) at the top of the section.

    Tone: reference-doc neutral. Bullet lists + tables, not paragraphs. Code citations are GitHub-style `path:line`. No code blocks for full function bodies — anchor cite only. Per CONTEXT.md §decisions, the doc is a CONTRACT, not a tutorial. ASCII-only — no emojis (per global CLAUDE.md).

    The file MUST end with a stub "## Citation Source Grammar (REQ-02 detail)", "## Recall-Fragment Shapes (REQ-03)", "## Diet-Mode (REQ-04)", and "## Appendix: How to Grep-Verify This Doc (REQ-05)" — each with a single placeholder line like `_Filled by Plan 101-02._` or `_Filled by Plan 101-03._` so the section anchors exist and the Reading guide forward-links resolve. This is a structural stub, not content — Plans 101-02 and 101-03 overwrite the placeholder lines with real prose.

    Do NOT use `Bash(cat << EOF)` — use the Write tool to create the file in one call.

    Use grep against the actual current source at write-time for EVERY file:line cite — do not trust the line numbers in this PLAN.md if grep disagrees with them; the grep result wins (REQ-05 contract starts at write-time).
  </action>
  <verify>
    <automated>test -f docs/PROMPT-COMPOSITION.md && grep -c '## Composite Event Contract' docs/PROMPT-COMPOSITION.md | grep -q '^1$' && grep -c 'EVIDENCE_SOURCES' docs/PROMPT-COMPOSITION.md | awk '$1>=1{exit 0}{exit 1}' && grep -c 'MIN_EVENT_GAP_PER_TYPE' docs/PROMPT-COMPOSITION.md | awk '$1>=1{exit 0}{exit 1}' && grep -c 'ACK_ELIGIBLE_EVENTS' docs/PROMPT-COMPOSITION.md | awk '$1>=1{exit 0}{exit 1}' && grep -c 'TRACK_CHANGE' docs/PROMPT-COMPOSITION.md | awk '$1>=8{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/PROMPT-COMPOSITION.md` exists at repo root.
    - File contains literal heading `## Composite Event Contract` (REQ-CONTRACT-01).
    - File contains literal token `EVIDENCE_SOURCES` (REQ-CONTRACT-02 anchor).
    - File contains literal token `MIN_EVENT_GAP_PER_TYPE` (REQ-CONTRACT-07 anchor).
    - File contains literal token `ACK_ELIGIBLE_EVENTS` (REQ-CONTRACT-04 anchor).
    - Composite table has rows for all 9 shipped EventTypes (KAAN_SPOKE, MANUAL, TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, KEY_CLASH, TRANSITION_OPPORTUNITY, HEARTBEAT). Verifiable: each event-type string appears at least once in the table region.
    - Every `event_detector.py:<N>` citation in the doc has `<N>` ∈ the grep-verified line numbers for `self._fire(...)` in current source (Plan 101-03 will rerun this grep against current file).
    - File ends with stub `##` headings for sections 4, 5, 6, 8 each carrying a single placeholder line — anchors must EXIST so Plans 101-02 / 101-03 can replace placeholders without restructuring.
    - File is ASCII-only, no emojis, no code blocks for full function bodies (only file:line anchor cites + the markdown tables).
  </acceptance_criteria>
  <done>
    Sections 1, 2, 3, 7 of docs/PROMPT-COMPOSITION.md are content-complete with every cell traceable to a current-source file:line. The skeleton (stub anchors for sections 4, 5, 6, 8) is in place so Plans 101-02 / 101-03 can land their content with simple section replacements. REQ-CONTRACT-01, -02 (table column), -04 (table column), -07 (full §7) are landed.
  </done>
</task>

</tasks>

<verification>
- `test -f docs/PROMPT-COMPOSITION.md` succeeds.
- `wc -l docs/PROMPT-COMPOSITION.md` ≥ 80 lines (sanity floor on table + skeleton).
- `grep -c '## Composite Event Contract' docs/PROMPT-COMPOSITION.md` returns 1.
- `grep -c '## Per-Event-Type Cooldowns' docs/PROMPT-COMPOSITION.md` returns 1.
- For each of TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, KEY_CLASH, TRANSITION_OPPORTUNITY, HEARTBEAT, KAAN_SPOKE, MANUAL: `grep -c "$EV" docs/PROMPT-COMPOSITION.md` ≥ 1.
- Plan 101-03's grep loop will subsequently confirm every file:line cite resolves — failures there are this plan's bugs.
</verification>

<success_criteria>
REQs CONTRACT-01 (composite EventType table), -02 (table column for citation sources — depth in Plan 101-02), -04 (table column for diet-mode — depth in Plan 101-02), -07 (full cooldown reference §7) are landed in `docs/PROMPT-COMPOSITION.md`. Skeleton anchors for §§4-6+8 exist so the next two plans can fill content without restructuring. Composite table is the load-bearing artifact and is complete to 9 EventTypes.
</success_criteria>

<output>
Create `.planning/phases/101-harden-contract-prompt-composition-doc-factor-3/101-01-SUMMARY.md` when done.
</output>
