---
phase: 101-harden-contract-prompt-composition-doc-factor-3
plan: 02
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - docs/PROMPT-COMPOSITION.md
autonomous: true
requirements:
  - HARDEN-CONTRACT-02
  - HARDEN-CONTRACT-03

must_haves:
  truths:
    - "Section 4 Citation Source Grammar enumerates all 11 EVIDENCE_SOURCES with body grammar + example + which event types may cite each"
    - "Section 5 Recall-Fragment Shapes documents the per-event-family recall templates from coach.py with worked examples"
    - "Section 6 Diet-Mode explains what triggers the diet branch, what gets stripped, and the TTFT-budget rationale"
    - "Plan 101-01 stub placeholder lines for §§4-6 are REPLACED with real prose; section headings are PRESERVED"
    - "Every code reference in the new content uses file:line citation form (path:N) — no inlined function bodies"
  artifacts:
    - path: "docs/PROMPT-COMPOSITION.md"
      provides: "Sections 4, 5, 6 — citation grammar depth + recall-fragment shapes + diet-mode rationale"
      contains: "## Citation Source Grammar"
      contains_also: "## Recall-Fragment Shapes"
      contains_also: "## Diet-Mode"
      contains_also: "recall_fragment_for_event"
      contains_also: "TRANSITION_SHAPE_RECALL_FRAGMENT_TPL"
      contains_also: "VOCABULARY_RECALL_FRAGMENT_TPL"
      min_lines: 180
  key_links:
    - from: "docs/PROMPT-COMPOSITION.md"
      to: "src/vibemix/state/evidence_registry.py"
      via: "cites of EVIDENCE_SOURCES + _SOURCE_ALT + CITATION_RE locations"
      pattern: "evidence_registry\\.py:(129|174|199)"
    - from: "docs/PROMPT-COMPOSITION.md"
      to: "src/vibemix/state/coach.py"
      via: "cites of recall_fragment_for_event + build_prompt + diet branch + ACK gate"
      pattern: "coach\\.py:(54|158|794|824|847)"
---

<objective>
Replace the Plan 101-01 stub placeholders in `docs/PROMPT-COMPOSITION.md` for sections 4 (Citation Source Grammar — REQ-02 depth), 5 (Recall-Fragment Shapes — REQ-03), and 6 (Diet-Mode — REQ-04) with real reference-doc prose. The composite table in §3 (Plan 101-01) gives the quick-reference view; this plan delivers the depth a contributor needs when actually editing the prompt path.

Purpose: a new mood/lens contributor needs three things beyond the table — (a) the EBNF body grammar for each of the 11 citation sources + which event types may legitimately cite which, (b) the recall-fragment templates that conditionally append to PHASE / TRACK_CHANGE / MIX_MOVE / LAYER_ARRIVAL prompts with their gating + worked examples, (c) what the diet-mode branch at coach.py:824 actually strips and why (the TTFT budget). This plan lands those three sections in one pass.

Output: docs/PROMPT-COMPOSITION.md with §§4-6 content-complete; §§1-3+7 from Plan 101-01 byte-preserved; §8 still a stub for Plan 101-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-harden-contract-prompt-composition-doc-factor-3/101-CONTEXT.md
@.planning/phases/101-harden-contract-prompt-composition-doc-factor-3/101-01-PLAN.md
@.planning/phases/101-harden-contract-prompt-composition-doc-factor-3/101-01-SUMMARY.md
@docs/PROMPT-COMPOSITION.md

# Source files for REQ-02/03/04 content
@src/vibemix/state/evidence_registry.py
@src/vibemix/state/coach.py

<interfaces>
<!-- All 11 EVIDENCE_SOURCES with their introducing phases (lifted from
     evidence_registry.py inline comments — these are reference anchors,
     read the file at write-time for the most current wording). -->

From src/vibemix/state/evidence_registry.py:129:
  "ev"        — Event-anchored citation (live event evidence)
  "aud"       — Audio-domain evidence
  "midi"      — MIDI controller evidence
  "track"     — Track metadata evidence
  "screen"    — Screen-watcher evidence
  "mix"       — Mix-state evidence
  "tend"      — Tendency/profile evidence
  "key"       — Camelot key evidence (Phase 59 / DECK-03; body grammar key:<deck>:<camelot>)
  "recall"    — Past-session record recall (Phase 65 / RECALL-01; body grammar recall:<record_id>)
  "exemplar"  — Library-grounded exemplar (Phase 93 / EXEMPLAR-05; body grammar exemplar:<track_id>)
  "cue"       — Cue-anchor evidence (Phase 96 / CURR-3.07; body grammar cue:<anchor_id>)

Inline grammar locked at evidence_registry.py:174:
  _SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key|recall|exemplar|cue"
  _INNER_ATOM = rf"(?:{_SOURCE_ALT}):[^\s,\]]+"
  EVIDENCE_CITATION_RE at :199 — compiled regex matching the LOCKED EBNF citation grammar
  (citation := '[' atom ( ',' atom )* ']'; atom := <source>:<body>; body non-empty,
   no whitespace / no comma / no bracket).

Schema-mirror law (per evidence_registry.py:126-128 comments):
  EVIDENCE_SOURCES is the source-of-truth; _SOURCE_ALT + prompts/matrix.py::CITATION_GRAMMAR_BLOCK
  + agent/dj_cohost.py::_build_citation_strip must move in lock-step.
  (Doc references this discipline; do not re-document the 4 mirror sites — pointer to
   the comment block is enough.)

Asymmetry (per evidence_registry.py:169-173): memory/ingest.py's alternation stays at
  8 sources — recall / exemplar / cue are RETRIEVAL-time only, never ingest-time.

From src/vibemix/state/coach.py:
  Line 54:  ACK_ELIGIBLE_EVENTS = frozenset({"HEARTBEAT", "MIX_MOVE", "LAYER_ARRIVAL", "KAAN_SPOKE"})
  Lines 109-126: TRANSITION_SHAPE_RECALL_FRAGMENT_TPL — gates on TRACK_CHANGE / MIX_MOVE / LAYER_ARRIVAL
  Lines 141-155: VOCABULARY_RECALL_FRAGMENT_TPL — gates on PHASE
  Line 158: def recall_fragment_for_event(ev, recall_moments) -> str
  Line 794: def build_prompt(ev, *, registry_snapshot, recall_moments, diet) -> str
  Line 824: if diet:  → branches into compact 5-field evidence_line + task tail, NO event= tag, NO corpus footer
  Line 825: ACK_ELIGIBLE_EVENTS hard gate (raises ValueError if diet=True on non-ack event)
  Line 829: AICoach._evidence_line_compact(ev.state) — the diet evidence assembler
  Line 847: recall_frag = recall_fragment_for_event(ev, recall_moments) — the conditional-append site
  Line 848: f"[{evidence} | event={ev.type}] {task}{recall_frag}" — full-prompt return

Diet-mode rationale (per build_prompt docstring at coach.py:813-822):
  Saves ≥500ms TTFT on the four ack-eligible event classes. Diet branch:
  - uses _evidence_line_compact (5-field) instead of full evidence_line
  - omits the "| event=TYPE" tag
  - omits the evidence-corpus footer
  - skips the recall_fragment conditional append (diet events are never retrieval events
    per the docstring comment at :810-811)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill §4 Citation Source Grammar + §5 Recall-Fragment Shapes + §6 Diet-Mode</name>
  <files>docs/PROMPT-COMPOSITION.md</files>
  <read_first>
    - docs/PROMPT-COMPOSITION.md (the file from Plan 101-01 — read fully so the Edit operation preserves §§1-3+7 byte-identically and replaces the stub placeholder lines only)
    - src/vibemix/state/evidence_registry.py lines 125-205 (EVIDENCE_SOURCES + _SOURCE_ALT + _INNER_ATOM + EVIDENCE_CITATION_RE + schema-mirror discipline comments)
    - src/vibemix/state/coach.py lines 50-170 (ACK_ELIGIBLE_EVENTS at :54; the two recall fragment templates TRANSITION_SHAPE_RECALL_FRAGMENT_TPL at ~:109-126 and VOCABULARY_RECALL_FRAGMENT_TPL at ~:141-155; recall_fragment_for_event def at :158 — read enough of the function body to learn its gating rule)
    - src/vibemix/state/coach.py lines 790-850 (build_prompt def at :794; diet branch at :824; ACK gate raise at :825; _evidence_line_compact call at :829; full-prompt assembly at :847-848 — read for the diet-vs-full prompt structural difference)
  </read_first>
  <action>
    Open the existing `docs/PROMPT-COMPOSITION.md` (created by Plan 101-01). Use the Edit tool to replace each of the three placeholder stub lines with full content. Sections §§1-3 and §7 MUST be preserved byte-identically — DO NOT restructure them, DO NOT renumber, DO NOT touch the composite table.

    Section content to land:

    **§4 — Citation Source Grammar** (replace placeholder line under `## Citation Source Grammar`).
    Opening sentence: pointer to `src/vibemix/state/evidence_registry.py:129` as the source-of-truth frozenset (11 sources). Then a markdown table with columns `Source | Body grammar | Introduced (phase) | Example | Event types that may cite`. One row per source — all 11. Body grammar follows the locked EBNF: `<source>:<body>` where body is non-empty, no whitespace, no comma, no bracket (cite `evidence_registry.py:174` for `_SOURCE_ALT` and `:199` for `EVIDENCE_CITATION_RE`). For sources with structured body (like `key:<deck>:<camelot>`, `recall:<record_id>`, `exemplar:<track_id>`, `cue:<anchor_id>`) show the inner shape — the FIRST `:` separates source from body, but inner colons survive (per the parser-split comment at evidence_registry.py:141). Worked example for each row, e.g. `[key:A:8A]`, `[recall:20260520-2200:7]`, `[exemplar:library:Marlon-Atlas]`, `[cue:phrase_boundary@45.2]`.

    The "Event types that may cite" column should reflect structural reality:
    - `ev` — any event with state.last_event populated
    - `aud` — events surfacing audio-domain evidence (TRACK_CHANGE, PHASE, LAYER_ARRIVAL, MIX_MOVE, KEY_CLASH, BREAKDOWN_KICK_KILL, REENTRY_KICK_LAND — anything reading audible-deck buffers)
    - `key` — KEY_CLASH primarily; TRACK_CHANGE when both decks have detected keys
    - `recall` — TRACK_CHANGE / MIX_MOVE / LAYER_ARRIVAL via TRANSITION_SHAPE_RECALL_FRAGMENT_TPL; PHASE via VOCABULARY_RECALL_FRAGMENT_TPL (cite coach.py:109-155 + :158)
    - `exemplar` — emitted in tutor-mode reactions (Phase 93+, exemplar-grounded responses)
    - `cue` — emitted in count-in / phrase-prediction reactions (Phase 96; TRANSITION_OPPORTUNITY may carry cue citations)
    - Other sources (`midi`, `track`, `screen`, `mix`, `tend`) — annotated with their domain ("MIDI controller events", "track metadata when populated", etc.).

    Add a short subsection "Schema-mirror discipline" calling out the 4-site mirror law from `evidence_registry.py:126-128` — `EVIDENCE_SOURCES` is the source-of-truth; `_SOURCE_ALT` (at :174), `prompts/matrix.py::CITATION_GRAMMAR_BLOCK`, and `agent/dj_cohost.py::_build_citation_strip` must move in lock-step. Two-line "Asymmetry" note about `memory/ingest.py` staying at 8 sources (recall/exemplar/cue are retrieval-time, not ingest-time) cited from `evidence_registry.py:169-173`.

    **§5 — Recall-Fragment Shapes** (replace placeholder line under `## Recall-Fragment Shapes`).
    Opening sentence: name the conditional-append seam at `src/vibemix/state/coach.py:847` (`recall_frag = recall_fragment_for_event(ev, recall_moments)`) and the dispatch helper at `coach.py:158` (`recall_fragment_for_event`). Then two subsections, one per template:

    1. **Transition-shape recall** (`TRANSITION_SHAPE_RECALL_FRAGMENT_TPL` at `coach.py:109-126`) — gates on `ev.type in {TRACK_CHANGE, MIX_MOVE, LAYER_ARRIVAL}` with non-empty `recall_moments`. Show the template shape in prose (NOT a copy of the full multi-line Python string — instead summarize: "a past-tense callback line referencing a similarly-shaped transition from a prior session, citing exactly `[recall:<record_id>]` once"). List the hard rules from the template body (cite EXACTLY ONCE, do NOT invent past moments, do NOT recommend next tracks, do NOT claim tendencies — quote the four "Hard rules" verbatim from coach.py:118-126 inside a markdown blockquote). Worked example: a TRACK_CHANGE turn where the recall_moment matches → fragment appended with `[recall:20260520-2200:7]`; same turn with no matching past moment → `recall_fragment_for_event` returns `""` and nothing is appended (cite the byte-identity gate at `coach.py:839` docstring + `:847`).

    2. **Vocabulary/register recall** (`VOCABULARY_RECALL_FRAGMENT_TPL` at `coach.py:141-155`) — gates on `ev.type == PHASE` with non-empty `recall_moments`. Same treatment: prose summary ("echo the DJ's own past phrasing in their own register"), the anti-paraphrase discipline (cite the inline comment at coach.py:135-140 about Gemini-paraphrasing failure mode), hard rules quoted, worked example.

    Close §5 with a structural-pin paragraph: the diet path SKIPS the recall fragment (cite coach.py:810-811 docstring) and the registry validates `[recall:<id>]` strict-subset at `agent/dj_cohost.py::_build_citation_strip` (a fabricated id strips the whole turn — Phase 65 anti-poisoning linter; cite coach.py:75-77 + evidence_registry.py:146-148).

    **§6 — Diet-Mode** (replace placeholder line under `## Diet-Mode`).
    Opening: cite the eligibility gate at `src/vibemix/state/coach.py:54` (`ACK_ELIGIBLE_EVENTS`) and the dispatch branch at `coach.py:824` (`if diet:`). State that diet mode is OPT-IN by caller (default `diet=False`) and that the four ack-eligible event types are HEARTBEAT, MIX_MOVE, LAYER_ARRIVAL, KAAN_SPOKE — passing `diet=True` on any other event raises `ValueError` (cite coach.py:825-828) so dispatch bugs fail loud.

    Then a "What diet strips" 4-bullet list (cite each from the build_prompt docstring at coach.py:813-822 + the branch at :824-831):
    - Uses `_evidence_line_compact` (5-field) instead of full `evidence_line` (no registry corpus footer)
    - Omits the `| event=TYPE` tag
    - Omits the evidence-corpus footer (no registry snapshot enumeration)
    - Skips the recall-fragment conditional append (diet events are never retrieval events)

    Close with the "Why" paragraph: TTFT budget — saves ≥500ms on ack-eligible event classes per the docstring at coach.py:816-817; the ack-bank fallback in `agent/dj_cohost.py` lands a fast vocal acknowledgment without the full reasoning-grounded reaction the heavy events get.

    Format discipline (all 3 sections): bullet lists + tables, not paragraphs (per CONTEXT.md tone-lock). file:line citations are `path:line` GitHub-style — clickable in most editors. NO code blocks reproducing function bodies — anchor cite only. The only verbatim quotes are SHORT (≤6-line) blockquotes from the recall-fragment template "Hard rules" tails. ASCII only.

    DO NOT touch §§1-3 or §7. DO NOT renumber sections. DO NOT touch the §8 stub (that's Plan 101-03's job).

    Use the Edit tool with section-level string replacements (find the stub placeholder line under each heading, replace with the new content); do NOT rewrite the whole file with Write.

    Use grep against current source at write-time for EVERY file:line cite — line numbers in this PLAN.md are reference anchors, not authoritative. The grep wins.
  </action>
  <verify>
    <automated>grep -c '## Citation Source Grammar' docs/PROMPT-COMPOSITION.md | grep -q '^1$' && grep -c '## Recall-Fragment Shapes' docs/PROMPT-COMPOSITION.md | grep -q '^1$' && grep -c '## Diet-Mode' docs/PROMPT-COMPOSITION.md | grep -q '^1$' && grep -c '## Composite Event Contract' docs/PROMPT-COMPOSITION.md | grep -q '^1$' && grep -c 'recall_fragment_for_event' docs/PROMPT-COMPOSITION.md | awk '$1>=1{exit 0}{exit 1}' && grep -c 'TRANSITION_SHAPE_RECALL_FRAGMENT_TPL' docs/PROMPT-COMPOSITION.md | awk '$1>=1{exit 0}{exit 1}' && grep -c 'VOCABULARY_RECALL_FRAGMENT_TPL' docs/PROMPT-COMPOSITION.md | awk '$1>=1{exit 0}{exit 1}' && grep -c 'ACK_ELIGIBLE_EVENTS' docs/PROMPT-COMPOSITION.md | awk '$1>=2{exit 0}{exit 1}' && wc -l docs/PROMPT-COMPOSITION.md | awk '$1>=180{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Section headings `## Citation Source Grammar`, `## Recall-Fragment Shapes`, `## Diet-Mode` each appear exactly once and now carry content (not the stub placeholder line from Plan 101-01).
    - `## Composite Event Contract` heading from Plan 101-01 still appears exactly once (sanity that §3 was not nuked).
    - File contains literal tokens `recall_fragment_for_event`, `TRANSITION_SHAPE_RECALL_FRAGMENT_TPL`, `VOCABULARY_RECALL_FRAGMENT_TPL`, `ACK_ELIGIBLE_EVENTS` (the latter ≥2 occurrences — once in §3, once in §6).
    - §4 table has 11 rows for the 11 EVIDENCE_SOURCES (each of "ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar", "cue" appears at least once in the doc).
    - File length ≥ 180 lines (sanity floor — §§4-6 add real prose on top of the Plan 101-01 skeleton).
    - Every coach.py cite uses a line number from {54, 109-126, 141-155, 158, 794, 824, 825, 847} or close-neighbor (line numbers shift with edits; Plan 101-03's grep-verify resolves drift).
    - §8 stub (Appendix: How to Grep-Verify This Doc) is UNTOUCHED — still a placeholder for Plan 101-03.
  </acceptance_criteria>
  <done>
    Sections 4, 5, 6 are content-complete. The doc is now reference-usable end-to-end EXCEPT the §8 grep-verify appendix and the CLAUDE.md pointer — both delivered by Plan 101-03. REQ-CONTRACT-02 (citation source grammar with body forms + which events cite which), -03 (recall-fragment shapes per family with worked examples), -04 (diet-mode rationale + what gets stripped) all landed.
  </done>
</task>

</tasks>

<verification>
- `grep -c '## Citation Source Grammar' docs/PROMPT-COMPOSITION.md` returns 1.
- `grep -c '## Recall-Fragment Shapes' docs/PROMPT-COMPOSITION.md` returns 1.
- `grep -c '## Diet-Mode' docs/PROMPT-COMPOSITION.md` returns 1.
- All 11 EVIDENCE_SOURCES strings appear in the doc (ev, aud, midi, track, screen, mix, tend, key, recall, exemplar, cue).
- Plan 101-01 sections (§§1-3 + §7) headings still resolve.
</verification>

<success_criteria>
REQs CONTRACT-02 (full grammar with 11 sources + body forms + cite-eligibility table), -03 (per-event-family recall-fragment shape documentation with worked examples + cite to coach.py:158), -04 (diet-mode dispatch rationale + what-gets-stripped enumeration + TTFT-budget) are landed in docs/PROMPT-COMPOSITION.md sections 4, 5, 6. The doc reads as a complete contract pending the §8 grep-verify appendix and CLAUDE.md pointer that Plan 101-03 delivers.
</success_criteria>

<output>
Create `.planning/phases/101-harden-contract-prompt-composition-doc-factor-3/101-02-SUMMARY.md` when done.
</output>
