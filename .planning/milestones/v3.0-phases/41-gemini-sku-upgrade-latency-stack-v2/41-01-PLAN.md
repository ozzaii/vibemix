---
phase: 41-gemini-sku-upgrade-latency-stack-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vibemix/llm/__init__.py
  - src/vibemix/llm/model_router.py
  - src/vibemix/llm/_router_config.py
  - src/vibemix/agent/config.py
  - src/vibemix/agent/tts_chain.py
  - src/vibemix/debrief/tldr.py
  - src/vibemix/debrief/drills.py
  - src/vibemix/library/embed.py
  - src/vibemix/library/grounding.py
  - tests/llm/__init__.py
  - tests/llm/test_model_router.py
  - tests/repo/test_model_literal_gate.py
  - scripts/release/check_no_hardcoded_model.sh
  - .github/workflows/model-literal-check.yml
autonomous: true
requirements: [LAT-01, LAT-07]
must_haves:
  truths:
    - "Every Gemini model literal in src/vibemix/ is resolved through ModelRouter.resolve(path)"
    - "CI grep gate fails any PR that introduces a hardcoded gemini-3-flash / gemini-3-pro / gemini-embedding-* / gemini-3.1-* literal in src/vibemix/ outside the router config file"
    - "Live coach path resolves to (gemini-3-flash-preview, ServiceTier.STANDARD); debrief path resolves to (gemini-3-pro-preview, ServiceTier.FLEX); library_auto_tag and embedding resolve to FLEX; OpenRouter TTS routing preserved as a separate router key"
    - "Existing imports of agent.config.LLM_MODEL / TTS_MODEL / OPENROUTER_TTS_MODEL keep working (backward-compat re-export via resolve())"
    - "Unknown router path raises KeyError with the list of valid paths"
  artifacts:
    - path: "src/vibemix/llm/model_router.py"
      provides: "resolve(path) -> (model_id, ServiceTier) routing function + _ROUTES dict"
      exports: ["resolve", "ROUTER_PATHS", "RouterPathError"]
    - path: "src/vibemix/llm/_router_config.py"
      provides: "Python literal _ROUTES dict (single source of truth for SKU + tier per path)"
    - path: "scripts/release/check_no_hardcoded_model.sh"
      provides: "Bash grep gate scoped to src/vibemix/, allowlisting vibemix/llm/_router_config.py"
    - path: ".github/workflows/model-literal-check.yml"
      provides: "CI workflow wrapping the bash gate on every PR"
    - path: "tests/llm/test_model_router.py"
      provides: "Unit tests for valid path resolution + tier dispatch + bad-path rejection"
    - path: "tests/repo/test_model_literal_gate.py"
      provides: "Cross-platform pytest mirror of the bash grep gate"
  key_links:
    - from: "src/vibemix/agent/config.py"
      to: "src/vibemix/llm/model_router.resolve"
      via: "module-load-time resolve() call assigning LLM_MODEL / TTS_MODEL / OPENROUTER_TTS_MODEL constants"
      pattern: "from vibemix.llm.model_router import resolve.*LLM_MODEL = resolve"
    - from: "src/vibemix/debrief/tldr.py"
      to: "src/vibemix/llm/model_router.resolve"
      via: "DEBRIEF_TLDR_MODEL = resolve('debrief')[0]"
      pattern: "resolve\\(\"debrief\""
    - from: "src/vibemix/library/embed.py"
      to: "src/vibemix/llm/model_router.resolve"
      via: "GEMINI_EMBEDDING_MODEL = resolve('embedding')[0]"
      pattern: "resolve\\(\"embedding\""
    - from: ".github/workflows/model-literal-check.yml"
      to: "scripts/release/check_no_hardcoded_model.sh"
      via: "GitHub Actions run step"
      pattern: "check_no_hardcoded_model.sh"
---

<objective>
Land the ModelRouter seam — single source of truth for `path -> (model_id, ServiceTier)` mapping across every Gemini SDK call site in `src/vibemix/`. Wires existing `agent.config`, `debrief/{tldr,drills}.py`, `library/{embed,grounding}.py`, and `agent/tts_chain.py` through `vibemix.llm.model_router.resolve(path)`. Ships the CI grep gate that fails any PR re-introducing a hardcoded Gemini model literal in `src/vibemix/` outside the router config file. This is the foundation plan — Plans 41-02..06 all consume `resolve()`.

Purpose: Future SKU bumps become config-only edits (one file: `_router_config.py`). Per-path tier dispatch (live coach = Standard, debrief / library / embedding = Flex per LAT-07) ships in the same seam. Closes LAT-01 + LAT-07.

Output: New `vibemix.llm` package with `model_router.py` + `_router_config.py`; CI gate bash script + GitHub Actions workflow; tests for routing + the gate; existing `agent.config` / `debrief/*` / `library/{embed,grounding}.py` literals migrated to `resolve()`-derived constants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-CONTEXT.md
@.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-RESEARCH.md

@src/vibemix/agent/config.py
@src/vibemix/agent/tts_chain.py
@src/vibemix/debrief/tldr.py
@src/vibemix/debrief/drills.py
@src/vibemix/library/embed.py
@src/vibemix/library/grounding.py
@.github/workflows/no-api-key-surface.yml

<interfaces>
<!-- Key SDK + repo facts the executor needs. Do not explore — use these. -->

From .venv/lib/python3.12/site-packages/google/genai/types.py (VERIFIED):
- ServiceTier enum (line 835): values FLEX, STANDARD, PRIORITY
- Accepts both `ServiceTier.FLEX` enum and the string `"flex"` on GenerateContentConfig.service_tier

From src/vibemix/agent/config.py (CURRENT — to migrate):
```python
LLM_MODEL: str = "gemini-3-flash-preview"           # line 16
TTS_MODEL: str = "gemini-3.1-flash-tts-preview"     # line 17
TTS_FALLBACK_MODEL: str = "gemini-2.5-flash-preview-tts"  # line 18
OPENROUTER_TTS_MODEL: str = "google/gemini-3.1-flash-tts-preview"  # line 23
```

From src/vibemix/debrief/tldr.py (CURRENT — to migrate):
```python
DEBRIEF_TLDR_MODEL = "gemini-3-pro-preview"          # line 45
DEBRIEF_TTS_MODEL = "gemini-3-flash-tts-preview"     # line 46
```

From src/vibemix/debrief/drills.py (CURRENT — to migrate):
```python
DEBRIEF_DRILLS_MODEL = "gemini-3-pro-preview"        # line 39
```

From src/vibemix/library/embed.py (CURRENT — to migrate):
```python
GEMINI_EMBEDDING_MODEL = "gemini-embedding-2"        # line 63
EXCERPT_STRATEGY_VERSION = "v1-3excerpt-mean"        # line 66 (DO NOT TOUCH — cache key invariant)
```

From src/vibemix/library/grounding.py:122 (CURRENT — inline literal to migrate):
`model="gemini-embedding-2"` inline call to `embed_content`

From src/vibemix/agent/tts_chain.py:29 (CURRENT — OpenRouter monkey-patch target):
`AUDIO_STREAM_MODELS.add("google/gemini-3.1-flash-tts-preview")` — this MUST keep working post-migration. The string is an OpenRouter-namespaced ID (NOT a Gemini-API model). Per Assumption A8, ModelRouter exposes it as a separate router key but the monkey-patch consumes it by reference.

From .github/workflows/no-api-key-surface.yml (template pattern to mirror):
- Triggers: pull_request + push to main
- Single bash step that runs a `scripts/release/check_*.sh` script and `exit 1` if pattern found
- Output uses `::error::` GitHub annotation format
</interfaces>

<router_paths>
<!-- The complete _ROUTES dict to ship. Tiers per CONTEXT.md locked decisions. -->

| Path | Model | Tier | Consumer |
|------|-------|------|----------|
| live_coach | gemini-3-flash-preview | STANDARD | agent/config.py LLM_MODEL → dj_cohost.llm_node |
| live_coach_tts | gemini-3.1-flash-tts-preview | STANDARD | agent/config.py TTS_MODEL → tts_chain Gemini-native path |
| live_coach_tts_fallback | gemini-2.5-flash-preview-tts | STANDARD | agent/config.py TTS_FALLBACK_MODEL → tts_chain fallback |
| live_coach_tts_openrouter | google/gemini-3.1-flash-tts-preview | (N/A — non-Gemini-API) | agent/config.py OPENROUTER_TTS_MODEL → tts_chain monkey-patch |
| debrief | gemini-3-pro-preview | FLEX | debrief/tldr.py + debrief/drills.py |
| debrief_tts | gemini-3-flash-tts-preview | FLEX | debrief/tldr.py |
| library_auto_tag | gemini-3-flash-preview | FLEX | (reserved for Phase 28 auto-tagger when wired) |
| embedding | gemini-embedding-2 | FLEX | library/embed.py + library/grounding.py |

The `live_coach_tts_openrouter` path's tier slot is set to a sentinel `None` (router signals "not a Gemini-API call; consume model_id only").
</router_paths>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModelRouter package + config + tests</name>
  <files>src/vibemix/llm/__init__.py, src/vibemix/llm/_router_config.py, src/vibemix/llm/model_router.py, tests/llm/__init__.py, tests/llm/test_model_router.py</files>
  <behavior>
    - test_resolve_live_coach_returns_standard_3_flash: resolve("live_coach") == ("gemini-3-flash-preview", ServiceTier.STANDARD)
    - test_resolve_debrief_returns_flex_3_pro: resolve("debrief") == ("gemini-3-pro-preview", ServiceTier.FLEX)
    - test_resolve_library_auto_tag_returns_flex_3_flash: resolve("library_auto_tag") == ("gemini-3-flash-preview", ServiceTier.FLEX)
    - test_resolve_embedding_returns_flex_emb2: resolve("embedding") == ("gemini-embedding-2", ServiceTier.FLEX)
    - test_resolve_live_coach_tts_returns_standard_3_1: resolve("live_coach_tts") == ("gemini-3.1-flash-tts-preview", ServiceTier.STANDARD)
    - test_resolve_openrouter_tts_returns_namespaced_id_and_none_tier: resolve("live_coach_tts_openrouter")[0] == "google/gemini-3.1-flash-tts-preview" and resolve(...)[1] is None
    - test_resolve_unknown_path_raises_router_path_error: resolve("not_a_path") raises RouterPathError mentioning every valid key
    - test_router_paths_is_frozen_tuple: ROUTER_PATHS is a tuple (not list) and contains exactly the 8 keys from the router_paths table
    - test_no_non_gemini_models: every model_id is either gemini-* prefixed or the single google/gemini-* OpenRouter id (defensive against accidental Anthropic/OpenAI/Ollama entries)
  </behavior>
  <action>
    Create `src/vibemix/llm/` package (empty `__init__.py` re-exporting `resolve`, `ROUTER_PATHS`, `RouterPathError`, `ServiceTier`).

    `_router_config.py` holds the SOLE literal `_ROUTES` dict per the router_paths table in context (8 entries). Use `google.genai.types.ServiceTier` enum values directly; OpenRouter path uses sentinel `None` for tier. Add a top-of-file comment: "This is the ONLY file in src/vibemix/ allowed to carry Gemini model literals. The CI grep gate allowlists this path. All other src/vibemix/ Gemini calls MUST go through model_router.resolve(path)."

    `model_router.py` exports:
    - `class RouterPathError(KeyError)`
    - `ROUTER_PATHS: tuple[str, ...]` — frozen tuple of valid keys
    - `def resolve(path: str) -> tuple[str, ServiceTier | None]` — KeyError-on-unknown with all-valid-paths-listed error message
    - Module docstring states: "If you find yourself adding a model literal anywhere in src/vibemix/, you are off-pattern — add the path to _router_config._ROUTES instead and call resolve(<new_path>)."

    Tests in `tests/llm/test_model_router.py` cover all behaviors above. Use pytest parametrize for the 4 GA path×tier assertions. Import `ServiceTier` from `google.genai.types` directly.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/llm/test_model_router.py -x -v</automated>
  </verify>
  <done>9 unit tests green; `vibemix.llm.model_router.resolve` importable from any module; `_router_config.py` is the only file in `src/vibemix/llm/` carrying model literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate call sites + backward-compat re-export</name>
  <files>src/vibemix/agent/config.py, src/vibemix/agent/tts_chain.py, src/vibemix/debrief/tldr.py, src/vibemix/debrief/drills.py, src/vibemix/library/embed.py, src/vibemix/library/grounding.py</files>
  <behavior>
    - test_agent_config_llm_model_matches_router (extend existing tests/agent/test_config.py): assert agent.config.LLM_MODEL == resolve("live_coach")[0]
    - test_agent_config_tts_model_matches_router: TTS_MODEL == resolve("live_coach_tts")[0]
    - test_agent_config_openrouter_unchanged: OPENROUTER_TTS_MODEL == resolve("live_coach_tts_openrouter")[0] AND the existing tts_chain monkey-patch still adds the string to AUDIO_STREAM_MODELS
    - test_debrief_tldr_model_matches_router: debrief.tldr.DEBRIEF_TLDR_MODEL == resolve("debrief")[0]
    - test_debrief_drills_model_matches_router: debrief.drills.DEBRIEF_DRILLS_MODEL == resolve("debrief")[0]
    - test_library_embed_model_matches_router: library.embed.GEMINI_EMBEDDING_MODEL == resolve("embedding")[0]
    - test_library_embed_cache_key_unchanged: SHA256 of (canary_bytes || GEMINI_EMBEDDING_MODEL || EXCERPT_STRATEGY_VERSION) byte-identical to a frozen golden value pinned in the test (proves cache invalidation NOT triggered by this refactor — A1 protection)
    - test_library_grounding_uses_router (regression — if grounding.py:122 currently inlines `model="gemini-embedding-2"`, the import-graph check asserts no remaining string literal in that file)
  </behavior>
  <action>
    For each migrating file, replace the literal with a `from vibemix.llm.model_router import resolve` import + a module-load-time `CONSTANT = resolve("path")[0]` assignment. Preserve the existing constant names (`LLM_MODEL`, `TTS_MODEL`, `DEBRIEF_TLDR_MODEL`, `GEMINI_EMBEDDING_MODEL`, etc.) per A7 backward-compat — `agent.config.LLM_MODEL` is consumed by `__main__.py`, `agent/cache.py`, multiple tests.

    `agent/config.py` (lines 16-23):
    Replace each of the 4 literals with `LLM_MODEL: str = resolve("live_coach")[0]` etc. The `LIVE_COACH_TIER`, `DEBRIEF_TIER` style tier constants do NOT need to be exposed in `agent/config.py` — consumers that need tier dispatch import `resolve` directly. Add: `LIVE_COACH_SERVICE_TIER = resolve("live_coach")[1]` for callers that want both values without a second resolve call.

    `agent/tts_chain.py` line 29 (monkey-patch): change `AUDIO_STREAM_MODELS.add("google/gemini-3.1-flash-tts-preview")` to `AUDIO_STREAM_MODELS.add(OPENROUTER_TTS_MODEL)` (import `OPENROUTER_TTS_MODEL` from `vibemix.agent.config`). The literal that gates the monkey-patch must NOT remain inline — it must reference the router-derived constant.

    `debrief/tldr.py` lines 45-46: replace `"gemini-3-pro-preview"` with `resolve("debrief")[0]` and `"gemini-3-flash-tts-preview"` with `resolve("debrief_tts")[0]`. Preserve constant names.

    `debrief/drills.py` line 39: `DEBRIEF_DRILLS_MODEL = resolve("debrief")[0]` (shares the debrief path with tldr; intentional — both run on Flex 3-Pro).

    `library/embed.py` line 63: `GEMINI_EMBEDDING_MODEL = resolve("embedding")[0]`. CRITICAL: DO NOT touch `EXCERPT_STRATEGY_VERSION = "v1-3excerpt-mean"` (line 66). Cache key invariant — Plan 41-05 owns the version bump if probe surfaces a GA model-ID rename. Bumping it here would invalidate every existing user's embedding cache.

    `library/grounding.py` line 122: replace the inline `model="gemini-embedding-2"` with `model=GEMINI_EMBEDDING_MODEL` (import from `vibemix.library.embed`) or `model=resolve("embedding")[0]` directly. Either is router-derived.

    Add 8 new tests in `tests/agent/test_config.py` (extend), `tests/debrief/test_tldr_model_dispatch.py` (NEW small file), `tests/debrief/test_drills_model_dispatch.py` (NEW small file), `tests/library/test_embed_router_dispatch.py` (NEW small file), `tests/library/test_grounding_router_dispatch.py` (NEW small file). For the cache-key invariant test, freeze a known SHA256 against a known byte string.

    Run full suite once: `uv run pytest tests/ -x --ignore=tests/e2e -q` to catch any consumer that broke from the migration. 99-test regression baseline from Phase 40 stays clean.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run pytest tests/agent/test_config.py tests/debrief/test_tldr_model_dispatch.py tests/debrief/test_drills_model_dispatch.py tests/library/test_embed_router_dispatch.py tests/library/test_grounding_router_dispatch.py tests/agent/test_tts_chain.py -x -v</automated>
  </verify>
  <done>Every Gemini model literal in `src/vibemix/` (except the allowlisted `_router_config.py`) replaced with a `resolve(path)` call. All 8 new tests green. Existing `agent/test_config.py` + `agent/test_tts_chain.py` regression-clean. `GEMINI_EMBEDDING_MODEL` cache-key SHA256 byte-identical to pre-migration golden (cache invariant preserved).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CI grep gate + pytest mirror + GitHub Actions workflow</name>
  <files>scripts/release/check_no_hardcoded_model.sh, .github/workflows/model-literal-check.yml, tests/repo/test_model_literal_gate.py</files>
  <behavior>
    - test_gate_passes_clean_tree: bash gate exit code == 0 against current src/vibemix/ (post-Task 2 migration; only _router_config.py has literals)
    - test_gate_fails_on_synthetic_violation: write a tempfile under src/vibemix/agent/_canary_violation.py containing `MODEL = "gemini-3-flash-preview"`, run the gate, assert exit code != 0 AND the failure message names the offending file (cleanup the tempfile in teardown)
    - test_gate_allowlists_router_config: confirm _router_config.py literals do NOT trigger the gate (regex excludes that path)
    - test_gate_allowlists_tests_dir: confirm tests/ literals do NOT trigger
    - test_gate_allowlists_scripts_dir: confirm scripts/ literals do NOT trigger (eval/judge.py keeps its own model dispatch per Open Question 4)
    - test_gate_rejects_legacy_embedding_001: synthetic file containing "gemini-embedding-001" under src/vibemix/ triggers failure (defensive — never used by vibemix; CI gate catches accidental introduction)
  </behavior>
  <action>
    Create `scripts/release/` directory (does not exist today). Mirror the structure of `.github/workflows/no-api-key-surface.yml`.

    `scripts/release/check_no_hardcoded_model.sh`:
    - Shebang `#!/usr/bin/env bash`, `set -euo pipefail`
    - Pattern: regex matches any of `gemini-3-flash`, `gemini-3-pro`, `gemini-embedding-`, `gemini-3.1-flash`, `gemini-2.5-flash`, `gemini-3.1-flash-live` (all current + plausible-near-future literals)
    - Scope: `src/vibemix/**/*.py`
    - Allowlist: `src/vibemix/llm/_router_config.py` only (NOT model_router.py — model_router.py should never carry literals itself)
    - Excludes: docstrings? NO — docstrings carrying example model strings are also a regression risk. If a docstring legitimately needs to name a model, it should reference the router path instead. Comment containing the literal counts as a violation.
    - Output uses `::error file=...,line=...::` GitHub annotation format on match.
    - Exit 0 on clean, exit 1 on any match.

    `.github/workflows/model-literal-check.yml`:
    - Triggers: pull_request (paths `src/vibemix/**`), push to main
    - Steps: checkout, run `bash scripts/release/check_no_hardcoded_model.sh`
    - Use `actions/checkout@v4` matching existing workflow pattern

    `tests/repo/test_model_literal_gate.py`:
    - Cross-platform pytest mirror — uses Python `re` + `pathlib.Path` to walk `src/vibemix/`, mirrors the same pattern + allowlist
    - Tests above all use subprocess to invoke the bash script for canonical behavior, PLUS a Python-only mirror function for the same logic (so the test suite runs on Windows where bash isn't guaranteed in CI)
    - The Python mirror is the LOCAL dev-loop fast feedback; the bash script is the GitHub Actions truth.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; bash scripts/release/check_no_hardcoded_model.sh &amp;&amp; uv run pytest tests/repo/test_model_literal_gate.py -x -v</automated>
  </verify>
  <done>Bash gate exits 0 against current tree post-Task 2. Synthetic-violation pytest case exits 1 and names the offending file. GitHub Actions workflow file passes `yamllint` (or basic YAML parse). 6 pytest cases in `test_model_literal_gate.py` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → grep gate | Gate logic runs in privileged CI context; pattern correctness must be reviewed |
| Test code → router | Tests can reference literals for contract pinning (allowlisted); router-bypass NOT permitted |
| External contributor PR → src/vibemix/ | PRs from untrusted forks could attempt to introduce non-Gemini provider strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | _router_config.py via PR | mitigate | CODEOWNERS requires review for `src/vibemix/llm/_router_config.py` (deferred — note for Phase 44 codeowners pass) |
| T-41-01-02 | Tampering | CI bypass via env var | mitigate | grep gate is unconditional bash script — no env-var toggle; cannot be disabled without modifying the workflow file (which itself appears in PR diff) |
| T-41-01-03 | Information Disclosure | model_router error messages leaking internal paths | accept | `ROUTER_PATHS` is public information (`live_coach`, `debrief`, etc.); not sensitive |
| T-41-01-04 | Elevation of Privilege | Unknown path → arbitrary model substitution | mitigate | `resolve()` raises `RouterPathError` on unknown path; no fallback / silent default. Callers must register a path explicitly. |
| T-41-01-05 | Tampering | Accidental non-Gemini model insertion | mitigate | Test `test_no_non_gemini_models` asserts every model_id in `_ROUTES` is prefixed `gemini-*` or the single `google/gemini-*` OpenRouter id |
</threat_model>

<verification>
- `uv run pytest tests/llm/ tests/repo/test_model_literal_gate.py tests/agent/test_config.py tests/agent/test_tts_chain.py tests/debrief/ tests/library/test_embed_router_dispatch.py tests/library/test_grounding_router_dispatch.py -x -v` → all green
- `bash scripts/release/check_no_hardcoded_model.sh` → exit 0 against migrated tree
- `grep -rn "gemini-3-flash\|gemini-3-pro\|gemini-embedding-\|gemini-3.1-flash\|gemini-2.5-flash" src/vibemix/ | grep -v "_router_config.py" | grep -v "^#" | wc -l` → output `0`
- Full regression: `uv run pytest tests/ -x --ignore=tests/e2e -q` → 99-test Phase 40 baseline still clean
</verification>

<success_criteria>
1. `src/vibemix/llm/model_router.py` + `_router_config.py` ship; `resolve("live_coach"..)` returns the locked tier/model per `router_paths` table.
2. 4 `agent/config.py` constants + 2 `debrief/` model constants + 1 `library/embed.py` constant + 1 `library/grounding.py` inline literal + 1 `agent/tts_chain.py` monkey-patch literal — all 9 sites — derive from `resolve()`.
3. `bash scripts/release/check_no_hardcoded_model.sh` exits 0 against the migrated tree.
4. `.github/workflows/model-literal-check.yml` exists and runs on every PR touching `src/vibemix/**`.
5. Cross-platform pytest mirror (`tests/repo/test_model_literal_gate.py`) green, including synthetic-violation case that confirms the gate fails on injected literals.
6. Backward-compat assertion: `LibraryEmbedder` cache-key SHA256 byte-identical to pre-migration golden (no cache invalidation triggered).
7. Phase 40 baseline (99 DJCoHostAgent tests + all other regression tests) stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/41-gemini-sku-upgrade-latency-stack-v2/41-01-SUMMARY.md` capturing:
- New router-path table actually shipped (in case planner's table got tuned during implementation)
- Pre-migration vs post-migration literal-count grep output proof
- Phase 40 regression test count + status
- Any deviations from Assumption A1/A7/A8 surfaced during integration
</output>
