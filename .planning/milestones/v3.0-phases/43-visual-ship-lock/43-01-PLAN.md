---
phase: 43-visual-ship-lock
plan: 01
title: "UI audit driver + first audit run (session surface)"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch/run_ui_audit.py
  - .planning/phases/43-visual-ship-lock/UI-REVIEW-session.md
  - .planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md
  - tests/launch/test_ui_audit_driver.py
autonomous: true
requirements: [VIS-01]
req_ids: [VIS-01]
must_haves:
  truths:
    - "`scripts/launch/run_ui_audit.py` is invokable with `--surface <name>` and writes a `UI-REVIEW-<surface>.md` file to `.planning/phases/43-visual-ship-lock/`"
    - "The driver script enumerates the 4 Tier-1 surfaces (session, mascot-overlay, wizard, calibration) and refuses any value outside that set"
    - "`UI-REVIEW-INDEX.md` documents the audit loop (gsd-ui-checker BLOCK/FLAG/PASS + gsd-ui-auditor 6-pillar score) and lists the 4 Tier-1 surfaces with their owning closure plans"
    - "`UI-REVIEW-session.md` is seeded with the first audit run findings for the session window (real findings, not placeholder) and lists each finding under HIGH / MEDIUM / LOW severity buckets"
    - "Closure plans 43-02 / 43-03 reference the same `.planning/phases/43-visual-ship-lock/UI-REVIEW-<surface>.md` filenames the driver produces"
    - "No real Tauri-window screenshot bytes are committed in this plan (driver writes the markdown audit, not screenshots)"
  artifacts:
    - path: "scripts/launch/run_ui_audit.py"
      provides: "Audit driver that invokes the gsd-ui-checker + gsd-ui-auditor agents (or documents the invocation contract when run with `--dry-run`) and writes findings to a markdown audit file per surface"
      exports: ["main", "TIER1_SURFACES", "write_audit_skeleton"]
    - path: ".planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md"
      provides: "Index of the 4 Tier-1 surface audits + closure-plan ownership map + audit-loop methodology"
      contains: "Tier-1 Surfaces"
    - path: ".planning/phases/43-visual-ship-lock/UI-REVIEW-session.md"
      provides: "First audit pass on the session window — real findings tagged HIGH / MEDIUM / LOW"
      contains: "## Surface: session"
  key_links:
    - from: "scripts/launch/run_ui_audit.py"
      to: ".planning/phases/43-visual-ship-lock/UI-REVIEW-<surface>.md"
      via: "write_audit_skeleton(surface) -> Path"
      pattern: "UI-REVIEW-.*\\.md"
    - from: ".planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md"
      to: "tauri/ui/src/{session,mascot,overlay,wizard,settings}/"
      via: "Tier-1 surface mapping per CONTEXT decisions"
      pattern: "tauri/ui/src/"
---

<objective>
Ship the Tier-1 surface audit driver and seed the first audit pass on the session window (VIS-01 scaffold). The driver enumerates the four Tier-1 surfaces from CONTEXT (session, mascot overlay, wizard, calibration), wraps the `gsd-ui-checker` + `gsd-ui-auditor` agent invocation contract, and emits one `UI-REVIEW-<surface>.md` per surface for the critique→execute loop closure plans (43-02 / 43-03) to consume.

Purpose: per CONTEXT §VIS-01 — Plans 43-02 / 43-03 close findings per Tier-1 surface bucket. They cannot run unless the driver script + audit markdown filenames are stable. This plan also produces the first real audit pass on the session window so Plan 43-02 has actionable HIGH/MEDIUM/LOW findings to close (it does not start from an empty critique).

Output:
- `scripts/launch/run_ui_audit.py` — driver script wrapping the paired audit invocation contract.
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md` — methodology + Tier-1 surface inventory.
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-session.md` — first audit pass on the session window with real findings.
- `tests/launch/test_ui_audit_driver.py` — invokability + surface-allowlist + markdown-skeleton sanity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-visual-ship-lock/43-CONTEXT.md

@tauri/ui/src/session/SessionLayout.ts
@tauri/ui/src/session/components/meter.ts
@tauri/ui/src/tokens.css
@mocks/vibemix-app-ui.html
@scripts/launch/README.md

<interfaces>
<!-- Repo facts the executor needs. Do not re-explore. -->

Tier-1 surface convention (CONTEXT §VIS-01):
- session         → `tauri/ui/src/session/`           (SessionLayout.ts is the entry)
- mascot-overlay  → `tauri/ui/src/overlay/` + `tauri/ui/src/mascot/`
- wizard          → `tauri/ui/src/wizard/`             (6-step onboarding: step0-intro, step1-permissions, step2-output-device, step3-controller, step-profile-consent, step-telemetry-consent)
- calibration     → `tauri/ui/src/wizard/step1-permissions.ts` + `step2-output-device.ts` (first-run calibration is in the wizard tail)

Closure-plan ownership (per CONTEXT default split):
- 43-02 closes:  session + mascot-overlay (Wave A polish on the live surface)
- 43-03 closes:  wizard + calibration (first-run + onboarding loop)

Audit loop methodology (CONTEXT §VIS-01):
- `gsd-ui-checker`  → BLOCK / FLAG / PASS verdicts on per-element interaction states
- `gsd-ui-auditor`  → scored 6-pillar audit (hierarchy / contrast / motion / typography / density / restraint)
- Loop until zero HIGH findings per surface; HIGH = blocking, MEDIUM = strongly-recommended-fix, LOW = nice-to-have
- Findings written to `.planning/phases/43-visual-ship-lock/UI-REVIEW-<surface>.md`

Filename contract (CONTEXT §VIS-01):
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-session.md`
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-mascot-overlay.md`
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-wizard.md`
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-calibration.md`

Scripts/launch convention (already established Phase 27+):
- All launch-prep helpers live in `scripts/launch/`
- Tests live in `tests/launch/`
- `scripts/launch/README.md` documents each script in 1-2 sentences

Driver script must NOT shell out to actual agent processes during `pytest` — it must support `--dry-run` (default) that writes the markdown skeleton without invoking real audit agents. Real audits are run interactively by the executor (closure plans 43-02 / 43-03 invoke the agents themselves).

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UI audit driver script + Tier-1 allowlist + tests</name>
  <files>scripts/launch/run_ui_audit.py, tests/launch/test_ui_audit_driver.py</files>
  <behavior>
    - Test 1: `TIER1_SURFACES` constant equals the exact set {"session", "mascot-overlay", "wizard", "calibration"} (per CONTEXT §VIS-01).
    - Test 2: `write_audit_skeleton("session", phase_dir=tmp_path)` writes `tmp_path/UI-REVIEW-session.md` and the file contains "## Surface: session" + "### HIGH findings" + "### MEDIUM findings" + "### LOW findings" + "### Audit Loop Log".
    - Test 3: `main(["--surface", "unknown", "--dry-run"])` exits with code 2 and stderr contains "unknown surface" and lists the 4 valid surfaces.
    - Test 4: `main(["--surface", "session", "--dry-run", "--phase-dir", str(tmp_path)])` exits 0 and creates the markdown skeleton (no agent invocation, no network).
    - Test 5: `main([])` (no surface arg) lists all 4 surfaces + their owning closure plans to stdout and exits 0.
  </behavior>
  <action>
Per D-VIS-01 audit-loop methodology — create the audit driver and pin its interface with tests.

(a) `scripts/launch/run_ui_audit.py`:
- Module docstring: cite Phase 43 §VIS-01, the audit-loop methodology (gsd-ui-checker BLOCK/FLAG/PASS + gsd-ui-auditor 6-pillar score), and the Tier-1 surface inventory.
- `TIER1_SURFACES: dict[str, dict]` — maps surface key → `{"dir": "tauri/ui/src/<dir>/", "owner_plan": "43-02" | "43-03", "entry": "SessionLayout.ts | overlay-runtime.ts | onboarding-flow.ts | step1-permissions.ts"}`. Exactly 4 entries: session, mascot-overlay, wizard, calibration.
- `write_audit_skeleton(surface: str, phase_dir: Path) -> Path` — writes a markdown skeleton with the canonical sections: front-matter (surface, audited_at, owner_plan, status), `## Surface: <name>`, `## Methodology` (block quoting the audit loop from CONTEXT), `### HIGH findings` (empty by default), `### MEDIUM findings`, `### LOW findings`, `### Audit Loop Log` (table with columns: iteration | agent | verdict | files_changed | notes). Returns the written path.
- `main()` argparse CLI: `--surface {session|mascot-overlay|wizard|calibration}` (required for write mode), `--phase-dir .planning/phases/43-visual-ship-lock` (default), `--dry-run` (default true; writes skeleton but does NOT invoke any agent process), `--list` flag prints all 4 surfaces + owning closure plan + entry file. Exit codes: 0 = clean, 2 = unknown surface.
- Use stdlib only (argparse, pathlib, dataclasses, sys, datetime). Do NOT subprocess-out to `gsd-ui-checker` or `gsd-ui-auditor` agents — those are invoked interactively by closure plans 43-02 / 43-03 via the Task tool. This script is the markdown-skeleton + invariant enforcement layer, not a runner.
- Idempotent: if `UI-REVIEW-<surface>.md` already exists, `--dry-run` MUST NOT overwrite it (it appends an audit-loop log row instead). Provide a `--force-rewrite` flag for the initial seed only.

(b) `tests/launch/test_ui_audit_driver.py`:
- Implements the 5 behaviors above using `pytest` + `tmp_path`.
- Import path: `from scripts.launch.run_ui_audit import TIER1_SURFACES, write_audit_skeleton, main`.
- No network, no subprocess. Use `monkeypatch.setattr` if subprocess invocation needs to be asserted absent.

Per D-VIS-01 Claude's-discretion clause — the driver is its own plan (43-01), not folded into closure plans. This is the explicit CONTEXT default.
  </action>
  <verify>
    <automated>uv run pytest tests/launch/test_ui_audit_driver.py -q --no-header</automated>
  </verify>
  <done>5 tests pass; `uv run python scripts/launch/run_ui_audit.py --list` prints the 4 Tier-1 surfaces + their owner_plan IDs; `--surface session --dry-run` writes `.planning/phases/43-visual-ship-lock/UI-REVIEW-session.md` skeleton when invoked with `--phase-dir <real>`.</done>
</task>

<task type="auto">
  <name>Task 2: UI-REVIEW-INDEX.md + seed session-window audit findings</name>
  <files>.planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md, .planning/phases/43-visual-ship-lock/UI-REVIEW-session.md</files>
  <action>
Per D-VIS-01 — the closure plans need actionable HIGH/MEDIUM/LOW findings to close, not an empty skeleton. Seed the session-window audit with the first audit pass.

(a) `.planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md`:
- Front-matter: `phase: 43-visual-ship-lock`, `audited_surfaces: 4`, `audit_loop: critique→execute until zero HIGH`.
- `## Tier-1 Surfaces` table:
  | Surface | Entry file | Owner closure plan | Status |
  |---------|------------|---------------------|--------|
  | session | `tauri/ui/src/session/SessionLayout.ts` | 43-02 | seeded |
  | mascot-overlay | `tauri/ui/src/overlay/overlay-runtime.ts` + `tauri/ui/src/mascot/renderer.ts` | 43-02 | pending |
  | wizard | `tauri/ui/src/wizard/onboarding-flow.ts` | 43-03 | pending |
  | calibration | `tauri/ui/src/wizard/step1-permissions.ts` + `step2-output-device.ts` | 43-03 | pending |
- `## Audit Loop Methodology` — block-quote from CONTEXT §VIS-01: gsd-ui-checker BLOCK/FLAG/PASS + gsd-ui-auditor 6-pillar score (hierarchy / contrast / motion / typography / density / restraint); critique→execute loop; zero HIGH gate.
- `## Severity Buckets` — HIGH = blocking ship, MEDIUM = strongly-recommended-fix, LOW = nice-to-have/deferred.
- `## CDJ Whisper Reference` — link `mocks/vibemix-direction-final.html` as the visual baseline (per memory `project_visual_direction_cdj_whisper`).

(b) `.planning/phases/43-visual-ship-lock/UI-REVIEW-session.md`:
- Front-matter: `surface: session`, `entry: tauri/ui/src/session/SessionLayout.ts`, `owner_plan: 43-02`, `seeded_by: 43-01`, `audited_at: 2026-05-16`, `status: HIGH-findings-open`.
- `## Surface: session` header.
- `## Methodology` block-quote (same as INDEX).
- `### HIGH findings` — seed with at least 3 real findings discovered by reading `tauri/ui/src/session/SessionLayout.ts` + `session/components/meter.ts` + `mocks/vibemix-app-ui.html`. Candidates the executor MUST audit and write up (filename + element + violation + remediation): (1) any interactive element in the session surface that lacks `--glow-faint` on hover state (cross-reference VIS-02 sweep — every `[data-interactive]` / `<button>` / `<a>` / `[role="button"]` must carry the token). (2) meter.ts gradient renderer that VIS-03 will rebuild to LED-strip — flagged HIGH because the current gradient breaks CDJ Whisper hardware-LED aesthetic (cross-link to 43-04 owner). (3) any hex literal in `session/components/*.ts` CSS strings (token-only contract per `frontend-enforcement` skill).
- `### MEDIUM findings` — seed with 2-3 medium-severity items (e.g. timecode font weight, picker hover state inconsistencies, status-bar density mismatch).
- `### LOW findings` — 1-2 items (cosmetic polish suggestions).
- `### Audit Loop Log` — table with first row: `iteration=0 | agent=43-01 (seed) | verdict=seeded | files_changed=- | notes=initial audit pass before Plan 43-02 critique→execute`.
- `### Cross-references` — bullet links to: VIS-02 owner plan (43-02 hover-state sweep), VIS-03 owner plan (43-04 meter rebuild).

Executor instructions:
1. Read `tauri/ui/src/session/SessionLayout.ts` + components/* in full to surface real findings (do NOT fabricate).
2. Cross-reference each finding against `mocks/vibemix-app-ui.html` (the locked UI shape) and `mocks/vibemix-direction-final.html` (the locked visual direction).
3. Each finding entry uses the format: `**[FINDING-ID]** `<file>:<line-or-element>` — <one-line violation>; **remediation:** <one-line fix>; **owner:** 43-02 | 43-04` (cross-link to the right closure plan).

This is the seed audit, not the closure pass — Plan 43-02 runs the actual gsd-ui-checker + gsd-ui-auditor agents and adds iteration=1+ rows to the loop log.
  </action>
  <verify>
    <automated>grep -qE "^## Surface: session" .planning/phases/43-visual-ship-lock/UI-REVIEW-session.md && grep -qE "^### HIGH findings" .planning/phases/43-visual-ship-lock/UI-REVIEW-session.md && grep -cE "^## Tier-1 Surfaces" .planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md | grep -q "^1$" && grep -qE "session.*43-02" .planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md && echo OK</automated>
  </verify>
  <done>INDEX lists 4 surfaces with owner plans; session audit has ≥3 HIGH + ≥2 MEDIUM + ≥1 LOW finding rows with file references; audit-loop log shows iteration=0 seed row; cross-references point to 43-02 + 43-04 by plan ID.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit-driver-script → planning-dir | The driver writes audit findings markdown into `.planning/phases/43-visual-ship-lock/`; no PII, no secrets. |
| seed audit → closure plans | The HIGH findings list is the executable contract Plans 43-02/03 close — tampering would cause closure plans to ship a degraded surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Tampering | `UI-REVIEW-session.md` HIGH findings | mitigate | Audit-loop log is append-only; every iteration row carries `agent` + `verdict` + `files_changed`; reviewer can trace each closure back to a discovered finding |
| T-43-01-02 | Repudiation | Audit findings | mitigate | Each finding has explicit `<file>:<line>` reference + remediation owner plan; closure can be verified by re-running `grep` against the production source |
| T-43-01-03 | Information Disclosure | Tier-1 surface paths in INDEX | accept | Surface paths are repo-public source code; no secrets in the audit markdown |
</threat_model>

<verification>
- Driver tests pass: `uv run pytest tests/launch/test_ui_audit_driver.py -q`
- INDEX + session audit exist with required structure (per Task 2 verify command)
- Driver invokable end-to-end: `uv run python scripts/launch/run_ui_audit.py --list` exits 0 and prints 4 surfaces
- No regression elsewhere: `uv run pytest tests/launch/ -q --no-header` stays green
</verification>

<success_criteria>
- `scripts/launch/run_ui_audit.py` is invokable, exposes `TIER1_SURFACES`, `write_audit_skeleton`, `main`
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-INDEX.md` lists 4 surfaces with owner closure plans
- `.planning/phases/43-visual-ship-lock/UI-REVIEW-session.md` carries ≥3 real HIGH findings + ≥2 MEDIUM + ≥1 LOW + iteration-0 seed log row
- 5 driver tests pass
- Closure plans 43-02 + 43-03 can run against stable markdown filenames
</success_criteria>

<output>
After completion, create `.planning/phases/43-visual-ship-lock/43-01-SUMMARY.md` using the standard summary template.
</output>
