---
phase: 42-hallucination-gate-v3-hybrid
plan: 06
type: execute
wave: 5
depends_on: [02, 03, 04, 05]
files_modified:
  - eval/README.md
  - tests/eval/test_eval_readme_public_facing.py
  - tests/eval/test_eval_readme_redacts_ear_test_content.py
autonomous: true
requirements: [GATE-09]
must_haves:
  truths:
    - "`eval/README.md` documents the v3.0 hybrid gate regime publicly — autonomous proxy (fast lane) + Kaan ear-test (slow lane) — in language an open-source contributor can follow"
    - "`eval/README.md` documents threshold values (linked from THRESHOLD-LOCK.md) and what they mean for end users in plain language"
    - "`eval/README.md` documents the 2-judge cross-check architecture (Gemini 3 Pro + Gemini 3 Flash) at a high level — no prompt or rubric leakage"
    - "`eval/README.md` documents the ear-test protocol SHAPE (30min, 14d, ≥2 genres, 4-flag taxonomy) but redacts session-specific content of `eval/ear-test-logs/*.json`"
    - "`eval/README.md` documents the reproducibility path — `pytest tests/eval/` + `scripts/eval/replay_harness.py` — so OSS contributors can re-run the proxy gate locally"
    - "`eval/README.md` cross-links the anti-slop manifesto (the project north-star — 'real DJ friend in your ear, no AI slop')"
    - "Two tests pin the public-doc contract: (a) all required sections present + threshold values accurately mirrored from THRESHOLD-LOCK.md; (b) no textual content from any `eval/ear-test-logs/*.json` free_form field is reachable in the README"
  artifacts:
    - path: "eval/README.md"
      provides: "Public-facing documentation of the hybrid hallucination gate regime — protocol shape only, no Kaan-private content"
      contains: "## Hybrid Gate"
    - path: "tests/eval/test_eval_readme_public_facing.py"
      provides: "Section-coverage + threshold-mirror tests for eval/README.md"
    - path: "tests/eval/test_eval_readme_redacts_ear_test_content.py"
      provides: "Privacy contract test — README cannot contain text from any ear-test log free_form field"
  key_links:
    - from: "eval/README.md"
      to: "eval/THRESHOLD-LOCK.md"
      via: "anchor link + mirrored numeric values"
      pattern: "THRESHOLD-LOCK.md"
    - from: "eval/README.md"
      to: "eval/EAR-TEST-PROTOCOL.md"
      via: "anchor link to the protocol document (Plan 42-03)"
      pattern: "EAR-TEST-PROTOCOL.md"
    - from: "eval/README.md"
      to: "scripts/eval/replay_harness.py"
      via: "documented invocation for OSS contributor reproducibility"
      pattern: "replay_harness.py"
    - from: "tests/eval/test_eval_readme_redacts_ear_test_content.py"
      to: "eval/ear-test-logs/"
      via: "iterates every *.json log file; asserts free_form content NOT substring of README"
      pattern: "ear-test-logs/.*\\.json"
---

<objective>
Ship the public-facing `eval/README.md` that documents the v3.0 hybrid hallucination gate so OSS contributors understand what the gate is, how the thresholds are calibrated, how to reproduce the proxy gate locally — without leaking Kaan-private ear-test content.

Purpose: Per CONTEXT D-GATE-09 — `eval/README.md` is public-facing. It MUST document the regime, threshold values, judge architecture, ear-test protocol shape, and reproducibility path. It MUST NOT include the textual content of any `eval/ear-test-logs/*.json` free_form field (per `feedback_privacy_scope_narrow` — log content stays in repo as audit trail but is NOT surfaced in public docs).

This plan ships LAST in the phase because it documents what Plans 42-01..05 built. Wave 5, depends_on=[02,03,04,05].

Output:
- `eval/README.md` — single Markdown document, ≤300 lines, public-tone.
- `tests/eval/test_eval_readme_public_facing.py` — section-coverage + threshold-mirror tests.
- `tests/eval/test_eval_readme_redacts_ear_test_content.py` — privacy contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-CONTEXT.md

@eval/THRESHOLD-LOCK.md
@scripts/eval/replay_harness.py
@.github/workflows/eval.yml

<interfaces>
<!-- The companion files this README references. -->

From `eval/THRESHOLD-LOCK.md` (Phase 27-04, autonomous-signed):
- thresholds: f1_min=0.80, substance_min=0.65, cited_cosine_min=0.40, bypass_max=0.15, per_genre_f1_min=0.70
- README MUST mirror these exact numerics in a "Threshold Values" table; the test pins value-equality

From `eval/EAR-TEST-PROTOCOL.md` (Plan 42-03):
- 30min/session, ≥2 genres in 14d window, 4 slop flags
- README cross-links to this doc for protocol shape; does NOT re-document the entire protocol

From `eval/THRESHOLD-RECALIBRATION-LOG.md` (Plan 42-02):
- README mentions the recalibration log as the audit trail for threshold movement

From `.planning/decisions/P85-OVERRIDE-RETIRED.md` (Plan 42-05):
- README mentions the v2.1→v3.0 transition + cross-links the decision log for history-aware readers

From `eval/ear-test-logs/` (Plan 42-03 creates the dir + schema.json):
- The README ITSELF must contain no `free_form` text from any *.json log
- README documents the existence of structured logs but redacts their content
- The privacy test iterates the dir and asserts no substring overlap

From `scripts/eval/replay_harness.py` (Phase 27-01):
- CLI: `--corpus`, `--judges`, `--output`, `--threshold-lock`
- README documents the OSS reproducibility one-liner:
  ```
  uv run pytest tests/eval/
  uv run python -m scripts.eval.replay_harness --corpus tests/eval/fixtures --judges noop --output /tmp/eval-out
  ```

Anti-slop manifesto: project's central thesis lives in CLAUDE.md ("real DJ friend in your ear, no AI slop"). README links to that thesis without copy-pasting Kaan-private content. The link target is the public `docs/` directory or a section of the public README — verify with a Bash check `ls docs/ 2>/dev/null && grep -l "real DJ friend" docs/ 2>/dev/null`; fall back to the project root README anchor if no dedicated manifesto doc exists.

</interfaces>

@.planning/phases/42-hallucination-gate-v3-hybrid/42-02-SUMMARY.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-03-SUMMARY.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-04-SUMMARY.md
@.planning/phases/42-hallucination-gate-v3-hybrid/42-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write eval/README.md public-facing documentation</name>
  <files>eval/README.md</files>
  <action>
Per D-GATE-09 — write the public-facing markdown documenting the hybrid gate.

Structure (sections, in order):

1. `# vibemix Hallucination Gate` — top-level title.

2. **Why this exists** — 1 paragraph, public-tone. The project's central thesis: "vibemix's value is not the AI talking — it's the AI talking IN GROUND TRUTH. Every reaction must be tied to a real event in the actual mix. This gate is how we keep that contract honest." Cross-link to the anti-slop manifesto (use the actual link target verified in Task 1's Bash probe).

3. **Hybrid Gate at a glance** — table:

   | Lane | What it checks | Cadence | Bypass-able? |
   |------|---------------|---------|--------------|
   | Fast (autonomous proxy) | 2-judge cross-check (Gemini 3 Pro + Gemini 3 Flash) against the corpus | Every PR + nightly canary | No — required for merge |
   | Slow (Kaan ear-test) | Real DJ session ear-pass via debrief window toggle | ≥2 sessions ≥2 genres in 14-day window | No — required for release cut |

4. **Threshold Values** — table mirroring `eval/THRESHOLD-LOCK.md` exactly:

   | Metric | Locked value | What it means |
   |--------|-------------|---------------|
   | `f1_min` | ≥ 0.80 | At least 80% of ground-truth events get a substantive, on-time response |
   | `substance_min` | ≥ 0.65 | At least 65% of responses are genuinely useful (not boilerplate) |
   | `cited_cosine_min` | ≥ 0.40 | Cited evidence in each response is semantically related to the event (Gemini Embedding 2 cosine ≥ 0.40) |
   | `bypass_max` | ≤ 0.15 | At most 15% of detected events go without a response (the cascade isn't dropping events on the floor) |
   | `per_genre_f1_min` | ≥ 0.70 | No single (detector × genre) cell falls below F1=0.70 — anti-overfit floor |

   Cross-link: `eval/THRESHOLD-LOCK.md` (signed), `eval/THRESHOLD-RECALIBRATION-LOG.md` (audit trail).

5. **2-Judge Architecture (high level)** — 1 paragraph. Describes: judge_pro (Gemini 3 Pro) is the careful judge, judge_flash (Gemini 3 Flash) is the broad-coverage judge, the gate takes `min(pro_f1, flash_f1)` to prevent single-judge collusion. Cross-link to `eval/rubrics/` (PUBLIC) for the actual prompts.

6. **Ear-Test Protocol (shape only)** — 1 paragraph. Describes: 30min minimum per session, ≥2 genres in 14d window, structured slop-flag capture (4 booleans). Cross-link to `eval/EAR-TEST-PROTOCOL.md` for full details. State explicitly: "The textual content of individual ear-test sessions is REDACTED from this public doc per the project's narrow privacy boundary. Only the protocol shape is documented publicly; structured log files (without prose) live in the repo as audit trail."

7. **Reproducing the proxy gate locally** — code block:
   ```bash
   # 1. Install deps
   uv sync --group dev

   # 2. Run the eval test suite (uses VCR cassettes, $0)
   uv run pytest tests/eval/

   # 3. Run the replay harness with the noop judge against synthetic fixtures
   uv run python -m scripts.eval.replay_harness \
     --corpus tests/eval/fixtures \
     --judges noop \
     --threshold-lock eval/THRESHOLD-LOCK.md \
     --output /tmp/eval-out
   ```
   Note: real Gemini API runs require `GEMINI_API_KEY` and incur cost (~$1-2 per full nightly canary).

8. **History — why v3.0 is hybrid** — 1 paragraph. References the v2.1 P85 override + v3.0 retirement + the `.planning/decisions/P85-OVERRIDE-RETIRED.md` decision log entry (cross-link).

9. **Anti-feature carveouts** — bullet list:
   - We are NOT building a more aggressive autonomous judge to replace the ear-test. The hybrid is the locked design.
   - We are NOT cross-DJ ear-testing in v3.0 — single-DJ (Kaan) signed only.
   - We are NOT gamifying the ear-test (no streaks, no badges).

10. **Cross-references**:
    - `eval/THRESHOLD-LOCK.md`
    - `eval/THRESHOLD-RECALIBRATION-LOG.md`
    - `eval/EAR-TEST-PROTOCOL.md`
    - `eval/corpus/MANIFEST.md`
    - `eval/corpus/LICENSES.md`
    - `scripts/eval/replay_harness.py`
    - `scripts/release/check_gate.sh`
    - `scripts/release/check_ear_test.sh`
    - `.planning/decisions/P85-OVERRIDE-RETIRED.md`

Tone: public-OSS, contributor-friendly, ≤300 lines. No Kaan-private content. No prompt or rubric body inline (link to `eval/rubrics/` instead — that directory is already public-facing per Phase 27).

NEVER paste the content of any `eval/ear-test-logs/*.json` `free_form` field into the README, even by example. Anecdotal examples are FORBIDDEN.
  </action>
  <verify>
    <automated>test -f eval/README.md && wc -l eval/README.md | awk '{exit ($1 > 350)}' && grep -qE "(0\.80|>= 0\.80|≥ 0\.80)" eval/README.md && grep -q "EAR-TEST-PROTOCOL.md" eval/README.md && grep -q "P85-OVERRIDE-RETIRED" eval/README.md && echo OK</automated>
  </verify>
  <done>`eval/README.md` exists, ≤350 lines, mirrors the 5 threshold values from THRESHOLD-LOCK.md, cross-links the protocol + recalibration log + decision entry + replay_harness, contains zero text from any ear-test log file.</done>
</task>

<task type="auto">
  <name>Task 2: Section-coverage + threshold-mirror tests for eval/README.md</name>
  <files>tests/eval/test_eval_readme_public_facing.py</files>
  <action>
Per D-GATE-09 — pin the README contract.

`tests/eval/test_eval_readme_public_facing.py`:
- Module docstring cites GATE-09, Plan 42-06.
- Constants:
  ```
  REPO_ROOT = Path(__file__).resolve().parents[2]
  README = REPO_ROOT / "eval" / "README.md"
  LOCK   = REPO_ROOT / "eval" / "THRESHOLD-LOCK.md"
  ```
- Helpers:
  - `_readme_text() -> str` — caches via `pytest.fixture(scope="module")` or local LRU.
  - `_locked_thresholds() -> dict` — calls `scripts.eval.threshold_lock.parse_threshold_lock_frontmatter(LOCK)`.
- Tests:
  - `test_readme_exists()` — file exists.
  - `test_readme_under_350_lines()` — `len(README.read_text().splitlines()) <= 350` (public docs stay scannable).
  - `test_readme_documents_hybrid_regime()` — body contains all of: "fast lane" / "Fast", "slow lane" / "Slow", "autonomous proxy", "ear-test" (case-insensitive).
  - `test_readme_documents_2_judge_architecture()` — body contains both `Gemini 3 Pro` AND `Gemini 3 Flash` (or `gemini-3-pro` AND `gemini-3-flash`).
  - `test_readme_mirrors_f1_threshold()` — body contains the literal threshold values: 0.80, 0.65, 0.40, 0.15, 0.70.
  - `test_readme_threshold_values_match_lock_file()` — for each key in locked thresholds, the value (formatted to 2 decimal places) appears in the README. Parametrize over the 5 keys.
  - `test_readme_documents_reproducibility_invocation()` — body contains both `replay_harness.py` AND `--threshold-lock` AND `--corpus`.
  - `test_readme_cross_links_protocol_doc()` — body contains `EAR-TEST-PROTOCOL.md`.
  - `test_readme_cross_links_decision_log()` — body contains `P85-OVERRIDE-RETIRED.md`.
  - `test_readme_documents_anti_feature_carveouts()` — body contains the phrase "anti-feature" OR "NOT building" (case-insensitive); plus at least one of: "cross-DJ", "single-DJ", "gamif".
  - `test_readme_documents_redaction_principle()` — body contains both "REDACTED" (or "redacts" / "redact") AND "ear-test" (case-insensitive); the privacy contract must be self-described.
  - `test_readme_no_inline_rubric_or_prompt_bodies()` — smoke check that the README has not slurped in a rubric body verbatim. Define a `LEAK_TOKENS` constant list inside the test function with three sentinel strings (do NOT inline them in this plan — name them in the test by deriving from the rubric file headers via Python: read `eval/rubrics/judge_flash.md` + `eval/rubrics/judge_pro.md`, take the first 40 non-empty chars of each file's content past its YAML frontmatter as the leak token); assert no LEAK_TOKEN substring appears in the README. This makes the test self-updating against the actual rubric content rather than maintaining a hardcoded sentinel list.
  </action>
  <verify>
    <automated>uv run pytest tests/eval/test_eval_readme_public_facing.py -q --no-header</automated>
  </verify>
  <done>≥11 section-coverage tests pass; threshold-mirror test parametrized across all 5 locked values; README structure verified against the contract spec; rubric-leak test derives sentinels from the actual rubric files (no hardcoded prompt strings in the test).</done>
</task>

<task type="auto">
  <name>Task 3: Privacy contract test — README cannot leak ear-test log content</name>
  <files>tests/eval/test_eval_readme_redacts_ear_test_content.py</files>
  <action>
Per D-GATE-09 redaction rule — pin the privacy invariant.

`tests/eval/test_eval_readme_redacts_ear_test_content.py`:
- Module docstring cites GATE-09 + `feedback_privacy_scope_narrow`. State: "Ear-test log files live in repo as audit trail; their textual content (free_form, signed_by-specific notes) MUST NOT appear in eval/README.md, which is public-facing."
- Constants:
  ```
  REPO_ROOT = Path(__file__).resolve().parents[2]
  README = REPO_ROOT / "eval" / "README.md"
  LOGS_DIR = REPO_ROOT / "eval" / "ear-test-logs"
  ```
- Helpers:
  - `_load_log_payloads() -> list[dict]` — globs `LOGS_DIR / "*.json"` (skip `schema.json`), reads each via json.load, returns the list.
  - `_extract_free_form_chunks(payload: dict) -> list[str]` — returns the `free_form` field value split into substrings of length ≥ 10 (so single common words like "good" don't trigger false positives; whole phrases ≥ 10 chars are the contract surface).
- Tests:
  - `test_no_free_form_text_in_readme()` — load every log file's free_form; for each chunk ≥ 10 chars, assert it is NOT a substring of README content. If LOGS_DIR has no log files yet (Kaan hasn't discharged §GATE-05 yet), the test passes trivially with a `pytest.skip("no ear-test logs to redact yet — discharge §GATE-05 first")` semantic.
  - `test_no_session_id_specific_anecdotes_in_readme()` — for each log file, extract `session_id` value; assert it does NOT appear in README. session_id is a structured field, not free_form, but it identifies a specific Kaan-DJ-session and should not be leaked publicly.
  - `test_no_signed_at_timestamps_in_readme()` — for each log file, assert the full ISO8601 `signed_at` timestamp is NOT a substring of README. (Individual digits / year strings are OK; full timestamps are the leak vector.)
  - `test_readme_does_document_redaction_explicitly()` — README contains a sentence stating the redaction principle (re-verifies the public contract): the body must contain ALL of: `ear-test`, `redact` (or `REDACTED`), and `audit trail`. This is the *positive* assertion that the policy itself is documented.
  - `test_schema_json_excluded_from_log_iteration()` — `schema.json` exists but is correctly excluded from the log scan (sanity — schema is not a "log").
  </action>
  <verify>
    <automated>uv run pytest tests/eval/test_eval_readme_redacts_ear_test_content.py -q --no-header</automated>
  </verify>
  <done>≥5 privacy tests pass (skip cleanly when no log files yet); README documents the redaction principle explicitly; future log files will trigger the leakage check on every PR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/ear-test-logs/*.json → eval/README.md | Private free_form text MUST NOT cross this boundary into the public doc |
| eval/THRESHOLD-LOCK.md → eval/README.md | Locked numeric thresholds MUST cross verbatim (mirror equality) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06-01 | Information Disclosure | Ear-test log free_form leak into README | mitigate | `test_no_free_form_text_in_readme` iterates every log file and asserts no ≥10-char free_form chunk substring-matches README content |
| T-42-06-02 | Information Disclosure | session_id / signed_at timestamps in README | mitigate | dedicated tests assert these structured identifiers do not appear in README |
| T-42-06-03 | Tampering | Threshold values drift between LOCK + README | mitigate | `test_readme_threshold_values_match_lock_file` parametrized over all 5 keys; fails if README drifts |
| T-42-06-04 | Information Disclosure | Rubric/prompt body leaked inline | mitigate | sentinel-derived smoke test reads `eval/rubrics/judge_*.md` first 40 chars past frontmatter and asserts absence in README; rubric bodies stay in `eval/rubrics/` cross-linked |
| T-42-06-05 | Repudiation | README silently published without redaction clause | mitigate | `test_readme_documents_redaction_explicitly` requires the doc to self-document the policy |
</threat_model>

<verification>
- README created: `test -f eval/README.md`
- Section-coverage tests pass: `uv run pytest tests/eval/test_eval_readme_public_facing.py -q`
- Privacy tests pass: `uv run pytest tests/eval/test_eval_readme_redacts_ear_test_content.py -q`
- Phase 27 + 42 baselines unchanged: `uv run pytest tests/eval/ -q --no-header`
- Manual review: README reads as a single page (≤350 lines), public-OSS tone, zero Kaan-private content
- Cross-links resolvable: `for f in eval/THRESHOLD-LOCK.md eval/EAR-TEST-PROTOCOL.md eval/THRESHOLD-RECALIBRATION-LOG.md .planning/decisions/P85-OVERRIDE-RETIRED.md; do test -f "$f" && echo OK $f || echo MISSING $f; done`
</verification>

<success_criteria>
- `eval/README.md` shipped, ≤350 lines, all 10 sections present
- Threshold values mirror THRESHOLD-LOCK.md exactly (5/5 metrics)
- Anti-feature carveouts documented
- Privacy redaction principle self-documented in the README
- ≥16 new tests pass across 2 test files
- No regression on Phase 27 + Phase 42 baselines
</success_criteria>

<output>
After completion, create `.planning/phases/42-hallucination-gate-v3-hybrid/42-06-SUMMARY.md`.
</output>
