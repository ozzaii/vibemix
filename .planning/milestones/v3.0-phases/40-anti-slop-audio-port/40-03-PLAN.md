---
phase: 40-anti-slop-audio-port
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/vibemix/agent/dj_cohost.py
  - src/vibemix/__main__.py
  - src/vibemix/prompts/matrix.py
  - tests/agent/test_dj_cohost_3part.py
  - tests/prompts/test_matrix_3part_labeling.py
autonomous: true
requirements: [AUDIO-02, AUDIO-04]
tags: [audio, lookahead, gemini-multimodal, prompt-engineering, anti-slop]

must_haves:
  truths:
    - "When a local file is playing AND lookahead extraction succeeds, Gemini receives a 3rd `audio/wav` Part containing 18s ending 3s past the current playhead."
    - "When the track is streaming-only or lookahead fails, the request gracefully degrades to 1-Part (live mix only) or 2-Part (live mix + mic when KAAN_SPOKE-recent) — never raises."
    - "The prompt template explicitly tells Gemini the contract: P1 = live BlackHole audience perspective, P2 = mic if attached, P3 = source-file lookahead NOT YET HEARD BY AUDIENCE (per locked decision Q2 in CONTEXT.md)."
  artifacts:
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "`lookahead: LookaheadProvider | None = None` kwarg on `DJCoHostAgent.__init__`; `llm_node` appends Part 3 when lookahead snapshot returns bytes."
      contains: "self._lookahead"
    - path: "src/vibemix/__main__.py"
      provides: "Per-session `LookaheadProvider()` instantiation passed into `DJCoHostAgent(..., lookahead=lookahead_provider)`."
      contains: "lookahead_provider = LookaheadProvider"
    - path: "src/vibemix/prompts/matrix.py"
      provides: "Part-aware prompt suffix builder — emits 1-Part / 2-Part / 3-Part descriptions with the `NOT YET HEARD BY AUDIENCE` label on Part 3 (per locked decision Q2)."
      contains: "NOT YET HEARD BY AUDIENCE"
    - path: "tests/agent/test_dj_cohost_3part.py"
      provides: "Integration tests asserting Part count: 1 baseline, 2 with mic, 2 with lookahead-only, 3 with both."
      min_lines: 80
    - path: "tests/prompts/test_matrix_3part_labeling.py"
      provides: "Unit tests asserting the prompt suffix correctly enumerates the Parts present in each scenario and includes the anti-slop labeling."
      min_lines: 40
  key_links:
    - from: "src/vibemix/agent/dj_cohost.py::llm_node"
      to: "src/vibemix/audio/lookahead.py::LookaheadProvider.snapshot_wav"
      via: "`lookahead_wav, lookahead_meta = self._lookahead.snapshot_wav()` immediately after mic Part 2 attach"
      pattern: "self\\._lookahead\\.snapshot_wav"
    - from: "src/vibemix/agent/dj_cohost.py::llm_node"
      to: "google.genai types.Part.from_bytes"
      via: "`if lookahead_wav: contents.append(types.Part.from_bytes(data=lookahead_wav, mime_type='audio/wav'))`"
      pattern: "lookahead_wav.*Part\\.from_bytes"
    - from: "src/vibemix/prompts/matrix.py"
      to: "DJCoHostAgent.llm_node prompt-suffix branch"
      via: "`build_parts_description(has_mic_part: bool, has_lookahead_part: bool, audio_seconds: float) -> str`"
      pattern: "build_parts_description"
---

<objective>
Wire `LookaheadProvider` (Plan 40-02) into `DJCoHostAgent.llm_node` as the **3rd Gemini multimodal Part** AND update the prompt template to honor the locked 3-Part additive contract (CONTEXT.md decisions Q1 + Q2 + AUDIO-04). This is the final integration step that closes the AUDIO-02 + AUDIO-04 requirements.

**Hallucination class closed (cumulative across plans 40-01 + 40-02 + 40-03):**
- Plan 40-01 closed "AI invents what Kaan said" (mic Part).
- Plan 40-02 built the lookahead extractor (no integration yet).
- This plan closes "AI reacts after the moment passed" by inserting near-future audio AND closes "AI claims to predict the future" by labeling Part 3 explicitly so Gemini knows it's seeing an unfair preview the audience hasn't heard yet (per locked Q2 — overrides v4 silent-offset behavior).

**Architectural note (binding):** The locked decision in `<locked_decisions>` chose **3-Part additive** (CONTEXT.md) over **2-Part swap** (v4 actual). Part 1 = live BlackHole 7s is ALWAYS sent (preserves audience-mix grounding). Part 3 = lookahead appended only when local file resolves. The v4 silent-offset trick is deliberately rejected — Gemini sees the "NOT YET HEARD BY AUDIENCE" label and the prompt-template anti-prediction language (per `<locked_decisions>` Q2) discourages "I predict the drop" commentary. RESEARCH §Anti-Patterns flagged this deviation; we proceed per user decision under `gsd-autonomous fully`.

Purpose: Final integration of the lookahead pipeline into the live runtime, with explicit prompt-template grounding.

Output:
- `DJCoHostAgent.llm_node` 3-Part contents assembly.
- Per-session `LookaheadProvider` instance in `__main__.py`.
- Part-aware prompt suffix builder in `prompts/matrix.py`.
- Integration + unit tests covering all 4 Part-count scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-anti-slop-audio-port/40-CONTEXT.md
@.planning/phases/40-anti-slop-audio-port/40-RESEARCH.md
@.planning/phases/40-anti-slop-audio-port/40-01-SUMMARY.md
@.planning/phases/40-anti-slop-audio-port/40-02-SUMMARY.md
@CLAUDE.md

@src/vibemix/agent/dj_cohost.py
@src/vibemix/audio/lookahead.py
@src/vibemix/__main__.py
@src/vibemix/prompts/matrix.py

<interfaces>
`LookaheadProvider` (from Plan 40-02, src/vibemix/audio/lookahead.py):
  - `LookaheadProvider(lookahead_sec=3.0, window_sec=18.0, sample_rate=16000)`
  - `.snapshot_wav() -> tuple[bytes | None, dict]` — bytes is 18s mono 16kHz WAV ending 3s past playhead, or None on graceful-degrade. meta dict carries `{ok, reason, title?, position_s?, path?, wall_ms?}`.
  - Per-session lifecycle (per RESEARCH Open Question 3 recommendation) — instantiated in `__main__.py` once per process; passed into `DJCoHostAgent(..., lookahead=lookahead_provider)`. The `_title_to_path` cache lives for the session.

`DJCoHostAgent.__init__` (from Plan 40-01 extension, src/vibemix/agent/dj_cohost.py:138):
  - Already accepts `mic_audio_buf: AudioBuffer | None = None` kwarg (Plan 40-01).
  - This plan adds `lookahead: "LookaheadProvider | None" = None` keyword-only kwarg in the same block.
  - When `lookahead is None`, Part 3 is never attached (backward-compat for unit tests that don't drive lookahead).

Part attach order in `llm_node` (from Plan 40-01 + this plan):
  1. Part 1 — always: `types.Part.from_bytes(data=audio_wav, mime_type="audio/wav")` (live BlackHole mix).
  2. Part 2 — conditional (Plan 40-01): mic ring when KAAN_SPOKE-recent AND has signal.
  3. Part 3 — conditional (this plan): lookahead WAV when `self._lookahead.snapshot_wav()` returns non-None bytes.

Prompt suffix helper (new in `prompts/matrix.py`):
```python
def build_parts_description(
    audio_seconds: float,
    has_mic_part: bool,
    has_lookahead_part: bool,
) -> str:
    """Anti-slop Part-aware prompt suffix. Encodes locked Q2 — Part 3 is
    explicitly labeled "NOT YET HEARD BY AUDIENCE" so Gemini knows it's
    seeing an unfair preview and shouldn't make prediction-shaped commentary.
    """
    # Build one of 4 strings: 1-Part, 1-Part+lookahead, 2-Part (mic), 3-Part.
    # All strings end with: "Your ears are the referee — the evidence above is grounded context."
```

The exact strings to emit (locked):
- 1-Part: `f"\n\nAttached: P1 = last {int(audio_seconds)}s of live BlackHole audio (audience perspective). Your ears are the referee — the evidence above is grounded context."`
- 1-Part + lookahead (no mic): `f"\n\nAttached: P1 = last {int(audio_seconds)}s of live BlackHole audio (audience perspective). P2 = 18s from the source file ending ~3s past now — NOT YET HEARD BY AUDIENCE; do NOT describe Part 2 as if it has played. Use it only to ground what you HEAR in Part 1 about to happen. Your ears are the referee — the evidence above is grounded context."`
- 2-Part (mic): `f"\n\nAttached: P1 = last {int(audio_seconds)}s of live BlackHole audio (audience perspective). P2 = your mic (last 8s, Kaan's literal voice). Your ears are the referee — the evidence above is grounded context."`
- 3-Part: `f"\n\nAttached: P1 = last {int(audio_seconds)}s of live BlackHole audio (audience perspective). P2 = your mic (last 8s, Kaan's literal voice). P3 = 18s from the source file ending ~3s past now — NOT YET HEARD BY AUDIENCE; do NOT describe Part 3 as if it has played. Use it only to ground what you HEAR in Part 1 about to happen. Your ears are the referee — the evidence above is grounded context."`

Note: When lookahead is present without mic, it occupies the P2 slot (not P3) to keep the label semantics tight — Gemini gets contiguous slot numbering. The "P2 = NOT YET HEARD" framing only appears when there's no mic in the way. This matches the conditional-positioning approach the locked Q2 implies.

`AICoach.build_prompt` (src/vibemix/state/coach.py) currently produces the text body that `llm_node` concatenates with the f-string suffix at lines 332-340. The change here is to delegate suffix construction to `build_parts_description` rather than inlining the f-string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Part-aware prompt suffix builder + unit tests</name>
  <files>src/vibemix/prompts/matrix.py, tests/prompts/test_matrix_3part_labeling.py</files>
  <behavior>
    - `build_parts_description(audio_seconds: float, has_mic_part: bool, has_lookahead_part: bool) -> str` returns one of 4 deterministic strings matching the spec above. The function is pure (no side effects, no globals read).
    - When `has_lookahead_part=True` AND `has_mic_part=False`, the lookahead is labeled P2 (not P3). When both are True, mic is P2 and lookahead is P3.
    - All variants end with `"Your ears are the referee — the evidence above is grounded context."` — preserves the v4 anti-slop refrain (per RESEARCH §Project Constraints "trust the audio").
    - The lookahead string ALWAYS contains the literal `"NOT YET HEARD BY AUDIENCE"` substring (locked Q2 per CONTEXT.md).
    - The lookahead string ALWAYS contains anti-prediction language like `"do NOT describe Part N as if it has played"` (closes the "AI claims to predict the future" hallucination class introduced by Part 3 being temporally ahead).
    - 4 unit tests cover the 4 boolean permutations of (has_mic_part, has_lookahead_part). 2 additional tests verify (a) the "NOT YET HEARD BY AUDIENCE" substring is present when `has_lookahead_part=True` and (b) absent otherwise.
  </behavior>
  <action>
    Add `build_parts_description` to `src/vibemix/prompts/matrix.py` immediately after the existing HYPE_INTERMEDIATE / COACH_* prompt cell constants. Use a docstring referencing locked CONTEXT decisions Q1 + Q2 and AUDIO-04.

    Create `tests/prompts/test_matrix_3part_labeling.py` covering:
    - **`test_1_part_baseline`** — `build_parts_description(7, False, False)` returns the 1-Part string, contains `"P1 ="`, does NOT contain `"P2"` or `"P3"`, does NOT contain `"NOT YET HEARD"`.
    - **`test_lookahead_only_at_p2`** — `build_parts_description(7, False, True)` returns the 1-Part+lookahead string with the lookahead labeled `"P2 ="`, contains `"NOT YET HEARD BY AUDIENCE"`.
    - **`test_mic_only_at_p2`** — `build_parts_description(7, True, False)` returns the 2-Part string with mic labeled `"P2 ="`, does NOT contain `"NOT YET HEARD"`.
    - **`test_full_3part`** — `build_parts_description(7, True, True)` returns the 3-Part string, mic labeled `"P2 ="`, lookahead labeled `"P3 ="`, contains `"NOT YET HEARD BY AUDIENCE"` and `"do NOT describe Part 3"`.
    - **`test_anti_prediction_phrase_in_lookahead_variants`** — both lookahead-present variants contain a phrase matching the pattern `do NOT describe Part [23] as if it has played`.
    - **`test_ears_are_the_referee_in_all_variants`** — all 4 variants end with the `"Your ears are the referee"` refrain (v4 anti-slop carry-over).
  </action>
  <verify>
    <automated>python -m pytest tests/prompts/test_matrix_3part_labeling.py -x -q</automated>
  </verify>
  <done>
    - 6 tests pass in `tests/prompts/test_matrix_3part_labeling.py`.
    - `python -c "from vibemix.prompts.matrix import build_parts_description; print(build_parts_description(7, True, True))"` prints the full 3-Part string containing `"NOT YET HEARD BY AUDIENCE"`.
    - `grep -v '^#' src/vibemix/prompts/matrix.py | grep -c "NOT YET HEARD BY AUDIENCE"` ≥ 2 (appears in the 2 lookahead variants).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DJCoHostAgent lookahead Part 3 wiring + 4-scenario integration test</name>
  <files>src/vibemix/agent/dj_cohost.py, src/vibemix/__main__.py, tests/agent/test_dj_cohost_3part.py</files>
  <behavior>
    - `DJCoHostAgent.__init__` accepts new `lookahead: "LookaheadProvider | None" = None` keyword-only kwarg. Stored as `self._lookahead`. Default None preserves all existing backward-compat (unit tests that don't drive lookahead).
    - In `llm_node`, immediately AFTER the mic Part 2 attach (from Plan 40-01) and BEFORE the `screen_jpeg` block, attempt the lookahead snapshot:
      ```
      lookahead_wav: bytes | None = None
      lookahead_meta: dict = {}
      if self._lookahead is not None:
          try:
              lookahead_wav, lookahead_meta = self._lookahead.snapshot_wav()
          except Exception as e:
              print(f"[lookahead err] {e}", file=sys.stderr)
              lookahead_wav, lookahead_meta = None, {"reason": f"exception: {e}"}
      ```
      (Exception path is belt-and-braces — `LookaheadProvider.snapshot_wav` already returns `(None, meta)` on every failure, but the try wrapper guarantees `llm_node` never crashes even if the provider misbehaves.)
    - When `lookahead_wav is not None`: `contents.append(types.Part.from_bytes(data=lookahead_wav, mime_type="audio/wav"))` AND `self._recorder.log_event("lookahead_part_attached", **lookahead_meta)`.
    - When `lookahead_wav is None`: `self._recorder.log_event("lookahead_part_skipped", **lookahead_meta)`.
    - The text-prompt f-string suffix (modified in Plan 40-01 from inline-strings to a flexible structure) NOW calls `build_parts_description(audio_seconds, has_mic_part=mic_attached, has_lookahead_part=lookahead_attached)` and appends the result to `text_prompt`. Replace the hand-rolled f-string in `llm_node` at lines ~334-337 with this call.
    - `__main__.py` instantiates `lookahead_provider = LookaheadProvider()` near where `clean_audio_buf` is created (line ~416). Passes it as `lookahead=lookahead_provider` into the `DJCoHostAgent(...)` constructor call.
    - Integration tests cover all 4 Part-count scenarios via `MagicMock`-substituted `genai_client.aio.models.generate_content_stream`:
      - **`test_part_count_1_no_mic_no_lookahead`** — both kwargs `mic_audio_buf=None, lookahead=None`; assert `contents` arg has 2 elements (text + Part 1).
      - **`test_part_count_2_lookahead_only`** — `mic_audio_buf=None`, lookahead mocked to return a non-None bytes; assert 3 elements (text + Part 1 + Part 2 lookahead); assert text prompt contains `"NOT YET HEARD BY AUDIENCE"`.
      - **`test_part_count_2_mic_only`** — mic_audio_buf with signal, lookahead mocked to return `(None, {"reason": "no file"})`; assert 3 elements (text + Part 1 + Part 2 mic); assert text prompt does NOT contain `"NOT YET HEARD"`.
      - **`test_part_count_3_mic_and_lookahead`** — both wired and returning bytes; assert 4 elements (text + Part 1 + Part 2 mic + Part 3 lookahead); assert text prompt contains both `"P3 ="` and `"NOT YET HEARD BY AUDIENCE"`.
      - **`test_lookahead_exception_does_not_crash_llm_node`** — `self._lookahead.snapshot_wav` monkeypatched to `raise RuntimeError("boom")`; `llm_node` completes; assert `contents` has 2 elements (Part 1 only); recorder logged a `"lookahead_part_skipped"` event with `reason` mentioning the exception.
  </behavior>
  <action>
    Modify `src/vibemix/agent/dj_cohost.py`:
    1. Add `lookahead: "LookaheadProvider | None" = None` kwarg to `__init__` (alongside `mic_audio_buf` from Plan 40-01). Add `self._lookahead = lookahead` storage. Add `from vibemix.audio.lookahead import LookaheadProvider` import under `if TYPE_CHECKING:` guard to avoid runtime import cost when lookahead is None.
    2. In `llm_node`, immediately after the Plan 40-01 mic Part attach (where `mic_attached: bool` is set), add the lookahead snapshot block per the behavior spec. Track `lookahead_attached: bool` for the prompt-suffix builder.
    3. Replace the hand-rolled f-string suffix at lines ~334-337 with `build_parts_description(audio_seconds, has_mic_part=mic_attached, has_lookahead_part=lookahead_attached)`. Import `build_parts_description` from `vibemix.prompts.matrix`.
    4. Add `from vibemix.prompts.matrix import build_parts_description` to the top-of-file imports.

    Modify `src/vibemix/__main__.py`:
    1. At line ~416 (next to `clean_audio_buf` allocation), add `lookahead_provider = LookaheadProvider()`. Import `from vibemix.audio import LookaheadProvider`.
    2. At the `DJCoHostAgent(...)` constructor call (locate via `grep -n "DJCoHostAgent(" src/vibemix/__main__.py`), add `lookahead=lookahead_provider` to the kwargs.
    3. Emit a startup log line: `print(f"-> lookahead: +{LOOKAHEAD_SECONDS:.1f}s @ {LOOKAHEAD_WINDOW_SECONDS:.0f}s window (degrades silently on streaming tracks)")`. Mirror the v4_tr:2314 announcement pattern.

    Create `tests/agent/test_dj_cohost_3part.py` covering all 5 tests (4 Part-count scenarios + 1 exception-safety). Use `MagicMock` for `genai_client.aio.models.generate_content_stream` and capture `call_args[1]["contents"]` to count Parts and inspect text content. For lookahead presence, monkeypatch `LookaheadProvider.snapshot_wav` directly on the instance: `provider.snapshot_wav = lambda: (mock_wav_bytes, {"ok": True})`.

    Reference: `cohost_v4.py:1791-1813` (Part assembly), `cohost_v4_tr.py:2314` (startup log), RESEARCH Pattern 2 (Part attachment order).
  </action>
  <verify>
    <automated>python -m pytest tests/agent/test_dj_cohost_3part.py tests/agent/test_dj_cohost_mic_part.py tests/agent/ tests/test_main_smoke.py -x -q</automated>
  </verify>
  <done>
    - 5 tests pass in `tests/agent/test_dj_cohost_3part.py`.
    - All existing `tests/agent/` tests still pass (no regression in the 1-Part / 2-Part baselines from Plan 40-01).
    - `tests/test_main_smoke.py` still passes (LookaheadProvider construction doesn't crash __main__.py boot).
    - `grep -n "self._lookahead" src/vibemix/agent/dj_cohost.py | wc -l` ≥ 3 (init, llm_node snapshot, log_event branches).
    - `grep -n "lookahead_provider" src/vibemix/__main__.py | wc -l` ≥ 2 (instantiation + agent construction).
    - The 3-Part text prompt rendered in `test_part_count_3_mic_and_lookahead` contains the exact substring `"NOT YET HEARD BY AUDIENCE"`.
    - POC immutability: `git status cohost_v4*.py cohost.py cohost_v2.py cohost_lk.py mascot.html` shows no changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LookaheadProvider stdout → Part 3 (Gemini) | Untrusted audio file content from disk; trusted via ffmpeg's input validation |
| Prompt suffix → Gemini system context | Untrusted in the sense that LLM may attempt to mimic the labeling; mitigated by explicit anti-prediction language |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03-01 | Spoofing | AI claiming to "predict" Part 3 content | mitigate | Prompt template explicitly says `"do NOT describe Part N as if it has played"`. Closes the v4 "silent offset" anti-pattern that the locked Q2 rejected. CI test `test_anti_prediction_phrase_in_lookahead_variants` pins the phrase. |
| T-40-03-02 | DoS | Lookahead provider exception crashing llm_node | mitigate | `try/except Exception` wrapper around `self._lookahead.snapshot_wav()` — `llm_node` completes with 1-Part fallback. Test `test_lookahead_exception_does_not_crash_llm_node` pins this. |
| T-40-03-03 | Information Disclosure | Lookahead title/path in events.jsonl | accept | Per CLAUDE.md `feedback_privacy_scope_narrow` — events.jsonl is gitignored, per-session, never transmitted. Same posture as existing track-resolver records. |
| T-40-03-04 | Repudiation | No audit trail of which Part configuration was sent | mitigate | Every llm_node invocation logs either `lookahead_part_attached` (with meta) or `lookahead_part_skipped` (with reason) — full audit trail in events.jsonl. |
</threat_model>

<verification>
- Unit + integration tests: `python -m pytest tests/agent/test_dj_cohost_3part.py tests/prompts/test_matrix_3part_labeling.py -x -q` (11 tests pass total across both files).
- Regression: `python -m pytest tests/agent/ tests/prompts/ tests/audio/ tests/state/ tests/test_main_smoke.py -x -q` (full impacted dirs green).
- Boot smoke (optional, dev only): `./run_v4.sh` is the POC equivalent; `python -m vibemix` (current src/vibemix runtime) starts and prints `"-> lookahead: +3.0s @ 18s window"` line.
- POC immutability gate: `git status cohost_v4*.py cohost.py cohost_v2.py cohost_lk.py mascot.html` clean.
</verification>

<success_criteria>
1. Live runtime now sends a 3-Part Gemini request `[text, P1=live_mix, P2=mic, P3=lookahead]` when all three signals are present; gracefully degrades to 2-Part or 1-Part otherwise.
2. The prompt suffix explicitly labels each Part AND tags lookahead as "NOT YET HEARD BY AUDIENCE" with anti-prediction guard language (locked Q2 enforced).
3. Lookahead failures NEVER crash `llm_node` — Plan 40-02's `(None, meta)` graceful-degrade contract + this plan's `try/except` wrapper double up the safety net.
4. Per-session lifecycle for `LookaheadProvider` (RESEARCH Open Question 3 resolution) — title→path cache lives for the whole DJ session, not just one invocation.
5. AUDIO-02 + AUDIO-04 requirements closed.
</success_criteria>

<output>
After completion, create `.planning/phases/40-anti-slop-audio-port/40-03-SUMMARY.md` documenting:
- Files modified + LOC delta (dj_cohost.py + __main__.py + matrix.py).
- Test count delta (11 new across 2 files).
- Confirmation that POC files are unchanged.
- 3-Part contract diagram (text + P1 + P2 + P3) and the conditional labeling rules.
- One-line note for Plan 40-04: "Cooldown re-tune in audio/constants.py — Part-count integration is now stable; cooldown changes won't affect Part assembly."
</output>
