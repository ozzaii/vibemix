---
phase: 45-external-discharge-public-rc-publish
plan: 05
title: "docs/launch-rotation.md SHIP-11 augment — 24h solo rotation + triage decision tree"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/launch-rotation.md
  - tests/launch/test_launch_rotation_ship_11.py
autonomous: true
requirements: [SHIP-11]
req_ids: [SHIP-11]
tags: [launch, rotation, monitoring, ship-11, docs, runbook]
must_haves:
  truths:
    - "docs/launch-rotation.md carries a `§SHIP-11` section that defines the v3.0 24h rotation as 4 × 6h shifts (Kaan solo) per CONTEXT decision."
    - "Triage decision tree documents comment-volume / crash-report / API-key rate-limit / Bravoh-server-down → action mapping."
    - "Monitoring signal sources enumerated: GH issues label triage, Bravoh healthz dashboard, Discord #bugs channel."
    - "Existing Phase 39 24h rotation table preserved verbatim (it covers the v2.1 launch — we are appending a new §SHIP-11 section for the v3.0 cut, not editing the v2.1 table)."
    - "CI gate pins the §SHIP-11 section structure so future doc edits can't accidentally drop the 4-shift contract."
  artifacts:
    - path: "docs/launch-rotation.md"
      provides: "Append-only §SHIP-11 section — 4 × 6h shifts table + triage decision tree + monitoring-signal sources + sign-off block stub."
      min_lines_added: 100
    - path: "tests/launch/test_launch_rotation_ship_11.py"
      provides: "Structural pin: §SHIP-11 H2 present, 4-shift table with CET ranges, triage tree H3, monitoring-sources H3."
      min_lines: 120
  key_links:
    - from: "docs/launch-rotation.md §SHIP-11"
      to: "KAAN-ACTION-LEGAL.md §SHIP-11 (Plan 45-06)"
      via: "runbook cross-reference"
      pattern: "§SHIP-11"
    - from: "docs/launch-rotation.md §SHIP-11 triage tree"
      to: "scripts/release/check_bravoh_server_ready.sh (Plan 45-03)"
      via: "incident-response command in the Bravoh-server-down branch"
      pattern: "check_bravoh_server_ready"
    - from: "docs/launch-rotation.md §SHIP-11 monitoring sources"
      to: "scripts/dayzero/healthz_check.sh (Phase 36)"
      via: "ongoing-watch tail command"
      pattern: "healthz_check"
---

<objective>
Augment the existing `docs/launch-rotation.md` (Phase 39 / SHIP-07) with a v3.0-specific `§SHIP-11` section: 4 × 6h shifts (Kaan solo per CONTEXT — Francesco + Momo additions deferred to v3.x), triage decision tree, and monitoring-signal sources. Pin the structural invariants via a small CI test so future doc drift can't silently break the v3.0 rotation contract.

Purpose: SHIP-11 is the 24h post-launch monitoring discharge. CONTEXT §SHIP-11 specifies "4 × 6h shifts (Kaan-only solo rotation for v3.0; v3.x may add Francesco/Momo)" plus "triage decision tree: comment volume / crash report / API key rate-limit / Bravoh server down". The existing launch-rotation.md is v2.1-scoped (24-row hourly table); v3.0 needs a simpler, leaner solo cadence + decision tree.

Output: Append-only doc edit + 1 test file. No new code or scripts — this is a runbook/doc plan. Plan 45-06 §SHIP-11 runbook in KAAN-ACTION-LEGAL.md cites this section as the operational source-of-truth.

Anti-scope: This plan does NOT modify the Phase 39 v2.1 24-row table (that's archived launch history). It also does NOT create new monitoring tooling per CONTEXT §SHIP-11 ("No new monitoring tooling. Kaan watches GH Actions + Bravoh healthz dashboard + Discord #bugs channel.").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-external-discharge-public-rc-publish/45-CONTEXT.md

@docs/launch-rotation.md
@docs/launch-prep/LAUNCH-SEQUENCE.md
@scripts/dayzero/healthz_check.sh

<interfaces>
Existing structure (Phase 39 launch-rotation.md, lines 1-102):
- H1: `# vibemix v2.1 Launch — 24h Monitoring Rotation`
- H2: `## Owners`, `## Per-hour shift (24h, all times CET)`, `## Per-hour checklist (every responder, every hour)`, `## Escalation paths`, `## Handoff format (Discord #vibemix-rota)`, `## Post-24h transition`, `## References`.

Append target: After the existing `## References` H2, add a new top-level `## §SHIP-11 — v3.0 24h Monitoring Rotation (4 × 6h shifts, Kaan solo)` section. The existing content is preserved as v2.1 archive; §SHIP-11 is the v3.0 successor.

Monitoring signal sources (from CONTEXT §SHIP-11):
- GH Actions email + Issues label `triage` + Discord #bugs channel
- Bravoh healthz dashboard (`bash scripts/dayzero/healthz_check.sh --target https://api.altidus.world/vibemix/healthz --max-iterations 1 --interval 0`)
- Bravoh server endpoint contract probe (`bash scripts/release/check_bravoh_server_ready.sh` — Plan 45-03)
- Memory `project_one_click_install_hard_req`: fresh-VM install issues are the HIGHEST-priority signal during the rotation.

Triage decision tree branches (from CONTEXT §SHIP-11 + Phase 39 launch-rotation Escalation paths):
- Comment volume spike → triage Discord + GH issues, label, respond within hour
- Crash report (issue labelled `crash`) → reproduce, patch decision (RC2 cycle vs hotfix), record in Plan 45-04 SHIP-V1-DECISION evidence
- API key rate-limit (Bravoh proxy at >80% capacity) → coordinate with Musa, autoscale or rate-limit
- Bravoh server down → run `check_bravoh_server_ready.sh`, post status to Discord, escalate to Bravoh team
</interfaces>

<section_skeleton>
The §SHIP-11 section structure (append target):

```
## §SHIP-11 — v3.0 24h Monitoring Rotation (4 × 6h shifts, Kaan solo)

**Phase 45 / SHIP-11 / KAAN-ACTION-LEGAL §SHIP-11**

The v3.0 launch uses a simpler rotation than v2.1's hourly table: 4 shifts × 6h each, all Kaan (Francesco + Momo deferred to v3.x). Launch slot: T+0 = 09:00 CET (HN sweet spot + EU morning + US wake-up).

### Shift table

| Shift | CET window | T-offset | Focus | Primary signals |
|---|---|---|---|---|
| Shift 1 (EU morning) | 08:00 – 14:00 | T+0 → T+6 | Cut + flip release; Discord launch post; HN front-page check; first-hour Issues triage | GH issues label triage; HN front-page rank; Discord #bugs |
| Shift 2 (EU afternoon) | 14:00 – 20:00 | T+6 → T+12 | US wake-up wave; first install-fail reports; first ear-test reactions | Discord #bugs (fresh-VM installs); GH issues label `install`; Bravoh healthz |
| Shift 3 (EU evening) | 20:00 – 02:00 | T+12 → T+18 | US prime time; Discord live engagement; first community-mascot reactions | Discord #show-and-tell + #bugs; GH issues; star velocity sample |
| Shift 4 (sleep / alerts) | 02:00 – 08:00 | T+18 → T+24 | Sleep; on alert only via GH Actions email + Discord ping | Showstopper-only pager |

### Triage decision tree

```
Signal arrives → categorize:

├── Comment volume spike (Discord, GH issues, HN)
│   └── Triage within the hour: label, react, reply with anchor message
│
├── Crash report (issue labelled `crash` OR Discord #bugs upload)
│   ├── Reproducible? → patch decision
│   │   ├── Hotfix path (single-file fix, no breaking) → cut v3.0.0-rc1.1
│   │   └── Multi-file or regression → RC2 cycle (record in SHIP-V1-DECISION)
│   └── Not reproducible → request session.jsonl + events.jsonl from reporter
│
├── API key rate-limit (Bravoh proxy >80% capacity)
│   ├── Spot-check via `bash scripts/dayzero/healthz_check.sh --target https://api.altidus.world/vibemix/healthz --max-iterations 1 --interval 0`
│   ├── Coordinate with Musa (Bravoh proxy on-call) to autoscale or raise rate limits
│   └── If unable to resolve in 15 min → post Discord `[notice] vibemix proxy queue active`
│
└── Bravoh server down (healthz 5xx, /vibemix/updates/latest.json 5xx)
    ├── Run `bash scripts/release/check_bravoh_server_ready.sh` for structured BLOCKED_BY line
    ├── Escalate to Bravoh team (Slack #bravoh-vibemix)
    ├── Post Discord `[notice] vibemix updater check intermittent — investigating`
    └── If down >30 min → pin a banner in README + freeze announcements
```

### Monitoring signal sources (per shift, every hour)

1. **GH Actions email** — any failed workflow alerts to Kaan.
2. **GH issues** — `gh issue list --state open --label triage` — respond within the shift.
3. **Discord #bugs** — scroll messages since last check, react ✅ to OK, escalate red flags.
4. **Bravoh healthz** — `bash scripts/dayzero/healthz_check.sh --target https://api.altidus.world/vibemix/healthz --max-iterations 1 --interval 0` (returns 200 or escalate per tree).
5. **Bravoh server contract probe** — `bash scripts/release/check_bravoh_server_ready.sh` (Plan 45-03) — green = all 3 updater endpoints up + healthz fresh.
6. **Star velocity sample** — `gh api /repos/bravoh/vibemix --jq .stargazers_count` — log delta vs previous shift.
7. **Fresh-VM install issues** — HIGHEST PRIORITY per project memory `one-click-install-hard-req`. Any "vibemix doesn't open on first launch" report → top of queue.

### Handoff format (Discord #vibemix-rota or self-note)

End of each shift, Kaan posts (or notes-to-self):

```
Shift <N> handoff (T+<offset>)
Open issues:        <N>
Open Discord:       <M> threads, <K> red flags
Stars delta:        +<X> since previous shift
healthz:            OK / WARN / FAIL
Bravoh server:      OK / WARN / FAIL (BLOCKED_BY line if FAIL)
Notes:              <one-line summary>
Next watch:         <bullet points if any>
```

### Post-24h transition

At T+24, hand off to async monitoring per `docs/launch-rotation.md` Post-24h section. The bake observation period (~2 weeks) runs in parallel with normal vibemix dev cadence; SHIP-13 (Plan 45-04) audit fires at T+30.

### Sign-off block

```
SHIP-11 ENGINEERING GREEN on:   _____________  (Plan 45-05 §SHIP-11 augment shipped)
SHIP-11 SHIFT 1 COMPLETE on:    _____________  (T+0 → T+6 — cut + launch post + first-hour triage)
SHIP-11 SHIFT 2 COMPLETE on:    _____________  (T+6 → T+12 — US wake-up wave triage)
SHIP-11 SHIFT 3 COMPLETE on:    _____________  (T+12 → T+18 — US prime time triage)
SHIP-11 SHIFT 4 COMPLETE on:    _____________  (T+18 → T+24 — sleep + alerts-only)
SHIP-11 T+24 HANDOFF on:        _____________  (handed off to async + bake-observation period)
Sign-off by (Kaan):             _____________
```

### References

- `KAAN-ACTION-LEGAL.md §SHIP-11` — Kaan-discharge runbook (Plan 45-06).
- `scripts/release/check_bravoh_server_ready.sh` — Plan 45-03 server contract probe.
- `scripts/dayzero/healthz_check.sh` — Phase 36 healthz watchdog.
- Memory `project_one_click_install_hard_req` — fresh-VM install issues = top priority signal.
```
</section_skeleton>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin §SHIP-11 section structure via CI test (RED)</name>
  <files>tests/launch/test_launch_rotation_ship_11.py</files>
  <behavior>
    - Test 1: docs/launch-rotation.md exists.
    - Test 2: Existing Phase 39 v2.1 24-row hourly table is preserved verbatim (anchor: `## Per-hour shift (24h, all times CET)` H2 present; first row `| 08:00 | T-1   | Kaan` present).
    - Test 3: New H2 `## §SHIP-11 — v3.0 24h Monitoring Rotation (4 × 6h shifts, Kaan solo)` exists.
    - Test 4: Shift table has exactly 4 rows (Shift 1 / 2 / 3 / 4) with the canonical CET windows (08:00–14:00, 14:00–20:00, 20:00–02:00, 02:00–08:00).
    - Test 5: Triage decision tree H3 `### Triage decision tree` exists; contains the 4 categorized branches (`Comment volume`, `Crash report`, `API key rate-limit`, `Bravoh server down`).
    - Test 6: Monitoring signal sources H3 `### Monitoring signal sources (per shift, every hour)` exists; lists at least 5 numbered sources including `Bravoh healthz`, `check_bravoh_server_ready.sh`, `Discord #bugs`, `GH issues`, `Star velocity`.
    - Test 7: Sign-off block has 6 `SHIP-11 ...` placeholder lines + `Sign-off by (Kaan):` line.
  </behavior>
  <action>Author tests/launch/test_launch_rotation_ship_11.py. Each test uses Path(`docs/launch-rotation.md`).read_text() + grep/regex for the structural invariants above. Tests MUST fail RED — the §SHIP-11 section doesn't exist yet (only the v2.1 Phase 39 content is present). The Phase 39 preservation test (Test 2) passes immediately (proves we won't damage history). Commit `test(45-05): pin §SHIP-11 launch-rotation section structure (RED — 6/7 fail expected)`.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_launch_rotation_ship_11.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Test file present with 7 tests; 6 fail RED (§SHIP-11 missing), 1 passes (Phase 39 preserved).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append §SHIP-11 section verbatim from skeleton (GREEN)</name>
  <files>docs/launch-rotation.md</files>
  <behavior>
    - All 7 tests from Task 1 pass.
    - Test 8: §SHIP-11 section appears AFTER the existing `## References` H2 (line-order assertion); never inserted earlier.
    - Test 9: AI-slop blocklist clean — §SHIP-11 section passes `python scripts/launch/check_no_ai_slop.py docs/launch-rotation.md` (no `leverage` / `synergize` / `revolutionize` / `seamless` / etc.).
  </behavior>
  <action>Edit docs/launch-rotation.md. Find the existing `## References` H2 (currently the last section, lines 96-102). Append the §SHIP-11 section literally from the <section_skeleton> above as a new top-level H2 BELOW the existing `## References`. Do NOT modify any existing line. Total file should grow from 102 lines to ~210 lines. Add 2 new tests (Test 8 line-order, Test 9 AI-slop clean). Commit `docs(45-05): append §SHIP-11 v3.0 rotation (4×6h Kaan solo) + triage tree + monitoring sources (GREEN)`.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_launch_rotation_ship_11.py -v 2>&1 | tail -20 && uv run python scripts/launch/check_no_ai_slop.py docs/launch-rotation.md; echo "exit=$?"</automated>
  </verify>
  <done>9 tests pass; doc grew by ~100 lines; existing Phase 39 content untouched; AI-slop clean; section structure pinned for future drift prevention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/launch-rotation.md → operator (Kaan) | Operational runbook; instructions executed by hand. |
| §SHIP-11 references → cross-linked scripts | Plan 45-03 probe, Phase 36 healthz_check, gh CLI — all repo-tracked + tested elsewhere. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-01 | Tampering | future doc edit silently drops §SHIP-11 structure | mitigate | Test 1-7 pin all structural invariants; CI catches drift. |
| T-45-05-02 | Info disclosure | doc accidentally references internal-only URLs or secrets | mitigate | Test 9 AI-slop blocklist clean + manual review of the section_skeleton (all URLs are public-facing: api.altidus.world, github.com/bravoh/vibemix). |
| T-45-05-03 | Repudiation | Kaan misremembers which shift covered which time | mitigate | Sign-off block has explicit `SHIP-11 SHIFT N COMPLETE on: ___` lines per shift; Kaan fills in each window. |
</threat_model>

<verification>
- All 9 tests in tests/launch/test_launch_rotation_ship_11.py pass.
- docs/launch-rotation.md is ~210 lines (was 102; +~108 for §SHIP-11).
- Phase 39 v2.1 hourly table preserved: `grep -c "^| 0[8-9]:00 | T" docs/launch-rotation.md` returns ≥3 (08:00, 09:00, 10:00 rows present).
- §SHIP-11 H2 present: `grep -c "^## §SHIP-11" docs/launch-rotation.md` returns 1.
- AI-slop clean: `uv run python scripts/launch/check_no_ai_slop.py docs/launch-rotation.md` exits 0.
</verification>

<success_criteria>
1. docs/launch-rotation.md carries a §SHIP-11 section per CONTEXT spec (4 × 6h shifts, Kaan solo, triage tree, monitoring sources).
2. Existing Phase 39 v2.1 24-row hourly table preserved verbatim (no archive damage).
3. 9 tests in tests/launch/test_launch_rotation_ship_11.py GREEN; structural drift prevention CI gate operational.
4. AI-slop blocklist clean.
5. Plan 45-06 §SHIP-11 runbook in KAAN-ACTION-LEGAL.md can cross-reference this section as the operational source-of-truth without further doc work.
</success_criteria>

<output>
After completion, create `.planning/phases/45-external-discharge-public-rc-publish/45-05-SUMMARY.md` documenting:
- Files shipped: docs/launch-rotation.md (~+108 lines) + test file (~120 lines)
- 2 atomic commits (RED test pins → GREEN doc append)
- Decisions: append-only preservation of v2.1 archive, 4-shift CET window mapping, CONTEXT §SHIP-11 cadence verbatim
- Plan 45-06 hand-off: §SHIP-11 runbook will cite §SHIP-11 doc anchor for operational protocol
</output>
