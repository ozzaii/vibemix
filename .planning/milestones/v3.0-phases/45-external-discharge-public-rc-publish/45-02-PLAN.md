---
phase: 45-external-discharge-public-rc-publish
plan: 02
title: "launch_trigger.sh orchestration + cadence_index.json (T-30 / T+0 / T+5h / T+24h)"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/launch/launch_trigger.sh
  - scripts/dayzero/launch_copy/cadence_index.json
  - tests/launch/test_launch_trigger_orchestration.py
autonomous: true
requirements: [SHIP-08]
req_ids: [SHIP-08]
tags: [launch, orchestration, social, discord, dry-run-default, ship-08]
must_haves:
  truths:
    - "`launch_trigger.sh --publish` orchestrates the 5-channel + Discord publish per cadence stage."
    - "`--phase {T-30,T+0,T+5h,T+24h}` selects per-cadence copy variants via `cadence_index.json`."
    - "Dry-run by default; `--live` required to make any network call. Without `--live`, tests prove zero-network."
    - "Asserts Kaan + Francesco sign-off footer present in all 5 copy files via `check_no_ai_slop.py`."
    - "Logs to `dist/launch-runs/<UTC>.jsonl` (append-only audit trail; Plan 45-04 reads this for SHIP-V1-DECISION evidence)."
  artifacts:
    - path: "scripts/launch/launch_trigger.sh"
      provides: "Single discharge entry point for SHIP-TWEET + SHIP-DISCORD post-cut publish actions."
      min_lines: 180
    - path: "scripts/dayzero/launch_copy/cadence_index.json"
      provides: "Per-channel × per-stage matrix: which copy file fires at which cadence stage. Versioned data; trigger.sh consumes."
    - path: "tests/launch/test_launch_trigger_orchestration.py"
      provides: "Zero-network dry-run pin + stage matrix correctness + sign-off footer gate."
      min_lines: 250
  key_links:
    - from: "scripts/launch/launch_trigger.sh"
      to: "scripts/dayzero/launch_copy/cadence_index.json"
      via: "json read on --phase resolution"
      pattern: "cadence_index\\.json"
    - from: "scripts/launch/launch_trigger.sh"
      to: "scripts/launch/check_no_ai_slop.py"
      via: "subprocess call before any publish"
      pattern: "check_no_ai_slop"
    - from: "scripts/launch/launch_trigger.sh"
      to: "scripts/launch/publish_social_posts.py"
      via: "subprocess call for the 4 web channels"
      pattern: "publish_social_posts"
    - from: "scripts/launch/launch_trigger.sh"
      to: "scripts/launch/post_discord_launch.py"
      via: "subprocess call for #announcements"
      pattern: "post_discord_launch"
    - from: "scripts/launch/launch_trigger.sh"
      to: "dist/launch-runs/<UTC>.jsonl"
      via: "appends event line per stage"
      pattern: "launch-runs"
---

<objective>
Ship the SHIP-08 (SHIP-TWEET) orchestration scaffolding: a single `scripts/launch/launch_trigger.sh` that picks the correct per-channel copy variant for the current cadence stage (T-30 / T+0 / T+5h / T+24h) and dispatches to the existing Phase 36 publish scripts. The cadence matrix is data-driven via `cadence_index.json` so adding/removing per-stage copy variants doesn't require editing bash.

Purpose: Phase 44-05 locked the 5-channel SHIP-TWEET copy files. Phase 44-07 locked the T-7 → T+30 launch sequence. This plan wires those two locks together so Kaan + Francesco can fire `launch_trigger.sh --live --phase T+0` on launch morning without manually re-routing per-channel + per-stage variants.

Output: Dry-run-default orchestrator + cadence matrix + zero-network tests. Plan 45-06 §SHIP-08 runbook cites the live invocation; this plan ships the scaffolding.

Naming clarification: A different `scripts/dayzero/launch_trigger.sh` exists from Phase 39 targeting v2.1.0. That script stays untouched (Phase 39 history). This plan's NEW script lives under `scripts/launch/` and is the v3.0 successor — Phase 45 publish actions invoke this one. Plan 45-06 §SHIP-08 cites the new path; the dayzero variant is archive-only and retains its existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-external-discharge-public-rc-publish/45-CONTEXT.md

@scripts/dayzero/launch_copy/twitter.txt
@scripts/dayzero/launch_copy/instagram.txt
@scripts/dayzero/launch_copy/linkedin.txt
@scripts/dayzero/launch_copy/reddit.txt
@scripts/dayzero/launch_copy/discord.txt
@scripts/launch/check_no_ai_slop.py
@scripts/launch/publish_social_posts.py
@scripts/launch/post_discord_launch.py
@docs/launch-prep/LAUNCH-SEQUENCE.md

<interfaces>
<!-- Subordinate publish scripts launch_trigger.sh shells out to. -->

From `scripts/launch/publish_social_posts.py` (Phase 36 / SHIP-03):
- Modes: `--dry-run` (default — webhook preview channel only) | `--real` (requires `LAUNCH_REAL=1` env).
- Reads copy per platform from `scripts/launch/social_templates/<platform>.txt.jinja`.
- Output: posts each rendered post to a Discord webhook preview unless `--real`.

From `scripts/launch/post_discord_launch.py` (Phase 36 / SHIP-04):
- Modes: `--dry-run` (default — DISCORD_PREVIEW_URL) | `--real` (requires `LAUNCH_REAL=1`).
- Renders the `ANNOUNCEMENT_TEMPLATE` with `release_url` + `role_mention`.

From `scripts/launch/check_no_ai_slop.py` (Phase 44-05):
- Accepts file paths as positional args; exit 0 = no slop tokens, exit 1 = found.
- Pattern: `python scripts/launch/check_no_ai_slop.py scripts/dayzero/launch_copy/*.txt`.

From `docs/launch-prep/LAUNCH-SEQUENCE.md` (Phase 44-07):
- 7 cadence stages: T-7, T-3, T-0 (= T+0 per CONTEXT §LAUNCH-08), T+24h, T+72h, T+7d, T+30.
- This plan's `--phase` accepts the 4 publish-touching stages from CONTEXT §SHIP-08:
  `T-30`, `T+0`, `T+5h`, `T+24h`. The other 3 (T-7 seed, T+72h Substack, T+7d transparency,
  T+30 SHIP-V1-DECISION) are NOT social-publish actions — they live in §LAUNCH-09 outreach
  + §SHIP-13 audit; this plan does not touch them.
</interfaces>

<cadence_schema>
<!-- scripts/dayzero/launch_copy/cadence_index.json — per-channel × per-stage matrix -->

```json
{
  "_doc": "vibemix SHIP-TWEET cadence matrix — Phase 45 SHIP-08. Per-channel × per-stage source file. launch_trigger.sh consumes this at runtime.",
  "version": 1,
  "stages": ["T-30", "T+0", "T+5h", "T+24h"],
  "channels": {
    "twitter":   {"T-30": "twitter.txt",   "T+0": "twitter.txt",   "T+5h": "twitter.txt",   "T+24h": "twitter.txt"},
    "instagram": {"T-30": "instagram.txt", "T+0": "instagram.txt", "T+5h": null,            "T+24h": "instagram.txt"},
    "linkedin":  {"T-30": null,            "T+0": "linkedin.txt",  "T+5h": null,            "T+24h": "linkedin.txt"},
    "reddit":    {"T-30": null,            "T+0": "reddit.txt",    "T+5h": null,            "T+24h": null},
    "discord":   {"T-30": "discord.txt",   "T+0": "discord.txt",   "T+5h": "discord.txt",   "T+24h": "discord.txt"}
  },
  "_routing": {
    "twitter|instagram|linkedin|reddit": "delegated to scripts/launch/publish_social_posts.py with --channel <name>",
    "discord": "delegated to scripts/launch/post_discord_launch.py"
  }
}
```

`null` = no publish for that channel × stage (per the LAUNCH-SEQUENCE.md cadence — e.g. Reddit only fires at T+0, not T+24h, to avoid karma-farming patterns).

`dist/launch-runs/<UTC>.jsonl` shape:

```jsonl
{"ts":"2026-05-17T11:04:22Z","stage":"T+0","channel":"twitter","mode":"dry-run","copy_file":"twitter.txt","status":"ok"}
{"ts":"2026-05-17T11:04:23Z","stage":"T+0","channel":"discord","mode":"dry-run","copy_file":"discord.txt","status":"ok"}
```
</cadence_schema>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin cadence_index.json schema + launch_trigger.sh CLI contract (RED)</name>
  <files>scripts/dayzero/launch_copy/cadence_index.json, scripts/launch/launch_trigger.sh, tests/launch/test_launch_trigger_orchestration.py</files>
  <behavior>
    - Test 1: `scripts/dayzero/launch_copy/cadence_index.json` exists, valid JSON, has `version: 1`, `stages` is the list `["T-30", "T+0", "T+5h", "T+24h"]` in that order.
    - Test 2: `channels` has exactly the 5 keys `twitter, instagram, linkedin, reddit, discord`; each value is a dict with all 4 stage keys; values are either a string (filename) or null.
    - Test 3: Every non-null `copy_file` in `cadence_index.json` corresponds to an existing file under `scripts/dayzero/launch_copy/` (the same 5 SHIP-TWEET copy files locked in Plan 44-05).
    - Test 4: `scripts/launch/launch_trigger.sh` exists + executable + `set -euo pipefail` + `bash -n` clean.
    - Test 5: `--help` exits 0, prints flag reference (`--phase`, `--live`, `--cadence-index`, `--copy-dir`, `--quiet`).
    - Test 6: Unknown flag → exit 2. Missing `--phase` → exit 2 with stderr `--phase required (one of: T-30, T+0, T+5h, T+24h)`. Invalid phase → exit 2 with stderr listing valid phases.
  </behavior>
  <action>Author `scripts/dayzero/launch_copy/cadence_index.json` from <cadence_schema> verbatim (the per-channel × per-stage matrix is the canonical LAUNCH-SEQUENCE cadence reduction). Stub `scripts/launch/launch_trigger.sh` with: `set -euo pipefail` header, `usage()` (`sed -n '1,35p'`), arg parser (`--phase`, `--live`, `--cadence-index PATH`, `--copy-dir PATH`, `--quiet`, `-h|--help`), phase validation against the literal allow-list. Write 6 pin tests. Tests MUST fail RED on missing flags / missing files / missing executable bit. Commit `test(45-02): pin launch_trigger.sh CLI + cadence_index.json schema (RED)`.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_launch_trigger_orchestration.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>Test file exists with 6 tests; JSON pinned to canonical 5-channel × 4-stage matrix; launch_trigger.sh skeleton + executable bit set; tests fail RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dry-run orchestration loop + zero-network pin (GREEN)</name>
  <files>scripts/launch/launch_trigger.sh, tests/launch/test_launch_trigger_orchestration.py</files>
  <behavior>
    - Test 7: `launch_trigger.sh --phase T+0` (no `--live`) — exit 0, stdout contains `[plan]` line per non-null channel for T+0 (twitter, instagram, linkedin, reddit, discord = 5 publishes). Discord routed through post_discord_launch; twitter/instagram/linkedin/reddit routed through publish_social_posts.
    - Test 8: `launch_trigger.sh --phase T-30` — only `twitter` + `instagram` + `discord` get `[plan]` lines (linkedin + reddit are null for T-30 per the matrix). Exit 0.
    - Test 9: `launch_trigger.sh --phase T+5h` — only `twitter` + `discord` get `[plan]` lines. Exit 0.
    - Test 10: `launch_trigger.sh --phase T+24h` — `twitter` + `instagram` + `linkedin` + `discord` (no reddit). Exit 0.
    - Test 11: Zero-network in dry-run: spawn the script under `http_proxy=http://127.0.0.1:1` (unreachable proxy); exit 0 still (proves no HTTP attempted). Also: subordinate scripts invoked with `--dry-run`, not `--real`, asserted via PATH-shim that records argv into a marker file (the shim is a python script writing the file then exit 0).
    - Test 12: `--live` without `LAUNCH_REAL=1` env → exit 2 with stderr `[launch_trigger] --live requires LAUNCH_REAL=1 env (matches publish_social_posts + post_discord_launch convention)`.
    - Test 13: Before any publish, the script invokes `scripts/launch/check_no_ai_slop.py` against the 5 SHIP-TWEET copy files; if that exits non-zero, launch_trigger aborts with exit 2 + stderr `[launch_trigger] AI-slop blocklist check failed — fix copy before publishing`. Tests use a PATH-shim of check_no_ai_slop.py simulating both pass + fail paths.
    - Test 14: Each `[plan]` line emitted appends an entry to `dist/launch-runs/<UTC>.jsonl` (UTC = process-start time, single file per invocation); the file uses one JSON object per line matching the <cadence_schema> shape.
  </behavior>
  <action>Implement the row loop in `scripts/launch/launch_trigger.sh`. Python heredoc loads the JSON, picks the row for `$STAGE`, iterates non-null channels. For each: log `[plan]` line + append JSONL line via Python heredoc (atomic-write friendly — open(jsonl, 'a')). When `--live` AND `LAUNCH_REAL=1`, exec the subordinate script with `--real`; else `--dry-run`. Invoke check_no_ai_slop on the 5 copy files at the very start (after arg parse, before the loop); on its non-zero exit, abort. UTC stamp = `date -u +%Y-%m-%dT%H-%M-%SZ`. Add 8 new tests; Tests 1–6 still green. Commit `feat(45-02): launch_trigger.sh dry-run orchestration + cadence routing + JSONL audit log (GREEN)`.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_launch_trigger_orchestration.py -v 2>&1 | tail -30 && bash scripts/launch/launch_trigger.sh --phase T+0 2>&1 | head -10; echo "exit=$?"</automated>
  </verify>
  <done>All 14 tests pass; default dry-run with --phase T+0 prints exactly 5 [plan] lines + writes JSONL audit; zero-network confirmed via unreachable-proxy test; check_no_ai_slop integrated as pre-publish gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: --live discharge contract + sign-off footer gate</name>
  <files>scripts/launch/launch_trigger.sh, tests/launch/test_launch_trigger_orchestration.py</files>
  <behavior>
    - Test 15: `--live --phase T+0` with `LAUNCH_REAL=1` env set BUT `GITHUB_TOKEN` unset → exit 2 with stderr `[launch_trigger] --live requires GITHUB_TOKEN env (KAAN-ACTION-LEGAL.md §SHIP-08)`.
    - Test 16: Same with `DISCORD_WEBHOOK_URL` unset → exit 2 with stderr `[launch_trigger] --live requires DISCORD_WEBHOOK_URL env (KAAN-ACTION-LEGAL.md §SHIP-08)`.
    - Test 17: Sign-off footer assertion: each copy file in `scripts/dayzero/launch_copy/*.txt` is checked for the locked Kaan + Francesco mutual sign-off footer (per Plan 44-05 lock). Footer regex: `Kaan.*Francesco|Francesco.*Kaan` present in each file. Missing on any → exit 2 with stderr `[launch_trigger] sign-off footer missing in <file> — Plan 44-05 contract broken`. If 44-05's lock-form differs (e.g. invisible HTML comment), the test fixture uses whatever check_no_ai_slop.py + the locked .txt files actually contain — Task 1 RED-time read of those files defines the literal regex.
    - Test 18: `--live --phase T+0` with ALL prerequisites met + shim subordinate scripts to assert: `publish_social_posts.py` called with `--real` for each non-null web channel; `post_discord_launch.py` called with `--real` for discord; JSONL audit row records `mode: "live"` not `"dry-run"`. Exit 0.
    - Test 19: GH Actions friendly: when `GITHUB_ACTIONS=true` env present, any failure also emits `::error::` annotation line to stdout per cut_release.sh / check_gate.sh convention.
  </behavior>
  <action>Add `--live` precondition checks (env presence) + sign-off footer regex (read each .txt at startup, abort if missing). Wire subordinate `--real` invocation when `--live` is on. Emit GH Actions `::error::` annotation lines on failure paths when `GITHUB_ACTIONS=true`. RED-time fixture peek: at Task 1 time, read each `scripts/dayzero/launch_copy/<file>.txt` to determine the literal sign-off footer pattern Plan 44-05 locked; pin that regex in the test (so future drift breaks the gate). Add 5 new tests; Tests 1–14 still pass. Commit `feat(45-02): --live discharge contract + Plan 44-05 sign-off footer gate (GREEN)`.</action>
  <verify>
    <automated>uv run pytest tests/launch/test_launch_trigger_orchestration.py -v 2>&1 | tail -25 && LAUNCH_REAL=1 bash scripts/launch/launch_trigger.sh --live --phase T+0 2>&1 | head -5; echo "exit=$?"</automated>
  </verify>
  <done>19 tests pass; --live requires LAUNCH_REAL=1 + GITHUB_TOKEN + DISCORD_WEBHOOK_URL; sign-off footer gate fires per Plan 44-05 lock; GH Actions ::error:: annotations emitted on failure. Plan 45-06 §SHIP-08 runbook will cite `LAUNCH_REAL=1 GITHUB_TOKEN=... DISCORD_WEBHOOK_URL=... bash scripts/launch/launch_trigger.sh --live --phase T+0` as the discharge command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launch_trigger.sh → cadence_index.json | JSON repo-tracked; rows define which copy file fires at which stage. |
| launch_trigger.sh → copy .txt files | Locked by Plan 44-05; check_no_ai_slop + sign-off footer gate enforce contract before any publish. |
| launch_trigger.sh → publish subordinates | When `--live`, subordinates ARE the network-touching layer; this script is the orchestrator + audit log. |
| dist/launch-runs/*.jsonl | Append-only audit log; consumed by Plan 45-04 SHIP-V1-DECISION audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Spoofing | --live without proper env | mitigate | Triple env check (LAUNCH_REAL + GITHUB_TOKEN + DISCORD_WEBHOOK_URL); each missing → exit 2 with named blocker. |
| T-45-02-02 | Tampering | cadence_index.json row swap (e.g. fire reddit at T-30 to spam) | mitigate | Test asserts canonical 5-channel × 4-stage matrix matches CONTEXT §SHIP-08 cadence; PR review surface. |
| T-45-02-03 | Injection | copy .txt contains a slop token introduced post-44-05 | mitigate | check_no_ai_slop pre-publish gate (subprocess check; non-zero aborts launch). |
| T-45-02-04 | Repudiation | "did we actually fire that stage?" | mitigate | JSONL audit log under dist/launch-runs/; Plan 45-04 SHIP-V1-DECISION reads this for evidence. |
| T-45-02-05 | DoS | accidental loop firing publishes multiple times | mitigate | Dry-run default; --live requires explicit env + flag; no auto-retry on subordinate failure (one-shot per invocation). |
| T-45-02-06 | Elevation of privilege | launch_trigger reads webhook URL from env | accept | Standard ops pattern; webhook URL = secret in GH; Plan 45-06 §SHIP-08 runbook documents the env source. |
</threat_model>

<verification>
- All 19 tests in `tests/launch/test_launch_trigger_orchestration.py` pass.
- `bash -n scripts/launch/launch_trigger.sh` exits 0.
- `bash scripts/launch/launch_trigger.sh --help` exits 0; usage references all 5 flags.
- `bash scripts/launch/launch_trigger.sh --phase T+0` exits 0 in dry-run, prints exactly 5 `[plan]` lines (twitter + instagram + linkedin + reddit + discord), writes 5 JSONL rows under `dist/launch-runs/<UTC>.jsonl`.
- `bash scripts/launch/launch_trigger.sh --phase T+5h` exits 0; prints exactly 2 `[plan]` lines (twitter + discord per matrix).
- `python3 -c 'import json; d=json.load(open("scripts/dayzero/launch_copy/cadence_index.json")); assert d["stages"]==["T-30","T+0","T+5h","T+24h"] and set(d["channels"])=={"twitter","instagram","linkedin","reddit","discord"}'` exits 0.
- `git ls-files --stage scripts/launch/launch_trigger.sh` shows `100755`.
- `LAUNCH_REAL=1 bash scripts/launch/launch_trigger.sh --live --phase T+0` (no GH_TOKEN) → exit 2 (autonomous safety holding).
</verification>

<success_criteria>
1. `scripts/launch/launch_trigger.sh` exists, dry-run default, cadence-aware, JSONL audit logged.
2. `cadence_index.json` matches CONTEXT §SHIP-08 cadence verbatim (canonical 5-channel × 4-stage matrix).
3. `--live` requires triple-env (LAUNCH_REAL + GITHUB_TOKEN + DISCORD_WEBHOOK_URL).
4. Plan 44-05 sign-off footer gate operational.
5. 19 tests in `tests/launch/test_launch_trigger_orchestration.py` GREEN; zero-network in CI.
6. Plan 45-04 SHIP-V1-DECISION audit can read `dist/launch-runs/*.jsonl` as authoritative publish evidence.
7. Plan 45-06 §SHIP-08 runbook cites the live invocation without further engineering.
</success_criteria>

<output>
After completion, create `.planning/phases/45-external-discharge-public-rc-publish/45-02-SUMMARY.md` documenting:
- Files shipped: launch_trigger.sh + cadence_index.json + test file
- 3 atomic commits (RED → GREEN-loop → GREEN-live)
- Decisions: dry-run default, cadence matrix authority, sign-off footer regex literal
- Plan 45-04 hand-off: JSONL audit log shape
- Plan 45-06 hand-off: literal live discharge command
</output>
