# Plan 31-06 — additive-layer.ts 3→4 marker + SkeletonHelper snapshot

**REQ:** MASCOT-26

## Goal
Add Phase-31 marker comments to `additive-layer.ts` documenting the 3→4 channel extension (without modifying its single-channel logic — that's the ADDITIVE invariant). SkeletonHelper visual regression test snapshot.

## Files
- MODIFY: `tauri/ui/src/mascot/additive-layer.ts` (doc comment only)
- NEW: `tauri/ui/src/mascot/__tests__/v2-1-skeleton-helper-snapshot.test.ts`

## Approach
`AdditiveLayer` itself stays a single-channel (anticipation) overlay on the same mixer. The "3 → 4 channel" view emerges at the composition level via PriorityStack + the new layer classes. We add an explicit marker comment showing where the 4-channel composition wires through, and a SkeletonHelper snapshot test that verifies the bone hierarchy is unchanged across the extension.

## Verification
- SkeletonHelper snapshot test asserts identical bone-tree shape pre/post the 4-layer wiring.
