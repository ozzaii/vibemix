# Plan 31-02 — Base layer + types.ts extensions

**REQ:** MASCOT-22

## Goal
Implement the always-on Base layer (priority 50, never canceled) and extend types.ts with `MascotEmotion` + `MascotReaction` unions for the emotion/reaction layers.

## Files
- NEW: `tauri/ui/src/mascot/layers/base.ts`
- NEW: `tauri/ui/src/mascot/layers/base.test.ts`
- MODIFY: `tauri/ui/src/mascot/types.ts` (ADDITIVE — new exports only)
- MODIFY: `tauri/ui/vitest.config.ts` (route `src/mascot/layers/*.test.ts` under jsdom)

## Contract
- `BaseLayer` class: wraps the existing `idle_breathe` action. `currentClip()` returns the active clip name. `play(now)` (default idle_breathe). Cancel = no-op.
- `MascotEmotion = "neutral" | "focused" | "hyped" | "concerned"` union.
- `MascotReaction = "wave" | "point_left" | "point_right" | "fist_pump" | "nod" | "headbang" | "surprised"` union — whitelist for emote_parser.

## Verification
- Test asserts BaseLayer.cancel() is a no-op (current state unchanged).
- Test asserts MASCOT_EMOTIONS and MASCOT_REACTIONS arrays are frozen (immutable).
