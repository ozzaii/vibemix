---
phase: 29-post-session-debrief-mvp-ui
plan: 02
type: execute
wave: 3
depends_on: ["29-00", "29-01", "29-03"]
files_modified:
  - src/vibemix/__main__.py
  - src/vibemix/debrief/main.py
  - src/vibemix/debrief/ws_server.py
  - src/vibemix/debrief/__init__.py
  - tests/debrief/test_ws_server_progressive_emit.py
  - tests/debrief/test_main_dispatch.py
  - tests/debrief/test_session_dir_path_traversal_rejected.py
autonomous: true
requirements:
  - DEBRIEF-08
  - DEBRIEF-09
tags: [debrief, sidecar, ipc, ws-bus, port-8766]

user_setup: []

must_haves:
  truths:
    - "`python -m vibemix --debrief <session_dir>` launches a websocket server on 127.0.0.1:8766 within 500ms of startup"
    - "Server emits ipc.debrief.session-loaded (immediate) → ipc.debrief.chapter-list (instant) → ipc.debrief.drills (~5s) → ipc.debrief.tldr-audio (~30s) in progressive order"
    - "Cache hit path: if session_debrief.json + debrief_tldr.mp3 exist with matching sha256, server emits all 4 messages within 1s (no Gemini calls)"
    - "Server canonicalizes session_dir + asserts it lives under recordings root; rejects path-traversal with ipc.debrief.error{reason=invalid_session_dir}"
    - "Server emits ipc.debrief.error{reason=events_missing|session_too_short} for the edge cases from session_loader and exits cleanly"
    - "Stdout/stderr log lines prefixed `[debrief]` so they're greppable in shared sidecar.log"
  artifacts:
    - path: "src/vibemix/__main__.py"
      provides: "_run_debrief_sidecar fills the Plan 25-03 banner-only no-op with a real call to debrief.main.run(session_dir)"
      contains: "debrief.main"
    - path: "src/vibemix/debrief/main.py"
      provides: "run(session_dir) — orchestrates load_session → cache-check → generate (chapters/drills/tldr) → ws_server.serve_forever"
      min_lines: 60
    - path: "src/vibemix/debrief/ws_server.py"
      provides: "Async WebSocket server on 127.0.0.1:8766; emits Phase 25 + new wrapper classes from Plan 03"
      min_lines: 80
  key_links:
    - from: "src/vibemix/__main__.py"
      to: "src/vibemix/debrief/main.py:run"
      via: "_run_debrief_sidecar dispatch (replacing Phase 25 banner)"
      pattern: "debrief\\.main\\.run"
    - from: "src/vibemix/debrief/main.py"
      to: "src/vibemix/debrief/{chapters,tldr,drills,stripper,persistence,session_loader}"
      via: "package imports"
      pattern: "from vibemix\\.debrief"
    - from: "src/vibemix/debrief/ws_server.py"
      to: "ws://127.0.0.1:8766"
      via: "websockets.serve()"
      pattern: "8766"
    - from: "src/vibemix/debrief/main.py"
      to: "validate_session_dir_under_recordings_root"
      via: "path canonicalization + prefix check (Python defense-in-depth alongside Rust validate_under_root)"
      pattern: "canonicalize|resolve"
---

<objective>
Replace Phase 25's banner-only `_run_debrief_sidecar` no-op in `__main__.py` with a real handler that orchestrates Plan 01 backend modules + serves a WS server on port 8766. Plan 03 defines the schemas this server emits; this plan is the runtime that produces them.

Purpose: Without this plan, the Tauri Rust shell (Plan 04) has nothing to spawn beyond a banner-printing process. This plan turns the Phase 25 architectural slot into a real sidecar.

Output: `--debrief` flag now produces a real WS server with progressive emit + cache hit + path-traversal defense; banner-only body removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-CONTEXT.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-RESEARCH.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-00-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-01-SUMMARY.md
@.planning/phases/29-post-session-debrief-mvp-ui/29-03-SUMMARY.md

@src/vibemix/__main__.py
@src/vibemix/ui_bus/schemas/debrief.py
@src/vibemix/ui_bus/messages.py

<interfaces>
<!-- Schemas from Plan 25-03 (already shipped — DO NOT MUTATE per P82) -->

From src/vibemix/ui_bus/schemas/debrief.py:
- `DebriefSessionLoadedPayload` — frozen-slotted dataclass, additive-only baseline
- `DebriefCitationSummaryPayload`
- `DebriefEventTimelinePayload`

From src/vibemix/ui_bus/messages.py:
- `DebriefSessionLoaded` / `DebriefCitationSummary` / `DebriefEventTimeline` wrapper classes — same additive-only constraint

<!-- New schemas from Plan 29-03 -->
- `DebriefChapterList(payload)` — emitted progressively, contains chapter regions for timeline render
- `DebriefTldrAudio(payload)` — points to `debrief_tldr.mp3` via relative path + tldr_sha256
- `DebriefDrills(payload)` — list of 3 SBI/STAR-AR drills
- `DebriefCitationTooltipReq(payload)` — request from renderer with event_id
- `DebriefCitationTooltip(payload)` — response with evidence_text + timestamp
- `DebriefError(payload)` — error envelope with reason code (events_missing | session_too_short | invalid_session_dir | sidecar_crashed | tldr_generation_failed | drills_generation_failed)

WS protocol:
- Server on `127.0.0.1:8766` (constant `DEBRIEF_PORT` already defined in __main__.py:199).
- Renderer connects + sends initial `{kind: "hello"}` frame; server replies with full debrief state (cache hit path) or kicks off progressive generation.
- Frames are JSON-encoded wrapper objects from `ui_bus/messages.py` (per existing port-8765 convention).
- Server accepts `{kind: "ipc.debrief.citation-tooltip-request", event_id}` and replies with `DebriefCitationTooltip` from in-memory evidence_snapshot.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: main.py orchestrator + path-traversal defense + cache-hit path</name>
  <files>src/vibemix/debrief/main.py, src/vibemix/debrief/__init__.py, src/vibemix/__main__.py, tests/debrief/test_main_dispatch.py, tests/debrief/test_session_dir_path_traversal_rejected.py</files>
  <behavior>
    - Test 1 (`test_main_dispatch.py`): `run(fixture_session_dir)` with cached debrief returns within 1s; calls `read_debrief` only (no Gemini calls — mock client raises if invoked).
    - Test 2: First-time run (no cache) calls chapters → drills → tldr in order, writes session_debrief.json + debrief_tldr.mp3, total time ≤ 60s with VCR cassette.
    - Test 3 (`test_session_dir_path_traversal_rejected.py`): `run(Path("../../etc/passwd"))` raises `InvalidSessionDir` BEFORE any file read — canonicalize via `Path.resolve(strict=True)` + assert under `~/Library/Application Support/vibemix/recordings/` (mac) or `%APPDATA%/vibemix/recordings/` (win).
    - Test 4: `_run_debrief_sidecar(args)` in `__main__.py` now dispatches to `debrief.main.run(args.debrief)` — no banner prints.
  </behavior>
  <action>
    Write tests first (RED).

    `src/vibemix/debrief/main.py`:
    1. `def run(session_dir: Path) -> None`:
       a. Canonicalize: `session_dir = Path(session_dir).resolve(strict=True)`. Compute `recordings_root` via existing platform-specific helper (look in `src/vibemix/runtime/paths.py` or define if missing — match the `$APPDATA/$APPLOCALDATA` paths used in `recordings.rs`).
       b. Path-traversal gate: `if not session_dir.is_relative_to(recordings_root): raise InvalidSessionDir(reason="invalid_session_dir")`.
       c. Try cache: `cached = read_debrief(session_dir)`. If present + valid, skip to step (e).
       d. First-time generation:
          - `events, evidence_snapshot, voice_meta = load_session(session_dir)` — raises SessionTooShort / EventsMissing.
          - `chapters = derive_chapters(events)`.
          - Build `cited_critique` from cited events + chapter summaries.
          - `drills = generate_drills(client, cited_critique, chapter_summaries, evidence_snapshot)`.
          - `tldr_mp3 = generate_tldr_mp3(chapter_summaries, cited_critique)`.
          - Build `debrief_dict = {chapters, drills, generated_at, ...}` and `write_debrief(session_dir, debrief_dict, tldr_mp3)`.
          - Run `strip_uncited_sentences` on any narration/critique text before persistence (defense-in-depth).
       e. Start ws_server with the assembled state (`chapters`, `drills`, `evidence_snapshot`, `voice_meta`, `tldr_mp3_path`). Block on `serve_forever()`.
    2. Errors from Plan 01's exceptions caught and surfaced via `ws_server.emit_error(reason, message)` THEN process exits with code 0 (the error is a normal IPC frame, not a crash). On unexpected exception, log `[debrief] CRASH: <repr>` to stderr and exit code 1 (Rust catches via dropped child).

    `src/vibemix/__main__.py`:
    1. Replace the existing `_run_debrief_sidecar(args)` body (Phase 25 banner-only) with:
       ```
       from vibemix.debrief.main import run as run_debrief
       run_debrief(Path(args.debrief))
       ```
       Keep the function signature + early-dispatch from Plan 00.
    2. Remove banner print + sleep loop.

    `src/vibemix/debrief/__init__.py`:
    Export `run`, `InvalidSessionDir` alongside Plan 01 exports.
  </action>
  <verify>
    <automated>pytest tests/debrief/test_main_dispatch.py tests/debrief/test_session_dir_path_traversal_rejected.py -x</automated>
  </verify>
  <done>main.py orchestrates load → cache → generate → ws_server.serve_forever; __main__.py dispatch points to it; path-traversal raises before any read; cache hit returns within 1s.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ws_server.py — async WebSocket server on port 8766 with progressive emit</name>
  <files>src/vibemix/debrief/ws_server.py, tests/debrief/test_ws_server_progressive_emit.py</files>
  <behavior>
    - Test 1: Server binds to 127.0.0.1:8766; a websocket client connects + receives `ipc.debrief.session-loaded` within 500ms.
    - Test 2: Progressive emit order: chapter-list → drills → tldr-audio messages arrive in that sequence on first-time run (use asyncio.Queue + mock generators).
    - Test 3: Citation-tooltip request → response round-trip: client sends `{kind: "ipc.debrief.citation-tooltip-request", event_id: "ev:DROP_HIT@01:23"}`, server replies with `DebriefCitationTooltip{evidence_text, timestamp}` from in-memory snapshot.
    - Test 4: Error path: `ws_server.emit_error("events_missing", "...")` → client receives `DebriefError` frame.
    - Test 5: Server respects `[debrief]` log prefix in all stdout output (capsys assertion).
  </behavior>
  <action>
    Write tests first (RED). Implement `ws_server.py`:

    1. `class DebriefWsServer`:
       - `__init__(self, host="127.0.0.1", port=8766, state: DebriefRuntimeState)` — state holds chapters/drills/evidence_snapshot/voice_meta/tldr_mp3_path + generation queue.
       - `async def handler(websocket)`:
         - On connect, emit `DebriefSessionLoaded` immediately.
         - Drain `state.emit_queue` (asyncio.Queue) — main.py pushes wrapper objects as generation steps complete; handler pulls + sends.
         - Listen for incoming frames; dispatch `citation-tooltip-request` → reply with `DebriefCitationTooltip` from `state.evidence_snapshot`.
       - `async def serve_forever()` — `async with websockets.serve(self.handler, host, port): await asyncio.Future()`.
       - `def emit(self, wrapper)` — synchronous puts onto async-safe queue.
       - `def emit_error(self, reason, message)` — emits `DebriefError`.
    2. main.py wires the queue: as soon as chapters derived → `ws.emit(DebriefChapterList(...))`; after drills → `ws.emit(DebriefDrills(...))`; after MP3 written → `ws.emit(DebriefTldrAudio(...))`.
    3. Use `websockets==16.0` (already shipped per STACK.md).
    4. Log every emit to stderr: `[debrief] emit <kind> <bytes> bytes`.
    5. Reuse Phase 25 wrapper-class serialization pattern (verify in `src/vibemix/ui_bus/messages.py`).
  </action>
  <verify>
    <automated>pytest tests/debrief/test_ws_server_progressive_emit.py -x</automated>
  </verify>
  <done>ws_server.py emits Phase 25 + Plan 03 wrappers in progressive order on port 8766; citation-tooltip round-trip works; `[debrief]` log prefix verified; 5 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→ws_server | Untrusted JSON frames cross this boundary; schema validation + allowlisted kinds gate dispatch. |
| __main__.py args.debrief→main.py | String arg from Rust spawn; capability validator (Plan 00) already constrained format, main.py canonicalizes again. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | session_dir path-traversal | mitigate | Canonicalize + assert under recordings_root via `Path.resolve(strict=True).is_relative_to(...)`. Test 3 covers. Defense-in-depth alongside Plan 04 Rust validate_under_root. |
| T-29-02-02 | Denial of Service | Renderer floods ws_server with citation-tooltip-request | mitigate | Rate-limit to 50 req/s per connection; drop excess with `[debrief] rate_limited` log. |
| T-29-02-03 | Spoofing | External process binds 8766 before sidecar | mitigate | `websockets.serve` raises OSError on bind conflict; main.py exits with `DebriefError(reason="port_in_use")` and code 1; Rust child detects + shows error banner. |
| T-29-02-04 | Information Disclosure | ws_server exposed to external network | mitigate | Bind to `127.0.0.1` only (not `0.0.0.0`); test asserts. |
| T-29-02-05 | Tampering | Frame with unknown `kind` triggers undefined behavior | mitigate | Allowlist dispatch — unknown kinds emit `DebriefError(reason="unknown_kind")` and continue serving. |
| T-29-02-06 | Repudiation | Sidecar crashes — Rust parent doesn't know why | mitigate | On unhandled exception, log `[debrief] CRASH: <traceback>` to stderr (Rust drains stderr into sidecar.log per Open Q #2 recommendation). |
</threat_model>

<verification>
- `pytest tests/debrief/ -x` — full backend test suite green.
- Manual smoke: `python -m vibemix --debrief recordings/20260515-112139/` runs without crash; `lsof -i :8766` shows binding; `wscat -c ws://127.0.0.1:8766` receives session-loaded frame.
- `ps aux | grep vibemix` — single sidecar PID; killing it cleans up port 8766.
</verification>

<success_criteria>
1. `python -m vibemix --debrief <real-session-dir>` produces a WS server on 127.0.0.1:8766 emitting Phase 25 + Plan 03 schemas in progressive order.
2. Cache hit path returns all data within 1 second (no Gemini calls).
3. Path-traversal session_dir rejected before any file read.
4. `pytest tests/debrief/test_ws_server_progressive_emit.py tests/debrief/test_main_dispatch.py tests/debrief/test_session_dir_path_traversal_rejected.py -x` exits 0.
5. `[debrief]` prefix on every stdout/stderr line — confirmed via capsys test.
6. `_run_debrief_sidecar` in `__main__.py` is no longer banner-only; full suite stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/29-post-session-debrief-mvp-ui/29-02-SUMMARY.md`
</output>
