# Plan 35-05 — Manifest Comment + Phase 22-02 Structural Contract + README Hero Block

**REQ-IDs:** ASSETS-03

## Goal

1. Document the placeholder status of the 5 `prep_*.glb` files via a top-level JSON comment in the manifest? — JSON has no comments. Use a sibling `PLACEHOLDER_NOTE.md` in `tauri/ui/assets/mascot/` instead.
2. Add Python-side structural contract test for Phase 22-02 idle-zero invariant (per-file existence + non-empty + TS test references all 5).
3. Update README with the hero block comment (PLACEHOLDER sentinel for now).

## Tasks

1. Create `tauri/ui/assets/mascot/animations/PLACEHOLDER_NOTE.md` — documents that the 5 prep_*.glb files are v2.0 placeholders to be replaced by Kaan-action ASSETS-PREP-REPLACE. Lists the Phase 22-02 idle-zero contract real GLBs must preserve.
2. Write `tests/repo/test_phase_22_02_prep_glb_contract.py`:
   - `test_all_5_prep_glb_files_exist` — enumerates the 5 expected paths.
   - `test_prep_glb_files_non_empty` — > 1 KB each (catches truncation, not bone validity).
   - `test_additive_layer_test_references_all_5_prep_states` — greps `additive-layer.test.ts` for all 5 prep_* state names.
3. Patch `README.md` — replace the placeholder GIF block with the new hero comment block carrying `sha256=PLACEHOLDER` sentinel + a `<picture>`/`<img>` fallback so the rendered README still shows the placeholder GIF until the real demo.mp4 lands.

## Verification

- `pytest tests/repo/test_phase_22_02_prep_glb_contract.py -v` → 3 pass.
- `python scripts/check_readme_hero_hash.py` → exit 0 (PLACEHOLDER sentinel).
- README still renders sensibly on GitHub (visual; placeholder GIF kept as fallback inside the block).

## Commit

`feat(35-05): prep_* placeholder note + Phase 22-02 contract test + README hero block`

