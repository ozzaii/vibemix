# Plan 35-02 — Demo Film Cut Driver

**REQ-IDs:** ASSETS-05
**Pitfall:** P57

## Goal

`scripts/demo_film/cut.sh` — ffmpeg manual cut driver. Reads `scripts/demo_film/cuts.json`, runs per-cut ffmpeg slice, concats to `docs/assets/demo.mp4`. NO auto-pacing. Cut count ≤ 8 enforced.

## Tasks

1. Create `scripts/demo_film/cuts.json` (template — empty cuts list, sentinel `source`).
2. Create `scripts/demo_film/cut.sh` — bash driver:
   - Read cuts.json via `jq` (fail with install hint if jq missing).
   - Validate cut count ≤ 8 (fail loud if exceeded).
   - Per cut: ffmpeg slice with stream-copy where possible.
   - Concat to `docs/assets/demo.mp4`.
   - `--dry-run` mode: print the ffmpeg commands without executing.
3. Create `scripts/demo_film/__init__.py` (empty — makes dir importable for tests).
4. Write `tests/scripts/test_demo_film_cuts.py`:
   - `test_cuts_under_8_in_template` — current cuts.json has ≤ 8.
   - `test_cuts_over_8_rejected` — write fixture with 9 cuts to tmpdir, invoke cut.sh --dry-run, expect exit 1.
   - `test_cut_script_exists_executable` — `cut.sh` is executable.

## Verification

- `pytest tests/scripts/test_demo_film_cuts.py -v` → 3 pass.
- `bash scripts/demo_film/cut.sh --dry-run` on template → exits cleanly (0 cuts is a no-op).

## Commit

`feat(35-02): scripts/demo_film/ — manual ffmpeg cut driver + ≤8 cuts gate`

