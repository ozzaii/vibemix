---
phase: 49-win-mac-one-click-installer-chain
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - .github/workflows/release.yml
  - scripts/audit/check_companion_signing.sh
  - scripts/dist/sign_macos.sh
  - scripts/dist/sign_windows.ps1
  - .planning/decisions/INSTALL-49-companion-fetch.md
  - tests/audit/test_companion_signing_gate.py
autonomous: true
requirements:
  - INSTALL-05
tags:
  - signing
  - release-pipeline
  - companion-sign
  - signpath

must_haves:
  truths:
    - "`.github/workflows/release.yml` gains a new `companion-sign` job slotted BETWEEN existing `build` and `sign` jobs; runs on macos-latest and windows-latest matrix; outputs signed artifacts as workflow artifacts named `companion-mac` and `companion-win`"
    - "Mac branch of companion-sign invokes `scripts/dist/sign_macos.sh --companion` (new flag) which codesigns every file under `installer/companion/*.{sh,py,json}` with the same Developer ID Application identity used for the main app bundle"
    - "Win branch of companion-sign invokes the existing SignPath GH Action (`signpath/github-action-submit-signing-request@<sha-pin>`) with a new `artifact-configuration-slug: companion-script` submission; manifest entry covers `installer/companion/fetch_drivers.ps1` and `installer/companion/audio_config.py` as separate signed artifacts"
    - "`scripts/audit/check_companion_signing.sh` is a NEW verifier: for each file in `installer/companion/*.{sh,py,ps1}`, asserts codesign signature (Mac: `codesign --verify --strict --deep`; Win: `Get-AuthenticodeSignature` status == Valid); runs at SIGN-stage gate AFTER companion-sign job; outputs report.json + exits non-zero on any unsigned"
    - "`scripts/dist/sign_macos.sh --companion` flag added (existing script preserved byte-identical when flag absent); new code path codesigns under same identity as main app, NO new entitlements"
    - "`scripts/dist/sign_windows.ps1` extended to accept `-Companion` switch that emits SignPath submission manifest snippet for the companion artifacts (consumed by the SignPath GH Action)"
    - "ADR `.planning/decisions/INSTALL-49-companion-fetch.md` documents: decision (post-install vendor fetch + companion-sign chain) + rationale (license + bundle ceiling) + integration plan (manifest schema + verify gate) + rollback path (revert release.yml job; remove companion artifacts)"
    - "`tests/audit/test_companion_signing_gate.py` validates the verifier script: on a tampered file (mock unsigned), the verifier exits non-zero; on a properly-signed file (mock), exits 0; on a `PLACEHOLDER_` SHA-256 in the manifest, emits a WARNING tagged as Kaan-action §INSTALL-COMPANION-SIGN (deferred at SignPath cert discharge)"
    - "Step-0 worktree-sync invariant included in any subagent prompts spawned by this plan: `cd <worktree> && git fetch origin main && git merge origin/main --no-edit`"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "companion-sign job slotted between build + sign + verifier gate"
      min_lines: 50
    - path: "scripts/audit/check_companion_signing.sh"
      provides: "Cross-platform companion signature verifier (codesign Mac / Authenticode Win)"
      min_lines: 60
    - path: "scripts/dist/sign_macos.sh"
      provides: "Extended with --companion flag (existing happy path preserved byte-identical)"
    - path: "scripts/dist/sign_windows.ps1"
      provides: "Extended with -Companion switch (SignPath submission manifest emitter)"
    - path: ".planning/decisions/INSTALL-49-companion-fetch.md"
      provides: "ADR for post-install vendor fetch + companion-sign chain"
      min_lines: 60
    - path: "tests/audit/test_companion_signing_gate.py"
      provides: "Verifier behavioral test (signed ok / unsigned fail / placeholder warning)"
      min_lines: 70
  key_links:
    - from: ".github/workflows/release.yml § companion-sign"
      to: "installer/companion/*"
      via: "signs every file under installer/companion/ before main SIGN job"
      pattern: "intermediate signing stage between build + sign"
    - from: "scripts/audit/check_companion_signing.sh"
      to: ".github/workflows/release.yml § verify"
      via: "called as a gate inside the existing verify job"
      pattern: "verifier gate fails publish on unsigned companion"
---

<objective>
Ship the `companion-sign` release.yml stage between BUILD + SIGN. Bravoh-codesigns every companion script (Mac codesign, Win Authenticode via SignPath OSS Foundation cert). Engineering ships the stage scaffold + verifier; Kaan discharges the actual SignPath cert at cert-approval time (Kaan-action §INSTALL-COMPANION-SIGN).

Out of scope: actual SignPath cert hand-off (Kaan-action), wizard UI integration (Plan 03), Inno Setup `[Run]` changes (Plan 04), VM matrix (Plan 05), anti-slop + uninstall (Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-win-mac-one-click-installer-chain/49-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.github/workflows/release.yml
@scripts/dist/sign_macos.sh
@scripts/dist/sign_windows.ps1
@scripts/dist/verify_signed.py

<interfaces>
release.yml currently has stages: build → sign → package → verify → publish. Phase 49 inserts `companion-sign` BETWEEN build and sign. The companion-sign job MUST run on the same matrix as build (macos-latest + windows-latest); failure blocks sign.

SignPath OSS Foundation cert (Kaan-action discharge): the existing GitHub Action `signpath/github-action-submit-signing-request@<sha-pin>` already covers the main Inno Setup .exe/.msi. For companion `.ps1` and `.py`, each artifact may need a separate submission per SignPath docs — verify at cert discharge time per CONTEXT.md canonical_refs §ARCHITECTURE.md § Open questions #2.

The verifier `scripts/audit/check_companion_signing.sh` MUST handle the PLACEHOLDER state cleanly: in CI on PR / dev branches where SignPath cert is not yet discharged, the verifier emits a WARNING but does NOT fail. Only on tag-push release builds does it fail-on-missing-signature. The branch detection uses `${GITHUB_REF_TYPE:-branch}` == "tag".

Step-0 worktree-sync invariant per memory `feedback_worktree_must_sync_main_first`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add companion-sign job to release.yml</name>
  <files>.github/workflows/release.yml</files>
  <action>
    Insert a new `companion-sign` job between the existing `build` and `sign` jobs in `.github/workflows/release.yml`:

    - `needs: [build]`
    - `strategy.matrix.os: [macos-latest, windows-latest]`
    - On macOS: run `bash scripts/dist/sign_macos.sh --companion`
    - On Windows: run `pwsh scripts/dist/sign_windows.ps1 -Companion` and then submit to SignPath via the existing pinned action (search for the existing `signpath/github-action-submit-signing-request` step and clone its `artifact-configuration-slug` pattern with the value `companion-script`)
    - Upload signed companion artifacts via `actions/upload-artifact@<existing-pin>` with names `companion-mac` and `companion-win`
    - The downstream `sign` job adds `needs: [build, companion-sign]` and downloads both companion artifacts before its existing logic
    - The downstream `verify` job adds a step that invokes `bash scripts/audit/check_companion_signing.sh` after existing verify-signed.py call

    PRESERVE all existing pins (action SHAs, container digests). Use Step-0 worktree-sync if executing in a worktree.
  </action>
  <check>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/release.yml')); jobs=d.get('jobs',{}); assert 'companion-sign' in jobs, list(jobs.keys()); print('ok')"</check>
  <on_check_fail>YAML structure validation; ensure indentation is preserved (release.yml is sensitive to it); re-run yaml.safe_load to catch syntax.</on_check_fail>
</task>

<task type="auto">
  <name>Task 2: Extend sign_macos.sh with --companion flag</name>
  <files>scripts/dist/sign_macos.sh</files>
  <action>
    Add `--companion` flag handler at the top of `scripts/dist/sign_macos.sh`. The new code path:
    - Reads `DEVELOPER_ID` from env (same source the main app code path uses)
    - For each file matching `installer/companion/*.{sh,py,json}`, runs `codesign --force --options runtime --sign "$DEVELOPER_ID" --timestamp <file>`
    - For `.json` files, codesigns the parent dir or use `--keychain` flag as appropriate (JSON files cannot be signed directly — use a detached signature `.sig` sibling file or include them in a `.tar.gz` that gets signed; document the chosen approach inline)
    - Emits structured log `{"file": "...", "signed": true, "identity": "..."}` per file
    - PRESERVE existing happy-path code byte-identical when `--companion` flag is absent
  </action>
  <check>bash -n scripts/dist/sign_macos.sh && grep -c "companion" scripts/dist/sign_macos.sh</check>
  <on_check_fail>Syntax error fix; if --companion code path conflicts with existing happy-path, wrap in case statement to ensure absent flag = unchanged behavior.</on_check_fail>
</task>

<task type="auto">
  <name>Task 3: Extend sign_windows.ps1 with -Companion switch</name>
  <files>scripts/dist/sign_windows.ps1</files>
  <action>
    Add `-Companion` switch at the top of `scripts/dist/sign_windows.ps1`. The new code path:
    - Emits a SignPath submission manifest snippet (JSON) listing `installer/companion/fetch_drivers.ps1` + `installer/companion/audio_config.py` as separate artifacts with `artifact-configuration-slug: companion-script`
    - Writes the manifest to `signpath-companion-manifest.json` in the repo root (gitignored)
    - PRESERVE existing happy-path code byte-identical when `-Companion` switch is absent
  </action>
  <check>powershell.exe -NoProfile -Command "& { . scripts/dist/sign_windows.ps1 -WhatIf 2>&1; exit 0 }" 2>/dev/null || python -c "p=open('scripts/dist/sign_windows.ps1').read(); assert 'Companion' in p; print('ok')"</check>
  <on_check_fail>Use python fallback if powershell unavailable; just assert text contains -Companion.</on_check_fail>
</task>

<task type="auto">
  <name>Task 4: Create check_companion_signing.sh verifier</name>
  <files>scripts/audit/check_companion_signing.sh</files>
  <action>
    Create executable bash script `scripts/audit/check_companion_signing.sh`:
    - `set -euo pipefail`
    - Detects platform via `uname -s` (Darwin / Linux for CI / Win-via-cygwin or skip)
    - For each `installer/companion/*.{sh,py,ps1}`:
      - On Darwin: run `codesign --verify --strict --deep <file>`; capture exit code + identity
      - On Linux (CI / Win analog): check sidecar `<file>.sig` exists AND has length > 0 (placeholder for SignPath check in non-Win CI)
    - Branch detection: if `${GITHUB_REF_TYPE:-branch}` == "tag", FAIL on any unsigned; else emit WARNING tagged §INSTALL-COMPANION-SIGN
    - Writes report to `report-companion-signing.json` with `{"signed": [...], "unsigned": [...], "warnings": [...], "exit_code": N}`
    - Exit code: 0 if all signed (or all warnings and not on tag-build), 1 otherwise
  </action>
  <check>bash -n scripts/audit/check_companion_signing.sh && bash scripts/audit/check_companion_signing.sh 2>&1 | tail -5</check>
  <on_check_fail>Syntax / shellcheck failures fix; ensure non-tag branch emits WARNING and exits 0.</on_check_fail>
</task>

<task type="auto">
  <name>Task 5: ADR INSTALL-49-companion-fetch.md</name>
  <files>.planning/decisions/INSTALL-49-companion-fetch.md</files>
  <action>
    Create ADR documenting:
    - **Status**: Accepted (2026-05-18)
    - **Context**: vibemix v3.1 needs one-click install. Driver redistribution legal blocker (VB-Audio EULA + BlackHole licensing). Bundle ceiling invariant (350 MB).
    - **Decision**: POST-install vendor fetch from official URLs + SHA-256 verify against `driver_manifest.json` + run vendor-signed installer. Companion scripts themselves Bravoh-codesigned via `companion-sign` release.yml stage between BUILD and SIGN.
    - **Consequences (+)**: license-clean, bundle ceiling preserved, vendor controls driver-signing chain, auditable manifest, Kaan-action discharge path is single point (SignPath cert).
    - **Consequences (-)**: requires network at install time (offline-fallback documented), Apple Silicon Reduced Security path still user-driven (Apple/MS constraint not addressable here).
    - **Integration plan**: Plan 49-01 → companion scripts. Plan 49-02 → signing stage + verifier (this plan). Plan 49-03 → wizard UI. Plan 49-04 → Inno Setup `[Run]` glue. Plan 49-05 → VM matrix. Plan 49-06 → anti-slop + uninstall.
    - **Rollback path**: revert release.yml job; remove `installer/companion/` dir; remove `tests/install/`; uninstall reverts to Phase 33 baseline (manual driver install path).
    - **Kaan-action surface**: §INSTALL-COMPANION-SIGN (SignPath cert discharge); §INSTALL-VM-RUN (real Tart run); §SHIP-CONTACT-VBAUDIO (OEM redistribution permission — future optimization).
  </action>
  <check>test -s .planning/decisions/INSTALL-49-companion-fetch.md && wc -l .planning/decisions/INSTALL-49-companion-fetch.md</check>
  <on_check_fail>Re-run if empty.</on_check_fail>
</task>

<task type="auto">
  <name>Task 6: test_companion_signing_gate.py</name>
  <files>tests/audit/test_companion_signing_gate.py</files>
  <action>
    Pytest covering:
    - `test_verifier_on_unsigned_file_fails_on_tag_build` — mock GITHUB_REF_TYPE=tag, mock codesign to fail, assert verifier exits non-zero
    - `test_verifier_on_unsigned_file_warns_on_branch_build` — mock GITHUB_REF_TYPE=branch, mock codesign to fail, assert verifier exits 0 with WARNING in report
    - `test_verifier_on_signed_file_exits_zero` — mock codesign to succeed, assert verifier exits 0
    - `test_placeholder_sha_in_manifest_emits_kaan_action_warning` — read driver_manifest.json, if any SHA-256 starts with `PLACEHOLDER_`, the verifier emits WARNING tagged with literal `§INSTALL-COMPANION-SIGN`
    - `test_report_json_shape` — assert `report-companion-signing.json` has `signed`, `unsigned`, `warnings`, `exit_code` keys

    Tests use subprocess.run to invoke the bash script with mocked environment.
  </action>
  <check>python -m pytest tests/audit/test_companion_signing_gate.py -v</check>
  <on_check_fail>Mock codesign via PATH manipulation; if tests cannot run on this dev host without codesign, skip with @pytest.mark.skipif(platform.system() != 'Darwin', ...).</on_check_fail>
</task>

</tasks>

<verification>
- `companion-sign` job present in release.yml; runs on both Mac + Win matrix
- `check_companion_signing.sh` exists and is executable
- `sign_macos.sh --companion` and `sign_windows.ps1 -Companion` both supported
- ADR exists at `.planning/decisions/INSTALL-49-companion-fetch.md`
- `pytest tests/audit/test_companion_signing_gate.py` passes
- Tag-vs-branch detection works (verified by tests)
</verification>

<commit>
gsd-sdk query commit "feat(49): companion-sign release stage + verifier gate (INSTALL-05)" --files .github/workflows/release.yml scripts/audit/check_companion_signing.sh scripts/dist/sign_macos.sh scripts/dist/sign_windows.ps1 .planning/decisions/INSTALL-49-companion-fetch.md tests/audit/test_companion_signing_gate.py
</commit>
