---
phase: 48-new-dep-integration-opportunity-scan
plan: 05
type: execute
wave: 4
depends_on: [48-02]
files_modified:
  - .planning/research/v3-buckets/v3.x-mixxx-osc.md
  - .planning/research/v3-buckets/v3.x-controller-map-transpiler.md
  - .planning/research/v3-buckets/v3.x-pyrekordbox-depth.md
  - .planning/research/v3-buckets/v3.x-beat-this.md
  - .planning/research/v3-buckets/v3.x-voicemeeter-banana.md
  - .planning/research/v3-buckets/v3.x-numark-hercules-controllers.md
  - .planning/research/v3-buckets/v3.x-macos-26-support.md
  - .planning/research/v3-buckets/v3.x-win11-24h2-wasapi.md
autonomous: true
requirements:
  - OPP-05
tags:
  - opportunity-scan
  - v3-buckets
  - yellow-defer

must_haves:
  truths:
    - "Each of the 8 yellow-defer DEP-OPP rows has a stub markdown file at .planning/research/v3-buckets/v3.x-<slug>.md"
    - "Every stub names the candidate, links back to docs/dep-opportunities/2026-05-scan.md row, names the re-eval trigger, names the relevant memory entry"
    - "All stubs pass anti-slop sibling checker if run against .planning/research/v3-buckets/"
  artifacts:
    - path: ".planning/research/v3-buckets/v3.x-mixxx-osc.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-controller-map-transpiler.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-pyrekordbox-depth.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-beat-this.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-voicemeeter-banana.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-numark-hercules-controllers.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-macos-26-support.md"
      min_lines: 15
    - path: ".planning/research/v3-buckets/v3.x-win11-24h2-wasapi.md"
      min_lines: 15
---

<objective>
Emit one Yellow-defer stub per yellow row in `scripts/audit/dep_ratings.yaml::opportunity_evaluations` (DEP-OPP-17..DEP-OPP-24). Per OPP-05, "deferred candidates (Yellow) carry forward into `.planning/research/v3-buckets/` for v3.x re-evaluation."

Each stub captures: candidate name, scan-row backlink, re-eval trigger conditions, originating memory entry, and brief context. Stubs are short — they exist so v3.x phases can resurface the candidate without re-discovering it.

Out of scope: verification rollup (Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-new-dep-integration-opportunity-scan/48-CONTEXT.md
@docs/dep-opportunities/2026-05-scan.md
@scripts/audit/dep_ratings.yaml
@.planning/research/STACK.md

<interfaces>
.planning/research/v3-buckets/ — directory already exists; established v3.x carry-forward surface. Existing tenants set the file-naming + section-shape convention (one stub per candidate; markdown front-matter optional).

Yellow-defer set (from Plan 48-02's opportunity_evaluations rows):
- DEP-OPP-17 → v3.x-mixxx-osc.md
- DEP-OPP-18 → v3.x-controller-map-transpiler.md
- DEP-OPP-19 → v3.x-pyrekordbox-depth.md
- DEP-OPP-20 → v3.x-beat-this.md
- DEP-OPP-21 → v3.x-voicemeeter-banana.md
- DEP-OPP-22 → v3.x-numark-hercules-controllers.md
- DEP-OPP-23 → v3.x-macos-26-support.md
- DEP-OPP-24 → v3.x-win11-24h2-wasapi.md

Step-0 worktree-sync invariant: `git fetch origin main && git merge origin/main --no-edit` before any edits if executing in a worktree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect v3-buckets directory + existing stub conventions</name>
  <files></files>
  <read_first>
    - .planning/research/v3-buckets/ (list contents; if any existing stubs are present, read one to confirm the file shape)
  </read_first>
  <action>
    Run:
    ```bash
    ls .planning/research/v3-buckets/ 2>/dev/null
    ```

    If existing stubs are present, read one to confirm the file naming + section conventions. If empty, use the schema below as the convention this plan establishes.

    Standard stub schema (used by all 8 files in Tasks 2-9):

    ```markdown
    # v3.x — <Candidate name>

    **Status:** Yellow-defer (carried forward from v3.1 opportunity scan)
    **Originating scan:** `docs/dep-opportunities/2026-05-scan.md` § <DEP-OPP-NN>
    **YAML row:** `scripts/audit/dep_ratings.yaml::opportunity_evaluations[id=<DEP-OPP-NN>]`
    **Memory anchor:** `<memory_entry_name>`
    **Date deferred:** 2026-05-18

    ## Why deferred

    <2-3 sentences explaining why this is yellow-defer, not green-adopt — typically install-impact, upstream readiness, or scope boundary.>

    ## Re-eval trigger

    <Concrete conditions that would flip this candidate from yellow to green: upstream landing a feature, a roadmap scope expansion, an external dependency hitting stable, etc.>

    ## Context

    <2-4 sentences with the relevant background a future v3.x phase would need to pick this up without re-research.>
    ```

    No file is written in this task — it's a discovery step that pins the schema.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; ls -la .planning/research/v3-buckets/ &amp;&amp; echo "OK directory exists"</automated>
  </verify>
  <done>v3-buckets directory confirmed; stub schema decided.</done>
</task>

<task type="auto">
  <name>Task 2: Write v3.x-mixxx-osc.md (DEP-OPP-17)</name>
  <files>.planning/research/v3-buckets/v3.x-mixxx-osc.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-17
    - .planning/research/STACK.md (search for mixxx-osc context)
  </read_first>
  <action>
    Write .planning/research/v3-buckets/v3.x-mixxx-osc.md:

    ```markdown
    # v3.x — Mixxx OSC controller adapter

    **Status:** Yellow-defer (carried forward from v3.1 opportunity scan)
    **Originating scan:** `docs/dep-opportunities/2026-05-scan.md` § DEP-OPP-17
    **YAML row:** `scripts/audit/dep_ratings.yaml::opportunity_evaluations[id=DEP-OPP-17]`
    **Memory anchor:** `project_v2_open_candidates`
    **Date deferred:** 2026-05-18

    ## Why deferred

    Mixxx upstream PR #14388 (OSC adapter) is still unmerged in the Mixxx mainline as of 2026-05-17 research. Adopting an unmerged upstream surface would either pin vibemix to a fork or carry a maintenance burden that the v3.1 distribution polish window cannot absorb.

    ## Re-eval trigger

    - Mixxx PR #14388 merges to mainline.
    - A vibemix v3.x phase explicitly takes on "Mixxx live-set integration" as a goal.
    - User demand from the Mixxx community surfaces in the GitHub issue tracker.

    ## Context

    Mixxx is open-source DJ software with a substantial controller-map library (the Mixxx-corpus-transpile path the controller-map-transpiler stub covers). An OSC adapter would let vibemix receive Mixxx state (deck loads, tempo, fader positions) without screen capture or audio analysis — a higher-fidelity grounding signal for the AI co-host. The pairing with `v3.x-controller-map-transpiler.md` is intentional: both surfaces unlock Mixxx-native vibemix sessions in tandem.
    ```
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-mixxx-osc.md &amp;&amp; grep -q "DEP-OPP-17" .planning/research/v3-buckets/v3.x-mixxx-osc.md &amp;&amp; grep -q "Re-eval trigger" .planning/research/v3-buckets/v3.x-mixxx-osc.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written, links back to scan row + yaml + memory anchor.</done>
</task>

<task type="auto">
  <name>Task 3: Write v3.x-controller-map-transpiler.md (DEP-OPP-18)</name>
  <files>.planning/research/v3-buckets/v3.x-controller-map-transpiler.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-18
  </read_first>
  <action>
    Write the stub using the standard schema. Key facts:

    - Candidate: Mixxx-corpus-transpile path. The Mixxx project ships hundreds of community-contributed controller maps; transpiling them into the vibemix controller library expands the supported hardware list without per-device manual work.
    - Why deferred: Gated on DEP-OPP-17 (Mixxx OSC) landing + a phase-scoped corpus selection.
    - Re-eval trigger: v3.x phase takes "controller library expansion" as an explicit goal.
    - Memory: `project_v2_open_candidates`

    File content follows the standard schema. Keep the body short (15-25 lines total).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-controller-map-transpiler.md &amp;&amp; grep -q "DEP-OPP-18" .planning/research/v3-buckets/v3.x-controller-map-transpiler.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 4: Write v3.x-pyrekordbox-depth.md (DEP-OPP-19)</name>
  <files>.planning/research/v3-buckets/v3.x-pyrekordbox-depth.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-19
    - pyproject.toml (verify current pyrekordbox pin + --no-deps recipe)
  </read_first>
  <action>
    Write the stub. Key facts:

    - Candidate: pyrekordbox depth extension — db6 reads (Rekordbox 6 binary library DB) via SQLCipher, plus richer ANLZ binary parsing.
    - Why deferred: Current Phase 25 pin is shallow XML parsing only (`--no-deps` recipe + dormant SQLCipher per CONTEXT D-01). Going deep means re-activating SQLCipher path + maybe shipping a key derivation routine — install-impact red. Not justified for v3.1 distribution polish.
    - Re-eval trigger: v3.x phase wants per-track waveform color cues, hot-cue timing, or beatgrid pulls from the live Rekordbox library.
    - Memory: `project_v2_open_candidates`

    Keep the body short (15-25 lines total).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-pyrekordbox-depth.md &amp;&amp; grep -q "DEP-OPP-19" .planning/research/v3-buckets/v3.x-pyrekordbox-depth.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 5: Write v3.x-beat-this.md (DEP-OPP-20)</name>
  <files>.planning/research/v3-buckets/v3.x-beat-this.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-20
  </read_first>
  <action>
    Write the stub using the standard schema. Key facts:

    - Candidate: Beat This! Rust beat-grid verifier sidecar — high-accuracy BPM + beat-grid detection that supplements the current numpy autocorr approach.
    - Why deferred: Non-Gemini grounding path (CPU-side DSP). Adds Rust crate weight to the bundle; needs separate phase for install-size budget analysis per memory `project_v2_open_candidates`. v3.1 is hardening, not stack widening.
    - Re-eval trigger: A v3.x phase pinpoints a hallucination class that the current numpy autocorr BPM cannot close.
    - Memory: `project_v2_open_candidates`

    Keep the body short (15-25 lines).
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-beat-this.md &amp;&amp; grep -q "DEP-OPP-20" .planning/research/v3-buckets/v3.x-beat-this.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 6: Write v3.x-voicemeeter-banana.md (DEP-OPP-21)</name>
  <files>.planning/research/v3-buckets/v3.x-voicemeeter-banana.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-21
  </read_first>
  <action>
    Write the stub. Key facts:

    - Candidate: Voicemeeter Banana — VB-Audio's advanced virtual-mixer for Windows, donationware tier above VB-CABLE. Adds multi-bus routing for streamers running multiple audio sources.
    - Why deferred: Free VB-CABLE covers the v3.1 install path. Banana is an advanced-user tier that does not need installer bundling; documented as an optional wizard path only.
    - Re-eval trigger: User feedback shows demand for multi-source routing on Windows that VB-CABLE alone cannot serve.
    - Memory: `project_v2_open_candidates`

    Keep the body short.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-voicemeeter-banana.md &amp;&amp; grep -q "DEP-OPP-21" .planning/research/v3-buckets/v3.x-voicemeeter-banana.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 7: Write v3.x-numark-hercules-controllers.md (DEP-OPP-22)</name>
  <files>.planning/research/v3-buckets/v3.x-numark-hercules-controllers.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-22
  </read_first>
  <action>
    Write the stub. Key facts:

    - Candidate: Numark / Hercules controller MIDI maps — library expansion beyond the v3.1 baseline 10-controller library.
    - Why deferred: Not a new dep; just JSON map files. The path to expansion is the controller-map-transpiler (DEP-OPP-18). v3.1 keeps the controller library at 10 per memory `project_v2_open_candidates`.
    - Re-eval trigger: DEP-OPP-17 + DEP-OPP-18 land + a v3.x phase explicitly takes "library expansion" as a goal.
    - Memory: `project_v2_open_candidates`

    Keep the body short.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-numark-hercules-controllers.md &amp;&amp; grep -q "DEP-OPP-22" .planning/research/v3-buckets/v3.x-numark-hercules-controllers.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 8: Write v3.x-macos-26-support.md (DEP-OPP-23)</name>
  <files>.planning/research/v3-buckets/v3.x-macos-26-support.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-23
  </read_first>
  <action>
    Write the stub. Key facts:

    - Candidate: macOS 26+ (Apple's next-major OS post-v3.1) verification.
    - Why deferred: Apple has not shipped the next-major OS as of v3.1 close. Phase 33 v2.1 ladder covers 12.3 / 14 / 15; Phase 50 SHIP-04 fresh-VM matrix targets those.
    - Re-eval trigger: Apple ships the next-major OS publicly; vibemix needs SHIP-04 matrix expansion.
    - Memory: none direct (project-scope decision per .planning/research/STACK.md § Bucket 3 candidates audited table).

    Keep the body short.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-macos-26-support.md &amp;&amp; grep -q "DEP-OPP-23" .planning/research/v3-buckets/v3.x-macos-26-support.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 9: Write v3.x-win11-24h2-wasapi.md (DEP-OPP-24)</name>
  <files>.planning/research/v3-buckets/v3.x-win11-24h2-wasapi.md</files>
  <read_first>
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-24
  </read_first>
  <action>
    Write the stub. Key facts:

    - Candidate: Win11 24H2 WASAPI verification — spot-check that the existing PyAudioWPatch path handles Win11 24H2's WASAPI loopback shape.
    - Why deferred: Win11 24H2 has not hit stable as of 2026-05. The existing PyAudioWPatch pin handles WASAPI loopback per Phase 7; defer verification to a v3.x phase once 24H2 is broadly deployed.
    - Re-eval trigger: Win11 24H2 hits stable + a vibemix user reports loopback issues.
    - Memory: none direct (project-scope decision per .planning/research/STACK.md § Bucket 3).

    Keep the body short.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/research/v3-buckets/v3.x-win11-24h2-wasapi.md &amp;&amp; grep -q "DEP-OPP-24" .planning/research/v3-buckets/v3.x-win11-24h2-wasapi.md &amp;&amp; echo "OK"</automated>
  </verify>
  <done>Stub written.</done>
</task>

<task type="auto">
  <name>Task 10: Verify all 8 stubs land + pass anti-slop check</name>
  <files></files>
  <read_first>
    - .planning/research/v3-buckets/ (verify all 8 files present)
    - scripts/audit/check_no_slop_opp.py (Plan 48-03 sibling)
  </read_first>
  <action>
    Run a sweep:

    ```bash
    # All 8 stubs present
    for slug in mixxx-osc controller-map-transpiler pyrekordbox-depth beat-this voicemeeter-banana numark-hercules-controllers macos-26-support win11-24h2-wasapi; do
      test -f ".planning/research/v3-buckets/v3.x-${slug}.md" || { echo "MISSING: $slug"; exit 1; }
    done
    echo "all 8 stubs present"

    # Anti-slop sweep on the new directory (sibling checker accepts --dir)
    uv run python scripts/audit/check_no_slop_opp.py --dir .planning/research/v3-buckets
    ```
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; for slug in mixxx-osc controller-map-transpiler pyrekordbox-depth beat-this voicemeeter-banana numark-hercules-controllers macos-26-support win11-24h2-wasapi; do test -f ".planning/research/v3-buckets/v3.x-${slug}.md" || { echo "MISSING: $slug"; exit 1; }; done &amp;&amp; uv run python scripts/audit/check_no_slop_opp.py --dir .planning/research/v3-buckets &amp;&amp; echo "OK 8 stubs + anti-slop clean"</automated>
  </verify>
  <done>All 8 Yellow-defer stubs present in .planning/research/v3-buckets/; anti-slop sibling sweep on the directory exits 0.</done>
</task>

</tasks>

<verification>
- [ ] 8 Yellow-defer stubs at .planning/research/v3-buckets/v3.x-<slug>.md for DEP-OPP-17 through DEP-OPP-24
- [ ] Each stub names: candidate, scan-row backlink, yaml-row reference, memory anchor, re-eval trigger, brief context
- [ ] scripts/audit/check_no_slop_opp.py --dir .planning/research/v3-buckets exits 0
- [ ] POC files untouched (cohost*.py, mascot.html, run*.sh)
- [ ] No new gemini-* model literals in stubs
</verification>

<commit_message>
feat(48-05): Yellow-defer stubs carried forward to v3-buckets (OPP-05)

8 Yellow-defer DEP-OPP rows from the v3.1 opportunity scan land as
short stubs at .planning/research/v3-buckets/v3.x-<slug>.md:

- v3.x-mixxx-osc.md (DEP-OPP-17)
- v3.x-controller-map-transpiler.md (DEP-OPP-18)
- v3.x-pyrekordbox-depth.md (DEP-OPP-19)
- v3.x-beat-this.md (DEP-OPP-20)
- v3.x-voicemeeter-banana.md (DEP-OPP-21)
- v3.x-numark-hercules-controllers.md (DEP-OPP-22)
- v3.x-macos-26-support.md (DEP-OPP-23)
- v3.x-win11-24h2-wasapi.md (DEP-OPP-24)

Each stub names the candidate, scan-row backlink, yaml-row reference,
memory anchor, re-eval trigger, and brief context. Future v3.x phases
can resurface any of these without re-discovering them.

Anti-slop sibling sweep on the new directory passes.

Refs OPP-05, CONTEXT Decision 7.
</commit_message>
