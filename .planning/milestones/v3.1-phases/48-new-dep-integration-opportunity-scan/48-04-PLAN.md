---
phase: 48-new-dep-integration-opportunity-scan
plan: 04
type: execute
wave: 4
depends_on: [48-03]
files_modified:
  - .planning/decisions/DEP-OPP-01-obs-browser-source.md
  - docs/integrations/obs-browser-source.md
  - README.md
autonomous: true
requirements:
  - OPP-04
  - OPP-06
tags:
  - opportunity-scan
  - adr
  - obs
  - integration-docs

must_haves:
  truths:
    - ".planning/decisions/DEP-OPP-01-obs-browser-source.md captures decision + rationale + integration plan + rollback path"
    - "docs/integrations/obs-browser-source.md gives step-by-step OBS Studio setup using the existing Tauri webview port 8765 + mascot bus"
    - "README.md cross-links to docs/integrations/obs-browser-source.md from an appropriate Streaming / Integrations anchor"
    - "Zero new runtime code, zero new IPC wrappers, zero new Tauri webview routes (mascot route already exists per v3.0)"
    - "All three documents pass anti-slop blocklist (15-token + \\bdeeply\\s+\\w+); scripts/audit/check_no_slop_opp.py exits 0 against docs/dep-opportunities/ AND a sibling sweep would clear docs/integrations/"
    - "scripts/audit/scan_opportunities.py ADR-existence gate passes once the ADR file lands"
  artifacts:
    - path: ".planning/decisions/DEP-OPP-01-obs-browser-source.md"
      provides: "ADR sidecar for the only green-adopt outcome in v3.1 opp-scan"
      min_lines: 60
    - path: "docs/integrations/obs-browser-source.md"
      provides: "Integration doc — OBS Studio setup using existing mascot bus"
      min_lines: 80
  key_links:
    - from: ".planning/decisions/DEP-OPP-01-obs-browser-source.md"
      to: "scripts/audit/dep_ratings.yaml::opportunity_evaluations[id=DEP-OPP-01]"
      via: "adr_sidecar field points to this file"
      pattern: ".planning/decisions/DEP-OPP-01-obs-browser-source.md"
    - from: "README.md"
      to: "docs/integrations/obs-browser-source.md"
      via: "cross-link in Streaming / Integrations section"
      pattern: "docs/integrations/obs-browser-source.md"
---

<objective>
Land the only green-adopt outcome from the opp-scan: OBS browser-source as a docs-only mascot integration path per CONTEXT Decision 6 + OPP-06. Three deliverables:

1. **ADR** at `.planning/decisions/DEP-OPP-01-obs-browser-source.md` — referenced by `scripts/audit/dep_ratings.yaml::opportunity_evaluations[id=DEP-OPP-01].adr_sidecar`; Plan 48-03's ADR-existence gate fails if this file is missing.
2. **Integration doc** at `docs/integrations/obs-browser-source.md` — step-by-step OBS Studio setup.
3. **README cross-link** so streamers discover the path.

Zero code changes. Tauri webview port 8765 + mascot bus + Three.js canvas already serve per v3.0 baseline — OBS browser-source plugs into the same surface.

Out of scope: Yellow stubs (Plan 05), verification (Plan 06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-new-dep-integration-opportunity-scan/48-CONTEXT.md
@.planning/decisions/P85-OVERRIDE-RETIRED.md
@docs/dep-opportunities/2026-05-scan.md
@scripts/audit/dep_ratings.yaml
@README.md
@scripts/audit/check_no_slop_opp.py
@scripts/launch/check_no_ai_slop.py

<interfaces>
.planning/decisions/P85-OVERRIDE-RETIRED.md — existing ADR establishes the ADR file conventions for this project. Mimic its front-matter + section structure where it makes sense; do not invent a new format.

scripts/audit/dep_ratings.yaml::opportunity_evaluations[0] — already has `adr_sidecar: ".planning/decisions/DEP-OPP-01-obs-browser-source.md"`. The path must MATCH EXACTLY (Plan 48-03 ADR gate is path-strict).

Mascot bus contract per v3.0 baseline:
- WebSocket server bound to `127.0.0.1:8765`
- Existing route `/mascot` serves the Three.js canvas (per CLAUDE.md project conventions section + Phase 31 4-layer state machine work)
- Three.js renders a single VTuber-style mascot per memory `project_mascot_as_vtuber_personality_surface`

README.md — find an appropriate anchor section. If "Integrations" or "Streaming" doesn't exist, add a short section. Do NOT rewrite README structure; surgical insertion only. Preserve all existing anti-slop + hero-lock invariants.

Anti-slop discipline: avoid the 16-token blocklist (see scripts/launch/check_no_ai_slop.py for the full list) — common offenders: seamless / effortless / delve / tapestry / cutting-edge / revolutionize / unleash / harness / journey / realm / meticulous / navigate / landscape / robust / leverage. Banned regex: `\bdeeply\s+\w+` (e.g. "deeply integrated"). Write straightforward technical prose.

Step-0 worktree-sync invariant: `git fetch origin main && git merge origin/main --no-edit` before any edits if executing in a worktree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR at .planning/decisions/DEP-OPP-01-obs-browser-source.md</name>
  <files>.planning/decisions/DEP-OPP-01-obs-browser-source.md</files>
  <read_first>
    - .planning/decisions/P85-OVERRIDE-RETIRED.md (first 30 lines — file conventions)
    - .planning/phases/48-new-dep-integration-opportunity-scan/48-CONTEXT.md § Decision 6 (full OBS adoption spec)
    - docs/dep-opportunities/2026-05-scan.md (the DEP-OPP-01 row + rationale)
  </read_first>
  <action>
    Write .planning/decisions/DEP-OPP-01-obs-browser-source.md:

    ```markdown
    # DEP-OPP-01 — Adopt OBS browser-source as docs-only mascot integration

    **Status:** Accepted
    **Date:** 2026-05-18
    **Phase:** 48 — New-Dep + Integration Opportunity Scan
    **Requirement:** OPP-06
    **Scan row:** `docs/dep-opportunities/2026-05-scan.md` § DEP-OPP-01
    **YAML row:** `scripts/audit/dep_ratings.yaml::opportunity_evaluations[id=DEP-OPP-01]`

    ## Context

    The v3.1 opportunity scan (Phase 48) identified one candidate that lands in v3.1 as a Green-adopt outcome: routing the existing Tauri webview mascot canvas into OBS Studio via its built-in browser-source plugin. Streamers and content creators commonly composite a virtual avatar overlay on top of camera feeds and screen captures — vibemix already renders that mascot, and OBS already knows how to capture a local webview. The integration is a documentation problem, not an engineering problem.

    The v3.0 baseline ships:

    - A Tauri webview hosting the mascot scene (Three.js + GLB clips), served alongside the main app surface.
    - A local WebSocket server bound to `127.0.0.1:8765` (the mascot bus) that broadcasts music / voice / mic levels at 30 Hz.
    - A four-layer additive state machine driving 23 Mixamo-retargeted GLB clips per Phase 47.

    OBS Studio's browser-source plugin loads a URL inside a Chromium Embedded Framework instance and renders the page as an overlay layer in any OBS scene. Pointing it at the existing mascot route lets the captured canvas land in the OBS scene without any vibemix-side code.

    ## Decision

    Adopt OBS browser-source as a **documentation-only** integration path. No new runtime code, no new IPC wrappers, no new Tauri webview routes. Ship two documents:

    - `docs/integrations/obs-browser-source.md` — step-by-step OBS Studio setup with troubleshooting.
    - A cross-link in `README.md` under the Streaming / Integrations anchor so streamers discover the path during first-run scan.

    The mascot WS bus stays bound to `127.0.0.1` — the OBS integration does NOT expose the bus to the network. OBS Studio and vibemix are expected to run on the same machine.

    ## Rationale

    1. **Zero install-time cost.** No new package dependency, no new bundle weight, no new permission prompt at first launch. Phase 49's installer companion already pulls BlackHole / VB-CABLE; this adoption adds nothing to that chain.
    2. **Zero runtime risk.** The mascot bus + Tauri webview are already on the stable production path per v3.0 ship. Adding documentation cannot regress that surface.
    3. **High audience value.** vibemix's mascot is the visual personality surface per memory `project_mascot_as_vtuber_personality_surface`; making it landable in OBS scenes is exactly the kind of multiplier streamers ask for.
    4. **No new IPC wrappers.** The 38-wrapper IPC schema is frozen per v3.1 invariants; an integration that needs zero IPC fits cleanly.
    5. **Reversible.** Removing this adoption means deleting two markdown files plus a README paragraph — no code revert, no migration.

    ## Integration plan

    1. Streamer installs vibemix per Phase 49 installer chain.
    2. vibemix's webview surfaces the mascot scene at the v3.0 baseline route (the same route the standalone app uses internally).
    3. Streamer adds a new OBS source of type "Browser" with the URL pointing to the local mascot route and a transparent background.
    4. The mascot canvas renders inside the OBS scene as an alpha-compositable overlay.
    5. Audio cue routing into vibemix continues to drive the same mascot per the existing mascot-bus contract.

    ## Rollback path

    Remove three artifacts:

    - Delete `docs/integrations/obs-browser-source.md`.
    - Delete the OBS cross-link paragraph in `README.md`.
    - Delete this ADR file.

    Optionally flip the YAML row's `rating` from `green-adopt` to `yellow-defer` and add a `rejected_constraints[]` entry citing the rollback rationale. No code revert is needed.

    ## Consequences

    Positive:

    - Streamers gain a first-class avatar-in-OBS path with zero install friction.
    - vibemix's mascot becomes the canonical visual hook for vibemix screencasts and tutorials.

    Negative / accepted risk:

    - First-time OBS users may need extra guidance on browser-source aspect ratio + alpha channel. The integration doc covers the common pitfalls.
    - If OBS Studio changes the browser-source plugin's URL scheme or revokes localhost access, the doc needs an update; this is upstream-OBS risk, not vibemix risk.

    ## References

    - `.planning/REQUIREMENTS.md` § OPP-06
    - `.planning/ROADMAP.md` § Phase 48
    - `.planning/phases/48-new-dep-integration-opportunity-scan/48-CONTEXT.md` § Decision 6
    - `docs/dep-opportunities/2026-05-scan.md` § DEP-OPP-01
    - `docs/integrations/obs-browser-source.md`
    - Memory: `project_mascot_as_vtuber_personality_surface`
    ```

    Anti-slop check before save: re-read your draft and rewrite any sentence that uses one of the blocklist tokens or the `\bdeeply\s+\w+` pattern. The text above is written to pass the gate; spot-check before commit.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f .planning/decisions/DEP-OPP-01-obs-browser-source.md &amp;&amp; grep -q "DEP-OPP-01" .planning/decisions/DEP-OPP-01-obs-browser-source.md &amp;&amp; grep -q "Status:" .planning/decisions/DEP-OPP-01-obs-browser-source.md &amp;&amp; grep -q "## Decision" .planning/decisions/DEP-OPP-01-obs-browser-source.md &amp;&amp; grep -q "## Rollback path" .planning/decisions/DEP-OPP-01-obs-browser-source.md &amp;&amp; grep -q "8765" .planning/decisions/DEP-OPP-01-obs-browser-source.md &amp;&amp; uv run python scripts/audit/scan_opportunities.py</automated>
  </verify>
  <done>ADR file exists with required sections (Context / Decision / Rationale / Integration plan / Rollback / Consequences / References) + scan_opportunities.py ADR-existence gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/integrations/obs-browser-source.md</name>
  <files>docs/integrations/obs-browser-source.md</files>
  <read_first>
    - .planning/decisions/DEP-OPP-01-obs-browser-source.md (just authored)
    - docs/dep-opportunities/2026-05-scan.md § DEP-OPP-01 (rationale section)
    - README.md (read the existing structure — find where the cross-link will land in Task 3)
  </read_first>
  <action>
    Create the directory `docs/integrations/` if it does not exist. Write docs/integrations/obs-browser-source.md:

    ```markdown
    # OBS Studio — vibemix mascot browser-source integration

    Pipe vibemix's VTuber-style mascot directly into an OBS Studio scene as a transparent overlay. The mascot reacts to your audio cues in real time and renders inside OBS without any extra runtime hops.

    > **Status:** Production. Lands in v3.1 per ADR `.planning/decisions/DEP-OPP-01-obs-browser-source.md`.

    ## What you get

    - A live mascot canvas composited on top of your camera or screen capture.
    - Alpha-channel support — the mascot's background is transparent.
    - Real-time reactions driven by the same mascot bus the standalone app uses.

    ## Prerequisites

    - vibemix installed per the README quick-start.
    - OBS Studio 28 or newer (the browser-source plugin is built in).
    - vibemix and OBS running on the same machine.

    ## Setup steps

    1. **Launch vibemix.** Start a live session as usual. The mascot surface starts serving once the app is running.

    2. **Note the mascot route.** vibemix exposes the mascot canvas at `http://127.0.0.1:8765/mascot` (the local mascot bus + webview route). Both vibemix and OBS must run on the same host.

    3. **Add a Browser source in OBS.**
       - In OBS, click `+` under Sources and pick `Browser`.
       - Name the source (e.g. `vibemix mascot`).
       - URL: `http://127.0.0.1:8765/mascot`
       - Width: 1280, Height: 720 (these match the mascot canvas aspect; adjust to fit your scene)
       - Check `Refresh browser when scene becomes active`.

    4. **Set a transparent background.** Under the Browser source properties, the mascot canvas is alpha-aware — leave the default CSS field empty. If you see a black background, tick `Use custom CSS` and clear any imported defaults.

    5. **Resize and position.** Drag the source in the OBS canvas to your preferred corner (lower-right is a common streamer choice). The mascot scales without aliasing.

    6. **Confirm it reacts.** Start playing audio into vibemix. The mascot in OBS should respond to track changes, phase shifts, and mix moves at the same cadence as the standalone vibemix surface.

    ## Troubleshooting

    | Symptom | Fix |
    |---|---|
    | OBS shows a blank source. | Check vibemix is running; the mascot route only serves while the app is live. |
    | The background is opaque black. | Set the Browser source's custom CSS field to empty; OBS sometimes injects a default body background. |
    | The mascot is frozen. | The mascot bus may have stopped emitting — restart vibemix. Reactions resume on the next event. |
    | Aspect ratio looks squashed. | Match the OBS Browser source resolution to the mascot canvas aspect (16:9 by default). |
    | Frame rate drops in OBS. | Reduce the Browser source FPS cap to 30 in OBS source properties — the mascot does not need 60 fps inside a scene composite. |

    ## Privacy + network notes

    - The mascot WS bus binds to `127.0.0.1` only. The OBS integration does NOT expose vibemix to the network.
    - OBS Studio must run on the same machine as vibemix. Remote-host setups would require firewall rules vibemix does not configure on your behalf.
    - No audio is sent to OBS via this integration — the Browser source captures the visual canvas only. Audio routing remains your existing BlackHole / VB-CABLE chain.

    ## Limitations

    - This integration is canvas-only. There is no two-way control (OBS scene change does not toggle vibemix state, and vice versa).
    - Multi-monitor screen captures inside OBS may conflict with the mascot's compositing on macOS Sonoma+; if you see flicker, isolate the mascot to a dedicated OBS scene.
    - If you run an extreme multi-PC setup with OBS on a separate capture box, you need a different integration (NDI, Spout) that vibemix does not ship.

    ## References

    - ADR: `.planning/decisions/DEP-OPP-01-obs-browser-source.md`
    - Scan row: `docs/dep-opportunities/2026-05-scan.md` § DEP-OPP-01
    - Memory: `project_mascot_as_vtuber_personality_surface`
    ```

    Anti-slop check before save: rewrite any sentence containing a blocklist token or the `\bdeeply\s+\w+` pattern.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; test -f docs/integrations/obs-browser-source.md &amp;&amp; grep -q "127.0.0.1:8765" docs/integrations/obs-browser-source.md &amp;&amp; grep -q "Browser source" docs/integrations/obs-browser-source.md &amp;&amp; grep -q "Troubleshooting" docs/integrations/obs-browser-source.md &amp;&amp; grep -q "Privacy" docs/integrations/obs-browser-source.md &amp;&amp; uv run python scripts/audit/check_no_slop_opp.py</automated>
  </verify>
  <done>Integration doc exists, references the mascot route at port 8765, includes setup + troubleshooting + privacy / network notes, passes anti-slop sibling gate against docs/dep-opportunities/.</done>
</task>

<task type="auto">
  <name>Task 3: README cross-link to OBS integration doc</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — find an appropriate anchor; look for existing "Integrations", "Streaming", "Recipes", "Setup", or similar section)
    - docs/integrations/obs-browser-source.md (just authored)
    - scripts/launch/check_readme_hero_lock.py (if present — verify the hero-lock contract that prevents arbitrary README edits)
  </read_first>
  <action>
    Inspect README.md structure first. Locate one of:

    - An existing "Integrations" section → add a one-paragraph entry under it.
    - An existing "Streaming" or "Recipes" section → same.
    - A "What's in v3.1" or "v3.1 changes" section → same.
    - If none exist: append a new short section at the END of README.md (after the last existing section, before any footer) titled `## Streaming integrations`.

    Insertion text (single paragraph + link, anti-slop clean):

    ```markdown
    ## Streaming integrations

    **OBS Studio (browser-source).** vibemix's mascot canvas can render directly inside an OBS scene as a transparent overlay. Point the OBS Browser source at the local mascot route and the mascot reacts to your live session in real time. Full setup steps live in [docs/integrations/obs-browser-source.md](docs/integrations/obs-browser-source.md).
    ```

    Constraints on the edit:

    - Do NOT modify the hero block, hero PNG/WebM references, hero-lock-verbatim text, or any section guarded by `scripts/launch/check_readme_hero_lock.py` / `scripts/launch/check_readme_grids_a11y.py`.
    - Do NOT modify the badges row, install instructions, or quick-start section.
    - If `check_readme_hero_lock.py` exists, run it after your edit to confirm the hero contract is intact.
    - The phrase "deeply" must not appear adjacent to a word in your new paragraph; the 16-token blocklist must not match.

    Verify after edit:

    1. `grep -q "docs/integrations/obs-browser-source.md" README.md` — true
    2. `bash scripts/launch/check_readme_hero_lock.py` — exits 0 (if the script exists)
    3. `uv run python scripts/audit/check_no_slop_opp.py --dir docs/integrations` — exits 0 (sibling checker pointed at the new integration doc)
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; grep -q "docs/integrations/obs-browser-source.md" README.md &amp;&amp; (test ! -f scripts/launch/check_readme_hero_lock.py || python scripts/launch/check_readme_hero_lock.py) &amp;&amp; uv run python scripts/audit/check_no_slop_opp.py --dir docs/integrations &amp;&amp; echo "OK README + hero-lock + slop"</automated>
  </verify>
  <done>README.md contains a cross-link to docs/integrations/obs-browser-source.md inside an appropriate section; hero-lock contract still passes (if script exists); anti-slop sibling exits clean for the new integration doc.</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end scan validator green run</name>
  <files></files>
  <read_first>
    - scripts/audit/scan_opportunities.py (validator)
    - scripts/audit/dep_ratings.yaml (opportunity_evaluations row for DEP-OPP-01 points at the ADR)
    - .planning/decisions/DEP-OPP-01-obs-browser-source.md (just authored)
  </read_first>
  <action>
    Run the full validator + anti-slop chain. Expected: all gates green.

    1. `uv run python scripts/audit/scan_opportunities.py` — parity + auto-Red + ADR-existence gates pass.
    2. `uv run python scripts/audit/check_no_slop_opp.py` — docs/dep-opportunities/ is slop-clean.
    3. `uv run python scripts/audit/check_no_slop_opp.py --dir docs/integrations` — docs/integrations/ is slop-clean.

    If any step fails, fix the offending file before considering this task done. Do NOT relax the anti-slop blocklist; rewrite the prose.
  </action>
  <verify>
    <automated>cd /Users/ozai/projects/dj-set-ai &amp;&amp; uv run python scripts/audit/scan_opportunities.py &amp;&amp; uv run python scripts/audit/check_no_slop_opp.py &amp;&amp; uv run python scripts/audit/check_no_slop_opp.py --dir docs/integrations &amp;&amp; echo "OK full chain green"</automated>
  </verify>
  <done>All three commands exit 0. OPP-04 + OPP-06 contractually satisfied end-to-end.</done>
</task>

</tasks>

<verification>
- [ ] .planning/decisions/DEP-OPP-01-obs-browser-source.md exists with required sections (Context / Decision / Rationale / Integration plan / Rollback / Consequences / References)
- [ ] docs/integrations/obs-browser-source.md exists with setup steps + troubleshooting + privacy/network notes
- [ ] README.md gained a Streaming integrations section (or paragraph in an existing section) cross-linking the new doc
- [ ] scripts/audit/scan_opportunities.py exits 0 (parity + auto-Red + ADR-existence all green)
- [ ] scripts/audit/check_no_slop_opp.py exits 0 against docs/dep-opportunities/ AND docs/integrations/
- [ ] scripts/launch/check_readme_hero_lock.py (if present) still passes — hero block intact
- [ ] POC files untouched (cohost*.py, mascot.html, run*.sh)
- [ ] No new IPC wrappers, no new Tauri webview routes, no new runtime code (zero-code adoption)
</verification>

<commit_message>
feat(48-04): OBS browser-source mascot path — ADR + integration doc + README link (OPP-04, OPP-06)

The one green-adopt outcome from the v3.1 opportunity scan lands as
docs-only adoption. Three artifacts:

1. .planning/decisions/DEP-OPP-01-obs-browser-source.md — ADR with
   decision + rationale + integration plan + rollback path. The
   scan validator's ADR-existence gate (Plan 48-03) requires this
   exact path.
2. docs/integrations/obs-browser-source.md — step-by-step OBS Studio
   setup using the existing Tauri webview mascot route at
   ws://127.0.0.1:8765. Setup + troubleshooting + privacy notes.
3. README.md cross-link in a Streaming integrations section so
   streamers discover the path.

Zero new runtime code, zero new IPC wrappers, zero new Tauri webview
routes. The mascot bus + webview route already serve per v3.0
baseline; OBS browser-source plugs into the same surface.

Refs OPP-04, OPP-06, CONTEXT Decision 6.
</commit_message>
