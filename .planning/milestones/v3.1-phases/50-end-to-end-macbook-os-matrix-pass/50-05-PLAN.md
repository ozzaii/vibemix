---
phase: 50-end-to-end-macbook-os-matrix-pass
plan: 05
type: execute
wave: 3
depends_on: [50-01, 50-02, 50-03, 50-04]
files_modified:
  - scripts/e2e/check_e2e_report.sh
  - scripts/e2e/test_check_e2e_report.sh
  - scripts/launch/cut_release.sh
  - tests/e2e/macbook/test_gate_2b_rerun.py
  - tests/e2e/macbook/os_matrix_smoke.py
  - tests/e2e/macbook/test_os_matrix_smoke.py
autonomous: true
requirements:
  - E2E-02
  - E2E-05
  - E2E-08
tags:
  - gate-6b
  - cut-release
  - os-matrix-50b
  - gate-2b-rerun

must_haves:
  truths:
    - "scripts/e2e/check_e2e_report.sh parses the latest dist/e2e-macbook-runs/<UTC>/report.html (by mtime), extracts the 5 dimension statuses, exits 1 if ANY dimension is FAIL. Exits 0 on PASS / PARTIAL / SKIPPED (PARTIAL is a graceful state for CI-tolerant fallbacks). Exits 2 if NO report.html exists (cut_release should not proceed)."
    - "scripts/e2e/check_e2e_report.sh extracts dimension status by grepping report.html for the locked label rows (e.g. 'Functional   FAIL'). Robust to whitespace variation. Uses ONLY standard Unix tools (bash + grep + awk) — no Python dep for the gate runner."
    - "scripts/launch/cut_release.sh adds Gate 6b immediately after Gate 2b (line ~94). New gate echoes '[Gate 6b] check_e2e_report.sh — blocks on FAIL in dist/e2e-macbook-runs/' and exits non-zero on FAIL. Does NOT duplicate Gate 2b logic (REQ E2E-08 verbatim)."
    - "scripts/e2e/test_check_e2e_report.sh is a bash test using a tmp dir + synthetic report.html files; verifies: all-PASS → exit 0, one FAIL → exit 1, no report.html → exit 2, mixed PASS/PARTIAL → exit 0."
    - "tests/e2e/macbook/test_gate_2b_rerun.py re-invokes scripts/release/check_gate.sh as a subprocess, asserts exit 0 (engineering-clean). Skips with explicit reason if 7-day proxy data unavailable in test env (CI-tolerant). REQ E2E-05."
    - "tests/e2e/macbook/os_matrix_smoke.py implements 50b objective smoke: install → launch → wait-for-first-event → shutdown. Extends Phase 49's scripts/dist/install_vm_matrix.sh with a --check-e2e flag (NOT a new harness — composition). Returns Dimension structured result."
    - "tests/e2e/macbook/test_os_matrix_smoke.py runs the smoke harness in dry-run mode against ≥2 reachable configs (CI macos-latest + one Tart 14 AS image if available). Real-VM run on all 5 OS configs gated on §INSTALL-VM-RUN Kaan-action — test asserts harness completes dry-run cleanly, marks unreachable configs SKIPPED."
    - "Gate 6b wired in cut_release.sh BEFORE existing Gate 6 (bundle ID lock) to fail-fast on dimension-FAIL rather than after all other gates. Sequence: Gate 2b → Gate 6b → Gate 3..5 → Gate 6."
    - "All subagent prompts include Step-0 worktree invariant per memory feedback_worktree_must_sync_main_first."
  artifacts:
    - path: "scripts/e2e/check_e2e_report.sh"
      provides: "Gate 6b runner; parses latest report.html; exits non-zero on FAIL"
      min_lines: 70
    - path: "scripts/e2e/test_check_e2e_report.sh"
      provides: "Bash test for the gate runner; 4 cases"
      min_lines: 70
    - path: "scripts/launch/cut_release.sh"
      provides: "MODIFIED — adds Gate 6b block after Gate 2b"
      min_lines: 0
    - path: "tests/e2e/macbook/test_gate_2b_rerun.py"
      provides: "Subprocess re-invocation of check_gate.sh; REQ E2E-05"
      min_lines: 40
    - path: "tests/e2e/macbook/os_matrix_smoke.py"
      provides: "50b smoke harness; install/launch/first-event/shutdown composition with Phase 49"
      min_lines: 120
    - path: "tests/e2e/macbook/test_os_matrix_smoke.py"
      provides: "50b dry-run test against ≥2 reachable configs"
      min_lines: 60
  key_links:
    - from: "scripts/launch/cut_release.sh § Gate 6b"
      to: "scripts/e2e/check_e2e_report.sh"
      via: "shell exec"
      pattern: "fail-fast release-cut gate"
    - from: "tests/e2e/macbook/os_matrix_smoke.py"
      to: "scripts/dist/install_vm_matrix.sh"
      via: "extends with --check-e2e flag"
      pattern: "composition over duplication (Phase 49 harness reuse)"
    - from: "tests/e2e/macbook/test_gate_2b_rerun.py"
      to: "scripts/release/check_gate.sh"
      via: "subprocess.run — does NOT redefine gate logic"
      pattern: "gate composition"
---

<objective>
Wire Gate 6b into `cut_release.sh` (release-cut block on FAIL), assert Gate 2b re-runs engineering-clean before pass marked PASS, and ship the 50b OS-matrix smoke harness extending Phase 49's `install_vm_matrix.sh`.

Out of scope: real-VM execution on all 5 OS configs (gated on §INSTALL-VM-RUN Kaan-action), modifying Gate 2b logic, modifying Phase 49's install_vm_matrix.sh internals (only adds --check-e2e flag).
</objective>

<execution_context>
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-CONTEXT.md` § Gates Wired
- `.planning/REQUIREMENTS.md` § E2E-02, E2E-05, E2E-08
- `scripts/launch/cut_release.sh` — existing 6-gate pre-flight (Gate 2b at line ~92)
- `scripts/release/check_gate.sh` — Gate 2b runner
- `scripts/dist/install_vm_matrix.sh` (Phase 49) — composes 50b harness
- `.planning/phases/49-win-mac-one-click-installer-chain/49-VERIFICATION.md` — onboarding stopwatch contract
</execution_context>

<steps>
1. Read existing `scripts/launch/cut_release.sh` around line 92 (Gate 2b block) to confirm shape.
2. Write `scripts/e2e/check_e2e_report.sh` — bash with `set -euo pipefail`. Find latest report by `ls -t dist/e2e-macbook-runs/*/report.html | head -1`. Grep dimension rows. Exit logic per truths.
3. Write `scripts/e2e/test_check_e2e_report.sh` — bash test with `tmp_dir` setup; 4 case fixtures (all-pass / one-fail / no-report / mixed-partial).
4. Edit `scripts/launch/cut_release.sh` — insert Gate 6b block immediately after Gate 2b. Pattern:
   ```bash
   # ── Gate 6b: e2e harness dimension-FAIL block (Phase 50 / E2E-08) ──────
   echo "[Gate 6b] check_e2e_report.sh — blocks on FAIL in dist/e2e-macbook-runs/"
   ./scripts/e2e/check_e2e_report.sh || exit 1
   ```
5. Write `tests/e2e/macbook/test_gate_2b_rerun.py` — uses `subprocess.run` with capture; asserts exit 0; skip with reason if proxy data env-var absent.
6. Write `tests/e2e/macbook/os_matrix_smoke.py` — wraps `install_vm_matrix.sh --check-e2e`. Implements 4-step smoke: install / launch / wait-for-first-event (timeout 60s) / shutdown. Returns `Dimension` dataclass from `tests/e2e/macbook/dimensions.py`.
7. Write `tests/e2e/macbook/test_os_matrix_smoke.py` — dry-runs harness; SKIPPED for unreachable configs.
8. Run `bash scripts/e2e/test_check_e2e_report.sh` + `pytest tests/e2e/macbook/test_gate_2b_rerun.py tests/e2e/macbook/test_os_matrix_smoke.py -v`. Green.
9. Run `bash scripts/launch/cut_release.sh --dry-run` (if dry-run supported; else verify Gate 6b block syntactically with `bash -n`). Verify Gate 6b appears in output.
10. Commit: `feat(50-05): Gate 6b + Gate 2b rerun + 50b OS-matrix smoke harness`.
</steps>

<verification>
- `scripts/launch/cut_release.sh` Gate 6b block present immediately after Gate 2b (does NOT duplicate Gate 2b logic)
- `scripts/e2e/check_e2e_report.sh` 4 test cases green
- `pytest tests/e2e/macbook/test_gate_2b_rerun.py -v` → pass or skip with proxy-data-absent reason
- `pytest tests/e2e/macbook/test_os_matrix_smoke.py -v` → green; unreachable configs SKIPPED
- Grep cut_release.sh for "Gate 6b" → exactly one match
- Grep cut_release.sh for "Gate 2b" → existing line preserved unchanged
- Step-0 worktree invariant documented in commit
</verification>
