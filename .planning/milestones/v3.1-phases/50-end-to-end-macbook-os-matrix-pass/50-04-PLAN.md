---
phase: 50-end-to-end-macbook-os-matrix-pass
plan: 04
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - tests/e2e/macbook/audio_loopback_fixture.py
  - tests/e2e/macbook/cassettes/gate_02_v3_0_baseline.yaml
  - tests/e2e/macbook/test_audio_loopback.py
  - tests/e2e/macbook/test_blackhole_48khz_probe.py
autonomous: true
requirements:
  - E2E-04
tags:
  - audio-loopback
  - vcr-cassette
  - blackhole-48khz
  - zero-live-gemini

must_haves:
  truths:
    - "tests/e2e/macbook/audio_loopback_fixture.py declares pytest fixture audio_loopback_recorder that replays the VCR cassette pinned to v3.0 GATE-02 baseline. ZERO live Gemini calls in CI — REQ E2E-04 verbatim."
    - "tests/e2e/macbook/cassettes/gate_02_v3_0_baseline.yaml is a VCR cassette in vcrpy YAML format that captures the v3.0 GATE-02 Gemini request/response shape. The cassette is REFERENCED-from / pinned-to the v3.0 GATE-02 baseline; if the existing v3.0 cassette artifact lives elsewhere, this is a symlink/copy with explicit provenance comment at top of file naming the source artifact."
    - "Fixture uses ModelRouter abstraction (NEVER inlines gemini-* literals). The cassette captures the abstracted call surface; mismatched ModelRouter calls in tests fail the fixture with a clear error pointing at cassette drift."
    - "tests/e2e/macbook/test_audio_loopback.py exercises the sidecar↔BlackHole/VB-CABLE audio path: writes 1s of synthetic 440Hz @ 48kHz to a mock BlackHole input, asserts sidecar receives equivalent samples within ±2 sample drift. Uses cassette for the Gemini-side roundtrip."
    - "tests/e2e/macbook/test_blackhole_48khz_probe.py asserts the post-install probe (installer/companion/audio_config.py from Phase 49) returns OK for 48000 Hz, FAIL with measured_khz field for 44100 Hz. Per memory project_v4_canonical_baseline (BlackHole 48 kHz format requirement)."
    - "Fixture explicitly forbids live mode: no environment variable or CLI flag can turn cassette replay off in CI. Local-dev cassette refresh requires running scripts/eval/record_cassettes.py separately (already exists, do not modify)."
    - "Tests are CI-tolerant: if BlackHole/VB-CABLE device absent (CI Linux runner), use sounddevice mock; produce SKIPPED dimension status, NOT FAIL. PITFALLS § 19 audio-loopback bootstrap paradox respected."
    - "Cassette YAML head comment documents: provenance (v3.0 GATE-02 baseline), refresh procedure (scripts/eval/record_cassettes.py), drift-detection (mismatched calls → clear error)."
  artifacts:
    - path: "tests/e2e/macbook/audio_loopback_fixture.py"
      provides: "Pytest fixture: VCR cassette replay + ModelRouter abstraction"
      min_lines: 100
    - path: "tests/e2e/macbook/cassettes/gate_02_v3_0_baseline.yaml"
      provides: "Pinned cassette; provenance documented at top"
      min_lines: 20
    - path: "tests/e2e/macbook/test_audio_loopback.py"
      provides: "Sidecar↔audio-driver loopback verification using cassette"
      min_lines: 80
    - path: "tests/e2e/macbook/test_blackhole_48khz_probe.py"
      provides: "Post-install 48 kHz probe assertion (per memory project_v4_canonical_baseline)"
      min_lines: 60
  key_links:
    - from: "tests/e2e/macbook/audio_loopback_fixture.py"
      to: "src/vibemix/model_router.py"
      via: "uses ModelRouter; no gemini-* literals"
      pattern: "ModelRouter seam preserved"
    - from: "tests/e2e/macbook/cassettes/gate_02_v3_0_baseline.yaml"
      to: "v3.0 GATE-02 baseline cassette artifact (existing)"
      via: "provenance comment + structural identity"
      pattern: "cassette pinning per CONTEXT.md auto-accepted decision"
    - from: "tests/e2e/macbook/test_blackhole_48khz_probe.py"
      to: "installer/companion/audio_config.py"
      via: "asserts post-install probe contract (Phase 49)"
      pattern: "INSTALL-10 carry-forward verification"
---

<objective>
Audio-loopback fixture validating sidecar↔BlackHole/VB-CABLE path against a VCR cassette pinned to v3.0 GATE-02 baseline. Zero live Gemini calls in CI — burns zero quota. Also re-asserts the BlackHole 48 kHz format requirement (project_v4_canonical_baseline memory).

Out of scope: re-recording the cassette (provenance from v3.0 — only pin), modifying ModelRouter (seam preserved), the post-install routine itself (Phase 49 already shipped audio_config.py).
</objective>

<execution_context>
- `.planning/phases/50-end-to-end-macbook-os-matrix-pass/50-CONTEXT.md` § Architecture audio-loopback section
- `.planning/REQUIREMENTS.md` § E2E-04
- `.planning/research/PITFALLS.md` § 19 — audio-loopback bootstrap paradox
- `.planning/research/PITFALLS.md` § 26 — VCR cassette invalidation
- `installer/companion/audio_config.py` (Phase 49) — 48 kHz probe contract
- `scripts/eval/record_cassettes.py` (existing) — cassette refresh tool
- Memory: `project_v4_canonical_baseline` — BlackHole 48 kHz hard requirement
</execution_context>

<steps>
1. Locate existing v3.0 GATE-02 cassette artifact (likely under `eval/cassettes/` or `scripts/eval/cassettes/`). Document path in cassette provenance header.
2. Write `tests/e2e/macbook/cassettes/gate_02_v3_0_baseline.yaml` — either symlink or content-pinned copy with provenance head comment.
3. Write `tests/e2e/macbook/audio_loopback_fixture.py` — pytest fixture using `vcrpy` or `pytest-recording`. Replay-only mode hardcoded. ModelRouter abstraction confirmed by import-grep test inside fixture init.
4. Write `tests/e2e/macbook/test_audio_loopback.py` — 1s 440Hz sine wave round-trip; mock BlackHole device via `sounddevice` test backend if device absent.
5. Write `tests/e2e/macbook/test_blackhole_48khz_probe.py` — imports `installer.companion.audio_config`; mocks CoreAudio/WASAPI; asserts 48000 → OK and 44100 → FAIL with measured_khz=44.1.
6. Run `pytest tests/e2e/macbook/test_audio_loopback.py tests/e2e/macbook/test_blackhole_48khz_probe.py -v`. Green.
7. Grep `tests/e2e/macbook/` for `gemini-2.5`, `gemini-3.0`, `gemini-flash` literals → zero matches (ModelRouter seam preserved).
8. Commit: `feat(50-04): audio loopback fixture + VCR cassette pin + 48kHz probe`.
</steps>

<verification>
- Cassette YAML head comment documents v3.0 GATE-02 provenance + refresh procedure
- `pytest tests/e2e/macbook/test_audio_loopback.py -v` → green (CI-tolerant on missing device)
- `pytest tests/e2e/macbook/test_blackhole_48khz_probe.py -v` → both states asserted
- Grep `gemini-` literal in tests/e2e/macbook/ → zero matches
- Step-0 worktree invariant documented in commit
</verification>
