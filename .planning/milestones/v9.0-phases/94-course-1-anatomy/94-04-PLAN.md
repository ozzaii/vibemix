---
phase: 94-course-1-anatomy
plan: 04
type: execute
wave: 3
depends_on:
  - 94-01
  - 94-02
  - 94-03
files_modified:
  - .planning/phases/94-course-1-anatomy/94-EAR-PASS.md
autonomous: false
requirements:
  - TONE-01
  - TONE-03
  - CURR-1.01
  - CURR-1.02
  - CURR-1.03
  - CURR-1.04
  - CURR-1.05
  - CURR-1.06
  - CURR-1.07
  - CURR-1.08
  - CURR-1.09
  - CURR-1.10
  - CURR-1.11
  - CURR-1.12
  - CURR-1.13
  - CURR-1.14
  - CURR-1.15
  - CURR-1.16
tags: [kaan-action, ear-pass, checkpoint, course-1-walk-through, audio-verification]
must_haves:
  truths:
    - "Kaan walks through Course 1 end-to-end on a real DDJ-FLX4 in a live cargo tauri dev session"
    - "The verbatim iconic opening dialog plays correctly on first launch (TONE-01 live verification)"
    - "Every lesson L1.02..L1.13 + L1.15 advances on the expected MIDI action without sticking"
    - "L1.14 EQ-as-Tutor demo plays an actual library track (or honest-null fallback) per band and the user hears the EQ band swell when sweeping the knob"
    - "L1.16 Course 1 Recital samples 5 prompts, scores honestly, and persists course_2_unlocked on 5/5"
    - "No tutor-tic slop heard during real LLM narration (TONE-03 live verification)"
    - "Ear-pass observations land at .planning/phases/94-course-1-anatomy/94-EAR-PASS.md for P98 audit pickup"
  artifacts:
    - path: .planning/phases/94-course-1-anatomy/94-EAR-PASS.md
      provides: "Pre-staged template for Kaan's ear-pass observations (per-lesson green/yellow/red rows + the cargo tauri dev walk-through recipe)"
      contains: "§LEARN-EAR-COURSE-1"
  key_links:
    - from: .planning/phases/94-course-1-anatomy/94-EAR-PASS.md
      to: .planning/STATE.md KAAN-ACTION queue
      via: "§LEARN-EAR-COURSE-1 — ride forward, do not block the autonomous run"
      pattern: "§LEARN-EAR-COURSE-1"
---

<objective>
Park Kaan's live Course 1 ear-pass as a KAAN-ACTION queue entry and stage a
ready-to-use observation log so the ear-pass is one cargo-tauri-dev session
away whenever he sits down at the FLX4.

v9.0 runs in `gsd-autonomous fully` mode (per STATE.md "Pre-Milestone
Direction" + the `feedback_autonomous_no_grey_area_pause` MEMORY note):
human ear-pass gates cannot be self-verified, so they ride forward into the
KAAN-ACTION queue rather than blocking the autonomous overnight run.

Per ROADMAP §v9.0 KAAN-ACTION Queue, `§LEARN-EAR-COURSE-1` is one of the
three BLOCKING ear-pass gates that resolve at P98 (Live Audit + Ear-Pass
Hand-Off). Plan 94-04 (a) confirms the queue entry exists in STATE.md +
ROADMAP.md (surface-and-confirm, no new content), (b) lays down the
observation log template Kaan fills in during the walk-through, and (c)
records the checkpoint as non-blocking so Phase 94 closes cleanly.

Output: 1 new markdown file (94-EAR-PASS.md) + 1 non-blocking checkpoint
task. No production-code touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-course-1-anatomy/94-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface §LEARN-EAR-COURSE-1 + lay down 94-EAR-PASS.md observation log</name>
  <files>.planning/phases/94-course-1-anatomy/94-EAR-PASS.md</files>
  <read_first>.planning/STATE.md (verify §LEARN-EAR-COURSE-1 already in BLOCKING queue), .planning/ROADMAP.md (verify same entry in v9.0 KAAN-ACTION queue), .planning/phases/94-course-1-anatomy/94-CONTEXT.md (locked decisions)</read_first>
  <action>
    Two sub-steps, both confirmation-then-write (no production-code touches).

    **Sub-step A — confirm KAAN-ACTION queue surface.** Grep both STATE.md + ROADMAP.md for `§LEARN-EAR-COURSE-1`:

    ```bash
    grep -c "§LEARN-EAR-COURSE-1" .planning/STATE.md
    grep -c "§LEARN-EAR-COURSE-1" .planning/ROADMAP.md
    ```

    Per STATE.md "v9.0 KAAN-ACTION Queue (parked, never faked)" + ROADMAP.md "KAAN-ACTION Queue (v9.0 — surfaced
    + parked, never faked)" both already carry the BLOCKING entry. The grep above MUST return ≥1 in BOTH files.
    If the grep returns 0 in either file (e.g. someone pruned the line accidentally), restore the entry to the
    BLOCKING section:

    - STATE.md `## v9.0 KAAN-ACTION Queue (parked, never faked)` → BLOCKING bullet: `§LEARN-EAR-COURSE-1 (P98 / AUDIT-01) — Kaan ear-pass on Course 1 (16 lessons) on real FLX4`
    - ROADMAP.md `## KAAN-ACTION Queue (v9.0 — surfaced + parked, never faked)` → BLOCKING bullet: same line with a leading red-circle emoji to match the existing list shape.

    The default expectation is BOTH greps return ≥1; if so, no edit is needed for sub-step A.

    **Sub-step B — write 94-EAR-PASS.md.** Create the new file
    `.planning/phases/94-course-1-anatomy/94-EAR-PASS.md` with this template (verbatim — Kaan fills the empty
    rows during the walk-through):

    ```markdown
    # Course 1 Ear-Pass Log (§LEARN-EAR-COURSE-1)

    **Plan:** 94-04 — Course 1 Anatomy live verification
    **Hardware:** Pioneer DDJ-FLX4 (Kaan's canonical golden — per ROADMAP §v9.0 hard constraints)
    **Mode:** non-blocking KAAN-ACTION ride-forward per `gsd-autonomous fully`
    **Discharge:** P98 audit close (AUDIT-01)

    ## Walk-through recipe

    Per `feedback_stale_sidecar_verify_current_source` (cargo tauri dev's bundled Python sidecar lags edited src/,
    so live verification runs against current source on the dev binary):

    1. Plug DDJ-FLX4 via USB. Confirm BlackHole 2ch + nowplaying-cli are routed (CLAUDE.md macOS prereqs).
    2. `cd /Users/ozai/projects/dj-set-ai && cargo tauri dev` (the dev shell spawns real `python -m vibemix`).
    3. Open the Learn webview. Pre-P97 (mode-picker on main window) entry path: invoke `uv run python -m vibemix
       learn start L1.01` (or whatever the Plan 92-04 CLI dispatch ships for course start). The Learn webview
       wakes via `ipc.learn.start_lesson` envelope.
    4. Walk lessons in order. Score each as 🟢 green / 🟡 yellow (works but rough) / 🔴 red (broken or slop heard).

    ## Per-lesson observations

    | Lesson | Expected | Observed | Status | Notes |
    |--------|----------|----------|--------|-------|
    | L1.01 Opening Dialog | 4 verbatim lines play (TONE-01 byte-equality already CI-pinned by Plan 94-02) | | | |
    | L1.02 Meet Your Controller | Rendered SVG glows; tutor names controller sections | | | |
    | L1.03 Channel Strip | Channel-fader-A sweep advances on full throw | | | |
    | L1.04 Crossfader | Full-throw advances | | | |
    | L1.05 Pitch Fader | ±4% advances | | | |
    | L1.06 Transport Buttons | Cue press advances | | | |
    | L1.07 Jog Wheel | Nudge advances | | | |
    | L1.08 Headphone Cueing | Cue-B button advances | | | |
    | L1.09 Master/Booth/Headphones | Master knob touch advances | | | |
    | L1.10 Anatomy of a Song | 5-beat narration over playing track | | | |
    | L1.11 Counting Bars | Tap-along advances | | | |
    | L1.12 Spot Breakdown By Ear | Cue press on breakdown advances | | | |
    | L1.13 Spot Breakdown By Eye | Cue press on waveform breakdown advances | | | |
    | L1.14 EQ-as-Tutor (MARQUEE) | Low + mid + high tracks play; EQ-knob sweep produces audible swell; `[exemplar:<track_id>]` cited | | | |
    | L1.15 Load Two Tracks | Load-B advances | | | |
    | L1.16 Course 1 Recital | 5 prompts sampled; 5/5 prints "course 2 unlocked." + `learn-progress.json` carries `"course_2_unlocked": true` | | | |

    ## Tone discipline observations

    - **Hand-authored fixtures** (Plan 94-02 CI-gates these — no review needed unless a fixture changed in flight):
      - Tutor-tic blocklist hits heard? 🟢 / 🔴 (list any)
    - **Runtime LLM interjections** (Gemini Flash via learn_tutor route — these are NOT CI-gated; ear is the gate):
      - Compliments heard ("great", "awesome", "you crushed it", "perfect", etc.)? 🟢 / 🔴 (list any)
      - Summaries heard ("you just learned X", "you've now mastered X")? 🟢 / 🔴 (list any)
      - Previews heard ("now let's", "next we'll", "coming up")? 🟢 / 🔴 (list any)
      - Upbeat hook closers ("exciting, right?", "this is where it gets fun", "you're going to love")? 🟢 / 🔴 (list any)

    Any runtime interjection slop heard → file a tutor system instruction tuning followup against the
    `_FORBIDDEN_TUTOR_MOVES_LOCK` in `src/vibemix/learn/prompts.py:52-80`. The lock is the live-tuning seam.

    ## Audio routing observations

    - L1.14 played through which device? (system output / BlackHole / dedicated headphone): _____
    - Default -12 dB gain — too loud / too quiet / just right? _____
    - L1.14 stop on band-cycle advance: clean cut / fades / overlaps? _____
    - Active-session guard tested? (P93's `can_play()` returns True in P93; P96 wires real guard) — 🟢 not-applicable-for-course-1

    ## Overall

    Status: 🟢 ship Course 1 / 🟡 acceptable with followups / 🔴 block on the followups

    Followups (file as new KAAN-ACTION entries or runtime tuning notes):
    - _____
    - _____
    ```

    The file is a TEMPLATE — Kaan fills the empty cells when he walks the course. The structure is pre-staged so
    he doesn't have to author the test plan on the day; he just executes against the recipe.
  </action>
  <verify>
    <automated>
      cd /Users/ozai/projects/dj-set-ai && bash -c '
      STATE_HITS=$(grep -c "§LEARN-EAR-COURSE-1" .planning/STATE.md 2>/dev/null || echo 0)
      ROADMAP_HITS=$(grep -c "§LEARN-EAR-COURSE-1" .planning/ROADMAP.md 2>/dev/null || echo 0)
      EARPASS_EXISTS=$(test -f .planning/phases/94-course-1-anatomy/94-EAR-PASS.md && echo 1 || echo 0)
      EARPASS_HAS_TABLE=$(grep -c "L1.14 EQ-as-Tutor (MARQUEE)" .planning/phases/94-course-1-anatomy/94-EAR-PASS.md 2>/dev/null || echo 0)
      if [ "$STATE_HITS" -lt 1 ]; then echo "FAIL: §LEARN-EAR-COURSE-1 missing from STATE.md (sub-step A)"; exit 1; fi
      if [ "$ROADMAP_HITS" -lt 1 ]; then echo "FAIL: §LEARN-EAR-COURSE-1 missing from ROADMAP.md (sub-step A)"; exit 1; fi
      if [ "$EARPASS_EXISTS" -ne 1 ]; then echo "FAIL: 94-EAR-PASS.md missing (sub-step B)"; exit 1; fi
      if [ "$EARPASS_HAS_TABLE" -lt 1 ]; then echo "FAIL: 94-EAR-PASS.md missing L1.14 row in per-lesson table"; exit 1; fi
      echo "PASS: queue entry surfaced; ear-pass log staged with 16-row per-lesson table"
      '
    </automated>
  </verify>
  <done>§LEARN-EAR-COURSE-1 appears in both STATE.md and ROADMAP.md. 94-EAR-PASS.md exists at `.planning/phases/94-course-1-anatomy/94-EAR-PASS.md` with the walk-through recipe + 16-row per-lesson observation table + tone + audio + overall sections. Kaan can execute the ear-pass without authoring anything new on the day.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Kaan FLX4 ear-pass on Course 1 (parked KAAN-ACTION, P98 discharge)</name>
  <what-built>
    Plans 94-01, 94-02, 94-03 ship Course 1 Anatomy end-to-end:

    - 16 hand-authored JSON lesson fixtures (Plan 94-01).
    - curriculum.py extension registering all 16 lessons + the course_1_anatomy frame (Plan 94-01).
    - TONE-03 `check_no_tutor_slop.py` CI gate + ≥20-token blocklist (Plan 94-02).
    - TONE-01 byte-equality test on the iconic 4-line opening dialog (Plan 94-02).
    - L1.14 EQ-as-Tutor backend wiring (ExemplarLessonController + ExemplarFinder/ExemplarPlayer + `[exemplar:]`
      citation) (Plan 94-03).
    - L1.16 Course 1 Recital backend (RecitalRuntime + deterministic 5-prompt sampling + course_2_unlocked
      persistence) (Plan 94-03).
    - LessonRuntime extension: `register_lesson_observer` seam delegating L1.14 / L1.16 to their controllers
      without touching Invariant #1 (Plan 94-03).
    - All P92 + P93 invariant pins remain green (single-writer / one-socket / tutor-system-lock /
      scripts-are-fixtures / no-pioneer-brand-marks).
    - 94-EAR-PASS.md log template staged by Task 1 of this plan.

    Engineering is complete. Course 1 is verifiable offline (unit tests + `check_no_tutor_slop.py`). Live audio
    verification requires Kaan's ear on a real FLX4.
  </what-built>
  <how-to-verify>
    This is a NON-BLOCKING checkpoint (per `feedback_autonomous_no_grey_area_pause` — gsd-autonomous fully mode
    rides ear-pass forward into the KAAN-ACTION queue; does not pause the autonomous run).

    When Kaan sits down at the FLX4:

    1. Follow the walk-through recipe in `.planning/phases/94-course-1-anatomy/94-EAR-PASS.md` (Task 1 staged it).
    2. Fill in the per-lesson observation table — 16 rows, one per L1.NN.
    3. Score each lesson 🟢 green / 🟡 yellow / 🔴 red with a one-line note.
    4. Fill in the tone discipline + audio routing + overall sections.
    5. Commit `94-EAR-PASS.md` with observations. P98 audit reads the file and rolls it into the
       `v9.0-MILESTONE-AUDIT.md` doc.

    The `<resume-signal>` below is for the executor's awareness — under `gsd-autonomous fully` the executor does
    not actually pause here. The signal text is the form Kaan uses when he reports back so a downstream phase
    can act on it.
  </how-to-verify>
  <resume-signal>
    AUTONOMOUS MODE (default for v9.0): this checkpoint does NOT pause execution. Plan 94 closes
    as soon as Plans 94-01..03 land green + Task 1 of this plan completes. §LEARN-EAR-COURSE-1
    rides forward to P98 audit close.

    If a downstream phase needs the ear-pass to land early (e.g. P95 transitions explicitly gate
    on Course 1 verification), promote this checkpoint to `gate="blocking"` and wait for Kaan's
    signal: "Course 1 ear-pass complete — observations in 94-EAR-PASS.md."

    Under default gsd-autonomous fully, Plan 94 closes engineering-complete with the ear-pass
    parked as a non-blocking KAAN-ACTION.
  </resume-signal>
  <files>.planning/phases/94-course-1-anatomy/94-EAR-PASS.md</files>
  <action>Kaan walks Course 1 end-to-end on the FLX4 per the recipe in 94-EAR-PASS.md; fills in the 16-row per-lesson observation table + tone + audio + overall sections; commits the filled file. Under `gsd-autonomous fully` mode this task does NOT pause executor; the queue entry §LEARN-EAR-COURSE-1 rides forward to P98 audit close.</action>
  <verify>
    <automated>test -f .planning/phases/94-course-1-anatomy/94-EAR-PASS.md &amp;&amp; grep -c "§LEARN-EAR-COURSE-1" .planning/phases/94-course-1-anatomy/94-EAR-PASS.md</automated>
    <human-check>16-row per-lesson table filled in with status emoji + notes; tone observations section filled in; overall status set.</human-check>
  </verify>
  <done>Either Kaan has committed the filled 94-EAR-PASS.md (live discharge) OR the autonomous run advances past this checkpoint with §LEARN-EAR-COURSE-1 still queued (parked discharge per gsd-autonomous fully). Both are valid Phase 94 close states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous run ↔ ear-pass | the autonomous run cannot self-verify audio; the queue entry IS the gate |
| KAAN-ACTION queue ↔ phase close | parking ≠ deferring; the entry MUST surface in STATE.md + ROADMAP.md so P98 audit can discharge it |
| 94-EAR-PASS.md template ↔ live observations | Kaan-authored cells are the audit evidence; pre-staged structure ensures the audit captures the right axes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04-01 | Repudiation | ear-pass result lost | mitigate | observations land at `.planning/phases/94-course-1-anatomy/94-EAR-PASS.md`; git history preserves; P98 audit re-reads |
| T-94-04-02 | Tampering | autonomous run claims ear-pass complete without Kaan | accept | enforced by `gsd-autonomous fully` mode contract — agents NEVER fake KAAN-ACTION discharge; STATE.md "Pre-Milestone Direction" carries this rule |
| T-94-04-03 | Denial of Service | ear-pass blocks v9.0 ship indefinitely | accept | parked status is the v9.0 contract per ROADMAP; v9.0 public ship gates on AUDIT-01 (P98) which closes only when all 3 course ear-passes ride green |
| T-94-04-SC | Tampering | npm/pip installs | accept | zero — Task 1 touches one new markdown file; Task 2 is a non-blocking human checkpoint with no install gate |
</threat_model>

<verification>
- `grep -c "§LEARN-EAR-COURSE-1" .planning/STATE.md .planning/ROADMAP.md` returns ≥1 in each file.
- `.planning/phases/94-course-1-anatomy/94-EAR-PASS.md` exists with the 16-row per-lesson observation table + tone + audio + overall sections.
- Plan 94-04 closes without blocking the autonomous run (Task 2 is non-blocking).
- The walk-through recipe references the right invocation pattern per `feedback_stale_sidecar_verify_current_source` (cargo tauri dev with dev sidecar, not the bundled binary).
</verification>

<success_criteria>
- KAAN-ACTION queue entry §LEARN-EAR-COURSE-1 is surfaced and confirmed in STATE.md + ROADMAP.md.
- 94-EAR-PASS.md template is staged for Kaan's walk-through.
- Phase 94 is engineering-complete (Plans 94-01..03 green + 94-04 Task 1 green) with the ear-pass parked as non-blocking.
- Plan 94-04 closes autonomously per `gsd-autonomous fully` mode.
</success_criteria>

<output>
Create `.planning/phases/94-course-1-anatomy/94-04-SUMMARY.md` when done (one paragraph confirming Task 1 landed the queue surface + ear-pass log template; Task 2 parked non-blocking with §LEARN-EAR-COURSE-1 ride-forward).
</output>
