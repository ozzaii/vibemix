---
phase: 93-exemplar-engine-evidence-source
plan: 03
type: execute
wave: 2
depends_on: [93-01]
files_modified:
  - src/vibemix/learn/audio_cue.py
  - src/vibemix/library/audio_decode.py
  - src/vibemix/runtime/config_store.py
  - tauri/ui/src/ipc/messages.schema.json
  - tauri/ui/src/ipc/validator.generated.mjs
  - src/vibemix/learn/__init__.py
  - tests/learn/test_exemplar_player.py
  - tests/learn/test_load_audio_stereo.py
  - tests/ipc/test_settings_set_envelope_learn_field.py
autonomous: true
requirements: [EXEMPLAR-04]

must_haves:
  truths:
    - "Calling `ExemplarPlayer.play(<file>)` opens a NEW `sd.OutputStream` with `device=<headphone_index>`, `channels=2`, `dtype='float32'` — never touches `audio.buffers.PlaybackQueue`"
    - "`_choose_gain_db(master_rms)` returns -12 dB by default, -18 dB only when `master_rms > 0.5` linear (above -6 dBFS heuristic)"
    - "`ExemplarPlayer.can_play()` returns `True` in P93 (scaffold-only); the MusicState argument is wired so Plan P96 can replace the body without a constructor change"
    - "`load_audio_stereo(path)` decodes any in-tree audio file via PyAV to `(samples_n2_float32, sr)` — mono inputs duplicate across channels, stereo passthrough preserves sample rate"
    - "`learn.headphone_device_index` is a writeable field on the existing `ipc.settings.set` envelope (int >= 0 OR null) — `additionalProperties: false` ajv-validator round-trips it"
    - "`tauri/ui/src/ipc/validator.generated.mjs` is regenerated by `npm run codegen:ipc` — stale validator silently rejects the new field (per `feedback_schema_edit_needs_codegen_ipc`)"
  artifacts:
    - path: "src/vibemix/learn/audio_cue.py"
      provides: "ExemplarPlayer + _choose_gain_db + gain constants; own sd.OutputStream lifecycle"
      exports: ["ExemplarPlayer", "_choose_gain_db"]
      min_lines: 120
    - path: "src/vibemix/library/audio_decode.py"
      provides: "EXTENDED with load_audio_stereo(path) -> (samples, sr) — mirrors existing load_audio_mono PyAV pattern"
      exports: ["load_audio_stereo"]
    - path: "tauri/ui/src/ipc/messages.schema.json"
      provides: "EDITED — SettingsSet.payload.field enum extended with 'learn.headphone_device_index'"
      contains: "learn.headphone_device_index"
    - path: "src/vibemix/runtime/config_store.py"
      provides: "EDITED — adds learn.headphone_device_index pass-through (via existing `extra` dict pattern) + reader helper"
      contains: "learn.headphone_device_index"
  key_links:
    - from: "src/vibemix/learn/audio_cue.py::ExemplarPlayer.play"
      to: "sounddevice.OutputStream"
      via: "device=device_index, channels=2, dtype='float32', latency='low'"
      pattern: "sd\\.OutputStream\\(\\s*device="
    - from: "src/vibemix/learn/audio_cue.py"
      to: "src/vibemix/audio/buffers.PlaybackQueue"
      via: "MUST NOT import / reference — mic-gated at audio/buffers.py:226 would mute the user"
      pattern: "PlaybackQueue"   # MUST NOT MATCH inside audio_cue.py
    - from: "tauri/ui/src/ipc/messages.schema.json"
      to: "tauri/ui/src/ipc/validator.generated.mjs"
      via: "npm run codegen:ipc (pre-compiled ajv validator)"
      pattern: "learn\\.headphone_device_index"
---

<objective>
Land the audio-routing half of the phase. Adds the dedicated `ExemplarPlayer` (own `sd.OutputStream`, NEVER `PlaybackQueue`), the stereo decode extension, and the settings persistence path for the headphone device index. NO library-pick logic — that ships in Plan 93-04's `ExemplarFinder`.

Purpose: P94/P95 lessons need to play band exemplars on headphones without muting the user's mic. P93-03 is the load-bearing surface.

Output:
- NEW `src/vibemix/learn/audio_cue.py` carrying `ExemplarPlayer` + `_choose_gain_db` + the three gain constants (verbatim 93-RESEARCH.md Pattern 6).
- EDIT `src/vibemix/library/audio_decode.py` — add `load_audio_stereo(path)` mirroring the existing `load_audio_mono` PyAV pattern (Pattern 7).
- EDIT `tauri/ui/src/ipc/messages.schema.json` — extend `SettingsSet.payload.field.enum` with `"learn.headphone_device_index"`; extend the value `anyOf` to allow integer ≥0 (already accepted; verify) + null (already accepted). Regenerate `validator.generated.mjs` via `npm run codegen:ipc` IMMEDIATELY (mandatory; the validator is pre-compiled).
- EDIT `src/vibemix/runtime/config_store.py` — store + read `learn.headphone_device_index` through the existing `extra: dict[str, Any]` pass-through; add a tiny reader helper.
- Skip-markers REMOVED from 3 test files; their tests flip RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-exemplar-engine-evidence-source/93-CONTEXT.md
@.planning/phases/93-exemplar-engine-evidence-source/93-RESEARCH.md
@src/vibemix/platform/_audio_macos.py
@src/vibemix/library/audio_decode.py
@src/vibemix/audio/buffers.py
@src/vibemix/audio/constants.py
@src/vibemix/runtime/config_store.py
@tauri/ui/src/ipc/messages.schema.json
@CLAUDE.md

<interfaces>
<!-- All these symbols already exist. Use verbatim. -->

From src/vibemix/platform/_audio_macos.py:298-326 (the precedent):
```python
def open_passthrough_output(
    self, device_index, *, sample_rate, channels, block_size, callback,
) -> AudioStream:
    """Open passthrough output (sd.OutputStream @ float32 — djay → speakers stereo)."""
    stream = sd.OutputStream(
        device=device_index, samplerate=sample_rate, channels=channels,
        dtype="float32", blocksize=block_size, latency="low", callback=callback,
    )
    stream.start()
    return _SoundDeviceStreamHandle(stream)
```

From src/vibemix/library/audio_decode.py (existing pattern to mirror):
```python
def load_audio_mono(path: str | Path, *, target_sr: int) -> np.ndarray:
    # uses av.open(...) and AudioResampler(format="flt", layout="mono", rate=target_sr)
    # returns float32 mono samples
```

From src/vibemix/audio/constants.py:53:
```python
SILENT_RMS = 0.012  # below this threshold = real silence between tracks
```

From src/vibemix/state/music_state.py:134:
```python
audible_deck: str = "none"  # 'A' / 'B' / 'mix' / 'none' — needed for P96 active-session guard scaffold
```

From src/vibemix/runtime/config_store.py:150-191 (existing ConfigStore shape):
```python
@dataclass
class ConfigStore:
    ...
    extra: dict[str, Any] = field(default_factory=dict)
    # pass-through for namespaced keys (e.g. mascot.*, recording.*, learn.*)
```

DO NOT touch PlaybackQueue. The grep gate in this plan's verification REJECTS any `PlaybackQueue` reference inside `learn/audio_cue.py`.

Verbatim research deliverables:
- 93-RESEARCH.md Pattern 6 — `ExemplarPlayer` + `_choose_gain_db` (full module body, lines 672-800)
- 93-RESEARCH.md Pattern 7 — `load_audio_stereo` (full function body, lines 808-838)
- 93-RESEARCH.md Pattern 8 — settings persistence shape
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExemplarPlayer + load_audio_stereo</name>
  <files>
    src/vibemix/learn/audio_cue.py,
    src/vibemix/library/audio_decode.py,
    src/vibemix/learn/__init__.py,
    tests/learn/test_exemplar_player.py,
    tests/learn/test_load_audio_stereo.py
  </files>
  <behavior>
    - `_choose_gain_db(0.0)` → `-12.0`; `_choose_gain_db(None)` → `-12.0`; `_choose_gain_db(0.001)` → `-12.0` (below SILENT_RMS); `_choose_gain_db(0.7)` → `-18.0`; `_choose_gain_db(0.5)` → `-12.0` (boundary: only strictly greater than 0.5 triggers -18 dB).
    - `ExemplarPlayer.play(<wav>)` opens an `sd.OutputStream(device=<dev_idx>, channels=2, dtype='float32', latency='low')`. The stream's `callback` writes from a stereo float32 buffer; on EOF the callback raises `sd.CallbackStop` and triggers the optional `on_stop` callable.
    - `ExemplarPlayer.play(...)` is idempotent: a second call BEFORE the prior playback finishes calls `.stop()` on the current stream first, then opens a fresh one.
    - `ExemplarPlayer.stop()` is idempotent (no exceptions when called with no prior `.play()`); always closes the stream + clears the buffer + invokes `on_stop` once.
    - `ExemplarPlayer.can_play()` returns `True` in P93 (scaffold-only); the body MUST read `state.session_active` + `state.audible_deck` (even if P93 ignores the result) so Plan 96's diff is one line.
    - The module file does NOT import or reference `PlaybackQueue` anywhere — grep gate `tests/learn/test_exemplar_player.py::test_exemplar_player_does_not_touch_playback_queue` is load-bearing.
    - `load_audio_stereo(path)` returns `(samples, sr)` with `samples.shape == (N, 2)`, `samples.dtype == np.float32`. Mono input → stereo upmix; stereo input → passthrough; missing audio frames → `ValueError`.
  </behavior>
  <action>
**File 1 — `src/vibemix/learn/audio_cue.py`** (NEW):

SPDX header + docstring quoting REQ-ID EXEMPLAR-04 + the "Pitfall 2 — PlaybackQueue mic-gating" warning verbatim.

Port 93-RESEARCH.md Pattern 6 verbatim:

```
from __future__ import annotations
import threading
import time
import numpy as np
import sounddevice as sd
from pathlib import Path

from vibemix.audio.constants import SILENT_RMS
# NOTE: PlaybackQueue is DELIBERATELY NOT imported. ExemplarPlayer owns its
# own sd.OutputStream because PlaybackQueue is mic-gated at audio/buffers.py:226
# — reuse would silence the user's mic during exemplar playback (Pitfall 2).
```

Then the module body: `_DEFAULT_GAIN_DB = -12.0`, `_LOUD_MASTER_GAIN_DB = -18.0`, `_LOUD_MASTER_RMS_LINEAR = 0.5`, `_db_to_gain(db) -> float`, `_choose_gain_db(master_rms) -> float`, `class ExemplarPlayer: ...`.

The `ExemplarPlayer` constructor signature MUST be `(self, device_index: int, *, state)` — the `state` kwarg is the MusicState reference Plan 96 needs for the active-session guard. Use `state: "MusicState"` quoted-forward-reference + `from typing import TYPE_CHECKING; if TYPE_CHECKING: from vibemix.state.music_state import MusicState` to avoid an import cycle if needed.

The `can_play()` body MUST evaluate `state.session_active` and `state.audible_deck` (even though the P93 scaffold returns True unconditionally). Add a comment `# Plan P96 (EXEMPLAR-06) lands the real guard: return not (state.session_active and state.audible_deck != "none")`. This way Plan 96 is a 1-line diff, not a reshape.

`.play()` calls the new `vibemix.library.audio_decode.load_audio_stereo(file_path)` lazily-imported so the module loads without PyAV when audio playback is never invoked (mirror the existing lazy-import pattern in `runtime/wizard.py`).

After the `_callback` raises `sd.CallbackStop` on EOF, the next `.stop()` invocation (or the synchronous `playback_finished` path inside the callback) MUST invoke `on_stop` exactly once. Use a one-shot atomic flip via the existing `self._on_stop` slot + lock to satisfy idempotency.

**File 2 — `src/vibemix/library/audio_decode.py`** (EDIT):

Append `load_audio_stereo(path: str | Path) -> tuple[np.ndarray, int]` verbatim per 93-RESEARCH.md Pattern 7 (lines 808-838). Use `av.AudioResampler(format="flt", layout="stereo", rate=sr)` so mono inputs upmix by channel duplication automatically (PyAV's documented behavior). Transpose each `r.to_ndarray()` from `(channels, samples)` to `(samples, channels)`. Stack chunks via `np.concatenate(axis=0)`. Raise `ValueError(f"load_audio_stereo: no audio frames in {path!r}")` if `chunks` is empty.

Do NOT modify `load_audio_mono` — purely additive.

**File 3 — `src/vibemix/learn/__init__.py`** (EDIT):

- Add `from vibemix.learn.audio_cue import ExemplarPlayer` to the import block.
- Append `"ExemplarPlayer"` to the alphabetically-sorted `__all__`.

**Tests — `tests/learn/test_exemplar_player.py` + `tests/learn/test_load_audio_stereo.py`** (EDIT):

Remove the `pytestmark = pytest.mark.skip(...)` line from each. The 7 stubs in `test_exemplar_player.py` + the 3 stubs in `test_load_audio_stereo.py` must PASS against the production code.

For `test_exemplar_player_does_not_touch_playback_queue`: implement as a grep using `inspect.getsource(vibemix.learn.audio_cue)` + `assert "PlaybackQueue" not in src` — static check at test-time.

For `test_load_audio_stereo_preserves_native_sample_rate`: synthesize two tiny WAV files at 44100 + 48000 Hz via PyAV write mode (or scipy if already available; check `pyproject.toml` for scipy; it is NOT pinned — so use PyAV). Pattern reference: `library/audio_decode.py::load_audio_mono` shows the read side; the write side uses `av.open(path, "w", format="wav")` + `add_stream("pcm_s16le", rate=sr)` — see PyAV docs.

After all edits, run:
```
PYTHONPATH=src python3 -m pytest -q tests/learn/test_exemplar_player.py tests/learn/test_load_audio_stereo.py -x
```
All 10 stubs PASS. The `PlaybackQueue` grep gate is the load-bearing assertion.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/learn/test_exemplar_player.py tests/learn/test_load_audio_stereo.py -x && python3 -c "import inspect, vibemix.learn.audio_cue as m; assert 'PlaybackQueue' not in inspect.getsource(m), 'Pitfall 2: PlaybackQueue leaked into audio_cue.py'"</automated>
  </verify>
  <done>`audio_cue.py` exports `ExemplarPlayer` (in `__all__`); `audio_decode.py` exports `load_audio_stereo` (purely additive — `load_audio_mono` byte-identical); `__init__.py` re-exports `ExemplarPlayer`; 2 test modules flipped green; `PlaybackQueue` grep gate against `audio_cue.py` source is CLEAN (zero matches).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings persistence for learn.headphone_device_index (schema + codegen + config_store)</name>
  <files>
    tauri/ui/src/ipc/messages.schema.json,
    tauri/ui/src/ipc/validator.generated.mjs,
    src/vibemix/runtime/config_store.py,
    src/vibemix/learn/settings.py,
    src/vibemix/learn/__init__.py,
    tests/ipc/test_settings_set_envelope_learn_field.py
  </files>
  <behavior>
    - The string `"learn.headphone_device_index"` appears in the `SettingsSet.payload.field.enum` array in `tauri/ui/src/ipc/messages.schema.json`.
    - The `SettingsSet.payload.value.anyOf` already accepts `{"type": "integer", "minimum": 0}` AND `{"type": "null"}` — no new entries needed; verify both are present.
    - `tauri/ui/src/ipc/validator.generated.mjs` is regenerated by `cd tauri/ui && npm run codegen:ipc`; the pre-compiled validator accepts `{type: "ipc.settings.set", ts: <iso>, payload: {field: "learn.headphone_device_index", value: 2}}` AND `{... value: null}`.
    - `ConfigStore` stores the integer or null under `extra["learn.headphone_device_index"]` (mirror of the existing `mascot.*` / `recording.*` pattern — the `extra` dict already exists in the dataclass at `runtime/config_store.py:191`).
    - `read_learn_headphone_device_index() -> int | None` in `src/vibemix/learn/settings.py` reads from `load_config()` and returns the int (`>= 0`) or `None`.
    - `tests/ipc/test_settings_set_envelope_learn_field.py` flips green: the 3 stubs from Plan 93-01 PASS against the regenerated validator.
  </behavior>
  <action>
**Step 1 — `tauri/ui/src/ipc/messages.schema.json`** (EDIT):

Open the JSON schema. Locate the `SettingsSet` definition (around line 1264 — `"const": "ipc.settings.set"`). Inside `payload.properties.field.enum` (currently 11 entries from "voice" through "skill"), APPEND `"learn.headphone_device_index"` as the 12th element. Keep the order: existing 11 entries unchanged, new entry appended last.

The `payload.properties.value.anyOf` block already enumerates:
- `{"type": "string"}`
- `{"type": "integer", "minimum": 0}`
- `{"type": "boolean"}`
- `{"type": "null"}`

So integer ≥0 + null are already supported — DO NOT modify `value.anyOf`. ONLY extend the `field.enum`. Add a short `$comment` annotation on the enum line: `"Phase 93 (EXEMPLAR-04) — learn.headphone_device_index is integer >= 0 OR null (system default)."`.

**Step 2 — Codegen regenerate** (MANDATORY, per `feedback_schema_edit_needs_codegen_ipc`):

```
cd tauri/ui && npm run codegen:ipc
```

This regenerates `tauri/ui/src/ipc/validator.generated.mjs` (pre-compiled ajv). Stale validator = silent rejection of the new field. After codegen, `git diff tauri/ui/src/ipc/validator.generated.mjs` must show the validator now references `"learn.headphone_device_index"`.

**Step 3 — `src/vibemix/runtime/config_store.py`** (EDIT):

Open the file. The `ConfigStore` dataclass at line 150 already has an `extra: dict[str, Any] = field(default_factory=dict)` slot at line 191 — that is the namespaced-key pass-through.

Locate the `on_settings_set` / `apply_setting` / similar dispatcher (whichever handler routes `ipc.settings.set` payloads into `ConfigStore.extra`). If the existing dispatcher already accepts arbitrary `field` strings into `extra`, no change to the dispatch is needed — the schema enum widening alone unlocks the new key. Otherwise, add an explicit branch:
```python
if field == "learn.headphone_device_index":
    if value is not None and (not isinstance(value, int) or value < 0):
        raise ValueError("learn.headphone_device_index must be int >= 0 or null")
    self.extra["learn.headphone_device_index"] = value
```

Grep the file for `if field ==` / `match field:` / similar dispatchers — if found, integrate; if absent, the `extra`-dict pass-through already covers it.

**Step 4 — `src/vibemix/learn/settings.py`** (NEW):

SPDX header. Module docstring quoting REQ-ID EXEMPLAR-04. Single function:
```python
def read_learn_headphone_device_index() -> int | None:
    """Return the headphone output device index, or None for system default."""
    try:
        from vibemix.runtime.config_store import load_config
        cfg = load_config()
    except Exception:
        return None
    val = cfg.extra.get("learn.headphone_device_index") if cfg is not None else None
    if val is None:
        return None
    try:
        idx = int(val)
    except (TypeError, ValueError):
        return None
    return idx if idx >= 0 else None
```

**Step 5 — `src/vibemix/learn/__init__.py`** (EDIT):

- Add `from vibemix.learn.settings import read_learn_headphone_device_index` to the import block.
- Append the name to alphabetically-sorted `__all__`.

**Step 6 — `tests/ipc/test_settings_set_envelope_learn_field.py`** (EDIT):

Remove the `pytestmark = pytest.mark.skip(...)` line. The 3 stubs from Plan 93-01 must PASS:
1. `test_settings_set_field_enum_includes_learn_headphone_device_index` — `json.loads(open(...).read())` + path-walk into `definitions.SettingsSet.properties.payload.properties.field.enum`; assert `"learn.headphone_device_index"` is present.
2. `test_settings_set_value_accepts_int_or_null` — assert `value.anyOf` contains an entry matching `{"type": "integer", "minimum": 0}` AND one matching `{"type": "null"}` (both already present pre-edit).
3. `test_settings_state_carries_headphone_device_index_after_set` — call `read_learn_headphone_device_index()` after seeding `ConfigStore.extra["learn.headphone_device_index"] = 2` via the dataclass; assert it returns `2`. Also assert that seeding with `None` returns `None`, and with `-1` returns `None` (validation rejection).

After all edits:
```
cd tauri/ui && npm run build      # tsc --noEmit + vite build — confirm validator wires green
PYTHONPATH=src python3 -m pytest -q tests/ipc/test_settings_set_envelope_learn_field.py -x
```
Both must PASS.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/ipc/test_settings_set_envelope_learn_field.py -x && grep -q "learn.headphone_device_index" tauri/ui/src/ipc/validator.generated.mjs</automated>
  </verify>
  <done>`messages.schema.json` field enum has 12 entries (the new "learn.headphone_device_index" appended); `validator.generated.mjs` regenerated and contains the new field; `config_store.py` validates the value through the existing `extra` pass-through (no schema-drift); `learn/settings.py::read_learn_headphone_device_index` returns int | None; `__init__.py` re-exports it; the IPC test module flips green; `cd tauri/ui && npm run build` is clean (tsc exits 0, vite build emits the new validator output).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ipc.settings.set` payload → `ConfigStore.extra` | Settings drawer / wizard / external client crosses ws:8765 with the integer index. |
| `ExemplarPlayer.play(file_path)` → `load_audio_stereo` PyAV decode | Audio file path crosses into a C extension. |
| `ExemplarPlayer.play()` → second `sd.OutputStream` on user-picked device | Privacy: an unintentional system-output picks could leak music to speakers Kaan didn't expect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-01 | Tampering | `ipc.settings.set` accepts `learn.headphone_device_index` over ws:8765 | mitigate | ajv pre-compiled validator gates the envelope at the boundary; the field enum is `additionalProperties: false`; value must satisfy `anyOf` (int ≥0 OR null). Re-codegen the validator after schema edit (mandatory per `feedback_schema_edit_needs_codegen_ipc`). |
| T-93-03-02 | DoS | `load_audio_stereo` PyAV decode of malformed file | accept | Same posture as Plan 93-02 / T-93-02-02 — PyAV is in-tree, no DoS observed across 1500+ tracks. Outer callers wrap in try/except. |
| T-93-03-03 | Information Disclosure | Headphone device-index pointing at system output (speakers) instead of headphones | mitigate | DEFAULT is `None` (system default — user did NOT pick). The P97 wizard surface will warn-and-confirm before persisting; P93 only stores the value, never auto-picks. CC-BY bank tracks are public-domain-equivalent; library tracks have implicit privacy via their local disk path which `ExemplarPlayer.play` does NOT log over ws. |
| T-93-03-04 | Tampering | `PlaybackQueue` mistaken reuse | mitigate | Plan 93-03 includes a CI grep gate `tests/learn/test_exemplar_player.py::test_exemplar_player_does_not_touch_playback_queue` that fails if `audio_cue.py` ever imports `PlaybackQueue`. Pitfall 2 binding. |
| T-93-03-SC | Tampering | No new package installs | accept | Plan 93-03 uses ONLY existing pinned deps (sounddevice, PyAV via `[ai-local]` extra, numpy). |
</threat_model>

<verification>
After both tasks, run the standard combined gate:

```bash
source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q tests/learn/ tests/ipc/ tests/state/ tests/prompts/ tests/agent/ -x 2>&1 | tail -5
cd tauri/ui && npm run build
```

Expected:
- 3 of the 11 Plan 93-01 modules flipped RED→GREEN here (`test_exemplar_player.py`, `test_load_audio_stereo.py`, `test_settings_set_envelope_learn_field.py`).
- `audio_cue.py` source grep returns zero `PlaybackQueue` occurrences (Pitfall 2 mitigation).
- `validator.generated.mjs` references `"learn.headphone_device_index"` post-codegen.
- tsc exits 0; vite build emits the regenerated validator output.
- Phase 92 invariants stay green; `EVIDENCE_SOURCES` count still 9 (Plan 93-05 flips to 10).
</verification>

<success_criteria>
- `src/vibemix/learn/audio_cue.py` exists with `ExemplarPlayer` + `_choose_gain_db` + the 3 gain constants
- The `audio_cue.py` source contains ZERO `PlaybackQueue` references (grep-gate)
- `src/vibemix/library/audio_decode.py` exports `load_audio_stereo` (purely additive; `load_audio_mono` byte-identical)
- `messages.schema.json::SettingsSet.payload.field.enum` includes `"learn.headphone_device_index"` (12 entries total)
- `tauri/ui/src/ipc/validator.generated.mjs` regenerated and includes the new field
- `src/vibemix/runtime/config_store.py` validates `learn.headphone_device_index` (int ≥0 or null) through the existing `extra` pass-through
- `src/vibemix/learn/settings.py::read_learn_headphone_device_index() -> int | None` exists and is exported from `learn/__init__.py`
- 3 of 11 Plan 93-01 stub modules flipped green
- `cd tauri/ui && npm run build` clean (tsc exits 0)
- Full pytest suite: no new red, 3 fewer SKIPPED modules over the Plan 93-01 baseline
</success_criteria>

<output>
Create `.planning/phases/93-exemplar-engine-evidence-source/93-03-SUMMARY.md` when done. Summary lists `ExemplarPlayer` + `load_audio_stereo` exports, the `messages.schema.json` enum diff (11 → 12 entries), the codegen:ipc invocation timestamp, the `validator.generated.mjs` grep-line referencing the new field, and the 3 test modules flipped to green. Note that `§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` KAAN-ACTION (BlackHole / Multi-Output Device wizard) remains parked for Plan 97 — P93 only ships the setting key.
</output>