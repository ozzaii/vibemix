---
phase: 93-exemplar-engine-evidence-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/learn/test_exemplar_finder.py
  - tests/learn/test_compute_band_shares.py
  - tests/learn/test_band_share_store.py
  - tests/learn/test_exemplar_kick_guard.py
  - tests/learn/test_exemplar_packaged_fallback.py
  - tests/learn/test_exemplar_player.py
  - tests/learn/test_load_audio_stereo.py
  - tests/learn/test_exemplar_citation_schema_mirror.py
  - tests/learn/test_exemplar_grounding_e2e.py
  - tests/learn/test_cli_learn_exemplar.py
  - tests/ipc/test_settings_set_envelope_learn_field.py
autonomous: true
requirements: [EXEMPLAR-01, EXEMPLAR-02, EXEMPLAR-03, EXEMPLAR-04, EXEMPLAR-05]

must_haves:
  truths:
    - "Eleven new test files exist under tests/learn/ and tests/ipc/ as RED skeletons covering every Phase 93 acceptance row"
    - "Every test function carries an explicit reason='awaiting Plan 93-XX' module-level skip marker tied to the downstream plan that flips it green"
    - "Running the full pytest suite stays green (skips ride forward — no new red, no broken imports)"
    - "The 4-site lockstep + grounding e2e tests fail with named-Plan dependencies, NOT with bare AssertionError on missing source code"
    - "The Phase 92 invariant pin tests (no_new_ws_port / runtime_invariants / tutor_system_instruction_lock) still pass after Plan 93-01 lands"
  artifacts:
    - path: "tests/learn/test_exemplar_finder.py"
      provides: "Top-K determinism + tie-break + registry-write test stubs for ExemplarFinder.find()"
      min_lines: 30
    - path: "tests/learn/test_compute_band_shares.py"
      provides: "5-key dict shape + full-track FFT sanity stubs"
      min_lines: 25
    - path: "tests/learn/test_band_share_store.py"
      provides: "upsert + top_for_band roundtrip + band-allowlist ValueError stubs"
      min_lines: 30
    - path: "tests/learn/test_exemplar_kick_guard.py"
      provides: "3 synthetic-fixture stubs (clean / distorted / balanced) for _kick_correlation"
      min_lines: 70
    - path: "tests/learn/test_exemplar_packaged_fallback.py"
      provides: "Empty-library fallback + bank directory layout + NOTICE.md attribution stubs"
      min_lines: 30
    - path: "tests/learn/test_exemplar_player.py"
      provides: "_choose_gain_db threshold + sd.OutputStream-mock + active-session-guard scaffold stubs"
      min_lines: 30
    - path: "tests/learn/test_load_audio_stereo.py"
      provides: "Stereo (N, 2) float32 PyAV decode stub + mono→stereo upmix stub"
      min_lines: 20
    - path: "tests/learn/test_exemplar_citation_schema_mirror.py"
      provides: "All 5 site checks (1+2+2b+3+4) + lockstep loop stub"
      min_lines: 60
    - path: "tests/learn/test_exemplar_grounding_e2e.py"
      provides: "Fabricated [exemplar:bogus] vs real cite + multi-atom parse stubs"
      min_lines: 30
    - path: "tests/learn/test_cli_learn_exemplar.py"
      provides: "subprocess CLI smoke stub for `vibemix learn exemplar <band>` (existing fallback + unknown-band paths)"
      min_lines: 30
    - path: "tests/ipc/test_settings_set_envelope_learn_field.py"
      provides: "learn.headphone_device_index enum + value-type ajv-validator stub"
      min_lines: 20
  key_links:
    - from: "tests/learn/test_exemplar_*.py"
      to: "Plan 93-02 / 93-03 / 93-04 / 93-05"
      via: "pytestmark = pytest.mark.skip(reason='awaiting Plan 93-XX')"
      pattern: 'pytest\.mark\.skip\(reason="awaiting Plan 93-'
---

<objective>
Plant 11 RED-state test scaffolds that cover every Phase 93 acceptance row BEFORE any production code lands. Mirrors the Plan 92-02 precedent (Nyquist-compliant gated-skip pattern) so each downstream plan flips a named subset of skips → green when its production code arrives.

Purpose: every <verify><automated> command in plans 93-02..93-06 must run a test that already exists — no executor wastes context inventing a test command at write-time.

Output:
- 10 new files under `tests/learn/` covering EXEMPLAR-01..05 (engine + ranker + kick-guard + fallback + audio + 4-site mirror + grounding e2e + CLI)
- 1 new file under `tests/ipc/` covering EXEMPLAR-04 settings persistence (sits with the other ajv-validator tests)
- Each module starts with `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-XX — <what>")` so the suite stays GREEN today and downstream plans drop the marker line when they land
- ZERO touches to existing files (pure-additive island under `tests/learn/` and `tests/ipc/`); commit by NAMED paths only (concurrent-session discipline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-exemplar-engine-evidence-source/93-CONTEXT.md
@.planning/phases/93-exemplar-engine-evidence-source/93-RESEARCH.md
@tests/learn/test_progress_persistence.py
@tests/state/test_evidence_registry.py
@CLAUDE.md

<interfaces>
<!-- Imports / fixtures the stubs reference. Symbols don't EXIST yet —
     pytestmark skip prevents ImportError at collection. -->

From src/vibemix/state/evidence_registry.py (EXISTING — read-only here):
```python
EVIDENCE_SOURCES: frozenset[str]  # currently 9 sources; Plan 93-05 adds "exemplar"
_SOURCE_ALT: str                  # currently "ev|aud|midi|track|screen|mix|tend|key|recall"
EVIDENCE_CITATION_RE: re.Pattern[str]
def parse_citations(text: str) -> list[tuple[str, str]]
class EvidenceRegistry:
    def write(self, source: str, key: str, t_session: float) -> None
    def has(self, source: str, key: str, t_session: float, *, tol: float) -> bool
```

From src/vibemix/prompts/matrix.py (EXISTING — read-only here):
```python
CITATION_GRAMMAR_BLOCK: str  # Plan 93-05 appends [exemplar:<track_id>] form
```

From src/vibemix/learn/ (NOT YET EXISTENT — Plans 93-02..04 ship):
```python
# vibemix.learn.exemplar (Plan 93-02 + 93-03):
def compute_band_shares(audio_path: str) -> dict[str, float]
def _kick_correlation(samples: np.ndarray, sr: int) -> float
def _packaged_bank_dir() -> Path
def _fallback_for_band(band: str) -> tuple[str, str, str] | None
@dataclass
class ExemplarPick:
    track_id: str; file_path: str; band_score: float; reason: str
class ExemplarFinder:
    def __init__(self, *, registry: EvidenceRegistry | None = None) -> None
    def find(self, band: str, k: int = 1, t_session: float | None = None) -> list[ExemplarPick]

# vibemix.learn.band_share_store (Plan 93-02):
def init_schema(conn: sqlite3.Connection) -> None
def open_default_db() -> sqlite3.Connection
def upsert(conn, track_id, sub, low, mid, high, kick_corr, ts) -> None
def top_for_band(conn, band, k=3, max_kick_corr=0.8) -> list[tuple[str, float, float]]

# vibemix.learn.audio_cue (Plan 93-03):
def _choose_gain_db(master_rms: float) -> float
class ExemplarPlayer:
    def __init__(self, device_index: int, *, state: MusicState) -> None
    def can_play(self) -> bool
    def play(self, file_path: str, *, on_stop=None) -> None
    def stop(self) -> None

# vibemix.library.audio_decode (Plan 93-03):
def load_audio_stereo(path: str | Path) -> tuple[np.ndarray, int]
```

CC-BY bank target (Plan 93-04 ships scaffold; assets KAAN-ACTION):
- `src/vibemix/learn/assets/band_exemplars/{sub,low,mid,high}/*.mp3`
- `src/vibemix/learn/assets/band_exemplars/MANIFEST.json`
- `NOTICE.md` (repo root)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storage / engine / kick-guard / fallback test scaffolds (5 files)</name>
  <files>
    tests/learn/test_compute_band_shares.py,
    tests/learn/test_band_share_store.py,
    tests/learn/test_exemplar_kick_guard.py,
    tests/learn/test_exemplar_finder.py,
    tests/learn/test_exemplar_packaged_fallback.py
  </files>
  <action>
Create 5 RED-state test files matching the Validation Architecture rows in 93-RESEARCH.md §Phase Requirements → Test Map. Mirror the Plan 92-02 gated-skip precedent at `tests/learn/test_progress_persistence.py` (module-level `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-XX — <what>")`).

Each file SPDX header: `# SPDX-License-Identifier: Apache-2.0`. Each module docstring states REQ-ID + which downstream Plan flips it green.

`tests/learn/test_compute_band_shares.py` (EXEMPLAR-01, awaits 93-02):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-02 — compute_band_shares helper")`
- Stub `def test_compute_band_shares_returns_5_key_dict()` — calls `compute_band_shares(<synthetic mp3 fixture path>)` and asserts result is a dict with the exact 5 keys `{"sub_share", "low_share", "mid_share", "high_share", "kick_corr"}`, each `float`.
- Stub `def test_compute_band_shares_silent_track_zero_floats()` — calls on a synthetic silent buffer (all zeros encoded), asserts all 5 keys ≈ 0.0.
- Stub `def test_compute_band_shares_normalized_sub_low_mid_high_sum_one()` — for a real-looking synthetic mix, the four `*_share` floats sum to within 1e-3 of 1.0.
- Imports: `from vibemix.learn.exemplar import compute_band_shares` (will resolve in 93-02).

`tests/learn/test_band_share_store.py` (EXEMPLAR-01, awaits 93-02):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-02 — band_share_store schema + upsert + top_for_band")`
- Use `tmp_path` fixture; monkeypatch `vibemix.learn.band_share_store.DB_PATH` so the test never touches the real `~/.cache/vibemix/library-clap.db`.
- Stub `test_upsert_then_top_for_band_roundtrip()` — insert 3 synthetic rows with varying low_share + kick_corr; assert `top_for_band(conn, "low", k=2)` returns the two highest-low-share rows in DESC order with kick_corr filter applied.
- Stub `test_top_for_band_kick_guard_filters_correlated_tracks()` — insert one row with kick_corr=0.95 (high) and one with kick_corr=0.3 (low) sharing similar mid_share; `top_for_band(conn, "mid")` returns only the kick_corr=0.3 row.
- Stub `test_top_for_band_unknown_band_raises_value_error()` — `top_for_band(conn, "ultrasonic")` raises `ValueError`.
- Stub `test_top_for_band_tie_break_by_track_id_alphabetical()` — two rows identical low_share, alphabetically earliest track_id ranks first.
- Stub `test_upsert_idempotent_on_conflict()` — second upsert with same track_id overwrites first row.

`tests/learn/test_exemplar_kick_guard.py` (EXEMPLAR-02, awaits 93-02):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-02 — _kick_correlation helper")`
- Embed VERBATIM the 3 synthetic-fixture functions + 3 sub-tests from 93-RESEARCH.md Pattern 3 ("Synthetic test fixture"): `_make_kick_only(distortion=...)`, `_make_balanced_mid_track()`, plus `test_clean_sub_kick_no_mid_passes_guard`, `test_distorted_kick_fires_guard`, `test_balanced_mid_track_passes_guard`. Import path is `from vibemix.learn.exemplar import _kick_correlation` (resolves in 93-02).
- Add a fourth stub `test_too_short_track_returns_zero()` — input `samples.size < sr * 2`, expect `_kick_correlation` returns `0.0` (the early-exit fallback per RESEARCH Pattern 3).

`tests/learn/test_exemplar_finder.py` (EXEMPLAR-01 + EXEMPLAR-02, awaits 93-03):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-03 — ExemplarFinder.find()")`
- Stub `test_find_returns_top_k_from_library_when_floor_met()` — monkeypatch `top_for_band` to return 4 synthetic rows; assert `finder.find("low", k=2)` returns 2 `ExemplarPick` instances with deterministic order.
- Stub `test_find_falls_back_when_under_library_floor()` — monkeypatch `top_for_band` to return only 2 rows (<_LIBRARY_FLOOR=3); assert fallback path engages and returns the packaged pick with the honest-null reason verbatim from RESEARCH Pattern 5.
- Stub `test_find_writes_to_evidence_registry()` — pass a real `EvidenceRegistry`; assert after `finder.find("low")`, `registry.has("exemplar", <returned track_id>, <t_session>, tol=2.0)` is True. Invariant #2 grounding contract.
- Stub `test_find_synthetic_id_format_for_packaged_pick()` — fallback pick `track_id` matches `_packaged:<band>:<filename_stem>` (deterministic shape).
- Stub `test_find_empty_library_and_no_bank_returns_empty_list()` — both library and bank empty → returns `[]` (degraded-install behavior per RESEARCH Example 1).

`tests/learn/test_exemplar_packaged_fallback.py` (EXEMPLAR-03, awaits 93-04):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-04 — packaged CC-BY bank scaffold + NOTICE.md attribution")`
- Embed the 3 sub-tests VERBATIM from 93-RESEARCH.md Code Example 2: `test_empty_library_falls_back_to_packaged_bank`, `test_packaged_bank_directory_layout`, `test_manifest_attribution_present_in_notice`. Keep the per-test `pytest.skip("Packaged bank not yet shipped")` early-returns inside the body (graceful when KAAN-ACTION asset population is pending) — the OUTER module skip blocks the whole file today.

Do NOT inline the production module code; the goal is RED tests with named-Plan dependencies.

After writing, verify:
  1. `PYTHONPATH=src python3 -m pytest -q --collect-only tests/learn/test_compute_band_shares.py tests/learn/test_band_share_store.py tests/learn/test_exemplar_kick_guard.py tests/learn/test_exemplar_finder.py tests/learn/test_exemplar_packaged_fallback.py` collects without ImportError (the module-level skip prevents the missing-symbol crash).
  2. `PYTHONPATH=src python3 -m pytest -q tests/learn/test_compute_band_shares.py tests/learn/test_band_share_store.py tests/learn/test_exemplar_kick_guard.py tests/learn/test_exemplar_finder.py tests/learn/test_exemplar_packaged_fallback.py` reports the 5 modules SKIPPED (count parity matches the named test stubs).
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q --collect-only tests/learn/test_compute_band_shares.py tests/learn/test_band_share_store.py tests/learn/test_exemplar_kick_guard.py tests/learn/test_exemplar_finder.py tests/learn/test_exemplar_packaged_fallback.py 2>&1 | grep -v '^#' | grep -c 'awaiting Plan 93-' | awk '{ exit !($1 >= 5) }'</automated>
  </verify>
  <done>5 test files exist with module-level skip markers naming the downstream Plan, every stub function calls into the named-but-not-yet-existent symbol from `vibemix.learn.exemplar` / `vibemix.learn.band_share_store`, pytest collect-only shows zero ImportError and 5 SKIPPED modules.</done>
</task>

<task type="auto">
  <name>Task 2: Audio routing / settings / 4-site mirror / grounding / CLI scaffolds (6 files)</name>
  <files>
    tests/learn/test_exemplar_player.py,
    tests/learn/test_load_audio_stereo.py,
    tests/learn/test_exemplar_citation_schema_mirror.py,
    tests/learn/test_exemplar_grounding_e2e.py,
    tests/learn/test_cli_learn_exemplar.py,
    tests/ipc/test_settings_set_envelope_learn_field.py
  </files>
  <action>
Create 6 more RED-state files completing the test scaffolding wave. Same SPDX header + module docstring + named-Plan skip marker pattern as Task 1.

`tests/learn/test_exemplar_player.py` (EXEMPLAR-04, awaits 93-03):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-03 — ExemplarPlayer + _choose_gain_db")`
- Stub `test_choose_gain_db_default_silent_master()` — `_choose_gain_db(0.0)` returns `-12.0`.
- Stub `test_choose_gain_db_lowers_when_master_loud()` — `_choose_gain_db(0.7)` returns `-18.0` (above the 0.5 linear-RMS threshold).
- Stub `test_choose_gain_db_default_when_master_below_silent_rms()` — `_choose_gain_db(0.001)` returns `-12.0` (below SILENT_RMS guard).
- Stub `test_exemplar_player_opens_own_output_stream(monkeypatch)` — monkeypatch `sounddevice.OutputStream` to a `MagicMock`; build `ExemplarPlayer(device_index=2, state=<MusicState stub>)`; assert `.play(<wav fixture>)` creates an `OutputStream` with `device=2, channels=2, dtype="float32"` (NOT PlaybackQueue).
- Stub `test_exemplar_player_does_not_touch_playback_queue()` — assert nothing from `vibemix.audio.buffers.PlaybackQueue` is imported / instantiated by the player module (static check: `inspect.getsource(audio_cue).find("PlaybackQueue") == -1`).
- Stub `test_can_play_scaffold_returns_true_in_p93()` — `ExemplarPlayer(...).can_play()` returns `True` (P93 scaffolding; P96 wires the real guard).
- Stub `test_stop_is_idempotent()` — `.stop()` called twice without `.play()` does not raise.

`tests/learn/test_load_audio_stereo.py` (EXEMPLAR-04, awaits 93-03):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-03 — load_audio_stereo PyAV extension")`
- Stub `test_load_audio_stereo_returns_n2_float32()` — synthesize a small mono WAV file in `tmp_path`, call `load_audio_stereo(path)`, assert `(samples, sr)` with `samples.shape == (N, 2)` and `samples.dtype == np.float32` (mono input → stereo upmix).
- Stub `test_load_audio_stereo_preserves_native_sample_rate()` — render two synthetic fixtures at 44100 + 48000 Hz; assert returned `sr` matches each source.
- Stub `test_load_audio_stereo_raises_on_empty_input(tmp_path)` — write an empty/broken audio file; expect `ValueError` with "no audio frames" substring.

`tests/learn/test_exemplar_citation_schema_mirror.py` (EXEMPLAR-05, awaits 93-05):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-05 — [exemplar:] 4-site schema mirror")`
- Embed the 5 test functions VERBATIM from 93-RESEARCH.md Pattern 10 ("The 4-Site Lock Test"): `test_site_1_evidence_sources_frozenset_contains_exemplar`, `test_site_2_source_alt_regex_includes_exemplar`, `test_site_2b_evidence_citation_re_matches_exemplar_atom`, `test_site_3_citation_grammar_block_includes_exemplar`, `test_site_4_citation_strip_allow_list_includes_exemplar`, `test_all_four_sites_lockstep`.

`tests/learn/test_exemplar_grounding_e2e.py` (EXEMPLAR-05, awaits 93-05):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-05 — [exemplar:] grounding e2e (Invariant #2 binding)")`
- Embed VERBATIM from 93-RESEARCH.md Pattern 11 ("The Grounding E2E Test"): `test_fabricated_exemplar_id_not_in_registry_means_unmatched`, `test_parse_citations_extracts_exemplar_atom`, `test_parse_citations_extracts_exemplar_in_multi_atom`.

`tests/learn/test_cli_learn_exemplar.py` (cross-cutting CLI, awaits 93-06):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-06 — vibemix learn exemplar CLI dispatch")`
- Stub `test_learn_exemplar_unknown_band_exits_2()` — invoke `subprocess.run([python, "-m", "vibemix", "learn", "exemplar", "ultrasonic"])` with `cwd=PROJECT_ROOT`, env injecting `PYTHONPATH=src`; assert `returncode == 2` and stderr contains "unknown band".
- Stub `test_learn_exemplar_low_returns_zero_or_one(tmp_path)` — invoke `["python", "-m", "vibemix", "learn", "exemplar", "low"]`; assert `returncode in (0, 1)` (0 = real or fallback pick, 1 = no pick + no bank); stdout includes a "track:" / "path:" / "score:" / "why:" line block on 0.
- Stub `test_learn_help_advertises_exemplar_subcommand()` — invoke `["python", "-m", "vibemix", "learn", "bogus"]`; stderr lists `available: reset, exemplar <sub|low|mid|high>`.

`tests/ipc/test_settings_set_envelope_learn_field.py` (EXEMPLAR-04, awaits 93-03):
- `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-03 — learn.headphone_device_index settings persistence")`
- Stub `test_settings_set_field_enum_includes_learn_headphone_device_index()` — load `tauri/ui/src/ipc/messages.schema.json`, walk into `definitions.SettingsSet.properties.payload.properties.field.enum`; assert `"learn.headphone_device_index"` is present.
- Stub `test_settings_set_value_accepts_int_or_null()` — Python ajv-equivalent: assert the field's `value.anyOf` accepts both `{"type": "integer", "minimum": 0}` and `{"type": "null"}`.
- Stub `test_settings_state_carries_headphone_device_index_after_set()` — Python-side: instantiate the dataclass mirror (Plan 93-03 adds it) and assert the round-trip envelope shape; defer the live ws-bus round-trip to a separate slow-marked test if the integration is too heavy here.

If `tests/ipc/` does not exist, create `tests/ipc/__init__.py` first.

After writing, verify:
  1. `PYTHONPATH=src python3 -m pytest -q --collect-only tests/learn/test_exemplar_player.py tests/learn/test_load_audio_stereo.py tests/learn/test_exemplar_citation_schema_mirror.py tests/learn/test_exemplar_grounding_e2e.py tests/learn/test_cli_learn_exemplar.py tests/ipc/test_settings_set_envelope_learn_field.py` collects without ImportError.
  2. The full suite stays green: `source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q` reports 6 additional SKIPPED modules over the Plan 92 baseline, ZERO new red.
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q --collect-only tests/learn/test_exemplar_player.py tests/learn/test_load_audio_stereo.py tests/learn/test_exemplar_citation_schema_mirror.py tests/learn/test_exemplar_grounding_e2e.py tests/learn/test_cli_learn_exemplar.py tests/ipc/test_settings_set_envelope_learn_field.py 2>&1 | grep -v '^#' | grep -c 'awaiting Plan 93-' | awk '{ exit !($1 >= 6) }'</automated>
  </verify>
  <done>6 more test files exist with named-Plan skip markers; pytest collect-only is clean; the full suite reports 11 new SKIPPED modules total across Tasks 1+2, zero ImportError, zero new red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-stub authors → production-stub authors | Plan 93-01 stubs describe contracts plan 93-02..06 must satisfy; a drift here misleads downstream implementers. |
| pytest collector → CI gate | Stale import errors in stubs would silently red-poison the suite; the module-level skip + named-Plan reason is the load-bearing safeguard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering | tests/learn/test_exemplar_*.py stub imports | mitigate | `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-XX ...")` at MODULE level (not function level), so collection succeeds even when symbols don't exist. Verify via `pytest --collect-only`. |
| T-93-01-02 | Tampering | Synthetic kick-guard fixtures | mitigate | Copy the 93-RESEARCH.md Pattern 3 fixture VERBATIM (no creative deviation) so Plan 93-02's `_kick_correlation` implementation is unambiguously the reference. |
| T-93-01-03 | Information Disclosure | None — pure additive tests | accept | No secrets, no PII, no external network. |
| T-93-01-04 | Repudiation | Test stub drift vs RESEARCH | mitigate | Every assertion cites the RESEARCH §pattern that defines the contract; reviewers compare verbatim. |
| T-93-01-SC | Tampering | No new package installs | accept | Plan 93-01 installs ZERO new packages; the Package Legitimacy Gate is N/A. |
</threat_model>

<verification>
Per-task `<verify>` already includes `<automated>` commands. Phase-level:

```bash
source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q 2>&1 | tail -5
```

Expected: 11 more SKIPPED modules vs the Plan 92-06 baseline, ZERO new red, ZERO new ImportError.

Concurrent-session discipline check: ALL new paths are under `tests/learn/` or `tests/ipc/` — pure-additive island, ZERO shared-file edits.
</verification>

<success_criteria>
- 10 new test files under `tests/learn/test_exemplar_*.py` + 1 under `tests/ipc/`, each with `# SPDX-License-Identifier: Apache-2.0` + module docstring naming the REQ-ID(s) + downstream Plan
- Every module starts with `pytestmark = pytest.mark.skip(reason="awaiting Plan 93-XX — <what>")`
- The kick-guard synthetic fixtures + 3 sub-tests are VERBATIM copies of 93-RESEARCH.md Pattern 3
- The 4-site lock-test functions are VERBATIM copies of 93-RESEARCH.md Pattern 10
- The grounding e2e functions are VERBATIM copies of 93-RESEARCH.md Pattern 11
- Each stub function carries one or more `assert` lines that fail-with-named-Plan-dependency once the module skip is removed
- `pytest --collect-only` over the 11 new files succeeds without ImportError
- Full pytest suite reports zero new red, 11 new SKIPPED modules
</success_criteria>

<output>
Create `.planning/phases/93-exemplar-engine-evidence-source/93-01-SUMMARY.md` when done. Summary must list the 11 files, the corresponding REQ-ID coverage, and the downstream Plan each module's skip names.
</output>