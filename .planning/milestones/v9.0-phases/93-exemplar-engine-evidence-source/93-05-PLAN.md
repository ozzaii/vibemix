---
phase: 93-exemplar-engine-evidence-source
plan: 05
type: execute
wave: 4
depends_on: [93-04]
files_modified:
  - src/vibemix/state/evidence_registry.py
  - src/vibemix/prompts/matrix.py
  - src/vibemix/agent/dj_cohost.py
  - tests/state/test_evidence_registry.py
  - tests/learn/test_exemplar_citation_schema_mirror.py
  - tests/learn/test_exemplar_grounding_e2e.py
autonomous: true
requirements: [EXEMPLAR-05]

must_haves:
  truths:
    - "`EVIDENCE_SOURCES` is a frozenset of exactly 10 source identifiers (the original 9 + `exemplar`)"
    - "`_SOURCE_ALT` regex alternation token-set equals `EVIDENCE_SOURCES` (the silent-poisoning-hole pair lands in ONE commit)"
    - "`CITATION_GRAMMAR_BLOCK` lists `[exemplar:<track_id>]` as the 10th citation form alongside the existing 9"
    - "`agent/dj_cohost.py::_build_citation_strip` allow-list includes `\"exemplar\"` and the per-source verb-derivation switch carries an `elif source == \"exemplar\": verb = \"exemplar\"` branch"
    - "Fabricated `[exemplar:bogus]` cites strip the whole AI turn via the existing Phase 20 linter (Invariant #2 binding)"
    - "All 6 site-coverage tests in `test_exemplar_citation_schema_mirror.py` PASS; the 3 grounding e2e tests in `test_exemplar_grounding_e2e.py` PASS; the legacy `test_evidence_11_sources_constant_locked_GROUND02` is updated to 10 sources and stays green"
  artifacts:
    - path: "src/vibemix/state/evidence_registry.py"
      provides: "EDITED — sites 1+2 (frozenset + regex) lockstep; EBNF docstring extended"
      contains: "exemplar"
    - path: "src/vibemix/prompts/matrix.py"
      provides: "EDITED — site 3, CITATION_GRAMMAR_BLOCK adds `[exemplar:<track_id>]` form"
      contains: "[exemplar:"
    - path: "src/vibemix/agent/dj_cohost.py"
      provides: "EDITED — site 4, allow-list tuple + verb-derivation elif branch"
      contains: "exemplar"
    - path: "tests/state/test_evidence_registry.py"
      provides: "EDITED — `test_evidence_11_sources_constant_locked_GROUND02` now asserts 10 sources including `exemplar`"
      contains: "exemplar"
  key_links:
    - from: "src/vibemix/state/evidence_registry.py::EVIDENCE_SOURCES"
      to: "src/vibemix/state/evidence_registry.py::_SOURCE_ALT"
      via: "MUST land in SAME commit (silent-poisoning-hole pair — v6 commit 2016e36b precedent)"
      pattern: "exemplar"
    - from: "src/vibemix/prompts/matrix.py::CITATION_GRAMMAR_BLOCK"
      to: "EVIDENCE_SOURCES"
      via: "lockstep test `test_prompt_R_evidence_sources_in_grammar_block_lockstep` (existing, auto-extends)"
      pattern: "\\[exemplar:"
    - from: "src/vibemix/agent/dj_cohost.py::_build_citation_strip"
      to: "src/vibemix/state/evidence_registry.py::EVIDENCE_SOURCES"
      via: 'source not in ("ev", "mix", "midi", "key", "recall", "exemplar")'
      pattern: '"exemplar"'
---

<objective>
Land the 4-site `[exemplar:<track_id>]` schema mirror — the citation-grounding Invariant #2 binding for v9.0. This is the LOAD-BEARING gate that turns Plan 93-04's `ExemplarFinder.find()` writes into a fabricated-cite strip contract.

Per 93-RESEARCH.md Pattern 1 (verbatim v6 `[recall:]` precedent), sites 1+2 MUST land in ONE commit (the silent-poisoning-hole pair); sites 3 and 4 can be separate commits but stay inside this single plan.

Output:
- EDIT `src/vibemix/state/evidence_registry.py` — sites 1+2 atomic: append `"exemplar"` to `EVIDENCE_SOURCES` + append `|exemplar` to `_SOURCE_ALT` + extend the EBNF docstring. ONE commit.
- EDIT `src/vibemix/prompts/matrix.py` — site 3: append `[exemplar:<track_id>]` form to `CITATION_GRAMMAR_BLOCK` + bump the lock-step header count (9 → 10). Separate commit.
- EDIT `src/vibemix/agent/dj_cohost.py` — site 4: append `"exemplar"` to the `_build_citation_strip` allow-list tuple at line 255 + add the `elif source == "exemplar"` branch in the verb-derivation switch. Separate commit.
- EDIT `tests/state/test_evidence_registry.py` — extend the explicit 9-source assertion to 10 sources (the test currently hardcodes the frozenset literal at line 226-228).
- Remove skip markers from `tests/learn/test_exemplar_citation_schema_mirror.py` + `tests/learn/test_exemplar_grounding_e2e.py`; all 9 stubs (6 + 3) PASS against the production diffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-exemplar-engine-evidence-source/93-CONTEXT.md
@.planning/phases/93-exemplar-engine-evidence-source/93-RESEARCH.md
@src/vibemix/state/evidence_registry.py
@src/vibemix/prompts/matrix.py
@src/vibemix/agent/dj_cohost.py
@src/vibemix/state/citation_linter.py
@tests/state/test_evidence_registry.py
@tests/prompts/test_matrix.py
@CLAUDE.md

<interfaces>
<!-- Exact line numbers verified in tree. Hold this map sacred. -->

src/vibemix/state/evidence_registry.py:
  - Line 90-110: docstring stanza describing the 9 existing sources + lock-step comment.
  - Line 111-113: `EVIDENCE_SOURCES: frozenset[str] = frozenset({"ev","aud","midi","track","screen","mix","tend","key","recall"})`
  - Line 116-138: docstring + `_SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key|recall"` + `_INNER_ATOM = rf"(?:{_SOURCE_ALT}):[^\s,\]]+"`
  - Line 140-157: EBNF docstring (the `source := ...` rule listing the 9 alternatives) + EVIDENCE_CITATION_RE
  - The lock-step comment block at lines 120-131 is the SILENT-POISONING-HOLE warning: sites 1+2 must land together.

src/vibemix/prompts/matrix.py:
  - Line 93-110: lock-step comment block referencing EVIDENCE_SOURCES and counting "9" sources.
  - Line 110-148: `CITATION_GRAMMAR_BLOCK` string literal — the 9 Forms list.
  - Lines 118-127 show the existing 9 Form lines; line 127 is the `[recall:<record_id>] past-moment reference` line. The new `[exemplar:<track_id>]` form goes BELOW it as the 10th.

src/vibemix/agent/dj_cohost.py:
  - Line 176: `def _build_citation_strip(`
  - Line 240-256: the allow-list `if source not in ("ev", "mix", "midi", "key", "recall"): continue`. Edit to append `"exemplar"` → `("ev", "mix", "midi", "key", "recall", "exemplar")`.
  - Line 270-290: the per-source verb-derivation switch (elif source == "recall": ...). New elif branch goes AFTER the recall branch, BEFORE any default/fallback.
  - Line 1215+: existing recall-clearing logic; NOT edited in P93-05 (exemplar source uses time.time() per Plan 93-04 — registry self-prunes via existing rolling buffer; no cooldown wiring).

tests/state/test_evidence_registry.py:
  - Line 208-228: `test_evidence_11_sources_constant_locked_GROUND02`. Currently asserts `EVIDENCE_SOURCES == frozenset({"ev","aud","midi","track","screen","mix","tend","key","recall"})` — the literal LIST is the test contract; update to include `"exemplar"`. Also update the docstring count to 10 sources.

tests/prompts/test_matrix.py:
  - Line 514-525: `test_prompt_R_evidence_sources_in_grammar_block_lockstep` loops over EVIDENCE_SOURCES asserting `f"[{source}:"` is in `CITATION_GRAMMAR_BLOCK`. Auto-extends once Plan 93-05 lands all sites; NO test edit needed.

Verbatim research deliverables (lift directly):
- 93-RESEARCH.md "The 4-Site Mirror — Verbatim Code" section (lines 936-1036) — the EXACT diffs for sites 1+2 (atomic), site 3, site 4.
- 93-RESEARCH.md Pattern 10 — the 5 test bodies + lockstep loop (already copied verbatim into `tests/learn/test_exemplar_citation_schema_mirror.py` by Plan 93-01).
- 93-RESEARCH.md Pattern 11 — the 3 grounding e2e bodies (already in `tests/learn/test_exemplar_grounding_e2e.py` from Plan 93-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sites 1+2 ATOMIC commit — evidence_registry.py + the legacy count test</name>
  <files>
    src/vibemix/state/evidence_registry.py,
    tests/state/test_evidence_registry.py,
    tests/learn/test_exemplar_citation_schema_mirror.py
  </files>
  <behavior>
    - `EVIDENCE_SOURCES` is a `frozenset` of exactly 10 elements: `{"ev","aud","midi","track","screen","mix","tend","key","recall","exemplar"}`.
    - `_SOURCE_ALT` is the string `"ev|aud|midi|track|screen|mix|tend|key|recall|exemplar"` — alternation token-set matches the frozenset.
    - The EBNF `source := ...` docstring rule lists `'exemplar'` as the 10th alternative.
    - `EVIDENCE_CITATION_RE.fullmatch("[exemplar:library:foo-bar]") is not None` — the compiled regex now matches the new atom shape.
    - `parse_citations("[exemplar:library:foo]")` returns `[("exemplar", "library:foo")]` (inner colon survives as body, mirroring `recall:<record_id>` behavior).
    - The legacy `test_evidence_11_sources_constant_locked_GROUND02` test in `tests/state/test_evidence_registry.py` is updated to assert the 10-source frozenset literal AND its docstring updates the count.
    - Sites 1, 2, and 2b in `tests/learn/test_exemplar_citation_schema_mirror.py` PASS (the first three test functions).
  </behavior>
  <action>
**STEP A — Edit `src/vibemix/state/evidence_registry.py` (sites 1+2 atomic):**

Locate the docstring stanza at lines 96-110. Between the existing `recall` block (lines 100-107) and the `SCHEMA-MIRROR:` lock-step comment (lines 108-110), insert a new stanza for `exemplar`:

```
#: ``exemplar`` = packaged or library band-exemplar reference, body
#: ``<track_id>`` — Phase 93 (EXEMPLAR-05). Existence-only retrieval-time
#: source that makes a fabricated ``[exemplar:<id>]`` uncitable-by-construction:
#: it joins the linter's EXISTENCE-ONLY set purely by being IN this frozenset
#: and ABSENT from ``citation_linter.py::_TIME_KEYED_SOURCES`` (mirrors
#: ``key``/``track``/``recall``). The ExemplarFinder writes
#: ``("exemplar", track_id, t_session)`` BEFORE the LLM emits the cite so
#: unregistered ids strip the turn (Plan 93-04 wiring).
```

Update line 111-113 — append `"exemplar"` to the frozenset literal:
```python
EVIDENCE_SOURCES: frozenset[str] = frozenset(
    {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar"}
)
```

Update line 137 — append `|exemplar` to `_SOURCE_ALT`:
```python
_SOURCE_ALT = "ev|aud|midi|track|screen|mix|tend|key|recall|exemplar"
```

DO NOT touch `_INNER_ATOM` (line 138). The body-regex `[^\s,\]]+` already accepts `library:Marlon-Atlas` style bodies (inner colon survives, no whitespace/comma/bracket — same posture as recall and key).

Update the EBNF docstring at lines 140-157. The `source := ...` rule (lines 144-145) currently reads `'ev' | 'aud' | 'midi' | 'track' | 'screen' | 'mix' | 'tend' | 'key' | 'recall'`. Append `| 'exemplar'`:
```
#:   source   := 'ev' | 'aud' | 'midi' | 'track' | 'screen' | 'mix' | 'tend'
#:             | 'key' | 'recall' | 'exemplar'
```

Append a new body-rule stanza below the existing `recall-body` line (line 149-150):
```
#:   exemplar-body := <track_id>          # e.g. "library:Marlon Hoffstadt - Atlas"
#:                                        # or "_packaged:low:track_03" — full
#:                                        # body survives parse_citations (no
#:                                        # whitespace/comma/bracket; P93)
```

Update line 152-153 wording: change "Matches the 9 single-citation forms" to "Matches the 10 single-citation forms".

Update the lock-step asymmetry comment at lines 133-136 (`memory/ingest.py` 8-source asymmetry note): the `recall` was retrieval-time-only; `exemplar` is ALSO retrieval-time-only (registered by `ExemplarFinder.find()`, never appears in stored past reactions). So the asymmetry STAYS at "memory/ingest stays at 8 sources" — `exemplar` does NOT join the ingest extractor allowlist. Re-confirm the comment language to mention both `recall` and `exemplar`.

**STEP B — Edit `tests/state/test_evidence_registry.py`** to update the count assertion.

Locate `test_evidence_11_sources_constant_locked_GROUND02` at line 208-228. The function asserts the literal 9-element frozenset. Update to 10:

```python
def test_evidence_11_sources_constant_locked_GROUND02() -> None:
    """EVIDENCE_SOURCES is a frozenset of exactly the 10 source identifiers.

    Phase 59 (DECK-03) added the dedicated ``key`` harmonic source to the
    original 7 CONTEXT.md sources. Phase 65 (RECALL-01) added ``recall`` —
    the past-session callback source. Phase 93 (EXEMPLAR-05) adds
    ``exemplar`` — the band-exemplar reference source that makes a
    fabricated ``[exemplar:<id>]`` uncitable-by-construction (existence-only,
    mirrors ``recall``/``key``/``track``).

    The frozenset is the schema-mirror source-of-truth — the regex
    alternation, EBNF docstring, prompt grammar block and citation-strip
    whitelist all carry ``exemplar`` in lock-step. (Adding it here but
    NOT to ``_SOURCE_ALT`` would be the silent poisoning hole the
    milestone exists to prevent.)
    """
    assert isinstance(EVIDENCE_SOURCES, frozenset)
    assert EVIDENCE_SOURCES == frozenset(
        {"ev", "aud", "midi", "track", "screen", "mix", "tend", "key", "recall", "exemplar"}
    )
```

KEEP THE FUNCTION NAME (`test_evidence_11_sources_constant_locked_GROUND02`) UNCHANGED — the historical name is the deliberate trail (Phase 11 GROUND-02 lock); only the literal + docstring update. (If a future linter complains about the "11" prefix being a phase number, ignore — the name is load-bearing for git-grep history.)

**STEP C — Flip the schema-mirror test partial:**

Open `tests/learn/test_exemplar_citation_schema_mirror.py`. Remove the module-level `pytestmark = pytest.mark.skip(...)` line. After Steps A + B, tests `test_site_1_evidence_sources_frozenset_contains_exemplar`, `test_site_2_source_alt_regex_includes_exemplar`, `test_site_2b_evidence_citation_re_matches_exemplar_atom`, and `test_all_four_sites_lockstep` should all PASS. Tests `test_site_3_*` and `test_site_4_*` STILL FAIL (matrix.py + dj_cohost.py not yet edited — Tasks 2 + 3 land those).

Run:
```
PYTHONPATH=src python3 -m pytest -q tests/state/test_evidence_registry.py tests/learn/test_exemplar_citation_schema_mirror.py -k "site_1 or site_2 or 11_sources or lockstep" -x
```

Expected: the 4 atomic-site tests + the existing 11_sources count test PASS. The site_3/site_4 tests fail (expected — landed in Tasks 2 + 3). Use `-k` to filter so this task's commit is verifiable in isolation.

**Commit message:**
```
feat(93-05): add exemplar evidence source to EVIDENCE_SOURCES + _SOURCE_ALT (sites 1+2)

ATOMIC — sites 1 and 2 in ONE commit (the silent-poisoning-hole pair —
mirrors v6 commit 2016e36b for [recall:]).

EVIDENCE_SOURCES 9 → 10, _SOURCE_ALT updated, EBNF docstring extended,
test_evidence_11_sources_constant_locked_GROUND02 updated to 10 sources.

Sites 3 (matrix.py CITATION_GRAMMAR_BLOCK) + 4 (dj_cohost.py
_build_citation_strip) land in follow-up commits inside Plan 93-05.

Plan 93-05 / EXEMPLAR-05 / Invariant #2 binding.
```
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/state/test_evidence_registry.py tests/learn/test_exemplar_citation_schema_mirror.py -k "site_1 or site_2 or 11_sources or lockstep" -x</automated>
  </verify>
  <done>One atomic commit lands sites 1+2 + the legacy 9-source test bump + the module-level skip removal in `test_exemplar_citation_schema_mirror.py`. The 4 atomic-site assertions + the existing count test PASS. Site_3 + Site_4 tests stay RED (will flip in Tasks 2 + 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Site 3 — CITATION_GRAMMAR_BLOCK in prompts/matrix.py</name>
  <files>
    src/vibemix/prompts/matrix.py
  </files>
  <behavior>
    - `CITATION_GRAMMAR_BLOCK` includes the literal substring `[exemplar:` somewhere in its body.
    - `[exemplar:<track_id>]` appears in the "Forms" list as the 10th form, with an example like `e.g. [exemplar:library:Marlon Hoffstadt - Atlas]`.
    - The lock-step header comment count updates from "9" to "10" sources.
    - The auto-extending lockstep test `tests/prompts/test_matrix.py::test_prompt_R_evidence_sources_in_grammar_block_lockstep` PASSES with the new source.
    - The site 3 test in `tests/learn/test_exemplar_citation_schema_mirror.py::test_site_3_citation_grammar_block_includes_exemplar` PASSES.
  </behavior>
  <action>
Open `src/vibemix/prompts/matrix.py`. The `CITATION_GRAMMAR_BLOCK` constant is at line 110 (currently a multi-line `"""--- CITATION GRAMMAR ...---"""` triple-quoted string).

Locate the lock-step header comment ABOVE the constant (line 93-110). It contains a phrase like `The 9 source forms are kept in lock-step with EVIDENCE_SOURCES`. Update count `9` → `10`. Append phrase `+ exemplar Phase 93 / EXEMPLAR-05` to the existing parenthetical chain (mirror the existing pattern: `, + recall added Phase 65 / RECALL-01, + exemplar added Phase 93 / EXEMPLAR-05`).

Locate the "Forms (each is a single citation..." list at lines 118-127. The list currently ends with `[recall:<record_id>] past-moment reference, e.g. [recall:20260520-2200:7]`. Append the 10th line BELOW the recall line:

```
  [exemplar:<track_id>]  band-exemplar track, e.g. [exemplar:library:Marlon Hoffstadt - Atlas]
```

Keep the same 2-space indentation as the other entries; keep alignment so the example column stays readable.

Run:
```
PYTHONPATH=src python3 -m pytest -q tests/prompts/test_matrix.py tests/learn/test_exemplar_citation_schema_mirror.py -k "grammar or site_3 or prompt_R" -x
```

Expected: the auto-extending lockstep test (`test_prompt_R_evidence_sources_in_grammar_block_lockstep`) PASSES. The site_3 test PASSES.

**Commit message:**
```
feat(93-05): add [exemplar:<track_id>] form to CITATION_GRAMMAR_BLOCK (site 3)

Mirrors v6 commit 977c0140 for [recall:]. Header count 9 → 10; new line
added to the Forms list as the 10th entry.

Plan 93-05 / EXEMPLAR-05.
```
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/prompts/test_matrix.py tests/learn/test_exemplar_citation_schema_mirror.py -k "grammar or site_3 or prompt_R" -x</automated>
  </verify>
  <done>`CITATION_GRAMMAR_BLOCK` now lists `[exemplar:<track_id>]` as the 10th form; the auto-extending lockstep test in `tests/prompts/test_matrix.py` PASSES; the explicit site_3 test in `tests/learn/test_exemplar_citation_schema_mirror.py` PASSES; one focused commit lands the diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Site 4 — _build_citation_strip in agent/dj_cohost.py + grounding e2e green</name>
  <files>
    src/vibemix/agent/dj_cohost.py,
    tests/learn/test_exemplar_grounding_e2e.py,
    tests/learn/test_exemplar_citation_schema_mirror.py
  </files>
  <behavior>
    - The allow-list tuple at `dj_cohost.py:255` reads `if source not in ("ev", "mix", "midi", "key", "recall", "exemplar"): continue` — 6-element tuple.
    - The per-source verb-derivation switch (after the `elif source == "recall":` branch around line 277-290) carries a new `elif source == "exemplar":` branch that sets `verb = "exemplar"` (single lowercase word; satisfies the locked verb-format regex `^[a-z]+( [a-z]+){0,2}$`).
    - Site 4 test in `tests/learn/test_exemplar_citation_schema_mirror.py::test_site_4_citation_strip_allow_list_includes_exemplar` PASSES (the regex-AST grep matches the 6-tuple shape).
    - All 3 tests in `tests/learn/test_exemplar_grounding_e2e.py` PASS — the fabricated-cite-strips-turn contract is now fully wired (Invariant #2 binding closed).
  </behavior>
  <action>
**Step A — Edit `src/vibemix/agent/dj_cohost.py`:**

Locate the allow-list at line 255. Currently:
```python
if source not in ("ev", "mix", "midi", "key", "recall"):
    continue
```

Edit to:
```python
if source not in ("ev", "mix", "midi", "key", "recall", "exemplar"):
    continue
```

Locate the per-source verb-derivation switch. The existing `recall` branch is at lines 277-290:
```python
elif source == "recall":
    # ... Phase 65/66 docstring ...
    verb = "recall"
```

Add a new branch IMMEDIATELY AFTER the recall branch (before any default/fallback):
```python
elif source == "exemplar":
    # Phase 93 (EXEMPLAR-05) — mirrors the ``recall`` and ``key`` precedents
    # above for opaque/structured bodies. The ``exemplar`` body shape is
    # ``<track_id>`` (e.g. ``library:Marlon Hoffstadt - Atlas`` or
    # ``_packaged:low:track_03``). The full track_id rides in ``event_id``
    # for the click→tutor-context deep-link. The chip verb is a fixed
    # letters-only ``"exemplar"`` label — deriving a verb from a track-id
    # string is meaningless and would leak embedding-internal values to
    # the UI surface (P93-RESEARCH.md §4-Site Mirror Pattern). A single
    # lowercase word trivially matches the locked verb-format regex
    # ``^[a-z]+( [a-z]+){0,2}$``.
    verb = "exemplar"
```

DO NOT add `exemplar` to any cooldown-clearing logic at line 1215+ (`self._registry.clear_source("recall")` etc.) — the exemplar source is registered fresh per `find()` call via `t_session=time.time()`; the registry's existing 50-turn rolling buffer self-prunes naturally. Stale-cite drift is not a concern in P93 (it might become one in P94 if a lesson re-runs over many turns; surface as `§EXEMPLAR-COOLDOWN` ride-forward IF Kaan ear-pass on Course 1.14 demonstrates the issue).

**Step B — Flip the grounding e2e + remaining mirror tests:**

Open `tests/learn/test_exemplar_grounding_e2e.py`. Remove the `pytestmark = pytest.mark.skip(...)` line. The 3 tests from Plan 93-01 (verbatim from RESEARCH Pattern 11) should now ALL pass:
- `test_fabricated_exemplar_id_not_in_registry_means_unmatched`
- `test_parse_citations_extracts_exemplar_atom`
- `test_parse_citations_extracts_exemplar_in_multi_atom`

The site_4 + lockstep tests in `tests/learn/test_exemplar_citation_schema_mirror.py` should also pass now (Task 1 already removed the module-level skip — Tasks 2 + 3 just complete the green flips).

Run:
```
PYTHONPATH=src python3 -m pytest -q tests/learn/test_exemplar_citation_schema_mirror.py tests/learn/test_exemplar_grounding_e2e.py tests/state/test_evidence_registry.py tests/prompts/test_matrix.py tests/agent/ -x
```

Expected:
- 6 / 6 tests in `test_exemplar_citation_schema_mirror.py` PASS
- 3 / 3 tests in `test_exemplar_grounding_e2e.py` PASS
- All previously-green tests in `test_evidence_registry.py` + `test_matrix.py` + `tests/agent/` stay green
- `test_fabricated_recall_strips_turn` (the v6 grounding precedent at `tests/agent/test_dj_cohost_linter.py:240-380`) stays green (we didn't break the recall path)

**Commit message:**
```
feat(93-05): add exemplar to _build_citation_strip + grounding e2e green (site 4)

Mirrors v6 commit 0bfc8bd0 for [recall:]. Allow-list 5-tuple → 6-tuple
(adds "exemplar"); verb-derivation switch gets the `elif source ==
"exemplar"` branch setting `verb = "exemplar"`.

With Tasks 1 + 2 + 3 landed, all 4 sites mirror and the Invariant #2
binding holds: a fabricated [exemplar:bogus] strips the whole turn via
the existing Phase 20 CitationLinter.

Plan 93-05 / EXEMPLAR-05 complete.
```
  </action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest -q tests/learn/test_exemplar_citation_schema_mirror.py tests/learn/test_exemplar_grounding_e2e.py tests/state/test_evidence_registry.py tests/prompts/test_matrix.py tests/agent/ -x</automated>
  </verify>
  <done>`dj_cohost.py` allow-list is now `("ev","mix","midi","key","recall","exemplar")`; the verb-derivation switch carries the exemplar elif branch; the 6 schema-mirror lock tests + 3 grounding e2e tests + all pre-existing evidence_registry / matrix / agent tests stay GREEN; the 4-site mirror is complete and Invariant #2 binds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gemini LLM tutor turn → `parse_citations()` extraction → `CitationLinter` validation | The grammar regex is the boundary that gates which atoms become cite-candidates; the registry-existence check is the boundary that gates which atoms survive. |
| Plan 93-04's `ExemplarFinder.find()` write → Plan 93-05's `EvidenceRegistry.has()` lookup | The grounding contract bridge. Sites 1+2 atomic landing is what makes the lookup actually run on `[exemplar:]` atoms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-01 | Spoofing / Misinformation | Fabricated `[exemplar:bogus]` riding through unmatched | mitigate | **THIS is the Invariant #2 binding for v9.0.** Sites 1+2 atomic landing closes the silent-poisoning-hole pair (the comment at `evidence_registry.py:120-131` explicitly names this); a fabricated atom now matches the regex, gets seen by the linter, fails the existence check, strips the whole turn. Pinned by `tests/learn/test_exemplar_grounding_e2e.py`. |
| T-93-05-02 | Tampering | Sites 1+2 split across commits | mitigate | This plan's Task 1 carries ONE atomic commit for sites 1+2 (the v6 `2016e36b` precedent). Pre-merge CI MUST run the schema-mirror lock test on every PR head to catch any rogue rebase that splits them. |
| T-93-05-03 | Repudiation | Drift between `EVIDENCE_SOURCES` (the frozenset) and `CITATION_GRAMMAR_BLOCK` (the prompt grammar) | mitigate | The existing auto-extending lockstep test in `tests/prompts/test_matrix.py:514-525` loops over `EVIDENCE_SOURCES`; auto-fails when any source is missing from the grammar block. |
| T-93-05-04 | Information Disclosure | Verb-derivation leak of track-id internals to UI chip surface | mitigate | The `verb = "exemplar"` fixed-string branch (per the comment block) explicitly avoids deriving a verb from the `event_id` body — `_packaged:low:foo` doesn't leak through the chip. |
| T-93-05-SC | Tampering | No new package installs | accept | Plan 93-05 is pure-code edit on existing modules. ZERO new deps. |
</threat_model>

<verification>
After all three tasks:

```bash
source .venv/bin/activate && PYTHONPATH=src python3 -m pytest -q -x 2>&1 | tail -10
```

Expected:
- Full suite GREEN. ZERO new red.
- The schema-mirror test in `tests/learn/test_exemplar_citation_schema_mirror.py` carries 6 GREEN tests (sites 1, 2, 2b, 3, 4, lockstep).
- The grounding e2e test in `tests/learn/test_exemplar_grounding_e2e.py` carries 3 GREEN tests.
- The legacy `test_evidence_11_sources_constant_locked_GROUND02` asserts 10 sources and is GREEN.
- The auto-extending lockstep test `tests/prompts/test_matrix.py::test_prompt_R_evidence_sources_in_grammar_block_lockstep` PASSES with 10 sources.
- The v6 `test_fabricated_recall_strips_turn` (`tests/agent/test_dj_cohost_linter.py:240-380`) STAYS GREEN — recall path not regressed.
- Phase 92 invariant pins (no_new_ws_port / runtime_invariants / tutor_system_instruction_lock) STAY GREEN.

`git log --oneline -3` shows three commits in order: sites 1+2 atomic → site 3 → site 4. Per Plan 93-05 commit-pattern from RESEARCH §4-Site Mirror Pattern.
</verification>

<success_criteria>
- `EVIDENCE_SOURCES` is `frozenset({"ev","aud","midi","track","screen","mix","tend","key","recall","exemplar"})` (10 elements)
- `_SOURCE_ALT == "ev|aud|midi|track|screen|mix|tend|key|recall|exemplar"`
- The EBNF docstring's `source := ...` rule includes `'exemplar'`
- `EVIDENCE_CITATION_RE.fullmatch("[exemplar:library:foo]") is not None`
- `parse_citations("[exemplar:library:foo]") == [("exemplar", "library:foo")]`
- `CITATION_GRAMMAR_BLOCK` contains the literal substring `[exemplar:` (10th Form line)
- The header comment counts "10" sources
- `_build_citation_strip`'s allow-list tuple has 6 elements: `("ev","mix","midi","key","recall","exemplar")`
- The verb-derivation switch carries an `elif source == "exemplar"` branch with `verb = "exemplar"`
- 6 schema-mirror tests + 3 grounding e2e tests all PASS
- `test_evidence_11_sources_constant_locked_GROUND02` asserts 10 sources and is GREEN
- `test_prompt_R_evidence_sources_in_grammar_block_lockstep` PASSES with 10 sources
- The v6 `test_fabricated_recall_strips_turn` STAYS GREEN (no recall regression)
- Three commits landed (sites 1+2 atomic / site 3 / site 4)
- Phase 92 invariants STAY GREEN
- Full pytest suite: no new red, 2 fewer SKIPPED modules over Plan 93-04 baseline
</success_criteria>

<output>
Create `.planning/phases/93-exemplar-engine-evidence-source/93-05-SUMMARY.md` when done. Summary lists:
- The three commit shas (sites 1+2 atomic / site 3 / site 4)
- Confirmation that `EVIDENCE_SOURCES.__len__() == 10`
- The 9 schema-mirror + grounding-e2e tests now passing (named explicitly)
- The Invariant #2 binding statement: "fabricated `[exemplar:bogus]` strips the whole turn"
- A one-line note: with Plan 93-05 landed, all Plan 93-01 stub modules covering the 4-site mirror + grounding e2e are GREEN; the remaining Plan 93-01 stub (`test_cli_learn_exemplar.py`) flips green in Plan 93-06.
</output>