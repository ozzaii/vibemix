---
phase: 91-controller-renderer-midi-mirror
plan: 04
type: execute
wave: 2
depends_on: [91-01, 91-02]
files_modified:
  - tauri/src-tauri/src/learn_window.rs
  - tauri/src-tauri/src/main.rs
autonomous: true
requirements: [RENDER-07]
must_haves:
  truths:
    - Invoking the `open_learn_window` Tauri command from any allowlisted webview opens a new WebviewWindow with label `learn` and title `Learn — vibemix` (or focuses an existing one — single-window semantics).
    - The Learn window does NOT spawn a second Python sidecar — it connects to the SAME ws:8765 the live deck uses (Invariant #4 preserved).
    - The `Cmd+Tab` (macOS) / `Alt+Tab` (Windows) cycle includes the Learn window when it's open (verifiable manually; the WebviewWindow defaults handle this).
  artifacts:
    - path: tauri/src-tauri/src/learn_window.rs
      provides: Rust module declaring `LEARN_WINDOW_LABEL: &str = "learn"` const + `open_learn_window(app: AppHandle)` Tauri command + a single Rust unit test pinning the label invariant.
      min_lines: 50
      contains: LEARN_WINDOW_LABEL
    - path: tauri/src-tauri/src/main.rs
      provides: Existing `main.rs` gains `mod learn_window;` declaration + `learn_window::open_learn_window` in the `invoke_handler` macro's `generate_handler![...]` list.
      contains: learn_window
  key_links:
    - from: tauri/src-tauri/src/learn_window.rs
      to: tauri/ui/learn.html
      via: WebviewUrl::App("learn.html".into())
      pattern: learn\\.html
    - from: tauri/src-tauri/src/main.rs
      to: tauri/src-tauri/src/learn_window.rs
      via: mod registration + invoke_handler entry
      pattern: learn_window
---

<objective>
Land the Tauri Rust shell half of P91: a new `tauri/src-tauri/src/learn_window.rs` module declaring `LEARN_WINDOW_LABEL: &str = "learn"`, exposing `open_learn_window(app: AppHandle)` as a Tauri command that opens a WebviewWindow (or focuses the existing one — single-window semantics matching `debrief_window.rs`), AND the 3 surgical insertions into `tauri/src-tauri/src/main.rs` (mod declaration + invoke_handler entry).

Purpose: With Plan 03 emitting envelopes onto ws:8765 and Plan 01's `capabilities/default.json` allowlisting the command, this plan gives the user the physical UI surface — a separate window where Plan 05's frontend code will mount. Without this Rust module, `cargo tauri dev` cannot open the Learn window at all.

Output: 1 new Rust file (~80 lines per research §Code Example 2) + 2 small additions to `main.rs`. After this plan, `cd tauri/src-tauri && cargo test learn_window` passes 2 Rust tests; `cargo check` confirms the module integrates cleanly into the existing Tauri 2.x app. The Tauri command can be invoked from any webview that has it on its capabilities allowlist (Plan 01 lands the allowlist entry — the round-trip is complete after both Plan 01 and Plan 04 land).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-controller-renderer-midi-mirror/91-CONTEXT.md
@.planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md
@.planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md
@tauri/src-tauri/src/debrief_window.rs
@tauri/src-tauri/src/main.rs

<interfaces>
The canonical precedent is `tauri/src-tauri/src/debrief_window.rs` (384 lines). For P91 we ship a TRIMMED mirror — research §Pattern 7 enumerates the diff:

| Aspect | debrief_window.rs | learn_window.rs (P91) |
|--------|-------------------|------------------------|
| Window label const | `"debrief"` | `"learn"` |
| Window title | `format!("Debrief — {session_label}")` | `"Learn — vibemix"` (static) |
| URL | `format!("debrief.html?session={url_encoded}")` | `"learn.html"` (no query params) |
| Sidecar spawn | Spawns NEW Python sidecar with `--debrief <session_dir>` | **NO sidecar spawn** — Learn shares the main sidecar |
| Session-dir validation | `recordings::validate_under_root` | **None** |
| Deep-link payload | `DebriefDeepLink` Optional | **None** |
| Sidecar handle State | `DebriefSidecarHandle` | **None** |
| Close-handler kill | `kill_debrief_child(&app_for_close)` | **None** |
| Crash watcher | Forwards `CommandEvent::Terminated` | **None** |
| Default inner_size | 1280.0 × 720.0 | 1280.0 × 720.0 ✓ |
| Min inner_size | 960.0 × 540.0 | 960.0 × 540.0 ✓ |

Net effect: `learn_window.rs` is much smaller (~80 lines vs 384). The pattern is just "open a new WebviewWindow pointing at `learn.html` — no sidecar lifecycle needed". Use research §Code Example 2 as the verbatim spec.

`tauri/src-tauri/src/main.rs` existing `mod` list (immediately before `mod ws_client;`):
```
mod config;
mod debrief_window;
mod debug_log;
... mod wizard_cmds;
mod ws_client;
```
Insert `mod learn_window;` alphabetically (between `mod hotkey;` and `mod library_cmds;`).

`main.rs` `invoke_handler` already enumerates `debrief_window::open_debrief_window` inside `generate_handler![...]`. Add `learn_window::open_learn_window` to the same list (preserve existing handler order; append).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author `tauri/src-tauri/src/learn_window.rs` per research §Code Example 2 — `LEARN_WINDOW_LABEL` const + `open_learn_window` Tauri command + 2 unit tests</name>
  <files>tauri/src-tauri/src/learn_window.rs</files>
  <read_first>
    - tauri/src-tauri/src/debrief_window.rs (the 384-line precedent — read the WebviewWindowBuilder usage + the focus-existing pattern + the `#[tauri::command]` annotation shape; ignore the sidecar-spawn / deep-link / session-dir logic which we explicitly drop per §Pattern 7)
    - .planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md §Code Example 2 (the verbatim ~50-line `learn_window.rs` source; use it as the spec)
    - .planning/phases/91-controller-renderer-midi-mirror/91-UI-SPEC.md §Design System (window title "Learn — vibemix" — em-dash; default 1280×720, min 960×540)
    - tauri/ui/tests/learn/test_learn_window_label.spec.ts (Plan 02 stub — TS-side gate that asserts `LEARN_WINDOW_LABEL == "learn"`; flips green when this file ships)
  </read_first>
  <behavior>
    - `LEARN_WINDOW_LABEL: &str = "learn"` is a public const at module scope.
    - `open_learn_window(app: AppHandle) -> Result<(), String>` is annotated `#[tauri::command]` and async.
    - If a window with label `LEARN_WINDOW_LABEL` already exists, the function calls `set_focus()` on it and returns `Ok(())` (single-window semantics).
    - Otherwise it builds a new `WebviewWindowBuilder` pointing at `WebviewUrl::App("learn.html".into())`, with `.title("Learn — vibemix")`, `.inner_size(1280.0, 720.0)`, `.min_inner_size(960.0, 540.0)`, `.resizable(true)`, `.decorations(true)`, and `.build()` — returning `Ok(())` on success or `Err(format!("window build: {e}"))` on failure.
    - The module declares 2 unit tests in `mod tests` block: `learn_window_label_const_is_lowercase_no_spaces` (asserts the const equals `"learn"`, is all-lowercase, contains no spaces) and `learn_window_default_dimensions` (asserts the named constants `DEFAULT_WIDTH=1280.0` / `DEFAULT_HEIGHT=720.0` / `MIN_WIDTH=960.0` / `MIN_HEIGHT=540.0` are exact). Both tests run via `cargo test learn_window`.
    - The file has SPDX header `// SPDX-License-Identifier: Apache-2.0` and a module docstring referencing P91 RENDER-07 + the trimmed-mirror diff from research §Pattern 7.
  </behavior>
  <action>Create `tauri/src-tauri/src/learn_window.rs` by following research §Code Example 2 line-for-line: SPDX header, module docstring naming P91 RENDER-07 + Invariant #4, `use tauri::{AppHandle, Manager, WebviewUrl, WebviewWindowBuilder};` import, `pub const LEARN_WINDOW_LABEL: &str = "learn";`, 4 named float constants `DEFAULT_WIDTH`/`DEFAULT_HEIGHT`/`MIN_WIDTH`/`MIN_HEIGHT` matching UI-SPEC (1280/720/960/540), the `#[tauri::command] pub async fn open_learn_window(app: AppHandle) -> Result<(), String>` function with focus-existing-or-build semantics. Inside the focus-existing branch: `if let Some(existing) = app.get_webview_window(LEARN_WINDOW_LABEL) { let _ = existing.set_focus(); return Ok(()); }`. Inside the build branch: chain `WebviewWindowBuilder::new(&app, LEARN_WINDOW_LABEL, WebviewUrl::App("learn.html".into())).title("Learn — vibemix").inner_size(DEFAULT_WIDTH, DEFAULT_HEIGHT).min_inner_size(MIN_WIDTH, MIN_HEIGHT).resizable(true).decorations(true).build()` and map err to `format!("window build: {e}")`. Append a `#[cfg(test)] mod tests` block with the 2 named tests. DO NOT add: sidecar spawn, session_dir validation, deep-link payload, or close-handler — these are explicitly dropped per research §Pattern 7. Reference: research §Code Example 2 holds the full source.</action>
  <verify>
    <automated>cd tauri/src-tauri &amp;&amp; cargo test learn_window 2>&amp;1 | tail -15 &amp;&amp; cargo check 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tauri/src-tauri/src/learn_window.rs` exists with byte count > 1000 (i.e. not a stub).
    - `git grep -n 'pub const LEARN_WINDOW_LABEL: &str = "learn"' tauri/src-tauri/src/learn_window.rs` returns 1 match.
    - `git grep -n '#\\[tauri::command\\]' tauri/src-tauri/src/learn_window.rs` returns 1 match (the `open_learn_window` decorator).
    - `git grep -n '"Learn — vibemix"' tauri/src-tauri/src/learn_window.rs` returns 1 match (the em-dash window title — UI-SPEC).
    - `git grep -n 'sidecar\\|session_dir\\|DebriefDeepLink\\|kill_' tauri/src-tauri/src/learn_window.rs` returns ZERO matches (trimmed-mirror discipline).
    - `cd tauri/src-tauri && cargo test learn_window` exits 0; both `learn_window_label_const_is_lowercase_no_spaces` and `learn_window_default_dimensions` PASS.
    - `cd tauri/src-tauri && cargo check` exits 0 (compiles cleanly).
    - File starts with `// SPDX-License-Identifier: Apache-2.0`.
  </acceptance_criteria>
  <done>`learn_window.rs` exists, compiles, both unit tests pass, no sidecar/session/deep-link logic leaked from the debrief precedent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register `mod learn_window;` in `main.rs` and add `learn_window::open_learn_window` to the `invoke_handler` generate_handler! list</name>
  <files>tauri/src-tauri/src/main.rs</files>
  <read_first>
    - tauri/src-tauri/src/main.rs (find the existing `mod debrief_window;` line and the `generate_handler![...]` macro block — both are the precedents for the 2 insertions in this task)
    - .planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md §Pattern 7 final note (the 3-line addition spec — `mod learn_window;` + 1 handler entry + NO `.manage(LearnSidecarHandle::default())` because Learn shares the main sidecar)
    - tauri/src-tauri/capabilities/default.json (Plan 01 output — confirms `open_learn_window` is on the allowlist; main.rs must invoke-handler the same name)
  </read_first>
  <behavior>
    - `main.rs` has `mod learn_window;` declaration in alphabetical position (between `mod hotkey;` and `mod library_cmds;`).
    - `learn_window::open_learn_window` is registered inside the `generate_handler![...]` macro list — appended to the existing entries (preserve existing order).
    - No `.manage(LearnSidecarHandle::default())` is added — Learn explicitly shares the main sidecar per §Pattern 7.
    - `cargo build` succeeds; the Tauri app binary builds without errors.
    - All other entries in `main.rs` are unchanged (`git diff main.rs` shows exactly the 2 added lines).
  </behavior>
  <action>Edit `tauri/src-tauri/src/main.rs`: (1) Find the alphabetical `mod` declarations near the top of the file; insert `mod learn_window;` between `mod hotkey;` and `mod library_cmds;` (preserve alphabetical order). (2) Find the `tauri::generate_handler![...]` macro invocation; append `learn_window::open_learn_window,` to the comma-separated list. Append at the end of the list (do not reorder existing entries). DO NOT add a `.manage(...)` line for a sidecar handle — research §Pattern 7 explicitly notes Learn shares the main sidecar; adding a `.manage(LearnSidecarHandle::default())` is forbidden. DO NOT touch any other `main.rs` content. After the edit, run `cd tauri/src-tauri && cargo check` to confirm the module integrates; then `cargo build --release` is NOT required (cargo check is sufficient — full build runs in CI). Reference: research §Pattern 7 + the existing precedent `mod debrief_window;` + `debrief_window::open_debrief_window,` already in `main.rs`.</action>
  <verify>
    <automated>git grep -n '^mod learn_window;' tauri/src-tauri/src/main.rs &amp;&amp; git grep -n 'learn_window::open_learn_window' tauri/src-tauri/src/main.rs &amp;&amp; ! git grep -n 'LearnSidecarHandle' tauri/src-tauri/src/main.rs &amp;&amp; cd tauri/src-tauri &amp;&amp; cargo check 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n '^mod learn_window;' tauri/src-tauri/src/main.rs` returns 1 match.
    - `git grep -n 'learn_window::open_learn_window' tauri/src-tauri/src/main.rs` returns 1 match (inside the `generate_handler![...]` macro).
    - `git grep -n 'LearnSidecarHandle\\|--learn\\|learn_sidecar' tauri/src-tauri/src/` returns ZERO matches (one-sidecar discipline preserved).
    - `cd tauri/src-tauri && cargo check` exits 0 (no compile errors).
    - `cd tauri/src-tauri && cargo test learn_window` exits 0 (the unit tests from Task 1 still pass after Task 2 wires the module).
    - `git diff tauri/src-tauri/src/main.rs` shows exactly 2 net added lines (the `mod` declaration and the handler-list entry) — no other changes.
  </acceptance_criteria>
  <done>`main.rs` declares the new module and registers the command; cargo check passes; learn-window unit tests still pass; no sidecar-handle pollution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri webview → Tauri command | `open_learn_window` is allowlisted (Plan 01) → Tauri 2.x default-deny enforces. |
| Tauri shell → Python sidecar | NO new sidecar spawn — Learn shares the main process; one-process discipline preserved. |
| WebviewWindow file URL | `WebviewUrl::App("learn.html".into())` is the only path; no user-controllable URL fragment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-01 | Elevation | unauthorized webview invokes `open_learn_window` | mitigate | Plan 01 adds `open_learn_window` to `capabilities/default.json`; Tauri 2.x rejects un-allowlisted commands by default. |
| T-91-04-02 | Tampering | path-traversal via WebviewUrl | mitigate | The URL is the hardcoded literal `"learn.html"` — no user input passes through. (Contrast: `debrief_window.rs` accepts `session_dir` query — explicitly excluded here.) |
| T-91-04-03 | Resource exhaustion | repeated `open_learn_window` calls spawn N windows | mitigate | Focus-existing semantics (`if let Some(existing) = app.get_webview_window(...)`) ensures at most ONE Learn window exists at a time. |
| T-91-04-SC | Tampering | cargo crate installs | accept | NONE — uses existing `tauri` crate from `Cargo.toml`; zero new crates. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd tauri/src-tauri && cargo check && cargo test learn_window
git grep -n '^mod learn_window;' tauri/src-tauri/src/main.rs
git grep -n 'learn_window::open_learn_window' tauri/src-tauri/src/main.rs
git grep -n 'LearnSidecarHandle\|--learn' tauri/src-tauri/src/  # MUST be empty
```

Expected:
- `cargo check` exits 0.
- `cargo test learn_window` exits 0 (2 PASSED).
- 1 match each for the mod line and the handler entry.
- ZERO matches for the forbidden sidecar-handle / --learn-flag strings.
</verification>

<success_criteria>
- `learn_window.rs` exists with `LEARN_WINDOW_LABEL`, the `open_learn_window` command, and 2 Rust unit tests that all pass.
- `main.rs` declares the new module and registers the command in `generate_handler![...]`.
- `cargo check` and `cargo test learn_window` both exit 0.
- No sidecar-handle / `--learn` flag / second-process logic introduced (one-process discipline preserved).
- `git diff` against `main.rs` shows exactly 2 net added lines.
</success_criteria>

<output>
Create `.planning/phases/91-controller-renderer-midi-mirror/91-04-SUMMARY.md` recording: (a) the line count + entrypoints of `learn_window.rs` (must match research §Code Example 2 within ±5 lines); (b) the exact `main.rs` diff (2-line addition); (c) `cargo test learn_window` output (both unit tests PASS); (d) confirmation that the forbidden strings (LearnSidecarHandle, --learn) have ZERO matches anywhere in `tauri/src-tauri/src/`. Note any deviation from §Pattern 7 with rationale.
</output>
