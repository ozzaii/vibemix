---
phase: 91-controller-renderer-midi-mirror
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/learn/__init__.py
  - tests/learn/test_midi_mirror_unit.py
  - tests/learn/test_no_new_ws_port.py
  - tests/learn/test_no_pioneer_brand_marks.py
  - tests/ipc/test_learn_envelope_parity.py
  - tests/runtime/test_ws_broadcast_30hz_under_learn_load.py
  - tauri/ui/tests/learn/highlight-latency.test.ts
  - tauri/ui/tests/learn/test_svg_profile_parity.spec.ts
  - tauri/ui/tests/learn/test_aria_labels_present.spec.ts
  - tauri/ui/tests/learn/test_keyboard_nav_order.spec.ts
  - tauri/ui/tests/learn/test_contrast_ratios.spec.ts
  - tauri/ui/tests/learn/test_sr_announcement.spec.ts
  - tauri/ui/tests/learn/test_dual_cue_slots_present.spec.ts
  - tauri/ui/tests/learn/test_no_pioneer_orange.spec.ts
  - tauri/ui/tests/learn/test_svg_currentcolor_only.spec.ts
  - tauri/ui/tests/learn/test_all_11_svgs_present.spec.ts
  - tauri/ui/tests/learn/test_generic_fallback.spec.ts
  - tauri/ui/tests/learn/test_controller_detected_mounts_svg.test.ts
  - tauri/ui/tests/learn/test_ws_client_uses_8765.spec.ts
  - tauri/ui/tests/learn/test_learn_window_label.spec.ts
autonomous: true
requirements: [RENDER-01, RENDER-02, RENDER-03, RENDER-05, RENDER-06, RENDER-07]
must_haves:
  truths:
    - Every REQ-ID for P91 has at least one automated test file scaffolded that an executor can flip from red to green.
    - The `tests/learn/test_no_new_ws_port.py` gate exists and pins Invariant #4 (one-socket) for `learn/` once that subpackage lands.
    - The 14 vitest/playwright test stubs cover every row in `91-VALIDATION.md` per-task verification map.
  artifacts:
    - path: tests/learn/__init__.py
      provides: Python tests package marker for the new `tests/learn/` directory
    - path: tests/learn/test_no_new_ws_port.py
      provides: Static grep gate — fails red if any file under `src/vibemix/learn/` contains `websockets.serve`
      contains: websockets.serve
    - path: tests/learn/test_no_pioneer_brand_marks.py
      provides: Static grep gate — fails red if 'Pioneer DJ' wordmark appears in any `learn/**.svg.ts`
    - path: tests/ipc/test_learn_envelope_parity.py
      provides: Round-trip + count-parity test for the 2 new IPC envelopes
      contains: LearnControllerDetected
    - path: tauri/ui/tests/learn/highlight-latency.test.ts
      provides: Synthetic P95 latency harness — 50ms target, 80ms red guardrail
      min_lines: 50
    - path: tauri/ui/tests/learn/test_svg_profile_parity.spec.ts
      provides: Bidirectional SVG ↔ profile JSON parity gate across all 11 controllers
  key_links:
    - from: tests/learn/test_no_new_ws_port.py
      to: src/vibemix/learn/
      via: grep `websockets.serve` (must return zero matches)
      pattern: "websockets\\.serve"
    - from: tauri/ui/tests/learn/test_svg_profile_parity.spec.ts
      to: src/vibemix/midi/profiles/*.json
      via: bi-directional set equality on `data-control-id` values vs profile `field`+`deck` keys
      pattern: data-control-id
---

<objective>
Scaffold every test file enumerated in `91-VALIDATION.md` per-task verification map: 5 Python tests under `tests/learn/`, `tests/ipc/`, `tests/runtime/`, plus 14 vitest/playwright spec files under `tauri/ui/tests/learn/`. These are RED-state stubs by design — they reference source files that will land in plans 03–06, so the test suite reflects the goal-backward truth ("here is what we will prove") without compiling against missing modules.

Stubs use `pytest.importorskip(...)` for Python tests waiting on source and `it.todo(...)` / `it.skip(...)` / `fs.existsSync` guards for TS tests, plus 5 static grep gates (`test_no_new_ws_port.py`, `test_no_pioneer_brand_marks.py`, `test_no_pioneer_orange.spec.ts`, `test_svg_currentcolor_only.spec.ts`, `test_ws_client_uses_8765.spec.ts`) which are GREEN day-one because they assert absence — perfect early-Wave gates.

Purpose: plans 03/04/05/06 each have a concrete "this test now passes" milestone. Writing the test surface first makes the executor's job in those plans deterministic — flip the test, not author new assertions. Aligns with `91-VALIDATION.md` Sampling Rate ("After every task commit: Run `cd tauri/ui && npm test -- tests/learn/` (~3-5s)").

Output: 5 new Python test files + 14 new TS test files + 1 `tests/learn/__init__.py` marker. After this plan, `cd tauri/ui && npx vitest run tests/learn/` runs 14 spec files (most are `it.todo`; 3 grep gates that scan files-that-don't-exist-yet are trivially green); `PYTHONPATH=src python3 -m pytest -q tests/learn/ tests/ipc/test_learn_envelope_parity.py tests/runtime/test_ws_broadcast_30hz_under_learn_load.py` runs 5 test files (envelope parity test goes green if plan 01 also landed; the others skip pending source).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-controller-renderer-midi-mirror/91-CONTEXT.md
@.planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md
@.planning/phases/91-controller-renderer-midi-mirror/91-VALIDATION.md
@tests/ipc/test_session_messages.py
@tauri/ui/tests/wizard/step0-intro.spec.ts

<interfaces>
Test conventions sampled from existing in-tree precedents.

`tests/ipc/test_session_messages.py` (precedent for envelope round-trip tests):
- Uses pytest, imports envelope dataclasses, calls `.to_dict()` and `_VALIDATOR.validate(...)`.
- Tests round-trip: dataclass -> JSON -> schema validation -> reverse parse.

`tauri/ui/tests/wizard/step0-intro.spec.ts` (precedent for vitest UI spec):
- Imports from `tauri/ui/src/...` relative; uses vitest `describe` / `it` / `expect`.
- Uses jsdom (already in `tauri/ui/package.json` devDeps) for DOM assertions.

`scripts/check_ipc_schema.py` — existing count-parity gate; runs at CI; counts oneOf length vs wrappers; no extension needed here.

Static grep gate pattern (Python, blocking-by-absence):
- `Path("src/vibemix/learn").rglob("*.py")` walk -> `if "websockets.serve" in path.read_text(): fail`.
- Filter comment lines with `if line.lstrip().startswith("#"): continue` discipline.

Test data sources:
- The 10 controller profiles live at `src/vibemix/midi/profiles/<id>.json`.
- Eventual SVG strings will live at `tauri/ui/src/learn/controllers/<id>.svg.ts` (do NOT exist yet — Plans 05+06 author them).

Test file naming convention:
- `.spec.ts` for vitest/playwright UI specs (matches `tauri/ui/tests/**/*.spec.ts`).
- `.test.ts` for unit tests not coupled to a spec.
- `test_<name>.py` for Python pytest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold all 5 Python test files under tests/learn/, tests/ipc/, tests/runtime/</name>
  <files>tests/learn/__init__.py, tests/learn/test_midi_mirror_unit.py, tests/learn/test_no_new_ws_port.py, tests/learn/test_no_pioneer_brand_marks.py, tests/ipc/test_learn_envelope_parity.py, tests/runtime/test_ws_broadcast_30hz_under_learn_load.py</files>
  <read_first>
    - tests/ipc/test_session_messages.py (the exact round-trip + `_VALIDATOR.validate(...)` pattern to follow for `test_learn_envelope_parity.py`)
    - tests/ipc/__init__.py (the existing IPC tests package marker — `test_learn_envelope_parity.py` joins that namespace)
    - tests/runtime/__init__.py (the runtime tests package marker)
    - .planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md §Code Example 1 (the `MidiMirror.snapshot()` skeleton — `test_midi_mirror_unit.py` must cover its `_no_emit_when_steady` + `_first_frame_after_bind` behaviours)
    - .planning/phases/91-controller-renderer-midi-mirror/91-VALIDATION.md Per-Task Verification Map (rows for RENDER-02 backend + IPC parity)
    - src/vibemix/ui_bus/messages.py — the `_VALIDATOR` import path used by Python envelope tests
  </read_first>
  <behavior>
    - `tests/learn/__init__.py` exists (empty file — Python package marker).
    - `tests/learn/test_no_new_ws_port.py`: walks `src/vibemix/learn/` for any `.py` file containing the substring `websockets.serve` (ignoring lines starting with `#`); fails red if any match found. If the `src/vibemix/learn/` directory does not yet exist, the test passes trivially (zero matches). GREEN day-one.
    - `tests/learn/test_no_pioneer_brand_marks.py`: walks `tauri/ui/src/learn/` for any `.svg.ts` file containing case-insensitive `pioneer dj` or `pioneer ®`; fails red if found. GREEN day-one (no SVGs yet).
    - `tests/ipc/test_learn_envelope_parity.py`: imports `LearnControllerDetected` + `LearnMidiPosition` from `vibemix.ui_bus.learn_messages` via `pytest.importorskip`; defines four tests: `test_controller_detected_roundtrip`, `test_controller_detected_rejects_missing_field`, `test_midi_position_roundtrip`, `test_midi_position_rejects_out_of_range`. After plan 01 lands, these go GREEN.
    - `tests/learn/test_midi_mirror_unit.py`: imports from `vibemix.learn.midi_mirror` inside `pytest.importorskip`; defines `test_no_emit_when_steady`, `test_first_frame_after_bind`, `test_delta_emit_when_one_field_changes`. Tests SKIP until plan 03 lands `learn/midi_mirror.py`.
    - `tests/runtime/test_ws_broadcast_30hz_under_learn_load.py`: integration stub that asserts the existing `ws_broadcast` 30 Hz tick cadence holds when a `MidiMirror` is wired in (pins research §Assumption A1). SKIP until plan 03 lands the wiring.
  </behavior>
  <action>Create `tests/learn/__init__.py` as an empty file (package marker). Create `tests/learn/test_no_new_ws_port.py` that imports `pathlib.Path`, walks `Path("src/vibemix/learn").rglob("*.py")` if the directory exists, reads each file line-by-line filtering lines whose `lstrip()` starts with `#`, and asserts the literal substring `websockets.serve` appears in zero remaining lines; if the directory does not yet exist (Plan 03 hasn't landed), trivially `assert True`. Create `tests/learn/test_no_pioneer_brand_marks.py` with the same Path-rglob pattern over `Path("tauri/ui/src/learn").rglob("*.svg.ts")`, asserting case-insensitive `pioneer dj` and `pioneer ®` appear in zero files; trivially pass if directory missing. Create `tests/ipc/test_learn_envelope_parity.py` per research §Code Example 4's envelope shapes — four tests: round-trip positive (both envelopes call `.to_dict()` then `_VALIDATOR.validate(...)`), drop-required-field negative (constructs a dict by `.to_dict()` then `del result["payload"]["controller_id"]` then expects `pytest.raises(jsonschema.ValidationError)` on validate), and integer-out-of-range negative for `LearnMidiPosition.positions` (mutate one position to 128 → ValidationError). Use `pytest.importorskip("vibemix.ui_bus.learn_messages")` at module top so this file gracefully skips when run before Plan 01 lands. Create `tests/learn/test_midi_mirror_unit.py` with `pytest.importorskip("vibemix.learn.midi_mirror")` at top, then three tests `test_no_emit_when_steady`, `test_first_frame_after_bind`, `test_delta_emit_when_one_field_changes` that construct a fake `controller_state` stub (a `types.SimpleNamespace` with a `deck_snapshot()` method returning a synthetic dict matching the shape from research §Code Example 1 `_read_current_positions`), bind a synthetic profile stub (`SimpleNamespace(id="pioneer_ddj_flx4", display_name="X")`), and exercise the delta-suppression + first-emit + delta-emit branches. Create `tests/runtime/test_ws_broadcast_30hz_under_learn_load.py` with `pytest.importorskip("vibemix.learn.midi_mirror")` + `pytest.importorskip("vibemix.runtime.ws_bus")`; defines one async test `test_30hz_cadence_holds_with_midi_mirror_kwarg` that constructs a fake `midi_mirror` (`SimpleNamespace` with `.snapshot()` returning a constant dict every call), runs the `ws_broadcast` loop for ~100 ticks (~3.3s simulated time, with an `asyncio.wait_for` 5s wall-clock cap), and asserts the mascot frame emit count is within ±5% of 100 (pins research §Assumption A1). Reference: research §Validation Architecture for the test framework + sampling rate.</action>
  <verify>
    <automated>PYTHONPATH=src python3 -m pytest tests/learn/test_no_new_ws_port.py tests/learn/test_no_pioneer_brand_marks.py -x -v &amp;&amp; PYTHONPATH=src python3 -m pytest tests/ipc/test_learn_envelope_parity.py tests/learn/test_midi_mirror_unit.py tests/runtime/test_ws_broadcast_30hz_under_learn_load.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Python test files exist with byte counts &gt; 0.
    - `tests/learn/__init__.py` exists (size may be 0).
    - `pytest tests/learn/test_no_new_ws_port.py -v` exits 0 (PASSED) — directory may be empty/missing, test still passes.
    - `pytest tests/learn/test_no_pioneer_brand_marks.py -v` exits 0 (PASSED) — SVG directory may be empty/missing, test still passes.
    - `pytest --collect-only tests/learn/test_midi_mirror_unit.py` reports 3 test items.
    - `pytest --collect-only tests/ipc/test_learn_envelope_parity.py` reports 4 test items.
    - `pytest --collect-only tests/runtime/test_ws_broadcast_30hz_under_learn_load.py` reports 1 test item.
    - Every test that imports `vibemix.learn.midi_mirror` or `vibemix.ui_bus.learn_messages` uses `pytest.importorskip(...)` at module top (so the file doesn't error at collection time even if source is missing).
    - Each Python test file has SPDX header `# SPDX-License-Identifier: Apache-2.0` and a docstring referencing the REQ-ID it targets.
  </acceptance_criteria>
  <done>5 Python test files + `__init__.py` exist; 2 grep gates are GREEN immediately; the 3 import-skip tests collect cleanly and will go green once their source dependencies land (plans 01 + 03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the 14 vitest/playwright TS test stubs under tauri/ui/tests/learn/</name>
  <files>tauri/ui/tests/learn/highlight-latency.test.ts, tauri/ui/tests/learn/test_svg_profile_parity.spec.ts, tauri/ui/tests/learn/test_aria_labels_present.spec.ts, tauri/ui/tests/learn/test_keyboard_nav_order.spec.ts, tauri/ui/tests/learn/test_contrast_ratios.spec.ts, tauri/ui/tests/learn/test_sr_announcement.spec.ts, tauri/ui/tests/learn/test_dual_cue_slots_present.spec.ts, tauri/ui/tests/learn/test_no_pioneer_orange.spec.ts, tauri/ui/tests/learn/test_svg_currentcolor_only.spec.ts, tauri/ui/tests/learn/test_all_11_svgs_present.spec.ts, tauri/ui/tests/learn/test_generic_fallback.spec.ts, tauri/ui/tests/learn/test_controller_detected_mounts_svg.test.ts, tauri/ui/tests/learn/test_ws_client_uses_8765.spec.ts, tauri/ui/tests/learn/test_learn_window_label.spec.ts</files>
  <read_first>
    - tauri/ui/tests/wizard/step0-intro.spec.ts (canonical vitest spec pattern in this repo — `describe` / `it` / `expect`; imports from `tauri/ui/src/...` relative)
    - .planning/phases/91-controller-renderer-midi-mirror/91-RESEARCH.md §Code Example 3 (verbatim `highlight-latency.test.ts` skeleton — 240-sample synthetic burst, 50ms target, 80ms red guardrail)
    - .planning/phases/91-controller-renderer-midi-mirror/91-VALIDATION.md Per-Task Verification Map (every row maps to one of these 14 stub files)
    - tauri/ui/vitest.config.ts (test directory glob — `tests/**/*.{test,spec}.ts` matches what we're authoring)
    - tauri/ui/playwright.config.ts (playwright spec discovery — `test_keyboard_nav_order.spec.ts` + `test_contrast_ratios.spec.ts` go to playwright)
    - tauri/ui/package.json devDeps section for `jsdom` (needed for DOM walks)
  </read_first>
  <behavior>
    - All 14 spec files exist under `tauri/ui/tests/learn/`; vitest auto-discovers them via the test-include glob.
    - `highlight-latency.test.ts`: full skeleton from research §Code Example 3 (240-sample triangle sweep, P95 calc, soft warn 50-80ms, hard fail >80ms). The `import { PIONEER_DDJ_FLX4_SVG } from ...` is wrapped in a dynamic-import catch so missing module yields `it.skip(...)` instead of a collection-time crash.
    - `test_svg_profile_parity.spec.ts`: parameterised over the 11 controller IDs; reads JSON via `fs.readFileSync` from `src/vibemix/midi/profiles/<id>.json`; reads SVG via dynamic import; extracts `data-control-id` set; asserts bi-directional equality. Uses `if (!fs.existsSync(svgPath)) return ctx.skip();` guard. Will go green for FLX4+generic after plan 05; for the rest after plan 06.
    - `test_aria_labels_present.spec.ts`: for each SVG that exists, asserts every `<g data-control-id>` has `role="button"` + non-empty `aria-label`. Skip-if-missing.
    - `test_keyboard_nav_order.spec.ts`: playwright spec — Tab cycles through control groups in canonical DOM order. Stub uses `test.skip("plan 05 lands the entrypoint")`.
    - `test_contrast_ratios.spec.ts`: playwright + axe-core run on the Learn window. Stub uses `test.skip(...)`.
    - `test_sr_announcement.spec.ts`: vitest — checks `aria-live="polite"` element receives `<display_name> connected.` on `controller_detected` envelope. Stub uses `it.todo(...)`.
    - `test_dual_cue_slots_present.spec.ts`: every `<g data-control-id>` in every SVG has two empty child `<g class="cue-color">` + `<g class="cue-shape">` slots. Annotated `// expect: stub-only` per UI-SPEC. Skip-if-missing.
    - `test_no_pioneer_orange.spec.ts`: scans every `.svg.ts` file under `tauri/ui/src/learn/` for `#FF7F00`, `#ff7f00`, `rgb(255,127,0)`, and hex literals in the ±10° hue range (concretely: `#ff8000`..`#ff7e00`). GREEN day-one (no SVGs yet).
    - `test_svg_currentcolor_only.spec.ts`: scans every `.svg.ts` for `fill="#..."` / `stroke="#..."` hex literal outside the allowed-list (`var(--silk-22)`, `var(--silk)`, `var(--learn-highlight)`, `none`, `currentColor`); fails on any hex literal. GREEN day-one.
    - `test_all_11_svgs_present.spec.ts`: asserts the existence of all 11 expected files: `pioneer_ddj_flx4`, `pioneer_ddj_flx6`, `pioneer_ddj_flx10`, `pioneer_ddj_400`, `pioneer_ddj_1000`, `pioneer_ddj_sx3`, `pioneer_xdj_rx3`, `numark_party_mix_live`, `hercules_inpulse_300`, `hercules_inpulse_500`, `_generic`. Use `it.each([...11 ids...])` with `it.todo` for not-yet-shipped names; plans 05 + 06 flip to `it.each(... fs.existsSync assertion ...)`.
    - `test_generic_fallback.spec.ts`: asserts `_generic.svg.ts` exports `GENERIC_CONTROLLER_SVG` string containing labeled-zone text `DECK A`, `MIXER`, `DECK B`. Skip until plan 05.
    - `test_controller_detected_mounts_svg.test.ts`: vitest harness simulates `ipc.learn.controller_detected` envelope arrival on a `LearnWindow` instance and asserts the matching SVG is mounted at `#learn-root` within 2000 ms. Skip-if-missing.
    - `test_ws_client_uses_8765.spec.ts`: greps `tauri/ui/src/learn/**/*.ts` for `new WebSocket(...)` calls and asserts every URL contains `:8765`. GREEN day-one (no source yet).
    - `test_learn_window_label.spec.ts`: greps `tauri/src-tauri/src/learn_window.rs` for `LEARN_WINDOW_LABEL` const and asserts it equals `"learn"`. Skip-if-missing until plan 04.
  </behavior>
  <action>Author all 14 TS stub files in `tauri/ui/tests/learn/`. For `highlight-latency.test.ts` use the verbatim ~50-line skeleton from research §Code Example 3, but wrap the `import { PIONEER_DDJ_FLX4_SVG } from ...` inside a dynamic import: `let SVG = null; try { SVG = (await import("../../src/learn/controllers/pioneer_ddj_flx4.svg.js")).PIONEER_DDJ_FLX4_SVG; } catch { /* plan 05 lands it */ }` so a missing module yields `it.skip(...)`. Every spec file MUST: (a) declare `// SPDX-License-Identifier: Apache-2.0` at the top; (b) name the REQ-ID it targets in a top-of-file comment; (c) use `describe(<test_filename>, ...)`; (d) use `it.skip` / `it.todo` / `test.skip` for any assertion that depends on plan 05/06 source, marked with `// TODO: plan 0N executor — flip to it.skip → it`. For the 3 grep-gate stubs (`test_no_pioneer_orange.spec.ts`, `test_svg_currentcolor_only.spec.ts`, `test_ws_client_uses_8765.spec.ts`) — these are GREEN day-one because they scan directories that don't exist yet (`fs.existsSync(...) && fs.readdirSync(...)` — empty list, zero matches, assertion passes). For `test_svg_profile_parity.spec.ts`, parameterise via `it.each([...11 controller ids array...])(...)` and inside the function body use `if (!fs.existsSync(svgPath)) return ctx.skip();` (or `it.skip`-style guard). For `test_keyboard_nav_order.spec.ts` and `test_contrast_ratios.spec.ts`, use the playwright `test.skip("plan 05 lands the entrypoint")` form. Use `fs.readFileSync` + `JSDOM` from the jsdom package (already in `tauri/ui/package.json` devDeps) for the DOM-walk stubs. Each file is 30-80 lines; keep them small and readable. Reference: research §Code Example 3 + §Validation Architecture + §Architecture Patterns 4, 5, 6 (which inform the parity, dual-cue, and aria-label stubs).</action>
  <verify>
    <automated>cd tauri/ui &amp;&amp; npx vitest run tests/learn/ --reporter=basic 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 14 TS test files exist under `tauri/ui/tests/learn/` with byte counts > 0.
    - `cd tauri/ui && npx vitest run tests/learn/` exits with 0 failures (all skipped/todo/passed is acceptable; no actual `expect(...).toBe(...)` red).
    - `test_no_pioneer_orange.spec.ts`, `test_svg_currentcolor_only.spec.ts`, `test_ws_client_uses_8765.spec.ts` each PASS in vitest output (not skipped — they actively assert no matches in empty directories).
    - `test_no_pioneer_brand_marks.py` (Python equivalent from Task 1) and these 3 TS grep gates form a 5-test brand-safety/one-socket gate that goes green at the close of this plan.
    - The remaining 11 TS test files appear as skipped or todo in vitest output (file is parsed, tests are declared, assertions deferred).
    - `highlight-latency.test.ts` is at least 50 lines and contains the `N_BURST`, `P95_TARGET_MS`, `P95_RED_GUARDRAIL_MS` constants exactly as in research §Code Example 3.
    - Every TS file has `// SPDX-License-Identifier: Apache-2.0` at the top.
    - Every TS file names its target REQ-ID in a top-of-file comment.
  </acceptance_criteria>
  <done>14 TS test files exist; vitest run exits clean; 3 grep gates are GREEN; the 11 source-dependent specs are properly skipped/todo and will go green as plans 03/04/05/06 ship source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production source | Tests must not modify production source under `src/vibemix/` or `tauri/ui/src/`; all writes here go to `tests/` and `tauri/ui/tests/` only. |
| static grep gates → file system | Gates read files but do not write; safe by construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02-01 | Tampering | static grep gate evaded by encoding tricks (e.g. `websockets.serve`) | accept | Plans 03+ executors are this-session's island; no adversary to evade gates. If future external commits land via mistake, follow-up CR adds AST-walk hardening. |
| T-91-02-02 | DoS | latency harness wall-clock takes too long in CI | mitigate | `N_BURST = 240` × ~1ms/sample = ~240ms total in jsdom — well under per-task 10s sampling latency. |
| T-91-02-SC | Tampering | npm/pip installs | mitigate | NONE — this plan installs no packages. jsdom, vitest, pytest, jsonschema already in pre-existing devDeps/dependencies. |
</threat_model>

<verification>
After both tasks complete:

```bash
PYTHONPATH=src python3 -m pytest tests/learn/test_no_new_ws_port.py tests/learn/test_no_pioneer_brand_marks.py -v
PYTHONPATH=src python3 -m pytest tests/learn/ tests/ipc/test_learn_envelope_parity.py tests/runtime/test_ws_broadcast_30hz_under_learn_load.py --collect-only -q
cd tauri/ui && npx vitest run tests/learn/ --reporter=basic
```

Expected outcome:
- 2 Python grep gates PASS green.
- pytest collects 8 test items across the 4 import-skip Python files (3+4+1).
- vitest runs 14 spec files: 3 grep gates PASS, 11 are skipped/todo, zero failures.
</verification>

<success_criteria>
- 5 Python test files exist under `tests/learn/`, `tests/ipc/`, `tests/runtime/`.
- 14 TS test files exist under `tauri/ui/tests/learn/`.
- `tests/learn/__init__.py` exists.
- The 2 Python grep gates (`test_no_new_ws_port.py`, `test_no_pioneer_brand_marks.py`) PASS green.
- The 3 TS grep gates (`test_no_pioneer_orange.spec.ts`, `test_svg_currentcolor_only.spec.ts`, `test_ws_client_uses_8765.spec.ts`) PASS green.
- Every other test stub is properly skipped/todo with no false failures.
- `highlight-latency.test.ts` matches the verbatim skeleton from research §Code Example 3 (constants + harness shape).
- Every stub file has SPDX header + REQ-ID reference.
</success_criteria>

<output>
Create `.planning/phases/91-controller-renderer-midi-mirror/91-02-SUMMARY.md` recording: (a) the exact list of 5+14 files created; (b) the vitest output (how many passed / skipped / todo); (c) the pytest collection counts; (d) confirmation that 5 grep gates (2 Python + 3 TS) are GREEN. Note any test stub that fails red despite the import-skip discipline (means a defensive guard is missing) and resolve before close.
</output>
