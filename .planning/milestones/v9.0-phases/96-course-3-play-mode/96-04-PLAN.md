---
phase: 96-course-3-play-mode
plan: 04
type: execute
wave: 4
depends_on:
  - 96-01
  - 96-02
  - 96-03
files_modified:
  - .planning/phases/96-course-3-play-mode/96-EAR-PASS.md
autonomous: false
requirements:
  - CURR-3.01
  - CURR-3.02
  - CURR-3.03
  - CURR-3.04
  - CURR-3.05
  - CURR-3.06
  - CURR-3.07
  - EXEMPLAR-06
tags: [kaan-action, ear-pass, checkpoint, course-3-walk-through, audio-verification, proactive-lens, ride-forward]
must_haves:
  truths:
    - "Kaan walks through Course 3 end-to-end on a real DDJ-FLX4 in a live cargo tauri dev session"
    - "Plays a real 30-minute set with proactive co-pilot ON; verifies count-ins only land on grounded [cue:] evidence; verifies retrospective-only narration when confidence floors fail"
    - "Verifies EXEMPLAR-06 holds — ZERO exemplar audio plays while a deck is audible during a Course 3 lesson"
    - "Verifies the iconic Course 3 milestone (the user is now actually DJing with vibemix coaching, not just learning anatomy or transitions in isolation)"
    - "No tutor-tic slop heard during live LLM narration (TONE-03 live verification — the runtime-LLM path is NOT CI-gated; ear is the gate)"
    - "Ear-pass observations land at .planning/phases/96-course-3-play-mode/96-EAR-PASS.md for P98 audit pickup"
    - "Plan 96 closes engineering-complete with §LEARN-EAR-COURSE-3 surfaced + parked as non-blocking per gsd-autonomous fully"
  artifacts:
    - path: .planning/phases/96-course-3-play-mode/96-EAR-PASS.md
      provides: "Pre-staged Kaan ear-pass log — 6-row per-lesson table + proactive-lens specific axes + EXEMPLAR-06 verification + tone observations + audio-routing notes + the cargo tauri dev walk-through recipe"
      contains: "§LEARN-EAR-COURSE-3"
  key_links:
    - from: .planning/phases/96-course-3-play-mode/96-EAR-PASS.md
      to: .planning/STATE.md KAAN-ACTION queue
      via: "§LEARN-EAR-COURSE-3 — ride forward, do not block the autonomous run"
      pattern: "§LEARN-EAR-COURSE-3"
    - from: .planning/phases/96-course-3-play-mode/96-EAR-PASS.md
      to: .planning/ROADMAP.md v9.0 KAAN-ACTION Queue
      via: "BLOCKING entry §LEARN-EAR-COURSE-3 resolves at P98 audit close (AUDIT-01)"
      pattern: "§LEARN-EAR-COURSE-3"
---

<objective>
Park Kaan's live Course 3 ear-pass as a KAAN-ACTION queue entry and stage a
ready-to-use observation log so the ear-pass is one cargo-tauri-dev session
away whenever he sits down at the FLX4. P96 closes engineering-complete
with the ear-pass parked as non-blocking per `gsd-autonomous fully`.

Course 3 is the milestone's BIGGEST live-verification surface — the user is
now actually DJing with vibemix coaching, not learning anatomy in isolation
(P94 / `§LEARN-EAR-COURSE-1`) or practicing transitions on a controlled
script (P95 / `§LEARN-EAR-COURSE-2`). The proactive lens + count-in
grounding + EXEMPLAR-06 active-session guard are all surfaces only Kaan's
ear can verify against a real 30-minute set.

Per ROADMAP §v9.0 KAAN-ACTION Queue, `§LEARN-EAR-COURSE-3` is one of the
three BLOCKING ear-pass gates that resolve at P98 (Live Audit + Ear-Pass
Hand-Off). Plan 96-04 (a) confirms the queue entry exists in STATE.md +
ROADMAP.md (surface-and-confirm, no new content), (b) lays down the
observation log template Kaan fills during the walk-through, and (c)
records the checkpoint as non-blocking so Phase 96 closes cleanly.

Output: 1 new markdown file (96-EAR-PASS.md) + 1 non-blocking checkpoint
task. No production-code touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-course-3-play-mode/96-CONTEXT.md
@.planning/phases/94-course-1-anatomy/94-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface §LEARN-EAR-COURSE-3 + lay down 96-EAR-PASS.md observation log</name>
  <files>.planning/phases/96-course-3-play-mode/96-EAR-PASS.md</files>
  <read_first>.planning/STATE.md (verify §LEARN-EAR-COURSE-3 already in BLOCKING queue), .planning/ROADMAP.md (verify same entry in v9.0 KAAN-ACTION queue at line ~232), .planning/phases/96-course-3-play-mode/96-CONTEXT.md (locked decisions), .planning/phases/94-course-1-anatomy/94-04-PLAN.md (P94-04 ear-pass template — Plan 96-04 mirrors its shape with Course-3-specific axes)</read_first>
  <action>
    Two sub-steps, both confirmation-then-write (no production-code touches).

    **Sub-step A — confirm KAAN-ACTION queue surface.** Grep both STATE.md +
    ROADMAP.md for `§LEARN-EAR-COURSE-3`:

    ```bash
    grep -c "§LEARN-EAR-COURSE-3" .planning/STATE.md
    grep -c "§LEARN-EAR-COURSE-3" .planning/ROADMAP.md
    ```

    Per ROADMAP.md line ~232 + STATE.md "v9.0 KAAN-ACTION Queue (parked,
    never faked)", both files already carry the BLOCKING entry. The grep
    above MUST return ≥1 in BOTH files. If either returns 0 (someone
    pruned the line accidentally), restore the entry:

    - STATE.md `## v9.0 KAAN-ACTION Queue (parked, never faked)` → BLOCKING
      bullet: `§LEARN-EAR-COURSE-3 (P98 / AUDIT-01) — Kaan ear-pass on
      Course 3 (6 lessons) on real FLX4 with 30-min live set`
    - ROADMAP.md `## KAAN-ACTION Queue (v9.0 — surfaced + parked, never
      faked)` → BLOCKING bullet (same line with the existing 🔴 red-circle
      emoji prefix).

    The default expectation is BOTH greps return ≥1; if so, no edit is
    needed for sub-step A.

    **Sub-step B — write 96-EAR-PASS.md.** Create
    `.planning/phases/96-course-3-play-mode/96-EAR-PASS.md` with the
    Course-3-specific template (mirrors P94-04's template, adds the
    proactive-lens + EXEMPLAR-06 axes that are unique to this course):

    ```markdown
    # Course 3 Ear-Pass Log (§LEARN-EAR-COURSE-3)

    **Plan:** 96-04 — Course 3 Play Mode live verification
    **Hardware:** Pioneer DDJ-FLX4 (Kaan's canonical golden — per ROADMAP §v9.0 hard constraints)
    **Mode:** non-blocking KAAN-ACTION ride-forward per `gsd-autonomous fully`
    **Discharge:** P98 audit close (AUDIT-01)

    ## Walk-through recipe

    Per `feedback_stale_sidecar_verify_current_source` (cargo tauri dev's
    bundled Python sidecar lags edited src/, so live verification runs
    against current source on the dev binary):

    1. Plug DDJ-FLX4 via USB. Confirm BlackHole 2ch + nowplaying-cli are routed (CLAUDE.md macOS prereqs).
    2. Confirm `GEMINI_API_KEY` is in `.env` (live co-host requires this).
    3. `cd /Users/ozai/projects/dj-set-ai && cargo tauri dev` (the dev shell spawns real `python -m vibemix`).
    4. Open the Learn webview. Pre-P97 (mode-picker on main window) entry path: invoke `uv run python -m vibemix learn start L3.01` (or whatever the CLI dispatch ships). The Learn webview wakes via `ipc.learn.start_lesson` envelope.
    5. **Course 1 + Course 2 should already be unlocked.** If not, run them first (or use the L1.16 / L2.14 recital "I got it" override).
    6. Walk lessons in order. Score each as 🟢 green / 🟡 yellow (works but rough) / 🔴 red (broken or slop heard).

    ## Per-lesson observations

    | Lesson | Expected | Observed | Status | Notes |
    |--------|----------|----------|--------|-------|
    | L3.01 First 5-Minute Mix | Proactive lens on; AI suggests one grounded move per minute; ZERO exemplar audio plays | | | |
    | L3.02 First 15-Minute Set | v8.2 Build-a-Set pool loads; AI live-coaches transitions; cites [cue:] when grounded | | | |
    | L3.03 Reading The Room | AI narrates energy curve mid-set; suggests next-track adjustments grounded in playback | | | |
    | L3.04 30-Minute Set Capstone (THE MILESTONE) | Full 30-min set; count-ins ONLY when [cue:] grounded; debrief surface (port 8766) opens at end | | | |
    | L3.05 Recovery Drills | AI introduces 2 synthetic train-wrecks (key clash + misaligned phrase); user bails within 4 bars | | | |
    | L3.06 DJ Profile Graduation | Post-set review; v8.1 long-term DJ profile + lesson completion summary in v2.1 debrief | | | |

    ## Invariant #3 verification (THE Course 3 release gate)

    For each lesson L3.01-L3.05, observe specifically:

    - **Count-in confidence gate** — when BPM is locked + phrase position locked:
      - AI emits forward-looking count-in: "breakdown in 16 beats — get ready" 🟢 / 🔴 (list any)
      - Count-in lands on the actual phrase boundary, NOT a phantom prediction 🟢 / 🔴
      - Count-in cites `[cue:<anchor_id>]` (visible in WS log or transcript) 🟢 / 🔴
    - **Confidence gate downgrade** — when BPM unsure OR phrase unsure OR no next_phrase_at:
      - AI downgrades to retrospective ("that was a breakdown — see how the bass dropped out") 🟢 / 🔴
      - AI does NOT emit forward-looking language during low-confidence stretches 🟢 / 🔴
    - **No phantom count-ins** — across the full 30-min capstone, ZERO forward-looking count-ins fire without a real cue boundary the user could verify by ear 🟢 / 🔴

    Trust-the-audio Invariant #3 is the release gate. Any "🔴 phantom count-in" is a BLOCKING red — pin against `state/coach.py::_COUNT_IN_BPM_FLOOR` / `_COUNT_IN_PHRASE_FLOOR` (tunable per CONTEXT.md §Claude's Discretion).

    ## EXEMPLAR-06 verification (active-session guard)

    During L3.01-L3.05 (proactive_lens_active=true + exemplar_audio_forbidden=true), specifically observe:

    - ZERO exemplar tracks play while ANY deck is audible 🟢 / 🔴
    - Between-track gaps (audible_deck="none") do NOT force the lesson to play exemplars either (Course 3 = verbal coaching only) 🟢 / 🔴
    - L3.06 (post-set review; proactive_lens_active=false) MAY play exemplars (e.g. reference tracks from the user's set) — confirm the guard doesn't over-refuse here 🟢 / 🔴 / 🟡 not-applicable

    ## Tone discipline observations

    - **Hand-authored Course 3 fixtures** (Plan 94-02 35-token blocklist CI-gates these recursively — no review needed unless a fixture changed in flight):
      - Tutor-tic blocklist hits heard? 🟢 / 🔴 (list any)
    - **Runtime LLM interjections** (Gemini Flash via learn_tutor route — these are NOT CI-gated; ear is the gate). During a 30-min set the LLM emits many lines; listen for:
      - Compliments heard ("great", "awesome", "you crushed it", "perfect", etc.)? 🟢 / 🔴 (list any)
      - Summaries heard ("you just learned X", "you've now mastered X")? 🟢 / 🔴 (list any)
      - Previews heard ("now let's", "next we'll", "coming up")? 🟢 / 🔴 (list any)
      - Upbeat hook closers ("exciting, right?", "this is where it gets fun", "you're going to love")? 🟢 / 🔴 (list any)
      - Generic "AI voice assistant" register (not "real DJ friend in your ear")? 🟢 / 🔴 (list any)

    Any runtime interjection slop heard → file a tutor system instruction tuning followup against the `_FORBIDDEN_TUTOR_MOVES_LOCK` in `src/vibemix/learn/prompts.py:52-80`. The lock is the live-tuning seam.

    ## Audio routing observations

    - Live co-host narration plays through which device? (system output / BlackHole / dedicated headphone): _____
    - Default narration gain — too loud / too quiet / just right? _____
    - Latency — does the AI react in time, or feel late? (the v9.0 release gate per CLAUDE.md: "If reactions feel forced, late, fake, or scripted, the product fails")
      - Track-change reactions: 🟢 in time / 🟡 close / 🔴 too late
      - Transition reactions: 🟢 / 🟡 / 🔴
      - Count-in timing relative to actual phrase boundary: 🟢 on the beat / 🟡 close / 🔴 missed
    - Debrief surface (port 8766) auto-opens after L3.04 capstone? 🟢 / 🔴

    ## L3.05 Recovery Drills specifics

    - Drill 1 (key clash on deck B) — did the synthetic clash actually sound dissonant? 🟢 / 🟡 / 🔴
    - Drill 2 (misaligned phrase on deck B) — did the drift produce an obvious train-wreck the user could hear? 🟢 / 🟡 / 🔴
    - Recovery within 4 bars — was the time pressure achievable for a beginner, or too tight? 🟢 / 🟡 / 🔴
    - AI bail-out coaching during recovery — quality + grounding? 🟢 / 🟡 / 🔴

    ## L3.06 DJ Profile Graduation specifics

    - v8.1 long-term DJ profile surfaces in the debrief panel? 🟢 / 🔴
    - Lesson completion summary lists all 36 lessons (Courses 1+2+3)? 🟢 / 🔴
    - "Beginner module complete; live co-host is open for you now" copy lands? 🟢 / 🔴

    ## Overall

    Status: 🟢 ship Course 3 / 🟡 acceptable with followups / 🔴 block on the followups

    Followups (file as new KAAN-ACTION entries or runtime tuning notes):
    - _____
    - _____

    ## v9.0 release-gate sentence

    Per CLAUDE.md "Quality bar": "Real DJ friend in your ear, no AI slop" — Kaan will block release if reactions feel scripted, late, hallucinated, or generic.

    Course 3 verdict against the release gate: _____
    ```

    The file is a TEMPLATE — Kaan fills the empty cells when he walks Course 3.
    The structure is pre-staged so he doesn't have to author the test plan on
    the day; he just executes against the recipe.

    **Concurrent-session discipline**: this is a NEW-file island (single new
    markdown under `.planning/phases/96-course-3-play-mode/`). Commit with
    named-path `git add .planning/phases/96-course-3-play-mode/96-EAR-PASS.md`.
    NEVER `git add -A`. NEVER `git stash`.

    **REQUIREMENTS.md status update.** Update CURR-3.01..CURR-3.07 + EXEMPLAR-06
    statuses in `.planning/REQUIREMENTS.md` to reflect P96 closure:
    - CURR-3.01..3.06 → `[x] SHIPPED P96-03 2026-05-28` (Plan 96-03 lands
      the fixtures + curriculum + active-session guard).
    - CURR-3.07 → `[x] SHIPPED P96-01 + P96-02 2026-05-28` (Plan 96-01 lands
      the AST gates + runtime confidence gate; Plan 96-02 lands the [cue:]
      4-site mirror).
    - EXEMPLAR-06 → `[x] SHIPPED P96-03 2026-05-28` (active-session guard
      wired in ExemplarPlayer.can_play).
    - Update the Traceability table (around REQUIREMENTS.md:180) — flip
      "Pending" → "Complete" for the 8 REQ-IDs.
    - Update the per-category coverage stats if present.

    Verify by re-running the REQ-ID round-trip test:

    ```bash
    PYTHONPATH=src python3 -m pytest -q tests/learn/test_course_3_curriculum.py tests/learn/test_cue_citation_schema_mirror.py tests/learn/test_no_speculative_phrase.py tests/learn/test_course3_uses_existing_coach.py tests/learn/test_course3_no_exemplar_during_live.py tests/state/test_coach_course3_confidence_gate.py -x
    ```

    All 6 should pass; that's the engineering-complete sign-off for P96.
  </action>
  <verify>
    <automated>
      cd /Users/ozai/projects/dj-set-ai && bash -c '
      STATE_HITS=$(grep -c "§LEARN-EAR-COURSE-3" .planning/STATE.md 2>/dev/null || echo 0)
      ROADMAP_HITS=$(grep -c "§LEARN-EAR-COURSE-3" .planning/ROADMAP.md 2>/dev/null || echo 0)
      EARPASS_EXISTS=$(test -f .planning/phases/96-course-3-play-mode/96-EAR-PASS.md && echo 1 || echo 0)
      EARPASS_HAS_TABLE=$(grep -c "L3.04 30-Minute Set Capstone" .planning/phases/96-course-3-play-mode/96-EAR-PASS.md 2>/dev/null || echo 0)
      EARPASS_HAS_INVARIANT=$(grep -c "Invariant #3 verification" .planning/phases/96-course-3-play-mode/96-EAR-PASS.md 2>/dev/null || echo 0)
      EARPASS_HAS_EXEMPLAR=$(grep -c "EXEMPLAR-06 verification" .planning/phases/96-course-3-play-mode/96-EAR-PASS.md 2>/dev/null || echo 0)
      if [ "$STATE_HITS" -lt 1 ]; then echo "FAIL: §LEARN-EAR-COURSE-3 missing from STATE.md (sub-step A)"; exit 1; fi
      if [ "$ROADMAP_HITS" -lt 1 ]; then echo "FAIL: §LEARN-EAR-COURSE-3 missing from ROADMAP.md (sub-step A)"; exit 1; fi
      if [ "$EARPASS_EXISTS" -ne 1 ]; then echo "FAIL: 96-EAR-PASS.md missing (sub-step B)"; exit 1; fi
      if [ "$EARPASS_HAS_TABLE" -lt 1 ]; then echo "FAIL: 96-EAR-PASS.md missing L3.04 row in per-lesson table"; exit 1; fi
      if [ "$EARPASS_HAS_INVARIANT" -lt 1 ]; then echo "FAIL: 96-EAR-PASS.md missing Invariant #3 verification section"; exit 1; fi
      if [ "$EARPASS_HAS_EXEMPLAR" -lt 1 ]; then echo "FAIL: 96-EAR-PASS.md missing EXEMPLAR-06 verification section"; exit 1; fi
      echo "PASS: queue entry surfaced; ear-pass log staged with 6-row per-lesson table + Invariant #3 + EXEMPLAR-06 + tone + audio sections"
      '
    </automated>
  </verify>
  <done>§LEARN-EAR-COURSE-3 appears in both STATE.md and ROADMAP.md. 96-EAR-PASS.md exists with the walk-through recipe + 6-row per-lesson observation table + Invariant #3 verification + EXEMPLAR-06 verification + tone observations + audio routing notes + L3.05/L3.06 specifics + overall + release-gate sentence. Kaan can execute the ear-pass without authoring anything new on the day. REQUIREMENTS.md statuses for CURR-3.01..3.07 + EXEMPLAR-06 updated to `[x] SHIPPED`.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Kaan FLX4 ear-pass on Course 3 (parked KAAN-ACTION, P98 discharge)</name>
  <what-built>
    Plans 96-01, 96-02, 96-03 ship Course 3 Play Mode end-to-end:

    - **Plan 96-01 (Invariant #3 BIND + confidence gate)**: AST gate
      `test_no_speculative_phrase.py` (no phrase-guessing primitives in
      learn/) lands BEFORE Gemini wiring. AST gate
      `test_course3_uses_existing_coach.py` pins reuse-existing-coach.
      MusicState gains 3 additive default-safe fields (session_active,
      phrase_position_confidence, next_phrase_at). AICoach.evidence_line
      gains the gated `lens=retrospective_only` / `lens=count_in_eligible[next@<t>]`
      marker. _COUNT_IN_BPM_FLOOR (0.8) + _COUNT_IN_PHRASE_FLOOR (0.7)
      exported for ear-pass tuning.
    - **Plan 96-02 (CURR-3.07 + [cue:] mirror)**: `[cue:<anchor_id>]`
      evidence source landed via atomic 4-site mirror commit.
      EVIDENCE_SOURCES count flipped 10 → 11. `test_cue_citation_schema_mirror.py`
      pins all four sites against drift.
    - **Plan 96-03 (CURR-3.01..3.06 + EXEMPLAR-06)**: 6 hand-authored
      Course 3 lesson fixtures land under
      `src/vibemix/learn/transcripts/course_3_play_mode/`. curriculum.py
      extension binds them. proactive_lens_active + exemplar_audio_forbidden
      fixture fields wire the runtime surface. ExemplarPlayer.can_play()
      flipped from P93 scaffold to real EXEMPLAR-06 guard
      (`return not (session_active and audible_deck != "none")`).
      `test_course3_no_exemplar_during_live.py` pins the truth table.
      `test_course_3_curriculum.py` pins the fixture + curriculum binding.
    - All P92+P93+P94+P95 invariant pins STAY GREEN
      (test_runtime_invariants / test_no_new_ws_port /
      test_tutor_system_instruction_lock / test_scripts_are_fixtures /
      test_no_tutor_slop_blocklist / test_progress_persistence /
      test_recital / test_exemplar_lesson / test_exemplar_citation_schema_mirror).
    - Task 1 of this plan stages 96-EAR-PASS.md.

    Engineering is complete. Course 3 is verifiable offline (unit tests +
    `check_no_tutor_slop.py`). Live audio + grounded-count-in verification
    require Kaan's ear on a real FLX4 + a real 30-min set.
  </what-built>
  <how-to-verify>
    This is a NON-BLOCKING checkpoint (per `feedback_autonomous_no_grey_area_pause` —
    gsd-autonomous fully mode rides ear-pass forward into the KAAN-ACTION
    queue; does not pause the autonomous run).

    When Kaan sits down at the FLX4:

    1. Follow the walk-through recipe in
       `.planning/phases/96-course-3-play-mode/96-EAR-PASS.md` (Task 1 staged it).
    2. Walk Course 1 + Course 2 first (or use the recital "I got it"
       overrides) to unlock Course 3.
    3. Run all 6 Course 3 lessons end-to-end, including the full 30-minute
       capstone at L3.04. This is the BIGGEST live-verification surface in
       the milestone.
    4. Fill in the per-lesson observation table — 6 rows, one per L3.NN.
    5. Fill in the Invariant #3 verification section (THE release gate —
       any phantom count-in = BLOCKING red).
    6. Fill in the EXEMPLAR-06 verification section (active-session guard).
    7. Fill in the tone discipline + audio routing + L3.05/L3.06 specifics
       + overall sections.
    8. Score the v9.0 release-gate sentence at the bottom.
    9. Commit `96-EAR-PASS.md` with observations. P98 audit reads the file
       and rolls it into the `v9.0-MILESTONE-AUDIT.md` doc.

    The `<resume-signal>` below is for the executor's awareness — under
    `gsd-autonomous fully` the executor does not actually pause here. The
    signal text is the form Kaan uses when he reports back so a downstream
    phase can act on it.
  </how-to-verify>
  <resume-signal>
    AUTONOMOUS MODE (default for v9.0): this checkpoint does NOT pause execution.
    Plan 96 closes as soon as Plans 96-01..03 land green + Task 1 of this plan
    completes. §LEARN-EAR-COURSE-3 rides forward to P98 audit close.

    If a downstream phase needs the ear-pass to land early (e.g. P98 milestone
    audit explicitly gates on Course 3 verification), promote this checkpoint
    to `gate="blocking"` and wait for Kaan's signal: "Course 3 ear-pass
    complete — observations in 96-EAR-PASS.md."

    Under default gsd-autonomous fully, Plan 96 closes engineering-complete
    with the ear-pass parked as a non-blocking KAAN-ACTION.
  </resume-signal>
  <files>.planning/phases/96-course-3-play-mode/96-EAR-PASS.md</files>
  <action>Kaan walks Course 3 end-to-end on the FLX4 per the recipe in 96-EAR-PASS.md (incl. the 30-min L3.04 capstone); fills in the 6-row per-lesson observation table + Invariant #3 verification + EXEMPLAR-06 verification + tone + audio + L3.05/L3.06 specifics + overall + release-gate sentence; commits the filled file. Under `gsd-autonomous fully` mode this task does NOT pause executor; the queue entry §LEARN-EAR-COURSE-3 rides forward to P98 audit close.</action>
  <verify>
    <automated>test -f .planning/phases/96-course-3-play-mode/96-EAR-PASS.md &amp;&amp; grep -c "§LEARN-EAR-COURSE-3" .planning/phases/96-course-3-play-mode/96-EAR-PASS.md</automated>
    <human-check>6-row per-lesson table filled in with status emoji + notes; Invariant #3 verification section filled in (especially the phantom-count-in floor); EXEMPLAR-06 verification section filled in; L3.04 capstone walkthrough completed; tone observations + audio routing + overall status set; v9.0 release-gate sentence answered.</human-check>
  </verify>
  <done>Either Kaan has committed the filled 96-EAR-PASS.md (live discharge) OR the autonomous run advances past this checkpoint with §LEARN-EAR-COURSE-3 still queued (parked discharge per gsd-autonomous fully). Both are valid Phase 96 close states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous run ↔ ear-pass | the autonomous run cannot self-verify audio + grounded count-ins; the queue entry IS the gate |
| KAAN-ACTION queue ↔ phase close | parking ≠ deferring; the entry MUST surface in STATE.md + ROADMAP.md so P98 audit can discharge it |
| 96-EAR-PASS.md template ↔ live observations | Kaan-authored cells are the audit evidence; pre-staged structure ensures the audit captures the right axes |
| Invariant #3 release gate | ANY phantom count-in heard during live verification = BLOCKING red against v9.0 ship |
| EXEMPLAR-06 release gate | ANY exemplar audio playing during an active session = BLOCKING red against v9.0 ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04-01 | Repudiation | ear-pass result lost | mitigate | observations land at `.planning/phases/96-course-3-play-mode/96-EAR-PASS.md`; git history preserves; P98 audit re-reads |
| T-96-04-02 | Tampering | autonomous run claims ear-pass complete without Kaan | accept | enforced by `gsd-autonomous fully` mode contract — agents NEVER fake KAAN-ACTION discharge; STATE.md "Pre-Milestone Direction" carries this rule |
| T-96-04-03 | Denial of Service | ear-pass blocks v9.0 ship indefinitely | accept | parked status is the v9.0 contract per ROADMAP; v9.0 public ship gates on AUDIT-01 (P98) which closes only when all 3 course ear-passes ride green; the 30-min L3.04 capstone is the headline live test |
| T-96-04-04 | Tampering | template authored without Invariant #3 / EXEMPLAR-06 specific sections | mitigate | Task 1 verify-script asserts both section headers exist in the file post-write |
| T-96-04-SC | Tampering | npm/pip installs | accept | zero — Task 1 touches one new markdown file + REQUIREMENTS.md status updates; Task 2 is a non-blocking human checkpoint with no install gate |
</threat_model>

<verification>
- `grep -c "§LEARN-EAR-COURSE-3" .planning/STATE.md .planning/ROADMAP.md` returns ≥1 in each file.
- `.planning/phases/96-course-3-play-mode/96-EAR-PASS.md` exists with: walk-through recipe + 6-row per-lesson observation table + Invariant #3 verification section + EXEMPLAR-06 verification section + tone observations + audio routing notes + L3.05/L3.06 specifics + overall + v9.0 release-gate sentence.
- Plan 96-04 closes without blocking the autonomous run (Task 2 is non-blocking).
- The walk-through recipe references the right invocation pattern per `feedback_stale_sidecar_verify_current_source` (cargo tauri dev with dev sidecar, not the bundled binary).
- REQUIREMENTS.md statuses flipped for CURR-3.01..3.07 + EXEMPLAR-06.
- The complete Plan 96 test surface exits 0:
  - test_no_speculative_phrase.py (Plan 96-01)
  - test_course3_uses_existing_coach.py (Plan 96-01)
  - test_coach_course3_confidence_gate.py (Plan 96-01)
  - test_cue_citation_schema_mirror.py (Plan 96-02)
  - test_course_3_curriculum.py (Plan 96-03)
  - test_course3_no_exemplar_during_live.py (Plan 96-03)
- Plus all carried-forward invariant pins stay green (P92/P93/P94/P95 surfaces).
</verification>

<success_criteria>
- KAAN-ACTION queue entry §LEARN-EAR-COURSE-3 is surfaced and confirmed in STATE.md + ROADMAP.md.
- 96-EAR-PASS.md template is staged for Kaan's walk-through with Course-3-specific Invariant #3 + EXEMPLAR-06 sections (the gates this milestone exists to enforce).
- Phase 96 is engineering-complete (Plans 96-01..03 green + 96-04 Task 1 green) with the ear-pass parked as non-blocking.
- Plan 96-04 closes autonomously per `gsd-autonomous fully` mode.
- REQUIREMENTS.md statuses updated for the 8 REQ-IDs this phase covers (CURR-3.01..3.07 + EXEMPLAR-06).
- All 4 cardinal invariants (#1 single-writer / #2 citation grounding / #3 trust-the-audio BIND / #4 one-socket) STAY GREEN at phase close.
</success_criteria>

<output>
Create `.planning/phases/96-course-3-play-mode/96-04-SUMMARY.md` when done (one paragraph confirming Task 1 landed the queue surface + ear-pass log template + REQUIREMENTS.md status updates; Task 2 parked non-blocking with §LEARN-EAR-COURSE-3 ride-forward; reference the `kaan_action_deferred` field per `gsd-autonomous fully`).
</output>
