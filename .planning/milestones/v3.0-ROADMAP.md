# Milestone v3.0: Clean OSS Ship

**Status:** ✅ SHIPPED 2026-05-17 (engineering-complete; status: `tech_debt` accepted — KAAN-ACTION-LEGAL discharges pending external clock for public RC publish)
**Phases:** 40 — 45 (6 phases total)
**Total Plans:** 41 (40=6 + 41=7 + 42=6 + 43=9 + 44=7 + 45=6 — all 41 plans engineering-complete; KAAN-ACTION-LEGAL surface deferred to external clock + operator discharge)
**Git range:** `v2.1.0` (2026-05-16) → `2aeb020` audit + commit (2026-05-17) — 250 commits since `v2.1.0` tag
**Code delta:** 529 files changed, +62,215 / -1,029 (net ~+61k LOC across `src/vibemix/`, `tauri/`, `scripts/`, `tests/`, `docs/`, `eval/`)
**Test baseline at close:** v3.0 phase-scope tests added on top of v2.1 baseline (every phase ships with new pytests pinning shipped behavior).
**Mode at close:** `gsd-autonomous fully` — every blocker + human-needed item discharged autonomously EXCEPT legal-capacity + real-hardware + customer-facing-publish carveouts per P46.

## Overview

v3.0 = the "Clean OSS Ship" milestone. The thesis: vibemix is the ONLY AI co-host that actually listens to your set, with anti-slop credibility baked into every surface — mic-as-2nd-Gemini-Part, file-based 3s lookahead labeled `NOT YET HEARD`, hybrid hallucination gate (autonomous proxy fast-lane + Kaan-ear release-cut veto), CDJ Whisper UI Tier-1 polish, EvidenceRegistry citation strip in live UI, and a launch surface (README + grids + Bravoh waitlist toggle + outreach calendar) tuned to "real DJ friend, no AI slop".

6 phases shipped engineering-green. 57 / 57 v3.0 REQ-IDs satisfied at the engineering surface. v3.0-MILESTONE-AUDIT.md (passed, 57/57 reqs, 6/6 phases, 3/3 integration seams, 5/5 flows) is the audit-of-record.

Critical path = external clock: Apple Developer Program Agreement update (Francesco, P46) + SignPath OSS Foundation approval (Kaan, ~1-week SLA, P46) gate the public RC publish in Phase 45. P40–P44 engineering parallelized around the external clock; SHIP-CUT v3.0.0-rc1 is one-button after approvals land via the §SHIP-01..13 cookbook in `KAAN-ACTION-LEGAL.md` (45-06 ships 13 of 26 discharge runbooks).

All 6 phases carry `human_needed` verification carveouts that are by-design Kaan-action under the autonomy mode (legal capacity, real-hardware ear-tests, real-asset production, customer-facing publishes). These live in `KAAN-ACTION-LEGAL.md` §AUDIO-05..07 / §LAT-09 / §GATE-01..05 / §VIS-04 / §LAUNCH-03/04/06/07/08 / §SHIP-01..13. Engineering ships green pending discharge.

POC reference files (`cohost*.py`, `mascot.html`) remain byte-identical to v2.0 tag (`test_g5_poc_files_untouched.py` enforces; Phase 42 hybrid gate retires Phase 16 ear-test override formally via `P85-OVERRIDE-RETIRED.md`).

## Phases

### Phase 40: Anti-Slop Audio Port

**Goal**: Close the biggest engineering anti-slop gap — Gemini now hears Kaan's voice (mic as 2nd Part) and gets a 3s structural preview (source-file lookahead as 3rd Part); event cooldowns re-tuned to v4 chat-tested intuition. Pre-stage independent KAAN-ACTION items (PGP, Tauri updater key, BlackHole fresh-Mac probe).
**Status**: 6/6 plans shipped (engineering-green); AUDIO-05/06/07 + ear-test routed to KAAN-ACTION-LEGAL.
**Depends on**: v2.1 shipped (Phases 27–39); `AudioBuffer` (Phase 02), `MusicState.last_kaan_spoke_at` (Phase 03), `DJCoHostAgent.llm_node` (Phase 04).
**Requirements**: AUDIO-01..07

Plans:

- [x] 40-01: Mic as 2nd Gemini Part + ring buffer + AI-talk zero-fill (AUDIO-01) — GREEN; 13 new tests; mic_audio_buf 16kHz int16 12s ring; Pitfall 1 zero-fill at sounddevice callback boundary; 3-gate Part 2 attach in `dj_cohost.py:417`.
- [x] 40-02: LookaheadProvider port from `cohost_v4_tr.py` (AUDIO-02) — GREEN; per-session provider with ffmpeg + mdfind + nowplaying-cli pipeline; 8 hermetic tests; graceful `(None, meta)` skip on streaming-only sources.
- [x] 40-03: 3rd Gemini Part wire-in + 'NOT YET HEARD BY AUDIENCE' prompt labeling (AUDIO-02, AUDIO-04) — GREEN; 12 new tests; 4-way `build_parts_description` dispatch (1-Part baseline / 2-Part mic-only / 2-Part lookahead-only / 3-Part); anti-prediction guard pinned.
- [x] 40-04: Event cooldown re-tune + replay-harness `--print-cooldowns` (AUDIO-03) — GREEN; `MIN_EVENT_GAP_PER_TYPE` re-tuned to v4 chat-tested values (PHASE 18→10s, LAYER_ARRIVAL 16→10s, MIX_MOVE 20→14s, HEARTBEAT 70→45s, TRACK_CHANGE 6→5s).
- [x] 40-05: PGP + Tauri updater key pre-stage scaffolding (AUDIO-05, AUDIO-06) — engineering pre-stage GREEN; slot file + SECURITY.md retarget + dual-mode gate test; Kaan-discharge runbooks in `KAAN-ACTION-LEGAL.md §AUDIO-05/06`.
- [x] 40-06: BlackHole probe structured-event emission + Pitfall 5 retry (AUDIO-07) — GREEN; 12 new tests; `probe_blackhole` emits `audio.probe.{detected,missing,cta_fired}` events; Kaan-discharge runbook in `KAAN-ACTION-LEGAL.md §AUDIO-07` (fresh-Mac walk).

**Details:** Closes the "AI invents what Kaan said" + "AI reacts after the moment passed" hallucination classes via mic-as-Part-2 + lookahead-as-Part-3. v4 cooldowns reduce stale-event coast-time. PGP / Tauri key / BlackHole probe pre-stage means external clock discharge is single-command at SHIP-CUT time.

### Phase 41: Gemini SKU Upgrade + Latency Stack v2

**Goal**: Adopt 2026 Gemini deltas — ModelRouter seam, implicit caching, LLM→TTS pipe-through, 3.1 Flash TTS, Embedding 2 GA, Flex tier for batch paths. Target: end-to-end latency 5-10s → 3-5s; perceived latency 0-2s with P40 lookahead offset.
**Status**: 7/7 plans shipped (engineering-green); TTFT p95 ear-test + LAT-09 spike verdict + FLEX live-billing routed to KAAN-ACTION-LEGAL.
**Depends on**: Phase 40 audio path stable; v2.1 `GeminiContextCache` + `TTFTMeter` + `AckBank`.
**Requirements**: LAT-01..09

Plans:

- [x] 41-01: ModelRouter seam + config + CI grep gate (LAT-01, LAT-07) — GREEN; `src/vibemix/llm/model_router.py` + `_router_config.py`; 9 hardcoded model literals migrated; grep gate in `scripts/release/check_no_hardcoded_model.sh` + `.github/workflows/model-literal-check.yml`; ServiceTier.FLEX wired for debrief/library_auto_tag/embedding; STANDARD for live coach paths.
- [x] 41-02: GeminiContextCache cleanup: TTL 3600s, mutation-driven refresh, cache_hit telemetry (LAT-02, LAT-03) — GREEN; explicit cache TTL 300s→3600s; `EvidenceRegistry.on_mutation` with 5s debounce + 30s min-interval guard; `cache_hit` telemetry to events.jsonl.
- [x] 41-03: `thinking_level=MINIMAL` runtime gate + FLEX-on-live defense (LAT-08) — GREEN; `validate_live_config` in `src/vibemix/llm/thinking_gate.py`; 3 call sites wired (`llm_factory.py:32/60`, `dj_cohost.py:294`); 18 tests; Pitfall 3 FLEX-on-live defense.
- [x] 41-04: LLM→TTS streaming pipe-through + dual-phase gate + 3.1 Flash TTS audio-tag DSL (LAT-04, LAT-05) — GREEN; `_streaming_pipe.py` with bracket-depth-aware sentence boundary (Pitfall 1); `LLMToTTSDeltaMeter` per-turn; 6-tag DSL (`[whisper]`, `[laugh]`, `[fast]`, etc.); OpenRouter Achird OPUS fallback chain preserved.
- [x] 41-05: Embedding 2 GA probe + auto-bump + 768-dim parity test + migration script (LAT-06) — GREEN; audit + parity + GA probe + migration script.
- [x] 41-06: Gemini 3.1 Flash Live spike framework + Kaan-action discharge runbook (LAT-09) — engineering pre-stage GREEN; spike harness + KAAN-ACTION-PROXY §LAT-09 runbook (real 5-min DJ clip + offline listen + verdict write).
- [x] 41-07: End-to-end integration test + replay harness metrics + 41-INTEGRATION-REPORT.md (LAT-01..09) — GREEN; live-path-only acceptance audit + replay-harness summary lines.

**Details:** Latency stack v2 unifies the 2026 Gemini deltas into one config-driven SKU router with Flex-tier 50% cost cut on batch paths and live coach pinned to Standard+thinking=MINIMAL. The €50/month CI gate stays under budget. LAT-09 (3.1 Flash Live music) deliberately gates on a real Kaan-ear spike before committing to a v3.x opt-in toggle.

### Phase 42: Hallucination Gate v3 — Hybrid

**Goal**: Adopt hybrid gate — Phase 27 autonomous proxy fast-lane (PR + nightly canary) + Kaan-ear release-cut veto. P85 override formally retired; corpus + thresholds calibrated against real audio; ear-test capture wired into debrief.
**Status**: 6/6 plans shipped (engineering-green); ACK-BANK-REMAINING-20 + EVAL-VCR-CASSETTES + EVAL-CORPUS-WAVS + ear-test logs routed to KAAN-ACTION-LEGAL.
**Depends on**: v2.1 Phase 27 (replay harness + 2-judge cross-check + THRESHOLD-LOCK.md); Phase 29 debrief window slot.
**Requirements**: GATE-01..09

Plans:

- [x] 42-01: Corpus scaffolding: ack-bank resume helper + VCR cassette recorder + git-LFS corpus layout + KAAN-ACTION §GATE-01/02/03 runbooks (GATE-01, GATE-02, GATE-03) — GREEN; populate-ack resume + record-cassettes + git-lfs corpus skeleton.
- [x] 42-02: Threshold recalibration tooling: `recalibrate_thresholds.py` + ±0.10 tolerance band + THRESHOLD-RECALIBRATION-LOG.md + eval.yml `--check-real-corpus` mode (GATE-04) — GREEN; deterministic + auditable recalibration.
- [x] 42-03: Ear-test protocol + capture surface: `EAR-TEST-PROTOCOL.md` + JSON Schema + debrief window toggle + `check_ear_test.sh` + KAAN-ACTION §GATE-05 (GATE-05, GATE-07) — GREEN; 41 new tests (40 pass + 1 conditional jq-missing skip); Phase 29 105-test debrief suite still green.
- [x] 42-04: `check_gate.sh` hybrid wire-in: bash gate combining 7-nightly + ear-test + `cut_release.sh` Gate 2b insertion + P85 reminder line removal (GATE-06) — GREEN; 22 new tests (14 bash-gate + 8 cut_release wire-in); P85 reminder retired from success-path echo block.
- [x] 42-05: P85 override retirement: Decision Log entry + delete `test_phase_16_override_expiry.py` + positive-assertion `test_gate_42_hybrid_in_force.py` + STATE.md annotation (GATE-08) — GREEN; `.planning/decisions/P85-OVERRIDE-RETIRED.md` committed; hybrid regime formally in force.
- [x] 42-06: Public eval docs: `eval/README.md` + threshold-mirror tests + ear-test log content redaction privacy contract test (GATE-09) — GREEN; 21 new tests across 2 files; threshold-mirror parametrized 2-decimal normalization; rubric-leak sentinels derived at test-time from `judge_pro.md` + `judge_flash.md`.

**Details:** Hybrid gate end-to-end: PR + nightly canary green (7 consecutive scorecards) AND `check_ear_test.sh` green (≥2 sessions ≥2 genres in 14d, zero slop flags) gate SHIP-CUT Gate 2b. P85 (Phase 16 ear-test memory override) formally retired post-v2.1 per Decision Log. Public `eval/README.md` documents the regime + redacts ear-test session content per `feedback_privacy_scope_narrow`.

### Phase 43: Visual Ship Lock

**Goal**: Lock CDJ Whisper UI to FL-Studio-grade polish; replace 5 mascot stub animations with Mixamo retargets; pre-produce 30s hero demo for launch hero. Three internal waves (UI / mascot / demo prep), critique→execute loop spanning the phase.
**Status**: 9/9 plans shipped (engineering-green); §VIS-04 Mixamo retargets + capture day routed to KAAN-ACTION-LEGAL.
**Depends on**: v2.1 Phase 31 4-layer mascot state machine; v2.1 CDJ Whisper UI tokens.
**Requirements**: VIS-01..09

Plans:

- [x] 43-01: UI audit driver + first audit run (session surface seed) (VIS-01) — GREEN; Tier-1 audit driver + 4-surface allowlist + UI-REVIEW skeleton writer.
- [x] 43-02: UI audit closure: session + mascot overlay + hover-glow sweep (VIS-01, VIS-02) — GREEN; zero HIGH findings on session + mascot overlay; `--glow-faint` hover/focus-visible sweep across 5 session components.
- [x] 43-03: UI audit closure: wizard + calibration + settings + hover-glow sweep (VIS-01, VIS-02) — GREEN; surface-uniform `--glow-faint` contract across 6 wizard steps + onboarding + settings drawer (22 sites, was 1); UI-REVIEW-wizard.md + UI-REVIEW-calibration.md flipped to HIGH-findings-closed.
- [x] 43-04: meter.ts spectrum rebuild: hardware-LED-strip + amber peak hold + silk-12 grid (VIS-03) — GREEN; 16 discrete segments, amber peak-hold with 1.2s decay, silk-12 minor grid; 8 vitest + 4 Playwright assertions.
- [x] 43-05: Mixamo retarget pipeline + 5 prep_*.glb swap-in scaffold + §VIS-04 KAAN-discharge (VIS-04) — engineering scaffold GREEN; CLI + slot taxonomy + draco shell-out + two-tier bundle-gate (25 MB total + 400 KB-1200 KB per-clip); real GLB retargets are Kaan-discharge per §VIS-04.
- [x] 43-06: Mood pool runtime validation (3 personas, 30s smoke) + integrated-GPU perf gate (VIS-05, VIS-06) — GREEN; Hype-man / Teacher / Coach pools wired to MANIFEST clips; 60fps p99 maintained.
- [x] 43-07: Memory + storyboard doc drift cleanup: DJ bat→Neon Rebel; Workbench/DSEG7→Saira/Geist (VIS-07) — GREEN; palette-drift gate + 6-test pytest spec.
- [x] 43-08: Hero demo storyboard v5 re-mock: 8-cut shot list + chip overlay frames + ≤8 cuts gate (VIS-08) — GREEN; 8-cut 30s sequence per CONTEXT §VIS-08; UI chip overlays in cuts 2-6 inline-rendered with CDJ Whisper v5; `scripts/launch/check_cut_count.py` + 6-test pytest gate.
- [x] 43-09: Francesco pre-production handoff package + demo-mode sequencer + §VIS-09 discharge (VIS-09) — GREEN; deterministic 30-event demo-mode sequencer (anchored at 2:33 kick_swap / 4:50 layer_drop / 6:00 track_end) + 4-doc launch-prep handoff package + canonical §VIS-09 Francesco discharge runbook.

**Details:** Tier-1 surfaces (session, mascot overlay, wizard, calibration) pass paired `gsd-ui-checker` + `gsd-ui-auditor` with zero HIGH findings. 5 prep_*.glb placeholders intentionally fail Tier 2 bundle-gate with exit-2 — visible signal that §VIS-04 Mixamo discharge is still pending. Memory + doc drift cleanup snapped "DJ bat" → "Neon Rebel" and migrated storyboard from Workbench + DSEG7 to Saira + Geist on the 5-warm-blacks + 1-amber CDJ Whisper palette.

### Phase 44: Launch Positioning + Pre-stage

**Goal**: README launch-ready; EvidenceRegistry citation strip visible in live UI (anti-slop receipts on screen); Bravoh funnel CTA placed; bravoh GH org stood up; SHIP-TWEET copy locked; Discord provisioning + outreach calendar finalized. Every pre-stage item discharged that doesn't require external clock.
**Status**: 7/7 plans shipped (engineering-green); 6+10 logo swaps + bravoh GH org standup + SHIP-TWEET sign-off + Discord live-execute routed to KAAN-ACTION-LEGAL.
**Depends on**: Phase 43 final UI for screenshots + hero demo references; v2.1 Phase 18 EvidenceRegistry; v2.1 Phase 29 debrief window.
**Requirements**: LAUNCH-01..10

Plans:

- [x] 44-01: README hero rewrite + 'no AI slop' hook section (LAUNCH-01) — GREEN; README hero `<em>` locked to "the only AI co-host that actually listens to your set" verbatim; new "## No AI slop" H2 with all 5 CONTEXT-pinned anchor phrases; 3-gate CI lock (`scripts/launch/check_readme_hero_lock.py` + 11-test pytest); case-sensitive exact-count=1 enforced; AI-slop blocklist (15 tokens + `\bdeeply\s+\w+` regex).
- [x] 44-02: DJ-software grid + controller grid + a11y check (LAUNCH-03, LAUNCH-04) — GREEN; DJ-software 6-cell grid + canonical-10 controllers grid reconciled to `src/vibemix/midi/controllers/*.json`; 4-gate a11y CI (alt + count + balance + slop); 16 SVG wordmark placeholders shipped (zero dead `<img src>` references); §LAUNCH-03 + §LAUNCH-04 Kaan-discharge runbooks.
- [x] 44-03: EvidenceRegistry citation strip in live UI + tag→debrief deep link (LAUNCH-02) — GREEN; new `SessionCohostReaction` IPC + token-driven amber chip strip + Tauri `open_debrief_window` deep_link extension; 2s region highlight in `timeline.ts`.
- [x] 44-04: Bravoh waitlist toggle in debrief settings (LAUNCH-05) — GREEN; `ConfigStore.bravoh_waitlist_opt_in` field + `mountBravohWaitlistToggle` component + verbatim UTM URL grep-gate + token-driven faint-amber-glow active state; subtle, opt-in only, default OFF, no signed-out telemetry.
- [x] 44-05: SHIP-TWEET 5-channel copy lock + AI-slop grep gate + §LAUNCH-07 runbook (LAUNCH-07) — GREEN; 5-channel locked copy + AI-slop blocklist grep + sign-off footer gate (literal Kaan + Francesco signature).
- [x] 44-06: Bravoh GH org standup + Discord auto-provision dry-run lock + check scripts (LAUNCH-06, LAUNCH-08) — GREEN; `discord_provision.py --dry-run` complete; `check_bravoh_org_ready.sh` polling gate.
- [x] 44-07: Outreach calendar + launch sequence T-7 → T+30 doc (LAUNCH-09, LAUNCH-10) — GREEN; T-7 / T-3 / T-0 / T+24h / T+72h / T+7d / T+30 sequence locked; `check_launch_docs.py` CI gate.

**Details:** Pre-stage closes every launch surface that doesn't depend on external clock. Citation strip surfaces anti-slop receipts on-screen (closes §6.2 white-space gap). Bravoh waitlist toggle is the canonical funnel into Bravoh proper (default-OFF, UTM-tracked, opt-in only).

### Phase 45: External Discharge + Public RC Publish

**Goal**: Apple Dev Agreement + SignPath OSS approvals land → cascading discharge (DIST-19 sign+verify → INSTALL-VM matrix → INSTALL-60S → SHIP-CUT v3.0.0-rc1) → social publish + Discord + repo transfer + 24h monitoring rotation → ~2-week bake → SHIP-V1-DECISION. KAAN-ACTION-LEGAL critical path.
**Status**: 6/6 plans shipped (engineering-green); all 13 SHIP-* awaiting external clock + operator discharge per KAAN-ACTION-LEGAL §SHIP-01..13 cookbook (45-06).
**Depends on**: v2.1 Phase 38 signing pipeline; v2.1 Phase 33 INSTALL-VM scaffolding; v2.1 Phase 39 `cut_release.sh` + `verify_signed.py` + 6-gate pre-flight.
**Requirements**: SHIP-01..13

Plans:

- [x] 45-01: tart-based INSTALL-VM matrix runner + 60s onboarding gate (SHIP-04, SHIP-05) — GREEN; tart-based 5-OS install matrix runner with JSON-driven config, dry-run-default semantics, `--check-60s` sub-gate.
- [x] 45-02: `launch_trigger.sh` orchestration + `cadence_index.json` T-30 / T+0 / T+5h / T+24h (SHIP-08) — GREEN; 22-test orchestration spec; `--live` triple-env (LAUNCH_REAL=1 + GITHUB_TOKEN + DISCORD_WEBHOOK_URL); pre-publish slop gate; sign-off footer gate; JSONL audit at `dist/launch-runs/<UTC>.jsonl`.
- [x] 45-03: `check_bravoh_server_ready.sh` 3-endpoint probe + `cut_release.sh` Gate 5b wire-in (SHIP-06) — GREEN; `POST /vibemix/updates/upload` + `GET /vibemix/updates/latest.json` + `GET /vibemix/healthz` probe.
- [x] 45-04: SHIP-V1-DECISION audit script + decision template + synthetic-telemetry fixtures (SHIP-13) — engineering scaffold GREEN; `audit_ship_v1_decision.py` (610 lines) with `--fixtures DIR` (CI hermetic) + `--live` (T+30 Kaan-discharge); `docs/SHIP-V1-DECISION-TEMPLATE.md`; 20/20 tests green; zero real network in CI; threat-model mitigations T-45-04-{01..05} all enforced.
- [x] 45-05: `docs/launch-rotation.md` §SHIP-11 augment — 24h solo rotation + triage decision tree (SHIP-11) — GREEN; 7-monitoring-source list + triage tree; shift table; operational source-of-truth for SHIP-11.
- [x] 45-06: `KAAN-ACTION-LEGAL.md` §SHIP-01..13 discharge cookbook (SHIP-01..13 all) — GREEN; 13 of 26 discharge runbooks shipped in canonical 8-block §LAUNCH-08 format; tag-regex bump flagged as §SHIP-07 prerequisite (NOT executed in plan); §SHIP-11 cross-links Plan 45-05 as operational SoT; §SHIP-12 framed as passive observation (SmartScreen unobservable from CLI).

**Details:** All 13 SHIP-* requirements have engineering pre-stage GREEN + KAAN-ACTION-LEGAL §SHIP-NN discharge runbooks. Apple Dev Agreement (Francesco — §SHIP-01) + SignPath OSS Foundation approval (Kaan — §SHIP-02) are the external clock. After approvals land, SHIP-CUT is a documented sequence — `gh release create v3.0.0-rc1 --draft` after `cut_release.sh` 6-gate green; 5-channel social publish via `launch_trigger.sh --live`; Discord #announcements; repo transfer to `bravoh/vibemix`; 24h rotation; ~2-week bake; SHIP-V1-DECISION audit + sign-off.

---

## Milestone Summary

**Decimal Phases:**

None. v3.0 phases ran sequentially as 40 → 45 with parallel waves within each phase.

**Key Decisions:**

- **Phase numbering CONTINUED** from v2.1 (closed at Phase 39). v3.0 starts at Phase 40 (no `--reset-phase-numbers`).
- **`gsd-autonomous fully` mode** applied throughout — every blocker + human-needed item discharged autonomously EXCEPT legal-capacity carveouts (P46), customer-facing publishes (SHIP-CUT/TWEET/DISCORD/TRANSFER/ROTATE), real-hardware (INSTALL-VM-RUN), real-asset production (Mixamo retargets), and SmartScreen propagation observation.
- **Hybrid hallucination gate** — Phase 27 autonomous proxy fast-lane + Kaan-ear release-cut veto. P85 Phase 16 ear-test override formally retired post-v2.1 (`P85-OVERRIDE-RETIRED.md` Decision Log).
- **Mic-as-Part-2 + Lookahead-as-Part-3** — closes "AI invents what Kaan said" + "AI reacts after the moment passed" hallucination classes via literal audio Parts in front of Gemini.
- **NOT YET HEARD BY AUDIENCE labeling** — explicit anti-prediction guard in 3-Part prompt; Gemini cannot react to lookahead Part 3 as if the audience has heard it.
- **Cooldowns re-tuned to v4 chat-tested values** — PHASE 18→10s, LAYER_ARRIVAL 16→10s, MIX_MOVE 20→14s, HEARTBEAT 70→45s, TRACK_CHANGE 6→5s. Live DJ-set intuition over conservative defaults.
- **ModelRouter seam + ServiceTier.FLEX** — config-driven SKU + tier dispatch; 50% cost cut on batch paths; live coach stays Standard + thinking=MINIMAL; €50/month CI gate stays under budget.
- **EvidenceRegistry citation strip in live UI** — anti-slop receipts on-screen; 2-3 word evidence tag per AI reaction (`[kick swap @ 2:33]`); click → debrief opens with 2s waveform region highlight.
- **Tag-regex bump deferred to §SHIP-07 prerequisite** — `cut_release.sh` line 44 ships `^v2\.1\.0-rc[0-9]+$` from Phase 39; v3.0 SHIP-CUT requires `^v3\.0\.0-rc[0-9]+$`. Bumps as its own commit at Kaan-discharge time; cut commit stays clean.
- **POC files BYTE-IDENTICAL to v2.0 tag** — `cohost*.py`, `mascot.html`, `cohost.streaming.py.bak`; Phase 37-06 immutability gate enforces (carried forward from v2.1).
- **Honest RC labeling** — `v3.0.0-rc1` not premature `v1.0.0`; v1.0.0 decision deferred to Kaan post-2-week bake (SHIP-V1-DECISION audit at T+30).
- **One-click install remains a HARD requirement** — every dep choice rated green/yellow/red on install impact; Linux still excluded; stem separation / CLAP / ProDJ Link still rejected as scope creep.

**Issues Resolved:**

- Closed shipped `dj_cohost.py:332-340` gap where mic was prompt-labeled but absent as audio Part.
- Closed "AI reacts after the moment passed" hallucination class via 3s file-based lookahead (Part 3 labeled NOT YET HEARD).
- Closed P85 Phase 16 ear-test override expiry — replaced by hybrid gate Decision Log entry.
- Closed §6.2 "no anti-slop receipts visible" white-space gap via EvidenceRegistry citation strip in live UI.
- Closed README "DJ-software 6-cell grid + canonical-10 controllers" drift (10 legacy controllers with zero JSON mapping + 10 referenced PNG paths that never existed in git).
- Closed v2.1 cut_release.sh P85 reminder echo lines.
- Closed Phase 27 autonomous-proxy-only override — replaced by hybrid regime.

**Issues Deferred:**

- 6 verification_gap human_needed items at close: 40/41/42/43/44/45-VERIFICATION.md — all by-design Kaan-action under `gsd-autonomous fully` mode (real-hardware ear-tests + legal-capacity carveouts).
- 13 SHIP-* customer-facing publishes — awaiting Apple Dev Agreement (Francesco) + SignPath OSS Foundation (Kaan) approvals before discharge.
- 3 AUDIO-* pre-stage items (AUDIO-05/06/07) — PGP + Tauri updater key + BlackHole fresh-Mac probe scaffolded; require external clock (Kaan custody + fresh macOS user account).
- 4 GATE-* corpus items (GATE-01/02/03/05) — ack-bank 20/40 + VCR cassettes + 6 × 30-min corpus WAVs + ear-test logs; require Gemini quota reset + Kaan ear-test session execution.
- 1 LAT-* spike (LAT-09) — Gemini 3.1 Flash Live music spike needs real 5-min DJ clip + offline listen + verdict write.
- 1 VIS-* (§VIS-04) — 5 Mixamo retargets need Adobe-account-gated download + Kaan-aesthetic Pioneer-CDJ-headbob selection.
- 5 LAUNCH-* discharges (LAUNCH-03/04/06/07/08) — real logo swaps + bravoh GH org standup + SHIP-TWEET sign-off + Discord live execute.

**Technical Debt Incurred:**

- Tauri capability missing for drag (v0.1.0-rc1 carryover; not closed in v3.0; tracked in `project_v0_1_0_rc1_open_bugs`).
- Mascot chrome strip + sidecar bundle schema path (v0.1.0-rc1 carryover).
- TCC list-population (v0.1.0-rc1 carryover).
- v2.1 ack-bank 20/40 quota gap carried into v3.0 GATE-01 discharge surface.
- 6 × 30-min DJ session WAVs in git-LFS still need download (EVAL-CORPUS-WAVS).
- VCR cassettes need one-time `VCR_RECORD_MODE=new_episodes` population (EVAL-VCR-CASSETTES).
- Bravoh proxy probe (BRAVOH-PROXY-PROBE) still in `MOCK_PROXY_FOR_DEV=1` dev mode pending Wave 0 real-host probe.

---

_For current project status, see .planning/ROADMAP.md_
