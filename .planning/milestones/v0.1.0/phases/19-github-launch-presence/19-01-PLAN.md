---
phase: 19-github-launch-presence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .gitignore
  - .github/repo-config.yml
  - scripts/dist/configure_repo.sh
  - scripts/hooks/pre-commit-no-binaries.sh
  - tests/repo/__init__.py
  - tests/repo/test_repo_scrub.py
  - tests/repo/test_repo_metadata.py
  - tauri/ui/assets/mascot/character.glb
  - "DELETE: _test_multimodal.py"
  - "DELETE: _test_tts.py"
  - "DELETE: sprite-1.png"
  - "DELETE: sprite-2.png"
  - "DELETE: sprite-3.png"
autonomous: true
requirements:
  - GH-01
  - GH-17
  - GH-18
user_setup: []

must_haves:
  truths:
    - "Repo root is free of scratch / sprite / .bak artifacts after this plan"
    - "Cloning the repo without git-lfs still works (large blobs streamed via LFS pointer)"
    - "Any future commit adding a >1 MB non-LFS file is rejected (CI + pre-commit hook)"
    - "Running `gh repo edit` from `.github/repo-config.yml` populates description + topics + homepage + squash-merge policy"
    - "GH-01 acknowledgement: actual repo at github.com/bravoh/vibemix is a Kaan-side action (create bravoh org OR transfer ozzaii/vibemix); this plan ships the configure_repo.sh automation that operates on the resulting repo and surfaces the manual step in 19-01-SUMMARY for Kaan to execute"
  artifacts:
    - path: .github/repo-config.yml
      provides: "Declarative repo metadata source consumed by configure_repo.sh"
      contains: "description:"
    - path: scripts/dist/configure_repo.sh
      provides: "Idempotent gh repo edit wrapper"
      exports: ["bash entrypoint"]
    - path: scripts/hooks/pre-commit-no-binaries.sh
      provides: "Pre-commit hook rejecting files >1 MB outside LFS"
    - path: tests/repo/test_repo_scrub.py
      provides: "CI gate for GH-18 hygiene rules"
    - path: tests/repo/test_repo_metadata.py
      provides: "CI gate for GH-17 repo-config.yml shape"
    - path: .gitattributes
      provides: "Git LFS tracking rule for character.glb"
      contains: "filter=lfs"
  key_links:
    - from: tests/repo/test_repo_scrub.py
      to: repo root
      via: "Path.glob + os.path.getsize"
      pattern: "_test_.*\\.py|\\.bak$|^\\.env$"
    - from: scripts/dist/configure_repo.sh
      to: GitHub repo settings
      via: "gh repo edit"
      pattern: "gh repo edit"
    - from: .gitattributes
      to: tauri/ui/assets/mascot/character.glb
      via: "git lfs track"
      pattern: "\\*.glb filter=lfs"
---

<objective>
Land the foundational repo hygiene + metadata gates for the GitHub launch presence: delete five scratch artifacts from repo root, track the 20 MB mascot GLB via Git LFS, ship the declarative `.github/repo-config.yml` + `gh repo edit` wrapper, install a pre-commit hook + CI tests that lock the no-binaries / no-scratch invariants going forward.

Purpose: Without this, README polish in Plan 19-03 lands on a repo whose first impression is "2.3 MB sprite-1.png in root". Close GH-17 + GH-18 first so everything downstream lands on a clean working tree.

Output: Clean repo root + LFS-tracked mascot + declarative repo-config + 2 CI gate test files + 1 pre-commit hook + extended `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-github-launch-presence/19-CONTEXT.md

# CONTEXT Area 5 (Repo Scrub) + Area 6 (Repo Metadata) are the load-bearing
# decisions for this plan. POC files (cohost*.py / mascot.html / mocks/ /
# fillers/ / cohost_v3.py / cohost_v4.py / run_v3.sh / run_v4.sh) are EXEMPT
# per CLAUDE.md "POC = reference, devour it" + CONTEXT Area 5 final paragraph.
# Archive-to-archive/poc/ is owned by Phase 1 (not this plan).

@.gitignore
@.github/workflows/release.yml
</context>

<interfaces>
<!-- Existing test pattern (tests/test_license.py) — match this style. -->

From tests/test_license.py:
```python
from __future__ import annotations
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent  # one level above tests/

def test_license_apache_2_0():
    license_path = REPO_ROOT / "LICENSE"
    assert license_path.exists(), "LICENSE missing at repo root"
```

REPO_ROOT for tests/repo/ resolves with `Path(__file__).resolve().parent.parent.parent` (two levels above tests/repo/).

POC-exempt list (DO NOT delete or flag in test):
  cohost.py, cohost_v2.py, cohost_lk.py, cohost_v3.py, cohost_v4.py,
  cohost.streaming.py.bak, mascot.html, run.sh, run_lk.sh, run_v2.sh,
  run_v3.sh, run_v4.sh, fillers/, mocks/, generate_bat.py
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repo scrub + .gitignore extensions + Git LFS for character.glb + CI test gate</name>
  <files>.gitattributes, .gitignore, tests/repo/__init__.py, tests/repo/test_repo_scrub.py, plus DELETE: _test_multimodal.py, _test_tts.py, sprite-1.png, sprite-2.png, sprite-3.png</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 5)
    - .gitignore (current rules — extend, do NOT replace)
    - tests/test_license.py (style template for repo-level tests)
    - Repo root listing: confirm presence of _test_multimodal.py, _test_tts.py, sprite-1.png, sprite-2.png, sprite-3.png
    - tauri/ui/assets/mascot/character.glb (20 MB — needs LFS migration)
  </read_first>
  <behavior>
    - Test 1: `_test_multimodal.py` and `_test_tts.py` are NOT present at repo root.
    - Test 2: `sprite-1.png`, `sprite-2.png`, `sprite-3.png` are NOT present at repo root.
    - Test 3: No tracked `.bak` files (cohost.streaming.py.bak is exempt — POC; assertion uses POC_EXEMPT allowlist).
    - Test 4: No tracked `.env` files (only `.env.example` if present).
    - Test 5: No tracked file >1 MB outside LFS, with POC files (cohost_v*.py, mascot.html) exempt.
    - Test 6: `.gitattributes` contains `*.glb filter=lfs diff=lfs merge=lfs -text`.
    - Test 7: POC-exempt files still exist at root (cohost.py, cohost_v2.py, cohost_v4.py, mascot.html).
    - Test 8: `.gitignore` contains entries for `*.bak`, `recordings/`, `node_modules/`, `.playwright-mcp/`, `*.png` at root (with explicit `!docs/assets/**/*.png` whitelist), v5 session screenshots (`v5-*.png`).
  </behavior>
  <action>
    1. Create `.gitattributes` at repo root with the single line `*.glb filter=lfs diff=lfs merge=lfs -text`. This satisfies CONTEXT Area 5 "git lfs track" recommendation. Do NOT run `git lfs install` or `git lfs migrate` interactively — that's developer-initiated. The `.gitattributes` line is the contract; the developer running `git lfs install --local` then re-staging `tauri/ui/assets/mascot/character.glb` does the migration. Document the one-line migration command in the task summary.

    2. Delete the five scratch files at repo root via `rm`: `_test_multimodal.py`, `_test_tts.py`, `sprite-1.png`, `sprite-2.png`, `sprite-3.png`. These are explicitly listed in CONTEXT Area 5. Do NOT touch `cohost*.py`, `mascot.html`, `run_v*.sh`, `fillers/`, `mocks/`, `generate_bat.py` — POC exempt per CLAUDE.md.

    3. Extend `.gitignore` (do NOT rewrite — append a new "Phase 19 — repo hygiene" block at end). Add: `*.bak` (defense-in-depth even though we're not deleting `cohost.streaming.py.bak` because it's POC), `v5-*.png` at root (the v5 session screenshots cluttering current `git status`), `.claude/worktrees/`, `node_modules/` (at root if any), `.playwright-mcp/`. Add a whitelist comment block: `# Docs assets (Phase 19) — Plan 19-03/04 ship hero.png + architecture.svg etc.` followed by `!docs/assets/`, `!docs/assets/**`.

    4. Create `tests/repo/__init__.py` (empty file, SPDX header line only matching project convention).

    5. Create `tests/repo/test_repo_scrub.py` (the GH-18 CI gate). Test file structure follows `tests/test_license.py` (SPDX header + `from __future__ import annotations` + `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`). Constants at top: `SCRATCH_NAMES = {"_test_multimodal.py", "_test_tts.py"}`, `SPRITE_NAMES = {"sprite-1.png", "sprite-2.png", "sprite-3.png"}`, `POC_EXEMPT = {"cohost.py", "cohost_v2.py", "cohost_lk.py", "cohost_v3.py", "cohost_v4.py", "cohost.streaming.py.bak", "mascot.html", "run.sh", "run_lk.sh", "run_v2.sh", "run_v3.sh", "run_v4.sh", "generate_bat.py"}` and dir-level `POC_EXEMPT_DIRS = {"fillers", "mocks", "archive"}`, `LFS_TRACKED_GLOBS = {"*.glb"}`. Eight tests per the `<behavior>` block above. The >1 MB test walks `git ls-files` output via `subprocess.run(["git", "ls-files"], cwd=REPO_ROOT, capture_output=True, text=True)`, splits lines, and for each non-LFS-globbed path checks `(REPO_ROOT / path).stat().st_size <= 1024 * 1024` UNLESS the basename is in POC_EXEMPT or its first path-segment is in POC_EXEMPT_DIRS. The `.bak` test walks `git ls-files | grep "\.bak$"` and asserts the only match is `cohost.streaming.py.bak` (POC).

    6. Run `pytest tests/repo/test_repo_scrub.py -x` from repo root. All 8 tests pass. If any fail, fix the actual file (i.e. the test is the gate, not a TODO).
  </action>
  <verify>
    <automated>pytest tests/repo/test_repo_scrub.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `ls _test_*.py sprite-*.png 2>&1` at repo root prints "No such file or directory" for all 5 names.
    - `cat .gitattributes` prints `*.glb filter=lfs diff=lfs merge=lfs -text`.
    - `grep -E "(\\*\\.bak|v5-\\*\\.png|\\.claude/worktrees/|\\.playwright-mcp/|!docs/assets/)" .gitignore` returns ≥5 matching lines.
    - `pytest tests/repo/test_repo_scrub.py -x` exits 0 with 8 tests passed.
    - POC presence sentinel: `ls cohost_v4.py mascot.html run_v4.sh mocks/ fillers/` all exist (proof we didn't over-delete).
  </acceptance_criteria>
  <done>
    Repo root scrubbed (5 deletions), `.gitattributes` tracks `*.glb` via LFS, `.gitignore` extended with the Phase 19 hygiene block + docs-assets whitelist, `tests/repo/test_repo_scrub.py` ships and gates 8 invariants, POC files unchanged. Migration command for the actual LFS upload documented in the SUMMARY: `git lfs install --local && git rm --cached tauri/ui/assets/mascot/character.glb && git add tauri/ui/assets/mascot/character.glb && git commit -m "chore(19): migrate mascot GLB to Git LFS"`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: .github/repo-config.yml + configure_repo.sh idempotent wrapper + pre-commit binary gate + GH-17 test</name>
  <files>.github/repo-config.yml, scripts/dist/configure_repo.sh, scripts/hooks/pre-commit-no-binaries.sh, tests/repo/test_repo_metadata.py</files>
  <read_first>
    - .planning/phases/19-github-launch-presence/19-CONTEXT.md (Area 6 — the literal YAML keys + values are locked there)
    - tests/repo/test_repo_scrub.py (after Task 1 — same REPO_ROOT pattern)
    - scripts/dist/sign_macos.sh (style template for bash scripts under scripts/dist/ — set -euo pipefail, shellcheck-clean, idempotent)
    - scripts/hooks/ (create if absent; mirror scripts/dist/ shebang + permission pattern)
  </read_first>
  <behavior>
    - Test 1: `.github/repo-config.yml` parses as valid YAML.
    - Test 2: Required keys present: `description`, `homepage`, `topics`, `default_branch`, `enable_issues`, `enable_projects`, `enable_wiki`, `delete_branch_on_merge`, `allow_squash_merge`, `allow_merge_commit`, `allow_rebase_merge`.
    - Test 3: `topics` is a list of length ≥10 and contains the locked set: `dj`, `livekit`, `gemini`, `ai-assistant`, `audio`, `midi`, `pioneer-ddj`, `realtime-ai`, `tauri`, `open-source`.
    - Test 4: `description` ≤ 350 chars (GitHub limit) and references "AI co-host" + "DJ" + "Mac" + "Windows".
    - Test 5: `enable_projects` and `enable_wiki` are `false`; `delete_branch_on_merge` is `true`; `allow_squash_merge` is `true`; `allow_merge_commit` and `allow_rebase_merge` are `false`.
    - Test 6: `scripts/dist/configure_repo.sh` exists and is executable (`os.access(..., os.X_OK)`).
    - Test 7: `scripts/hooks/pre-commit-no-binaries.sh` exists and is executable.
    - Test 8: `bash -n scripts/dist/configure_repo.sh` exits 0 (syntax-clean).
    - Test 9: `bash -n scripts/hooks/pre-commit-no-binaries.sh` exits 0.
    - Test 10: `configure_repo.sh` references `gh repo edit` and reads `.github/repo-config.yml`.
    - Test 11: `pre-commit-no-binaries.sh` references `git lfs ls-files` (LFS exemption) and a `1048576` byte threshold (1 MB).
  </behavior>
  <action>
    1. Create `.github/repo-config.yml` with the exact values locked in CONTEXT Area 6:
       - `description`: `AI co-host for your DJ set. Listens to your master output, watches your DJ software, talks back into your headphones. Open source. Mac + Windows.`
       - `homepage`: `https://altidus.world/vibemix`
       - `topics`: list — `dj`, `livekit`, `gemini`, `ai-assistant`, `audio`, `midi`, `pioneer-ddj`, `realtime-ai`, `tauri`, `open-source`
       - `default_branch`: `main`
       - `enable_issues`: `true`
       - `enable_projects`: `false`
       - `enable_wiki`: `false`
       - `delete_branch_on_merge`: `true`
       - `allow_squash_merge`: `true`
       - `allow_merge_commit`: `false`
       - `allow_rebase_merge`: `false`
       Header comment block: `# Phase 19 — Repo metadata source of truth. Consumed by scripts/dist/configure_repo.sh which wraps `gh repo edit`. See .planning/phases/19-github-launch-presence/19-CONTEXT.md Area 6.`

    2. Create `scripts/dist/configure_repo.sh` (chmod +x). Style: mirror `scripts/dist/sign_macos.sh` opening (`#!/usr/bin/env bash`, `set -euo pipefail`, top-comment block, `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"`). Read `.github/repo-config.yml` via `yq` if available else fail with an actionable error message pointing to `brew install yq`. Build a single `gh repo edit` invocation reading: description, homepage, topics (comma-joined for `--add-topic` repeated flags), and the merge/issue/wiki/project switches. Idempotent: running twice yields the same end state. Final command: actually fires `gh repo edit` ONLY when `--apply` flag is passed; without `--apply` it prints the planned command for review (defense against accidental runs). Exit 0 on success.

    3. Create `scripts/hooks/pre-commit-no-binaries.sh` (chmod +x). Shebang + `set -euo pipefail`. Logic: iterate over `git diff --cached --name-only --diff-filter=A` (added files only), for each file check size via `wc -c < "$file"` against threshold 1048576 (1 MB). If size > threshold AND file is NOT in `git lfs ls-files` output, print a clear rejection (`echo "ERROR: $file is N MB and not LFS-tracked..."`) and exit 1. Exit 0 if no blockers. Header comment block instructs developer to install via `ln -sf ../../scripts/hooks/pre-commit-no-binaries.sh .git/hooks/pre-commit` — installation is developer-initiated (this hook lands as a script under version control, not in `.git/hooks/` directly, since that dir is per-clone).

    4. Create `tests/repo/test_repo_metadata.py`. Use the REPO_ROOT pattern from Task 1's test file. 11 tests per the `<behavior>` block above. YAML loader is `yaml.safe_load` (PyYAML is already a transitive dep — verify with `python -c "import yaml; print(yaml.__version__)"`; if not present, add `pyyaml>=6.0` to `[dependency-groups] dev` in pyproject.toml). Bash syntax tests use `subprocess.run(["bash", "-n", str(path)], check=False)` and assert returncode == 0.

    5. Run `pytest tests/repo/ -x`. All tests from Task 1 + Task 2 pass (target: 19 tests total — 8 scrub + 11 metadata).
  </action>
  <verify>
    <automated>pytest tests/repo/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; print(yaml.safe_load(open('.github/repo-config.yml')))"` prints a dict containing all 11 required keys.
    - `bash -n scripts/dist/configure_repo.sh && bash -n scripts/hooks/pre-commit-no-binaries.sh` exits 0.
    - `test -x scripts/dist/configure_repo.sh && test -x scripts/hooks/pre-commit-no-binaries.sh` exits 0.
    - `grep -c "gh repo edit" scripts/dist/configure_repo.sh` ≥ 1.
    - `grep -v '^#' scripts/hooks/pre-commit-no-binaries.sh | grep -c "1048576"` ≥ 1.
    - `pytest tests/repo/ -x` exits 0 with ≥19 tests passing.
  </acceptance_criteria>
  <done>
    `.github/repo-config.yml` declarative source-of-truth lands with all 11 keys locked from CONTEXT Area 6; `configure_repo.sh` idempotent wrapper ships with `--apply` safety gate; pre-commit binary hook ships under `scripts/hooks/`; `tests/repo/test_repo_metadata.py` gates the YAML shape + executable bits + shellcheck-clean syntax. Kaan runs `bash scripts/dist/configure_repo.sh --apply` once after Phase 19 ships to push metadata to GitHub (documented in SUMMARY as post-phase developer action).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → repo | Files added to working tree may contain secrets / oversize blobs / scratch. Pre-commit hook + CI test enforce hygiene. |
| repo → GitHub | `configure_repo.sh` mutates remote repo settings via `gh` CLI. Idempotent + `--apply` gate prevents accidental config drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | repo root | mitigate | Repo scrub test gates `.env` / `_test_*.py` / `>1MB-non-LFS` on every PR. Secrets in repo would leak Gemini API keys (Phase 5 invariant). |
| T-19-02 | Tampering | configure_repo.sh | mitigate | `--apply` flag required; without it the script prints the planned command. Prevents accidental description/topic overwrite by automation. |
| T-19-03 | Denial of Service (clone bloat) | tauri/ui/assets/mascot/character.glb | mitigate | Git LFS via `.gitattributes` keeps the file logically in-repo for Tauri build but unblocks fresh clones. |
| T-19-04 | Spoofing | pre-commit hook | accept | Hook is a script under version control; developer installs via symlink. Cannot enforce on every clone, but CI test in Task 1 catches what the local hook misses. |
</threat_model>

<verification>
- `pytest tests/repo/ -x` exits 0 (≥19 tests across both files).
- `ls _test_*.py sprite-*.png 2>&1` reports all 5 files absent.
- `cat .gitattributes` includes `*.glb filter=lfs ...`.
- POC sentinel still present: `ls cohost_v4.py mascot.html mocks/ fillers/` all hit.
- `python -c "import yaml; d=yaml.safe_load(open('.github/repo-config.yml')); assert len(d['topics'])>=10"` exits 0.
</verification>

<success_criteria>
GH-17 (repo description + topics tags) and GH-18 (repo hygiene scrub) are gated by automated CI tests + declarative source-of-truth files. Repo root has zero scratch artifacts. Mascot GLB is LFS-tracked. Pre-commit hook + CI test prevent regression.
</success_criteria>

<output>
After completion, create `.planning/phases/19-github-launch-presence/19-01-SUMMARY.md`.
</output>
