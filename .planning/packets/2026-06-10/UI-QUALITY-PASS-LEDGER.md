# UI Quality Pass — Ledger (2026-06-10)

9-surface audit vs `mocks/vibemix-bravoh-grade-pink.html` (the contract) + frontend-enforcement skill. Full findings with file:line + fix sketches: `UI-QUALITY-AUDIT-FULL.json` (key `audits`; ~90 findings, 14 P0). Before-screenshots `/tmp/uxshots/audit-before/`. Baseline at start: **174 files / 1614 tests green** on the dirty working tree.

Grades: wizard **5.5** · viber **6** · tokens **6** · motion **6** · learn/debrief **6** · a11y **6.5** · deck **7** · shell **7** · settings **7**.

## Fix units (atomic commits, test pins move in the same commit)

- [x] **U1 wizard re-materialization** (fa2842b1 + 7702acf9 — also: live clock, spoken done-handoff, sticky CTA) (worst grade, self-contained): kill v5 cool-blue + `#d6cfc7` literals in `primary-panel.ts:37-52`; port rose keycap into `button.ts` armed; collapse green/red/yellow LED triad → rose+ink (fault-red only on true failure); pin "Open vibemix" CTA above the fold; style the bare done-div + dead `00:00` clock.
- [x] **U2 settings organs** (f2ffdb48 — banner, voice readout, genre labels, mascot DEV-gate, dead help LEDs, warm toast, sentinels; mono retexture + blur rocker still OPEN): style+hide staleness banner (`staleness-banner.ts:36-49`, ships as raw text "Library is … old."); replace dead 14-voice MOSS picker (`SettingsDrawer.ts:493-515,932-943`) with honest `SVEN · LOCAL VOICE` readout — KEEP the config field/schema (only the control dies); mono-label retexture (`group.ts:72-91`, `rocker.ts:62-66` — shared with deck, two suites move).
- [x] **U3 shell** (3487c2c0 — footer seam-sync, frosted palette, Learn tease voice, panel ×+idle furniture, cmdk keycap, TONIGHT gate, badge words, settings empty fix; debrief labels still OPEN): footer/panel occlusion — offset `.shell-footer` by `--panel-cur`, matched 250ms (`shell.css:1596-1612`; var must exist =0 on every surface); Learn tease copy `app.ts:97-120` sprint-memo → Sven first person (also `drills-panel.ts:144/152`); palette frosted glass (MUST honor lighter-blur flag / blur-perf-ladder spec); grounding drawer: panel-label slab idle copy + 24px ×-close.
- [x] **U4 deck instruments** (1c37ed44 — phrase tape + nowPct playhead + RMS→opacity, ~N bars grammar, MUTE/STOP powered, ghost mark, dead 780 rules; receipt restructure + persona strip still OPEN): render `phase.chunks`/`nowPct` as segmented phrase-line + playhead (props flow and die unrendered, `SessionLayout.ts:1051-1093,1932-1943`); drop-chip fake `:00` timecode → "~N bars" phrase (`drop-chip.ts:149-154`; pin `tests/session/components.spec.ts:165` moves); fmeter fill/peak clamp mismatch; MUTE/STOP rest material (`SessionLayout.ts:358-374` kill 0.58 dimmer); text floor → no glyph below `--ink-20`.
- [x] **U5a tokens** (f3c25123 — 8 undefined tokens → semantic ladder, --ok/--warn/--danger added, foreign #ff6b6b dead): undefined tokens `--silk-90/-72/-58/-38/-35/-50`, `--mint-pale`, `--radius-sm` → real ladder; add `--danger/--ok/--warn` aliases (error must stop rendering as brand rose / `#ff6b6b`). Sites: `library.css:1098-1625,2509-2529`, `learn.css:1594`, `pill.css:1337,2436`, `library-panel.ts:169`.
- [x] **U5b radius collapse** (eb200c08 — one ladder: new `--r-xs: 4px` canonical micro step, `--rad-sm/md/lg` now alias `--r-xs/--r-sm/--r-md`; 2px micro corner retired; probe-verified in live page).
- [x] **U6 a11y** (a82dd075 — 8 focus-ring suppressions deleted, global rose ring applies; text-floor sweep still OPEN): delete six `:focus-visible { outline: none }` overrides (mode-picker, rocker, titlebar, status-bar, picker, citation-strip) or upgrade to 2px brand-50; reduced-motion leak sweep.
- [x] **U7 motion** (648f7645 — gate = ONE ambient runtime-verified, perimeter stilled, hidden-foot loops paused, BPM playhead glide, 3 reduced-motion leaks; easing collapse 108 literals still OPEN): one-breath law on gate screen (sidebar ear yields — reuse `shell.css:303` pattern; still the 22s perimeter sweep); BPM-locked fmeter peak glide + receipt bar duration (reuse `--bpm-period-ms` feed; `--bpm-beat/--bpm-bar` sit dead in tokens.css); easing collapse: 108× hardcoded `cubic-bezier(0.16,1,0.3,1)` → `var(--ease-brand)`; wizard router inline X-slide also ignores reduced-motion.
- [x] **U8 debrief dock flatten** (a1506b75 — stage-as-page: dock+mast plates killed, readiness = ONE lead card (.nr.lead recipe, serif line, left brand bar), rows hairline .nr, sentinel stat wall hidden at data-state=empty, meter → squared recessed rail, blocker→waiting on / unlocks→you get, drills-panel Sven voice + raw lesson-id off the glass).

## SESSION 2026-06-10 CLOSED — 8 commits fa2842b1..648f7645, suite 1614 green at every step, prod build green. After-shots /tmp/uxshots/{u-final,wiz-final,settings-final}. Also done: frontend-enforcement SKILL.md is STALE (still says amber/charcoal v5) — refresh when touching docs.

## SESSION 2026-06-10 EVENING (continuation) — 6 more commits eb200c08..af8b3001, suite 1615 green each step, build green.

- [x] U5b radius collapse (eb200c08) · U8 debrief flatten (a1506b75) · **U2b settings retexture** (08cfa6e7 — engraved mono headers/labels/rocker segs; LIGHTER BLUR chip → OFF|ON rocker; About version build-time injected from Cargo.toml via vite define; single-option DEVICE dropdown → honest readout (graduates to picker when a saved device exists); profile transport-error → calm sentence; VOICE badge + AUTO pill demoted to ink; `settings.output.device` wire contracted) · **easing collapse** (1f035af9 — 107 brand-curve literals → var(--ease-brand), +--ease-draw/--ease-snap tokens, wizard router X-slide → rise language + reduced-motion gate) · **deck remainder** (af8b3001 — receipt → two-line proof object w/ cite-linked hover, persona → 3-chip direct-select strip, deck text-floor sweep to --text-disabled minimum).
- **WAVE-2 AUDIT DONE** (9 agents): `UI-AUDIT-WAVE2-FULL.json` — 84 findings/8 P0 over pill (6.5), overlay (7), debrief WINDOW (4.5!), deck-failure (6), wizard-failure (4!), go-live (7), FOUC (7.5), copy (6.5) + adversarial critic (8 suspect findings corrected in-file, gaps: quit-guard likely-P0 `quit-guard.ts:37-40` keys off hardcoded livekit=ok; tray.rs unaudited; voice "Adam" default product-wide `voice_presets.py:6`). Critic's top 3 fixes: (1) one honest run/capture state on the wire (ws_bus.py:1503 hardcoded livekit=ok) with 4 consumers, (2) gold-lane swap (pill Camelot rose→--gold + --amber alias quarantine), (3) ONE enforced copy-contract vitest sweep instead of ~20 point edits. READ THE JSON before the next fix wave — several line anchors drifted (critic flagged), re-pin before fixing.

## WAVE-2 P0 FIX WAVE — ALL 8 P0s CLOSED (3 commits 39bbf92c, 8ea1376f, b7e6c1e9; suite 1620 green, build green)

- [x] **Deck** (39bbf92c): invisible keyboard-focusable RESTART button hard-gated to fault mode (disabled+tabIndex -1+aria-hidden elsewhere; named in fault). Spec pin added.
- [x] **Wizard** (8ea1376f): driver-fetch Continue now PAINTS armed via setButtonState (probe-proven {dataState:armed, cursor:pointer}); the 3 fake setTimeout probe rows + "Bravoh proxy: reachable" jargon DELETED (one row, one real probe; copy keys removed); smoke-test failure renders its own honest branch (failed field, fault disc, Retry, CTA stays armed) instead of the pixel-identical success surface; static 3-bar meter deleted. New spec smoke-test-honest-failure.spec.ts.
- [x] **Debrief window** (b7e6c1e9): verdict counts only kind==track chapters as TRACKS (else MOMENTS — "9 TRACKS" on a 3-track set is dead); chapter rail wired to the vmx-debrief-deeplink scroll+pulse path (was a fully-styled dead control); raw ws onerror no longer paints "Debrief crashed unexpectedly" during normal late-bind boot (banner requires a verdict-carrying reason; transport noise → ws-error). buildVerdictText pins added.

## NEXT QUEUE (post-compact, in order of leverage)

1. **Gold-lane swap** (task open): pill `deck-chips.ts` resolved-Camelot `var(--amber)`→`var(--gold)`+gold-glow; debrief.css raw gold literals → tokens; peek cue-rail gold gradient → brand. Check `--amber: var(--brand)` alias trap (tokens.css:288) — authors reaching for --amber silently get rose.
2. **Copy-contract vitest sweep** (critic lever #3): one spec banning em dashes / "VIBEMIX-CORE" / "Adam" / port numbers / livekit-gemini-codex jargon in user-visible strings. Also fix `voice_presets.py:6` DEFAULT_VOICE="Adam" (product-wide, python side).
3. **Honest run/capture state** (critic lever #1, cross-island): ws_bus.py:1503 hardcoded livekit=ok → real armed/warming/capturing/live; consumers: deck mode, armed gate, tray.rs, quit-guard.ts:37-40 (quit-guard keying off the hardcoded ok = likely P0: live set may quit without guard or idle may block quit).
4. Wave-2 P1s by surface (pill rose-budget + dead click zone [anchor drifted — re-pin], FOUC preloads, go-live feedback) + critic gaps (tray, updater, settings interior failure states, Windows wizard parity).
5. Deferred carryovers: v5→v6 rename sweep (+legacy-gate rewrite SAME commit), NEXT UP (needs live suggestion_service proof), viber index.ts (other session's WIP — check if still dirty).

## DEFERRED (not tonight, with reasons)

- **Viber `index.ts` fixes** (data-role "you"→"user" P0 at `:1773`, raw-hash receipts `:2285-2287,2574`, tool-rack into active turn `:2180-2210`): file is ANOTHER SESSION'S uncommitted WIP — hands off. Also the role fix is 3 coupled sites + persisted history compat (map role→data-role at render only, never rename stored values) + `chat.test.ts:2084` pin (also dirty). CSS-only viber fixes (progress bar → recessed hairline, `library.css:1574-1601`) are fair game.
- **NEXT UP queue on deck**: gated on a dark producer (`suggestion_service=None` in `__main__.py`) — shipping the slab without proving SuggestionService fires on the live bus yields a permanently-empty lead card, worse than the void. Needs drive-vibemix proof first.
- **v5→v6 full rename sweep** (914 refs silk/amber/glass → ink/brand/border): MUST land with the legacy-gate rewrite in the SAME commit — `tests/tokens.legacy-detect.test.ts` regex currently bans the canonical `--ink-*` ladder (gate inversion) and is byte-synced with `scripts/check_v5_migration.sh`. Big, do as its own session-closing unit or next session.
- **Unaudited surfaces** (critic): floating PILL (the default in-set surface!), mascot overlay (in-flight WIP anyway), click-through overlay, crash/muted/error banners, wizard failure paths, deck failure states (`ai service unreachable` hero), go-live warming/ARMED in-between, tray/quit-guard/window behavior, font-load FOUC, Sven-voice copy consistency pass. → next audit wave.

## Standing risks (from the adversarial critic — read before each unit)

1. Test pins: `components.spec.ts:165` (DROP IN), `start-gate.spec.ts` captions, `drills-panel-shape.spec.ts:89` copy, settings/shell suites, visual snapshots — move atomically with fixes.
2. Concurrent WIP: tree dirty on `library/index.ts`, mascot, `vitest.config.ts`, `tsconfig.json`, `package.json` etc. Pathspec commits only; verify `git diff --cached --name-only` before every commit.
3. Invariant #5: anything near status/grounding copy keeps the failure timer gated on ACTIVE (`grounding-failure.spec.ts`).
4. Invariant #4: new wiring rides the Rust IPC bridge, never a second ws client; new ipc fields need `npm run codegen:ipc`.
5. Optimistic repaint: every rebuilt settings control flips `data-active` locally.
6. Shared components (rocker/picker) re-skin deck AND settings; tokens.css edits re-skin pill/mascot/debrief — re-capture after.
7. New glass honors the lighter-blur flag (`blur-perf-ladder.spec.ts`).

## Verdict cross-ref

Framework question settled: **stay-tauri (0.86)** — `.planning/research/2026-06-10-native-ui-verdict.md`. Judge conditions feeding engineering: nspanel pill now, Windows/WebView2 QA lane, webview footprint budget, IPC seam stays UI-agnostic.

## SESSION 2026-06-10 NIGHT (post-compact continuation) — 11 commits 09fe253c..59403abe, suite 1629 green, build green, cargo compile green

- [x] **Gold-lane swap** (09fe253c): pill resolved-Camelot → --gold+glow (CSS pin bans --amber in deck-chips); peek cue-rail gradient → brand-only; debrief verdict gold → brand-glow (tracks/runtime = structural, not heat); tokens.css --amber alias block carries the explicit quarantine note.
- [x] **Copy contract** (584945ba + 989033a5): NEW `tests/copy-contract.spec.ts` — string-literal scanner (comments stripped, html entries included) banning em dashes / vibemix-core / port-prose / LiveKit-Gemini-Codex prose / "Adam" / cache-path-prose. 42 violations fixed same commit (crash banner, debrief errors, deck notices, wizard copy, titles, dev logs). Python: DEFAULT_VOICE "Adam"→"Sven" + "adam" added to legacy normalize set; ~20 pins moved. EXCLUDED w/ reasons: library/index.ts (other session's WIP — Codex jargon queued), library/api.ts (mock DATA).
- [x] **Quit-guard P0** (36891383): isLiveSessionActive → runState==="running" (status.livekit==="ok" arrives 1Hz from boot IDLE INCLUDED → guard was armed forever; idle quit threw STILL LIVE). Pin: livekit=ok at idle must NOT arm.
- [x] **Deck/go-live honesty** (7c4df2c5): Start/Stop emit-failure parks runState back + deck notice (was: forever-fake LIVE deck / fake-armed during live set); fault blank hero → FAULT_HERO_LINE placeholder; idle readiness ghosts drop the false "earlier" provenance label (data-kind=readiness); "Voice muted."→"Voice off." (engine-down ≠ user mute). New spec start-emit-failure.spec.ts.
- [x] **Noun unification** (18123daf): BRAIN + VOICE LINK everywhere (badges, tooltips, fault line, idle notes, debrief "Sven's brain"); 'key' jargon dropped from error copy.
- [x] **Pill honesty** (516b5adc): wave decoration exempted from the no-drag click guard (half the face was a dead zone w/ pointer cursor); notch moved INSIDE the 44px face (top:40px — never painted before); aria "load suggestion"→"pin this next" + 1.8s PINNED reactionEcho on accept; text floor silk-40(×0.72)→silk-65 (unknown key/bpm, fallback chip, next-card label/why).
- [x] **Wizard failures** (32b207cc): row_failed label (red LED no longer wears "Checking…"); native window.confirm → styled SETUP DIDN'T SAVE dialog; raw err.message out of feed chips. ⚠️ SOURCE onboarding_copy.json aligned with the mirror — it still carried the dead probe keys (a sync would have resurrected "Bravoh proxy: reachable").
- [x] **Settings drawer inert** (1aa44092): closed drawer = inert+aria-hidden (controls leave tab order; was keyboard-focusable invisible rows).
- [x] **Overlay tokens** (99b547a7): 3× hardcoded brand curve → var(--ease-draw); sheens → one --ring-spec-rgb triplet. (The copy gate caught a `*/` inside my new CSS comment terminating it early — fixed; gate proven useful same-day.)
- [x] **FOUC** (59403abe): debrief/learn/library WebviewWindowBuilder.background_color(#1A1618) — no more platform-white flash; window titles em-dash → ' · '. NOTE: bare `cargo check` fails at HEAD pre-existing ("Not a directory (os error 20)" in tauri-build resource walk, reproduced with changes stashed; compile verified via TAURI_CONFIG='{"bundle":{"resources":[]}}' cargo check = green). The stale target/release/installer delete did NOT clear it — different instance of the gotcha, packaging lane to investigate before next bundle.

## NEXT QUEUE v2 (what remains, in order of leverage)

1. **Debrief interior P1s**: TL;DR meta clipped at 1440x900 (player well too short); evidence tooltip never anchored (opens at document tail); 27 raw color literals incl. 3 retired-palette values (mechanical sweep vs --brand-XX/--silk-XX/--gold-glow); cold skeletons claim perpetual work + teach nothing.
2. **Pill rose budget** (design pass — needs screenshots): 5+ simultaneous rose accents per reaction vs One-Rose; do with live captures, not blind.
3. **Warming ≠ live-listening** (python wire): the go-live-deterministic packet owns it (capture-callback-gated "started" + ARMED status). Same lane: "audio input dropped" fault is dead-wired (no producer), armed-gate "checking" states ride never-faulting values.
4. **Critic gaps**: tray.rs classify() Error lane keyed to livekit=down (never emitted — dead lane, harmless but lying code); updater unaudited; settings interior failure states; Windows wizard parity.
5. **Carryovers**: v5→v6 rename sweep (+legacy-gate rewrite SAME commit); NEXT UP (blocked on live suggestion_service proof); viber index.ts P0s (STILL another session's dirty WIP — copy-contract EXCLUDED entry waits on it); screen-noun P2 unification (screen proof/capture/recording/grounding).
6. **Packaging lane**: investigate the pre-existing cargo resource-walk failure before the next DMG (does NOT block dev — vite/vitest unaffected; bundling may be).

---

## SESSION 2026-06-10 LATE NIGHT (impeccable continuation) — 7 commits ab24672d..d94fcff7

Queue executed: NEXT QUEUE v2 #1 (debrief interior, the 4.5/10 surface) end-to-end, plus the impeccable-run arming task. Re-pinned all 10 audit findings against HEAD first (6-agent fan-out): 2 already closed (gold verdict was re-toned in 09fe253c; lesson_id/"parked" copy fixed by the copy sweep), 8 fixed below. Suite 1644 green + 1 todo (178 files), build green, key fixes live-verified in the browser at 1440x900.

- [x] **frontend-enforcement SKILL.md refresh** (ab24672d): was STALE v5 (phosphor-amber #ff8a1a accent / charcoal #14171c dominant / faux-3D bevels / DSEG7 fonts) — any agent leaning on it polished the wrong contract. Rewritten from DESIGN.md: One-Rose, gold quarantine, hex-outside-tokens, inset-bezel tactility, Saira/JBM/Instrument-Serif lock, copy-contract + idle≠fault gates, authority chain (pink mock → DESIGN.md → this file).
- [x] **Skeleton terminal states** (f88991f0): resolveSkeletonsToTerminal settles "Generating…" skeletons on terminal errors (scoped per reason; success frames never overwritten; settled dot dims to neutral). Timeline empty copy teaches. NEW gate ban: ascii-ellipsis (7 violations normalized to … across debrief/dock/pill/learn/settings).
- [x] **Noun unification** (f2e6ee52): moments (sidebar/HUD/hover/empty; verdict TRACKS stays — different concept), recap (panel title, skeleton, errors), debrief (window + DebriefDock + surfaces; "review" survives only as a verb). Error copy: "expected layout"/"event data"/"≥ 5 minutes" rewritten in co-host voice; unmapped reasons no longer echo raw sidecar prose (console.warn instead).
- [x] **Evidence tooltip anchored** (8e7e0435): chip/region clicks carry viewport coords (keyboard → element rect), window stashes across the ws round-trip, tooltip opens at the click clamped to viewport, position:fixed. NEW citation-tooltip.spec.ts (8 tests). Note: mock mode never wired citation-click — chips are dead in mock only (pre-existing).
- [x] **Recap well clipping** (378cda6c): align-content start (center split overflow across BOTH clipped edges), min-content row floor, max-height:940px diet for mock rail/HUD. Live re-measured: meta whole, rail whole, contentFits=true. Morning panel mock-overflow (283 vs 219) is PRE-EXISTING and untouched.
- [x] **Color-literal sweep** (1648274c): 26 literals → tokens (retired v5 silk ×4, retired amber-orange hover, fault-red ×2 via NEW --danger-10/--danger-58 in tokens.css, 19 rose ladder steps, border-pink rivets, cool blue-cast root/titlebar darks → warm glass-3/void-85). One sanctioned literal remains: signal-bar runtime alpha (documented in place).
- [x] **Feedback honesty + dead chrome** (d94fcff7): "Was this call right?" caption, un-vote on re-click (local only, no fake retraction), sendMomentFeedback→boolean + moment-feedback-rejected reverts pressed state on closed socket. Deleted: phantom "ear" grid rows, ~40 lines of unmounted titlebar __status styles, the permanently-empty session slot.

### NEXT QUEUE v3 (carried forward, leverage order)
1. **Pill rose budget** (design pass — needs live screenshots, `localhost:1420/pill.html` + capture): 5+ simultaneous rose accents per reaction vs One-Rose.
2. **Warming ≠ live-listening** (python wire): owned by the go-live-deterministic packet (capture-callback-gated "started" + ARMED status). Same lane: "audio input dropped" fault dead-wired; armed-gate "checking" states ride never-faulting values.
3. **Critic gaps**: tray.rs classify() Error lane keyed to never-emitted livekit=down (dead lane); updater unaudited; settings interior failure states; Windows wizard parity.
4. **Carryovers**: v5→v6 rename sweep (+legacy-gate rewrite SAME commit); NEXT UP (blocked on live suggestion_service proof); viber index.ts P0s (other session's dirty WIP — copy-contract EXCLUDED entry waits); screen-noun P2; debrief mock-mode citation chips dead (no listener) + morning panel mock overflow (both pre-existing, low).
5. **Packaging lane**: pre-existing bare `cargo check` failure ("Not a directory" resource walk; compile-verify via TAURI_CONFIG='{"bundle":{"resources":[]}}') — investigate before next DMG.

## SESSION 2026-06-10 DEEP NIGHT (goal: fable pass to perfection) — 3 commits 18599d49..6de78789 + packaging unblock

NEXT QUEUE v3 #1 (pill rose budget) + the pill P2 tail + v3 #3 tray lane + v3 #5 packaging investigation. Pill states driven LIVE in the browser via a WebSocket mock (no demo env needed: addInitScript replaces window.WebSocket, pushes real wire frames); before/after captures + computed-style measurements in `.planning/packets/2026-06-10/pill-rose-budget/`.

- [x] **Pill rose budget** (18599d49): measured 8 simultaneous rose sources in one BOMB frame (border+glow, lead+triple glow, receipt rule, stage rail 0.88, panel bloom 0.72, duplicate reaction underline, chip borders, 7 lit wave bars + blinking dot). One-Rose applied: lead owns the rose at expand (lozenge edge → hover-grade silk, stage rail + receipt → structural silk w/ fault-on-negative, duplicate reaction::before rail DELETED + its keyframes, bloom 0.72→0.36, chips rest on glass-edge); speaking = wave is the single breath, dot static lit (blink only at listening); echo holds the dot static. fx palettes: 6 off-brand RGB triplet sets (incl. gold secondary = quarantine break) → NEW tokens.css channel triplets --brand-rgb/--silk-rgb/--warn-rgb/--fault-rgb. waveform lit bar spells --brand (alias out); 6 dead :not([data-feedback]) guards deleted. AFTER verified: BOMB frame = rose lead + gold Camelot only; NEG = fault-loud no-rose; CLEAN = silk-quiet.
- [x] **Pill register + keyboard** (11500e6a): face label COHOST→SVEN, hover DJ KNOWS→NEXT READY (pins moved in vitest + pw specs); keyboard re-activation trap closed (fresh pick while root focused re-arms peek — pure helper + 2 specs); fx TS fallbacks aligned to brand/silk channels.
- [x] **Tray honesty** (6de78789): derive_tray_state faulted on livekit=down (wire hardcodes "ok" — dead lane) AND screen=denied (badge-only by contract; audio-only DJs got a permanent ERROR icon). Now gemini=down is the only status fault; dead fields dropped from SnapshotView/listener; contract pinned in a unit test. cargo test tray:: 9/9.
- [x] **Packaging unblock** (no commit — build artifact): the bare `cargo check` "Not a directory (os error 20)" was a stray 2-byte `target/debug/INSTALLER` file (content "uv", a dist-info artifact from 2026-05-12) case-colliding with the `installer/` resource dir on macOS — the DEBUG-profile twin of the documented release gotcha. Deleted → bare `cargo check` green in 6.75s, no TAURI_CONFIG override needed. If it recurs after a sidecar rebuild, look for dist-info INSTALLER files landing at target/<profile>/ root.

Gates: full vitest 1646 passed + 1 todo (178 files), npm run build green, tsc green, cargo test tray 9/9, bare cargo check green. Captures: pill-rose-budget/{before,after}/*.png.

### NEXT QUEUE v4 — items 1+2(updater) CLOSED same session (2 more commits 99e90fdf, acdbca64)

- [x] **Warming ≠ live (v4 #1)**: RE-PINNED against HEAD — the python keystone ALREADY LANDED in the gobble wave (capture spawns BEFORE Chatterbox warmup `__main__.py:2501` vs `:2502+` "warming Chatterbox while capture arms"; "started" gated on stream bind; capture_bound/capture_failed events + ipc.error original_type=audio.capture; activation-time device re-resolve). The packet's ARMED wire status remains unbuilt but is now marginal (bind-gated started + deck notices cover it). What was STILL stale = the TS half, closed in **99e90fdf**: faultInput's dead livekit→"audio" arm + unreachable "screen" cause deleted (brain = the one honest fault); the never-lightable status-row "audio" chip DELETED with its wiring; armedCaptureText's dead "connecting"/"dropped" arms dropped (checking=pre-tick, armed=tick-seen, master in=real RMS). NO state invented from absence of signal (first attempt keyed a chip to missing captureDevice — 2 specs correctly rejected it as inverted invariant-#5; reverted to deletion).
- [x] **Updater audit (v4 #2 part)**: REAL consent bug found+fixed (**acdbca64**) — `dialog: true` is Tauri v1; the v2 plugin Config silently ignores unknown fields (verified in vendored 2.10.1 source), so the shipped path was boot → silent download_and_install, zero interaction, while comments promised a prompt. Now: native ask via tauri-plugin-dialog ("Install now / Later") before any download; dead `dialog`/`active` fields deleted from tauri.conf.json5; lore comments fixed in 3 files. cargo check green; test_updater_key_rotated.py 7 passed.

### NEXT QUEUE v5 (carried forward)
1. **Critic gaps left**: settings interior failure states; Windows wizard parity.
2. **Carryovers**: v5→v6 rename sweep (+legacy-gate rewrite SAME commit — own session); NEXT UP (blocked on live suggestion_service proof); viber index.ts P0s (other session's WIP); screen-noun P2; debrief mock-mode citation chips dead + morning panel mock overflow (mock-only, low).
3. **Live-verify lane**: the updater prompt + tray gemini-down error icon + deck fault flow want a packaged-app eyeball (browser can't reach native dialog/tray).

## SESSION 2026-06-10 LATE (loop continuation) — 6 commits 45db225e..2b8a9292 + 4-lane audit wf_9fa1416e-7eb

4-agent fan-out audited the v5 lanes (~580k tokens, anchors verified at HEAD): settings-failures 8 findings, Windows wizard parity 9 (incl. the VB-CABLE P1 below), screen-noun 12 sites, debrief-mock 4. Fix wave:

- [x] **Settings honesty trio** (45db225e): all-failed import no longer paints "N processed" success (reads failed/failure_reason: "Couldn't index any of the N files (reason)" / "42 indexed, 8 skipped"); VOICE readout reads status.voice (muted → "voice asleep · model not loaded"); drawer rebuild inside the 4s undo window now COMMITS the pending recording delete (was silently dropped → row resurrected).
- [x] **Import failure terminal frame** (a4ea4ba3, cross-island): ingest exception + rejected staleness re-index now emit a terminal import_progress frame (failed=1, failure_reason) — the drawer sat on "Preparing folder…" + Cancel forever because the ipc.error landed on the deck notice BEHIND the open drawer. already-running rejection stays silent on purpose (would stomp the live stream). New python pin + vitest shape.
- [x] **Screen-noun unification** (53381b9a): "screen proof" = the feature (badges/hovers/permission subs/TCC copy/djay tooltip; "screen grounding" + "screen-capture" + "watches djay's window" killed), "Screen Recording" = the OS pane tier (recovery copy, toast casing, System Settings naming). Wizard + session now hover the same word.
- [x] **Debrief mock fidelity** (c719673d): citation chips + timeline regions resolve LOCALLY in mock (canned receipts through the same tooltip+anchor path); chapter rail deeplink bridge extracted + wired in BOTH mounts (comment claimed it already was); data-mock CSS diet hides the dead demo <audio> + slims the rail (morning 217/217 fits, was 283/219). All four live-verified.
- [x] **Timeline recomposition** (2b8a9292, Kaan by-eye: "so ugly chaotic"): the pink wall dismantled — silk signal bed (runtime alpha, no per-bar glow), floating staggered rose region boxes → full-height quiet bands w/ one shared label baseline, morning thumb + recap ticks → silk. ONE resting rose per panel (timeline = the replay window). Re-captured: debrief-recomposed-2.png.

### NEXT QUEUE v6 (carried forward)
1. **Windows wizard parity** (audit complete, fixes pending — the big one: P1 every Windows first-run routes into a VB-CABLE kernel-driver install the product NEVER uses (backend is WASAPI loopback, "no virtual cable required"); P1 failure fallback shows BlackHole copy on Windows; P2 mic card always-granted stub + dead recovery; P2 SHA-256 verification claim vs placeholder skip; P3s version literal "0.6.0", missing progress states, unmounted smartscreen step, bare python3 latent). Full findings in wf_9fa1416e-7eb output.
2. **Settings remainder**: S4 drawer-rebuild-kills-progress (module-scope job state rehydration); S6 failed-delete '—' copy; S8 DOCS/SOURCE rows → 404 (internal repo — gate or repoint at launch).
3. **Carryovers**: v5→v6 rename — NEW BLOCKER FINDING: silk-65 etc are ALPHA composites, ink-80 etc are OPAQUE hexes; a mechanical rename changes rendering at ~1065 sites. Needs a token-design decision (alpha-preserving v6 names) before any sweep; NOT a sed job. Plus: NEXT UP (blocked), viber index.ts (other session), debrief de-boxing round 2 (page still grid-of-boxes, calmer now).
4. **Live-verify lane**: updater prompt, tray gemini-down icon, deck fault flow, recomposed debrief on the real window — packaged-app eyeball.
