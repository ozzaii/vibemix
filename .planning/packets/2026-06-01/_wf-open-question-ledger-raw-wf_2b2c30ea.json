{
  "summary": "Read-only: harvest unresolved vibemix questions; for each → decision LAND/HOLD/DROP/RESEARCH + why + evidence file:line + risk + exact Codex package. Schema-less prose. No product edits/stage/commit/app-launch.",
  "agentCount": 12,
  "logs": [
    "Deciding 11 open questions (schema-less)."
  ],
  "result": {
    "workflow": "open-question-decision-ledger",
    "question_count": 11,
    "blocks": [
      "I have everything needed. The evidence is conclusive: the guard is wired at two chokepoints on the single spoken/TTS path, tested at unit and integration level, tests green, and the packets confirm it RESOLVED the prior MEDIUM finding. The only remaining caveat is a precision/scope point (Turkish-prose-only, plus live-ear pass), not a wiring gap.\n\n### 1. English-only RUNTIME backstop\n- **Decision:** LAND / close-out\n- **Why:** Non-English Turkish prose is now blocked at runtime on the only spoken path, not just in the prompt: commit `b5916eb0` added `agent/language_guard.py` and wired it at two chokepoints in `llm_node` — a sticky mid-stream defer that stops yields to TTS, and a post-stream `suppression=\"non_english\"` that empties `spoken_text`/`buffered_chunks` and pushes a silence pad. The remaining caveat is precision/scope (Turkish-only, deliberately) plus a live-ear pass, not a wiring gap.\n- **Evidence:** `src/vibemix/agent/language_guard.py:73,106`; wired at `dj_cohost.py:62` (import), `:2757-2764` (mid-stream defer → `continue` before the yield-to-TTS block at `:2773-2802`), `:3006-3011` + `:3034-3050` (post-stream suppress, zeroes spoken text, cancels head if already yielded); tests `tests/agent/test_language_guard.py` + `test_dj_cohost.py::test_llm_node_suppresses_non_english_spoken_text_but_keeps_raw_artifact` (asserts `chunks == []`, `suppression=\"non_english\"`, raw artifact preserved) and `::..._preserves_grounded_english_response` — **7 passed** in repo .venv. Confirmed RESOLVED in `CLAUDE_LAST_DELTA_DRIFT.md:28,39,58` and `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:137,166` (`[WORKS_AND_PROVEN]`). Both the Gemini direct and OpenRouter brain paths flow through this same `llm_node` loop (`stream_or` yields genai-shaped chunks), so there is no second un-guarded spoken path.\n- **Risk:** Precision gap, not a correctness/honesty failure: the guard is intentionally a Turkish-DJ-chat detector (openers/prose-token/phrase/Turkish-char heuristics), NOT a general language detector — fluent French/Spanish/German output would slip through. It also won't suppress a single ambiguous ASCII word (by design, to protect English sentences naming Turkish tracks). Given the persona's leakage is Turkish, this matches the real-world failure mode, but the \"English-only at runtime\" claim is scoped to Turkish-prose leakage, not arbitrary non-English. No slop/honesty risk: when it fires it emits silence + an honest `non_english_suppressed` log, never a fabricated English fallback.\n- **Codex package:** none / close-out. Optional follow-up only if Kaan's live-ear pass surfaces non-Turkish leakage: \"broaden english-only guard beyond Turkish heuristics\" — files-in `src/vibemix/agent/language_guard.py` + `tests/agent/test_language_guard.py`, files-out none; gate = new non-Turkish-leak cases blocked while the existing 7 tests + Turkish-track-name allow-case stay green. Do NOT open this preemptively — the current scope is the documented product contract (\"Sven speaks English\", Turkish being the persona's actual leak language).",
      "I have all the evidence needed. The `_push_transcript(spoken_stripped[:140])` receives `spoken_stripped`, which is `strip_emote_tags(full_text)` — strips only emote/voice tags, leaving citation atoms intact. So the visible transcript preserves atoms, the recorder receipts (ai_text/ai_message/session artifact) preserve the full raw `response`, and only the TTS yield path strips citations. Wiring is verified at both ends with passing tests.\n\n### 2. Citation atoms reaching MOSS — stripped from TTS input, preserved on transcript/receipt\n- **Decision:** LAND / close-out\n- **Why:** Resolved at HEAD by `54ec38be`. `model_text_for_tts` (emote-tags + `EVIDENCE_CITATION_RE` strip) is wired into the single TTS yield helper `_prepare_tts_segment`, so every yielded segment is sanitized — while the visible transcript (`_push_transcript(spoken_stripped)`) and all recorder receipts (`ai_text`, `ai_message`, `session_response` artifact) are fed `strip_emote_tags(full_text)`, which leaves citation atoms intact. The split is exactly as specified, with a passing integration assertion.\n- **Evidence:** `src/vibemix/agent/tts_sanitizer.py:37-40` (sanitizer); `src/vibemix/agent/dj_cohost.py:2638` (the ONLY TTS-text gate, called at every yield site — 2778/2799/3102/3134/3203); transcript/receipt source `dj_cohost.py:2999-3000` + `3107-3117` (`spoken_stripped` = emote-only strip, atoms kept); integration test `tests/agent/test_dj_cohost.py:413-430` asserts TTS chunks `\"That low end is moving.\"` vs receipts `\"...[aud:rms@12.0].\"` — **passed**; `tests/agent/test_tts_sanitizer.py` 5/5 + streaming-pipe/linter 33/33 **passed**.\n- **Risk:** Low. The sanitizer is the single choke-point through `_prepare_tts_segment`, so no yield path can leak atoms; if a future yield site bypassed the helper it would re-leak, but no such site exists at HEAD. MOSS-side honesty intact: atoms removed from speech, kept as the visible grounding receipt — no fake fallback introduced.\n- **Codex package:** none / close-out. Wiring + tests complete; nothing to land. (Optional, non-blocking: a one-line grep-guard test asserting no `yield` of model text bypasses `model_text_for_tts` in `dj_cohost.py` would harden against future regressions, but not required.)",
      "I have everything I need. The deck identity resolver is fully mapped and tested. Let me synthesize the decision.\n\nThe picture is clear:\n- **Resolver exists and is deterministic** (`deck_poller.py` source ladder, `track_resolver.derive_audible_deck` for attribution).\n- **Source ladder**: ① Rekordbox-XML library match on the now-playing title (audible deck only) → ② Gemini-vision (GATED OFF, `vision_enabled=False` by default, no screen source wired) → ③ numpy key (deferred slot).\n- **The audible deck** resolves via MIDI controller fader/xfader weights + now-playing title cross-ref to the XML library. The **second/non-audible deck is suppressed to unknown by construction** — no independent identity source exists when vision is off.\n- The FLX4 hold proof showed exactly this: `A_active+B_silent`, `nowplaying_owner=com.apple.webkit.gpu` (not a DJ app), `resolution=no_single_attributable_deck`, vision disabled. The blockers are *missing independent identity sources*, not a broken resolver.\n\n### 3. Deck identity source — what resolves which deck is which\n- **Decision:** HOLD\n- **Why:** A deterministic resolver already ships and is test-pinned — `DeckPoller` runs a confidence-floored source ladder (Rekordbox-XML title-match → gated vision → numpy slot), and `derive_audible_deck` attributes the single now-playing title to a deck via MIDI fader/xfader weights. It correctly resolves the **audible** deck and suppresses the silent deck to honest `unknown` by construction. The FLX4 hold is not a resolver bug — it is the **absence of an independent identity source for the second deck** (vision is the only planned one and it is eval-gated OFF), plus a live rig where now-playing ownership is a non-DJ app (`com.apple.webkit.gpu`) and B audio is silent. Landing more \"resolver\" code can't conjure proof the hardware/sources don't emit; the gate is real hardware + an enabled identity source, which is HOLD-pending by the project's own anti-hallucination posture.\n- **Evidence:** `src/vibemix/state/deck_poller.py:315-421` (`_build_decks` ladder + cross-deck suppression); `src/vibemix/state/track_resolver.py:31-89` (`derive_audible_deck`, MIDI-weighted attribution, FLX4 play-LED desync handled); `deck_poller.py:68-70,407` (XML/VISION/NUMPY floors, `vision_enabled=False` default); `.planning/packets/2026-06-01/CODEX_HOLD-flx4-live-acceptance-current.md` (\"deck_state had no resolved deck row… nowplaying_owner=com.apple.webkit.gpu… screen_vision=disabled… A_active+B_silent\"); `tests/state/test_deck_poller.py:338,395-456` (suppression + \"never borrows audible title\" pins); `deck_vision.py:1-40` (the gated universal second-deck identity leg).\n- **Risk:** If wrong (i.e., if we LAND a new deterministic resolver to force `deck_state_pair_resolved=true`), we'd be inventing a second-deck identity from the audible deck's title or from configured-but-silent BlackHole channels — exactly the cross-deck hallucination the suppression rule (`second_deck_source=suppressed_requires_independent_source`) exists to prevent. That fabricates a citable `[track:]`/`[key:]` for a deck we cannot actually hear: a direct Invariant #3 (trust the audio) and #2 (citation grounding) violation — the headline slop class.\n- **Codex package:** HOLD — no resolver package. The honest unblock is a tiny **diagnostics/eval** package, not new identity logic: `deck_vision` real-screenshot accuracy eval to clear the `VISION_CONF_FLOOR` per app (in: `eval/deck_vision/run_eval.py` + committed screenshot fixtures + per-app floor table; out: untouched `deck_poller.py`/`dj_cohost.py` reaction path; gate: eval passes the documented floor → KAAN-ACTION sign-off flips `vision_enabled`). That is the only path that turns the second deck citable without inventing identity. Everything else on this question is `NEEDS_LIVE_PROOF_FIRST` (FLX4 rig with B audible + Rekordbox-owned now-playing), which is hardware-gated, not a code package.",
      "BlackHole probe tests pass (17/17). Now I have a complete picture. Let me make one final check: confirm the wizard's tone-test (the only audio \"verify\") was explicitly *demoted to informational* (step2 lines 186-194 said the tone test is fragile and Continue no longer requires it) — this is the honesty-relevant detail. I have enough evidence.\n\nSummary of findings:\n- **What EXISTS at HEAD:** BlackHole presence probe (`install/blackhole_probe.py`, retry-on-race, event emission, 17 tests green), wizard device enumeration + BlackHole banner with install CTA (`step2-output-device.ts:103-111`, `blackhole-step.spec.ts`), a 1kHz sine-tone playback verify with user-confirm (`wizard.py:263-332`), driver auto-fetch step, and a deck-pair `setup_hint`/`setup_block` detector in the proof CLI (`__main__.py:4708`, `5188`).\n- **What does NOT exist (the actual question's ask):** a \"your routing is WRONG\" honest detector. Specifically (a) no master-input-silence check (DJ app not feeding BlackHole → live input is silent but nothing tells the user), and (b) no loopback/feedback detector for the phantom-music problem (CLAUDE.md's documented #1 footgun: system output also feeding BlackHole → AI voice re-amplifies). The tone test was deliberately demoted to informational (`step2-output-device.ts:186-194`).\n- `docs/audio-routing.md:111-124` explicitly marks guided Recipe 2/3 routing as an unbuilt KAAN-ACTION (`§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE`).\n\n### 4. BlackHole / Rekordbox setup UX — in-app setup + honest \"routing is wrong\" detector\n- **Decision:** RESEARCH\n- **Why:** Setup *discovery* already ships and is green — BlackHole presence probe, install CTA banner, device picker, sine-tone verify, driver auto-fetch — so a basic in-app setup/verify flow is LAND-already. The genuinely missing, high-value piece is the **honest \"your routing is wrong\" detector** for the two real footguns (DJ app not feeding BlackHole = silent input; system/Multi-Output also feeding BlackHole = phantom-music loopback). Building it right needs a measured RMS/correlation threshold spike on real signal — that's read-only-unfinishable, so RESEARCH the detector, not a blind LAND.\n- **Evidence:** `src/vibemix/install/blackhole_probe.py:153` (probe, 17 tests green: `tests/install/test_blackhole_probe*.py`); `src/vibemix/runtime/wizard.py:263-332` (sine-tone verify) + `step2-output-device.ts:103-111` (install banner) — setup surface EXISTS; `step2-output-device.ts:186-194` (tone test demoted to informational — current verify is weak); `docs/audio-routing.md:111-124` (`§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` — guided routing is an explicit unbuilt KAAN-ACTION); routing-wrong/loopback master-input detector = **NOT-FOUND** (grep of `__main__.py`/`audio/` finds only deck-pair `setup_hint`/`setup_block` at `__main__.py:4708,5188`, never a master-input-silence or feedback check); CLAUDE.md documents the phantom-music loopback as the #1 real-world footgun.\n- **Risk:** A naive \"no input → routing wrong\" banner is itself slop if it fires during legitimate silence (track loaded, not playing; breakdown; idle) — false-positive flips contradict Invariant #5 (idle ≠ fault). The loopback detector is harder: distinguishing the AI's own re-amplified voice from real music needs a correlation/echo test, not a level threshold, or it will mislabel a correct rig. Ship a guessed threshold and you teach users to ignore the one banner that matters.\n- **Codex package:** **Spike: \"routing-wrong\" honest detector thresholds** — (in) read `audio/deck_capture.py:288` (`_DECK_ACTIVE_RMS` silent boundary), `audio/cues.py:17` (`SILENCE` −60dBFS), `audio/deck_signal.py`, `runtime/wizard.py` probe path; capture 3 real-rig RMS traces (correct route playing / DJ-app-not-routed / loopback-feedback with empty deck) to derive (a) a master-input-silence-while-deck-should-be-active threshold and (b) a self-voice-vs-music correlation test. (out) a RESEARCH.md with measured thresholds + the false-positive guard tied to ACTIVE state (Invariant #5) + a proposed `ipc.calibration.routing_check` shape. (gate) thresholds backed by captured traces, not guessed; detector abstains (no banner) on legitimate idle/silence. Do NOT ship a detector or banner this package — measurement first.",
      "I have everything I need. The test `test_llm_node_03_screen_jpeg_none_unconditional` and the `has_screen: false` across all fixtures confirm screen is unconditionally excluded from the live prompt at HEAD. The FLX4-hold packet (from commit 173399ae) records that even the MIDI/audio second-deck source proof is on HOLD pending Kaan acceptance — vision is even further behind that.\n\nHere is my decision.\n\n### 5. Screen vision vs Rekordbox live source — which is authoritative, are both wired, do they conflict, is one redundant?\n- **Decision:** DROP (screen vision as a live source) + LAND (Rekordbox-XML enrichment + audio as the authoritative live stack, already the shipped state) — i.e. keep the source ladder as-is, retire/quarantine the dead screen-vision leg.\n- **Why:** They are not competing live sources and do not conflict — they sit on different layers. The **authoritative live truth is the captured deck audio + DSP** (RMS/BPM/phase/MIX_MOVE), with **nowplaying-cli + Rekordbox-`collection.xml`** providing read-only *track-identity enrichment* (it opens **no** live DJ DB — `deck_poller.py:30`). **Screen vision is fully dead-wired at runtime**: the capture loop fills a buffer (`__main__.py:2425`), but the JPEG never enters the prompt (`screen_jpeg = None` hardcoded anti-hallucination gate, `dj_cohost.py:2192-2199`, test-pinned) **and** the gated `DeckVisionReader` identity leg is never injected into the live `DeckPoller` (`__main__.py:2434-2438` passes no `vision_reader`/`vision_enabled`, both default off, `deck_poller.py:138-148`). So nothing is redundant — one source is simply never read.\n- **Evidence:** `dj_cohost.py:2192-2199` + test `tests/agent/test_dj_cohost.py:468` (screen_jpeg=None unconditional, v4:1502 anti-hallucination invariant) · `deck_poller.py:30` (\"opens NO DJ-software DB\") + `deck_poller.py:138-148` (`vision_enabled=False` default, eval-gated KAAN-ACTION) · `__main__.py:2434-2438` (live DeckPoller constructed with no vision args) · capability inventory line 61 (\"Overlay highlight … next: live screen-cited highlight capture\") + line 324 (every live screen/vision producer \"UNBUILT\") · `fixtures/hype_trace_genre1.jsonl` all rows `has_screen:false`.\n- **Risk:** The honest/slop risk runs the *other* way — re-enabling screen as a live source would re-open the v4 hallucination class it was killed to close (the gate is load-bearing). The only cost of dropping it is the ~1Hz `screen_task` capture loop + ScreenCaptureKit permission prompt burning CPU/UX for a buffer no one reads, plus a misleading `screen vision is not currently resolving the independent second deck` blocker string (`__main__.py:5066`) implying a near-ready feature. If the second-deck identity problem is ever truly needed, screen is the *only* candidate independent source — but that's a future eval, not a live source today.\n- **Codex package:** **\"Quarantine the dead screen-vision live leg\"** — files-in: `src/vibemix/__main__.py` (stop spawning `screen_task` / `screen_macos.run_capture_loop` while `screen_jpeg` is hardcoded `None`; gate it behind an explicit off-by-default `VIBEMIX_DECK_VISION` flag so the SCKit permission prompt + 1Hz capture don't fire for a buffer nothing reads), `tests/agent/test_dj_cohost.py` (keep the `screen_jpeg=None` pin), a new `tests/runtime/` assertion that no screen capture task runs unless the flag is set. files-out: do NOT touch `dj_cohost.py:2199` (the `None` gate is the anti-hallucination invariant — leave it), do NOT delete `_screen_macos.py`/`deck_vision.py` (preserve as the gated future second-deck path behind `eval/deck_vision/run_eval.py`). gate: `uv run pytest -q tests/agent/test_dj_cohost.py tests/runtime/ tests/state/` green + the `vibemix-grounding-review` skill confirms invariant #3 intact. (If a lighter touch is preferred: close-out as documentation-only, marking screen-vision `[DORMANT_BY_DESIGN]` in the capability inventory and fixing the misleading `__main__.py:5066` blocker copy.)",
      "All evidence is in. The watcher is complete: real `watchfiles` event-driven implementation, declared dependency, bundled in both PyInstaller specs, wired and armed in `__main__.py`, with passing tests including a change-reaction test. The b8d19ba2 commit was a maintainability refactor (extracting the watcher loop into its own module) on top of already-shipped functionality. This is a close-out, not a finish-the-watcher task.\n\n### 6. Viber filesystem watcher (event-driven library freshness)\n- **Decision:** LAND (close-out — already shipped at HEAD)\n- **Why:** A real `watchfiles.awatch` event-driven watcher already exists, is a declared/bundled dependency, is wired + armed in the live session, and is test-covered; `b8d19ba2` was a pure maintainability refactor (extract loop into `watcher.py`), not a stub or pending work. Nothing left to finish.\n- **Evidence:** `src/vibemix/library/watcher.py:44-104` (real `from watchfiles import awatch` + `_one_change` loop with `watch_filter`/`debounce=500`, fail-soft to bounded poll on `poll_seconds=10.0`); wired/armed at `src/vibemix/__main__.py:2112-2122` (\"library freshness watcher armed\"); dep declared `pyproject.toml:101` (`watchfiles>=1.1.1`) and bundled in both specs (`vibemix-core.macos.spec:150`, `vibemix-core.windows.spec:161`); tests pass (`tests/library/test_staleness.py` — 24 passed incl. `test_watch_library_freshness_rechecks_on_file_change`); proof note `.planning/handoffs/2026-05-31-package-checklist.md` (ws_observe captured `reason=source_newer_than_cache` nudge).\n- **Risk:** Low. The watcher only emits the lightweight `ipc.library.staleness_nudge` (closed schema, no payload widening) — it does NOT auto-re-ingest, so a mid-session re-export still requires the user to re-import; that is by-design (explicitly \"deliberately smaller than a full auto-ingest daemon\" per the checklist), not a slop/honesty gap. If `watchfiles` were ever pruned the loop silently degrades to the 10s poll (honest, no fake behavior). No risk of an unwired/dead feature.\n- **Codex package:** none / close-out. (Auto-re-ingest-on-change would be a *new* scoped feature, not finishing this one — out of scope unless Kaan explicitly wants the daemon; current event-driven freshness nudge is complete.)",
      "Cue-export is fully wired end-to-end and tested: `invoke(\"library_cue_folder\")` at api.ts:2546, called from `runCueExport` at index.ts:1844, rendered via `renderCueExport` at index.ts:581, with frontend tests in 3 files. This fully resolves the CORRECTION's #1 priority gap (\"No cue-export GUI\"). I have everything I need.\n\nSummary of findings:\n- **15 MCP tools** exposed in `mcp_server.py`, all delegating to `LibraryToolset` handlers (all 17 toolset handlers exist; 2 are internal helpers).\n- **All tools tested** at the toolset boundary (90 targeted tests pass; `discover_pool`/`web_search`/`export_set`/`export_smart_cues`/etc. exercised; `quote_moment` tested at module level; `request_clarification` heavily tested incl. MCP-level).\n- **GUI surface (Tauri invoke path):** search/similar/curate/build_set/chat/stats/models/embed_folder + **cue_folder** are all registered (main.rs:105-114), bridged (library_cmds.rs), invoked (api.ts), and rendered (index.ts). The CORRECTION proved this path WIRED.\n- **The CORRECTION doc is itself now stale** at HEAD: its #1 gap (\"No cue-export GUI\") and its #2 gap (\"No library watcher\") are both RESOLVED.\n- **No claimed-but-absent tools.** **CLI-only (not in GUI crate):** the granular Codex/MCP set-prep tools (transition_slate, compile_musical_context, smart_hot_cues, export_smart_cues, get_track_*, sequence_set, web_search, fetch_url, quote_moment, retrieve_dj_knowledge) — these are agent-internal, reached only through `library chat/curate/build-set` which ARE GUI-surfaced. **Dead vestigial:** the 5 `ipc.library.*` ws schema types (zero UI call-sites).\n\n### 7. Viber tool surfaces — inventory + surface/drop decisions\n- **Decision:** LAND (with two narrow close-outs: a doc-stale correction and a 5-line schema cleanup)\n- **Why:** All 15 MCP tools are real, wired to `LibraryToolset` handlers, and tested at the grounding boundary; the entire GUI Crate path (incl. cue-export, the CORRECTION's old #1 gap) is wired end-to-end and the freshness watcher (old #2 gap) is armed — so the Viber surface is in good shape and the `viber-capability-CORRECTION.md` packet is itself now partly stale. The only genuine residue is 5 dead `ipc.library.*` ws schema types with zero UI call-sites.\n- **Evidence:** `src/vibemix/library/mcp_server.py:163-417` (15 `@mcp.tool()` defs) → all 17 `LibraryToolset` handlers exist (`toolset.py:226-1185`); GUI: `tauri/src-tauri/src/main.rs:105-114` (10 cmds incl. `library_cue_folder:109`), `library_cmds.rs:820` (cue_folder bridge), `api.ts:2536-2546` (`invoke(\"library_cue_folder\")`), `index.ts:1844 runCueExport`/`581 renderCueExport`; watcher LANDED: `src/vibemix/library/watcher.py` wired at `__main__.py:2111-2122` (\"library freshness watcher armed\"); `export_smart_cues` grounding gate intact (`toolset.py:925-944` — proposal must be issued this run AND `track_id in self.seen`); tests: `pytest tests/library/test_setprep_tools.py … test_mcp_server_clarification.py` → **90 passed**; CORRECTION-confirmed dead path: 5 `ipc.library.*` schema types, `rg ipc.library. tauri/ui/src/library` → empty (`viber-capability-CORRECTION.md:43-47`).\n- **Risk:** Low. The \"WIRED but live-correctness UNVERIFIED\" caveat (CORRECTION:34-36) still holds — a `drive-vibemix` pass on `library chat/curate/build-set/cue` would confirm grounded data actually returns, but that's read-only-out-of-scope for this researcher and not a blocker. Honesty risk if anyone acts on the stale CORRECTION packet and rebuilds the cue-GUI or watcher that already exist (redundant second path).\n- **Codex package:** **\"Delete dead ipc.library.* ws types + refresh stale Viber correction packet.\"** Files-out: remove the 5 `ipc.library.*` entries from `tauri/ui/src/ipc/messages.schema.json` (then `npm run codegen:ipc`); annotate `.planning/packets/2026-06-01/viber-capability-CORRECTION.md` priorities #1 (cue-GUI) and #2 (watcher) as RESOLVED-at-HEAD (d6c14ba7). Gate: `cd tauri/ui && npm run build && npm test` green + `rg \"ipc.library.\" tauri/ui/src` empty. No new runtime build needed — the Viber surface itself is LAND/close-out.",
      "This is the decisive finding. The Round-2 loop idea (#1, the \"R-SLOP-immune\" headline) depends on having a live beatgrid to compute beat-size. But: (a) the FLX4 profile only emits manual `loop_in`/`loop_out` notes — NOT beatloop-size pads — so frames→beat-size requires a beatgrid; and (b) `from_anlz` exists but there's NO wiring of the ANLZ beatgrid into live `MusicState`/`refresh.py`. So loop geometry's \"deterministic, zero inference\" claim has a hidden prerequisite: a live grid that isn't wired. That tempers idea #1's feasibility.\n\nThe EQ band-delta (#2) and LUFS (#4) ideas need only `band_features.py` (present) and a new numpy module — no grid dependency, no hardware. They attack the exact named live release blocker (R-SLOP: ungrounded EQ/fader effect claims). I have enough evidence. Let me produce the decision.\n\nBased on all evidence, here is my decision block.\n\n### 8. Mixxx goldmine ideas worth adopting (shortest honest-ship + grounding/slop gap)\n\n- **Decision:** RESEARCH (overall: ADOPT idea A, RESEARCH idea B, HOLD idea C)\n- **Why:** The 1-3 highest-value, shortest-honest-ship picks are all from **Round 2** because they attack the named live release blocker (R-SLOP — the co-host claiming an EQ/fader move had an effect it can't ground) and are passive-listener-safe with zero new deps. The Round-1 headline (Practice-Deck Loop / H1) is *not* shortest-ship: it's already on the board as **H2 HOLD** needing live owned-deck hardware proof, and its sibling \"loop geometry\" idea has a hidden unwired prerequisite (live beatgrid). Ranked picks: **(A) Short-term LUFS receipt** — cleanest standalone honesty unlock; **(B) EQ band-delta \"Spectral Receipt\"** — biggest R-SLOP kill but needs the inseparable settling-lag window; **(C) Loop-geometry rule** — strongest-in-theory but grid-blocked today.\n- **Evidence:** Round-2 map `wf_d9ddbeed-f5f__...round-2.md:36-49,72-91` (5 grounding axes, passive-listener reframe); guard exists but predictors absent — `deck_context.py:2186 live_claim_policy`, `:2308 apply_live_claim_guard`, `:2093 dsp_delta_not_causal_proof`; `state/loop_geometry.py`/`intel/eq_move_model.py`/`audio/lufs.py` all **NOT-FOUND** (verified absent); `band_features.py:30 band_energy_ratios` present (substrate ready); loop idea grid-blocked — FLX4 profile emits only `loop_in/out` notes 16/17 (`pioneer_ddj_flx4.json:33-36`), and `from_anlz` exists (`grid.py:39`, landed `d70de2ba`) but is **NOT wired into runtime `refresh.py`/`MusicState`** (NOT-FOUND); Round-1 H1 is board-HOLD H2 (`NEXT-LAND-BOARD-after-c88d5f56.md` H2: `NEEDS_LIVE_PROOF_FIRST`); GPL ledger clean — every item learn-from-spec, RBJ-cookbook/BS.1770 are the real sources not Mixxx (`round-2.md:142-153`).\n- **Risk:** **GPL vendoring** is the live trap — do NOT port Mixxx's Bessel4 IIR coefficients or paste any C++; source biquad math from the public RBJ cookbook and K-weighting from ITU-R BS.1770 (Mixxx itself wraps MIT libebur128, so even same-shape-as-impl is avoidable). **Slop/honesty risk:** any of these touches what licenses a spoken causal claim → must clear `vibemix-grounding-review` (Invariants #2/#3); the inverse false-negative is real — a naive single-frame EQ compare REFUTES true kills (the settling-lag window is mandatory, ships WITH #2 or not at all). Causation humility must hold: predicted∩measured agreement upgrades the gate, it does not license a bare \"your kill removed the lows.\"\n- **Codex package:** Three narrow packages, build LUFS first (no settling-window coupling, cleanest proof):\n  1. **`audio/lufs.py` short-term LUFS receipt** — files-in: NEW `src/vibemix/audio/lufs.py` (K-weight 2-biquad cascade + 3s sliding mean-square, ungated, from BS.1770), `tests/audio/test_lufs.py`; out: read beside `Levels.snapshot()` in state-refresh (single-writer), attach `master_lufs_delta` atom into the guard's audio-delta path. Gate: −3.01 LUFS conformance tone passes @44.1k AND 48k + `vibemix-grounding-review` PASS (grounds +3 LU window, strips flat 0 LU). No new dep, no spec edit.\n  2. **`intel/eq_move_model.py` Spectral Receipt + settling window** (RESEARCH→package only after #1) — NEW `intel/eq_move_model.py` (`predicted_band_gains(move, fs)` via RBJ `|H|²` over `_BANDS`), call site beside `_has_unsupported_mixer_low_kill_claim:2580`, consumes `band_energy_ratios` per-lane. Gate: two-frame bass-killed fixture GROUNDS, no-bass REFUTES, settling-window included, grounding-review PASS. Flag: GPL — RBJ cookbook is the source, never Bessel4 coeffs.\n  3. **Loop-geometry — close-out as RESEARCH/spike, not a package yet** — blocked: needs the ANLZ `from_anlz` grid wired into live `MusicState` first (separate prerequisite), and route loop labels through `_record_event` not the 400ms-dedup `_record_move` (`midi/state.py:230`). Do not draft until the runtime-grid wiring spike lands.",
      "I have everything I need. Let me synthesize the decision.\n\n**Summary of findings:**\n\nThe infrastructure is unusually complete for an orphaned feature:\n- **Grader** (`grade_beatmatch`, `grade_to_event_extra`) — done, clean-room Mixxx math, tested.\n- **Consumer** (`skill_recognizer` `BEATMATCH_GRADED` branch) — done, credits only LOCKED+cited grades.\n- **Owned-deck audio engine** (`MiniDeck`/`DeckState`) — renders blocks, owns playheads; only smoke scripts drive it, no production audio OutputStream.\n- **Beat-phase oracle** (`BeatGrid`) — just gained `from_anlz` (commit `d70de2ba`, today) to build a real grid from Rekordbox metadata.\n- **Control surface** — `ControllerState.deck_snapshot()` carries `tempo`/`jog`/`xfader`, the live FLX4 inputs a producer needs to drive `rate_b`/`xfader`.\n\nWhat's missing is purely the **live wiring**: a practice loop that (1) loads two tracks into a MiniDeck, (2) drives playback through a real audio OutputStream, (3) maps FLX4 tempo/jog/xfader → `rate_b`/`xfader`, (4) calls `grade_beatmatch` on live `DeckState`, (5) emits the cited `BEATMATCH_GRADED` event. Three reality-pins (`tests/repo/test_live_reality_pins.py`) actively guard the orphan and will flip red the moment a producer is wired — by design, the signal to retire the pins.\n\nThe board (`NEXT-LAND-BOARD #H2`) already ranks this HOLD with gate `NEEDS_LIVE_PROOF_FIRST`. Crucially, this needs real audio output + live FLX4 hardware + a Mastered vocal — none verifiable read-only, and the FLX4 source-proof is itself on hold (`173399ae`).\n\n### 9. BEATMATCH_GRADED production producer (owned-deck real-time grading)\n- **Decision:** HOLD\n- **Why:** Every offline piece exists and is tested (grader, consumer, `MiniDeck` engine, `BeatGrid.from_anlz`, controller tempo/jog/xfader snapshot), but the missing producer is *exactly* the part that cannot be built or proven read-only: a live owned-deck practice loop needs a real audio `OutputStream`, live FLX4 hardware mapped to `rate_b`/`xfader`, and a Mastered vocal — and \"Mastered\" is the headline anti-slop claim, so a wrong/late grade is catastrophic false-expertise (Invariant #3). This is wiring + hardware proof, not research; the design is already settled.\n- **Evidence:** `src/vibemix/learn/beatmatch_judge.py:83,145` (grader done) · `skill_recognizer.py:154-172` (consumer wired, LOCKED+cited only) · `tests/repo/test_live_reality_pins.py` 20-pass confirms producer orphaned (`_emitter_call_sites()==[]`) · `MiniDeck.render_block` (`miniplayer.py:130`) has only smoke-script callers, no production `OutputStream` · `BeatGrid.from_anlz` landed `d70de2ba` (today) · board `NEXT-LAND-BOARD-after-c88d5f56.md#H2` = HOLD `NEEDS_LIVE_PROOF_FIRST`; FLX4 source-proof itself on hold (`173399ae`).\n- **Risk:** If wired and shipped without on-rig proof, a phantom/late grade credits \"beatmatching Mastered\" the app never actually observed — the single worst false-expertise slop, breaking Invariant #3 and the v11 \"Earned\" moat. Reverse risk (leaving HOLD) is zero: the reality-pins keep the masked-uncreditable boundary honest indefinitely.\n- **Codex package:** none / HOLD — do NOT package an emitter now. The only read-safe adjacent move is the *plumbing spec, not the live emitter*: define a `learn/beatmatch_practice.py` skeleton (load 2 tracks → `MiniDeck` → `DeckState`) behind an explicit feature flag that stops short of any `grade_beatmatch(...)` call (so the three reality-pins stay green), files-in: `src/vibemix/learn/beatmatch_practice.py` + unit tests; files-out: NO edit to `skill_recognizer.py`, `beatmatch_judge.py`, or the pins. **Gate to actually emit + unmask:** real audio `OutputStream` playback + live FLX4 → `rate_b`/`xfader` mapping + on-rig proof of a genuine LOCKED grade + `drive-vibemix`/`vibemix-grounding-review` PASS on the Mastered vocal; only then delete the reality-pins and clear `_HONEST_UNCREDITABLE_V11`. Not landable this read-only pass.",
      "I now have everything I need. The picture is fully resolved. Key facts:\n\n1. **The DROP-call lane is DIRTY-ONLY** across `coach.py`, `event_detector.py`, `drop_predict.py` (+ test files) — confirmed by `git status`. The `__main__.py` wire-up shares the highest-contention file. `refresh.py` (writer) and `drop_reaction.py` (line bank) are already committed at HEAD.\n2. **The flag `VIBEMIX_DROP_CALL` is dormant/opt-in** (env gate, default OFF) — the dangerous \"on-by-default setdefault\" aggravator is GONE per the land board.\n3. **Tests are green** (21 passed) but it is `WIRED_BUT_UNPROVEN` — no captured live artifact of the flag flipped against a real drop.\n4. **The signal `predicted_drop_in_sec` requires a `section_source`** (cue/ANLZ sections) + deck snapshot with track_id to be non-None; without ANLZ/cue data ingested the countdown never populates, so a live proof requires that data path active.\n5. **The slop risk is catastrophic**: firing on a phantom predicted drop is exactly the \"real DJ friend, no AI slop\" release blocker, and the build-hygiene risk is that a whole-file `git add` ships ungated DROP speech into a signed binary.\n\nThis is a clean HOLD — the value is real but the gate (grounding-review + on-beat live proof + FP rate) is genuinely unmet and the feature is not finished/proven. Here is my decision block.\n\n### 10. DROP-call live hype line — keep HOLD, drop, or land?\n- **Decision:** HOLD\n- **Why:** The lane is correct in shape (deterministic fixed-text `say()`, no LLM → no tag/English leak, opt-in flag, green unit tests) but is `WIRED_BUT_UNPROVEN` — zero captured proof the call lands ON the beat against a *real* drop, and a fire on a phantom predicted-drop is exactly the catastrophic \"AI slop\" release blocker. The feature has real value, so DROP is wrong; the gate is genuinely unmet, so LAND is wrong.\n- **Evidence:** `4ac30b5d` records the hold; dirty-only confirmed (`git status`: `coach.py`/`event_detector.py`/`drop_predict.py` + `test_event_detector_drop.py` untracked, while writer `refresh.py:957-982` and bank `drop_reaction.py:68` are already at HEAD); `event_detector.py:285-290` gated by `VIBEMIX_DROP_CALL` default-OFF (`_drop_call_enabled_from_env`); `coach.py:704-718` speak path; tests green (ran `tests/state/test_event_detector_drop.py tests/state/test_drop_predict.py` → 21 passed); inventory classifies it `[WIRED_BUT_UNPROVEN]` (`CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:145-146`); land board H1 = HOLD with this exact gate (`NEXT-LAND-BOARD-after-c88d5f56.md:36-40,74`).\n- **Exact gate to clear before promotion (all three, ordered):**\n  1. **grounding-review PASS** — run the `vibemix-grounding-review` skill over the `coach.py` DROP hunk + `event_detector.py` gate; it must confirm the spoken line is reaction-bank-only (never LLM), cannot fire while idle, and respects cooldown. (DROP has no per-type gap → falls back to `EVENT_GLOBAL_MIN_GAP`; verify that floor is acceptable, or add an explicit DROP entry to `MIN_EVENT_GAP_PER_TYPE`.)\n  2. **on-beat live proof** — a captured `drive-vibemix` artifact with `VIBEMIX_DROP_CALL=1` AND a populated `section_source` (cue/ANLZ ingested, since `predicted_drop_in_sec` stays `None` without it per `refresh.py:961-980`), showing the spoken line arrives inside the 2.0s arm window (`DROP_ARM_WINDOW_S`) and audibly lands on the drop, not seconds late.\n  3. **false-positive rate** — over that live run, count predicted-drop crossings vs actual drops; the spoken call must NOT fire on a phantom/early prediction. A single audible phantom call fails the gate.\n- **Risk:** If promoted before the gate: a phantom on-beat hype line = the #1 anti-slop release blocker. Build-hygiene risk is acute — a whole-file `git add` on `coach.py` or `__main__.py` (~13 lanes) sweeps the ungated DROP speech into a signed binary (`NEXT-LAND-BOARD-after-c88d5f56.md:31,53-54`). If DROPped instead: a high-value, already-built on-beat hype moment is discarded for a gate that is achievable once cue/ANLZ data is live.\n- **Codex package:** none / keep-on-hold close-out. Do NOT cut a LAND package. The only safe Codex action now is the build-hygiene guard already chartered (`CODEX_READY-no-spoken-fallbacks-live-guard.md` / hunk-stage discipline): ensure the dirty DROP block stays OUT of any release build from a clean `c88d5f56` tree. Promotion is a Kaan-owned live ear-pass (gate above), not a code package — the engineering is done, the proof is not.",
      "Confirmed (the `n`/`ln` are ripgrep highlight artifacts masking literal text, but the substance is clear): no production `BEATMATCH_GRADED` emitter exists, `practice_loop.py`/`practice_runtime.py` are absent, and `beatmatch_judge` has zero non-test callers in `src/`. The honesty boundary (`live_creditable=False` + `_HONEST_UNCREDITABLE_V11`) correctly masks this.\n\nI now have complete evidence. Here is my decision.\n\n### 11. Learn module reality — v11.0 Earned: does lesson→Competent / cited-live-demo→Mastered actually work end-to-end, or are stages credited without real producers?\n- **Decision:** LAND (close-out — already resolved at HEAD) with ONE narrow honesty-audit package; the over-scoped producer is correctly HELD by an honest mask, not DROPPED.\n- **Why:** The skill-tree spine is wired end-to-end and honest: lesson-fill + recital AND-gate → Competent, and `_credit_live_skill_demo` (live call-site `coach.py:624`, fired every event) → `recognize` → `record_live_demo` with the citation gate as sole arbiter → Mastered, with an Earned-Wall refresh + fire-once vocal. 5 of 6 skills have **real production event producers** (LAYER_ARRIVAL/PHASE/PHRASE_BOUNDARY/MIX_MOVE all fire in `event_detector.py`; harmonic_mixing via the live Judge `_run_live_judge`→`transition_judged` at `coach.py:812`); only **beatmatching** has no producer, and it is honestly capped at Competent (`live_creditable=False`) — not credited without a producer. The one CI-red fixture the inventory flagged was already fixed by HEAD commit `d6c14ba7`.\n- **Evidence:** `skill_tree.py:291-356` (pure compute + COMP-02 AND-gate), `:437-504` (`record_live_demo` MAST-01 floor), `skill_recognizer.py:200-289` (citation gate sole arbiter) + `:105,150` (honesty mask); live wiring `coach.py:124-217,276-402,624`; producers real `event_detector.py:317-378`; **NOT-FOUND**: `BEATMATCH_GRADED` emitter / `learn/practice_loop.py` / any non-test `grade_beatmatch` caller (grep clean); `d6c14ba7` re-pointed the stale `beatmatching`→`harmonic_mixing` fixture; ran `pytest tests/learn/{skill_tree,skill_recognizer,creditability_drift,progress_snapshot_skill_wall}` → **35 passed**.\n- **Risk:** The whole live path is unit/synthetic-proven only — **never captured on a real rig** (`record_live_demo`/recognizer = WORKS_SOURCE_ONLY, Earned-Wall refresh = WIRED_BUT_UNPROVEN). Slop risk if anyone flips `beatmatching.live_creditable=True` before the H1 producer ships — that is the one place stages could be credited without a real producer; `test_creditability_drift.py` guards it. The Mastered unlock vocal tone is a parked ear-pass HOLD (`§EARNED-MASTERED-VOCAL-EAR`).\n- **Codex package:** **\"Earned-Wall live-credit capture + refresh proof\"** — files-in: `runtime/coach.py` (`_credit_live_skill_demo`/`_emit_earned_wall_refresh`/`_run_live_judge`), `learn/skill_recognizer.py`, `tests/runtime/`; files-out: NONE under `learn/skill_tree.py` or the honesty mask (do not touch `_HONEST_UNCREDITABLE_V11` / `live_creditable=False`). Gate: a `drive-vibemix` run that fires one real LAYER_ARRIVAL/MIX_MOVE or a two-deck `transition_judged`, asserts the `EvidenceRegistry` citation resolved, that `live_proof_count` advanced for a creditable skill, and that an `ipc.learn.progress_state` envelope repainted the wall — promoting the cluster from SOURCE_ONLY/WIRED to PROVEN in one capture. (The beatmatching H1 Practice-Deck producer is a separate, larger milestone — keep HELD behind the honest mask, do **not** fold it into this package.)"
    ],
    "docMarkdown": "### Decision spread\n\n11 questions: 6 LAND/close-out, 2 HOLD, 2 RESEARCH, 1 DROP (Q5 is a paired DROP-the-dead-leg + LAND-the-shipped-stack).\n\n### Decision table\n\n| # | Question | Decision | Codex package |\n|---|----------|----------|---------------|\n| 1 | English-only RUNTIME backstop | LAND / close-out | none (optional broaden-guard, do not pre-open) |\n| 2 | Citation atoms → MOSS (strip TTS, keep transcript) | LAND / close-out | none (optional grep-guard) |\n| 6 | Viber filesystem watcher | LAND / close-out | none |\n| 7 | Viber tool surfaces inventory | LAND (2 close-outs) | delete dead `ipc.library.*` types + refresh stale CORRECTION packet |\n| 11 | Learn module reality (Earned end-to-end) | LAND / close-out | Earned-Wall live-credit capture + refresh proof |\n| 5 | Screen vision vs Rekordbox live source | DROP screen leg + LAND shipped stack | quarantine dead screen-vision live leg (or doc-only) |\n| 3 | Deck identity source | HOLD | deck_vision real-screenshot accuracy eval (diagnostics only) |\n| 9 | BEATMATCH_GRADED production producer | HOLD | none (read-safe skeleton only, no emitter) |\n| 10 | DROP-call live hype line | HOLD | none (build-hygiene guard only) |\n| 4 | BlackHole/Rekordbox setup UX + routing-wrong detector | RESEARCH | spike: routing-wrong detector thresholds |\n| 8 | Mixxx goldmine ideas | RESEARCH (adopt A / research B / hold C) | `audio/lufs.py` LUFS receipt → `intel/eq_move_model.py` spectral receipt → loop-geometry spike |\n\n---\n\n### 1. English-only RUNTIME backstop\n- **Decision:** LAND / close-out\n- **Why:** Non-English Turkish prose is now blocked at runtime on the only spoken path, not just in the prompt: commit `b5916eb0` added `agent/language_guard.py` and wired it at two chokepoints in `llm_node` — a sticky mid-stream defer that stops yields to TTS, and a post-stream `suppression=\"non_english\"` that empties `spoken_text`/`buffered_chunks` and pushes a silence pad. The remaining caveat is precision/scope (Turkish-only, deliberately) plus a live-ear pass, not a wiring gap.\n- **Evidence:** `src/vibemix/agent/language_guard.py:73,106`; wired at `dj_cohost.py:62` (import), `:2757-2764` (mid-stream defer → `continue` before the yield-to-TTS block at `:2773-2802`), `:3006-3011` + `:3034-3050` (post-stream suppress, zeroes spoken text, cancels head if already yielded); tests `tests/agent/test_language_guard.py` + `test_dj_cohost.py::test_llm_node_suppresses_non_english_spoken_text_but_keeps_raw_artifact` (asserts `chunks == []`, `suppression=\"non_english\"`, raw artifact preserved) and `::..._preserves_grounded_english_response` — **7 passed** in repo .venv. Confirmed RESOLVED in `CLAUDE_LAST_DELTA_DRIFT.md:28,39,58` and `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:137,166` (`[WORKS_AND_PROVEN]`). Both the Gemini direct and OpenRouter brain paths flow through this same `llm_node` loop (`stream_or` yields genai-shaped chunks), so there is no second un-guarded spoken path.\n- **Risk:** Precision gap, not a correctness/honesty failure: the guard is intentionally a Turkish-DJ-chat detector (openers/prose-token/phrase/Turkish-char heuristics), NOT a general language detector — fluent French/Spanish/German output would slip through. It also won't suppress a single ambiguous ASCII word (by design, to protect English sentences naming Turkish tracks). Given the persona's leakage is Turkish, this matches the real-world failure mode, but the \"English-only at runtime\" claim is scoped to Turkish-prose leakage, not arbitrary non-English. No slop/honesty risk: when it fires it emits silence + an honest `non_english_suppressed` log, never a fabricated English fallback.\n- **Codex package:** none / close-out. Optional follow-up only if Kaan's live-ear pass surfaces non-Turkish leakage: \"broaden english-only guard beyond Turkish heuristics\" — files-in `src/vibemix/agent/language_guard.py` + `tests/agent/test_language_guard.py`, files-out none; gate = new non-Turkish-leak cases blocked while the existing 7 tests + Turkish-track-name allow-case stay green. Do NOT open this preemptively — the current scope is the documented product contract (\"Sven speaks English\", Turkish being the persona's actual leak language).\n\n### 2. Citation atoms reaching MOSS — stripped from TTS input, preserved on transcript/receipt\n- **Decision:** LAND / close-out\n- **Why:** Resolved at HEAD by `54ec38be`. `model_text_for_tts` (emote-tags + `EVIDENCE_CITATION_RE` strip) is wired into the single TTS yield helper `_prepare_tts_segment`, so every yielded segment is sanitized — while the visible transcript (`_push_transcript(spoken_stripped)`) and all recorder receipts (`ai_text`, `ai_message`, `session_response` artifact) are fed `strip_emote_tags(full_text)`, which leaves citation atoms intact. The split is exactly as specified, with a passing integration assertion.\n- **Evidence:** `src/vibemix/agent/tts_sanitizer.py:37-40` (sanitizer); `src/vibemix/agent/dj_cohost.py:2638` (the ONLY TTS-text gate, called at every yield site — 2778/2799/3102/3134/3203); transcript/receipt source `dj_cohost.py:2999-3000` + `3107-3117` (`spoken_stripped` = emote-only strip, atoms kept); integration test `tests/agent/test_dj_cohost.py:413-430` asserts TTS chunks `\"That low end is moving.\"` vs receipts `\"...[aud:rms@12.0].\"` — **passed**; `tests/agent/test_tts_sanitizer.py` 5/5 + streaming-pipe/linter 33/33 **passed**.\n- **Risk:** Low. The sanitizer is the single choke-point through `_prepare_tts_segment`, so no yield path can leak atoms; if a future yield site bypassed the helper it would re-leak, but no such site exists at HEAD. MOSS-side honesty intact: atoms removed from speech, kept as the visible grounding receipt — no fake fallback introduced.\n- **Codex package:** none / close-out. Wiring + tests complete; nothing to land. (Optional, non-blocking: a one-line grep-guard test asserting no `yield` of model text bypasses `model_text_for_tts` in `dj_cohost.py` would harden against future regressions, but not required.)\n\n### 6. Viber filesystem watcher (event-driven library freshness)\n- **Decision:** LAND (close-out — already shipped at HEAD)\n- **Why:** A real `watchfiles.awatch` event-driven watcher already exists, is a declared/bundled dependency, is wired + armed in the live session, and is test-covered; `b8d19ba2` was a pure maintainability refactor (extract loop into `watcher.py`), not a stub or pending work. Nothing left to finish.\n- **Evidence:** `src/vibemix/library/watcher.py:44-104` (real `from watchfiles import awatch` + `_one_change` loop with `watch_filter`/`debounce=500`, fail-soft to bounded poll on `poll_seconds=10.0`); wired/armed at `src/vibemix/__main__.py:2112-2122` (\"library freshness watcher armed\"); dep declared `pyproject.toml:101` (`watchfiles>=1.1.1`) and bundled in both specs (`vibemix-core.macos.spec:150`, `vibemix-core.windows.spec:161`); tests pass (`tests/library/test_staleness.py` — 24 passed incl. `test_watch_library_freshness_rechecks_on_file_change`); proof note `.planning/handoffs/2026-05-31-package-checklist.md` (ws_observe captured `reason=source_newer_than_cache` nudge).\n- **Risk:** Low. The watcher only emits the lightweight `ipc.library.staleness_nudge` (closed schema, no payload widening) — it does NOT auto-re-ingest, so a mid-session re-export still requires the user to re-import; that is by-design (explicitly \"deliberately smaller than a full auto-ingest daemon\" per the checklist), not a slop/honesty gap. If `watchfiles` were ever pruned the loop silently degrades to the 10s poll (honest, no fake behavior). No risk of an unwired/dead feature.\n- **Codex package:** none / close-out. (Auto-re-ingest-on-change would be a *new* scoped feature, not finishing this one — out of scope unless Kaan explicitly wants the daemon; current event-driven freshness nudge is complete.)\n\n### 7. Viber tool surfaces — inventory + surface/drop decisions\n- **Decision:** LAND (with two narrow close-outs: a doc-stale correction and a 5-line schema cleanup)\n- **Why:** All 15 MCP tools are real, wired to `LibraryToolset` handlers, and tested at the grounding boundary; the entire GUI Crate path (incl. cue-export, the CORRECTION's old #1 gap) is wired end-to-end and the freshness watcher (old #2 gap) is armed — so the Viber surface is in good shape and the `viber-capability-CORRECTION.md` packet is itself now partly stale. The only genuine residue is 5 dead `ipc.library.*` ws schema types with zero UI call-sites.\n- **Evidence:** `src/vibemix/library/mcp_server.py:163-417` (15 `@mcp.tool()` defs) → all 17 `LibraryToolset` handlers exist (`toolset.py:226-1185`); GUI: `tauri/src-tauri/src/main.rs:105-114` (10 cmds incl. `library_cue_folder:109`), `library_cmds.rs:820` (cue_folder bridge), `api.ts:2536-2546` (`invoke(\"library_cue_folder\")`), `index.ts:1844 runCueExport`/`581 renderCueExport`; watcher LANDED: `src/vibemix/library/watcher.py` wired at `__main__.py:2111-2122` (\"library freshness watcher armed\"); `export_smart_cues` grounding gate intact (`toolset.py:925-944` — proposal must be issued this run AND `track_id in self.seen`); tests: `pytest tests/library/test_setprep_tools.py … test_mcp_server_clarification.py` → **90 passed**; CORRECTION-confirmed dead path: 5 `ipc.library.*` schema types, `rg ipc.library. tauri/ui/src/library` → empty (`viber-capability-CORRECTION.md:43-47`).\n- **Risk:** Low. The \"WIRED but live-correctness UNVERIFIED\" caveat (CORRECTION:34-36) still holds — a `drive-vibemix` pass on `library chat/curate/build-set/cue` would confirm grounded data actually returns, but that's read-only-out-of-scope for this researcher and not a blocker. Honesty risk if anyone acts on the stale CORRECTION packet and rebuilds the cue-GUI or watcher that already exist (redundant second path).\n- **Codex package:** **\"Delete dead ipc.library.* ws types + refresh stale Viber correction packet.\"** Files-out: remove the 5 `ipc.library.*` entries from `tauri/ui/src/ipc/messages.schema.json` (then `npm run codegen:ipc`); annotate `.planning/packets/2026-06-01/viber-capability-CORRECTION.md` priorities #1 (cue-GUI) and #2 (watcher) as RESOLVED-at-HEAD (d6c14ba7). Gate: `cd tauri/ui && npm run build && npm test` green + `rg \"ipc.library.\" tauri/ui/src` empty. No new runtime build needed — the Viber surface itself is LAND/close-out.\n\n### 11. Learn module reality — v11.0 Earned: does lesson→Competent / cited-live-demo→Mastered actually work end-to-end, or are stages credited without real producers?\n- **Decision:** LAND (close-out — already resolved at HEAD) with ONE narrow honesty-audit package; the over-scoped producer is correctly HELD by an honest mask, not DROPPED.\n- **Why:** The skill-tree spine is wired end-to-end and honest: lesson-fill + recital AND-gate → Competent, and `_credit_live_skill_demo` (live call-site `coach.py:624`, fired every event) → `recognize` → `record_live_demo` with the citation gate as sole arbiter → Mastered, with an Earned-Wall refresh + fire-once vocal. 5 of 6 skills have **real production event producers** (LAYER_ARRIVAL/PHASE/PHRASE_BOUNDARY/MIX_MOVE all fire in `event_detector.py`; harmonic_mixing via the live Judge `_run_live_judge`→`transition_judged` at `coach.py:812`); only **beatmatching** has no producer, and it is honestly capped at Competent (`live_creditable=False`) — not credited without a producer. The one CI-red fixture the inventory flagged was already fixed by HEAD commit `d6c14ba7`.\n- **Evidence:** `skill_tree.py:291-356` (pure compute + COMP-02 AND-gate), `:437-504` (`record_live_demo` MAST-01 floor), `skill_recognizer.py:200-289` (citation gate sole arbiter) + `:105,150` (honesty mask); live wiring `coach.py:124-217,276-402,624`; producers real `event_detector.py:317-378`; **NOT-FOUND**: `BEATMATCH_GRADED` emitter / `learn/practice_loop.py` / any non-test `grade_beatmatch` caller (grep clean); `d6c14ba7` re-pointed the stale `beatmatching`→`harmonic_mixing` fixture; ran `pytest tests/learn/{skill_tree,skill_recognizer,creditability_drift,progress_snapshot_skill_wall}` → **35 passed**.\n- **Risk:** The whole live path is unit/synthetic-proven only — **never captured on a real rig** (`record_live_demo`/recognizer = WORKS_SOURCE_ONLY, Earned-Wall refresh = WIRED_BUT_UNPROVEN). Slop risk if anyone flips `beatmatching.live_creditable=True` before the H1 producer ships — that is the one place stages could be credited without a real producer; `test_creditability_drift.py` guards it. The Mastered unlock vocal tone is a parked ear-pass HOLD (`§EARNED-MASTERED-VOCAL-EAR`).\n- **Codex package:** **\"Earned-Wall live-credit capture + refresh proof\"** — files-in: `runtime/coach.py` (`_credit_live_skill_demo`/`_emit_earned_wall_refresh`/`_run_live_judge`), `learn/skill_recognizer.py`, `tests/runtime/`; files-out: NONE under `learn/skill_tree.py` or the honesty mask (do not touch `_HONEST_UNCREDITABLE_V11` / `live_creditable=False`). Gate: a `drive-vibemix` run that fires one real LAYER_ARRIVAL/MIX_MOVE or a two-deck `transition_judged`, asserts the `EvidenceRegistry` citation resolved, that `live_proof_count` advanced for a creditable skill, and that an `ipc.learn.progress_state` envelope repainted the wall — promoting the cluster from SOURCE_ONLY/WIRED to PROVEN in one capture. (The beatmatching H1 Practice-Deck producer is a separate, larger milestone — keep HELD behind the honest mask, do **not** fold it into this package.)\n\n### 5. Screen vision vs Rekordbox live source — which is authoritative, are both wired, do they conflict, is one redundant?\n- **Decision:** DROP (screen vision as a live source) + LAND (Rekordbox-XML enrichment + audio as the authoritative live stack, already the shipped state) — i.e. keep the source ladder as-is, retire/quarantine the dead screen-vision leg.\n- **Why:** They are not competing live sources and do not conflict — they sit on different layers. The **authoritative live truth is the captured deck audio + DSP** (RMS/BPM/phase/MIX_MOVE), with **nowplaying-cli + Rekordbox-`collection.xml`** providing read-only *track-identity enrichment* (it opens **no** live DJ DB — `deck_poller.py:30`). **Screen vision is fully dead-wired at runtime**: the capture loop fills a buffer (`__main__.py:2425`), but the JPEG never enters the prompt (`screen_jpeg = None` hardcoded anti-hallucination gate, `dj_cohost.py:2192-2199`, test-pinned) **and** the gated `DeckVisionReader` identity leg is never injected into the live `DeckPoller` (`__main__.py:2434-2438` passes no `vision_reader`/`vision_enabled`, both default off, `deck_poller.py:138-148`). So nothing is redundant — one source is simply never read.\n- **Evidence:** `dj_cohost.py:2192-2199` + test `tests/agent/test_dj_cohost.py:468` (screen_jpeg=None unconditional, v4:1502 anti-hallucination invariant) · `deck_poller.py:30` (\"opens NO DJ-software DB\") + `deck_poller.py:138-148` (`vision_enabled=False` default, eval-gated KAAN-ACTION) · `__main__.py:2434-2438` (live DeckPoller constructed with no vision args) · capability inventory line 61 (\"Overlay highlight … next: live screen-cited highlight capture\") + line 324 (every live screen/vision producer \"UNBUILT\") · `fixtures/hype_trace_genre1.jsonl` all rows `has_screen:false`.\n- **Risk:** The honest/slop risk runs the *other* way — re-enabling screen as a live source would re-open the v4 hallucination class it was killed to close (the gate is load-bearing). The only cost of dropping it is the ~1Hz `screen_task` capture loop + ScreenCaptureKit permission prompt burning CPU/UX for a buffer no one reads, plus a misleading `screen vision is not currently resolving the independent second deck` blocker string (`__main__.py:5066`) implying a near-ready feature. If the second-deck identity problem is ever truly needed, screen is the *only* candidate independent source — but that's a future eval, not a live source today.\n- **Codex package:** **\"Quarantine the dead screen-vision live leg\"** — files-in: `src/vibemix/__main__.py` (stop spawning `screen_task` / `screen_macos.run_capture_loop` while `screen_jpeg` is hardcoded `None`; gate it behind an explicit off-by-default `VIBEMIX_DECK_VISION` flag so the SCKit permission prompt + 1Hz capture don't fire for a buffer nothing reads), `tests/agent/test_dj_cohost.py` (keep the `screen_jpeg=None` pin), a new `tests/runtime/` assertion that no screen capture task runs unless the flag is set. files-out: do NOT touch `dj_cohost.py:2199` (the `None` gate is the anti-hallucination invariant — leave it), do NOT delete `_screen_macos.py`/`deck_vision.py` (preserve as the gated future second-deck path behind `eval/deck_vision/run_eval.py`). gate: `uv run pytest -q tests/agent/test_dj_cohost.py tests/runtime/ tests/state/` green + the `vibemix-grounding-review` skill confirms invariant #3 intact. (If a lighter touch is preferred: close-out as documentation-only, marking screen-vision `[DORMANT_BY_DESIGN]` in the capability inventory and fixing the misleading `__main__.py:5066` blocker copy.)\n\n### 3. Deck identity source — what resolves which deck is which\n- **Decision:** HOLD\n- **Why:** A deterministic resolver already ships and is test-pinned — `DeckPoller` runs a confidence-floored source ladder (Rekordbox-XML title-match → gated vision → numpy slot), and `derive_audible_deck` attributes the single now-playing title to a deck via MIDI fader/xfader weights. It correctly resolves the **audible** deck and suppresses the silent deck to honest `unknown` by construction. The FLX4 hold is not a resolver bug — it is the **absence of an independent identity source for the second deck** (vision is the only planned one and it is eval-gated OFF), plus a live rig where now-playing ownership is a non-DJ app (`com.apple.webkit.gpu`) and B audio is silent. Landing more \"resolver\" code can't conjure proof the hardware/sources don't emit; the gate is real hardware + an enabled identity source, which is HOLD-pending by the project's own anti-hallucination posture.\n- **Evidence:** `src/vibemix/state/deck_poller.py:315-421` (`_build_decks` ladder + cross-deck suppression); `src/vibemix/state/track_resolver.py:31-89` (`derive_audible_deck`, MIDI-weighted attribution, FLX4 play-LED desync handled); `deck_poller.py:68-70,407` (XML/VISION/NUMPY floors, `vision_enabled=False` default); `.planning/packets/2026-06-01/CODEX_HOLD-flx4-live-acceptance-current.md` (\"deck_state had no resolved deck row… nowplaying_owner=com.apple.webkit.gpu… screen_vision=disabled… A_active+B_silent\"); `tests/state/test_deck_poller.py:338,395-456` (suppression + \"never borrows audible title\" pins); `deck_vision.py:1-40` (the gated universal second-deck identity leg).\n- **Risk:** If wrong (i.e., if we LAND a new deterministic resolver to force `deck_state_pair_resolved=true`), we'd be inventing a second-deck identity from the audible deck's title or from configured-but-silent BlackHole channels — exactly the cross-deck hallucination the suppression rule (`second_deck_source=suppressed_requires_independent_source`) exists to prevent. That fabricates a citable `[track:]`/`[key:]` for a deck we cannot actually hear: a direct Invariant #3 (trust the audio) and #2 (citation grounding) violation — the headline slop class.\n- **Codex package:** HOLD — no resolver package. The honest unblock is a tiny **diagnostics/eval** package, not new identity logic: `deck_vision` real-screenshot accuracy eval to clear the `VISION_CONF_FLOOR` per app (in: `eval/deck_vision/run_eval.py` + committed screenshot fixtures + per-app floor table; out: untouched `deck_poller.py`/`dj_cohost.py` reaction path; gate: eval passes the documented floor → KAAN-ACTION sign-off flips `vision_enabled`). That is the only path that turns the second deck citable without inventing identity. Everything else on this question is `NEEDS_LIVE_PROOF_FIRST` (FLX4 rig with B audible + Rekordbox-owned now-playing), which is hardware-gated, not a code package.\n\n### 9. BEATMATCH_GRADED production producer (owned-deck real-time grading)\n- **Decision:** HOLD\n- **Why:** Every offline piece exists and is tested (grader, consumer, `MiniDeck` engine, `BeatGrid.from_anlz`, controller tempo/jog/xfader snapshot), but the missing producer is *exactly* the part that cannot be built or proven read-only: a live owned-deck practice loop needs a real audio `OutputStream`, live FLX4 hardware mapped to `rate_b`/`xfader`, and a Mastered vocal — and \"Mastered\" is the headline anti-slop claim, so a wrong/late grade is catastrophic false-expertise (Invariant #3). This is wiring + hardware proof, not research; the design is already settled.\n- **Evidence:** `src/vibemix/learn/beatmatch_judge.py:83,145` (grader done) · `skill_recognizer.py:154-172` (consumer wired, LOCKED+cited only) · `tests/repo/test_live_reality_pins.py` 20-pass confirms producer orphaned (`_emitter_call_sites()==[]`) · `MiniDeck.render_block` (`miniplayer.py:130`) has only smoke-script callers, no production `OutputStream` · `BeatGrid.from_anlz` landed `d70de2ba` (today) · board `NEXT-LAND-BOARD-after-c88d5f56.md#H2` = HOLD `NEEDS_LIVE_PROOF_FIRST`; FLX4 source-proof itself on hold (`173399ae`).\n- **Risk:** If wired and shipped without on-rig proof, a phantom/late grade credits \"beatmatching Mastered\" the app never actually observed — the single worst false-expertise slop, breaking Invariant #3 and the v11 \"Earned\" moat. Reverse risk (leaving HOLD) is zero: the reality-pins keep the masked-uncreditable boundary honest indefinitely.\n- **Codex package:** none / HOLD — do NOT package an emitter now. The only read-safe adjacent move is the *plumbing spec, not the live emitter*: define a `learn/beatmatch_practice.py` skeleton (load 2 tracks → `MiniDeck` → `DeckState`) behind an explicit feature flag that stops short of any `grade_beatmatch(...)` call (so the three reality-pins stay green), files-in: `src/vibemix/learn/beatmatch_practice.py` + unit tests; files-out: NO edit to `skill_recognizer.py`, `beatmatch_judge.py`, or the pins. **Gate to actually emit + unmask:** real audio `OutputStream` playback + live FLX4 → `rate_b`/`xfader` mapping + on-rig proof of a genuine LOCKED grade + `drive-vibemix`/`vibemix-grounding-review` PASS on the Mastered vocal; only then delete the reality-pins and clear `_HONEST_UNCREDITABLE_V11`. Not landable this read-only pass.\n\n### 10. DROP-call live hype line — keep HOLD, drop, or land?\n- **Decision:** HOLD\n- **Why:** The lane is correct in shape (deterministic fixed-text `say()`, no LLM → no tag/English leak, opt-in flag, green unit tests) but is `WIRED_BUT_UNPROVEN` — zero captured proof the call lands ON the beat against a *real* drop, and a fire on a phantom predicted-drop is exactly the catastrophic \"AI slop\" release blocker. The feature has real value, so DROP is wrong; the gate is genuinely unmet, so LAND is wrong.\n- **Evidence:** `4ac30b5d` records the hold; dirty-only confirmed (`git status`: `coach.py`/`event_detector.py`/`drop_predict.py` + `test_event_detector_drop.py` untracked, while writer `refresh.py:957-982` and bank `drop_reaction.py:68` are already at HEAD); `event_detector.py:285-290` gated by `VIBEMIX_DROP_CALL` default-OFF (`_drop_call_enabled_from_env`); `coach.py:704-718` speak path; tests green (ran `tests/state/test_event_detector_drop.py tests/state/test_drop_predict.py` → 21 passed); inventory classifies it `[WIRED_BUT_UNPROVEN]` (`CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:145-146`); land board H1 = HOLD with this exact gate (`NEXT-LAND-BOARD-after-c88d5f56.md:36-40,74`).\n- **Exact gate to clear before promotion (all three, ordered):**\n  1. **grounding-review PASS** — run the `vibemix-grounding-review` skill over the `coach.py` DROP hunk + `event_detector.py` gate; it must confirm the spoken line is reaction-bank-only (never LLM), cannot fire while idle, and respects cooldown. (DROP has no per-type gap → falls back to `EVENT_GLOBAL_MIN_GAP`; verify that floor is acceptable, or add an explicit DROP entry to `MIN_EVENT_GAP_PER_TYPE`.)\n  2. **on-beat live proof** — a captured `drive-vibemix` artifact with `VIBEMIX_DROP_CALL=1` AND a populated `section_source` (cue/ANLZ ingested, since `predicted_drop_in_sec` stays `None` without it per `refresh.py:961-980`), showing the spoken line arrives inside the 2.0s arm window (`DROP_ARM_WINDOW_S`) and audibly lands on the drop, not seconds late.\n  3. **false-positive rate** — over that live run, count predicted-drop crossings vs actual drops; the spoken call must NOT fire on a phantom/early prediction. A single audible phantom call fails the gate.\n- **Risk:** If promoted before the gate: a phantom on-beat hype line = the #1 anti-slop release blocker. Build-hygiene risk is acute — a whole-file `git add` on `coach.py` or `__main__.py` (~13 lanes) sweeps the ungated DROP speech into a signed binary (`NEXT-LAND-BOARD-after-c88d5f56.md:31,53-54`). If DROPped instead: a high-value, already-built on-beat hype moment is discarded for a gate that is achievable once cue/ANLZ data is live.\n- **Codex package:** none / keep-on-hold close-out. Do NOT cut a LAND package. The only safe Codex action now is the build-hygiene guard already chartered (`CODEX_READY-no-spoken-fallbacks-live-guard.md` / hunk-stage discipline): ensure the dirty DROP block stays OUT of any release build from a clean `c88d5f56` tree. Promotion is a Kaan-owned live ear-pass (gate above), not a code package — the engineering is done, the proof is not.\n\n### 4. BlackHole / Rekordbox setup UX — in-app setup + honest \"routing is wrong\" detector\n- **Decision:** RESEARCH\n- **Why:** Setup *discovery* already ships and is green — BlackHole presence probe, install CTA banner, device picker, sine-tone verify, driver auto-fetch — so a basic in-app setup/verify flow is LAND-already. The genuinely missing, high-value piece is the **honest \"your routing is wrong\" detector** for the two real footguns (DJ app not feeding BlackHole = silent input; system/Multi-Output also feeding BlackHole = phantom-music loopback). Building it right needs a measured RMS/correlation threshold spike on real signal — that's read-only-unfinishable, so RESEARCH the detector, not a blind LAND.\n- **Evidence:** `src/vibemix/install/blackhole_probe.py:153` (probe, 17 tests green: `tests/install/test_blackhole_probe*.py`); `src/vibemix/runtime/wizard.py:263-332` (sine-tone verify) + `step2-output-device.ts:103-111` (install banner) — setup surface EXISTS; `step2-output-device.ts:186-194` (tone test demoted to informational — current verify is weak); `docs/audio-routing.md:111-124` (`§LEARN-AUDIO-ROUTING-WIZARD-DISCHARGE` — guided routing is an explicit unbuilt KAAN-ACTION); routing-wrong/loopback master-input detector = **NOT-FOUND** (grep of `__main__.py`/`audio/` finds only deck-pair `setup_hint`/`setup_block` at `__main__.py:4708,5188`, never a master-input-silence or feedback check); CLAUDE.md documents the phantom-music loopback as the #1 real-world footgun.\n- **Risk:** A naive \"no input → routing wrong\" banner is itself slop if it fires during legitimate silence (track loaded, not playing; breakdown; idle) — false-positive flips contradict Invariant #5 (idle ≠ fault). The loopback detector is harder: distinguishing the AI's own re-amplified voice from real music needs a correlation/echo test, not a level threshold, or it will mislabel a correct rig. Ship a guessed threshold and you teach users to ignore the one banner that matters.\n- **Codex package:** **Spike: \"routing-wrong\" honest detector thresholds** — (in) read `audio/deck_capture.py:288` (`_DECK_ACTIVE_RMS` silent boundary), `audio/cues.py:17` (`SILENCE` −60dBFS), `audio/deck_signal.py`, `runtime/wizard.py` probe path; capture 3 real-rig RMS traces (correct route playing / DJ-app-not-routed / loopback-feedback with empty deck) to derive (a) a master-input-silence-while-deck-should-be-active threshold and (b) a self-voice-vs-music correlation test. (out) a RESEARCH.md with measured thresholds + the false-positive guard tied to ACTIVE state (Invariant #5) + a proposed `ipc.calibration.routing_check` shape. (gate) thresholds backed by captured traces, not guessed; detector abstains (no banner) on legitimate idle/silence. Do NOT ship a detector or banner this package — measurement first.\n\n### 8. Mixxx goldmine ideas worth adopting (shortest honest-ship + grounding/slop gap)\n- **Decision:** RESEARCH (overall: ADOPT idea A, RESEARCH idea B, HOLD idea C)\n- **Why:** The 1-3 highest-value, shortest-honest-ship picks are all from **Round 2** because they attack the named live release blocker (R-SLOP — the co-host claiming an EQ/fader move had an effect it can't ground) and are passive-listener-safe with zero new deps. The Round-1 headline (Practice-Deck Loop / H1) is *not* shortest-ship: it's already on the board as **H2 HOLD** needing live owned-deck hardware proof, and its sibling \"loop geometry\" idea has a hidden unwired prerequisite (live beatgrid). Ranked picks: **(A) Short-term LUFS receipt** — cleanest standalone honesty unlock; **(B) EQ band-delta \"Spectral Receipt\"** — biggest R-SLOP kill but needs the inseparable settling-lag window; **(C) Loop-geometry rule** — strongest-in-theory but grid-blocked today.\n- **Evidence:** Round-2 map `wf_d9ddbeed-f5f__...round-2.md:36-49,72-91` (5 grounding axes, passive-listener reframe); guard exists but predictors absent — `deck_context.py:2186 live_claim_policy`, `:2308 apply_live_claim_guard`, `:2093 dsp_delta_not_causal_proof`; `state/loop_geometry.py`/`intel/eq_move_model.py`/`audio/lufs.py` all **NOT-FOUND** (verified absent); `band_features.py:30 band_energy_ratios` present (substrate ready); loop idea grid-blocked — FLX4 profile emits only `loop_in/out` notes 16/17 (`pioneer_ddj_flx4.json:33-36`), and `from_anlz` exists (`grid.py:39`, landed `d70de2ba`) but is **NOT wired into runtime `refresh.py`/`MusicState`** (NOT-FOUND); Round-1 H1 is board-HOLD H2 (`NEXT-LAND-BOARD-after-c88d5f56.md` H2: `NEEDS_LIVE_PROOF_FIRST`); GPL ledger clean — every item learn-from-spec, RBJ-cookbook/BS.1770 are the real sources not Mixxx (`round-2.md:142-153`).\n- **Risk:** **GPL vendoring** is the live trap — do NOT port Mixxx's Bessel4 IIR coefficients or paste any C++; source biquad math from the public RBJ cookbook and K-weighting from ITU-R BS.1770 (Mixxx itself wraps MIT libebur128, so even same-shape-as-impl is avoidable). **Slop/honesty risk:** any of these touches what licenses a spoken causal claim → must clear `vibemix-grounding-review` (Invariants #2/#3); the inverse false-negative is real — a naive single-frame EQ compare REFUTES true kills (the settling-lag window is mandatory, ships WITH #2 or not at all). Causation humility must hold: predicted∩measured agreement upgrades the gate, it does not license a bare \"your kill removed the lows.\"\n- **Codex package:** Three narrow packages, build LUFS first (no settling-window coupling, cleanest proof):\n  1. **`audio/lufs.py` short-term LUFS receipt** — files-in: NEW `src/vibemix/audio/lufs.py` (K-weight 2-biquad cascade + 3s sliding mean-square, ungated, from BS.1770), `tests/audio/test_lufs.py`; out: read beside `Levels.snapshot()` in state-refresh (single-writer), attach `master_lufs_delta` atom into the guard's audio-delta path. Gate: −3.01 LUFS conformance tone passes @44.1k AND 48k + `vibemix-grounding-review` PASS (grounds +3 LU window, strips flat 0 LU). No new dep, no spec edit.\n  2. **`intel/eq_move_model.py` Spectral Receipt + settling window** (RESEARCH→package only after #1) — NEW `intel/eq_move_model.py` (`predicted_band_gains(move, fs)` via RBJ `|H|²` over `_BANDS`), call site beside `_has_unsupported_mixer_low_kill_claim:2580`, consumes `band_energy_ratios` per-lane. Gate: two-frame bass-killed fixture GROUNDS, no-bass REFUTES, settling-window included, grounding-review PASS. Flag: GPL — RBJ cookbook is the source, never Bessel4 coeffs.\n  3. **Loop-geometry — close-out as RESEARCH/spike, not a package yet** — blocked: needs the ANLZ `from_anlz` grid wired into live `MusicState` first (separate prerequisite), and route loop labels through `_record_event` not the 400ms-dedup `_record_move` (`midi/state.py:230`). Do not draft until the runtime-grid wiring spike lands."
  }
}