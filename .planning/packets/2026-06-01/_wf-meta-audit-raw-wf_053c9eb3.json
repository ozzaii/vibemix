{
  "summary": "Read-only cross-system audit → ranked Codex board (SAFE_NOW / HOLD_WITH_GATE / DROP / RESEARCH_WITH_WORKFLOW). Synthesizes code + packets + inventory + drift + ledger + Mixxx research. Schema-less. No product edits/stage/commit/app-launch.",
  "agentCount": 10,
  "logs": [
    "Running 8 cross-system audit lenses (schema-less)."
  ],
  "result": {
    "workflow": "meta-audit-next-board",
    "lensBlocks": [
      "API Error: Server is temporarily limiting requests (not your usage limit) · Rate limited",
      "I have full evidence. The Crate surface already exposes search/similar/curate/build/cue/budget. The genuinely-hidden capabilities are: `demo_mode` (zero callers), `beatmatch_judge`/`xfade_gains` (orphaned moat math, no producer), Telegram (no GUI surface, CLI-only by design), `library budget`/`stats`/`doctor`/`models` (CLI-only operator tools), and MOSS being unreachable on fresh machines (a different lens). Let me synthesize the built-not-surfaced findings.\n\n## built-not-surfaced\n\nThe Tauri Crate/Deck/Learn surfaces are broader than the inventory's CLI_ONLY labels suggest — search/similar/curate/build-set/cue-folder/budget all have both-ends-wired GUI consumers (`tauri/ui/src/library/index.ts`). So the real \"built but a user can't reach it\" set is narrower and sharper than 282 caps imply. Ranked highest-value first.\n\n- **Auto-cue / smart-hot-cue export is built, tested on real audio, but has NO discoverable in-app entry** — `[SAFE_NOW]` — This is the documented MOAT (offline audio→intro/build/drop/outro→Rekordbox/Serato/M3U8), the cue *heuristic* is one of only ~4 truly end-to-end-proven capabilities (`test_cue_detect_eval.py`, label_recall=1.0 on 6 real mp3s), yet the Crate \"cue\" mode is buried behind a folder-input the inventory never confirms a user finds. Surfacing it as a first-class Crate action is the single highest-leverage hidden-capability win — it's the differentiator and it actually works. — evidence: producer `library/cue_folder.py:178 export_cued_folder`, eval `tests/bench/data/*.mp3` proven; GUI hook only a raw input `tauri/ui/src/library/index.ts:1689` (`vmx-lib-cue-folder`), no nav/discovery surface confirmed in inventory (`library-clap-cue` silo: \"real-folder cue-export capture\" still next) — gate: none\n\n- **next_suggestion pill engine (\"what's next\") — wired both ends but never confirmed firing on the live ws bus** — `[HOLD_WITH_GATE]` — The pill is the PRIMARY product surface and its mean-centered next-track engine is built (`library/next_suggestion.py`), scheduled on TRACK_CHANGE, merged onto the mascot frame, with the choose/feedback inbound actions wired at `ws_bus.py:875-894` — but no capture proves a `next_suggestion` frame ever reaches the pill live. If it silently never fires, the headline surface is dead. — evidence: `runtime/coach.py` schedules; consumer `ws_bus.py:875-910`; FE `pill/next-suggestion.ts`; inventory `[WIRED_BUT_UNPROVEN]` \"confirm a live next_suggestion frame on the ws bus\" — gate: `drive-vibemix` live capture of one `next_suggestion` frame on `127.0.0.1:8765` during a real two-track session before claiming the pill works\n\n- **demo_mode (DEMO_SEQUENCE / load_sequence) — 198 lines + passing test, ZERO live importers** — `[DROP]` — A scripted-event playback engine with its own green test (`test_demo_mode_sequence.py`) but no import anywhere in `__main__`/`dj_cohost`/`session_loop`. It is not reachable by any user and not on the ship path; it's the cleanest orphan in the tree. Either wire it to a \"Demo\" entry-point (would make first-run/marketing demos trivial) or delete it. — evidence: `runtime/demo_mode.py`; grep of live callers = NOT-FOUND (confirmed this pass: `rg demo_mode src/vibemix` returns only the module + its test) — gate: none (delete unless a demo entry-point is explicitly wanted)\n\n- **Beatmatch Judge math (\"the moat\") + crossfader/transition-planner ports — orphaned with zero src callers** — `[HOLD_WITH_GATE]` — `grade_beatmatch`, `_phase_error`, `_octave_fold_multiplier`, `xfade_gains`, and the AutoDJ `calculate_transition` curve are real, license-clean (clean-room from Mixxx), and unit-proven, but every live PRODUCER (the H1 Practice-Deck loop, `learn/practice_loop.py`) is UNBUILT — so the user can never trigger them and `BEATMATCH_GRADED` has a consumer with no emitter (a silent dead branch). High latent value (it's the named moat), but surfacing it is a feature build, not a wiring win. — evidence: `learn/beatmatch_judge.py:83` zero `src/` callers; `audio/xfade.py:39-75` zero consumers; `BEATMATCH_GRADED` emitter NOT-FOUND; `practice_loop.py` absent — gate: scope decision (build H1 Practice-Deck producer as a milestone) OR park honestly as Competent-capped; do NOT ship the dead consumer claiming the skill\n\n- **Operator/diagnostic CLIs (budget, stats, doctor, models, live-context, verify-live-reply) — CLI-only by design, no GUI** — `[HOLD_WITH_GATE]` — These are legitimately built and tested (`__main__.py:3120-3176`, `test_budget/stats/doctor/models_cli.py`) but invisible to a non-terminal user. `library doctor` (env/model readiness) and `library models --install` are the two with real end-user value — a fresh machine that boots voice-muted has no in-app way to discover *why* or fix it. The model-install button exists in Settings (`library/index.ts:2006`) but is `[WIRED_BUT_UNPROVEN]`. — evidence: `__main__.py:3163` `_cmd_library_models`, `:3176` `doctor`; Settings install button `tauri/ui/src/library/index.ts:2006` unproven — gate: only surface `doctor`/`models` readiness in-app IF the MOSS-fresh-machine-mute problem is being solved (otherwise the diagnostic surfaces a dead end); `budget`/`stats` stay operator-only\n\n- **Telegram Viber surface — fully built, privacy-scrubbed, NO desktop-app discovery** — `[HOLD_WITH_GATE]` — The mobile curate-from-phone bridge is real (`library/telegram_bridge.py`, allow-list auth, leak-strip, 90s timeout) and CLI-launchable, but there is ZERO reference to it in `tauri/ui/src/` — a user would never know it exists. It's an opt-in power-user feature gated on a BotFather token, so hiding it from the default UI is defensible, but a one-line \"connect Telegram\" affordance in Settings would surface a complete, tested capability for near-zero cost. — evidence: `library/telegram_bridge.py` (pure logic tested), CLI `__main__.py:6419`; `rg telegram tauri/ui/src` = no GUI surface — gate: product call on whether Telegram is a v1 advertised feature; if yes, add a Settings \"Telegram bot\" pill (token + chat-id), do not auto-enable\n\n- **Overlay highlight ([screen:*] cited on-screen) — wired, never captured firing** — `[HOLD_WITH_GATE]` — A click-through highlight window that draws on the DJ app's screen when the co-host cites `[screen:waveform_a]` is fully wired (`overlay/overlay-highlight.ts:43` → `show_overlay_highlight` Rust cmd, producer `dj_cohost.py:3285`) and has a publish test, but there is no live capture proving a screen-cited reaction ever lights it up. It's a genuinely differentiated \"grounded receipt you can see on your deck\" surface that may be silently never-firing. — evidence: producer `agent/dj_cohost.py:3285-3300`, FE `overlay/overlay-highlight.ts:43`, `test_overlay_publish.py`; inventory `[WORKS_SOURCE_ONLY]` \"live screen-cited highlight capture\" owed — gate: `drive-vibemix` capture of one `ipc.session.overlay-highlight` firing on a real screen-cited reaction before claiming the feature\n\n**Highest-value to surface:** the **auto-cue / smart-hot-cue export** — it is the named moat, it is one of the few capabilities proven end-to-end on real audio, and the only thing between \"built\" and \"a user uses it daily\" is a discoverable Crate action plus one real-folder export eye-check. Everything else above is either a delete (`demo_mode`), an unbuilt producer (`beatmatch_judge`), or an opt-in/operator surface that's defensibly hidden.",
      "API Error: Server is temporarily limiting requests (not your usage limit) · Rate limited",
      "All drop candidates verified against the tree. The orphan/dead/absent claims hold: `demo_mode` has zero importers, `BEATMATCH_GRADED` has no production emitter (only consumer + comments), `grade_beatmatch`/`xfade_gains` have zero non-test callers, the `gemini_native_tts_class`/`GeminiTTS` path has no non-self caller, `CARTESIA_*` constants are threaded-but-ignored (the `_ = (...)` discard line at `tts_chain.py:41` proves dead), and the OpenRouter monkey-patch runs at import with no provider instantiated. Mascot is default-off (53 frontend modules + html, built only when `primary_surface=mascot`). I have enough verified evidence to write the lens.\n\n## valuable-but-drop\n\n- **Mascot 3D VTuber surface (53 frontend modules + `mascot.html` + Rust window + CI fence)** — `[DROP]` — A full second co-host face (Canvas/Three.js orb, mood rig, emote bridge, `mascot-audit` CI) that is already DEMOTED to opt-in (default `PrimarySurface::Pill`); it is the largest single body of code serving a surface no default user sees, and the product's value (a real DJ voice in your ear) is fully delivered by the Pill + speech. Maintaining it taxes every IPC/emote/reaction change for zero default-path benefit. — evidence: `tauri/src-tauri/src/main.rs:186-218` (built only if `primary_surface=mascot`), 53 files under `tauri/ui/src/mascot/`, inventory `app-ui` silo + `CODEX_READY-emote-mascot-reaction-bridge.md` (Gold 5, still owes \"visible Tauri mascot reaction\" live proof) — gate: none (defer-to-v2 acceptable; if kept, freeze it — no new wiring spent on it pre-launch).\n\n- **`runtime/demo_mode.py` (DEMO_SEQUENCE / load_sequence, 198 lines + passing test)** — `[DROP]` — A scripted canned-reaction sequencer with zero live importers anywhere in `__main__`/`dj_cohost`/`session_loop`; only its own test references it. A scripted-reaction player is also philosophically adverse to the \"trust the audio, never scripted\" core value — it is exactly the kind of fake-flow surface the product forbids. — evidence: verified `rg \"demo_mode|DEMO_SEQUENCE|load_sequence\"` over `src/vibemix` excluding self+test = EMPTY; inventory runtime-ipc silo `[PRESENT_ORPHANED]` — gate: none (delete module + its test; or if a demo entry-point is genuinely wanted for marketing, that is a RESEARCH_WITH_WORKFLOW item, not a silent orphan).\n\n- **Beatmatch Judge moat + xfade/transition-clock ports (math built, producer absent)** — `[DROP]` (drop the *claim/consumer scaffolding* now; the math is a DEFER) — `grade_beatmatch`/`_phase_error`/`_octave_fold_multiplier`, `xfade_gains`, and the `BEATMATCH_GRADED` consumer branch are real and unit-tested but have **zero production caller** — no `practice_loop.py`/`practice_runtime.py` exists to ever fire them. This is an entire orphaned \"moat\" feature family masquerading as wired; shipping it as-is means a v11 skill that can never reach Mastered. — evidence: verified `BEATMATCH_GRADED` has consumer (`skill_recognizer.py:154`) + comments only, no emitter; `grade_beatmatch`/`xfade_gains` non-test callers = EMPTY; `learn/practice_loop.py` NOT-FOUND; inventory learn-earned + mixxx-goldmine silos (independently confirmed) — gate: the honesty boundary (`_HONEST_UNCREDITABLE_V11=(\"beatmatching\",)`, `skill_recognizer.py:105`) must STAY so beatmatching is capped at Competent in the shipped UI; only build the H1 Practice-Deck producer if the skill-tree is a launch priority (it is a large net-new build, not a wiring fix).\n\n- **Mixxx DB/OSC live-integration lanes (M4 `mixxxdb.sqlite` writer, M5 `MixxxEvidenceBackend` OSC, H2-H8 producer family)** — `[DROP]` (defer post-launch) — A whole research-stream of CLAIMED-BUT-ABSENT capabilities: `library/export_mixxxdb.py`, `platform/mixxx_osc.py`, `library/detect_key.py`, `audio/beatgrid_detect.py`, `audio/wsola.py` all NOT-FOUND. Stock Mixxx has no OSC read path (fork-gated on an unmerged PR), so any \"works with Mixxx live\" marketing would be dishonest. These dilute a coherent v1 — the shipped Mixxx value is the file-format SINKS (M2 M3U8, M3 Serato carrier), which are real. — evidence: inventory mixxx-goldmine silo (12× `[CLAIMED_BUT_ABSENT]`, each grep-confirmed NOT-FOUND); `CODEX_GOLD_STRUCTURE.md:143` Shape C explicitly \"DON'T BUILD\" — gate: keep ONLY M2/M3 file sinks in v1; do not let any \"Mixxx integration\" launch copy imply live grounding (Gold 7 launch-collateral HOLD already flags this).\n\n- **Dead cloud-TTS residue (Gemini-native TTS class, OpenRouter monkey-patch, Cartesia config threading)** — `[DROP]` — `gemini_native_tts_class`/`GeminiTTS` (`_livekit_google_slim.py`), the import-time `AUDIO_STREAM_MODELS.add(OPENROUTER_TTS_MODEL)` patch (`tts_chain.py:23`), and the `cartesia_api_key`/`CARTESIA_*` params that are threaded then explicitly discarded (`tts_chain.py:41` `_ = (...)`) are all dead import-only residue of the retired chain. Beyond maintenance debt, leaving cloud-TTS seams in a MOSS-only product is an honesty/audit liability — a reader/OSS-roaster sees \"Cartesia/Gemini TTS\" symbols and assumes a hidden cloud fallback. — evidence: verified zero non-self callers for `gemini_native_tts_class`/`GeminiTTS`; `tts_chain.py:38-66` accepts then ignores cloud keys; `config.py:41-54` `CARTESIA_*` orphaned; inventory tts-voices silo (3× `[DROP_OR_DELETE]` + 1 intentional `[CLAIMED_BUT_ABSENT]`) — gate: keep `google_llm_class` (that's the brain transport, not TTS); delete only the TTS-class/patch/params. Loops back to the MOSS-only invariant — removing these makes \"no fake spoken fallback\" provable by absence, not just by behavior.\n\n- **`library budget` free-at-scale / under-€50 gate** — `[DROP]` (drop the *claim*, keep the tool) — The cost telemetry CLI is fine, but `test_monthly_projection_under_50_eur` is XFAIL — the \"free/cheap at scale\" budget claim is unproven and contradicted by the live-stack study (TTS dominates per-turn cost). Any launch copy resting on the €50/mo figure is a CLAIMED_BUT_ABSENT honesty gap. — evidence: inventory library-clap-cue silo (XFAIL); `project_live_stack_cost_and_capability_study` memory (TTS ≈70%/turn) — gate: treat the under-€50 number as unverified internal-only until the cost model is reworked; keep `library budget` as an internal tool, never as external pricing copy.\n\n- **Stale Python orphans behind live Rust paths (`debrief/ear_test_capture.py`, transformers `audio_decode` branch, `debrief/ln.py` doc-ghost)** — `[DROP]` — The live ear-test path is Rust-only (`ear_test_cmds.rs`); the Python `ear_test_capture.py::write_ear_test_log` has no Python/Rust importer (dead duplicate). The `audio_decode` transformers branch is SKIPPED/dead against the torch-free ship path. `debrief/ln.py` is referenced by three docs but does not exist. These are small but pure dead-weight/duplicate-system debt that contradicts \"every surviving capability is wired.\" — evidence: inventory memory-observability + library-clap-cue silos; `ls debrief/ln.py` NOT-FOUND — gate: none — delete the dead Python ear-test module (or re-point Rust at it, pick one), confirm `audio_decode.py` has no live transformers dependency and strip the branch, fix the three stale `ln.py` doc-anchors.",
      "Confirmed — `SqliteVecMemoryStore` self-describes as a \"sibling of `SqliteVecStore`\" with near-identical vec0 DDL, INSERT-replace workaround, and `add_batch`/`search`. Genuine duplication. I have all evidence. Synthesizing.\n\n## duplicate-architecture\n\n- **The two `coach.py` files are NOT duplicates — but the name collision is a maintenance trap** — `[SAFE_NOW]` — `runtime/coach.py` is the 10Hz reaction *loop* (`coach_loop`, orchestration); `state/coach.py` is `AICoach`, the stateless per-event *prompt-construction* namespace. Different layers, justified split — but two files named `coach.py` imported as `runtime.coach` vs `state.coach` repeatedly confuses greps and risks an import-wrong-coach bug. Rename `state/coach.py` → `prompt_builder.py` / `ai_coach.py` (its only public exports are `AICoach`, `ACK_ELIGIBLE_EVENTS`, `EVIDENCE_CITATION_RE`-adjacent helpers; 6 importers to update). — evidence: `src/vibemix/runtime/coach.py:1-30` (loop), `src/vibemix/state/coach.py:1-25` (`AICoach`), importers at `src/vibemix/__main__.py:105`, `src/vibemix/agent/dj_cohost.py:90`, `src/vibemix/bench/assemble.py:34` — gate: none\n\n- **TTS builders are already collapsed to MOSS-only — but CLAUDE.md still documents a live \"TWO builders\" fork** — `[SAFE_NOW]` — `_build_direct_chain`, `build_tts_chain(mode=...)`, and `proxy_client.build_proxy_tts_chain` ALL ignore their args and return `_build_moss_chain()` → `build_local_tts_adapter()`. The provider-selection duplication is gone (good, matches MOSS-only product truth), leaving three thin compatibility shims with dead `gemini/openrouter/cartesia/jwt/voice` params. Drop the dead params (or collapse the three shims to one) and fix the CLAUDE.md `tts_chain.py` architecture note that still claims \"_build_direct_chain (BYO-key) and build_proxy_tts_chain (keyless) — a voice change must be wired into BOTH\". — evidence: `src/vibemix/agent/tts_chain.py:33-72` (all roads → `_build_moss_chain`), `src/vibemix/agent/proxy_client.py:49-56` (\"Compatibility shim: proxy mode also uses local MOSS\"), CLAUDE.md `agent/` architecture block — gate: none\n\n- **`embed.py::LibraryEmbedder` (Gemini embedder, ~430 lines) is dead product code kept only for migration tests** — `[HOLD_WITH_GATE]` — the product factory `build_embedder()` hard-returns `ClapEmbedder` and explicitly does NOT import `embed.py`; `EMBED_BACKEND = \"clap\"` is fixed and `VIBEMIX_EMBED_BACKEND` is dead. `LibraryEmbedder` survives only in docstrings, mirror-comments (`ingest.py:568,618`), and the legacy `_router_config` Gemini-embed entry — a whole parallel embedding stack (GA-model probe, genai client, cache-key SHA) shadowing the real one. — evidence: `src/vibemix/library/embed_factory.py:4-7,32-34`, `src/vibemix/library/_cosine.py:36`, `src/vibemix/library/embed.py:151` (`__all__` = `LibraryEmbedder`), only non-clap importers are docstring mirrors — gate: confirm no test imports `LibraryEmbedder` for live coverage (only migration/compat) and the `model_router` grep-gate has no Gemini-embed literal dependency, THEN delete `embed.py` + its router entry. If migration-from-old-Gemini-library is a still-shipping feature, justify the keep with a dated note instead.\n\n- **GUI library import (`importer.py`) re-implements the CLI ingest pipeline instead of calling `ingest_source`** — `[HOLD_WITH_GATE]` — `LibraryImporter.import_library` hand-rolls its own `for track in lib.tracks → has_cached_embedding → embed_track → add_batch` loop, duplicating the exact embed/cache/batch logic in `ingest.py::ingest_source`. Two code paths embed a Rekordbox library — they can drift (cue-anchoring, ANLZ structure index, cue-agreement calibration all live in `ingest_source` and are absent from the GUI path). This is the classic \"parallel CLI-vs-GUI doing the same job\" debt. — evidence: `src/vibemix/library/importer.py:64-162` (own loop, never calls `ingest_source`) vs `src/vibemix/library/ingest.py:756-768` (`ingest_source` with `anlz_index`, `cue_agreement_calibration` params) — gate: verify the GUI path is *meant* to skip ANLZ/cue-agreement (e.g. for speed/progress-streaming) — if not, route `import_library` through `ingest_source` with a progress callback; if yes, document the deliberate feature gap so the GUI isn't a silently-weaker importer.\n\n- **`ingest.py` and `folder_ingest.py` duplicate the `_Embedder`/`_Store`/`_Source` Protocols and the ingest skeleton** — `[HOLD_WITH_GATE]` — both modules redeclare structural-typing Protocols (`_Embedder`, `_Store`) with overlapping methods and both expose a near-identically-shaped `ingest_*(source/root, embedder, store, *, persist_library, progress, ...) -> IngestReport` entry. `ingest_source` is Rekordbox-XML-driven, `ingest_folder` is filesystem-walk-driven — a real domain split, but the shared Protocols + report-assembly + batch-persist loop should live in one place. — evidence: `src/vibemix/library/ingest.py:106-118` vs `src/vibemix/library/folder_ingest.py:67-74` (duplicate Protocols), `ingest.py:756` / `folder_ingest.py:316` (parallel entries) — gate: extract the shared `_Embedder/_Store` Protocols + the persist-batch loop into one helper (e.g. `_ingest_core.py`), keeping the two *source-discovery* front-ends thin — only after confirming the `embed_track` vs `embed_audio_bytes`/`embed_audio_file` Protocol method difference is reconciled (they currently diverge, which is why two Protocols exist).\n\n- **Three hand-maintained sqlite-vec `vec0` store implementations (`library`, `memory`, `learn`)** — `[HOLD_WITH_GATE]` — `library/index_sqlite_vec.py::SqliteVecStore` and `memory/index_sqlite_vec_memory.py::SqliteVecMemoryStore` are self-described siblings: same `sqlite_vec.load` guard, same `CREATE VIRTUAL TABLE ... vec0(FLOAT[N])` DDL, same \"vec0 doesn't support INSERT OR REPLACE\" delete-then-insert workaround, same `add_batch`. `learn/band_share_store.py` mirrors `library/embed_cache.py::init_cache_schema` for its plain-row sidecar. Each carries its own copy of the Win-ARM64 fallback probe. A vec0 quirk fixed in one won't propagate to the others. — evidence: `src/vibemix/library/index_sqlite_vec.py:32-176`, `src/vibemix/memory/index_sqlite_vec_memory.py:60-176` (\"sibling of `SqliteVecStore`\"), `src/vibemix/learn/band_share_store.py:51` (\"Mirror of library/embed_cache.py\") — gate: extract a shared `_vec0_base` mixin (load-guard + DDL + replace-workaround + Win-fallback probe) parameterized by table name + dim, and have all three subclass it — only after confirming the `memory` store's single-connection-for-vec+metadata constraint (`index_sqlite_vec_memory.py:14,39`) can be expressed in the shared base without breaking its moments-table coupling.\n\n- **Per-detector cooldown gate idiom is copy-pasted across 7+ `state/detectors/*.py` (single constant, duplicated logic)** — `[HOLD_WITH_GATE]` — the cooldown *source of truth* is correctly centralized (`MIN_EVENT_GAP_PER_TYPE` in `audio/constants.py`), but every detector re-writes the same gate body: `cooldown = MIN_EVENT_GAP_PER_TYPE[\"X\"]; if now - self.last_event_at < cooldown: return None` plus a baseline-rotation comment. That's logic duplication, not data — a fix to the gate semantics (e.g. baseline-rotation-on-cooldown) must be hand-applied 7 times and they already diverge (kick_swap/distortion rotate baseline on cooldown; sub_layer/kick_density deliberately don't). — evidence: identical gate at `src/vibemix/state/detectors/acid_line_entry.py:96-97`, `kick_swap.py:117-118`, `distortion_climb.py:113-114`, divergence noted `sub_layer_arrival.py:108-109` vs `kick_swap.py:115-116` — gate: extract a `CooldownGate` helper / base-detector method taking the event-type key + a `rotate_baseline: bool` flag, so the divergence becomes an explicit parameter rather than a silently-different copy — only after the per-detector `last_event_at` ownership and baseline-rotation hooks are confirmed uniform enough to share.\n\n- **`__main__.py` is a 7,183-line monolith mixing CLI dispatch, IPC handler registration, and library/embed orchestration** — `[RESEARCH_WITH_WORKFLOW]` — this is the root maintainability liability behind several duplications above: the library CLI commands (`_cmd_library_embed_folder`, `_cmd_library_ingest`), the IPC handlers (`ipc.library.import`), and library helpers (`_library_genai_client`, `_library_cache_probe`, `_library_model_gap_hint`) all live in one file alongside the async orchestrator. CLI and GUI library paths diverge partly because they're tangled in the same megafile rather than sharing a `library/cli.py` + `library/service.py` seam (note: there is NO `library/cli.py` despite the per-subcommand structure). — evidence: `src/vibemix/__main__.py` 7,183 lines; library CLI at `:2760,3213,6483,6548`, IPC at `:2066-2067`, helpers at `:3216,3967,4428,6792` — gate: scope a Workflow to carve a `library/cli.py` (argparse subcommands) + `library/service.py` (the embed/ingest/import service both CLI and IPC call) out of `__main__.py`, which also resolves the importer-vs-ingest_source duplication by giving both a single service entry. Research-gate because the file is the live orchestrator and the concurrent-session collision risk on `__main__.py` is high — needs an isolated branch + careful staging, not an inline edit.\n\n- **`line_voice.py` / `synthesize_line` is a live demo seam, NOT orphaned (verified)** — `[SAFE_NOW]` — flagged-then-cleared: it has zero `src/vibemix/` runtime callers but IS wired into `scripts/automix_demo_smoke.py` (the viral auto-mix reel) and unit-tested. `build_default_line_adapter` correctly delegates to `build_tts_chain(mode=\"direct\")` (no parallel voice build). Keep as-is; just be aware it's the one consumer of the now-vestigial `mode=` param — if you drop `mode` from `build_tts_chain`, update `line_voice.py:72`. — evidence: `src/vibemix/agent/line_voice.py:67-72`, callers `scripts/automix_demo_smoke.py:57,61,65`, `tests/agent/test_line_voice.py:19` — gate: none\n\n- **Cue exporters are 4 distinct format targets, not duplicates (verified — justified split)** — `[SAFE_NOW]` — `export_rekordbox.py` (collection.xml: order+cues+beatgrid), `export_serato.py` (Markers2 in-file carrier for Serato/Mixxx), `cue_export.py` (non-destructive Rekordbox round-trip for AI-placed cues), `cue_folder.py` (folder→auto-cue→Rekordbox bridge that *reuses* `export_rekordbox.export_set`). Each carries a different on-disk format; `cue_folder` already reuses rather than re-implements. No consolidation needed. — evidence: `src/vibemix/library/export_rekordbox.py:1-10`, `export_serato.py:1-8`, `cue_export.py:1-10`, `cue_folder.py:1-8` (\"emit the exact track-dict shape `export_rekordbox.export_set` already consumes\") — gate: none\n\n- **`index_numpy.py` vs `index_sqlite_vec.py` is the documented cross-platform parity split (justified)** — `[SAFE_NOW]` — `store.py` selects `NumpyStore` (Windows / no-sqlite-vec) vs `SqliteVecStore` (macOS) with bit-identical top-K parity as a tested invariant per CLAUDE.md. This is a deliberate platform backend, not accidental duplication. Note this is separate from (and not to be confused with) the cross-*package* vec0 duplication flagged above. — evidence: `src/vibemix/library/store.py:30,163`, CLAP/sqlite-vec parity note in CLAUDE.md `library/` block — gate: none",
      "I now have a complete, evidence-anchored picture across all silos. I have enough to synthesize the coherent-first-product audit. No further exploration needed.\n\n## coherent-first-product\n\n> **Synthesis basis:** the 282-cap inventory (`CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md`, HEAD `173399ae`), the drift audit (`CLAUDE_LAST_DELTA_DRIFT.md`), the open-question ledger (`CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md`), and the cohost grounding/TTS verification (`CODEX_VERIFICATION-cohost-grounding-tts-tags.md`). Lens = the MINIMUM coherent feature set that is honest + grounded + maintainable + demoable across Free / €4.99 Pro / €9.99 Studio.\n\n### The coherent v1 spine (what must be TRUE end-to-end)\n\n- **The v1 spine = [grounded co-host speaks via MOSS] + [Viber/library set-prep] + [one honest live proof]** — `[SAFE_NOW]` (as a named scope decision) — These are the only two capability families that are both deeply-built AND honest-by-construction: the perception→event→grounded-prompt→reaction loop, and the Codex/CLAP library+curation path. Everything else (beatmatch grading, Mixxx OSC, screen vision, deck-pair identity, Learn-Mastered live credit) is either a HOLD or CLAIMED_BUT_ABSENT and must sit OUTSIDE the v1 spine. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:19` (state-of-product), §cohost-brain `:122`, §viber-curation `:172` — gate: none\n\n- **BLOCKER #1 — the shipped product cannot speak on any machine but Kaan's** — `[HOLD_WITH_GATE]` — This is the single most consequential finding across all 12 silos and the hardest gate on a \"stranger hears the co-host\" claim: MOSS is neither bundled in the DMG nor auto-downloadable (no hosted URL). A fresh machine boots voice-muted. The co-host spine is otherwise excellent, but a mute co-host is not a coherent product. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:262` (no `moss-tts-onnx` in `vibemix-core.macos.spec` datas), `:263`/`:399` (`model_assets.py:571` `moss_model_installable()` true only if `VIBEMIX_MOSS_TTS_ARCHIVE_URL` set; `install_moss_model:576` errors \"not hosted by default\") — gate: bundle the `moss-tts-onnx` weights into the spec `datas` OR host the archive + pin `VIBEMIX_MOSS_TTS_ARCHIVE_URL`/SHA/size so `install_moss_model` has a real default. ONE of these MUST land before any public/monetized claim. This is task #1 on every board.\n\n- **BLOCKER #2 — shipped DMG is 18 src-commits stale and arm64-only** — `[HOLD_WITH_GATE]` — The signed/notarized/Gatekeeper-accepted DMG is real but applies to a STALE artifact (`629de2e8`, HEAD `173399ae`) that predates English-only, citation-out-of-TTS, MOSS-only-mute, and the grounding hardening — i.e. the demoable binary does not contain the honesty fixes that make v1 coherent. No Intel/Universal2 artifact exists despite the CI matrix claim. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:261` (18-commit gap), `:259` (notarytool ticket `arch:arm64` ×168, ZERO x86_64) — gate: rebuild+sign+notarize the DMG from clean HEAD (after BLOCKER #1), AND either run the CI x86_64 leg for a proven Intel artifact OR formally scope v1 to Apple Silicon in launch copy.\n\n- **BLOCKER #3 — zero captured live-rig proof of the perception loop** — `[HOLD_WITH_GATE]` — The grounded-reaction spine, the live Vibe Judge, `_credit_live_skill_demo`, the cohost-reaction citation chip, and the next_suggestion pill frame are all unit/synthetic-proven only — never captured firing together on a real rig. \"Real DJ friend in your ear, no AI slop\" is unproven until ONE real event→registry-citation→grounded-reaction→(optional skill credit) artifact exists. This is the product's core value claim. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:139` (Judge downgraded to SOURCE_ONLY — every cited test synthetic), `:47` (cohost-reaction chip WIRED_BUT_UNPROVEN), `:209` (next_suggestion frame unconfirmed on wire); ledger Q11 `CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md:66` (drive-vibemix capture promotes the whole cluster at once) — gate: ONE `drive-vibemix` capture firing a real LAYER_ARRIVAL/MIX_MOVE/TRACK_CHANGE, asserting the EvidenceRegistry citation resolved and a grounded reaction emitted. This single artifact promotes a large WIRED_BUT_UNPROVEN cluster.\n\n### What is honest + maintainable + IN the spine today\n\n- **Co-host anti-slop spine is honest-by-construction and tightening** — `[SAFE_NOW]` — The grounding gates that make \"no AI slop\" a real contract are all source-correct and unit-proven: single-writer MusicState, EvidenceRegistry, the response-level CitationLinter (citation-strip→silence, no ack-bank fallback), the slop filter, English-only runtime guard, and TTS citation sanitizer. The last drift pass confirms zero HOLD-bleed and that the two prior MEDIUMs (English-only unenforced, citation atoms in TTS) are RESOLVED at HEAD. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:132` (CitationLinter), `:137` (`language_guard.py`), `:164` (`tts_sanitizer.py`); `CLAUDE_LAST_DELTA_DRIFT.md:96` (tests/repo fully GREEN, 364 passed, no bleed) — gate: none\n\n- **MOSS-only TTS enforcement holds with zero active fake fallback** — `[SAFE_NOW]` — Both direct and proxy chains resolve to `_build_moss_chain`; all cloud keys are ignored; a missing model mutes (raises `LocalTTSUnavailable`→muted `NOT_GIVEN`) rather than falling back to a fake voice. This is exactly the \"no fake spoken fallback — abstain honestly\" posture the product requires. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:152` (`tts_chain.py:33-72`), `:162` (`_build_tts_chain_or_mute`) — gate: none\n\n- **Viber/library set-prep is the coherent Pro/Studio monetization surface and is fully wired** — `[SAFE_NOW]` — The Codex-backed Viber path (CLI → Rust Tauri commands → frontend) is wired end-to-end with the seen-set grounding gate at the tool boundary; the Crate GUI (incl. cue-export) is wired both ends; the freshness watcher is armed. This is the natural home for the €4.99 Pro / €9.99 Studio tiers (set-prep, curate, build-set) — it is the paid value that is also honest and demoable. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:172` (silo summary), `CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md:57` (15 MCP tools wired, GUI cue-export wired, watcher armed) — gate: none for source-coherence; a single real `codex exec` capture (BLOCKER #3 sibling) would promote it off SOURCE_ONLY.\n\n- **CLAP retrieval + cue heuristic are among the only TRUE end-to-end proofs — keep them in the spine** — `[SAFE_NOW]` — Mean-centered cosine retrieval is proven on REAL committed CLAP vectors (raw ~0.85 → centered ~−0.15; recall@1 floor 0.5), and the cue heuristic is proven on real audio (label_recall=1.0, 0 timing misses). These are the load-bearing \"library actually works\" proofs under the Viber surface. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:204-205` (`test_clap_real_retrieval.py`), `:212` (`test_cue_detect_eval.py`) — gate: none (grow fixture to 12-20 clips is a hardening nicety, not a blocker)\n\n- **Tauri shell folds 5 surfaces with no dead buttons — the demoable container is coherent** — `[SAFE_NOW]` — DesktopShell mounts deck/crate/learn/debrief/settings into one keep-alive window; every user-facing control routes through a both-ends-wired ipc.* type or registered Tauri command; mascot is opt-in (default Pill). The frontend is unit-proven (jsdom), not live-captured, but no dead-button/blank-panel risk was found. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:38` (app-ui silo), `:42` (sidebar nav proven), `:54` (pill overlay) — gate: none for coherence; live `cargo tauri dev` capture is the same artifact as BLOCKER #3.\n\n### What is OUTSIDE the v1 spine (do not ship, do not market as present)\n\n- **Beatmatching \"Mastered\" / BEATMATCH_GRADED — keep behind the honest mask, OUT of v1** — `[HOLD_WITH_GATE]` — The consumer is wired but there is NO production emitter; it is honestly capped at Competent (`live_creditable=False`). \"Mastered\" is the headline anti-slop claim, so a phantom/late grade is the single worst false-expertise slop. Building the H1 Practice-Deck producer needs real audio OutputStream + live FLX4 hardware + on-rig proof — not v1, not read-only-buildable. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:246`/`401` (BEATMATCH_GRADED no emitter), `:336`/`402` (H1 `practice_loop.py` absent); ledger Q9 `CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md:87` (HOLD, reality-pins stay green) — gate: real audio OutputStream + live FLX4 `rate_b`/`xfader` mapping + on-rig LOCKED-grade proof + grounding-review PASS on the Mastered vocal; only then clear `_HONEST_UNCREDITABLE_V11`.\n\n- **DROP-call live hype line — engineering done, proof not; OUT of v1 until gated** — `[HOLD_WITH_GATE]` — Correct in shape (deterministic fixed-text say, opt-in flag, green unit tests) but WIRED_BUT_UNPROVEN: zero captured proof it lands ON the beat against a real drop, and a fire on a phantom predicted-drop is the #1 anti-slop release blocker. The activation block is dirty-only (uncommitted) — keep it out of any signed build. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:145` (WIRED_BUT_UNPROVEN); ledger Q10 `CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md:93-96` (three-part gate) — gate: (1) grounding-review PASS, (2) captured on-beat live proof inside the 2.0s arm window with populated section_source, (3) zero phantom-fire over the run. Plus build-hygiene: never whole-file `git add` `coach.py`/`__main__.py` (sweeps ungated DROP speech into a binary).\n\n- **Screen vision as a live source — DROP the live leg, keep it dormant-by-design** — `[DROP]` — Fully dead-wired at runtime (`screen_jpeg = None` hardcoded anti-hallucination gate; vision_reader never injected into live DeckPoller), yet still spawns a 1Hz capture loop + ScreenCaptureKit permission prompt for a buffer nothing reads, with a misleading \"screen vision is not currently resolving the second deck\" blocker string. Re-enabling it as a live source would re-open the v4 hallucination class. — evidence: ledger Q5 `CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md:70-73` (`dj_cohost.py:2192-2199` None gate, `__main__.py:2434-2438` no vision args) — gate: none to DROP from v1; quarantine package = gate the `screen_task` spawn behind off-by-default `VIBEMIX_DECK_VISION` so the permission prompt/CPU don't fire (do NOT touch the `None` anti-hallucination invariant).\n\n- **Mixxx H1-H8 / OSC / mixxxdb integration — entirely absent, do NOT market \"works with Mixxx\"** — `[DROP]` (from v1 scope) — Every Mixxx-goldmine producer (H1-H8, beatgrid detect, WSOLA, M4 db writer, M5 OSC backend) is CLAIMED_BUT_ABSENT — math ports are real and tested but no live producer exists, and stock Mixxx has no OSC read path. Marketing Mixxx support before the fork merges would be dishonest. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:336-346`/`402-412`, `:345-346`/`412` (M4/M5 absent, OSC dishonest-to-claim) — gate: none — these are post-v1 milestones, not launch claims.\n\n- **Windows deck-pair grounding is macOS-only today — scope v1 platform honestly** — `[HOLD_WITH_GATE]` — `_audio_windows.py` is WASAPI master-loopback (stereo global mix) only; no per-deck `DeckAudioCapture`. Live deck-pair grounding + the live Judge are macOS-only. The base co-host (master listen + grounded reaction) still works on Windows; only the deck-pair/Judge live-proof features don't. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:294`/`413` (NOT-FOUND, master-loopback only) — gate: either spike multichannel WASAPI capture OR explicitly scope deck-pair grounding + live Judge as macOS-v1-only in launch copy (CLAUDE.md says Windows is a v1 platform — this gap must be resolved before a Windows release claim).\n\n### Coherence-hardening (in-spine cleanups that make v1 honest + maintainable)\n\n- **Delete the dead cloud-TTS seams so MOSS-only is unambiguous** — `[SAFE_NOW]` — The Gemini-native TTS class (+ its only-caller test), the OpenRouter monkey-patch, and the orphaned `CARTESIA_*` config constants are import-only residue with ZERO live call-sites. Removing them makes the MOSS-only mandate self-evident in source and removes a maintainability/audit hazard. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:168` (`_livekit_google_slim.py:33-36`), `:169` (`tts_chain.py:23` patch), `:170`/`414` (`config.py:41-54` orphaned) — gate: none\n\n- **Fix the two stale-golden / CI-drift tests so the honesty boundary IS the asserted contract** — `[SAFE_NOW]` — The HEARTBEAT golden (`test_coach.py:723` asserts an old string; code was hardened) and `test_mastered_demo_reaches_the_envelope_wall` are stale fixtures, not code bugs — but a green suite where the asserted strings match the hardened output is what makes \"the boundary is the contract\" true. (The earlier tests/repo RED is already fixed by `d321eef5`.) — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:151` (HEARTBEAT golden stale), `:427` (both drifts); `CLAUDE_LAST_DELTA_DRIFT.md:96` (tests/repo now GREEN) — gate: none\n\n- **Delete 5 dead `ipc.library.*` ws types + refresh the stale Viber CORRECTION packet** — `[SAFE_NOW]` — Five `ipc.library.*` schema types have zero UI call-sites (dead), and the `viber-capability-CORRECTION.md` packet's old #1/#2 gaps (cue-GUI, watcher) are now RESOLVED-at-HEAD — anyone acting on the stale packet would rebuild a path that already exists. — evidence: ledger Q7 `CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md:59` (rg `ipc.library.` empty in UI); INDEX note `INDEX.md:28` (conditionally both-ended — reconcile the `if ipc_router is not None` gating before deleting) — gate: after removing schema entries run `npm run codegen:ipc`; `cd tauri/ui && npm run build && npm test` green + `rg \"ipc.library.\" tauri/ui/src` empty.\n\n- **Fix stale doc-anchors pointing at non-existent `debrief/ln.py`** — `[SAFE_NOW]` — Three docs (EAR-TEST-PROTOCOL.md, KAAN-ACTION-LEGAL.md, debrief.html) reference `debrief/ln.py` which does not exist (the symbol `write_ear_test_log` lives in `ear_test_capture.py`). Doc/code drift undermines the \"honest\" bar and confuses future work. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:376`/`415` (NOT-FOUND) — gate: none\n\n- **Do NOT ship the `library budget` under-€50 / \"free at scale\" claim** — `[HOLD_WITH_GATE]` — `test_monthly_projection_under_50_eur` is XFAIL — the cost model does not currently prove the €50/mo gate, and the live-stack study confirms TTS cost dominates (which MOSS-local now changes, but the model isn't reworked). A monetization page asserting free-at-scale economics would be a CLAIMED_BUT_ABSENT honesty gap. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:222`/`416` (XFAIL) — gate: rework the cost model (cue-anchored single-region / server-side dedup, and re-baseline now that MOSS is local-free TTS) and flip the XFAIL to a passing gate before any cost claim ships.\n\n### Bottom line\n\n- **The coherent v1 = (a) co-host that SPEAKS grounded via MOSS on a stranger's machine, (b) Viber/library set-prep as the paid Pro/Studio surface, (c) ONE captured live proof of the perception loop — all behind the existing honest gates** — `[SAFE_NOW]` (as the scope decision) — The engineering for the spine largely exists and is honest; what stands between source-coherent and shippable-monetized is THREE proof/packaging gates, not new invention: MOSS-on-every-machine (BLOCKER #1), fresh signed binary (BLOCKER #2), one live-rig capture (BLOCKER #3). Everything labeled HOLD/CLAIMED_BUT_ABSENT (beatmatch Mastered, DROP-call, screen-vision, Mixxx, Windows deck-pair) must be explicitly OUT of the v1 boundary and absent from launch copy. — evidence: `CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:418-426` (top packages: MOSS, rebuild, live capture, codex capture rank 1-4) — gate: ship-gate = BLOCKER #1 ∧ #2 ∧ #3 cleared; market-gate = no HOLD/ABSENT capability named as present.",
      "API Error: Server is temporarily limiting requests (not your usage limit) · Rate limited",
      "Key drift confirmed: **H2 (`from_anlz`) is already built and tested** but has no production caller (only `library/ingest.py` uses `anchors_from_anlz` for cues, not `BeatGrid.from_anlz`). And **`MiniDeck.seek()` does NOT exist** — only `render_block` and `state`, confirming the practice-loop precondition gap. This sharpens the bucketing significantly. I have enough verified evidence to synthesize.\n\n## mixxx-goldmine-leverage\n\nThe Mixxx goldmine maps (`.planning/packets/2026-06-01/recovered/wf_c297207c-6ae__the-mixxx-goldmine-map.md`, `wf_d9ddbeed-f5f__...round-2-dsp-performance-ux.md`) are GPL learn-from-spec research. I verified the load-bearing claims against HEAD. The 1-3 ideas that most cheaply close a real grounding/slop gap, ranked, plus the GPL vendoring flags:\n\n- **Loop/beatjump R-SLOP-immune rule (`state/loop_geometry.py` + beatgrid-exact atom)** — `[SAFE_NOW]` — The single cleanest slop kill in the whole goldmine: a loop/beatjump effect (\"held a 4-beat loop for 8 bars, then +8 into the phrase\") is *deterministically* provable from MIDI timestamp + beatgrid alone — the two highest-trust evidence sources — so it grounds where every fader/EQ causal claim today strips to the ack-bank. Producer module is genuinely absent and the capture path already exists but only emits two bare buttons. — evidence: `state/loop_geometry.py` NOT-FOUND (verified absent); loop capture is `midi/state.py:395-400` (`loop_in_hit`/`loop_out_hit`, no beat-size); guard entry `deck_context.py:2308 apply_live_claim_guard` + humility rule `:2384 dsp_delta_not_causal_proof`; learn-from `loopingcontrol.cpp:1416/27`, `beats.cpp:504` — gate: none — but route loop-rolls through the no-dedup `_record_event` path (`midi/state.py:247`), the `_record_move` 400ms same-label dedup collapses a rapid roll-stack.\n\n- **Practice-Deck Loop (H1 keystone — `learn/practice_loop.py` driving MiniDeck → grade_beatmatch → BEATMATCH_GRADED)** — `[HOLD_WITH_GATE]` — The highest-scored goldmine idea (19/20) and the truest moat: it converts *test-enforced dead code* into the first measured-mastery competency. I verified the orphan chain exactly — `grade_beatmatch` has ZERO production callers (only tests + a pin asserting orphan-status), and the recognizer's credit branch is live and waiting. But it is NOT a quick win: `MiniDeck` has no write path (only `render_block`/`state`), so a `seek()` + cursor accessors must be built first, and the loop puts the engine on a multi-day path. — evidence: zero prod callers of `grade_beatmatch` (`tests/repo/test_live_reality_pins.py:59` pins it, only test call-sites); `BEATMATCH_GRADED` branch live at `skill_recognizer.py:154`; payload helper `beatmatch_judge.py:146 grade_to_event_extra`; `MiniDeck.seek` NOT-FOUND (only `miniplayer.py:130 render_block`, `:137 state`) — gate: build `MiniDeck.seek()` + cursor accessors FIRST (additive, render_block untouched), then drive the loop through `vibemix-grounding-review` (touches Invariant #2/#3); ship the rate-writer scripted, not from a live FLX4 read.\n\n- **Wire existing `xfade.py` into the live R-SLOP guard (#5 Move-Effect Resolver)** — `[SAFE_NOW]` — Upgrades the crossfader path from correlation to causation (does the audio move in the DIRECTION the curve law predicts) by reusing a module that ALREADY exists and is Apache-clean — no new GPL surface, no new dep. I confirmed `xfade.py` is imported by 37 modules but NOT by `deck_context.py` (the live guard), exactly as the map claims. The two missing laws (dB-taper fader, pregain/headroom) are ~12 lines each from public cookbook math. — evidence: `audio/xfade.py` exists; zero `xfade` import in `deck_context.py` (verified); guard cross-checks already present at `deck_context.py:2236 _supports_grounded_transition_verdict`; `band_energy_ratios` per-lane at `band_features.py:30` — gate: none for the wiring; predict-on-lane / measure-on-lane only (never master-sum), and keep `dsp_delta_not_causal_proof` humility — sign-agreement upgrades the gate, it does not license a bare causal verdict.\n\n**Two corrections to the goldmine maps the next session must not re-do (DROP as proposed):**\n\n- **`BeatGrid.from_anlz()` (H2) — proposed BUILD_NOW, but ALREADY BUILT** — `[DROP]` as a build item — The Round-1 map lists `from_anlz` as a ~15-line BUILD_NOW; it is fully implemented and tested at `grid.py:39` with downbeat-anchoring + BPM fallback. The real remaining gap is a thin one — it has no *production* caller (only `library/ingest.py` uses `anchors_from_anlz` for cues), so it's wiring-into-the-practice-loop work, not a new module. — evidence: `audio/grid.py:39 def from_anlz` (full body present); tests `tests/audio/test_grid.py:60-97`; no prod caller of `BeatGrid.from_anlz` — gate: none (fold into the H1 practice-loop wiring, do not re-author).\n\n- **Crossing Detector / \"crossed the live fader value\" (#3) and 14-bit precision** — `[DROP]` the un-computable framing — vibemix is a PASSIVE listener with no read of the DJ software's internal control value, so \"crossed the host's live value\" and \"tempo to within a cent\" are physically un-computable and would ship the exact confident-but-blind slop class the round exists to kill. The crossing *math* is reusable only reframed to the controller's OWN timeline + audio-delta corroboration. — evidence: guard reads MIDI knob tier not host state (`deck_context.py:2412 _has_unsupported_mixer_low_kill_claim`); map's own §4 self-correction — gate: if pursued, reframe to \"deliberate grab vs jitter on the controller timeline, corroborated by measured per-deck delta\"; CUT the 14-bit/sub-BPM precision claim.\n\n**GPL vendoring risk (flag, do not skip):** every item above is clean-room reimplement-from-spec — the standing `xfade.py` / `transition_clock.py` / `beatmatch_judge.py` ports are the in-repo precedent. Hard vendoring traps to refuse: Mixxx's Bessel-4 IIR coefficients (`enginefilterbessel4.cpp` — use rFFT band-sum instead), the qm-dsp beat tracker and `GetKeyMode` key core (external GPL, not even in the checkout — reimplement from QM-DSP / Krumhansl-Schmuckler papers or use MIT Beat-This ONNX), RubberBand/SoundTouch keylock (reimplement WSOLA from the 1993 Verhelst-Roelands paper), and community `.js` mapping scripts (GPL — harvest only non-scripted `.xml`). For loudness, source ITU-R BS.1770, not Mixxx (it wraps MIT libebur128). None of the three lead items need torch/transformers/librosa or a new third-party dep.\n\n**The honest-ship shortener:** of the three, the **loop-geometry rule** and the **xfade-into-guard wiring** are the two that most cheaply close a real R-SLOP gap *without* the multi-day practice-deck build — both are SAFE_NOW, ship as evidence under the existing humility rule, and directly attack the named #1 live slop bug. The practice-loop (H1) is the bigger moat but is gated behind a `MiniDeck.seek()` precondition I verified is missing — schedule it as a milestone, not a quick win."
    ],
    "critic": "I have what I need. Critical new finding confirmed: a `NOTICE`/`THIRD_PARTY` attribution file is ABSENT despite Mixxx clean-room ports carrying inline GPL-derivation comments — an Apache-2.0/GPL legal honesty gap no lens owned. And the proxy `build_proxy_tts_chain` discards `(jwt, proxy_base_url, voice)` confirming the keyless path is a documented dead shim. Both the LLM-brain proxy path and TTS are scrubbed in output but the structure is clear. Writing the critic section.\n\n## Critic — gaps\n\n- **No `NOTICE` / `THIRD_PARTY_LICENSES` file despite Mixxx GPL clean-room ports carrying explicit derivation comments** — `[HOLD_WITH_GATE]` — This is a legal/honesty gap NO lens owned. `xfade.py`, `beatmatch_judge.py`, and `transition_clock.py` carry `SPDX: Apache-2.0` headers yet inline-document that the math is \"ported verbatim from [Mixxx] sync (scar dossier 07)\" / \"Clean-room port of [Mixxx] EngineXfader\". Clean-room reimplementation from spec is legally defensible, but shipping an Apache-2.0 binary that embeds GPL-*derived* algorithms with zero attribution/NOTICE file is exactly the audit liability that gets an OSS launch roasted — and the mixxx-goldmine lens proposes adding MORE such ports (loop_geometry, xfade laws) without flagging the missing attribution scaffold. — evidence: `head -3 src/vibemix/audio/xfade.py` (Apache SPDX + \"Clean-room port of [Mixxx] EngineXfader\"), `beatmatch_judge.py` (\"ported verbatim from [Mixxx] sync\"); `ls NOTICE* THIRD_PARTY*` = NOT-FOUND — gate: before any public release, add a `NOTICE` documenting each clean-room-from-spec derivation (Mixxx algorithms, paper citations for WSOLA/key-detection if built) and get a one-line legal-posture sign-off from Kaan; the goldmine lens's new ports must land WITH their NOTICE entry, not after.\n\n- **Cross-silo interaction: `demo_mode` is BOTH an orphan (built-not-surfaced lens) AND a fake-flow surface (valuable-but-drop lens) — the two findings COMPOUND into a hard DROP** — `[DROP]` — The built-not-surfaced lens hedges (\"wire it to a Demo entry-point OR delete\") and valuable-but-drop calls it philosophically adverse. No lens connected them: a scripted canned-reaction sequencer is not just an orphan, it is the single capability whose *purpose* directly violates Invariant #3 (\"trust the audio, never scripted\"). Wiring it to a marketing-demo entry-point would ship a surface that fakes the exact \"real DJ friend\" reactions the product's anti-slop bar forbids — so the \"OR wire it\" option the first lens leaves open is not actually available. Delete is the only honest call. — evidence: `runtime/demo_mode.py` (zero live importers, verified both lenses); Invariant #3 (CLAUDE.md cardinal invariants) — gate: none (delete module + test; if a demo is wanted, it must replay REAL captured session events, not a hand-authored `DEMO_SEQUENCE`).\n\n- **The keyless/proxy ship path is a DOCUMENTED-DEAD SHIM, yet B2 memory + coherent-first lens treat proxy as the monetization spine's delivery** — `[HOLD_WITH_GATE]` — Cross-silo gap: the coherent-first lens names \"stranger hears the co-host\" as the ship gate and memory `project_vibemix_proxy_deployed_altidus` calls the keyless proxy \"LIVE-VERIFIED\", but at HEAD `build_proxy_tts_chain` explicitly discards `(jwt, proxy_base_url, voice)` and returns local MOSS (`proxy_client.py:53 _ = (jwt, proxy_base_url, voice)`), and the LLM-brain proxy path still hard-defaults to `api.altidus.world`. So in proxy mode TTS is local-MOSS (fine, matches MOSS-only) but the BRAIN still round-trips a proxy that memory flags as needing a live Gemini key — meaning BLOCKER #1 (MOSS on every machine) does NOT by itself make a stranger's co-host speak words; the proxy brain must also be live. No lens stated that MOSS-bundling and proxy-brain-liveness are TWO independent ship gates, not one. — evidence: `proxy_client.py:49-53` (TTS args discarded → MOSS), `__main__.py` proxy-brain block (defaults `api.altidus.world`, requires JWT); memory `project_vibemix_proxy_deployed_altidus` (needs valid Gemini key) — gate: confirm the proxy BRAIN passthrough is live with a working upstream key AND MOSS is reachable, as two separate checkboxes, before any \"stranger hears it\" claim.\n\n- **Idle co-host auto-speaks every 45s (HEARTBEAT) — an unaudited slop-surface no lens examined for grounding** — `[HOLD_WITH_GATE]` — Every lens audited event-driven reactions; none audited the IDLE heartbeat path, which auto-speaks every `HEARTBEAT_SEC=45.0` with no music event to ground to. A heartbeat utterance fired into silence (or worse, fired during the BlackHole-feedback phantom-music condition documented in CLAUDE.md) is a prime \"talking when nothing happened\" slop vector — exactly the late/scripted/generic failure the product bar forbids — and it is on by default. — evidence: `audio/constants.py:65 HEARTBEAT_SEC = 45.0`, `:84` HEARTBEAT in per-type cooldown map; CLAUDE.md (\"Idle co-host auto-speaks every HEARTBEAT_SEC=45\") — gate: run `vibemix-grounding-review` against the HEARTBEAT branch specifically — confirm idle heartbeat lines are ack-bank/persona-only (no invented event claims) and cannot fire during the phantom-music/feedback-loop condition; if they can emit a causal claim with empty evidence, that is a release blocker the coherent-first spine must close.\n\n- **Loop-roll dedup interaction: the mixxx-goldmine lens's loop-geometry rule routes through a 400ms same-label dedup that WILL collapse the evidence it depends on** — `[HOLD_WITH_GATE]` — The mixxx lens flagged this in passing (\"route through the no-dedup `_record_event` path\") but understated it as a footnote. Verified: `_record_move` dedups same-label-within-400ms (`midi/state.py:226`), and loop hits already route through `_record_move` (`:398-400 loop_in_hit/loop_out_hit`). A 4-beat loop roll at 128 BPM = 4×(60/128) = 1.875s for a 4-beat loop but a *roll* re-triggers every 1/8 or 1/16 beat = ~58-117ms intervals — i.e. INSIDE the 400ms window — so the dedup silently swallows the rapid re-triggers that constitute the roll, leaving the proposed grounded loop-claim with collapsed/missing evidence. This is a cross-silo gap: a SAFE_NOW grounding win (mixxx lens) is gated on a MIDI-plumbing bug (state lens didn't own loops). — evidence: `midi/state.py:226` (dedup same-label-within-400ms), `:398-400` (loop hits via `_record_move`); 1/16-roll interval ≈58-117ms < 400ms — gate: before claiming loop-geometry as SAFE_NOW, route loop/beatjump events through a no-dedup path OR make the dedup label-unique-per-beat-size; add a unit test asserting a rapid roll-stack survives to the evidence registry.\n\n- **Unaudited area: the co-host has no in-app way to tell the user WHY it is mute — a silent-failure UX that compounds BLOCKER #1** — `[HOLD_WITH_GATE]` — The built-not-surfaced lens noted `library doctor`/`models` are CLI-only; no lens connected that to the coherent-first BLOCKER #1 (fresh machine boots mute). The interaction: a stranger installs the DMG, MOSS isn't present, `_build_tts_chain_or_mute` raises `LocalTTSUnavailable` and returns muted `NOT_GIVEN` — and there is NO surfaced signal explaining the silence. The product appears broken, not \"needs a model download\". The Settings model-install button exists but is `[WIRED_BUT_UNPROVEN]`. A mute co-host with no in-app explanation is a worse first-run than a clear \"downloading voice…\" state. — evidence: `tts_chain.py` (`LocalTTSUnavailable`→muted `NOT_GIVEN`, no user-facing surface); Settings install button `library/index.ts:2006` unproven (built-not-surfaced lens) — gate: tie to BLOCKER #1 — whatever solves MOSS-on-every-machine MUST surface a first-run \"preparing voice / voice unavailable: [reason]\" state in the Tauri shell, and that surface must be live-captured (`drive-vibemix`) proving it appears when the model is absent.\n\n- **Honesty risk no lens owns: profile-gated taste/feedback persistence default-state vs CLAUDE.md's \"locked opt-in posture\"** — `[RESEARCH_WITH_WORKFLOW]` — The privacy/telemetry grep was harness-scrubbed, but it confirms a live path exists: pill feedback is written as profile-gated \"taste rows\" and read back for live transition scoring, behind a default-off profile flag. The observability-milestone memory (`project_observability_milestone_direction`) records an UNRESOLVED tension: \"aggressive-default-on COLLIDES with locked opt-in posture.\" No lens audited whether the SHIPPED default actually matches the opt-in posture CLAUDE.md mandates — a GDPR/OSS-roast exposure if a default-on data path ships under an opt-in claim. — evidence: `__main__.py` pill-feedback path (profile-gated taste-row write + readback, default-off per grep); memory `project_observability_milestone_direction` (UNRESOLVED privacy default); `VIBEMIX_RECALL_ENABLED` default-off (CLAUDE.md memory subpackage) — gate: scope a focused privacy-default audit (Workflow) enumerating every local-data-persistence path (taste rows, memory.db, events.jsonl, recordings index) and its default flag state, cross-checked against the opt-in posture, BEFORE launch; this is a Kaan decision-gate per the memory, not an autonomous call.\n\n- **Shortest-honest-ship blocker the lenses under-counted: the demoable binary at HEAD vs the honesty fixes is a HARD sequencing dependency, not three parallel blockers** — `[HOLD_WITH_GATE]` — Coherent-first lists BLOCKER #1/#2/#3 as a conjunction but presents them as independently-clearable. They are SEQUENCED: #1 (MOSS bundled/hosted) must land in SOURCE, then #2 (rebuild+sign+notarize from clean HEAD, currently 18+ commits stale at `629de2e8` per inventory) must consume that source, then #3 (live capture) must run against THAT rebuilt binary — capturing on a dev-source run (`drive-vibemix`) proves the engine but NOT the shipped artifact. The drift between \"engine works in dev-source\" and \"the signed DMG a stranger downloads works\" is the real last-mile, and no lens stated that #3's capture must ultimately be re-run against the post-#1/#2 signed binary to be ship-valid. — evidence: inventory `:261` (18-commit stale DMG), coherent-first BLOCKER cluster; `drive-vibemix` runs dev-source (skill description: \"dev-source mode\") not the frozen/signed sidecar — gate: ship-sequence = #1-in-source → #2-rebuild-from-HEAD → #3-capture-against-the-rebuilt-signed-binary (not just dev-source); a dev-source live capture is necessary but NOT sufficient for the ship claim.\n\n- **Unverified strong claim flagged for the record: the built-not-surfaced lens's \"Crate exposes search/similar/curate/build/cue/budget both-ends-wired\" rests on a single FE file read, not an ipc-wiring-checker pass** — `[HOLD_WITH_GATE]` — The lens asserts the Crate GUI consumers are \"both-ends-wired\" citing `tauri/ui/src/library/index.ts`, and uses that to NARROW the hidden-capability set — but a one-sided FE read does not prove the Rust command + Python handler ends are live. If any of those Crate actions is FE-only (button emits, no handler), the lens's central claim (\"the real built-but-unreachable set is narrower than 282 imply\") is overstated and a dead Crate button ships. — evidence: built-not-surfaced lens cites only `library/index.ts:1689/2006`; no `ipc-wiring-checker` run referenced — gate: run `ipc-wiring-checker` on each Crate action (search/similar/curate/build-set/cue-folder/budget) confirming sender + handler/consumer both exist before relying on \"Crate is fully wired\" to scope down the hidden-capability work.",
    "docMarkdown": "## Are we building the right thing?\n\n**Verdict: yes — but the project is carrying ~2-3x more surface than a coherent v1 needs, and the gap between \"engine works in dev-source\" and \"a stranger's downloaded DMG speaks grounded words\" is the entire remaining ship.** vibemix has two capability families that are deeply-built AND honest-by-construction: the grounded co-host (perception → typed event → evidence-cited prompt → MOSS reaction, gated by single-writer MusicState + EvidenceRegistry + CitationLinter + English-only guard) and the Viber/CLAP library set-prep path (Codex MCP tools + mean-centered retrieval proven on REAL vectors + cue heuristic proven on real audio). Everything else — beatmatch \"Mastered\", DROP-call hype line, screen vision, Mixxx OSC/db, deck-pair on Windows, mascot, demo_mode — is either WIRED_BUT_UNPROVEN, CLAIMED_BUT_ABSENT, or philosophically adverse to the anti-slop bar, and must sit OUTSIDE the v1 boundary and absent from launch copy.\n\n**The coherent-first-product spine = (a) a co-host that SPEAKS grounded via MOSS on a stranger's machine, (b) Viber/library set-prep as the paid Pro/Studio surface, (c) ONE captured live proof of the perception loop — all behind the existing honest gates.** The engineering for this spine largely exists and is honest. What stands between source-coherent and shippable-monetized is proof/packaging gates, not new invention.\n\n**The 3-5 things that actually block an honest ship:**\n1. **MOSS reaches every machine** — not bundled in the DMG, no hosted download URL; a fresh machine boots voice-mute (`install_moss_model` errors \"not hosted by default\"). A mute co-host is not a product.\n2. **Proxy BRAIN is live** — independent of #1: even with MOSS local, the keyless proxy brain still round-trips `api.altidus.world` and needs a working upstream Gemini key. MOSS-on-machine and proxy-brain-liveness are TWO ship gates, not one.\n3. **A fresh, non-stale signed binary** — the notarized DMG is 18+ src-commits stale (`629de2e8` vs HEAD `173399ae`) and arm64-only; it predates English-only, citation-out-of-TTS, and the grounding hardening. The demoable artifact does not contain the honesty fixes.\n4. **ONE captured live-rig proof of the loop** — the grounded-reaction chain, Vibe Judge, next_suggestion pill, and skill-credit are all unit/synthetic-proven only. \"Real DJ friend, no AI slop\" is unverified until one real event → registry-citation → grounded reaction is captured.\n5. **A NOTICE/attribution file for the Mixxx clean-room ports** — Apache-2.0 binary embedding GPL-*derived* algorithms (`xfade.py`, `beatmatch_judge.py`, `transition_clock.py` carry inline derivation comments) with zero NOTICE file is an OSS-launch legal liability.\n\nThese are sequenced, not parallel: #1-in-source → fresh rebuild → live capture against the *rebuilt signed binary* (a dev-source capture proves the engine, not the artifact).\n\n## Ranked implementation board\n\n| Rank | Item | Bucket | Why | Gate | Evidence |\n|------|------|--------|-----|------|----------|\n| 1 | Delete dead cloud-TTS seams (Gemini-native TTS class + only-caller test, OpenRouter import-patch, orphaned `CARTESIA_*` config) | SAFE_NOW | Makes MOSS-only provable by *absence*; removes the audit hazard where a reader sees \"Cartesia/Gemini TTS\" symbols and assumes a hidden cloud fallback | none | `_livekit_google_slim.py:33-36`, `tts_chain.py:23` (`AUDIO_STREAM_MODELS.add(OPENROUTER_TTS_MODEL)`), `tts_chain.py:41` (`_ = (...)` discard), `config.py:41-54` |\n| 2 | Add `NOTICE` / `THIRD_PARTY_LICENSES` for Mixxx clean-room ports + paper citations | SAFE_NOW | Apache-2.0 binary embeds GPL-*derived* math with inline \"ported from Mixxx\" comments and zero attribution — the exact thing that gets an OSS launch roasted | none (one-line Kaan legal-posture sign-off folds in) | `xfade.py` header (\"Clean-room port of [Mixxx] EngineXfader\"), `beatmatch_judge.py` (\"ported verbatim from sync\"); `ls NOTICE* THIRD_PARTY*` = NOT-FOUND |\n| 3 | Delete `runtime/demo_mode.py` + its test | SAFE_NOW | Zero live importers AND a scripted canned-reaction sequencer directly violates Invariant #3 (\"trust the audio, never scripted\") — the \"OR wire it to a demo\" option is not actually available | none (if a demo is wanted it must replay REAL captured events) | `runtime/demo_mode.py`; `rg \"demo_mode\\|DEMO_SEQUENCE\\|load_sequence\" src/vibemix` (excl. self+test) = EMPTY |\n| 4 | Fix two stale-golden tests (HEARTBEAT golden `test_coach.py:723`, `test_mastered_demo_reaches_the_envelope_wall`) | SAFE_NOW | Green suite where asserted strings match hardened output is what makes \"the boundary IS the contract\" true | none | inventory `:151`, `:427`; `CLAUDE_LAST_DELTA_DRIFT.md:96` |\n| 5 | Delete 5 dead `ipc.library.*` ws types + refresh stale Viber CORRECTION packet | SAFE_NOW | Five schema types have zero UI call-sites; the packet's #1/#2 gaps (cue-GUI, watcher) are RESOLVED-at-HEAD so anyone acting on it rebuilds existing paths | after removal: `npm run codegen:ipc`; `cd tauri/ui && npm run build && npm test` green + `rg \"ipc.library.\" tauri/ui/src` empty | ledger Q7 `:59`; INDEX `:28` (reconcile `if ipc_router is not None` gating first) |\n| 6 | Fix 3 stale doc-anchors → non-existent `debrief/ln.py` (symbol lives in `ear_test_capture.py`) | SAFE_NOW | Doc/code drift undermines the honesty bar | none | inventory `:376`/`:415`; `ls debrief/ln.py` = NOT-FOUND |\n| 7 | Rename `state/coach.py` → `ai_coach.py`/`prompt_builder.py` | SAFE_NOW | Two files named `coach.py` (`runtime.coach` loop vs `state.coach` `AICoach` prompt builder) confuses greps and risks an import-wrong-coach bug | none (6 importers to update) | `runtime/coach.py:1-30`, `state/coach.py:1-25`; importers `__main__.py:105`, `dj_cohost.py:90`, `bench/assemble.py:34` |\n| 8 | Surface auto-cue / smart-hot-cue export as a first-class Crate action | SAFE_NOW | The named MOAT, one of only ~4 truly end-to-end-proven caps (label_recall=1.0 on 6 real mp3s), but buried behind a raw folder-input no nav surface confirms a user finds | none | producer `cue_folder.py:178 export_cued_folder`; GUI hook only `tauri/ui/src/library/index.ts:1689` (`vmx-lib-cue-folder`) |\n| 9 | **BLOCKER #1 — bundle OR host MOSS weights** | HOLD_WITH_GATE | The single hardest gate: fresh machine boots voice-mute; a mute co-host is not a coherent product | bundle `moss-tts-onnx` into spec `datas`, OR host the archive + pin `VIBEMIX_MOSS_TTS_ARCHIVE_URL`/SHA/size so `install_moss_model` has a real default | inventory `:262` (no moss in `vibemix-core.macos.spec` datas), `model_assets.py:571/576` |\n| 10 | First-run \"preparing voice / voice unavailable: [reason]\" surface in the Tauri shell | HOLD_WITH_GATE | Tied to #1: a stranger installs, MOSS absent, `_build_tts_chain_or_mute` returns muted `NOT_GIVEN` with NO surfaced signal — looks broken, not \"needs a download\" | whatever solves #1 MUST surface a first-run readiness state, live-captured (`drive-vibemix`) proving it appears when the model is absent | `tts_chain.py` (`LocalTTSUnavailable`→`NOT_GIVEN`); Settings install button `library/index.ts:2006` `[WIRED_BUT_UNPROVEN]` |\n| 11 | **BLOCKER #2 — proxy BRAIN passthrough live** | HOLD_WITH_GATE | Independent of #1: proxy-mode TTS is local-MOSS (fine) but the brain still defaults to `api.altidus.world` needing a working upstream key — \"stranger hears it\" needs BOTH checkboxes | confirm proxy brain passthrough live with a working upstream key AND MOSS reachable, as two separate checks | `proxy_client.py:49-53` (TTS args discarded → MOSS), `__main__.py` proxy-brain block; memory `project_vibemix_proxy_deployed_altidus` |\n| 12 | **BLOCKER #3 — ONE live-rig capture of the perception loop** | HOLD_WITH_GATE | \"Real DJ friend, no AI slop\" is unproven; one capture promotes a large WIRED_BUT_UNPROVEN cluster (cohost-reaction chip, Judge, next_suggestion pill, skill-credit) at once | `drive-vibemix` capture of a real LAYER_ARRIVAL/MIX_MOVE/TRACK_CHANGE asserting EvidenceRegistry citation resolved + grounded reaction emitted; ultimately re-run against the rebuilt signed binary | inventory `:139` (Judge SOURCE_ONLY, all cited tests synthetic), `:47`, `:209`; ledger Q11 `:66` |\n| 13 | **BLOCKER #2b (sequencing) — rebuild+sign+notarize from clean HEAD** | HOLD_WITH_GATE | Signed DMG is 18+ commits stale and arm64-only; the demoable binary lacks the honesty fixes that make v1 coherent | rebuild from HEAD after #1; either run CI x86_64 leg for a proven Intel artifact OR scope v1 to Apple Silicon in launch copy | inventory `:261` (18-commit gap), `:259` (notarytool `arch:arm64` ×168, zero x86_64) |\n| 14 | Audit HEARTBEAT idle auto-speak (45s) for grounding | HOLD_WITH_GATE | No lens audited the idle path; a heartbeat fired into silence — or during the BlackHole phantom-music/feedback condition — is prime \"talking when nothing happened\" slop, and it's on by default | `vibemix-grounding-review` on the HEARTBEAT branch: confirm idle lines are ack-bank/persona-only (no invented event claims) and cannot fire during phantom-music; a causal-claim-on-empty-evidence is a release blocker | `audio/constants.py:65 HEARTBEAT_SEC=45.0`, `:84` in cooldown map; CLAUDE.md |\n| 15 | Loop/beatjump grounding rule (`state/loop_geometry.py` + beatgrid-exact atom) | HOLD_WITH_GATE | Cleanest slop kill in the goldmine: a loop effect is deterministically provable from MIDI-timestamp + beatgrid (the two highest-trust sources), grounding where every fader/EQ causal claim today strips to ack-bank | route loop/beatjump events through a no-dedup path OR make the 400ms dedup label-unique-per-beat-size (a 1/16 roll re-triggers at ~58-117ms, INSIDE the window — dedup swallows the evidence); unit test a rapid roll-stack survives to the registry | `state/loop_geometry.py` NOT-FOUND; capture `midi/state.py:395-400`; dedup `midi/state.py:226`; learn-from `loopingcontrol.cpp:1416` |\n| 16 | Wire existing `xfade.py` into the live R-SLOP guard (#5 Move-Effect Resolver) | HOLD_WITH_GATE | Upgrades crossfader claims from correlation to causation reusing an Apache-clean module already imported by 37 modules but NOT by the guard — no new GPL surface, no new dep | predict-on-lane / measure-on-lane only (never master-sum); keep `dsp_delta_not_causal_proof` humility — sign-agreement upgrades the gate, does not license a bare causal verdict | `audio/xfade.py` (zero `xfade` import in `deck_context.py`); guard `deck_context.py:2236 _supports_grounded_transition_verdict` |\n| 17 | Privacy-default audit of all local-persistence paths vs CLAUDE.md opt-in posture | HOLD_WITH_GATE | GDPR/OSS-roast exposure if a default-on data path ships under an opt-in claim; observability-memory records this tension UNRESOLVED | Workflow enumerating every persistence path (taste rows, memory.db, events.jsonl, recordings index) + its default flag, cross-checked vs opt-in — a Kaan decision-gate, not autonomous | `__main__.py` pill-feedback taste-row write+readback; memory `project_observability_milestone_direction`; `VIBEMIX_RECALL_ENABLED` default-off |\n| 18 | Don't ship `library budget` under-€50 / \"free at scale\" claim | HOLD_WITH_GATE | `test_monthly_projection_under_50_eur` is XFAIL; cost model doesn't prove the gate. A pricing page on it is a CLAIMED_BUT_ABSENT honesty gap | rework cost model (re-baseline now MOSS is local-free TTS) and flip the XFAIL to passing before any cost claim ships; keep `budget` internal-only meanwhile | inventory `:222`/`:416` (XFAIL); memory `project_live_stack_cost_and_capability_study` |\n| 19 | Verify Crate actions both-ends-wired before relying on \"Crate is fully wired\" | HOLD_WITH_GATE | The \"hidden-capability set is narrower than 282 imply\" claim rests on a one-sided FE read; if any Crate action is FE-only, a dead button ships | `ipc-wiring-checker` on each (search/similar/curate/build-set/cue-folder/budget) confirming sender + handler both exist | built-not-surfaced lens cites only `library/index.ts:1689/2006`; no `ipc-wiring-checker` run referenced |\n| 20 | Telegram \"connect bot\" affordance in Settings | HOLD_WITH_GATE | Fully built + privacy-scrubbed but ZERO `tauri/ui/src` reference — a complete tested capability invisible to users; one-line Settings pill surfaces it for near-zero cost | product call on whether Telegram is a v1 advertised feature; if yes add a token + chat-id pill, never auto-enable | `library/telegram_bridge.py`; CLI `__main__.py:6419`; `rg telegram tauri/ui/src` = no GUI |\n| 21 | Build the H1 Practice-Deck producer (`learn/practice_loop.py`) for BEATMATCH_GRADED | RESEARCH_WITH_WORKFLOW | The truest moat — converts test-enforced dead `grade_beatmatch` into the first measured-mastery competency — but `MiniDeck` has no `seek()`/write path; it's a multi-day build, not a wiring win | build `MiniDeck.seek()` + cursor accessors FIRST (additive, `render_block` untouched); drive the loop through `vibemix-grounding-review`; keep `_HONEST_UNCREDITABLE_V11=(\"beatmatching\",)` so it's Competent-capped until on-rig LOCKED-grade proof | `grade_beatmatch` zero prod callers (`test_live_reality_pins.py:59` pins orphan); `BEATMATCH_GRADED` consumer live `skill_recognizer.py:154`; `MiniDeck.seek` NOT-FOUND (only `miniplayer.py:130/137`) |\n| 22 | Carve `library/cli.py` + `library/service.py` out of the 7,183-line `__main__.py` | RESEARCH_WITH_WORKFLOW | Root maintainability liability behind the importer-vs-`ingest_source` duplication: CLI commands, IPC handlers, and library helpers all tangled in one megafile, so CLI and GUI paths diverge | scope an isolated-branch Workflow (high concurrent-collision risk on `__main__.py`) giving both CLI and IPC a single service entry, resolving the GUI-importer duplication | `__main__.py` 7,183 lines; library CLI `:2760,3213`, IPC `:2066`, helpers `:3216,3967` |\n\n## Architecture & honesty cleanup\n\n**LAND-able now (no gate):**\n- **Dead cloud-TTS residue** — `gemini_native_tts_class`/`GeminiTTS` (zero non-self callers), the import-time OpenRouter `AUDIO_STREAM_MODELS.add` patch, and `CARTESIA_*` params threaded then explicitly discarded (`tts_chain.py:41 _ = (...)`). Keep `google_llm_class` (brain transport, not TTS); delete only the TTS class/patch/params. *(Board #1.)*\n- **`demo_mode`** — pure orphan AND anti-Invariant-#3; delete. *(Board #3.)*\n- **`debrief/ear_test_capture.py::write_ear_test_log`** — live ear-test path is Rust-only (`ear_test_cmds.rs`); the Python module has no importer (dead duplicate system). Delete or re-point Rust at it — pick one.\n- **`audio_decode.py` transformers branch** — SKIPPED/dead against the torch-free ship path; confirm no live transformers dep and strip it.\n- **3 stale `debrief/ln.py` doc-anchors** — symbol lives in `ear_test_capture.py`; fix the anchors. *(Board #6.)*\n- **`coach.py` name collision** — rename `state/coach.py` (the `AICoach` prompt builder) so it doesn't shadow `runtime/coach.py` (the loop). *(Board #7.)*\n- **CLAUDE.md `tts_chain.py` \"TWO builders\" note is stale** — `_build_direct_chain`, `build_tts_chain(mode=)`, and `build_proxy_tts_chain` ALL ignore args and return `_build_moss_chain()`; the doc still claims a live BYO-key vs keyless fork. Update the architecture note. (`tts_chain.py:33-72`, `proxy_client.py:49-56`.)\n\n**HOLD-gated consolidations (real debt, but verify-before-merge):**\n- **`embed.py::LibraryEmbedder` (~430-line dead Gemini embedder)** — `build_embedder()` hard-returns `ClapEmbedder`, `EMBED_BACKEND=\"clap\"` fixed, `VIBEMIX_EMBED_BACKEND` dead. Survives only in docstrings/mirror-comments + the legacy `_router_config` Gemini-embed entry. Gate: confirm no test imports it for *live* coverage (only migration), then delete module + router entry. *(`embed_factory.py:4-7,32-34`, `_cosine.py:36`.)*\n- **GUI `importer.py` re-implements the CLI ingest pipeline** — hand-rolls its own embed/cache/batch loop instead of calling `ingest_source`, so cue-anchoring + ANLZ index + cue-agreement calibration are SILENTLY ABSENT from the GUI path (a weaker importer). Gate: verify the gap is deliberate (speed/progress-streaming) or route through `ingest_source` with a progress callback. *(`importer.py:64-162` vs `ingest.py:756-768`.)*\n- **Three hand-maintained sqlite-vec `vec0` stores** (`library`/`memory`/`learn`) — `SqliteVecMemoryStore` self-describes as a \"sibling of `SqliteVecStore`\": same load-guard, DDL, INSERT-replace workaround, Win-ARM64 fallback. A vec0 fix in one won't propagate. Gate: extract a `_vec0_base` mixin after confirming the memory store's single-connection moments-table coupling survives sharing. *(`index_sqlite_vec.py:32-176`, `index_sqlite_vec_memory.py:60-176`.)*\n- **Per-detector cooldown gate copy-pasted across 7+ `state/detectors/*.py`** — already diverged (kick_swap/distortion rotate baseline on cooldown; sub_layer/kick_density don't). Gate: extract a `CooldownGate` helper with a `rotate_baseline` flag so the divergence is an explicit param, after confirming `last_event_at` ownership is uniform. *(`acid_line_entry.py:96-97`, `kick_swap.py:117-118`, `sub_layer_arrival.py:108-109`.)*\n\n**Verified NOT duplicates (leave alone):** the two `coach.py` (different layers, just rename), `index_numpy.py` vs `index_sqlite_vec.py` (documented cross-platform parity backend), the 4 cue exporters (4 distinct on-disk formats; `cue_folder` already reuses `export_rekordbox.export_set`), `line_voice.py::synthesize_line` (live demo seam wired into `automix_demo_smoke.py`, not orphaned).\n\n## Drop / defer\n\nCut these from the coherent v1 boundary and from all launch copy:\n- **Mascot 3D VTuber surface** — 53 frontend modules + `mascot.html` + Rust window + `mascot-audit` CI, already DEMOTED to opt-in (default `PrimarySurface::Pill`). Largest single body of code serving a surface no default user sees; the product value is fully delivered by Pill + speech. Defer-to-v2; if kept, FREEZE it — no new wiring spent pre-launch. *(`main.rs:186-218` built only if `primary_surface=mascot`.)*\n- **Screen vision as a live source** — dead-wired at runtime (`screen_jpeg=None` hardcoded anti-hallucination gate, `dj_cohost.py:2192-2199`; vision_reader never injected) yet still spawns a 1Hz capture loop + ScreenCaptureKit permission prompt for a buffer nothing reads. Re-enabling re-opens the v4 hallucination class. DROP the live leg; quarantine the `screen_task` spawn behind off-by-default `VIBEMIX_DECK_VISION` so the permission prompt/CPU don't fire. Do NOT touch the `None` invariant.\n- **Mixxx H1-H8 / OSC / `mixxxdb` writer** — every producer is CLAIMED_BUT_ABSENT (`mixxx_osc.py`, `export_mixxxdb.py`, `detect_key.py`, `beatgrid_detect.py`, `wsola.py` all NOT-FOUND); stock Mixxx has no OSC read path (fork-gated). Keep ONLY the file-format SINKS (M2 M3U8, M3 Serato carrier) in v1; never imply live Mixxx grounding. *(`CODEX_GOLD_STRUCTURE.md:143` Shape C \"DON'T BUILD\".)*\n- **Beatmatch \"Mastered\" live credit** — keep behind the honest mask (`_HONEST_UNCREDITABLE_V11=(\"beatmatching\",)`, capped at Competent). A phantom/late grade is the single worst false-expertise slop. The math stays; only build the producer if the skill-tree is a deliberate launch priority (Board #21). Do NOT ship the dead `BEATMATCH_GRADED` consumer branch claiming the skill.\n- **DROP-call live hype line** — engineering done but WIRED_BUT_UNPROVEN; a fire on a phantom predicted-drop is the #1 anti-slop release blocker. The activation block is dirty/uncommitted — keep it out of any signed build (never whole-file `git add` `coach.py`/`__main__.py`, which sweeps ungated DROP speech into a binary).\n- **Windows deck-pair grounding + live Judge** — `_audio_windows.py` is WASAPI master-loopback only; no per-deck capture. Base co-host works on Windows; deck-pair/Judge live-proof is macOS-only. Scope v1 platform honestly (CLAUDE.md says Windows is a v1 platform — resolve before a Windows release claim).\n- **Crossing-detector / 14-bit precision / \"crossed the host's live value\"** (goldmine #3) — physically un-computable for a passive listener with no read of the DJ software's internal control value; would ship exactly the confident-but-blind slop the goldmine exists to kill. CUT the precision claim; the math is reusable only reframed to the controller's OWN timeline + audio-delta corroboration.\n\n## Research-with-workflow\n\n- **H1 Practice-Deck producer for measured beatmatch mastery** — the truest moat, gated on a missing `MiniDeck.seek()` write path. Workflow: (1) build `MiniDeck.seek()` + cursor accessors as an additive change (`render_block` untouched), (2) author `learn/practice_loop.py` driving MiniDeck → `grade_beatmatch` → `BEATMATCH_GRADED`, (3) drive the loop through `vibemix-grounding-review` (touches Invariant #2/#3), (4) ship the rate-writer scripted (not from a live FLX4 read) until on-rig LOCKED-grade proof clears `_HONEST_UNCREDITABLE_V11`. Multi-day milestone, not a quick win.\n- **`__main__.py` decomposition (`library/cli.py` + `library/service.py`)** — the root cause of the CLI-vs-GUI importer divergence. Workflow: isolated branch (high concurrent-collision risk on `__main__.py`), carve argparse subcommands + a shared embed/ingest/import service both CLI and IPC call, verify via `main()` on current source (`IpcRouterBus 'N types wired'` log) + e2e ws-bus dispatch — not the frozen sidecar.\n- **Privacy-default audit** — enumerate every local-data-persistence path (taste rows, `memory.db`, `events.jsonl`, recordings index) and its default flag state, cross-check against the CLAUDE.md opt-in posture. A Kaan decision-gate per `project_observability_milestone_direction`, not an autonomous call — surfaces any default-on path that would ship under an opt-in claim before launch.\n- **`library budget` cost-model rework** — re-baseline now that MOSS is local-free TTS (the live-stack study's TTS-dominates-per-turn premise changes), then flip `test_monthly_projection_under_50_eur` from XFAIL to a passing gate. Until it passes, no cost/pricing copy may rest on the €50/mo figure."
  }
}