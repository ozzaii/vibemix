{
  "summary": "Brutal red-team: what actually ships at land + every way vibemix breaks/wont-sell/fails. Kill-squad + ship-reality map, codegraph-verified, mining old workflows/Mixxx-gold/research.",
  "agentCount": 11,
  "logs": [],
  "result": {
    "blows": [
      "Final picture confirmed. The `library_models --install required` path exists and the Rust command wraps it (`library_cmds.rs:1929-1930`), but `--install required` calls `install_moss_model` which **errors out with no default URL** unless `VIBEMIX_MOSS_TTS_ARCHIVE_URL` is set. And no wizard step calls it automatically (grep empty for wizard model-install). So even the \"Install local models\" button would fail on MOSS for a stranger. The kill-shot is fully evidenced. I have enough to write the assault.\n\n## SHIP-REALITY #1 — What a stranger ACTUALLY gets at ship (HEAD `cb16053d`, shared/moving tree)\n\n**Bottom line:** The perception→event→grounded-reaction engine is real, wired both-ends, and proven HONEST on real audio+MIDI (LIVE capture today, evidence below). But there is a **single, unambiguous KILL-SHOT**: on any machine but Kaan's, **the co-host is mute** — no voice ships, no voice auto-installs. A DJ co-host that cannot talk is not a product. Everything else (the gap between landing-page and reality) is honest-to-mild.\n\n---\n\n### (1) The honest shipped feature list — what a stranger can actually DO\n\nAll verified wired both-ends (UI surface → IPC/CLI → backend) via codegraph + inventory cross-check:\n\n1. **Live co-host that reacts to your set, grounded, anti-slop** — `DJCoHostAgent` (`dj_cohost.py:1004`) hears 10s audio + screen, fires on typed events with per-type cooldowns, strips any un-cited claim to silence. Proven LIVE today: real `music.rms 0.04–0.20`, FLX4 filter twist grounded as `[midi:A_filter:...]`, ungrounded transition claim → `action=strip` (`LIVE-CAPTURE-2026-06-01:14-23`). **This is the strongest part of the product and it is real.** (SRC + LIVE-engine; *but see kill-shot — it can't be heard.*)\n2. **The Pill — \"what's next\" suggestion overlay** — default surface (`main.rs:188`, `config.rs:127`), `SuggestionService` consumed off the ws bus, rendered in `pill/index.ts:42` (`renderNextSuggestion`). Wired both ends. (SRC, unit-proven; no live capture.)\n3. **Library vibe-search / similar / curate / build-set (Crate surface)** — `library/api.ts:8-15` → `library_cmds.rs` (registered `main.rs:105-114`) → `vibemix library` CLI. CLAP embeddings download cleanly out-of-box (real HF URL `_CLAP_BASE_URL`, `model_assets.py:27`). (SRC; GUI round-trip uncaptured.)\n4. **Viber set-prep agent (Codex-backed, 17 grounded tools, seen-set anti-hallucination)** — fully wired FE→Rust→Python; but **requires the user to install Codex CLI + `codex login`** (not bundled, fails actionably when absent). (SRC; no real `codex exec` artifact.)\n5. **Learn / Earned skill-wall** — 10 `ipc.learn.*` types both-end wired (`learn/ws-client.ts`, `learn/ipc_handlers.py:524`), ~30 vitest pass. (SRC.)\n6. **Settings drawer (voice/mode/lens/genre/skill/output, mute, hotkey, library import)** — every control routes through a both-ends-wired `ipc.*` type, optimistic repaint. (SRC, 25 drawer tests.)\n7. **Debrief post-session review** (second window :8766) + **push-to-mute global hotkey** + **first-run wizard/calibration**. (SRC.)\n\nThe Tauri shell folds these into one keep-alive window with **no dead buttons found** across two independent silo audits.\n\n---\n\n### (2) The marketing-lie risk — what we'd be TEMPTED to claim but that is CLI-only / unwired / ABSENT\n\n| Tempting claim | Reality | Evidence |\n|---|---|---|\n| **\"Free AI voice, works out of the box\"** | **The voice does not ship and does not auto-install.** | See kill-shot below. |\n| **\"Grades your beatmatching / unlocks skills as you mix\"** | `BEATMATCH_GRADED` has a wired consumer (`skill_recognizer.py:154`) but **no producer** — the H1 Practice-Deck loop (`learn/practice_loop.py`) **does not exist**. `_HONEST_UNCREDITABLE_V11=(\"beatmatching\",)` hard-codes this. Beatmatching can never reach Mastered. | inventory §402, grep NOT-FOUND |\n| **\"Works with Mixxx\"** | M4 sqlite writer + M5 OSC grounding both **ABSENT** (`export_mixxxdb.py`, `mixxx_osc.py` do not exist); stock Mixxx has no OSC read path. Only file-export (M3U8/Serato tags) works. | inventory §345-346 |\n| **\"Per-deck transition judging on any rig\"** | macOS-only and **never captured firing on real deck audio**; Windows has **no per-deck capture at all** (`_audio_windows.py` is master-loopback stereo only). Abstain-first means master-only rigs (the common case) get **zero** transition verdicts. | inventory §294, §301-302; LIVE §10 (deck B silent, judge correctly abstained) |\n| **\"web search / DJ knowledge\"** | CLI_ONLY — needs unprovisioned `TAVILY_API_KEY` + empty KB corpus. | inventory §185-186 |\n| **\"Free at scale / under €50/mo\"** | `test_monthly_projection_under_50_eur` is **XFAIL**. | inventory §416 |\n| **Live recall of past sessions** | Built but **default OFF** (`VIBEMIX_RECALL_ENABLED`), no UI toggle found. | inventory §362, §367 |\n\nHonest claims would be: *narration/coaching on master+MIDI, library vibe-search, Codex set-prep (BYO Codex), file-format cue export.* The \"skill mastery\", \"Mixxx integration\", \"per-deck judging\", and \"free voice\" framings are the lie-traps.\n\n---\n\n### (3) Live co-host end-to-end at ship — the experience for a stranger\n\nToday's LIVE capture is the honest script. On the **common rig (master mix + FLX4 MIDI, no per-deck routing, empty library)**:\n- Engine grounds correctly, refuses to hallucinate, attributes filter/EQ moves to the right deck. **But with no track identity and no deck-B audio, it can only NARRATE** (\"heavy grinding synth, vocal repeating\") — which is exactly Kaan's complaint *\"sadece açıklama yapıyor\"* (`LIVE §5,§22`). That's a starved-input experience, not a bug — but a stranger experiences it as *\"it just describes my music.\"* **WOUND** on its own.\n- And critically: **they hear none of it.** The text is generated, grounded, clean — and then dropped into a muted TTS.\n\n---\n\n### THE KILL-SHOT\n\n**A stranger's co-host is mute. The product's entire promise is \"a real DJ friend *in your ear*\" — and there is no voice in the ear.**\n\nChain of evidence, all SRC-verified at `cb16053d`:\n- MOSS is now the **single, default-ON** TTS provider — `VIBEMIX_LOCAL_TTS=0` *disables*, anything else enables (`local_tts.py:188`); all cloud TTS paths are dead residue (`tts_chain.py:33-72`, Cartesia/Gemini/OpenRouter all ignored/absent). No fallback voice exists by design.\n- The MOSS model is **not bundled in the build** — `vibemix-core.macos.spec` `datas` collects only `sqlite_vec`, `vibemix` json, the IPC schema, and `onnxruntime` libs; grep for `moss-tts-onnx`/`.onnx` weights in `datas` = **NOT-FOUND**. Same for the Windows spec.\n- The MOSS model is **not auto-downloadable** — `install_moss_model` errors out: *\"MOSS TTS ONNX is not hosted by default\"* unless `VIBEMIX_MOSS_TTS_ARCHIVE_URL`+SHA+SIZE are set (`model_assets.py:618-625`). **No default URL is wired** (`moss_model_installable()` = `bool(env)`, `:573`). Contrast CLAP, which *does* have a real default URL (`_CLAP_BASE_URL` HuggingFace).\n- The \"Install local models\" button calls `library models --install required` (`library_cmds.rs:1929-1930`) → `install_moss_model` → **the same error**. No wizard step calls it automatically (grep of `tauri/ui/src/wizard/` for model-install = empty).\n- Net: `_build_tts_chain_or_mute` (`__main__.py:212-228`) catches `LocalTTSUnavailable` → boots **muted, no cloud fallback**. On a fresh machine the co-host runs, grounds, generates clean text — and says nothing aloud. The inventory calls this *\"the single most consequential finding across all 12 silos\"* (§399) and the LIVE-engine proof only worked because Kaan's machine has the model in `~/.cache`.\n\nThere IS a release safety net — `scripts/dist/pretag_check.sh --require-moss-source` (`_moss_release_source_ready`) blocks a tag unless MOSS is bundled OR archive pins are set. **But it is NOT wired into CI/.github** (grep empty), it's a manual pretag gate, and the **current signed DMG was built from `629de2e8` — 18 src-touching commits stale** (inventory §400) with no evidence it bundled MOSS. So the artifact that exists today both can't speak AND is stale.\n\n**Rating: KILL-SHOT.** Ships and dies — a mute DJ co-host gets uninstalled in under 60 seconds, before the (genuinely good) grounding engine ever gets a chance to impress. The landing-page-vs-reality gap on *\"free AI voice in your ear\"* is a **lie** unless this is fixed first.\n\n**FIXABLE-BEFORE-SHIP: YES.** Single fix: **host the MOSS archive (~600MB) on a public URL and pin `VIBEMIX_MOSS_TTS_ARCHIVE_URL`/SHA/SIZE into the release build env** (then `install_moss_model` works and the wizard/models button can pull it on first run), **OR** add the weights to the spec `datas` to bundle them in the DMG. The plumbing (`install_moss_model`, the pretag gate, the Rust command) already exists — only a hosted artifact + a wizard-triggered call are missing. Until then, the honest landing-page claim is *\"bring your own MOSS model dir,\"* which is not a consumer product.\n\n---\n\n**Secondary WOUND (fix alongside):** even with voice restored, the *common* rig (master+MIDI, no per-deck routing, empty library) yields narration-only. The product note in the LIVE capture (§37) is the right call — **ship for master+MIDI, set expectations that it coaches your moves rather than names tracks** — but the marketing must NOT promise per-deck/track-name awareness or \"skill mastery\" or \"Mixxx integration,\" all of which are absent/macOS-only/unwired. **FIXABLE-BEFORE-SHIP: HARD** (real feature work for full parity) / **YES** for honest copy.\n\n**FUD check:** the dead cloud-TTS seams (Cartesia/Gemini/OpenRouter) sound scary (\"is it secretly phoning a paid API?\") but evidence says they're inert import-only residue — MOSS-only is genuinely enforced. That's a **FUD/SCRATCH** (delete for cleanliness), not a risk.",
      "Confirmed: the wizard has NO BlackHole install handler. There are 13 registered handlers — `list_devices`, `probe_audio`, `permission.check`, `smoke_test`, etc. — but zero `install_blackhole` / brew handler. It only *detects* BlackHole presence (`blackhole_present` flag). The docs claim \"the wizard does this for you on Step 2\" — that is **false against the code** (`docs/audio-routing.md:58` vs `wizard.py:110-141`). The stranger must `brew install blackhole-2ch` themselves (a manual brew install = \"Red\" tier per `docs/AUDIT.md:19`).\n\nI have a complete, evidence-backed chain. Writing the verdict.\n\n## A Stranger's First 10 Minutes — The Co-Host Never Speaks, and Nothing in the App Tells Them Why\n\nHEAD pinned at `cb16053d` (tree shared/moving, Codex landing concurrently). I traced install → first launch → first reaction for a DJ who is NOT on Kaan's rig. The path is a chain of three hard stops, any one of which alone is a rage-quit. They compound.\n\n### STEP 1 — Install: the voice is not in the box, and there's no way to get it\n- `VIBEMIX_LOCAL_TTS` defaults to `\"1\"` (`src/vibemix/__main__.py:837`) → **MOSS is the ONLY voice**; there is explicitly no cloud fallback (`local_tts.py:16-18`, `_build_tts_chain_or_mute` at `__main__.py:212-228`).\n- The 728MB MOSS model is **NOT bundled in the build.** `vibemix-core.macos.spec` datas pull only `**/*.json`/`**/*.txt`, the IPC schema, MIDI/genre profiles, pyobjc, livekit native libs, sqlite_vec (lines 184-285). No `moss-tts-onnx` data block exists. Confirmed by reading the whole datas section.\n- First-run auto-download? **No.** `install_moss_model` has exactly one caller, `install_models` (codegraph: `model_assets.py:734`) — the CLI `library models --install` path, never `main()` boot or the wizard.\n- Even if a user finds that CLI button: `_moss_archive_config()` returns `None` unless ops sets `VIBEMIX_MOSS_TTS_ARCHIVE_URL/SHA256/SIZE` (`model_assets.py:545-568`). None ship. The frontend then renders **\"MOSS manual setup\"** (`library/index.ts:819-828`, gated on `moss.installable === true`). The user is told to hand-place a model directory that has no published URL (`model_assets.py:621-627`).\n- **Net:** out of the box on a stranger's Mac, MOSS is absent → `local_tts_enabled()` returns `False` (`local_tts.py:191-199`) → `_build_tts_chain_or_mute` swallows `LocalTTSUnavailable` and **boots muted** to stderr only (`__main__.py:217-228`). A \"real DJ friend in your ear\" that is silent by design on install.\n- **Rating: KILL-SHOT.** Ships and is mute. FIXABLE-BEFORE-SHIP = **HARD.** Single fix: host the MOSS archive and pin `VIBEMIX_MOSS_TTS_ARCHIVE_URL/SHA256/SIZE` into the release build + auto-trigger `install_moss_model` on first run with a progress UI. Until that URL exists, every install is silent.\n\n### STEP 2 — First launch: needs a Gemini key, with NO UI to enter one\n- Default mode is `\"direct\"` (`__main__.py:910-912`, falls back to `\"direct\"`).\n- `mode=direct` with no `GEMINI_API_KEY` → `[FATAL] ... will never speak`, `sys.exit(4)` (`__main__.py:924-946`).\n- The ONLY key surface in the entire frontend/Rust is a **crash banner** that fires AFTER the crash and tells the user to hand-edit `~/Library/Application Support/vibemix/.env` (`tauri/ui/src/crash-banner.ts:49-54`). I grepped all of `tauri/ui/src/` + `tauri/src-tauri/src/` — there is **no key-entry field, no .env writer, no settings input**. A normal DJ does not own a Gemini API key and will not create a Google Cloud project to use a \"free\" app.\n- The keyless escape hatch (`mode=proxy` → Bravoh `/register`) is **not the default** and there is no UI toggle for it — flipping to proxy is documented as KAAN-ACTION pending live Bravoh endpoints (`__main__.py:900-904`).\n- **Rating: KILL-SHOT.** Without a key the app exits before it can speak, and the product gives the user no in-app way to supply one. FIXABLE-BEFORE-SHIP = **YES** (it's the intended design — flip default to `proxy` once `api.altidus.world` `/register` + `/v1` are live; this is the single fix). Until then, direct-mode-default = dead on every stranger's machine.\n\n### STEP 3 — Audio routing: the wizard lies about installing BlackHole, then the deaf/feedback traps\n- `docs/audio-routing.md:58` claims *\"the wizard does this for you on Step 2\"* (install BlackHole). **False against code:** the wizard registers 13 handlers (`wizard.py:110-141`) and only *detects* BlackHole (`_on_list_devices` sets `blackhole_present`, lines 227-259). There is no install/brew handler. The stranger must `brew install blackhole-2ch` manually — a \"Red\" tier dep per `docs/AUDIT.md:19`. A DJ without Homebrew is stuck here.\n- The \"route ONLY the DJ app into BlackHole\" trap is real and self-inflicting: per CLAUDE.md's own warning, if system output also feeds BlackHole the AI voice loops back and re-amplifies into phantom \"music\"/feedback. The passthrough is always-on with no off-switch (`__main__.py` `PASSTHROUGH_GAIN`).\n- The deaf-cohost failure is LIVE-proven: the prior session captured `music.rms~0.02` silence (`LIVE-CAPTURE-2026-06-01` Finding 1) — a misrouted rig means the co-host hears nothing and never reacts.\n- **Rating: WOUND** (gates adoption; survivable for technical users). FIXABLE-BEFORE-SHIP = **YES.** Single fix: either ship a real BlackHole installer step or correct the doc to stop promising auto-install, and surface a live RMS meter in the wizard so a deaf/misrouted rig is visible before the session starts.\n\n### STEP 4 — If they somehow clear all three: the first line is narration, not coaching\nLIVE truth (`LIVE-CAPTURE-2026-06-01`, events.jsonl 0011–0016, HEAD post-`769e32a3`): on the **common** user rig (master mix mono + controller MIDI, no per-deck split, empty library), the co-host can only **narrate** — *\"heavy grinding synth, vocal repeating\"* — which is Kaan's exact complaint (\"sadece açıklama yapıyor\"). Grounding/anti-slop works correctly (HEARTBEAT with no deck proof → `action=strip`), but the honest output under starved input *is* narration. Per-deck identity (`A_active+B_active`, named tracks, transition calls) requires an advanced FLX4→4-channel BlackHole split + `library ingest` that the live note itself flags as \"most users won't have\" (Product note, decision owner: Kaan).\n- **Rating: WOUND.** Not slop (the engine is honest), but a stranger who fought through three setup walls to hear a robot describe a synth — instead of the promised \"DJ friend\" reacting to a transition — uninstalls. FIXABLE-BEFORE-SHIP = **YES** (the live note's own recommendation): ship for the master+MIDI common case with coaching tuned to single-stream moves, and treat per-deck identity as a bonus that lights up for advanced rigs — don't promise two-deck transition calls the common rig can't deliver.\n\n### The worst one (the kill order)\n**A stranger downloads the .dmg, launches it, and gets a silent app that crashes to a banner telling them to hand-edit a hidden `.env` file with a Gemini API key they don't have — for a voice whose model isn't even in the box and has no download URL.** STEP 1 (mute by default, model not bundled, no fetch URL — KILL-SHOT/HARD) and STEP 2 (direct-mode default + no key-entry UI — KILL-SHOT/YES) together mean **today, on an unconfigured machine, the co-host literally cannot speak and the app offers no in-product path to fix it.** The audio traps and narration problem are downstream — the stranger never reaches them. These are the two that ship-and-die.\n\nNot-a-kill correction: the perception/anti-slop engine is **not** the failure (it's LIVE-proven honest, `LIVE-CAPTURE` Findings 2-4). Any \"the AI hallucinates / sounds like slop\" claim against current source is **FUD** — the stale-DMG slop lines were the 18-commit-old binary (Finding 6), not HEAD. The kill shots are all in onboarding/packaging, not the brain.",
      "Final confirmation locked. `calculate_transition` (the Mixxx transition planner) has exactly ONE caller: `build_automix_reel` in `automix_demo.py` — a demo, not the live coach. And the crude `_xfader_factor` bucket the gold doc said `mixer_physics` would replace is STILL the live xfader path. I have everything I need.\n\n## SHIP-REALITY #3 — Research/Mixxx-Gold Promised vs. Shipped (HEAD `cb16053d`, shared/moving tree, Codex landing concurrently)\n\nThe Mixxx-gold story is the same shape across the board: the **math got ported and unit-tested, every live PRODUCER that turns that math into something a user hears is UNBUILT.** The capability inventory itself confirms 17 `CLAIMED_BUT_ABSENT` (`CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md:30`). I verified the headline golds independently against codegraph — not trusting the doc.\n\n### Per-gold verdict (codegraph-verified)\n\n| Gold | Status | Evidence | Weakens \"grounded friend\"? |\n|---|---|---|---|\n| **`mixer_physics.py` — the \"Move-Effect Resolver\" (the doc's own keystone R-SLOP killer)** | **ABSENT** | `codegraph_search \"mixer_physics\"` → No results; `expected_move_effect` → No results. The crude `_xfader_factor` 0-127 bucket it was meant to replace is STILL the live path (`deck_context.py:3458`, used `:3497`; also `suggestion.py:220`). | **YES — this is the gap that matters.** The gold doc itself calls this the module that \"KILLS R-SLOP… the whole anti-slop thesis made computable.\" It does not exist. |\n| **LUFS receipt / loudness-flat handoff** | **ABSENT** | `codegraph_search \"lufs\"` → No results. The \"Clean Gain Staging Mastered\" Earned node depends on it. | YES — no measured-loudness receipt means no honest \"you kept the master within 1 dB\" coaching; that competency can't be earned. |\n| **EQ-move / spectral receipt** | **ABSENT as a receipt; PRESENT as a NEGATIVE guard** | `audio_delta`/`audio_window` exist in `deck_context.py` but only as sanitizers with rules `deck_audio_delta_not_causal_proof` (`:325`) and `deck_audio_window_not_causal_or_quality_verdict` (`:338`). The audio window is text the brain is forbidden to use as causal proof — the *opposite* of the gold's claim that it's a measurable receipt to license claims. | YES — the friend can hear \"the top end thinned out\" but is barred from saying *your EQ move did it*, because there's no spectral producer to license causality. |\n| **Loop-geometry receipt** | **ABSENT** | `codegraph_search \"loop_geometry\"` → No results; grep `loop_length/loop_bars/LOOP_SET` in `src/vibemix/` → empty. | SCRATCH — loop coaching is a nice-to-have, not core. |\n| **Crossing-detector** | **ABSENT (as named)** | `codegraph_search \"crossing_detector\"` → No results. Only `should_arm_drop_call` exists (`drop_predict.py:64`, `WORKS_SOURCE_ONLY`, never fired in a live artifact). | SCRATCH/WOUND — drop-arm is unproven-live but exists; the dedicated crossing-detector doesn't. |\n| **Beatmatch grading** | **HALF — math real, ZERO production callers** | `codegraph_callers grade_beatmatch` → 6 callers, ALL in `test_beatmatch_judge.py`. `grade_to_event_extra` → **No callers found**. Inventory: `[PRESENT_ORPHANED]` (`:245`). No `BEATMATCH_GRADED` emitter (`:246` `CLAIMED_BUT_ABSENT`); the keystone H1 Practice-Deck Loop (`learn/practice_loop.py`) doesn't exist (`:336`). | Honestly fenced (see fake-confidence note) — so SCRATCH, not a kill. The wall says \"Mastered isn't live-graded.\" |\n| **Transition planner (`calculate_transition`)** | **HALF — wired to a DEMO, not the live brain** | `codegraph_callers calculate_transition` → 1 caller: `build_automix_reel` in `runtime/automix_demo.py:73`. Not reached from `coach_loop`. | WOUND — the energy-curve sequencing exists but only feeds an offline automix demo, not live coaching. |\n| **Deck-identity dream (per-deck A/B grounding)** | **HALF on macOS, ABSENT on Windows** | LIVE capture (`LIVE-CAPTURE-2026-06-01`) proves the engine *honestly blocks* deck-B claims (`B_silent`, `nowplaying=blocked_non_deck_owner`) — designed-correct, but it has never lit up `A_active+B_active` on a real two-deck source. Windows: `[CLAIMED_BUT_ABSENT]` (`:294`) — `_audio_windows.py` is master-loopback only, no `DeckAudioCapture`. | WOUND — the marquee \"I hear both your decks\" feature is macOS-only AND unproven even there; v1 realistically ships master+MIDI mono, with per-deck as an honestly-blocked bonus. |\n| **Screen-vision (deck OCR)** | **HALF — wired but OFF by default** | `deck_vision.py` + `_screen_macos.py` exist and are wired (`__main__.py:1628,2588`), but gated behind `VIBEMIX_DECK_VISION=1` (default prints \"disabled\", `:2590`). macOS-only. | SCRATCH — a stranger never sees it; now-playing covers track identity. |\n\n### Fake-confidence landmines (doc claims vs. codegraph)\n\n- **The gold doc (`wf_d9ddbeed`) describes `mixer_physics.expected_move_effect` as if it plugs into `apply_live_claim_guard` at `deck_context.py:2239` to *upgrade* the guard.** Codegraph: the symbol doesn't exist. Anyone reading that packet as shipped state would believe the #1 slop bug is closed by physics. It is not — the live guard still *refuses* the causal claim rather than *licensing* it. **This is the most dangerous doc-vs-code gap**, but note: it's a PROPOSAL packet (ALT6 idea), not a \"READY\"/\"works\" claim, so it's a near-miss rather than a lie.\n- **`grade_beatmatch`/`grade_to_event_extra` are honestly fenced — NOT a landmine.** The inventory labels them `PRESENT_ORPHANED`/`CLAIMED_BUT_ABSENT` and `tests/repo/test_live_reality_pins.py:126-141` actively *fails* if a production emitter appears. The Earned Wall masks stale \"Mastered.\" This is the project policing its own fake-confidence — credit where due.\n- **`debrief/ln.py`** — `[CLAIMED_BUT_ABSENT]` (`:376`): three docs (`EAR-TEST-PROTOCOL.md`, `KAAN-ACTION-LEGAL.md`, `debrief.html`) anchor to a file that doesn't exist (symbol moved to `ear_test_capture.py`). Pure doc rot, SCRATCH.\n\n### Most damaging gap — rated\n\n**KILL-SHOT-adjacent → WOUND:** The absence of `mixer_physics.py` (the move-effect/spectral receipt). The core promise is \"real DJ friend who reacts to what you DID.\" Right now the engine can ground *that* a move happened (MIDI) and *that* the audio changed (RMS), but it is **structurally forbidden from saying the move CAUSED the change** — the `deck_audio_delta_not_causal_proof` guard exists precisely because the producer that would license causality was never built. So the friend is reduced to narration (\"heavy grinding synth, vocal repeating\") — which is *exactly* Kaan's live complaint (\"sadece açıklama yapıyor\", `LIVE-CAPTURE:22`). It doesn't crash or hallucinate (the guards hold the anti-slop line, so it's not a true KILL-SHOT), but it caps the product at \"honest narrator\" instead of \"DJ friend who gets what you just pulled off\" — and that ceiling is what makes a stranger shrug and close it.\n\n- **Rating: WOUND** (kills the \"alive friend\" delight that drives adoption; the engine stays honest so it won't ship-and-die, but it under-delivers the headline promise).\n- **FIXABLE-BEFORE-SHIP: HARD.** Single fix = build the ~30-line numpy clean-room `state/mixer_physics.py` (`mixxx_xfade_gains` + `db_taper_gain`) and wire `expected_move_effect` into `apply_live_claim_guard` so a computed-sign ∧ measured-sign agreement *licenses* the causal claim instead of stripping it. Real engineering + a live two-deck proof pass — not a one-line flip. The honest v1 alternative (per `LIVE-CAPTURE:37` product note) is to **ship master+MIDI coaching as the promise** and not market per-deck causality at all, which sidesteps the gap.\n\n**Net:** Nothing here is a hidden lie that ships and dies — the project's reality-pin tests and live-claim guards are unusually disciplined about fencing unbuilt producers. The risk is **under-promise-shaped**: a pile of researched golds (mixer_physics, LUFS, spectral receipt, transition→live, deck-identity, screen-vision) sit as tested math or OFF-by-default code with no live producer, so the shipped co-host is a grounded *narrator*, not yet the grounded *friend-who-felt-your-move* the gold docs promise.",
      "`wired` requires `cancel_gate AND ttft_meter AND playback` all non-None. When `wired=True`, the stale-clear path at 488 clears `in_flight` but does NOT clear `in_flight_handle`/`in_flight_ev` — so the next tick's cancel branch (838) CAN cancel the overlapping handle. But the stale-clear at 487-490 only runs `in_flight=False`; it never invokes `cancel_gate.try_cancel`. The cancel only happens when a NEW event is detected AND passes all the mic/voice/cooldown gates. The `levels.voice > AI_TALK_THRESHOLD` check at 498 should normally block re-fire while audio plays — but that depends on the AI's own voice being routed back through the levels meter. I have the full picture. Writing the report.\n\n## ASSASSIN — Technical Breakage (HEAD `cb16053d`, shared/moving tree, Codex landing concurrently)\n\nThree confirmed kill/wound-class failures, the scariest being a **silent-mute-by-default voice path** that ships and bricks the core experience. Proof tiers marked SRC/PKG/LIVE.\n\n---\n\n### KILL-SHOT #1 — The shipped app has NO voice by default. A stranger installs the DMG and hears total silence, with the only error printed to stderr they never see.\n\nThe entire TTS stack was gutted to **MOSS-local-only; cloud fallback DELETED** — and the MOSS model has **no default download URL**.\n\n- `agent/tts_chain.py:36-63` — `build_tts_chain()` in EVERY mode (`direct`/`proxy`) returns `_build_moss_chain()`. Docstring line 2: *\"MOSS-only TTS factory... cloud TTS keys are intentionally [ignored]\"*. Cartesia/Gemini/OpenRouter args are accepted and thrown away (`tts_chain.py:32` `_ = (gemini_api_key, openrouter_api_key, ...)`).\n- `library/model_assets.py:571-573` — `moss_model_installable()` returns `bool(os.environ.get(\"VIBEMIX_MOSS_TTS_ARCHIVE_URL\"))`. **There is no baked-in URL.** Docstring line 581: *\"The repo does not ship or guess a public 600MB+ model URL.\"*\n- `agent/local_tts.py:191-199` — `local_tts_enabled()` = `resolve_model_dir() is not None`. No model cached + no env URL → False.\n- `__main__.py:212-228` — `_build_tts_chain_or_mute()` catches `LocalTTSUnavailable`, prints to **stderr** (`\"-> tts: unavailable... voice muted, no cloud fallback\"`), returns `NOT_GIVEN`.\n- `__main__.py:1789-1791` — `session.output.set_audio_enabled(False)` → headless. The co-host's brain runs, grounds on real audio, builds reactions — and says them to no one.\n- **No GUI gate:** `grep` of `tauri/ui/src/wizard/` and `tauri/ui/src/session/` for moss/tts-ready returned **empty**. The only voice-readiness surface is buried in the Library models panel (`library/index.ts:724,741` \"MOSS voice setup unavailable\"). Nothing stops a user clicking \"start session\" into silence.\n\nThis is corroborated LIVE: the 2026-05-30 live-verify note in memory says *\"THE ENGINE WORKS... TTS is MUTE (Gemini fails → switch)\"* and the 2026-06-01 LIVE-CAPTURE packet flags *\"stale-DMG slop\"*. The mute is now structural, not a config slip.\n\n- **Tier:** SRC (code-confirmed) + corroborating LIVE (two capture notes report mute). **KILL-SHOT** — breaks the literal product (\"a friend in your ear\" that says nothing). A 5-minute uninstall is the optimistic outcome; the user thinks the app is broken.\n- **FIXABLE-BEFORE-SHIP: YES.** Single fix: set the three release-build env pins (`VIBEMIX_MOSS_TTS_ARCHIVE_URL`/`_SHA256`/`_SIZE`) to a hosted MOSS archive AND wire a `models --install required` step into install/first-run, OR gate session-start on `local_tts_enabled()` with a renderer-visible \"voice not installed\" blocker. It's purely an ops/wiring gap — the player exists.\n\n---\n\n### WOUND #2 — Windows users get a structurally inferior product: single-mix master only, NO per-deck separation. And the Windows path is UNTESTED on the only dev machine.\n\n- `platform/_audio_windows.py:8-13, 218-224` — capture is *\"WASAPI loopback on the default playback device\"*; `device_index` is *\"IGNORED for the v1 WASAPI loopback path — we always target the default playback device's loopback.\"* That is one stereo master mix. The whole per-deck grounding edge (deck A/B audio separation, the `render_deck_audio_separation_context` the macOS path feeds the prompt at `coach.py:758-771`) **does not exist on Windows**.\n- Every Windows test is `@windows_only` (`tests/test_audio_windows_live.py`, `test_midi_windows_live.py`, etc.) — **all skipped on this Mac**, which is the only verification machine. PKG/LIVE proof for Windows = **NONE**. The default `pytest -q` green bar says nothing about Windows.\n- The deck-pair grounding the team just shipped two commits ago (`43c10715` \"weight deck pairs from touched controls\", touching `deck_capture.py`+`midi/state.py`) is a macOS-only quality lift. Windows is the v1 platform per CLAUDE.md and gets the \"only-narration when starved\" degraded path the LIVE-CAPTURE packet already calls out.\n- **Tier:** SRC-confirmed; Windows entirely UNVERIFIED (no LIVE, no runnable PKG test here). **WOUND** — ships two-tier; Windows reviews will read \"generic/late\" because it's running the inferior grounding path. Not a crash, but it kills adoption on half the platform matrix.\n- **FIXABLE-BEFORE-SHIP: HARD.** Per-deck on WASAPI loopback is a real engineering lift (no virtual-cable per-deck routing equivalent to BlackHole multi-out). Single honest fix: ship Windows v1 with the master-mix path *and set reaction expectations accordingly* (don't market per-deck coaching on Windows), or block Windows release until a real per-deck capture story exists. Do NOT ship it claiming parity.\n\n---\n\n### SCRATCH/WOUND #3 — `in_flight` stale-clear (12s) fires INSIDE the playout wait (20s) → a long reaction can get a second `generate_reply` stacked on top → two overlapping voices.\n\n- `coach.py:860` — `await asyncio.wait_for(handle.wait_for_playout(), timeout=20.0)`. The first reaction can legitimately hold `in_flight=True` for up to 20s.\n- `coach.py:487-490` — the watchdog clears `in_flight` at `age > 12.0`. **The clear threshold is shorter than the playout timeout.** For any reaction whose playout takes 12–20s, the gate opens while the first voice is still playing.\n- The stale-clear at 488-490 only sets `in_flight=False` — it does NOT call `cancel_gate.try_cancel`. De-collision relies on (a) a *new* event passing all mic/cooldown gates, then the cancel branch at `coach.py:838-844`, and (b) the AI's own voice tripping `levels.voice > AI_TALK_THRESHOLD` at `coach.py:498` to suppress re-fire. (b) only holds if the AI's TTS output is actually routed back into the `levels` meter — and with #1 above, on the mute path there is no voice to self-suppress against, and the per-rig passthrough routing is exactly the loopback the CLAUDE.md warns is fragile.\n- **Tier:** SRC (timing logic confirmed; the 12s<20s asymmetry is real and in the hot path). No LIVE evidence of an actual double-voice yet — only one live session captured, and MOSS reactions there were likely short. **SCRATCH today, WOUND if MOSS playout runs long** (longer coach-mode sentences). It manifests as the co-host talking over itself — squarely \"AI slop / breaks the flow\", the founder's kill condition.\n- **FIXABLE-BEFORE-SHIP: YES.** Single fix: raise the stale-clear threshold above the playout timeout (e.g. `age > 22.0`), or have the stale-clear path invoke `cancel_gate.try_cancel` before reopening the gate. One-line constant change.\n\n---\n\n### Checked and CLEARED (FUD — evidence says fine)\n\n- **`in_flight` wedge:** Cannot permanently wedge. `in_flight=False` is set in BOTH the `finally` (`coach.py:877-878`) AND the outer `except` (`coach.py:884-885`), plus the 12s watchdog. A `generate_reply` exception or timeout always reopens the gate. Not a wedge — the *opposite* problem (#3, it reopens too eagerly).\n- **Single-writer MusicState:** The HEAD~2 deck-pair commit (`43c10715`) touched `deck_capture.py`/`midi/state.py`/`__main__.py` but added NO `MusicState` writer — `git show` of `deck_capture.py` shows zero `state.` assignments. Invariant #1 intact.\n- **MIDI unplug stale-move hallucination:** Defended — `__main__.py:1838-1842` calls `mark_disconnected()` on unplug, which clears the moves/events rings (`state.py:193`), so a post-unplug coach reaction can't cite a phantom move. The doubled-listener-thread bug (CR-01) is explicitly avoided.\n- **FLX4 play=off mis-read / cc34 b-jog:** Both addressed in-tree. `pioneer_ddj_flx4.json:25-26` maps `play_a/play_b` (kind `play`, note 11), and the cc34 b-jog alt mapping landed at line 19 (`jog_move_b_alt`, commit `4446f674` \"accept live flx4 b-jog cc34\"). These are SRC-fixed; the original live mis-read predates the fix.\n\n**Scariest, ranked:** #1 (silent-mute-by-default) is the one that ships and dies — it is the difference between \"the engine works on Kaan's rig because he has MOSS cached + env set\" (LIVE-true) and \"a stranger downloads the DMG and hears nothing\" (the actual shipped experience). Everything else is recoverable; a voiceless voice-assistant is not.",
      "Confirmed: the linter IS wired live (288 `citation_count`, 30 `live_claim_guard`, 3 `citation_strip`) — yet only 3 strips against 236 emits. The gate is running and passing 98.7% of turns. So the slop isn't lying-class slop the gate catches; it's *narration-class* and *generic-banter* slop the gate was never designed to catch. The gate verifies citations are real, not that the reaction sounds like a friend. The evidence is complete.\n\n## ASSASSIN — The Core Promise (\"real DJ friend, no AI slop\") is met in 6% of reactions\n\n**HEAD `cb16053d`** (shared tree, Codex landing concurrently). Evidence base: 236 real spoken reactions across **54 LIVE-tier sessions** in `~/Library/Application Support/vibemix/recordings/*/events.jsonl` (vibemix's own session logs — not off-limits AI logs), cross-checked against source.\n\n### THE KILL SHOT — quantified from 236 live reactions\n\nI cross-tabulated every emitted reaction on the two axes the promise demands: **grounded** (carries a real `[src:cite]`) × **friend-shaped** (2nd-person/advice/banter vs 3rd-person audio narration):\n\n| | friend-shaped | narration / no-\"you\" |\n|---|---|---|\n| **cited** | **13 (6%)** ← the promise met | 60 (25%) grounded but sportscaster |\n| **uncited** | 60 (25%) banter, no proof | **103 (44%)** vibe-slop, ungrounded |\n\n- **\"Grounded AND friend\" = 13/236 = 6%.** That is the product's entire reason to exist, and 94% of reactions miss it.\n- **66% are sportscaster narration** (no \"you\", describing the audio in 3rd person). Real LIVE samples: *\"A heavy, repeating low-end pulse dominated the spectrum while a metallic texture rasped over the top\"* `[3.21s]`; *\"Harsh, metallic resonance scraping on top of that heavy sub\"*; *\"High-end washed out, just that dark kick pulsing under those long pad sweeps.\"* This is the exact \"sadece açıklama yapıyor\" complaint, now measured at scale — not a starved-input fluke, it's the dominant output mode.\n- **Why the gate doesn't catch it:** the linter IS wired live (288 `citation_count`, 30 `live_claim_guard` events) yet stripped only **3/236** turns (`citation_linter.py:119` — strip rule is `citations_found == 0`). The gate verifies that *citations resolve* (Invariant #2), it has **no opinion on whether the reaction sounds like a friend**. A narration line cites a real `[aud:rms@9.0]` and sails through. The anti-slop machine is built against *hallucination-slop* and is blind to *narration-slop* and *generic-banter-slop* — which together are 88% of output.\n\n**Rating: KILL-SHOT.** This breaks the founder's explicit kill condition (\"if reactions feel forced, late, fake, or scripted, the product fails\"). It is the single most important finding here. **FIXABLE-BEFORE-SHIP: HARD** — there is no single fix; \"friend not sportscaster\" is a prompt/persona + a *new* output classifier the gate doesn't have. The current grounding gate is necessary but provably insufficient.\n\n### Attack (b) — LATENCY: reaction lands after the moment, 98% of the time\n- Product defines on-the-moment as `IN_BAR_TOLERANCE_S = 2.0` (`audio/constants.py:75`). Across 236 live reactions: **median 3.09s, p90 12.85s, max 51.34s. In-bar (≤2.0s) = 4/236 = 2%.** 69/236 (29%) land >4s after the moment.\n- A late true reaction feels as fake as a wrong one — by the product's own bar it is *always* late. **Rating: WOUND** (borderline KILL for a hype product where timing IS the value). **FIXABLE: HARD** — Gemini Flash round-trip dominates; the speculative `head_yielded` path (`dj_cohost.py:2781`) already tries to mask it but only helps TTFT, not the 3s+ floor.\n\n### Attack (d) — REPETITION: the no-repeat guard is prompt-only and fails\n- No-repeat memory is a `deque(maxlen=10)` (`dj_cohost.py:1290`) injected as prose (\"Do NOT repeat...\", `:2206`) — **no programmatic dedup on output.** It demonstrably fails live: *\"I'm listening\"* ×6, *\"I caught the live move. the useful note is the sound change\"* ×8, *\"I'm here, just listening\"* ×2. Over a 60-min set the canned openers recur and the thing sounds scripted. **Rating: WOUND. FIXABLE-BEFORE-SHIP: YES** — add an output-side n-gram/embedding similarity reject before TTS (the machinery to record outcomes already exists in `StrippedRateTracker`).\n\n### Attack (a) — gate strips EVERYTHING → dead air: mostly FUD, but inverts\n- Silence rate (strip+slop_suppressed)/total = **1.3% (3/239)**. The \"user hears silence\" fear is **FUD** — the ack-bank was retired 2026-05-19 (`dj_cohost.py:3089`, `runtime/coach.py:3`) so a strip = pure silence, but strips almost never fire. **The danger is the opposite:** the gate is so permissive it's effectively decorative against the slop that actually ships (narration/banter). **Rating: FUD on the literal claim; the inverse is the KILL-SHOT above.**\n\n### Live-reachable slop bypass (code-confirmed, not yet observed firing)\n`StrippedRateTracker.should_bypass()` is wired into `llm_node` (`dj_cohost.py:3126`) and, on a strip-burst, **emits the unverified text verbatim to TTS** (`citation_action = \"bypass\"`, `:3131`, `print(\"[ai_text:unverified]\")`). By design, a cold-start grammar-drift streak lets ungrounded text *through* to the user with an `[unverified]` log row nobody hears. Bypass count in 54 live sessions = 0 (never tripped yet), so it's a latent KILL-SHOT, not an active one. **Rating: WOUND (latent). FIXABLE: YES** — gate the bypass behind party-mode-only, or cap it so a citation-claiming line never bypasses.\n\n### Bottom line for the founder\nThe grounding gate works exactly as built and is **honest** — it doesn't *lie*. But \"doesn't lie\" was never the bar; \"real DJ friend, no slop\" was. On 236 real reactions that bar is hit **6%** of the time, reactions are **in-bar 2%** of the time, and the dominant voice is a **sportscaster (66%)**, not a friend. A stranger demoing this hears late, third-person audio descriptions that repeat — they uninstall. The anti-slop release gate, as currently scoped, does not protect the core promise; it protects only against the one slop class (hallucination) that is the *smallest* share of what actually ships.",
      "I now have everything I need with hard evidence. The economics doc itself confirms the per-DJ cost is €9.74/month against a €4.99 Pro price that has zero code enforcement, the cost-cap is measure-only (`SessionMeter` \"NEVER raises\"), and the funnel is one passive opt-in link.\n\n## ASSASSIN VERDICT — MARKET / SALES: the money model is a slide deck, not a product. The free tool LOSES money on every active user, the €4.99 \"Pro\" has no paywall in the binary, and the funnel is one optional link.\n\n**HEAD pinned `cb16053d` (2026-06-01 20:36); tree shared/moving (5 uncommitted files, Codex landing concurrently). Findings are SRC/doc-tier unless flagged.**\n\n---\n\n### KILL-SHOT #1 — Unit economics are inverted: free product costs ~€9.74/active-DJ/month; the €4.99 price is below the cost of the thing you'd give away free.\nThe founder's CLAUDE.md still carries the original assumption: *\"~50 €/month ongoing Gemini API for end-user requests\"* (`CLAUDE.md:20`). The repo's own verified cost study demolishes it. `docs/pricing/live-stack-economics.en.md:13,23` — **\"One daily-active DJ costs about €9.74 per month to run\"**, and the memory note records the founder's reaction to the gap: *\"Old '€50/mo' was embedding-only... ~700–1700× off\"* (`project_live_stack_cost_and_capability_study.md`).\n\nThe math that kills it:\n- Fleet at 10k daily-active = **€97,374/month** (`live-stack-economics.en.md:45`) on a project whose total launch budget is *150-200 €* (`CLAUDE.md:20`).\n- TTS is ~74% of every turn and **does not cache** (cost study). There is no architectural lever to make a free voice-in-ear product cheap — the voice IS the product.\n- The €9.74 itself rests on a **90% cache-hit assumption the repo never measures** (`live-stack-economics.en.md:65`: *\"treat 90% as a hopeful default, not a fact\"*). Cold cache → €12.34 (+38%), and the modeled 1,900-token prompt is conceded to be closer to 3,300-4,600 real → another +€0.67–3.53. The honest worst case is **~€14–16/DJ/month**.\n\nThe skeptical-investor read: this is a product where **going viral is the failure mode, not the win**. A successful free launch with 10k engaged DJs is a €100k+/month bill against a €200 budget. The only thing protecting the founder today is that nobody's using it.\n- **Rating: KILL-SHOT (breaks the money). FIXABLE-BEFORE-SHIP = HARD.** Single fix: ship the local MOSS TTS as the *default* free voice (it's benched: 71ms TTFT, $0 — `project_local_tts_moss_nano_benched.md`), make cloud voice the paid upgrade. But that's a product-architecture decision (`VIBEMIX_LOCAL_TTS` is opt-in today, `__main__.py`), not a config flip, and the founder hasn't given the ear-verdict.\n\n---\n\n### KILL-SHOT #2 — There is no Pro tier in the binary. €4.99 is a pricing slide with zero enforcement code.\nThe plan (`project_viber_launch_and_pro_pricing.md`): set-prep/sequencing becomes the €4.99 Pro feature. I searched the entire tree for any monetization surface:\n- `grep -rniE \"stripe|in.app.purchase|StoreKit|license_key|app_store|paddle|lemonsqueezy|checkout|entitlement|is_pro|paywall\"` across `src/` + `tauri/` → **zero hits** outside Apache-license headers and the dev-MCP \"checkout\" (git) comments.\n- No set-count limiter: the cost study itself recommends *\"cap Pro@8-10 sets, Free@2-4 sets\"* — that limiter **does not exist** (`grep sets_per_month|free_limit|max_sessions` → only `bench/` and a `max_sessions=10` soak-test default, `__main__.py:3967`).\n- The Viber/set-prep CLI commands (`library curate`, `library build-set`) gate on `VIBEMIX_CODEX_ALLOW_SHELL=1` and a local Codex login — i.e. they're **free and unmetered to anyone who runs them.**\n\nSo today the \"monetizable surface\" is given away with no gate. To charge €4.99 you'd need an account system, an IAP/Stripe integration, and a feature flag — none exist. And the cost study already shows €4.99 is *underwater*: a daily DJ costs €9.74, and the Studio €9.99 flat *\"LOSES €2.01/mo via app-store on daily-DJ\"* (cost study). **The price was set by gut (\"I would, def\" — `project_viber_launch_and_pro_pricing.md`) before anyone modeled the cost.**\n- **Rating: KILL-SHOT for the revenue claim (the €4.99 line is fiction until built). FIXABLE-BEFORE-SHIP = NO** (it's net-new account+billing infra, weeks of work, and the price is below cost regardless). Single fix: don't claim revenue — treat vibemix as pure marketing spend, not a P&L line.\n\n---\n\n### WOUND #1 — The Bravoh funnel is one passive opt-in link in a window most users never open.\nThe entire conversion mechanism is `tauri/ui/src/debrief/components/bravoh-waitlist-toggle.ts` — a toggle (default presumably off; *\"optional\"* in the label, line 37) that, when enabled, surfaces a `<a target=\"_blank\">` to `bravoh.com/waitlist?utm_source=vibemix` (line 35). It's mounted only in the **debrief** window (port 8766, a *separate* second Tauri window, `debrief-window.ts:25-27,60`) — the post-set review surface a casual user opens approximately never. The comment is explicit: *\"Not gating, not a form\"* (`bravoh-waitlist-toggle.ts:6`).\n\nFunnel premise attack (audience mismatch): the OSS user is a **bedroom DJ who wants a friend in their ear** (the founder's own north star: *\"I wanted somebody to share the music experience with\"* — pricing memory). Bravoh is an **AI operating system for professional recording/performing artists**. These are overlapping-but-different humans — a hobbyist mixing techno at home is not obviously a Bravoh-artist-OS lead. The funnel converts on the assumption that a DJ-toy user wants an artist-career platform, with **no measured conversion data** and a CTA buried behind an off-by-default toggle in a window they don't visit.\n- **Rating: WOUND (kills the funnel ROI). FIXABLE-BEFORE-SHIP = YES.** Single fix: surface the Bravoh CTA in the *main* deck/settings surface (not just debrief) and instrument click-through with the UTM you already built, so the conversion assumption becomes a measured number instead of a hope.\n\n---\n\n### WOUND #2 — No 2nd-session hook; the cost study confirms the voice is a novelty you'd mute.\nDoes a DJ want a voice mid-set? The repo's own LIVE truth says the current behavior is **\"only-narration when starved\"** and **\"stale-DMG slop\"** (`LIVE-CAPTURE-2026-06-01-perception-loop.md`, per task brief). The founder set the explicit kill condition himself (`CLAUDE.md:14`: *\"If reactions feel forced, late, fake, or scripted, the product fails\"*). A hype-man that talks over your mix is the single most mute-able feature in audio software. The retention surfaces that exist — Debrief, Learn skill-tree (v11 \"Earned\"), Library — are genuinely the stickier reasons to reopen, but they're **secondary windows**, and the hero (\"voice in your ear\") is the part most likely to get muted after session one. There is no streak, no progress nudge, no \"your set last night\" notification — nothing pulls the user back on day 2.\n- **Rating: WOUND (retention). FIXABLE-BEFORE-SHIP = HARD** (retention is earned by reaction quality, the unsolved core problem). Single fix: lead the product on the **Debrief/Learn** loop (a reason to come back) and make the live voice a per-session opt-in, inverting the current default.\n\n---\n\n### FUD (sounds scary, evidence says fine) — \"Rekordbox/Mixxx already sort and cue, so set-prep has no willingness-to-pay.\"\nPartly fair, but the set-prep engine is **not** the same thing: CLAP cross-modal vibe-search (\"find tracks that feel like this\", `library/clap_engine.py`, 512-dim on-device) + energy-curve sequencing (`library build-set`) is genuinely beyond Rekordbox's BPM/key sort. The differentiator is real. It just isn't worth charging for *yet* because (a) text→vibe is conceded coarse-only (`project_clap_text_audio_unreliable.md`) and (b) it's underwater at €4.99 anyway (see KILL-SHOT #2). So the \"free tools already do this\" objection is **FUD** — the tech is differentiated — but it doesn't rescue the price.\n\n---\n\n### THE ONE ASSUMPTION THAT, IF WRONG, KILLS THE MONEY\n> *\"~50 €/month ongoing Gemini API for end-user requests\"* — `CLAUDE.md:20`.\n\nIt is wrong by **700–1700×** (the repo's own number, `project_live_stack_cost_and_capability_study.md`). The real figure is €9.74/active-DJ/month, dominated by un-cacheable TTS, against a €4.99 price that has no enforcement code and a €200 total budget. Every other business decision (free-funnel, €4.99 Pro, \"market harder if it goes viral\" — pricing memory) was built on top of this broken assumption.\n\n---\n\n### IS THIS A REAL BUSINESS OR A PORTFOLIO PIECE?\n**Today: a portfolio piece / marketing experiment, not a business.** As a *Bravoh marketing funnel* it can work — but only if (a) the live voice runs on the free **local MOSS TTS** (else virality bankrupts it), (b) the Bravoh CTA moves out of the debrief window and gets instrumented, and (c) the team stops counting €4.99 as revenue (it's below cost and unbuilt). The honest framing the founder should adopt: **vibemix is 100% marketing spend for Bravoh, capped by routing the voice on-device, with conversion measured not assumed.** Priced and architected as it stands, the win condition (lots of users) and the bankruptcy condition (lots of users) are the same event.",
      "I have a complete picture. The grounding gate strips at a 40% threshold over a 15s window, and on strip the user hears silence (ack-bank retired). MOSS model weights are confirmed NOT in the spec datas (no `.onnx`/`.data` model files, only runtime libs). I have everything needed for the competitive assassination. Writing the verdict.\n\n## ASSASSIN — Competition & \"why not just…\": vibemix has a real moat, but it's UNWIRED, and the moat that IS wired makes the product go SILENT\n\n**HEAD pinned: `cb16053d`** (tree is shared/moving — Codex landing concurrently; `git log` shows `cb16053d`/`38ff0679`/`43c10715`). All claims below are SRC-tier unless marked LIVE (the 2026-06-01 real-rig capture).\n\n---\n\n### The competitive thesis the founder is betting on, stated plainly\nCLAUDE.md says the moat is \"grounded, no slop — a real DJ friend in your ear.\" Everything else (MIDI maps, library embeddings, set-prep) is **table stakes Mixxx/Rekordbox already have or a fast-follower clones in a weekend**. So the entire competitive case rests on one question: **is the grounding engine defensible, and does it actually produce a delightful experience?** I attacked both. Verdict: the engine is real and honest, but it is tuned to *suppress* rather than *delight*, and the feature that would make it un-cloneable (the teaching/Judge \"moat\") is dead code.\n\n---\n\n### KILL-SHOT #1 — The wired moat (citation grounding) makes the co-host go SILENT, not charming. A stranger hears nothing and uninstalls.\nThis is the single most lethal finding, and it's an *own-goal*, not a competitor.\n\n- The anti-slop gate is binary whole-turn strip: one bad citation atom or one banned phrase → the entire utterance is replaced with `<silence/>` and never spoken (`prompts/filter.py:37-40` returns `SILENCE_TOKEN`; `agent/dj_cohost.py:3021` `[ai_text] <silence/> (suppressed)`).\n- **The ack-bank fallback that used to cover a strip with a short canned reaction was RETIRED** — `agent/dj_cohost.py:1177`: *\"the ack-bank was retired 2026-05-19 — strip-to-silence replaced pre-canned acks.\"* So a stripped turn = dead air. There is no graceful \"yeah\" / \"nice\" fallback anymore.\n- LIVE evidence (`LIVE-CAPTURE-2026-06-01-perception-loop.md` Finding 3 + 5): on the **common** user input (mono master mix + controller MIDI, no per-deck split, empty library) the brain *\"can only narrate\"* and HEARTBEATs with no two-deck proof get `action=strip` → *\"I can't call that a transition until I have clear two-deck proof.\"* That is the founder's own complaint quoted verbatim: *\"sadece açıklama yapıyor\"* / *\"bok gibi.\"*\n- The grounding is so strict it self-throttles: `STRIPPED_RATE_THRESHOLD = 0.4` over a `15s` window (`coach/constants.py:26,32`) — i.e. the system *expects* up to 40% of turns to be stripped before it even trips a bypass.\n\n**Why this kills against EVERY competitor:** A beginner comparing this to a YouTube tutorial gets a tutorial that always talks. A hype-man on Discord always hypes. vibemix, on the realistic home rig, alternates between *narrating what it hears* and *saying nothing*. \"Real DJ friend in your ear\" fails not because it hallucinates — the honesty engine works perfectly — but because **an honest friend who mostly stays quiet is indistinguishable from a broken app.** The grounding solved hallucination by trading it for silence, and silence reads as \"dead.\"\n**Rating: KILL-SHOT. FIXABLE-BEFORE-SHIP: HARD.** Single fix: restore a grounded ack/observation fallback on strip (narration *is* allowed if it's the honest move) so the co-host is never silent for >1 turn — but tuning \"lively yet grounded\" is the entire unsolved product problem, not a code line.\n\n---\n\n### KILL-SHOT #2 — The genuinely un-cloneable moat (Beatmatch Judge / teaching skill-tree) is DEAD CODE. Zero production callers.\nThe one thing a fast-follower *can't* trivially clone is the Mixxx-ported deterministic beatmatch grader feeding an earned skill-tree (\"lessons fill a skill to Competent, a cited live demo unlocks Mastered\"). That's the real differentiation vs. a competitor with a better TTS voice. **It is not wired.**\n\n- `codegraph_callers grade_beatmatch` → **6 callers, ALL of them tests** (`tests/learn/test_beatmatch_judge.py:63/77/89/101/111/124`). Zero `src/` callers. The \"moat\" math runs only in unit tests.\n- The consumer (`learn/skill_recognizer.py:154` `if ev_type == \"BEATMATCH_GRADED\"`) exists but **nothing emits `BEATMATCH_GRADED` in production** — grep of `src/` for an emitter: NOT-FOUND (only docstrings at `beatmatch_judge.py:151-153` and `skill_tree.py:147` *describe* a future emitter). The keystone producer `learn/practice_loop.py` / `practice_runtime.py` does not exist (capability inventory L336, AUDIT-CONFIRMED twice independently).\n- So the v11.0 \"Earned\" teaching milestone — the headline differentiator over \"Mixxx + a louder voice\" — ships as scaffolding that can never fire. Same gap on H3 cue-drop judge, H4 nudge coach, H6 blend grade, H7 phase-align, H8 detect-key: all `[CLAIMED_BUT_ABSENT]` (inventory L338-342).\n\n**Why this is lethal competitively:** Strip out the dead teaching engine and what remains — listen to master audio, react with an LLM, MIDI maps for 10 controllers, CLAP library search — is **exactly the feature set a fast-follower forks from this Apache-2.0 repo and re-skins with ElevenLabs in 3 weeks.** The moat exists in the design docs and the test suite, not in the shipping binary.\n**Rating: KILL-SHOT (to the *defensibility* claim, not the boot). FIXABLE-BEFORE-SHIP: NO for v1** — building H1 practice-deck loop + emitter is net-new feature work, not a wiring fix.\n\n---\n\n### WOUND #1 — \"Why not just use Mixxx/Rekordbox\" has a strong answer the product can't yet deliver, and a weak answer it ships today.\n- **Mixxx** (free, GPL, mature) already has: 10+ controller mappings, beatgrids, key detection, auto-DJ, cue points — everything in vibemix's `midi/profiles/` (12 controllers) and `library/`. vibemix's *only* differentiator over Mixxx is the AI co-host voice. Per KILL-SHOT #1 that voice is often silent; per KILL-SHOT #2 the coaching depth is dead. So today's honest pitch reduces to \"Mixxx-adjacent feature parity + an LLM that narrates sometimes.\"\n- vibemix doesn't even *replace* the DJ software — it listens to Mixxx/Rekordbox/Serato's master out. So it's an *add-on*, which raises the bar: it must be worth installing BlackHole + a second app + (for the good experience) a fiddly 4-channel per-deck route that the LIVE capture (Finding/owed-recapture, \"advanced setup most users won't have\") admits most users won't do. The common-case input is exactly the starved input that produces narration-or-silence.\n**Rating: WOUND. FIXABLE-BEFORE-SHIP: HARD** (the answer is KILL-SHOT #1 + #2 being fixed).\n\n### WOUND #2 — A stranger on a fresh Mac boots VOICE-MUTED, compounding the \"is it broken?\" read.\n- MOSS-TTS model weights are **not in the DMG**: `vibemix-core.macos.spec` `datas` bundle onnxruntime libs but **no `moss-tts-onnx/*.onnx` / `*.data`** (my grep of the spec returns only runtime libs L76-81/139-170; inventory L262 AUDIT-CONFIRMED \"a fresh machine boots voice-muted\"). The 671MB model lives only in `~/.cache` on the dev's own Mac.\n- Combined with the proxy-key situation (memory: both Bravoh Gemini keys DEAD; keyless proxy needs a valid key + TTS provider choice), the realistic first-run is: install → grant audio perms → route BlackHole → **hear nothing** (either muted TTS or strip-to-silence). The \"5-minute uninstall\" is not hypothetical.\n**Rating: WOUND. FIXABLE-BEFORE-SHIP: YES.** Single fix: first-run model download OR bundle MOSS in the DMG + ship a working proxy key.\n\n### SCRATCH — The 63-phrase slop regex is trivially cloneable and partially a liability.\n`prompts/negative_dict.py` is a static 63-phrase list (`NEGATIVE_PHRASES`) — any fork copies it in 10 seconds; it's not a moat, it's a `\\b`-bounded regex. It can also nuke a genuinely good reaction that happens to contain \"epic\"/\"love it\" (whole-turn suppression), feeding KILL-SHOT #1's silence. Not lethal, but it's marketed as part of the moat and isn't one.\n**Rating: SCRATCH. FIXABLE: YES** (downgrade to per-phrase soft-rewrite instead of whole-turn kill).\n\n### FUD (sounds scary, evidence says fine) — \"The grounding engine itself is hallucinating / unreliable.\"\nSome prior framing implied the engine fakes events. **Evidence refutes this.** LIVE capture Findings 2-4: filter twist correctly attributed to deck A (`[midi:A_filter:...@302.5]`), refused to attribute a browser now-playing to a deck (`nowplaying=blocked_non_deck_owner`), reported `B_silent` instead of inventing deck-B activity. The single-writer + citation-linter + whole-turn strip (`citation_linter.py`: response-level binary validation against `EvidenceRegistry`) genuinely close the hallucination class. **The engine is honest. Its problem is the opposite of slop — it's too quiet.** Don't let anyone \"fix\" grounding; the bug is the *fallback*, not the gate.\n\n---\n\n### MOST LETHAL competitive threat, ranked\n**A fast-follower who forks this Apache-2.0 repo and ships the SAME grounding engine with a NEVER-SILENT voice.** Because:\n1. The grounding moat is ~2000 lines of readable Python (`coach.py` 1068, `evidence_registry.py` 580, `citation_linter.py` 213) — fully forkable, no model training, no data moat.\n2. The *un-cloneable* part (Beatmatch Judge teaching tree) is dead code that ships zero value, so a forker loses nothing by skipping it.\n3. The forker's one change — replace strip-to-silence with a grounded-narration fallback — directly fixes vibemix's KILL-SHOT #1 and produces the \"alive\" experience vibemix is failing to deliver. **The fast-follower beats the original at the original's own core promise with a one-file change.**\n\nThe defensible moat would be the live-proven teaching/Judge loop (cited in-set skill demonstrations — that requires the per-deck rig integration + Mixxx-ported DSP that's genuinely hard). **That moat is real but unbuilt.** Today there is no moat in the shipping artifact — only in the planning docs.\n\n**Bottom line for sales:** vibemix as-built loses to \"Mixxx + ElevenLabs glued by a weekend hacker\" because its honest-but-silent gate reads as broken, its differentiating teaching engine doesn't fire, and its anti-slop list is copy-pasteable. The path to a real moat (cited live skill-mastery) exists in the codebase as tests and TODOs — shipping it is net-new work gated on the per-deck rig the LIVE capture says most users won't set up.",
      "I have everything. The live capture confirms the exact \"it only narrated\" first impression and the deck-B-silent routing trap even Kaan couldn't get working. Writing the assault.\n\n## ASSASSIN — UX / Setup Friction / Disgust (HEAD `cb16053d`, shared/moving tree)\n\nThe funnel I'm attacking: download → 4-step wizard → first live session. The wizard's actual steps are `[\"tcc-grants\",\"audio-device\",\"controller-probe\",\"ai-test-reaction\"]` (`tauri/ui/src/wizard/onboarding-flow.ts:68`). Two things a stranger MUST have to get the core promise are **not in those four steps**: the Gemini API key, and the per-deck channel map. That gap is where they die.\n\n---\n\n### KILL-SHOT #1 — Fresh install ships MUTE. The single voice (MOSS, ~700MB) is neither bundled nor auto-downloadable.\nThis is the whole product. \"A DJ friend in your ear\" that makes no sound.\n\nEvidence chain:\n- MOSS is now the **single TTS provider, no cloud fallback**: `local_tts.py:194` — *\"MOSS is the single TTS provider. VIBEMIX_LOCAL_TTS=0 disables speech, but it never enables a cloud fallback.\"*\n- On boot with no model → mute, by design: `__main__.py:212` `_build_tts_chain_or_mute` → *\"Build MOSS-only TTS for app boot, or start muted without cloud fallback\"*; `__main__.py:223` prints `-> tts: unavailable (... voice muted, no cloud fallback)`.\n- The model is **NOT in the build**: grep for `moss`/`MOSS-TTS` in `vibemix-core.macos.spec` / `.windows.spec` = no model entry. The shipped `dist/vibemix-0.0.1.dmg` is **120MB**; the MOSS model on disk is **~700MB** (`moss_tts_global_shared.data` 441M + `moss_tts_local_shared.data` 230M). It cannot be in a 120MB dmg.\n- There is **no public download source**. `model_assets.py:581` — *\"The repo does not ship or guess a public 600MB+ model URL.\"* `moss_model_installable()` (`:571-573`) returns True **only if** `VIBEMIX_MOSS_TTS_ARCHIVE_URL` is set. No release/CI/spec script sets it (grep across `scripts/`, `.github/`, specs, `pyproject.toml` = the only hit is `check_sidecar_bundle_ready.py`, a *checker*, not a setter). So `library models --install required` returns the actionable-error branch (`:621-627`), not a voice.\n- `resolve_model_dir()` (`local_tts.py:217`) only **checks the cache** — it never downloads. No first-run/wizard/boot trigger calls `install_moss_model`; the install lives in the **Library panel** (`tauri/ui/src/library/model-setup.test.ts`), which onboarding never visits.\n\nNet: a stranger downloads the dmg, completes the wizard, and the co-host is silent forever. The wizard's `ai-test-reaction` smoke step is meant to be the \"first good moment\" — and its bundled fallback audio **doesn't even exist** (`tauri/ui/public/audio/offline-greeting.wav` NOT FOUND; only `sine-1khz-1500ms.wav` is present, the test tone). So the very step designed to prove \"you'll hear me\" can produce no greeting.\n- Rating: **KILL-SHOT**. Ships and dies on launch.\n- FIXABLE-BEFORE-SHIP: **HARD**. Single fix that unblocks ship: set the verified `VIBEMIX_MOSS_TTS_ARCHIVE_URL`/`_SHA256`/`_SIZE` pins in the release build AND wire `library models --install required` into the wizard's `driver_fetch`/`ai-test-reaction` step so it downloads before the smoke test. Until that 700MB host + pins exist, every install is mute. (Bundling it into the dmg makes the download 6x bigger — the other horn of the dilemma.)\n\n---\n\n### KILL-SHOT #2 — The brain needs a Gemini key the wizard never asks for.\n- Default LLM mode is `direct`, which **requires** `GEMINI_API_KEY`: `__main__.py:874` (`VIBEMIX_LLM_MODE = 'direct' (default) | 'proxy'`). Missing key → fatal exit code 4 → `\"api-key-missing\"` (`sidecar.rs:575,586`).\n- There is **no API-key step in `ONBOARDING_STEPS`** (`onboarding-flow.ts:68`) and no key-entry UI in the wizard (grep for key entry in `tauri/ui/src/wizard/` = none; the only `api-key-missing` surface is `crash-banner.ts`, i.e. an error banner *after* the co-host already failed).\n- The keyless proxy path (`VIBEMIX_PROXY_JWT` / `VIBEMIX_PROXY_BASE_URL`) exists but is **not the default**, and per the proxy memory the live Gemini key behind it is the open task — so even proxy mode isn't a guaranteed silent-success path for a stranger.\n\nNet: a stranger who clears the funnel meets either a crash-banner (\"set your API key\") or silence. Combined with #1, the first session is mute-and/or-broken with zero in-wizard remedy.\n- Rating: **KILL-SHOT** (compounds #1; either one alone breaks the promise).\n- FIXABLE-BEFORE-SHIP: **YES**. Single fix: add a key/auth step to `ONBOARDING_STEPS` (paste BYO key → keychain, OR finish the keyless proxy as the install default so the stranger never sees a key). The Rust env-forward seam already exists (`sidecar.rs:44` forwards `GEMINI_API_KEY`).\n\n---\n\n### KILL-SHOT #3 — The per-deck channel map (the thing that makes it more than narration) has NO UI and even Kaan couldn't route it.\nThis is the \"it only narrated\" first impression, proven LIVE today.\n- Per-deck routing is **env-var only**: `VIBEMIX_DECK_AUDIO_CHANNELS` (`deck_capture.py:448`). The code comment itself concedes nobody sets it: `deck_capture.py:456` — *\"Kaan 2026-05-30: 'it should be global — nobody will set this [env var]'\"*. The auto-path depends on a high-confidence Rekordbox external-mixer hint (`:478`).\n- Today's real-rig capture (`LIVE-CAPTURE-2026-06-01-perception-loop.md`): even with BlackHole 16ch and the channel pair opened, **deck B audio never arrived** (`B_silent` throughout, *\"even after Kaan brought deck B's EQ up... MIDI saw it; audio channel stayed silent\"*). With only mono-master + deck-A + no deck B + no library, *\"the brain can only **narrate**... Kaan's exact complaint (sadece açıklama yapıyor).\"* The capture doc flags this routing as *\"an advanced setup.\"*\n- So the realistic first session for a stranger = single-source master, no track identity (`library_tracks=0`), no second deck → the engine's honest-grounding gate (which works) correctly refuses to talk about transitions and falls back to **describing the sound**. That reads as \"generic AI slop\" to a user even though it's technically grounded — and it's the explicit kill condition (\"reactions feel... generic\").\n- Rating: **KILL-SHOT** for the differentiated promise; **WOUND** at minimum even if mute (#1) were fixed. The product without per-deck = a narrator, which Kaan already called \"bok gibi.\"\n- FIXABLE-BEFORE-SHIP: **HARD**. Single fix: add a guided per-deck routing step to the wizard that detects Rekordbox external-mixer config and verifies signal on ch 3+4 (the live `upgrade_path=verify_rekordbox_deck_routing_or_use_manual_map` is exactly this). Hard because it depends on the user's DJ-software output config, which the app can't fully control — but leaving it env-only guarantees ~0% of strangers ever hear the differentiated product.\n\n---\n\n### WOUND — The setup funnel before any of this is a driver-install gauntlet.\nFrom `installer/companion/onboarding_copy.json`: install BlackHole (macOS Privacy & Security approval, *\"the only step Apple keeps user-driven\"*) / VB-CABLE (Windows signed-driver UAC), then a **48kHz format check** with a \"BlackHole is at {measured_khz} kHz\" fail state sending users to Audio MIDI Setup. Plus the always-on passthrough feedback-loop trap (CLAUDE.md: *\"route ONLY the DJ app into BlackHole\"* or the AI voice loops back as phantom music). A virtual-audio-driver + sample-rate + routing gauntlet is a steep climb for a \"one tap, no terminal\" promise (`onboarding_copy.json` welcome: `\"ONE TAP. NO TERMINAL.\"` — a claim the funnel can't honor).\n- Rating: **WOUND** (kills adoption for non-technical DJs; survivable for the pro target).\n- FIXABLE-BEFORE-SHIP: **YES** for polish (auto-fix 48kHz CTA already exists: `\"Fix it for me\"`); the feedback-loop trap needs a detector, not just docs.\n\n---\n\n### SCRATCH — 45s idle heartbeat auto-speak.\n`HEARTBEAT_SEC = 45.0` (`audio/constants.py:65`); idle co-host auto-speaks on that cadence. Given #1 it's currently moot (mute), and grounding strips ungrounded heartbeats (live capture: HEARTBEAT with no deck proof → `action=strip`). If it ever speaks, a fixed 45s \"I'm still here\" while a DJ is mid-focus reads as needy, but it's tunable, not fatal.\n- Rating: **SCRATCH**. FIXABLE-BEFORE-SHIP: **YES** (raise interval / gate on user-active).\n\n### FUD I'm killing — \"the engine hallucinates / is slop.\"\nEvidence says the **engine is the strongest part**. Live capture: grounding gate works *\"PERFECTLY\"* (ungrounded claims stripped, citations resolve, refused to attribute a browser now-playing to a deck). The \"100% slop\" lines Kaan saw were from the **18-commit-stale DMG**, not current source (`B-4`). Don't sell \"the AI is fake\" — the AI is honest; the problem is **starved input + mute output + a funnel that never feeds it two decks or a voice**.\n\n---\n\n### Verdict\nA stranger's 5-minute path on the current dmg: clear a driver/48kHz/routing gauntlet → finish a 4-step wizard that never asked for a key or a deck map → land in a live session that is **mute** (no MOSS model, no download), or if voice were fixed, **brain-dead** (no key → crash banner), or if both were fixed, a **mono narrator** (no per-deck) saying \"heavy grinding synth, vocal repeating.\" Three independent KILL-SHOTs, each sufficient to cause an uninstall, and the worst (#1 mute) is HARD because there is literally no host for the 700MB voice the product depends on. **The single most users-lost item is KILL-SHOT #1**: fix the MOSS distribution + auto-install-in-wizard before anything else, because every other fix is invisible behind silence.",
      "All confirmed. The DMG is properly notarized and stapled (Gatekeeper-accepted), but everything else is a kill field. No Windows artifact exists. Default mode is `direct` (BYO-key). Models total ~1.6GB of post-install downloads. Here's my verdict.\n\n## ASSASSIN — DISTRIBUTION / INSTALL: \"the stranger never hears it\"\n\n**HEAD `cb16053d`** (`live-tuning-or-brain`, 2026-06-01 20:36). Tree is moving — Codex landing concurrently (139 commits since the DMG was cut). 3-tier discipline applied: SRC = source/spec, PKG = the one DMG on disk, LIVE = the 2026-06-01 real-rig capture.\n\n### KILL-SHOT #1 — First run with no key = `sys.exit(4)`, the co-host is born mute\nThe shipped default is `mode=\"direct\"` which **requires a `GEMINI_API_KEY`** (`__main__.py:910` defaults `llm_mode` to `\"direct\"`; `config_store.py` comment confirms \"Ships defaulting to direct — the packaged-default flip to proxy is KAAN-ACTION\"). A stranger who downloads the DMG, has no `.env`, and clicks the icon hits `__main__.py:932-946`: a `[FATAL] GEMINI_API_KEY not set` and `sys.exit(4)`. The sidecar dies on boot.\n\nThe keyless escape hatch (proxy mode) is **not wired as the default** and is gated on Bravoh's `/register` being live — and there is **no first-run wizard that collects a key**. I grepped `wizard.py` + all of `tauri/ui/src` for key entry: the only hit is `crash-banner.ts:54`, which tells the user to *go hand-edit* `~/Library/Application Support/vibemix/.env`. That is the entire onboarding for the brain. A normal user cannot produce a Gemini API key, and the app never asks for one in-UI.\n- **Tier: PKG-confirmed** (default mode is in source + the DMG carries it). **KILL-SHOT.** Download → launch → instant silent death for 100% of strangers. This is *the* single thing most likely to make \"download → hear it\" fail TODAY.\n- **FIXABLE-BEFORE-SHIP: HARD.** The single fix is the one the codebase already names KAAN-ACTION: flip the packaged default to `proxy` AND confirm a live Gemini key sits behind `api.altidus.world`. Per memory (`project_vibemix_proxy_deployed_altidus`), *both Bravoh .env Gemini keys were DEAD* as of 2026-05-30 — so even proxy mode may yield a mute brain (a 200 from `/register`, then upstream 5xx on generate). Not fixable by a code edit alone; needs live infra + a key with quota.\n\n### KILL-SHOT #2 — Windows does not exist as a shippable artifact\nv1 is \"macOS + Windows\" per CLAUDE.md. There is **no `.exe`/`.msi` anywhere in `dist/`** (`find dist -iname \"*.exe\" -o \"*.msi\"` → empty; `dist/` holds only the one macOS DMG + run logs). `vibemix-core.windows.spec` exists but `codesign_identity=None` (line 366) — even if built, it would be **unsigned** (no SignPath wiring in the spec), so Windows SmartScreen throws the blue \"unrecognized app\" wall. And the WASAPI/per-deck path is unverified on real hardware — the LIVE doc is macOS+BlackHole only; nobody has run the Windows loopback path on a box.\n- **Tier: PKG (absent) / LIVE (never run).** **KILL-SHOT for half the promised platform.** Half the addressable market literally has nothing to download.\n- **FIXABLE-BEFORE-SHIP: NO (today).** Single fix: build on a Windows box + SignPath cert + a real-hardware WASAPI/per-deck pass. That's a hardware+external-approval critical path, not an edit.\n\n### WOUND #1 — arm64-only DMG: Intel Macs get a broken binary\n`dist/vibemix-core/vibemix-core: Mach-O 64-bit executable arm64`. The spec uses `target_arch=None` (line 410, \"auto-detects host arch\") — built on Kaan's M4 Max, so it's **arm64-only, not universal2**. Intel Macs (still a large installed base in 2026) will fail to run the sidecar or be refused at install. The notarization is real (`stapler validate` → \"worked!\", `spctl` → \"accepted, Developer ID Application: Francesco Fasanella\") so Apple Silicon Gatekeeper is clean — but that's Apple Silicon only.\n- **Tier: PKG-confirmed.** **WOUND** (silently excludes Intel users; they'll think the app is broken).\n- **FIXABLE-BEFORE-SHIP: YES.** Single fix: set `target_arch='universal2'` in the spec and rebuild (needs both-arch wheels available, but it's a one-line spec change + rebuild).\n\n### WOUND #2 — ~1.6 GB of un-bundled model weights download on first feature-use\nThe spec bundles `**/*.json`/`**/*.txt` + native libs but **zero `.onnx` weights** (grep of spec `datas` for `.onnx`/model files → nothing). A fresh machine's `~/.cache/vibemix/` is empty. Real local sizes confirm the download burden: **CLAP 750 MB, MOSS-TTS 728 MB, CUE-DETR 159 MB**. CLAP autodownloads \"~785 MB on first Library open\" (`clap_engine.py:170`). MOSS is *worse*: `model_assets.py:621-627` — **there is no hosted URL at all**; absent `VIBEMIX_MOSS_TTS_ARCHIVE_URL` it returns \"MOSS TTS ONNX is not hosted by default; set VIBEMIX_MOSS_TTS_DIR…\". So the free on-device voice is **un-downloadable by a stranger** unless a release build pins an archive URL.\n- **Tier: SRC+PKG.** **WOUND** (huge silent first-run download for Library; and the \"free local voice\" selling point is effectively absent for strangers). The DMG is 120 MB but the real footprint is ~1.7 GB.\n- **FIXABLE-BEFORE-SHIP: YES.** Single fix: host the MOSS archive + set the `VIBEMIX_MOSS_TTS_ARCHIVE_URL/SHA256/SIZE` pins in the release build, and surface the CLAP download as a visible progress step (the progress hook already exists at `clap_engine.py:151`).\n\n### SCRATCH — the DMG on disk is 139 commits stale\nDMG mtime 2026-05-31 19:02; HEAD 2026-06-01 20:36 → **139 commits behind** (not \"18\" — that figure is itself stale). The LIVE doc Finding 6 already caught this: the \"100% slop `[chill] That crowd vocal…`\" lines came from this stale DMG, while fresh dev-source is grounded. So **ear-testing the current DMG tests already-fixed bugs.**\n- **Tier: PKG vs SRC drift.** **SCRATCH** (process, not a shipped defect) — but it *manufactures* false KILL-SHOTs: anyone judging the product off this DMG sees slop that source no longer emits.\n- **FIXABLE-BEFORE-SHIP: YES.** Rebuild from HEAD before any ear-test or release.\n\n### FUD-check (being fair)\n- **Notarization/Gatekeeper: NOT a problem.** `stapler validate` → worked; `spctl -a` → accepted, Developer ID. The macOS trust chain is genuinely done. Don't claim Gatekeeper blocks it.\n- **\"sqlite-vec/livekit native libs missing in bundle\": NOT a problem.** The spec explicitly collects `liblivekit_ffi.dylib`, `vec0.*`, pyobjc frameworks, onnxruntime dylibs (lines 176-250). Packaging of native deps is handled.\n\n### The one-line answer to \"what most likely breaks download → hear it TODAY\"\n**KILL-SHOT #1.** A stranger downloads the (notarized, working) DMG, opens it, and the brain exits 4 because there's no key and no in-app way to add one — the default is BYO-key `direct` mode, the keyless proxy isn't the default, and per memory the proxy's upstream Gemini keys were dead. **The app is mute on first run for everyone, on the one platform that even has a binary.** It's not a packaging bug — it's that the \"download → hear it\" path was never actually closed: it terminates at \"go hand-edit a hidden `.env`.\"",
      "I have complete evidence. Writing the assault report.\n\n## ASSASSIN — Legal / Cost-at-Scale / Founder-Promise / OSS-Roast (HEAD `cb16053d`, shared/moving tree)\n\n### (a) LEGAL — Clean-room boundary is the most lethal finding\n\n**KILL-SHOT (legal). FIXABLE-BEFORE-SHIP: HARD.**\n\nThe clean-room defense is structurally the *weakest* kind: no person-firewall. The same author who wrote the \"clean-room\" ports has the actual GPL source checked out and studied it line-by-line.\n\n- `~/projects/mixxx-study/mixxx/` is a real `git remote -v → https://github.com/mixxxdj/mixxx.git` checkout with `COPYING`/`LICENSE` (GPLv2 or later) on disk, last touched 30–31 May 2026 — **days before** the 2026-06-01 port commits.\n- It ships a `scars/` extraction layer (`05-scaler-linear.md`, `07-sync-engine.md`, `02-beatgrid-model.md`, `10-serato-geob-parser.md`) that distills GPL facts, and the shipped files cite it directly: `src/vibemix/audio/miniplayer.py:9-12` says *\"numpy port of Mixxx `EngineBufferScaleLinear::do_scale` … source-verified in `~/projects/mixxx-study/MIXXX-SCARS.md`\"*.\n- The 6 ported files carry **24 line-level GPL source citations** (`enginexfader.cpp:66-77`, `enginebuffer.cpp:52`, `enginexfader.h:7`, `beats.cpp`, `AnalyzerSilence`, `AutoDJProcessor`). A true clean-room is spec-derived by someone who never saw the source; line-and-file citations of GPL internals are the opposite — they're the audit trail a plaintiff uses.\n- Also on disk: `~/projects/mixxx-study/libkeyfinder/` (Mixxx's key detector, **also GPL**) and a `03-key-detection.md` scar — so the contamination surface is wider than the 6 files NOTICE admits.\n\n**Verdict:** \"facts re-derived clean-room, no GPL code copied\" (the docstring in every file) is *probably* literally true at the code-text level — but the legal standard for clean-room (a clean-room *team* that never touched the original) is not met, and the file comments themselves document that the same author read the GPL source. On an HN/Reddit post this is the headline: *\"Apache-2.0 'AI DJ' is a line-by-line reimplementation of GPL Mixxx by someone with the Mixxx repo open.\"* Whether it's actual infringement is for a lawyer, but the **reputational** damage lands regardless of the legal outcome. Single fix is HARD because you can't un-read the source: either (1) get a written legal opinion before launch and keep `mixxx-study/` out of any public artifact, or (2) strip the line-level citations to behavioral/spec language only (reduces the paper trail but doesn't cure the firewall problem).\n\n**NOTICE / THIRD_PARTY_LICENSES — this is FUD, evidence says fine.** The LAND_QUEUE flag that `THIRD_PARTY_LICENSES.md` is \"missing\" is **false on this tree**: `git ls-files` shows `THIRD_PARTY_LICENSES.md` (tracked, with a clean clean-room reference map), `NOTICE` (rebuilt today 18:15), and `test_third_party_attribution.py` gating it. mutagen GPL handling is correct — it's an optional `serato` extra, lazy-imported, documented as \"runtime dep, never vendored\" (`pyproject.toml:180`), which is the defensible aggregation line. **Rate: FUD.**\n\n### (b) COST-AT-SCALE — the €50/mo claim survives, but only because TTS was zeroed out today\n\n**WOUND. FIXABLE-BEFORE-SHIP: N/A (already mitigated, but fragile).**\n\n- The flagged budget XFAIL (`tests/library/test_budget.py:28`) is **real but mis-aimed at the assault**: it documents that the *legacy Gemini-embedding* indexing model costs ~€897/mo at 1000 DAU (`test_true_price_naive_free_tier_exceeds_ceiling` asserts it's over ceiling). But the CLI confirms `projection_kind == \"legacy_gemini_embedding_what_if\"` and `active_embedding_backend == \"clap\"` (local/keyless) — so that €897 is a what-if for a path the product no longer uses. Embedding is now on-device CLAP = €0 server cost. **The XFAIL is dead-weight FUD against the live product.**\n- The live-brain cost was neutralized **today** (`f306c68b fix(library-cost): price live voice as local moss`, `d6bcb865 use moss for tldr narration`). `tts_chain.py` is now **MOSS-only** — `build_tts_chain()` ignores all cloud keys and returns `_build_moss_chain()` in every mode. TTS is ~70% of per-turn cost (per the cost-study memory); zeroing it (`cost.py:7` \"default TTS leg is explicit zero-cost\") collapses the budget. With MOSS (€0 TTS) + €0 LiveKit self-host (`cost.py:43`) + local CLAP, the only paid leg left is the Gemini brain text+audio-in, which is plausibly ≤€50/mo at modest DAU.\n\n**The real cost risk is reliability, not money:** because MOSS is the *only* voice with **no cloud fallback by design** (`tts_chain.py:49-51`, raises `LocalTTSUnavailable`), the product trades the cost-blowup risk for a **mute-on-failure** risk (see below). The €50/mo claim is now defensible on-paper; I'd rate the cost angle a WOUND only because the headline rests entirely on a same-day pivot that hasn't been LIVE-verified at scale.\n\n### (c) FOUNDER PROMISE — the team is shipping features TODAY; \"no new features\" is half-myth\n\n**WOUND. FIXABLE-BEFORE-SHIP: N/A (it's a discipline call, not a code fix).**\n\n- **17 `feat()` commits, ALL dated 2026-06-01 (today)** — including `feat(learn): grade cue placement against beatgrid`, `feat(library): expose sequence novelty dial`, `feat(intel): voice transition judge evidence`, `feat(observability): ground spoken ai turns`. This is a feature spree on the literal day this assault runs. Whole-history ratio is **737 feat : 325 fix** — a 2.3:1 build-to-fix posture, not a stabilize-to-ship one.\n- BUT the \"no new features\" promise I can find in-repo is *post-launch-scoped*, not a pre-launch freeze: `docs/post-launch-playbook.md:70` reads *\"No new feature work; only critical-bug hotfixes\"* under the **T+24h→T+48h** (Day-2) section. So today's feats don't violate *that* document. The \"Kaan promised Francesco no new features\" claim (per session memory) has **no dated artifact in the repo** — I cannot substantiate it, so I won't assert a broken promise; I assert the *trajectory*: this is a team still actively building, not freezing for ship. If the promise to Francesco is real and current, the git log directly contradicts the spirit of it.\n\n### (d) OSS-ROAST — what a hostile engineer finds in 10 minutes\n\n**WOUND (reputational). FIXABLE-BEFORE-SHIP: YES.**\n\n1. **The Mixxx clean-room story (above)** — the #1 roast, lands hardest.\n2. **`__main__.py` = 7,416 lines, 115 top-level defs/classes** (`wc -l`, grep count). A single god-file entry point is the easiest \"this is AI-generated sprawl\" dunk on HN. The docstring even admits it (`__main__.py:841` \"Verbatim port of cohost_v4.py:1925-2080\").\n3. **91 MB of `.planning/` ships in every clone — 1,119 tracked files** (`git ls-files .planning | wc -l`, `du -sh`). This includes the entire GSD planning corpus, eval-runs, packets, and handoffs. A cloner gets a 91MB planning archive they never asked for; it's also where the *internal* model names (\"Gemini\"), cost-study, and \"co-host slop\" admissions live — i.e. the roast material is **shipped inside the repo**. The session memory itself flags `recent_moves=[]` ungrounded-slop as the \"#1 live bug.\"\n4. **118 `.py` scripts in `scripts/`** ship in the clone too — most of the last-50 commits are `test(...) clean ... lint` churn over them, signaling a repo carrying heavy dev scaffolding into the public artifact.\n5. **Defused FUD (be fair):** the shipped `src/vibemix/` is genuinely clean — only **9 TODO/FIXME/HACK markers across ~105k LoC**. The 67-symbol orphan inventory (`.planning/codebase/orphans.csv`) looks alarming but I verified two: `validate_and_degrade → decide` is LIVE-wired to `runtime/suggestion.py:777` (codegraph_callers), so the `intel/` \"orphans\" are mostly public-API surface, not dead code. No fake demos found. **Rate those: FUD.**\n\nThe single fix for the roast surface is cheap and high-leverage: `.gitignore` or split `.planning/`, `scripts/dev-only`, and `mixxx-study` references out of the published OSS tarball, and break up `__main__.py`. That doesn't cure the legal firewall problem, but it removes 3 of the 4 easy dunks.\n\n---\n\n### MOST LETHAL (ranked)\n\n1. **KILL-SHOT — Mixxx GPL clean-room boundary (a).** Reputationally + legally the highest-variance item: the author's own checked-out `mixxxdj/mixxx` GPL repo + 24 line-level source citations + a `scars/` fact-extraction layer make the \"clean-room\" label legally contestable and PR-toxic. **FIXABLE: HARD** — needs a legal opinion + de-citation, not a code change.\n2. **WOUND — feature-spree vs. ship-discipline (c)** and **MOSS-mute-no-fallback reliability (b/tts_chain.py:49-51)**: the €50/mo claim is only safe because TTS was zeroed *today*, and the trade is a co-host that goes silent (`-> tts: unavailable … voice muted, no cloud fallback`, `__main__.py:223`) if the local model is missing on a stranger's machine — that's the 5-minute-uninstall risk the founder fears, just relocated from cost to silence. **FIXABLE: YES** (ship MOSS in the bundle as a hard install gate, which `619e83ca fix(packaging): require moss model source for release` appears to attempt — verify it's PKG-tier, not just SRC).\n3. **WOUND — 91MB `.planning/` + 7.4k-line `__main__.py` OSS-roast surface (d).** **FIXABLE: YES**, cheaply.\n\n**Proof-tier note:** everything above is **SRC-tier** (source + git log + codegraph). The MOSS-mute path, the €50/mo headline, and \"require moss model source for release\" are *unverified at PKG/LIVE tier* — I did not open the signed build or run a rig capture, so whether a stranger's DMG actually contains MOSS and speaks is **not proven here**."
    ],
    "verdict": "All kill-shots verified against live code. The default LLM mode is `direct` (needs `GEMINI_API_KEY`) with proxy mode to `api.altidus.world` available but B-3 says proxy brain passthrough is unconfirmed live. I have everything. Synthesizing now.\n\n## vibemix Ship Synthesis — the brutal one-pager (HEAD `cb16053d`/`d17afead`, tree moving, Codex landing concurrently)\n\n**VERDICT: FIXABLE-NEAR-MISS.** The brain is real and honest — it grounds on live audio+MIDI and *refuses to slop* (proven LIVE today). But shipped as-is, a stranger's co-host is **silent on first launch** (MOSS weights aren't bundled or hosted) and the shipped DMG is **140 commits stale**. The engine earns the \"real DJ friend\" promise; the packaging strands it. One reason it's not \"credible today\": **a fresh machine cannot speak.**\n\n---\n\n### 1. WHAT ACTUALLY SHIPS (real vs claim)\n- **REAL:** Listens to master audio + FLX4 MIDI, detects events, emits **grounded, anti-slop** reactions. Proven LIVE 2026-06-01 (`LIVE-CAPTURE...perception-loop.md`): citations resolve, ungrounded HEARTBEAT → `action=strip` → \"I can't call that a transition until I have clear two-deck proof.\"\n- **CLAIM vs REAL — voice:** \"talks back into your headphones\" → on any machine but Kaan's, **mute**. MOSS weights not in the spec, `model_assets.py:624` literally says *\"MOSS TTS ONNX is not hosted by default.\"*\n- **CLAIM vs REAL — two decks:** \"deck-aware\" → in practice **master-mix + MIDI only**; deck B stayed silent on the real rig (per-deck BlackHole routing is an advanced setup most users won't do). Brain honestly reports `B_silent` rather than inventing it.\n- **REAL but thin:** With starved input (no library, mono master, one deck) the co-host can only **narrate** (\"heavy grinding synth, vocal repeating\") — Kaan's \"sadece açıklama yapıyor\" complaint. Root cause = input, not code.\n- **NOT in v1 reality:** Mascot 3D, screen-vision live (`dj_cohost.py:2199` `screen_jpeg = None`, deliberate), Mixxx OSC/key/beatgrid (all NOT-FOUND), beatmatch \"Mastered\" credit (no producer).\n\n---\n\n### 2. THE 5 WAYS THIS DIES (ranked by lethality)\n\n**① Fresh machine = silent co-host.** MOSS weights neither bundled nor hosted.\n- Evidence: `vibemix-core.macos.spec` has no moss in `datas` (only a lazy-import comment :139); `model_assets.py:624-626` — manual `VIBEMIX_MOSS_TTS_DIR`/`ARCHIVE_URL` only; `local_tts_enabled()` = flag AND model cached → false on every new install.\n- Tier: **PKG.** **KILL-SHOT** (breaks core promise: a co-host that can't talk). **FIXABLE-BEFORE-SHIP: YES** — host the MOSS archive + pin `VIBEMIX_MOSS_TTS_ARCHIVE_URL`/SHA/size with a first-run auto-download (B-1).\n\n**② The shipped DMG is 140 commits stale + arm64-only.** Ear-tests on it test already-fixed bugs; Intel Macs excluded.\n- Evidence: `dist/vibemix-0.0.1.dmg` dated 31 May; `git log --since` = **140 commits** after it (packet said 18; tree moved). LIVE Finding 6: the 100%-slop `[chill]` lines came from this stale binary, not current source.\n- Tier: **PKG.** **KILL-SHOT** (every install ships known-bad behavior). **FIXABLE: YES** — rebuild+sign+notarize from clean HEAD *after* ① lands; add x86_64 CI leg OR scope v1 \"Apple Silicon\" in launch copy (B-4).\n\n**③ Keyless brain (proxy) unconfirmed live.** Default mode is `direct` → needs `GEMINI_API_KEY`; without it: `[FATAL] co-host cannot speak` (`__main__.py:933`). Proxy mode points at `api.altidus.world` (:913) but the brain round-trip was never confirmed live.\n- Tier: **LIVE (infra).** **WOUND** (BYO-key users work; keyless strangers — the actual market — are unproven). **FIXABLE: HARD** — needs a valid upstream Gemini key on the Bravoh proxy + one live round-trip check (B-3, Kaan/infra).\n\n**④ Per-deck identity needs a rig 90% of users won't build.** Deck B silent even when MIDI saw the move; library empty → never names tracks.\n- Evidence: LIVE Findings 2,5; `deck_pairs=A:0,1+B:2,3` opened but `B_silent`; `library_tracks=0`.\n- Tier: **LIVE.** **WOUND** (the headline \"deck-aware\" demo won't reproduce on a stranger's rig). **FIXABLE: YES (by scoping)** — ship the **master+MIDI common case** as v1; keep per-deck as an honest-bonus that lights up for advanced rigs (Product note, Kaan-decision).\n\n**⑤ Cost claim can't be backed.** \"free at scale / <€50\" copy un-provable.\n- Evidence: `pricing.py:117` asserts on-device MOSS = no per-call cost (true *only if* ① ships); budget XFAIL (H7) until re-baselined.\n- Tier: **SRC.** **WOUND** (legal/credibility if it appears in launch copy). **FIXABLE: YES** — keep `library budget` internal-only; strip the claim from launch copy until re-baselined.\n\n---\n\n### 3. FUD GRAVEYARD (scary-sounding, evidence says FINE — stop worrying)\n- **\"The co-host slops / hallucinates\"** — FUD on current source. LIVE-verified the grounding gate strips ungrounded lines and refuses to invent decks. The slop Kaan heard was the **stale DMG** (kill-shot ②), not the engine.\n- **\"Mixxx / OSC / key-detect / beatgrid features are broken\"** — not broken, **never existed** (all NOT-FOUND). Just DROP the claims from copy; nothing to fix.\n- **\"Clean-room Mixxx ports = legal risk\"** — already mitigated: `NOTICE`, `NOTICE.md`, `THIRD_PARTY_LICENSES.md` now exist (S2 landed; packet was stale). Needs only a 1-line Kaan posture sign-off.\n- **\"demo_mode violates trust-the-audio\"** — already deleted (S3 landed; zero importers).\n- **\"Screen-vision is dead\"** — intentional: `screen_jpeg=None` is a deliberate load-bearing invariant (`dj_cohost.py:32`), not a bug. DROP the live-vision claim, keep the seam off.\n\n---\n\n### 4. WHAT TO DO WITH THIS (routing per kill-shot)\n1. **① MOSS bundling → new Codex lane.** Build: host the MOSS-TTS-Nano ONNX archive, pin URL+SHA+size, auto-download on first run with a `drive-vibemix`-captured \"preparing voice / unavailable: [reason]\" first-run state (folds in B-2). *This is the release-critical lane.*\n2. **② DMG rebuild → Codex lane, but SEQUENCED after ①.** Rebuild+sign+notarize from clean HEAD; decide x86_64 CI leg vs \"Apple Silicon v1\" copy (Kaan). Don't rebuild before ① or you re-ship the mute.\n3. **③ Proxy brain → Kaan-only infra.** Put a live Gemini key on the `api.altidus.world` proxy; run one keyless round-trip. Blocks the actual go-to-market user.\n4. **④ Per-deck scope → Kaan-only product decision** (then a small copy edit, no engineering): adopt the master+MIDI v1 posture; per-deck = honest bonus. Optionally a **research spike** (R3) for a routing-wrong detector.\n5. **⑤ Cost claim → DROP-the-claim now** (keep `budget` internal); re-baseline later (R5).\n\n**THE SINGLE NEXT MOVE:** Spin the **MOSS-weights hosting + first-run auto-download** Codex lane (①). Until a fresh machine can speak, nothing else matters — and it must land *before* the DMG rebuild so the signed build isn't born mute."
  }
}