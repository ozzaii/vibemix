{
  "summary": "Map every orphaned Mixxx-derived intelligence engine to its live-brain wiring: what crude placeholder it replaces, what it unlocks (narration->coaching), exact producer+call-site, build order, mixer_physics keystone spec.",
  "agentCount": 8,
  "logs": [],
  "result": {
    "cards": [
      "I have everything needed. The `caused_by_move=true` atom is structurally FORBIDDEN — there is no producer that licenses causal attribution. This confirms the baseline. Let me write the final menu.\n\n## BASELINE — What the Live Coach Brain Actually Eats Today (HEAD `ce909a87`)\n\n**The pipe.** Per-event live reaction → `AICoach.build_prompt` (`state/coach.py:1008`, the only non-test caller of `evidence_line`) → `evidence_line` (`coach.py:339-649`) builds the `\" | \"`-joined evidence string → `DJCoHostAgent` wraps it with per-event framing + the attached-audio context clause (`agent/dj_cohost.py:383-490`, `_build_attached_audio_context_clause`) → Gemini. The MIX_MOVE per-event prose is `coach.py:775-823`. This is the LIVE path; everything below is a real field in that string.\n\n### The honest input menu (every field, with source line + class)\n\n| Field | Built at | Class | Note |\n|---|---|---|---|\n| `hearing[rms sub low mid high bpm]` | `coach.py:371` from `state.bands`/`state.rms`/`state.bpm` | **RICH** | Real measured master-mix DSP. But it's GLOBAL mix energy, not per-deck. |\n| `Δ[kick density; RMS; onset density]` | `coach.py:383-395` via `render_delta` vs `state.prev_perceive` | **RICH** | Real tick-over-tick measured deltas; floor-gated, abstains on cold prior. |\n| `track=…` / `track=unknown` | `coach.py:399-402` (`audible_track_confidence>=0.3`) | RICH-ish | CLAP/now-playing identity; honest-unknown below floor. |\n| `deck=<audible_deck>`, `set_time` | `coach.py:404-405` | bookkeeping | — |\n| `decks[A=… B=… camelot bpm]` + `blend[<why> harmonic-ok/clash/drift]` | `coach.py:418-471` via `compute_relation` (TrackRelation S6) | **RICH** | Deterministic Camelot+tempo relation, the one well-wired intelligence join. Gated to ≥2 resolved decks. |\n| `deck_context[…]`, `deck_lane_context`, `deck_reference_context`, `deck_source_context` | `coach.py:472-483` | mixed | Routing/identity bookkeeping, honest about what's resolved. |\n| **`mixer_context[xfader=<label> deck_conf … decks=A(vol low mid hi filter play)]`** | `coach.py:485` → `render_mixer_context` `deck_context.py:2146` → `_mixer_deck_summary:3577` | **CRUDE** | Raw 0-127 MIDI → coarse text tiers (`killed/cut/flat/boost`). `xfader` via `_xfader_label:3446`. A label of controller POSTURE, never a measured acoustic effect. |\n| `deck_audio_context[source=global_mix isolated_decks=false routing=…]` | `coach.py:488` → `render_deck_audio_context:946` | **CRUDE** | Routing is a heuristic from `_deck_route_scores` (vol × `_xfader_factor:3458`, a hardcoded 4-bucket gain). Packet itself declares `isolated_decks=false`. |\n| `genre=…` | `coach.py:500` (conf≥0.5) | RICH-ish | — |\n| `phase_age`, `track_age`, `phase_history`, `recent_tracks`, `set_arc`, `trajectory[…]` | `coach.py:504-560` | RICH | Real measured history/arc. |\n| **`recent_moves[8s]: 2.0s ago <label>` / `NONE`** | `coach.py:512-519` from `state.recent_moves` | **CRUDE** | MIDI move LABELS only (\"killed lows on A\") — a text name of a knob turn with NO measured audio effect attached. |\n| `grounding_refs[[midi:…@t] [mix:…]]` | `coach.py:521` → `render_grounding_ref_context:2040` | citation index | Exact citable atoms that exist in the registry — what the brain is ALLOWED to cite. |\n| `live_evidence[refs= mix= midi= rule=evidence_categories_not_quality_verdict]` | `coach.py:528` → `render_live_evidence_context:1985` | **CRUDE-by-design** | Lists evidence CATEGORIES, then explicitly tells the brain they're \"not quality verdict.\" |\n| `evidence_corpus[ev,aud,mix]`, `recall[…]`, `lens=…` | `coach.py:580-647` | meta | Citation-corpus counts / past-session fence / tutor-lens marker. |\n| MIX_MOVE-only: `move_effect_context[deltas=…; deck_deltas; rule=…_not_causal_proof]` | `coach.py:785` → `render_move_effect_context:2070` (also `dj_cohost.py:467/667`) | **CRUDE-by-design** | The closest thing to move→effect physics. Carries the global `audio_delta` (RICH) but `deck_deltas`/`deck_windows` only populate when `deck_audio_capture_enabled` (4-ch per-deck, default-OFF), and EVERY path stamps `rule=move_audio_timing_not_causal_or_quality_proof` / `dsp_delta_not_causal_proof`. |\n\n### The 3-4 crude / forbidden-causal fields that most starve the brain\n\n1. **`mixer_context` deck EQ as 0-127→text tiers** (`render_mixer_context:2146` / `_mixer_deck_summary:3577`). The brain is handed `A(low=killed mid=flat hi=boost)` — a name of where the knob SITS, with no measured \"your low-cut thinned the master by X.\" This is exactly why it narrates (\"heavy grinding synth\") instead of coaching (\"your EQ move thinned the top end\"). **The loudness/spectral-causality receipt that would replace it is NOT-FOUND** (LUFS/`expected_move_effect`/`mixer_physics` exist only as a `.planning` proposal).\n\n2. **`recent_moves[8s]` = bare MIDI labels** (`coach.py:512`). \"2.0s ago killed lows on A\" with zero attached audio consequence. The brain knows a knob moved but not what it DID to the sound — the move and the effect are never causally joined.\n\n3. **`move_effect_context` / `deck_audio_delta` framed `not_causal_proof`** (`render_move_effect_context:2070`, `render_deck_audio_delta_context:1165`, rules `deck_context.py:325/338`). Even when DSP deltas ARE measured around a move, the prompt structurally forbids attributing them to the move: the atom `caused_by_move=true` is in `_DECK_AUDIO_DELTA_CONTEXT_FORBIDDEN_ATOMS` (`deck_context.py:330`). The result-boundary `apply_live_claim_guard` (`deck_context.py:2313`) + `live_claim_policy:2191` then strip any causal/quality verdict the model emits, because no producer licenses it. The MIX_MOVE prose itself (`coach.py:817`) tells the brain: *\"live_evidence categories, not causal proof or skill grade.\"*\n\n4. **`deck_audio_context: source=global_mix isolated_decks=false`** (`render_deck_audio_context:946`). The authoritative audio Part is the master mix; per-deck stems require the default-off 4-channel `deck_audio_capture_enabled` path. So the brain literally cannot hear \"deck A's top end\" vs \"deck B's\" — only the blended result — which is the physical reason move→effect attribution is forbidden upstream.\n\n**Net:** today the brain eats real GLOBAL master-mix DSP + harmonic relation (the one good join) + a pile of 0-127→text controller-posture labels and history bookkeeping. The single missing ingredient that gates \"coach\" mode is a **measured move→acoustic-effect attribution** — every candidate field that could supply it is either default-off (per-deck capture) or stamped `not_causal_proof` because no producer licenses the causal claim.",
      "I have everything I need. The registry is permissive (accepts any `source`/`key` atom), so a new `[move_effect: ...]` citation source needs no registry schema change — it just needs to be registered and emitted into `grounding_refs`. Here is the complete wiring map.\n\n## KEYSTONE — `mixer_physics.py` / Move-Effect Resolver (the R-SLOP killer)\n\nHEAD `ce909a87` (branch `live-tuning-or-brain`). All claims verified against live source; codegraph confirms absence.\n\n### Design recovered (proposal packets — NOT in src)\nThe keystone is specified across three recovered packets under `.planning/packets/2026-06-01/recovered/wf_d9ddbeed-f5f__*`:\n- `ALT6__vibemix-capabilities-unlocked...` (lines 1-13) — **IDEA 1 \"Grounded Move-Effect Resolver\"**: a `src/vibemix/state/mixer_physics.py` clean-room of Mixxx `enginexfader.cpp` (`mixxx_xfade_gains(position, transform, mode)` — additive/const-power/reverse) + `controlbehavior.cpp` (`db_taper_gain` line-fader dB taper). Given a known move read off the deck_mixer snapshot, it computes the **expected orientation-gain delta in dB per deck**, then double-gates: \"claim licensed ONLY when computed-expected-sign ∧ measured-sign agree and magnitudes within tolerance.\" It explicitly names replacing the crude bucket `_xfader_factor` and emitting a citation `[move_effect: xfaderB +17dB ✓measured +14dB]`.\n- `ALT1__mixxx-eq-filter-dsp-the-r-slop-grounding-unlock` (lines 134-170) — the EQ half: a sibling `src/vibemix/intel/eq_move_model.py::predicted_band_gains(move, fs) -> dict[str,float]` (RBJ-cookbook biquad magnitude `∫|H|²` per band → predicted `{sub,low,mid,high}` dB), to upgrade `_has_unsupported_mixer_low_kill_claim` from \"controller-tier says so\" to \"controller move ∧ measured spectral delta matches prediction\" — and to ground EQ moves on **master-only / no-controller rigs** by inferring from the measured band-ratio shift alone.\n\n### Live-source verification\n- **ABSENT in src**: `mixer_physics.py`, `eq_move_model.py`, `expected_move_effect`, `predicted_band_gains`, `mixxx_xfade_gains` — `ls` = \"No such file\"; `grep src/` = zero hits; `codegraph_search \"mixer_physics\"` / `\"expected_move_effect\"` = \"No results found\". SPEC-ONLY.\n- **Crude placeholder LIVE**: `_xfader_factor` (`deck_context.py:3458`) is a 4-step 0-127→{0.0,0.3,0.7,1.0} bucket, consumed at `deck_context.py:3497` (`score = volume * _xfader_factor(...)`) and `suggestion.py:220`. No dB, no curve, no audio confirmation.\n- **Context-emitter LIVE but non-causal**: `render_move_effect_context` (`deck_context.py:2070`) IS in the live prompt — called at `dj_cohost.py:467` and `:667` (prompt builder), `coach.py:785`, `runtime/coach.py:789`. But it attaches raw deltas tagged `rule=dsp_delta_not_causal_proof` / `rule=move_audio_timing_not_causal_or_quality_proof` (lines 2096-2098) — i.e. it shows the delta but forbids causality.\n- **Guard REFUSES causality LIVE**: `apply_live_claim_guard` (`deck_context.py:2313`) is wired at the reaction result-boundary `dj_cohost.py:2964`. At lines 2371-2391 it detects a move+delta+causal-verdict and returns `LIVE_MOVE_EFFECT_HELD_REPLY` with `policy=\"move_effect_not_verdict\", reason=\"dsp_delta_not_causal_proof\"`. The low-kill guard `_has_unsupported_mixer_low_kill_claim` (`:2585`) gates on `controller_connected` (`:2586`) + `_control_now_tier(raw,\"low\")` ∈ {killed,deep-cut} — pure MIDI label, zero audio.\n- Codegraph found \"no callers\" for both guard/context (Python dynamic-dispatch blind spot) — refuted by grep: both are live-imported and called in `dj_cohost.py`. The producer's true absence is the reliable codegraph signal.\n- Existing primitives to wire into are all present & torch-free: `band_features.py:30 band_energy_ratios` (bands `sub 20-100, low 100-300, mid 300-4000, high 4000-8000`), `deck_context.py:1794 render_audio_delta_items`, `deltas.py:24 DELTA_FLOOR=0.10` + `render_delta`. `EvidenceRegistry` (`evidence_registry.py:222`) is **permissive** — a new `move_effect` citation source needs no schema change.\n\n### WIRING CARD\n- **STATUS**: SPEC-ONLY (codegraph_search `mixer_physics`/`expected_move_effect` = \"No results found\"; `ls` both target files = \"No such file\"; design exists only in `.planning/packets/2026-06-01/recovered/wf_d9ddbeed-f5f__ALT1/ALT6`). The *guard it upgrades* is NEGATIVE-GUARD-ONLY today (`apply_live_claim_guard:2379` refuses the causal claim).\n- **WHAT IT DOES**: Computes the predicted spectral/loudness effect of a known MIDI move (xfader/line-fader → per-deck orientation-gain dB; EQ-knob → per-band dB) on the moving lane, and licenses a causal claim only when the prediction's sign/magnitude agrees with the measured on-lane audio delta.\n- **CRUDE LIVE PLACEHOLDER IT REPLACES**: `_xfader_factor` (`deck_context.py:3458`, used `:3497`; `suggestion.py:220`) — a 4-bucket 0-127 step with no dB curve and no audio confirmation. EQ side: `_has_unsupported_mixer_low_kill_claim` (`deck_context.py:2585`) — controller-tier MIDI label only, no spectral check.\n- **UNLOCKS (user HEARS differently)**: narration *\"heavy grinding synth, the low end feels thick\"* → coaching *\"that crossfader sweep just brought deck B up about 14 dB — the new track's the loud one now, clean handoff\"* and *\"your EQ low-cut on A pulled the sub down ~20 dB — bass swapped cleanly to B, no mud in the overlap.\"* On a master-only rig (no controller) it stops going mute on EQ moves: the measured sub+low crater alone licenses *\"you just killed the lows there.\"*\n- **WIRING**: build `state/mixer_physics.py::expected_move_effect(move, deck_mixer, audio_window) -> ExpectedEffect{deck, predicted_db, band}` (+ sibling `intel/eq_move_model.py::predicted_band_gains(move, fs)`) → call it inside `live_claim_policy`/`apply_live_claim_guard` at the existing move-effect refusal branch (`deck_context.py:2371-2391`), and beside the low-kill guard `:2342`. Where today it unconditionally returns `LIVE_MOVE_EFFECT_HELD_REPLY`, instead: if predicted-sign ∧ measured `render_audio_delta_items` sign agree beyond `DELTA_FLOOR` (over the post-move `deck_audio_window` window, not the single frame — Mixxx ramp/group-delay lag), **license** the claim. Emit into `render_move_effect_context` (`deck_context.py:2085-2098`), replacing `rule=dsp_delta_not_causal_proof` with the causal evidence atom: `[move_effect: A_low_cut -> sub_-20dB ✓measured -18dB]` (registered via `EvidenceRegistry.register`, permissive source `move_effect`, surfaced in `grounding_refs`). Add a `_MOVE_EFFECT_LICENSED_RE` exemption so the guard's `effect_claim` branch (`:2379`) passes when this atom is present.\n- **CLASS**: BUILD (from spec) — the producer module is net-new (~30 lines numpy xfader curve + ~60 lines RBJ biquad magnitude), but every consumer seam (guard branch, context emitter, registry, delta machinery) already exists and is live. Rough effort: **1-2 days** (xfader/level half is the faster ~half-day; EQ-band biquad-magnitude half + master-only inference is the rest).\n- **LEVERAGE on narrator→coach**: **HIGH** — this is the #1 unlock. It directly flips the most-cited live slop bug (\"you brought the faders up\" with `recent_moves=[]`) from a *refused* claim into a *grounded, double-gated* causal coaching line, and is the only path that makes EQ/fader coaching work on the common master-only rig. It converts \"trust the audio\" (Invariant #3) from a label-lookup into a quantitative predicted-vs-measured test.\n- **DEPENDENCIES**: none blocking — all primitives present (`band_energy_ratios`, `render_audio_delta_items`, `DELTA_FLOOR`, permissive `EvidenceRegistry`). For per-deck (not master-sum) attribution the xfader/EQ-knob move strings must carry a side — already in the `moves` vocabulary (`event_detector.py:33-37` fires on `'killed'/'_low:'/'_mid:'/'_hi:'/'_filter:'/'xfader'`). Optional follow-ons that share the same module (NOT blockers): Move-Intent classifier (curve-mode tool attribution from `midi/profiles/`), the practice mixer rewrite (`miniplayer.py:69`), and Earned gain-staging/curve nodes (`skill_recognizer`).",
      "Confirmed. `AICoach.build_prompt` is called inside `llm_node` at dj_cohost.py:2149/2152 (within the live coach path, llm_node def @1924). All evidence gathered. Writing the card.\n\n## GOLD — LUFS / Loudness Receipt (WIRING MAP, HEAD `ce909a87`)\n\n- **STATUS**: **ABSENT.** codegraph `codegraph_search \"lufs\"` → \"No results found\". `codegraph_search \"loudness\"` → 7 hits, all offline library-energy scoring or UI mascot constants — the only impl is `library/energy.py:321 _loudness_dbfs(rms, samples)` (P80 of busy-frame RMS in dBFS, `ENERGY_LOUDNESS_WINDOW=(-30,-6)` @ `audio/constants.py:320`), called only from `library/energy.py:365` (offline track-energy feature, never live, never in the coach prompt). No `integrated_loudness`/`momentary`/`K-weight`/`EBU-R128` anywhere in `src/vibemix/`. `EVIDENCE_SOURCES` (`evidence_registry.py:130`) = `{ev, aud, midi, track, screen, mix, tend, key, recall, exemplar, cue, judge}` — **no `gain`/`loudness` source exists.**\n\n- **WHAT IT DOES**: Short-term (≈3 s) integrated LUFS + true-peak/headroom from the master capture buffer, so the co-host has a perceptual energy/level receipt instead of a raw linear-RMS number.\n\n- **CRUDE LIVE PLACEHOLDER IT REPLACES**: `src/vibemix/state/coach.py:371` (and the compact twin `:677`) — `f\"hearing[rms={state.rms:.3f} sub=... bpm=...]\"`. `state.rms` is the raw linear RMS written once at `refresh.py:672` (`state.rms = rms`, straight off `feats`), surfaced into the live prompt via `evidence_line`/`_evidence_line_compact` → `AICoach.build_prompt` (`coach.py:1008`) → `DJCoHostAgent.llm_node` (`dj_cohost.py:1924`, calls `build_prompt` at `:2149/:2152`). The Δ-line at `coach.py:388` also feeds raw `state.rms` as \"RMS\" delta — same crude scalar. Raw RMS is unitless, gain-staging-blind, and meaningless to a DJ.\n\n- **UNLOCKS (user HEARS differently)**: narration *\"heavy grinding synth, the energy's really up right now\"* → coaching *\"you're sitting at −5 LUFS on that drop, master's about 3 dB hot — pull the trim a touch, you've got no headroom left\"* / on a quiet passage *\"that breakdown dropped to −18, nice and dynamic — you've got room to slam the next drop\"*. The honest energy verb (\"hot\", \"loud\", \"got headroom\", \"clipping the red\") instead of today's vague/mute. Today the brain literally cannot say \"too hot\" — it has no level reference and the guard forbids the verdict.\n\n- **WIRING**:\n  - **Producer (BUILD)** — a short-term LUFS estimator over the master ring buffer. Approximate EBU-R128: K-weight the captured PCM (two-stage biquad: high-shelf ~+4 dB >2 kHz + high-pass ~38 Hz — both numpy `scipy.signal`-free, hand-rolled biquad like the existing detector DSP in `state/detectors/_dsp.py`), mean-square over a ~3 s gated window, `-0.691 + 10*log10(ms)`. True-peak/headroom = `20*log10(max(|x|))` over the same window. **No new heavy dep** — reuse PyAV-decoded PCM already in the capture ring + numpy. Fold it into the existing feature pass so it rides the single-writer loop.\n  - **Live call-site (the single insertion point)** — `src/vibemix/state/refresh.py:672`, right beside `state.rms = rms` inside the `with state._lock:` block: add `state.lufs_st = <est>` / `state.headroom_db = <est>` (new fields on `MusicState` @ `music_state.py:31`, next to `rms: float = 0.0`). Then render at **`coach.py:371`**: extend the hearing atom to `hearing[rms=… lufs={state.lufs_st:.1f} headroom={state.headroom_db:.1f} sub=… bpm=…]` (mirror in the compact twin `:677` and add a \"loudness\" entry to the Δ-tuple at `coach.py:386`). This is single-writer-clean (Invariant #1) — only the refresh loop writes it.\n  - **Grounding evidence atom (so live_claim_guard LICENSES the gain claim)** — register an `aud:lufs@<t>` / `aud:headroom@<t>` time-keyed atom in `EvidenceRegistry` at the refresh registration seam (`refresh.py:~1047`, where `aud:` atoms are noted). `aud` is ALREADY in `EVIDENCE_SOURCES` and the linter whitelist, so a `[aud:lufs@45.2]` citation parses and validates with **zero grammar change** — unlike a net-new `gain:` source which would require a synchronized edit across `EVIDENCE_SOURCES` + `_INNER_ATOM` + `citation_linter` + `matrix.py`. Because the atom now physically exists and is registered, the brain can cite it and assert a leveled judgement — it is no longer the un-licensed `quality_verdict=` that `deck_context.py:318/329/342` forbids (those guards fire because *no producer backs the verdict today*; a registered loudness receipt is the producer). Caveat: keep the brain to a leveled *observation* (\"you're at −5, that's hot\") rather than a deck-causal verdict — the causal guards (`deck_audio_delta_not_causal_proof`) stay correct.\n\n- **CLASS**: **BUILD** (from spec). The DSP is ~80 lines of well-trodden K-weight biquad + RMS-in-dB (the `_loudness_dbfs` offline version proves the band-energy plumbing already exists; this is the live, K-weighted, registered variant). Field + render + atom registration are mechanical. **Rough effort: 1–2 days** (most of it: getting the K-weight coefficients honest + picking the gating window, then the live-claim-guard licensing test so a cited loudness claim survives the linter while a `quality_verdict` still strips).\n\n- **LEVERAGE on narrator→coach**: **HIGH.** This is the single most-cited evidence atom on every music-reaction turn (`hearing[…]` is in EVERY non-silent prompt). Replacing the meaningless raw RMS with a perceptual, unit-bearing level receipt upgrades *every* energy/intensity utterance the co-host makes — the highest-frequency surface in the whole prompt. It is also the only path to an honest \"you're clipping / pull the trim\" class of coaching, which today is impossible (no level reference → the brain either stays mute or vibes vaguely). Net-new signal with the widest blast radius of the GOLD set.\n\n- **DEPENDENCIES**: \n  1. New `MusicState` fields `lufs_st` / `headroom_db` (`music_state.py:31`) — must land WITH the refresh writer (single-writer invariant; a field read before it's written reads the 0.0 default → false \"silent/no-headroom\").\n  2. The `aud:lufs@t` registration at `refresh.py:~1047` must land in the SAME change as the prompt-render, or a cited `[aud:lufs@…]` rides un-validated (the silent-poisoning-hole pattern the registry comments warn about).\n  3. **Earned \"Clean Gain Staging\" node — does NOT yet exist.** `_SKILL_IDS` (`learn/progress.py:81`) = `(deck_control, beatmatching, eq_mixing, harmonic_mixing, transitions, phrasing_performance)` — no gain-staging competency. The concept lives ONLY as the lesson transcript `learn/transcripts/course_1_anatomy/09_master_booth_headphones.json` (\"anything above minus six on the meter is the red zone: that is clipping, not loudness\") — a *lesson*, not a tracked/recognizable skill. To wire the Earned dependency you'd add a 7th `_SKILL_ID` (e.g. `gain_staging`) AND teach `skill_recognizer._candidate_skills` (`skill_recognizer.py:108`) to credit it off a cited `aud:headroom`/`aud:lufs`-backed live demonstration — that recognizer is the same MAST-03 cited-demo gate beatmatching uses (`learn/skill_recognizer.py:57` → `skill_tree.record_live_demo`). So the LUFS receipt is the *prerequisite producer* the Earned \"Clean Gain Staging\" node would depend on — without the atom there is nothing to cite, so the node can never reach \"Mastered\". (Adding that node is a separate FINISH-class follow-on, downstream of this BUILD.)",
      "This is the keystone. Line 2371-2391: even when there ARE moves AND there ARE deltas, the moment the model says a control word + a causal verdict (or a bare verdict) about the effect, the guard **strips it to `LIVE_MOVE_EFFECT_HELD_REPLY`** with `reason=\"dsp_delta_not_causal_proof\"`. The reason is structural: the delta is a *time-coincident* DSP change, with no producer asserting the band-change is *attributable to the EQ move*. That's exactly the narration trap.\n\nI have everything needed. Producing the wiring card.\n\n## GOLD — EQ-move / spectral receipt (turn the negative guard into a licensed producer)\n\nHEAD `ce909a87` (branch `live-tuning-or-brain`). Every claim below is file:line-pinned. This card documents the single highest-leverage narrator→coach unlock: the brain already *receives* spectral change next to a MIDI move but is *structurally forbidden* to attribute it.\n\n### Why the guard gags EQ causality today (the trap, proven)\nThe live coach prompt already carries the raw spectral change AND the move, but a result-boundary guard scrubs any attribution:\n- The per-deck delta producer `_feature_delta_tokens` (`src/vibemix/audio/deck_capture.py:394`) emits change tokens over **rms/peak/flux/zcr/crest only** (line 401: `for key in (\"rms\",\"peak\",\"flux\",\"zcr\",\"crest\")`). There is **no per-EQ-band (sub/low/mid/high) delta** in the lane path — so even the *substrate* of \"your top end thinned\" is absent from `deck_audio_deltas`.\n- These deltas reach the prompt relabeled `move_effect=...` when moves are present (`src/vibemix/state/deck_context.py:1935`: `prefix = \"move_effect\" if labels else \"audio_delta\"`) and via `render_move_effect_context` (`deck_context.py:2070`), wired live at `src/vibemix/runtime/coach.py:789-800`.\n- But every producer stamps an **anti-causal rule atom**: `rule=deck_audio_delta_not_causal_proof` (`deck_context.py:1197`), `rule=deck_audio_window_not_causal_or_quality_verdict` (`:1245`), `rule=move_audio_timing_not_causal_or_quality_proof` / `rule=dsp_delta_not_causal_proof` (`:2096`,`:2098`). The required-atom sets `_DECK_AUDIO_DELTA_CONTEXT_REQUIRED_ATOMS` (`:322`) / `_WINDOW_CONTEXT_REQUIRED_ATOMS` (`:334`) force these rules in, and the forbidden-atom sets (`:327`,`:340`) explicitly ban `caused_by_move=true`.\n- The keystone gag is `apply_live_claim_guard` (`deck_context.py:2313`), block `:2371-2391`: when `moves AND effect_deltas` exist and the model voices a control word + causal verdict, it **replaces the whole reaction** with `LIVE_MOVE_EFFECT_HELD_REPLY = \"I can't tell from this live proof whether the control caused that.\"` (`:374-376`), `reason=\"dsp_delta_not_causal_proof\"`. So the brain hears \"high fell on Deck A, EQ_HI move 1.2s ago\" and is forced to say \"I can't tell.\"\n\nThe gag is **correct** under current evidence: nothing ties the band change to the EQ move beyond time-coincidence. No `mixer_physics` / `expected_move_effect` producer exists in `src/` (grep-confirmed NOT-FOUND — the only `src/` hit for those strings is this card's own search target; the engine is PROPOSED-ONLY in a `.planning` packet). The fix is to **build the producer that earns `caused_by_move=true`**, then flip the forbidden atom to a licensed one for that specific gated case.\n\n---\n\n### WIRING CARD — EQ-move / spectral receipt\n\n- **STATUS**: NEGATIVE-GUARD-ONLY (the spectral signal flows but is gagged) + BUILD for the licensing producer. Evidence: `move_effect`/`audio_delta` atoms render live at `coach.py:789-800` and `deck_context.py:1935`, but every path stamps a `*_not_causal_proof` rule (`deck_context.py:1197/1245/2096/2098`) and `apply_live_claim_guard` strips any attribution at `deck_context.py:2371-2391`. `mixer_physics`/`expected_move_effect`/`caused_by_move=true`-as-emitted = NOT-FOUND in `src/` (grep). Per-deck EQ-band delta = ABSENT: `_feature_delta_tokens` (`deck_capture.py:401`) covers rms/peak/flux/zcr/crest, no sub/low/mid/high.\n\n- **WHAT IT DOES**: Computes per-EQ-band energy delta on the moved deck's lane across a pre/post window bracketing a MIDI EQ/filter move, checks the *sign and band match the move's expected direction*, and emits a receipt atom that licenses one causal sentence (\"that EQ_HI cut is why the top end dropped\").\n\n- **CRUDE LIVE PLACEHOLDER IT REPLACES**:\n  - Spectral substrate: `_feature_delta_tokens` rms/peak/flux/zcr/crest tokens (`deck_capture.py:394-414`) — broadband only, no EQ band, so the brain literally cannot say *which* part of the spectrum moved.\n  - Causality substrate: `_xfader_factor` (`runtime/suggestion.py:1703`, used `:220`) and the raw 0-127 EQ buckets in `render_mixer_context` (`deck_context.py:2146`, `eq_low/eq_mid/eq_hi` at `:602-605`) — these say what the *knob position* is, never whether the move *did* anything to the sound.\n\n- **UNLOCKS (user HEARS differently)**:\n  - narration \"Heavy grinding synth, the top end feels a bit thinner now.\" → coaching \"That high-EQ cut you just made on Deck A is what pulled the air out of it — top band dropped about a third right after your hand moved.\"\n  - narration \"I can't tell from this live proof whether the control caused that.\" (the literal gag string today) → coaching \"Yeah, your filter sweep choked the lows on cue — sub band collapsed in the second after you turned it.\"\n\n- **WIRING**:\n  - **Producer to BUILD** — `state/mixer_physics.py::resolve_move_effect(side, move_label, pre_bands, post_bands, lag_s) -> MoveEffect`. Reuse the existing band engine `band_energy_ratios` (`audio/band_features.py:30`, already producing {sub,low,mid,high} and already proven None-on-silent) instead of inventing a new DSP path — call it on the lane's pre/post PCM windows the way `audio/deck_signal.py:48` already does for the Judge. Add an EQ-band branch to `_feature_delta_tokens` (`deck_capture.py:394`) OR aggregate bands in the `_window_context` pre/current rows (`deck_capture.py:249-282`) so `deck_audio_windows` carries per-band pre/current. The resolver's job is the *match*: map `move_label` (e.g. `deckA_eq_hi_down`, from `_move_controls`/`_move_label` at `deck_context.py:1872`) to an expected band+direction, confirm the measured band delta agrees in sign within a short lag window (MIDI move `t_session` is already available — `midi_atoms` at `deck_context.py:1838-1854`), and only then assert attribution.\n  - **EXACT live call-site to insert** — `src/vibemix/runtime/coach.py:789`, inside the `if moves:` block, immediately around the existing `render_move_effect_context(...)` call (`coach.py:789-800`). Pass the resolver output into a new `caused=true` branch of `render_move_effect_context` (`deck_context.py:2070`) so the licensed atom rides the same `event_payload[\"move_effect_context\"]` that already reaches the prompt. (Same evidence dict also surfaces to the Judge at `coach.py:812-814` and to memory ingest at `memory/ingest.py:158`, so the receipt is reused, not duplicated.)\n  - **Grounding atom it must emit** — replace, *only for a sign-matched move*, the gag rule with a licensed one: emit `caused_by_move=true band=high dir=fell lag=1.2s confidence=clear rule=move_effect_band_match_licenses_one_causal_sentence`. Then:\n    1. Remove `caused_by_move=true` from `_DECK_AUDIO_DELTA_CONTEXT_FORBIDDEN_ATOMS` (`deck_context.py:327`) / `_WINDOW...FORBIDDEN_ATOMS` (`:340`) **conditionally** (only when the band-match rule atom is present), and add the licensed rule to the required-atom validator.\n    2. Add a guard exemption in `apply_live_claim_guard` at the `effect_claim` block (`deck_context.py:2371-2391`): when the evidence carries `caused_by_move=true` + `rule=move_effect_band_match_*`, license a *single* attributing sentence scoped to that band+direction instead of returning `LIVE_MOVE_EFFECT_HELD_REPLY`. Keep the gag for any unmatched / multi-band / quality-verdict overreach (so \"great EQ work\" still strips — attribution ≠ praise).\n\n- **CLASS**: FINISH + BUILD. The signal plumbing (per-deck windows, move timestamps, `move_effect` prompt atom, guard hook, band engine) all EXIST — what's missing is the band-delta in the lane path and the *resolver that earns the license*. Rough effort: **1–2 days** (band-delta in `_feature_delta_tokens`/window rows ≈ 2–3h reusing `band_energy_ratios`; `mixer_physics.resolve_move_effect` + move→expected-band map ≈ half a day; the conditional guard/atom flip + its TDD ≈ half a day, since `apply_live_claim_guard` is heavily test-pinned).\n\n- **LEVERAGE on narrator→coach**: **HIGH** — this is the narration trap itself. The guard's `LIVE_MOVE_EFFECT_HELD_REPLY` string is the *exact* \"I can't tell\" the founder heard; flipping this one producer is what converts \"top end feels thinner\" (description) into \"your EQ move thinned the top end\" (coaching). It is the single most-cited failure mode on the live rig.\n\n- **DEPENDENCIES**:\n  1. **Per-deck EQ-band delta must land first** — without sub/low/mid/high in `deck_audio_deltas`/`deck_audio_windows` (`deck_capture.py:_feature_delta_tokens:394` / `_window_context:249`), there is no band to attribute; `band_energy_ratios` (`band_features.py:30`) is the ready primitive to reuse.\n  2. **Tight overlap with the `mixer_physics` / Move-Effect Resolver keystone** (PROPOSED-only, NOT-FOUND in `src/`) — this card *is* the first concrete slice of that engine. The expected-move→band-direction map (what an `eq_hi_down` *should* do to the high band) is the shared core; building it here delivers the resolver's MVP. Do NOT build a parallel one.\n  3. **Deck-pair capture must be verified-active** — the whole per-lane path is gated by `effective_enabled()` (`deck_capture.py:170-184`); on the master-only / single-stereo-mix rig the receipt correctly abstains (honest-null), so this unlock only fires when A/B lanes are genuinely isolated. Pairs with deck-vision (`VIBEMIX_DECK_VISION`, default-off) as a redundant move source but does not require it.",
      "Confirmed: `score_transition_slate` (intel/transition_scorer.py) IS live — reached via `next_suggestion.py:753` → `_select_set_aware_option` → `transition_payload_for_candidate` → the live `SuggestionService`/Pill. So \"what's next\" transition coaching already runs on the real intel scorer. The GOLD target `calculate_transition` is a *different* engine (drop-timing within an owned mix), and the live drop-call uses the crude `drop_predict` instead. I have all evidence pinned. Writing the wiring card.\n\n## GOLD — `calculate_transition` / energy-curve planner: WIRING MAP (HEAD `ce909a87`)\n\n**Critical correction up front (verify-don't-trust paid off):** the brief conflated two distinct engines. `calculate_transition` is NOT an energy-curve sequencer. It is the **scar-20 drop-timing oracle** (`state/transition_clock.py:192`): a deterministic 15 Hz clock that reduces one owned mix to a frozen `TransitionPlan` (`from_fade_begin`/`from_fade_end`/`to_start` + monotonic `transition_progress` + `is_cut`). The *energy-curve set planner* is a **separate** module, `library/sequencer.py::sequence_set` (`:289`, beam-search on an energy curve). And the LIVE \"what's next\" transition coaching already runs on a THIRD engine — `intel/transition_scorer.py::score_transition_slate` — which IS live-wired (`next_suggestion.py:753` → Pill). I document all three with their real statuses.\n\n---\n\n### CARD 1 — `calculate_transition` (scar-20 drop-timing clock) — THE GOLD TARGET\n\n- **STATUS**: DEMO-WIRED. codegraph_callers `calculate_transition` → **1 caller: `build_automix_reel` (runtime/automix_demo.py:73)**. That reel's narrator `narrate_reel` (drop_reaction.py:79) has **1 caller: `scripts/automix_demo_smoke.py:141`** (a script, not the app). `TransitionPlan`/`transition_phase`/`drop_eta_seconds` appear in `src/` ONLY inside automix_demo.py (grep, non-test). The clock's own docstring admits it: *\"The LIVE co-host has no playposition/cue data, but an OWNED mix does… so the clock first earns its keep here\"* (automix_demo.py:5-8).\n- **WHAT IT DOES**: Freezes a two-track transition into normalized fade fractions + a monotonic progress scalar so a callout phrase-locks to the mix (mixing-in vs slam vs hard-cut), on the 66.7 ms grid the engine actually updates on.\n- **CRUDE LIVE PLACEHOLDER IT REPLACES**: `state/drop_predict.py:25 predict_drop_in_sec` + `drop_call_cue` (:88). The live DROP path (`event_detector.py:290` → `coach.py:704-718`) computes ETA as a flat `start_s − position_s` of the next section whose role==drop, and `drop_call_cue` **only ever returns the single string `\"drop_incoming\"`** (drop_predict.py:88-92). It cannot tell a blend from a jump-cut, has no `transition_progress`, and fires `reaction_line(\"drop_incoming\")` from a fixed bank (coach.py:706,714). Whole path is gated OFF by default (`VIBEMIX_DROP_CALL`, event_detector.py:66-69).\n- **UNLOCKS (user HEARS differently)**: narration *\"…drop incoming\"* (one canned line, possibly seconds off, same words every time) → coaching that tracks the actual mix: *\"here it comes…\"* (ARMED, ~2 s lead) → *\"and THERE it is, you mixed it in clean\"* (FADING, blend) vs *\"nice — slammed straight in\"* (is_cut, no blend beat) → *\"clean, fully on the new one\"* (LANDED). The beat lands ON the transition because progress is monotonic + grid-snapped, not over it.\n- **WIRING**: Producer already exists (`calculate_transition`) — the gap is it has **no live cue feed**. Build a thin live adapter: (a) source `TrackCues` from the already-live cue data — `audio/cues.py:15` already imports `TrackCues` and is the documented bridge from analyzer/Rekordbox cues; (b) source the live `from_playposition` from `audible_track_position_s` (already on MusicState — read at suggestion.py:175,698) ÷ duration; (c) call `calculate_transition` once when the fade arms, then `transition_phase(plan, frac, prev_phase, …)` each tick. **Live call-site to insert**: replace the cue/eta computation feeding `event_detector.py:290` (`extra={\"cue\": drop_call_cue(predicted_drop), \"eta\": predicted_drop}`) so `cue` comes from `reaction_cue(phase, is_cut=plan.is_cut, center_start=plan.crossfader_start_center)` (the keys `drop_incoming`/`mixing_in`/`slam_in`/`landed_clean`/`slammed_cut` are already in `drop_reaction.REACTION_BANK`, drop_reaction.py:55). The consumer at `coach.py:706-714` then needs zero change — it already does `reaction_line(drop_cue)`. **Evidence atom**: the frozen `TransitionPlan` must publish a grounded fact (`drop_eta_s` + `transition_progress` + `is_cut`) into `EvidenceRegistry` so `apply_live_claim_guard` (deck_context.py:2239) licenses a causal/timed claim. Today the guard REFUSES timed-transition claims because no producer registers this atom (rules `deck_audio_delta_not_causal_proof`, `deck_audio_window_not_causal_or_quality_verdict`).\n- **CLASS**: **FINISH** — the clock + plan + phase machine + narration bank all exist and are unit-tested; what's missing is the live cue/position adapter feeding it and the evidence-atom registration. The DROP firing path (event_detector → coach) is already plumbed; you redirect its cue source from `drop_predict` to `transition_clock`. Effort: **1–2 days** (live cue derivation + position confidence gating + evidence atom + un-gating `VIBEMIX_DROP_CALL` behind grounding).\n- **LEVERAGE on narrator→coach**: **HIGH** — this is the single most viral \"AI that GETS the drop\" moment and it's currently demo-only firing a one-word canned line. It directly converts the placeholder `\"drop_incoming\"` into a phase-accurate, cut-vs-blend-aware call.\n- **DEPENDENCIES**: (1) a live `from_playposition`/cue feed — `audible_track_position_s` exists but is confidence-gated (suggestion.py:179 needs ≥0.5); folder-only libraries with no cues degrade to the crude path. (2) The `TransitionPlan→EvidenceRegistry` atom must land first or the guard strips the claim. (3) Leave `VIBEMIX_DROP_CALL` as the opt-in firing gate until ear-verified.\n\n---\n\n### CARD 2 — `sequence_set` (energy-curve set planner) — the OTHER thing the brief named\n\n- **STATUS**: ORPHANED FROM LIVE (Viber/prep-wired only). codegraph_callers `sequence_set` (sequencer.py:289) → all non-test callers are `library/mcp_server.py:342` and `library/toolset.py:724` — i.e. the **Codex/Viber set-prep** path (build-set CLI), never the live coach/Pill. No `coach.py`/`dj_cohost.py`/`session_loop.py` reaches it.\n- **WHAT IT DOES**: Beam-search (`_DEFAULT_WEIGHTS` alpha=energy-curve adherence, sequencer.py:77) orders a track pool to follow a target energy curve with harmonic/BPM edge costs — the offline \"build me a peak-time set\" planner.\n- **CRUDE LIVE PLACEHOLDER IT REPLACES**: none directly in the live brain — the live Pill does single-step next-track via `next_suggestion` (CLAP shortlist + `score_transition_slate`), not multi-slot curve planning. So this is **net-new live signal** (\"you're 3 tracks into a build; next move should lift, not plateau\"), not a replacement.\n- **UNLOCKS**: narration of the current track only → forward-looking arc coaching: *\"you've been flat for two tracks — the curve wants a lift here.\"* Requires a live energy-arc state to plan against.\n- **WIRING**: connect `sequence_set` output (or its per-edge `edge_cost`, sequencer.py:379) to the live coach by feeding it the session's played-arc + the Pill shortlist as the pool, surfaced as a coach prompt fact. **Live call-site**: a new branch in `runtime/coach.py`'s suggestion path (around the `_select_set_aware_option` feed) — not yet present. Evidence atom: an `energy_arc` fact registered from session history.\n- **CLASS**: **BUILD** (live integration from scratch; the planner itself exists & is tested). Effort: **longer** — needs a live session energy-arc accumulator that doesn't exist yet.\n- **LEVERAGE**: **MED** — strong \"real DJ friend\" arc awareness, but lower-urgency than the drop moment and needs new live state.\n- **DEPENDENCIES**: a live per-session energy-arc store (absent); the Pill shortlist as pool (exists).\n\n---\n\n### CARD 3 — `score_transition_slate` (intel/transition_scorer) — already live, for contrast\n\n- **STATUS**: **LIVE-WIRED** (already a coach, not a placeholder). `score_transition_slate` reached at `next_suggestion.py:753` → `_select_set_aware_option` (called by `next_suggestion`, next_suggestion.py:100) → `transition_payload_for_candidate` → `SuggestionService` (suggestion.py) → Pill. Weights include `energy_shape`, `harmonic`, `bpm`, `phrase_alignment`, `cue_operability` (transition_scorer.py:24-34).\n- **NOTE**: This is why the live Pill's *next-track + harmonic + move-grade* coaching is already grounded today. The GOLD gap (Card 1) is specifically the **in-mix drop-timing** call, which this scorer does NOT cover — it scores *which* track and *whether* the pair mixes, not the *moment-by-moment progress of the live blend*. The two are complementary: Card 1 makes the call land on the beat; Card 3 already picks the right next track.\n\n---\n\n**Bottom line for the build map:** the highest-leverage GOLD wire is **Card 1 = FINISH**: redirect the live DROP cue source from the crude `drop_predict.drop_call_cue` (always `\"drop_incoming\"`) to the existing scar-20 `calculate_transition`/`transition_phase`/`reaction_cue`, gated on a live playposition feed + a `TransitionPlan` evidence atom so `apply_live_claim_guard` licenses the timed claim. The narration→coaching consumers (`coach.py:706`, `drop_reaction.REACTION_BANK`) are already in place; only the producer-to-live-feed seam is missing.",
      "I have everything I need. The complete chain is confirmed: live consumer exists (coach.py:624 → `_credit_live_skill_demo` → `recognize` at :177, which already has the `BEATMATCH_GRADED` branch at skill_recognizer.py:154), the math + payload + audio engine all landed (`grade_beatmatch`, `grade_to_event_extra`, `miniplayer.py`, `grid.py`), BUT there is NO live producer — `beatmatch_judge.grade_beatmatch` has 6 callers all in `test_beatmatch_judge.py`, the miniplayer is imported only by the Judge/demo, and `learn/runtime.py` drives a HarmonicPractice loop but not a beatmatch MiniDeck loop. The skill remains hard-masked (`_HONEST_UNCREDITABLE_V11 = (\"beatmatching\",)`, `live_creditable=False`).\n\n## GOLD — Beatmatch Grading + the Missing H1 Practice-Deck Producer (HEAD `ce909a87`)\n\nThe red-team flagged this as \"math ported, never wired.\" Since then, commits `208a4e9a` / `6ffe0b5d` / `5f14d25d` landed MORE than the red-team saw — but the keystone gap is unchanged. The deck moved from \"math exists\" to \"math + event-payload + audio-engine + live-consumer all exist; the single missing link is the production loop that drives the owned decks and fires the event.\" This is now a **FINISH**, not a BUILD.\n\n**What I verified at HEAD `ce909a87`:**\n- `grade_beatmatch` (beatmatch_judge.py:83) — math is complete, clean-room Mixxx port. **6 callers, ALL in `tests/learn/test_beatmatch_judge.py`** (codegraph_callers). ORPHANED from live.\n- `grade_to_event_extra` (beatmatch_judge.py:145) — the canonical `BEATMATCH_GRADED` event payload helper. **Zero callers** (codegraph: \"No callers found\"). Net-new since red-team; still orphaned.\n- `src/vibemix/audio/miniplayer.py` (`MiniPlayer`/`DeckState`, owned-deck tempo engine, numpy Mixxx port) + `src/vibemix/audio/grid.py` (`BeatGrid`) — the H1 \"owned-deck MiniDeck\" engine LANDED (commit `208a4e9a`). But its only non-test importers are `beatmatch_judge.py` and `runtime/automix_demo.py` (DEMO, not live). No practice runtime drives it.\n- `practice_loop.py` — still **NOT-FOUND** (`find src -name practice_loop.py` empty; no MiniDeck-driving loop in `learn/runtime.py`, `lesson_flow.py`, `teaching_loop.py`).\n- `BEATMATCH_GRADED` emit — **NOT-FOUND** in `state/`, `runtime/`, or any `learn/*.py` (only doc-comments referencing the future emitter).\n- The consumer is LIVE-WIRED: `recognize` (skill_recognizer.py) has a real `BEATMATCH_GRADED` branch (skill_recognizer.py:154-172) credited only on a non-abstain LOCKED grade. It is reached live: `runtime/coach.py:624` → `_credit_live_skill_demo` (coach.py:177 calls `recognize(..., event_t=t_session)`). So a `BEATMATCH_GRADED` event injected into the coach loop's event stream **would already be consumed and could credit Mastered.**\n- The mask is STILL ON: `_HONEST_UNCREDITABLE_V11 = (\"beatmatching\",)` (skill_recognizer.py:105) AND `SkillSpec(..., live_creditable=False)` for beatmatching (skill_tree.py:143). Both must flip the moment a real producer exists.\n\n### WIRING CARD — BEATMATCH_GRADED live producer (the H1 practice-deck loop)\n\n- **STATUS**: ORPHANED. `grade_beatmatch` codegraph_callers = 6, all `test_beatmatch_judge.py`. `grade_to_event_extra` callers = NONE. `BEATMATCH_GRADED` emit = NOT-FOUND in any non-doc src path. `practice_loop.py` = NOT-FOUND. The owned-deck engine (`miniplayer.py`/`grid.py`) exists but is reached only from the Judge + `automix_demo.py`.\n- **WHAT IT DOES**: A learn-mode practice loop that owns both decks (loads two tracks, drives the playheads via `MiniPlayer`), grades the student's beatmatch attempt against exact ground truth via `grade_beatmatch`, and fires a `BEATMATCH_GRADED` event so the recognizer can credit the \"beatmatching\" skill toward Mastered.\n- **CRUDE LIVE PLACEHOLDER IT REPLACES**: none — net-new signal. The live brain has NO beatmatch grading at all today; the skill is hard-masked, so the co-host never makes a beatmatch-quality claim (the live-claim guard would strip it). This adds a credit path, not a swap.\n- **UNLOCKS (user HEARS differently)**: narration today (in a beatmatch lesson) \"*nice, the tracks are playing together*\" (ungrounded, generic — and the Earned Wall shows beatmatching permanently fenced at \"Competent\" with no path to Mastered) → coaching after \"*locked — tempo's dead-on and you're inside a hundredth of a beat on the one. That's your beatmatch Mastered.*\" OR the honest negative: \"*you're a sixteenth behind on the one, drifting — nudge the jog forward.*\" The Mastered unlock vocal (coach.py:190, `mastered_unlock_line`) fires the rare earned trophy.\n- **WIRING**:\n  - **Producer to build** (`src/vibemix/learn/practice_loop.py`, net-new ~150 LOC): instantiate two `MiniPlayer` decks + their `BeatGrid`s, advance them under student jog/tempo MIDI, sample `DeckState` (frame cursors + rates), call `grade_beatmatch(grid_a, grid_b, state)` on a cadence, and on a non-abstain grade construct an `Event(type=\"BEATMATCH_GRADED\", extra=grade_to_event_extra(grade))`.\n  - **Live call-site to insert it**: the loop must feed the SAME event stream the coach loop drains. The consumer end is already at `runtime/coach.py:624` (`_credit_live_skill_demo(ev, ...)`). The practice loop should run as a learn-mode coroutine alongside `learn/runtime.py`'s `LessonRuntime` (which already shares the one `LearnProgress` object and the asyncio loop — see the RMW-safety note at coach.py:207-212) and push its `BEATMATCH_GRADED` events into the event detector / coach feed. **Insert point: emit into the same event path `event_detector` feeds `coach.py:624`** — NOT a test, NOT `automix_demo.py`.\n  - **Grounding atom for the live_claim_guard**: the producer MUST register the matching citation `(\"ev\", \"BEATMATCH_GRADED\", t_session)` in the `EvidenceRegistry` (the recognizer's `citation_check=lambda s,k,t: evidence_registry.has(s,k,t,tol=1.0)` at coach.py:179 requires it — an uncited grade credits nothing). This is the exact contract spelled out in the `grade_to_event_extra` docstring (beatmatch_judge.py:152-153). The owned-deck DeckState (exact frame + rate) IS the authoritative evidence that licenses a causal \"you're 14ms late\" claim — unlike the live observer path, the guard can trust it.\n  - **Unmask**: flip `_HONEST_UNCREDITABLE_V11` (skill_recognizer.py:105) to `()` and set `live_creditable=True` for beatmatching (skill_tree.py:143) — ONLY after the producer lands and is live-proven (the comments at both sites say exactly this).\n- **CLASS**: **FINISH** + ~1-2 days. The hard parts already shipped (math, octave-fold/phase scars, owned-deck tempo engine, event payload, recognizer branch, live consumer). What remains is the orchestration loop + MIDI binding + citation registration + the two-line unmask. The red-team's \"BUILD from spec\" is now stale — `208a4e9a`/`6ffe0b5d` did the spec build.\n- **LEVERAGE on narrator→coach**: **HIGH**. This is the ONLY path to \"Mastered\" for a core DJ competency that's currently fenced off, and beatmatch grading is the single most credible \"real DJ friend\" moment — exact tempo/phase feedback is impossible for the live observer and only the owned-deck loop can deliver it. It converts the learn module from \"lessons + vague encouragement\" into a graded skill with an earned unlock.\n- **DEPENDENCIES**: (1) a learn-mode MIDI binding so student jog/tempo input drives the two `MiniPlayer` rates/cursors (jog→cursor nudge, tempo fader→rate) — check whether `learn/midi_mirror.py` already routes this; (2) `BeatGrid`s for the two practice tracks (the H1 loop owns the tracks, so grids can be precomputed or shipped as lesson assets); (3) event-stream + `EvidenceRegistry` injection from the learn coroutine into the coach loop's feed (coach.py:624 path). No new external dep, no model, no API key.",
      "I have everything needed. taste_model + profile_projection are live-wired but only as static persona style-tags (consent-gated, cold-by-default), NOT live transition scoring. Final classification is complete. Writing the GOLD SWEEP.\n\n## GOLD SWEEP — intel/ + taste_model + transition_judge: live-brain wiring map\n\nHEAD `ce909a87`. \"Live brain\" = what feeds the coach VOICE (`runtime/coach.py::coach_loop` → `agent/dj_cohost.py` prompt build / `apply_live_claim_guard`). The PILL (`runtime/suggestion.py::SuggestionService`) is a **separate live surface** — text on screen, not the co-host's spoken reaction — so modules that only reach the pill are *not* feeding the spoken coach.\n\n### Two live targets, kept distinct\n- **VOICE** = `coach_loop` (coach.py:405, sole caller `__main__:870`) → `dj_cohost` prompt + `apply_live_claim_guard` (dj_cohost.py:2964). This is what the founder hears narrating.\n- **PILL** = `SuggestionService.refresh_from_state` (suggestion.py:1102) — live runtime, but renders the \"what's next\" pill, not speech.\n\n### The verdict table\n\n| Module (entry symbol) | Live-wired? | What it would add to the spoken co-host if wired to VOICE | WIRE/FINISH/BUILD |\n|---|---|---|---|\n| **transition_judge** (`judge_transition`) | **WIRED-TO-VOICE** ✅ | Already live: `judge_and_record` ← `_run_live_judge` (coach.py:327) ← `coach_loop` (coach.py:820). Verdict line → `ev.extra[\"judge_evidence_line\"]` (coach.py:831) → consumed by `apply_live_claim_guard(..., judge_evidence_line=...)` (dj_cohost.py:2971). This is the one real causal-grade engine in the voice today: \"[judge:transition@t] clean blend / harmonic clash\". Abstain-first on master-only rig. | done (verify ear-pass; it's the template the others should follow) |\n| **judge_voice** (`verdict_evidence_line`) | **WIRED-TO-VOICE** ✅ | Live at coach.py:389 inside `_run_live_judge`; turns the judge verdict into the citation-bearing spoken line. Coupled to the judge above. | done |\n| **taste_model** (`load_taste_model`/`taste_score_for`) | **WIRED-TO-VOICE (thin)** ⚠️ | Live at dj_cohost.py:957-963 — but ONLY emits static `transition_style_tags` into the persona overlay, consent-gated, cold/empty by default (`_render_taste_overlay`). `taste_score_for` (the actual per-transition taste scorer) has **zero live callers** — only `taste_model.py` self + eval `intel_taste_scorecard.py` + tests. So the live brain knows your *style label* but never scores \"this blend fits your taste\" out loud. | FINISH (scorer exists; wire `taste_score_for` into the reaction, not just the persona tag) |\n| **profile_projection** (`project_profile`) | **WIRED-TO-VOICE (thin)** ⚠️ | Live at dj_cohost.py:956/967, same consent-gated persona-tag path as taste_model. Read-only style overlay, not a live claim. (Codegraph's \"caller `_resolve_prompt_cell:891`\" is a mis-attribution — the real call is :967.) | done for what it is; depends on taste FINISH above |\n| **transition_scorer** (`score_transition`) | **FEEDS-PILL/VIBER-ONLY** ❌ for voice | Non-test callers: `next_suggestion.py:657`, `toolset.py:313` (Viber), `suggestion.py` (pill). NOT in `coach_loop`/`dj_cohost`. Wired to voice it would let the co-host say *why* a transition ranks (\"role pairing + energy delta + cue-operability\"), grounded in the same scorer the pill uses. | WIRE (engine mature, pill-proven; route its score-components into a reaction evidence field) |\n| **musical_ontology** (`normalize_role`/`role_pair_score`/`risk_penalty`) | **FEEDS-PILL/VIBER-ONLY** ❌ for voice | Callers all inside `transition_scorer` + `taste_model` (themselves pill/Viber). Reaches voice only transitively *if* transition_scorer is wired. Would give the voice role-pair vocabulary (\"driver into a breakdown — risky pair\"). | WIRE (rides along with transition_scorer) |\n| **decision_runtime** (`decide`) | **FEEDS-PILL-ONLY** ❌ for voice | Live caller `_decision_for_suggestion` (suggestion.py:777) → pill. The deterministic select/hold/ask + claim-degrade engine. It already gates the pill's text; the VOICE has no equivalent grounded decision arbiter — it free-generates then post-filters with `live_claim_guard`. | FINISH/WIRE (high-leverage; see flags) |\n| **decision_validator** (`validate_agent_decision`) | **FEEDS-PILL-ONLY** ❌ for voice | Live only via `decide` (decision_runtime.py:54) → pill. Rejects claim-IDs from unselected candidates, timing/ask overclaims. | WIRE (rides with decision_runtime) |\n| **claim_validator** (`validate_decision_claims`) | **FEEDS-PILL-ONLY** ❌ for voice | Only caller `validate_agent_decision` (decision_validator.py:25) → pill. The phrase→required-claim table (\"tempo phrase needs a bpm claim\", \"texture needs a semantic claim\"). The VOICE has NO equivalent — it relies on the looser regex `apply_live_claim_guard`, which (per deck_context.py:325/338) just *refuses* causal/quality claims rather than *licensing* the grounded ones. | **WIRE (highest leverage — see #1)** |\n| **context_compiler** (`compile_suggestion_context`/`compile_transition_context`) | **FEEDS-PILL + VIBER** ❌ for voice | `compile_suggestion_context` ← `_context_for_suggestion` (suggestion.py:672, pill); `compile_transition_context` ← `compile_musical_context` (toolset.py:407, Viber). Bounds/redacts the evidence packet the decision engine consumes. NOT used to build the voice prompt. | WIRE (prerequisite plumbing for routing decision_runtime to voice) |\n| **move_grade** (`grade_move`) | **ORPHANED from Python live** ❌ | Only caller is the **frontend** `syncMoveGrade` (tauri/ui/src/pill/index.ts:1955) — i.e. the Python grader has no live Python call-site; the pill grades client-side. | FINISH (grader exists; no live Python producer feeds the voice) |\n| **decision_trace / agent_contract / claims** | infra for decision_runtime | Trace/contract/claim-types backing the decision subsystem; live only as far as decision_runtime is (pill). | n/a (ride with decision_runtime) |\n| **feedback** (`load/append_feedback_event`) | feeds taste_model persistence | Writes `taste_feedback.jsonl` the persona overlay reads; not a reaction producer. | n/a |\n| **gold_labels / gold_sampling / gold_validation** | **ORPHANED (eval-only)** ❌ | Offline gold-label tooling, scripts/eval + tests only. Never live. | n/a (not a runtime feature) |\n\n### The crude placeholder that the brief flagged — CONFIRMED\nThe \"Move-Effect Resolver\" (`expected_move_effect`/`mixer_physics`) is **NOT-FOUND** — only the non-causal placeholder ships and IS in the live voice prompt: `render_move_effect_context` (deck_context.py:2070, called dj_cohost.py:467 + coach.py:789) emits raw DSP deltas plus an explicit self-disqualifying tag — `rule=move_audio_timing_not_causal_or_quality_proof` / `rule=dsp_delta_not_causal_proof` (deck_context.py:2096/2098). And `apply_live_claim_guard` actively **refuses** causal claims (deck_context.py:2388 `policy=\"move_effect_not_verdict\"`). So the voice is *structurally barred* from \"your EQ thinned the top\" because no producer licenses it. This is the keystone BUILD, not a WIRE.\n\n### Top 3 highest-leverage orphans to route into the VOICE\n1. **claim_validator + decision_validator (BUILD-bridge / WIRE).** The pill has a *positive* claim-licensing table (phrase → required grounded claim) that the voice lacks entirely. Today the voice path only has a *negative* guard (`apply_live_claim_guard` refuses causal/quality claims). Porting `validate_decision_claims`' phrase→claim contract into the reaction guard is what flips \"refuses to coach\" into \"coaches when evidence exists\" — directly closing the narrator-vs-coach gap with code that already passes 30+ tests.\n2. **transition_scorer + musical_ontology (WIRE).** Mature, pill-proven (`score_transition` ranks live in the pill). Its score-components (role-pair, energy-delta, cue-operability, harmonic) are exactly the *reasons* a coach would speak. Route the component breakdown into a reaction evidence field so the voice can explain a transition instead of narrating audio texture. Rides musical_ontology's role vocabulary for free.\n3. **decision_runtime (FINISH).** The voice free-generates then post-filters; the pill runs a deterministic `decide` (select/hold/ask + `validate_and_degrade`) FIRST and only voices grounded output. Giving the coach voice the same decision-arbiter-before-speech is the structural fix for \"reacts late/scripted\" — but it's a larger lift (needs context_compiler plumbing wired to the voice prompt first), so it's the #3 not #1.\n\n**The pattern:** the entire decision/claim/context subsystem is already live — just pointed at the **pill**, not the **voice**. The fastest win is not new engines; it's re-pointing the pill's claim-licensing + scoring at the reaction path, exactly mirroring how transition_judge was already wired into both."
    ],
    "map": "I have all evidence verified. The `move_grade` is wired to the suggestion/library path (next-track pill) — NOT the live reaction prompt. `transition_scorer` likewise feeds suggestion/library. The low_kill guard confirms the controller-tier-only grounding gap. Assembling the final document.\n\n## GOLD-WIRING-MAP — Turning the vibemix Narrator into a Coach\n\n*HEAD `ce909a87` (branch `live-tuning-or-brain`). READ-ONLY. Every claim file:line / codegraph-verified.*\n\n---\n\n### 1. THE ONE-LINE TRUTH\n\nThe live brain eats **raw band ratios + a 0-127 xfader bucket + MIDI move labels**; the DJ intelligence that would let it *coach* (mixer-move physics, spectral causality, loudness receipts, beatmatch grading, transition geometry) is **clean-room-built and unit-tested but almost entirely ORPHANED to tests/demos/the library path** — never reaching `coach.py`'s prompt. Connect it in this order: **(1) the EQ-move physics keystone + its spectral-delta receipt** (the one engine that does not exist yet and is the named R-SLOP killer), then **(2) finish the already-live Vibe Judge so it speaks on a master-only rig**, then **(3) wire the orphaned scorers/grader that already run elsewhere.**\n\n---\n\n### 2. WHAT THE BRAIN EATS TODAY (the crude input menu)\n\nThe grounded live prompt is assembled in `dj_cohost.py::_build_attached_audio_context_clause` (:383) from these render functions. Verbatim atoms:\n\n- **`hearing[...]`** — `coach.py:371`: `hearing[rms={state.rms:.3f} sub={b['sub']:.2f} low=… mid=… high=… bpm=…]`. Raw band *ratios*, no loudness, no spectral centroid.\n- **`mixer_context[...]`** — `deck_context.py:2146`: `xfader={_xfader_label(...)}`, per-deck `vol`/EQ as **0-127 MIDI buckets** (`_int_0_127`). The xfader→audibility model is a 4-step bucket lookup, `_xfader_factor` (`deck_context.py:3458`: `if xfader >= 112: return 0.0` …).\n- **`move_effect_context[...]`** — `deck_context.py:2070`: shows `audio_delta` band shifts but **stamps `rule=dsp_delta_not_causal_proof`** (:2098) — it is *forbidden* from asserting the move caused the change.\n- **`audio_delta`** — `deck_context.py:1794` `render_audio_delta_items`: frame-to-frame `{sub,low,mid,high,rms,onset}` deltas vs `prev_perceive`. Correlation, never attribution.\n- **deck audio features** — `deck_capture.py:307` `_deck_frame_features`: `{rms,peak,zcr,flux,crest}`. Cheap descriptors; **no LUFS, no centroid, no per-band attribution.**\n\n**Why this is narration, not coaching:** the brain is handed *what the room sounds like* and *what knob moved (as a number)* but never *the physics that connects the move to the sound*. The live guard `apply_live_claim_guard` (`deck_context.py:2313`) actively **REFUSES** every causal/quality verdict (`rule=deck_audio_delta_not_causal_proof` :325; `:338`) because no producer licenses one — so the only safe output is description (\"heavy grinding synth\").\n\n---\n\n### 3. THE WIRING MAP (ranked by narrator→coach leverage)\n\n---\n\n#### CARD 1 — EQ-Move Physics Resolver `intel/eq_move_model.py` ★ KEYSTONE\n\n- **STATUS: BUILD** (does not exist — `grep mixer_physics|expected_move_effect src/` = **NOT-FOUND**; only PROPOSED in `.planning/packets/2026-06-01/recovered/wf_d9ddbeed-f5f__ALT1__mixxx-eq-filter-dsp-the-r-slop-grounding-unlock-round-2.md`).\n- **REPLACES:** the controller-tier-only guard `_has_unsupported_mixer_low_kill_claim` (`deck_context.py:2585`) which checks `_control_now_tier(raw,\"low\") ∈ {killed,deep-cut}` — a MIDI label with **zero connection to what the audio did** (and `return False` when no controller, line :2586 — i.e. dead on Kaan's master-only rig).\n- **UNLOCKS:** the co-host can say **\"your low-kill actually thinned the bass — sub+low cratered 18 dB\"** *and refuse* it on a breakdown where the bass wasn't there. Turns Invariant #3 (\"trust the audio\") from a label-lookup into a quantitative test. **This is the single move that flips REFUSE→CLAIM.**\n- **WIRING:** new pure module `predicted_band_gains(move, fs) -> {sub,low,mid,high} dB` (clean-room RBJ cookbook, Mixxx corners 246/2484 Hz, kill −23 dB). Call site: **beside** `_has_unsupported_mixer_low_kill_claim` inside `apply_live_claim_guard` (`deck_context.py:2342`), comparing prediction to measured `render_audio_delta_items` delta over the post-move window.\n- **CLASS: BUILD** · **LEVERAGE: HIGHEST** · **DEPS:** none new — measurement side `band_features.band_energy_ratios` (`band_features.py:30`) + `render_audio_delta_items` (`:1794`) already torch-free and present.\n\n---\n\n#### CARD 2 — Spectral / Loudness Receipt (the measurement half of the keystone)\n\n- **STATUS: FINISH** — band ratios exist (`band_features.band_energy_ratios`), per-deck features exist (`deck_capture.py:307`), but they carry **no LUFS and no spectral centroid**. The only loudness code (`library/energy.py::_loudness_dbfs` :321) is **library-only** — callers are `toolset.py` + `constants.py` (codegraph: not the coach loop).\n- **REPLACES:** `hearing[rms=…]` raw-RMS — RMS is not perceived loudness, so the brain can't say \"you brought it up 3 dB.\"\n- **UNLOCKS:** a *citable* loudness/brightness receipt the EQ-physics verdict cites (\"sub dropped, mid rose to fill\") and that licenses energy-arc coaching.\n- **WIRING:** extend `_deck_frame_features` (`deck_capture.py:307`) + `render_audio_delta_items` to add a centroid + dB-scaled band delta; surface in `move_effect_context`.\n- **CLASS: FINISH** · **LEVERAGE: HIGH (keystone-paired)** · **DEPS:** Card 1 consumes it.\n\n---\n\n#### CARD 3 — Vibe Judge full-rig→master-only (the already-live engine that mostly abstains)\n\n- **STATUS: FINISH** — **already wired live.** `coach_loop` (`runtime/coach.py:405`) → `_run_live_judge` (`:327`) → `verdict_evidence_line` (`intel/judge_voice.py`) + `judge_and_record`. This is the *one* gold engine reaching the live brain today.\n- **REPLACES:** nothing yet on the common rig — it **abstains** because `signal_frame_from_capture` (`deck_signal.py:22`) sets `routing_enabled=False` on master-only rigs (:37) and `live_claim_policy` only returns `supported_verdict` with **two active decks + per-lane source-trust** (`deck_context.py:2241` `_supports_grounded_transition_verdict`). Kaan's rig = one stereo master → honest-null.\n- **UNLOCKS:** transition quality verdicts (`[judge:transition@t]`, harmonic clash/clean) on the rig people actually run.\n- **WIRING:** the Card 1 spectral-causality path is what lets a *single-master* frame infer the executed mix (filter-sweep signature, ALT1 §1D) instead of requiring two routed lanes — so Card 3's master-only unlock **depends on Card 1**.\n- **CLASS: FINISH** · **LEVERAGE: HIGH** · **DEPS:** Card 1 (single-stream mix inference).\n\n---\n\n#### CARD 4 — Transition Scorer / geometry `intel/transition_scorer.py`\n\n- **STATUS: WIRE** (exists, runs) — but **ORPHANED from the live reaction loop.** `score_transition*` callers (codegraph): `library/next_suggestion.py`, `library/toolset.py`, `library/sequencer.py`, `library/section_builder.py`, `intel/move_grade.py`, `intel/context_compiler.py` — **all the next-track/Viber/library path, none in `coach.py`'s reaction prompt.**\n- **REPLACES:** nothing in the live prompt — harmonic/BPM/phrase geometry never reaches the spoken reaction.\n- **UNLOCKS:** \"these two are a 9A→4A clean harmonic jump, phrases aligned\" as a *live* observation, not just a next-track suggestion.\n- **WIRING:** feed `TransitionScoringInput` from live deck-state into the coach prompt (new render fn) — the function is pure and present.\n- **CLASS: WIRE** · **LEVERAGE: MEDIUM** · **DEPS:** trusted two-deck state (so realistically post-Card 3).\n\n---\n\n#### CARD 5 — Beatmatch Grader `learn/beatmatch_judge.py::grade_beatmatch`\n\n- **STATUS: WIRE (orphaned)** — `grade_beatmatch` (`:83`) callers = **6, ALL in `tests/learn/test_beatmatch_judge.py`** (codegraph). The H1 practice loop is **NOT-FOUND** (no `practice_loop.py` in `learn/` — confirmed dir listing).\n- **REPLACES:** nothing — beat alignment is never spoken.\n- **UNLOCKS:** \"you're an eighth-beat behind, nudge up\" — the single most-requested DJ coaching line.\n- **WIRING:** needs a live `BeatGrid` per deck + a call site in `coach_loop`. The grader is pure; the grids are the gap.\n- **CLASS: WIRE engine + BUILD live grid feed** · **LEVERAGE: MEDIUM-HIGH (but no live input today)** · **DEPS:** live beatgrid extraction (heaviest new input).\n\n---\n\n#### CARD 6 — Move-Effect / Transition planner `transition_clock.calculate_transition`\n\n- **STATUS: WIRE (orphaned to a DEMO)** — `calculate_transition` (`transition_clock.py:192`) has **1 caller: `build_automix_reel` in `runtime/automix_demo.py:73`** (codegraph). Not live. `intel/move_grade.grade_move` likewise only reaches `tauri/ui/src/pill/index.ts:1955` (the pill), not the reaction prompt.\n- **REPLACES:** the `_xfader_factor` 4-bucket audibility guess (`deck_context.py:3458`).\n- **UNLOCKS:** timing-aware coaching (\"you've got 16 bars to bring B in\").\n- **CLASS: WIRE** · **LEVERAGE: LOW-MEDIUM** · **DEPS:** Cards 1+3.\n\n*(Screen-vision is wired but `VIBEMIX_DECK_VISION` default-off — `__main__.py:683-684`, banner \"disabled\" :2590 — out of scope for the audio-coach unlock.)*\n\n---\n\n### 4. BUILD ORDER\n\n| # | Step | Class | Effort | Francesco-promise-safe? |\n|---|------|-------|--------|--------------------------|\n| 1 | **`intel/eq_move_model.py`** — clean-room RBJ biquad `predicted_band_gains` (Card 1) | **BUILD** | ~1d (pure numpy, no dep, no spec change) | **NEW BUILD** (but spec is fully written) |\n| 2 | **Spectral/LUFS receipt** in `_deck_frame_features` + `render_audio_delta_items` (Card 2) | **FINISH** | ~0.5d | **finishing built code** |\n| 3 | **Wire 1+2 into `apply_live_claim_guard`** at `deck_context.py:2342` — verdict = move ∧ predicted-delta matches measured (Card 1 unlock) | **FINISH** | ~0.5d | **finishing built code** |\n| 4 | **Master-only Judge** — single-stream mix inference into `signal_frame_from_capture`/`live_claim_policy` (Card 3) | **FINISH** | ~1d | **finishing live code** |\n| 5 | **Wire transition_scorer into coach prompt** (Card 4) | **WIRE** | ~0.5d | **wiring existing code** |\n| 6 | **Beatmatch live grid + `grade_beatmatch` call site** (Card 5) | WIRE+BUILD | ~1.5d (grid is the cost) | **mixed** |\n\nSteps 2-5 are *finishing/wiring already-built, already-tested code* — Francesco-promise-safe. Only Step 1 (the keystone) and the Step 6 beatgrid feed are genuinely new BUILD, and Step 1's algorithm is fully specified in the ALT1 packet.\n\n---\n\n### 5. THE KEYSTONE SPEC (ready for Codex)\n\n**New module `src/vibemix/intel/eq_move_model.py`** (intel = import-light, no audio capture / no model clients — correct home).\n\n- **Inputs:** `move: str` (vocabulary from `event_detector.py` MIX_MOVE strings: `killed / _low: / _mid: / _hi: / _filter: / xfader`), `fs: int`.\n- **Core:** clean-room RBJ cookbook coefficients (Mixxx-matched, GPL never vendored): `LO_MID_CORNER=246`, `MID_HI_CORNER=2484`, `LOW_KILL_FC=99`, `HIGH_KILL_FC=3700`, `KILL_GAIN_DB=-23`, `Q_KILL=0.9 / Q_SHELF=0.4 / Q_BOOST=0.3`. Magnitude response `|H(e^jω)|` via `polyval(b,z)/polyval(a,z)` (pure `np.exp`, no scipy). Predicted per-band gain = `∫|H|²` over each `band_features._BANDS` window.\n- **Output evidence atom:** `predicted_band_gains('low_kill', fs) -> {'sub':-23,'low':-21,'mid':-0.5,'high':0.0}` (dB).\n- **Call site:** `state/deck_context.py::apply_live_claim_guard` at `:2342`, *beside* `_has_unsupported_mixer_low_kill_claim`. Compare predicted-direction delta to the **measured** `render_audio_delta_items` / `band_energy_ratios` delta **over the post-move window** (`deck_audio_window_context`, `deck_context.py:571`) — not the instant frame (Mixxx coefficient-ramp + Bessel group delay lag the move ~1 buffer; ALT1 §caveat). Match → license the claim; no band move → REFUTE to `LIVE_MOVE_EFFECT_HELD_REPLY`.\n- **Guard upgrade:** flip the rule from `deck_audio_delta_not_causal_proof` (`:325`) to a *grounded causal verdict* when prediction matches measurement — the first time the live brain is allowed to assert cause.\n\n---\n\n### 6. HONEST BOTTOM LINE\n\n**~70% of the missing intelligence is recoverable by WIRING/FINISHING code that already exists and is unit-tested** — the Vibe Judge is already in `coach_loop` (just abstains), `transition_scorer`/`move_grade`/`grade_beatmatch`/`calculate_transition` all run *somewhere* (library, pill, demo, tests) and need a coach-loop call site, not invention. **~30% is genuine new BUILD**, concentrated in two places: the **`eq_move_model.py` keystone** (spec fully written in the ALT1 packet) and the **live per-deck beatgrid feed** for the beatmatch grader.\n\n**The single highest-leverage first move:** build `intel/eq_move_model.py` and wire it into `apply_live_claim_guard` (`deck_context.py:2342`). It is the *only* engine that converts the live guard from REFUSE-all-causal-claims into licensed cause-and-effect (\"your EQ move thinned the top end\"), it works on the master-only rig everyone actually runs, and it is the prerequisite that unlocks the master-only Vibe Judge (Card 3). That is the line between narrator and coach."
  }
}