# CLAUDE — LAND QUEUE (routing surface for Codex A)

> # ⏩ PULL-NEXT — the live queue (RE-PINNED 2026-06-03 #2, HEAD `0ec01fc9`). THIS block is authoritative for "what's next"; everything below is history and its statuses may be stale. **Whole board re-aligned by workflow `wqw0fbe6s` (3 waves: every landed commit verified · every open item re-derived vs HEAD · 8-surface opportunity sweep → synth). CLOSE-MOVELESS supply verified separately → [`../2026-06-03/CODEX_VERIFICATION-CLOSE-MOVELESS-SUPPLY.md`](../2026-06-03/CODEX_VERIFICATION-CLOSE-MOVELESS-SUPPLY.md).**
>
> **🎨 FRONTEND `tauri/ui/` IS OPEN TO CODEX (2026-06-03 UPDATE, Kaan: "don't ban codex from working on UI").** The earlier off-limits rule is LIFTED. Codex may work on the frontend — full-stack a feature end-to-end (backend + its `tauri/ui/` half) instead of stopping at the backend seam. A `tauri/ui/` commit from Codex is now **legitimate work, not presumptive drift** — it just needs the same `CODEX_VERDICT-<stem>` + by-eye receipt as any backend commit. **This SUPERSEDES the per-item "UI half = UI-session note" splits below — Codex may now take those UI halves too** (or hand them off; both fine).
> **Coordination, NOT a ban (don't clobber a concurrent session):** a dedicated UI session is running the UX-redesign lane on the **`ux-redesign-impeccable` branch** (screenshots → audit [`../2026-06-03/CURRENT-STATE-UI-AUDIT.md`](../2026-06-03/CURRENT-STATE-UI-AUDIT.md) → impeccable spec [`../2026-06-03/CODEX_READY-UX-REDESIGN-IMPECCABLE.md`](../2026-06-03/CODEX_READY-UX-REDESIGN-IMPECCABLE.md) → handoff [`../2026-06-03/UX-REDESIGN-START-HERE-NEW-SESSION.md`](../2026-06-03/UX-REDESIGN-START-HERE-NEW-SESSION.md)). When Codex touches `tauri/ui/`: ① follow that impeccable spec as the design contract (don't re-skin against it); ② gate every FE change with `cd tauri/ui && npm run build && npm test` (+ `npm run codegen:ipc` after a `messages.schema.json` edit); ③ surgical-stage commits (`git diff --cached --name-only` before commit — shared tree across concurrent sessions, see [[feedback_concurrent_sessions_one_tree]]).
>
> **Loop discipline (from [`OPTIMIZE-CLAUDE-CODEX-LOOP.md`](../2026-06-03/OPTIMIZE-CLAUDE-CODEX-LOOP.md) — adopt both sides):** ① every `CODEX_READY` packet carries a proof-ledger header (`anchors N/N · premise HOLDS · default-path traced · invariants clean`) — today's ground→verify pass caught **3 false premises** (B15/B2/salvage); don't skip it. ② **No commit lands without a `CODEX_VERDICT-<stem>.md` naming its queue item + a by-ear/by-eye receipt** — a verdict-less or item-less commit is presumptive drift regardless of which tree it touches (the ~11h/~26-commit reskin relapse this morning had neither; the lesson is "no verdict + no receipt", NOT "UI = drift").
>
> **⚠️ DRIFT FLAG (re-aligned):** of the 9 backend/frontend commits audited since the last pin, **8 landed off the WIRE-THE-GOLD spine.** BUT the picture is more nuanced than "drift": Codex DID land 2 blessed BUILDs (**B16** audio-key, **B5** auto_crate) + the **B1 rate-ramp** (`cdc369fd`, Codex co-author) + Q8/B15 — real, test-green spine progress. **No frontend-by-Codex drift fired** — every `tauri/ui/` commit is on the `ux-redesign-impeccable` branch authored by the dedicated UI session, which is *correctly executing the UX-audit spec* (deck-clamp `4e79523d`, ghost-controller silk-65 `c4639791`, Settings-Coach-button `0ec01fc9` = my FIX A). The REAL drift = Codex grinding **off-spine backend cheap wins** (KAAN_SPOKE full-window `8926fcae`, audio-callback churn `a08a5305`, BPM tolerance `2ca3b6ac`, Telegram timeout `71f6d306`, Viber pre-tool watchdog `0e39127d` — only this one LIVE-proven) **while CLOSE-MOVELESS (#1 anti-slop fix) stayed dark.** Corrective: start at the TOP of PULL-NEXT below; `drive-vibemix` by-ear before "done" (CODEX-GOAL-V2: test-green-but-dark = 0).
>
> **PULL IN THIS ORDER (re-derived open at HEAD `0ec01fc9` — shipped items DROPPED):**
> 1. **CLOSE-MOVELESS** → [`CODEX_READY-CLOSE-MOVELESS-DESCRIBEBANK.md`](../2026-06-03/CODEX_READY-CLOSE-MOVELESS-DESCRIBEBANK.md) + verified supply [`CODEX_VERIFICATION-CLOSE-MOVELESS-SUPPLY.md`](../2026-06-03/CODEX_VERIFICATION-CLOSE-MOVELESS-SUPPLY.md) — **still OPEN at HEAD** (`speak_gate.py:29` frozenset still lacks TRACK_CHANGE; `suggestion_voice.py:64` still hedge-first). Supply prereq `b1322c09` landed + 4/4-skeptic verified — the grounded-payload path NOW fires on a populated+embedded library (attach-rate gates on "now-playing title → embedded library row", NOT a citable deck). PRIMARY: add `"TRACK_CHANGE"` to the frozenset + flip `tests/runtime/test_speak_gate.py:72-76`. SECONDARY: reframe receipt forward-fact-first. Then `drive-vibemix` by-ear. **THE one thing.**
> 2. **Repeat/novelty hard-gate** → ✅ packet ready → [`CODEX_READY-NOVELTY-SPEAK-GATE.md`](../2026-06-03/CODEX_READY-NOVELTY-SPEAK-GATE.md) (9/9 anchors @HEAD, premise HOLDS) — built-but-dark. The `_ai_text_history` recent-lines ring (`dj_cohost.py:1312/1616`) is live-populated but consumed *prompt-only* (`:2249-2259`); the documented twice-spoken describe-bank line is unsuppressed. Fingerprint the EVENT + novelty short-circuit in `speak_gate.py:65` (silent on `repeat_of_recent`). Only ever REDUCES speech (Inv #2/#3 trivially safe). ~30 LOC helper. MANDATORY grounding-review.
> 3. **Deck-attribution refresh.py leg** → [`CODEX_READY-DECK-ATTRIBUTION-NONMIDI-LEG.md`](../2026-06-03/CODEX_READY-DECK-ATTRIBUTION-NONMIDI-LEG.md) — **PARTIAL.** The `deck_poller` leg shipped (`b1322c09`) and won the headline next-song seed (via `resolve_seed_context` max-conf fallback). The `refresh.py:1254-1259` `audible_deck` leg is still controller-only → on a **no-MIDI master-only rig** `audible_deck="none"` caps track-confidence at 0.3 < 0.5 so **TRACK_CHANGE never fires** there (Kaan's FLX4 has MIDI, so it fires for him). **🟡 KAAN-GATE:** accept TRACK_CHANGE-dark-on-no-MIDI and close, OR authorize a narrow `refresh.py` wire (reuse `_nowplaying_playback_attribution` as a shared helper) WITH an ear-pass gate. Do NOT blind-build.
> 4. **predicted_drop_in_sec → live brain prompt** → ✅ packet ready → [`CODEX_READY-DROP-INCOMING-PROMPT.md`](../2026-06-03/CODEX_READY-DROP-INCOMING-PROMPT.md) (12/12 anchors @HEAD, premise HOLDS) — built-but-dark. Drop ETA is live (`refresh.py:1414`) + paints the chip but never reaches `prompt_builder` (grep: zero refs). Write a `[cue:]` registry anchor in the drop block (`refresh.py:~1419`) + a confidence-gated `drop_incoming[eta@s]` lens in `evidence_line` (`prompt_builder.py:~685`), mirroring the already-safe `count_in_eligible`. Packet flags: needs a tiny `_next_drop_section` helper (ETA-only signal has no section) + keep the fragment inside the `session_active`+non-None guard so cold-path goldens stay byte-identical. Inv #2/#3 safe. MANDATORY grounding-review.
>
> **🌟 NEW OPPORTUNITIES (ranked — from the 8-surface sweep; deduped vs PULL-NEXT above):**
> 1. **Vibe Judge verdict → debrief critique (FLAGSHIP pure-wire)** → ✅ packet ready → [`CODEX_READY-JUDGE-TO-DEBRIEF-CRITIQUE.md`](../2026-06-03/CODEX_READY-JUDGE-TO-DEBRIEF-CRITIQUE.md) (8/8 anchors @HEAD, premise HOLDS, Claude-QA'd). The Judge runs LIVE on every TRACK_CHANGE + persists a grounded `transition_judged` row (score/components/`citation_id`) to `events.jsonl`, but `debrief/main.py` `_build_cited_critique` (loop 151-173) drops that kind on the floor (grep: zero consumers). One branch: on `kind=="transition_judged"` + `verdict_state=="judged"`, emit a cited line reusing the row's own `citation_id` (mirror `judge_voice.py:32 verdict_evidence_line` verbatim), skip abstain rows. **Make-or-break detail (verified): `judge` IS a first-class evidence source (`evidence_registry.py:141/196`) so `[judge:transition@t]` survives the debrief stripper** — reuse the stored id, do NOT rewrite to `[ev:]`. No new compute/event/schema. Opens "earned review / best+worst blends". **Codex.**
> 2. **auto_crate as the UI set-prep front door** → ✅ packet ready (Rust/CLI half) → [`CODEX_READY-AUTOCRATE-UI-FRONTDOOR.md`](../2026-06-03/CODEX_READY-AUTOCRATE-UI-FRONTDOOR.md) (9/9 anchors @HEAD, premise HOLDS). B5 is built+keyless but reachable only via CLI; fresh users with no `codex login` get ZERO working set-prep (`index.ts:1380` faults the Viber surface). Add a `library_auto_crate` Rust `#[tauri::command]` (mirror `library_build_set` `library_cmds.rs:779`, NO `--backend` so it stays keyless; `map_curate_result` reusable VERBATIM) + register in `main.rs` invoke_handler. **Codex owns the Rust/CLI half; the UI button + agent_ready default-flip is a UI-session note.** Owner-gate O7/O10 (front-door/tier).
> 3. ~~**B13 — light deck-vision for launchd users**~~ → ❌ **REJECTED** (adversarial draft caught a false premise → [`CODEX_READY-B13-DECK-VISION-SETDEFAULT.md`](../2026-06-03/CODEX_READY-B13-DECK-VISION-SETDEFAULT.md) documents the rejection). Flipping `VIBEMIX_DECK_VISION` only starts a 1Hz capture loop whose JPEGs land in a buffer **NO live consumer reads**: the brain hard-wires `screen_jpeg=None` (v4 anti-hallucination killswitch, `dj_cohost.py:2247`) and the `deck_poller` vision leg is gated OFF pending Kaan's unfinished accuracy eval (`deck_poller.py:140`, Plan 59-05 Task 3). So the setdefault has ZERO product effect (NOT "a second grounded source" — that claim was false). Sub-claim also wrong: `is_available()` is deps-only, not a TCC probe. **Realizing it needs: (1) pass the deck-vision accuracy eval + Kaan sign-off, (2) wire the screen source into DeckPoller w/ `vision_enabled=True`, THEN (3) the default.** → reclass as **Kaan-eval-gated**, not an engineering quick-win.
> 4. **phase-tape: SessionSnapshot hardcodes `phase=()`** → ⏳ NEW packet. `_build_session_snapshot` (`ws_bus.py:770-771`) emits empty phase + 0% even though `state.phase`/`phase_history` are live and the UI maps `p.phase`→`PhaseChunk[]` already. Map history→`tuple[PhaseChunk,...]` + `phase_now_pct` from `time_in_phase()`. **Codex backend half; UI must mount `renderPhaseTape` (UI-session note).**
> 5. **Listen-by-ear lesson audio (L1.10/11/12)** → ⏳ NEW packet (after spine). The ExemplarPlayer/Finder/headphone-bus stack is built+LIVE but reaches only 1 of 37 lessons; L1.12 says "listen for the kick disappear" over silence. Thin generic observer + 1 track/cue ref per script, then `register_lesson_observer` (`__main__.py:~2928`). Small-build. **Codex.**
> 6. **debrief→lesson recommendation → tappable chip** → ⏳ NEW packet (after spine). `build_graduation_summary` computes a cited `debrief_recommended_lesson_id` but the `LearnTutorSpeak` payload (`learn_messages.py:651`) drops the id → DJ only HEARS it. Add the id to the payload; the actionable rail (`ipc.learn.start_lesson` → `ipc_handlers.py:225`) is already live. **Codex backend half; UI chip is UI-session.**
>
> **🔬 NEW FROM FRESH-SURFACE SWEEP (wf `wriq33fc0` — 7 surfaces, accidental-vs-deliberate-dark verified; ⏳ packets TBD):**
> - **🟠 HIGH — Direct-mode genai client has NO request timeout** → the live reaction brain can HANG indefinitely (a stalled Gemini stream read; only the 20s playout-timeout recovers = dead-air, a never-late violation). One-line wire: mirror the proxy's `timeout=120_000` at `__main__.py:1563` (direct `genai.Client`) + the twin at `:4725` (proxy path already bounds it `proxy_client.py:44`/`llm_factory.py:83`; direct never got the symmetric treatment). **Codex — high-value robustness, do EARLY.**
> - **🟠 HIGH — MOSS live voice-swap nukes the 728MB engine** → `set_voice` (`local_tts.py:438`) sets `_engine=None` → next reaction synchronously reloads 9 ORT sessions + warmup (~multi-sec in-ear stall) just to change the prompt-codes array, which is session-INDEPENDENT. Add an in-place setter rebuilding `_OrtCpuEngine._prompt_audio_codes` (`local_tts.py:308` ← `ort_cpu_runtime.py:516`) on the warm engine; keep `engine=None` only for not-yet-loaded. Small-build. **Codex.** (UI hedge "voice changes when Sven restarts" `SettingsDrawer.ts:1298` becomes obsolete → UI-session note.)
> - **🟡 MED — AgentDecisionTrace persisted nowhere** → built every live pill decision (`decision_runtime.py:577`, 14 fields) but only 2 reach the wire; `to_redacted_dict()` has 1 caller (a test). One-line: `recorder.log_event("decision_trace", **result.trace.to_redacted_dict())` at `suggestion.py:822-862`. Feeds debrief + Respan + the Inv#2 audit. **Codex.**
> - ✅ CLEARED (NOT opportunities — saved the effort): proxy keyless path (engineering-complete; Kaan-gate = Gemini billing + default-flip; `/register` LIVE-200 + brain passthrough hits Gemini 429 "credits depleted"), profile→persona (already wired both surfaces), recall lane (deliberate privacy gate). Respan eval-regression = MED net-new build, provider gate already cleared.
>
> **🌙 OVERNIGHT REAL-USER QA HARNESS** → ✅ packet ready + Claude-QA'd → [`CODEX_READY-OVERNIGHT-QA-HARNESS.md`](../2026-06-03/CODEX_READY-OVERNIGHT-QA-HARNESS.md) (28 anchors @HEAD `dbe86b17`, feasible, invariants clean). **The linchpin for autonomous overnight quality — high build priority.** Mostly WIRE: the keystone replay backend EXTENDS the partial `scripts/eval/replay_harness.py:482` (already loads a set WAV via `fill_from_wav` + drives real EvidenceRegistry/EventDetector/CitationLinter, no sounddevice — just stops at detector emissions); inject recorded frames at the `audio_buf.push` ring via an env-gated (`VIBEMIX_REPLAY_SESSION`) swap at `__main__.py:3170`/`platform/__init__.py:52`. **FAST** = time-warp deterministic replay (a 60-min set in seconds, proven by `tune_detectors.py:256`). **PARALLEL** = N headless instances on distinct ports + per-instance `HOME` isolation (config/recordings/ui.log all honor HOME, no code change). **One small BUILD prerequisite:** a `VIBEMIX_WS_PORT`/`VIBEMIX_DEBRIEF_PORT` env knob (both hardcoded today, `constants.py:49`/`__main__.py:586`) — do FIRST to unblock fan-out. Judge = 3 layers (A keyless grounded-checklist via `eval/session_report.py:244`, B Sven blind-judge gated on Respan key, C broken/dead detector). **Honest scope cut:** the live LiveKit/MOSS leg can't time-warp (single in-flight gate) → grade it realtime via control-drive scenarios; the music-perception throughput runs the deterministic refresh+detect+coach-prompt path. Build order in §4. Closing loop = read findings → fix → re-run scenario → commit = the auto-coding-excellence flywheel.
>
> **✅ SHIPPED-REAL / FIXED since the last pin (verified — do NOT re-surface):**
> - **B16 audio-key estimator** (`20f7a189`) — SHIPPED-REAL. `key_estimator.py` (KK/KS pure-numpy, honest-None) wired producer (`folder_ingest`/`ingest`) → consumer (`transition_scorer:185` lifts off `key_unknown`); Inv-#3-safe (`deck_poller:276` suppresses `numpy_ks` from citable deck proof); 51 tests green; `CODEX_VERDICT-B16-AUDIO-KEY.md` exists. Only Kaan LIVE folder-crate ear-pass remains. **DROPPED from pull (was #3).**
> - **B1 rate-ramp** (`cdc369fd`, Codex co-author) — SHIPPED-REAL. `np.linspace`+`np.cumsum` per-frame ramp in `miniplayer.py:63-65` (exact hardening-packet spec), clamp-once at the `two_deck_player` boundary, 2 test files. **DROPPED from pull (was #4).**
> - **B5 auto_crate** (`bde5df09`), **Q8 voice-muted banner** (`85e3c6d6`), **B15 ANLZ-cues** (`c1486520`) — SHIPPED-REAL (as last pin).
> - **SALVAGE-GATE Inv-2** — FIXED (`f0c5a15f`); the 🔴 RELEASE BLOCKER row in the legacy block below is STALE.
> - **B1 practice-audio silence** — FIXED (real loop, RMS 0.19).
> - **UX-LANE (UI session, `ux-redesign-impeccable` branch):** Settings Coach-not-dead-button `0ec01fc9` (audit FIX A), deck hero clamp `4e79523d`, ghost-controller silk-65 `c4639791`. The UI session is executing the UX audit on its branch — coordinate, don't clobber its in-flight work (per the OPEN-TO-CODEX banner up top). Codex is no longer barred from `tauri/ui/`.
> - **UX-LANE — DECLUTTER + IMPECCABLE pass (2026-06-03 #2, UI session, `ux-redesign-impeccable`):** ran `/impeccable audit layout delight` + the declutter packet surface-by-surface, each commit gate-green (`build && test`, 1548 pass) + **deck verified LIVE** (`?dev=session-mock`). **Audit = 13/20 "Acceptable"** (not slop — over-containerized + raw-branded-rgba; 0 P0/P1). LANDED: deck layout `eb18efb0` (one lit slab on void: cut grille, de-card rail+foot, unify bpm/key, cut PERSONA label) + delight `f5062715` (persona dial perk-up) + status-row text cut `c49ee32d` (drop set-once voice/genre); shell `ca90cdb4` (de-card Cmd+K summary grid, plain kbd legend, verb-led placeholder, active-nav weight, idle dash-chips hidden); library gold-quarantine `c36d0680` (slot ordinals off gold). **Full impl-log + 3 VERIFIED-UNSAFE corrections in [`../2026-06-03/CODEX_READY-DECLUTTER-LAYOUT.md`](../2026-06-03/CODEX_READY-DECLUTTER-LAYOUT.md) §6.** ⚠️ **CODEX: do NOT land these 3 "Tier-1 safe"-rated items — they're wrong:** (1) library `command-field` orbit cut — it's a **declared wire** (`mock-transfer/contract.ts:102`), under-wired not decorative; (2) HELP DOCS+SOURCE merge — **reverses Kaan's H10-final decision** (`help-group.ts:283`) + breaks `help-group.spec.ts`; (3) settings 12-group de-containerize — **reverses Kaan's "saçmalık→recessed modules" call** (`group.ts:25`); the seam is static, not a One-Rose breathing violation. **REMAINING declutter = eyes-on/owner-call, NOT a blind sweep:** pill choreography (live-rendered, gate can't confirm) + learn booth grid-rhythm (eyes) + debrief/settings DOM cuts (need lockstep spec updates). The deck is clean + shippable as-is.
>
> **📝 OWED PACKETS — status (waved-drafted by wf `wwomhf7cq`, each ground→verify'd at HEAD):** ✅ WRITTEN + Claude-confirmed: novelty-gate · Vibe-Judge→debrief · drop→prompt · **B7 voice section-pairing** ([`CODEX_READY-B7-VOICE-SECTION-PAIRING.md`](../2026-06-03/CODEX_READY-B7-VOICE-SECTION-PAIRING.md), 10/10 anchors — section pairing computed+rendered but never spoken; one citation-gated clause in `suggestion_voice.py`) · auto_crate Rust front-door. ❌ B13 deck-vision REJECTED (false premise, see opp #3). **Still owed (next writes):** ⑤ **phase-tape snapshot** (`ws_bus.py:770` hardcodes `phase=()`; backend half is Codex, UI mount is UI-session) · **listen-by-ear lesson audio** (L1.10/11/12, after spine) · **debrief→lesson tappable chip** (after spine). ⑥ **B4/B6/B8** — write but mark **Kaan-gate-blocked on O1** (CUE-DETR hosting+accuracy; `REKORDBOX-VS-CUEDETR` re-ranks B4 OFF the critical path). ⑦ B12-rewrite (lowest, 0.05 nudge).
>
> **🔧 BACKLOG FIX (stale §4):** in `WIRE-THE-GOLD-MASTER-BACKLOG.md` §4 PACKET INDEX — B5+B15 now HAVE packets (listed NEEDS-WRITING); B16 row → SHIPPED; the B4 row "EXISTS in ENGINES-10X-TOP" is misleading (that packet scopes B4 OUT).
>
> **✅ SHIPPED EARLIER TODAY (do not re-surface):** Q4, Q5, Q6, B2(tutor→MOSS), B3(grade meter), B9(skill frontier), B10(audible EQ decks); sven slop cluster (salvage-recheck, heartbeat-steer, auto-event-defer, playout-timeout, bpm-freeze); viber (chat-timeout, batch-inspect, fast-path). **Q1/Q2 render shipped but proven only on synthetic frames → real proof gated by deck-attribution / a live MIDI rig.**
>
> ---
>
> ## 🎨 UX-REDESIGN LANE (2026-06-03, PARALLEL to the backend spine — frontend-only, zero collision; Kaan flagged "UI hiç kullanıcı dostu değil" + "pill aşırı çirkin")
>
> Grounded in 5 live screenshots → located audit ([`CURRENT-STATE-UI-AUDIT.md`](../2026-06-03/CURRENT-STATE-UI-AUDIT.md)) → impeccable-lens redesign ([`CODEX_READY-UX-REDESIGN-IMPECCABLE.md`](../2026-06-03/CODEX_READY-UX-REDESIGN-IMPECCABLE.md), 7 targets, each distill/quieter/clarify/critique/contrast/ux-writing + AI-slop adversarial-verified). All specs read-only; Codex implements via `cd tauri/ui && npm run build && npm test`, optimistic repaint, Inv #5 (idle≠fault). **Systemic disease = engine jargon leaking as user copy; the codebase already has the cure pattern (`footLabelFor` `grounded`→"READING THE ROOM" `cohost.ts:867`).**
>
> **PULL IN THIS ORDER (cheapest+safest first):**
> 1. **COPY-only (zero-risk, ship FIRST):** strategy-pills→labeled radiogroup (`library.html:90-91`, keep `data-strategy` byte-for-byte) · VIBE-SCOPE de-jargon (`CLAP 512D`/cosine→"Sound match/Close/Far") · KEY null `title`/aria · Learn brief jargon→coach (`learn-window.ts:353-370`) · Debrief "SESSION CRASHED"→"stopped before saved" + de-jargon. Build the systemic **`shell/dj-vocab.ts` translate-at-edge helper** + extend `design-slop-gate.spec.ts` CI leak-gate (the glossary is in the packet §5).
> 2. **REAL BUGS (frontend):** **SETTINGS persona-collision = live-correctness bug** — 3 co-equal controls (mode/mood/lens) over 1 axis; **FIX A `render-loop.ts:380` re-derive `interaction:` from `lens`** (today toggling Coach/TEACH visibly does nothing) + FIX B aria order `SessionLayout.ts:1287`. · DECK hero clips mid-word → `-webkit-line-clamp:3` (`.vmx-now` `SessionLayout.ts:573-583`). · DECK confident hero over a DEAD pipe → plumb shell `connection` → `data-mode="stale"` demote (no new IPC). · LEARN ghost controller `--silk-40`→`--silk-65` (real 3.50→7.18:1). **Each bug-fix that touches pinned tests must update them in the SAME commit (packet lists the exact specs) or the gate fails.**
> 3. **PILL DISTILL — THE CENTERPIECE (bigger, careful):** `pill/pill.css` 4127 lines → **91 keyframes→~11, 91 animations→~11, 71 box-shadows→~24, 68 reaction-tones→3 states (good/neutral/careful), ZERO gold**. **Subtraction of CHOREOGRAPHY, not surface** — KEEP the load-bearing sealing `--glass-3` glass body verbatim (DMG/Win parity, tauri#13415) + the TS contracts (`PillReactionTone` enum, `REACTION_LEAD_PATTERNS`, `pillReactionPulseSlot` replay) or tests break. **Critique caught a real Gold-Quarantine violation:** delete the rose→gold gradient fills `pill.css:1168/1310/1727` + gold halos `1556/1741` + `2171/3262`. GATED sub-cut: the `burst/overdrive/streak` gamification TS is a FEATURE removal — confirm with grade-vocab owner, don't let a visual pass delete shipping grade/streak. Heur 25→32.
> 4. **BACKEND-DARK (no UI edit):** the describe-bank hero copy + empty KEY are HONEST UI over dark data — gated on the spine above (CLOSE-MOVELESS / B16), not UI bugs.
>
> Per-target verdicts: PILL CONFIRM · STRATEGY-PILLS REVISE · DECK CONFIRM(+A/B/C) · LEARN REVISE(3 mandatory) · DEBRIEF CONFIRM(+2) · SETTINGS REVISE(A+B) · VOCABULARY REVISE. Heuristics before→after in the packet board.
>
> ---
>
> ## ★★★★★ START HERE — NEW SINGLE QUEUE (2026-06-03, supersedes the scattered rows below as the priority queue)
>
> - **Operating contract = [`CODEX-GOAL-V2.md`](../2026-06-03/CODEX-GOAL-V2.md)** (replaces the old "find maximal upgrades / level up modules" goal — that drift cost ~24/33 commits of UI-reskin + test-greening with ZERO by-ear proof). Loop is now NON-SKIPPABLE: **build → run the real app (`drive-vibemix` / `VIBEMIX_DEV_SIDECAR=1`) → prove by-ear/eye → `vibemix-grounding-review` → surgical atomic commit.** A **test-passing-but-dark feature = 0**. **WIRE-not-BUILD** is default; only **B4** (host CUE-DETR) and **B5** (`auto_crate.py`) are blessed BUILDs.
> - **Work-queue = [`WIRE-THE-GOLD-MASTER-BACKLOG.md`](../2026-06-03/WIRE-THE-GOLD-MASTER-BACKLOG.md)** — codegraph-verified, critic-checked. "What's next" is a LOOKUP of §7 dependency spine, not a re-decision. §4 = packet index, §6 = owner gates, §8 = critic check-in (incl. the A1 guard-rail). The whole day's audit consolidated the scattered W1b/X1/L3/MOSS rows below INTO this backlog — defer to it.
> - **🔴 RELEASE BLOCKER (fix BEFORE the pull order below) → [`CODEX_READY-FIX-SALVAGE-GATE-INV2-BYPASS.md`](../2026-06-03/CODEX_READY-FIX-SALVAGE-GATE-INV2-BYPASS.md)** — Claude-source-verified during the spine verification (`CODEX_VERIFICATION` below). Commit `0a60849a`'s harmonic-salvage branch `return`s at `deck_context.py:2956` BEFORE the mixer-low-kill (`:2971`)/no-move-control (`:2981`) guards, and `VIBEMIX_CITATION_LINT` is OFF by default → an ungrounded non-harmonic clause riding in a salvaged reply is SPOKEN. This is the anti-slop release gate (Inv #2); it cannot ship as-is. Fix = continue the guard ladder on the salvaged text (don't early-return). Packet has the TDD test + the do-NOT (don't force the linter on).
> - **VERIFICATION of the just-landed spine → [`CODEX_VERIFICATION-CODEX-SPINE-2026-06-03.md`](../2026-06-03/CLAUDE_VERIFICATION-CODEX-SPINE-2026-06-03.md)** — Q1/Q2/B1/B2/B3/Q6 + sven slop cluster, all SRC-traced to the live caller. Spine is mostly WIRED-REAL; 2 real defects: the salvage bypass above (HIGH) + **B1 practice audio is SILENT** (MiniDeck fed `np.zeros`, `beatmatch_practice_driver.py:65` — grading works, learner hears nothing; "audible decks" is overclaimed). Also: V3 diet-window now sends a **60s** audio Part on every MIX_MOVE (not 18s) — measure TTFT on the never-late budget. A1 holds (practice audio can't bleed onto a live set) but via the lesson-context gate, NOT the dead `exemplar_audio_forbidden` flag.
> - **🟢 LIVE BLOCKER (Kaan hitting Viber NOW) → [`CODEX_READY-VIBER-UNBLOCK-TIMEOUT-AND-BATCH-TOOLS.md`](../2026-06-03/CODEX_READY-VIBER-UNBLOCK-TIMEOUT-AND-BATCH-TOOLS.md)** — Viber chat dies at `CHAT_TIMEOUT_S = 90.0` (`codex_curate.py:1372`; curate=120, build-set=180 — chat is the stingy one). **Instant unblock = raise that line to 180+.** Real "parallelize tools": the MCP transport SERIALIZES (`mcp_server.py:151` + single-worker `dispatch`), so concurrent calls are impossible — the fix is a **batch `inspect_candidates([ids])` tool** (collapses 3×N one-by-one `get_track_*` round-trips → 1, preserves the seen-set guard) + a positive prompt line to prefer it. Capability fix, not a guard. Feeds Respan Lane C (Viber trace).
> - **🟢 MAKE-SVEN-GOOD (measured + ready) → [`CODEX_READY-CLOSE-MOVELESS-DESCRIBEBANK.md`](../2026-06-03/CODEX_READY-CLOSE-MOVELESS-DESCRIBEBANK.md)** — Respan-measured gap: events that fire WITHOUT a controller move (TRACK_CHANGE) still describe-bank (friend **0** live AND sim; idle + real-moves already fixed by the speak-gate). Fix (positive, no NOT-X, honesty moat intact): PRIMARY = add `"TRACK_CHANGE"` to `_DESCRIBE_BANK_EVENT_TYPES` (`speak_gate.py:29`) → routes it through the ALREADY-wired grounded-payload gate (silent unless a real next-track/verdict receipt is attached; `coach.py:746-779` + `suggestion_voice.py:20` already feed it). SECONDARY = reframe the receipt (`suggestion_voice.py:62-67`) so the forward fact leads, not the hedges (lifts grounded-branch friend 1→2). Acceptance = a `respan_sven_sim.py` scenario. Evidence: `RESPAN-SVEN-HEARTBEAT-EVAL-RESULTS.md` + 4 Respan datasets. Reusable instruments: `scripts/eval/respan_sven_heartbeat_judge.py` (judge real lines) + `respan_sven_sim.py` (controlled move sim).
> - **PULL ORDER (all first items have packets — clear runway):**
>   1. **Q1 + Q2** — render next-song `reasons[]` + cue-confidence receipts in the pill → [`CODEX_READY-ENGINES-10X-TOP.md`](../2026-06-03/CODEX_READY-ENGINES-10X-TOP.md). Frontend-only, zero risk, **ship FIRST** (first visible "it's alive" proof).
>   2. **Q3 → B1 → B2 → B3 (+ Q6)** — the fused beatmatch loop → [`CODEX_READY-LEARN-FUSED-BEATMATCH.md`](../2026-06-03/CODEX_READY-LEARN-FUSED-BEATMATCH.md) (6 increments). NOTE: the beatmatch phase grade is **degenerate today** (MiniDeck frozen at 0.0) — B1 routing `render_block` to a stream makes phase real for the first time; land Q6 (octave-fold) with it.
>   3. **Q4 / Q5 / Q7 / Q8** — cheap correctness flips alongside.
> - **GUARD-RAIL (do NOT miss — anti-slop regression):** A1 `exemplar_audio_forbidden` (`learn/lesson_flow.py:115/266`) — B1/B10 audio wiring must respect it; audio must NEVER play over a live set (Inv #3).
> - **NEXT packets Claude still owes (write before Codex reaches them):** B4 host/accuracy ¶, **B5** `auto_crate.py`, **B15** (wire imported Rekordbox phrase/cue → live grounding; HIGH-leverage — Rekordbox already parses phrase+cues, it's imported-but-dark), B6/B7/B8, B12-rewrite.
> - **★ MINING SYNTHESIS (2026-06-03, "what to snort") → [`WHAT-TO-SNORT-TODAY.md`](../2026-06-03/WHAT-TO-SNORT-TODAY.md)** — 9-miner sweep of today's 42 packets, deduped vs the backlog + today's ship-list. THE ONE THING = route CLOSE-MOVELESS (above). SNORT-NOW lane = the Q1/Q2/Q3/Q4/Q5/Q6/Q8 items already queued above. **Two genuinely NET-NEW golds NOT yet in the backlog:** (1) **Audio KEY estimator (B16) → [`CODEX_READY-AUDIO-KEY-ESTIMATOR-KS.md`](../2026-06-03/CODEX_READY-AUDIO-KEY-ESTIMATOR-KS.md) — WRITTEN + source-corrected, ready to build.** Krumhansl-Schmuckler ~80-130 LOC pure-numpy (adds its OWN 12-bin chroma — no chroma exists today, only mel), runs offline at ingest → writes camelot to the library entry. **CORRECTED PREMISE: this is NOT a Judge un-abstain** (the Judge abstains on `routing_disabled` first + needs an executed-mix signal — that's the moat, do NOT touch it). The real win = the next-song scorer/pill receipt (`harmonic_score` returns `0.55 key_unknown` for every folder-only track today, `transition_scorer.py:185`) + the live "what's next" seed + Viber sequencing — turns harmonic intelligence ON for untagged libraries. Owner-gate NONE, license-clean (no Essentia/librosa/torch), deterministic (no model literal). Real-audio bench gate + drive-vibemix live-proof in the packet. (2) **Deck-attribution / cue-identity live blocker** (`no_single_attributable_deck` / `connected_no_midi_traffic`) — the supply choke that silently keeps Q1/Q2/TRACK_CHANGE receipts DARK on the FLX4 rig; a focused live-debug lane, not a feature. Also pin to B1 before build: `exemplar_audio_forbidden` guard, per-frame gain RAMP in `two_deck_player.py` (zipper-click), and B2 live-voice-swap needs a MOSS engine reload (boot-read alone leaves live-swap silently broken).
> - **★★ 6 NEW CODEX_READY PACKETS (2026-06-03, wf `3a9b9b9b` — each ground→adversarial-verify'd; the verify caught 3 FALSE premises, do NOT skip the corrections blocks at the top of each):**
>   - **B5 → [`CODEX_READY-B5-AUTO-CRATE.md`](../2026-06-03/CODEX_READY-B5-AUTO-CRATE.md)** — keyless deterministic set/crate builder (blessed BUILD). Anchors 26/26 clean. (Correction: B16 is a separate build, not a prereq.)
>   - **Deck-attribution → [`CODEX_READY-DECK-ATTRIBUTION-NONMIDI-LEG.md`](../2026-06-03/CODEX_READY-DECK-ATTRIBUTION-NONMIDI-LEG.md)** — a non-MIDI (nowplaying) attribution leg so the next-song receipt lights on the FLX4/master-only rig. ⚠️ MUST wire BOTH `refresh.py:1229` AND `deck_poller.py:369` or the headline receipt stays dark. Owner-gate the 0.6 cite-floor.
>   - **beat-this bench → [`CODEX_READY-BEAT-THIS-BEATGRID-BENCH.md`](../2026-06-03/CODEX_READY-BEAT-THIS-BEATGRID-BENCH.md)** — dev-tooling beatgrid bench (not release-blocking, zero product wiring; bench vs the POST-guard BPM, not raw).
>   - **Q8 → [`CODEX_READY-Q8-VOICE-MUTED-BANNER.md`](../2026-06-03/CODEX_READY-Q8-VOICE-MUTED-BANNER.md)** — voice-muted badge via `ipc.status.tick` + 1 field. ⚠️ land schema+`codegen:ipc` FIRST (else `additionalProperties:false` rejects it); `messages.ts` is auto-generated.
>   - **B1/B2 pins → [`CODEX_READY-B1B2-HARDENING-PINS.md`](../2026-06-03/CODEX_READY-B1B2-HARDENING-PINS.md)** — A1 guard + B1 rate/gain ramp (the real build). **B2-voice DEMOTED to ear-pass only — it's ALREADY WIRED (`__main__.py:2035` passes MossLocalTTS as `cascade_agent`, `_apply_voice:344` calls `set_voice`), building it = duplicate-path regression.**
>   - **B15 → [`CODEX_READY-B15-VERIFY-ANLZ-CUES-LIVE.md`](../2026-06-03/CODEX_READY-B15-VERIFY-ANLZ-CUES-LIVE.md)** — ⚠️ **BACKLOG CORRECTION: B15 is NOT an owed wire — the ANLZ→`TrackEntry.cues` persist ALREADY SHIPPED** (`_materialize_anlz_cues` `ingest.py:268/831`; live consumers read it). Re-issued as VERIFY-not-build + a fix to the stale `REKORDBOX-VS-CUEDETR.md`. **Update the WIRE-THE-GOLD B15 row to "persist DONE — verify-only."**
> - **Owner gates (Kaan, not engineering):** O1 CUE-DETR host (now LOW pri — Rekordbox covers it, B4=fallback) · O2 MOSS CI · O9 practice-loop WAV licensing (blocks B1 packaged audio) · O10 free-vs-Pro.
> - **Respan (observability/evals) = POST-COMPACT, not now.** For Viber + Sven evals + agent-ops; live-product gateway privacy-gated.
> - **KAAN's one action to start the loop:** set Codex's run-goal to `CODEX-GOAL-V2.md` (replace the old goal). Optional auto-pickup: add one pointer line to `AGENTS.md` Architecture-notes → "active work queue = `.planning/packets/2026-06-03/WIRE-THE-GOLD-MASTER-BACKLOG.md` under the CODEX-GOAL-V2 contract."
>
> *(Everything below is the 2026-06-01/02 routing history — still valid as evidence/detail, but the backlog above is now the queue.)*


- **Date:** 2026-06-01 (re-pinned 2026-06-02) · **Pinned HEAD:** `d0506c5f` (re-pin before judging if Codex lands again) · **Maintained by:** Claude read-only swarm.
- **🟢 KAAN DECISION (2026-06-02): MOSS voice = LOCAL (bundle into the DMG).** Resolves the #1 ship-kill-shot — the host-archive / `VIBEMIX_MOSS_TTS_ARCHIVE_URL` / auto-fetch path is now MOOT. Ship path = bundle the ~MOSS model into the signed build + rebuild/sign/notarize. (The voiceless-build gate in `prepare_tauri_build.py` should arm `require_moss_source=True` against the bundled model.)
- **★★★★ WHAT CODEX SHIPPED in 40h (audit, Claude-verified) → [`CODEX_WHAT-CODEX-SHIPPED-40H.md`](../2026-06-02/CODEX_WHAT-CODEX-SHIPPED-40H.md)** — 262 product commits classified (6 lanes, each RAN tests + re-grepped). **VERDICT: CONVERGING, not thrashing** — 2.8:1 fix:feat = a hardening+honesty window; **~15 SHIPPED-LIVE capabilities, zero false-ships / zero reverts / zero headline test-theater.** Claude-spot-verified @`d0506c5f`: the 06-01 orphan gaps are CLOSED (`virtual_position_sec`✓, `decide_speak_gate`✓, beatmatch driver wired ✓ 9 hits), + multi-ecosystem ingest (Serato/Engine/Traktor/VirtualDJ), EQ-move grounding keystone, MOSS-only rip-out done, eval/repair harness. **Headline gap (✅verified): DROP live speech STILL NOT COMMITTED** — `reaction_line` = 0 in committed `coach.py` at HEAD (2 in working tree); 40h of DROP churn, still dirty. Release button untouched (DROP commit + DMG rebuild pending — now unblocked by the MOSS-local decision above). Flag: `learn/` is a ~35-module subpackage not yet in CLAUDE.md architecture.
- **★★★★★ READ FIRST (THE RELEASE GATE — voice quality) → [`CODEX_VOICE-QUALITY-VERDICT.md`](../2026-06-02/CODEX_VOICE-QUALITY-VERDICT.md)** — the bar Kaan blocks on: does Sven sound like a real DJ friend or AI slop? Audited GROUNDED in the REAL eval-run transcripts (the one LIVE-evidence axis). **VERDICT: STILL-SLOPS — ~85-90% of real spoken lines are pure describe-bank "anca tarif"** (matches Kaan's live verdict). **Two findings Claude HEAD-verified @`50b7e11e`:** (1) ✅ **grounding is NOT the slop source and does NOT make Sven robotic** — the feared "I can't call that a transition without two-deck proof" lines are stripped to SILENCE, never spoken; so do NOT loosen grounding to fix this. (2) ✅ **the fix is real + cheap + subtractive:** the X1 speak-gate is HEARTBEAT-ONLY (`speak_gate.py` `if ev.type != "HEARTBEAT": return speak` — verified; live via `coach.py:795`), so PHASE/LAYER events bypass it and leak describe-bank (the worst real line — a PHASE "metallic synth dissolved into acoustic guitar texture" — was spoken TWICE). **TOP FIX (5/6 lanes): widen `decide_speak_gate` from HEARTBEAT-only → any automatic event lacking a grounded-voice-payload/cited-cause + a last-N novelty ring** — ~3 lines, only ever REDUCES speech (Inv #2/#3 trivially safe), routes through vibemix-grounding-review. **Prompt lever:** `matrix.py:342` literally licenses "describe what you hear as texture/weight/motion" as output (✅verified — the "anca tarif" root); lift the anti-narration ban out of the coach-only block into the shared block so describe is step-1-internal, never the whole line. 3 worst real slop lines quoted verbatim in the doc. **This is the product's soul + a 3-line fix — highest ROI on the board.**- **★★★★★ READ (SHIP GO/NO-GO) → [`CODEX_SHIP-READINESS-VERDICT.md`](../2026-06-02/CODEX_SHIP-READINESS-VERDICT.md)** — 31-agent adversarial "are we good to ship?" audit across every surface + seam. **VERDICT: GO-WITH-CAVEATS** — code is SRC-solid + honest, but the artifact on disk can't ship and 2 headline features are dark on the default rig. **Claude HEAD-verified the 4 load-bearing claims myself at `9acad098` (the swarm had a false-alarm cascade — trust only what's stamped):**
  - 🔴 **Stale signed DMG** (May 31, predates speak-gate/MOSS/DROP) + **zero LIVE proof** MOSS speaks / reactions ground on a real rig — the ship-floor kill-shots (consistent with the MOSS-host + stale-DMG gates already on this board).
  - 🔴 **Live transition Judge is DARK on the default 2ch rig** — ✅VERIFIED: `deck_audio_capture = DeckAudioCapture(...) if deck_audio_routing.enabled or opened_channels != 2 else None` (`__main__.py:1288`) → `None` on the standard BlackHole-2ch master rig → `coach.py:902 if deck_audio_capture is not None` False → Judge never fires unless the rig is >2ch/routing-enabled. The "transition coaching" headline is invisible to ~99% of rigs. (This is the master-only reality the Mixxx-goldmine names; the per-deck beatgrid feed is its real unlock.)
  - 🟠 **intel claim/decision VALIDATION layer is DORMANT** — ✅VERIFIED clean: `decision_payload_for_state`/`context_for_state`/`decision_for_state` = **0 callers**; `claim_validator`/`decision_validator`/`decision_trace`/`musical_ontology`/`claims` have no live-path importer. **Correction to the synth:** `intel.decision_runtime.decide` IS live via the pill (`runtime/suggestion.py:790`) — so it's the *validation/trace/ontology* cluster that's dead, not the whole decision brain.
  - 🟠 **Citation linter is DEFAULT-OFF** — ✅VERIFIED: `VIBEMIX_CITATION_LINT` defaults `"off"` (`__main__.py:1488`), linter `None` unless opted in. The real live anti-slop gate (Inv #2) is the `<silence/>` + `filter_for_slop` path, NOT the linter — size release claims accordingly.
  - 🔴 **Always-on recording, no consent surface** — ✅VERIFIED: `VoiceRecorder` constructed unconditionally (`__main__.py:1053`); a distributed binary persists mic + master audio + AI transcripts by default. Privacy = a hard release gate (Kaan decision).
  - ✅ **Kaan's four (waveform now Claude-TRACED):** **waveform** = the pill `waveform.ts` is a LIVE **TTS voice-RMS** strip (real `setWaveform(rms,peak)`/rAF, NOT a mock) — but there is **NO live MUSIC/track waveform** (ws/overlay frame carries no waveform array; `overlay.py:32` only names `waveform_a` as a Rust EQ-map key; `audio_features.py` waveform = offline-embed only). The DJ-style scrolling **track-waveform-with-hot-cue-markers is UNBUILT** = opportunity, same substrate as semantic-hot-cue (needs the beat/cue layer → Mixxx M5 waveform mine + cue model). · **learn-engine** = COMPOSED end-to-end (beatmatch synthetic-deck caveat) · **semantic hot-cue** = WIRED for library/pill, NOT a live co-host feature · **curate overcharge** = BOUNDED (local Codex not metered Gemini; timeout + allow-list).
  - ⚠️ **DON'T CHASE GHOSTS:** the swarm re-flagged "beatmatch orphaned" in 13/14 lanes — it is ALREADY FIXED at HEAD (memory/INDEX were pinned to old SHAs). Ignore that finding.
  Scorecard + ranked fixes in the doc; census `_ship-census.md`, completeness `_ship-completeness.md`.
- **★★★★ SHIP-GATES DEEP-DIVE (companion to the verdict — Claude-corrected) → [`CODEX_SHIP-GATES-UNCOVERED.md`](../2026-06-02/CODEX_SHIP-GATES-UNCOVERED.md)** — dove the 5 surfaces the audit left UNVERIFIED (privacy, Windows, proxy, packaging, FE-IPC). **The doc carries a CLAUDE HEAD-VERIFICATION STAMP at its top — read it; the synth OVERCLAIMED 2 of 3 headlines, I git-checked at `f6ea6a3f`:** (1) ✅ **voiceless build CONFIRMED** — spec `datas` carries no MOSS `.onnx`, no auto-fetch caller → rebuilt DMG still mute, **but = the SAME existing MOSS-host kill-shot** (packaging angle), not net-new; (2) ❌ **"Windows never dispatches" REFUTED** — dispatch exists `platform/__init__.py:52-66`, `_audio_windows.py` is **568 lines of real WASAPI loopback** (not a stub); true gate = "Windows is built but UNPROVEN on a Win-11 box" = a LIVE smoke-pass + Kaan scope call; (3) ❌ **"not mic" REFUTED** — `recorder.py:172` input.wav = music+**mic** mix, so the privacy gate stays real. **Genuine value:** the G1 privacy consent-surface (Kaan decision: disclosure-only vs a true Off) + the packaging angle on the MOSS kill-shot. Proxy + FE-IPC came back ALREADY-HANDLED/decision-only. Lane raws `_gate-*.md` / `_gate-verify-*.md`.
- **★★★★★ SHOVEL-READY BUILD (what Codex builds WHILE the ship kill-shot ticks) → [`CODEX_MOAT-BUILD-SHEET.md`](../2026-06-02/CODEX_MOAT-BUILD-SHEET.md)** — the 5 verified moat opportunities turned into TDD-ready `CODEX_READY-W{1..5}-moat.md` specs (exact file:symbol, no-theater test w/ degrade-path, invariant + grounding-review angle). **Claude-verified prereqs @`7b76ea2e`:** **W1** beat-phase clock = extend the EXISTING `state/position_clock.py:virtual_position_sec` (substrate, gates on nothing — land FIRST) · **W2** per-band `band_env` envelope (✅ABSENT, ship-now parallel) · **W3** Mastered→hot-cue = ✅`learn/mastered_marker_writer.py` REAL (9.7k) + `coach.py:135` hook param both exist, only the in-track position-source gap remains (`coach.py:171 t_session` is set-relative) · **W4-a** arm clash gate via opt-in flag (✅`event_detector.py:114` default False, ~2-3h, grounding-review). **BLOCKED — do NOT build as-is: W4-b** (citable 2nd key) — ✅verified: `last_known` deck carries `camelot=None` BY DESIGN (poller leaves it None, refresh/single-writer normalizes — `deck_poller.py:19-20,274`), so it silent-no-ops; needs camelot folded into the remember path first + it's a MIDI-blend-window feature, not "99% of rigs". **Build order:** W1 → (W2/W3/W4-a parallel) → W5 phrase-clock on W1 → W4-b last. Grounding-review gates before flag-on: W1 §3b, W3, W4-a. This extends the moat WITHOUT touching the MOSS/privacy/LIVE ship-blockers.
- **★★★★★ MIXXX GOLDMINE (re-run off LOCAL clone, synth-rewritten + Claude-verified) → [`CODEX_MIXXX-GOLDMINE-EXHAUSTIVE.md`](../2026-06-02/CODEX_MIXXX-GOLDMINE-EXHAUSTIVE.md)** — the merged authority; I cloned Mixxx to `/tmp/mixxx-clean-room` and re-ran the dig reading local source (no WebFetch). **WINNER (Claude-verified @`92e5ad53`) = beat-tight VIRTUAL position clock** — and the keystone is that **`state/position_clock.py:virtual_position_sec` ALREADY EXISTS** (created today, already has the `bpm_confidence` gate at `:47 BEAT_TRUST_CONFIDENCE_FLOOR`). So the slice is **EXTEND it** to return the wrapped beat-phase `(thisPos−prevBeat)/(nextBeat−prevBeat)` (Mixxx source `bpmcontrol.cpp:650 getBeatDistance`) + consume in `drop_predict.should_arm_drop_call` + `refresh.py:_resolve_course3_phrase_anchor` — NOT invent. Pure arithmetic over numbers vibemix already computes; 5 primitives gate on it (on-beat DROP, phrase clock, key-locked transition, quantized advice). **Runner-up ship-now = M5 per-band envelope `band_env`** (✅Claude-verified ABSENT at HEAD — genuinely unbuilt; "where the energy's going" to the clock's "where the beat is"). **3 stale corpus claims Claude-KILLED (don't chase):** `.mixxx-tmp-loop/` IS gitignored (`.gitignore:225`, the swarm's #1 "GPL hazard" is neutralized) · psytrance band present + timbre-gated (`constants.py:149`) · `beatgrid_exact_atom` wired (`refresh.py:495→:500`). **PSYTRANCE VERDICT = SAFE_NOW** (techno reuse fine v1, keep DistortionClimb/AcidLineEntry excluded; residual = eval the 0.55 centroid floor, route on `band_env` to kill single-frame flip). **HONEST:** only M5 produced a real mine brief; M2/M3/M4/M6/M9/M10/M12 = phantom (never written) — the WINNER + M5 + corrections are the verified payload, the rest of the 14-table is leverage-ranked intent. Brain-map `_mixxx-baseline.md`, source anchors in the doc.
- **★★★★★ READ → [`CODEX_DROP-LIVE-PROOF-READINESS.md`](../2026-06-02/CODEX_DROP-LIVE-PROOF-READINESS.md)** — DROP/Sven live-proof readiness (5-agent, Claude-reverified at live HEAD). Builds on the dirty-tree DROP status below. **8/8 PROVEN** (signal DORMANT, slop FAIL-CLOSED `1ec73e9d`, cooldown `DROP:24.0`, registry holds resolvable `[ev:DROP@t]` `event_detector.py:546`). **2 gates remain to flip `VIBEMIX_DROP_CALL` for a real ear:** (b) the dirty `coach.py:755-769` DROP branch `continue`s BEFORE `recorder.log_event` (`:891`) → **NO durable `events.jsonl` row**, so the FP/FN audit reads nothing → **NEXT #1 = add `recorder.log_event("drop_call", …, arm_skipped=)`** referencing `[ev:DROP@t]`, + route the un-cited fixed line to `vibemix-grounding-review` for the binding Inv#2/#3 verdict; (c) a LIVE predicted-vs-actual artifact — NONE in tree. **Protocol (stress-corrected):** ground truth = human downbeat TAP, NOT the Rekordbox cue (grading vs the cue is CIRCULAR — it is the predictor's own input); N≥8 / ≥3 genres incl hardtechno; on-beat PASS ≤ ±1 **BAR** (NOT sub-second — position is 1Hz `_track_macos.py`); **MEASURE FN** (fail-closed biases to silence → a mute co-host passes an FP-only gate). Drop is **cue/section-derived, NOT master-RMS**.
- **★★★★★ VISION → [`CODEX_GREATER-ENGINE-TRAJECTORY.md`](../2026-06-02/CODEX_GREATER-ENGINE-TRAJECTORY.md)** — "is Codex on the right path or losing the greater engine?" 8-agent synthesis of the deep workflows (narrator→coach / Learn Army / context-engineering / orphan census) scored vs the 60-commit trajectory, devil's-advocate balanced. **VERDICT: ON THE RIGHT PATH — foundation-first is correct; the GOLD-WIRING manifest is being executed in dependency order (EQ keystone, beatmatch+cue credit, beatgrid-atom, X3 AIM, slop-spine all LANDED+verified live).** 3 genuine NOT-blocked losses nobody is on (ranked): **O7** = run the `drive-vibemix` live-capture proof that the wired credit paths actually fire on a real rig (THE Invariant-#3 release gate; protocol-not-code = zero commit cost; only `harmonic_mixing` has real-capture proof, the other 5 event→skill paths are synthetic-green only); **L3** = Mastered-moment → library hot-cue via `export_serato.py:201` Markers2 carrier (the only owned/replayable retention artifact, ~1-2d, `CODEX_READY-L3` exists UNLANDED); **X4-start** = begin `set_window_context` (TEXT) + reserve the one-line `live_coach_deep` route (the named greater-engine leap, ~80% scaffolded / 0% assembled — the AUDIT→BUILD shift). Runner-up = Judge timbre/spectral signal (skill-tree multiplier, cheapest moat BUILD). The ONE big future build = a live per-deck/phrase **beatgrid feed** (unlocks master-only Judge + phrase-clock + live-beatmatch *together* — correctly deferred behind the MOSS-mute + stale-DMG kill-shots). ⚠️ **Watch:** the last 6-commit cycle (`36225bae..b1c69a3b`) was pure route-bookkeeping (zero product code) — fine once, but if the next 30 skip BOTH the kill-shots AND the moat axis, foundation-first curdles into motion-without-progress.
- **★★★★ DIRTY-TREE (Codex-curated) → [`CODEX_DIRTY-TREE-CLASSIFICATION.md`](../2026-06-02/CODEX_DIRTY-TREE-CLASSIFICATION.md)** — dirty/untracked tree classified @ HEAD `264af2c4`, then partially consumed by Codex. **LANDED:** the DROP *signal* layer is committed as `d8a2e556` — `state/drop_predict.py` + `state/event_detector.py` + tests (21 passed; `VIBEMIX_DROP_CALL` default OFF, one flag-gated producer, registry-observable without audio). **LANDED:** the fixed-line anti-slop sub-gate is committed as `1ec73e9d` — `runtime/drop_reaction.py::reaction_line()` now runs `filter_for_slop` and raises before speech if a future bank edit trips it (35 focused tests passed). **LANDED:** the DROP per-type cooldown is committed as `3e9af0d9` — `MIN_EVENT_GAP_PER_TYPE["DROP"] = 24.0` with detector tests proving separate crossings throttle before the gap and re-arm after it (28 focused tests passed). **STILL HOLD:** the dirty `coach.py` DROP *speech* hunk — remaining gate = (a) grounding-review on the un-cited fixed speech text (Inv #2/#3), (b) live on-beat/false-positive artifact. **Invariant to keep: exactly one DROP producer, flag-gated** (a second producer risks ungated speech). **DROP_IGNORE:** `.planning/eval-runs/*/` (large local proof archives, not release input) · strays `.cp_probe.txt` / `.mixxx-tmp-loop/` / `.planning/.tmp_filelist.txt`. **CLAUDE.md runtime guidance corrected at `9b3855c5`:** product TTS is MOSS-only, passthrough ships silent (`PASSTHROUGH_GAIN=0.0`), and heartbeat is `180.0` + speak-gated. Lane raws: `_dirty-raw-*.md` + `_dirty-verify-drop-split.md`.
- **★★★★ PRIOR BRUTE-FORCE GAPS BOARD → [`CLAUDE_BRUTE_FORCE_NEXT_GAPS.md`](../2026-06-02/CLAUDE_BRUTE_FORCE_NEXT_GAPS.md)** — 26-agent read-only brute-force gap census @ HEAD `80598f4f` (12 lane-sweeps → adversarial verify → critic → synth). Treat as historical input, not a live queue: HEAD moved during that run and many rows have since landed. **Current re-check at `d8a2e556`:** `npm --prefix tauri/ui test -- tests/mock-transfer-contract.spec.ts` is GREEN (18 passed), so the old `ipc.library.import` red item is resolved. Still-useful claims must be re-grepped by symbol, not line. DROP-call speech remains OPEN_BUT_HOLD (needs grounding-review + on-beat proof, dormant behind `VIBEMIX_DROP_CALL`).
- **★★★★ PRIOR DRIFT BOARD → [`CODEX_CONTEXT-current-head-drift-and-next-packages.md`](../2026-06-02/CODEX_CONTEXT-current-head-drift-and-next-packages.md)** — fresh drift audit @ HEAD `75a11735`, evidence-backed (test-passes + NOT-FOUND greps shown), index [`INDEX.md`](../2026-06-02/INDEX.md). Headlines: 🔴 **HOLD** — your in-flight DROP work (`drop_predict.py`/`event_detector.py`/`coach.py` DIRTY) wires a predicted-drop line into the spoken ear via `mastered_speak`, **dormant behind `VIBEMIX_DROP_CALL` (default OFF, verified)** — keep building, just don't flip the flag for a real listener before `vibemix-grounding-review` + on-beat/false-positive proof. **ALREADY_LANDED, do NOT rebuild:** X1 speak-gate (`e5d9c434`), X3 AIM (`b0df605e`), L2 bass (`cc1af8f4`), EQ keystone (`8c629905`/`bac2046e`/`68c62e28`), FLX4 truth surface (`c3b8928a`), MOSS-only voice (CLEAN, 44 tests), folder/setup + `reindex_folder` (CLEAN, 40 tests). **STALE:** its L1 beatmatch "zero live callers" claim is now false at current source; see the L1 row below for focused proof. Top-5 next + the cue-practice sibling orphan in §4 of the packet.
- **★★★ PRIOR PACK → [`CODEX-CONTEXT-EVIDENCE-PACK.md`](../2026-06-02/CODEX-CONTEXT-EVIDENCE-PACK.md)** — Kaan's note: *"i am doing insanely bad nowadays so these are just for CONTEXT, not orders — build from what's solid, ignore the rest."* ⚠️ Citation-lint was OFF this session → the "75% describe-bank / keystone 2-14 / harmonic=1" live numbers are UNVERIFIED. The drift packet above supersedes this pack's "recommended next" (state-license + move-delta are now BOTH shipped). All line-anchors drift — grep by `def` name.
- **⚠️ STALE-SKIP (Codex verified clean 2026-06-02 — do NOT route, do NOT fake-commit):** the `[tend:]` dead-citation-source removal (grammar+registry already 11 sources, no `tend`) and the `folder_ingest` folder-cache docstring (S11) are **already correct in current source**. Codex is right to skip them. Same for most of the dead-cloud-TTS family (S1): router aliases retired, direct+proxy both build local MOSS, Cartesia ignored by the runtime chain — verify-and-skip the residue, only cut what still *pretends* to be product voice.
- **This is the terse routing surface.** Rationale + full evidence per item: [`CLAUDE_META_AUDIT_NEXT_BOARD.md`](CLAUDE_META_AUDIT_NEXT_BOARD.md) (board rows referenced as `B#`). Decisions: [`CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md`](CLAUDE_OPEN_QUESTION_DECISION_LEDGER.md) (`Q#`). Full map: [`CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md`](CLAUDE_EVERYTHING_CAPABILITY_INVENTORY.md).
- **Latest verifier packet:** [`CODEX_VERIFICATION-769e32a3-library-freshness.md`](CODEX_VERIFICATION-769e32a3-library-freshness.md) — `769e32a3`+`32873bcb` ACCEPTED (SRC); spawned S9/S10/S11 + H8 + R6 below.
- **Proof tier** = where each item is stuck: `SRC`=source+unit-test ✓ · `PKG`=in the signed build · `LIVE`=real-rig FLX4/Rekordbox capture. (Read-only: all current "works" is `SRC` only — app never launched.)
- **Codex A routes** each item to a lane. Claude does NOT implement — these are packets, not orders.
- **⏹ B & C STOOD DOWN (Kaan, 2026-06-01).** Only **Codex A** is building now. Claude stays read-only verifier. A owns the whole tree.
- **★★★ TOP BUILD — the narrator→coach unlock (NEW DIRECTION, 2026-06-01).** Root cause of "it just narrates" = the DJ intelligence is built/tested but ORPHANED from the live brain (`GOLD-WIRING-MAP.md`: ~70% is wiring, ~30% build). The keystone **`intel/eq_move_model.py` LANDED + WIRED** (`970e165b` → guard `deck_context.py:2519`) — narrator→coach for the EQ/filter axis is real in source. Claude owes the LIVE verify.

---

## 🔁 RE-PIN + WHAT CODEX LANDED (2026-06-02, HEAD `e49b2f63`)

> The tree moved `769e32a3 → e49b2f63` (~80 commits). Codex A crushed the backlog. **Re-verified at `e49b2f63`** — these are CLOSED, do not re-route:
> - **EQ keystone** `eq_move_model.py` built+wired (`970e165b`, guard `:2519`). · **Grounding siblings** `lufs.py` (`1e67ae61`), `loop_geometry.py` (`6117dffd`), **xfade move-effects licensed** (`7a9f2c45`) — GOLD Cards 2/3 + LAND H2 DONE. · **Universal Ingest** Traktor + Engine DJ + VirtualDJ readers landed (`sources/{traktor,engine,virtualdj}.py`). · **Spoken next-track recommendation** landed + cited (`runtime/suggestion_voice.py`, `coach.py:708`, `[track:]`/`[mix:]`, abstains). · **X1 voice-picker FUNCTIONALLY FIXED** — `voice_presets.py` ships real MOSS names (`MOSS_UI_VOICE_OPTIONS` Adam..Xiaoyu, mirrored in `SettingsDrawer.ts:547`), `LEGACY_CLOUD_TTS_VOICES` maps the 8 old Gemini names, `normalize_stored_voice` + `select_moss_voice_row` fallback → no more "8 picks → one wrong voice". *Residual nicety only:* `normalize_stored_voice` not called at the config read-site (legacy stored value falls to default gracefully, doesn't crash) → **VERIFY-and-close, not a build.** · **Heartbeat stays silent without a grounded read** (`953449bd`) — H3 done. · Deck-capture honesty fixes (`4f31b567`/`21af59d5`/`a836ae73`).

## ★ NEW PACKETS (2026-06-02) — the post-blitz open gaps, grounded at `e49b2f63`

> Written this session after the FEATURE-STATE / CRITIQUE / LICENSE / WIRING / FUTURE / MONEY workflow (`MASTER-WHERE-WE-WIN-2026-06-01.md`). Each re-verified against current HEAD (the synthesis docs' anchors were already going stale).

| Pkt | Packet | What's left (gap re-verified) | Gate | Effort | Priority |
|---|---|---|---|---|---|
| **MOSS** | [`CODEX_READY-B1-moss-voice-default-host.md`](CODEX_READY-B1-moss-voice-default-host.md) | Download infra DONE; missing **default URL/SHA/SIZE pin + first-run auto-download wiring**. ⚠️ **Kaan/Momo ops action blocks it: host the 728MB archive → URL+SHA+size.** | LIVE clean-cache speak | small code + ops | **★★★ #1 ship-gate** — fresh machine MUTE until this |
| **W1b** | [`CODEX_READY-W1b-transition-scorer-coach-line.md`](CODEX_READY-W1b-transition-scorer-coach-line.md) | next-track half LANDED; wire the **10-dim `score_transition_slate`** into coach (still zero coach callers). Insertion point ready at `coach.py:698-716`. | `vibemix-grounding-review` | ~0.5–1d | ★★★ sharpest wiring |
| ~~B1b~~ | [`CODEX_READY-B1b-serato-library-reader.md`](CODEX_READY-B1b-serato-library-reader.md) | **STALE/CLOSED at `36225bae`: do NOT route.** Serato reader is already in current source (`library/sources/serato.py`), with setup discovery, CLI `--source serato`, `_Serato_/database V2` path detection, and Markers2 cue reads via the owned `export_serato.read_serato_cues`. Focused proof: `PYTHONPATH=src uv run pytest -q tests/library/test_sources_serato.py tests/library/test_ingest_cli_anlz.py::test_library_ingest_cli_can_select_serato_source tests/library/test_setup_discovery.py::test_setup_discovery_finds_standard_serato_database` -> **8 passed**; Ruff on the Serato slice passed; package checker green. Remaining truth: real Serato-library import is a LIVE/user-file proof, not a missing source island. | — | — | ✅ closed |
| ~~B2~~ | ~~`CODEX_READY-B2-practice-deck-beatmatch-producer.md`~~ → **SUPERSEDED by L1.** STALE: claims "`BEATMATCH_GRADED` emitter NOT-FOUND" — false at HEAD `2b1e8a5a` (the `practice_loop.py` producer MODULE landed). The real gap is the missing live CALL-SITE; see L1. | — | — | ⛔ do not route |
| **W2** | [`CODEX_READY-W2-prep-live-set-awareness.md`](CODEX_READY-W2-prep-live-set-awareness.md) | `built_set`/`set_plan` absent from state/coach — prep→live amnesia. Thread Viber set JSON → `refresh.py` computes `set_progress` → cited coach line. | `vibemix-grounding-review` | ~1d | ★★ "ahead of your curve" |

**Suggested order:** MOSS (unblock ship, parallel with the ops hosting) → W1b (collision-light but speech-gated) → W2. **B1b/Serato, L1/beatmatch-practice, and L2/bass-credit are closed at current source; do not rebuild them.** After this drop, the remaining depth lane = house/DnB genre detectors (MONEY Phase 2 — *most of the paying market abstains into silence without it*) + the debrief Receipt-PNG viral graft (B5).

## ★ LEARN-ARMY PACKETS (2026-06-02) — grounded at `2b1e8a5a` (supersede stale beatmatch rows below)

> From the Learn exploration army (16 explorers + 4 cluster synths; `LEARN-*.md` + `LEARN-ARMY-HEAD-VERIFY.md`). Historical context only: current source has since closed L1 and L2, so re-verify every row before routing.

| Pkt | Packet | What's left (HEAD-verified gap) | Gate | Effort | Priority |
|---|---|---|---|---|---|
| ~~L1~~ | [`CODEX_READY-L1-beatmatch-practice-lesson-runtime.md`](../2026-06-02/CODEX_READY-L1-beatmatch-practice-lesson-runtime.md) | **STALE/CLOSED at `d1cf6356`: do NOT route.** Current source constructs `BeatmatchPracticeDriver()` in `__main__.py`, passes `snapshot` + `record_action` into `LessonRuntime`, records matched beatmatch actions, grades via `_grade_beatmatch_practice_tick()`, writes `BEATMATCH_GRADED`, credits `beatmatching`, and logs `learn_beatmatch_practice_graded`. Focused proof: beatmatch driver + runtime evidence + live reality pins -> **9 passed**; Ruff on the Learn slice passed; package checker green. Remaining truth: physical Learn exercise proof is LIVE, not a missing source caller. | — | — | ✅ closed |
| ~~L2~~ | [`CODEX_READY-L2-credit-measured-bass-collision.md`](../2026-06-02/CODEX_READY-L2-credit-measured-bass-collision.md) | **STALE/CLOSED at `34c041ec`: do NOT route.** `skill_recognizer.py` already credits `transitions` from a cited positive `transition_judged.extra.components["bass_collision"]`, rejects absent/zero/NaN/uncited values, keeps harmonic and bass-collision credit distinct, and explicitly refuses to proxy beatmatching from bass collision. Focused proof: `PYTHONPATH=src uv run pytest -q tests/learn/test_judge_credits_harmonic.py` -> **8 passed**; Ruff on the recognizer slice passed; package checker green. | — | — | ✅ closed |
| **L3** | [`CODEX_READY-L3-mastered-moment-hotcue-writeback.md`](../2026-06-02/CODEX_READY-L3-mastered-moment-hotcue-writeback.md) | On the Mastered flip (`coach.py:198-207`) write a named, **additive** hot-cue into the user's track file (`export_serato.py` carrier). ⚠️ OPEN: resolve in-track POSITION source first (`t_session` is set-relative, NOT track position). | `vibemix-grounding-review` + library-file-mutation (additive only) | ~1–2d | ★★ B1+B3+B5 convergence (proof artifact) |

## ★★★ X-SERIES — the co-host VOICE redesign (2026-06-02, LIVE-driven: Kaan "bok gibi, anca tarif")

> LIVE root cause (session `20260602-075843`): 46 HEARTBEATs → 34 `citation_count=0`, 22 pure **describe-bank** ("a dry woodblock over a muffled kick"). NOT hallucination (grounds real audio) — it was a **pacing/value** failure: the old heartbeat cadence + `matrix.py:333` encouraged constant description. Current source has since hardened the cadence to `HEARTBEAT_SEC=180.0` and speak-gated it. Kaan's direction: voice **rare + earned** (kill describe-bank) + routine read → **visual pill** + coaching → **debrief**, and when it DOES speak, hand it a **rich 4-5min context bundle**. Two research workflows backed these (`speak-gate-research/`, `context-engineering-research/` — both read in full by Claude). **Key finding: the system is already ~80% of the rich bundle; the 4-5min must be TEXT not audio (5× cost + Gemini can't localize long-audio timestamps).**

| Pkt | Packet | What it does | Gate | Effort | Priority |
|---|---|---|---|---|---|
| **X1** | [`CODEX_READY-X1-live-speak-gate-kill-describe-bank.md`](../2026-06-02/CODEX_READY-X1-live-speak-gate-kill-describe-bank.md) | `should_speak()` gate UPSTREAM of the LLM call: SILENCE default, describe-bank → silent (`HEARTBEAT_SEC=180.0` + session cap 3), voice only on graded-coachable+novel+urgent. Fixes "anca tarif". | `vibemix-grounding-review` | ~2–3d | ★★★ #1 (the live complaint) |
| **X3** | [`CODEX_READY-X3-aim-wire-skilltree-to-live-coach.md`](../2026-06-02/CODEX_READY-X3-aim-wire-skilltree-to-live-coach.md) | Wire the **AIM** — skill-tree `competent`-not-`mastered` (ZPD) → live coach system-instruction (cached, coach-mode only). Orphaned gold; **closes the half-open learn loop**. Converts narration→coaching, ZERO new claims. | `vibemix-grounding-review` | ~1–2d | ★★★ highest-leverage, lowest-risk |
| **X4** | [`CODEX_READY-X4-sectioned-rich-context-bundle.md`](../2026-06-02/CODEX_READY-X4-sectioned-rich-context-bundle.md) | Re-layout flat `evidence_line` → named edge-weighted sections (AIM first/TASK last) + `set_window_context` long-window **TEXT** digest (4-5min as text, audio stays 60-90s). Master-only honest. Deep tier = new model_router key (same Flash, no literal). | `vibemix-grounding-review` | ~3–5d | ★★ depth, A/B-gated |
| **X2** | (half-spec, parked) route routine read → visual pill (`suggestion.py:233` SuggestionService) + coaching → debrief (`debrief/main.py:142` already harvests events.jsonl) | the "where the suppressed value goes" half of Kaan's option-2 | `vibemix-grounding-review` + `ipc-wiring-checker` | ~3-5d | 🅿️ after X1 |

**X-series order:** X1 (kill the slop) → X3 (the aim, small+high-leverage) → X4 (the sectioned depth) → X2 (pill/debrief routing). X1+X3 are the narration→coaching core; X4 makes the rare line deep; X2 moves the routine value off the voice.

---

## 🟢 SAFE_NOW — land anytime, no gate (source hygiene; blocks nothing)

| # | Item | Proof now | Evidence | Note |
|---|------|-----------|----------|------|
| ~~S1~~ | Delete dead cloud-TTS seams (`GeminiTTS` class + only-caller test, OpenRouter `AUDIO_STREAM_MODELS.add` import-patch, orphaned `CARTESIA_*`) | **STALE/CLOSED at `16971ecb`: do NOT route as a delete package.** Current runtime voice is structurally MOSS-only: `tts_chain.build_tts_chain()` accepts only `mode`, direct/proxy both call local MOSS, `proxy_client.build_proxy_tts_chain()` is a compatibility shim to local MOSS, and retired cloud TTS router aliases raise. Focused proof: voice-policy tests -> **57 passed, 8 deselected**; focused Ruff on TTS/router files passed. Remaining cloud-TTS names are guard tests, docs, OpenRouter brain transport, or cost-model what-if rows (`library/cost.py`/`pricing.py`) and must not be mistaken for product voice. | ✅ closed |
| ~~S2~~ | Add `NOTICE`/`THIRD_PARTY_LICENSES` for Mixxx clean-room ports | **STALE/CLOSED at `9ad0f11b`: do NOT route.** `NOTICE`, `NOTICE.md`, `THIRD_PARTY_LICENSES.md`, and `scripts/dist/gen_notice.py` are present in current source and document the clean-room Mixxx references without bundling Mixxx source/binaries. Focused proof: `tests/test_license.py`, `tests/repo/test_third_party_attribution.py`, `tests/repo/test_oss_hygiene.py::test_notice_passes_gen_notice_check`, and `tests/learn/test_beatmatch_judge.py` -> **14 passed**. | ✅ closed |
| ~~S3~~ | Delete `runtime/demo_mode.py` + test | **STALE/CLOSED at `9ad0f11b`: do NOT route.** `src/vibemix/runtime/demo_mode.py` and `tests/runtime/test_demo_mode_sequence.py` are absent, and `rg "demo_mode|DEMO_SEQUENCE" src/vibemix tests` returns no live residue. Focused runtime proof: `PYTHONPATH=src uv run pytest -q tests/runtime -k 'not demo_mode'` -> **436 passed**. | ✅ closed |
| ~~S4~~ | Fix 2 stale-golden tests (HEARTBEAT `test_coach.py:723`, `test_mastered_demo_reaches_the_envelope_wall`) | **STALE/CLOSED at `37dbe868`: do NOT route.** Current golden tests have moved/settled: `tests/runtime/test_coach.py`, `tests/learn/test_progress_snapshot_skill_wall.py`, and `tests/learn/test_mastered_vocal_fires_once.py` pass with hardened heartbeat/mastered behavior. Focused proof -> **33 passed**; Ruff on the same tests passed; package checker green. | ✅ closed |
| ~~S5~~ | Delete 5 dead `ipc.library.*` ws types + refresh stale Viber CORRECTION packet | **STALE/CLOSED at current source: do NOT delete.** `ipc.library.import`, `ipc.library.import_cancel`, and `ipc.library.staleness_action` are now live ws-bus paths, not vestigial Crate code: `which_handler` reports each as **WIRED (both ends)**, `check_ipc_wiring.py` reports **72/72 wired**, and `check_ipc_schema.py` reports **72 schema/dataclass parity**. Exact senders/handlers exist in `SettingsDrawer.ts`, `staleness-banner.ts`, `library-panel.ts`, `library/api.ts`, `mock-transfer/contract.ts`, `src/vibemix/__main__.py`, and `ui_bus/messages.py`. Remaining truth: Tauri `library_*` commands and ws `ipc.library.*` coexist for different surfaces; deleting the ws types would break Settings/Library import and staleness actions. | ✅ closed |
| ~~S6~~ | Fix 3 stale doc-anchors → non-existent `debrief/ln.py` (lives in `ear_test_capture.py`) | **STALE/CLOSED at current source: do NOT route as a code/docs fix.** The supposed live anchors are gone from tracked product/docs: `git grep "debrief/ln.py\|ln.py"` outside the route/inventory packets returns no production docs, `src/vibemix/debrief/ln.py` is absent, and the obsolete Python `src/vibemix/debrief/ear_test_capture.py` is absent too. The live ear-test writer is Rust (`tauri/src-tauri/src/ear_test_cmds.rs`, registered from `tauri/src-tauri/src/main.rs`) and `tauri/ui/debrief.html` names the Rust `write_ear_test_log` command directly. Focused proof: `tests/debrief` -> **96 passed**. Remaining references are historical audit/packet statements, not source-of-truth anchors to edit. | ✅ closed |
| ~~S7~~ | Rename `state/coach.py` → `ai_coach.py`/`prompt_builder.py` (un-shadow `runtime/coach.py`) | **STALE/CLOSED at current source: do NOT rename.** `src/vibemix/state/coach.py` is no longer tracked or present; `git grep` finds no `vibemix.state.coach` import residue in `src/` or `tests/`; the only tracked coach module is `src/vibemix/runtime/coach.py`. A rename package here would be pure churn in a dirty shared tree. | ✅ closed |
| ~~S8~~ | Surface auto-cue / smart-hot-cue export as a first-class Crate action | **STALE/CLOSED at current source: do NOT rebuild.** The Crate/Library GUI has a first-class Cue tab (`tauri/ui/library.html`), `runCueExport()` calls `libraryCueFolder()`, `library/api.ts` normalizes the receipt, `tauri/src-tauri/src/main.rs` registers `library_cmds::library_cue_folder`, and the Rust command runs the Python cue-folder/export pipeline. Focused proof: `npm --prefix tauri/ui test -- tests/mock-transfer-contract.spec.ts` -> **18 passed**; `tests/library/test_cue_folder.py`, `test_export_rekordbox.py`, and `test_export_serato.py` -> **44 passed**. Remaining truth: real Rekordbox/Serato eye-check is LIVE, not a missing GUI route. | ✅ closed |
| ~~S9~~ | Quarantine the poisoned primary on fixture-reject (rename/clear `library.pkl`) instead of only declining it | **STALE/CLOSED at current source: do NOT rebuild.** `src/vibemix/library/rekordbox.py` now detects user-cache blobs whose recorded source points inside this repo's `tests/fixtures`, moves the hot cache to `library.pkl.fixturebak`, and refuses to answer from it. `RekordboxSource.iter_tracks()` then recovers by parsing the requested real XML and rewrites a clean cache. It also avoids the false positive for a real user folder named `tests/fixtures`. Focused proof: `test_user_cache_ignores_repo_fixture_source`, `test_user_cache_accepts_relative_non_repo_tests_fixtures_path`, `test_rekordbox_source_iter_tracks_recovers_from_poisoned_user_cache`, and `test_rekordbox_source_accepts_relative_user_tests_fixtures_cache` -> **4 passed**; Ruff on `rekordbox.py` + those tests passed. | ✅ closed |
| ~~S10~~ | Close PKG tier on fixture-reject: prove no `tests/fixtures` ship in the build | **CLOSED at current local package artifact.** The unpacked app bundle under `tauri/src-tauri/target/release/bundle/macos/vibemix.app` contains no `tests/fixtures`, `tests/library/fixtures`, `synthetic_collection.xml`, `clap_real_corpus`, or `cue_real_corpus` paths. The local DMG `tauri/src-tauri/target/release/bundle/dmg/vibemix_0.0.1_aarch64.dmg` was mounted read-only and the same fixture scan returned no paths; `rg "tests/library/fixtures|synthetic_collection|tests/fixtures"` inside the mounted DMG returned **0 hits**. Caveat: this proves the current local package artifact, not a future rebuilt/signed release. | ✅ closed |
| ~~S11~~ | Fix `folder_ingest._write_library_cache` docstring (`folder:<root>` marker → it writes a plain resolved path) | **STALE/CLOSED at current source: do NOT edit.** `src/vibemix/library/folder_ingest.py` already documents the correct split: `_CacheBlob.xml_path` is the resolved folder path and `folder:<sha1[:16]>` is only the `TrackEntry.track_id` namespace. No live `src/` or `tests/` file contains the stale `folder:<root>` marker. Focused proof: `test_track_id_stable_and_title_from_stem` + `test_library_pkl_written_and_loadable` -> **2 passed**; Ruff on `folder_ingest.py` + `test_folder_ingest.py` passed. | ✅ closed |

Also LAND-able now (cleanup, B§Architecture): delete dead `debrief/ear_test_capture.py::write_ear_test_log` duplicate (Rust-only live path) · strip `audio_decode.py` transformers dead branch · fix stale CLAUDE.md `tts_chain.py` "TWO builders" note (all return `_build_moss_chain()`).

---

## 🟡 HOLD_WITH_GATE — do not land without the named gate

| # | Item | Stuck at | Gate to clear | Evidence |
|---|------|----------|---------------|----------|
| H1 | Loop/beatjump grounding rule (`state/loop_geometry.py` + beatgrid-exact atom) | SRC build | route loop events past the 400ms dedup (a 1/16 roll re-triggers ~58-117ms, INSIDE the window) OR make dedup label-unique-per-beat-size; unit-test a roll-stack survives to the registry | `loop_geometry.py`=NOT-FOUND; dedup `midi/state.py:226`; capture `:395-400` |
| H2 | Wire existing `xfade.py` into the live R-SLOP guard (Move-Effect Resolver) | SRC | predict-on-lane/measure-on-lane only (never master-sum); keep `dsp_delta_not_causal_proof` humility | `audio/xfade.py` (zero import in `deck_context.py`); guard `deck_context.py:2236` |
| H3 | Audit HEARTBEAT idle speech for grounding | SRC (review) | `vibemix-grounding-review` on the HEARTBEAT branch: idle lines ack-bank/persona-only, cannot fire during BlackHole phantom-music | current source: `audio/constants.py` `HEARTBEAT_SEC=180.0`, speak-gated |
| H4 | Verify Crate actions both-ends-wired before "Crate is fully wired" | SRC | `ipc-wiring-checker` on each (search/similar/curate/build-set/cue-folder/budget): sender + handler both exist | built-not-surfaced lens cites only FE `library/index.ts` |
| H5 | Telegram "connect bot" affordance in Settings | SRC | product call: Telegram a v1 feature? If yes add token + chat-id pill, never auto-enable | `library/telegram_bridge.py`; `rg telegram tauri/ui/src`=no GUI |
| H6 | Privacy-default audit of all local-persistence paths vs opt-in posture | RESEARCH (Kaan-gate) | enumerate every persistence path + default flag vs CLAUDE.md opt-in — Kaan decision-gate | `VIBEMIX_RECALL_ENABLED` default-off; memory `project_observability_milestone_direction` |
| H7 | Don't ship `library budget` "< €50 / free at scale" claim | SRC (XFAIL) | re-baseline cost model (MOSS now local-free TTS), flip `test_monthly_projection_under_50_eur` XFAIL→pass; `budget` internal-only until then | inventory `:222,:416` |
| H8 | **`folder-reindex-from-nudge`** — folder DJs get a correct stale nudge but only "Drop the Rekordbox XML" (no re-index button). Add a "Re-index folder" action routing the recorded folder path to `ingest_folder`. **Blocks: release-only.** | SRC | `vibemix-grounding-review` (changes UI library-state copy + triggers re-embed feeding EvidenceRegistry) + `ipc-wiring-checker` + `npm run codegen:ipc`. **Trap: do NOT reuse `ipc.library.import` — `load_xml` raises on a dir.** Files smallest-first: `staleness.py:67-75` (accept `is_dir`), payload+schema `source_kind`, `staleness-banner.ts:71-110`, `__main__.py:2057-2064` (`reindex`→`ingest_folder`, recorded path NOT renderer input). | Verif packet ★NEXT LAND PACKAGE |

---

## 🔴 BLOCKERS — release-gating (separate proof tiers; **blocks: see tag**)

> These are the honest-ship gates. None block dev lanes A/B/C continuing; they block **release**. Sequenced, not parallel: #B1 in source → fresh rebuild (#B4) → live capture (#B3) against the *rebuilt* binary.

| # | Blocker | Stuck at | Blocks | Gate | Evidence |
|---|---------|----------|--------|------|----------|
| B-1 | **MOSS weights not bundled/hosted** — fresh machine boots voice-mute | PKG | **release-only** (co-host can't speak on any machine but Kaan's) | bundle `moss-tts-onnx` into spec `datas` OR host archive + pin `VIBEMIX_MOSS_TTS_ARCHIVE_URL`/SHA/size | inventory `:262` (no moss in `vibemix-core.macos.spec` datas); `model_assets.py:571,576` |
| B-2 | First-run "preparing voice / unavailable: [reason]" surface absent | PKG→LIVE | **release-only** | the #B1 fix MUST surface a first-run readiness state, `drive-vibemix`-captured when model absent | `tts_chain.py` `LocalTTSUnavailable`→`NOT_GIVEN`; Settings button `library/index.ts:2006` WIRED_BUT_UNPROVEN |
| B-3 | **Proxy BRAIN passthrough not confirmed live** | LIVE (infra) | **release-only** (independent of B-1) | confirm proxy brain round-trip with a working upstream key AND MOSS reachable — two separate checks | `proxy_client.py:49-53`; memory `project_vibemix_proxy_deployed_altidus` |
| B-4 | **Signed DMG 18+ commits stale + arm64-only** | PKG | **release-only** | rebuild+sign+notarize from clean HEAD after B-1; CI x86_64 leg for Intel OR scope v1 Apple-Silicon in launch copy | inventory `:261` (18-commit gap), `:259` (notarytool arm64 ×168, zero x86_64) |
| B-5 | **No live-rig capture of the perception loop** | LIVE-FLX4 | **release-only** (validates cohost lane A) | `drive-vibemix` capture: real LAYER_ARRIVAL/MIX_MOVE/TRACK_CHANGE → EvidenceRegistry citation resolved → grounded reaction; re-run vs the rebuilt signed binary | inventory `:139,:47,:209`; ledger Q11 |
| B-6 | **FLX4 deck-pair identity unresolved** (latest acceptance FAILED honestly) | LIVE-FLX4 | **release-only** + **lane A** (per-deck grounding) | real two-deck audio (not A_active+B_silent) + resolved deck_state row + direct OS MIDI motion in-window | `CODEX_HOLD-flx4-live-acceptance-current.md`; `deck_poller.py:315-421`; ledger Q3 |

---

## ⚫ DROP / DEFER — cut from coherent v1 + all launch copy

| Item | Action | Evidence |
|------|--------|----------|
| Mascot 3D VTuber surface (53 FE modules + Rust window + CI) | DEFER-v2; FREEZE (no new wiring pre-launch) — already opt-in, Pill+speech delivers the value | `main.rs:186-218` built only if `primary_surface=mascot` |
| Screen vision as a LIVE source | DROP live leg; quarantine `screen_task` behind off-by-default `VIBEMIX_DECK_VISION` (don't touch the `screen_jpeg=None` invariant) | `dj_cohost.py:2192-2199` hardcoded `None`; ledger Q5 |
| Mixxx H1-H8 / OSC / `mixxxdb` writer | DROP — every producer CLAIMED_BUT_ABSENT; stock Mixxx has no OSC read. Keep only file-format SINKS (M3U8, Serato carrier) | `mixxx_osc.py`/`export_mixxxdb.py`/`detect_key.py`/`beatgrid_detect.py`=NOT-FOUND; `CODEX_GOLD_STRUCTURE.md:143` |
| ~~Beatmatch "Mastered" live credit — KEEP MASKED~~ | **STALE/REVERSED at `2b1e8a5a`** — cap `_HONEST_UNCREDITABLE_V11 = ()` is now EMPTY, `beatmatching` is gated by per-skill `live_creditable` + the missing producer call-site, NOT a deny-list. Route via **L1** (wire the landed producer), do not "keep masked". | `skill_recognizer.py:104` (`()`); `skill_tree.py:143` |
| DROP-call live hype line | KEEP OUT of any signed build (dirty/uncommitted); never whole-file `git add coach.py`/`__main__.py` | ledger Q10; WIRED_BUT_UNPROVEN |
| Windows deck-pair grounding + live Judge | Scope v1 platform honestly — `_audio_windows.py` is master-loopback only, no per-deck capture | CLAUDE.md (Windows = v1) — resolve before a Windows release claim |
| Crossing-detector / 14-bit "crossed the host's live value" | CUT the precision claim — un-computable for a passive listener; confident-but-blind slop | goldmine #3 |

---

## 🔵 RESEARCH_ONLY — needs a spike/workflow before any LAND

| # | Item | Workflow to run | Evidence |
|---|------|-----------------|----------|
| ~~R1~~ | **FOLDED INTO L1** — `learn/practice_loop.py` (producer + grader + recognizer branch) all landed since this row was written; only the practice-runtime CALL-SITE remains. The build is now scoped + grounded in **L1**, not a research spike. | superseded — see L1 |
| R2 | Carve `library/cli.py` + `library/service.py` out of 7,183-line `__main__.py` | isolated-branch Workflow (high collision risk on `__main__.py`); one service entry for CLI+IPC; resolves GUI-importer divergence | `__main__.py` 7,183 lines; CLI `:2760,3213`, IPC `:2066` |
| R3 | BlackHole/Rekordbox setup UX + "routing-wrong" detector | spike: routing-wrong detector thresholds (phantom-music/feedback signature) | ledger Q4 |
| R4 | Mixxx goldmine adopt: LUFS receipt → spectral EQ-move receipt → loop-geometry | adopt `audio/lufs.py` LUFS receipt (A) → research `intel/eq_move_model.py` (B) → loop-geometry spike (C) | ledger Q8 |
| R5 | `library budget` cost-model rework | re-baseline now MOSS = local-free TTS; flip XFAIL → passing gate before any cost copy | ledger/inventory `:222` |
| R6 | Coverage test for the 2nd cache-load entry `RekordboxSource.iter_tracks` (the curate/build-set read path) | add a test that a fixture-poisoned primary → empty `xml_path` → fresh XML parse (safe today, never traced); also the relative-path false-positive (`tests`+`fixtures` substring) rejecting a legit user lib with only a stderr warning | `sources/rekordbox.py:120`; `rekordbox.py:94-97`; Verif packet Secondary MED/LOW |

---

**KAAN-VERIFY (LIVE):** dev cache is self-healing via a 10-day-old `.v1bak` (1547 real tracks). Run a fresh `uv run python -m vibemix library ingest` (or `embed-folder ~/Music`) so LIVE proofs run against a *current* real library AND the poisoned primary gets overwritten by a clean v2 cache. See verifier packet ★ LIVE finding.

*Re-pin HEAD and re-verify the SRC anchors before routing — this queue was verified at `769e32a3` on a moving tree. Paste a commit SHA and I'll write its verification packet; ask a keep/drop/change question and I'll append a ledger decision.*
