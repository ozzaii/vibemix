## 2026-06-07 In-App UX Teardown

- Closed: commit `91090cd3` suppresses the folded `vmx-statusrow` only under the shell Deck surface.
  Proof: `npm --prefix tauri/ui run build` passed. Full `npm --prefix tauri/ui test` was red only on the accepted beginner Learn contract. Current-source browser proof showed `.surface--deck .vmx-statusrow` at `display: none`.

- Closed: commit `91b5a2ff` makes Viber/library a chat-first surface by removing the top-level Build/Music tabs, adding a folder action, and keeping the scroll pane usable.
  Proof: `npm --prefix tauri/ui run build` passed. Full `npm --prefix tauri/ui test` was red only on the accepted beginner Learn contract. Current-source browser proof showed `body[data-mode=chat]`, a single Viber tab, the folder button, and no Build/Music controls.

- Closed in this change: F10 surfaces first-run/local embed progress in the launch card and adds an ambient shell footer indexing state from live import progress.
  Proof: `npm --prefix tauri/ui test -- tests/shell/library-freshness-badge.spec.ts src/wizard/__tests__/library-feed-embed-progress.spec.ts` passed, 9 tests. `npm --prefix tauri/ui test -- src/wizard/__tests__/first-run-continuity.spec.ts` passed, 10 tests. `npm --prefix tauri/ui run build` passed. Full `npm --prefix tauri/ui test` was red only on `tests/learn/test_beginner_path_contract.spec.ts` (`open_learn_window` invoke count 0), with 163 files passed and 1577 tests passed.
  Live proof: launched current source with `VIBEMIX_DEV_SIDECAR=1 cargo tauri dev`; the active sidecar on `127.0.0.1:8765` was `.venv/bin/python3 vibemix` with `VIBEMIX_DEV_SIDECAR=1`. Triggering `ipc.library.import` for `tests/bench/data` produced real `ipc.library.import_progress` frames (`done=3 total=6` observed) and the shell footer visibly changed to `indexing 2/6`, then reverted to `library fresh`. Screenshots: `/tmp/vibemix-f10-settings-visible.png`, `/tmp/vibemix-f10-footer-reverted.png`.
  Current-source launch-card proof: Vite render at `http://localhost:1420/?step=library-feed` showed the Privacy copy `Embedding locally on your device - your library never leaves this machine.`, two `2/6 t2_mix_anchor.mp3` progress labels, `--feed-progress: 33.33333333333333%`, and the `Choose a folder` action. Screenshot: `/tmp/vibemix-f10-library-feed-current-source-bright.png`.
  Flagged proof gap: the macOS native folder sheet itself was not visually exercised because the Settings `RE-RUN WIZARD` path did not foreground the wizard during this proof run. I did not flip the user's local first-run app config to force the native wizard. The folder action is covered by the focused render/callback test and visible in current source; native dialog selection should be spot-checked once a safe wizard-launch verifier is available.

- Closed in this change: F7 adds a Browse pill to the library Music ingest folder field and wires it to the existing native directory picker path.
  Proof: `npm --prefix tauri/ui test -- src/library/chat.test.ts` passed, 39 tests. `cd tauri/ui && npm run build && npm test` passed build and was red only on `tests/learn/test_beginner_path_contract.spec.ts` (`open_learn_window` invoke count 0), with 163 files passed and 1578 tests passed. Current-source Vite proof showed `body[data-mode=ingest]`, visible `Browse`, `~/Music` in the field, and `Index folder`; after setting the field to `/Users/ozai/projects/dj-set-ai/tests/bench/data`, the UI entered `Cancel import` with progress `4 / 8 - Safeword [BABYNYMPH Remix]`. Screenshots: `/tmp/vibemix-f7-browse-visible-current-source.png`, `/tmp/vibemix-f7-indexing-current-source.png`.
  Live source note: `VIBEMIX_DEV_SIDECAR=1 cargo tauri dev` launched and the active sidecar on `127.0.0.1:8765` was `.venv/bin/python3 vibemix` with `VIBEMIX_DEV_SIDECAR=1`.
  Flagged proof gap: the Tauri shell process did not expose a visible/accessibility window in this run and `screencapture` returned a black frame, so the native macOS folder sheet was not visually exercised. The click-to-picker route is covered by the focused test through the real dynamic `@tauri-apps/plugin-dialog` mock; a future verifier with a visible Tauri window should spot-check the actual OS sheet.

- Closed in this change: F8 confirms the wizard launch candidate by rendering music-folder track count/confidence or catalog reason text, plus a Recheck action.
  Proof: `npm --prefix tauri/ui test -- src/wizard/__tests__/first-run-continuity.spec.ts` passed, 11 tests. `cd tauri/ui && npm run build && npm test` passed build and was red only on `tests/learn/test_beginner_path_contract.spec.ts` (`open_learn_window` invoke count 0), with 163 files passed and 1579 tests passed. Current-source Vite proof rendered the launch card with `/Users/ozai/Music/crates`, `47 tracks found - high confidence`, and visible `Recheck` without clipping. Screenshot: `/tmp/vibemix-f8-library-feed-candidate-detail-bright.png`.
  Flagged proof gap: I used the Vite current-source wizard render rather than a visible `cargo tauri dev` window because the Tauri dev window remained inaccessible/black-frame in this environment after the F7 verifier. The DOM and visual proof cover the shipped TypeScript/CSS path; a future visible Tauri run should spot-check the same launch card with a real `setup_discovery` candidate.

- Closed in this change: F6 makes the wizard output-device dropdown escape the primary-panel overflow clip and adds a screen-recording restart-to-apply affordance after Settings is opened.
  Proof: `npm --prefix tauri/ui test -- tests/wizard/dropdown-device-fixed.spec.ts tests/wizard/perm-card-restart-affordance.spec.ts` passed, 7 tests. `npm --prefix tauri/ui test -- tests/wizard/test_step2_headphone_picker.spec.ts` passed, 3 tests. `cd tauri/ui && npm run build && npm test` passed build and was red only on `tests/learn/test_beginner_path_contract.spec.ts` (`open_learn_window` invoke count 0), with 165 files passed and 1586 tests passed.
  Current-source proof: Vite render at `http://127.0.0.1:1420/?step=audio` showed the dropdown menu open under the trigger without clipping; DOM metrics reported `parentIsBody=true`, `position=fixed`, `zIndex=1000`, `left=382`, `top=298.1875`, `width=263.421875`, and 7 options. Screenshot: `/tmp/vibemix-f6-dropdown-fixed-current-source.png`. Vite render at `?step=permissions` showed Screen Recording denied with `GRANTED? RESTART TO APPLY` routed as a button and Microphone still `GRANTED`. Screenshot: `/tmp/vibemix-f6-perm-restart-current-source.png`.
  Implementation note: the open dropdown panel is temporarily mounted under `document.body` before writing fixed left/top/width; this avoids the wizard step transition transform becoming the fixed-position containing block while still restoring the panel to the component root on close.
  Flagged proof gap: I did not exercise a visible `cargo tauri dev` macOS TCC flip in this environment because previous Tauri verifier runs exposed an inaccessible/black-frame window. The `restart_sidecar` wiring is covered by DOM/click-route tests and current-source Vite proof; a future visible native run should spot-check the real Screen Recording grant -> focus -> sidecar restart -> granted transition.

- Closed in this change: F3 removes the Library/Viber VIBE SCOPE radar end-to-end while keeping the `#vmx-lib-scope-state` status badge and chat-side receipt nodes in the DOM.
  Proof: `npm --prefix tauri/ui test -- src/library/curate.test.ts src/library/build.test.ts src/library/chat.test.ts tests/mock-transfer-contract.spec.ts` passed, 94 tests. `cd tauri/ui && npm run build && npm test` passed build and was red only on `tests/learn/test_beginner_path_contract.spec.ts` (`open_learn_window` invoke count 0), with 164 files passed and 1575 tests passed.
  Current-source proof: Vite `library.html` chat render showed `bodyMode=chat`, only the `Viber` tab, `scopeWrapExists=false`, `scopeSvgExists=false`, `contractedScopeWires=0`, `groundingDisplay=none`, and no scope-related console errors. Screenshot: `/tmp/vibemix-f3-library-chat-no-scope.png`. The built-in Add Music jump to ingest showed a two-column grid `326px 1068px`, `groundingDisplay=none`, `scopeWrapExists=false`, `scopeSvgExists=false`, `contractedScopeWires=0`, and `scopeStateText=ready`. Screenshot: `/tmp/vibemix-f3-library-ingest-no-scope.png`.
  Note: search/similar/curate/build mode machinery remains private to the current Viber-first surface, so the live proof used the exposed chat and Add Music routes plus source/contract proof for the removed radar ids and wires.
