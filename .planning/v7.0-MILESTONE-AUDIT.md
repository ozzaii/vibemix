---
milestone: v7.0
milestone_name: Open House
audited: 2026-05-24T00:00:00Z
status: tech_debt
scores:
  requirements: 19/19
  phases: 4/4
  integration: 6/6
  flows: 5/5
nyquist:
  compliant_phases: 0
  partial_phases: 0
  missing_phases: 4
  overall: NOT_APPLICABLE
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 67-all-tests-pass
    items:
      - "KAAN-ACTION §V7-LIVE-01..05 — TEST-02 11 Tier-B opt-in tests on real hardware (BlackHole-hosted-mac · Win 11 desktop SKU · FLX4 USB · live full-stack) + TEST-04 first-CI-green observation on the next push to GitHub. Engineering side GREEN (full-test-matrix.yml ships 21-job grid, SHA-pinned, fork-PR-safe; static skip/flake gates land). Live confirmation rides Kaan's clock. §V7-LIVE-06 v7.0 flake-hunt baseline DISCHARGED 2026-05-23 (10/10 GREEN @ SHA 23c4203)."
  - phase: 68-all-devices-ready
    items:
      - "KAAN-ACTION §V7-LIVE-07..10 — DEV-03 live FLX4 plug/unplug ear-pass + DEV-04 real BlackHole 2ch/16ch + real WASAPI loopback capture + DEV-05 contributor-recipe <30-min smoke. Engineering side GREEN (10-row contract + 10-row synthetic-MIDI smoke + 3-profile hot-plug matrix + 5-fixture audio backend matrix, all CI-mocked; catalog reconciled to single `profiles/` source of truth; contributor recipe + discover_midi_port.py + _template.json shipped). Live hardware confirmation rides Kaan's clock."
  - phase: 69-oss-fully-integrated
    items:
      - "KAAN-ACTION §V7-PROXY — OSS-02 server-side Bravoh proxy hardening (per-install-UUID rate limit + token-bucket + Prom /metrics + Sentry DSN) lives in the separate api.altidus.world ops repo, not vibemix. Client-side fallback (`Co-host unavailable this session` pill/mascot, no crash, no hallucinated coach line, /health canary offloaded off the reaction path) is GREEN engineering-side (31 integration tests; cardinal invariants held). Server-side coordination rides Kaan's Bravoh-ops clock."
      - "KAAN-ACTION §V7-LIVE-11 — OSS-03 BYO fresh-account walk. docs/byo-key.md + test_byo_doc_shape.py GREEN (10 tests; documents the live VIBEMIX_LLM_MODE=direct + GEMINI_API_KEY path the code already honours at session_loop.py:842). The fresh-account walkthrough recording (docs/byo-key-walk.md) rides Kaan's clock."
      - "KAAN-ACTION §SHIP-V4 (HARD external clock) — OSS-04 real `cut_release.sh v0.1.0-rc1` publish. Gated on Apple Developer Agreement (Francesco) + SignPath OSS Foundation cert (Kaan). `cut_release.sh --dry-run v0.1.0-rc1` re-verified GREEN (all 8 gates pass; everything-but-the-signature ready); the v7.0 OSS-04 autonomous-mode route sub-section pre-stages the exact `bash scripts/launch/cut_release.sh v0.1.0-rc1` + `gh release create v0.1.0-rc1` invocations. The hard guard (cut_release.sh never auto-invokes gh release create) holds — Kaan's hand on the trigger post-pre-flight. v4.0 SHIP closes alongside when the real cut fires."
      - "DEFERRED (split, documented) — OSS-05 Homebrew tap + Scoop bucket *actual publish* (`git push bravoh-ai/homebrew-tap` + `bravoh-ai/scoop-bucket`) is a future milestone per the OSS-05 success criterion. The scaffolds (vibemix.rb + vibemix.json) + sync_packaging.sh + packaging-audit.yml CI are GREEN engineering-side; docs/release-process.md documents the split rationale."
      - "ADVISORY (3 Info findings, code review, open) — OSS-02 proxy_client: untested UnknownApiResponseError bad-body branch; `original` attr asserted only on the 5xx classifier test; duplicated prod-proxy-URL magic literal. Non-blocking; a follow-up Info-scope pass can address. (Per 69-REVIEW.md.)"
  - phase: 70-github-sexified-generated-tested
    items:
      - "KAAN-ACTION §V7-LANDING — GH-02 Pages enablement (repo Settings → Pages) + live-URL Lighthouse run + Kaan-felt CDJ-Whisper aesthetic sign-off + real Bravoh-waitlist URL + repo social-preview image setting. Engineering side GREEN (docs/landing/index.html in CDJ-Whisper, anti-backsliding grep ZERO, lighthouse.yml local-build a11y≥95/perf≥90 gate, UI-REVIEW PASS 23/24 — the implemented landing reads as Pioneer-grade hardware, not slop). Live/felt items ride Kaan's clock."
      - "KAAN-ACTION §ASSETS-DEMO-CUT — GH-01 real 30-sec CDJ-Whisper demo film (Francesco capture per §VIS-09 runbook) + ≤8 MB cap + cross-browser inline-play. Engineering side GREEN (demo-poster.png Chrome-rendered + MANIFEST-pinned; README hero `sha256=PLACEHOLDER` sentinel kept green via check_readme_hero_hash.py). The real .mp4 + PLACEHOLDER→real-SHA flip rides Francesco+Kaan's clock."
      - "ADVISORY (1 UI-REVIEW finding, open) — GH-02 landing material vocabulary is recess-only; the SPEC's raised-bevel primary-panel recipe is defined as a token but never applied (missing the raised-hardware-affordance pole). Non-blocking, routes to §V7-LANDING taste note. (Per 70-UI-REVIEW.md.)"
acceptance:
  mode: gsd-autonomous-fully
  rationale: |
    All 19 v7.0 REQ-IDs verified at engineering level (TEST-01..04 + DEV-01..05
    + OSS-01..05 + GH-01..05). All 4 phases ENGINEERING-COMPLETE with
    VERIFICATION.md on disk (67 passed; 68 passed; 69 human_needed engineering-
    PASS; 70 human_needed engineering-PASS — the human_needed status surfaces
    the KAAN-ACTION live/external deferrals as explicit checklists, NOT
    engineering gaps). All 6 cross-phase wirings WIRED end-to-end (CI matrix →
    device contracts → OSS docs → presence suite; OSS docs ↔ README; packaging
    scaffolds → release flow; asset pipeline → landing → presence suite;
    KAAN-ACTION cluster coherence; the 2 README feature-matrix drift failures
    CLEARED via scripts/launch/sync_feature_matrix.py). Full default grid:
    4233+ passed / 26 skipped / 4 xpassed / 0 failed (the 2 pre-existing
    feature-matrix failures resolved during this audit). Four cardinal
    invariants (single-writer / citation-grounding / trust-the-audio /
    one-socket) hold by ZERO-TOUCH — only Phase 69 touched src/vibemix
    (narrowly, agent/ for the proxy fallback). Zero net-new dependencies,
    zero new ws ports, zero new IPC envelopes — v7.0 is a WIRING + DISCHARGE
    + POLISH milestone with no product-surface growth.

    Outstanding items are all KAAN-ACTION external-clock / live-hardware /
    felt-quality discharges (§V7-LIVE-01..11 hardware + first-CI-green,
    §V7-PROXY server-side, §V7-LANDING Pages-live + Kaan-felt sign-off,
    §ASSETS-DEMO-CUT demo film, §SHIP-V4 Apple Dev + SignPath signatures)
    plus 4 advisory Info/UI-review findings (none blocking) and one
    documented future-milestone split (OSS-05 tap/bucket publish). Per Kaan's
    `gsd-autonomous fully` directive, these accumulate to the KAAN-ACTION
    surface and do NOT block milestone completion — mirrors the
    v3.0/v3.1/v4.0/v5.0/v6.0 carveout pattern exactly. The ONE hard external
    gate (OSS-04 publish, gated on Apple Dev + SignPath) closes v4.0 "SHIP"
    alongside when the signatures land and the real cut fires; v4.0 stays
    OPEN until then.
nyquist_note: |
  Nyquist VALIDATION.md was not created for v7.0 phases — this is a
  WIRING/DISCHARGE/POLISH milestone with no new product capability (no new
  prompt, evidence source, or reaction-path module to validate at the Nyquist
  sampling bar). Marked NOT_APPLICABLE rather than MISSING: the validation
  dimension applies to behavior-adding phases, and v7.0 adds none.
---

# vibemix v7.0 "Open House" — Milestone Audit

**Audited:** 2026-05-24
**Status:** tech_debt (engineering-green, KAAN-ACTION items carried forward)
**Mode:** `gsd-autonomous fully`
**Score:** 19/19 requirements satisfied (engineering-side) · 4/4 phases verified · 6/6 integration wirings sound · 5/5 OSS-readiness flows complete

## Milestone Goal Achievement

v7.0 turned vibemix from "engineering-green internal code" into "a stranger can clone, install, contribute, trust the proxy, and see a finished front-porch." The four pillars all landed engineering-side:

- **TEST (P67)** — default `pytest -q` green, 21-job CI matrix (`full-test-matrix.yml`), static skip/flake anti-drift gates, 10× flake-hunt baseline discharged.
- **DEV (P68)** — 10 bundled MIDI profiles each contract-tested + synthetic-MIDI-smoked, duplicate catalog reconciled to a single `profiles/` source of truth, hot-plug + audio-backend matrices in CI, "Add Your Controller" contributor recipe.
- **OSS (P69)** — 4 OSS docs + presence test, client-side proxy fallback ("Co-host unavailable this session", no slop, canary offloaded off the reaction path), BYO-key doc, packaging scaffolds + CI audit, `cut_release.sh --dry-run v0.1.0-rc1` green with the real cut pre-staged in §SHIP-V4.
- **GH (P70)** — asset reproducibility pipeline (regenerate_assets.sh + MANIFEST + asset-bitrot CI), auto-generated 1200×630 og-card, CDJ-Whisper Pages landing (UI-review PASS 23/24), demo poster + hero-hash green, one-stop `test_github_presence.py`.

## Cardinal Invariants

Held by **zero-touch**. Only Phase 69 touched `src/vibemix/` — narrowly, in `agent/{proxy_client,dj_cohost,__init__}.py` for the client-side proxy fallback, which adds a fixed "unavailable" message + an offloaded /health canary and emits no LLM/coach output on the fallback path (on-thesis: the brain refuses to lie when grounding is missing). No reaction-path module (`coach/state/memory/recall/decks/grounding/llm`) was modified across any v7.0 phase. Single-writer · citation-grounding · trust-the-audio · one-socket all hold by construction.

## Requirements Coverage (19/19 engineering-side)

| Pillar | REQ-IDs | Engineering status | KAAN-ACTION route |
|--------|---------|--------------------|--------------------|
| TEST | TEST-01..04 | ✅ all closed | §V7-LIVE-01..05 (live hardware + first-CI-green) |
| DEV | DEV-01..05 | ✅ all closed | §V7-LIVE-07..10 (live FLX4 + BlackHole + WASAPI + recipe smoke) |
| OSS | OSS-01..05 | ✅ all closed engineering-side | §V7-PROXY (server), §V7-LIVE-11 (BYO walk), §SHIP-V4 (publish, HARD external clock), OSS-05 tap/bucket publish (future milestone) |
| GH | GH-01..05 | ✅ all closed engineering-side | §V7-LANDING (Pages-live + felt sign-off), §ASSETS-DEMO-CUT (demo film) |

No unsatisfied requirements. No orphans. No NEW test failures (the 2 pre-existing README feature-matrix drift failures were cleared during this audit via `sync_feature_matrix.py`).

## Cross-Phase Integration (6/6 WIRED)

Per the integration checker: (1) CI matrix runs all P68/69/70 tests (default + integration + network markers correctly partitioned); (2) OSS docs ↔ README links sound + re-pinned by the presence suite; (3) packaging scaffolds reference the v0.1.0-rc1 artifacts cut_release.sh gates on; (4) asset pipeline → og-card SHA consistent across MANIFEST + presence test + actual bytes, landing references it via og:image; (5) all 5 KAAN-ACTION clusters internally consistent, no dangling cross-refs; (6) the 2 README failures confirmed cosmetic + now cleared.

## v4.0 "SHIP" linkage

v4.0 stays OPEN. It closes alongside v7.0 OSS-04 when the Apple Dev Agreement + SignPath cert land and the real `cut_release.sh v0.1.0-rc1` fires. The §SHIP-V4 cluster pre-stages the exact invocation; the publish hard-guard is regression-pinned.

## Disposition

`tech_debt` accepted under `gsd-autonomous fully`. Engineering is green across all 19 requirements; every outstanding item is an external-clock/live-hardware/felt-quality discharge correctly routed to the KAAN-ACTION surface, or a documented future-milestone split (OSS-05 publish), or a non-blocking advisory finding (3 Info on proxy_client, 1 UI-review raised-bevel note). This mirrors the v3.0/v3.1/v4.0/v5.0/v6.0 close pattern. Proceed to complete-milestone.
