---
quick_id: 260530-3lh
slug: land-backend-live-tuning-brain
date: 2026-05-30
milestone: v11.0
subsystem: learn + runtime
tags: [skill-recognizer, citation-grounding, mastered, anti-slop, tdd, backend-wiring, ship-ready]
mode: gsd-autonomous-fully
---

# Quick Task 260530-3lh: Land the in-flight backend of `live-tuning-or-brain`

## Origin

The 2026-05-30 SHIP-READY directive: document the 417-commit / 75-dirty tree →
package orphaned work → **wire BACKEND** (frontend = another session) → TDD →
cross-verify agents. This session owns **backend wiring only**; never touch
`tauri/ui/` visuals; surgical commits; concurrent-session discipline ON.

Scope grounded in `.planning/2026-05-30-state-of-the-tree-inventory.md` + a
fresh 9-stream state-of-tree audit (map → wiring-verify → synthesize).

## What the audit + re-baseline found (the plan adapted to reality)

- **Package orphaned work — DONE by a concurrent session mid-run.** While the
  baseline suite ran, a sibling session committed the headline backend as
  clean-checkout-safe atomic islands: `7347cff4` (deck-context grounding brain
  + Viber + Gemini), `14431357` (Course-2 harmonic-pair lesson), `250db877`
  (inventory doc). The two untracked modules (`audio/deck_capture.py`,
  `learn/harmonic_practice.py`) landed WITH their top-level importers — the
  clean-checkout landmine is defused (gate green). Remaining dirty tree is
  ~all `tauri/ui/` = the frontend session's lane (untouched here).
- **The 2 "logic regressions" from the inventory baseline were already fixed**
  by concurrent work (curator MusicState boundary CLEAN; byte-identical pin
  green). Re-measured directly — no fix needed.
- **`skill_recognizer.recognize()` was STILL orphaned** (zero live call-site) —
  the one true remaining backend deliverable, explicitly in this session's lane
  per the inventory punch list.
- **The 12 ship-gate failures were pre-existing artifact drift**, not caused by
  this work; they went green on the current tree (orphan-inventory resolved on
  the module landings + wiring; the rest regenerated by concurrent sessions).

## Tasks (as executed)

1. **Wire the orphaned skill_recognizer into the live event path (TDD).**
   - Files: `src/vibemix/runtime/coach.py`, `src/vibemix/__main__.py`,
     `tests/runtime/test_coach_skill_credit.py` (new).
   - Action: new never-raises glue `_credit_live_skill_demo` in `coach.py`
     closes the live `EvidenceRegistry.has` over the recognizer's citation gate,
     recomputes the session-relative `t_session` the EventDetector wrote
     (`event_detector.py:508`), credits the skill(s) a CITED detected event
     demonstrates, and persists via `save_progress` only on a real credit.
     Two new `coach_loop` kwargs (`evidence_registry`, `learn_progress`) threaded
     from `main()` (both handles already existed). None-path is byte-identical.
   - Verify: RED→GREEN. Cited→credit+persist, un-cited→ZERO credit (MAST-03
     anti-slop control), gate-off no-op, never-raises (broken progress + raising
     registry), MIX_MOVE dual-skill credit, unset-clock self-cancel, unmapped
     event credits nothing.
   - Done: `6dc07ab3` (feat) + `41ccd726` (test hardening from cross-verify).

2. **Cross-verify with two independent adversarial agents.**
   - Correctness/contract review → CORRECT (LIVE-WIRING CONTRACT honored on all
     6 axes; no `await` between detect and credit so t_session is always within
     ±1.0s tol; no over-credit — per-type cooldowns gate re-fires).
   - Concurrency/safety/invariants review → SAFE (shared `_learn_progress` is
     clobber-free on the one asyncio loop with atomic full-object save; reads
     MusicState only — Invariant #1 intact; #2/#3 strengthened; #4 untouched).
   - Folded their actionable coverage feedback into `41ccd726`.

## Out of scope (flagged, NOT touched)

- All `tauri/ui/` dirty work (pill polish, library UI, learn UI) — frontend session.
- `tauri/src-tauri/` Rust (library_cmds.rs) — Tauri session.
- The github-meta `altidus`-vs-`bravoh.ai` domain gate — Kaan's flagged domain
  decision; never auto-edited.
- P104 skill-tree UI panel + the rare grounded "Mastered" vocal — frontend + ear-pass.

## must_haves

- truths:
  - `learn/skill_recognizer.recognize()` has a live call-site (no longer dead code).
  - A cited detected event credits its skill; an un-cited one credits nothing.
  - Existing `coach_loop` callers are byte-identical (None-path no-op).
  - Clean-checkout gate stays green.
- artifacts:
  - `src/vibemix/runtime/coach.py::_credit_live_skill_demo`
  - `tests/runtime/test_coach_skill_credit.py` (8 tests)
- key_links:
  - `src/vibemix/__main__.py` coach_loop call threads evidence_registry + _learn_progress
