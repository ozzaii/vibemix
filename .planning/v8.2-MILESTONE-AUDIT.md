---
milestone: v8.2
name: Set Builder
audited: 2026-05-26
status: gaps_found   # 2/15 requirements (UI-01/02) intentionally DEFERRED to Phase 88 — not a defect, a known carve-out
scores:
  requirements: 13/15   # 13 satisfied (engine+agent+CLI); 2 deferred (UI)
  phases: 5/6           # 83-87 shipped; 88 (UI) deferred
  integration: 11/11    # all cross-phase wirings WIRED (integration-checker, file:line)
  flows: 1/1            # build-set: discover→sequence→explain→export E2E wired + export-set self-verified live
gaps:
  requirements:
    - id: "UI-01"
      status: "deferred"
      phase: "88 (UI)"
      verification_status: "not_implemented_intentional"
      evidence: "No tauri/ui 'Build a Set' consumer. DELIBERATELY parked to ride the concurrent 'The Deck Speaks' full-app UI rebuild (mock mocks/vibemix-rebuild-session.html) — building now would collide + be thrown away. CLI delivers the capability today."
    - id: "UI-02"
      status: "deferred"
      phase: "88 (UI)"
      verification_status: "not_implemented_intentional"
      evidence: "Live full-app button-audit (cargo tauri dev + ui.log) can only be honest against the SETTLED rebuild, not a half-rebuilt tree. Parked as KAAN-ACTION."
  integration: []   # 0 wiring gaps
  flows: []         # 0 broken flows
tech_debt:
  - phase: 86-export
    items:
      - "export_rekordbox.export_set accepts a library= param that is unused in v1 (handler does Gate-2 re-validation before the call) — harmless dead-parameter, documented (export_rekordbox.py:114-118)."
  - phase: 83-87
    items:
      - "Verification-method deviation: no per-phase VERIFICATION.md/SUMMARY.md (built via parallel bg-agents + code-review + fixer + consolidated v8.2-STATUS.md, not the gsd-executor/verifier path). Evidence is equivalent-or-stronger: full suite 4700/0, a full code-review pass (2 BLOCKER + 5 WARNING caught + fixed + regression-pinned), the integration-checker (this audit), and a live export-set self-verify on the real library."
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases: [83, 84, 85, 86, 87]   # no *-VALIDATION.md (discovery only; non-blocking — engines are offline-unit-tested with direction/edge regressions)
  overall: missing
---

# v8.2 "Set Builder" — Milestone Audit

## Verdict: GAPS_FOUND (intentional deferral) — engine + agent + CLI SHIPPED & verified; UI (Phase 88) deferred

The delivered scope (phases 83–87: the perceived-energy model, discovery, sequencer, Rekordbox export, the 4 grounded tools, the `build_set` co-host flow, and the `build-set`/`export-set` CLIs) is **complete, integration-verified, and honest-green (4700/0)**. The milestone is marked `gaps_found` ONLY because 2 of 15 requirements — **UI-01/UI-02 (the Tauri "Build a Set" GUI + its live button-audit)** — were **deliberately deferred to Phase 88** so they ride the concurrent "The Deck Speaks" full-app rebuild rather than collide with it. This is a documented carve-out, not a broken or incomplete build: the capability is fully usable today via the CLI.

## Requirements coverage (13/15 satisfied, 2 deferred)

| REQ-ID | Phase | Status | Evidence |
|---|---|---|---|
| ENERGY-01 | 83 | ✅ satisfied | `energy.score_energy` 0-100, reuses DSP + spectral flux |
| ENERGY-02 | 83 | ✅ satisfied | fixed-window norm + busy-frame mask; pairwise + hypnotic-regression unit tests; BL-01 direction fix pinned |
| ENERGY-03 | 83 | ✅ wired | `score_energy_cached` (atomic) → `get_track_energy` tool (honest-null), both backends |
| DISCOVER-01 | 84 | ✅ satisfied | `intent_centroid` (multi-ref + α-text), zero-vec guarded |
| DISCOVER-02 | 84 | ✅ satisfied | `hard_filter` both-known degrade + `mmr_rerank` diversity |
| DISCOVER-03 | 84 | ✅ wired | `discover_pool` tool adds ids to `seen` (grounding) |
| SEQUENCE-01 | 85 | ✅ satisfied | `CURVE_PRESETS` + np.interp resample to n_slots |
| SEQUENCE-02 | 85 | ✅ satisfied | Camelot+BPM±6% gate, graceful degrade, relaxation ladder TAGS relaxed edges |
| SEQUENCE-03 | 85 | ✅ wired | 3-5 Jaccard-diverse `SetCandidate`s; `sequence_set` tool |
| EXPORT-01 | 86 | ✅ satisfied | `export_set` → Rekordbox XML, order + cues + beatgrid (pyrekordbox write path) |
| EXPORT-02 | 86 | ✅ wired | `harmonics.to_classical` (24-code round-trip); `export_set` tool + CLI; **self-verified live on the real library** |
| AGENT-01 | 87 | ✅ wired | `ViberAgent.build_set` discover→sequence→explain-why→export, grounded |
| AGENT-02 | 87 | ✅ wired | reuses bounded no-hang harness + shared lens; set-prep tools additive (curate unchanged) |
| **UI-01** | 88 | ⏸ **deferred** | no GUI consumer — rides the "Deck Speaks" rebuild (KAAN-ACTION) |
| **UI-02** | 88 | ⏸ **deferred** | live button-audit honest only post-rebuild (KAAN-ACTION) |

## Cross-phase integration (integration-checker, opus) — PASS

All 11 expected cross-phase wirings resolve **WIRED** with file:line evidence; 0 orphaned exports, 0 broken E2E flows. Confirmed: the full `build_set` chain (agent declarations ↔ toolset dispatch ↔ mcp wrappers, arg shapes consistent 3 ways); engine→tool seams build `PoolTrack`/export-item dicts against the real signatures; **grounding invariant #2 holds end-to-end** (`seen` mutated only by `search_vibe`+`discover_pool`; `sequence_set`+`export_set` reject unseen; export 2-gate re-validates vs the live library — an invented track cannot flow discover→sequence→export); both backends inherit the 4 tools from the one shared `LibraryToolset`; the 3-tool curate flow is byte-unchanged; dim-agnostic (no hardcoded 1536). One non-blocking WARNING: `export_set(library=...)` param unused in v1 (harmless).

## Constraints upheld
Gemini-only · sqlite-vec local · **zero new deps** (essentia AGPL excluded + confirmed not-installed) · 4 cardinal invariants intact (grounding gate unchanged) · dim-agnostic (survives the staged CLAP 1536→512 swap) · honest-green offline-unit-testable · did NOT touch concurrent-session files (clap_engine/cue_types/cue_detect/embed) · did NOT refresh `orphans.csv` (only the other session's `ClapEngine` remains its sole pre-existing failure).

## Nyquist
No `*-VALIDATION.md` for 83-87 (discovery: MISSING). Non-blocking — the engines carry direction/edge/grounding regression tests (the code-review fixes are each test-pinned). `/gsd:validate-phase` can retroactively formalize coverage if wanted; not required for the shipped capability.

## To close the milestone fully (Phase 88 — KAAN-ACTION, when the rebuild settles)
1. Build the "Build a Set" GUI (brief + curve picker → sequenced result w/ per-transition why → Export) on the rebuilt "Deck Speaks" shell (UI-01).
2. Live full-app button-audit (`cargo tauri dev` + `ui.log`, every control) — the goal's "check all buttons" ask, honest only against the settled rebuild (UI-02). Dead-control inventory: `.planning/research/vibe-mix-ui-ipc-button-audit.md`.
3. Funded-key `build-set` ear-pass: does the sequenced set + the "why" feel like a real DJ friend (anti-slop bar). NEVER faked.
