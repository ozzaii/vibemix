# SPDX-License-Identifier: Apache-2.0
"""Vibe Mix Slice 2 — harmonic-grounded narrative arc ordering.

OUT of the src/vibemix grep gate (see spikes/__init__.py). Proves the
genuinely-hard ordering piece: a set must follow an energy arc (opener ->
builder -> peak -> cooldown) AND mix in key (Camelot adjacency), or it sounds
like a machine guessed. Energy ordering alone is AI slop; harmonic grounding
is what makes it feel like a real DJ built it. Pure, deterministic, offline.
"""
