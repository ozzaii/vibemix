# SPDX-License-Identifier: Apache-2.0
"""Phase 11 Wave 1 sidecar packaging tests.

Pins the ``--wizard`` CLI flag plumbing + the ``scripts/build_sidecar.py``
rename / AIza-leak-gate logic. Does NOT run a full PyInstaller build —
that's a manual checkpoint on Kaan's macOS rig (per 11-02-PLAN.md).
"""
