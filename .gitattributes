# SPDX-License-Identifier: Apache-2.0
#
# 2026-05-19 — git-lfs removed (history demoted via `git lfs migrate export
# --everything`). Working tree total ~23 MB (character.glb 20 MB + animation
# GLBs 2.9 MB) is well below GitHub's 100 MB per-file hard limit and worth
# the simplification: fresh clones no longer need git-lfs installed, which
# aligns with the one-click install thesis (every external dep removed).
#
# Do NOT re-introduce LFS-routing rules without explicit alignment — the
# GitHub LFS data-pack billing model bit us once.
