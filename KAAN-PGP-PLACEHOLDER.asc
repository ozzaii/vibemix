-----BEGIN PGP PLACEHOLDER BLOCK-----
Comment: This is a placeholder file — NOT a real PGP key.
Comment: Phase 34 / SEC-06 — vibemix open-source security pass.

PLACEHOLDER-FINGERPRINT-NOT-REAL

Kaan Özkan must:

  1. Generate a real 4096-bit RSA or ed25519 PGP key for
     `security@bravoh.com` (or a dedicated `security@<bravoh-domain>`
     address with delivery confirmed).
  2. Upload the public key to `keys.openpgp.org` and `pgp.mit.edu`.
  3. Replace this file's contents with the ASCII-armored public key
     block.
  4. Update SECURITY.md's fingerprint table with the real fingerprint.
  5. Commit + tag the change as `chore(security): publish real PGP key`.

Until then, plaintext email to `security@bravoh.com` is accepted; we'll
move to an encrypted thread on first reply.

The KAAN-ACTION-LEGAL.md file at the repo root has the full step-by-step
runbook including the gpg --gen-key invocation, key-server upload, and
revocation certificate storage policy.

-----END PGP PLACEHOLDER BLOCK-----
