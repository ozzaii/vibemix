/* macOS Privacy & Security shield glyph for the Screen Recording permission
 * card. Uses currentColor so the parent CSS controls fill — phosphor when
 * pending, --ok when granted, --rec when denied (UI-SPEC §6 permissions card
 * state matrix). */
export const SHIELD_SVG = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 2.5 4 5v6.2c0 4.3 3.1 8.2 8 9.8 4.9-1.6 8-5.5 8-9.8V5l-8-2.5Z"/><path d="m9 12 2.2 2.2L15 10.4" /></svg>`;
