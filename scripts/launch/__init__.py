# SPDX-License-Identifier: Apache-2.0
"""scripts.launch — vibemix launch-prep helpers (Phase 27+ convention).

Importable so `tests/launch/*` can unit-test helpers without subprocess
overhead. Each helper script in this package documents its phase + plan
provenance in its module docstring.
"""
